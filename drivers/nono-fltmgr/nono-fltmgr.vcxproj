<?xml version="1.0" encoding="utf-8"?>
<!--
  nono-fltmgr WDK MSBuild project
  Source: structure mirrors github.com/microsoft/Windows-driver-samples
          filesys/miniFilter/nullFilter/nullFilter.vcxproj [CITED]

  ConfigurationType = Driver (required for .sys output)
  DriverType        = WDM   (minifilters are WDM-class FS filters, NOT KMDF)

  Build on the Azure WDK VM from an EWDK/Developer command prompt:
      msbuild nono-fltmgr.vcxproj /p:Configuration=Release /p:Platform=x64
  Expected output: x64\Release\nono-fltmgr.sys  (SC2 proof artifact; NOT committed)

  NOTE: The .sys binary is a throwaway VM-local compile artifact (Pitfall 6).
        Only the source scaffold is committed to the repo.

  The WDK build logic is supplied by PlatformToolset=WindowsKernelModeDriver10.0
  through the standard Microsoft.Cpp.* import chain (the WDK/EWDK installs that
  toolset into $(VCTargetsPath)). Driver projects MUST NOT hand-import
  $(WDKContentRoot)\build\...\WindowsDriver.props/.targets -- those paths do not
  exist in the kit and break the build (MSB4019). See SC2 (Phase 63).
-->
<Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <ItemGroup Label="ProjectConfigurations">
    <ProjectConfiguration Include="Debug|x64">
      <Configuration>Debug</Configuration>
      <Platform>x64</Platform>
    </ProjectConfiguration>
    <ProjectConfiguration Include="Release|x64">
      <Configuration>Release</Configuration>
      <Platform>x64</Platform>
    </ProjectConfiguration>
  </ItemGroup>

  <PropertyGroup Label="Globals">
    <ProjectGuid>{A1B2C3D4-E5F6-7890-ABCD-EF1234567890}</ProjectGuid>
    <TargetName>nono-fltmgr</TargetName>
    <Platform Condition="'$(Platform)' == ''">x64</Platform>
  </PropertyGroup>

  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.Default.props" />

  <!-- Driver configuration MUST come after Cpp.Default.props and before Cpp.props.
       PlatformToolset=WindowsKernelModeDriver10.0 pulls in all WDK build logic; no
       manual WDK props/targets import is needed (or correct). -->
  <PropertyGroup Label="Configuration">
    <ConfigurationType>Driver</ConfigurationType>
    <DriverType>WDM</DriverType>
    <PlatformToolset>WindowsKernelModeDriver10.0</PlatformToolset>
  </PropertyGroup>

  <PropertyGroup Label="Configuration" Condition="'$(Configuration)|$(Platform)'=='Release|x64'">
    <UseDebugLibraries>false</UseDebugLibraries>
    <WholeProgramOptimization>false</WholeProgramOptimization>
  </PropertyGroup>

  <PropertyGroup Label="Configuration" Condition="'$(Configuration)|$(Platform)'=='Debug|x64'">
    <UseDebugLibraries>true</UseDebugLibraries>
  </PropertyGroup>

  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.props" />

  <ItemDefinitionGroup>
    <ClCompile>
      <!-- Spectre mitigation (nullFilter precedent; required for driver certification) -->
      <SpectreMitigation>Spectre</SpectreMitigation>
      <!-- Kernel entry points need C linkage -->
      <CompileAs>CompileAsC</CompileAs>
    </ClCompile>
    <Link>
      <!-- Minifilters link against FltMgr.lib for FltRegisterFilter etc. -->
      <AdditionalDependencies>fltMgr.lib;%(AdditionalDependencies)</AdditionalDependencies>
    </Link>
    <Inf>
      <TimeStamp>$(DefaultInfTimeStamp)</TimeStamp>
    </Inf>
  </ItemDefinitionGroup>

  <ItemGroup>
    <ClCompile Include="nono-fltmgr.c" />
  </ItemGroup>

  <ItemGroup>
    <Inf Include="nono-fltmgr.inf" />
  </ItemGroup>

  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.targets" />
</Project>
