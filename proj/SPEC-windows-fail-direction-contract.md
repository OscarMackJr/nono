# Windows Fail-Direction Contract

**Status:** Living specification (CINT-01, Phase 117). This document describes shipped
behaviour; it is not a decision record. Genuine decisions that produced this document's
content live in `.planning/phases/117-fail-direction-contract-startup-self-attestation/117-CONTEXT.md`.

**Source of truth:** D-01 — the code-resident registry at
`crates/nono-cli/src/exec_strategy_windows/layer_registry.rs` (`LayerId`, `ALL`,
`all_entries()`) is authoritative. This document is drift-checked against that registry by
`crates/nono-cli/tests/layer_registry_selfcheck.rs::spec_matches_registry`, which fails the
build the moment a `LayerId` variant exists in the registry without a corresponding row here.
If this document and the registry ever disagree, the registry wins and the disagreement is a
bug in this document, not in the registry.

**Consumers:** Phase 118's per-session enforcement receipts (D-04, the shared layer-identity
type) and Phase 119's security-model boundary statement (D-12's Unix non-coverage note, handed
over below).

## Scope

**D-09: Windows-only.** This contract enumerates only the layers the Windows backend
(`crates/nono-cli/src/exec_strategy_windows/`, `crates/nono-shell-broker/`,
`crates/nono-cli/src/agent_daemon/`) composes into its deny-by-composition confinement model.
It is not a general nono confinement contract — see "Windows layers only" below for what this
deliberately excludes and why.

## Startup-only claim (D-20)

**This contract, and the self-attestation pass CINT-02 builds on top of it, describe a single
point in time: process launch.** The attestation gate runs once, against the real spawned
child while it is still suspended (`CREATE_SUSPENDED`), before `ResumeThread` — see D-21. It
never re-runs during the life of the session.

**Consequence stated plainly, per D-20:** mid-session layer removal is outside this claim. If
an external actor tears down the WFP filters for a running session's package SID after launch,
or otherwise revokes a grant this contract describes as "confirmed" at startup, that
degradation is not detected, not reported, and not part of what this document or the
attestation pass promises. A confirmed claim describes the state observed at
`CREATE_SUSPENDED`, not a continuous guarantee. Continuous / periodic re-attestation is
explicitly out of scope for this phase (see CONTEXT.md's Deferred Ideas).

## Layer registry

One row per `LayerId` variant (13 total), transcribed directly from
`crates/nono-cli/src/exec_strategy_windows/layer_registry.rs`. Each row's "Expected on" column
summarizes the `ArmExpectancy` matrix (D-08); see the registry source for the exact per-arm
cells. The four-value D-05 outcome vocabulary plus D-06's `FailOpen` is used verbatim from
`ContractOutcome`. `ProbeKind` values match D-17/D-18's attestation vocabulary.

| `LayerId` | Name | Enforcing call site(s) | Expected on | Outcome | Probe |
|---|---|---|---|---|---|
| `RestrictedToken` | restricted-token | `restricted_token.rs::create_restricted_token_with_sid`, `launch.rs::spawn_windows_child` | `DirectCli` / `WriteRestricted` arm only | `Abort` | `LiveTokenOrJobQuery` |
| `MandatoryIntegrityLabel` | mandatory-integrity-label | `labels_guard.rs::AppliedLabelsGuard::snapshot_and_apply`, `mod.rs::prepare_live_windows_launch`, `nono-shell-broker/src/main.rs::broker_resume_gate` | All 5 `DirectCli` arms (`Null`/`WriteRestricted`/`LowIlPrimary`/`BrokerLaunch`/`BrokerLaunchNoPty`) + `Broker` (the broker's own second, independent application to the real grandchild's token) | `Abort` | `ConfiguredOnly` — see RF-01. Was `LiveTokenOrJobQuery`; that probe read the child TOKEN's integrity level, a different kernel object from the mandatory-label ACE this row describes, and succeeded for every Windows token so it could never deny. On the `Broker` arm `nono-shell-broker` DOES perform a real token-RID check (`<= SECURITY_MANDATORY_LOW_RID`) — that is the arm where the observed process is the confined grandchild. |
| `AppContainerProfile` | app-container-profile | `nono-shell-broker/src/main.rs::run`, `nono-shell-broker/src/main.rs::broker_resume_gate`, `agent_daemon/launch.rs::launch_agent` | `Broker` **scoped to the `BrokerLaunchNoPty` spawn shape** (RF-02: the legacy/PTY broker shape is spawned without `--app-container-name` and creates no AppContainer at all), `Daemon` only — **never** any `DirectCli` cell, including `BrokerLaunchNoPty` (Blocker-1: `nono-cli`'s own spawn observes `nono-shell-broker.exe` itself, not the real confined grandchild) | `Abort` | `LiveTokenOrJobQuery` — the probed SID is compared against the expected per-run package SID, not merely tested for presence (RF-05) |
| `DaclSessionSidGrant` | dacl-session-sid-grant | *(none in the shipped tree — see RF-03)* | **Nowhere — expectancy is empty.** RF-03: the only production construction of `AppliedDaclGrantsGuard` is passed `config.package_sid`, not `config.session_sid`; the synthetic per-session restricting SID is granted on no DACL anywhere. The row previously claimed `(DirectCli, WriteRestricted)` and reported `EstablishedNotIndependentlyObservable` for a grant that was never applied. | `Abort` (unreachable — the row is never expected) | `ConfiguredOnly` |
| `DaclPackageSidGrant` | dacl-package-sid-grant | `dacl_guard.rs::AppliedDaclGrantsGuard::snapshot_and_apply`, `mod.rs::prepare_live_windows_launch`, `agent_daemon/launch.rs::DaemonDaclGuard::apply`, `agent_daemon/launch.rs::launch_agent` | All 5 `DirectCli` arms + `Daemon` (RF-04: `execution_runtime.rs` sets `package_sid: Some(..)` unconditionally, so the guard is constructed on every arm — the previous `BrokerLaunchNoPty`-only cell made a genuinely applied layer classify `NotApplicable` on four arms of five) | `Abort` | `ConfiguredOnly` — classified from the caller's per-launch application report (RF-06) |
| `DaclAncestorTraverse` | dacl-ancestor-traverse | `dacl_guard.rs::AppliedAncestorTraverseGuard::snapshot_and_apply`, `mod.rs::prepare_live_windows_launch` | All 5 `DirectCli` arms + `Daemon` (RF-04) | `Abort` | `ConfiguredOnly` — RF-06 |
| `DaclAncestorReadAttrs` | dacl-ancestor-read-attrs | `dacl_guard.rs::AppliedAncestorReadAttributesGuard::snapshot_and_apply_targets`, `mod.rs::prepare_live_windows_launch` | All 5 `DirectCli` arms. **Not `Daemon`** — RF-07: `DaemonDaclGuard::apply` performs traverse-on-read-only-rules, write-on-workspace and traverse-on-workspace-ancestors, and never calls `grant_sid_read_attributes_on_path`; the cited call sites are CLI-side files the daemon binary does not link. | `Abort` | `ConfiguredOnly` — RF-06 |
| `WfpEgressFilters` | wfp-egress-filters | `network.rs::WfpNetworkBackend::install`, `network.rs::assert_wfp_activation_installed_filters`, `agent_daemon/launch.rs::wfp_filter_add` | All 5 `DirectCli` arms + `Daemon`, **conditional on the resolved network backend** (RF-08): `NotApplicable` unless this launch actually selected the WFP backend. See SC4-5 for the "confirmed, not unobservable" correction. | `Abort` | `ConfirmedByEnforcingComponentReport` |
| `FirewallRulesEgress` | firewall-rules-egress | `network.rs::FirewallRulesNetworkBackend::install`, `network.rs::run_netsh_firewall` | All 5 `DirectCli` arms (program-path-scoped, not session-scoped), **conditional on the resolved network backend** (RF-08): `NotApplicable` unless this launch actually selected the `netsh advfirewall` backend. | `Abort` | `ConfiguredOnly` — RF-06 |
| `MinifilterAbsence` | minifilter-absence | *(none — no such layer exists in this tree, ADR-65/D-33)* | Every `(entry_path, None)` cell: `DirectCli`, `Broker`, `Daemon` — the one row with no per-arm variation | `FailOpen` — justification: "ADR-65: no minifilter exists; per-file read policy inside one directory is explicitly not claimed" | `NotApplicable` |
| `JobObjectContainment` | job-object-containment | `launch.rs::create_process_containment`, `launch.rs::apply_process_handle_to_containment`, `agent_daemon/launch.rs::create_agent_job`, `agent_daemon/launch.rs::assign_process_to_agent_job` | All 5 `DirectCli` arms + `Daemon` | `Abort` | `LiveTokenOrJobQuery` |
| `BrokerAuthenticodeTrustGate` | broker-authenticode-trust-gate | `launch.rs::verify_broker_authenticode`, `launch.rs::spawn_windows_child` | `DirectCli` / `BrokerLaunch` and `BrokerLaunchNoPty` arms only (scoped to the arms that spawn `nono-shell-broker.exe`) | `Abort` | `ConfiguredOnly` |
| `InterpreterCoverageGate` | interpreter-coverage-gate | `crates/nono/src/sandbox/windows.rs::validate_launch_paths`, `crates/nono/src/sandbox/mod.rs::Sandbox::validate_windows_launch_paths`, `mod.rs::prepare_live_windows_launch` | All 5 `DirectCli` arms (runs once, before arm selection) | `Abort` | `NotApplicable` — pre-flight, build-time-of-the-launch-plan gate; validated before spawn, not re-checked against a live child afterward |

## Windows layers only — the Unix boundary (D-12)

**Linux Landlock and macOS Seatbelt fail-direction are a named boundary deliberately not
enumerated in this document.** This is not an omission — D-09 scopes this contract to the
Windows-backend layer composition, and the reasoning is on the record here so Phase 119's
security-model boundary statement (BOUND-02/03) can pick it up as a recorded decision rather
than re-deriving it.

**Nearest analog:** Linux Landlock already performs a structurally similar best-effort
downgrade. `crates/nono/src/sandbox/linux.rs:184` declares `ABI_PROBE_ORDER`, the
highest-to-lowest sequence of Landlock ABI versions the sandbox will accept
(`[V6, V5, V4, V3, V2, V1]`); `detect_abi_uncached()` (`linux.rs:209-230`) walks that list and
accepts the first ABI version the running kernel actually supports, silently enforcing fewer
Landlock rule types on an older kernel than it would on a newer one. That is a real
fail-direction decision — a form of graceful degradation, not a hard requirement of "the exact
ABI version compiled against or abort" — and it deserves the same contract-level treatment this
document gives the Windows composite. It is out of scope here only because CINT-01 as written
scopes this phase's registry to Windows, not because the underlying question does not exist on
Linux.

**Disposition:** handed to Phase 119 as a recorded decision, not silence. Phase 119's
BOUND-02/BOUND-03 own writing the equivalent contract (or an explicit "why not" for) the Linux
ABI-downgrade behavior and the macOS Seatbelt backend's own fail-direction posture.

## Latency budget (D-24)

The token and job probes this document's rows use are cheap local calls
(`GetTokenInformation`, `IsProcessInJob`); the project constraint is explicit that the Windows
backend must maintain zero startup latency. The actual measured cost of the startup
self-attestation pass (Plan 08/09/10) is recorded per gated path below. Placeholders are filled
in by Plan 117-12, which owns the measurement:

Measured 2026-08-10 on the development host via dedicated `std::time::Instant`-wrapped unit
tests (real Win32 calls against a real `GetCurrentProcess()` pseudo-handle, not a null/mock
handle — a null handle short-circuits every probe on its own `Err` before reaching the real
`GetTokenInformation`/`IsProcessInJob`/`TokenAppContainerSid` calls, which would measure
error-path cost, not the real probe cost this table needs). None of these are asserted as strict
pass/fail bounds beyond a generous 250ms sanity guard — the numbers below are what the project's
"zero startup latency" constraint (CLAUDE.md) actually costs today, not a benchmark target.

| Gated path (D-23) | Attestation cost | Measurement |
|---|---|---|
| Direct `nono run` (`DirectCli`, `launch.rs` gate point) | **394.4µs** | `exec_strategy_windows::attestation::latency_measurement::attest_and_decide_direct_cli_write_restricted_write_restricted_latency` — `attest_and_decide()` called with `EntryPath::DirectCli`/`WindowsTokenArm::WriteRestricted` against a real `GetCurrentProcess()` handle. |
| Daemon `nono agent launch` (`Daemon`, `agent_daemon/launch.rs` gate point) | **35.7µs** | `agent_daemon::launch::windows_impl::attestation_gate_tests::daemon_attest_and_decide_latency` — `daemon_attest_and_decide()` against a real `GetCurrentProcess()` handle. |
| Broker arm (`Broker`, `nono-shell-broker/src/main.rs` gate point) | **82.8µs** (pre-RF measurement; RF-02 adds a second real Win32 probe to this path, so the current figure is higher — re-measure via the test named here) | `nono-shell-broker`'s `broker::broker_resume_gate_tests::broker_resume_gate_latency_with_real_probes` — `nono::attestation::probe_app_container_sid()` **and** `probe_integrity_level()` (both real Win32 calls) plus `broker_resume_gate()`'s own pure decision logic, against a real `GetCurrentProcess()` handle. |
| Per-tool-call hook (`claude_code_hook.rs`, re-enters the `DirectCli` gate point) | **394.4µs attestation + 1.6962ms cold / 74.8µs warm dedup marker** (see below) | Direct-path attestation cost (row 1 above) plus `output::tests::attestation_downgrade_banner_cold_vs_warm_dedup_marker_latency`, which measures `print_attestation_downgrade_banner`'s per-session dedup marker check SEPARATELY cold (first occurrence this session: `create_dir_all` + `write`, **1.6962ms**) versus warm (an identical repeat: `path.exists()` stat only, **74.8µs**) — the hook path re-enters the direct spawn path on every tool call, so the WARM figure is the steady-state per-invocation cost for a long-running session; the COLD figure is paid once per session per distinct downgraded-layer-set. |

**Broker-arm note:** `broker_resume_gate` itself (the pure decision function, excluding the
probe calls) is allocation-only string/slice comparison with no OS calls — its own cost is not
separable from the measured figure above with `std::time::Instant`'s resolution on this host;
the probe calls dominate.

## Contract vs. code discrepancies

Per D-15, discrepancies between what this contract says and what the code actually does live
here, in a standing section, rather than being quietly reconciled. Every row states its
resolution (fixed here / deferred to which plan) per D-13/D-14 — none reads "quietly
reconciled." Every grep below was re-run live against this working tree on **2026-08-09** and
the command shown is the literal one executed (D-16: greps discover their targets, they do not
confirm a pre-named answer).

| # | Finding | Command run | Hit count | Severity (D-13) | Resolution |
|---|---|---|---|---|---|
| SC4-1 | `crates/nono/src/sandbox/windows.rs::apply` validates `CapabilitySet` *shape* only — it never confirms the CLI's separate WFP runtime activation actually happened. This is the ADR-86-documented carve-out (library applies only what's in `CapabilitySet`; the CLI owns the runtime WFP pipeline), not a bug — but a reader of `apply()` alone should not assume network enforcement occurred just because `apply()` returned `Ok(())`. | `grep -n "^pub fn apply\(" crates/nono/src/sandbox/windows.rs` | 1 (`:52`), measured 2026-08-09 | Low — already correctly carved out by ADR-86; the risk is documentation clarity, not behavior. Recorded as `fail-open` (justified under ADR-86), not `fail-open-defect`. | **Record only — no code change needed.** Cited here with the ADR-86 boundary explicit. |
| SC4-2 | The `PreparedWindowsLaunch` struct-field comments assert Rust drop order is "reverse-of-declaration" for **struct fields**. This is factually wrong: Rust struct fields drop in forward declaration order; only local stack bindings are LIFO/reverse-of-declaration. The code's actual runtime behavior is still correct today (declaration order already matches the intended labels→dacls→ancestors→network unwind sequence), but the comment's stated mechanism is wrong and could mislead a future editor who reorders fields "to preserve" the (incorrect) rule. | `grep -n "reverse-of-declaration" crates/nono-cli/src/exec_strategy_windows/*.rs` | 6, measured 2026-08-09 (`launch.rs:290` — correctly reverse-of-declaration, since that one IS a local stack binding in `create_process_containment`; `mod.rs:298`, `:307`, `:318`, `:328`, `:922` — the five struct-field-comment sites that are factually wrong) | Low functional risk today (the code happens to behave correctly); Medium latent risk (a future field reorder trusting the wrong stated rule could silently break the unwind sequence). Not a `fail-open-defect` — nothing fails open today, this is a documentation-accuracy defect. | **RESOLVED IN THIS PHASE by Plan 117-10** (RF-10 corrects this cell, which still read "Deferred to Plan 117-10" after that plan had landed). `mod.rs`'s `PreparedWindowsLaunch` doc comment now states the correct rule — struct fields drop in FORWARD declaration order, and this struct's declaration order already IS the desired labels→dacls→ancestors→network unwind order — and instructs future editors to place a new field where it should drop rather than relying on a reverse rule that does not exist for struct fields. |
| SC4-3 | `FirewallRulesNetworkBackend` (the legacy/fallback `netsh advfirewall` egress backend) — is it reachable in production, or dead code superseded by WFP? Resolved by Plan 01 (this phase, prior wave): it is a live, reachable dispatch arm keyed on the compiled policy's `active_backend` (`network.rs:1500`, the `(Blocked, FirewallRules)` match arm), a structurally distinct alternative to WFP's `(Blocked, Wfp)` arm — not a fallback chain where one supersedes the other. Fails closed via `run_netsh_firewall` `Err` propagation with partial-rule rollback (`network.rs:1567-1586`). | `grep -n "FirewallRulesNetworkBackend" crates/nono-cli/src/exec_strategy_windows/network.rs` | 1 declaration site plus the `install()` impl and dispatch arm, measured 2026-08-09 (see `layer_registry.rs`'s module doc comment, "Evidence ledger" §1, for the full citation trail) | N/A — resolved, not an open risk. | **Closed by Plan 01.** `FirewallRulesEgress` is promoted to its own registry row (`LayerId::FirewallRulesEgress`, `outcome: Abort`) rather than folded into `WfpEgressFilters` as a variant. Not a residual discrepancy — recorded here only so the SC4 ledger shows its resolution alongside the still-open items. |
| SC4-4 | `set_windows_wfp_test_force_ready`'s fault-injection toggle is gated by a **runtime** check (`std::env::var_os("NONO_TEST_HARNESS").is_none())`, not a compile-time feature. D-30 requires the seam be compiled out of release builds entirely — a runtime-gated "turn off a confinement layer" switch is live attack surface: the flag exists and is settable in every build, production included, and the only thing standing between an attacker (who can already spawn the process) and flipping it is an environment variable. | `grep -n "NONO_TEST_HARNESS" crates/nono-cli/src/exec_strategy_windows/mod.rs` | 4, measured 2026-08-09 (`:557` doc comment, `:581` doc comment, `:589` the check, `:592` the warning message) | Medium — always-compiled in every release binary, gated only by an attacker-settable env var; currently affects only the `WfpEgressFilters` row (does not silently widen a boundary the operator does not already control via other flags), and requires local process-spawn control to exploit. | **RESOLVED IN THIS PHASE by Plan 117-04** (RF-10 corrects this cell, which still described the runtime gate as current behaviour after that plan had removed it). The `NONO_TEST_HARNESS` runtime gate no longer exists: every fault-injection seam, and `--dangerous-force-wfp-ready` itself, is behind `#[cfg(feature = "layer-fault-injection")]` and is not compiled into a default build at all. The "Finding" column above is retained verbatim as the historical record of what was wrong; the behaviour it describes is no longer shipped. |
| SC4-5 | **New finding, this plan.** CONTEXT.md's D-18 discussion assumed WFP filter state would *normally* sit in an "established, not independently observable" state on ordinary sessions, because a non-elevated supervisor cannot re-query live WFP filter engine state directly. That premise is correct for a *live re-query* — but the pre-spawn `installed_filter_count` IPC check (`assert_wfp_activation_installed_filters`) already fails closed **before** `CREATE_SUSPENDED` even fires, for the specific session's package SID / policy, over the existing named-pipe IPC to the elevated `nono-wfp-service`. The code's own behavior is already `Abort`, not "normally downgrades" — the code is correct; D-18's framing in CONTEXT.md was the thing that needed correcting. | `grep -n "installed_filter_count\|assert_wfp_activation_installed_filters" crates/nono-cli/src/exec_strategy_windows/network.rs` | 9, measured 2026-08-09 (declaration/call/test sites; the load-bearing pair is `:1730` the call and `:1775` the function definition) | Low — this corrects the contract's classification, not the code; the code's fail-closed behavior is already what D-18 wants. | **Record only — no code change needed.** This document's `WfpEgressFilters` row above states `outcome: Abort` / `probe: ConfirmedByEnforcingComponentReport` (a report *from* the elevated enforcing component, not an independent live kernel-state observation — hence not `LiveTokenOrJobQuery`), matching the code as it actually behaves rather than the "unobservable, normally downgraded" framing CONTEXT.md's D-18 discussion assumed. |

## Structural constraints

**Broker-abort-only invariant (Item-1, checker pass 3).** Every registry row expected at
`EntryPath::Broker` (with an attestable `probe`, i.e. anything other than
`ProbeKind::NotApplicable`) carries `outcome: ContractOutcome::Abort` — no
`DegradeWithVisibleClaim`, `SubstituteEquivalentMechanism`, or `FailOpenDefect` value is
permitted on a broker-expected row. This is a structural constraint, not an incidental
property: `required_layers_for_broker()` (Plan 08) filters registry rows to those with
`(EntryPath::Broker, expected: true)` **and** `outcome: Abort`; a row expected at the broker
arm with any other outcome would be silently excluded from that filter *and* already excluded
from `attest_and_decide`'s own dispatch (which never runs with `EntryPath::Broker` — that
attestation lives inside `nono-shell-broker` itself, a separate binary, per Blocker-1's
resolution). Such a row would therefore go **completely unattested** on the broker arm — the
exact green-by-absence failure mode this phase exists to close, re-armed for the next added
row if the constraint were only enforced by convention.

This is why every `EntryPath::Broker`-expected row in the table above **with an attestable
probe** (`MandatoryIntegrityLabel`, `AppContainerProfile`) reads `Abort`: broker-arm rows
structurally cannot carry a degrade or fail-open option, because there is no attestation call
site on that arm capable of downgrading a claim gracefully — only `nono-shell-broker`'s own
probe-then-decide (Plan 11), which can only proceed or abort.

`MinifilterAbsence` is `expected: true` at `(Broker, None)` and reads `FailOpen`, not `Abort`.
That is **not** a violation and this paragraph previously contradicted both the table above and
the code (RF-09): the invariant is scoped to rows with `probe != ProbeKind::NotApplicable`,
exactly as `broker_expected_rows_are_abort_only` implements it. A row that documents a layer
which does not exist has nothing to attest on any arm, so it cannot go "silently unattested" —
the failure mode the invariant closes requires something attestable to begin with.

**Enforced by:** `broker_expected_rows_are_abort_only`
(`crates/nono-cli/src/exec_strategy_windows/layer_registry.rs`, `#[cfg(all(test,
target_os = "windows"))] mod tests`) — a discovery-based unit test that iterates
`all_entries()` and asserts the invariant for every row with an attestable probe, never naming
a specific `LayerId` variant by string. A future row added with `EntryPath::Broker` expectancy
and a non-`Abort` outcome fails this test without the test itself needing to change.

## Downgrade banner behavior

**Per-session dedup policy (Item-2, checker pass 3).** `claude_code_hook.rs` re-enters the
direct spawn path on every single tool call. On a host where a row — WFP is the named candidate
under D-18's own accepted tension — persistently sits non-`Confirmed`, an un-deduplicated
downgrade banner would fire on every single tool invocation, which is both noisy and would
train an operator to ignore the signal entirely.

**Decision:** the downgrade banner dedupes **per session**, not per spawn. The first occurrence
of a given downgraded-layer-set always prints, for every session — this preserves the D-27/
Warning-8 no-silent-downgrade property in full. Only an *identical repeat* of that same
downgraded-layer-set within the same session is suppressed. `--silent` never suppresses the
first occurrence of a downgrade banner; no new global silence switch is introduced by this
policy. In practice, an operator on an affected host observes one banner per session, not one
banner per tool call.

This is a contract-visible behavior — an operator can rely on "one banner per session" as the
observable shape of a persistent downgrade, not an implementation detail buried in code
comments.

**RF-06: what counts as a downgrade.** A `ProbeKind::ConfiguredOnly` row that the supervisor
actually applied for this launch classifies `EstablishedNotIndependentlyObservable` and is **not**
a downgrade — it is the expected baseline for a layer with no independent post-hoc query. Before
this review-fix pass every such row was counted as downgraded unconditionally, which made
`AttestationDecision::Proceed` unreachable on every production path: the non-silenceable banner
printed on every Windows session forever and an un-deduplicated `LayerAttestationDowngraded`
record was appended to the HMAC audit chain on every per-tool-call hook invocation. A
permanently-on warning is functionally identical to no warning, and it meant a session could
never be reported as fully attested — the state this phase exists to be able to claim. A row that
is expected on this arm and was **not** applied now classifies `Unconfirmed` and hits its own
per-row outcome (overwhelmingly `Abort`).

**RF-15 (iteration 2): "applied" means the apply took EFFECT, and there is a third state.**
RF-06's caller-supplied report was derived from *guard construction* — `mandatory_integrity_label`
was the literal `true`, and the DACL fields were `config.package_sid.is_some()`, which
`execution_runtime.rs` sets unconditionally. `AppliedLabelsGuard::snapshot_and_apply` returns
`Ok` when it recorded a skip for **every** path, so a launch that wrote zero mandatory-label ACEs
was still reported established and reached `Proceed`. Each guard now reports its own coverage
(`labels_guard::LabelCoverage`, `dacl_guard::DaclGrantCoverage`), and the contract carries three
application states rather than two:

| Guard coverage | `LayerApplication` | Decision |
|---|---|---|
| Nothing in this launch's policy for the layer to act on | `NotApplicable` | Row dropped |
| The apply ran and covered every non-exempt target | `Applied` | `EstablishedNotIndependentlyObservable`, the expected baseline — not a downgrade |
| Covered some targets, not all (a path already carried a third-party mandatory label; a writable path is not owned so its DACL cannot be edited) | `PartiallyApplied` | `ProceedDowngraded` — the D-27 banner and audit event fire |
| Zero targets covered, on a non-empty policy | `NotApplied` | `Unconfirmed` → `Abort` |

**RF-16 (iteration 2): the downgrade channel must be reachable.** No registry row carries
`DegradeWithVisibleClaim` or `FailOpenDefect`, and the single `FailOpen` row
(`MinifilterAbsence`) is deliberately excluded from `downgraded` — so after RF-06,
`AttestationDecision::ProceedDowngraded` was unreachable on every production path and the D-27
banner, the per-session dedup marker, the `LayerAttestationDowngraded` audit event and the
`session_id` path validator guarding the marker were all dead code in the shipped build.
Iteration 1's defect was "the signal always fires"; RF-06 inverted it to "the signal never
fires". `LayerApplication::PartiallyApplied` (RF-15) is the live production route into that
channel. Two tests pin it — `attestation.rs`'s
`partially_applied_configured_only_row_proceeds_downgraded` (decision core) and `launch.rs`'s
`partially_applied_launch_is_downgraded_not_silently_passed` (real registry, real job-contained
suspended child). A D-26-tightened requirement on a partially established layer still aborts.

**Contract-exempt skips are NOT downgrades.** A mandatory-label skip on a path nono does not own
(`C:\Windows`, granted read by the `system_read_windows` policy group) is a documented D-02
exemption, not a coverage gap — labelling it is structurally impossible for an unprivileged user
and unnecessary on the merits. Counting it would fire the banner on essentially every profile,
recreating the permanently-on warning RF-06 removed. Likewise a read-only rule needs no
package-SID DACL ACE. Only genuine gaps downgrade: a path carrying a label nono did not write,
and a WRITABLE path whose DACL nono cannot edit.

## Review-fix pass (code review 2026-08-10)

A deep code review of this phase found that several probes and guards in the shipped pass could
not return a negative result, so the phase failed its own stated goal ("nono can no longer report
enforcing while a confinement layer is silently inert"). The RF-NN identifiers below are cited
from the registry table and the sections above. Full per-finding evidence is in
`.planning/phases/117-fail-direction-contract-startup-self-attestation/117-REVIEW-FIX.md`.

| # | What was wrong | What changed |
|---|---|---|
| RF-01 | `MandatoryIntegrityLabel` probed the child TOKEN's integrity level and discarded the RID. `GetTokenInformation(TokenIntegrityLevel)` succeeds for every Windows token, so a fully unconfined Medium-IL child classified `Confirmed`; the deny branch was unreachable, and the probed object was not the file-ACE the row describes. | Row is `ConfiguredOnly`; the token-RID observation moved to `nono-shell-broker`, which observes the actual confined grandchild and requires `<= SECURITY_MANDATORY_LOW_RID`. |
| RF-02 | The broker's resume gate sat inside `if is_app_container`, so the legacy/PTY broker arm was never gated (and had no suspend window). It matched only the literal `"AppContainerProfile"` and silently ignored every other required name, including `MandatoryIntegrityLabel`. An absent `NONO_BROKER_REQUIRED_LAYERS` became `""` via `unwrap_or_default()` and read as "require nothing". | Gate hoisted to cover both spawn shapes; the legacy arm spawns `CREATE_SUSPENDED`; unknown required names refuse resume; a missing or empty contract refuses resume; `MandatoryIntegrityLabel` is genuinely attested. |
| RF-03 | `DaclSessionSidGrant` was reported "established" on every `WriteRestricted` launch for a grant that is never applied. | Expectancy emptied; the discrepancy is recorded above and in the registry source. **Open operator decision:** whether the `WriteRestricted` arm should also grant `config.session_sid` (it would widen DACLs on user-owned paths) — deliberately not guessed. |
| RF-04 | Three genuinely-applied DACL layers were expected only at `BrokerLaunchNoPty` and therefore classified `NotApplicable` on four arms of five, silently omitted from the attested claim. | Expectancy widened to all five `DirectCli` arms, matching the unconditional `package_sid`. |
| RF-05 | The AppContainer and restricted-SID probes discarded the value they returned, so a child in a *different* AppContainer attested identically to the correct one. | The broker and daemon cores compare the probed package SID against the expected per-run value. |
| RF-06 | `ConfiguredOnly` rows classified `EstablishedNotIndependentlyObservable` unconditionally — a free pass applied even on paths where no apply ever happened. | The caller reports, per launch, which layers it applied; an expected-but-unapplied row classifies `Unconfirmed`. See "Downgrade banner behavior" above. |
| RF-07 | The daemon reported `DaclAncestorReadAttrs` as established although `DaemonDaclGuard::apply` never grants read-attributes. | Row dropped from the daemon's set and from its `(Daemon, None)` expectancy cell. |
| RF-08 | `WfpEgressFilters` was `expected: true` unconditionally on `BrokerLaunchNoPty` while its status came only from the WFP pre-spawn report, so every non-WFP-backed launch on the primary supervised Windows arm was refused. | Both network rows are conditional on the resolved backend: not applicable, not degraded. |
| RF-09 | The "Structural constraints" paragraph asserted every `Broker`-expected row reads `Abort`, contradicting the table's own `MinifilterAbsence` row and the test that enforces the invariant. | Paragraph reconciled with the table and with `broker_expected_rows_are_abort_only`'s `probe != NotApplicable` scoping. |
| RF-10 | SC4-2 and SC4-4 still read "Deferred to Plan 117-10 / 117-04" after both plans had landed in this same phase. | Both moved to a resolved state. |
| RF-11 | `--dangerous-force-wfp-ready` is compiled out of default builds, but four Windows block-net integration tests still passed it and were gated only on `target_os`. Two failed; two passed for the wrong reason (a clap parse error satisfies `!status.success()`). | The four tests are gated on `feature = "layer-fault-injection"`, and `make test-layer-fault-injection` plus a `Windows Layer Fault Injection` CI job now run that build. |
| RF-12 | The CINT-03 forced-unavailable suites, the eight in-crate seam regression tests and the D-32 `every_registry_row_has_a_test` drift gate were behind a feature no build, `make` target or CI job enabled — so none of this phase's own evidence executed, and adding a 14th `LayerId` failed nothing. | `layer_registry_meta_test.rs` (pure source-text scanning) drops the feature gate entirely; the `make` target and CI job above run the rest. |
| RF-13 | `HKLM\SOFTWARE\Policies\nono\RequiredLayers` is documented as a fleet control but is never read and never enforced — a silently ignored security control. | The reader now DETECTS a configured sub-key and emits a loud `RequiredLayersNotEnforced` warning; the type's doc comment states plainly that it is not enforced. **Open operator decision:** where the already-read machine policy is carried to the Windows launch path, and acceptance that a fleet registry key can then refuse launches. Enforcement is NOT implemented. |
| RF-15 | *(Iteration 2, CR-14.)* `AppliedLayers` reported guard **construction**, not guard **effect**: `mandatory_integrity_label` and `interpreter_coverage_gate` were literal `true`s and the DACL fields were the unconditionally-`Some` `config.package_sid`. A launch that wrote zero mandatory-label ACEs was reported fully attested. | Each guard reports its own coverage; `LayerApplication` gains `PartiallyApplied` and a fail-secure `#[default]` of `NotApplied`; the dead `interpreter_coverage_gate` field is removed (its row's `ProbeKind::NotApplicable` meant `classify_row` never consulted it). See RF-15 above. |
| RF-16 | *(Iteration 2, NR-02.)* `AttestationDecision::ProceedDowngraded` was unreachable on every production path, making the D-27 banner, dedup marker and `LayerAttestationDowngraded` audit event dead code. | `LayerApplication::PartiallyApplied` routes an `Abort`-outcome row into `downgraded`. See RF-16 above. |
| RF-14 | *(Found during the fix pass, not in the review.)* A Plan 117-10 unit-test fixture constructed `NetworkEnforcementGuard::FirewallRules { staged_dir: PathBuf::from("."), .. }`; the guard's `Drop` ran an unconditional `remove_dir_all` on that field, deleting the whole `crates/nono-cli` package tree (cargo's test CWD) every time the test ran. | `cleanup_network_enforcement_staging` now applies three ordered defenses — reject any `Component::ParentDir`/`Component::CurDir` outright, the original literal `Path::starts_with` component test, and canonicalize-both-sides re-applying the strict-subdirectory test (fail-secure on canonicalization error) — rather than the pre-NR-06 single literal-component check this row previously described; the fixture points at a never-created path under that root. See NR-06 below for the full fix and its TOCTOU-not-closed honest limit. |
| NR-04 | *(Iteration 2.)* The network attestation rows derived both the WFP composition classification ("did this launch select the backend") and the enforcement-evidence facts ("did the backend actually confirm N filters/rules") from the same discriminant — `NetworkEnforcementGuard`'s variant — so a caller could never independently verify enforcement occurred versus merely being selected. | `NetworkEnforcementGuard::WfpServiceManaged` gained `installed_filter_count: u32`; `FirewallRules` gained `installed_rule_count: u8`; new `firewall_rules_report()`/`wfp_composition_report()` split composition from enforcement evidence; `derive_wfp_preconfirmed` now reads the recorded count the elevated service actually reported, not the discriminant. **Honest limit:** on the shipped path the `(selected, zero filters)` state still cannot arise in production, because `assert_wfp_activation_installed_filters` already fails closed earlier and harder — this is genuine defence in depth (a second, independent read of the enforcing component's own report, catching a future construction site), not a currently-reachable negative. |
| NR-05 | *(Iteration 2.)* The daemon's `daemon_attest_and_decide` abort predicate for `DaclPackageSidGrant`/`WfpEgressFilters` was fed a literal `true` for `dacl_guard_applied` and the SAME `network_scoping_required` expression for both `wfp_filters_installed` and its comparison partner — a compile-time-constant `x && !x` that could never fire. | `DaemonDaclGuard::granted_write_access()` and a `wfp_filters_installed` local set only where `wfp_filter_add` returned `Ok` now feed the gate call; a new source-text test (`daemon_attestation_gate_is_wired_to_real_outcomes_not_the_gate_condition`) requires neither predicate input be a bool literal and the two be distinct expressions. **Honest limit:** on today's control flow both values are still `true` whenever the gate is reached, because earlier fail-closed paths already terminate first — the fix makes the negative representable and its erasure loud, it does not manufacture a currently-reachable negative. |
| NR-06 | *(Iteration 2.)* `cleanup_network_enforcement_staging` refused paths via only a literal `Path::starts_with` component test — it neither rejected `Component::ParentDir`/`Component::CurDir` traversal components nor canonicalized either side, so a crafted staged-directory value (or a symlink/junction planted under the staging root) could delete a directory outside `%TEMP%/nono-net-block`. | Three ordered defences: (1) reject any `Component::ParentDir`/`Component::CurDir` outright — closes the class canonicalization alone cannot, since a not-yet-existing traversal target can't be canonicalized; (2) keep the existing literal `Path::starts_with` component test; (3) canonicalize both sides and re-apply the strict-subdirectory test, fail-secure on a canonicalization error. `cleanup_refuses_traversal_the_root_and_paths_outside_the_root` proves every refusal by survival of a real on-disk victim directory, including the positive direction (a genuine staging subdirectory IS still removed). **Honest limit:** TOCTOU between canonicalize and delete is NOT closed — named in the function's own doc comment; exposure is bounded to an attacker who already holds this same user's own `%TEMP%` write access. |
| NR3-01 (Iteration 3, gap-closure Plan 117-13) | (BLOCKER) CR-14's fix made `applied == 0` on a non-empty policy a hard `Abort` — correct for a genuine zero-coverage launch, but `applied == 0` is also nono's own steady-state residue: an abnormal exit (Ctrl-C/kill/crash) leaves mandatory-label ACEs nono itself wrote, and the NEXT launch of the same policy records `SkipPreExistingLabel` for every one of them, hard-locking the operator out of their own workspace. `NonoError::remediation()` had no arm for `LayerAttestationFailed`, there was no CLI command to clear a stale label, and the same mechanism silently revoked the module's own documented "concurrent sessions: last session out restores" contract. | `AppliedLabelsGuard::snapshot_and_apply` now recognizes an exact match (prior RID == `SECURITY_MANDATORY_LOW_RID` AND prior mask == this launch's own `label_mask_for_access_mode`) as `AppliedLabel::AlreadyAtRequiredLevel` — the contracted ACE genuinely IS present, so it counts toward `LabelCoverage::applied` and is never reverted by this guard's own `Drop` (not this session's residue to tear down; a concurrent session may still depend on it). A genuine zero-coverage launch (no matching residue) still hard-aborts, and a mask-mismatched prior label still records a real coverage gap. `NonoError::remediation()` gained a `NonoRemediation::ClearStaleLayerResidue { layer }` arm for `LayerAttestationFailed` instead of falling through to `None`. **Iteration 4 update:** this residue predicate was found to accept a structurally-ineffective `INHERIT_ONLY_ACE` (CR-01) and to run before the ownership gate (WR-01); see those rows above for the fix Plan 117-20 landed. |
| NR3-02 (Iteration 3, gap-closure Plan 117-14) | CR-14's "report guard effect, not guard construction" fix reached only two of four `LayerApplication` fields; `dacl_ancestor_traverse` and `dacl_ancestor_read_attrs` still derived from `Option::is_some()` on a field `execution_runtime.rs` sets `Some(..)` unconditionally on every `DirectCli` launch — both rows were the constant `Applied` and structurally could never report a negative. | `AppliedAncestorTraverseGuard`/`AppliedAncestorReadAttributesGuard` each gained an `application()` coverage accessor (mirroring `DaclGrantCoverage::application()`'s shape): `NotApplicable` when the walk's own `applied: Vec<PathBuf>` is empty (the walk legitimately found nothing user-owned to grant — full coverage of an empty contract), `Applied` otherwise; `mod.rs::applied_layers` now calls these accessors instead of `Option::is_some()`. The now-dead `application_of` helper was deleted. |
| NR3-03 (Iteration 3, gap-closure Plan 117-15) | `layer_registry.rs`'s `DACL_SESSION_SID_GRANT_EXPECTANCY` doc comment cited `dacl_session_sid_grant_is_not_claimed_anywhere` as a build-failing guard test; the test did not exist anywhere in the tree (`grep -rn` returned only the comment itself). `AppliedLayers::status()` hardcodes `DaclSessionSidGrant` to `NotApplicable` regardless of expectancy — if the row's expectancy is ever restored (RF-03's still-open operator decision), the row would silently drop out of every decision instead of aborting: a live fail-open landmine guarded by a promise, not a test. | Added `dacl_session_sid_grant_is_not_claimed_anywhere`, which now genuinely fails the build if `DaclSessionSidGrant`'s expectancy is ever widened without `status()` also being updated, plus its discovery-based converse `every_consulting_row_with_an_expectancy_has_a_reported_field`, protecting every OTHER `ConfiguredOnly`/`ConfirmedByEnforcingComponentReport` row with an `expected: true` cell from the same shape. RF-03's underlying operator decision (whether `WriteRestricted` should grant `config.session_sid`) is left explicitly open — this closes the citation lie, not the decision. |
| NR3-04 (Iteration 3, gap-closure Plan 117-16) | The D-27 downgrade banner ("see diagnostic output for details") pointed at a channel `apply_startup_attestation_gate`'s `ProceedDowngraded` arm never populated on the success path — the only `tracing::warn!` carrying `downgraded_layers=` fired solely inside the audit-emission-failure arms, so an operator following the banner's own instruction on the common (audit-succeeds) path found nothing there. | `apply_startup_attestation_gate`'s `ProceedDowngraded` arm now emits an unconditional `tracing::warn!` carrying `downgraded_layers`/`downgraded_count` as structured fields (mirroring the daemon's pre-existing unconditional warn), positioned after the audit-emission match and before the banner print. A capturing-subscriber test asserts the FIELD is present (not a message substring), proven non-vacuous by a counterfactual: removing the new warn line makes the test fail even though the pre-existing conditional warns still contain the literal text `downgraded_layers=...` inside their message. **Iteration 4 update:** the unconditional warn this row describes was found to violate D-28 by carrying layer names onto the shared console by default (CR-02); see that row above for the gating fix Plan 117-21 landed. |
| NR3-05 (Iteration 3, gap-closure Plan 117-17) | `DaemonAttestationDecision::ProceedDowngraded` had no constructor anywhere in the shipped daemon binary — a fully-implemented ~60-line audit-emission arm (dedup key, Event Log warning) that could never fire, diverging from the CLI's genuine three-state model (`Proceed`/`ProceedDowngraded`/`Abort`) the daemon's own doc comment claimed to mirror. | **Deviated from the review's suggested fix** (wire a real downgrade path) **in favor of deletion**, because every layer `daemon_attest_and_decide` models is fail-closed end-to-end in the daemon's own guards today — `DaemonDaclGuard`'s three-pass apply has no partial-success return and `wfp_filter_add`'s `Err` path terminates before the gate — so there is no partial-coverage state left for a downgrade arm to represent. `DaemonAttestationDecision` was shrunk to a genuine two-state enum (`Proceed`/`Abort`); the module doc now states the daemon's design is deliberately narrower than the CLI's, and why. A discovery-based `daemon_attestation_decision_is_deliberately_two_state` test parses the enum's variant names straight from source text and fails if a third variant is ever reintroduced. **Iteration 4 update:** this row's premise that `DaemonDaclGuard::apply` "has no partial-success return" was found false (WR-06) — passes 1 and 3 do have skip/break arms, but their fail direction is under-granting, never under-confining; see that row above for the corrected rationale Plan 117-22 landed. |
| NR3-08 (Iteration 3, gap-closure Plans 117-15 and 117-19) | Registry call-site citations drifted a further ~145 lines in the iteration-2 fix pass — the third consecutive review pass this citation drift had been deferred — and this SPEC's own review-fix ledger, the standing artifact D-15 exists specifically to prevent "quietly reconciled" drift, was itself not updated for NR-04/NR-05/NR-06, with its existing RF-14 row still describing the pre-NR-06 guard. `spec_matches_registry` only checks `LayerId` names, so none of this was caught. | The four affected rows' citations (`MandatoryIntegrityLabel`, `DaclPackageSidGrant`, `DaclAncestorTraverse`, `DaclAncestorReadAttrs`) were converted from `"file:line"` to `"file.rs::Symbol"` form, immune to line drift; `registry_call_sites_exist` now additionally verifies the symbol form by file CONTENT (a renamed/removed symbol fails the build, not just a moved/deleted file) — Plan 117-15. This plan (117-19) closes the other half of the finding — porting NR-04/NR-05/NR-06 into this ledger and correcting RF-14 (see rows above) and updating the `## Layer registry` table's citations below to match the current symbol-form source. |
| CR-01 (Iteration 4, gap-closure Plan 117-20) | (BLOCKER) The `AlreadyAtRequiredLevel` residue-equivalence predicate 117-13 introduced matched a prior mandatory-label ACE on `(rid, mask)` alone — `SYSTEM_MANDATORY_LABEL_ACE` is actually `(AceType, AceFlags, Mask, Sid)`, and the predicate never inspected `AceFlags`. A structurally-ineffective `INHERIT_ONLY_ACE` (an ACE the OS never evaluates against the object it sits on) was therefore indistinguishable from a genuinely-effective one nono itself writes, so a launch with zero effective mandatory-label ACEs could report the `MandatoryIntegrityLabel` layer fully `Applied` — the CR-14 defect this phase exists to close, reopened through a new door. | `crates/nono/src/sandbox/windows.rs` gained `low_integrity_label_ace(path) -> Option<(rid, mask, AceFlags)>`; re-run `grep -c "header.AceFlags" crates/nono/src/sandbox/windows.rs` (2026-08-11) → `2`. `labels_guard.rs`'s residue predicate now additionally requires `(prior_flags & INHERIT_ONLY_ACE) == 0` — re-run `grep -n "INHERIT_ONLY_ACE) == 0" crates/nono-cli/src/exec_strategy_windows/labels_guard.rs` (2026-08-11) → 1 hit, line 304 — while still accepting OS-propagated `INHERITED_ACE`. New regression test `inherit_only_residue_is_not_treated_as_already_covered` plants an inherit-only ACE via raw SDDL and asserts `SkipPreExistingLabel`, not `AlreadyAtRequiredLevel`. |
| CR-02 (Iteration 4, gap-closure Plan 117-21) | (BLOCKER) 117-16's downgrade warn printed specific `LayerId` names unconditionally and undeduplicated on `tracing::warn!`, justified by a comment claiming this routes "to the log file / Event Log subscriber, never to the confined child's stderr." `init_tracing`'s default and file-open-failure arms both write to `stderr`, which the confined child shares on the non-detached-stdio path (`STARTF_USESTDHANDLES` unset, `bInheritHandles = 0`) — directly violating D-28 ("layer-specific downgrade detail stays off channels the confined process can read"). | Added `cli_bootstrap::log_target_is_private()` (Windows-only), flipped `true` only when the `--log-file` arm is actually selected; re-run `grep -c "fn log_target_is_private" crates/nono-cli/src/cli_bootstrap.rs` (2026-08-11) → `1`. `apply_startup_attestation_gate`'s downgrade warn now emits `downgraded_layers` (the specific `LayerId` names) only when `log_target_is_private()` is true; the shared-console arms get a `downgraded_count`-only message pointing at the audit ledger instead. The false "never reaches stderr" comment is corrected in place. |
| WR-01 (Iteration 4, gap-closure Plan 117-20) | The residue predicate ran BEFORE the ownership gate (`path_is_owned_by_current_user`) and tested nothing about who wrote the prior ACE — a path the current user did not own, carrying a Low-IL ACE whose mask happened to equal the rule's wanted mask, could still record `AlreadyAtRequiredLevel`, a state nono itself could never have produced on a non-owned path (it structurally lacks `WRITE_OWNER` there). | `AppliedLabelsGuard::snapshot_and_apply`'s loop now runs the ownership match first and only evaluates the residue-equivalence predicate inside the owned branch; re-run `grep -n "match path_is_owned_by_current_user" crates/nono-cli/src/exec_strategy_windows/labels_guard.rs` (2026-08-11) → line 258, ahead of the `AlreadyAtRequiredLevel` push at line 314. The residue predicate can now only ever fire on a path nono itself could have labelled. |
| WR-02 (Iteration 4, gap-closure Plan 117-20) | The `AlreadyAtRequiredLevel` variant's doc comment asserted it "preserves the module doc's 'concurrent sessions sharing the same path: last session out restores' contract," but the code never reverts an *adopted* residue ACE while the session that *applied* it reverts at its own `Drop` — this is first-session-out-restores, the inverse of the claimed property, and a second concurrent session's attested layer can be silently torn down by the first session's normal exit. | **Accepted, not mechanically fixed** — per the plan's own prescribed disposition, no cross-session refcount/lease mechanism was built. The module doc and the `AlreadyAtRequiredLevel` variant doc were corrected to state the code's true behavior: "the session that APPLIED the label reverts it at its own Drop; a session that only ADOPTED it as residue never reverts it." Re-run `grep -c "session that APPLIED the label reverts it" crates/nono-cli/src/exec_strategy_windows/labels_guard.rs` (2026-08-11) → `1`. New test `residue_is_not_reverted_on_drop` pins this corrected property. The underlying first-out-restores race is a documented, accepted D-20-adjacent limitation, not a closed gap. |
| WR-03 (Iteration 4, gap-closure Plan 117-20) | 117-13 silently retargeted `guard_skips_apply_and_revert_when_path_already_has_any_mandatory_label`'s pre-label mask (`0x5`) to an exact match of `AccessMode::Read`'s own wanted mask, so the test actually drove `AlreadyAtRequiredLevel` instead of the `SkipPreExistingLabel` path its name and comments claimed; a dead `let _skip_variant_reference = AppliedLabel::SkipPreExistingLabel;` binding asserted nothing. | The test's pre-label mask was changed to `label_mask_for_access_mode(AccessMode::Write)` with an `assert_ne!` precondition (mirroring the sibling test's existing pattern), restoring genuine `SkipPreExistingLabel` coverage; the inert `_skip_variant_reference` binding was removed. Re-run `grep -n "fn guard_skips_apply_and_revert_when_path_already_has_any_mandatory_label" crates/nono-cli/src/exec_strategy_windows/labels_guard.rs` (2026-08-11) → present, now asserting `matches!(guard.entries[0], AppliedLabel::SkipPreExistingLabel)`. |
| WR-04 (Iteration 4, gap-closure Plan 117-21) | `NonoRemediation::ClearStaleLayerResidue` (NR3-01's remediation) was unreachable on every CLI operator path: `main.rs`'s generic error branch printed only the bare `Display` string, `format_follow_up_from_diagnostics` fell into `_ => {}` for this variant, and both `suggested_flag_for_remediation` copies returned `None` — an operator hitting a real stale-residue abort saw zero guidance despite the remediation type already existing. | Added `crates/nono-cli/src/main.rs::render_error_for_operator`, an independently unit-tested helper that renders `NonoError::remediation()`'s `ClearStaleLayerResidue { layer }` as an actionable second line (pointing at `nono setup --check-only`) and is now called from `main`'s generic error branch instead of a bare `eprintln!("nono: {e}")`. Re-run `grep -c "fn render_error_for_operator" crates/nono-cli/src/main.rs` (2026-08-11) → `3` (the fn plus 2 unit tests). Other `NonoRemediation` variants remain unrendered — WR-04's stated scope was `ClearStaleLayerResidue` only. |
| WR-05 (Iteration 4, gap-closure Plan 117-25) | The `NONO_CI_HAS_WFP: true` env block added to the `windows-layer-fault-injection` CI job was dead configuration — that job's only steps are `cargo test --features layer-fault-injection`, which never invokes `windows-test-harness.ps1`, the env var's one consumer in the whole tree. | Removed the block from the `windows-layer-fault-injection` job only. Re-run `grep -n "NONO_CI_HAS_WFP" .github/workflows/ci.yml` (2026-08-11) → 1 remaining hit, line 243, inside the separate `windows-security` job — that copy is genuinely load-bearing (`windows-test-harness.ps1:189` gates the WFP-filtered test subset on it) and was deliberately left in place; removing it too would have silently dropped real CI coverage. |
| WR-06 (Iteration 4, gap-closure Plan 117-22) | 117-17 deleted `DaemonAttestationDecision::ProceedDowngraded` on a doc-comment premise that `DaemonDaclGuard::apply`'s three-pass apply "has no partial-success return." Passes 1 and 3 do have real skip/`break` arms `granted_write_access()` cannot express (an unowned read-only-rule path is skipped-and-warned, not recorded); the stated premise was false even though the deletion's outcome (no downgrade arm) was defensible for a different reason. | The doc comment was corrected to state the true rationale: passes 1 and 3's skip arms are real, but their fail direction is always under-granting (less filesystem access than the policy intended), never under-confining, unlike the CLI's `SkipWritableNotOwned` which gates a promised WRITE grant — so a downgrade state would carry no security-relevant claim. Re-run `grep -c "under-granting" crates/nono-cli/src/agent_daemon/launch.rs` (2026-08-11) → `1`. Added `every_daemon_variant_is_in_the_cli_variant_set_or_a_documented_divergence`, a discovery-based cross-mirror test parsing both enums' variant names from source and asserting the daemon side is a documented subset of the CLI's three-state `AttestationDecision`. |
| WR-07 (Iteration 4, gap-closure Plan 117-23) | NR3-02's fix made both ancestor-DACL guards' `application()` accessors two-valued (`Applied`/`NotApplicable`, `if applied.is_empty() { NotApplicable } else { Applied }`) — neither could ever produce a negative, and worse, the new `NotApplicable` branch caused `decide_from_entries` to `continue` past an `expected: true` / `outcome: Abort` row entirely, making a walk that granted nothing **vanish from the attested claim** rather than downgrade it — the exact "green by absence" shape the SPEC's own Structural constraints section names as the failure mode this phase exists to close. | Both `AppliedAncestorTraverseGuard` and `AppliedAncestorReadAttributesGuard` gained a `walked: bool`, set on the first loop iteration regardless of which branch is taken, making `application()` genuinely three-valued: `NotApplicable` only when nothing was walked at all, `PartiallyApplied` when walked but zero owned ancestors were granted, `Applied` otherwise. Re-run `grep -c "walked: bool" crates/nono-cli/src/exec_strategy_windows/dacl_guard.rs` (2026-08-11) → `2`. New gate-level test `dacl_ancestor_traverse_row_reports_partially_applied_from_a_real_gate` drives a real suspended child through `attest_and_decide` and asserts the `ProceedDowngraded` result names `LayerId::DaclAncestorTraverse` specifically — proving the row downgrades visibly rather than silently dropping. |
| WR-08 (Iteration 4, gap-closure Plan 117-24) | 9 of 13 registry rows still carried raw `"file:line"` citations, and every one of 15 sampled by the reviewer pointed at unrelated code (comments, closing braces, doc lines) — `registry_call_sites_exist` checked only file existence for those, so nothing failed, and the SPEC's own "Enforcing call site(s)" column mirrored the same stale numbers. | All 9 remaining rows were converted to `"file.rs::Symbol"` form, each replacement symbol re-confirmed to exist at its declaration by direct grep read. `registry_call_sites_exist` now calls a new `content_defines_symbol` helper instead of a bare `content.contains(symbol)`, requiring the symbol's last `::`-segment to appear on a genuine `fn`/`impl` definition line (qualifier-keyword prefix only), not a comment or string literal. Re-run `grep -c "fn content_defines_symbol" crates/nono-cli/tests/layer_registry_selfcheck.rs` (2026-08-11) → `3` (the fn plus 2 unit tests proving it rejects a doc-comment mention and accepts a real definition). This plan's own Task 1 (above) then re-synced the SPEC's `## Layer registry` table to match these same 9 rows' new citations — closing the drift between code and this document, not just within the code's own self-check. |
| WR-09 (Iteration 4, gap-closure Plan 117-25) | 117-17's relocation of a second `impl SecurityEventLayer` block into `exec_strategy_windows/` widened `SecurityEventLayerInner`'s `chain`/`session_id`/`config` fields to `pub(crate)`, giving every module in the binary direct mutable access to the tamper-evident HMAC chain's state with no accessor discipline and no test guarding it — a future edit anywhere in `nono-cli` could rewrite `chain.head`/`chain.sequence` directly, silently forging or resetting the audit chain. | `SecurityEventLayerInner`'s `chain`/`session_id`/`config` fields are private again; re-run `grep -c "pub(crate) chain:" crates/nono-cli/src/telemetry/mod.rs` (2026-08-11) → `0`. The only cross-module access is now `SecurityEventLayer::advance_and_snapshot`, which always routes the mutation through `advance_chain`; `attestation_downgrade_event.rs::emit_attestation_event` was rewritten to call it. `advance_and_snapshot` carries `#[allow(dead_code)]` with a justification comment for the `nono-agentd` binary's independent `#[path]`-copy of this module (its only caller is `nono`-binary-only), mirroring the codebase's existing precedent on `emit_override_event`. |
| WR-10 (Iteration 4, gap-closure Plan 117-24) | `contains_fn_exact`'s doc comment documented a trailing-boundary rule ("neither an identifier character nor `!`"), but the code implemented only the identifier-character half — `'!'` was accepted as a valid boundary, so `fn foo!` (a macro invocation, not a definition) could false-positive-match `fn foo`. The matcher also had no definition-line check, so a doc comment or string literal spelling out `fn <name>` could satisfy the discovery-coverage test `ALSO_AUTOMATED` relies on for 8 of 13 rows. | Implemented the documented `!` exclusion and added the same qualifier-keyword definition-line prefix check `content_defines_symbol` (WR-08) uses. Re-run `grep -c "c == '!'" crates/nono-cli/tests/layer_registry_meta_test.rs` (2026-08-11) → `1`. A rejected match does not short-circuit — scanning continues to a later genuine definition in the same file. |
| WR-11 (Iteration 4, gap-closure Plan 117-22) | `daemon_attestation_decision_is_deliberately_two_state` (117-17) parsed the first textual occurrence of `"enum DaemonAttestationDecision {"`, a literal that appears twice in `launch.rs` — at the declaration and inside the test's own body — the same fragility class iteration 3 raised as NR3-07, reproduced in a brand-new test. It worked only because the declaration happened to come first; the test was also purely lexical, pinning variant names rather than proving `daemon_attest_and_decide` has no third return shape. | The test now anchors on a `DAEMON-DECISION-ENUM` marker comment placed at the declaration site and asserts the marker occurs exactly twice before trusting which segment holds the real enum. Re-run `grep -c "DAEMON-DECISION-ENUM" crates/nono-cli/src/agent_daemon/launch.rs` (2026-08-11) → `2` (the anchor comment plus the hardened test's own reference to it). Added `daemon_attest_and_decide_result_matches_exhaustively`, an exhaustive `match` with no wildcard arm driving the real function against a deterministic fixture — a future third variant now fails to *compile*, proven by deliberately adding a perturbation variant during this plan's execution and confirming `E0004: non-exhaustive patterns`. **Iteration 5 update:** the anchor hardening this row introduced was applied to only 1 of 2 daemon-enum discovery tests — the cross-mirror test WR-06 added in the same round reintroduced the unanchored first-textual-occurrence parse this row was written to close; see WR-14, which routes both tests through a shared `daemon_enum_segment()` helper and bumps the anchor's expected occurrence count to 3. |
| CR-03 (Iteration 5, gap-closure Plan 117-27) | WR-07/CR-02's D-28 gate was applied to only 1 of 3 `ProceedDowngraded`-arm emission sites in the same function — the two audit-emission-failure warns (`SECURITY_LAYER.get() == None`, and `emit_attestation_event` returning `Err`) still interpolated `downgraded_layers={dedup_key}` as unconditional message text, and the new D-28 withholding test asserted only that no captured event carried a *field* named `downgraded_layers`, structurally blind to a leak inside `message` text. | All three sites now compute one shared `layer_detail` gated on `log_target_is_private()` and interpolate it identically. Re-run `grep -n "let layer_detail" crates/nono-cli/src/exec_strategy_windows/launch.rs` (2026-08-11) → line 1501. Re-run `grep -c "layer_detail" crates/nono-cli/src/exec_strategy_windows/launch.rs` (2026-08-11) → `9` (declared once, referenced at all three sites plus tests/docs). The withholding test was widened to a discovery-based scan of every captured event's every `(name, value)` pair for the downgraded `LayerId`'s `{id:?}` text, not just a field name, and a new sibling test drives the previously-untested `SECURITY_LAYER.get() == None` arm. |
| WR-12 (Iteration 5, gap-closure Plan 117-28) | WR-07's fix reclassified "a walk that ran, granted nothing, and stopped at the first non-owned ancestor" as a coverage gap (`PartiallyApplied` → `ProceedDowngraded` → the non-silenceable D-27 banner on every session for any workspace one level under a non-owned parent) — directly contradicting the rationale Plan 117-22 wrote for the byte-for-byte identical physical condition in the daemon mirror ("under-granting, never under-confining, carries no operator-facing signal") in the same round, and contradicting this SPEC's own standing rule ("Contract-exempt skips are NOT downgrades"). **D-37** (`117-CONTEXT.md`, LOCKED 2026-08-11, operator call) resolved the contradiction: 117-22's reading wins — a stop at the first non-owned ancestor is the D-04 contract-exempt outcome (`NotApplicable`), not a downgrade; a walk that ran, granted nothing, and did NOT stop that way remains a real gap (`NotApplied`). | Both `AppliedAncestorTraverseGuard` and `AppliedAncestorReadAttributesGuard` gained `stopped_at_non_owned: bool` and a three-arm `application()`: `(walked=false, _, _) → NotApplicable`; `(true, applied.is_empty()=true, stopped_at_non_owned=true) → NotApplicable` (D-37 exempt); `(true, true, false) → NotApplied` (real gap); `(true, false, _) → Applied`. Re-run `grep -c "stopped_at_non_owned" crates/nono-cli/src/exec_strategy_windows/dacl_guard.rs` (2026-08-11) → `19` occurrences across both guards' fields, doc comments, and tests. The self-contradicting `mod.rs` comment and `dacl_guard.rs`'s struct doc are reconciled, both citing D-37 by name — re-run `grep -c "D-37" crates/nono-cli/src/exec_strategy_windows/mod.rs` (2026-08-11) → `2`. The cross-mirror test was extended from comparing variant *name sets* (which could not see this divergence — both enums' names were unchanged) to a `D37_CLASSIFICATION_TABLE` the CLI-mirror `ancestor_guard_classification_matches_the_d37_table` test drives real `snapshot_and_apply` scenarios against, plus a daemon-mirror behavioral test (`daemon_dacl_guard_apply_succeeds_when_immediate_ancestor_is_non_owned`) proving `DaemonDaclGuard::apply` still returns `Ok` for the identical physical condition — the daemon's `Result<Self>` return has no `LayerApplication`-typed outcome, so a shared table cannot be asserted against it as written; this type asymmetry is stated explicitly in both tests' doc comments rather than faked. |
| WR-13 (Iteration 5, gap-closure Plan 117-31) | `content_defines_symbol` (the selfcheck matcher) omitted the trailing word-boundary check its sibling `contains_fn_exact` (the meta-test matcher) already implemented in the same plan, and discarded the `Type::` qualifier on a `Type::method` citation — so a prefix-preserving rename (`create_restricted_token_with_sid` → `create_restricted_token_with_sid_v2`) still satisfied the search, and `WfpNetworkBackend::install` / `FirewallRulesNetworkBackend::install` (same method name, different `impl` blocks) were indistinguishable, falsifying the SPEC's own "a renamed enforcing function fails the build" claim. | `is_ident_boundary` was extracted to a single shared predicate in `crates/nono-cli/tests/common/mod.rs`, used by both matchers. Re-run `grep -n "fn is_ident_boundary" crates/nono-cli/tests/common/mod.rs` (2026-08-11) → line 41. `content_defines_symbol` now resolves `Type::method` citations against the nearest preceding `impl Type` block via `nearest_preceding_impl_names`. Re-run `grep -n "fn nearest_preceding_impl_names\|fn content_defines_symbol" crates/nono-cli/tests/layer_registry_selfcheck.rs` (2026-08-11) → lines 218, 262. Both hardening checks were proven load-bearing by live perturbation (temporarily forcing `boundary_ok = true` / `impl_ok = true` and confirming the specific regression tests fail, then reverting) — see 117-31-SUMMARY.md. |
| WR-14 (Iteration 5, gap-closure Plan 117-28) | WR-11's anchor hardening (`DAEMON-DECISION-ENUM`, asserting the marker occurs exactly twice before trusting the split) was applied to only 1 of 2 daemon-enum discovery tests — the brand-new cross-mirror test `every_daemon_variant_is_in_the_cli_variant_set_or_a_documented_divergence`, added in the same plan, still called `src.split(enum_marker).nth(1)` directly on the first textual occurrence, reproducing the exact fragility WR-11 closed elsewhere. `parse_enum_variant_names`'s candidate filter also excluded `///`, `#`, `}` and blank lines but not plain `//` comments, so a bare `// note` at variant indent inside either enum body would parse as a false variant. | A shared `daemon_enum_segment()` anchor-resolution helper now routes both `DAEMON-DECISION-ENUM`-anchored discovery tests. Re-run `grep -n "fn daemon_enum_segment" crates/nono-cli/src/agent_daemon/launch.rs` (2026-08-11) → line 2211. The anchor's expected occurrence count was bumped to 3 (declaration comment + both tests' shared reference through the helper). Re-run `grep -c "DAEMON-DECISION-ENUM" crates/nono-cli/src/agent_daemon/launch.rs` (2026-08-11) → `3`. `parse_enum_variant_names`'s filter now also excludes bare `//` comment lines. Re-run `grep -n "trimmed.starts_with(\"//\")" crates/nono-cli/src/agent_daemon/launch.rs` (2026-08-11) → line 2167. |
| WR-15 (Iteration 5, gap-closure Plan 117-27) | CR-02's replacement pointer ("see the audit ledger for layer-level detail") named the wrong channel: `emit_attestation_event` never touches the session `AuditRecorder` ledger (`<session_dir>/audit-events.ndjson`) — it writes the Windows Application Event Log and a `tracing::warn!` with no `downgraded_layers` field. An operator following the banner's own instruction on the common (audit-succeeds) path found nothing at the named destination, and with telemetry disabled (an HKLM `MachineEgressPolicy` admin control) the layer names reached no channel at all. | The banner (`output.rs`) and the withheld-detail operator warn now name the real destination — the Windows Application event log, source `nono`, event id 10011 — instead of the nonexistent "audit ledger". Re-run `grep -c "audit ledger" crates/nono-cli/src/output.rs crates/nono-cli/src/exec_strategy_windows/launch.rs` (2026-08-11) → `0` in both files. Re-run `grep -n "event id 10011" crates/nono-cli/src/output.rs` (2026-08-11) → lines 120-121, present in the banner text. |
| WR-16 (Iteration 5, gap-closure Plan 117-27) | `log_target_is_private()`'s doc comment claimed the file-log arm "writes to a channel private to the operator (a file the confined child does not share)" without checking that: `--log-file` takes an arbitrary operator-supplied path, and the common convenience invocation (`--log-file .\nono.log` inside the granted workspace) puts the log file inside the very directory the confined child is granted read+write on, letting it read the `downgraded_layers=` line the D-28 gate deliberately withheld from its console. | `log_target_is_private()` now validates the resolved `--log-file` path against this launch's own granted `CapabilitySet` paths (component-wise `Path::starts_with`, both sides canonicalized, fail-secure `false` on any unknown/error case). `spawn_windows_child` records the launch's granted filesystem paths once via `set_granted_read_paths()`. Re-run `grep -n "fn log_target_is_private\|fn set_granted_read_paths" crates/nono-cli/src/cli_bootstrap.rs` (2026-08-11) → lines 72, 123. Re-run `grep -n "canonical_log_path.starts_with(&canonical_granted)" crates/nono-cli/src/cli_bootstrap.rs` (2026-08-11) → line 114. Both the positive (path inside a granted dir → not private) and perturbed-negative (path outside every granted dir → private) cases are pinned by dedicated tests. |
| WR-17 (Iteration 5, gap-closure Plan 117-29) | WR-04's remediation line told the operator to run `nono setup --check-only` — a command that prints sandbox support status, storage roots, and trust-root status but performs no mandatory-label inspection on any policy path, so it could never diagnose the stale-residue condition it named — and `NonoError::remediation()` mapped **every** `LayerAttestationFailed` (not just `MandatoryIntegrityLabel`) to this same `ClearStaleLayerResidue` guidance, giving a `WfpEgressFilters`/`AppContainerProfile`/`JobObjectContainment` abort a wrong lead. | `render_error_for_operator` now branches on the failed layer name: `MandatoryIntegrityLabel` gets the real remedy (`icacls <path> /setintegritylevel Medium`); every other layer points at the Windows Application event log instead of the always-wrong check-only command. Re-run `grep -n "\"MandatoryIntegrityLabel\" =>" crates/nono-cli/src/main.rs` (2026-08-11) → line 262. The emitting match arms carry zero occurrences of `"nono setup --check-only"`; one literal occurrence remains, deliberately, inside a negative-assertion regression test guarding against reintroduction. |
| WR-18 (Iteration 5, gap-closure Plan 117-29) | CR-01's `INHERIT_ONLY_ACE` rejection was applied to `low_integrity_label_ace` but not to `low_integrity_label_rid` — the other mandatory-label SACL reader in the same file, and the sole reader behind `is_low_integrity_compatible_dir` / the public `Sandbox::windows_supports_direct_writable_dir` API — which walked the identical SACL, filtered on the identical `SYSTEM_MANDATORY_LABEL_ACE_TYPE`, and returned the RID without ever reading `header.AceFlags`, reopening CR-01's exact "an inert ACE is indistinguishable from an effective one" defect in a second reader. | `low_integrity_label_rid` was reduced from a standalone SACL walk to a 4-line delegating wrapper over the CR-01-hardened `low_integrity_label_ace`, filtering `INHERIT_ONLY_ACE` the same way. Re-run `grep -n -A4 "fn low_integrity_label_rid" crates/nono/src/sandbox/windows.rs` (2026-08-11) → lines 1994-1998, confirms the `.filter(|(_, _, flags)| (u32::from(*flags) & INHERIT_ONLY_ACE) == 0)` delegation. A new regression test, the mirror of `inherit_only_residue_is_not_treated_as_already_covered`, plants an inherit-only Low-RID label and asserts both `low_integrity_label_rid` and `is_low_integrity_compatible_dir` reject it. Enumerated every `SYSTEM_MANDATORY_LABEL_ACE_TYPE` reader in the workspace; confirmed exactly these two exist and both now share one implementation. |
| WR-19 (Iteration 5, gap-closure Plan 117-32) | The SPEC's *Manual verification* table still carried a stale `launch.rs:2190`/`:2194` line citation for `BrokerAuthenticodeTrustGate`, pointing at unrelated `InitializeProcThreadAttributeList` buffer-sizing code (the real call sites are `:1800`/`:2129`, declaration `:2515`) — the same WR-08 class of drift, outside `spec_matches_registry`'s (name-only) coverage and outside the Layer registry table Plans 117-24/117-26 had already swept, so three consecutive citation-drift rounds (NR3-08, WR-08, WR-19) each closed only the scope the prior round checked, not the document as a whole. | Fixed to the symbol form `launch.rs::is_dev_build_layout`. Re-run `grep -n "is_dev_build_layout" proj/SPEC-windows-fail-direction-contract.md` (2026-08-11) → line 315. Two structural drift gates now close the class rather than the instance: `spec_call_site_cells_match_registry_call_sites` compares every Layer-registry-table row's citation cell against `layer_registry.rs`'s own per-entry `call_sites` array (re-run `grep -n "fn spec_call_site_cells_match_registry_call_sites" crates/nono-cli/tests/layer_registry_selfcheck.rs` (2026-08-11) → line 627); `every_spec_symbol_citation_resolves_to_a_real_definition` content-verifies every `file.rs::Symbol`-shaped citation anywhere in the whole SPEC document, not only inside the registry table (re-run `grep -n "fn every_spec_symbol_citation_resolves_to_a_real_definition" crates/nono-cli/tests/layer_registry_selfcheck.rs` (2026-08-11) → line 690). Building the whole-document scan surfaced and fixed 4 additional non-registry-table citations plus a second stray unquoted illustrative example (WR-08 row), beyond this row's own scope — see 117-32-SUMMARY.md. |
| WR-20 (Iteration 5, gap-closure Plan 117-30) | The WR-01 ownership-first reorder silently loosened the coverage claim for a path that is both non-owned **and** carries a foreign mandatory label: before the reorder this state recorded `SkipPreExistingLabel` → counted toward `skipped_pre_existing_label` → `PartiallyApplied` → a visible downgrade; after the reorder `Ok(false)` records `SkipNotOwned` and `continue`s before `low_integrity_label_ace` is ever called, so the identical path is now contract-exempt and uncounted — a real reclassification the WR-01 ledger row's text never stated, pinned by no test (the existing `guard_skips_path_not_owned_by_current_user` uses `C:\Windows`, whose label state is incidental and controls only one of the two variables). | A new regression test, `non_owned_path_with_a_foreign_label_is_exempt_not_a_coverage_gap`, constructs a real Windows file that is both non-owned by the current user AND carries a genuine, non-inherit-only foreign mandatory-label ACE (own → plant label → give ownership away via `icacls /setowner`), then asserts `AppliedLabel::SkipNotOwned` (never `SkipPreExistingLabel`). Re-run `grep -n "fn non_owned_path_with_a_foreign_label_is_exempt_not_a_coverage_gap" crates/nono-cli/src/exec_strategy_windows/labels_guard.rs` (2026-08-11) → line 1140, present in the tree. **HONEST STATUS — NOT FULLY VERIFIED ON THIS HOST:** this dev host's shell is a non-elevated domain account lacking `SeRestorePrivilege`/`SeTakeOwnershipPrivilege`/`SeBackupPrivilege` (confirmed via `whoami /priv`); the test's own ownership-reassignment setup step fails and the test panics loudly at setup (D-31: a documented, loud test-setup failure, not a silent skip or pass), before `AppliedLabelsGuard::snapshot_and_apply` is ever reached. No runtime perturbation proof was producible on this host for the same reason — perturbing code downstream of a step that never executes shows the identical panic regardless of what the production code says. The test is authored and structurally correct by inspection (the `Ok(false)` arm's `continue` makes it compile-time-impossible for the residue check to run afterward), and is ready to provide real perturbation evidence — **but requires verification on an elevated/CI Windows runner (`SYSTEM`/`Administrator`, or a locally elevated session) before this reclassification can be considered test-proven rather than merely test-authored.** Full-suite runs from this plan onward carry this test as failure #12 in the documented baseline, distinct from and additional to the 11 pre-existing baseline failures. |
| WR-21 (Iteration 5, gap-closure Plan 117-33) | Two residuals from WR-09's otherwise-correct field-privacy fix: (1) `emit_attestation_event` held `inner`'s lock across `advance_chain` but released it (via `advance_and_snapshot`) before its own `emit_security_event` call, so chain advancement and Event Log emission were no longer atomic for this event type — two concurrent emitters could advance the chain in order A,B and write records in order B,A — while sibling emitter `emit_override_event` still held the lock across its own emit, giving the two chain emitters different atomicity guarantees for the same tamper-evident HMAC chain; (2) `SecurityEventLayer::inner` stayed `pub(crate)` behind a comment pointing at an unrelated follow-up note, with its only remaining cross-module use being a `#[cfg(test)]` mutex-poisoning helper reaching through raw field access. | `advance_and_snapshot` was replaced with `advance_and_emit`, which holds the lock across the full build+advance+emit sequence — `emit_attestation_event` now shares the exact atomicity guarantee `emit_override_event` and `on_event` already had. Re-run `grep -c "fn advance_and_snapshot" crates/nono-cli/src/telemetry/mod.rs` (2026-08-11) → `0`; re-run `grep -n "fn advance_and_emit" crates/nono-cli/src/telemetry/mod.rs` (2026-08-11) → line 487. `inner` is narrowed back to private. Re-run `grep -n "    inner:" crates/nono-cli/src/telemetry/mod.rs` (2026-08-11) → line 237, no `pub(crate)` qualifier. The sole remaining cross-module test need is served by a new, named, documented `poison_for_test` accessor. Re-run `grep -n "fn poison_for_test" crates/nono-cli/src/telemetry/mod.rs` (2026-08-11) → line 297. Atomicity was proven load-bearing via a live two-thread perturbation (see 117-33-SUMMARY.md); no fourth chain-advancing emitter exists (enumerated via `grep -n "advance_chain("`). |

## Manual verification (D-31)

Per D-31, a row that cannot be tested on an ordinary host is a **loud, named gap** — never a
silent `#[ignore]`. CINT-03's own wording is "a contract entry with no such test is not
satisfied"; the entries below are the rows for which this plan's automated
`crates/nono-cli/tests/layer_force_unavailable.rs` cannot itself provide that test on an ordinary
(non-elevated, non-domain-joined, non-production-signed) development host, mechanically enforced
loud by `crates/nono-cli/tests/layer_registry_meta_test.rs`'s `host_gated_rows_are_loud` test
(every entry below must appear, by name, in this section, or that test fails the build).

### `LayerId`-scoped rows

**Current count (Plan 117-18, SC3 gap closure):** 10 of 13 rows are automated — 2 via
`layer_force_unavailable.rs`'s external-subprocess `force_unavailable_*` convention, 8 via
direct in-process/gate-level tests discovered by `layer_registry_meta_test.rs`'s
`ALSO_AUTOMATED` mechanism (`RestrictedToken`, `JobObjectContainment`, `DaclAncestorTraverse`,
`DaclAncestorReadAttrs`, `WfpEgressFilters`, `FirewallRulesEgress`, `AppContainerProfile`,
`InterpreterCoverageGate`). The 3 rows below are the operator-accepted, genuinely host-gated or
structural remainder — this session's locked decision was "automate what is automatable; for
the genuinely host-gated/structural remainder, record an explicit per-row accepted downgrade
naming why the row cannot be automated" (2026-08-10 gap-closure session).

| `LayerId` | Why manual | Manual verification steps |
|---|---|---|
| `DaclSessionSidGrant` | RF-03: this layer does not exist in the shipped tree — the only production construction of `AppliedDaclGrantsGuard` is passed `config.package_sid`, and the synthetic per-session restricting SID is granted on no DACL anywhere. The row's expectancy is empty, so there is nothing to force unavailable. **OPERATOR-ACCEPTED DOWNGRADE:** this row's own expectancy is empty by design, so no test seam is even meaningful until RF-03's open decision is resolved — the 2026-08-10 gap-closure session accepted a code-read as this row's manual-verification form rather than inventing a test for a layer that is not currently claimed. | Manual verification is a CODE READ, not a run: confirm `grep -rn "session_sid" crates/nono-cli/src` still shows no DACL grant of `config.session_sid`. If that ever changes, the registry expectancy must be restored in the same commit. See RF-03 for the open operator decision on whether the `WriteRestricted` arm SHOULD grant it. |
| `MinifilterAbsence` | Structurally untestable — no minifilter driver exists in this tree (ADR-65 stands). The row documents a deliberate structural absence, not a probeable mechanism. **OPERATOR-ACCEPTED DOWNGRADE:** ADR-65 is a standing "No-go" decision on a production minifilter; there is nothing to force unavailable, so the 2026-08-10 gap-closure session accepted this row as permanently structural rather than automatable. | Not a skip: recorded here as an explicit "structurally absent / not applicable" entry citing ADR-65, per Phase 116 D-08's `structurally-blocked` row form. |
| `BrokerAuthenticodeTrustGate` | The gate is skipped entirely under `launch.rs::is_dev_build_layout` — active only in a signed, production (non-dev-layout) install. **OPERATOR-ACCEPTED DOWNGRADE:** an ordinary dev/CI host cannot produce a signed, non-dev-layout install; the 2026-08-10 gap-closure session accepted the live-install round-trip as this row's genuinely host-gated remainder, distinct from `verify_broker_authenticode`'s own already-automated unit coverage. | From a signed release install outside `target/...`, stage a `nono-shell-broker.exe` signed by a different identity than `nono.exe` and confirm the broker-arm spawn refuses (`crates/nono-cli/tests/broker_authenticode.rs::broker_signature_mismatch_refuses_spawn` already covers `verify_broker_authenticode` directly; this item is the live-install end-to-end round-trip). |

### Cross-cutting security assumptions

Distinct from the `LayerId`-scoped rows above: a cross-cutting security assumption not tied to
one registry row (Blocker-3, checker pass 2 — the concrete pickup Plan 09's flagged assumption
needed and did not get in the prior pass).

| Assumption | Why manual | Manual verification steps |
|---|---|---|
| `etw-applog-child-readability` | Plan 09 added `SecurityEvent.downgraded_layers` on the documented-but-unverified assumption that ETW/Application Event Log is unreadable by a Low-IL/AppContainer child (D-28's "layer-specific detail stays off channels the confined process can read"). A static SDDL parse could be added as a follow-up, but asserting a specific SDDL shape from memory without live verification risks baking in a wrong "safe" claim — worse than a named, loud manual gap. | Run `wevtutil gl Application` (or the equivalent `EvtOpenChannelConfig`/`EvtGetChannelConfigProperty` Win32 call for `EvtChannelConfigPropertyAccess`) and inspect the returned `channelAccess` SDDL string for any ACE granting read (`0x1`, generic-read) to a SID broader than the expected administrative/eventlog-reader set — in particular check whether `S-1-1-0` (Everyone) or any low-integrity/AppContainer-relevant SID is present. If such a SID is present, `SecurityEvent.downgraded_layers` is NOT operator-only and D-28's channel-separation property is violated; escalate as a new SC4 discrepancy. |

**Enforced by:** `crates/nono-cli/tests/layer_registry_meta_test.rs`'s `every_registry_row_has_a_test`
(D-32 discovery), `host_gated_rows_are_loud`, and `security_assumptions_are_loud` — all three fail
the build if a row is added to `MANUALLY_VERIFIED`/`MANUAL_SECURITY_ASSUMPTIONS` without a
non-empty reason, or if a listed entry stops appearing in this section.
