---
milestone: v2.8
milestone_name: UPST7 + v2.7 Drain & Release
audited: 2026-06-06
status: tech_debt
scope: Phases 53–59 (v2.9 track — Phases 60/61/62 — audited separately)
re_audit: true
re_audit_note: >
  Re-run of the 2026-06-06 audit (commit 2de33323, which found gaps_found on the
  REQ-UPST7-01 verification-bookkeeping gap). That gap was remediated by commit
  8a6c0634 (validate UPST7 audit): 54-VALIDATION.md is now status:validated /
  nyquist_compliant:true / Approval:validated with all 5 SC gates green, 54-01-SUMMARY
  lists REQ-UPST7-01, and REQUIREMENTS.md marks it [x] Complete. REQ-UPST7-01 is now
  satisfied. Status drops from gaps_found → tech_debt (no blockers; accumulated debt remains).
scores:
  requirements: 10/10 satisfied
  phases: 7/7 verified (Phase 54 verified via VALIDATION.md per audit-phase convention)
  integration: 5/5 seams WIRED (0 blockers)
  flows: 9/9 requirement integration paths WIRED
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 53-release-drain
    items:
      - "HAZARD: v0.57.4 GitHub release still carries unsigned-payload MSIs — delete or annotate to prevent inadvertent distribution (flagged non-blocking in 53-04)"
      - "INFO: scripts/sign-poc-local.ps1 $VersionTag default stale at 'v0.57.4' (local POC helper; CI unaffected)"
      - "INFO: ROADMAP.md Progress table still shows Phase 53 'In Progress 3/4' and Phase 61 'In Progress 3/4' (doc-sync drift; REQUIREMENTS.md correct)"
  - phase: 55-upst7-cherry-pick-wave
    items:
      - "WR-01: NONO_DETACH_STARTUP_TIMEOUT env + clap `requires=detached` breaks `nono run` for non-detached invocations (upstream bug absorbed as-is)"
      - "WR-04: opencode_linux group orphaned in policy.json after opencode profile removal (dead config)"
      - "Cross-target Linux/macOS clippy CI-deferred (Windows host cannot cross-compile aws-lc-sys/ring); merge-gated on tag"
  - phase: 56-fine-grained-network-filtering
    items:
      - "WR-02: nono why path_matches_endpoint_rules is method-blind in display (diagnostic only; proxy enforcement uses method check)"
      - "WR-03: profile+CLI same-host Plain/WithEndpoints concatenated without merge (fail-secure via CONNECT block, but fragile)"
      - "Live proxy enforcement HUMAN-UAT is Windows-OS-blocked (fail-secure); exercise on Linux/macOS"
      - "56-VALIDATION.md status:draft (nyquist_compliant:true — sign-off shell unfinished, tests green)"
  - phase: 57-bitwarden-credential-source
    items:
      - "WR-01: classify_bw_error/classify_bws_error echo raw subprocess stderr into error messages (inherited op:// pattern)"
      - "WR-03: is_valid_bw_id accepts leading-hyphen IDs (defense-in-depth; `--` arg terminator currently prevents exploitation)"
      - "Live bw/bws CLI end-to-end secret delivery requires an operator with a real Bitwarden vault"
  - phase: 58-session-lifecycle-hooks
    items:
      - "D-05 / CR-01 (SECURITY, defense-in-depth): Windows hooks run at Medium-IL — create_low_integrity_primary_token() is called but NOT plumbed into CreateProcessAsUserW. ADR-tracked deferral (Research Open Question 1); real confinement gap, close in a follow-up phase"
      - "58-VALIDATION.md status:draft / nyquist_compliant:false — only PARTIAL nyquist phase; run /gsd:validate-phase 58 to finalize (tests exist + pass per 58-VERIFICATION)"
      - "WR-02: run_hook background thread leaks Child stdio handle on timeout"
      - "WR-03: WindowsEnvFileGuard Drop uses meta.len() as usize without try_from guard"
      - "WR-04: Unix LD_*/DYLD_* prefix check is case-sensitive"
  - phase: 59-supervisor-ipc-robustness
    items:
      - "WR-01: read_exact_bounded only checks deadline when avail==0 — slow-trickle can evade (fail-secure)"
      - "WR-02: macOS keep-alive classifier matches WouldBlock substring; SO_RCVTIMEO timeout may be misclassified as disconnect (fail-secure; CI-deferred)"
      - "WR-03: Linux run_supervisor_loop permanently demotes IPC socket on partial-frame timeout (fail-secure; CI-deferred)"
      - "WR-04: seen_request_ids.insert before session-token check — unauthenticated peer can pollute replay namespace (fail-secure; DACL-gated)"
  - phase: cross-phase
    items:
      - "INFO (integration): timeouts.rs:83,91 #[allow(dead_code)] on SUPERVISOR_IPC_READ_TIMEOUT is over-conservative — the accessor fn is reachable on both targets; benign cross-target artifact"
nyquist:
  compliant_phases: ["54", "56", "57", "59"]
  partial_phases: ["58"]
  missing_phases: ["53", "55"]
  overall: partial
---

# v2.8 Milestone Audit — UPST7 + v2.7 Drain & Release

**Audited:** 2026-06-06 (re-audit)
**Scope:** Phases 53–59 (10 requirements). The v2.9 track (Phases 60, 61, 62 / REQ-STW, REQ-WFP, REQ-RLS-03/04) is a separate initiative and is **not** part of this v2.8 audit.
**Status:** ⚡ **tech_debt** — all 10 requirements satisfied, 0 integration blockers; accumulated fail-secure debt + one security defense-in-depth gap remain for review.

> **Re-audit note.** The first audit run on 2026-06-06 (commit `2de33323`) returned `gaps_found` on a single REQ-UPST7-01 *verification-bookkeeping* gap (Phase 54's ledger was substantive but unsigned). That gap was closed by commit `8a6c0634`: `54-VALIDATION.md` is now `status: validated` / `nyquist_compliant: true` / `Approval: validated 2026-06-06` with all 5 SC gates green; `54-01-SUMMARY` lists `REQ-UPST7-01`; `REQUIREMENTS.md` marks it `[x]` Complete (traceability row `Complete`). **REQ-UPST7-01 is now satisfied.** With no unsatisfied requirements, the FAIL gate no longer fires and status drops to `tech_debt`.

---

## Requirements Coverage (3-source cross-reference)

VERIFICATION.md status × SUMMARY `requirements` frontmatter × REQUIREMENTS.md traceability:

| REQ-ID | Phase | VERIFICATION/VALIDATION | SUMMARY frontmatter | REQUIREMENTS.md | Final |
|--------|-------|-------------------------|---------------------|-----------------|-------|
| REQ-RLS-01 | 53 | VERIFICATION passed 5/5 | (empty) | `[x]` Complete | ✅ satisfied |
| REQ-RLS-02 | 53 | VERIFICATION passed 5/5 | (empty) | `[x]` Complete | ✅ satisfied |
| REQ-DRN-01 | 53 | VERIFICATION passed 5/5 | (empty) | `[x]` Complete | ✅ satisfied |
| REQ-DRN-02 | 53 | VERIFICATION passed 5/5 | (empty) | `[x]` Complete | ✅ satisfied |
| REQ-UPST7-01 | 54 | VALIDATION **validated** (5/5 gates green) | listed `[REQ-UPST7-01]` | `[x]` Complete | ✅ satisfied |
| REQ-UPST7-02 | 55 | VERIFICATION 9/9 verified (human_needed = CI clippy) | listed `[REQ-UPST7-02]` | `[x]` Complete | ✅ satisfied |
| REQ-NET-01 | 56 | VERIFICATION 4/4 verified (human_needed = live proxy) | (empty) | `[x]` Complete | ✅ satisfied |
| REQ-CRED-01 | 57 | VERIFICATION 3/3 verified (human_needed = live vault) | (empty) | `[x]` Complete | ✅ satisfied |
| REQ-HOOK-01 | 58 | VERIFICATION passed 4/4 | listed | `[x]` Complete | ✅ satisfied |
| REQ-IPC-01 | 59 | VERIFICATION passed 9/9 | listed `[REQ-IPC-01]` | `[x]` Complete | ✅ satisfied |

**Score: 10/10 satisfied.** No orphaned requirements (all 10 map to a milestone phase, all confirmed by VERIFICATION/VALIDATION + REQUIREMENTS.md).

> **Phase 54 convention note.** Phase 54 is a doc-producing audit (output: `54-DIVERGENCE-LEDGER.md`, zero code). GSD audit phases produce a `VALIDATION.md` (grep-falsifiable SC-gate set), not a `VERIFICATION.md` — so "no 54-VERIFICATION.md" is the expected shape, not a gap. The signed `54-VALIDATION.md` is the canonical verification artifact and is green.
>
> **SUMMARY frontmatter hygiene.** `requirements`/`requirements-completed` was left empty for REQ-DRN-01/02, REQ-NET-01, REQ-CRED-01. This is a metadata-hygiene artifact, not a coverage gap — each is independently confirmed SATISFIED in its phase VERIFICATION.md and `[x]` Complete in REQUIREMENTS.md.

---

## Phase Verification Summary

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 53 Release & Drain | passed | 5/5 | v0.57.5 signed MSIs + no-PTY path + WFP uninstall all operator-confirmed |
| 54 UPST7 Audit | **validated** (VALIDATION.md) | 5/5 gates | Ledger substantive + consumed by Phase 55; now signed off (gap closed) |
| 55 UPST7 Cherry-pick Wave | human_needed | 9/9 | CR-01 security blocker fixed pre-verify; cross-target clippy CI-deferred |
| 56 Fine-grained Network Filtering | human_needed | 4/4 | CR-01/CR-02 fixed pre-verify; live proxy UAT Windows-OS-blocked (fail-secure) |
| 57 Bitwarden Credential Source | human_needed | 3/3 | All automated checks pass; live bw/bws UAT needs operator vault |
| 58 Session Lifecycle Hooks | passed | 4/4 | Live Win11 UAT PASS; D-05 Low-IL token plumbing deferred (ADR-tracked) |
| 59 Supervisor IPC Robustness | passed | 9/9 | Operator UAT PASS (SC1+SC2) on real Win11 build 26200 |

The `human_needed` statuses on 55/56/57 are **environmental deferrals** (cross-target CI clippy; live proxy/Bitwarden runtime this Windows host cannot exercise), not implementation gaps — each is documented and accepted. Phase 56's live-proxy UAT is Windows-OS-blocked and fail-secure (exercise on Linux/macOS).

---

## Cross-Phase Integration (gsd-integration-checker)

**0 blockers. All 5 shared-surface seams WIRED end-to-end** (verified by trace, not existence). `cargo build --workspace` exit 0 on the Windows host (workspace at v0.62.0).

| Seam | Phases | Status | Evidence |
|------|--------|--------|----------|
| Profile pipeline coexistence (allow_domain + session_hooks + binary across the 4-lockstep, no clobber) | 55→56→58 | WIRED | `profile/mod.rs` 2125/2248/2311/3012/3109/3230/3257; rustc struct-completeness enforces no drops |
| timeouts.rs centralized constants consumed by supervisor IPC | 55→59 | WIRED | `timeouts.rs:84-97` → `exec_strategy.rs:1293,4313` (Unix) + `exec_strategy_windows/supervisor.rs:590,595` (Win) |
| Proxy endpoint rules before credential selection (no IPC regression) | 56 / 59 | WIRED | `nono-proxy/src/reverse.rs:93-128` — `endpoint_rules.is_allowed()` 403 before credential lookup |
| keystore bw:// dispatch coexists with file/env/op/keyring | 57 | WIRED | `keystore.rs:220-234` mutually-exclusive prefix chain; tests assert no scheme overlap |
| Release surface — 5 crates + FFI pinned consistently, builds clean | 53 / all | WIRED | all Cargo.toml at 0.58.0→0.62.0; sigstore 0.8.0 consistent; release.yml sign-before-harvest + payload-verify gate |

**1 non-blocking note:** over-conservative `#[allow(dead_code)]` on the supervisor-IPC timeout const in `timeouts.rs:83,91` — the accessor fn is genuinely reachable on both targets; benign cross-target artifact.

**Terminology (not a defect):** Phase-55 "target_binary" ships as the profile field `binary` (`profile/mod.rs:2193`); `target_binary` is not a field name in-tree.

---

## Nyquist Coverage

| Phase | VALIDATION.md | nyquist_compliant | Class | Suggested action |
|-------|---------------|-------------------|-------|------------------|
| 53 | missing | — | MISSING | Release/HUMAN-UAT phase; little to unit-validate. Optional. |
| 54 | exists (validated) | true | COMPLIANT | — (gap closed) |
| 55 | missing | — | MISSING | Cherry-pick wave; covered by absorbed + existing tests. Optional. |
| 56 | exists (draft) | true | COMPLIANT | Sign-off shell unfinished but compliant; optional `/gsd:validate-phase 56` |
| 57 | exists (validated) | true | COMPLIANT | — |
| 58 | exists (draft) | **false** | **PARTIAL** | `/gsd:validate-phase 58` — tests exist & pass (4/4 VERIFICATION); only the nyquist sign-off is unfinished |
| 59 | exists (validated) | true | COMPLIANT | — |

**Overall: partial** (4 compliant, 1 partial, 2 missing). The only actionable nyquist item is **Phase 58** — its feature is verified passed (4/4) with live UAT, but the VALIDATION.md sign-off was never finalized. Lightweight to close.

---

## Tech Debt Highlights

Most items are fail-secure advisory warnings from per-phase code reviews (full list in frontmatter). Three stand out:

1. **Phase 58 D-05 (SECURITY, defense-in-depth):** Windows session hooks currently run at **Medium-IL**, not the intended Low-IL — `create_low_integrity_primary_token()` is created but not plumbed into the spawn. Documented and tracked in `adr-58-windows-hook-executor.md` (Research Open Question 1) and follow-up CR-01. A real confinement gap (not a blocker because the ADR records it as an accepted, tracked deferral); close in a dedicated follow-up phase.
2. **v0.57.4 release distribution hazard:** the superseded v0.57.4 GitHub release still carries **unsigned-payload MSIs**. Delete or annotate it to prevent inadvertent distribution (flagged non-blocking in 53-04; note Phase 61 D-07 also tracks a verify-absent/delete-if-found).
3. **Phase 58 VALIDATION sign-off:** the lone PARTIAL nyquist phase — feature verified passed, sign-off unfinished. `/gsd:validate-phase 58`.

---

## Verdict

The v2.8 milestone is **substantively complete and clean of blockers**: **10/10 requirements verified-satisfied**, every cross-phase seam WIRED, 0 integration blockers, `cargo build --workspace` clean, and two passing operator UATs (Phases 58, 59) confirm the headline Windows features work on real Win11 hardware (build 26200). The prior audit's single gap (REQ-UPST7-01 sign-off) is now closed.

What remains is **tech debt for review**, not gaps: a tracked Windows defense-in-depth confinement gap (Phase 58 D-05 Medium-IL hooks), a v0.57.4 unsigned-MSI distribution hazard to clean up, one unfinished nyquist sign-off (Phase 58), and a set of fail-secure advisory warnings already accepted at phase close. None block completion — they can be accepted into the backlog or addressed in a cleanup pass before `/gsd:complete-milestone v2.8`.

_Audited by gsd-audit-milestone · 2026-06-06 (re-audit)_
