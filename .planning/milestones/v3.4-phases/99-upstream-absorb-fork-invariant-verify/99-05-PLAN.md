---
phase: 99-upstream-absorb-fork-invariant-verify
plan: 05
type: execute
wave: 4
depends_on:
  - 99-03
files_modified:
  - crates/nono/Cargo.toml
  - Cargo.lock
  - crates/nono-cli/src/cli.rs
  - crates/nono-cli/src/cli_bootstrap.rs
  - crates/nono-proxy/src/token.rs
  - crates/nono-proxy/README.md
autonomous: true
requirements:
  - UPST11-03

must_haves:
  truths:
    - "sigstore-trust-root = \"=0.9.0\" present in crates/nono/Cargo.toml (not =0.8.0)"
    - "Cargo.lock regenerated and consistent with sigstore-trust-root 0.9.0"
    - "make build exits 0 after sigstore-trust-root bump (workspace builds clean)"
    - "cargo audit exits 0 or shows no HIGH/CRITICAL advisories for the new resolved versions"
    - "2e64798d replay commit in git log with upstream SHA trailer and DCO sign-off"
    - "X-Nono-Token stale authentication claim removed from crates/nono-proxy/README.md"
    - "a4d68189 replay commit in git log with upstream SHA trailer and DCO sign-off"
    - "cargo test -p nono-proxy --lib exits 0 (token.rs metadata changes do not break tests)"
  artifacts:
    - path: "crates/nono/Cargo.toml"
      provides: "sigstore-trust-root = \"=0.9.0\" dependency"
      contains: "sigstore-trust-root"
    - path: "Cargo.lock"
      provides: "regenerated lock file consistent with sigstore-trust-root 0.9.0 resolution"
      contains: "sigstore-trust-root"
    - path: "crates/nono-proxy/README.md"
      provides: "corrected proxy authentication documentation (no X-Nono-Token claim)"
      contains: "credential"
  key_links:
    - from: "crates/nono/Cargo.toml sigstore-trust-root"
      to: "sigstore-verify version compatibility"
      via: "cascade check: must evaluate if sigstore-verify 0.8.0 (fork) is compatible with sigstore-trust-root 0.9.0 (bump)"
      pattern: "sigstore-trust-root"
    - from: "crates/nono-proxy/src/token.rs"
      to: "EffectiveProxySettings model"
      via: "a4d68189 modifies token.rs metadata only — must not touch EffectiveProxySettings or _ep_ key namespace"
      pattern: "EffectiveProxySettings"
---

<objective>
Manually replay two will-sync commits: 2e64798d (Cluster F — sigstore-trust-root 0.8.0→0.9.0 dep bump with cascade check) and a4d68189 (Cluster G — proxy docs + deprecated flag metadata fix). These touch non-overlapping files and can be applied in sequence. Plan 05 runs in Wave 4 parallel with Plan 04 (no file overlap).

Purpose: Close two remaining simple will-sync rows in the ledger. Cluster F requires care: the fork is at sigstore-verify 0.8.0 while upstream was already at 0.9.0 at the window base — the executor must evaluate and resolve the cascade compatibility. Cluster G is accuracy-only fixes.
Output: Two DCO-signed replay commits. make build GREEN. cargo audit clean.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/99-upstream-absorb-fork-invariant-verify/99-CONTEXT.md
@.planning/phases/99-upstream-absorb-fork-invariant-verify/99-RESEARCH.md
@.planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md
@CLAUDE.md

<interfaces>
<!-- Cluster F cascade context (from 99-RESEARCH.md §Cluster F — Sigstore Cascade WARNING) -->
Current fork: sigstore-verify = { version = "0.8.0", features = ["tuf"] } in crates/nono/Cargo.toml
Upstream at v0.65.1 base: sigstore-verify = "0.9.0" (no "tuf" feature) — bump 9e084cbb predated window
2e64798d only changes: sigstore-trust-root "=0.8.0" → "=0.9.0" in nono/Cargo.toml + Cargo.lock regen

Cascade evaluation steps:
1. Edit crates/nono/Cargo.toml: sigstore-trust-root = "=0.9.0"
2. Run: cargo update -p sigstore-trust-root
   Inspect Cargo.lock diff — does the resolver ALSO pull sigstore-verify 0.9.0?
   If YES (sigstore-verify pulled to 0.9.0 by resolver): also edit sigstore-verify version; check if "tuf" feature still exists in 0.9.0
   If NO (resolver keeps sigstore-verify 0.8.0): confirm trust-root 0.9.0 / verify 0.8.0 coexist cleanly (no hard dep conflict)
3. Run: make build (full workspace build — must exit 0)
4. Run: cargo audit (check for RUSTSEC advisories on newly resolved versions)
5. If cargo audit shows HIGH/CRITICAL: evaluate; bump affected package if needed (per 260624-p1c quinn-proto precedent)

<!-- Cluster G context (from 99-RESEARCH.md §Cluster G) -->
a4d68189 changes — 4 files:
  cli.rs: +2/-2 (deprecated flag metadata)
  cli_bootstrap.rs: +62/-30 (formatting/restructuring around deprecated flag display — not behavioral)
  nono-proxy/README.md: +4/-2 (remove stale X-Nono-Token claim; correct authentication description)
  token.rs: +11/-7 (metadata accuracy only — NOT behavioral; must not touch EffectiveProxySettings or _ep_ key namespace)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Manual replay 2e64798d (Cluster F — sigstore-trust-root bump with cascade check)</name>
  <files>crates/nono/Cargo.toml, Cargo.lock</files>
  <read_first>
    Run: git show 2e64798d (see the exact Cargo.toml change: sigstore-trust-root "=0.8.0" → "=0.9.0")
    Read: crates/nono/Cargo.toml (find sigstore-trust-root + sigstore-verify + sigstore-sign current pins)
    Read: .planning/phases/99-upstream-absorb-fork-invariant-verify/99-RESEARCH.md §"Cluster F — Sigstore-Trust-Root Bump (CASCADE WARNING)" — the full cascade evaluation procedure is there
    Read: .planning/phases/99-upstream-absorb-fork-invariant-verify/99-RESEARCH.md §"Pitfall 4: Missing the sigstore-verify version gap" for the specific failure mode to avoid
  </read_first>
  <action>
    Step 1: Edit crates/nono/Cargo.toml — change the sigstore-trust-root exact-pin from "=0.8.0" to "=0.9.0". This is the direct replay of 2e64798d's Cargo.toml change.

    Step 2: Run cargo update -p sigstore-trust-root (update only this package to get the new resolution). Inspect the Cargo.lock diff carefully:
    - If the resolver ALSO bumps sigstore-verify from 0.8.0 to 0.9.0 (because sigstore-trust-root 0.9.0 has a hard dep on sigstore-verify >= 0.9.0): also edit crates/nono/Cargo.toml to change sigstore-verify from "0.8.0" to "0.9.0". Check whether the features = ["tuf"] flag still exists in sigstore-verify 0.9.0 (look at the Cargo.lock entry or run cargo metadata; if "tuf" is absent in 0.9.0, remove the features line).
    - If the resolver keeps sigstore-verify at 0.8.0 alongside sigstore-trust-root 0.9.0: accept the dual-version resolution (upstream's post-2e64798d Cargo.lock also showed dual versioning coexisting); proceed.

    Step 3: Run make build (full workspace build). If it fails with sigstore API incompatibility (e.g., sigstore_trust_root::TrustRoot API changed in 0.9.0), investigate the exact usage site in crates/nono/src/ and adapt — do not panic, the change is bounded to the sigstore trust-root surface.

    Step 4: Run cargo audit. If HIGH/CRITICAL advisories appear for newly resolved packages: bump the affected package pin (following the 260624-p1c quinn-proto precedent for RUSTSEC advisories). If only INFORMATIONAL or MEDIUM advisories that pre-existed: document in commit body.

    Step 5: Create ONE atomic DCO-signed commit:
    Subject: chore(deps): bump sigstore-trust-root from 0.8.0 to 0.9.0
    Body: Replay of upstream 2e64798d (#1229). Sigstore-trust-root exact-pin bumped from 0.8.0 to 0.9.0 in crates/nono/Cargo.toml. [Add actual cascade resolution: "sigstore-verify co-bumped to 0.9.0 (resolver required)" OR "sigstore-verify 0.8.0 retained (dual-version resolution, compatible)". Add cargo audit result.] Cargo.lock regenerated.
    Trailer: (cherry picked from commit 2e64798d...)
    Sign-off: Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>
  </action>
  <verify>
    <automated>grep "sigstore-trust-root" crates/nono/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - grep "sigstore-trust-root" crates/nono/Cargo.toml shows "=0.9.0" (not "=0.8.0")
    - git log --oneline -1 shows commit with subject starting "chore(deps): bump sigstore-trust-root"
    - git show HEAD | grep "(cherry picked from commit 2e64798d" returns a match
    - git show HEAD | grep "Signed-off-by: Oscar Mack Jr" returns a match
    - make build exits 0 (full workspace builds clean post-bump)
    - cargo audit exits 0 OR shows no HIGH/CRITICAL advisories for the new sigstore versions
    - Cargo.lock is regenerated (git diff HEAD~1 -- Cargo.lock shows changes)
  </acceptance_criteria>
  <done>2e64798d replayed as one DCO-signed commit. sigstore-trust-root is at 0.9.0. Cascade compatibility evaluated and resolved (either dual-version or co-bump, per executor finding). make build GREEN. cargo audit clean.</done>
</task>

<task type="auto">
  <name>Task 2: Manual replay a4d68189 (Cluster G — proxy docs + X-Nono-Token fix)</name>
  <files>crates/nono-cli/src/cli.rs, crates/nono-cli/src/cli_bootstrap.rs, crates/nono-proxy/src/token.rs, crates/nono-proxy/README.md</files>
  <read_first>
    Run: git show a4d68189 (see the full diff: cli.rs +2/-2, cli_bootstrap.rs +62/-30, README.md +4/-2, token.rs +11/-7)
    Read: crates/nono-proxy/src/token.rs (confirm the EffectiveProxySettings model and _ep_ key namespace are present — these must NOT be touched by the replay)
    Read: .planning/phases/99-upstream-absorb-fork-invariant-verify/99-RESEARCH.md §"Cluster G" for the summary of what changes to apply
  </read_first>
  <action>
    Inspect `git show a4d68189` and apply the changes to the four files.

    README.md (nono-proxy): Apply the +4/-2 changes. The diff removes the stale claim that proxy requests use an X-Nono-Token header and NONO_PROXY_TOKEN env var. The correct description is transparent credential shadowing. Apply verbatim from upstream diff.

    cli.rs: Apply the +2/-2 changes (deprecated flag metadata). These are small annotation-level changes to CLI flag definitions. Apply per upstream diff.

    cli_bootstrap.rs: Apply the +62/-30 changes (formatting/restructuring around deprecated flag display). This is the largest delta in this cluster but is non-behavioral — it restructures how deprecated flags are presented in the help output. Apply per upstream diff. If the fork's cli_bootstrap.rs has diverged significantly (it is a fork-modified file per Plan 03's Cluster A adoption), resolve any conflicts by preserving the fork's structural changes while applying the upstream's deprecated-flag-display restructuring on top.

    token.rs: Apply the +11/-7 changes (metadata accuracy only). This file corrects internal token handling metadata. Explicitly verify after applying: grep -n "EffectiveProxySettings" token.rs returns the same count as before (the struct is not removed or modified). Verify the _ep_ key namespace handling is not touched.

    After all four files edited:
    cargo test -p nono-proxy --lib exits 0 (token.rs changes don't break any tests)
    cargo test -p nono-cli --lib exits 0

    Then create ONE atomic DCO-signed commit:
    Subject: docs(proxy): fix stale X-Nono-Token authentication claim
    Body: Replay of upstream a4d68189 (#1246). Corrects README.md: removes X-Nono-Token header / NONO_PROXY_TOKEN env var claims (neither exists; actual mechanism is transparent credential shadowing). Fixes deprecated flag metadata in cli.rs/cli_bootstrap.rs. Corrects token.rs internal metadata (annotation-only; EffectiveProxySettings model and _ep_ namespace unchanged).
    Trailer: (cherry picked from commit a4d68189...)
    Sign-off: Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>
  </action>
  <verify>
    <automated>cargo test -p nono-proxy --lib 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - git log --oneline -1 shows commit with subject "docs(proxy): fix stale X-Nono-Token authentication claim"
    - git show HEAD | grep "(cherry picked from commit a4d68189" returns a match
    - git show HEAD | grep "Signed-off-by: Oscar Mack Jr" returns a match
    - grep -n "X-Nono-Token" crates/nono-proxy/README.md returns 0 (stale claim removed)
    - grep -n "EffectiveProxySettings" crates/nono-proxy/src/token.rs returns same count as before (struct not removed)
    - cargo test -p nono-proxy --lib exits 0
    - cargo test -p nono-cli --lib exits 0
  </acceptance_criteria>
  <done>a4d68189 replayed as one DCO-signed commit. X-Nono-Token stale claim removed from README. Deprecated flag metadata corrected in cli.rs/cli_bootstrap.rs. token.rs EffectiveProxySettings and _ep_ namespace preserved. All proxy tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sigstore-trust-root 0.9.0 supply chain | New exact-pin dep version must not introduce RUSTSEC advisories |
| proxy authentication documentation | Incorrect docs could mislead operators about security model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-11 | Tampering | sigstore-trust-root 0.9.0 supply chain (dependabot-generated) | mitigate | cargo audit after Cargo.lock regen; HIGH/CRITICAL advisories trigger remediation per 260624-p1c precedent |
| T-99-12 | Tampering | sigstore-verify API break during cascade if co-bumped to 0.9.0 | mitigate | make build must exit 0 after bump; executor must check Cargo.lock diff and adapt usage sites if API changed |
| T-99-13 | Information Disclosure | EffectiveProxySettings or _ep_ key namespace accidentally removed in token.rs | mitigate | grep EffectiveProxySettings before and after edit; count must be same |
| T-99-SC | Tampering | slopcheck — cargo update is used (not a new arbitrary install) | mitigate | cargo update -p sigstore-trust-root is a scoped update of an existing dep (not a new package); cargo audit confirms legitimacy of resolved versions |
</threat_model>

<verification>
After both tasks:
- git log --oneline -2 shows a4d68189 replay (HEAD) and 2e64798d replay (HEAD~1)
- grep "sigstore-trust-root" crates/nono/Cargo.toml: shows "=0.9.0"
- make build exits 0
- cargo audit exits 0 (or no HIGH/CRITICAL)
- grep "X-Nono-Token" crates/nono-proxy/README.md: 0 matches
- grep "EffectiveProxySettings" crates/nono-proxy/src/token.rs: >= 1 (struct preserved)
- cargo test -p nono-proxy --lib exits 0
- cargo test -p nono-cli --lib exits 0
</verification>

<success_criteria>
Clusters F and G are absorbed: 2e64798d (sigstore-trust-root bump) and a4d68189 (proxy docs fix) are in git history as two atomic DCO-signed commits. sigstore-trust-root is at 0.9.0 with cascade compatibility resolved. Cargo.lock is regenerated and make build is GREEN. cargo audit is clean. All proxy tests pass. The fork is ready for Plan 06 (Cluster C) which depends on both Plan 04 and Plan 05 completing.
</success_criteria>

<output>
Create `.planning/phases/99-upstream-absorb-fork-invariant-verify/99-05-SUMMARY.md` when done.
</output>
