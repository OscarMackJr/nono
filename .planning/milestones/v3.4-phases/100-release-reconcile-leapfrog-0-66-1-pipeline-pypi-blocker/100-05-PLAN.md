---
phase: 100-release-reconcile-leapfrog-0-66-1-pipeline-pypi-blocker
plan: 05
type: execute
wave: 3
depends_on: ["100-04"]
files_modified:
  - .planning/todos/pending/20260611-msi-vcredist-prereq.md
  - .planning/todos/pending/20260611-poc-cert-broker-clean-host.md
autonomous: false
requirements: [RLS-13]
must_haves:
  truths:
    - "The clean-host-install gate is invoked via -File on this dev host and its verdict (expected SKIP_HOST_UNAVAILABLE, since no clean Win11 VM is available on this dev host) is captured"
    - "Both folded todo files record an explicit Phase 100 status (host-gated / externally-blocked) and are NOT marked resolved or closed"
    - "The poc-cert-broker-clean-host todo explicitly notes it remains externally blocked on the active Azure Trusted Signing verify-gate thread, not attempted in this plan"
  artifacts:
    - path: ".planning/todos/pending/20260611-msi-vcredist-prereq.md"
      provides: "Phase 100 verdict note appended, still pending"
      contains: "Phase 100"
    - path: ".planning/todos/pending/20260611-poc-cert-broker-clean-host.md"
      provides: "Phase 100 externally-blocked note appended, still pending"
      contains: "Phase 100"
  key_links:
    - from: ".planning/todos/pending/20260611-msi-vcredist-prereq.md"
      to: "scripts/gates/clean-host-install.ps1"
      via: "verify-dark.ps1 -Gate clean-host-install verdict captured in this plan"
      pattern: "clean-host-install"
---

<objective>
Fold the two remaining host-gated clean-host install/UAT todos into the Phase 100
closeout: run the existing `clean-host-install` gate on this dev host and record its
verdict, and explicitly document the `poc-cert-broker-clean-host` item's external block on
the active Azure Trusted Signing verify-gate thread. Neither item is a hard Phase 100 exit
criterion (per CONTEXT.md D-09/Folded Todos) — this plan records status, it does not force
resolution.

Purpose: the code fixes underlying both todos already shipped (`+crt-static` commit
`a517284b`; Azure Trusted Signing migration commit `20cd68d9`) — only the empirical
clean-host confirmation remains, and that confirmation needs infrastructure (a clean Win11
VM, and separately a fully-verified trusted-signed release) this dev host does not have.
Output: a captured gate verdict (expected `SKIP_HOST_UNAVAILABLE`) and two todo files
updated with an explicit, non-resolving Phase 100 status note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-release-reconcile-leapfrog-0-66-1-pipeline-pypi-blocker/100-CONTEXT.md
@.planning/todos/pending/20260611-msi-vcredist-prereq.md
@.planning/todos/pending/20260611-poc-cert-broker-clean-host.md

<interfaces>
<!-- Existing gate contract, verified by direct read. No gate-script code changes in this
     plan (mirrors the v3.1 Phase 90 D-04 precedent: a broken gate is a debug finding, not
     patched here). -->

scripts/gates/clean-host-install.ps1 — exports Test-Precondition/Invoke-Gate per the
verify-dark.ps1 contract. Test-Precondition returns a SKIP reason string (not $null) on
any of: not elevated; nono.exe already present under C:\Program Files\nono (dirty host);
nono-wfp-service/nono-agentd already registered; or the MSI not staged at
$MsiPath (default `dist\windows\nono-machine.msi`). Confirmed via directory listing: this
repo's dist/windows/ directory contains only version-suffixed MSI filenames (e.g.
nono-v0.57.0-x86_64-pc-windows-msvc-machine.msi) — there is no unversioned
`nono-machine.msi` staged at the gate's default path on this dev host, so
Test-Precondition is expected to return the "MSI not found" SKIP reason regardless of host
cleanliness. This is the expected, acceptable outcome per CONTEXT.md's
"may SKIP_HOST_UNAVAILABLE by design" — not a failure to investigate.

There is no equivalent automated gate for `poc-cert-broker-clean-host` — its remaining
step is "cut a trusted-signed release + clean-host UAT", which requires both an actual
tag push (operator-gated, out of this phase) and the currently-unresolved Azure Trusted
Signing verify-gate fix (per MEMORY `azure_trusted_signing_golive.md`: signing works,
verify gate fails `UnknownError`, tracked in quick-task `260630-trusted-signing-golive`).
Document this status; do not attempt to build or stage a fresh MSI in this plan.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The existing, unmodified `clean-host-install` verify-dark gate, to be invoked on this dev host and its verdict captured.</what-built>
  <read_first>
    - scripts/gates/clean-host-install.ps1 (Test-Precondition SKIP conditions, Invoke-Gate steps)
    - scripts/verify-dark.ps1 (exit-code mapping: PASS=0, FAIL=2, SKIP_HOST_UNAVAILABLE=3, harness-internal=4; the -File invocation rule)
  </read_first>
  <how-to-verify>
    Run from a PowerShell 7 prompt:
      pwsh -File scripts/verify-dark.ps1 -Gate clean-host-install
    NEVER invoke via `pwsh -Command "<bare path>"` (swallows exit N -> 1). Capture the
    stdout verdict line, the process exit code, and the persisted
    `.nono-runtime/verdicts/clean-host-install.json`. A `SKIP_HOST_UNAVAILABLE` (exit 3) —
    most likely due to "MSI not found at dist\windows\nono-machine.msi" on this dev host —
    is the EXPECTED, acceptable outcome per CONTEXT.md's folded-todo language; it is NOT a
    failure and does NOT block Phase 100 completion. If you have access to a genuinely
    clean Win11 VM and want to pursue a real PASS: stage a freshly-built machine MSI at
    that path on the VM and re-run the gate there instead — but this is optional, not
    required for this plan.
  </how-to-verify>
  <resume-signal>Paste the verdict line + exit code (expected: SKIP_HOST_UNAVAILABLE / exit 3, or describe an actual PASS/FAIL if you ran it on a real clean VM).</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Record Phase 100 status on both folded todos without marking them resolved</name>
  <files>.planning/todos/pending/20260611-msi-vcredist-prereq.md, .planning/todos/pending/20260611-poc-cert-broker-clean-host.md</files>
  <read_first>
    - .planning/todos/pending/20260611-msi-vcredist-prereq.md (existing "Remaining — host-gated UAT only" section to append to)
    - .planning/todos/pending/20260611-poc-cert-broker-clean-host.md (existing "REMAINING" section to append to)
    - The checkpoint task's captured verdict (this task's read_first extends to that output)
  </read_first>
  <action>
    Append a dated "## Phase 100 status (2026-07-01)" section to
    `20260611-msi-vcredist-prereq.md` recording the checkpoint task's captured gate verdict
    verbatim (verdict string + exit code), and stating explicitly that the item remains
    OPEN/host-gated — a `SKIP_HOST_UNAVAILABLE` result here does not close it; a clean Win11
    VM run is still required for genuine closure. Append a dated "## Phase 100 status
    (2026-07-01)" section to `20260611-poc-cert-broker-clean-host.md` stating explicitly
    that this item remains OPEN and externally blocked: no trusted-signed release has been
    cut at 0.66.1 yet (tag push is operator-gated, outside this phase), and the Azure
    Trusted Signing verify-gate currently fails with `UnknownError` (per the active
    `quick 260630-trusted-signing-golive` thread) — clean-host UAT cannot even be attempted
    until that unblocks. Do not change either file's original problem/acceptance sections
    or move either file out of `.planning/todos/pending/`.
  </action>
  <verify>
    <automated>grep -c 'Phase 100' .planning/todos/pending/20260611-msi-vcredist-prereq.md .planning/todos/pending/20260611-poc-cert-broker-clean-host.md</automated>
  </verify>
  <acceptance_criteria>
    - The grep-count verify command shows at least 1 match in each file
    - `20260611-msi-vcredist-prereq.md` records the captured gate verdict + exit code from the checkpoint task
    - `20260611-poc-cert-broker-clean-host.md` explicitly cites the Azure Trusted Signing verify-gate `UnknownError` block
    - Neither file's status changes to resolved/closed; both remain under `.planning/todos/pending/`
  </acceptance_criteria>
  <done>Both folded todos have an explicit, dated Phase 100 status note recording their gated/deferred verification state, without being marked resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev host ↔ absent clean Win11 VM | The clean-host-install gate's proof only holds on a genuinely fresh host; this dev host cannot substitute |
| Documented status ↔ actual security posture | A todo marked "done" without a real clean-host proof would be a false-confidence record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-11 | Repudiation (false-confidence closure) | Todo status records | mitigate | Explicitly record `SKIP_HOST_UNAVAILABLE`/externally-blocked status; do not mark either todo resolved; verified by the grep check in Task 1 and by the todos remaining under `.planning/todos/pending/` |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | No package installs — this plan only invokes an existing unmodified gate script and edits two markdown todo files |
</threat_model>

<verification>
- `pwsh -File scripts/verify-dark.ps1 -Gate clean-host-install` verdict + exit code captured (expected SKIP_HOST_UNAVAILABLE).
- Both todo files carry a dated Phase 100 status section; neither is marked resolved.
</verification>

<success_criteria>
The two folded host-gated clean-host todos are explicitly recorded as gated/deferred
verification at Phase 100 close, not silently dropped and not falsely marked resolved.
</success_criteria>

<output>
Create `.planning/phases/100-release-reconcile-leapfrog-0-66-1-pipeline-pypi-blocker/100-05-SUMMARY.md` when done.
</output>
