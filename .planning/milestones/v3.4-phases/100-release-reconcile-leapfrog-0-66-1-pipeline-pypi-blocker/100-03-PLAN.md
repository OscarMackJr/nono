---
phase: 100-release-reconcile-leapfrog-0-66-1-pipeline-pypi-blocker
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/OMack/nono-py/Cargo.toml
  - C:/Users/OMack/nono-py/pyproject.toml
  - C:/Users/OMack/nono-py/src/proxy.rs
  - C:/Users/OMack/nono-py/src/policy.rs
  - C:/Users/OMack/nono-ts/Cargo.toml
  - C:/Users/OMack/nono-ts/package.json
  - C:/Users/OMack/nono-ts/package-lock.json
  - C:/Users/OMack/nono-ts/npm/darwin-arm64/package.json
  - C:/Users/OMack/nono-ts/npm/darwin-x64/package.json
  - C:/Users/OMack/nono-ts/npm/linux-arm64-gnu/package.json
  - C:/Users/OMack/nono-ts/npm/linux-x64-gnu/package.json
autonomous: true
requirements: [RLS-10, RLS-12]
must_haves:
  truths:
    - "nono-py's Cargo.toml and pyproject.toml report 0.66.1, committed in the nono-py repo with a DCO sign-off, never having committed the intermediate 0.66.0 value as its own commit"
    - "nono-py's RouteConfig has endpoint_policy: None at both construction sites (proxy.rs, policy.rs) and maturin build exits 0"
    - "nono-ts's Cargo.toml, package.json (all 5 optionalDependencies entries), package-lock.json, and the 4 tracked npm/*/package.json files all report 0.66.1, committed with a DCO sign-off"
    - "npm publish --dry-run from nono-ts shows index.js + index.d.ts in the tarball manifest"
    - "The 3 untracked nono-ts scratch files (decode-test.js, test-broker.js, test-confined.js) are not staged or committed"
    - "D-01: both binding repos (nono-py, nono-ts) get the 0.66.1 bump AND nono-py the endpoint_policy fix, each as separate DCO-signed commits in their own git repos"
    - "D-02: binding publish (PyPI wheel, npm package) stays operator-gated — this plan builds/validates clean and version-consistent but runs no push/publish"
    - "D-07: only the minimal endpoint_policy: None stub is added at both nono-py RouteConfig construction sites so the wheel builds and validation passes"
    - "D-08: fully threading endpoint_policy through nono-py RouteConfig is deferred to a named future phase — NOT attempted here"
  artifacts:
    - path: "C:/Users/OMack/nono-py/src/proxy.rs"
      provides: "endpoint_policy: None in RouteConfig::new's struct literal"
      contains: "endpoint_policy: None"
    - path: "C:/Users/OMack/nono-py/src/policy.rs"
      provides: "endpoint_policy: None in impl From<PolicyRouteConfig> for RustRouteConfig"
      contains: "endpoint_policy: None"
    - path: "C:/Users/OMack/nono-ts/package.json"
      provides: "0.66.1 package version + 5 optionalDependencies entries at 0.66.1"
      contains: "0.66.1"
  key_links:
    - from: "C:/Users/OMack/nono-py/src/proxy.rs"
      to: "crates/nono-proxy/src/config.rs RouteConfig.endpoint_policy field"
      via: "Option<EndpointPolicyConfig> struct-literal assignment"
      pattern: "endpoint_policy: None"
    - from: "C:/Users/OMack/nono-ts/Cargo.toml"
      to: "crates/nono/Cargo.toml (this repo, via ../Nono/crates/nono path-dep)"
      via: "path-dep caret version requirement"
      pattern: "nono = \\{ path = \"\\.\\./Nono/crates/nono\", version = \"0\\.66\" \\}"
---

<objective>
Finish the already-in-progress `0.66.0` -> `0.66.1` version bump in both separate binding
repos (`nono-py`, `nono-ts`) AND close the carried-forward `nono-py` `RouteConfig` PyPI
blocker (RLS-12), each as a separate DCO-signed commit in its own git repository, per D-01.

Purpose: both binding repos already have uncommitted, in-progress version-bump edits
sitting at the intermediate `0.66.0` value (left over from a prior session) — landing that
value as its own commit would fabricate a release that collides with upstream's real,
already-published `0.66.0`. This plan finishes the edits to `0.66.1` and commits once per
repo, and unblocks the `nono-py` wheel build so `twine check`/maturin validation passes.
Output: one DCO-signed commit in `nono-py` (version bump + `endpoint_policy` fix) and one
DCO-signed commit in `nono-ts` (version bump), both at `0.66.1`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-release-reconcile-leapfrog-0-66-1-pipeline-pypi-blocker/100-CONTEXT.md
@.planning/phases/100-release-reconcile-leapfrog-0-66-1-pipeline-pypi-blocker/100-RESEARCH.md

<interfaces>
<!-- Exact current uncommitted working-tree state of both binding repos, verified via git
     diff/git status during research (2026-07-01) — treat "finish these edits" as the task,
     NOT "start a fresh bump from committed HEAD". Re-verify with git status before editing;
     this state may have drifted if another process touched these repos. -->

`C:/Users/OMack/nono-py` (git status: Cargo.toml + pyproject.toml modified, uncommitted):
- Cargo.toml:3 — currently uncommitted `version = "0.66.0"` (committed HEAD is `0.9.0`) -> finish to `0.66.1`
- pyproject.toml:7 — currently uncommitted `version = "0.66.0"` (committed HEAD is `0.9.0`) -> finish to `0.66.1`
- python/nono_py/__init__.py:277 — `__version__ = "0.9.0"`, UNCHANGED and NOT part of this
  version family — an independent marker; do NOT touch it.
- src/proxy.rs:190-226 — `RouteConfig::new` constructor; the `Self { inner: RustRouteConfig
  { ... } }` literal's last field before the closing brace is `tls_ca,` at line 223. Add
  `endpoint_policy: None,` as a new line immediately after it (before the closing `},`/`}`).
- src/policy.rs:741-760 — `impl From<PolicyRouteConfig> for RustRouteConfig`; the `Self {
  ... }` literal's last field is `tls_ca: route.tls_ca,` at line 757. Add
  `endpoint_policy: None,` as a new line immediately after it.
- Field-type confirmation: `crates/nono-proxy/src/config.rs:195` (this repo, path-dep
  target) declares `pub endpoint_policy: Option<EndpointPolicyConfig>` — `None` is
  unambiguously valid; `crates/nono-proxy/src/{credential.rs,route.rs,server.rs}` use
  `endpoint_policy: None,` at ~15 other construction sites already. No new import needed —
  `None` type-infers from the field's declared type.

`C:/Users/OMack/nono-ts` (git status: Cargo.toml, package.json, package-lock.json, and 4
npm/*/package.json modified, uncommitted; 3 untracked scratch files at repo root):
- Cargo.toml:3 — currently uncommitted `version = "0.66.0"` (committed HEAD is `0.4.0`) -> finish to `0.66.1`
- Cargo.toml:18 — the `nono` path-dep is ALREADY at `version = "0.66"` (caret-style
  minor-version requirement, uncommitted; committed HEAD was `"0.62"`) — `"0.66"` already
  satisfies `0.66.1`; NO further edit needed on this specific line.
- package.json:3 — currently uncommitted `"version": "0.66.0"` (committed HEAD is `"0.4.0"`) -> finish to `0.66.1`
- package.json optionalDependencies block (currently 5 entries, all uncommitted at
  `"0.66.0"`: nono-ts-darwin-x64, nono-ts-darwin-arm64, nono-ts-linux-x64-gnu,
  nono-ts-linux-arm64-gnu, nono-ts-win32-x64-msvc) -> finish all 5 to `"0.66.1"`.
- npm/darwin-arm64/package.json:3, npm/darwin-x64/package.json:3,
  npm/linux-arm64-gnu/package.json:3, npm/linux-x64-gnu/package.json:3 — each currently
  uncommitted `"version": "0.66.0"` -> finish each to `"0.66.1"`. There are only 4 tracked
  npm/*/package.json files in this repo (`git ls-files npm/` lists darwin-arm64,
  darwin-x64, linux-arm64-gnu, linux-x64-gnu only) — do NOT expect or create a 5th
  `npm/win32-x64-msvc/package.json`; it is generated/published from elsewhere and not
  checked into this repo (out of RLS-10's version-string-sync scope).
- package-lock.json — STILL shows the OLD `0.4.0` values (root `name`/`version` at lines
  2-3 and 8-9, plus the `optionalDependencies` block at lines 20-25 still listing
  `"0.4.0"` for the 4 present entries) — this file was NOT part of the prior session's
  uncommitted edit and needs a real regen, not a hand-edit: run
  `npm install --package-lock-only` from the nono-ts repo root after the package.json
  edits above land, to sync package-lock.json's version fields to `0.66.1`.
- Untracked scratch files at repo root: decode-test.js, test-broker.js, test-confined.js —
  unrelated to this phase. Do NOT `git add -A`; stage only the specific version-bump files
  named in files_modified above.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finish nono-py version bump to 0.66.1 + close the RouteConfig endpoint_policy PyPI blocker</name>
  <files>C:/Users/OMack/nono-py/Cargo.toml, C:/Users/OMack/nono-py/pyproject.toml, C:/Users/OMack/nono-py/src/proxy.rs, C:/Users/OMack/nono-py/src/policy.rs</files>
  <read_first>
    - C:/Users/OMack/nono-py/Cargo.toml, C:/Users/OMack/nono-py/pyproject.toml (confirm current uncommitted 0.66.0 state via `git diff` before editing — re-verify, do not assume the RESEARCH.md snapshot is still current)
    - C:/Users/OMack/nono-py/src/proxy.rs:190-226, C:/Users/OMack/nono-py/src/policy.rs:741-760 (exact insertion points)
    - crates/nono-proxy/src/config.rs:195 (field-type confirmation: `pub endpoint_policy: Option<EndpointPolicyConfig>`)
  </read_first>
  <action>
    In the `C:/Users/OMack/nono-py` repo: edit Cargo.toml:3 and pyproject.toml:7 from the
    currently-uncommitted `0.66.0` to `0.66.1` (do not touch
    python/nono_py/__init__.py:277, an independent, unrelated version marker). Add
    `endpoint_policy: None,` as a new field immediately after `tls_ca,` inside
    `RouteConfig::new`'s `Self { inner: RustRouteConfig { ... } }` literal in src/proxy.rs
    (currently ending at line 223), and immediately after `tls_ca: route.tls_ca,` inside
    `impl From<PolicyRouteConfig> for RustRouteConfig`'s `Self { ... }` literal in
    src/policy.rs (currently ending at line 757). Run `maturin build` from the nono-py repo
    root to confirm the wheel builds with the field addition. Stage exactly these 4 files
    (`git add Cargo.toml pyproject.toml src/proxy.rs src/policy.rs` — never `git add -A`)
    and create ONE commit in the nono-py repo with message describing the 0.66.1 version
    bump + endpoint_policy stub fix, ending with a DCO trailer
    `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`. Never commit the intermediate
    `0.66.0` value as its own commit — edit directly to `0.66.1` before this single commit.
  </action>
  <verify>
    <automated>cd C:/Users/OMack/nono-py && maturin build</automated>
  </verify>
  <acceptance_criteria>
    - `maturin build` exits 0 from the nono-py repo root
    - `grep -c 'endpoint_policy: None' C:/Users/OMack/nono-py/src/proxy.rs C:/Users/OMack/nono-py/src/policy.rs` returns `2` (one per file)
    - `grep '"0.66.1"' C:/Users/OMack/nono-py/Cargo.toml C:/Users/OMack/nono-py/pyproject.toml` matches both files
    - `python/nono_py/__init__.py` is unchanged (still `__version__ = "0.9.0"`)
    - Exactly one new commit exists in the nono-py repo, DCO-signed, and `git log -p -1` never shows a prior commit at `0.66.0`
  </acceptance_criteria>
  <done>nono-py is committed at 0.66.1 with the endpoint_policy field present at both RouteConfig construction sites; maturin build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Finish nono-ts version bump to 0.66.1 across all tracked manifests</name>
  <files>C:/Users/OMack/nono-ts/Cargo.toml, C:/Users/OMack/nono-ts/package.json, C:/Users/OMack/nono-ts/package-lock.json, C:/Users/OMack/nono-ts/npm/darwin-arm64/package.json, C:/Users/OMack/nono-ts/npm/darwin-x64/package.json, C:/Users/OMack/nono-ts/npm/linux-arm64-gnu/package.json, C:/Users/OMack/nono-ts/npm/linux-x64-gnu/package.json</files>
  <read_first>
    - C:/Users/OMack/nono-ts/Cargo.toml, package.json, package-lock.json (confirm current uncommitted state via `git diff`/`git status` before editing)
    - .planning/phases/100-release-reconcile-leapfrog-0-66-1-pipeline-pypi-blocker/100-RESEARCH.md § "Common Pitfalls / Pitfall 2" (no 5th npm/win32-x64-msvc/package.json exists in this repo — do not create one)
  </read_first>
  <action>
    In the `C:/Users/OMack/nono-ts` repo: edit Cargo.toml:3 from the currently-uncommitted
    `0.66.0` to `0.66.1` (leave Cargo.toml:18's `nono` path-dep at `version = "0.66"` —
    the caret range already satisfies `0.66.1`, no edit needed). Edit package.json:3 and
    all 5 `optionalDependencies` entries (nono-ts-darwin-x64, nono-ts-darwin-arm64,
    nono-ts-linux-x64-gnu, nono-ts-linux-arm64-gnu, nono-ts-win32-x64-msvc) from the
    currently-uncommitted `0.66.0` to `0.66.1`. Edit each of the 4 tracked
    npm/*/package.json files' `version` field (darwin-arm64, darwin-x64, linux-arm64-gnu,
    linux-x64-gnu) from `0.66.0` to `0.66.1` — do not create a 5th
    npm/win32-x64-msvc/package.json (it doesn't exist in this repo; out of scope). Run
    `npm install --package-lock-only` from the nono-ts repo root to regenerate
    package-lock.json's version fields (it currently still shows stale `0.4.0` values —
    this needs a real regen, not a hand-edit). Run `npm publish --dry-run` to confirm the
    tarball manifest. Stage exactly the 7 files in this task's `<files>` list (never
    `git add -A` — the 3 untracked scratch files decode-test.js/test-broker.js/
    test-confined.js must NOT be staged) and create ONE commit in the nono-ts repo with a
    message describing the 0.66.1 version bump, ending with a DCO trailer
    `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`. Never commit the intermediate
    `0.66.0` value as its own commit.
  </action>
  <verify>
    <automated>cd C:/Users/OMack/nono-ts && npm publish --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `npm publish --dry-run` exits 0 and its manifest output includes `index.js` and `index.d.ts`
    - `grep -c '0.66.1' C:/Users/OMack/nono-ts/package.json` shows the package version plus all 5 optionalDependencies entries updated
    - `grep '"version"' C:/Users/OMack/nono-ts/package-lock.json` shows `0.66.1` (not stale `0.4.0`)
    - Each of the 4 npm/*/package.json files reads `"version": "0.66.1"`
    - `git status` in nono-ts shows the 3 scratch files (decode-test.js, test-broker.js, test-confined.js) still untracked, not staged
    - Exactly one new commit exists in the nono-ts repo, DCO-signed, and `git log -p -1` never shows a prior commit at `0.66.0`
  </acceptance_criteria>
  <done>nono-ts is committed at 0.66.1 across Cargo.toml, package.json, package-lock.json, and all 4 tracked npm/*/package.json files; npm publish --dry-run passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nono workspace repo → separate binding repos (nono-py, nono-ts) | Each binding repo is independently publishable to a public registry (PyPI, npm); a bad commit here has its own blast radius separate from the crates.io publish |
| Rust `RouteConfig` struct field addition → Python-facing wheel behavior | `endpoint_policy: None` is a binding-layer stub, not a policy or business-logic change — must not silently alter routing behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-06 | Tampering | Binding-repo commit history | mitigate | Never commit the intermediate `0.66.0` value as its own commit — edit directly to `0.66.1` before the single DCO-signed commit in each repo; verified by `git log -p -1` never showing a `0.66.0` commit |
| T-100-07 | Information Disclosure | nono-ts untracked scratch files (decode-test.js, test-broker.js, test-confined.js) | mitigate | Stage exactly the enumerated version-bump files; never `git add -A`; verified by `git status` showing the 3 files still untracked after commit |
| T-100-08 | Tampering (behavior drift) | `endpoint_policy: None` stub | accept | Field type confirmed `Option<EndpointPolicyConfig>`; `None` is the pre-existing default used at ~15 other `nono-proxy` construction sites — a no-op for routing behavior, verified by `maturin build` passing with no other code path touched |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | No new package introduced in either binding repo — this plan edits existing manifest version strings and one struct-literal field; `npm install --package-lock-only` only regenerates lockfile version metadata, does not add packages |
</threat_model>

<verification>
- `maturin build` exits 0 from `C:/Users/OMack/nono-py`; `endpoint_policy: None` present at both construction sites.
- `npm publish --dry-run` exits 0 from `C:/Users/OMack/nono-ts` with index.js + index.d.ts in the manifest.
- One DCO-signed commit exists in each binding repo, landing directly at `0.66.1` (never `0.66.0`).
</verification>

<success_criteria>
Both binding repos build/validate clean and are version-consistent at `0.66.1`; the
`nono-py` `RouteConfig` PyPI blocker is closed. Publish to PyPI/npm remains operator-gated
(D-02) — this plan does not push.
</success_criteria>

<output>
Create `.planning/phases/100-release-reconcile-leapfrog-0-66-1-pipeline-pypi-blocker/100-03-SUMMARY.md` when done.
</output>
