---
phase: 98-upst11-divergence-audit
plan: 03
type: execute
wave: 2
depends_on: [98-01]
files_modified:
  - proj/ADR-98-network-intent-disposition.md
autonomous: true
requirements: [UPST11-01]

must_haves:
  truths:
    - "The real upstream shape of #1225 NetworkIntent is captured from the actual commit diff (git show), not the CHANGELOG summary (D-04.1)"
    - "Every fork touchpoint of the surface #1225 replaces (NetworkMode::ProxyOnly + CompiledEndpointPolicy) is mapped to a file:line (D-04.2)"
    - "The ADR renders an explicit adopt-vs-diverge recommendation tied to which fork invariants each option affects (D-04.3 / D-05)"
    - "The ADR is a standalone file proj/ADR-98-network-intent-disposition.md mirroring ADR-86/87 naming (D-06)"
  artifacts:
    - path: "proj/ADR-98-network-intent-disposition.md"
      provides: "The settled #1225 NetworkIntent-vs-ProxyOnly disposition with diff-grounded analysis + recommendation"
      contains: "## Decision"
  key_links:
    - from: "ADR-98 Decision"
      to: "ADR-86 policy-free library boundary + Windows WFP/AppContainer network model"
      via: "explicit non-regression rationale"
      pattern: "ADR-86"
---

<objective>
Settle the headline fork-specific decision of this milestone: whether the fork adopts upstream's
#1225 `NetworkIntent` refactor (precedent: v3.1 Phase 86 boundary-convergence) or writes a fork
-divergence carve-out preserving `NetworkMode::ProxyOnly`. The decision is rendered as a
standalone, diff-grounded ADR — `proj/ADR-98-network-intent-disposition.md` — that maps every fork
touchpoint and ties the recommendation to the fork invariants each option affects.

Purpose: Per the v3.4 roadmap, #1225 touches the core network-capability enum the Windows backends
key off; the adopt-vs-diverge call must be deliberate and documented BEFORE any cherry-pick (D-04:
open, diff-grounded, no pre-commitment). The ADR is the gate for Phase 99's #1225 handling and is
independently citable, surviving ledger archival (D-06).
Output: `proj/ADR-98-network-intent-disposition.md`. The ledger cross-references it (Plan 04); it
does not duplicate the analysis inline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/98-upst11-divergence-audit/98-CONTEXT.md

# ADR format to mirror (Status / Phase / Date / Context / Decision / Rationale / Consequences):
@proj/ADR-87-cr02-audit-bypass.md
# The policy-free boundary the disposition must NOT regress AND the adopt-precedent:
@proj/ADR-86-library-boundary-convergence.md
# The #1225 surface in the fork (map every ProxyOnly touchpoint here):
@crates/nono/src/capability.rs
# Secondary ProxyOnly consumers + the v3.3 Phase 95 endpoint-policy surface #1225 collides with:
@crates/nono/src/manifest_convert.rs
@crates/nono/src/sandbox/linux.rs
@crates/nono-cli/src/network_policy.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: git-show #1225 + map every fork ProxyOnly/CompiledEndpointPolicy touchpoint</name>
  <files>proj/ADR-98-network-intent-disposition.md</files>
  <read_first>
    - crates/nono/src/capability.rs (NetworkMode::ProxyOnly constructors ~1045/1065, match arms ~843/1386/2711+, doc ~707 per D-04)
    - crates/nono/src/manifest_convert.rs (ProxyOnly at ~:47)
    - crates/nono/src/sandbox/linux.rs (ProxyOnly at ~:386/620)
    - crates/nono-cli/src/network_policy.rs (CompiledEndpointPolicy / endpoint_policy.evaluate())
    - .planning/phases/98-upst11-divergence-audit/98-CONTEXT.md (D-04, D-05)
  </read_first>
  <action>
    Capture upstream's real #1225 shape: from the Plan 01 fetched window, identify the #1225
    commit(s) (`git log --oneline <from>..<to> | grep -iE '1225|NetworkIntent'` and
    `git show <sha>`) and record the actual `NetworkIntent` enum/struct definition, what it
    replaces, and which files upstream touches — NOT the CHANGELOG summary (D-04.1). Then map EVERY
    fork touchpoint of the surface it replaces using `grep -rn 'ProxyOnly' crates/` and
    `grep -rn 'CompiledEndpointPolicy\|endpoint_policy' crates/`: enumerate each as file:line with a
    one-line role note, covering at minimum `crates/nono/src/capability.rs` (constructors, match
    arms, doc), `crates/nono/src/manifest_convert.rs`, `crates/nono/src/sandbox/linux.rs`, the
    Windows WFP/AppContainer backends, the proxy-filter post-fork path, and the v3.3 Phase 95
    `CompiledEndpointPolicy`/`endpoint_policy.evaluate()` surface across
    `crates/nono-cli/src/network_policy.rs` and
    `crates/nono-proxy/src/{config,credential,reverse,route,server}.rs`. Confirm the fork has no
    `NetworkIntent` (`grep -rl NetworkIntent crates/` empty). Create
    `proj/ADR-98-network-intent-disposition.md` with the ADR-87 header shape
    (`# ADR-98: ...`, **Status: Proposed**, **Phase: 98**, **Date**, **Authors**), a `## Context`
    section embedding upstream's diff-grounded #1225 shape, and a `## Fork Touchpoint Map` section
    listing every enumerated file:line touchpoint. Use the Write tool.
  </action>
  <acceptance_criteria>
    - `proj/ADR-98-network-intent-disposition.md` exists with the ADR-87 header shape (title, Status, Phase 98, Date).
    - `## Context` captures upstream's #1225 NetworkIntent shape derived from `git show` of the real commit (cites a SHA), not the CHANGELOG.
    - `## Fork Touchpoint Map` enumerates ProxyOnly + CompiledEndpointPolicy touchpoints as file:line entries covering capability.rs, manifest_convert.rs, sandbox/linux.rs, the Windows backends, and the Phase 95 endpoint-policy proxy surface.
    - The map confirms the fork has no `NetworkIntent`.
  </acceptance_criteria>
  <verify>
    <automated>test -f proj/ADR-98-network-intent-disposition.md && grep -q '# ADR-98' proj/ADR-98-network-intent-disposition.md && grep -q '## Context' proj/ADR-98-network-intent-disposition.md && grep -qi 'touchpoint' proj/ADR-98-network-intent-disposition.md && grep -q 'ProxyOnly' proj/ADR-98-network-intent-disposition.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Weigh adopt-vs-diverge options and render the recommendation</name>
  <files>proj/ADR-98-network-intent-disposition.md</files>
  <read_first>
    - proj/ADR-98-network-intent-disposition.md (the Context + Touchpoint Map from Task 1)
    - proj/ADR-86-library-boundary-convergence.md (the adopt precedent + the policy-free boundary the decision must not regress)
    - .planning/phases/98-upst11-divergence-audit/98-CONTEXT.md (D-05, D-06)
  </read_first>
  <action>
    Add an `## Options Considered` section weighing the two options per ROADMAP SC2:
    (A) full-sync-adopt the upstream `NetworkIntent` refactor (precedent: Phase 86 adopted
    upstream's high-conflict boundary refactors) and (B) a written fork-divergence carve-out
    preserving `NetworkMode::ProxyOnly`. For EACH option, state which fork invariants it affects:
    the policy-free library boundary (ADR-86), the Windows WFP/AppContainer/broker network model,
    and the v3.3 Phase 95 `CompiledEndpointPolicy`/`evaluate()` endpoint-policy surface; and flag
    the cfg-gated surfaces (`#[cfg(target_os = "linux")]`/`"macos"`) that Phase 99 must
    cross-target-clippy-verify. Add a `## Decision` section rendering an EXPLICIT adopt-vs-diverge
    recommendation, and a `## Consequences` section stating, for the recommended option, the
    concrete Phase 99 absorb implications (which touchpoints change, which guard tests/invariants
    must be preserved, the non-regression guarantee for ADR-86 and the Windows network model). The
    recommendation MUST NOT regress the policy-free boundary or the Windows network model regardless
    of which option wins (D-05). Edit the existing ADR file. Keep the analysis here only — the
    ledger merely cross-references this file (D-06).
  </action>
  <acceptance_criteria>
    - `## Options Considered` weighs BOTH full-sync-adopt and fork-divergence carve-out, each with its affected-fork-invariants list (ADR-86 boundary, Windows network model, Phase 95 endpoint-policy surface) and a cfg-gated-surface flag for Phase 99.
    - `## Decision` renders one explicit recommendation (adopt OR diverge) — not a deferral or a bare TBD.
    - `## Consequences` states the recommended option's concrete Phase 99 absorb implications and the ADR-86 / Windows-network-model non-regression guarantee.
    - The ADR contains no inline duplication of the cluster ledger (it is the standalone analysis per D-06).
  </acceptance_criteria>
  <verify>
    <automated>grep -q '## Options Considered' proj/ADR-98-network-intent-disposition.md && grep -q '## Decision' proj/ADR-98-network-intent-disposition.md && grep -q '## Consequences' proj/ADR-98-network-intent-disposition.md && grep -q 'ADR-86' proj/ADR-98-network-intent-disposition.md && ! grep -nE '\| *TBD *\|' proj/ADR-98-network-intent-disposition.md</automated>
  </verify>
</task>

</tasks>

<threat_model>
N/A — documentation/audit phase. This plan produces a markdown ADR analyzing a future code
disposition; it introduces no code, no attack surface, and no data handling. The SECURITY content
(whether adopting #1225 would regress the Windows network model or the ADR-86 policy-free boundary)
is the analytical subject of the ADR's Options/Decision sections, not an enforced code change here.
The actual #1225 implementation — and its cross-target clippy verification — happens in Phase 99.
</threat_model>

<verification>
- `proj/ADR-98-network-intent-disposition.md` exists with Context, Fork Touchpoint Map, Options Considered, Decision, Consequences.
- Upstream #1225 shape is diff-grounded (cites the real commit SHA).
- Every ProxyOnly/CompiledEndpointPolicy fork touchpoint is mapped to file:line.
- The Decision is an explicit adopt-vs-diverge recommendation tied to fork invariants.
</verification>

<success_criteria>
- ROADMAP SC2 satisfied: the #1225 NetworkIntent-vs-ProxyOnly call is settled in an ADR (full-sync-adopt OR fork-divergence carve-out, with rationale for which invariants each affects).
- D-04/D-05/D-06 all traceably satisfied in a standalone, independently-citable file.
</success_criteria>

<output>
Create `.planning/phases/98-upst11-divergence-audit/98-03-SUMMARY.md` when done.
</output>
