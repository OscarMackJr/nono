---
phase: 98-upst11-divergence-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md
  - ci-logs-local/drift/
autonomous: true
requirements: [UPST11-01]

must_haves:
  truths:
    - "The audit window v0.65.1..v0.66.0 is fetched from upstream and its tip SHAs are pinned in the ledger Reproduction block (D-01/D-02)"
    - "The drift tool is re-run against explicit window SHAs (not tag names) and its output count is recorded (D-02/D-10)"
    - "Every commit in the window lands in exactly one place: substantive + noise = total window commit count (D-12)"
  artifacts:
    - path: ".planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md"
      provides: "Ledger frontmatter, Reproduction block, Excluded-as-Noise section, Cluster Summary scaffold"
      contains: "## Reproduction"
    - path: "ci-logs-local/drift/"
      provides: "Drift-tool JSON output for the window, run against explicit SHAs"
  key_links:
    - from: "98-DIVERGENCE-LEDGER.md frontmatter"
      to: "git ls-remote --tags upstream (v0.65.1, v0.66.0 SHAs)"
      via: "pinned upstream_head_at_audit + range SHAs"
      pattern: "drift_tool_invocation"
---

<objective>
Establish the empirical foundation of the UPST11 divergence audit: fetch the upstream
`v0.65.1..v0.66.0` window, pin the window tip SHAs and the drift-tool SHA, re-run the drift
tool against explicit SHAs, and reconcile the full commit accounting (substantive vs noise vs
merges) so every commit in the window is provably accounted for exactly once.

Purpose: This is the reproducibility spine the rest of the audit hangs on. Per D-02 the audit
MUST run against explicit window SHAs (not tag names — the SHA-not-tag guard from Phase 85/94),
and per D-12 the noise reconciliation must close (substantive + noise = total).
Output: `98-DIVERGENCE-LEDGER.md` created with frontmatter, Reproduction block, Excluded-as-Noise
section, and an empty Cluster Summary scaffold (cluster bodies are Plan 02). The drift JSON
artifact under `ci-logs-local/drift/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-upst11-divergence-audit/98-CONTEXT.md

# The DIRECT structural template — reproduce this header + Reproduction + Noise shape exactly:
@.planning/milestones/v3.3-phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md
# The UPST9 ledger — Reproduction-block + SHA-guard precedent (range note, ls-remote):
@.planning/milestones/v3.1-phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch window, pin SHAs + drift-tool SHA, run drift tool, write ledger Reproduction block</name>
  <files>.planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md, ci-logs-local/drift/</files>
  <read_first>
    - .planning/milestones/v3.3-phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md (frontmatter + Reproduction block shape — copy field names: range, range_note, upstream_head_at_audit, refetch_date, drift_tool_sh_sha, drift_tool_ps1_sha, drift_tool_invocation, fork_baseline, total_unique_commits, date)
    - .planning/phases/98-upst11-divergence-audit/98-CONTEXT.md (D-01, D-02, D-10)
    - scripts/check-upstream-drift.sh (the drift tool — note its path-inclusion filter and exclusion patterns)
  </read_first>
  <action>
    Run `git fetch upstream --tags` (remote `upstream` is already `nolabs-ai/nono`; do NOT repoint
    it). Resolve the window tip SHAs with `git ls-remote --tags upstream v0.65.1` and
    `git ls-remote --tags upstream v0.66.0`; also resolve the #1293 release-cut commit
    (`605bb6c5`, Luke Hinds) if reachable. Verify each window tip is a real object with
    `git cat-file -t <sha>` (expect `commit`). Pin the drift-tool SHAs with
    `git log -1 --format=%H -- scripts/check-upstream-drift.sh` and `... -- scripts/check-upstream-drift.ps1`.
    Create `ci-logs-local/drift/` and run the drift tool against EXPLICIT window SHAs (NOT tag names,
    per D-02 SHA-not-tag guard): `bash scripts/check-upstream-drift.sh --from <v0.65.1-sha> --to <v0.66.0-sha> --format json > ci-logs-local/drift/<UTCstamp>-v0651-v0660-upst11.json`.
    Record `total_unique_commits` from the JSON. Create
    `.planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md` with: frontmatter mirroring
    the Phase 94 field set (range = `<v0.65.1-sha>..<v0.66.0-sha> (v0.65.1..v0.66.0)`,
    `fork_baseline: v0.65.1` per D-01, the pinned drift SHAs, the exact `drift_tool_invocation`
    string, `upstream_head_at_audit` = v0.66.0 SHA, `date`); and a `## Reproduction` section
    containing the verbatim commands above with their observed outputs, plus an "Auditor rerun"
    note. Use the Write tool. Do NOT classify clusters yet (Plan 02).
  </action>
  <acceptance_criteria>
    - Ledger frontmatter present with all 10 Phase-94 field names; `range` carries explicit SHAs (not bare tag names) AND the `(v0.65.1..v0.66.0)` human label.
    - `drift_tool_sh_sha` and `drift_tool_ps1_sha` are 40-char SHAs obtained from `git log -1 --format=%H`.
    - `drift_tool_invocation` references explicit `--from <sha> --to <sha>` (SHA-not-tag guard satisfied — no `v0.65.1`/`v0.66.0` tag names inside the `--from`/`--to` args).
    - A drift JSON file exists under `ci-logs-local/drift/` and its `total_unique_commits` equals the value recorded in frontmatter.
    - `## Reproduction` block contains the fetch, ls-remote, cat-file, drift-tool, and commit-accounting commands with observed outputs.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md && grep -q '## Reproduction' .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md && grep -q 'drift_tool_invocation' .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md && ls ci-logs-local/drift/*upst11*.json</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Reconcile noise vs substantive, write Excluded-as-Noise section + Cluster Summary scaffold</name>
  <files>.planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md</files>
  <read_first>
    - .planning/milestones/v3.3-phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md (the "Excluded as Noise" section + "Cluster Summary" table shape)
    - .planning/quick/260629-toe-v066-parity/PLAN.md (the preliminary per-PR disposition list — the starting hypothesis for the cluster scaffold, per D-03; NOT ground truth)
    - .planning/phases/98-upst11-divergence-audit/98-CONTEXT.md (D-11, D-12)
  </read_first>
  <action>
    Compute the full commit accounting against the SAME explicit window SHAs from Task 1:
    `git log --oneline <from>..<to> | wc -l` (total), `git log --merges --oneline <from>..<to> | wc -l`
    (merges), `git log --no-merges --oneline <from>..<to> | wc -l`. Cross-reference the drift JSON's
    substantive count. Enumerate EVERY non-substantive commit (merge commits + non-merge
    out-of-filter commits — Cargo.lock-only dep bumps, CI-yaml-only, docs-only) into an
    `## Excluded as Noise` section, mirroring the Phase 94 section: list path-filter criteria
    (`crates/nono/src/`, `crates/nono-cli/src/`, `crates/nono-proxy/src/`, `crates/nono/Cargo.toml`;
    exclusion patterns `*_windows.rs`, `crates/nono-cli/src/exec_strategy_windows/`), the counts
    block, and a one-line-per-commit noise list with sha + subject + reason. Add a
    "Completeness verification" line asserting substantive + noise = total. Then add a
    `## Cluster Summary` scaffold table with the Phase 94 columns
    (`cluster_id | theme | commits | disposition | windows-touch | rationale | phase-99-status`)
    using FRESH letter cluster IDs A, B, C... (D-11, independent of Phase 85/94 lettering),
    seeded from the 260629-toe hypothesis — but leave per-cluster theme/disposition cells as a
    first-pass draft to be confirmed by Plan 02's actual-diff inspection. Edit the existing ledger
    file (do not recreate). Reserve the `won't-sync` cluster for release/version commits per D-15.
  </action>
  <acceptance_criteria>
    - `## Excluded as Noise` section present with path-filter criteria, a counts block, a per-commit noise list, and a "Completeness verification" line.
    - The completeness line asserts substantive + noise = total and the arithmetic is internally consistent with the counts block.
    - `## Cluster Summary` scaffold table present with the 7 Phase-94 columns and fresh letter cluster IDs starting at A.
    - At least one cluster row is reserved for release/version metadata with disposition `won't-sync` (D-15).
    - No commit from the window appears both in a cluster row and in the noise list.
  </acceptance_criteria>
  <verify>
    <automated>grep -q '## Excluded as Noise' .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md && grep -q '## Cluster Summary' .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md && grep -q "won't-sync" .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md && grep -qi 'completeness' .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md</automated>
  </verify>
</task>

</tasks>

<threat_model>
N/A — documentation/audit phase. This plan introduces no production code, no attack surface, no
input-handling, and no data flow; the deliverables are markdown analysis artifacts plus a drift-tool
JSON log. STRIDE enumeration is not applicable. The security CONTENT being analyzed (fork
invariants, the #1225 disposition) is assessed in the ADR (Plan 03) and the carve-out check (Plan
04), not enforced by code here.
</threat_model>

<verification>
- Ledger file exists with frontmatter, Reproduction block, Excluded-as-Noise, and Cluster Summary scaffold.
- Drift JSON exists under `ci-logs-local/drift/` and matches `total_unique_commits` in frontmatter.
- Noise reconciliation closes: substantive + noise = total window commit count.
</verification>

<success_criteria>
- The audit is reproducible from the Reproduction block alone (pinned SHAs + drift-tool SHA + exact invocation).
- Every commit in `v0.65.1..v0.66.0` is accounted for exactly once (cluster scaffold or noise list).
- Plan 02 and Plan 03 can begin from a populated, fetched window.
</success_criteria>

<output>
Create `.planning/phases/98-upst11-divergence-audit/98-01-SUMMARY.md` when done.
</output>
