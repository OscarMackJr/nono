---
phase: 98-upst11-divergence-audit
plan: 04
type: execute
wave: 3
depends_on: [98-02, 98-03]
files_modified:
  - .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md
autonomous: true
requirements: [UPST11-01]

must_haves:
  truths:
    - "All SIX expanded carve-outs are re-touch-checked with git log against their exact paths (D-07)"
    - "Every carve-out has an explicit verdict line — a zero-hit is recorded as 'clean — no re-touch in window' (silence is not evidence) (D-07)"
    - "Every carve-out hit is flagged 'expected conflict — preserve fork expression' and carried into Phase 99 guidance (D-08)"
    - "The ledger cross-references ADR-98 for the #1225 disposition rather than inlining it (D-06)"
  artifacts:
    - path: ".planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md"
      provides: "Carve-out Re-touch Check section (6 carve-outs) + ADR-98 cross-reference"
      contains: "## Carve-out Re-touch Check"
  key_links:
    - from: "Carve-out Re-touch Check"
      to: "Phase 99 cherry-pick guidance"
      via: "each hit flagged expected-conflict-preserve-fork-expression"
      pattern: "preserve fork expression"
---

<objective>
Run the expanded mandatory carve-out re-touch check — the second headline fork-specific deliverable
of this phase — covering all SIX accumulated carve-outs, and wire the ledger's cross-reference to
the standalone ADR-98. For each carve-out, `git log <window> -- <exact path>` and record an
explicit verdict: a zero-hit is "clean — no re-touch in window" (silence is not evidence, D-07);
a hit is flagged "expected conflict — preserve fork expression" and carried into Phase 99 guidance
(D-08).

Purpose: The carve-out check is how deliberate fork divergences survive an upstream sync. This
cycle expands the Phase 94 three-carve-out check to six (D-07) — including the v3.3 Phase 95
endpoint-policy wiring, which is the very surface #1225 restructures (highest re-touch exposure
this window).
Output: the `## Carve-out Re-touch Check` section of `98-DIVERGENCE-LEDGER.md` plus the ADR-98
cross-reference in the headline/routing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-upst11-divergence-audit/98-CONTEXT.md
@.planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md
@proj/ADR-98-network-intent-disposition.md

# The carve-out check format to mirror (per-carve-out git log + verdict + Phase-N guidance):
@.planning/milestones/v3.3-phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md
# Carve-outs 1-3 authoritative source (CR-02 / CR-01 / Cluster F addenda):
@.planning/milestones/v3.1-phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md
@proj/ADR-87-cr02-audit-bypass.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-touch check all six carve-outs with explicit verdicts</name>
  <files>.planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md</files>
  <read_first>
    - .planning/milestones/v3.3-phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md (the "Carve-out Re-touch Check" section format — per-carve-out git log block, HIT/clean verdict, guard-test name, Phase-N guidance)
    - .planning/milestones/v3.1-phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md (the three addenda defining carve-outs 1-3: CR-02, CR-01, Cluster F — exact files/lines + future-sync-note wording)
    - .planning/phases/98-upst11-divergence-audit/98-CONTEXT.md (D-07 — the exact six-carve-out path set; D-08)
  </read_first>
  <action>
    Add a `## Carve-out Re-touch Check` section to the ledger. For EACH of the six carve-outs, run
    `git log --oneline <from>..<to> -- <exact paths>` (using the Plan 01 window SHAs) and record the
    command, its output, and an EXPLICIT verdict line. The six (per D-07):
    (1) CR-02 — `crates/nono/src/audit.rs` (`records_verified: event_count > 0`; guard test
    `verify_empty_log_with_no_stored_metadata_is_not_valid`; ADR-87).
    (2) CR-01 — `bindings/c/src/{diagnostic,lib,capability_set,fs_capability,sandbox,state,query}.rs`
    (`clear_last_call_state()` at entry; guard test `diagnostic_code_is_cleared_between_calls`).
    (3) Cluster F proxy fork model — `crates/nono-proxy/src/{route,connect,reverse,server}.rs`,
    `crates/nono-cli/src/proxy_runtime.rs`, the ABSENT `crates/nono-proxy/src/tls_intercept/` dir
    (confirm absence with `ls crates/nono-proxy/src/`), and the `EffectiveProxySettings` model.
    (4) v3.3 Phase 95 endpoint-policy wiring — `crates/nono-cli/src/network_policy.rs` and
    `crates/nono-proxy/src/{config,credential,reverse,route,server}.rs`
    (`CompiledEndpointPolicy`/`endpoint_policy.evaluate()`) — flag as highest re-touch exposure
    because #1225 restructures this surface; cross-reference ADR-98.
    (5) v3.3 Phase 95 restored fork invariants — `crates/nono/src/sandbox/linux.rs` (arch-portable
    `offset_of!` msghdr derivation, GPU Landlock enforcement, post-fork fork-safety, AF_UNIX/seccomp
    paths).
    (6) v3.2 override surface — `PolicyOverrideApplied` audit variant / SecurityEventLayer EventIDs
    10006-10010 (lives in nono-py; flag any core-crate `nono` audit-variant re-touch via
    `git log <window> -- crates/nono/src/audit.rs` filtered for the override variant).
    A zero-hit MUST be written as "clean — no re-touch in window" (D-07). Every HIT MUST be flagged
    "expected conflict — preserve fork expression", name the guard test, and state the Phase 99
    cherry-pick guidance (D-08). Edit the existing ledger file.
  </action>
  <acceptance_criteria>
    - `## Carve-out Re-touch Check` present with exactly six labelled subsections (CR-02, CR-01, Cluster F, Phase 95 endpoint-policy, Phase 95 restored invariants, v3.2 override surface).
    - Each subsection shows the `git log ... -- <path>` command, its output, and an explicit verdict line ("clean — no re-touch in window" OR "HIT — expected conflict — preserve fork expression").
    - No carve-out is left without an explicit verdict (no silence/blank).
    - Every HIT names a guard test and states Phase 99 preserve-fork-expression guidance (D-08).
    - The Phase 95 endpoint-policy carve-out cross-references ADR-98 (it is the #1225-restructured surface).
    - The `tls_intercept/` absence is confirmed with `ls`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q '## Carve-out Re-touch Check' .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md && grep -q 'CR-02' .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md && grep -q 'CR-01' .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md && grep -qi 'no re-touch in window\|expected conflict' .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md && grep -q 'endpoint_policy\|CompiledEndpointPolicy' .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Wire ADR-98 cross-reference + final completeness sweep of the ledger</name>
  <files>.planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md</files>
  <read_first>
    - .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md (the full assembled ledger)
    - proj/ADR-98-network-intent-disposition.md (the recommendation to cite)
    - .planning/phases/98-upst11-divergence-audit/98-CONTEXT.md (D-06)
  </read_first>
  <action>
    Ensure the ledger cross-references the standalone ADR for #1225 rather than inlining its
    analysis (D-06): in the Headline, the relevant cluster body, and the Carve-out Re-touch Check
    (Phase 95 endpoint-policy item), add an explicit pointer to
    `proj/ADR-98-network-intent-disposition.md` and state the ADR's recommendation in one line.
    Run a final completeness sweep: confirm (a) every substantive SHA from the drift JSON is in
    exactly one per-commit table; (b) substantive + noise = total (matches
    `git log --oneline <from>..<to> | wc -l`); (c) no cluster carries a bare TBD; (d) all six
    carve-outs have explicit verdicts; (e) the won't-sync cluster carries the 0.66.1 floor cross-ref.
    Record the sweep results as a short "Completeness verification" closing note in the ledger.
    Edit the existing ledger file.
  </action>
  <acceptance_criteria>
    - The ledger cross-references `proj/ADR-98-network-intent-disposition.md` in at least the Headline and the Phase-95-endpoint-policy carve-out, stating the ADR's recommendation in one line, with no inline duplication of the ADR analysis.
    - A closing "Completeness verification" note asserts: every substantive SHA classified once; substantive + noise = total; no bare TBD; six carve-out verdicts present; 0.66.1 floor cross-ref present.
    - The five sweep assertions are internally consistent with the rest of the ledger.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'ADR-98' .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md && grep -qi 'completeness verification' .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md && ! grep -nE '\| *TBD *\|' .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md</automated>
  </verify>
</task>

</tasks>

<threat_model>
N/A — documentation/audit phase. This plan records git-log re-touch results and a cross-reference
into a markdown ledger; no code, attack surface, or data handling is introduced. The carve-outs
being checked ARE security-relevant fork divergences, but this plan only verifies they are flagged
for preservation in Phase 99 — it enforces nothing in code.
</threat_model>

<verification>
- `## Carve-out Re-touch Check` present with six explicit verdicts (no silence).
- Every HIT flagged expected-conflict-preserve-fork-expression with guard test + Phase 99 guidance.
- Ledger cross-references ADR-98 (no inlined #1225 analysis).
- Final completeness sweep recorded and consistent.
</verification>

<success_criteria>
- ROADMAP SC1/SC3/SC4 reinforced: the ledger is complete, every commit classified, carve-outs preserved, no bare TBD, audit is the sole deliverable (no cherry-picks initiated).
- D-06/D-07/D-08 all traceably satisfied.
</success_criteria>

<output>
Create `.planning/phases/98-upst11-divergence-audit/98-04-SUMMARY.md` when done.
</output>
