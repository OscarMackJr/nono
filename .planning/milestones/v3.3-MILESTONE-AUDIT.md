---
milestone: v3.3
milestone_name: UPST10 Upstream Sync (v0.64→v0.65.1) + First Real Release
audited: 2026-06-26
status: tech_debt
scores:
  requirements: 13/13
  phases: 4/4
  integration: 5/5
  flows: 1/1
gaps:
  requirements: []   # No unsatisfied requirements — FAIL gate NOT triggered
  integration: []    # No broken cross-phase handoffs
  flows: []          # Operator release E2E path complete (static); execution-dependent stages orchestrator-confirmed
nyquist:
  compliant_phases: [95, 97]
  partial_phases: [96]
  missing_phases: [94]
  overall: partial
tech_debt:
  - phase: 97-release-engineering
    items:
      - "WARNING-1 (RESOLVED 2026-06-26): 97-VERIFICATION.md body was stale vs its own resolved frontmatter (Status: human_needed, 'IS publishable', WR-02 Human-Verification section). Reconciled — body now reads Status: PASSED, artifacts table reflects publish=false, WR-02 section marked RESOLVED. Archived record is now self-consistent."
      - "WARNING-3 / operator blocker: nono-py (read-only sibling repo) maturin build fails — two RouteConfig initializers missing endpoint_policy field (nono-py/src/policy.rs:743, nono-py/src/proxy.rs:206). Documented in RELEASE-RUNBOOK.md known-blockers. Out-of-milestone-scope (release = PREPARE-ONLY) but MUST be fixed by operator before the PyPI publish leg."
      - "WR-01 (latent): release-dry-run.ps1:88 PRE_PUBLISH_REGISTRY_BLOCKED classification applies to all crates incl. always-runnable nono — a genuine nono dep failure containing 'failed to select a version' would be silently classified as expected. Did NOT manifest."
      - "IN-01/02/03: release-dry-run.ps1 cosmetic ($NoonoPyDir three-o typo; no Test-Path guard before Push-Location on sibling dirs); release-readiness.ps1:196 private-path regexes require trailing slash."
      - "IN-04 (pre-existing, out-of-scope): release.yml publish-crates uses --allow-dirty on all three cargo publish steps — weakens tag→artifact provenance."
  - phase: 95-upstream-absorb
    items:
      - "WARNING-2: Cluster B absorbed audit-event types (SandboxRuntimeAuditEvent / CommandPolicyAuditEvent + record_* recorders, audit.rs:585,591) are exported-not-consumed by design — their tool-sandbox/ consumer was deliberately SKIPPED in the P95 split. Carries #[cfg_attr(not(linux), allow(dead_code))] which CLAUDE.md discourages. Absorbed surface ahead of its consumer, not a regression."
      - "keystore.rs ~862/~870: is_cmd_uri / validate_cmd_uri have zero callers (inline starts_with used instead) — Cluster B upstream inheritance. Non-blocking."
      - "resl_nix_async_signal_safety.rs:163-170: WR-01 guard test's comment-stripper truncates at first // including inside string literals — weaker than the stated mandate. Harden in follow-up."
  - phase: 96-cross-target-toolchain
    items:
      - "96-VALIDATION.md is status: draft / nyquist_compliant: false / wave_0_complete: false — Nyquist PARTIAL. Phase is a tooling+docs phase; both gates were independently re-run to exit 0 by the verifier, so functional coverage is real even though the formal validation contract is unfinished."
  - phase: 94-upst10-divergence-audit
    items:
      - "No 94-VALIDATION.md — Nyquist MISSING. Docs/audit phase: no runnable code produced, behavioral spot-checks correctly SKIPPED. Validation is structurally N/A but the formal artifact is absent."
---

# Milestone v3.3 — Audit Report

**Milestone:** UPST10 Upstream Sync (v0.64→v0.65.1) + First Real Release
**Phases:** 94, 95, 96, 97 (all complete)
**Audited:** 2026-06-26
**Status:** `tech_debt` — all requirements satisfied, cross-phase integration fully wired, no blockers; accumulated deferred/operator items warrant review before archive.

---

## Verdict

The four-phase dependency chain holds end-to-end. Every will-sync cluster the Phase 94 ledger routed is absorbed in Phase 95; the two fork invariants Phase 95 silently dropped (SEC-01 AF_UNIX seccomp filter, cgroup v2 module) were caught by Phase 96's newly stood-up **local** linux-gnu clippy gate and are restored **and invoked** (not stubbed); Phase 96 genuinely retires the chronic PARTIAL→CI default that closed Phase 95's only open item; and Phase 97's version bump + publish-set are internally consistent across `release.yml`, the dry-run orchestrator, and the runbook. **No unsatisfied requirements. No broken handoffs. No critical blockers.**

Classified `tech_debt` (not `passed`) because three documented items deserve operator attention before the milestone is sealed: a stale Phase-97 verification body, the out-of-scope nono-py PyPI blocker, and a handful of low-severity script/anti-pattern items. None block the prepare-only release.

---

## Requirements Coverage (13/13 satisfied)

> **Note:** `REQUIREMENTS.md` was intentionally `git rm`'d for the v3.3 milestone (commit `1c53eb62`). Requirement status is sourced from each phase's `VERIFICATION.md` requirements table + `SUMMARY.md` frontmatter, cross-referenced with the integration checker's per-requirement map.

| REQ-ID | Phase | Description | Status | Evidence |
|--------|-------|-------------|--------|----------|
| UPST10-01 | 94 | DIVERGENCE-LEDGER for nolabs-ai/nono v0.64.0..v0.65.1, every commit classified, windows-touch flag, per-cell ADR verdict | **satisfied** | 94-DIVERGENCE-LEDGER.md (562 lines); 8 substantive + 9 noise = 17 (live-verified); no bare TBD |
| UPST10-02 | 95 | All will-sync clusters absorbed without regressing Windows security model | **satisfied** | Clusters A/B/C cherry-picked w/ upstream-SHA trailer + DCO (`ae77d198`, `91d526e6`, `62dbf013`); exec_strategy_windows/ byte-unchanged |
| UPST10-03 | 95 | Fork-divergent invariants explicitly preserved & verified post-sync | **satisfied** | WR-01/02/03 + CR-01 restored & wired; ADR-86 audit boundary intact (records_verified: event_count > 0) |
| UPST10-04 | 94 | Upstream relocation recorded — git remote + PROJECT.md → nolabs-ai/nono; Future Cycles stub | **satisfied** | `git remote get-url upstream` = nolabs-ai/nono.git; upstream-legacy retained; PROJECT.md updated |
| XTGT-01 | 96 | Local cross C toolchain installed + documented (reproducible) | **satisfied** | cross 0.2.5 + Docker; zig 0.16.0 + cargo-zigbuild 0.23.0; setup in cross-target-verify-checklist.md |
| XTGT-02 | 96 | linux-gnu clippy passes locally; cfg-gated drift fixed | **satisfied** | Verifier re-ran gate → exit 0; SEC-01 filter + cgroup v2 restored & invoked (commit `1a804977`) |
| XTGT-03 | 96 | apple-darwin gate passes locally OR documented hard-blocker | **satisfied** | Verifier re-ran cargo-zigbuild clippy → exit 0 (branch a, SDKROOT unset) |
| XTGT-04 | 96 | CLAUDE.md + checklist updated; PARTIAL→CI default retired per-gate | **satisfied** | CLAUDE.md:140 one-line pointer (both commands + "cargo check NOT a substitute" mandate preserved) |
| RLS-05 | 97 | All crates + bindings leapfrogged to ≥0.65.0; Cargo.lock regenerated; builds clean | **satisfied** | All 6 crates at 0.66.0; no stale 0.62.2 pins; Cargo.lock 7× 0.66.0; build green |
| RLS-06 | 97 | Pipeline builds + signs all artifacts; sign-before-harvest preserved; MSI pair produced | **satisfied** | release.yml sign (L167) → package (L186) → admin-extract gate (L281); MSI pair produced locally |
| RLS-07 | 97 | Dry-run publish validated across crates.io, PyPI, npm; gated GREEN | **satisfied (with sanctioned carry-forward)** | crates.io + npm PASS; publish-set consistent (3 crates); **PyPI maturin FAIL = out-of-scope nono-py operator blocker** (see tech debt) |
| RLS-08 | 97 | release.yml no 0s startup_failure; all build legs green | **satisfied** | No reusable-workflow-call job; 5 build matrix legs present; YAML valid |
| RLS-09 | 97 | One-step-push ready with operator runbook + green readiness gate | **satisfied** | RELEASE-RUNBOOK.md complete; release-readiness gate PASS (exit 0) |

**FAIL gate:** NOT triggered — zero `unsatisfied` requirements. No orphaned requirements (every REQ-ID appears in a phase VERIFICATION.md table).

---

## Phase Verification Summary (4/4)

| Phase | VERIFICATION status | Score | Notes |
|-------|--------------------|-------|-------|
| 94 — UPST10 Divergence Audit | passed | 10/10 truths | Docs/audit phase; no runnable code |
| 95 — Upstream Absorb + Fork-Invariant Verify | passed* | 11/11 must-haves | Frontmatter `human_needed` for cross-target CI confirmation — **resolved by Phase 96** (local gates re-run exit 0) |
| 96 — Cross-Target Toolchain | passed | 4/4 criteria | Both gates independently re-run by verifier → exit 0; caught + restored 2 more dropped fork invariants |
| 97 — Release Engineering | passed | 9/9 truths | Frontmatter resolved (WR-02 publish-set → 3-crate set); body text stale (WARNING-1) |

\* Phase 95's only outstanding item (PARTIAL→CI cross-target clippy) was structurally closed by Phase 96, which stood up the local toolchain and re-ran both gates to exit 0. This is the milestone's headline cross-phase resolution.

---

## Cross-Phase Integration (5/5 wired)

| # | Handoff | Status |
|---|---------|--------|
| 1 | P94 ledger clusters → P95 absorb (cherry-pick trailers + DCO; no open will-sync row) | **WIRED** |
| 2 | P95 dropped-invariant chain → P96 catch (SEC-01 filter + cgroup restored AND invoked, not stubs) | **WIRED** |
| 3 | P95 human_needed cross-target → P96 resolution (PARTIAL→CI default retired in CLAUDE.md + checklist) | **WIRED** |
| 4 | P97 version-bump consistency (6 crates + Cargo.lock at 0.66.0; no stale pins) | **WIRED** |
| 5 | P97 publish-set (release.yml = dry-run = runbook = 3 crates; nono-shell-broker publish=false) | **WIRED / RESOLVED** |

**E2E operator release path** (prepared workspace → dry-run GREEN → readiness gate PASS → one operator push): complete statically. The two execution-dependent stages (dry-run, readiness gate) rest on orchestrator-reported execution; the script/gate files exist with correct fail-closed contracts and passing static preconditions.

---

## Nyquist Compliance (overall: partial)

| Phase | VALIDATION.md | nyquist_compliant | Classification | Action |
|-------|---------------|-------------------|----------------|--------|
| 94 | absent | — | **MISSING** | Docs/audit phase — no runnable code; behavioral checks correctly N/A. Formal artifact absent. `/gsd:validate-phase 94` optional (likely no-op). |
| 95 | present | true | **COMPLIANT** | — |
| 96 | present | false (draft) | **PARTIAL** | Tooling phase; both gates re-run to exit 0 (real functional coverage). Validation contract unfinished. `/gsd:validate-phase 96` to formalize. |
| 97 | present | true | **COMPLIANT** | — |

Discovery only — no `/gsd:validate-phase` auto-invoked. Phases 94 (MISSING) and 96 (PARTIAL) are tooling/docs phases where the validation gap is largely formal, not a coverage hole.

---

## Tech Debt Summary

**Phase 97 (Release Engineering):**
- **WARNING-1 (recommend fixing before archive):** `97-VERIFICATION.md` body contradicts its own resolved frontmatter — still reads `Status: human_needed`, claims nono-shell-broker "IS publishable", retains the WR-02 Human-Verification section. The *code* is correct and consistent; this is doc staleness that would mislead an operator reading the body at push time.
- **WARNING-3 / operator blocker:** nono-py (read-only sibling) `maturin build` fails on missing `endpoint_policy` in two `RouteConfig` initializers (`policy.rs:743`, `proxy.rs:206`). Documented in RELEASE-RUNBOOK.md known-blockers. Out-of-milestone-scope (release = PREPARE-ONLY) but **must be fixed by the operator before the PyPI leg**.
- WR-01 latent classification over-broad in `release-dry-run.ps1:88`; IN-01/02/03 cosmetic script issues; IN-04 pre-existing `--allow-dirty` in release.yml publish-crates.

**Phase 95 (Upstream Absorb):**
- **WARNING-2:** Cluster B audit-event types exported-not-consumed *by design* (tool-sandbox/ consumer deliberately skipped in the split); uses `allow(dead_code)` CLAUDE.md discourages.
- `keystore.rs` unused `is_cmd_uri`/`validate_cmd_uri` (Cluster B upstream inheritance); WR-01 guard-test comment-stripper weakness.

**Phase 96 / 94 (tooling/docs):** Nyquist artifacts incomplete (96 draft) / absent (94) — see table above.

---

## Recommendation

The milestone is structurally complete and the prepare-only release is genuinely one operator push away. Before `/gsd:complete-milestone v3.3`:

1. **WARNING-1 — RESOLVED (2026-06-26):** the `97-VERIFICATION.md` body was reconciled to its resolved frontmatter; the archived record is now self-consistent.
2. **(Operator, out of scope) Track WARNING-3** — the nono-py `endpoint_policy` fix is a prerequisite for the PyPI publish leg, already captured in RELEASE-RUNBOOK.md.

No remaining item blocks completion. All other debt is low-severity, by-design, or operator/host-gated. Proceed to `/gsd:complete-milestone v3.3` (accepting tracked tech debt).

---

_Audited: 2026-06-26 · Auditor: Claude (gsd-audit-milestone orchestrator) · Integration check: gsd-integration-checker (5/5 wired, 0 blockers)_
