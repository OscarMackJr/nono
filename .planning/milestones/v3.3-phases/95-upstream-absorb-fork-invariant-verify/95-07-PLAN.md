---
phase: 95-upstream-absorb-fork-invariant-verify
plan: 07
type: execute
wave: 6
depends_on:
  - 95-05
  - 95-06
files_modified: []
autonomous: true
requirements:
  - UPST10-02
  - UPST10-03
gap_closure: true

must_haves:
  truths:
    - "Cross-target clippy x86_64-unknown-linux-gnu MUST pass locally (Linux toolchain was stood up in Phase 94; gap-closure changes touch linux.rs cfg-gated code — the exact Windows blind-spot that let WR-02/WR-03 slip); x86_64-apple-darwin local PASS preferred, PARTIAL→CI fallback allowed only if osxcross toolchain is provably absent (executor must record the absence proof per cross-target-verify-checklist.md)"
    - "Fork-divergent invariants are explicitly preserved — the WR-04 arch-portable msghdr offset derivation, GPU enforcement, CR-01 fork-safety, and endpoint_policy wiring are all confirmed present by static grep on the final HEAD"
  artifacts:
    - path: ".planning/phases/95-upstream-absorb-fork-invariant-verify/95-VERIFICATION.md"
      provides: "Updated gap-closure status for WR-02/WR-03/WR-01/CR-01"
      contains: "gaps_closed"
  key_links:
    - from: "cross-target clippy gate"
      to: "crates/nono/src/sandbox/linux.rs"
      via: "cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used"
      pattern: "Compiling nono"
    - from: "cross-target clippy gate"
      to: "crates/nono-cli/src/exec_strategy.rs"
      via: "cargo clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used"
      pattern: "Compiling nono-cli"
---

<objective>
Run the [BLOCKING] cross-target clippy verification gate against the gap-closure HEAD
(after 95-05 and 95-06 complete). Plans 95-05 and 95-06 touch cfg-gated Unix code:
linux.rs (#[cfg(target_os = "linux")]) and exec_strategy.rs (post-fork Unix path). Per
CLAUDE.md § Cross-target clippy verification: any commit touching cfg-gated Unix code MUST
be verified via cargo clippy --workspace --target x86_64-unknown-linux-gnu and
--target x86_64-apple-darwin. A local cross-target toolchain was stood up in Phase 94 and
must be used here unless proven unavailable.

The gap-closure context overrides Phase 95 D-03 (which deferred cross-target to Phase 96)
for these specific gap-closure changes. The regressions being fixed are on Linux-only code
paths; the cross-target gate is the only way to confirm the restored code compiles cleanly
on the target platforms.

Purpose: Confirm the gap-closure changes (WR-02/WR-03 linux.rs restorations, WR-01
exec_strategy.rs static-byte fix) produce no new clippy warnings or errors on Linux and
macOS targets. If the local toolchain is unavailable, record PARTIAL→CI per
.planning/templates/cross-target-verify-checklist.md with per-target status.

Output: Cross-target clippy gate status recorded in this plan's SUMMARY. If PARTIAL→CI,
the CI lanes on the phase-95 gap-closure commit are the decisive signal.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-upstream-absorb-fork-invariant-verify/95-CONTEXT.md
@.planning/phases/95-upstream-absorb-fork-invariant-verify/95-VERIFICATION.md
@.planning/templates/cross-target-verify-checklist.md

<interfaces>
Files changed by Plans 95-05 and 95-06 that require cross-target verification:

1. crates/nono/src/sandbox/linux.rs — #[cfg(target_os = "linux")] gated throughout.
   Changes: WR-04 msghdr offset derivation (offset_of!, size_of, compile-time assertions),
   #[must_use] attributes, is_nvidia_compute_device, collect_linux_gpu_paths, caps.gpu() branch.
   Must compile clean on x86_64-unknown-linux-gnu. Also present on apple-darwin (cfg-excluded,
   but clippy still parses the file for non-cfg-gated items).

2. crates/nono-cli/src/exec_strategy.rs — post-fork Unix code, not cfg-gated at file level
   but the changed sections are inside Unix-path code that runs only on non-Windows.
   Changes: const MSG_PROXY_WRITE_FAIL, MSG_PROXY_FILTER_FAIL replacing format!() calls.
   Must compile clean on both Linux and macOS targets.

3. crates/nono-proxy/src/reverse.rs — not cfg-gated. Changes: endpoint_policy.evaluate()
   wiring. Must compile clean on both targets (this is cross-platform code).

Cross-target toolchain check:
  rustup target list --installed | grep -E "x86_64-unknown-linux-gnu|x86_64-apple-darwin"
  If both targets are installed, proceed with local cross-target clippy.
  If either is missing, follow .planning/templates/cross-target-verify-checklist.md
  PARTIAL→CI protocol and record the status per-target.

Phase 94 stood up the local cross-target toolchain. Verify it is still present before
running cross-target clippy.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Cross-target clippy gate — Linux and macOS targets</name>
  <files></files>
  <read_first>
    1. Read .planning/templates/cross-target-verify-checklist.md — the PARTIAL→CI protocol.
    2. Run: rustup target list --installed | grep -E "x86_64-unknown-linux-gnu|x86_64-apple-darwin"
       to confirm the local toolchain is available (stood up in Phase 94).
    3. Confirm Plans 95-05 and 95-06 are complete (all 4 gap commits in git log) before running
       clippy: git log --oneline -10 and verify WR-02/WR-03/WR-01/CR-01 commits present.
  </read_first>
  <action>
    Step 1 — Check local cross-target toolchain availability:
      rustup target list --installed

    IF x86_64-unknown-linux-gnu IS installed:
      Run: cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used
      Record result: PASS (exit 0) or FAIL (exit non-zero with errors).

    IF x86_64-apple-darwin IS installed:
      Run: cargo clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used
      Record result: PASS (exit 0) or FAIL (exit non-zero with errors).

    IF x86_64-unknown-linux-gnu is NOT installed:
      STOP — do NOT proceed to PARTIAL→CI for the Linux target. The Phase 94 cross-target
      toolchain setup was specifically intended to prevent this. Diagnose why the target is
      absent (e.g. `rustup target add x86_64-unknown-linux-gnu`) and install it before
      continuing. Linux PARTIAL→CI is NOT acceptable for these gap-closure changes because
      linux.rs is the exact file that contained the WR-02/WR-03 regressions — the cross-target
      gate is the only way to confirm the restored cfg-gated code compiles cleanly.
      Record the toolchain-absent situation in the SUMMARY if truly unavoidable and escalate.

    IF x86_64-apple-darwin is NOT installed:
      Follow .planning/templates/cross-target-verify-checklist.md PARTIAL→CI protocol:
      - Record x86_64-apple-darwin as PARTIAL→CI with reason "osxcross toolchain not
        installed on Windows dev host"
      - The CI macOS Clippy lane on the gap-closure HEAD SHA is the decisive gate for macOS
      - Note this in the plan SUMMARY under "## PARTIAL Deferrals" with the HEAD SHA

    IF cross-target clippy produces errors:
      These are BLOCKING — fix the errors before marking this plan complete. The most likely
      sources are:
      - offset_of!(libc::msghdr, ...) not recognized on non-Linux: the macro is in std and
        should work on all targets, but if libc::msghdr is not defined on a target, the
        const will fail. Mitigation: gate the const defs under #[cfg(target_os = "linux")]
        if not already gated.
      - collect_linux_gpu_paths or is_nvidia_compute_device visibility error: both must be
        #[cfg(target_os = "linux")] — if the restore missed the cfg gate, add it.
      - MSG_PROXY_WRITE_FAIL or MSG_PROXY_FILTER_FAIL: these are inside cfg-gated Unix blocks;
        confirm the enclosing block has the cfg gate (it should, since the original format!()
        calls were in the same block).

    Step 2 — Run final full make ci on Windows host (native):
      make ci
    Confirm: only the pre-existing 5-red baseline failures (try_set_mandatory_label + 3 protected_paths + profile_cmd). Zero new failures.

    Step 3 — Produce a static grep verification of all 4 closed gaps:
      grep "core::mem::offset_of!(libc::msghdr, msg_name)" crates/nono/src/sandbox/linux.rs
      grep "fn collect_linux_gpu_paths" crates/nono/src/sandbox/linux.rs
      grep "caps\.gpu()" crates/nono/src/sandbox/linux.rs
      grep "MSG_PROXY_WRITE_FAIL\|MSG_PROXY_FILTER_FAIL" crates/nono-cli/src/exec_strategy.rs
      grep "endpoint_policy\.evaluate" crates/nono-proxy/src/reverse.rs
    All 5 greps must return at least one match. Record results in the SUMMARY.

    Step 4 — Create a gap-closure status commit:
    After all checks pass, produce a final DCO-signed commit noting gap closure:
      git add .planning/
      git commit -s -m "docs(phase-95): gap-closure complete — WR-02/WR-03/WR-01/CR-01 closed"
    The Signed-off-by trailer must be: "Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>"
  </action>
  <verify>
    <automated>grep "core::mem::offset_of!(libc::msghdr, msg_name)" crates/nono/src/sandbox/linux.rs && grep "fn collect_linux_gpu_paths" crates/nono/src/sandbox/linux.rs && grep "endpoint_policy\.evaluate" crates/nono-proxy/src/reverse.rs && grep "MSG_PROXY_WRITE_FAIL" crates/nono-cli/src/exec_strategy.rs</automated>
  </verify>
  <acceptance_criteria>
    - rustup target list --installed output checked; toolchain availability recorded in SUMMARY
    - Linux target (x86_64-unknown-linux-gnu) MUST pass locally: cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used exits 0
      (PARTIAL→CI is NOT an acceptable outcome for the Linux target; the Phase 94 toolchain must be used)
    - macOS target (x86_64-apple-darwin): cargo clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used exits 0 (preferred); PARTIAL→CI with absence proof is
      acceptable fallback if osxcross is provably not installed on the Windows dev host
    - make ci (native Windows) exits 0 with only the pre-existing 5-red baseline
    - grep "core::mem::offset_of!(libc::msghdr, msg_name)" crates/nono/src/sandbox/linux.rs returns a match
    - grep "fn collect_linux_gpu_paths" crates/nono/src/sandbox/linux.rs returns a match
    - grep "caps\.gpu()" crates/nono/src/sandbox/linux.rs returns a match
    - grep "MSG_PROXY_WRITE_FAIL" crates/nono-cli/src/exec_strategy.rs returns a match
    - grep "MSG_PROXY_FILTER_FAIL" crates/nono-cli/src/exec_strategy.rs returns a match
    - grep "endpoint_policy\.evaluate" crates/nono-proxy/src/reverse.rs returns a match
    - grep "const MSGHDR_MIN_READ: usize = 12" crates/nono/src/sandbox/linux.rs returns NO MATCH (hardcoded value gone)
    - git log --format="%B" HEAD | grep "Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>" returns a match on the gap-closure commit
  </acceptance_criteria>
  <done>Cross-target clippy gate passed (or PARTIAL→CI recorded); all 4 gap static-grep probes return matches; make ci green; gap-closure commit DCO-signed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Windows dev host → Linux/macOS target compilation | cfg-gated Unix code invisible to Windows clippy; cross-target clippy is the gate |
| gap-closure commits → CI | PARTIAL→CI records which targets need CI confirmation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-07-01 | Tampering | cross-target clippy skip (Windows blind-spot exemplar from Phase 87) | mitigate | BLOCKING cross-target gate runs before SUMMARY is written; toolchain absence triggers PARTIAL→CI (not skip) |
| T-95-07-02 | Repudiation | Gap status claimed closed without verification | mitigate | Static grep verification of all 5 identifiers performed in this task; results recorded verbatim in SUMMARY |
| T-95-07-SC | Tampering | No npm/pip/cargo new package installs | accept | No new packages; slopcheck not required |
</threat_model>

<verification>
Final state after Plan 95-07:
- All 4 gap truths from 95-VERIFICATION.md now VERIFIED:
  WR-02: grep "offset_of!(libc::msghdr" linux.rs → match
  WR-03: grep "collect_linux_gpu_paths" linux.rs → match
  WR-01: grep "MSG_PROXY_WRITE_FAIL" exec_strategy.rs → match
  CR-01: grep "endpoint_policy\.evaluate" reverse.rs → match
- Cross-target clippy: PASS locally or PARTIAL→CI if toolchain absent
- make ci: exits 0 (only pre-existing 5-red baseline)
- All gap-closure commits DCO-signed
</verification>

<success_criteria>
- Cross-target clippy gate: Linux target PASS (local, required); macOS target PASS (preferred) or PARTIAL→CI with absence proof (fallback)
- All 5 static grep probes return matches (4 gaps closed + hardcoded 12 gone)
- make ci exits 0 on native Windows
- Phase 95 gap-closure complete: 10/10 must-haves achievable after these plans execute
</success_criteria>

<output>
Create .planning/phases/95-upstream-absorb-fork-invariant-verify/95-07-SUMMARY.md when done.

Include a "## Gap Closure Status" section recording the final state of all 4 gaps:
  WR-02: [CLOSED | PARTIAL→CI] — static grep result
  WR-03: [CLOSED | PARTIAL→CI] — static grep result
  WR-01: [CLOSED | PARTIAL→CI] — static grep result
  CR-01: [CLOSED | PARTIAL→CI] — static grep result

Include a "## Cross-Target Clippy" section recording:
  x86_64-unknown-linux-gnu: [PASS | PARTIAL→CI (toolchain absent)]
  x86_64-apple-darwin:      [PASS | PARTIAL→CI (toolchain absent)]
</output>
