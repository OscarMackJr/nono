---
phase: 95-upstream-absorb-fork-invariant-verify
plan: 04
type: execute
wave: 3
depends_on:
  - 95-03
files_modified:
  - .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md
  - .planning/phases/95-upstream-absorb-fork-invariant-verify/95-VERIFICATION.md
autonomous: true
requirements:
  - UPST10-02
  - UPST10-03

must_haves:
  truths:
    - "exec_strategy_windows/ is byte-for-byte unchanged relative to Phase 95 start (git diff HEAD~N for N absorb commits is empty for this path)"
    - "ADR-86 boundary: records_verified: event_count > 0 is in crates/nono/src/audit.rs; DiagnosticFormatter is in crates/nono-cli/src/diagnostic/; bindings/c/src/ is untouched"
    - "ADR-86 D-03 carve-out: exec_strategy_windows/launch.rs and network.rs are unchanged; diagnostic_code() and remediation() methods exist in error.rs"
    - "CR-02 guard test verify_empty_log_with_no_stored_metadata_is_not_valid passes"
    - "Cluster F proxy guard test proxy_activates_with_custom_credentials_only passes"
    - "SC4 security note documents Cluster A AF_UNIX fix Windows-equivalent intact"
    - "94-DIVERGENCE-LEDGER.md will-sync rows (Cluster A) and split rows (Clusters B, C) are all marked absorbed"
    - "PARTIAL→96 deferral record lists all commits with cfg-gated Unix blocks for Phase 96"
  artifacts:
    - path: ".planning/phases/95-upstream-absorb-fork-invariant-verify/95-VERIFICATION.md"
      provides: "Fork-invariant checklist (3 invariants, none regressed) + SC4 security note + PARTIAL→96 record"
      contains: "Invariant 1"
    - path: ".planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md"
      provides: "Will-sync rows marked absorbed; no open will-sync entries remain"
      contains: "absorbed"
  key_links:
    - from: "95-VERIFICATION.md Invariant 1"
      to: "git diff HEAD~N -- crates/nono-cli/src/exec_strategy_windows/"
      via: "must be empty"
      pattern: "exec_strategy_windows"
    - from: "95-VERIFICATION.md Invariant 2"
      to: "crates/nono/src/audit.rs records_verified line"
      via: "grep records_verified | grep event_count"
      pattern: "event_count > 0"
    - from: "DIVERGENCE-LEDGER will-sync rows"
      to: "absorbed status"
      via: "each Cluster A row updated"
      pattern: "absorbed"
---

<objective>
Produce the Phase 95 fork-invariant verification checklist and close out all bookkeeping:
update the DIVERGENCE-LEDGER to mark will-sync and split rows as absorbed, write the
VERIFICATION.md with one checklist entry per invariant (3 invariants — none regressed),
document the SC4 security note for Cluster A, and record the PARTIAL→96 deferral for
cross-target clippy.

Purpose: UPST10-03 requires that fork-divergent invariants are EXPLICITLY verified
post-sync — not just implicitly preserved. A human verifier (and Phase 96 planning) needs
a single document that shows the verification evidence, the PARTIAL→CI deferrals, and
the security notes per invariant. This plan produces that document and closes the ledger.

Output: 95-VERIFICATION.md (fork-invariant checklist + SC4 note + PARTIAL→96 record);
updated 94-DIVERGENCE-LEDGER.md (will-sync/split rows marked absorbed); final make ci
confirmation.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-upstream-absorb-fork-invariant-verify/95-CONTEXT.md
@.planning/phases/95-upstream-absorb-fork-invariant-verify/95-RESEARCH.md
@.planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md
@.planning/phases/95-upstream-absorb-fork-invariant-verify/95-01-SUMMARY.md
@.planning/phases/95-upstream-absorb-fork-invariant-verify/95-02-SUMMARY.md
@.planning/phases/95-upstream-absorb-fork-invariant-verify/95-03-SUMMARY.md
@proj/ADR-86-library-boundary-convergence.md
@proj/ADR-87-cr02-audit-bypass.md
@.planning/templates/cross-target-verify-checklist.md

<interfaces>
<!-- Invariant verification methods — each verified by running a command; record actual output. -->

Invariant 1 verification (AppContainer/WFP/Broker Windows Backend):
  git diff HEAD~N -- crates/nono-cli/src/exec_strategy_windows/
  where N = number of absorb commits (should be 3: Cluster A + Cluster B + Cluster C)
  Expected: empty output (no diff)
  Also check: git diff HEAD~N -- crates/nono/src/sandbox/windows.rs
  Expected: empty output

Invariant 2 verification (ADR-86 audit/diagnostics library-boundary carve-out):
  grep -n "records_verified" crates/nono/src/audit.rs | grep "event_count"
  Expected: line ~1435: records_verified: event_count > 0

  cargo test -p nono --lib -- audit::tests::verify_empty_log_with_no_stored_metadata_is_not_valid
  Expected: test ... ok

  git diff HEAD~N -- bindings/c/src/
  Expected: empty output (CR-01 clean)

  ls crates/nono-cli/src/diagnostic/
  Expected: formatter.rs present (DiagnosticFormatter stays CLI-side)

Invariant 3 verification (exec_strategy_windows/ denial-rendering fork ADR-86 D-03 carve-out):
  git diff HEAD~N -- crates/nono-cli/src/exec_strategy_windows/launch.rs
  Expected: empty output

  git diff HEAD~N -- crates/nono-cli/src/exec_strategy_windows/network.rs
  Expected: empty output

  grep -n "diagnostic_code\|remediation" crates/nono/src/error.rs | head -5
  Expected: method implementations present (Phase 86 output)

SC4 security note (Cluster A AF_UNIX fix):
  Cluster A 9ce74e92 fixes 4 bugs in AF_UNIX pathname mediation. The Windows equivalent
  path (exec_strategy_windows/) is unaffected (AF_UNIX mediation is Linux-only).
  Verification: exec_strategy_windows/ git diff is empty (same as Invariant 1/3).
  Note: socket_access_run.rs tests are Linux cfg-gated — they don't run on Windows but
  must not cause compile errors: make build exits 0 (already confirmed by make ci).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run fork-invariant verification commands and produce 95-VERIFICATION.md</name>
  <files>.planning/phases/95-upstream-absorb-fork-invariant-verify/95-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/95-upstream-absorb-fork-invariant-verify/95-RESEARCH.md § "Fork-Invariant Verification Checklist (UPST10-03)" — all three invariant verification methods + SC4 security note
    - proj/ADR-86-library-boundary-convergence.md — invariant boundary definitions
    - proj/ADR-87-cr02-audit-bypass.md — CR-02 expression definition
    - .planning/templates/cross-target-verify-checklist.md § "PARTIAL Disposition" — exact PARTIAL prose template
    - .planning/phases/95-upstream-absorb-fork-invariant-verify/95-01-SUMMARY.md, 95-02-SUMMARY.md, 95-03-SUMMARY.md — confirm absorb commit count (N) for git diff HEAD~N commands
  </read_first>
  <action>
    Determine N (the number of absorb commits on top of the baseline commit):
      git log --oneline HEAD~5..HEAD

    Expected: 3 absorb commits (Cluster A + Cluster B + Cluster C) + 1 baseline commit = N=3
    (the baseline capture commit is HEAD~3, the absorb commits are HEAD~2, HEAD~1, HEAD).

    Run each verification command and record the ACTUAL output:

    INVARIANT 1 — AppContainer/WFP/Broker Windows Backend:
      git diff HEAD~3 -- crates/nono-cli/src/exec_strategy_windows/ > /tmp/inv1-exec-windows.txt
      git diff HEAD~3 -- crates/nono/src/sandbox/windows.rs > /tmp/inv1-sandbox-windows.txt
      cat /tmp/inv1-exec-windows.txt   # must be empty
      cat /tmp/inv1-sandbox-windows.txt  # must be empty

    INVARIANT 2 — ADR-86 Library-Boundary Carve-Out:
      grep -n "records_verified" crates/nono/src/audit.rs | grep "event_count" > /tmp/inv2-cr02.txt
      cat /tmp/inv2-cr02.txt  # must show: records_verified: event_count > 0
      cargo test -p nono --lib -- audit::tests::verify_empty_log_with_no_stored_metadata_is_not_valid 2>&1 > /tmp/inv2-cr02-test.txt
      cat /tmp/inv2-cr02-test.txt  # must show: test ... ok
      git diff HEAD~3 -- bindings/c/src/ > /tmp/inv2-cr01.txt
      cat /tmp/inv2-cr01.txt  # must be empty (CR-01 clean)
      ls crates/nono-cli/src/diagnostic/ > /tmp/inv2-diagnostic.txt
      cat /tmp/inv2-diagnostic.txt  # must include formatter.rs

    INVARIANT 3 — exec_strategy_windows/ Denial-Rendering Fork:
      git diff HEAD~3 -- crates/nono-cli/src/exec_strategy_windows/launch.rs > /tmp/inv3-launch.txt
      git diff HEAD~3 -- crates/nono-cli/src/exec_strategy_windows/network.rs > /tmp/inv3-network.txt
      cat /tmp/inv3-launch.txt  # must be empty
      cat /tmp/inv3-network.txt  # must be empty
      grep -n "diagnostic_code\|remediation" crates/nono/src/error.rs | head -5 > /tmp/inv3-error-methods.txt
      cat /tmp/inv3-error-methods.txt  # must show method implementations

    SC4 — Security Note (Cluster F proxy guard):
      cargo test -p nono-cli --lib -- proxy_runtime::tests::proxy_activates_with_custom_credentials_only 2>&1 > /tmp/sc4-proxy-guard.txt
      cat /tmp/sc4-proxy-guard.txt  # must show: test ... ok

    Write .planning/phases/95-upstream-absorb-fork-invariant-verify/95-VERIFICATION.md using
    the template below, filling in ACTUAL command outputs (not expected values):

    ---
    # Phase 95 Fork-Invariant Verification

    **Date:** 2026-06-26
    **Post-sync commit:** [git log --oneline HEAD | head -1]
    **Baseline commit:** [git log --oneline HEAD~3 | head -1] (phase-base before cherry-picks)

    ## Invariant Checklist (UPST10-03)

    ### Invariant 1: AppContainer/WFP/Broker Windows Backend

    **Status:** [PASS / FAIL]
    **Files checked:** crates/nono-cli/src/exec_strategy_windows/ (all), crates/nono/src/sandbox/windows.rs
    **Verification:**
    ```
    git diff HEAD~3 -- crates/nono-cli/src/exec_strategy_windows/
    [ACTUAL OUTPUT — must be empty]

    git diff HEAD~3 -- crates/nono/src/sandbox/windows.rs
    [ACTUAL OUTPUT — must be empty]
    ```
    **Evidence:** [All cherry-picks are windows-touch: no per the Phase 94 ledger; diff confirms zero modifications to Windows backend files.]

    ### Invariant 2: ADR-86 Audit/Diagnostics Library-Boundary Carve-Out

    **Status:** [PASS / FAIL]
    **Files checked:** crates/nono/src/audit.rs (CR-02), bindings/c/src/ (CR-01), crates/nono-cli/src/diagnostic/ (UX stays CLI-side)
    **Verification:**
    ```
    grep -n "records_verified" crates/nono/src/audit.rs | grep "event_count"
    [ACTUAL OUTPUT — must show records_verified: event_count > 0]

    cargo test -p nono --lib -- audit::tests::verify_empty_log_with_no_stored_metadata_is_not_valid
    [ACTUAL OUTPUT — must show test ... ok]

    git diff HEAD~3 -- bindings/c/src/
    [ACTUAL OUTPUT — must be empty]
    ```
    **Evidence:** [Cluster B audit.rs additions are additive structs that do not touch the verify_audit_log return block. ADR-87 CR-02 guard test passes.]

    ### Invariant 3: exec_strategy_windows/ Denial-Rendering Fork (ADR-86 D-03 Carve-Out)

    **Status:** [PASS / FAIL]
    **Files checked:** crates/nono-cli/src/exec_strategy_windows/launch.rs, network.rs; crates/nono/src/error.rs (diagnostic_code/remediation bridge)
    **Verification:**
    ```
    git diff HEAD~3 -- crates/nono-cli/src/exec_strategy_windows/launch.rs
    [ACTUAL OUTPUT — must be empty]

    git diff HEAD~3 -- crates/nono-cli/src/exec_strategy_windows/network.rs
    [ACTUAL OUTPUT — must be empty]

    grep -n "diagnostic_code\|remediation" crates/nono/src/error.rs | head -5
    [ACTUAL OUTPUT — must show method implementations]
    ```
    **Evidence:** [No absorb commit in Phase 95 touches exec_strategy_windows/. ADR-86 D-03 carve-out is at the same location as when Phase 86 established it.]

    ## SC4: Security-Relevant Will-Sync Commit Notes

    ### Cluster A (9ce74e92) — AF_UNIX Mediation Deadlock Fix

    **Security relevance:** Fixes 4 bugs in AF_UNIX pathname mediation (deadlock, wrong jt offsets,
    rate-limiter starvation, dup2 bypass). BPF filter now installed AFTER IPC handshake; pure
    allowlist with no fd-based holes.

    **Windows equivalents intact?** YES — AF_UNIX mediation is Linux-only (uses Landlock + seccomp
    BPF). The fork's Windows path (exec_strategy_windows/) is NOT touched by this commit
    (windows-touch: no per Phase 94 ledger; Invariant 1 diff confirms empty).

    **Verification of Windows-equivalent preservation:**
    ```
    git diff HEAD~3 -- crates/nono-cli/src/exec_strategy_windows/
    [ACTUAL OUTPUT — empty confirms Windows path untouched]
    ```

    **Proxy Cluster F guard (Phase 89 D-02 divergence sentinel):**
    ```
    cargo test -p nono-cli --lib -- proxy_runtime::tests::proxy_activates_with_custom_credentials_only
    [ACTUAL OUTPUT — must show test ... ok]
    ```

    ## PARTIAL → Phase 96 Deferral Record

    Cross-target clippy gate SKIPPED on Windows dev host due to missing toolchain
    (x86_64-unknown-linux-gnu, x86_64-apple-darwin). The following commits touch
    cfg-gated Unix code blocks requiring Phase 96 cross-target verification:

    | Commit | Files with #[cfg(unix/linux/macos)] blocks | Phase 96 verification required |
    |--------|--------------------------------------------|-------------------------------|
    | 9ce74e92 (Cluster A) | crates/nono/src/sandbox/linux.rs, crates/nono-cli/src/exec_strategy/supervisor_linux.rs | cargo clippy --workspace --target x86_64-unknown-linux-gnu AND --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used |
    | Cluster B applied hunks | crates/nono/src/sandbox/linux.rs, crates/nono/src/sandbox/mod.rs | Same cross-target gates |

    Per .planning/templates/cross-target-verify-checklist.md PARTIAL Disposition:
    The live GH Actions Linux Clippy / macOS Clippy lanes on the head SHA are the decisive
    signals. UPST10-02 marked PARTIAL pending Phase 96 XTGT verification.

    Carry-forward PARTIAL→CI items from earlier phases (NOT resolved by Phase 95):
    - SEC-01/SEC-02 AF_UNIX guards (v3.1 — Phase 87)
    - ZTL-04 AWS_* strip (v3.2 — Phase 92/93)
    These remain Phase 96's resolution vehicle.
    ---

    If any invariant check FAILS (non-empty diff where empty is expected, or test fails):
    STOP. Do not mark Phase 95 complete. Report the specific failure to the operator.
    The failure means a cherry-pick unintentionally modified a protected path and must be
    fixed (via git revert + targeted re-apply) before proceeding.
  </action>
  <verify>
    <automated>cargo test -p nono --lib -- audit::tests::verify_empty_log_with_no_stored_metadata_is_not_valid && cargo test -p nono-cli --lib -- proxy_runtime::tests::proxy_activates_with_custom_credentials_only</automated>
  </verify>
  <acceptance_criteria>
    - 95-VERIFICATION.md exists under .planning/phases/95-upstream-absorb-fork-invariant-verify/
    - All 3 invariants marked PASS in the document (actual command outputs recorded, not placeholders)
    - git diff HEAD~3 -- crates/nono-cli/src/exec_strategy_windows/ is empty (Invariant 1 pass)
    - git diff HEAD~3 -- bindings/c/src/ is empty (CR-01 clean)
    - grep "records_verified" crates/nono/src/audit.rs shows "event_count > 0" (Invariant 2 CR-02 pass)
    - cargo test -p nono --lib -- audit::tests::verify_empty_log_with_no_stored_metadata_is_not_valid exits 0 (Invariant 2 guard test pass)
    - git diff HEAD~3 -- crates/nono-cli/src/exec_strategy_windows/launch.rs is empty (Invariant 3 pass)
    - git diff HEAD~3 -- crates/nono-cli/src/exec_strategy_windows/network.rs is empty (Invariant 3 pass)
    - SC4 section present with Cluster A Windows-equivalent note
    - PARTIAL→96 deferral table lists 9ce74e92 and the Cluster B applied hunks
    - No invariant is marked REGRESSED or FAIL
  </acceptance_criteria>
  <done>95-VERIFICATION.md produced with all 3 invariants explicitly verified (none regressed) + SC4 + PARTIAL→96 record.</done>
</task>

<task type="auto">
  <name>Task 2: Update DIVERGENCE-LEDGER absorbed status and commit Phase 95 closeout</name>
  <files>.planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md</files>
  <read_first>
    - .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md — current state of the Cluster A/B/C per-commit tables and the "Downstream routing" block
    - .planning/phases/95-upstream-absorb-fork-invariant-verify/95-03-SUMMARY.md — Cluster C outcome (partial apply or structural no-op) to record in ledger
    - .planning/phases/95-upstream-absorb-fork-invariant-verify/95-VERIFICATION.md — newly written (confirms all 3 invariants PASS)
  </read_first>
  <action>
    Update 94-DIVERGENCE-LEDGER.md to mark all will-sync and split rows as absorbed. Edit
    the file to add "Phase 95 status" annotations:

    In the Cluster Summary table, add a "phase-95-status" column or update the rationale
    column for clusters A, B, C to include: "absorbed Phase 95 [date]":

    | cluster_id | ... | phase-95-status |
    | A          | ... | absorbed 2026-06-26 — git cherry-pick -x 9ce74e92 |
    | B          | ... | absorbed 2026-06-26 — shared-surface extraction (11fd10e0 hunks; tool-sandbox/tls_intercept/ skipped per D-01) |
    | C          | ... | absorbed 2026-06-26 — credentials_intent fix / structural no-op per D-02; Phase 89 active predicate preserved |
    | D          | ... | won't-sync → Phase 97 (unchanged; leapfrog floor >= 0.65.0) |

    Also update the "Downstream routing" block at the end of the ADR Review section to change:
    "Cluster A → Phase 95 (will-sync...)" to "Cluster A → Phase 95 ABSORBED (9ce74e92, DCO signed)"
    "Cluster B → Phase 95 (split...)" to "Cluster B → Phase 95 ABSORBED (shared-surface extraction, DCO signed)"
    "Cluster C → Phase 95 (split...)" to "Cluster C → Phase 95 ABSORBED (D-02 split, DCO signed)"
    "Cluster D → Phase 97 (won't-sync...)" stays unchanged

    No will-sync rows should remain open in the ledger after this edit.

    Commit the VERIFICATION.md and the DIVERGENCE-LEDGER update together:
      git add .planning/phases/95-upstream-absorb-fork-invariant-verify/95-VERIFICATION.md
      git add .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md
      git commit -s -m "docs(phase-95): fork-invariant verification + DIVERGENCE-LEDGER closeout

    95-VERIFICATION.md: all 3 fork-invariant checklist entries PASS (AppContainer/WFP/broker
    backend, ADR-86 audit/diagnostics boundary, exec_strategy_windows/ denial-rendering fork).
    SC4 security note: Cluster A AF_UNIX fix — Windows equivalents intact (AF_UNIX is
    Linux-only; exec_strategy_windows/ untouched by all Phase 95 absorbs).
    PARTIAL→96 record: 9ce74e92 + Cluster B applied hunks (sandbox/linux.rs, mod.rs)
    carry forward to Phase 96 cross-target clippy.

    94-DIVERGENCE-LEDGER.md: Clusters A, B, C marked absorbed; no will-sync rows open.
    Cluster D remains won't-sync → Phase 97."

    Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com> (via -s flag)

    After commit, run one final make ci to confirm the closeout commit hasn't broken anything
    (it shouldn't — this is docs-only, but run it as a precaution):
      make ci

    Record the final git log HEAD (post-closeout SHA) in the SUMMARY.md as the Phase 95 completion commit.
  </action>
  <verify>
    <automated>make ci</automated>
  </verify>
  <acceptance_criteria>
    - 94-DIVERGENCE-LEDGER.md contains "ABSORBED" (or "absorbed") text for Clusters A, B, C
    - grep -c "will-sync" .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md shows rows but all A/B/C now carry absorbed annotation
    - 95-VERIFICATION.md is committed (git log --oneline HEAD shows closeout commit)
    - git log --format="%B" HEAD | grep "Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>" returns 1 match
    - make ci exits 0 on the closeout commit
    - git log --oneline -8 shows the full Phase 95 commit sequence: baseline + 3 absorb commits + closeout
  </acceptance_criteria>
  <done>DIVERGENCE-LEDGER closed; 95-VERIFICATION.md committed; Phase 95 closeout committed; make ci green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Post-absorb tree → invariant evidence | The verification evidence is only valid if run against the final post-absorb tree (not mid-absorb) |
| DIVERGENCE-LEDGER editorial update | Marking rows "absorbed" must accurately reflect what was actually applied; misrecording leads to false assurance in future sync audits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-15 | Repudiation | Invariant checklist not backed by actual command outputs — records "PASS" without running the commands | mitigate | Each verification command in Task 1 produces a /tmp/*.txt output that must be transcribed into 95-VERIFICATION.md; placeholder text is forbidden |
| T-95-16 | Tampering | DIVERGENCE-LEDGER marks Cluster B as fully absorbed when tool-sandbox dir was intentionally skipped | mitigate | Ledger annotation for Cluster B MUST include "shared-surface extraction; tool-sandbox/tls_intercept/ skipped per D-01" — partial absorb, not full absorb |
| T-95-17 | Elevation of Privilege | PARTIAL→96 record omits a commit that touched cfg-gated Unix code — Phase 96 misses a cross-target lint gate | mitigate | PARTIAL→96 table explicitly lists all commits from Phase 95 SUMMARY.md "## PARTIAL Deferrals" sections; cross-referenced against 94-DIVERGENCE-LEDGER cross-target clippy notes |
| T-95-SC | Tampering | No new package installs in this plan | accept | Docs-only plan; no Cargo.toml changes |
</threat_model>

<verification>
Phase 95 completion gate (all must be true before calling phase done):

SC1 — All will-sync commits present in git log:
  git log --oneline | grep -c "9ce74e92\|Cherry-picked from.*9ce74e92"
  Expected: at least 1 (Cluster A absorbed)
  git log --format="%B" HEAD~5..HEAD | grep "11fd10e0"
  Expected: 1 match (Cluster B manual absorb references upstream SHA)
  git log --format="%B" HEAD~5..HEAD | grep "9b37dc52"
  Expected: 1 match (Cluster C absorb references upstream SHA)

SC2 — No new test failures:
  cargo test --workspace 2>&1 | grep "FAILED" | wc -l
  Expected: 5 (same as D-04 baseline)

SC3 — Fork-invariant checklist all PASS:
  grep -c "PASS" .planning/phases/95-upstream-absorb-fork-invariant-verify/95-VERIFICATION.md
  Expected: at least 3

SC4 — Security note for Cluster A documented:
  grep "AF_UNIX\|dup2\|9ce74e92" .planning/phases/95-upstream-absorb-fork-invariant-verify/95-VERIFICATION.md
  Expected: matches in SC4 section

SC5 — No open will-sync rows in ledger:
  grep "will-sync" .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md
  All lines with "will-sync" should also contain "absorbed" or be in the Cluster Summary table header
</verification>

<success_criteria>
- 95-VERIFICATION.md produced with 3 invariant checklist entries all PASS (actual outputs, no placeholders)
- SC4 security note for Cluster A AF_UNIX fix present and correct
- PARTIAL→96 deferral record lists all cfg-gated-Unix-touching commits
- 94-DIVERGENCE-LEDGER.md updated: all will-sync/split rows (A, B, C) marked absorbed; no open will-sync
- Final make ci green on the closeout commit
- Phase 95 completion commit in git log with DCO sign-off
</success_criteria>

<output>
Create .planning/phases/95-upstream-absorb-fork-invariant-verify/95-04-SUMMARY.md when done.
This is the final plan in the phase. Include:
  - The Phase 95 completion commit SHA (git log --oneline HEAD)
  - SC1..SC5 verification results (all PASS or PARTIAL as appropriate)
  - The PARTIAL→96 deferral items carry-forward into Phase 96 planning

Also update .planning/REQUIREMENTS.md to mark UPST10-02 and UPST10-03 as:
  UPST10-02: PARTIAL (absorbed; cross-target clippy PARTIAL→96)
  UPST10-03: PASS (fork-invariants verified; PARTIAL for cross-target-touching commits → Phase 96)
</output>
