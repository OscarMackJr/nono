---
phase: 96-cross-target-toolchain
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - .planning/phases/96-cross-target-toolchain/96-02-XTGT-APPLE-DARWIN-RECORD.md
autonomous: true
requirements: [XTGT-03]
must_haves:
  truths:
    - "zig + cargo-zigbuild are installed on the host with their resolved versions recorded (Wave 2 precondition)."
    - "Exactly ONE bounded `cargo zigbuild clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used` attempt was made (D-03 one-shot cap)."
    - "The apple-darwin gate is resolved to ONE explicit end-state: EITHER clippy exited 0 (flip to local-runnable) OR a written hard-blocker record commits apple-darwin to PARTIAL→CI citing the SDK/framework/Mach-O failure signature (D-04)."
    - "The proprietary macOS SDK was NOT extracted and `SDKROOT` was NOT set (D-04(b) licensing hard stop)."
  artifacts:
    - path: ".planning/phases/96-cross-target-toolchain/96-02-XTGT-APPLE-DARWIN-RECORD.md"
      provides: "apple-darwin disposition: recorded tool versions, the single zigbuild invocation, the captured outcome, and either the green exit OR the XTGT-03(b) hard-blocker rationale"
      contains: "x86_64-apple-darwin"
  key_links:
    - from: "cargo zigbuild clippy --target x86_64-apple-darwin"
      to: "aws-lc-sys build.rs C feature probe (memcmp_invalid_stripped_check.c)"
      via: "zig cc -target x86_64-macos compile+link of the build-dep probe"
      pattern: "x86_64-apple-darwin"
---

<objective>
Make exactly ONE bounded attempt to run apple-darwin clippy locally via cargo-zigbuild, then resolve XTGT-03 to one explicit end-state: a clean clippy exit (flip apple-darwin to local-runnable) OR a written hard-blocker record committing apple-darwin to PARTIAL→CI with the SDK-licensing rationale.

Purpose: apple-darwin is the genuinely hard target — clippy needs the macOS SDK only for the build-dep C link probe, and the very likely outcome is the `aws-lc-sys` SDK/framework wall (the D-04(b) licensing stop). This plan is time-boxed to one attempt and must handle BOTH branches without iterating past the first wall.
Output: zig + cargo-zigbuild installed (versions recorded), one zigbuild clippy run captured, and a disposition record consumed by Plan 03's per-gate doc rewrite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-cross-target-toolchain/96-CONTEXT.md
@.planning/phases/96-cross-target-toolchain/96-RESEARCH.md

<interfaces>
<!-- Install (record the resolved versions): -->
winget install zig.zig            (or: scoop install zig)
cargo install --locked cargo-zigbuild
zig version
cargo zigbuild --version

<!-- The ONE bounded attempt (D-03 one-shot). Do NOT set SDKROOT. Do NOT iterate past the first wall: -->
cargo zigbuild clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used

<!-- D-04 STOP SIGNATURES (any one = the wall → write the hard-blocker record, STOP):
     - fatal error: '<header>.h' file not found        (darwin C build-dep compile)
     - framework not found for '-framework CoreFoundation'  (or any -framework)
     - error: unable to find Darwin SDK / prompt to set SDKROOT
     - linker-emulation mismatch / "linker command failed" from the aws-lc-sys probe -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install zig + cargo-zigbuild, run ONE bounded apple-darwin clippy attempt, capture the outcome</name>
  <files>.planning/phases/96-cross-target-toolchain/96-02-XTGT-APPLE-DARWIN-RECORD.md</files>
  <read_first>
    - .planning/phases/96-cross-target-toolchain/96-CONTEXT.md (§D-03, §D-04, § Specific Ideas licensing wall)
    - .planning/phases/96-cross-target-toolchain/96-RESEARCH.md (§ Pattern 3 invocation, § Pitfall 5 stop signatures, § apple-darwin install steps, § Code Examples "apple-darwin bounded attempt")
    - Cargo.lock (repo root — confirm aws-lc-sys / ring versions that drive the C probe)
  </read_first>
  <action>
    Install the Wave 2 tools and record their resolved versions: `winget install zig.zig` (fall back to `scoop install zig` if winget unavailable), then `cargo install --locked cargo-zigbuild`. Capture `zig version` and `cargo zigbuild --version` output verbatim into the record.
    Run EXACTLY ONE attempt (D-03 cap): `cargo zigbuild clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used`. Do NOT set `SDKROOT`. Capture the full tail of the output.
    STOP on whichever fires first: (a) the run produces no clean clippy exit by the end of this plan, OR (b) ANY output requires acquiring/extracting the proprietary macOS SDK — recognized by the D-04 stop signatures (`'<x>.h' file not found`, `framework not found for '-framework ...'`, `unable to find Darwin SDK`, SDKROOT-required, aws-lc-sys link/emulation mismatch). On (b) STOP IMMEDIATELY on licensing grounds — do not attempt extraction, do not set SDKROOT, do not iterate.
    Create `96-02-XTGT-APPLE-DARWIN-RECORD.md` capturing: the recorded zig + cargo-zigbuild versions, the verbatim single invocation, and the captured outcome (the exit code and the failure-signature tail, OR the clean exit).
  </action>
  <verify>
    <automated>test -f .planning/phases/96-cross-target-toolchain/96-02-XTGT-APPLE-DARWIN-RECORD.md && grep -q "x86_64-apple-darwin" .planning/phases/96-cross-target-toolchain/96-02-XTGT-APPLE-DARWIN-RECORD.md && echo RECORD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `zig version` and `cargo zigbuild --version` both produce output (tools installed) and those versions are recorded in the record file.
    - `96-02-XTGT-APPLE-DARWIN-RECORD.md` exists and contains the literal target `x86_64-apple-darwin`, the single verbatim invocation, and the captured outcome (exit code + failure-signature tail OR clean exit).
    - The record shows `SDKROOT` was NOT set and no macOS SDK extraction was attempted.
    - Exactly one `cargo zigbuild clippy` invocation appears in the record (no iteration past the first wall).
  </acceptance_criteria>
  <done>zig + cargo-zigbuild installed with versions recorded; one bounded apple-darwin clippy attempt made; outcome captured in the record file; no SDK extraction attempted.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve XTGT-03 disposition — flip to local-runnable OR write the XTGT-03(b) hard-blocker</name>
  <files>.planning/phases/96-cross-target-toolchain/96-02-XTGT-APPLE-DARWIN-RECORD.md</files>
  <read_first>
    - .planning/phases/96-cross-target-toolchain/96-02-XTGT-APPLE-DARWIN-RECORD.md (the Task 1 captured outcome)
    - .planning/phases/96-cross-target-toolchain/96-CONTEXT.md (§D-04 disposition branches, §D-07 the doc consumes this)
    - .planning/phases/96-cross-target-toolchain/96-RESEARCH.md (§ Security Domain — SDK extraction is the licensing/supply-chain threat)
  </read_first>
  <action>
    Branch on the Task 1 outcome and finalize the record's disposition section:
    - If the attempt exited 0: declare apple-darwin LOCAL-RUNNABLE. Record the exact working invocation (`cargo zigbuild clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used`) and the resolved zig/cargo-zigbuild versions as the reproducible setup. Flag for Plan 03 that the checklist's apple-darwin Q3 branch flips to "MUST run locally".
    - If the attempt walled out: write the XTGT-03(b) HARD-BLOCKER RECORD. State the exact failure signature captured (the `'<x>.h' file not found` / `-framework` / SDKROOT-required line), explain that closing it requires extracting the proprietary macOS SDK on the Windows host (the D-04(b) licensing wall), and explicitly commit apple-darwin to PARTIAL→CI with that rationale. Flag for Plan 03 that the checklist's apple-darwin Q3 branch STAYS PARTIAL→CI with this recorded rationale.
    Commit the record with a lowercase conventional-commit subject (`docs(96): ...`) and the DCO trailer `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`.
  </action>
  <verify>
    <automated>grep -Eq "LOCAL-RUNNABLE|PARTIAL" .planning/phases/96-cross-target-toolchain/96-02-XTGT-APPLE-DARWIN-RECORD.md && echo DISPOSITION_SET</automated>
  </verify>
  <acceptance_criteria>
    - The record's disposition section states EXACTLY ONE of: apple-darwin is LOCAL-RUNNABLE (with the working invocation + tool versions), OR apple-darwin stays PARTIAL→CI (the XTGT-03(b) hard-blocker record citing the captured SDK/framework/Mach-O failure signature and the D-04(b) licensing rationale).
    - If PARTIAL→CI: the record names the specific failure signature line captured in Task 1, not generic prose.
    - The record carries a clear flag for Plan 03 stating which way the checklist's apple-darwin Q3 branch resolves.
  </acceptance_criteria>
  <done>XTGT-03 resolved to one explicit end-state; the record either flips apple-darwin to local-runnable or commits it to PARTIAL→CI with the cited hard-blocker rationale; Plan 03 handoff flag set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → newly installed zig + cargo-zigbuild | new host tools introduced for the bounded attempt (supply-chain trust) |
| host → proprietary macOS SDK | the licensing boundary — the SDK must NOT be acquired/extracted on this host (D-04(b)) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-02-SC | Tampering | `zig` + `cargo-zigbuild` host installs | mitigate | Both from official orgs (ziglang, rust-cross) per Research Package Legitimacy Audit (OK verdict); installed via winget `zig.zig` and `cargo install --locked cargo-zigbuild`; resolved versions recorded. No npm/PyPI packages introduced (slopcheck N/A). |
| T-96-02-01 | (licensing/supply-chain) | proprietary macOS SDK extraction to force apple-darwin green | accept-by-prohibition | D-04(b) hard stop — `SDKROOT` stays UNSET, no extraction attempted; acceptance criteria assert SDKROOT not set. The threat is mitigated by NOT taking the prohibited action; the resulting PARTIAL→CI is the accepted disposition. |
| T-96-02-02 | Repudiation | a false "passed" claim hiding an un-attempted/iterated gate | mitigate | One-shot cap (D-03) enforced by acceptance criteria (exactly one invocation recorded); disposition must cite the literal captured failure signature, not generic prose. |
</threat_model>

<verification>
- zig + cargo-zigbuild installed; versions recorded.
- Exactly one `cargo zigbuild clippy --target x86_64-apple-darwin` invocation recorded.
- `SDKROOT` not set; no SDK extraction attempted.
- Disposition section resolves to LOCAL-RUNNABLE or PARTIAL→CI (with cited signature).
- `96-02-XTGT-APPLE-DARWIN-RECORD.md` contains `x86_64-apple-darwin`.
</verification>

<success_criteria>
- SC#3: the apple-darwin gate is resolved to ONE explicit end-state — (a) passing locally, or (b) a written hard-blocker record committing it to PARTIAL→CI with rationale — either outcome closes XTGT-03.
</success_criteria>

<output>
Create `.planning/phases/96-cross-target-toolchain/96-02-SUMMARY.md` when done.
</output>
