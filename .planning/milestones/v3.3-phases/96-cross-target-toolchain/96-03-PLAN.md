---
phase: 96-cross-target-toolchain
plan: 03
type: execute
wave: 3
depends_on: [96-01, 96-02]
files_modified:
  - .planning/templates/cross-target-verify-checklist.md
  - CLAUDE.md
autonomous: true
requirements: [XTGT-04]
must_haves:
  truths:
    - "The checklist's canonical linux-gnu invocation is the `cross clippy` form, and the bare `cargo clippy --target x86_64-unknown-linux-gnu` is no longer documented as the runnable command (D-02)."
    - "The setup steps + canonical invocation live in `.planning/templates/cross-target-verify-checklist.md` (D-06 home), including the recorded cross image tag from Plan 01."
    - "CLAUDE.md's cross-target bullet is a ONE-LINE pointer to the checklist (D-06) carrying the `cross` form, not a duplicated runbook."
    - "linux-gnu's auto-default-to-PARTIAL is retired: PARTIAL is allowed ONLY on a *documented* cross/Docker failure — a stopped daemon is explicitly NOT such a failure (D-07)."
    - "apple-darwin's checklist disposition matches Plan 02's actual outcome (stays PARTIAL→CI with the hard-blocker rationale, OR flips to local-required) — the checklist does not contradict the Wave 2 verdict (D-07)."
  artifacts:
    - path: ".planning/templates/cross-target-verify-checklist.md"
      provides: "rewritten decision tree, setup section, per-gate PARTIAL disposition reflecting actual Wave 1/Wave 2 verdicts"
      contains: "cross clippy"
    - path: "CLAUDE.md"
      provides: "one-line cross-target bullet pointing at the checklist with the cross form + apple-darwin disposition"
      contains: "cross clippy"
  key_links:
    - from: "CLAUDE.md § Coding Standards cross-target bullet"
      to: ".planning/templates/cross-target-verify-checklist.md"
      via: "one-line pointer (no duplicated runbook)"
      pattern: "cross-target-verify-checklist"
    - from: ".planning/templates/cross-target-verify-checklist.md apple-darwin Q3"
      to: ".planning/phases/96-cross-target-toolchain/96-02-XTGT-APPLE-DARWIN-RECORD.md"
      via: "disposition reflects the recorded Wave 2 verdict"
      pattern: "apple-darwin"
---

<objective>
Rewrite the cross-target verification protocol so the auto-default-to-PARTIAL is retired per-gate and evidence-based, reflecting the actual Wave 1 (linux-gnu green) and Wave 2 (apple-darwin disposition) verdicts. The checklist owns the setup + canonical invocation (D-06); CLAUDE.md gets a one-line pointer carrying the `cross` form (D-02).

Purpose: Close XTGT-04. The bare `cargo clippy --target x86_64-unknown-linux-gnu` string was never runnable on this host — replacing it with the `cross clippy` form removes a doc lie, and narrowing the PARTIAL escape hatch stops a daemon-down state from defeating the gate. This is the protocol Phase 97's release tree inherits.
Output: rewritten checklist + a one-line CLAUDE.md bullet, both reflecting the recorded Wave 1/Wave 2 verdicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-cross-target-toolchain/96-CONTEXT.md
@.planning/phases/96-cross-target-toolchain/96-RESEARCH.md
@.planning/phases/96-cross-target-toolchain/96-01-XTGT-LINUX-GNU-RECORD.md
@.planning/phases/96-cross-target-toolchain/96-02-XTGT-APPLE-DARWIN-RECORD.md

<interfaces>
<!-- Target shape for the checklist decision tree (Research § Code Examples "Doc/Protocol Rewrite"):
     Q2 (linux-gnu): MUST run locally via:
        cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used
       - clean exit → REQ may flip to VERIFIED.
       - errors → close before flipping.
       - PARTIAL allowed ONLY on a DOCUMENTED cross/Docker failure (image-pull failure, daemon capability gap)
         — NOT a stopped daemon (start it) and NOT "toolchain absent" (it is present).
     Q3 (apple-darwin): disposition = the recorded Wave 2 verdict from 96-02-XTGT-APPLE-DARWIN-RECORD.md:
       - if walled out → STAYS PARTIAL→CI with the hard-blocker rationale; bare cargo clippy --target
         x86_64-apple-darwin remains documented as not-locally-runnable.
       - if passed → flips to "MUST run locally via cargo zigbuild clippy …".
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the checklist — cross-form invocation, setup home, per-gate evidence-based retirement</name>
  <files>.planning/templates/cross-target-verify-checklist.md</files>
  <read_first>
    - .planning/templates/cross-target-verify-checklist.md (current Decision Tree, Cross-Toolchain Setup, PARTIAL Disposition, Anti-Patterns)
    - .planning/phases/96-cross-target-toolchain/96-CONTEXT.md (§D-06, §D-07)
    - .planning/phases/96-cross-target-toolchain/96-01-XTGT-LINUX-GNU-RECORD.md (the recorded cross image tag + green-exit evidence)
    - .planning/phases/96-cross-target-toolchain/96-02-XTGT-APPLE-DARWIN-RECORD.md (the recorded apple-darwin disposition + Plan 03 handoff flag)
    - .planning/phases/96-cross-target-toolchain/96-RESEARCH.md (§ Architecture Patterns "Doc/Protocol Rewrite", § State of the Art deprecated sections)
  </read_first>
  <action>
    Edit `.planning/templates/cross-target-verify-checklist.md` in place:
    - Decision Tree Q2 (linux-gnu): replace the bare `cargo clippy --workspace --target x86_64-unknown-linux-gnu ...` with the canonical `cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used`. Remove the "No (toolchain missing — linker not found) → auto-PARTIAL" branch for linux-gnu; replace it with "MUST run locally via `cross clippy`; PARTIAL allowed ONLY on a *documented* cross/Docker failure (image-pull failure or daemon capability gap), NOT a stopped daemon (start it) and NOT 'toolchain absent' (cross + Docker are present)."
    - Decision Tree Q3 (apple-darwin): set the disposition to match the recorded Wave 2 verdict in `96-02-XTGT-APPLE-DARWIN-RECORD.md`. If that record committed apple-darwin to PARTIAL→CI, keep Q3 explicitly PARTIAL→CI and cite the hard-blocker rationale (the bare `cargo clippy --target x86_64-apple-darwin` stays documented as not-locally-runnable). If that record flipped apple-darwin to local-runnable, change Q3 to "MUST run locally via `cargo zigbuild clippy ...`". Do NOT retire both gates' default unconditionally.
    - Cross-Toolchain Setup section: rewrite per D-06 so the real setup is "Docker Linux engine running + `cross clippy`" for linux-gnu (record the exact pulled cross image tag from `96-01-XTGT-LINUX-GNU-RECORD.md`) and, for apple-darwin, the zig + cargo-zigbuild step only if Wave 2 flipped it to runnable. The stale `rustup target add ...`-as-primary-setup block is demoted (those targets are already added).
    - Keep the Anti-Patterns list intact (especially Anti-pattern 2: no `#[allow]` silencing). Narrow the PARTIAL Disposition prose so a daemon-down state cannot satisfy it.
  </action>
  <verify>
    <automated>grep -q "cross clippy" .planning/templates/cross-target-verify-checklist.md && grep -qv "^#" .planning/templates/cross-target-verify-checklist.md; grep -c "cross clippy" .planning/templates/cross-target-verify-checklist.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/templates/cross-target-verify-checklist.md` contains the string `cross clippy` (the canonical linux-gnu invocation).
    - The checklist no longer presents the bare `cargo clippy --workspace --target x86_64-unknown-linux-gnu` as the runnable linux-gnu command (it is replaced by / subordinated to the `cross clippy` form).
    - The linux-gnu auto-default-to-PARTIAL branch is removed; PARTIAL for linux-gnu is gated on a *documented* cross/Docker failure and a stopped daemon is explicitly excluded.
    - The apple-darwin Q3 disposition matches `96-02-XTGT-APPLE-DARWIN-RECORD.md` (PARTIAL→CI with cited rationale, OR local-runnable) — verifiable by cross-reading the two files; the checklist does not contradict the recorded verdict.
    - The recorded cross image tag from `96-01-XTGT-LINUX-GNU-RECORD.md` appears in the setup section.
  </acceptance_criteria>
  <done>Checklist rewritten: cross-form invocation, setup home with recorded image tag, linux-gnu auto-PARTIAL retired, apple-darwin disposition matching the Wave 2 record.</done>
</task>

<task type="auto">
  <name>Task 2: Collapse the CLAUDE.md cross-target bullet to a one-line pointer carrying the cross form</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (§ Coding Standards → "Cross-target clippy verification" bullet — the current MUST/NEVER prose)
    - .planning/phases/96-cross-target-toolchain/96-CONTEXT.md (§D-02, §D-06 one-line-pointer rule)
    - .planning/templates/cross-target-verify-checklist.md (the rewritten checklist Task 1 just produced — the pointer target)
  </read_first>
  <action>
    Replace the CLAUDE.md "Cross-target clippy verification" bullet so it (a) names the `cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used` form as the linux-gnu command, (b) states the apple-darwin disposition matching the Wave 2 record (PARTIAL→CI with hard-blocker rationale, or local-runnable), and (c) points to `.planning/templates/cross-target-verify-checklist.md` for the full runbook — WITHOUT duplicating the setup/decision-tree (D-06: one-line pointer, not a duplicate runbook). Remove the now-false claim that the bare `cargo clippy --workspace --target x86_64-unknown-linux-gnu` is the verification command. Keep the "Windows-host `cargo check` is NOT a substitute" warning.
    Run `make fmt-check` is N/A for markdown; instead confirm the bullet is well-formed and commit with a lowercase conventional-commit subject (`docs(96): ...`) and the DCO trailer `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`.
  </action>
  <verify>
    <automated>grep -q "cross clippy" CLAUDE.md && grep -q "cross-target-verify-checklist" CLAUDE.md && echo CLAUDE_OK; grep -c "cargo clippy --workspace --target x86_64-unknown-linux-gnu" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains `cross clippy` and a pointer string `cross-target-verify-checklist`.
    - CLAUDE.md no longer presents the bare `cargo clippy --workspace --target x86_64-unknown-linux-gnu` as the runnable verification command (the `cross` form supersedes it).
    - The bullet is a one-line pointer (states the command + disposition + checklist reference), not a duplicated setup/decision-tree runbook.
    - The "Windows-host `cargo check` is NOT a substitute" warning is preserved.
  </acceptance_criteria>
  <done>CLAUDE.md cross-target bullet collapsed to a one-line pointer carrying the cross form + apple-darwin disposition + checklist reference; the false bare-command claim removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification protocol → REQ verdict | the checklist's decision tree decides whether a Unix-touching REQ may flip to VERIFIED locally (integrity of the protocol itself) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-03-01 | Tampering | a falsely-retired PARTIAL default that lets unverified Unix drift pass | mitigate | The linux-gnu PARTIAL retirement is narrowed (Task 1) so PARTIAL requires a *documented* cross/Docker failure; a stopped daemon is explicitly excluded — closing the escape hatch a daemon-down state could exploit (Research § Security Domain threat). |
| T-96-03-02 | Repudiation | checklist apple-darwin disposition contradicting the actual Wave 2 verdict | mitigate | Task 1 keys Q3 to the recorded `96-02-XTGT-APPLE-DARWIN-RECORD.md` verdict; acceptance criteria require the two files to agree, so the protocol cannot claim a status the evidence does not support. |
| T-96-03-03 | Tampering | duplicated runbook in CLAUDE.md drifting out of sync with the checklist | mitigate | D-06 one-line-pointer rule (Task 2) keeps the single source of truth in the checklist; CLAUDE.md only references it. |
</threat_model>

<verification>
- Checklist contains the `cross clippy` form; bare linux-gnu `cargo clippy --target` no longer the runnable command.
- linux-gnu auto-PARTIAL retired; daemon-down excluded from PARTIAL.
- apple-darwin Q3 disposition matches `96-02-XTGT-APPLE-DARWIN-RECORD.md`.
- CLAUDE.md bullet is a one-line pointer to the checklist carrying the cross form.
- Recorded cross image tag present in the checklist setup section.
</verification>

<success_criteria>
- SC#4: CLAUDE.md and the checklist are updated to reflect which gates are now locally runnable, retiring the PARTIAL→CI *default* per-gate and evidence-based.
- SC#1 (completion): the setup + canonical invocation (with the recorded image tag) are documented, sufficient for another developer to reproduce.
</success_criteria>

<output>
Create `.planning/phases/96-cross-target-toolchain/96-03-SUMMARY.md` when done.
</output>
