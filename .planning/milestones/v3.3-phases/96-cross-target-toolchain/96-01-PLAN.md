---
phase: 96-cross-target-toolchain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono/src/sandbox/linux.rs
  - crates/nono-cli/src/exec_strategy/
  - bindings/c/src/
  - .planning/phases/96-cross-target-toolchain/96-01-XTGT-LINUX-GNU-RECORD.md
autonomous: true
requirements: [XTGT-01, XTGT-02]
must_haves:
  truths:
    - "The Docker Linux engine daemon is confirmed running (`docker info` prints a Server section) before the gate runs (per D-01 precondition)."
    - "`cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used` exits 0 on this Windows host (SC#2)."
    - "All drift the linux-gnu gate surfaced is fixed structurally — no `#[allow(...)]` added to silence cross-target lints (D-05)."
    - "The exact cross image tag actually pulled by `cross` is recorded for reproducibility (SC#1)."
    - "Native `make ci` (clippy + fmt + tests) still passes on the Windows host after the drift fixes (no native-target regression)."
  artifacts:
    - path: ".planning/phases/96-cross-target-toolchain/96-01-XTGT-LINUX-GNU-RECORD.md"
      provides: "linux-gnu gate evidence: recorded cross image tag, green exit, drift fixes summary"
      contains: "cross clippy"
  key_links:
    - from: "cross clippy --target x86_64-unknown-linux-gnu"
      to: "crates/nono/src/sandbox/linux.rs (+ exec_strategy/, bindings/c/src/)"
      via: "containerized cargo clippy exercising cfg(target_os=\"linux\") branches"
      pattern: "cfg\\(target_os = \"linux\"\\)"
---

<objective>
Stand up the `x86_64-unknown-linux-gnu` clippy gate as a provably-runnable local command on this Windows dev host, and fix every piece of cfg-gated Unix drift it surfaces — structurally, with no `#[allow]` silencing — until it exits 0.

Purpose: Retire the Phase 95 PARTIAL→CI deferral for linux-gnu by proving the gate runs green locally against the post-sync tree. This is the first-ever local run of the gate, so the drift backlog size is unknown and must be triaged then fixed in a loop.
Output: A green `cross clippy` linux-gnu run, structural drift fixes committed, and a record file capturing the exact pulled image tag + green exit (consumed by Plan 03's doc rewrite and by the verifier).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-cross-target-toolchain/96-CONTEXT.md
@.planning/phases/96-cross-target-toolchain/96-RESEARCH.md

<interfaces>
<!-- Canonical command (D-01, verbatim) — use exactly this string, do not paraphrase: -->
cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used

<!-- Docker daemon precondition gate (Research Pattern 2). A daemon-down state is an operator
     precondition, NOT a "documented Docker failure" — start the engine and re-run, never defer to PARTIAL: -->
docker info 2>&1 | grep -q "Server Version" || { echo "Docker Linux engine NOT running — start Docker Desktop (Linux engine)"; exit 1; }

<!-- Drift fix patterns (D-05 / Research § Code Examples) — structural only:
     - dead_code / unused on a cfg sibling branch → tighten the cfg gate or pub(crate) visibility (NOT #[allow(dead_code)])
     - unwrap_used / expect_used → propagate via NonoError with `?` (CLAUDE.md Error Handling)
     - missing_safety_doc on bindings/c/src/* → add // SAFETY: docs (CLAUDE.md Unsafe Code)
     - needless_return / redundant_clone / useless_conversion → structural cleanup -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm Docker Linux engine, run the linux-gnu gate, record the image tag, triage drift</name>
  <files>.planning/phases/96-cross-target-toolchain/96-01-XTGT-LINUX-GNU-RECORD.md</files>
  <read_first>
    - .planning/phases/96-cross-target-toolchain/96-CONTEXT.md (§D-01, §D-02, §D-05)
    - .planning/phases/96-cross-target-toolchain/96-RESEARCH.md (§ Pattern 2 daemon precondition, § Pitfall 1, § Pitfall 3 drift classes, § Pattern 1 image tag)
    - Cross.toml (repo root — the x86_64-unknown-linux-gnu stanza)
  </read_first>
  <action>
    Assert the Docker Linux engine is up FIRST: run `docker info` and confirm output contains a "Server Version" line. If absent, start Docker Desktop's Linux engine and re-check until `docker info` exits 0 with a Server section. Do NOT treat a daemon-down state as a documented Docker/cross failure and do NOT defer to PARTIAL — this is the exact Phase 95 trap (Research Pitfall 1).
    Then run the verbatim D-01 command: `cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used`. From cross's pull log, capture the EXACT image tag it pulled (expected `ghcr.io/cross-rs/x86_64-unknown-linux-gnu:0.2.5` per Research A1, but record the literal string cross prints — the log is authoritative).
    Create `96-01-XTGT-LINUX-GNU-RECORD.md` capturing: the recorded image tag string, the `docker info` Server-confirmed precondition, the verbatim gate command, and a triage list of every distinct lint/error class the first run emitted (file path + lint name) for Task 2 to fix. If the first run already exits 0 with zero drift, record that and note Task 2 is a no-op confirmation pass.
  </action>
  <verify>
    <automated>docker info 2>&1 | grep -q "Server Version" && echo PRECONDITION_OK</automated>
  </verify>
  <acceptance_criteria>
    - `docker info` exits 0 and its output contains "Server Version" (daemon precondition satisfied).
    - `96-01-XTGT-LINUX-GNU-RECORD.md` exists and contains the literal string `cross clippy` and the recorded image tag string pulled by cross.
    - The record file lists the triaged drift classes from the first run (or explicitly states "zero drift, gate green on first run").
    - The record file does NOT claim PARTIAL on the basis of a stopped daemon.
  </acceptance_criteria>
  <done>Docker Linux engine confirmed up; first linux-gnu `cross clippy` run executed; image tag recorded; drift triaged into the record file.</done>
</task>

<task type="auto">
  <name>Task 2: Fix all surfaced drift structurally until the linux-gnu gate exits 0</name>
  <files>crates/nono/src/sandbox/linux.rs, crates/nono-cli/src/exec_strategy/, bindings/c/src/, .planning/phases/96-cross-target-toolchain/96-01-XTGT-LINUX-GNU-RECORD.md</files>
  <read_first>
    - .planning/phases/96-cross-target-toolchain/96-01-XTGT-LINUX-GNU-RECORD.md (the Task 1 triage list)
    - .planning/phases/96-cross-target-toolchain/96-RESEARCH.md (§ Pitfall 3 fix mapping, § Code Examples "Drift fix pattern", § Pitfall 4 fmt-clean)
    - CLAUDE.md (§ Coding Standards: Error Handling, Unwrap Policy, Unsafe Code, "Lazy use of dead code")
    - The specific source files named in the Task 1 triage list (read each before editing it)
  </read_first>
  <action>
    Work the triage list from Task 1 as a fix loop: for each surfaced lint/error, apply a STRUCTURAL fix per the D-05 mapping — `unwrap_used`/`expect_used` → `?`-propagation via `NonoError`; `dead_code`/unused → tighten the `#[cfg(...)]` gate or change visibility to `pub(crate)`; `missing_safety_doc` on `bindings/c/src/*` → add `// SAFETY:` docs; `needless_return`/`redundant_clone`/`useless_conversion` → structural cleanup. NEVER add `#[allow(dead_code)]` or `#[allow(clippy::unwrap_used)]` to silence a cross-target lint (D-05 / checklist Anti-pattern 2 / CLAUDE.md).
    After each fix batch, re-run `cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used` and re-run `make ci` (so a structural fix doesn't break native clippy/fmt/tests — Research Pitfall 4: rustfmt-clean ≠ clippy-clean). Loop until the cross gate exits 0 AND `make ci` passes. Update `96-01-XTGT-LINUX-GNU-RECORD.md` with the final green exit confirmation and a one-line-per-fix summary.
    Commit drift fixes with lowercase conventional-commit subjects (`fix(...)`/`chore(...)`) and the DCO trailer `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`. Note: the 11 nono-cli + 1 nono pre-existing baseline test fails (memory `nono_cli_windows_baseline_test_failures`) are NOT regressions from this phase — do not chase them.
  </action>
  <verify>
    <automated>cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used</automated>
  </verify>
  <acceptance_criteria>
    - `cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used` exits 0.
    - `grep -rn "#\[allow(dead_code)\]\|#\[allow(clippy::unwrap_used)\]" crates/nono/src/sandbox/linux.rs crates/nono-cli/src/exec_strategy bindings/c/src` shows NO new allows introduced by the drift fixes (structural fixes only).
    - `make ci` passes on the Windows host (native clippy + fmt + tests; pre-existing baseline fails excluded).
    - `96-01-XTGT-LINUX-GNU-RECORD.md` records the final green exit (exit 0) and a per-fix summary.
  </acceptance_criteria>
  <done>linux-gnu gate exits 0; all drift fixed structurally with no silencing allows; native `make ci` green; record file finalized with green-exit evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → pinned `cross` Docker image | the linux-gnu toolchain executes inside a pulled container image (supply-chain trust on the image tag) |
| verification protocol → REQ verdict | the gate's exit code decides whether a Unix-touching REQ may flip to VERIFIED (integrity of the gate itself) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01-SC | Tampering | pulled `cross` linux-gnu Docker image | mitigate | Record the EXACT image tag cross pulls (Task 1) — the default tag is version-pinned to the `cross` binary (0.2.5), giving reproducibility; no unpinned `:latest` used. cross/Docker are pre-installed official tools (Research Package Legitimacy Audit: OK). |
| T-96-01-01 | Tampering | drift fix that silences a real Unix bug | mitigate | D-05 forbids `#[allow(...)]`; acceptance criteria grep-asserts no new silencing allows; fixes are structural (cfg-gate/visibility/`?`-propagation). |
| T-96-01-02 | Repudiation | daemon-down misread as "documented Docker failure" → false PARTIAL → un-linted Unix security code (Landlock/seccomp/path-handling) ships | mitigate | Pattern 2 daemon precondition gate (Task 1) asserts a Server section before the gate; record file forbidden from claiming PARTIAL on a stopped daemon. |
</threat_model>

<verification>
- `docker info` prints a Server section before the gate runs.
- `cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used` exits 0.
- No new `#[allow(...)]` silencing in the touched cfg-gated Unix files.
- Native `make ci` passes (no native-target regression).
- `96-01-XTGT-LINUX-GNU-RECORD.md` records the image tag + green exit.
</verification>

<success_criteria>
- SC#1 (partial): the exact linux-gnu cross image tag + invocation are recorded for reproducibility.
- SC#2: the linux-gnu clippy gate runs to completion and exits 0; all surfaced cfg-gated Unix drift is fixed structurally.
</success_criteria>

<output>
Create `.planning/phases/96-cross-target-toolchain/96-01-SUMMARY.md` when done.
</output>
