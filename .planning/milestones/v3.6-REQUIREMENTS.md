# Requirements Archive: nono v3.6 — UPST12 Upstream Sync (v0.66.0 → v0.69.0)

**Status:** ✅ SHIPPED 2026-08-08 (LOCAL)
**Phases:** 108–114
**Outcome:** 23/23 requirements delivered. 8/23 verified by a phase whose VERIFICATION.md
reached `passed`; the remaining 15 sit under phases verified `human_needed` and
operator-approved at their own gates. No requirement `unsatisfied`, no orphans.
**Audit:** `v3.6-MILESTONE-AUDIT.md` — `passed_with_accepted_risk`.

> Extracted from `.planning/REQUIREMENTS.md`, which remains live because it also carries the
> still-open v3.5 milestone's requirements. Only the v3.6 section was archived and removed.

---

# Requirements: nono v3.6 — UPST12: Upstream Sync v0.66.0 → v0.69.0
# ══════════════════════════════════════════════════════════════════

**Defined:** 2026-07-28
**Parallel to:** v3.5 (HELD on the external Azure Trusted-Signing root-propagation block; engineering proceeds on v3.6 meanwhile — operator decision 2026-07-28). Both milestones are simultaneously active; v3.5 owns phases 101–107, v3.6 owns phases 108–111.
**Core Value:** Keep the fork current with upstream `nolabs-ai/nono` without regressing the Windows security model or the ADR-86 policy-free-library boundary — a routine drain-then-sync (mirrors v3.1/v3.3/v3.4).

**Scope:** absorb the cross-platform delta from upstream `v0.66.0..v0.69.0` (v0.67.0, v0.67.1, v0.68.0, v0.69.0) — proxy/network features, profile/policy features (incl. `platform_overrides`), macOS Seatbelt carry, and resource-CLI alignment — then leapfrog all 6 workspace crates + both binding repos to `0.70.0` (prepare-only). **Explicitly EXCLUDES** the `tool-sandbox/` subsystem and its refinements: upstream introduced it in v0.65.0 (PR #1105) and the fork never absorbed it — it is a standing structural divergence handled by a dedicated later milestone (v3.7 Windows Tool-Sandbox Parity), not this routine sync. Scope source: quick task `260727-jkn` (`.planning/quick/260727-jkn-map-macos-0-69-parity-gap-phases-for-the/260727-jkn-PLAN.md`).

> **Architecture invariants:**
> - **tool-sandbox is OUT.** The 7 tool-sandbox refinement PRs in this window (#1280/#1322/#1325/#1384/#1394/#1413/#1417) ride on a subsystem the fork does not have; they are recorded DEFERRED→v3.7 in the ledger, not absorbed here.
> - **Never regress the Windows security model** (AppContainer / Low-IL / WFP / Job-Object) or the ADR-86 policy-free-library boundary. Verifier + code review + both cross-target clippy gates are mandatory before close.
> - **Cross-target clippy is MUST** for cfg-gated Unix edits — `cross` (linux-gnu) + `cargo-zigbuild` (apple-darwin), both GREEN locally, no PARTIAL→CI (retired in v3.3 Phase 96). `make ci` (clippy+fmt+tests), not clippy-only.
> - **`platform_overrides` is the intended vehicle for Windows divergence** — migrating the fork's `windows_low_il_broker`/`windows_interpreters` top-level flags into it retires flag-sprawl; keep back-compat aliases.
> - **WFP expresses port ranges natively** — the Windows remote-port-range emitter is simpler than macOS Seatbelt's per-port unroll; keep discrete-list back-compat.
> - **Binding struct-drift is caught only by building** — re-run `maturin build` (nono-py) + `napi build` (nono-ts) after any nono-proxy struct touch ([[project_v34_opened]] durable).
> - **Release scope = PREPARE ONLY** — leapfrog `0.70.0` (collision-free above upstream 0.69.0), no operator push. Repo PUBLIC; no `build_notes/`/`.gsd/` staged. DCO-signed.

## v3.6 Requirements

### UPST12 Divergence Audit (UPST12)
- [x] **UPST12-01**: An authoritative `108-DIVERGENCE-LEDGER.md` for upstream `v0.66.0..v0.69.0` exists — every substantive commit classified (adopt / adapt / skip / split) with a `windows-touch` flag and ADR-review verdict; re-export surfaces diff-inspected (not just `--name-only`, per the cluster-isolation-can-be-empirically-false lesson); and the 7 tool-sandbox refinement PRs (#1280/#1322/#1325/#1384/#1394/#1413/#1417) explicitly recorded DEFERRED→v3.7.

### Proxy / Network Absorb (NET)
- [x] **NET-01**: `deny_domain` deny-list network filtering (#1374) is absorbed into the proxy filter + profile schema, composing correctly with the fork's existing `allow_domain` allowlist model without weakening default-deny.
- [x] **NET-02**: SPIFFE/SPIRE workload-identity auth for upstream routes (#1272) is absorbed. **Complete (Phase 113, 2026-08-06)**: `c831dade` absorbed as ADAPT-DOWN per `proj/ADR-113-spiffe-disposition.md` — direct JWT-SVID bearer injection (`handle_spiffe_route`) and RFC 7523 jwt-bearer OAuth2 assertion (`handle_spiffe_assertion_credential`) both landed in `reverse.rs`, configurable via profile (`RouteConfig.spiffe`/`CustomCredentialDef.spiffe`), fail-closed on every non-SPIFFE-implementing proxy path (D-03) and on an unreachable SPIRE Workload API socket (D-04). ADR-86 boundary confirmed non-regressed (D-08/SC3, one named concept-leak caveat). Both cross-target clippy gates GREEN, both sibling bindings (`../nono-py`, `../nono-ts`) rebuild green. 6 of 8 SPIFFE tests SKIP locally (no local SPIRE agent, compensated by the new `spire.yml` CI lane) and `handle_spiffe_assertion_credential`'s successful-forward path remains untested anywhere — both named residuals in the ADR, not silently closed.
- [x] **NET-03**: The SigV4 encoded-URI generation fix (#1430) and the sibling-route cross-deny fix (#1437) are confirmed non-applicable — both target upstream subsystems (`aws/sign.rs` full SigV4 signing, `tls_intercept/handle.rs` multi-route TLS-intercept dispatch) absent from the fork's architecture, per `109-AWS-SIGV4-TLS-INTERCEPT-FINDING.md`; `no_proxy` bypass and `HTTP_PROXY` forward-proxy are verified non-regressed; `maturin` + `napi` binding builds are green.

### Profile / Policy Absorb (PROF)
- [x] **PROF-01**: `platform_overrides` per-OS profile patching (#1371) is absorbed and preserved through `extends` resolution (#1380); the fork's `windows_low_il_broker` and `windows_interpreters` top-level flags are migrated into `platform_overrides.windows` with back-compat aliases.
- [x] **PROF-02**: `$VAR` process-env token expansion (#1296) and `@git:*` dynamic token expansion (#1298) in profile filesystem paths are absorbed.
- [x] **PROF-03**: The port-range profile schema (#1398) is absorbed with a WFP-native remote-port-range emitter on Windows and discrete-list back-compat. **PROF-03e live-kernel proof PASSED 2026-08-04** — 8 filters installed from a verified-zero baseline, 4 `FWP_MATCH_RANGE` conditions at `49200..49210` (one per layer, never per-port unrolled), each `ALE_USER_ID`-scoped to the child, torn down back to 0 on exit. Evidence: `110-06-PROF-03e-VERDICT.md`. The behavioural inside/outside connect probe is **NOT RUN** (`0xC0000142` — no client binary starts under the AppContainer with this minimal profile); closure accepted on the filter-table proof, which is what Task 3 specifies.
- [x] **PROF-04**: The `bun` (#1305) and `mise` (#1387) runtime presets are absorbed.

### macOS / Core Carry + Resource CLI (CORE)
- [x] **CORE-01**: The macOS Seatbelt/core carry lands as-is for cross-target parity — `~/.cache` grant (#1378) and `MAX_CRYPTO_THREADS`=12 libdispatch tuning (#1424). **AMENDED 2026-07-30:** the `macos.rs` port-range emitter (#1398) is REMOVED from this requirement — Phase 110 absorbs commit `d5803b99` in full (schema + `capability.rs` + both Unix emitters + the WFP-native Windows emitter) so the port-range feature is coherent and testable in one phase. Phase 111 must not re-absorb it.
- [x] **CORE-02**: The upstream resource-limit CLI surface (`--memory` / `--max-processes`, #1269/#1403) is reconciled with the fork's existing kernel-enforced Job Object implementation — flag names/semantics aligned, no new enforcement, no regression to `--cpu-percent`/`--timeout`. Split across two plans (Phase 110's PROF-03 precedent): **111-02** corrected the stale `cli.rs`/`docs/cli/usage/flags.mdx` help text falsely claiming Linux/macOS enforcement was pending; **111-03** settled the D-01 ADAPT-not-adopt disposition in `proj/ADR-111-resource-limits-boundary.md` and recorded the standing divergence in `108-DIVERGENCE-LEDGER.md`. No new enforcement; D-04 flag surface frozen (no rename needed — upstream spells all four flags identically).

### Security + Residual Sync (SEC / RES) — Phase 112
> **Added 2026-07-29** by operator approval of the Phase 108 ledger's D-18/D-19 coverage-gap amendment. Phase 108 measured **27 hand-verified CODE commits** in the `v0.66.0..v0.69.0` window mapping to none of v3.6's original 12 requirements. Work-list: `108-DIVERGENCE-LEDGER.md` §"Requirement Coverage Gap" + the `security-residual-and-misc` cluster.

- [x] **SEC-01**: AWS SigV4 authentication for the MiTM proxy (#1195, `0ecc476b`) is absorbed. **RESOLVED 2026-08-05, not absorbed (won't-sync):** the target subsystem (`crates/nono-proxy/src/aws/*`, `tls_intercept/*`) is absent from the fork — a deliberate prior fork decision (D-15) ships a structural 501 stub instead of building AWS SigV4 signing, and nothing in Phases 108-111 changed that posture. See `112-AWS-SIGV4-PROXY-AUTH-FINDING.md` and the `108-DIVERGENCE-LEDGER.md` Phase 112 addendum. Checkbox flipped per the SEC-08/SEC-09/RES-01/RES-02 won't-sync/skip-biased convention — reconciled at the Phase 112 gate, `112-08`.
- [x] **SEC-02**: Declarative sandboxed OAuth capture (`9b692e07`), capture-boundary hardening (`3c59c62e`), and the stdin fixture test (`d033c631`) are absorbed. **RESOLVED 2026-08-07 (Phase 114), ADAPTED-with-scope-limit:** built a fork-native response buffer-and-rewrite enforcement point (`relay_response_with_capture()`, `crates/nono-proxy/src/reverse.rs`), wired into all three reverse-proxy relay sites, rather than importing upstream's absent `forward.rs`/`oauth_capture/` subsystem. Real OAuth tokens are rewritten to sandbox-visible phantoms before reaching the sandboxed client; the mint-to-resolve loop is closed with a live production call site (`resolve_capture_request_body()`, `reverse.rs:427-428`) **on relay site 1 (the static-credential path) only** — sites 2 and 3 (`handle_spiffe_route`, `handle_spiffe_assertion_credential`) apply the response-side rewrite but not the request-side resolve, a deliberate scope limit recorded in `114-06-SUMMARY.md` and corrected here per v3.6 milestone audit finding ACC-01 (the earlier wording said "closed end-to-end", which overstated a reachable `spiffe`+`capture` configuration). **Scope limit:** capture works only for OAuth token endpoints reachable as configured reverse-proxy routes — agent-initiated CONNECT flows to arbitrary hosts remain out of reach without TLS interception, which this fork declines by standing decision (`149abde0` not absorbed). Full disposition record: `proj/ADR-114-oauth-capture-disposition.md`. Carry-forward note filed in `108-DIVERGENCE-LEDGER.md` ("SEC-02 Carry-Forward Note (Phase 114, D-10)").
- [x] **SEC-03**: NVIDIA procfs mediation hardening (#1284, `a3243907`) is absorbed without regressing the fork's own GPU tests.
- [x] **SEC-04**: The trust-policy `predicate` field distinguishing nono trust policies from foreign JSON (#1333, `f943fb5a`) is absorbed.
- [x] **SEC-05**: The Linux execute-restriction `Refer` grant (#1397, `d84b4818`) is absorbed; cross-target clippy GREEN.
- [x] **SEC-06**: Seccomp supervisor-ancestry for orphaned descendants (#1401, `ac5ccd70`) is absorbed. Adapted to the fork's `linux_child_requires_dumpable()` predicate (upstream's `tool_sandbox_runtime`/`SeccompPolicy` gate symbols are absent). `PR_SET_CHILD_SUBREAPER` + `reap_reparented_orphans()` wired into the Linux-only supervisor loop; genuine RED/GREEN TDD confirmed live via `cross test`. See `112-06-SUMMARY.md`.
- [x] **SEC-07**: The standalone `nono proxy` command (#1261, `2663e990`) is absorbed.
- [x] **SEC-08**: The `allow_vars` empty-list env-strip fix (#1204, `a5a441c2`) is absorbed. **RESOLVED 2026-08-05, not absorbed:** per D-05's contingent ADR-112 escalation trigger, upstream's fix reverses the fork's own deliberate fail-closed default (Plan 34-08a/D-20) — adopting it verbatim would reopen an env-var-leak class the fork already closed. Decision recorded in `proj/ADR-112-allow-vars-fail-closed-preserved.md`: PRESERVE, zero source-code change. Checkbox flipped at the Phase 112 gate (`112-08`) per the SEC-01/SEC-09/RES-01/RES-02 won't-sync/skip-biased convention — resolved-but-not-literally-absorbed items are reconciled, not left silently open.
- [x] **SEC-09**: The credential-broker non-shim-entry guard relaxation (#1301, `f6f02751`) is absorbed. *(Found security-relevant during Phase 108's Cluster Summary rollup — not a D-18-named anchor.)* **RESOLVED 2026-08-05, not absorbed (won't-sync, target subsystem absent):** `execution_runtime.rs` carries no `command_policies`/`tool_sandbox_runtime`/`tool_sandbox_initial_shim` guard to relax — the fork never absorbed the base tool-sandbox subsystem (PR #1105). A carry-forward note is filed in `108-DIVERGENCE-LEDGER.md`'s tool-sandbox-surface cluster section ("SEC-09 Carry-Forward Note") obligating whichever future plan absorbs that base subsystem to consciously decide whether to bring `f6f02751`'s relaxed guard along with it. Checkbox flipped per the SEC-01/SEC-08/RES-01/RES-02 won't-sync/skip-biased convention — the note IS the deliverable.
- [x] **RES-01**: The registry/update-check header residual (#1405/#1383/#1386/#1341/#1340) is individually reviewed and absorbed or explicitly skipped with reasoning. **RESOLVED 2026-08-05 (Plan 112-01):** all 4 SHAs (`f0506434`, `7fe0c828`, `0158d52f`, `762eb05b`) recorded skip-with-reasoning per D-03 — the fork's own HKLM machine-policy spine (`HKLM\SOFTWARE\Policies\nono`, v3.0) and divergent update behavior collide with upstream's registry/update-check surface. `f0506434`'s `always-further/*` pack-registry-namespace staleness (16 grep hits) flagged as a real but UX-only, out-of-scope finding — not acted on. See `112-OAUTH-CAPTURE-DISPOSITION.md` Part 2. Checkbox flipped at the Phase 112 gate (`112-08`) per the skip-biased convention.
- [x] **RES-02**: The PTY-teardown (#1258) + test-infra residual is individually reviewed and absorbed or explicitly skipped with reasoning. **RESOLVED 2026-08-05 (Plan 112-04):** all 3 commits individually dispositioned — `503045801a` adopted verbatim (late-CPR-reply teardown drain, live-tested via `cross test`, 38/38 `pty_proxy` tests pass); `4cc0af2c52` explicitly SKIPPED with corrected, symbol-level-verified reasoning (target `socket_test_dir()` helper absent from the fork entirely — superseding `112-DISPOSITION-TABLE.md`'s original file-presence-only 'adapt with caution, or skip' call); `9840a16f35` adopted with a necessary fork-specific marker adaptation (`NONO_NO_SAVE_PROMPT` env guard + denial-marker assertion rebuilt around the socket path, since this fork's diagnostic formatter never emits upstream's literal `"send {path}"` string). See `112-04-SUMMARY.md`. Checkbox flipped at the Phase 112 gate (`112-08`) per the skip-biased convention.

### Fork-Invariant Verify + Release (VERIFY / RLS)
- [x] **VERIFY-01**: Both cross-target clippy gates (`cross` linux-gnu + `cargo-zigbuild` apple-darwin) and `make ci` (clippy+fmt+tests) are GREEN locally, and a fork-invariant pass confirms the Windows security model + ADR-86 boundary are non-regressed.
- [x] **RLS-14**: All 6 workspace crates + internal path-dep pins + both binding repos (`../nono-py`, `../nono-ts`) are leapfrogged to `0.70.0` (collision-free above upstream 0.69.0), Cargo.lock shows zero unexpected third-party drift, and the prepare-only release gate is GREEN — no operator push.

## v3.6 Out of Scope

| Feature | Reason |
|---------|--------|
| The `tool-sandbox/` subsystem (PR #1105) + its 7 refinement PRs | Standing structural divergence (fork never absorbed the v0.65.0 subsystem); handled by the dedicated v3.7 Windows Tool-Sandbox Parity milestone, not a routine sync. |
| A Windows `tool-sandbox/platform/windows.rs` driver | Belongs to v3.7 (needs the SCM_RIGHTS→handle-passing + peer-auth spike + an adopt-vs-formalize ADR). |
| macOS-only behavioral testing of the carried Seatbelt fixes | The fork ships macOS binaries but validates macOS via cross-target clippy + CI, not a live macOS host (established posture). |
| WFP daemon-path-only reachability hardening / filter-leak fix | Fork-internal hardening surfaced by the audit; tracked separately, not part of the upstream-parity sync. |
| Any operator push / live registry publish of `0.70.0` | Prepare-only (mirrors v3.1/v3.3/v3.4); live publish is a separate operator-gated step. |

## v3.6 Traceability

Phase numbering continues (v3.5 owns 101–107) → v3.6 owns **Phases 108–114**.

| Requirement | Phase | Status |
|-------------|-------|--------|
| UPST12-01 | Phase 108 | Complete |
| NET-01 | Phase 109 | Complete |
| NET-02 | Phase 113 | Complete |
| NET-03 | Phase 109 | Complete |
| PROF-01 | Phase 110 | Complete |
| PROF-02 | Phase 110 | Complete |
| PROF-03 | Phase 110 | Complete |
| PROF-04 | Phase 110 | Complete |
| CORE-01 | Phase 111 | Complete |
| CORE-02 | Phase 111 | Complete |
| VERIFY-01 | Phase 111 | Complete |
| RLS-14 | Phase 111 | Complete |
| SEC-01 | Phase 112 | Complete (won't-sync) |
| SEC-02 | Phase 114 | Complete (ADAPTED-with-scope-limit) |
| SEC-03 | Phase 112 | Complete |
| SEC-04 | Phase 112 | Complete |
| SEC-05 | Phase 112 | Complete |
| SEC-06 | Phase 112 | Complete |
| SEC-07 | Phase 112 | Complete |
| SEC-08 | Phase 112 | Complete (preserved, ADR-112) |
| SEC-09 | Phase 112 | Complete (won't-sync + carry-forward) |
| RES-01 | Phase 112 | Complete (skip-with-reasoning) |
| RES-02 | Phase 112 | Complete |
</content>

---

## Outcome Notes

**Resolved without literal absorb** (won't-sync / skip-with-reasoning — the recorded decision IS
the deliverable, per the skip-biased convention reconciled at the Phase 112 gate `112-08`):

| Req | Outcome |
|-----|---------|
| SEC-01 | Target subsystem (`aws/*`, `tls_intercept/*`) absent from the fork by prior decision D-15; structural 501 stub retained. |
| SEC-08 | **PRESERVE** — upstream's fix reverses the fork's deliberate fail-closed `allow_vars` default. Zero source change (ADR-112). |
| SEC-09 | Target guard does not exist in the fork (base tool-sandbox subsystem never absorbed). Carry-forward note filed against whoever absorbs it. |
| RES-01 | All 4 SHAs skip-with-reasoning — the fork's HKLM machine-policy spine collides with upstream's registry/update-check surface. |
| NET-03 | Both fixes confirmed non-applicable — target subsystems absent. |

**Adapted rather than adopted:**

| Req | Outcome |
|-----|---------|
| NET-01 | ADAPT per ADR-108 — deny-list as defense-in-depth, checked strictly *before* the allowlist, never weakening default-deny. |
| NET-02 | ADAPT-DOWN per ADR-113. Live path never exercised (no SPIRE agent, `spire.yml` never run) — a named residual, not silently closed. |
| CORE-02 | ADAPT-not-adopt per ADR-111 — flag surface aligned, no new enforcement. |
| SEC-02 | ADAPTED-with-scope-limit per ADR-114 — fork-native enforcement point built; capture works only for OAuth endpoints reachable as configured reverse-proxy routes. Mint→resolve loop closed at relay site 1 only. |
| SEC-06 | Adapted to the fork's `linux_child_requires_dumpable()` predicate (upstream's gate symbols absent). |

**Amended mid-milestone:** CORE-01 lost the `macos.rs` port-range emitter to PROF-03 (2026-07-30)
so commit `d5803b99` landed coherently in one phase.

**Requirement-record correction (audit ACC-01):** SEC-02's text originally claimed the
mint-to-resolve loop was "closed end-to-end." Corrected to name relay site 1 only, matching
`reverse.rs`.
