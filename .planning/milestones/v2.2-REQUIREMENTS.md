---
milestone: v2.2
milestone_name: Windows/macOS Parity Sweep
status: shipped
shipped: 2026-04-29
created: 2026-04-24
updated: 2026-04-29
---

# Requirements — v2.2 (ARCHIVED)

**Status:** ✅ SHIPPED 2026-04-29. All 21 requirements closed. Snapshot preserved below as the authoritative v2.2 acceptance record. For v2.3+ work, see the live `.planning/REQUIREMENTS.md` in the milestone root (created at v2.3 milestone start).

## Traceability (final)

| ID | Requirement | Phase | Status | Notes |
|----|-------------|-------|--------|-------|
| PROF-01 | `unsafe_macos_seatbelt_rules` deserializes on Windows | 22 (Plan 22-01) | ✅ Complete | commit d12b6535 (manual replay of upstream `14c644ce`) |
| PROF-02 | `packs` + `command_args` deserialize on Windows | 22 (Plan 22-01) | ✅ Complete | commit 5040411c |
| PROF-03 | `custom_credentials.oauth2` deserializes; HTTPS-required `token_url` | 22 (Plan 22-01) | ✅ Complete | commits bb79552a + 41ac5898 |
| PROF-04 | `claude-no-keychain` builtin profile | 22 (Plan 22-01) | ✅ Complete | commit 52d4ee49 — verified `nono policy show claude-no-kc` resolves 31 security groups |
| POLY-01 | Orphan `override_deny` fails closed at profile load | 22 (Plan 22-02) | ✅ Complete | commits 0d83a1e2 + a47c5962 — `NonoError::SandboxInit` + `.exists()` pre-filter cross-platform safety |
| POLY-02 | `--rollback` + `--no-audit` rejected at parse time | 22 (Plan 22-02) | ✅ Complete | commit 490a8a5c integration test; `clap conflicts_with` at `cli.rs:1602` was already in fork (CONTRADICTION-B confirmed); CL-01-M carve-out preserves `--no-audit-integrity` orthogonality |
| POLY-03 | `.claude.lock` moved to `allow_file` | 22 (Plan 22-02) | ✅ Complete | commit ef0facdc; `.claude.lock` in `filesystem.allow_file` for both `claude-code` and `claude-no-kc` |
| PKG-01 | `nono package pull/remove/search/list` (flat-shape `nono pull/remove/update/search/list`) on Windows | 22 (Plan 22-03) | ⚠️ Complete-partial | 6/8 cherry-picks landed; **PKG streaming follow-up deferred to v2.3** — needs `ArtifactType::Plugin` variant + `bundle_json` field + Rule-4 architectural call on `validate_path_within` belt-and-suspenders |
| PKG-02 | Windows `install_dir` resolution + long-path handling | 22 (Plan 22-03) | ✅ Complete | `nono remove` flat top-level subcommand; hook unregistration via fork's `hooks.rs` |
| PKG-03 | Hook registration/unregistration on Windows | 22 (Plan 22-03) | ✅ Complete | `nono pull` flat top-level + Claude-Code hook registration via fork's `hooks.rs` |
| PKG-04 | Signed-artifact streaming download | 22 (Plan 22-03) | ✅ Complete | `nono search` + `nono list` flat top-level subcommands; signed-artifact verification at install time |
| OAUTH-01 | `nono-proxy` client-credentials Bearer-token injection | 22 (Plan 22-04) | ✅ Complete | commit 5c8df06a; `OAuth2Config` + `nono-proxy/src/oauth2.rs` |
| OAUTH-02 | Reverse-proxy HTTP upstream loopback-only | 22 (Plan 22-04) | ✅ Complete | `custom_credentials.oauth2` parses in profile; PROF-04 `claude-no-kc` builtin verified |
| OAUTH-03 | `--allow-domain` preserved in strict proxy-only mode | 22 (Plan 22-04) | ✅ Complete | reverse-proxy HTTP upstream loopback-only by default + `--allow-domain` strict-proxy composition; CL-03-M literal `client_secret` warning + CL-04-M manifest-export skip + HG-01-M Debug redaction |
| AUD-01 | `--audit-integrity` hash-chained Merkle-rooted ledger | 22 (Plan 22-05a) | ✅ Complete | commit d15a3ab6; `audit-events.ndjson` per session; `AuditRecorder` lifecycle integrated into supervisor; pre/post-merkle roots captured |
| AUD-02 | `--audit-sign-key` DSSE/in-toto attestation + `nono audit verify` | 22 (Plan 22-05a) | ✅ Complete | `nono audit verify <id>` recomputes per-event leaves + chain head + Merkle root with fail-closed mismatch detection; `--public-key-file <PATH>` flag; HG-01-H upgrade landed cryptographic DSSE bundle verification (commit cffb43b1); 2 fixture-driven tests `#[ignore]`'d pending sigstore-rs `KeyPair::from_pkcs8` re-enablement (deferred to v2.3) |
| AUD-03 | Executable identity recorded on Windows (path + signature/hash) | 22 (Plan 22-05a SHA-256 + Plan 22-05b Authenticode) | ⚠️ Complete-partial | `executable_identity` SHA-256 portion shipped; on Windows, `WinVerifyTrust` records `Valid` / `Unsigned` / `InvalidSignature{hresult}` discriminant; **chain-walker subject extraction deferred to v2.3** — `windows-sys 0.59` does not expose `WTHelperProvDataFromStateData` without `Win32_Security_Cryptography_Catalog` + `Win32_Security_Cryptography_Sip` features |
| AUD-04 | `prune` → `session cleanup` rename preserves CLEAN-04 invariants | 22 (Plan 22-05b) | ✅ Complete | commit b5640cd4; v2.1 CLEAN-04 byte-identical preservation guaranteed by formal `applied_labels_guard::audit_flush_before_drop` 83 LOC regression test; 5 rename lifecycle items: new `nono session cleanup` + peer `nono audit cleanup` + hidden `nono prune` with stderr deprecation + ALL_SUBCOMMANDS test + `NONO_CAP_FILE` no-op preserved; live UAT 10/10 verified 2026-04-28 |
| AUD-05 | Windows supervisor emits ledger events for AIPC broker decisions | 23 (Plan 23-01) | ✅ Complete | 2026-04-29; commits 427e1283, a9307802, 263795a9; `RejectStage` enum + `reject_stage` field on `AuditEventPayload::CapabilityDecision`; dispatcher emits `capability_decision` ledger event at all 5 push sites with WR-01-locked stage; `nono audit show <id>` surfaces `Capability Decisions: N (M before-prompt, K after-prompt rejections)` counter + `capability_decisions` JSON array; layer-1 multi-kind E2E (`audit_integrity_records_5_handle_kinds_in_ledger`) covers AUD-05 acceptance #1 per plan's authorized Step-7 fallback (since `handle_windows_supervisor_message` is `pub(super)`); T-23-01 sanitization regression test asserts no raw session token bytes appear in NDJSON; `audit_flush_before_drop` invariant preserved; D-19/D-21/D-03 invariants hold across 3 commits |
| DRIFT-01 | `check-upstream-drift` reports cross-platform gap | 24 (Plan 24-01) | ✅ Complete | 2026-04-27; twin `.sh`+`.ps1` scripts, Makefile `$(OS)==Windows_NT` dispatch, integration test, 3 frozen golden JSON fixtures |
| DRIFT-02 | GSD quick-task template for upstream sync | 24 (Plan 24-02) | ✅ Complete | 2026-04-27; `.planning/templates/upstream-sync-quick.md` + PROJECT.md `## Upstream Parity Process` cross-link + `docs/cli/development/upstream-drift.mdx` long-form runbook |

**Coverage:**

- v2.2 requirements: 21 total
- Mapped to phases: 21
- Closed: 21 (19 fully complete + 2 complete-partial with explicit v2.3 deferrals: PKG-01, AUD-03)
- Unmapped / orphan: 0

---

# Original Requirements Specification (preserved for traceability)

The text below is the original v2.2 requirements specification authored 2026-04-24 at milestone scope-lock by gsd-roadmapper. It is preserved verbatim as the acceptance record. For closure status, see the Traceability table above.

---

# Requirements — v2.2 Windows/macOS Parity Sweep

**Defined:** 2026-04-24
**Core Value:** Every nono command that works on Linux/macOS must work on Windows with equivalent security guarantees.

**Context:** v2.1 Phase 20 synced the fork to upstream `always-further/nono` v0.37.1. Upstream has since shipped v0.38.0, v0.39.0, v0.40.0, and v0.40.1 — each release opens a new Windows-vs-macOS gap because the fork cannot take upstream PRs directly (see quick task 260424-upr SUMMARY.md for the 78-commit / 9k-LOC review). v2.2 closes the current gap and installs a drift-prevention mechanism so v0.41+ don't recreate the problem.

**Scope shape:** All requirements are framed Windows-first. Ports preserve upstream commit provenance via `Upstream-commit:` trailer per the v2.1 Phase 20 cherry-pick-per-commit pattern.

**Pre-milestone:** Merge `windows-squash` → `main` is a separate quick task (not a v2.2 phase). Must land before Phase 22 begins so cherry-picks target stable mainline.

---

## PROF — Profile Struct Alignment

Context: Upstream added 4 new profile fields in v0.38–v0.40 + 1 new built-in profile. Windows must deserialize every field without breaking cross-platform profile parse, apply the macOS-only escape-hatch field on macOS only, and ship the new built-in as a compiled-in option for Windows users.

### REQ-PROF-01 — Profile deserializes `unsafe_macos_seatbelt_rules` field

- **What:** Add `unsafe_macos_seatbelt_rules: Vec<String>` to `Profile` struct with `#[serde(default)]`. Field merges via `dedup_append` in profile inheritance. Rules applied via `add_platform_rule` on macOS only; Windows and Linux parse the field but emit no platform rule. Malformed S-expressions fail fast with a warning.
- **Maps to:** Upstream `14c644ce` + `e3decf9d` + `ecd09313`.

### REQ-PROF-02 — Profile deserializes `packs` + `command_args`

- **What:** Add `packs: Vec<PackRef>` and `command_args: Vec<String>` to `Profile` struct.
- **Maps to:** Upstream `088bdad7` + `115b5cfa`.

### REQ-PROF-03 — Profile `custom_credentials.oauth2` deserializes

- **What:** Extend `custom_credentials` in Profile to accept an `oauth2: OAuth2Config` block.
- **Maps to:** Upstream `fbf5c06e` + `b1ecbc02`.

### REQ-PROF-04 — `claude-no-keychain` built-in profile available

- **What:** Ship `claude-no-keychain` as a compiled-in built-in profile.
- **Maps to:** Upstream `3c8b6756` + `713b2e0f`.

---

## POLY — Policy Tightening

### REQ-POLY-01 — `override_deny` requires matching grant

- **What:** Orphan `override_deny` (no matching allow) returns a fail-closed error at profile load time.
- **Maps to:** Upstream `5c301e8d` + `b83da813` + `930d82b4`.

### REQ-POLY-02 — `--rollback` + `--no-audit` conflict rejected

- **What:** `clap` `conflicts_with` attribute at the CLI layer.
- **Maps to:** Upstream `5c301e8d`.

### REQ-POLY-03 — `.claude.lock` moved to `allow_file` (least-privilege)

- **What:** Default-profile rule granting access to `.claude.lock` moves from a broader allow-directory scope to a narrow `allow_file` entry.
- **Maps to:** Upstream `49925bbf` + `713b2e0f` + `a524b1a7` + `7d1d9a0d`.

---

## PKG — Package Manager + Packs

### REQ-PKG-01 — `nono package pull/remove/search/list` subcommands on Windows

- **What:** All four subcommands execute successfully on Windows. (Fork ships flat-shape `nono pull/remove/update/search/list` per upstream's actual subcommand registration.)
- **Maps to:** Upstream `8b46573d` + `71d82cd0` + `58b5a24e` + `9ebad89a`.

### REQ-PKG-02 — Windows `install_dir` path resolution

- **What:** `install_dir` on Windows resolves to `%LOCALAPPDATA%\nono\packages\<name>`, with long-path (`\\?\` prefix) handling.
- **Maps to:** Upstream `55fb42b8` + `ec49a7af`.

### REQ-PKG-03 — Hook registration/unregistration on Windows

- **What:** `nono package pull` registers hooks via the fork's existing Windows hook installer.
- **Maps to:** Upstream `55fb42b8` + `8b2a5ffb`.

### REQ-PKG-04 — Signed-artifact streaming download on Windows

- **What:** Artifact download uses streaming HTTP (chunked) through `hyper` + `rustls` + `rustls-webpki`.
- **Maps to:** Upstream `9ebad89a` + `600ba4ec` + `0cbb7e62`.

---

## OAUTH — OAuth2 Proxy Credential Injection

### REQ-OAUTH-01 — `nono-proxy` client-credentials token exchange with cache

- **Maps to:** Upstream `9546c879` + `fbf5c06e`.

### REQ-OAUTH-02 — Reverse-proxy HTTP upstream gated to local-only

- **Maps to:** Upstream `2bf5668f` + `0340ebff` + `b2a24402` + `0c990116`.

### REQ-OAUTH-03 — `--allow-domain` preserved in strict proxy-only mode

- **Maps to:** Upstream `10bcd054` + `60ad1eb3` + `005579a9` + `d44e404e`.

---

## AUD — Audit Integrity + Attestation

### REQ-AUD-01 — `--audit-integrity` hash-chained + Merkle-rooted ledger

- **Maps to:** Upstream `4f9552ec` + `4ec61c29`.

### REQ-AUD-02 — `--audit-sign-key` DSSE/in-toto attestation + `nono audit verify`

- **Maps to:** Upstream `6ecade2e` + `0b1822a9`.

### REQ-AUD-03 — Executable identity recorded on Windows

- **Maps to:** Upstream `02ee0bd1` + `7b7815f7`.

### REQ-AUD-04 — `prune` → `session cleanup` rename preserves CLEAN-04 invariants

- **Maps to:** Upstream `4f9552ec` (rename section).

### REQ-AUD-05 — Windows supervisor emits ledger events for AIPC broker decisions

- **What:** Windows supervisor emits capability-decision events to the audit ledger for ALL 5 AIPC broker paths.
- **Maps to:** Derived requirement (Windows-specific parity retrofit).

---

## DRIFT — Parity-Drift Prevention

### REQ-DRIFT-01 — `check-upstream-drift` reports cross-platform gap

- **What:** A script (PowerShell + Bash twin) that reports commits in `upstream/main..HEAD` touching cross-platform files.

### REQ-DRIFT-02 — GSD quick-task template for upstream sync

- **What:** A reusable template that scaffolds an upstream-sync quick task.

---

## Out of Scope (Explicit Deferrals at scope-lock)

| Item | Reason | Destination |
|------|--------|-------------|
| WR-01 reject-stage unification | Windows-internal consistency, not a Windows-vs-macOS gap | v2.3+ |
| AIPC G-04 wire-protocol compile-time tightening | Same — internal type-system hardening | v2.3+ |
| Cross-platform RESL Unix backends | Reverse-direction drift | v2.3+ |
| WR-02 EDR HUMAN-UAT | Requires EDR-instrumented runner | v3.0 |
| Upstream v0.41.0+ ingestion | Scope cap: v2.2 focuses on v0.38–v0.40 | v2.3 first quick task |
| `unsafe_macos_seatbelt_rules` runtime application on Windows | macOS-only by design | Out of scope forever |

---

*Requirements defined: 2026-04-24.*
*Archived: 2026-04-29 at v2.2 milestone close. See Traceability table above for closure status of each REQ-* row.*
