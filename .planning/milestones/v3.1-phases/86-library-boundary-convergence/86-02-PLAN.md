---
phase: 86-library-boundary-convergence
plan: 02
type: execute
wave: 2
depends_on:
  - 86-01
files_modified:
  - crates/nono/src/diagnostic.rs
  - crates/nono/src/diagnostic/mod.rs
  - crates/nono/src/diagnostic/codes.rs
  - crates/nono/src/diagnostic/observation.rs
  - crates/nono/src/diagnostic/records.rs
  - crates/nono/src/diagnostic/report.rs
  - crates/nono/src/diagnostic/detail.rs
  - crates/nono/src/error.rs
  - crates/nono/src/lib.rs
  - crates/nono-cli/src/diagnostic_formatter.rs
  - crates/nono-cli/src/diagnostic/mod.rs
  - crates/nono-cli/src/diagnostic/formatter.rs
  - crates/nono-cli/src/exec_strategy.rs
  - crates/nono-cli/src/exec_strategy/supervisor_linux.rs
  - crates/nono-cli/src/main.rs
  - crates/nono-cli/src/output.rs
  - crates/nono-cli/src/sandbox_log.rs
  - bindings/c/src/diagnostic.rs
  - bindings/c/src/lib.rs
  - bindings/c/src/types.rs
  - bindings/c/Cargo.toml
  - bindings/c/include/nono.h
  - crates/nono-proxy/src/diagnostic.rs
  - crates/nono-proxy/src/lib.rs
autonomous: true
requirements:
  - BND-02

must_haves:
  truths:
    - "crates/nono/src/diagnostic/ exists as a 6-file module directory (codes.rs, detail.rs, mod.rs, observation.rs, records.rs, report.rs)"
    - "crates/nono/src/diagnostic.rs (single file) is DELETED — replaced by the module directory"
    - "NonoError has diagnostic_code() and remediation() methods (in crates/nono/src/error.rs)"
    - "bindings/c/src/diagnostic.rs exists with 3 pub extern C fns (nono_last_diagnostic_code, nono_last_remediation_json, nono_session_diagnostic_report_to_json)"
    - "NonoDiagnosticCode repr-C enum exists in bindings/c/src/types.rs"
    - "crates/nono-proxy/src/diagnostic.rs exists (ProxyDiagnostic, ProxyDiagnosticCode, ProxyDiagnosticSeverity)"
    - "crates/nono-cli/src/diagnostic/formatter.rs exists (DiagnosticFormatter UX logic moved from core)"
    - "cargo clippy --workspace --all-targets exits 0 (NonoDiagnosticCode arms exhaustive)"
    - "Windows denial paths in exec_strategy_windows/ are UNCHANGED (D-02 preserve-and-bridge)"
  artifacts:
    - path: "crates/nono/src/diagnostic/mod.rs"
      provides: "diagnostic module root — pub use codes::*, detail::*, observation::*, records::*, report::*"
    - path: "crates/nono/src/diagnostic/codes.rs"
      provides: "NonoDiagnostic, suggested_flag_for_remediation (+188 LOC)"
    - path: "crates/nono/src/diagnostic/report.rs"
      provides: "SessionDiagnosticReport (+446 LOC)"
    - path: "bindings/c/src/diagnostic.rs"
      provides: "nono_last_diagnostic_code, nono_last_remediation_json, nono_session_diagnostic_report_to_json (3 pub extern C fns)"
    - path: "crates/nono-proxy/src/diagnostic.rs"
      provides: "ProxyDiagnostic, ProxyDiagnosticCode, ProxyDiagnosticSeverity (+99 LOC)"
    - path: "crates/nono-cli/src/diagnostic/formatter.rs"
      provides: "DiagnosticFormatter and all rendering UX moved from core"
  key_links:
    - from: "bindings/c/src/diagnostic.rs"
      to: "crates/nono::error::NonoError"
      via: "LAST_ERROR thread-local + diagnostic_code() / remediation() methods"
      pattern: "LAST_ERROR.*diagnostic_code"
    - from: "crates/nono/src/lib.rs"
      to: "crates/nono/src/diagnostic/"
      via: "pub mod diagnostic + pub use diagnostic::*"
      pattern: "pub mod diagnostic"
    - from: "crates/nono-proxy/src/lib.rs"
      to: "crates/nono-proxy/src/diagnostic.rs"
      via: "pub use diagnostic::{ProxyDiagnostic, ProxyDiagnosticCode, ProxyDiagnosticSeverity}"
      pattern: "pub use diagnostic::"
---

<objective>
Port Theme B (diagnostics → core + FFI): cherry-pick 4 upstream commits in LOCKED ORDER
`4ad8ba92` → `f867aba2` → `a6aa5995` → `7f319b9e`, resolving the heaviest conflict surface in
Phase 86. This replaces the fork's single-file core diagnostic.rs with a 6-file module directory,
moves DiagnosticFormatter UX to CLI, adds NonoError::{diagnostic_code, remediation} methods, adds
3 FFI extern-C fns, adds the proxy ProxyDiagnostic surface, and bridges Windows denial paths per
D-02 (zero code change in exec_strategy_windows/).

Purpose: BND-02 — structured-diagnostics model in core crate, FFI-exposed, reconciled with
Windows paths and proxy ProxyDiagnostic surface; no regression in Windows denial output (SC#3).

Output: diagnostic/ module dir in core, 3 FFI fns, NonoDiagnosticCode repr-C enum, ProxyDiagnostic
in proxy, DiagnosticFormatter in CLI diagnostic/ subdir, green --workspace --all-targets gate.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-library-boundary-convergence/86-CONTEXT.md
@.planning/phases/86-library-boundary-convergence/86-RESEARCH.md
@.planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md
@.planning/phases/86-library-boundary-convergence/86-01-SUMMARY.md
@.planning/templates/cross-target-verify-checklist.md

<interfaces>
<!-- Extracted from 86-RESEARCH.md at plan-time. Executor uses these directly. -->

Fork starting state for diagnostic.rs (verified at research time):
  crates/nono/src/diagnostic.rs — 3,872 lines (single file containing):
    STAYS IN CORE: DenialReason, DenialRecord, IpcDenialRecord, SandboxViolation, seatbelt_operation_to_access
    MOVES TO CLI:  DiagnosticFormatter, DiagnosticMode, analyze_error_output, ErrorObservation,
                   PolicyExplanation, ObservedPathHint, ErrorVerdict, CommandContext, all format_* methods,
                   render_diagnostic_block
    FORK-LOCAL:    try_canonicalize fn at lines 26-43 (comment: "Fork-local copy") — DELETE during 4ad8ba92
                   resolution; crates/nono/src/lib.rs already exports path::try_canonicalize.

Fork lib.rs re-exports to strip after 4ad8ba92 (86-RESEARCH.md Pitfall 2):
  REMOVE: CommandContext, DiagnosticFormatter, DiagnosticMode  (moved to CLI)
  KEEP:   DenialReason, DenialRecord, IpcDenialRecord, SandboxViolation
  ADD after a6aa5995: pub mod diagnostic + pub use diagnostic::*

Upstream module dir structure after a6aa5995 (from 86-RESEARCH.md):
  crates/nono/src/diagnostic/
    codes.rs        (+188 LOC — NonoDiagnostic, suggested_flag_for_remediation)
    detail.rs       (additional diagnostic detail types)
    mod.rs          (pub use codes::*, detail::*, observation::*, records::*, report::*)
    observation.rs  (+199 LOC — into_diagnostics, follow_up_diagnostics, diagnostic_* fns)
    records.rs      (+168 LOC — DenialRecord::new, seatbelt_operation_to_access)
    report.rs       (+446 LOC — SessionDiagnosticReport)

FFI thread-local pattern (from 86-RESEARCH.md FFI Exhaustive-Match Closure):
  bindings/c/src/lib.rs LAST_ERROR thread-local (lines 41-68) — already established
  diagnostic_code() and remediation() are METHODS on NonoError (not new variants)
  map_error exhaustive match is NOT broken by adding methods — only new NonoError variants break it
  3 new pub extern "C" fns in net-new bindings/c/src/diagnostic.rs:
    nono_last_diagnostic_code() -> NonoDiagnosticCode
    nono_last_remediation_json() -> *mut c_char
    pub unsafe extern "C" fn nono_session_diagnostic_report_to_json(...)
  Wire-up: bindings/c/src/lib.rs gains pub mod diagnostic + pub use diagnostic::*

Windows bridge contract (D-02, from 86-RESEARCH.md Windows Bridge Field-Mapping):
  exec_strategy_windows/{launch.rs,network.rs,mod.rs} — DO NOT MODIFY THESE FILES
  These files return NonoError variants (SandboxInit, Setup, UnsupportedPlatform)
  After a6aa5995, NonoError gains diagnostic_code() and remediation() methods automatically
  Windows denial paths already populate LAST_ERROR via the existing map_error path
  NO imports of nono::diagnostic::* should be added to exec_strategy_windows/ files

Proxy DiagnosticCode surface (from 86-RESEARCH.md):
  crates/nono-proxy/src/diagnostic.rs — net-new file, arrives wholesale with a6aa5995
  crates/nono-proxy/src/lib.rs — gains: pub use diagnostic::{ProxyDiagnostic, ProxyDiagnosticCode, ProxyDiagnosticSeverity}

print_proxy_diagnostics question (Q3 from planning_context):
  Run: grep -n "print_proxy_diagnostics" crates/nono-cli/src/output.rs
  BEFORE cherry-picking a6aa5995 to determine if the function already exists or arrives with the commit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cherry-pick 4ad8ba92 — move diagnostic UX out of core, create CLI diagnostic/ subdir</name>
  <files>
    crates/nono/src/diagnostic.rs,
    crates/nono/src/lib.rs,
    crates/nono-cli/src/diagnostic/mod.rs,
    crates/nono-cli/src/diagnostic/formatter.rs,
    crates/nono-cli/src/diagnostic_formatter.rs,
    crates/nono-cli/src/exec_strategy.rs,
    crates/nono-cli/src/main.rs,
    crates/nono-cli/src/profile_save_runtime.rs,
    crates/nono-cli/src/query_ext.rs
  </files>

  <read_first>
    1. Read the current crates/nono/src/lib.rs re-export block (lines 71-74) to understand the exact types to strip post-cherry-pick. The current list is: CommandContext, DenialReason, DenialRecord, DiagnosticFormatter, DiagnosticMode, IpcDenialRecord, SandboxViolation.
    2. Run `git show 4ad8ba92 -- crates/nono/src/lib.rs` — upstream's exact re-export diff after moving UX out.
    3. Run `git show 4ad8ba92 -- crates/nono/src/diagnostic.rs` — see exactly what stays in core (structural facts only).
    4. Run `git show 4ad8ba92 -- crates/nono-cli/src/` — see the new diagnostic/ directory files upstream creates and the importers it updates.
    5. Read 86-RESEARCH.md § "Theme B Module-Dir Conflict Detail" — especially the fork-local try_canonicalize note at lines 26-43 (Pitfall 4).
    6. Read 86-RESEARCH.md § "Pitfall 2: Single-File → Module-Dir Re-export Drift" — the exact types to remove from lib.rs re-exports.
  </read_first>

  <action>
    Step 1 — Cherry-pick:
    `git cherry-pick -x 4ad8ba92`

    Step 2 — Resolve the CRITICAL conflict on crates/nono/src/diagnostic.rs:
    The fork's diagnostic.rs is 3,872 lines. Upstream strips it to structural facts only.
    Resolution target: keep ONLY these types in crates/nono/src/diagnostic.rs after the cherry-pick:
      DenialReason, DenialRecord, IpcDenialRecord, SandboxViolation, seatbelt_operation_to_access
    REMOVE: DiagnosticFormatter, DiagnosticMode, ErrorObservation, PolicyExplanation,
            ObservedPathHint, ErrorVerdict, CommandContext, all format_* methods, render_diagnostic_block,
            analyze_error_output.
    CRITICAL: DELETE the fork-local try_canonicalize fn (lines 26-43 in current fork) — do not leave
    it in the stripped diagnostic.rs. The lib.rs already exports path::try_canonicalize.
    Use `git show 4ad8ba92 -- crates/nono/src/diagnostic.rs` as the authoritative end-state.

    Step 3 — Create the CLI diagnostic/ subdirectory:
    Upstream creates crates/nono-cli/src/diagnostic/mod.rs and diagnostic/formatter.rs.
    The fork has crates/nono-cli/src/diagnostic_formatter.rs (flat file, no subdirectory).
    Resolution:
      a. Create directory: mkdir crates/nono-cli/src/diagnostic/
      b. Create crates/nono-cli/src/diagnostic/formatter.rs — body from the fork's diagnostic_formatter.rs
         plus upstream's additions from `git show 4ad8ba92 -- crates/nono-cli/src/diagnostic/formatter.rs`.
      c. Create crates/nono-cli/src/diagnostic/mod.rs — use upstream's content from
         `git show 4ad8ba92 -- crates/nono-cli/src/diagnostic/mod.rs`.
      d. The old flat crates/nono-cli/src/diagnostic_formatter.rs should be removed
         (git rm crates/nono-cli/src/diagnostic_formatter.rs) since its content moved to formatter.rs.

    Step 4 — Resolve crates/nono/src/lib.rs conflict:
    Remove CommandContext, DiagnosticFormatter, DiagnosticMode from the pub use diagnostic::{...} list.
    After 4ad8ba92, the core lib.rs should re-export only: DenialReason, DenialRecord, IpcDenialRecord, SandboxViolation.
    Keep all fork-added module declarations (machine_policy, supervisor, trust, agent, scrub, net_filter, path, audit).

    Step 5 — Resolve importer updates (exec_strategy.rs, main.rs, profile_save_runtime.rs, query_ext.rs):
    These files update their imports from use nono::DiagnosticFormatter to use crate::diagnostic::formatter::DiagnosticFormatter (or the new module path). Use upstream's end-state for each via `git show 4ad8ba92 -- crates/nono-cli/src/<file>`.

    Step 6 — DCO sign-off: `git commit --amend -s --no-edit`

    Step 7 — Gate:
    `cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used`
    Fix any errors before proceeding. Pay special attention to:
      - "unresolved import nono::diagnostic::DiagnosticFormatter" (Pitfall 2)
      - "unresolved import nono::diagnostic::CommandContext" (Pitfall 2)
      - Any dead_code on the fork's diagnostic types in the CLI module
  </action>

  <verify>
    <automated>
      git log -1 --format='%B' | grep -q "cherry picked from commit" && echo OK || echo MISSING-TRACER
      test -f crates/nono-cli/src/diagnostic/mod.rs && echo OK || echo MISSING-CLI-DIAG-MOD
      test -f crates/nono-cli/src/diagnostic/formatter.rs && echo OK || echo MISSING-CLI-DIAG-FMT
      test ! -f crates/nono-cli/src/diagnostic_formatter.rs && echo OK || echo OLD-FLAT-FILE-STILL-EXISTS
      grep -v "^#" crates/nono/src/lib.rs | grep -q "DiagnosticFormatter" && echo PITFALL2-REEXPORT-STILL-PRESENT || echo OK
      cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used 2>&1 | tail -5
    </automated>
  </verify>

  <done>
    - crates/nono/src/diagnostic.rs is the stripped structural-facts-only file (DenialReason, DenialRecord, IpcDenialRecord, SandboxViolation)
    - Fork-local try_canonicalize fn is REMOVED from diagnostic.rs
    - crates/nono-cli/src/diagnostic/mod.rs and formatter.rs exist; old flat diagnostic_formatter.rs is gone
    - crates/nono/src/lib.rs re-exports only: DenialReason, DenialRecord, IpcDenialRecord, SandboxViolation from diagnostic (no DiagnosticFormatter, no CommandContext, no DiagnosticMode)
    - cargo clippy --workspace --all-targets exits 0
    - Commit carries upstream tracer and fork DCO sign-off
  </done>
</task>

<task type="auto">
  <name>Task 2: Cherry-pick f867aba2 — fix blocked-operation reporting</name>
  <files>
    crates/nono-cli/src/exec_strategy/supervisor_linux.rs,
    crates/nono-cli/src/sandbox_log.rs,
    crates/nono/src/diagnostic.rs
  </files>

  <read_first>
    1. Run `git show f867aba2 -- crates/nono/src/diagnostic.rs` — see the 3 files, 144+/24- behavioral fix. After 4ad8ba92, diagnostic.rs is the stripped structural-facts file; confirm the f867aba2 patch applies to the same types (DenialRecord::new or seatbelt_operation_to_access area). Note the exact symbol name (field or function) that the fix introduces — you will need it for the content-presence assertion in the verify step.
    2. Run `git show f867aba2 -- crates/nono-cli/src/exec_strategy/supervisor_linux.rs` — this is a Linux cfg-gated file. Note: the CLAUDE.md cross-target clippy MUST/NEVER rule requires cross-toolchain verification for this file; see the PARTIAL disposition note in the verification section.
    3. Read .planning/templates/cross-target-verify-checklist.md — understand the PARTIAL disposition procedure since this file is Linux cfg-gated and the dev host is Windows 11.
  </read_first>

  <action>
    Step 1 — Cherry-pick:
    `git cherry-pick -x f867aba2`

    Step 2 — Resolve conflicts on crates/nono/src/diagnostic.rs:
    The patch is a behavioral fix (report blocked operation, not readable target path) — 144+/24-.
    After 4ad8ba92, the fork's diagnostic.rs is the stripped structural-facts version.
    The upstream patch may target DenialRecord fields or seatbelt_operation_to_access.
    Resolution target: apply upstream's fix to the stripped diagnostic.rs. Use
    `git show f867aba2 -- crates/nono/src/diagnostic.rs` as the exact diff to apply.

    Step 3 — Resolve conflicts on crates/nono-cli/src/sandbox_log.rs:
    This is a shared surface. Use `git show f867aba2 -- crates/nono-cli/src/sandbox_log.rs`
    as the answer key and resolve toward upstream's end-state.

    Step 4 — Resolve conflicts on crates/nono-cli/src/exec_strategy/supervisor_linux.rs:
    This is Linux cfg-gated code. Resolve toward upstream's end-state using
    `git show f867aba2 -- crates/nono-cli/src/exec_strategy/supervisor_linux.rs`.
    Do NOT add any unwrap() or expect() — use the ? operator or map_err per CLAUDE.md.

    Step 5 — DCO sign-off: `git commit --amend -s --no-edit`

    Step 6 — Windows-host gate (best effort):
    `cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used`
    Note: This does NOT exercise supervisor_linux.rs on Windows host (Linux cfg-gated branch
    excluded). Cross-target verification for this file is marked PARTIAL — see threat model
    and verification section for the live-CI deferral note.
  </action>

  <verify>
    <automated>
      git log -1 --format='%B' | grep -q "cherry picked from commit" && echo OK || echo MISSING-TRACER
      git show f867aba2 -- crates/nono/src/diagnostic.rs | grep "^+" | grep -v "^+++" | head -5
      grep -n "blocked_operation\|blocked_op\|operation_blocked\|DenialRecord" crates/nono/src/diagnostic.rs | head -10
      cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used 2>&1 | tail -5
    </automated>
    <human-check>
      CONTENT ASSERTION: The read_first step (git show f867aba2 -- crates/nono/src/diagnostic.rs)
      reveals the exact symbol/field that the blocked-operation fix introduces. Verify that symbol
      is present in the current crates/nono/src/diagnostic.rs by running:
        grep -n "<symbol-from-read-first>" crates/nono/src/diagnostic.rs
      If the symbol is not found, the cherry-pick conflict resolution dropped the fix — reapply
      the relevant hunks from `git show f867aba2 -- crates/nono/src/diagnostic.rs` manually.

      CROSS-TARGET PARTIAL: supervisor_linux.rs is #[cfg(target_os = "linux")] gated.
      Windows-host clippy does NOT exercise this branch.
      Defer to live CI (GitHub Actions Linux Clippy lane) per .planning/templates/cross-target-verify-checklist.md.
      Mark BND-02 PARTIAL for cross-target verification pending CI confirmation.
      Document in SUMMARY.md: "T-86B-05 cross-target clippy SKIPPED on Windows dev host due to
      missing toolchain. Live GH Actions Linux Clippy lane is the decisive signal per
      cross-target-verify-checklist.md. BND-02 marked PARTIAL pending CI confirmation."
    </human-check>
  </verify>

  <done>
    - f867aba2 cherry-pick applied with upstream tracer and fork DCO sign-off
    - Blocked-operation reporting fix lands in crates/nono/src/diagnostic.rs (DenialRecord or seatbelt_operation_to_access area); the specific symbol introduced by the fix is present in the file (confirmed via grep)
    - cargo clippy --workspace --all-targets exits 0 on Windows host
    - Cross-target clippy for supervisor_linux.rs deferred to CI (PARTIAL disposition documented in SUMMARY.md)
  </done>
</task>

<task type="auto">
  <name>Task 3: Cherry-pick a6aa5995 — structured diagnostics, FFI surface, proxy ProxyDiagnostic</name>
  <files>
    crates/nono/src/diagnostic/,
    crates/nono/src/error.rs,
    crates/nono/src/lib.rs,
    bindings/c/src/diagnostic.rs,
    bindings/c/src/lib.rs,
    bindings/c/src/types.rs,
    bindings/c/Cargo.toml,
    bindings/c/include/nono.h,
    crates/nono-cli/src/diagnostic/formatter.rs,
    crates/nono-proxy/src/diagnostic.rs,
    crates/nono-proxy/src/lib.rs
  </files>

  <read_first>
    1. Run `git show a6aa5995 -- crates/nono/src/error.rs` BEFORE cherry-picking to answer Q2 from planning_context: are diagnostic_code and remediation methods or new NonoError enum variants? This is the single most important pre-step for the FFI exhaustive-match closure.
       - If METHODS (impl block additions): map_error exhaustive match is NOT broken; proceed.
       - If NEW VARIANTS: immediately plan 2 new arms in bindings/c/src/lib.rs map_error after cherry-picking.
    2. Run `grep -n "print_proxy_diagnostics" crates/nono-cli/src/output.rs` to answer Q3: does this fn already exist or does it arrive with a6aa5995?
    3. Run `git show a6aa5995 -- bindings/c/src/` to see the exact 3 pub extern C fn signatures and the NonoDiagnosticCode enum variants for types.rs.
    4. Run `git show a6aa5995 -- crates/nono/src/diagnostic/` to see the 6-file module directory structure that replaces diagnostic.rs.
    5. Read 86-RESEARCH.md § "FFI Exhaustive-Match Closure" — the current map_error covers 28 variant patterns; confirm A1 assumption after reading error.rs diff.
    6. Read 86-RESEARCH.md § "Pitfall 1: --bin nono Gate Scope" — internalize why --workspace --all-targets is mandatory after this commit.
    7. Read 86-RESEARCH.md § "Windows Bridge Field-Mapping" — confirm exec_strategy_windows/ files need ZERO changes.
  </read_first>

  <action>
    Step 1 — Pre-cherry-pick invariant checks (do not skip):
    a. `git show a6aa5995 -- crates/nono/src/error.rs` → verify diagnostic_code/remediation shape.
    b. `grep -n "print_proxy_diagnostics" crates/nono-cli/src/output.rs` → note result.
    c. `git show a6aa5995 -- bindings/c/src/types.rs` → get NonoDiagnosticCode enum variant list.
    Record findings for use in conflict resolution below.

    Step 2 — Delete the single-file diagnostic.rs and create the module directory:
    Before cherry-picking, the fork has crates/nono/src/diagnostic.rs (stripped, after 4ad8ba92).
    a6aa5995 REPLACES it with a module directory. The cherry-pick may handle this automatically
    (git treats file delete + directory create as separate operations), or it may fail.
    If the cherry-pick fails because diagnostic.rs exists as a file:
      `git rm crates/nono/src/diagnostic.rs` first, then continue resolving.
    The module directory will be created by the cherry-pick's file-CREATE operations.

    Step 3 — Cherry-pick:
    `git cherry-pick -x a6aa5995`

    Step 4 — Resolve CRITICAL conflicts:

    a. crates/nono/src/diagnostic/ (CREATE 6 files):
       Use `git show a6aa5995 -- crates/nono/src/diagnostic/<file>` for each of the 6 files.
       Apply wholesale — these are net-new to the fork (no content conflict, but the cherry-pick
       must create each file). If any file fails to create, manually write it from `git show`.

    b. crates/nono/src/error.rs:
       Add diagnostic_code() and remediation() methods to the NonoError impl block.
       Use upstream's exact implementation from `git show a6aa5995 -- crates/nono/src/error.rs`.
       If they are methods (A1 confirmed): place in an impl NonoError block after the existing variants.
       If they are NEW VARIANTS (A1 falsified): add them to the enum AND add arms in bindings/c/src/lib.rs map_error.

    c. crates/nono/src/lib.rs:
       Replace the existing `pub use diagnostic::{DenialReason, ...}` explicit list with
       `pub mod diagnostic; pub use diagnostic::*;` per upstream's post-a6aa5995 shape.
       Keep all fork-added module declarations.

    d. bindings/c/src/types.rs — add NonoDiagnosticCode repr-C enum:
       Apply wholesale from `git show a6aa5995 -- bindings/c/src/types.rs`.
       The existing NonoErrorCode enum is unchanged — NonoDiagnosticCode is a NEW separate enum.

    e. bindings/c/src/diagnostic.rs — CREATE (net-new, no conflict):
       Apply wholesale from `git show a6aa5995 -- bindings/c/src/diagnostic.rs`.
       Verify the 3 pub extern "C" fn signatures:
         pub extern "C" fn nono_last_diagnostic_code() -> NonoDiagnosticCode
         pub extern "C" fn nono_last_remediation_json() -> *mut c_char
         pub unsafe extern "C" fn nono_session_diagnostic_report_to_json(...)
       All SAFETY: comments must be present per CLAUDE.md § Unsafe Code.

    f. bindings/c/src/lib.rs — wire diagnostic module:
       Add `pub mod diagnostic; pub use diagnostic::*;`
       Do NOT change the map_error match (unless A1 is falsified and new variants exist).

    g. crates/nono-proxy/src/diagnostic.rs — CREATE (net-new, no conflict):
       Apply wholesale from `git show a6aa5995 -- crates/nono-proxy/src/diagnostic.rs`.
       Contains ProxyDiagnostic::{ warning, with_credential_ref, with_hint },
       ProxyDiagnosticCode::as_str, ProxyDiagnosticSeverity.

    h. crates/nono-proxy/src/lib.rs — add re-export:
       Add: `pub use diagnostic::{ProxyDiagnostic, ProxyDiagnosticCode, ProxyDiagnosticSeverity};`

    i. crates/nono-cli/src/diagnostic/formatter.rs — add suggested_flag_for_remediation:
       Apply upstream's addition from `git show a6aa5995 -- crates/nono-cli/src/diagnostic/formatter.rs`.
       This adds the fn `suggested_flag_for_remediation(rem: &nono::NonoRemediation)` to the formatter.

    j. output.rs — print_proxy_diagnostics:
       If grep from Step 1 showed fn is MISSING: apply from `git show a6aa5995 -- crates/nono-cli/src/output.rs`.
       If it ALREADY EXISTS: no action needed; the cherry-pick may update it — resolve toward upstream's version.

    k. D-02 Windows bridge — VERIFY NO CHANGES to exec_strategy_windows/:
       After all resolutions: `git diff HEAD -- crates/nono-cli/src/exec_strategy_windows/`
       This must show ZERO changes. Windows files gain diagnostic_code()/remediation() methods
       automatically because they already return NonoError variants; no code change needed.

    Step 5 — bindings/c/Cargo.toml and nono.h:
    Apply upstream's changes. The Cargo.toml change is expected to be a version bump or feature
    flag (not a new dependency — assumption A2). Verify: `git show a6aa5995 -- bindings/c/Cargo.toml`
    should show no new [dependencies] entries. nono.h is regenerated by cbindgen build.rs — accept
    upstream's version; do NOT hand-edit it.

    Step 6 — DCO sign-off: `git commit --amend -s --no-edit`

    Step 7 — CRITICAL gate (D-05):
    `cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used`
    This is the non-negotiable gate for NonoDiagnosticCode exhaustive-match closure.
    Fix ALL errors before proceeding. Common failures:
      - E0004 non-exhaustive match on NonoDiagnosticCode → add missing arms
      - E0432 unresolved import of old diagnostic types → strip from lib.rs re-exports
      - Missing SAFETY comments on extern "C" fns → add per CLAUDE.md
  </action>

  <verify>
    <automated>
      git log -1 --format='%B' | grep -q "cherry picked from commit" && echo OK || echo MISSING-TRACER
      test -f bindings/c/src/diagnostic.rs && echo OK || echo MISSING-FFI-DIAGNOSTIC
      test -f crates/nono-proxy/src/diagnostic.rs && echo OK || echo MISSING-PROXY-DIAGNOSTIC
      test -d crates/nono/src/diagnostic && echo OK || echo MISSING-DIAGNOSTIC-DIR
      test ! -f crates/nono/src/diagnostic.rs && echo OK || echo OLD-SINGLE-FILE-STILL-EXISTS
      grep -q "NonoDiagnosticCode" bindings/c/src/types.rs && echo OK || echo MISSING-REPR-C-ENUM
      grep -q "nono_last_diagnostic_code" bindings/c/src/diagnostic.rs && echo OK || echo MISSING-FFI-FN
      grep -q "nono_last_remediation_json" bindings/c/src/diagnostic.rs && echo OK || echo MISSING-FFI-FN2
      grep -q "nono_session_diagnostic_report_to_json" bindings/c/src/diagnostic.rs && echo OK || echo MISSING-FFI-FN3
      git diff HEAD -- crates/nono-cli/src/exec_strategy_windows/ | wc -l | grep -q "^0$" && echo D02-PRESERVED || echo D02-VIOLATED
      cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used 2>&1 | tail -10
      cargo test -p nono-proxy -- diagnostic 2>&1 | tail -5
    </automated>
  </verify>

  <done>
    - crates/nono/src/diagnostic/ exists as 6-file module directory; crates/nono/src/diagnostic.rs (single file) is gone
    - NonoError has diagnostic_code() and remediation() methods in error.rs
    - bindings/c/src/diagnostic.rs has 3 pub extern C fns with SAFETY comments
    - NonoDiagnosticCode repr-C enum exists in bindings/c/src/types.rs
    - crates/nono-proxy/src/diagnostic.rs has ProxyDiagnostic, ProxyDiagnosticCode, ProxyDiagnosticSeverity
    - exec_strategy_windows/ files are UNCHANGED (git diff shows 0 lines changed) — D-02 preserved
    - cargo clippy --workspace --all-targets exits 0 — D-05 gate satisfied for FFI exhaustive-match
    - Commit carries upstream tracer and fork DCO sign-off
  </done>
</task>

<task type="auto">
  <name>Task 4: Cherry-pick 7f319b9e and run full Theme B gate</name>
  <files>
    crates/nono-cli/src/diagnostic/formatter.rs
  </files>

  <read_first>
    1. Run `git show 7f319b9e -- crates/nono-cli/src/diagnostic/formatter.rs` to see the exact string replacement (8+/6- — "nono learn" → "nono run").
    2. Confirm the string "nono learn" exists in the fork's formatter.rs post-4ad8ba92: `grep -n "nono learn" crates/nono-cli/src/diagnostic/formatter.rs`.
       If the string is NOT in formatter.rs (because the fork's flat diagnostic_formatter.rs used a different phrase): apply the patch manually using upstream's exact replacement.
  </read_first>

  <action>
    Step 1 — Verify target string exists:
    `grep -n "nono learn" crates/nono-cli/src/diagnostic/formatter.rs`
    If missing: check crates/nono-cli/src/diagnostic/formatter.rs for equivalent output string and apply manually.

    Step 2 — Cherry-pick:
    `git cherry-pick -x 7f319b9e`
    If apply fails (because flat-file path differs from module path): manually apply the 8+/6- string replacement in formatter.rs. The replacement is: change occurrences of "nono learn --" to "nono run --" (or equivalent per upstream diff).

    Step 3 — DCO sign-off: `git commit --amend -s --no-edit`

    Step 4 — Full Theme B gate:
    `cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used`
    `make ci`
    `cargo test -p nono -- diagnostic_code 2>&1`
    `cargo test -p nono-proxy -- diagnostic 2>&1`
    Verify the replacement: `grep -n "nono run" crates/nono-cli/src/diagnostic/formatter.rs | head -5`
    Verify NO nono-learn reference remains: `grep -rn "nono learn" crates/nono-cli/src/diagnostic/ | wc -l`
  </action>

  <verify>
    <automated>
      git log -1 --format='%B' | grep -q "cherry picked from commit" && echo OK || echo MISSING-TRACER
      grep -q "nono run" crates/nono-cli/src/diagnostic/formatter.rs && echo OK || echo MISSING-RUN-STRING
      grep -rn "nono learn --" crates/nono-cli/src/diagnostic/ | wc -l | grep -q "^0$" && echo OK || echo STALE-LEARN-STRING
      git log --format='%B' -8 | grep -c "cherry picked from commit" | grep -q "^8$" && echo ALL-8-PICKS-LANDED || echo MISSING-PICKS
      cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used 2>&1 | tail -5
      make ci 2>&1 | tail -10
    </automated>
  </verify>

  <done>
    - 7f319b9e cherry-pick applied: "nono learn" replaced with "nono run" in formatter.rs
    - All 8 Theme A + B cherry-pick commits exist in git log with upstream tracers and fork DCO sign-offs
    - make ci exits 0 (Theme B full gate)
    - cargo clippy --workspace --all-targets exits 0
    - cargo test -p nono -- diagnostic_code passes
    - cargo test -p nono-proxy -- diagnostic passes
    - git diff HEAD -- crates/nono-cli/src/exec_strategy_windows/ shows 0 lines changed (D-02 bridge intact)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FFI C boundary (nono_last_remediation_json, nono_session_diagnostic_report_to_json) | Raw *mut c_char returned to C callers; ownership must be explicit; caller owns returned pointer and must call nono_free_string |
| Diagnostic string construction | Attacker-controlled file paths may appear in DenialRecord/SandboxViolation; sanitize_for_diagnostic fn must move with DiagnosticFormatter to CLI (not stay in core) |
| Windows denial rendering | exec_strategy_windows/ files must not import nono::diagnostic::* types — bridge is via NonoError method surface only (D-02) |
| Core library → proxy boundary | ProxyDiagnostic surface in nono-proxy/src/diagnostic.rs is additive; no privilege boundary crossed; ProxyDiagnosticCode string values must not leak internal proxy credential state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86B-01 | Information Disclosure | nono_last_remediation_json FFI fn | mitigate | Verify remediation JSON output does not include credential values or internal paths; SAFETY comment required per CLAUDE.md; null return on no-error is correct and must be tested |
| T-86B-02 | Information Disclosure | nono_session_diagnostic_report_to_json FFI fn | mitigate | Same as T-86B-01; session report must not include secrets from LAST_ERROR; verify SAFETY comment and null-termination per established FFI pattern |
| T-86B-03 | Tampering | diagnostic_code/remediation methods on NonoError | mitigate | Methods are read-only (return Option<T>); no mutation possible; accept |
| T-86B-04 | Tampering | FFI exhaustive-match on NonoDiagnosticCode | mitigate | cargo clippy --workspace --all-targets enforces E0004; no --bin nono scope (D-05 non-negotiable); any missing arm is a compile error |
| T-86B-05 | Tampering | supervisor_linux.rs cfg-gated Linux code (f867aba2) | mitigate / PARTIAL | Windows-host clippy does NOT exercise this branch; mark BND-02 PARTIAL; defer to live CI per .planning/templates/cross-target-verify-checklist.md; document in SUMMARY.md |
| T-86B-06 | Elevation of Privilege | exec_strategy_windows/ Windows bridge | mitigate | D-02 invariant: zero code changes in exec_strategy_windows/; verified by git diff HEAD -- exec_strategy_windows/ showing 0 lines changed; any non-zero diff is a plan violation |
| T-86B-07 | Information Disclosure | sanitize_for_diagnostic path in formatter | mitigate | Verify sanitize_for_diagnostic fn moves WITH DiagnosticFormatter into crates/nono-cli/src/diagnostic/formatter.rs (not left in stripped core diagnostic.rs); attacker-controlled paths only processed in CLI, not returned via FFI |
| T-86B-SC | Tampering | cherry-pick provenance | mitigate | -x flag adds upstream SHA tracer; git commit --amend -s appends fork DCO; both verified in acceptance criteria per Pitfall 6 in 86-RESEARCH.md |
</threat_model>

<verification>
Full Theme A + B gate (run after all 4 tasks complete):
  make ci
  cargo test -p nono -- diagnostic_code
  cargo test -p nono -- audit
  cargo test -p nono-proxy -- diagnostic
  git log --format='%B' -8 | grep -c "cherry picked from commit"   # expect 8
  git diff HEAD -- crates/nono-cli/src/exec_strategy_windows/  # expect empty diff (D-02)
  test -d crates/nono/src/diagnostic && echo OK
  test ! -f crates/nono/src/diagnostic.rs && echo OK           # single file gone
  grep -q "nono_last_diagnostic_code" bindings/c/src/diagnostic.rs && echo OK
  wc -l crates/nono-cli/src/audit_integrity.rs   # thin wrapper expected < 150 LOC

Cross-target note for f867aba2 (supervisor_linux.rs):
  This file is #[cfg(target_os = "linux")] gated.
  Windows-host clippy DOES NOT exercise it — per CLAUDE.md MUST/NEVER rule and
  .planning/templates/cross-target-verify-checklist.md.
  BND-02 MUST be marked PARTIAL with the following note in SUMMARY.md:
  "Cross-target clippy gate SKIPPED on Windows dev host due to missing toolchain
  (x86_64-unknown-linux-gnu). The live GH Actions Linux Clippy lane on the head SHA is the
  decisive signal per .planning/templates/cross-target-verify-checklist.md.
  BND-02 marked PARTIAL pending CI confirmation."

bindings/c/src/ also falls under the cross-target checklist scope (FFI consumed by Linux/macOS).
  The NonoDiagnosticCode exhaustive-match CHECK is enforced by cargo clippy --workspace --all-targets
  which runs on Windows host (cdylib/staticlib targets are compiled); the Linux cfg-gated ARMS
  within map_error (#[cfg(target_os = "linux")] Landlock/LandlockPath) are the PARTIAL scope.
</verification>

<success_criteria>
1. crates/nono/src/diagnostic/ is a 6-file module directory; crates/nono/src/diagnostic.rs (single file) is deleted — BND-02 core structure.
2. NonoError has diagnostic_code() and remediation() methods; 3 pub extern "C" fns exist in bindings/c/src/diagnostic.rs with SAFETY comments; NonoDiagnosticCode repr-C enum is in bindings/c/src/types.rs — BND-02 FFI surface.
3. git diff HEAD -- crates/nono-cli/src/exec_strategy_windows/ shows 0 lines changed — D-02 preserve-and-bridge intact.
4. crates/nono-proxy/src/diagnostic.rs exists with ProxyDiagnostic, ProxyDiagnosticCode, ProxyDiagnosticSeverity — BND-02 proxy surface reconciled.
5. cargo clippy --workspace --all-targets exits 0 — D-05 FFI exhaustive-match gate satisfied.
6. make ci exits 0 — Theme B full gate.
7. BND-02 marked PARTIAL for supervisor_linux.rs cross-target clippy (documented in SUMMARY.md with live-CI deferral note per cross-target-verify-checklist.md).
8. All 8 cherry-pick commits (4 Theme A + 4 Theme B) carry both upstream tracer and fork DCO sign-off.
</success_criteria>

<output>
Create .planning/phases/86-library-boundary-convergence/86-02-SUMMARY.md when done.
</output>
