---
phase: 89-proxy-hardening-sync
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono-proxy/src/connect.rs
  - crates/nono-proxy/src/reverse.rs
autonomous: true
requirements: [PROXY-01, PROXY-02]
must_haves:
  truths:
    - "On CONNECT with missing/invalid Proxy-Authorization to a filter-allowed host, the fork keeps the connection open (does NOT send 407, does NOT reject on auth) — #1151 / D-02 equivalence proven by test"
    - "A denied non-CONNECT reverse-proxy request returns HTTP/1.1 403 and emits exactly one audit Deny event with denial_category == EndpointPolicy — #1077 / D-09 equivalence proven by test"
  artifacts:
    - path: "crates/nono-proxy/src/connect.rs"
      provides: "D-02 keep-open equivalence test driving handle_connect with no Proxy-Authorization"
      contains: "handle_connect"
    - path: "crates/nono-proxy/src/reverse.rs"
      provides: "D-09 403+audit equivalence test driving handle_reverse_proxy against an endpoint-deny route"
      contains: "EndpointPolicy"
  key_links:
    - from: "crates/nono-proxy/src/connect.rs test"
      to: "connect.rs:43-48 lenient undici-compat auth (logs at debug, continues)"
      via: "tokio::io::duplex CONNECT with no Proxy-Authorization"
      pattern: "handle_connect"
    - from: "crates/nono-proxy/src/reverse.rs test"
      to: "reverse.rs:96-115 endpoint default-deny → log_denied(EndpointPolicy) + 403"
      via: "drain_audit_events assertion"
      pattern: "NetworkAuditDenialCategory::EndpointPolicy"
---

<objective>
Prove two Cluster F equivalences with fork tests (no code change): #1151 / D-02 (CONNECT keeps
the connection open on missing auth) and #1077 / D-09 (denied non-CONNECT returns 403 + audit).

Purpose: The fork's CONNECT auth is lenient-by-design (undici compat) — missing
`Proxy-Authorization` logs at debug and CONTINUES, which IS #1151's keep-open intent. The fork's
reverse proxy already returns 403 + `audit::log_denied(EndpointPolicy)` on endpoint default-deny
before any credential op, which IS #1077's intent. Per the phase's default-to-no-op method (D-01),
each is proven with a fork test, recorded as equivalence in the 89-04 ledger addendum, and the
cherry-picks are skipped. Port behavioral intent ONLY if a test exposes a real gap.

Output: One new `#[tokio::test]` in connect.rs (D-02) + one async test + harness imports in
reverse.rs (D-09).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-proxy-hardening-sync/89-CONTEXT.md
@.planning/phases/89-proxy-hardening-sync/89-RESEARCH.md
@.planning/phases/89-proxy-hardening-sync/89-PATTERNS.md
@crates/nono-proxy/src/connect.rs
@crates/nono-proxy/src/reverse.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-02 keep-open equivalence test (direct CONNECT, no Proxy-Authorization)</name>
  <files>crates/nono-proxy/src/connect.rs</files>
  <read_first>
    - crates/nono-proxy/src/connect.rs — `handle_connect` + the lenient auth block at ~lines 43-48 (`validate_proxy_auth` error → `debug!("CONNECT auth skipped: {}", e)` and CONTINUE, no return/407); the existing `#[cfg(test)] #[allow(clippy::unwrap_used)] mod tests` (~223-225) and the `write_upstream_failure_*` test family (~287-417) with the `duplex` + `read_to_string` helper (~277-285) — REUSE these, no new imports
    - 89-PATTERNS.md lines 188-204 — the D-02 test scenario, the "target the DIRECT path not the bypass arm" rule, and the failure-mode assertion (UpstreamConnect/HostDenied, never an auth rejection/407)
    - 89-RESEARCH.md lines 289-317 — D-02 finding, the bypass-arm strict-auth caveat (Pitfall 3: do NOT target server.rs:572-586), and the keep-open test scenario
  </read_first>
  <behavior>
    - Test (`connect_keeps_open_on_missing_proxy_auth`): drive `handle_connect` via `tokio::io::duplex` with a CONNECT request line to a filter-allowed host and NO `Proxy-Authorization` header. Assert the handler does NOT reject on missing auth and does NOT send a 407: any error returned must be an upstream/host error (e.g. UpstreamConnect / HostDenied), never an authentication rejection. The connection-open observation IS the #1151 equivalence.
  </behavior>
  <action>
    Add one `#[tokio::test]` `connect_keeps_open_on_missing_proxy_auth` inside the existing
    connect.rs test module. Reuse the in-module `duplex` + `read_to_string` helper (~277-285) and
    the existing test setup conventions from `write_upstream_failure_*` (~287-417) — no new imports.
    Construct a CONNECT request line for a host the filter ALLOWS, with NO `Proxy-Authorization`
    header. Drive `handle_connect`. Assert that the outcome is NOT an auth rejection / 407 — i.e.
    either the handler proceeds (and any error is `UpstreamConnect`/`HostDenied` because there is no
    real upstream in a unit context), or the response written to the client does NOT start with a
    407 / proxy-auth-required status. Target the DIRECT `connect::handle_connect` path ONLY — do NOT
    exercise the external-proxy bypass arm (`server.rs:572-586`), which is deliberately strict
    (Pitfall 3). This is equivalence-only: record the result, do NOT modify `connect.rs` auth logic.
    If — against expectation — the handler DROPS on missing auth, port keep-open into connect.rs
    (D-02 fallback) and record it as a deliberate fork-divergence with this test as its guard.
  </action>
  <verify>
    <automated>cargo test -p nono-proxy connect_keeps_open_on_missing_proxy_auth</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p nono-proxy connect_keeps_open_on_missing_proxy_auth` exits 0.
    - Behavior assertion: the test asserts the failure (if any) is NOT an authentication rejection and the client did NOT receive a 407 (proxy-auth-required) status.
    - The test drives `handle_connect` directly (grep confirms `handle_connect` is the call under test), NOT the server.rs bypass arm.
    - No change to `connect.rs` non-test code (equivalence-only) — unless the test reproduced a drop, in which case the keep-open port is recorded as deliberate fork-divergence.
  </acceptance_criteria>
  <done>The D-02 keep-open equivalence test passes against the direct CONNECT path; test name captured for the 89-04 ledger addendum.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-09 403+audit equivalence test (denied non-CONNECT → 403 + EndpointPolicy audit)</name>
  <files>crates/nono-proxy/src/reverse.rs</files>
  <read_first>
    - crates/nono-proxy/src/reverse.rs — the endpoint default-deny block at ~lines 96-115 (`endpoint_rules.is_allowed` false → `audit::log_denied(.., EndpointPolicy, ..)` then `send_error(stream, 403, "Forbidden")`, BEFORE any credential op); the `ReverseProxyCtx<'_>` struct (~43-56: route_store, credential_store, session_token, filter, tls_connector, audit_log); `handle_reverse_proxy` signature; `parse_service_prefix` (~396-404); the existing `#[cfg(test)] #[allow(clippy::unwrap_used)] mod tests` (~945-947)
    - crates/nono-proxy/src/connect.rs lines 276-337 — the `tokio::io::duplex` + `read_to_string` + `audit::drain_audit_events` harness to COPY into reverse.rs's test module (these async harness imports are NEW to reverse.rs); confirmed audit-event field names (`mode`, `decision`, `denial_category`, `target`, `port`, `reason`) at connect.rs:328-336
    - 89-PATTERNS.md lines 145-185 — the D-09 test pattern, harness imports to add, the assertion shape, and the HARNESS CAVEAT (handle_reverse_proxy may take a real `&mut TcpStream` not a duplex half; if so use a `tokio::net::TcpListener::bind("127.0.0.1:0")` loopback pair rather than refactoring the handler signature)
    - 89-RESEARCH.md lines 246-271 — D-09 finding (already-present), the exact code path, and the duplex/drain_audit_events test assertion
  </read_first>
  <behavior>
    - Test (`denied_endpoint_returns_403_and_audit`): drive `handle_reverse_proxy` with a route whose `endpoint_rules` deny the request's method+path. Assert: (1) the client receives a response starting with `HTTP/1.1 403`; (2) `audit::drain_audit_events(&log)` yields one event with `decision == NetworkAuditDecision::Deny` and `denial_category == Some(NetworkAuditDenialCategory::EndpointPolicy)`.
  </behavior>
  <action>
    Add one `#[tokio::test]` `denied_endpoint_returns_403_and_audit` inside reverse.rs's existing
    test module. Add the async harness imports COPIED from connect.rs (~276-277):
    `use tokio::io::{duplex, AsyncReadExt};` `use nono::undo::{NetworkAuditDecision, NetworkAuditDenialCategory, NetworkAuditMode};`
    and the `read_to_string` helper. Build a `ReverseProxyCtx` with a `RouteStore` containing one
    route whose `endpoint_rules` DENY the test request's method+path (e.g. a route allowing only
    `GET /v1/models` but the request is `GET /forbidden`), an `audit_log` from `audit::new_audit_log()`,
    and the other ctx fields per the struct definition. Drive `handle_reverse_proxy`. Read the client
    side; assert the response starts with `HTTP/1.1 403`. Then `audit::drain_audit_events(&log)`;
    assert the first event's `decision == NetworkAuditDecision::Deny` and
    `denial_category == Some(NetworkAuditDenialCategory::EndpointPolicy)`.
    HARNESS CAVEAT: if `handle_reverse_proxy` requires a real `tokio::net::TcpStream` (not a duplex
    half), use a `tokio::net::TcpListener::bind("127.0.0.1:0")` loopback pair as the standard
    substitute — do NOT refactor the handler signature. The 403 + EndpointPolicy assertion is
    unchanged either way. This is equivalence-only: do NOT modify reverse.rs non-test code; record
    the result for the 89-04 ledger addendum and skip the #1077 cherry-pick.
  </action>
  <verify>
    <automated>cargo test -p nono-proxy denied_endpoint_returns_403_and_audit</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p nono-proxy denied_endpoint_returns_403_and_audit` exits 0.
    - Behavior assertion: the test asserts the client response starts with `HTTP/1.1 403`.
    - Audit assertion: the test asserts a drained event with `decision == NetworkAuditDecision::Deny` AND `denial_category == Some(NetworkAuditDenialCategory::EndpointPolicy)`.
    - No change to `reverse.rs` non-test code (equivalence-only); the AWS 501 stub at reverse.rs:189 is untouched (D-08).
  </acceptance_criteria>
  <done>The D-09 403+audit equivalence test passes; test name captured for the 89-04 ledger addendum.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sandboxed child → CONNECT tunnel | Untrusted CONNECT requests; lenient auth is a deliberate undici-compat relaxation, not a regression |
| sandboxed child → reverse-proxy L7 | Non-CONNECT requests subject to endpoint default-deny; 403+audit is the access-control + logging control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-04 | Information Disclosure | connect.rs lenient CONNECT auth | accept | Lenient missing-auth-continue is a deliberate, documented undici-compat relaxation (ASVS V2 note in RESEARCH.md §Security Domain). The D-02 test must NOT tighten it into strictness; it only PROVES keep-open. The strict bypass-arm carve-out (server.rs:572-577) stays intact and is out of this test's scope. |
| T-89-05 | Elevation of Privilege | reverse.rs endpoint default-deny | mitigate | The D-09 test confirms denied non-CONNECT requests stay denied (403) AND audited (EndpointPolicy) — the 403+audit IS the access-control + V7 logging control. Equivalence test guards against silent reversion of the deny-before-credential-op ordering. |
| T-89-06 | Repudiation | audit coverage on denied requests | mitigate | The D-09 test asserts exactly one `Deny` audit event with category `EndpointPolicy`, proving denied requests are non-repudiably logged (`audit::log_denied`). |
| T-89-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this phase (RESEARCH.md §Package Legitimacy Audit = N/A). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `cargo test -p nono-proxy connect reverse` — both new tests green plus existing connect/reverse tests.
- `cargo build -p nono-proxy` exits 0.
- The proxy crate has ZERO `cfg(target_os)` gates (verified in RESEARCH.md), so these tests run on the Windows dev-host; no cross-target cfg surface in this plan. Standard `cargo test` suffices; note in SUMMARY that no cfg-gated lines were touched.
</verification>

<success_criteria>
- CONNECT keeps connection open on missing auth (#1151 / D-02) — `connect_keeps_open_on_missing_proxy_auth` passes; cherry-pick skipped.
- Denied non-CONNECT returns 403 + EndpointPolicy audit (#1077 / D-09) — `denied_endpoint_returns_403_and_audit` passes; cherry-pick skipped.
- No upstream abstraction imported; no relaxation of any denial; AWS 501 stub untouched.
</success_criteria>

<output>
Create `.planning/phases/89-proxy-hardening-sync/89-02-SUMMARY.md` when done. Record: the two
test names (needed by plan 89-04 ledger addendum), whether either test reproduced a real gap
(expected: neither did → both are equivalence findings), and confirmation that no non-test proxy
code changed.
</output>
