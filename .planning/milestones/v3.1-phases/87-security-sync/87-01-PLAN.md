---
phase: 87-security-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono/src/sandbox/linux.rs
  - crates/nono/src/capability.rs
  - crates/nono-cli/src/exec_strategy/supervisor_linux.rs
autonomous: true
requirements:
  - SEC-01

must_haves:
  truths:
    - "When CapabilitySet has no unix-socket grant, sendto/sendmsg/sendmmsg are denied with static SECCOMP_RET_ERRNO(EPERM) — NOT routed to USER_NOTIF — verified by code inspection of build_seccomp_af_unix_nogrant_filter and its install condition in exec_strategy.rs"
    - "build_seccomp_af_unix_filter returns 8 instructions (was 5) with SYS_SENDTO/SENDMSG/SENDMMSG JEQ arms at indices 3-5"
    - "build_seccomp_proxy_filter returns 23 instructions (was 19) with SYS_SENDTO/SENDMSG/SENDMMSG JEQ arms inserted before the IO_URING arm"
    - "SYS_SENDTO, SYS_SENDMSG, SYS_SENDMMSG are exported as pub const i32 from linux.rs using libc::SYS_* as i32"
    - "read_msghdr_dest and read_mmsghdr_dests are pub functions in linux.rs with #[must_use], no unwrap, error propagated via ?"
    - "UnixSocketOp::Send variant exists in capability.rs with Display arm write!(f, send)"
    - "unix_socket_op_for_syscall maps SYS_SENDTO|SYS_SENDMSG|SYS_SENDMMSG to UnixSocketOp::Send"
    - "handle_network_notification dispatches on notif.data.nr to build Vec<SockaddrInfo> for sendto/sendmsg/sendmmsg with fail-secure deny on any read failure"
    - "All match-on-UnixSocketOp sites in supervisor_linux.rs have a Send arm (decide_af_unix_pathname, ipc_denial_details, record_af_unix_ipc_denial)"
    - "linux.rs:844-847 TODO comment is resolved/removed as part of this commit"
    - "BPF filter tests assert 8-insn af_unix filter and 23-insn proxy filter with correct jt offsets"
    - "Each cherry-pick commit carries (cherry picked from commit e2086877) line and DCO Signed-off-by trailer"
    - "cargo check --workspace passes on Windows dev-host (cross-target clippy deferred to Plan 87-03)"
    - "Decisions covered (87-CONTEXT.md): D-01: hybrid no-grant/grant gate; D-02: fail-secure silent EPERM default; D-03: resolves linux.rs:844-847 TODO; D-04: cherry-pick -x + DCO trailer per SHA; D-05: port upstream semantics onto fork structures; D-06: fork-specific grant-present sockaddr_un USER_NOTIF test"
  artifacts:
    - path: "crates/nono/src/sandbox/linux.rs"
      provides: "Extended BPF filters + SYS_SEND* consts + read_msghdr_dest + read_mmsghdr_dests + build_seccomp_af_unix_nogrant_filter"
      contains: "SYS_SENDTO"
    - path: "crates/nono/src/capability.rs"
      provides: "UnixSocketOp::Send variant + Display arm"
      contains: "UnixSocketOp::Send"
    - path: "crates/nono-cli/src/exec_strategy/supervisor_linux.rs"
      provides: "Multi-sockaddr dispatch loop for send-family syscalls"
      contains: "SYS_SENDMMSG"
  key_links:
    - from: "crates/nono-cli/src/exec_strategy/supervisor_linux.rs"
      to: "crates/nono/src/sandbox/linux.rs"
      via: "use nono::sandbox::{SYS_SENDTO, SYS_SENDMSG, SYS_SENDMMSG, read_msghdr_dest, read_mmsghdr_dests}"
      pattern: "SYS_SEND"
    - from: "crates/nono-cli/src/exec_strategy/supervisor_linux.rs"
      to: "crates/nono/src/capability.rs"
      via: "UnixSocketOp::Send"
      pattern: "UnixSocketOp::Send"
    - from: "crates/nono-cli/src/exec_strategy.rs (install condition)"
      to: "crates/nono/src/sandbox/linux.rs build_seccomp_af_unix_nogrant_filter"
      via: "installed when !config.af_unix_mediation.is_pathname() on the no-grant path"
      pattern: "install_seccomp_af_unix_nogrant"
---

<objective>
Port upstream commit `e2086877` (fix(linux): trap sendto/sendmsg to prevent AF_UNIX datagram
bypass, v0.64.0) onto the fork's divergent Linux enforcement code. This closes a SOCK_DGRAM
bypass where `sendto`/`sendmsg`/`sendmmsg` fell through the seccomp BPF filter to
`SECCOMP_RET_ALLOW` because the filter only trapped `connect`/`bind`.

Purpose: SEC-01 — the AF_UNIX datagram bypass is structurally impossible after this plan.
A sandboxed child cannot send datagrams to arbitrary Unix-socket paths without a grant and
supervisor validation (D-01 hybrid gate: no grant = static EPERM baked into a new
`build_seccomp_af_unix_nogrant_filter`; grant present = USER_NOTIF route to existing supervisor
validation loop).

Output: Extended BPF filters in `linux.rs` (grant-present filter extended to 8 insns; new
no-grant static-EPERM filter); `UnixSocketOp::Send` + send-family constants in `capability.rs`;
multi-sockaddr dispatch loop in `supervisor_linux.rs`; updated filter tests.
All as one atomic cherry-pick commit per D-04.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-security-sync/87-CONTEXT.md
@.planning/phases/87-security-sync/87-RESEARCH.md
@.planning/phases/87-security-sync/87-PATTERNS.md
@.planning/templates/cross-target-verify-checklist.md
@.planning/phases/86-library-boundary-convergence/86-03-SUMMARY.md

<interfaces>
<!-- Extracted from codebase at plan-time. Executor must read source files directly
     (see read_first on each task) — excerpts below orient the executor before first read. -->

From crates/nono/src/sandbox/linux.rs (existing patterns):
  pub const SYS_CONNECT: i32 = libc::SYS_connect as i32;    // line ~1119
  pub const SYS_BIND: i32 = libc::SYS_bind as i32;          // line ~1121
  build_seccomp_af_unix_filter() -> Vec<SockFilterInsn>       // lines ~2172-2205, 5 insns (grant-present, USER_NOTIF)
  build_seccomp_proxy_filter() -> Vec<SockFilterInsn>         // lines ~1964-2155, 19 insns
  build_seccomp_block_network_filter() -> [SockFilterInsn; 10] // line ~1326 — traps socket()/socketpair()/io_uring_setup() ONLY
  install_seccomp_af_unix_filter() -> Result<OwnedFd>         // line ~2227 (grant-present path, called from exec_strategy.rs)
  read_notif_sockaddr(pid: u32, addr_ptr: u64, addrlen: u64)  // lines ~2298-2358
  // TODO comment at lines 844-847: "AF_UNIX grants not distinguishable until seccomp AF_UNIX trap"

  // CRITICAL: build_seccomp_block_network_filter (BlockAll) does NOT trap sendto/sendmsg/sendmmsg.
  // It only traps socket()/socketpair()/io_uring_setup(). In the no-grant case, a child can already
  // hold an AF_UNIX socket fd (obtained before sandbox) and call sendto() → SECCOMP_RET_ALLOW.
  // A new build_seccomp_af_unix_nogrant_filter is required for the D-01 no-grant static-EPERM path.

From crates/nono-cli/src/exec_strategy.rs (install condition, lines ~1226-1248):
  // install_seccomp_af_unix_filter() is called only when config.af_unix_mediation.is_pathname()
  // i.e., only when grants exist. The no-grant path installs nothing for send-family syscalls today.
  let install_network_notify = config.seccomp_proxy_fallback || config.af_unix_mediation.is_pathname();
  if install_network_notify {
      if config.seccomp_proxy_fallback {
          install_seccomp_proxy_filter(has_bind)  // proxy path
      } else {
          install_seccomp_af_unix_filter()        // grant-present path → USER_NOTIF
      }
  }
  // No-grant path: install_network_notify is false → sendto falls through to ALLOW. Bug is here.

From crates/nono/src/capability.rs (existing patterns):
  pub enum UnixSocketOp { Connect, Bind }                     // lines ~218-233
  impl Display for UnixSocketOp { ... }                       // lines ~226-232
  pub fn unix_socket_allowed(&self, path, op: UnixSocketOp)   // exists on CapabilitySet

From crates/nono-cli/src/exec_strategy/supervisor_linux.rs (existing patterns):
  enum LinuxNetworkNotifyMode { ProxyOnly, AfUnixOnly }       // AfUnixOnly exists (line 610)
  fn unix_socket_op_for_syscall(syscall: i32) -> Option<UnixSocketOp>  // lines ~746-754
  fn decide_network_notification(pid, syscall, sockaddr, config)        // lines ~574-656
  fn handle_network_notification(notify_fd, notif, config, ...)         // lines ~812-868
  fn decide_af_unix_pathname(path, op, config) -> NetworkDecision       // line ~706
  fn ipc_denial_details(sockaddr, op) -> String                         // line ~939
  fn record_af_unix_ipc_denial(sockaddr, pid, syscall, ...)             // line ~897
  // AfUnixOnly mode: decide_network_notification checks for AfUnixOnly at line ~608 AFTER the
  // AF_UNIX pathname check — so new send-family arms flow through existing AfUnixOnly logic
  // automatically once unix_socket_op_for_syscall returns Some(UnixSocketOp::Send) for them.
  // No special AfUnixOnly handling needed beyond adding the SYS_SEND* arm to unix_socket_op_for_syscall.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Verify no-grant path and add build_seccomp_af_unix_nogrant_filter + install hook</name>
  <files>crates/nono/src/sandbox/linux.rs, crates/nono-cli/src/exec_strategy.rs</files>
  <read_first>
    - crates/nono/src/sandbox/linux.rs — lines 1320-1410 (build_seccomp_block_network_filter — note that it only traps socket/socketpair/io_uring, NOT sendto/sendmsg/sendmmsg); lines 1907-1945 (SeccompNetFallback enum, seccomp_network_fallback_mode); lines 2172-2230 (build_seccomp_af_unix_filter + install_seccomp_af_unix_filter)
    - crates/nono-cli/src/exec_strategy.rs — lines 1220-1290 (the install_network_notify gate and the call to install_seccomp_af_unix_filter)
    - 87-RESEARCH.md §SEC-01 §The hybrid gate (D-01) — the no-grant path analysis and the note about BlockAll not covering sendto
    - 87-CONTEXT.md D-01, D-02 — the hybrid gate decisions
  </read_first>
  <action>
    This task addresses D-01's no-grant static-EPERM path (Blocker 1). Code inspection has
    confirmed that:
    (a) `build_seccomp_block_network_filter` (BlockAll) traps only `SYS_SOCKET`, `SYS_SOCKETPAIR`,
        `SYS_IO_URING_SETUP` — it does NOT trap `sendto`/`sendmsg`/`sendmmsg`.
    (b) `install_seccomp_af_unix_filter()` is only called when `config.af_unix_mediation.is_pathname()`
        (grant-present path). In the no-grant case, no filter is installed for send-family syscalls.
    (c) Therefore, a child that already holds an AF_UNIX socket fd (obtained before sandbox) can
        call `sendto(fd, ..., dest_addr)` and it falls through to `SECCOMP_RET_ALLOW`. This is
        the open bypass path that D-01 requires the static-EPERM filter to close.

    Step 1 — Add `build_seccomp_af_unix_nogrant_filter` to linux.rs:
    Place immediately before or after `build_seccomp_af_unix_filter` (lines ~2172). This is a
    simple 4-instruction filter that bakes a static `SECCOMP_RET_ERRNO(EPERM)` for
    `SYS_SENDTO`, `SYS_SENDMSG`, `SYS_SENDMMSG` without routing to USER_NOTIF:

    Instruction layout:
     0: ld  [nr]
     1: jeq SYS_SENDTO   jt=+2 (-> 4: EPERM) jf=0 (-> 2)
     2: jeq SYS_SENDMSG  jt=+1 (-> 4: EPERM) jf=0 (-> 3)
     3: jeq SYS_SENDMMSG jt=+0 (-> 4: EPERM) jf=0 (-> 4: ALLOW? wrong — use explicit ret)

    Use 5 instructions for clarity (ld + 3 jeq + ALLOW + EPERM):
     0: ld  [nr]
     1: jeq SYS_SENDTO   jt=+3 (-> 5: EPERM)
     2: jeq SYS_SENDMSG  jt=+2 (-> 5: EPERM)
     3: jeq SYS_SENDMMSG jt=+1 (-> 5: EPERM)
     4: ret ALLOW
     5: ret SECCOMP_RET_ERRNO(EPERM)

    Function signature:
      fn build_seccomp_af_unix_nogrant_filter() -> Vec<SockFilterInsn>

    Private (not pub) — only used internally by `install_seccomp_af_unix_nogrant_filter`.
    Add the leading instruction-index comment table (same format as `build_seccomp_af_unix_filter`).

    Add a companion install function (pub, following the pattern of `install_seccomp_af_unix_filter`):
      pub fn install_seccomp_af_unix_nogrant_filter() -> Result<()>

    This function installs the filter via `prctl(PR_SET_SECCOMP, SECCOMP_MODE_FILTER, ...)`.
    Unlike the USER_NOTIF filter (which returns an OwnedFd), the no-grant filter uses
    SECCOMP_RET_ERRNO and does NOT produce a notify fd. Use `install_seccomp_filter_nonotify`
    (or the equivalent pattern — check how `install_seccomp_block_network` is implemented at
    lines ~1399-1410; follow the exact same pattern).

    Return type: `Result<()>` (no fd returned — static deny, no supervisor needed).

    Add a doc-comment on `install_seccomp_af_unix_nogrant_filter` explaining:
    "Installs a static seccomp filter that denies sendto/sendmsg/sendmmsg with EPERM when no
    AF_UNIX unix-socket grant exists. This is the D-01 no-grant path: fail-secure silent EPERM
    without involving the USER_NOTIF supervisor. Called by the CLI child process before exec
    when unix-socket mediation is active but no grants are present."

    Step 2 — Export `install_seccomp_af_unix_nogrant_filter` from sandbox/mod.rs:
    Read crates/nono/src/sandbox/mod.rs lines 34-42 and add `install_seccomp_af_unix_nogrant_filter`
    to the `pub use linux::{...}` block alongside `install_seccomp_af_unix_filter`.

    Step 3 — Install the no-grant filter in exec_strategy.rs:
    Read crates/nono-cli/src/exec_strategy.rs lines 1220-1290.
    After the existing `if install_network_notify { ... }` block (which handles the grant-present
    and proxy paths), add a new install for the no-grant case. The install condition:
    unix-socket mediation is active (the CapabilitySet has AF_UNIX mediation configured, meaning
    the sandbox policy asks for AF_UNIX control) but `config.af_unix_mediation.is_pathname()` is
    false (no grants). Read the `af_unix_mediation` field on `config` to understand the type
    (likely an enum or Option); derive the correct condition from the existing `is_pathname()` call.

    Condition draft (adjust to actual types):
      if config.af_unix_mediation.is_active() && !config.af_unix_mediation.is_pathname() {
          // No unix-socket grants → install static EPERM filter for send-family (D-01)
          if let Err(_e) = nono::sandbox::install_seccomp_af_unix_nogrant_filter() {
              // Post-fork: use async-signal-safe error path (same pattern as existing PROXY_FAIL)
          }
      }

    If `is_active()` does not exist, inspect the `AfUnixMediation` type and derive the equivalent
    predicate. The key invariant: the filter is installed exactly when the sandbox policy includes
    AF_UNIX mediation (so the child is not allowed to bypass) but no grant path is present to
    route to USER_NOTIF.

    Step 4 — Add a test for build_seccomp_af_unix_nogrant_filter in linux.rs:
    In the existing seccomp filter test module (lines ~3217+), add:
      #[test]
      fn test_build_seccomp_af_unix_nogrant_filter_denies_send_family()
    Assert: filter.len() == 6; filter[1].k == SYS_SENDTO as u32; filter[2].k == SYS_SENDMSG;
    filter[3].k == SYS_SENDMMSG; filter[4].k == SECCOMP_RET_ALLOW; filter[5].k == expected EPERM ret.
    Assert jt values for each JEQ arm (3, 2, 1 respectively targeting the EPERM instruction).

    Step 5 — Do NOT commit yet. Stage all changes in linux.rs, sandbox/mod.rs, and exec_strategy.rs.
    The cherry-pick commit happens after Tasks 1, 2, and 3 are all staged.
  </action>
  <verify>
    <automated>grep -n "build_seccomp_af_unix_nogrant_filter\|install_seccomp_af_unix_nogrant" crates/nono/src/sandbox/linux.rs</automated>
    Must show both the build and install function definitions.
    <automated>grep -n "install_seccomp_af_unix_nogrant" crates/nono/src/sandbox/mod.rs crates/nono-cli/src/exec_strategy.rs</automated>
    Must show the re-export in mod.rs and the install call in exec_strategy.rs.
  </verify>
  <acceptance_criteria>
    - `build_seccomp_af_unix_nogrant_filter` exists in linux.rs as a private fn returning Vec<SockFilterInsn> with 6 instructions (ld + 3 JEQ + ALLOW + EPERM)
    - `install_seccomp_af_unix_nogrant_filter` exists in linux.rs as a pub fn returning Result<()>
    - `install_seccomp_af_unix_nogrant_filter` is re-exported from sandbox/mod.rs in the linux pub use block
    - exec_strategy.rs installs the no-grant filter when mediation is active but no pathname grants exist
    - test_build_seccomp_af_unix_nogrant_filter_denies_send_family asserts filter.len() == 6 and correct k/jt values
    - No .unwrap() or .expect() in new non-test code
    - In the no-grant case, sendto/sendmsg/sendmmsg are denied with static EPERM — code path confirmed by the install condition in exec_strategy.rs (not by runtime test, which requires Linux)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 1: Extend linux.rs — SYS_SEND* constants, BPF filter expansion, read_msghdr_dest, read_mmsghdr_dests</name>
  <files>crates/nono/src/sandbox/linux.rs</files>
  <read_first>
    - crates/nono/src/sandbox/linux.rs — full file, focus on: lines 1-30 (imports), 1119-1125 (SYS_CONNECT/SYS_BIND consts), 1964-2250 (build_seccomp_proxy_filter and build_seccomp_af_unix_filter), 2298-2360 (read_notif_sockaddr pattern), 840-848 (the TODO comment to resolve), 3519-3540 (existing BPF filter tests)
    - 87-RESEARCH.md §SEC-01 Detailed Port Analysis — BPF mechanics, exact instruction tables, read_msghdr_dest implementation
    - 87-PATTERNS.md §linux.rs — SYS_* pattern, BPF instruction construction pattern, proxy filter extension, read_notif_sockaddr analog, read_mmsghdr_dests pattern
    - git show e2086877 — run this to review the upstream diff before touching anything; use it to confirm the exact location of each change
  </read_first>
  <action>
    Atomically extend linux.rs as part of the `git cherry-pick -x e2086877` resolution. Do NOT
    accept upstream's version of the file wholesale — the fork's structures are divergent. Port
    upstream semantics onto the fork's existing code:

    Step 1 — Attempt the cherry-pick. Expected to conflict in the BPF filter functions and
    potentially the const block. Run: git cherry-pick -x e2086877

    Step 2 — Resolve conflicts in linux.rs by applying all of the following changes (per D-05):

    (a) SYS_SEND* constants: After SYS_BIND (line ~1123), add three pub const declarations using
    the identical pattern as SYS_CONNECT/SYS_BIND — same #[cfg(target_os = "linux")] guard, same
    `libc::SYS_* as i32` cast, same pub const. Constants: SYS_SENDTO, SYS_SENDMSG, SYS_SENDMMSG.
    Block comment: "Syscall numbers for send-family (public for CLI supervisor handler)".

    (b) build_seccomp_af_unix_filter: Replace the 5-instruction filter body and its comment table
    with the 8-instruction version. All existing JEQ jt offsets change: SYS_CONNECT jt becomes 5,
    SYS_BIND jt becomes 4. Three new JEQ arms added: SYS_SENDTO jt=3, SYS_SENDMSG jt=2,
    SYS_SENDMMSG jt=1. All targeting the USER_NOTIF ret at instruction index 7.
    Update the leading comment table to enumerate all 8 instructions with -> target notation
    (mandatory for BPF auditability — see 87-PATTERNS.md §in-code BPF instruction-index comment
    table for the required format). This is the grant-present filter; it routes all send-family
    calls to USER_NOTIF for supervisor validation (D-01).

    (c) build_seccomp_proxy_filter: Grow from 19 to 23 instructions by inserting three send-family
    JEQ arms BEFORE the IO_URING arm (currently at the 5th position in the 19-insn filter).
    Recount ALL jt offsets for every instruction that follows the insertion point — any jt
    targeting an instruction after index 4 shifts by +3. Update the leading comment table to
    enumerate all 23 instructions. Follow 87-PATTERNS.md §build_seccomp_proxy_filter extension
    exactly.

    (d) Resolve the TODO comment at lines 844-847. The comment reads something like: "AF_UNIX
    pathname grants are not distinguishable on this Linux path until the seccomp AF_UNIX [trap]."
    Remove the TODO marker and replace with an informative comment documenting that the trap is now
    present via build_seccomp_af_unix_filter (grant-present) and build_seccomp_af_unix_nogrant_filter
    (no-grant static EPERM per D-01). Exact wording at executor's discretion; must remove the word "TODO".

    (e) Add read_msghdr_dest: Place after read_notif_sockaddr (which is the structural analog).
    Function sig: pub fn read_msghdr_dest(pid: u32, msghdr_ptr: u64) -> Result<Option<(u64, u64)>>
    Apply #[must_use] and #[cfg(target_os = "linux")]. Read 12 bytes via /proc/{pid}/mem at
    msghdr_ptr using File::open + Seek + read_exact. Use try_into() for byte-slice conversions
    (no .unwrap()). Return Ok(None) if msg_name == 0; Ok(Some((msg_name, msg_namelen as u64)))
    otherwise. Error messages: "Failed to open {mem_path}: {e}", "Failed to seek in {mem_path}:
    {e}", "Failed to read msghdr from {mem_path}: {e}". See 87-PATTERNS.md for exact
    implementation excerpt.

    (f) Add read_mmsghdr_dests: Place immediately after read_msghdr_dest.
    Function sig: pub fn read_mmsghdr_dests(pid: u32, msgvec_ptr: u64, vlen: u32) ->
    Result<Vec<Option<(u64, u64)>>>
    Apply #[must_use] and #[cfg(target_os = "linux")]. Cap vlen at MAX_MMSGHDRS = 1024 (add
    const MAX_MMSGHDRS: u32 = 1024 before the function). Use
    std::mem::size_of::<libc::mmsghdr>() as u64 for stride — never hardcode 28. Overflow-safe
    iteration: checked_mul then checked_add per CLAUDE.md arithmetic rule, returning
    NonoError::SandboxInit("mmsghdr offset overflow...") on overflow. See 87-PATTERNS.md for
    exact implementation excerpt.

    Step 3 — Update BPF filter tests:
    - test_build_seccomp_af_unix_filter_notifies_connect_bind_only: rename to
      test_build_seccomp_af_unix_filter_notifies_connect_bind_send; update assert_eq!(filter.len(), 5)
      to 8; add assertions for filter[3].k == SYS_SENDTO, [4].k == SYS_SENDMSG,
      [5].k == SYS_SENDMMSG; add jt assertions for all 5 JEQ arms per 87-PATTERNS.md.
    - test for build_seccomp_proxy_filter: update assert_eq!(filter.len(), 19) to 23.

    Step 4 — Stage all changes in linux.rs and the test updates. Do NOT commit yet — the
    cherry-pick commit also needs the capability.rs and supervisor_linux.rs changes (Tasks 2 and 3
    must all be staged before committing as one atomic cherry-pick).

    Note: Use `use std::io::Read` inside read_msghdr_dest body (not at module top), following
    read_notif_sockaddr's local-import pattern.
  </action>
  <verify>
    <automated>grep -n "SYS_SENDTO" crates/nono/src/sandbox/linux.rs</automated>
    Must return the pub const declaration and usages in both BPF filter functions. This grep is
    meaningful in isolation — it confirms the constant and filter arms exist even before Tasks 2
    and 3 are staged. Note: `cargo check --workspace` is expected to exit non-zero after Task 1
    alone because `UnixSocketOp::Send` (added in Task 2) is not yet present — the match sites in
    supervisor_linux.rs (Task 3) will be non-exhaustive until all three files are staged. Run
    `cargo check` only after Task 3 commits all three files.
  </verify>
  <acceptance_criteria>
    - linux.rs contains `pub const SYS_SENDTO: i32 = libc::SYS_sendto as i32;` (grep: `SYS_SENDTO`)
    - build_seccomp_af_unix_filter body contains exactly 8 SockFilterInsn entries
    - build_seccomp_proxy_filter body contains exactly 23 SockFilterInsn entries
    - read_msghdr_dest function exists with #[must_use] and #[cfg(target_os = "linux")]
    - read_mmsghdr_dests function exists with MAX_MMSGHDRS = 1024 and checked_mul
    - No .unwrap() or .expect() in new code (grep -n "unwrap\|expect" new lines = 0 outside #[cfg(test)])
    - The TODO comment at lines 844-847 is resolved (grep -n "TODO.*AF_UNIX" linux.rs = 0)
    - BPF test asserts filter.len() == 8 for af_unix filter and 23 for proxy filter
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend capability.rs — UnixSocketOp::Send variant + Display arm</name>
  <files>crates/nono/src/capability.rs</files>
  <read_first>
    - crates/nono/src/capability.rs — lines 218-240 (UnixSocketOp enum + Display impl); also read
      lines 1480-1520 (deduplicate function header, to understand where capability.rs dedup sits
      for context — SEC-02 guard lands here in Plan 87-02, not this task)
    - 87-PATTERNS.md §capability.rs §UnixSocketOp enum extension — exact variant doc comment format
    - 87-PATTERNS.md §supervisor_linux.rs §UnixSocketOp enum extension — exhaustiveness sites list
  </read_first>
  <action>
    Extend capability.rs as part of the same e2086877 cherry-pick (staged alongside Task 1 before
    the final commit).

    (a) Add UnixSocketOp::Send to the enum (lines ~218-233). Place after Bind. Doc comment:
    "/// About to call `sendto(2)`, `sendmsg(2)`, or `sendmmsg(2)`." Follow the exact doc-comment
    style of Connect and Bind (one sentence, code-reference the syscall names).

    (b) Add Send arm to the Display impl immediately below the enum (lines ~226-232):
    `UnixSocketOp::Send => write!(f, "send")`. Place after the Bind arm.

    These two additions are minimal — do not touch any other code in capability.rs in this task.
    The is_procfs_remap_original guard and SEC-02 changes are handled in Plan 87-02.

    Stage the change. Do NOT commit yet.
  </action>
  <verify>
    <automated>grep -n "UnixSocketOp::Send" crates/nono/src/capability.rs</automated>
  </verify>
  <acceptance_criteria>
    - capability.rs contains `Send,` inside `pub enum UnixSocketOp`
    - capability.rs contains `UnixSocketOp::Send => write!(f, "send")` in the Display impl
    - No other code in capability.rs is changed by this task
    - grep "UnixSocketOp::Send" crates/nono/src/capability.rs returns at least 2 lines
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Extend supervisor_linux.rs — send-family dispatch + commit the cherry-pick</name>
  <files>crates/nono-cli/src/exec_strategy/supervisor_linux.rs</files>
  <read_first>
    - crates/nono-cli/src/exec_strategy/supervisor_linux.rs — full read focus: lines 1-30 (imports),
      574-660 (decide_network_notification), 700-760 (decide_af_unix_pathname,
      unix_socket_op_for_syscall, unix_socket_allowlist_allows), 812-870 (handle_network_notification),
      897-945 (record_af_unix_ipc_denial, ipc_denial_details), 1316-1470 (mod network_decision tests)
    - 87-RESEARCH.md §Open Questions (RESOLVED) Q2 — LinuxNetworkNotifyMode::AfUnixOnly: the enum
      variant exists (line 610); no special AfUnixOnly handling is needed beyond adding the SYS_SEND*
      arm to unix_socket_op_for_syscall, because decide_network_notification's AfUnixOnly guard at
      line ~608 operates AFTER AF_UNIX pathname routing, so it automatically applies to send-family.
    - 87-PATTERNS.md §supervisor_linux.rs — all pattern subsections:
      imports extension, unix_socket_op_for_syscall, handle_network_notification multi-sockaddr loop,
      decide_network_notification extension, test pattern, UnixSocketOp exhaustiveness sites list
    - 87-RESEARCH.md §SEC-01 Pitfall 1 (sendmmsg NULL-destination fast-path)
    - 87-RESEARCH.md §SEC-01 Pitfall 4 (cherry-pick conflicts in handle_network_notification)
    - 87-RESEARCH.md §SEC-01 Pitfall 5 (cross-target match exhaustiveness on UnixSocketOp::Send)
  </read_first>
  <action>
    Resolve all supervisor_linux.rs changes as part of the e2086877 cherry-pick resolution:

    Step 1 — Extend unix_socket_op_for_syscall (lines ~746-754):
    Add `SYS_SENDTO | SYS_SENDMSG | SYS_SENDMMSG => Some(UnixSocketOp::Send)` arm.
    Add SYS_SENDMSG and SYS_SENDMMSG and SYS_SENDTO to the use block inside the function.

    Step 2 — Extend decide_network_notification (lines ~574-656):
    Add `SYS_SENDTO | SYS_SENDMSG | SYS_SENDMMSG` to the SYS_CONNECT arm in the terminal
    `match syscall` block. These syscalls use the same loopback+proxy_port check as connect for
    TCP; AF_UNIX pathname routing is already handled upstream in decide_af_unix_pathname.
    Add the new SYS_* constants to the use block at line ~580.

    Step 3 — Extend decide_af_unix_pathname (line ~706):
    If it has `match op { Connect => ..., Bind => ... }`, add a Send arm. The Send-path behavior
    is identical to Connect for pathname sockets — route through the allowlist check.

    Step 4 — Extend ipc_denial_details (line ~939):
    If it has `match op { Connect => ..., Bind => ... }`, add `UnixSocketOp::Send => ...` arm.
    Denial detail string for Send: follow the connect pattern (e.g., "send to" instead of
    "connect to"), produce a meaningful human-readable denial description.

    Step 5 — Extend record_af_unix_ipc_denial (line ~897):
    If it has a match on op, add the Send arm. Follow the existing pattern exactly.

    Step 6 — Extend handle_network_notification (lines ~812-868):
    Replace the current single-sockaddr approach with the multi-sockaddr Vec<SockaddrInfo> loop.
    Branch on notif.data.nr to handle sendto/sendmsg/sendmmsg distinctly:
    - SYS_SENDTO: args[4] is dest_addr*, args[5] is addrlen. If args[4] == 0 (connected socket),
      fast-path continue_notif and return Ok(()).
    - SYS_SENDMSG: args[1] is msghdr*. Call read_msghdr_dest; if Ok(None) → connected socket
      fast-path allow; if Ok(Some((addr_ptr, addrlen))) → read_notif_sockaddr.
    - SYS_SENDMMSG: args[1] is msgvec*, args[2] is vlen. Call read_mmsghdr_dests; if all entries
      None → connected-socket fast-path allow; otherwise collect non-None addrs into Vec.
    - Fall-through `_` arm: existing single-sockaddr path for SYS_CONNECT / SYS_BIND.
    Add read_msghdr_dest and read_mmsghdr_dests to the use block at lines ~819-822.
    Fail-secure on every read error: debug!(...); let _ = deny_notif(...); return Ok(()).
    TOCTOU check (notif_id_valid) must be performed ONCE before the per-sockaddr loop, not inside.
    Deny on ANY single sockaddr denial (fail-secure for multi-message).
    Follow 87-PATTERNS.md §handle_network_notification multi-sockaddr loop exactly for styling.

    Step 7 — Confirm LinuxNetworkNotifyMode::AfUnixOnly handling:
    Grep for `LinuxNetworkNotifyMode` in supervisor_linux.rs. The `AfUnixOnly` variant exists
    at line ~610. The existing guard in `decide_network_notification` (line ~608) operates after
    AF_UNIX pathname routing — so once `unix_socket_op_for_syscall` returns `Some(UnixSocketOp::Send)`
    for SYS_SEND*, the send-family syscalls automatically flow through the AfUnixOnly guard without
    additional code. Confirm this by reading the guard and add a note in the commit message:
    "AfUnixOnly mode: send-family syscalls route through existing AfUnixOnly guard via unix_socket_op_for_syscall."

    Step 8 — Add tests into mod network_decision:
    - af_unix_pathname_sendto_is_allowed_by_grant (D-06 fork-specific test — no upstream equivalent)
    - af_unix_abstract_sendto_is_denied
    - sendto_connected_socket_is_allowed (args[4] == 0 fast-path)
    Follow 87-PATTERNS.md §test pattern exactly (make_config, helper structure, assert_eq! with message).

    Step 9 — Commit the cherry-pick. Stage all four modified files (linux.rs, sandbox/mod.rs,
    capability.rs, supervisor_linux.rs, and exec_strategy.rs from Task 0). Add a DCO trailer to
    the cherry-pick commit message:
    `git cherry-pick --continue` will preserve the (cherry picked from commit e2086877...) line.
    Amend with DCO if needed: `git commit --amend --no-edit -s` to add
    "Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>".
    Final commit must have BOTH the "(cherry picked from commit e2086877)" line AND the DCO line.
  </action>
  <verify>
    <automated>cargo check --workspace 2>&1; echo "exit: $?"</automated>
    cargo check --workspace must exit 0. Then:
    <automated>grep -rn "SYS_SENDMMSG" crates/ | grep -v "target/"</automated>
    Must show hits in linux.rs and supervisor_linux.rs.
    <automated>git log --oneline -3</automated>
    Most recent commit must contain "cherry picked from commit e2086877" in the body.
  </verify>
  <acceptance_criteria>
    - cargo check --workspace exits 0 after all files committed
    - supervisor_linux.rs contains `SYS_SENDMMSG` (grep returns matches in both crates/nono and crates/nono-cli)
    - unix_socket_op_for_syscall returns Some(UnixSocketOp::Send) for SYS_SENDTO/SENDMSG/SENDMMSG
    - handle_network_notification contains the Vec<SockaddrInfo> multi-sockaddr loop
    - All match-on-UnixSocketOp sites have a Send arm (no non-exhaustive match warnings in cargo check)
    - git log shows cherry-pick commit with "(cherry picked from commit e2086877)" AND "Signed-off-by: Oscar Mack Jr"
    - At least 3 new tests in mod network_decision for send-family behavior
    - build_seccomp_af_unix_nogrant_filter and install_seccomp_af_unix_nogrant_filter present in linux.rs (from Task 0)
    - exec_strategy.rs installs the no-grant static-EPERM filter when mediation is active but no grants exist (from Task 0)
    - PARTIAL→CI deferral note: cross-target clippy for this commit is deferred to Plan 87-03 and live CI per .planning/templates/cross-target-verify-checklist.md
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sandboxed child → kernel | Syscalls from the untrusted child process cross into the kernel BPF filter |
| Child address space → supervisor | Supervisor reads child memory via /proc/PID/mem to extract sockaddr/msghdr data |
| Seccomp USER_NOTIF → supervisor decision | Notification carries raw args (pointer values) that the supervisor must validate before trusting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01 | Tampering | AF_UNIX SOCK_DGRAM bypass via sendto/sendmsg/sendmmsg (pre-fix) | mitigate | Two-path D-01 hybrid gate: (1) no unix-socket grant → build_seccomp_af_unix_nogrant_filter installs static SECCOMP_RET_ERRNO(EPERM) for SYS_SENDTO/SENDMSG/SENDMMSG — no USER_NOTIF, deterministic deny; (2) grant present → build_seccomp_af_unix_filter (8-insn) routes to USER_NOTIF for per-call sockaddr validation. Closed by this plan. |
| T-87-02 | Elevation of Privilege | Abstract-namespace AF_UNIX sockets bypassing pathname allowlist | mitigate | classify_af_unix() returns UnixSocketKind::Abstract → denied by decide_network_notification for all send-family ops (existing path, confirmed still applied after port) |
| T-87-03 | Denial of Service | sendmmsg with large vlen (up to 2^32-1) causing unbounded /proc/PID/mem reads | mitigate | MAX_MMSGHDRS = 1024 cap in read_mmsghdr_dests; overflow-safe arithmetic with checked_mul/checked_add |
| T-87-04 | Information Disclosure | TOCTOU: child changes sockaddr between BPF trap and supervisor read | mitigate | notif_id_valid check before decision; if pid changes or notif expires → deny. Existing mitigation; confirmed applied after multi-sockaddr refactor. |
| T-87-05 | Tampering | Null/zero msg_name in sendmsg/sendto (connected socket) being misclassified as a bypass | accept | Explicit fast-path: if args[4]==0 (sendto) or msg_name==NULL (sendmsg/sendmmsg) → continue_notif. Connected-socket sends carry no per-call destination; no bypass possible. |
| T-87-SC | Tampering | npm/pip/cargo installs | accept | No new packages added (pure Rust edits to existing files). Package legitimacy gate N/A. |
</threat_model>

<verification>
## Post-Task Verification

After Task 3 commit:

1. cargo check --workspace — must exit 0 on Windows dev-host (local checkable gate)
2. grep -n "SYS_SENDTO\|SYS_SENDMSG\|SYS_SENDMMSG" crates/nono/src/sandbox/linux.rs — must return 3 const declarations + filter usage in both grant-present and no-grant filters
3. grep -n "build_seccomp_af_unix_nogrant_filter" crates/nono/src/sandbox/linux.rs — must show both the build fn and install fn
4. grep -n "UnixSocketOp::Send" crates/ -r --include="*.rs" | grep -v target/ — must show capability.rs (enum + Display) and supervisor_linux.rs (match arms + test)
5. git show HEAD | grep -c "cherry picked from commit e2086877" — must be 1
6. git show HEAD | grep -c "Signed-off-by" — must be >= 1
7. Cross-target clippy: ATTEMPTED but expected PARTIAL on Windows dev-host.
   Run: cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used
   If cross-toolchain missing (error: linker x86_64-linux-gnu-gcc not found), record:
   "Cross-target clippy SKIPPED on Windows dev-host due to missing toolchain. GH Actions Linux CI
   lane is the decisive gate per .planning/templates/cross-target-verify-checklist.md."
   SEC-01 verification status: PARTIAL pending CI.
</verification>

<success_criteria>
- D-01 hybrid gate fully implemented: no-grant path → static SECCOMP_RET_ERRNO(EPERM) via build_seccomp_af_unix_nogrant_filter; grant-present path → USER_NOTIF via extended build_seccomp_af_unix_filter (8 insns)
- BPF filter bypass is structurally closed: build_seccomp_af_unix_filter traps sendto/sendmsg/sendmmsg (8 insns) and build_seccomp_proxy_filter is extended to 23 insns
- SYS_SENDTO/SYS_SENDMSG/SYS_SENDMMSG exported from linux.rs; supervisor dispatch extended
- UnixSocketOp::Send exists in capability.rs; all match sites are exhaustive
- Cherry-pick commit e2086877 landed with (cherry picked from) + DCO trailer
- cargo check --workspace passes locally; cross-target clippy PARTIAL→CI (deferred to Plan 87-03)
- SEC-01 requirement: PARTIAL pending Plan 87-03 CI-deferral record
</success_criteria>

<output>
Create .planning/phases/87-security-sync/87-01-SUMMARY.md when done.
</output>
