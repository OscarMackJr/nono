---
phase: 87-security-sync
plan: 02
type: execute
wave: 2
depends_on:
  - 87-01
files_modified:
  - crates/nono/src/capability.rs
  - crates/nono/src/audit.rs
  - proj/ADR-87-cr02-audit-bypass.md
  - .planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md
autonomous: true
requirements:
  - SEC-02

must_haves:
  truths:
    - "regression test remap_preserves_dev_null_when_deduped_with_dev_stdin exists in capability.rs and confirms the bug was present (test fails before the guard, passes after)"
    - "is_procfs_remap_original private helper exists in capability.rs at #[cfg(target_os = linux)] immediately after rewrite_procfs_self_reference"
    - "Both original_updates push sites in deduplicate() are guarded with && !is_procfs_remap_original(...)"
    - "cherry-pick commit 6b3eb013 carries (cherry picked from commit 6b3eb013) line and DCO trailer"
    - "verify_audit_log returns records_verified: event_count > 0 (not hardcoded true)"
    - "AuditVerificationResult.records_verified field doc-comment is updated to reflect the new semantic"
    - "is_valid() doc-comment explains vacuous-false for empty log"
    - "test verify_empty_log_with_no_stored_metadata_is_not_valid exists and passes"
    - "ADR-87-cr02-audit-bypass.md exists in proj/ documenting the deliberate fork-divergence from upstream e9529312"
    - "85-DIVERGENCE-LEDGER.md has a Phase 87 CR-02 addendum recording the fork-divergence at audit.rs line 1406"
    - "CR-02 fix commit is a separate fork-hardening commit (not part of the 6b3eb013 cherry-pick)"
    - "cargo check --workspace passes after both commits"
    - "Decisions covered (87-CONTEXT.md): D-04: cherry-pick -x + DCO for 6b3eb013; D-05: port semantics onto fork capability.rs; D-09: confirm-then-port — regression test written first; D-10: port guard adapted to fork keying if test fails; D-11: keep test + document if fork already immune; D-12: CR-02 hardening recorded as deliberate fork-divergence (ledger + ADR); D-13: CR-01 explicitly deferred to Phase 88, not addressed here"
  artifacts:
    - path: "crates/nono/src/capability.rs"
      provides: "is_procfs_remap_original helper + deduplicate() guards at both original_updates push sites"
      contains: "is_procfs_remap_original"
    - path: "crates/nono/src/audit.rs"
      provides: "records_verified: event_count > 0 with fork-hardening comment"
      contains: "event_count > 0"
    - path: "proj/ADR-87-cr02-audit-bypass.md"
      provides: "ADR recording deliberate fork-divergence from upstream e9529312"
      contains: "e9529312"
    - path: ".planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md"
      provides: "Phase 87 CR-02 addendum section"
      contains: "CR-02"
  key_links:
    - from: "crates/nono/src/capability.rs is_procfs_remap_original"
      to: "crates/nono/src/capability.rs rewrite_procfs_self_reference"
      via: "rewrite_procfs_self_reference(path, 0, None).is_some()"
      pattern: "is_procfs_remap_original"
    - from: "crates/nono/src/capability.rs deduplicate()"
      to: "is_procfs_remap_original"
      via: "!is_procfs_remap_original(&existing.original)"
      pattern: "is_procfs_remap_original"
---

<objective>
Port upstream commit `6b3eb013` (fix: guard deduplicate() against inheriting procfs-remap
originals, v0.63.0) onto the fork's capability.rs using the confirm-then-port approach (D-09/D-10).
Additionally harden CR-02: fix `verify_audit_log`'s hardcoded `records_verified: true` in audit.rs
and record it as a deliberate fork-divergence from upstream (D-12).

Purpose: SEC-02 closes the `/dev/null`-dropped-by-`/dev/stdin` dedup bug that breaks
`--detached` mode Landlock rules. CR-02 closes an audit-integrity bypass where an empty
log with no stored metadata falsely reports `is_valid() = true`.

Output: Two atomic commits — one cherry-pick commit for `6b3eb013` (SEC-02 guard + regression
test), one fork-hardening commit for CR-02 (audit.rs fix + ADR + divergence ledger addendum).
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-security-sync/87-CONTEXT.md
@.planning/phases/87-security-sync/87-RESEARCH.md
@.planning/phases/87-security-sync/87-PATTERNS.md
@.planning/phases/87-security-sync/87-01-SUMMARY.md
@.planning/templates/cross-target-verify-checklist.md
@proj/ADR-86-library-boundary-convergence.md

<interfaces>
<!-- Extracted from codebase at plan-time. Executor must read source files (see read_first). -->

From crates/nono/src/capability.rs (existing patterns):
  fn rewrite_procfs_self_reference(path, pid, prefix)         // line ~1804
  fn deduplicate(&mut self)                                    // line ~1491
  // In deduplicate(), two original_updates push sites:
  //   Site 1 (keep_new branch, lines ~1586-1589):
  //     #[cfg(target_os = "linux")]
  //     if cap.original == cap.resolved && existing.original != existing.resolved {
  //         original_updates.push((i, existing.original.clone()));
  //     }
  //   Site 2 (keep_new=false branch, lines ~1597-1600):
  //     #[cfg(target_os = "linux")]
  //     if existing.original == existing.resolved && cap.original != cap.resolved {
  //         original_updates.push((existing_idx, cap.original.clone()));
  //     }
  // mod procfs_remap_tests at line ~1862 — regression test goes here

From crates/nono/src/audit.rs (existing):
  pub struct AuditVerificationResult { ... records_verified: bool, ... } // lines ~152-171
  pub fn is_valid(&self) -> bool                               // lines ~163-171
  pub fn verify_audit_log(dir, stored: Option<...>) -> Result<AuditVerificationResult>
  //   records_verified: true,  // BUG at line ~1406 (the hardcoded value)
  //   event_count: usize (computed in verification loop)
  // mod tests at line ~1412; AUDIT_EVENTS_FILENAME constant nearby
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEC-02 — confirm-then-port (regression test first, then guard)</name>
  <files>crates/nono/src/capability.rs</files>
  <read_first>
    - crates/nono/src/capability.rs — lines 1480-1620 (full deduplicate function), 1800-1870
      (rewrite_procfs_self_reference + the procfs_remap_tests module that follows it)
    - 87-RESEARCH.md §SEC-02 Detailed Port Analysis — the bug explanation, fork divergence analysis,
      the guard to port (both site 1 and site 2), and the regression test
    - 87-PATTERNS.md §capability.rs — is_procfs_remap_original helper placement, deduplicate() guard
      insertion (Sites 1 and 2), SEC-02 regression test placement and exact structure
    - git show 6b3eb013 — run this before writing any code; confirm the upstream diff matches what
      RESEARCH.md documents
  </read_first>
  <action>
    Step 1 — Attempt the cherry-pick: git cherry-pick -x 6b3eb013
    This commit only touches capability.rs. Expect a clean or near-clean apply. If conflicts arise
    in deduplicate(), port upstream semantics onto the fork's divergent structure (per D-05).

    Step 2 — WRITE THE REGRESSION TEST FIRST (D-09 mandate — confirm-then-port):
    In the mod procfs_remap_tests block at line ~1862, add the test
    remap_preserves_dev_null_when_deduped_with_dev_stdin (see 87-RESEARCH.md §SEC-02 regression test
    and 87-PATTERNS.md §SEC-02 regression test placement for the exact test body).
    The test adds two FsCapability entries: (original=/dev/null, resolved=/dev/null) and
    (original=/dev/stdin, resolved=/dev/null). Calls caps.deduplicate(). Asserts the surviving
    entry has original == /dev/null (not /dev/stdin). Then calls remap_procfs_self_references(4242,
    None) and asserts resolved remains /dev/null.
    Apply #[cfg(target_os = "linux")] to the test.

    Step 3 — Run the test BEFORE adding the guard (D-09 confirm step):
    cargo test -p nono -- remap_preserves_dev_null_when_deduped_with_dev_stdin 2>&1
    Per RESEARCH.md (Assumption A1 + fork divergence analysis), this test is expected to FAIL
    because is_procfs_remap_original does not yet exist. Document the failure as confirmation.
    If for any reason the test passes (D-11 outcome), do NOT add the guard; instead document why
    the fork's keying already avoids the bug and proceed directly to committing the test only.

    Step 4 — Add is_procfs_remap_original helper (D-10 port step, applies because test failed):
    Place immediately after rewrite_procfs_self_reference closes (line ~1859), before the
    #[cfg(test)] block. Exact signature:
      #[cfg(target_os = "linux")]
      fn is_procfs_remap_original(path: &Path) -> bool {
          rewrite_procfs_self_reference(path, 0, None).is_some()
      }
    Include the doc-comment from 87-PATTERNS.md explaining why the helper exists. Private fn
    (not pub fn). No #[allow(dead_code)] — the two guard sites and the regression test use it.

    Step 5 — Guard both original_updates push sites in deduplicate():
    Site 1 (keep_new = true branch, lines ~1586-1589): add `&& !is_procfs_remap_original(&existing.original)` as the third condition in the if-expression.
    Site 2 (keep_new = false branch, lines ~1597-1600): add `&& !is_procfs_remap_original(&cap.original)` as the third condition.
    Do NOT change the #[cfg(target_os = "linux")] wrapping of either site. Do NOT change the
    access_upgrades logic (orthogonal, untouched per RESEARCH.md).

    Step 6 — Run the regression test to confirm it now passes:
    cargo test -p nono -- remap_preserves_dev_null_when_deduped_with_dev_stdin 2>&1
    Must exit 0.

    Step 7 — Complete the cherry-pick commit:
    Stage capability.rs. If the cherry-pick --continue flow is needed, run it. Amend to add DCO:
    git commit --amend --no-edit -s
    Final commit must have "(cherry picked from commit 6b3eb013)" AND "Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>".
  </action>
  <verify>
    <automated>cargo test -p nono -- remap_preserves_dev_null_when_deduped_with_dev_stdin 2>&1</automated>
    Must exit 0 (test passes after the guard is in place).
    <automated>grep -n "is_procfs_remap_original" crates/nono/src/capability.rs</automated>
    Must show: the helper definition AND the two guard sites in deduplicate().
    <automated>git log --oneline -3</automated>
    SEC-02 commit must appear with "(cherry picked from commit 6b3eb013)" in the body.
  </verify>
  <acceptance_criteria>
    - is_procfs_remap_original private helper exists immediately after rewrite_procfs_self_reference in capability.rs
    - Both original_updates push sites have the !is_procfs_remap_original guard as a third && condition
    - Test remap_preserves_dev_null_when_deduped_with_dev_stdin exists and passes (cargo test exits 0)
    - The cherry-pick commit carries "(cherry picked from commit 6b3eb013)" + DCO "Signed-off-by: Oscar Mack Jr"
    - cargo check --workspace exits 0 after this commit
    - No .unwrap() or .expect() in new non-test code
    - If D-11 outcome (test passed before guard): comment in capability.rs documents why no guard was needed; test kept as regression guard; commit message explains the outcome
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: CR-02 — audit-integrity fix + ADR + divergence ledger addendum</name>
  <files>
    crates/nono/src/audit.rs
    proj/ADR-87-cr02-audit-bypass.md
    .planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md
  </files>
  <read_first>
    - crates/nono/src/audit.rs — lines 140-175 (AuditVerificationResult struct + is_valid() +
      records_verified field), lines 1390-1415 (verify_audit_log return block and the hardcoded
      records_verified: true at line ~1406), lines 1412-1460 (mod tests, for test placement)
    - 87-RESEARCH.md §CR-02 Detailed Analysis — the bypass, the fix (records_verified: event_count > 0),
      and the required divergence record
    - 87-PATTERNS.md §audit.rs — verify_audit_log return struct pattern, AuditVerificationResult doc
      comment update, is_valid() doc comment update, CR-02 test structure
    - proj/ADR-86-library-boundary-convergence.md — structure to follow for the new ADR
    - .planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md — read the existing structure
      to understand where to add the Phase 87 CR-02 addendum (likely at the end)
    - 87-CONTEXT.md D-12 — the divergence record mandate
  </read_first>
  <action>
    This is a SEPARATE fork-hardening commit — NOT a cherry-pick. Commit message format:
    "fix(audit): records_verified now false for empty logs — CR-02 fork hardening"

    Step 1 — Fix verify_audit_log in audit.rs (line ~1406):
    Change `records_verified: true,` to `records_verified: event_count > 0,`.
    Add the mandatory fork-hardening comment on the same line or the line above:
    "// Fork-hardening (CR-02): set records_verified to false when no records were processed.
    // Upstream hardcodes true (e9529312). Deliberate divergence; see ADR-87-cr02-audit-bypass.md."
    Exact comment format: match the "Fork-extension:" comment style at lines ~1388 per PATTERNS.md.

    Step 2 — Update AuditVerificationResult.records_verified doc-comment (lines ~152-153):
    Replace "True when all record-level checks passed." with the updated doc that explains the
    semantic: "True when at least one record was processed and all record-level checks passed.
    False for an empty log, which means no integrity claim was made. (Fork divergence from
    upstream — see ADR-87-cr02-audit-bypass.md.)" See 87-PATTERNS.md §audit.rs for exact wording.

    Step 3 — Update is_valid() doc-comment (lines ~163-171):
    Add a note that is_valid() returns false for an empty log with stored: None. See 87-PATTERNS.md
    §audit.rs §is_valid() doc comment update for the exact addition.

    Step 4 — Add regression test in mod tests (line ~1412):
    Test name: verify_empty_log_with_no_stored_metadata_is_not_valid
    Use tempfile::tempdir(); write an empty AUDIT_EVENTS_FILENAME file; call verify_audit_log(dir.path(), None);
    assert result.event_count == 0, !result.records_verified, !result.is_valid().
    Inside the test block, #[allow(clippy::unwrap_used)] is acceptable (mod tests convention — per
    87-PATTERNS.md §no-unwrap pattern). See 87-PATTERNS.md §CR-02 test for the exact test body.

    Step 5 — Run the test to confirm it passes:
    cargo test -p nono -- verify_empty_log_with_no_stored_metadata_is_not_valid 2>&1
    Must exit 0.

    Step 6 — Write proj/ADR-87-cr02-audit-bypass.md:
    Follow the structure of proj/ADR-86-library-boundary-convergence.md (Status / Context /
    Decision / Rationale / Consequences sections). Record:
    - Status: Accepted (Phase 87)
    - Context: verify_audit_log hardcoded records_verified: true; upstream-identical at e9529312;
      empty-log with stored: None returns is_valid() = true (misleading).
    - Decision: Set records_verified to event_count > 0.
    - Rationale: Security-relevant — callers must not mistake "nothing checked" for "all verified".
      This is the first intentional divergence from upstream's audit code since Phase 86 convergence.
    - Consequences: Future upstream syncs will conflict on this line; reviewers must recognize it as
      intentional; the divergence ledger documents it.

    Step 7 — Add CR-02 addendum to 85-DIVERGENCE-LEDGER.md:
    Append a "## Phase 87 CR-02 Addendum" section at the end of the file. Record:
    - File: crates/nono/src/audit.rs, line ~1406 (verify_audit_log return block)
    - Upstream reference: e9529312 (~line 875/915)
    - Upstream behavior: records_verified hardcoded to true
    - Fork behavior after Phase 87: records_verified = event_count > 0
    - Reason: Audit-integrity bypass — empty-log with no stored metadata incorrectly reports is_valid() = true
    - Classification: deliberate fork-divergence (security hardening)
    - Expect conflict on future upstream syncs at this line — preserve the fork's behavior.

    Step 8 — Commit all four files together in one fork-hardening commit:
    git add crates/nono/src/audit.rs proj/ADR-87-cr02-audit-bypass.md .planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md
    git commit -m "fix(audit): records_verified now false for empty logs — CR-02 fork hardening

    Upstream hardcodes records_verified: true in verify_audit_log (e9529312).
    An empty log with stored: None returned is_valid() = true — no integrity claim
    was made but callers could not distinguish this from a verified-clean log.

    Set records_verified to event_count > 0. Add regression test.
    Record deliberate fork-divergence in ADR-87 and 85-DIVERGENCE-LEDGER.md.

    Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>"
  </action>
  <verify>
    <automated>cargo test -p nono -- verify_empty_log_with_no_stored_metadata_is_not_valid 2>&1</automated>
    Must exit 0.
    <automated>grep -n "event_count > 0" crates/nono/src/audit.rs</automated>
    Must return at least one line at or near line 1406.
    <automated>ls proj/ADR-87-cr02-audit-bypass.md</automated>
    File must exist.
    <automated>grep -c "CR-02" ".planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md"</automated>
    Must return >= 1.
    <automated>cargo check --workspace 2>&1 | tail -5</automated>
    Must exit 0.
  </verify>
  <acceptance_criteria>
    - audit.rs line ~1406 reads `records_verified: event_count > 0,` (not `true`)
    - The fork-hardening comment is present on the same line or above (per PATTERNS.md format)
    - AuditVerificationResult.records_verified field doc-comment updated to reflect false-for-empty semantics
    - is_valid() doc-comment updated with vacuous-false explanation
    - Test verify_empty_log_with_no_stored_metadata_is_not_valid passes (cargo test exits 0)
    - proj/ADR-87-cr02-audit-bypass.md exists, contains "e9529312", references the divergence
    - 85-DIVERGENCE-LEDGER.md has a Phase 87 CR-02 addendum section
    - Commit is a standalone fork-hardening commit (NOT a cherry-pick, NO "cherry picked from" line)
    - Commit carries "Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>"
    - cargo check --workspace exits 0 after this commit
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CapabilitySet.deduplicate() output → Landlock apply | The surviving FsCapability entries after dedup determine which Landlock rules are installed — a wrong original field corrupts the remap and causes incorrect rule paths |
| verify_audit_log result → caller's is_valid() decision | Callers use is_valid() to determine audit integrity; a false-true misleads security-relevant decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-06 | Tampering | procfs-remap dedup bug: /dev/null original overwritten by /dev/stdin symlink alias, corrupting Landlock rule to /proc/PID/fd/0 (SEC-02) | mitigate | is_procfs_remap_original guard at both original_updates push sites; regression test confirms fix. Closed by Task 1. |
| T-87-07 | Information Disclosure | audit-integrity bypass: verify_audit_log returns is_valid()=true for empty log with no stored metadata (CR-02) | mitigate | records_verified: event_count > 0; is_valid() now correctly returns false for empty log. Closed by Task 2. |
| T-87-08 | Repudiation | Missing divergence record for CR-02 fix could cause future syncs to silently revert the security hardening to upstream's hardcoded-true | mitigate | ADR-87-cr02-audit-bypass.md + 85-DIVERGENCE-LEDGER.md addendum explicitly mark this as intentional fork-divergence with expect-conflict note for future syncs. |
| T-87-SC | Tampering | npm/pip/cargo installs | accept | No new packages added. Package legitimacy gate N/A. |
</threat_model>

<verification>
## Post-Task Verification

After both Task 1 and Task 2 commits:

1. cargo check --workspace — must exit 0
2. git log --oneline -4 — must show: (HEAD) CR-02 fork-hardening commit; (HEAD~1) SEC-02 cherry-pick (6b3eb013); (HEAD~2) SEC-01 cherry-pick (e2086877)
3. grep -n "is_procfs_remap_original" crates/nono/src/capability.rs — must show helper + 2 guard sites + test usage
4. grep -n "event_count > 0" crates/nono/src/audit.rs — must show 1 hit at line ~1406
5. cargo test -p nono -- remap_preserves_dev_null_when_deduped_with_dev_stdin 2>&1 — exits 0
6. cargo test -p nono -- verify_empty_log_with_no_stored_metadata_is_not_valid 2>&1 — exits 0
7. Cross-target clippy: ATTEMPTED. If toolchain missing on Windows dev-host → record PARTIAL→CI
   per .planning/templates/cross-target-verify-checklist.md. SEC-02/CR-02 verification: PARTIAL
   pending Plan 87-03 CI-deferral confirmation.
</verification>

<success_criteria>
- SEC-02 guard closes the deduplicate() procfs-remap alias inheritance bug; regression test confirms fix
- CR-02 audit-integrity bypass closed; is_valid() returns false for empty log; divergence documented in ADR + ledger
- Two commits landed: one cherry-pick (6b3eb013 + DCO), one fork-hardening commit (DCO only)
- cargo check --workspace passes locally
- Cross-target clippy deferred to Plan 87-03 per checklist
- SEC-02 requirement: PARTIAL pending Plan 87-03
</success_criteria>

<output>
Create .planning/phases/87-security-sync/87-02-SUMMARY.md when done.
</output>
