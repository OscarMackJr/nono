---
phase: 88-feature-dependency-cherry-pick-wave
plan: 06
type: execute
wave: 6
depends_on:
  - 88-05
files_modified:
  - bindings/c/src/diagnostic.rs
  - bindings/c/src/lib.rs
  - crates/nono/Cargo.toml
  - Cargo.lock
  - .planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md
  - .planning/phases/88-feature-dependency-cherry-pick-wave/88-PARTIAL-CI.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - DEPS-02
must_haves:
  truths:
    - "nono_session_diagnostic_report_to_json() resets LAST_DIAGNOSTIC_CODE at entry (CR-01, D-10: clear-on-entry across ALL FFI entry points)"
    - "nono_merge_diagnostic_report_json() resets LAST_DIAGNOSTIC_CODE at entry (CR-01, D-10)"
    - "A C caller reading nono_last_diagnostic_code() after nono_merge_diagnostic_report_json(null, ...) gets NonoDiagnosticCode::Other (reset), not a stale code from a prior call"
    - "typify in Cargo.lock is version 0.7.x after DEPS-02; transitive bumps (x509-parser, time) resolved via cargo update -p, only typify is a direct Cargo.toml spec edit (D-04)"
    - "All 9 dep bumps land in ONE atomic DEPS-02 commit — typify spec edit + Cargo.lock updates together (D-05)"
    - "cargo test -p nono-ffi (or cargo test -p nono-cli --all-features) includes the CR-01 regression test"
    - "85-DIVERGENCE-LEDGER.md has a Phase 88 CR-01 addendum mirroring the Phase 87 CR-02 addendum (D-11: CR-01 lands as its own fork-divergence commit, not a cherry-pick)"
    - "D-06 path-dep pin gate: all 5 crate Cargo.toml internal version pins are consistent"
    - "make ci exits 0 on Windows (DEPS-02 lockfile changes compatible)"
    - "The 88-PARTIAL-CI.md COMPLETE section lists all deferred commits from plans 01-06"
  artifacts:
    - path: "bindings/c/src/lib.rs"
      provides: "clear_last_call_state() helper resetting all three thread-locals"
      contains: "clear_last_call_state"
    - path: "bindings/c/src/diagnostic.rs"
      provides: "CR-01 clear-on-entry at start of nono_session_diagnostic_report_to_json and nono_merge_diagnostic_report_json"
      contains: "clear_last_call_state"
    - path: "crates/nono/Cargo.toml"
      provides: "typify spec updated to 0.7"
      contains: "typify"
    - path: ".planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md"
      provides: "Phase 88 CR-01 addendum (fork-divergence record for CR-01)"
      contains: "CR-01"
  key_links:
    - from: "bindings/c/src/diagnostic.rs"
      to: "bindings/c/src/lib.rs"
      via: "crate::clear_last_call_state() call at FFI entry"
      pattern: "clear_last_call_state"
---

<objective>
Land CR-01 (FFI stale diagnostic state fix) as a deliberate fork-divergence commit with regression test, then land the DEPS-02 atomic dep bump commit. Record CR-01 in the DIVERGENCE-LEDGER addendum (D-11). Close out the phase with the D-06 5-crate pin gate, a PARTIAL→CI completion record, and a final `make ci` gate.

Purpose: CR-01 is the second deliberate fork-divergence on the Phase-86-converged FFI surface (after CR-02 in Phase 87). It fixes the stale LAST_DIAGNOSTIC_CODE bug found in code-review. DEPS-02 absorbs 9 dep bumps atomically. D-06 ensures no accidental version drift across the 5-crate workspace. The phase closes with the PARTIAL→CI record finalizing the GH Actions deferral list.

Output: Two commits (CR-01 fork fix, DEPS-02 dep bump) with DCO. DIVERGENCE-LEDGER Phase 88 CR-01 addendum. 88-PARTIAL-CI.md complete. `make ci` green. ROADMAP Phase 88 plans list updated.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/88-feature-dependency-cherry-pick-wave/88-CONTEXT.md
@.planning/phases/88-feature-dependency-cherry-pick-wave/88-RESEARCH.md
@.planning/phases/88-feature-dependency-cherry-pick-wave/88-PATTERNS.md
@.planning/phases/88-feature-dependency-cherry-pick-wave/88-PARTIAL-CI.md
@.planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md

<interfaces>
From bindings/c/src/lib.rs (existing nono_clear_error pattern — clear_last_call_state mirrors this):
```rust
// Existing public clear (nono_clear_error at lines 263-273):
pub extern "C" fn nono_clear_error() {
    LAST_ERROR.with(|cell| { *cell.borrow_mut() = None; });
    LAST_DIAGNOSTIC_CODE.with(|cell| { *cell.borrow_mut() = None; });
    LAST_REMEDIATION_JSON.with(|cell| { *cell.borrow_mut() = None; });
}
// New INTERNAL helper (pub(crate), not #[unsafe(no_mangle)]):
pub(crate) fn clear_last_call_state() {
    LAST_ERROR.with(|c| *c.borrow_mut() = None);
    LAST_DIAGNOSTIC_CODE.with(|c| *c.borrow_mut() = None);
    LAST_REMEDIATION_JSON.with(|c| *c.borrow_mut() = None);
}
```

From bindings/c/src/diagnostic.rs (entry points to fix — lines 37+ and lines 92+):
```rust
// At entry of nono_session_diagnostic_report_to_json:
crate::clear_last_call_state();  // CR-01: reset stale diagnostic state from prior call
// At entry of nono_merge_diagnostic_report_json:
crate::clear_last_call_state();  // CR-01: reset stale diagnostic state from prior call
```

D-11 regression test pattern (88-RESEARCH.md §CR-01 and 88-PATTERNS.md §diagnostic.rs):
```rust
#[test]
fn diagnostic_code_is_cleared_between_calls() {
    // after a map_error path sets LAST_DIAGNOSTIC_CODE to something non-Other,
    // the next call to nono_merge_diagnostic_report_json (which uses set_last_error-only path)
    // must result in nono_last_diagnostic_code() == NonoDiagnosticCode::Other
    let json_ptr = unsafe {
        nono_merge_diagnostic_report_json(std::ptr::null(), std::ptr::null())
    };
    assert!(json_ptr.is_null());
    assert_eq!(nono_last_diagnostic_code(), NonoDiagnosticCode::Other);
}
```

DEPS-02 commit sequence (D-05 atomic):
  Step 1: Edit crates/nono/Cargo.toml line 71 — change typify = "0.6" → typify = "0.7"
  Step 2: cargo update -p typify; cargo update -p cbindgen; cargo update -p hyper;
           cargo update -p zeroize; cargo update -p time; cargo update -p chrono;
           cargo update -p ignore; cargo update -p which; cargo update -p x509-parser
  Step 3: One atomic commit: Cargo.toml change + Cargo.lock changes

D-06 gate command:
  grep "version" Cargo.toml crates/nono/Cargo.toml crates/nono-cli/Cargo.toml
      crates/nono-proxy/Cargo.toml bindings/c/Cargo.toml | grep -E 'nono = \{|nono-proxy = \{'
  All path-dep version pins should be consistent. Phase 88 does NOT change crate [package] versions.

Phase 87 CR-02 addendum pattern (from 85-DIVERGENCE-LEDGER.md lines 807-828):
  Mirror the table structure exactly for CR-01:
  | File | bindings/c/src/diagnostic.rs + lib.rs |
  | Reason | stale LAST_DIAGNOSTIC_CODE bug (86-REVIEW.md CR-01) |
  | Upstream reference | a6aa9995 (already absorbed Phase 86; this is the extension) |
  | Fork behavior | clear_last_call_state() at entry of every pub unsafe extern "C" fn |
  | Classification | Deliberate fork-divergence — correctness hardening |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01 FFI clear-on-entry fix + regression test + DIVERGENCE-LEDGER addendum</name>
  <files>
    bindings/c/src/lib.rs,
    bindings/c/src/diagnostic.rs,
    .planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md
  </files>
  <read_first>
    - bindings/c/src/lib.rs lines 46-51 (LAST_ERROR, LAST_DIAGNOSTIC_CODE, LAST_REMEDIATION_JSON thread-locals)
    - bindings/c/src/lib.rs lines 263-273 (nono_clear_error — template for clear_last_call_state)
    - bindings/c/src/lib.rs lines 87-193 (map_error — the correctly-implemented path that sets all three)
    - bindings/c/src/diagnostic.rs lines 37-48 (nono_session_diagnostic_report_to_json entry)
    - bindings/c/src/diagnostic.rs lines 92-99 (nono_merge_diagnostic_report_json entry — the two set_last_error-only paths)
    - bindings/c/src/diagnostic.rs lines 146+ (test section — where regression test goes)
    - 88-PATTERNS.md §lib.rs for the clear_last_call_state() implementation
    - 88-PATTERNS.md §diagnostic.rs for the exact CR-01 fix pattern and regression test
    - 88-RESEARCH.md §CR-01 for the bug description and D-10/D-11 fix pattern
    - .planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md lines 807-828 (Phase 87 CR-02 addendum template)
    - .planning/phases/86-library-boundary-convergence/86-REVIEW.md (find CR-01 finding text — the full description)
  </read_first>
  <behavior>
    - nono_merge_diagnostic_report_json(null, ...) returns null AND nono_last_diagnostic_code() returns Other (not stale code from prior call)
    - nono_session_diagnostic_report_to_json(invalid args) returns null AND nono_last_diagnostic_code() returns Other
    - After a successful map_error call that sets a specific diagnostic code, calling nono_merge_diagnostic_report_json(null, ...) resets the code to Other
    - clear_last_call_state() is pub(crate) and resets all three thread-locals atomically
    - All other pub unsafe extern "C" fns in bindings/c/src/ that can set thread-locals also call clear_last_call_state() at entry (systematic fix per D-10)
  </behavior>
  <action>
    This is NOT a cherry-pick. This is a deliberate fork-divergence commit (D-11).

    Step 1 — Add clear_last_call_state() helper to bindings/c/src/lib.rs:
    Insert a new pub(crate) function after the existing set_last_error() function (around line 85). The function body mirrors nono_clear_error() (lines 263-273) but is internal (pub(crate), no #[unsafe(no_mangle)]). Reference 88-PATTERNS.md §lib.rs for the exact implementation.

    Step 2 — Add clear_last_call_state() at entry of EVERY pub unsafe extern "C" fn in bindings/c/src/ that can set any thread-local. Per D-10 (systematic fix):
    - bindings/c/src/diagnostic.rs::nono_session_diagnostic_report_to_json — add at line ~40 (before parse_denials call)
    - bindings/c/src/diagnostic.rs::nono_merge_diagnostic_report_json — add at line ~95 (before the null check)
    - Scan ALL other pub unsafe extern "C" fns in bindings/c/src/ (lib.rs, capability_set.rs, fs_capability.rs, sandbox.rs, query.rs, state.rs) that call set_last_error() or map_error() — add clear_last_call_state() at entry of each.

    Step 3 — Add regression test to bindings/c/src/diagnostic.rs test section:
    Add the diagnostic_code_is_cleared_between_calls test from 88-PATTERNS.md §diagnostic.rs. The test must: (a) invoke a path that sets LAST_DIAGNOSTIC_CODE to something non-Other, (b) call nono_merge_diagnostic_report_json(null, null), (c) assert nono_last_diagnostic_code() == Other.

    Step 4 — Commit with the deliberate fork-divergence message pattern from 88-PATTERNS.md §commit message:
    ```
    fix(ffi): clear stale diagnostic state on every FFI entry (CR-01)

    Deliberate fork-divergence: clear LAST_DIAGNOSTIC_CODE and LAST_REMEDIATION_JSON
    at the entry of all pub unsafe extern "C" functions to prevent stale diagnostic
    codes from a prior call on the same thread.

    Upstream-equivalent fix at a6aa9995 (already absorbed via Phase 86 boundary
    convergence; this is the fork-specific extension to the clear-on-entry pattern).
    Recorded as fork-divergence in 85-DIVERGENCE-LEDGER.md Phase 88 addendum.
    ```
    Signed-off-by: Oscar Mack Jr &lt;oscar.mack.jr@gmail.com&gt;
    DO NOT use `git cherry-pick`. DO NOT include "(cherry picked from commit ...)". This is a fork fix.

    Step 5 — Add Phase 88 CR-01 addendum to 85-DIVERGENCE-LEDGER.md:
    Add a new section "## Phase 88 CR-01 Addendum" after the Phase 87 CR-02 addendum (lines 807-828).
    Mirror the table structure exactly with these values:
    - File: `bindings/c/src/diagnostic.rs` + `bindings/c/src/lib.rs`
    - Fork lines: nono_session_diagnostic_report_to_json() line ~40, nono_merge_diagnostic_report_json() line ~95 (+ all other pub extern "C" entry points)
    - Upstream reference commit: a6aa9995 (absorbed in Phase 86; this extends the pattern)
    - Upstream behavior: upstream had a similar stale-code pattern; a6aa9995 was the upstream fix for the diagnostic module; CR-01 extends the fix to the set_last_error-only paths in the fork
    - Fork behavior after Phase 88: clear_last_call_state() called at every pub unsafe extern "C" fn entry
    - Reason: 86-REVIEW.md CR-01 finding — set_last_error() callers in nono_session_diagnostic_report_to_json and nono_merge_diagnostic_report_json did not reset LAST_DIAGNOSTIC_CODE before calling set_last_error, leaving callers with stale diagnostic codes from prior FFI calls on the same thread
    - Classification: Deliberate fork-divergence — correctness hardening
    - Future sync note: When upstream a6aa9995 surface is re-synced, the clear_last_call_state() call at entry MUST be preserved. Any upstream commit removing these entry-point clears must be evaluated carefully.

    Commit the DIVERGENCE-LEDGER update: `git add .planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md && git commit -m "docs(ledger): add Phase 88 CR-01 fork-divergence addendum" -s`
  </action>
  <verify>
    <automated>cargo test -p nono-ffi 2>&1 | tail -20</automated>
    <automated>grep -n "clear_last_call_state" bindings/c/src/lib.rs bindings/c/src/diagnostic.rs 2>&1</automated>
    <automated>grep -n "diagnostic_code_is_cleared_between_calls" bindings/c/src/diagnostic.rs 2>&1</automated>
    <automated>grep -n "Phase 88 CR-01" .planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md 2>&1</automated>
  </verify>
  <done>
    CR-01 fix committed as fork-divergence (NOT cherry-pick) with DCO. clear_last_call_state() helper exists in lib.rs. Both diagnostic.rs entry points call it. Regression test exists and passes. DIVERGENCE-LEDGER has Phase 88 CR-01 addendum mirroring the CR-02 format.
  </done>
</task>

<task type="auto">
  <name>Task 2: DEPS-02 atomic dep bump commit + D-06 path-dep pin gate</name>
  <files>
    crates/nono/Cargo.toml,
    Cargo.lock
  </files>
  <read_first>
    - crates/nono/Cargo.toml line 71 (typify spec: should be "0.6" before this task, changes to "0.7")
    - Cargo.toml (workspace root), crates/nono-cli/Cargo.toml, crates/nono-proxy/Cargo.toml, bindings/c/Cargo.toml (scan for internal nono path-dep version pins — D-06 gate)
    - 88-PATTERNS.md §Cargo.toml for the typify spec edit and the cargo update command sequence
    - 88-RESEARCH.md §DEPS-02 for the complete bump list and D-06 gate command
    - 88-RESEARCH.md §Standard Stack table (current → target versions for all 9 deps)
  </read_first>
  <action>
    Step 1 — D-06 pre-flight gate: Run the path-dep pin consistency check BEFORE making any changes:
    `grep "version" Cargo.toml crates/nono/Cargo.toml crates/nono-cli/Cargo.toml crates/nono-proxy/Cargo.toml bindings/c/Cargo.toml | grep -E "nono = \{|nono-proxy = \{"`
    Record the output. If any version pins are already inconsistent (pre-existing drift), note them and fix before proceeding. Phase 88 does NOT change crate [package] versions, so all pins should already be consistent.

    Step 2 — Typify spec edit in crates/nono/Cargo.toml line 71:
    Change `typify = { version = "0.6", ... }` to `typify = { version = "0.7", ... }`.
    ONLY this file needs a Cargo.toml edit for DEPS-02.

    Step 3 — Lockfile-only bumps via cargo update:
    Run each command in sequence:
    - `cargo update -p typify` (resolves 0.7.0)
    - `cargo update -p cbindgen` (0.29.2 → 0.29.4)
    - `cargo update -p hyper` (1.9.0 → 1.10.1; note: hyper 0.14.x legacy slot is separate, bump v1 slot only)
    - `cargo update -p zeroize` (1.8.2 → 1.9.0)
    - `cargo update -p time` (0.3.47 → 0.3.49; transitive)
    - `cargo update -p chrono` (0.4.44 → 0.4.45)
    - `cargo update -p ignore` (0.4.25 → 0.4.26)
    - `cargo update -p which` (8.0.2 → 8.0.3)
    - `cargo update -p x509-parser` (transitive; pulls in 0.18.1 if available)

    Step 4 — Verify no codegen failures: Run `make build-lib` to trigger the typify-driven build.rs. If typify 0.7 produces different codegen for the schema, the build.rs may regenerate capability_manifest_types.rs differently. `make build-lib` success confirms no breaking codegen change (Pitfall 5 from RESEARCH.md).

    Step 5 — D-06 post-flight gate: Re-run the path-dep pin consistency check and confirm all 5 crate Cargo.toml files have consistent version pins. If the cargo update accidentally modified any internal path-dep version (unlikely but possible if workspace deps changed), fix it now.

    Step 6 — One atomic commit:
    `git add crates/nono/Cargo.toml Cargo.lock`
    `git commit -m "chore(deps): absorb 9 dep bumps from v0.62..v0.64 window (DEPS-02)" -s`
    Include in body: "typify 0.6→0.7 (only Cargo.toml edit; non-breaking per CHANGELOG.adoc). All other bumps are lockfile-only. D-06 path-dep pin gate: all 5 crate Cargo.toml version pins verified consistent."
  </action>
  <verify>
    <automated>make build-lib 2>&1 | tail -10</automated>
    <automated>grep -A2 "typify" crates/nono/Cargo.toml 2>&1 | head -5</automated>
    <automated>grep "^typify " Cargo.lock 2>&1 | head -3</automated>
    <automated>grep "version" Cargo.toml crates/nono/Cargo.toml crates/nono-cli/Cargo.toml crates/nono-proxy/Cargo.toml bindings/c/Cargo.toml 2>&1 | grep -E "nono = \{|nono-proxy = \{"</automated>
  </verify>
  <done>
    Cargo.toml typify spec is "0.7". Cargo.lock shows typify 0.7.x. `make build-lib` exits 0. D-06 gate shows consistent path-dep pins across all 5 Cargo.toml files. One atomic commit with DCO.
  </done>
</task>

<task type="auto">
  <name>Task 3: make ci final gate + 88-PARTIAL-CI.md closeout + ROADMAP update</name>
  <files>
    .planning/phases/88-feature-dependency-cherry-pick-wave/88-PARTIAL-CI.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/phases/88-feature-dependency-cherry-pick-wave/88-PARTIAL-CI.md (current state — should have entries from Plans 01-05; add closeout section)
    - .planning/ROADMAP.md §"Phase 88: Feature + Dependency Cherry-Pick Wave" (update plans list from TBD to the 6 actual plans)
    - .planning/templates/cross-target-verify-checklist.md (PARTIAL→CI completion format)
  </read_first>
  <action>
    Step 1 — Run `make ci`. This is the definitive Windows-host gate for Phase 88. Fix any failures before proceeding.

    Step 2 — Finalize 88-PARTIAL-CI.md with a "## Summary" section listing all PARTIAL→CI deferrals across the 6 plans:
    ```
    ## Summary of PARTIAL→CI Deferrals for Phase 88

    All items below require GH Actions Linux/macOS CI lanes to achieve a PASS verdict.
    Per CLAUDE.md §"Cross-target clippy verification" MUST/NEVER rule, Windows-host
    cargo clippy is NOT a substitute.

    | Plan | Commit | File | Deferred Verification |
    |------|--------|------|----------------------|
    | 88-01 | d48aeb7b | exec_strategy.rs | cfg-gated Unix blocks in file |
    | 88-02 | e8293b36 | audit_session.rs | #[cfg(unix)] perms block |
    | 88-02 | D-02 fork | state_paths.rs | #[cfg(target_os="windows")] + not-windows arm |
    | 88-03 | 7d274cf7 | hook_runtime.rs | #[cfg(unix)] pre_exec block |
    | 88-04 | 4179ce03 | exec_strategy.rs | nix:: PTY functions (cfg-gated) |
    | 88-04 | 4179ce03 | pty_proxy.rs | Unix-only module |
    | 88-05 | e54cf9cb | hook_runtime.rs | #[cfg(unix)] elements |

    Status: PARTIAL→CI — Decisive verification pending GH Actions Linux + macOS CI lanes.
    ```

    Step 3 — Update ROADMAP.md §"Phase 88" to replace "Plans: TBD" with the actual 6 plans:
    ```
    **Plans**: 6 plans
    Plans:
    **Wave 1**
    - [ ] 88-01-PLAN.md — FEAT-01 set_vars + FEAT-04 keyring timeout
    **Wave 2** *(blocked on Wave 1)*
    - [ ] 88-02-PLAN.md — FEAT-02 XDG state dirs + D-01/D-02/D-03 reconciliation
    **Wave 3** *(blocked on Wave 2)*
    - [ ] 88-03-PLAN.md — FEAT-03 AWS auth (partial) + fork 501 stub + FEAT-05 $PACK_DIR
    **Wave 4** *(blocked on Wave 3)*
    - [ ] 88-04-PLAN.md — DEPS-01 PTY ctrl-z + FEAT-06a CI provider + FEAT-06b profile namespace
    **Wave 5** *(blocked on Wave 4)*
    - [ ] 88-05-PLAN.md — FEAT-06c truthy flags + Cluster M misc + e54cf9cb env_clear Unix-only
    **Wave 6** *(blocked on Wave 5)*
    - [ ] 88-06-PLAN.md — CR-01 FFI fix + DEPS-02 dep bumps + D-06 gate + PARTIAL→CI closeout
    ```

    Step 4 — Commit both files:
    `git add .planning/phases/88-feature-dependency-cherry-pick-wave/88-PARTIAL-CI.md .planning/ROADMAP.md && git commit -m "docs(88): finalize PARTIAL→CI deferral record + update ROADMAP plan list" -s`
  </action>
  <verify>
    <automated>make ci 2>&1 | tail -20</automated>
    <automated>grep -n "88-01-PLAN\|88-06-PLAN" .planning/ROADMAP.md 2>&1</automated>
    <automated>grep -n "Summary of PARTIAL" .planning/phases/88-feature-dependency-cherry-pick-wave/88-PARTIAL-CI.md 2>&1</automated>
  </verify>
  <done>
    `make ci` exits 0. ROADMAP.md shows all 6 Phase 88 plans. 88-PARTIAL-CI.md has a complete Summary table with all deferrals. All Phase 88 commits have DCO sign-offs. The definitive Windows CI gate is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FFI thread-local state → C caller | LAST_DIAGNOSTIC_CODE must reflect ONLY the current FFI call, not bleed from prior calls |
| Dependency supply chain (DEPS-02) | 9 packages bumped; all are existing trusted deps (minor/patch versions) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-22 | Spoofing | Stale LAST_DIAGNOSTIC_CODE leaking between FFI calls (CR-01) | mitigate | clear_last_call_state() called at entry of ALL pub unsafe extern "C" functions that can set thread-locals; regression test asserts the code is Other after a set_last_error-only path |
| T-88-23 | Tampering | typify 0.7 codegen producing different output that alters generated types | accept | CHANGELOG.adoc verified: 0.7 only adds two JSON Schema pattern features; TypeSpaceSettings::default().with_struct_builder(true) is unchanged; `make build-lib` is the verification gate; no snapshot tests exist for generated code |
| T-88-24 | Tampering | cargo update pulling unexpected transitive dependency upgrades | mitigate | Run `cargo update -p <package>` per-package (not `cargo update`); inspect Cargo.lock diff for unexpected changes before committing; D-06 gate confirms no path-dep drift |
| T-88-25 | Repudiation | CR-01 fix not recorded in DIVERGENCE-LEDGER | mitigate | Phase 88 CR-01 addendum in 85-DIVERGENCE-LEDGER.md per D-11; mirrors the Phase 87 CR-02 addendum template; future sync auditors will see the expected conflict |
| T-88-SC | Tampering | cargo dependency installs | mitigate | All 9 bumped packages are existing trusted deps (minor/patch only); Package Legitimacy Audit in RESEARCH.md shows all Approved; no [SUS] packages; slopcheck not required for patch bumps of established crates |
</threat_model>

<verification>
Phase 88 complete verification:
1. `git log --oneline -20` — shows 17 commits (14 cherry-picks + CR-01 + DEPS-02 + ledger update + at least 1 docs commit); each cherry-pick has "(cherry picked from commit XXXX)"
2. `grep -n "clear_last_call_state" bindings/c/src/lib.rs bindings/c/src/diagnostic.rs` — helper defined in lib.rs, called in both diagnostic.rs entry points
3. `cargo test -p nono-ffi -- diagnostic_code_is_cleared_between_calls` → test passes
4. `grep "typify" crates/nono/Cargo.toml` → "0.7"
5. `grep "^name = \"typify\"" Cargo.lock -A 1 | grep version` → 0.7.x
6. `grep -n "Phase 88 CR-01" .planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md` → addendum present
7. `make ci` exits 0
8. `grep -n "88-06-PLAN" .planning/ROADMAP.md` → ROADMAP updated
9. `grep -n "Summary of PARTIAL" .planning/phases/88-feature-dependency-cherry-pick-wave/88-PARTIAL-CI.md` → closeout section present

FEAT requirements verification:
- FEAT-01: `grep -n "validate_set_vars" crates/nono-cli/src/exec_strategy/env_sanitization.rs` → exists
- FEAT-02: `ls crates/nono-cli/src/state_paths.rs` → exists; `grep -n "LOCALAPPDATA" crates/nono-cli/src/state_paths.rs` → Windows arm
- FEAT-03: `grep -n "AwsAuthConfig" crates/nono-proxy/src/config.rs` → exists; `grep -n "get_aws" crates/nono-proxy/src/reverse.rs` → 501 stub present
- FEAT-04: `grep -n "NONO_KEYRING_TIMEOUT_SECS" crates/nono/src/keystore.rs` → exists
- FEAT-05: `grep -n "PACK_DIR" crates/nono-cli/src/hook_runtime.rs` → exists
- FEAT-06: `grep -n "detect_ci_provider" crates/nono-cli/src/update_check.rs` → exists; `grep -n "always-further/claude" crates/nono-cli/data/policy.json` → alias present; `grep -n "BoolishValueParser" crates/nono-cli/src/cli.rs` → exists
- DEPS-01: `grep -n "handle_pty_suspension\|signal_pty_foreground_group" crates/nono-cli/src/exec_strategy.rs` → exists
- DEPS-02: `grep "^name = \"typify\"" Cargo.lock -A 1` → 0.7.x
</verification>

<success_criteria>
- CR-01: clear_last_call_state() helper in lib.rs; called at entry of all pub unsafe extern "C" fns in bindings/c/src/; regression test passes; committed as deliberate fork-divergence (not cherry-pick) with DCO (D-11)
- Phase 88 CR-01 addendum in 85-DIVERGENCE-LEDGER.md mirroring Phase 87 CR-02 format (D-11)
- DEPS-02: typify 0.7 in Cargo.toml + lockfile; all 9 bumps in one atomic commit with DCO (D-05)
- D-06 path-dep pin gate: all 5 Cargo.toml files have consistent internal version pins (verified pre- and post-DEPS-02)
- make ci exits 0 (definitive Windows gate for the entire Phase 88 wave)
- 88-PARTIAL-CI.md finalized with summary table of all 7 deferrals across Plans 01-06
- ROADMAP.md Phase 88 plans section updated from TBD to 6 actual plan entries
</success_criteria>

<output>
Create `.planning/phases/88-feature-dependency-cherry-pick-wave/88-06-SUMMARY.md` when done
</output>
