---
phase: 88-feature-dependency-cherry-pick-wave
plan: 03
type: execute
wave: 3
depends_on:
  - 88-02
files_modified:
  - crates/nono-proxy/src/config.rs
  - crates/nono-proxy/src/credential.rs
  - crates/nono-proxy/src/route.rs
  - crates/nono-proxy/src/server.rs
  - crates/nono-proxy/src/reverse.rs
  - crates/nono-cli/src/network_policy.rs
  - crates/nono-cli/src/profile/mod.rs
  - crates/nono-cli/src/hook_runtime.rs
  - crates/nono-cli/src/profile_runtime.rs
autonomous: true
requirements:
  - FEAT-03
  - FEAT-05
must_haves:
  truths:
    - "A profile with aws_auth config is accepted and mutually excludes credential_key and oauth2"
    - "A profile with aws_auth AND credential_key returns a profile-load-time validation error"
    - "A route with aws_auth.is_some() returns 501 from the non-TLS proxy path"
    - "A session hook with $PACK_DIR resolves the pack directory correctly"
    - "Cherry-pick 7d274cf7 ($PACK_DIR) is committed BEFORE any e54cf9cb apply in this wave"
  artifacts:
    - path: "crates/nono-proxy/src/config.rs"
      provides: "AwsAuthConfig struct and aws_auth field on RouteConfig"
      contains: "AwsAuthConfig"
    - path: "crates/nono-proxy/src/credential.rs"
      provides: "aws_routes HashMap and get_aws() accessor in CredentialStore"
      contains: "aws_routes"
    - path: "crates/nono-proxy/src/reverse.rs"
      provides: "501 stub for aws_route on non-TLS path (D-15 fork adaptation)"
      contains: "NOT_IMPLEMENTED"
    - path: "crates/nono-cli/src/hook_runtime.rs"
      provides: "PACK_DIR env injection in build_hook_command() (I-before-M per D-13)"
      contains: "PACK_DIR"
  key_links:
    - from: "crates/nono-cli/src/profile/mod.rs"
      to: "crates/nono-proxy/src/config.rs"
      via: "validate_custom_credential mutual-exclusion check for AwsAuthConfig"
      pattern: "validate_custom_credential.*aws_auth\|aws_auth.*validate"
    - from: "crates/nono-proxy/src/reverse.rs"
      to: "crates/nono-proxy/src/credential.rs"
      via: "credential_store.get_aws() → 501 short-circuit"
      pattern: "get_aws.*NOT_IMPLEMENTED\|aws.*501"
---

<objective>
Cherry-pick `5bb098cd` (FEAT-03: AWS auth, partial — tls_intercept hunk SKIPPED per D-15) + wire the fork-specific 501 stub in reverse.rs. Then cherry-pick `7d274cf7` (FEAT-05: $PACK_DIR session hooks). This plan enforces the I-before-M ordering constraint (D-13): `7d274cf7` (Cluster I) MUST commit before the `e54cf9cb` env_clear removal in Plan 88-05.

Purpose: Deliver the proxy AWS credential path and the $PACK_DIR session hook expansion. The AWS 501 stub is a fork-specific adaptation (D-15): upstream's 501 lives in tls_intercept/handle.rs (won't-apply on this fork); the fork needs an equivalent guard in reverse.rs.

Output: Two committed cherry-picks (one partial for FEAT-03 with a fork amendment, one clean for FEAT-05), each with DCO. `make ci` green on Windows.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/88-feature-dependency-cherry-pick-wave/88-CONTEXT.md
@.planning/phases/88-feature-dependency-cherry-pick-wave/88-RESEARCH.md
@.planning/phases/88-feature-dependency-cherry-pick-wave/88-PATTERNS.md
@.planning/phases/88-feature-dependency-cherry-pick-wave/88-PARTIAL-CI.md

<interfaces>
From crates/nono-proxy/src/config.rs (new struct shape from 5bb098cd):
```rust
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct AwsAuthConfig {
    #[serde(default)] pub profile: Option<String>,
    #[serde(default)] pub region: Option<String>,
    #[serde(default)] pub service: Option<String>,
}
// Added to RouteConfig:
#[serde(default)]
pub aws_auth: Option<AwsAuthConfig>,
```

From crates/nono-proxy/src/credential.rs (new accessor):
```rust
#[must_use]
pub fn get_aws(&self, prefix: &str) -> Option<&()> {
    self.aws_routes.get(prefix)
}
```

From crates/nono-proxy/src/reverse.rs (fork 501 stub — after service prefix parse):
After parsing `service` (the route prefix), before forwarding — check
`ctx.credential_store.get_aws(&service)`. If Some, send a 501 response:
  send_error(stream, 501, "AWS auth not supported on this proxy path").await

From crates/nono-cli/src/profile/mod.rs (mutual-exclusion per 88-RESEARCH.md §FEAT-03):
```rust
// In validate_custom_credential():
if cred.aws_auth.is_some() && (cred.credential_key.is_some() || cred.oauth2.is_some()) {
    return Err(ConfigError("aws_auth is mutually exclusive with credential_key and oauth2"))
}
```

I-before-M ordering constraint (D-13):
  7d274cf7 ($PACK_DIR) touches hook_runtime.rs::build_hook_command()
  e54cf9cb (in Plan 88-05) removes env_clear() from the same function
  7d274cf7 MUST be committed in this plan before e54cf9cb in Plan 88-05
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cherry-pick 5bb098cd partial (FEAT-03 AWS auth) + fork 501 stub</name>
  <files>
    crates/nono-proxy/src/config.rs,
    crates/nono-proxy/src/credential.rs,
    crates/nono-proxy/src/route.rs,
    crates/nono-proxy/src/server.rs,
    crates/nono-proxy/src/reverse.rs,
    crates/nono-cli/src/network_policy.rs,
    crates/nono-cli/src/profile/mod.rs
  </files>
  <read_first>
    - Run: git show 5bb098cd (inspect ALL hunks; identify the tls_intercept/handle.rs hunk to skip)
    - crates/nono-proxy/src/config.rs (find RouteConfig struct; find OAuth2Config as the pattern for AwsAuthConfig shape)
    - crates/nono-proxy/src/credential.rs lines 123-261 (CredentialStore struct + load/empty/is_empty/len/loaded_prefixes methods)
    - crates/nono-proxy/src/route.rs lines 450-474 (fork-divergence comment about requires_managed_credential being absent)
    - crates/nono-proxy/src/reverse.rs lines 71-120 (handle_reverse_proxy entry; find the service prefix parse → forwarding path where 501 stub goes)
    - crates/nono-proxy/src/server.rs lines 598-615 (non-CONNECT → reverse proxy dispatch)
    - crates/nono-cli/src/profile/mod.rs (find validate_custom_credential; find the oauth2 mutual-exclusion pattern as template)
    - 88-PATTERNS.md §config.rs, §credential.rs, §route.rs, §profile/mod.rs for exact code shapes
    - 88-RESEARCH.md §FEAT-03 for the shared-surface hunk list and the 501 stub analysis
  </read_first>
  <behavior>
    - RouteConfig has aws_auth: Option&lt;AwsAuthConfig&gt; with serde(default) — backwards compatible
    - CredentialStore has aws_routes: HashMap&lt;String, ()&gt; and get_aws() accessor
    - CredentialStore::load() populates aws_routes from routes with aws_auth.is_some()
    - route.rs: requires_managed_credential() returns true when aws_auth.is_some()
    - profile/mod.rs validate_custom_credential(): aws_auth + credential_key → Err; aws_auth + oauth2 → Err
    - reverse.rs: request to an aws_auth route returns 501 (NOT_IMPLEMENTED) with body "AWS auth not supported on this proxy path"
    - All RouteConfig struct literal constructions in tests compile (aws_auth: None added where missing)
    - tls_intercept/handle.rs hunk is NOT present (the fork has no tls_intercept directory)
  </behavior>
  <action>
    Use `git cherry-pick -x 5bb098cd` BUT apply with `--no-commit` first:
    `git cherry-pick -x --no-commit 5bb098cd`

    Then inspect the staged changes. Find and DISCARD any changes touching `crates/nono-proxy/src/tls_intercept/` (the fork has no such directory). If git reports a conflict on that path, use `git checkout HEAD -- crates/nono-proxy/src/tls_intercept/` to discard.

    Apply the remaining shared-surface hunks (config.rs, credential.rs, route.rs, server.rs, network_policy.rs, profile/mod.rs).

    Fork-specific adaptations to apply BEFORE committing:

    1. AWS 501 stub in reverse.rs (D-15 fork gap — this file is NOT in the upstream 5bb098cd diff): After the cherry-pick stages the shared hunks, open reverse.rs and add a guard in `handle_reverse_proxy()`. The insertion point is AFTER parsing the service prefix from the request path (around line 94, after `let (service, _rest) = parse_service_prefix(&path)?`) and BEFORE the credential lookup at line 118. Add:
       `if ctx.credential_store.get_aws(&service).is_some() { send_error(stream, 501, "AWS auth not supported on this proxy path").await?; return Ok(()); }`
       Import hyper::StatusCode is not needed if send_error() takes u16 — check the function signature at line 634.

    2. Mutual-exclusion in profile/mod.rs validate_custom_credential(): Add the aws_auth check AFTER the existing oauth2 check. The exact upstream pattern is in 88-PATTERNS.md §profile/mod.rs. Ensure the error message names all three fields.

    3. All RouteConfig struct literals in test code (server.rs tests, route.rs tests) must compile — add `aws_auth: None` to every literal. The compiler will catch missed cases at cargo check.

    4. In CredentialStore::empty(), add `aws_routes: HashMap::new()` to the literal. In CredentialStore::load(), populate aws_routes by iterating routes and inserting prefix → () for routes with aws_auth.is_some().

    Commit with: `git commit -m "feat(proxy): add AwsAuthConfig + 501 stub on non-TLS path (FEAT-03)" -s`
    Include "(cherry picked from commit 5bb098cd)" in the body manually since --no-commit was used.
    Note in the commit body: "tls_intercept/handle.rs hunk skipped — fork has no tls_intercept directory (D-15). Fork-specific: 501 stub added to reverse.rs non-TLS path."
  </action>
  <verify>
    <automated>cargo test -p nono-proxy 2>&1 | tail -20</automated>
    <automated>grep -n "AwsAuthConfig\|aws_auth\|aws_routes" crates/nono-proxy/src/config.rs crates/nono-proxy/src/credential.rs 2>&1 | head -20</automated>
    <automated>grep -n "NOT_IMPLEMENTED\|501\|get_aws" crates/nono-proxy/src/reverse.rs 2>&1</automated>
    <automated>grep -n "aws_auth.*credential_key\|validate_custom_credential" crates/nono-cli/src/profile/mod.rs 2>&1 | head -5</automated>
  </verify>
  <done>
    Commit shows "(cherry picked from commit 5bb098cd)" and DCO. AwsAuthConfig struct exists in proxy/config.rs. aws_routes HashMap exists in credential.rs. reverse.rs returns 501 when get_aws() returns Some. profile/mod.rs validates mutual-exclusion at profile-load time. `cargo test -p nono-proxy` passes on Windows host. No tls_intercept directory created.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cherry-pick 7d274cf7 (FEAT-05 $PACK_DIR session hooks) — I-before-M per D-13</name>
  <files>
    crates/nono-cli/src/hook_runtime.rs,
    crates/nono-cli/src/profile/mod.rs,
    crates/nono-cli/src/profile_runtime.rs
  </files>
  <read_first>
    - Run: git show 7d274cf7 (inspect full diff — 1002+/78-; large commit touching hook_runtime.rs + profile/mod.rs + profile_runtime.rs)
    - crates/nono-cli/src/hook_runtime.rs lines 189-223 (current build_hook_command — find env_clear() and the env injections pattern; PACK_DIR goes after other injections)
    - 88-PATTERNS.md §hook_runtime.rs for the PACK_DIR injection placement and I-before-M ordering rationale
    - 88-RESEARCH.md §FEAT-05 for the hook_runtime.rs change description (3 lines added) and the I/M conflict explanation
    - D-13 decision: this cherry-pick MUST precede e54cf9cb (in Plan 88-05); verify by checking that after this commit, hook_runtime.rs still has `cmd.env_clear()` on its original line (env_clear is not removed yet)
  </read_first>
  <action>
    Run `git cherry-pick -x 7d274cf7`. This is the largest commit in the wave (1002+/78-) and touches profile/mod.rs which was already modified by Plans 88-01 and 88-02. Resolve any conflicts by preferring the upstream changes for the pack/source_pack additions while preserving the set_vars and aws_auth changes from prior plans.

    Post-apply verification for D-13 ordering constraint:
    - Run `grep -n "env_clear" crates/nono-cli/src/hook_runtime.rs`
    - The `cmd.env_clear()` line MUST still be present after this cherry-pick (e54cb in Plan 88-05 will remove it). If the cherry-pick already removed env_clear(), that is a merge error — restore it.
    - Run `grep -n "PACK_DIR" crates/nono-cli/src/hook_runtime.rs` — must show the new env injection.

    D-14 invariant check:
    - Run `grep -n "env_clear" crates/nono-cli/src/hook_runtime_windows.rs`
    - The Windows file MUST NOT be modified by this cherry-pick. env_clear() and the SystemRoot/windir/SystemDrive restore MUST remain.

    Add DCO sign-off: `git commit --amend --no-edit -s`.
  </action>
  <verify>
    <automated>grep -n "PACK_DIR\|env_clear" crates/nono-cli/src/hook_runtime.rs 2>&1</automated>
    <automated>grep -n "env_clear" crates/nono-cli/src/hook_runtime_windows.rs 2>&1</automated>
    <automated>cargo test -p nono-cli hook 2>&1 | tail -10</automated>
    <automated>cargo check -p nono-cli 2>&1 | tail -5</automated>
  </verify>
  <done>
    Cherry-pick committed with "(cherry picked from commit 7d274cf7)" and DCO. hook_runtime.rs has PACK_DIR injection AND still has cmd.env_clear() (env_clear not yet removed — Plan 88-05 does that). hook_runtime_windows.rs is untouched. `cargo check -p nono-cli` passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: make ci gate (Windows) + PARTIAL→CI update</name>
  <files>
    .planning/phases/88-feature-dependency-cherry-pick-wave/88-PARTIAL-CI.md
  </files>
  <read_first>
    - .planning/phases/88-feature-dependency-cherry-pick-wave/88-PARTIAL-CI.md (current state)
    - CLAUDE.md §"Cross-target clippy verification" (identify which files in this plan trigger the rule)
  </read_first>
  <action>
    Run `make ci` on the Windows host. Fix any clippy or test failures.

    After make ci passes, append to 88-PARTIAL-CI.md under "Plan 88-03 Deferrals":
    - 7d274cf7 / hook_runtime.rs: file has #[cfg(unix)] in pre_exec block; new PACK_DIR code is std-only but the file triggers the MUST/NEVER rule. → GH Actions Linux/macOS CI

    Commit the PARTIAL-CI.md update: `git add .planning/phases/88-feature-dependency-cherry-pick-wave/88-PARTIAL-CI.md && git commit -m "docs(88): add plan 88-03 PARTIAL→CI entries" -s`
  </action>
  <verify>
    <automated>make ci 2>&1 | tail -20</automated>
  </verify>
  <done>
    `make ci` exits 0. 88-PARTIAL-CI.md has Plan 88-03 deferral entry for hook_runtime.rs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proxy route config → AWS credential path | The aws_auth field on RouteConfig crosses into credential routing; mutual-exclusion must be enforced at profile-load time |
| non-TLS proxy path → AWS-auth route | Request to an AWS-auth route on the non-TLS path must return 501 (not silently pass unauthenticated) |
| session hook env → PACK_DIR value | PACK_DIR is injected into hook subprocess env; value comes from the profile pack configuration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-10 | Elevation of Privilege | AWS auth route accepting non-TLS requests without 501 guard | mitigate | reverse.rs 501 stub: get_aws(&service).is_some() → send_error(501) before credential forwarding; prevents unauthenticated pass-through on fork's non-TLS path (D-15 fork gap) |
| T-88-11 | Tampering | AwsAuthConfig coexisting with credential_key or oauth2 | mitigate | validate_custom_credential() rejects aws_auth + credential_key or aws_auth + oauth2 at profile-load time; proxy never receives an ambiguous credential config |
| T-88-12 | Information Disclosure | PACK_DIR env injection into hook subprocess | accept | PACK_DIR value comes from the profile pack configuration (operator-controlled); hook subprocess env is already cleared (env_clear prior to injection); no credential data in pack paths |
| T-88-13 | Tampering | 5bb098cd partial apply (tls_intercept hunk skipped) | mitigate | Explicitly discard tls_intercept/ hunks; verify no tls_intercept directory created in the fork; commit note documents the skip |
| T-88-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
After both cherry-picks:
1. `grep -rn "AwsAuthConfig" crates/nono-proxy/src/config.rs` — returns at least one match
2. `grep -n "get_aws\|aws_routes" crates/nono-proxy/src/credential.rs` — returns at least two matches
3. `grep -n "501\|NOT_IMPLEMENTED\|get_aws" crates/nono-proxy/src/reverse.rs` — 501 guard present
4. `grep -n "PACK_DIR" crates/nono-cli/src/hook_runtime.rs` — PACK_DIR injection present
5. `grep -n "env_clear" crates/nono-cli/src/hook_runtime.rs` — env_clear STILL PRESENT (I-before-M: e54cb not yet applied)
6. `grep -n "env_clear" crates/nono-cli/src/hook_runtime_windows.rs` — Windows file unchanged
7. `make ci` exits 0
8. 88-PARTIAL-CI.md has Plan 88-03 deferral for hook_runtime.rs
</verification>

<success_criteria>
- FEAT-03: AwsAuthConfig struct exists in proxy; mutual-exclusion enforced at profile-load time; 501 stub in reverse.rs for non-TLS aws_auth routes
- FEAT-05: $PACK_DIR environment variable injected in build_hook_command(); source_pack propagation in profile_runtime.rs
- D-13 ordering: 7d274cf7 committed before e54cb (hook_runtime.rs still has env_clear() after this plan)
- D-15: tls_intercept/ hunk skipped; no tls_intercept directory created
- D-14 invariant: hook_runtime_windows.rs untouched
- Both commits with DCO; `make ci` green
</success_criteria>

<output>
Create `.planning/phases/88-feature-dependency-cherry-pick-wave/88-03-SUMMARY.md` when done
</output>
