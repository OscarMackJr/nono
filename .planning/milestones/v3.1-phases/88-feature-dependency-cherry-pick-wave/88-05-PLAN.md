---
phase: 88-feature-dependency-cherry-pick-wave
plan: 05
type: execute
wave: 5
depends_on:
  - 88-04
files_modified:
  - crates/nono-cli/src/cli.rs
  - crates/nono-cli/src/output.rs
  - crates/nono-cli/src/capability_ext.rs
  - crates/nono-cli/src/sandbox_prepare.rs
  - crates/nono-cli/src/startup_runtime.rs
  - crates/nono-cli/src/pull_ui.rs
  - crates/nono-cli/src/profile/mod.rs
  - crates/nono-cli/src/profile_runtime.rs
  - crates/nono-cli/src/hook_runtime.rs
autonomous: true
requirements:
  - FEAT-06
must_haves:
  truths:
    - "--trust-proxy-ca=yes, --trust-proxy-ca=1, --trust-proxy-ca=true all accepted as truthy (FEAT-06c)"
    - "NONO_TRUST_OVERRIDE env var accepts truthy string values (FEAT-06c)"
    - "macOS blocked grants display shows correct output (a0bba5eb)"
    - "Schema references nono.sh not nono.dev (ee7a3bda)"
    - "Sigstore provenance is not displayed in pull UI (7e076d2d)"
    - "Pack verification is skipped when --dry-run is set (9800f307)"
    - "hook_runtime.rs does NOT have cmd.env_clear() after this plan (e54cf9cb applied to Unix path)"
    - "hook_runtime_windows.rs still has cmd.env_clear() and SystemRoot/windir/SystemDrive restore after this plan (D-14 invariant)"
    - "PARTIAL→CI deferral recorded for e54cf9cb (hook_runtime.rs cfg(unix) blocks)"
  artifacts:
    - path: "crates/nono-cli/src/cli.rs"
      provides: "BoolishValueParser on trust-proxy-ca, trust-override, capability-elevation; NONO_TRUST_OVERRIDE env source"
      contains: "BoolishValueParser"
    - path: "crates/nono-cli/src/hook_runtime.rs"
      provides: "env_clear() REMOVED from build_hook_command() on Unix path"
      not_contains: "env_clear"
  key_links:
    - from: "crates/nono-cli/src/hook_runtime.rs"
      to: "crates/nono-cli/src/hook_runtime_windows.rs"
      via: "D-14 invariant: Windows file retains env_clear() + SystemRoot restore"
      pattern: "env_clear.*SystemRoot\|hook_runtime_windows"
---

<objective>
Cherry-pick the remaining Cluster M commits in the prescribed sequence: `42e5bf73` (FEAT-06c: truthy env bool flags), `a0bba5eb` (macOS blocked grants display), `ee7a3bda` (schema domain rename nono.dev→nono.sh), `7e076d2d` (sigstore provenance removal), `9800f307` (pack-verification dry-run skip), and `e54cf9cb` (env_clear removal — Unix path ONLY per D-14).

Purpose: Complete the Cluster M misc fixes and FEAT-06c. The env_clear removal (`e54cf9cb`) is the final cherry-pick in this plan and MUST be applied ONLY to `hook_runtime.rs` (Unix path); `hook_runtime_windows.rs` retains env_clear() + CLR baseline per D-14 and `windows_hook_interpreter_spawn_gotchas` memory.

Output: Six committed cherry-picks, each with DCO. D-14 invariant confirmed after e54cf9cb. PARTIAL→CI entry for hook_runtime.rs. `make ci` green.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/88-feature-dependency-cherry-pick-wave/88-CONTEXT.md
@.planning/phases/88-feature-dependency-cherry-pick-wave/88-RESEARCH.md
@.planning/phases/88-feature-dependency-cherry-pick-wave/88-PATTERNS.md
@.planning/phases/88-feature-dependency-cherry-pick-wave/88-PARTIAL-CI.md

<interfaces>
D-14 CRITICAL invariant — hook_runtime_windows.rs after e54cf9cb:
  MUST retain: cmd.env_clear(); (line ~301)
  MUST retain: cmd.env("SystemRoot", ...); (line ~307-326 SystemRoot/windir/SystemDrive restore)
  If either is absent after the cherry-pick → Windows CLR init failure 0xFFFF0000 / -65536

FEAT-06c BoolishValueParser in cli.rs:
  The upstream adds clap's BoolishValueParser to --trust-proxy-ca, --trust-override,
  --capability-elevation flags. It also wires NONO_TRUST_OVERRIDE as an env() source.
  Pattern: look at how the existing .action(ArgAction::SetTrue) flags are defined; the
  BoolishValueParser change converts them to accept "yes"/"no"/"1"/"0"/"true"/"false".

a0bba5eb — startup_runtime.rs EXISTS in fork (verified: file present in crates/nono-cli/src/).
  The 4+/0- change is additive to macOS capability summary display.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cherry-pick 42e5bf73 (FEAT-06c truthy env bool flags)</name>
  <files>
    crates/nono-cli/src/cli.rs
  </files>
  <read_first>
    - Run: git show 42e5bf73 (inspect diff — 65+/3- in cli.rs, 46+/0- new file env_vars.rs test)
    - crates/nono-cli/src/cli.rs (find --trust-proxy-ca, --trust-override, --capability-elevation arg definitions; find existing .env() wiring pattern for NONO_* vars)
    - 88-PATTERNS.md §cli.rs for the BoolishValueParser pattern and NONO_TRUST_OVERRIDE env source
    - 88-RESEARCH.md §FEAT-06c confirming clean apply, no cfg gates
  </read_first>
  <action>
    Run `git cherry-pick -x 42e5bf73`. Expect clean apply — touches only cli.rs and adds a test file. Resolve any conflicts with previous cli.rs changes from Plan 88-04 (profile namespace doc changes).

    Verify BoolishValueParser is applied to all three flags: --trust-proxy-ca, --trust-override, --capability-elevation. Verify NONO_TRUST_OVERRIDE is wired as an .env() source on the appropriate flag.

    Add DCO sign-off: `git commit --amend --no-edit -s`.
  </action>
  <verify>
    <automated>cargo test -p nono-cli env_vars 2>&1 | tail -10</automated>
    <automated>grep -n "BoolishValueParser\|NONO_TRUST_OVERRIDE" crates/nono-cli/src/cli.rs 2>&1 | head -10</automated>
  </verify>
  <done>
    Cherry-pick committed with DCO. BoolishValueParser appears in cli.rs. NONO_TRUST_OVERRIDE wired. env_vars tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cherry-pick a0bba5eb, ee7a3bda, 7e076d2d, 9800f307 (Cluster M misc)</name>
  <files>
    crates/nono-cli/src/capability_ext.rs,
    crates/nono-cli/src/output.rs,
    crates/nono-cli/src/sandbox_prepare.rs,
    crates/nono-cli/src/startup_runtime.rs,
    crates/nono-cli/src/pull_ui.rs,
    crates/nono-cli/src/profile/mod.rs,
    crates/nono-cli/src/profile_runtime.rs
  </files>
  <read_first>
    - Run each: git show a0bba5eb; git show ee7a3bda; git show 7e076d2d; git show 9800f307
    - crates/nono-cli/src/startup_runtime.rs (verify file exists and check ~lines that a0bba5eb touches — the RESEARCH.md says 4+/0- additive to macOS blocked grants display)
    - crates/nono-cli/src/pull_ui.rs (find sigstore provenance display that 7e076d2d removes)
    - crates/nono-cli/src/profile_runtime.rs (find pack verification path that 9800f307 adds dry-run skip)
    - 88-RESEARCH.md §Cluster M misc for the risk assessment per SHA
  </read_first>
  <action>
    Apply the four commits in sequence. Each is additive or a deletion; expect clean applies:

    1. `git cherry-pick -x a0bba5eb` — macOS blocked grants display. startup_runtime.rs EXISTS (verified). Add DCO: `git commit --amend --no-edit -s`.

    2. `git cherry-pick -x ee7a3bda` — nono.dev → nono.sh in profile/mod.rs, profile_cmd.rs (profile_cmd.rs may be in this plan's scope; add if needed), and schema JSON. String replacement; safe. Add DCO: `git commit --amend --no-edit -s`.

    3. `git cherry-pick -x 7e076d2d` — Remove sigstore provenance display from pull_ui.rs. Deletion-only change. Add DCO: `git commit --amend --no-edit -s`.

    4. `git cherry-pick -x 9800f307` — Pack-verification dry-run skip in profile_runtime.rs. Also touches integration tests — if the fork's integration test structure differs, the test hunk may not apply; adapt the test to the fork's test layout. Add DCO: `git commit --amend --no-edit -s`.

    After all four, run `cargo check -p nono-cli` to catch any compilation issues before proceeding to the env_clear cherry-pick in Task 3.
  </action>
  <verify>
    <automated>cargo check -p nono-cli 2>&1 | tail -10</automated>
    <automated>grep -n "nono\.sh" crates/nono-cli/src/profile/mod.rs 2>&1 | head -3</automated>
    <automated>grep -n "dry.run\|dry_run" crates/nono-cli/src/profile_runtime.rs 2>&1 | head -5</automated>
  </verify>
  <done>
    Four cherry-picks committed, each with DCO. `cargo check -p nono-cli` passes. Schema references nono.sh. Dry-run skip present in profile_runtime.rs. Sigstore provenance removed from pull_ui.rs.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cherry-pick e54cf9cb (env_clear removal — UNIX PATH ONLY per D-14)</name>
  <files>
    crates/nono-cli/src/hook_runtime.rs
  </files>
  <read_first>
    - Run: git show e54cf9cb (inspect diff — specifically which file(s) are changed; should be hook_runtime.rs removing cmd.env_clear())
    - crates/nono-cli/src/hook_runtime.rs lines 189-230 (current build_hook_command after Plan 88-03 Task 2 added PACK_DIR; verify cmd.env_clear() is still present — it MUST be there for this cherry-pick to apply)
    - crates/nono-cli/src/hook_runtime_windows.rs (entire file; note the cmd.env_clear() at line ~301 and the SystemRoot/windir/SystemDrive restore block at lines ~307-326 — THESE MUST NOT CHANGE)
    - 88-PATTERNS.md §hook_runtime.rs for the D-14 invariant documentation
    - 88-RESEARCH.md §e54cb for the "I-before-M ordering proof" confirming 7d274cf7 applied first means this applies cleanly
    - Memory windows_hook_interpreter_spawn_gotchas — DO NOT REMOVE env_clear from Windows hook path
  </read_first>
  <action>
    CRITICAL: Apply e54cf9cb to hook_runtime.rs ONLY. Do not let git touch hook_runtime_windows.rs.

    Method: Use `git cherry-pick -x --no-commit e54cf9cb` then inspect the staged changes. If the cherry-pick only modifies hook_runtime.rs (expected), commit. If it touches hook_runtime_windows.rs, run `git checkout HEAD -- crates/nono-cli/src/hook_runtime_windows.rs` to restore the Windows file.

    Post-apply D-14 invariant check (MANDATORY before committing):
    - `grep -n "env_clear" crates/nono-cli/src/hook_runtime.rs` → MUST return ZERO results (env_clear removed)
    - `grep -n "env_clear" crates/nono-cli/src/hook_runtime_windows.rs` → MUST return at least ONE result (Windows keeps env_clear)
    - `grep -n "SystemRoot\|windir\|SystemDrive" crates/nono-cli/src/hook_runtime_windows.rs` → MUST return results (CLR baseline restore present)

    If D-14 invariant fails (Windows file was accidentally modified), abort with `git cherry-pick --abort` and re-apply manually: only remove `cmd.env_clear();` from hook_runtime.rs's build_hook_command() function.

    DCO sign-off for the commit: `git commit -m "fix(hooks): remove env_clear from Unix hook path (e54cf9cb)" -s`
    Include in commit body: "(cherry picked from commit e54cf9cb)" and "Applied to hook_runtime.rs (Unix path) only; hook_runtime_windows.rs retains env_clear() + CLR baseline per windows_hook_interpreter_spawn_gotchas (D-14)."

    Update 88-PARTIAL-CI.md with Plan 88-05 deferral entry:
    - e54cf9cb / hook_runtime.rs: file has #[cfg(unix)] pre_exec block; env_clear removal is within a function called from the Unix exec path; Windows-host clippy cannot verify #[cfg(unix)] arms. → GH Actions Linux/macOS CI

    Commit the PARTIAL-CI.md update as a separate commit.
  </action>
  <verify>
    <automated>grep -n "env_clear" crates/nono-cli/src/hook_runtime.rs 2>&1</automated>
    <automated>grep -n "env_clear" crates/nono-cli/src/hook_runtime_windows.rs 2>&1</automated>
    <automated>grep -n "SystemRoot\|windir\|SystemDrive" crates/nono-cli/src/hook_runtime_windows.rs 2>&1 | head -5</automated>
    <automated>cargo check -p nono-cli 2>&1 | tail -5</automated>
  </verify>
  <done>
    e54cf9cb committed for hook_runtime.rs only. hook_runtime.rs has zero env_clear hits. hook_runtime_windows.rs unchanged (env_clear present, SystemRoot/windir/SystemDrive restore present). 88-PARTIAL-CI.md updated with Plan 88-05 entry.
  </done>
</task>

<task type="auto">
  <name>Task 4: make ci gate (Windows)</name>
  <files></files>
  <read_first>
    - .planning/phases/88-feature-dependency-cherry-pick-wave/88-PARTIAL-CI.md (verify Plan 88-05 entry added)
  </read_first>
  <action>
    Run `make ci` on the Windows host. Fix any clippy or test failures.

    If Windows hook tests fail with env-clear-related failures (e.g., tests that expected env_clear to have been called), update the test to match the new Unix behavior where env is NOT cleared. The Windows hook tests should still pass since hook_runtime_windows.rs is unchanged.

    After make ci passes, verify the D-14 invariant one final time:
    `grep -c "env_clear" crates/nono-cli/src/hook_runtime.rs` → must return 0
    `grep -c "env_clear" crates/nono-cli/src/hook_runtime_windows.rs` → must return non-zero
  </action>
  <verify>
    <automated>make ci 2>&1 | tail -20</automated>
    <automated>grep -c "env_clear" crates/nono-cli/src/hook_runtime.rs 2>&1</automated>
    <automated>grep -c "env_clear" crates/nono-cli/src/hook_runtime_windows.rs 2>&1</automated>
  </verify>
  <done>
    `make ci` exits 0. hook_runtime.rs env_clear count is 0. hook_runtime_windows.rs env_clear count is non-zero. 88-PARTIAL-CI.md has Plan 88-05 deferral for hook_runtime.rs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hook subprocess env | Unix hook now inherits parent env (env_clear removed); Windows hook still clears env before CLR baseline restore |
| CLI flag env sources (NONO_TRUST_OVERRIDE) | New env-var-backed bool flags; values accepted as truthy strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-18 | Elevation of Privilege | Unix hook subprocess inheriting parent env after env_clear removal (e54cf9cb) | accept | This is the upstream design intent: hook subprocesses are trusted operator-authored scripts; they inherit the session env intentionally. The PACK_DIR injection (from Plan 88-03) adds to this env rather than replacing it. |
| T-88-19 | Tampering | Accidental env_clear removal from Windows hook path (D-14 violation) | mitigate | Mandatory D-14 check: grep hook_runtime_windows.rs for env_clear MUST return non-zero; if check fails, abort the plan and re-apply manually |
| T-88-20 | Tampering | NONO_TRUST_OVERRIDE env var enabling trust-override without explicit flag | accept | Trust-override decisions are operator-controlled (env vars are set in the session, not by sandboxed code); BoolishValueParser enforces valid truthy/falsy values, rejects arbitrary strings |
| T-88-21 | Spoofing | BoolishValueParser accepting "yes" as truthy for security flags | accept | This is intentional and consistent with standard CLI conventions; explicit operator action required to set the env var |
| T-88-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
After all six cherry-picks:
1. `grep -c "env_clear" crates/nono-cli/src/hook_runtime.rs` → 0 (env_clear removed from Unix path)
2. `grep -c "env_clear" crates/nono-cli/src/hook_runtime_windows.rs` → non-zero (D-14 Windows carve-out)
3. `grep -n "SystemRoot\|windir\|SystemDrive" crates/nono-cli/src/hook_runtime_windows.rs` → CLR baseline restore present
4. `grep -n "BoolishValueParser" crates/nono-cli/src/cli.rs` → at least one match
5. `grep -n "nono\.sh" crates/nono-cli/src/profile/mod.rs` → at least one match (domain renamed)
6. `grep -n "dry_run\|dry.run" crates/nono-cli/src/profile_runtime.rs` → dry-run skip present
7. `make ci` exits 0
8. 88-PARTIAL-CI.md has Plan 88-05 entry for hook_runtime.rs (e54cf9cb)
</verification>

<success_criteria>
- FEAT-06c: BoolishValueParser on trust flags; NONO_TRUST_OVERRIDE env source wired
- Cluster M misc: macOS blocked grants display (a0bba5eb); nono.sh domain (ee7a3bda); sigstore removal (7e076d2d); dry-run pack skip (9800f307)
- e54cf9cb applied ONLY to hook_runtime.rs (Unix path); D-14 invariant confirmed — hook_runtime_windows.rs unchanged
- Six cherry-picks committed with DCO (D-12); e54cf9cb has explicit D-14 note in commit body
- `make ci` green on Windows host
- 88-PARTIAL-CI.md updated with Plan 88-05 deferral
</success_criteria>

<output>
Create `.planning/phases/88-feature-dependency-cherry-pick-wave/88-05-SUMMARY.md` when done
</output>
