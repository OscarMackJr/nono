# Requirements (ARCHIVED): nono — v2.12 AI Agent Abstraction

**Defined:** 2026-06-13 · **Shipped:** 2026-06-16 · **Status:** ✅ 12/12 satisfied (milestone audit PASSED)

**Core Value:** Windows security must be as structurally impossible and feature-complete as Unix
platforms — and that confinement must apply to *any* AI agent engine, not just Claude Code.

## Requirement Outcomes

| Requirement | Phase | Outcome |
|-------------|-------|---------|
| ENG-01 | 71 | ✅ validated — Aider confined end-to-end, live Win11 |
| ENG-02 | 71 | ✅ validated — fail-secure coverage + R-B3 diagnostics |
| ENG-03 | 71 | ✅ validated — per-engine profiles via `windows_interpreters` |
| ABI-01 | 72 | ✅ validated — nono-py `confined_run` + born-confined `confine`, no Claude hook |
| ABI-02 | 72 | ✅ validated — E1-E5 contract documented (`proj/DESIGN-engine-abstraction.md`) |
| MARK-01 | 73 | ✅ validated — unforgeable AI_AGENT identity (AgentRegistry + SID); SC4 authoritative tests pass. Cross-process authoritative classify deferred to Phase 74+ (by design) |
| DMON-01 | 74 | ✅ validated — multi-concurrent confined agents, fresh token+job, reaped to baseline |
| DMON-02 | 74 | ✅ validated — tenant-isolated control pipe (ImpersonateNamedPipeClient + per-tenant SID) |
| DMON-03 | 74 | ✅ validated — least-privilege USER daemon, split from elevated WFP service |
| SUPP-01 | 75 | ✅ validated — operator `nono agent demote` (IL-drop + WFP-cut, not reap) |
| SUPP-02 | 75 | ✅ validated — per-agent WFP egress (add@launch/remove@reap); A1 empirical isolation deferred |
| SUPP-03 | 75 | ✅ validated (adjusted) — nono-ts parity + Copilot profile; **Engine-2 = claude-code** end-to-end (Copilot confine-only, Node-ESM/AppContainer limit per 75-08) |

## v1 Requirements (as shipped)

### Engine-Agnostic Launch (ENG)
- [x] **ENG-01**: A user can run a non-Claude agent engine (e.g. Aider) confined by nono end-to-end on a real Win11 host — files written inside the granted workspace land; writes outside it are denied (`NO_WRITE_UP`) — regardless of engine.
- [x] **ENG-02**: The launcher fails **secure with an actionable message** when an engine's executable/interpreter path is not covered by the launch policy, or when the granted workspace is not owned by the session user (R-B3) — never silent partial confinement.
- [x] **ENG-03**: A user can declare a **per-engine launch profile** (executable + interpreter path(s), absolute writable workspace, network identity) and launch any profiled engine through one engine-neutral path.

### Engine Abstraction & Bindings (ABI)
- [x] **ABI-01**: A Python/LangChain agent can be confined through the **`nono-py` binding with no Claude hook** — both by launching it confined (`confined_run`) and by self-confining at interpreter startup (`confine`).
- [x] **ABI-02**: The **engine-abstraction contract** (executable/interpreter path, ownable launch command, absolute workspace grant, network identity, optional pre-exec hook) is documented as a stable boundary.

### AI_AGENT Marker (MARK)
- [x] **MARK-01**: Each confined agent carries an **unforgeable `AI_AGENT` identity** bound to its daemon-minted token SID; a non-agent process cannot claim the identity and a confined agent cannot shed it.

### Multi-Tenant Daemon (DMON)
- [x] **DMON-01**: A persistent local daemon launches and confines **multiple concurrent agents**, each with a fresh token + job object, deterministically reaped on agent exit (no leak).
- [x] **DMON-02**: The daemon's multi-tenant capability pipe **isolates tenants** (server-side `ImpersonateNamedPipeClient` + per-tenant SID match) — a cross-tenant request is denied.
- [x] **DMON-03**: The daemon runs at **least privilege** (user, not LocalSystem), split from the elevated WFP-control service. (Backed by a privilege-model ADR.)

### Supplementary Controls & Reach (SUPP)
- [x] **SUPP-01**: An operator can **demote a running/misbehaving agent** on the fly (post-hoc token IL-drop), with leak/soundness limits documented (explicitly not a standalone boundary).
- [x] **SUPP-02**: Outbound network egress is **scoped per confined agent** (WFP keyed to the agent's identity).
- [x] **SUPP-03**: A second non-Claude engine ships as a profile and the **`nono-ts` (Node) binding reaches parity** with `nono-py` — proving the abstraction across ≥2 engines and ≥2 bindings. (Engine-2 shipped as `claude-code` end-to-end; Copilot CLI confine-only.)

## Deferred to future milestones (was v2 / out of scope)
- Signed-policy / decentralized attestation (SEED-005).
- Sound adoption of an already-running agent (blocked by post-hoc-IL-drop leak, spike 002).
- Cursor native-Windows confinement (Linux/macOS/WSL-only today).
- Kernel driver / minifilter (ADR-65 No-go).
- Carry-forwards from v2.12 audit: Copilot end-to-end (Node-ESM/AppContainer drive-root fix), A1 empirical WFP isolation, cross-process authoritative classify, nono-ts arm-default ergonomics.

## Traceability (final)

| Requirement | Phase | Status |
|-------------|-------|--------|
| ENG-01/02/03 | 71 | Complete |
| ABI-01/02 | 72 | Complete |
| MARK-01 | 73 | Complete |
| DMON-01/02/03 | 74 | Complete |
| SUPP-01/02/03 | 75 | Complete |

**Coverage: 12/12 mapped, no orphans, no duplicates.**
