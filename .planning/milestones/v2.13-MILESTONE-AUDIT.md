---
milestone: v2.13
milestone_name: Carry-Forward Closeout (Dark Factory)
audited: 2026-06-18
status: tech_debt
scores:
  requirements: 10/10
  phases: 6/6
  integration: WIRED (0 defects)
  flows: 4/4 traced (2 host-gated by design)
gaps:
  requirements: []        # No unsatisfied requirements — FAIL gate not tripped
  integration: []         # 0 wiring defects, 0 orphaned exports, 0 broken flows
  flows: []
tech_debt:
  - phase: 77-copilot-cli-end-to-end-confinement
    items:
      - "CPLT-03: literal green copilot-e2e PASS is host-gated — the GitHub org policy on this account denies Copilot CLI, so the (correct, fails-closed) gate returns SKIP_HOST_UNAVAILABLE. Phase 77 verification accepted this via override. A real green PASS requires a Copilot-CLI-enabled GitHub account."
  - phase: 80-clean-host-install-uat
    items:
      - "INST-01: build fix (D-03 +crt-static, D-04 Vital=no) + clean-host-install gate are delivered and dev-host-verified, but the LIVE clean-VM PASS is operator-gated per D-01 (needs a fresh Win11 VM + the MSI rebuilt after Plan 80-01). Aggregator consumes SKIP on the dirty dev host, PASS on the clean VM."
      - "DIST-SIGN-01 (deferred, recorded in 80-02): untrusted-POC-cert broker/supervised path is NOT exercised by the clean-host gate (install-level only, D-05)."
  - phase: 81-milestone-close-aggregator
    items:
      - "Live aggregate close signal currently reports overall=FAIL (exit 2). NOT an aggregator defect — it faithfully rolls up the wfp-egress-isolation gate, which FAILs because C:\\Program Files\\nono\\nono.exe on PATH is a STALE build lacking the `agent` subcommand. For a clean PASS_WITH_SKIPS close signal, prepend the fresh target\\release build to PATH (+ the wfp gate's admin / non-elevated-daemon / nono-wfp-service host setup) and re-run `pwsh -File scripts\\verify-dark.ps1`."
nyquist:
  compliant_phases: 0
  partial_phases: 0
  missing_phases: 6
  overall: "not_applicable — research was skipped for all v2.13 phases (no RESEARCH.md → no VALIDATION.md); Nyquist not required for this carry-forward-closeout milestone"
---

# v2.13 Carry-Forward Closeout (Dark Factory) — Milestone Audit

**Audited:** 2026-06-18 · **Status:** `tech_debt` (all requirements met at code/gate level; no critical blockers; host-gated deferrals + carry-forwards to review before final close)

## Verdict

All **10/10 requirements** are satisfied at the code/gate level, all **6/6 phases** are verified, and cross-phase integration is **WIRED with 0 defects**. The FAIL gate is **not** tripped — no requirement is `unsatisfied`. Two requirements (CPLT-03, INST-01) have their *literal live verdicts* host-gated by design; their deliverables (a correct, fails-closed scripted gate; the build fix) are complete. This is `tech_debt` rather than `passed` only because of those accepted host-execution deferrals and the stale-binary aggregate FAIL on the dev host.

## Requirements Coverage (3-source cross-reference)

| REQ-ID | Phase | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md | Final Status |
|--------|-------|--------------|---------------------|-----------------|--------------|
| DARK-01 | 76 | passed | DARK-01 listed | `[x]` | **satisfied** |
| CPLT-01 | 77 | passed | CPLT-01 listed (77-01) | `[x]` (fixed in audit) | **satisfied** |
| CPLT-02 | 77 | passed | CPLT-02 listed (77-02) | `[x]` (fixed in audit) | **satisfied** |
| CPLT-03 | 77 | passed (SC-4 override) | not in frontmatter | `[x]` (fixed in audit) | **satisfied — literal green PASS host-gated (org policy)** |
| CLAS-01 | 78 | passed (VERIFIED, host pid=31500→AiAgent) | not in frontmatter | `[x]` | **satisfied** |
| CLAS-02 | 78 | passed (SDDL unchanged, no SID in response) | not in frontmatter | `[x]` | **satisfied** |
| WFP-01 | 79 | human_needed → resolved (stale-metadata only; gate PASS verdict stored) | not in frontmatter | `[x]` | **satisfied** |
| TSRG-01 | 79 | human_needed → resolved (confinedRun default verified, npm test PASS) | not in frontmatter | `[x]` | **satisfied** |
| INST-01 | 80 | human_needed (partial, host-gated) | not in frontmatter | `[x]` | **satisfied — live clean-VM PASS host-gated per D-01** |
| DARK-02 | 81 | passed (GOAL ACHIEVED 4/4 SCs) | not in frontmatter | `[x]` | **satisfied** |

**Orphan detection:** None. Every REQ-ID in the traceability table maps to a phase VERIFICATION.md.

**Process note (not a blocker):** Most phase SUMMARY.md files have an empty `requirements_completed` frontmatter array (only 76-02, 77-01, 77-02 populated it). The VERIFICATION.md files are the authoritative evidence and verify each requirement with concrete evidence; the empty frontmatter is a process-hygiene gap for future milestones, not a coverage gap here.

**Tracking fix applied during audit:** CPLT-01/02/03 were stale (`[ ]` / "Pending") in REQUIREMENTS.md despite Phase 77 being verified `passed`. Corrected to `[x]` / "Complete" (the known `feedback_sdk_roadmap_checklist_not_flipped` SDK drift). Phase 79's similar stale-metadata human-verify item was already resolved (WFP-01/TSRG-01 show Complete).

## Phase Verifications

| Phase | Status | Notes |
|-------|--------|-------|
| 76 Self-Verifying Harness Foundation | passed | `verify-dark.ps1` runner + gate contract + harness-self-check gate (DARK-01) |
| 77 Copilot CLI End-to-End Confinement | passed | ancestor-RA guard (binary+workspace chains) + `nono setup --grant-ancestors` + copilot-e2e gate; CPLT-03 literal PASS org-policy-gated (override-accepted) |
| 78 Cross-Process Classification | passed | daemon `Classify` verb wired CLI→daemon→registry; SC4 no-SID enforced; host pid=31500→AiAgent |
| 79 WFP Egress Isolation + nono-ts Ergonomics | passed (was human_needed: stale-metadata only) | per-SID WFP structural proof (PASS verdict stored, distinct SIDs); confinedRun Low-IL default + auto-cover |
| 80 Clean-Host Install UAT | passed/partial (host-gated) | build fix (+crt-static, Vital=no) + clean-host gate dev-host-verified; live clean-VM PASS deferred per D-01 |
| 81 Milestone Close Aggregator | passed | aggregator GOAL ACHIEVED 4/4 SCs; `_aggregate.json` close signal; CI exit contract |

## Cross-Phase Integration: WIRED

Integration checker verified the dark-factory spine end-to-end against the actual codebase (not just SUMMARYs):
- **P76 contract → P77/79/80 gates:** all 4 gate files (`harness-self-check`, `copilot-e2e`, `wfp-egress-isolation`, `clean-host-install`) export `Test-Precondition` + `Invoke-Gate`, no bare `exit`, no `Persist-Verdict` call.
- **P76/P81 discovery:** aggregator derives the gate set from `scripts/gates/*.ps1` auto-discovery, never from globbing `verdicts/` — stub `.json` files cannot leak into the rollup.
- **P78 Classify:** wired at every link (`cli.rs` → `app_runtime.rs::run_classify` → `agent_cli::classify_daemon_request` → control pipe → `control_loop.rs::handle_classify` → `AgentRegistry::classify`); SC4 no-SID via `AiAgent { .. }` wildcard.
- **P77 ancestor-RA:** `nono::grant_sid_read_attributes_on_path` → `dacl_guard` → both binary + workspace chains in `exec_strategy_windows`; `--grant-ancestors` one-time-admin path wired.
- **P79 WFP + P80 build fix + P81 aggregator:** all artifacts present and wired.

**0 wiring defects · 0 orphaned exports · 0 broken flows.**

## Definition of Done (dark-factory mandate)

The milestone's DoD = every historically host-gated item collapses to a single unattended scripted gate emitting a machine-readable verdict, and a single aggregator makes v2.13 completion evaluable from harness output alone.

- ✅ Every host-gated item ships as a `scripts/gates/*.ps1` gate conforming to the Phase 76 contract.
- ✅ `pwsh -File scripts\verify-dark.ps1` (no flags) is the single milestone-close signal → `.nono-runtime/verdicts/_aggregate.json` + CI exit code.
- ⚠ The live close signal requires the proper host to go green (fresh `nono.exe` on PATH for the wfp gate; Copilot-enabled account for copilot-e2e; clean Win11 VM for clean-host-install). On a correctly-provisioned host the expected outcome is `PASS_WITH_SKIPS`.

## Outstanding (tech debt — review before/at final close)

1. **Aggregate close signal is FAIL on the dev host** — stale `C:\Program Files\nono\nono.exe` (no `agent` subcommand) makes the wfp gate FAIL. Deploy the fresh build to PATH (+ wfp host setup) and re-run for a clean `PASS_WITH_SKIPS`.
2. **CPLT-03 literal green PASS** — needs a Copilot-CLI-enabled GitHub account (org policy blocks it here).
3. **INST-01 live clean-VM PASS** — operator-provided fresh Win11 VM + MSI rebuilt after 80-01.
4. **DIST-SIGN-01** (deferred) — untrusted-POC-cert broker/supervised path not exercised by the clean-host gate.
5. **SUMMARY `requirements_completed` frontmatter hygiene** — populate going forward (process improvement, not a coverage gap).

## Recommendation

Proceed to `/gsd:complete-milestone v2.13` accepting the above as documented tech debt (consistent with how v2.5 and v2.10 closed). The engineering is complete and integrated; the outstanding items are host-execution deferrals and tracking hygiene, not code deficiencies. Optionally, provision a clean/fresh host first to capture a green `PASS_WITH_SKIPS` aggregate as the close artifact.
