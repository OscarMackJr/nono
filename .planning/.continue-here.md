---
context: default
phase: post-v2.7 (milestone complete)
task: null
total_tasks: null
status: paused
last_updated: 2026-05-26T23:15:27.883Z
supersedes: previous .continue-here.md (v2.6 Phase 50 era, 2026-05-22) — resolved; v2.6 then v2.7 both shipped since
---

# Resume Here — Post-v2.7 Release Prep

> **Context note:** Milestone **v2.7 (Windows supervised-run hardening) is 100% COMPLETE**
> with zero verification debt. This session was post-milestone release prep + one bug
> fix — NOT phase execution. There is no active phase to resume; the open work is a
> **release/ship decision**.

<current_state>
v2.7 fully closed (2/2 phases, all REQ-WSRH-* satisfied, 0 outstanding UAT/verification
debt). Working tree is clean except two regenerated build files (see Infrastructure State).
The headline open item: **nothing is pushed or tagged** — `main` is **56 commits ahead of
`origin/main`**, with no `v2.7` and no `v0.57.2` tag. The v0.57.2 MSIs are built and verified
but unsigned.
</current_state>

<completed_work>

This session (all committed):
- **`539e2c48`** — Reconciled Phase 51 SC-4 (`human_needed` → `passed`) using Phase 52 repro A
  evidence. Cleared the last cross-phase verification-debt item (1 → 0).
- **`df1566a7`** — Fixed `nono why` to accept a **positional PATH** (was `--path`-only; clap
  rejected bare paths). Added `path_arg` with `conflicts_with = "path"`; `why_runtime` resolves
  `path.or(path_arg)`. 2 tests added; build/test/clippy green. Debug session archived to
  `.planning/debug/resolved/nono-why-positional-path-arg.md`.
- **`ce05c615`** — Bumped all 5 workspace crates + internal path-dep pins + `Cargo.lock`
  from 0.57.1 → 0.57.2.
- **`5ecb5075`, `c971cd23`** — STATE.md Quick Tasks log entries.
- Rebuilt release binaries at 0.57.2 (verified `nono.exe` reports `0.57.2` + carries the why
  fix) and built **v0.57.2 machine + user MSIs** into `dist/windows/` (gitignored, not committed).
</completed_work>

<remaining_work>

1. **Ship/tag/push (the main open item).** `main` is 56 commits ahead of `origin/main`,
   no tags. Recommended: `/gsd:ship` to push and tag `v2.7` (milestone) and/or `v0.57.2`.
2. **(Optional) Sign the 0.57.2 MSIs.** Pipeline ready; blocked only on the code-signing cert.
3. **(Deferred — needs a scoped phase, NOT `/gsd:fast`) Real WFP driver.** Today's `.sys` is a
   156-byte text stub; no driver source exists. Realistic target is **attestation-signing** of
   a C/WDK WFP callout, not full WHQL.
</remaining_work>

<decisions_made>
- Phase 51 SC-4 closed via Phase 52 evidence rather than re-testing — Phase 52 already
  attested repro A + B live on Win11 build 26200; the flag was stale bookkeeping. The original
  dev-host blocker was **misattributed** to the Phase 27 launch-path gate; the real obstacle
  was the **cwd-coverage gate** (`crates/nono/src/sandbox/windows.rs:1304-1309`).
- `nono why` fix = option (c) positional path + improved help (user-selected). `--op` already
  defaults to `Read` when a path is given, so `nono why <path>` works alone.
- MSIs built from `target/release` (host triple = x86_64-pc-windows-msvc), matching how the
  prior 0.57.1 MSIs were built.
</decisions_made>

<blockers>
- 0.57.2 MSIs are **unsigned** (same as prior local builds). Signing needs the code-signing
  `.pfx` + password, which is a secret not present on this host. Provide a path (don't paste
  the key) and run `scripts/sign-windows-artifacts.ps1` over `dist/windows/*.msi`.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — structured machine-readable state (mirror of this file).
2. `.planning/STATE.md` — milestone status (`milestone_complete`) + Quick Tasks log.
3. `.planning/debug/resolved/nono-why-positional-path-arg.md` — the why-fix root cause/resolution.

## Critical Anti-Patterns (do NOT repeat these)
- None discovered through failure this session. **(Advisory)** version bumps MUST touch all
  **5** crate `Cargo.toml` files + their path-dep `version` pins + `Cargo.lock` — partial bumps
  break workspace resolution. The bundled `nono-wfp-driver.sys` is a placeholder, not a real driver.
- **(Advisory, from prior sessions)** Windows worktree CWD divergence: after any
  `git worktree remove`, `cd /c/Users/OMack/Nono` and verify `pwd && git rev-parse --abbrev-ref HEAD`
  before further git ops. Memory: `feedback_windows_worktree_cwd`.

## Infrastructure State
- **Git:** `main` 56 commits ahead of `origin/main`; no `v2.7` / `v0.57.2` tags. Working tree
  clean EXCEPT two regenerated, tracked build files: `dist/windows/nono-machine.wxs` and
  `dist/windows/nono-user.wxs` (rewritten with 0.57.2 strings by the MSI build). Incidental —
  commit or `git checkout` them as preferred; not swept into the WIP commit.
- **Built artifacts (gitignored `dist/windows/`):** `nono-v0.57.2-x86_64-pc-windows-msvc-machine.msi`
  (5.55 MB, bundles WFP service + stub driver) and `-user.msi` (5.42 MB). Both UNSIGNED.
- **Toolchain present:** WiX 7.0.0 (`~/.dotnet/tools/wix`), signtool at
  `C:\Program Files (x86)\Windows Kits\10\bin\10.0.26100.0\x64\signtool.exe` (not on PATH).

<context>
Everything substantive is committed and verified. The work is "done" in the build sense; what
remains is a release/governance decision (push + tag), an optional signing pass that needs a
cert, and a much larger deferred driver effort. No code is mid-flight; resuming is safe.
</context>

<next_action>
Start with: ask the user whether to ship. If yes, run `/gsd:ship` to push `main` (56 commits
ahead) and tag `v2.7` / `v0.57.2`. If they provide a signing cert, sign the
`dist/windows/nono-v0.57.2-*.msi` files via `scripts/sign-windows-artifacts.ps1`.
</next_action>
