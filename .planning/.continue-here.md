---
context: default
phase: post-v2.7 (milestone complete + shipped/tagged, not yet archived)
task: null
total_tasks: null
status: paused
last_updated: 2026-05-27T15:58:20.725Z
supersedes: previous .continue-here.md (2026-05-26 post-v2.7 release-prep era) — that ship/tag/sign work is DONE (v2.7 + v0.57.2 pushed + tagged at 637a426c)
---

# Resume Here — Post-v2.7 Maintenance

> **Context:** Milestone **v2.7 (Windows supervised-run hardening) is 100% COMPLETE,
> shipped, pushed, and tagged** (`v2.7` / `v0.57.2` at `637a426c`). There is **no active
> phase** to resume. This session was post-milestone maintenance: two debug sessions
> resolved, a version bump, and three WFP service stop/uninstall fixes. The debug ledger
> is now **clean (0 active sessions)**. Working tree is **clean (all committed)**.

<current_state>
Workspace is at **v0.57.3** (bumped this session, unreleased). A full release build is
green (all 5 crates; `nono.exe` reports 0.57.3). All this session's work is committed
(clean `git status`). The single open thread is post-fix verification + the choice to
archive v2.7.
</current_state>

<completed_work>

This session (newest first):
- `147b722f` docs: update WFP-uninstall todo (Fix #2b drafted, live-UAT remains)
- `59808e2d` **Fix #2b** — WiX `CaUninstallWfpServices` custom action auto-removes WFP
  services on MSI uninstall (machine-scope, deferred/no-impersonate, fail-open
  `Return="ignore"`, `Before="RemoveFiles"`, `(REMOVE="ALL") AND NOT UPGRADINGPRODUCTCODE`).
  **Compile-validated** (machine MSI builds clean with WiX 7.0.0); runtime UNVALIDATED.
- `3f52b43a` docs: capture todo (WFP uninstall follow-up)
- `2ff7144a` docs(debug): resolve `wfp-service-stop-uninstall`
- `b852826b` **Fix #2a** — `nono setup --uninstall-wfp` stops+deletes the WFP user-mode
  service AND the `nono-wfp-driver` kernel service (4 mock-runner unit tests).
- `0cbeb3be` **Fix #1** — `nono-wfp-service` now honors `SERVICE_CONTROL_STOP` (tokio
  `Notify` shutdown + biased `select!`; fail-secure dynamic-engine teardown). Fixes the
  stop error AND the user-mode uninstall file-leftover.
- `8083d334` docs(debug): resolve `claude-exe-dll-init-failed` (0xC0000142 fixed by v2.7
  Phase 51; verified via Phase 52 HUMAN-UAT).
- `27acdb01` chore(release): bump workspace 0.57.2 -> 0.57.3.
- `7f80c3b5` / `647824f1` debug `unsigned-broker-trust-fail` resolved + guard edits.
</completed_work>

<remaining_work>

1. **Archive v2.7** — `/gsd:complete-milestone` (v2.7 is done + tagged; ledger clean).
2. **Elevated live-UAT for WFP fixes #1/#2a/#2b** — the gating remaining item; needs an
   elevated Win11 box. Todo:
   `.planning/todos/pending/2026-05-27-wix-auto-uninstall-wfp-custom-action-plus-live-uat.md`.
3. Older pending todos (unrelated): `validate_deny_overlaps` pre-flight (needs Linux host);
   `validate_restore_target` fd-relative TOCTOU hardening.
</remaining_work>

<decisions_made>

- Unblock the unsigned-MSI broker failure via **dev-layout run** (`target/release/nono.exe`,
  which `is_dev_build_layout()` exempts from the broker trust gate) rather than signing the
  installed binary — avoids a machine-wide self-signed-cert trust import. The refusal itself
  is correct fail-secure behavior (Phase 32 D-32-12), not a bug.
- WFP **service-stop** fixed with a `tokio::sync::Notify` shutdown + biased `select!` on
  `connect()`: the handler ACKed STOP but never broke the infinite accept loop, so STOPPED
  was unreachable (SCM fast-fail) — which also blocked MSI removal of the locked binary.
- **Fix #2b** WiX custom action is **strictly fail-open** so a mistake can never block
  uninstall (worst case = driver left behind, today's behavior); uninstall-only +
  `NOT UPGRADINGPRODUCTCODE` so upgrades don't tear down the services.
</decisions_made>

<blockers>
- WFP fixes are statically verified (build + 4 unit tests + clippy + WiX compile) but
  **behaviorally unverified** — live-UAT needs elevation this shell lacks. Not blocking
  further work, but gates closing the WFP todo.
- Shipped MSIs are UNSIGNED → runtime broker gate correctly rejects them. Operational fix:
  set `WINDOWS_SIGNING_CERT` / `WINDOWS_SIGNING_CERT_PASSWORD` repo secrets with a real CA
  cert (the release.yml signing pipeline is already wired) + verify on next `v*` tag push.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable twin of this file (full task/blocker/decision lists).
2. `.planning/debug/resolved/wfp-service-stop-uninstall.md` — full root cause + the three fixes.
3. `.planning/todos/pending/2026-05-27-wix-auto-uninstall-wfp-custom-action-plus-live-uat.md` — the exact elevated UAT steps.

## Critical Anti-Patterns (do NOT repeat)
- **Treating a fail-secure refusal as a bug** → the unsigned-broker and the service-stop
  symptoms each had a "looks broken" surface but distinct causes; read the cited code path
  before hypothesizing (see `[[feedback_verify_debug_hypothesis]]`).
- **Claiming Windows supervised/MSI behavior "works" from this shell** → it can't be
  exercised here (no real console, no elevation). Static checks ≠ live verification; say so.

## Infrastructure State
- Bash tool = non-elevated git-bash/MSYS; intermittently dies with cygwin
  `add_item ("...Program Files/Git", "/", ...)` fork errors (exit 5) on `ls`/`mv`/`git add` —
  **retry the same command once**, it succeeds.
- WiX 7.0.0 CLI available at `~/.dotnet/tools/wix` (used to compile-validate the MSI).
- Release artifacts present + current at 0.57.3 under `target/release/`.

<context>
Everything is committed and tidy. The natural next move is to archive v2.7
(`/gsd:complete-milestone`) and/or run the elevated WFP live-UAT on a real Win11 box.
Nothing is mid-edit; no background processes left running.
</context>

<next_action>
Start with: `/gsd:complete-milestone` to archive v2.7 — OR, if on an elevated Win11 box,
run the WFP live-UAT from the todo above to validate Fix #1/#2a/#2b and close that todo.
</next_action>
