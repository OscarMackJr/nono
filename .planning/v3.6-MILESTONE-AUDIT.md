---
milestone: v3.6
milestone_name: "UPST12: Upstream Sync v0.66.0 -> v0.69.0"
audited: 2026-08-08
status: gaps_found
scores:
  requirements: 20/20 delivered, 8/20 fully verified
  phases: 5/7 verified (2 unverified, 3 human_needed)
  integration: 11/14 chains wired
  flows: 3 partial
gaps:
  requirements:
    - id: "PROF-01"
      status: "partial"
      phase: "110"
      verification_status: "missing"
      evidence: "Phase 110 has NO VERIFICATION.md. All 4 PROF requirements are marked Complete in REQUIREMENTS.md and listed in SUMMARY frontmatter, but no phase-level verification artifact exists. Per audit-milestone step 2 an unverified phase is a blocker. Additionally NEW-02 below is a live defect in PROF-01's own merge path."
    - id: "PROF-02"
      status: "partial"
      phase: "110"
      verification_status: "missing"
      evidence: "Same missing-VERIFICATION.md blocker as PROF-01."
    - id: "PROF-03"
      status: "partial"
      phase: "110"
      verification_status: "missing"
      evidence: "Same blocker. Integration itself verified WIRED end-to-end (caps -> WindowsNetworkPolicy -> WFP FWP_MATCH_RANGE)."
    - id: "PROF-04"
      status: "partial"
      phase: "110"
      verification_status: "missing"
      evidence: "Same blocker."
    - id: "SEC-02"
      status: "partial"
      phase: "114"
      verification_status: "missing"
      evidence: "Phase 114 has NO VERIFICATION.md — gsd-verifier was deliberately skipped by the orchestrator and this was disclosed at the time. Code review (3 Criticals found and fixed) and UAT (5/5 after one fix) both ran, but neither is the phase-goal verification gate. Additionally REQUIREMENTS.md:131 overstates the delivered surface (see ACC-01)."
    - id: "NET-02"
      status: "partial"
      phase: "113"
      verification_status: "human_needed"
      evidence: "Verified human_needed, not passed. The live SPIFFE/SPIRE path has been exercised zero times anywhere — no SPIRE agent on this host and the CI lane has never run."
    - id: "CORE-01, CORE-02, VERIFY-01, RLS-14"
      status: "partial"
      phase: "111"
      verification_status: "human_needed"
      evidence: "Verified human_needed. VERIFY-01 specifically: its fork-invariant evidence predates phases 112-114 entirely."
    - id: "SEC-01, SEC-03..SEC-09, RES-01, RES-02"
      status: "partial"
      phase: "112"
      verification_status: "human_needed"
      evidence: "Verified human_needed with 3 items in 112-HUMAN-UAT.md, operator-approved. ROADMAP SC4's 'make ci GREEN' remains only PARTIALLY evidenced (the 36-binary sweep stalled and was killed)."
  integration:
    - id: "NEW-01"
      severity: "blocker"
      requirements: ["NET-01", "SEC-02"]
      summary: "Three denial sites emit denial_category: None, and the CONNECT-path one is NET-01's primary deny_domain enforcement point for HTTPS."
    - id: "NEW-02"
      severity: "warning-security"
      requirements: ["PROF-01", "NET-02", "SEC-02"]
      summary: "platform_overrides merges custom_credentials by whole-value replace per key, so an override can silently DROP a route's capture or spiffe on one OS."
    - id: "NEW-03"
      severity: "warning"
      requirements: ["SEC-01", "SEC-02"]
      summary: "aws_auth + capture passes profile validation but is unreachable at runtime on every path."
    - id: "NEW-04"
      severity: "warning"
      requirements: ["NET-01", "SEC-02"]
      summary: "direct_connect_ports is a port-only OS grant that voids the capture guarantee for any port the profile opens; not named in ADR-114's scope limit."
    - id: "ACC-01"
      severity: "accuracy"
      requirements: ["SEC-02"]
      summary: "REQUIREMENTS.md:131 says the mint-to-resolve loop is 'closed end-to-end'. True only for relay site 1; sites 2/3 apply the response relay but not the request-side resolve."
  flows: []
tech_debt:
  - phase: 114-oauth-capture-absorb-sec-02
    items:
      - "9 Warning + 6 Info findings open by operator scoping (114-REVIEW.md) — incl. the false 'always held in Zeroizing' claim at 5 points, reject_unrewritten_token_fields' matches! letting accessToken through, unbounded phantom-store growth, nono-py decoder raising ValueError on its own encoder's strings"
      - "Both cross-target clippy gates were last run at 66bf465b — before the 3 Critical fixes and before 1195e1f9"
  - phase: 112-security-residual-sync
    items:
      - "12 Warning + 5 Info open (112-REVIEW.md) — highest-value WR-06: --allow-gpu is now a hard error on non-NVIDIA hosts / WSL2 / containers without CAP_SYS_ADMIN"
      - "ROADMAP SC4 'make ci GREEN' only PARTIALLY evidenced"
  - phase: 113-spiffe-spire-workload-identity
    items:
      - "WR-02 handle_spiffe_assertion_credential success path untested anywhere"
      - "WR-03 Windows SPIRE-socket isolation is a log-only no-op relying on unverified AppContainer deny-by-omission"
  - phase: 111-core-carry-resource-cli-verify-release-leapfrog
    items:
      - "Documented full-sweep baseline is a 17-name failing set; not re-established after 112-114"
nyquist:
  compliant_phases: [110, 111, 112, 113, 114]
  partial_phases: []
  missing_phases: [108, 109]
  overall: "5/7 compliant, 2 missing VALIDATION.md (108, 109 — both predate the Nyquist workflow's adoption in this milestone)"
---

# Milestone v3.6 — UPST12 Upstream Sync (v0.66.0 → v0.69.0) — Audit

**Status: `gaps_found`.** All 20 requirements were *delivered* and no requirement is
`unsatisfied` or orphaned. The gate trips on **verification coverage**, not on missing work:
two of seven phases have no VERIFICATION.md at all, three more verified `human_needed` rather
than `passed`, and the integration check surfaced one blocker-class and three warning-class
cross-phase findings that no single phase's review could have caught.

## Phase Verification Coverage

| Phase | VERIFICATION.md | Status |
|-------|-----------------|--------|
| 108 UPST12 Divergence Audit | present | `passed` |
| 109 Proxy/Network Absorb | present | `passed` |
| **110 Profile/Policy Absorb** | **MISSING** | **unverified — blocker** |
| 111 Core Carry + Release Leapfrog | present | `human_needed` |
| 112 Security + Residual Sync | present | `human_needed` |
| 113 SPIFFE/SPIRE | present | `human_needed` |
| **114 OAuth Capture (SEC-02)** | **MISSING** | **unverified — blocker** |

Phase 114's missing artifact is a known, disclosed orchestrator deviation: `gsd-verifier` was
skipped and the phase was marked complete out of workflow order. Code review and UAT both ran
and were substantive, but neither is the phase-goal gate. Phase 110's absence is older and
was not previously flagged.

## Requirements — 3-Source Cross-Reference

All 20 milestone requirements were cross-referenced against REQUIREMENTS.md traceability,
phase VERIFICATION.md tables, and SUMMARY.md `requirements-completed` frontmatter.

**No requirement is `unsatisfied`. No orphans.** Every REQ-ID assigned to a v3.6 phase appears
in at least one SUMMARY frontmatter and at least one phase artifact.

**Bookkeeping drift found and correctable:** NET-01 and NET-03 read `Pending` / `[ ]` in the
traceability table while Phase 109 verified `passed` and both appear in SUMMARY frontmatter.
Per the status-determination matrix (`passed` + listed + `[ ]` → **satisfied, update checkbox**)
these are satisfied and the table is simply stale.

## Cross-Phase Integration Findings

The integration check traced the layered proxy/sandbox stack rather than API routes, because
that is where this milestone's real risk sits — phases 109 → 113 → 114 all modified the same
five files in `crates/nono-proxy/src/`.

### NEW-01 — BLOCKER — uncategorised denials on the dominant HTTPS path
*Affects NET-01, SEC-02*

Phase 112 built the structured `NetworkAuditDenialCategory` spine; 113/114 extended it. Three
denial sites were never connected to it and pass `&audit::EventContext::default()`:

- `crates/nono-proxy/src/connect.rs:83-90` — **verified at source.** CONNECT host denied by
  `ProxyFilter`. This is NET-01's `deny_domain` enforcement point for **HTTPS**, i.e. the
  dominant path.
- `crates/nono-proxy/src/external.rs:133-140` and `:197-204` — external-proxy denials.

The identical denial on the other two paths *does* carry the category (`reverse.rs:365-376`,
`server.rs:1146-1156`, both `HostDenied`). So a `deny_domain` block produces a categorised
audit record over plain HTTP and an uncategorised one over HTTPS — any consumer filtering on
`denial_category == HostDenied` silently under-reports enforcement.

`NetworkAuditDenialCategory::ExternalProxyRejected` (`crates/nono/src/undo/types.rs:245`) has
**zero production constructors** — verified independently: its only two workspace references
are nono-py's encoder (`../nono-py/src/proxy.rs:92`) and decoder (`../nono-py/src/undo.rs:613`),
neither of which produces it. The site that should (`external.rs:197`) uses the default context.

Not present in `112-REVIEW.md`, `113-REVIEW.md`, or `114-REVIEW.md` — it is invisible to any
single-phase review because it requires comparing 112's spine against 109's check sites.

### NEW-02 — WARNING (security) — `platform_overrides` can silently drop `capture`/`spiffe` on one OS
*Affects PROF-01 × NET-02 × SEC-02*

**Verified at source.** `crates/nono-cli/src/profile/mod.rs:3683-3687`:

```rust
custom_credentials: {
    let mut merged = base.network.custom_credentials;
    merged.extend(child.network.custom_credentials);
    merged
},
```

`custom_credentials` is a `HashMap<String, CustomCredentialDef>`, so `.extend()` is
**whole-value replace per key**, and `apply_platform_overrides` merges the per-OS block in as
an ordinary `merge_profiles` child. A `platform_overrides.windows` block redefining an existing
credential name **without** `capture` therefore removes the OAuth-capture rewrite for that route
on Windows only — the real token then reaches the sandboxed agent unrewritten. The same channel
drops `spiffe` (route falls back to unauthenticated forwarding).

This contradicts Phase 110's own stated D-08a invariant at `mod.rs:2687-2694` — that an override
"may tighten (add) but never silently loosen" — and it is the odd one out among its neighbours,
all of which are fail-secure-additive: `block` is OR (`:3644`), `deny_domain` / `no_proxy` are
`dedup_append` (`:3653-3654`). Post-override re-validation does not catch it: an override entry
carrying only `credential_key` satisfies the "at least one of" check at `mod.rs:1183-1194`.

### NEW-03 — WARNING — `aws_auth` + `capture` validates but is unreachable
*Affects SEC-01, SEC-02*

`validate_custom_credential` excludes `aws_auth` only against `credential_key`/`auth`, and
`capture` joins no exclusion arm, so the pair loads cleanly. At runtime `reverse.rs:328-331`
returns 501 on `aws_route.is_some()` **before** the capture branch, and the cross-path guards
then deny the same host. Fail-closed, so no leak — but a fully unusable route presenting as
valid config violates "never silently degrade" for an operator-facing control.

### NEW-04 — WARNING — `direct_connect_ports` voids the capture guarantee for granted ports
*Affects NET-01, SEC-02*

`direct_connect_ports` (from `caps.tcp_connect_ports()`, `proxy_runtime.rs:590`) maps to a
sandbox grant that is **port-scoped with no host dimension**. Any profile granting
`--allow-connect-port N` lets the agent open a direct TCP connection to a capture-declared host
on port N without contacting the proxy, so none of Phase 114's three guards can fire. ADR-114's
scope limit covers "CONNECT flows to arbitrary hosts" but does not name this grant as a
mechanism — it should, explicitly.

*Note:* the adjacent NO_PROXY path was traced specifically for this bypass class and is
**correctly closed** — `merge_no_proxy_hosts` (`server.rs:331-360`) re-filters every entry
host-only via `route_host_from_host_port`, so the exact-port `is_route_upstream` at `:705`
cannot leak a capture host into NO_PROXY.

### ACC-01 — ACCURACY — SEC-02's requirement text overstates the delivered surface
*Affects SEC-02*

`REQUIREMENTS.md:131` states the mint-to-resolve loop is "**closed end-to-end** with a live
production call site." That is true only for relay site 1: `resolve_capture_request_body` has
exactly one production call site (`reverse.rs:427-428`), which sits *after* the SPIFFE dispatches
at `:222-247` have already returned. Sites 2 and 3 apply the response relay but not the
request-side resolve. `114-06-SUMMARY.md:131` records this as a deliberate scope limit — but
since `spiffe` + `capture` is both accepted by validation and explicitly supported on the
response side, the requirement record overstates a reachable configuration.

Given this project's twice-burned history with inaccurate records, this is worth correcting to
match ADR-114's more careful Consequence-2 wording.

## Integration Verified WIRED

11 cross-phase chains traced end-to-end with no defect, including: the full NET-01 `deny_domain`
path; **113↔114 guard coexistence on both forward-HTTP and CONNECT** (all three predicates
evaluated before the gate and OR'd — the CR-03 fix is present and correct, SPIFFE retains
category precedence); all three response-relay sites applying the capture enforcement point;
`CapturePhantomStore` scoped per `ProxyState` so phantoms survive across requests; route load
failing closed on unreachable SPIRE socket; **the ADR-86 policy-free boundary intact** (core
`nono` carries only observability types for 113/114 — zero `spiffe`/`capture` policy symbols
anywhere else in `crates/nono/src`); and binding struct-drift closed in both sibling repos with
fail-secure (not zero) defaults.

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant |
|-------|---------------|-----------|
| 108, 109 | missing | — |
| 110, 111, 112, 113, 114 | present | `true` |

108 and 109 predate this milestone's adoption of the Nyquist workflow; 5/5 phases since are
compliant. Not treated as a blocker.

## Assessment

The milestone delivered everything it set out to. What it lacks is **verification symmetry** —
the later, riskiest phases were verified least formally, and the two phases with no
VERIFICATION.md at all (110, 114) are the ones carrying the most cross-phase surface. The
integration findings above are direct evidence that this matters: NEW-01 and NEW-02 are both
defects that no single-phase review could see, and NEW-02 is a live path by which a
`platform_overrides` block can void the security property Phase 114 exists to provide.
