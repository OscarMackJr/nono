---
milestone: v3.0
milestone_name: Enterprise Hardening I (Deploy · Control · Compliance)
audited: 2026-06-19T13:10:00.000Z
status: tech_debt
scores:
  requirements: 17/17 (12 verified, 5 host-gated partial; 0 unsatisfied)
  phases: 3/3 complete (all human_needed = structural pass + host-gated UAT)
  integration: 5/5 wires WIRED (1 warning — daemon telemetry)
  flows: nono-run spine composes E2E; daemon-launched-agent denial telemetry is the one uncomposed seam (warning)
gaps:
  requirements: []   # no unsatisfied, blocked, or orphaned requirements
  integration:
    - wire: "daemon enforcement path → telemetry"
      severity: warning
      detail: "nono-agentd registers no SecurityEventLayer / no init_tracing; agents launched via the multi-tenant daemon (the Phase 83 marquee enforcement path) produce zero nono_security::* telemetry. The nono run / exec_strategy path (which the Phase 84 gate exercises) IS fully wired. No Phase 84 plan or success criterion scoped daemon-side emission, so this is correctly classified WARNING, not a blocker."
      affects: ["TELEM-01", "EGRESS-02+TELEM-01 composition"]
      follow_up: "First item for any v3.x telemetry-coverage phase: call init_tracing from nono-agentd + wire daemon denial/launch events to nono_security::* targets."
  flows: []
tech_debt:
  - phase: 82-fleet-deployment-infrastructure
    items:
      - "DEPLOY-01: clean-VM silent msiexec /qn install + exit codes — structural verified (CRT-static, .wxs non-fatal ServiceInstall/cert), live clean-VM run host-gated"
      - "DEPLOY-03: live R-B3 user-owned WRITE_OWNER scratch verification on an installed host — host-gated"
      - "DEPLOY-05: full three-client (CryptoAPI/Node/rustls) TLS-through-proxy round-trip — host-gated (needs running proxy + clean VM)"
  - phase: 83-machine-policy-spine-egress-control
    items:
      - "EGRESS-02: dual-layer (proxy + WFP) live block proof — structurally wired from one MachineEgressPolicy read; live kernel-WFP block proof host-gated"
  - phase: 84-siem-edr-telemetry
    items:
      - "TELEM-01: live telemetry-event-emit gate PASS (admin + MSI-registered Event Log source + logman) — host-gated (84-HUMAN-UAT.md item 1)"
      - "TELEM-04: live admin TelemetryEnabled=0 opt-out + min_severity filtering HKLM→emit proof — host-gated (84-HUMAN-UAT.md items 2-3)"
      - "Daemon-side telemetry emission — cross-phase follow-up (see integration warning)"
nyquist:
  overall: skipped
  note: "Nyquist validation was disabled for the v3.0 execution runs (no *-VALIDATION.md files); not a milestone blocker."
notes:
  - "Traceability table reconciled during this audit: DEPLOY-01/03/05 Pending→Partial(host-gated), DEPLOY-02/04/06 Pending→Complete, POLICY-03 + EGRESS-01/02 Pending→Complete — they were stale vs the authoritative per-phase VERIFICATION.md verdicts (82: 5/5, 83: 7/7)."
  - "SUMMARY.md requirements-completed frontmatter was left empty by most executors (metadata-hygiene gap, not a functional gap); requirement satisfaction is authoritative in the per-phase VERIFICATION.md bodies + reconciled traceability table."
  - "Phase 84 in-session gap closure: code-review + verifier caught the 84-04 executor's premature-complete + masked blockers (CR-01 gate happy-path, WR-01 TELEM-04 config-unwired); both fixed inline + re-verified 5/5 structural. A nono-ffi exhaustive-match build regression (Phase 83 PolicyLoadFailed + 84 telemetry variants, hidden by the --bin nono gate) was also fixed."
---

# v3.0 Enterprise Hardening I — Milestone Audit

**Status: `tech_debt`** — all 17 requirements satisfied structurally, no critical blockers, no unsatisfied/orphaned requirements. Accumulated debt is host-gated live UAT (Dark Factory) + one cross-phase telemetry-coverage warning (daemon path).

## Requirements Coverage (17/17)

| Requirement | Phase | Verdict | Basis |
|---|---|---|---|
| DEPLOY-01 | 82 | Partial (host-gated) | MSI generator + crt-static verified; clean-VM install host-gated |
| DEPLOY-02 | 82 | Verified | machine-wide PATH `Environment System="yes"` in .wxs + contract assert |
| DEPLOY-03 | 82 | Partial (host-gated) | provision_windows.rs scratch + icacls owner unit-tested; live R-B3 host-gated |
| DEPLOY-04 | 82/83 | Verified | nono.admx/.adml → `SOFTWARE\Policies\nono`; reader opens same path |
| DEPLOY-05 | 82 | Partial (host-gated) | machine+user cert import wired; 3-client TLS-through-proxy host-gated |
| DEPLOY-06 | 82 | Verified | health.rs tri-state JSON + non-fatal ServiceInstall Vital=no; 8 tests |
| POLICY-01 | 83 | Verified | reader KEY_WOW64_64KEY; absent→Ok(None), present→Some |
| POLICY-02 | 83 | Verified | malformed→Err(PolicyLoadFailed), never fall-open; SC-2 gate |
| POLICY-03 | 83 | Verified | single SOLE read feeds proxy+WFP; WFP never reads HKLM |
| EGRESS-01 | 83 | Verified | N×REG_SZ allowlist; is_unconfigured() sentinel gate |
| EGRESS-02 | 83 | Verified (live dual-layer host-gated) | one struct → ProxyFilter + wfp_filter_add(proxy-only) |
| EGRESS-03 | 83 | Verified | DNS-component wildcard matrix tests pass |
| EGRESS-04 | 83 | Verified | AI-provider presets in network-policy.json + ADMX toggles |
| TELEM-01 | 84 | Verified (live gate host-gated) | 3 denial sources → SecurityEventLayer → `nono` source; CR-01 closed |
| TELEM-02 | 84 | Verified | real Hmac<Sha256> chain + ChainHead + WEF ADR |
| TELEM-03 | 84 | Verified | path hashing + scrub_value; SC-3 no-raw-path |
| TELEM-04 | 84 | Verified (live opt-out host-gated) | WR-01 config-from-policy + WR-02 min_severity wired |

No requirement is `unsatisfied`, `partial-unverified`, or `orphaned`. The 5 "Partial (host-gated)" entries are structurally complete with live runtime proof deferred as Dark Factory tech-debt.

## Cross-Phase Integration (5/5 wires WIRED)

1. **82→83** MSI/ADMX HKLM key path + value names → reader — WIRED (`build-windows-msi.ps1:305`, `machine_policy.rs:463`)
2. **83 one-struct** single read → proxy AND WFP, no drift — WIRED (`agent_daemon/mod.rs:358`, `launch.rs:710/729`; WFP never re-reads HKLM)
3. **83→84** telemetry config from `MachineEgressPolicy.telemetry` (WR-01) — WIRED (`main.rs:171`, `cli_bootstrap.rs:120`)
4. **82→84** emit source == registered source == gate query (`"nono"`) — WIRED (`windows.rs:35`, `build-windows-msi.ps1:323`, gate:60)
5. **denial sources → nono_security::\*** → dual-emit — WIRED (3 sources; 10003 reserved-by-design)

**Warning:** daemon enforcement path → telemetry is NOT wired (`nono-agentd` registers no SecurityEventLayer). Daemon-launched agent denials produce zero telemetry. Out of Phase 84 scope; tracked follow-up.

## Verdict

Proceed to milestone close accepting the documented tech-debt: host-gated live UAT (clean-VM install, dual-layer WFP block, live HKLM→emit) + the daemon-telemetry cross-phase follow-up. No code changes required to seal v3.0.
