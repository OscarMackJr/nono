---
gsd_state_version: 1.0
milestone: v3.7
milestone_name: Composite Integrity + Tool-Sandbox Disposition
status: Phase 118 (Per-Session Enforcement Receipts) 10/10 plans done, close-out ~complete 2026-09-06 with TWO items still open. **Task 3's blocking human-verify checkpoint was RUN on a real host and FAILED** — a confined child reads every receipt on BOTH arms, because `deny_sid_on_path` uses `NO_INHERITANCE` and the mandatory label carries no `(OI)(CI)`, so both guards are directory-scoped and neither reaches the receipt FILES. Operator decision: narrow D-08 to WRITE-INTEGRITY ONLY and amend the docs (050d0710, ab749561); read confidentiality is explicitly NOT claimed. `/gsd:code-review` (D-22) then found **6 Criticals**, all now fixed and independently re-verified: CR-01 lying `Ran` receipt + 2 silent exits (0203ab61), CR-02 chain restart at sequence 0 (c731c4e0), CR-03 `NONO_SESSION_ID` unset on broker (b555c392), CR-04 raw `%PROGRAMDATA%` on both producers (aa48cdbb), CR-05 unbounded sink DENY-ACE growth (3e23ba44), CR-06 broker receipts written to a sandbox-local path since 2026-08-16 (4d0c5ded, root-caused via /gsd:debug, both arms live-verified). WR-02 fixed (06393e57). `make ci` GREEN (was red on pre-existing dead_code since 118-05); 3 integration failures adjudicated PRE-EXISTING; verifier 4/4 SC met, status `human_needed`. ~~OPEN 1~~ **CLOSED 2026-09-06 (a583566e)** — CR-05 now covers all THREE producers; `BrokerSinkSidGuard` mirrors `SinkSidGuard` in `nono-shell-broker`. Verified on the arm that actually exercises it (`-p claude-code`, `app_container=true`): package-SID DENY ACEs held at 2→2 and total DENY at 16→16 across a broker session, with the broker receipt still written. The earlier 16→16 claim was made from a `WriteRestricted`-arm run where the broker never executes — one producer measured, three asserted. **OPEN 2 — RCPT-02's requirement text is STALE and needs an operator call:** REQUIREMENTS.md and ROADMAP SC-3 say "HMAC-chained `SecurityEventLayer`"; what shipped is a KEYLESS SHA-256 chain (D-25), because `SecurityEventLayer`'s key is per-process OsRng zeroized on Drop and would make receipts unverifiable after process exit. Amend the text or record an accepted divergence — do not let the table read as if the stated mechanism shipped. Phase 117 Complete (1 human item open: WR-20 needs an elevated/CI Windows runner).
shipped_milestone: v3.6
shipped_milestone_name: "UPST12: Upstream Sync v0.66.0 -> v0.69.0"
shipped_date: 2026-08-08
shipped_tag: v3.6 (LOCAL — not pushed)
shipped_audit_status: passed_with_accepted_risk
stopped_at: "**v3.7 Phase 118 (Per-Session Enforcement Receipts) PLANNED 2026-08-16 — 10 plans across 6 waves, READY TO EXECUTE.** Research + pattern map + validation strategy all landed; `gsd-plan-checker` returned 0 blockers on the full pass and a delta re-check confirmed the revision landed with no regressions. **Three operator decisions were taken mid-planning (D-25/D-26/D-27, recorded in 118-CONTEXT.md) because research falsified two of the discuss-phase's working assumptions.** (1) D-25: the receipt chain is a KEYLESS SHA-256 hash chain on the core `audit.rs::hash_chain` construction, NOT the telemetry HMAC — `SecurityEventLayer`'s key is per-process, OsRng-generated and zeroized on Drop, so mirroring it literally as D-11 prescribed would have produced receipts nobody could verify after process exit. D-11's own-chain-domain argument survives; only the primitive changed. No code or output may claim 'only the key holder could have produced this'. (2) D-26: `daemon_attest_and_decide` is restructured from a 5-of-13 early-return chain to collect-all-then-decide — a fail-direction change to shipped security code that needs a decision-equivalence proof, not just a row-count assertion. (3) D-27: the broker's 11 unmodelled census rows come over a WIDENED WIRE CONTRACT, not hardcoded knowledge, keeping `layer_registry.rs` the single source of truth. **DURABLE FINDING FROM THE VERIFY ROUND: essentially every `cargo test` command in the first plan set silently ran ZERO tests and exited 0.** Two independent defects — wrong package names (`-p nono`/`-p nono-cli` don't resolve; the real names are `nono-sandbox`/`nono-sandbox-cli`, and nono-cli has NO `[lib]` target) and a wrong module path (the Windows module is bound as `exec_strategy`, not `exec_strategy_windows`). Both were caught only because a delta re-check questioned the COMMANDS rather than the design; the full first-pass review missed them entirely. Verified live with a control repro. All 10 plans + 118-VALIDATION.md swept, and every verify command now asserts a non-zero test count. **Note the decision-coverage gate returned a VACUOUS GREEN** (`passed:true skipped:true total:0`) against this CONTEXT.md's bullet format — all 27 decisions were confirmed covered by hand instead. STATE/ROADMAP updated by hand per D-24 (two milestones open; SDK writers stay banned).

PRIOR (2026-08-11): **v3.7 Phase 117 GAP-CLOSURE ROUND 3 EXECUTED 2026-08-11 - all 8 plans (117-27..117-34, waves 12/13/14) landed, 34/34 plans now executed, 32 commits.** Round 3 was planned by CLASS rather than by site, and that discipline held: 117-29 enumerated every SACL reader and confirmed no third site existed; 117-33 enumerated all three chain-advancing emitters; 117-31 confirmed exactly two source-scanning matchers; 117-32 widened the SPEC gate from LayerId NAMES to whole-document citation scanning and surfaced 4 further stale citations beyond its own plan scope. **D-37 is implemented** as the three-arm ancestor classification (walked+empty+stopped-at-non-owned -> NotApplicable/contract-exempt; walked+empty+NOT that stop -> NotApplied/real gap; walked+granted -> Applied), with the daemon mirror proven by a CONCRETE BEHAVIORAL assertion (DaemonDaclGuard::apply returns Ok for that condition) rather than a variant-name comparison, and the CLI/daemon type asymmetry stated explicitly. **THE ONE ITEM AN OPERATOR MUST ACT ON: WR-20 IS AUTHORED BUT UNVERIFIED.** Plan 117-30's test non_owned_path_with_a_foreign_label_is_exempt_not_a_coverage_gap constructs the combined non-owned + foreign-labeled condition correctly, but CANNOT RUN on this dev host - the shell is a non-elevated TWGGLOBAL\OMack domain account with no SeTakeOwnershipPrivilege/SeRestorePrivilege/SeBackupPrivilege, so its icacls /setowner setup fails and the test fails LOUDLY by design per D-31 rather than skipping silently. **No perturbation proof was producible for WR-20** - every other new discovery test in this round carries one with real command output. This makes the expected full-suite result 1624 passed / 12 failed = the 11 documented Windows-host baseline failures PLUS this one intentional host-blocked test; do NOT treat the 12th as a regression and do NOT weaken it to a silent skip. WR-20 needs an elevated or CI Windows runner (SYSTEM/Administrator) before it can be called proven, and the 117-34 SPEC ledger row records it as such. Also open: deferred-items.md logs one stale docstring in exec_strategy_windows/launch.rs (out of scope for 117-28). NEXT: code review iteration 6 + re-verification - note that the previous THREE rounds each had their fixes produce the next round's blocker, so iteration 6 is the real test of whether planning-by-class broke that cycle."

PRIOR (2026-08-11): **v3.7 Phase 117 GAP-CLOSURE ROUND 2 EXECUTED 2026-08-11 - 7 plans (117-20..117-26) across waves 9/10/11, phase now 26/26 plans executed, AWAITING A THIRD VERIFICATION PASS.** Ran sequentially on the main tree (`workflow.use_worktrees=false`), branch `milestone/v2.13-carryforward-closeout`, pre-round base `97ff0962`. Wave 9 = 117-20/21/22/24/25, Wave 10 = 117-23 (serialized behind 117-21 on shared `exec_strategy_windows/launch.rs`), Wave 11 = 117-26 (doc-only, last so the ledger records the round). **WHAT SHIPPED:** **117-20 (CR-01 BLOCKER)** widened `crates/nono/src/sandbox/windows.rs`'s mandatory-label reader to `low_integrity_label_ace(path) -> Option<(rid, mask, AceFlags)>` (old `low_integrity_label_and_mask` is now a thin wrapper, all 6 call sites unchanged) so `AppliedLabelsGuard::snapshot_and_apply`'s residue-equivalence predicate can reject a matching `(rid, mask)` ACE carrying `INHERIT_ONLY_ACE` - a structurally-inert ACE could previously be mistaken for nono's own effective label; `INHERITED_ACE` is still accepted because the OS does evaluate it. Also reordered the ownership check ahead of any prior-label inspection (WR-01), corrected the revert-semantics doc (WR-02), and unbroke a test whose pre-label mask collided with `AccessMode::Read` (WR-03). **117-21 (CR-02, D-28)** added a Windows-only `log_target_is_private()` to `cli_bootstrap.rs` and gated the downgrade warn's layer-name detail on it; wired the invisible `ClearStaleLayerResidue` remediation (WR-04). **117-22 (WR-06 + WR-11)** replaced `DaemonAttestationDecision`'s FALSE premise (it claimed `DaemonDaclGuard::apply` has no partial-success return - it does: pass 1 skips and pass 3 breaks) with the true rationale: those two arms skip only TRAVERSE grants, pass 2 is the sole WRITE-granting pass and IS fail-closed, so the daemon's fail direction is under-GRANTING, never under-CONFINING - which is why no `ProceedDowngraded` state is warranted. Added a `DAEMON-DECISION-ENUM` anchor marker, an occurrence-count-hardened discovery test, a cross-mirror subset test against the CLI's `AttestationDecision`, and an exhaustive-match behavioral test; each proven to actually fail under deliberate perturbation. **117-23 (WR-07)** gave `AppliedAncestorTraverseGuard` and `AppliedAncestorReadAttributesGuard` a `walked: bool` so `application()` is genuinely 3-state (`NotApplicable` only when nothing walked, `PartiallyApplied` when walked-but-zero-granted, `Applied` otherwise) - the predicate-width class this phase has now fixed three times. **117-24 (SC1/CINT-01 + WR-10)** converted the remaining 9/13 registry rows to symbol-form citations, each re-confirmed by grep at its declaration, made `registry_call_sites_exist` require a real definition site, and actually implemented `contains_fn_exact`'s documented `!` boundary. **117-25 (WR-05 + WR-09)** removed the dead `NONO_CI_HAS_WFP` block from the layer-fault-injection CI job ONLY - the `windows-security` job's copy is load-bearing (`scripts/windows-test-harness.ps1:189` gates WFP tests on it) and was correctly left in place, contradicting the finding's own literal wording; and re-narrowed `SecurityEventLayerInner`'s fields to private. **117-26 (SC4)** reconciled the SPEC's Review-fix pass ledger against grep'd source rather than the disposing plans' prose, which caught two cases where a plan's example text did not match the true outcome. **GATES:** workspace `cargo clippy --all-targets -D warnings -D clippy::unwrap_used` GREEN, `cargo fmt --check` clean, registry self-check 6/6 + meta 9/9, `nono-agentd` 89/89, `nono` bin 1613 passed / 11 failed - the EXACT 11 pre-existing Windows HOME/env-race baseline failures on record for this host, no new ones (pass count rose by 3 from 117-23's new tests). One additional core-lib failure (`supervisor::aipc_sdk::tests::windows_loopback_tests::helper_stamps_session_token_from_env`) PASSES in isolation - the parallel env-var race class CLAUDE.md warns about, in a subsystem no 117 plan touches. Cross-target gates ran per-plan where CLAUDE.md's MUST applied (117-21, 117-24, 117-25 ran both linux-gnu `cross clippy` and apple-darwin `cargo-zigbuild clippy`, all GREEN; 117-21's linux-gnu gate CAUGHT a real dead-code break and forced `log_target_is_private()` to be cfg-gated Windows-only - the gate earned its keep again). **NO SDK STATE/ROADMAP WRITER VERB RUN** - tenth consecutive phase using this deviation; every executor was explicitly forbidden from touching STATE.md/ROADMAP.md and from running any `gsd-sdk` tracking-write verb, and the orchestrator hand-edited both afterwards (7 plan checkboxes flipped, summary line and progress-table row updated to 26/26; parallel v3.5 block intact). **RESUME NOTE:** the previous run of this round was interrupted mid-117-22 leaving ~229 uncommitted lines in `agent_daemon/launch.rs`; that diff was recovered, audited claim-by-claim against the real `DaemonDaclGuard::apply`, found correct, and split into proper per-task commits rather than discarded. **NEXT:** `/gsd:code-review 117` then a third `117-VERIFICATION.md` pass against SC1-SC4; CINT-01/02/03 remain Pending in REQUIREMENTS.md - promoting them is re-verification's call, not the executor's.

PRIOR (2026-08-10): **v3.7 Phase 117 GAP-CLOSURE ROUND 2 PLANNED 2026-08-10 - 7 new plans (117-20..117-26) in 3 new waves (9/10/11), READY TO EXECUTE.** Ran `/gsd:plan-phase 117 --gaps` against the iteration-2 `117-VERIFICATION.md` (`status: gaps_found`, 0/4 truths verified) plus `117-REVIEW.md` iteration 4 (2 BLOCKER + 11 WARNING). Plan-checker returned VERIFICATION PASSED with 2 non-blocking observations (117-20 Task 2's action prose shows an intermediate wrong formula before the correct `(u32::from(prior_flags) & INHERIT_ONLY_ACE) == 0`; 117-22's WR-11 fix stays lexical, paired with a compiler-exhaustive second test). Plans committed by the planner as `c63e5b0b`; `117-01`..`117-19` untouched (all have SUMMARY.md). **NO SDK STATE/ROADMAP WRITER VERB RUN** - `state.planned-phase` and `roadmap.annotate-dependencies` deliberately skipped again (ninth consecutive phase using this deviation: 113, 114, 115, 116, 117 x2); STATE hand-edited and the ROADMAP Phase 117 section APPENDED (waves 9-11 added below wave 8, nothing overwritten; parallel v3.5 milestone block intact). Research/pattern-mapper skipped (`--gaps`, artifacts already exist); UI gate matched only on substrings in the roadmap section - false positive on a Rust/Windows backend phase, treated as `--skip-ui`. **The decision-coverage gate returned the known vacuous green** (`total: 0`, `no trackable decisions` - the `- **D-NN (LOCKED):**` bullet-form no-op), so it was checked by hand: 36/36 real CONTEXT decisions are cited across the 26 plans (`D-00` is a false substring match from `SEED-004`, not a decision). **THE HEADLINE, AND IT INDICTS ROUND 1:** the previous gap-closure round's own fix reopened the exact defect this phase exists to close. Plan 117-13's residue-equivalence predicate counts a structurally-inert `INHERIT_ONLY_ACE` as applied mandatory-label coverage, because the reader it depends on, `low_integrity_label_and_mask` in `crates/nono/src/sandbox/windows.rs`, never reads `header.AceFlags` (`grep -c AceFlags` -> 0) - so a launch with ZERO effective label enforcement can report `MandatoryIntegrityLabel` Confirmed (CR-01, closed by 117-20 together with WR-01 ownership-gate ordering, WR-02 revert semantics, WR-03 retargeted test). **Second BLOCKER:** 117-16's downgrade `tracing::warn!` carries specific `LayerId` names onto the default stderr arm the confined child shares, violating LOCKED D-28, justified by a comment whose premise is false - 2 of `init_tracing`'s 3 arms call `.with_writer(std::io::stderr)` and only the `--log-file` arm does not (CR-02, closed by 117-21, which also wires `NonoError::remediation()` into `main.rs` for WR-04). CINT-01/02/03 all remain `[ ] Pending` in REQUIREMENTS.md and 117-26 explicitly keeps them there - the previous round's `[x]` on CINT-02 (set by 117-17, a plan downstream of and blind to CR-01) was reverted 2026-08-10 and must NOT be re-flipped until re-verification passes. 117-26 is ALONE in wave 11 by design: SC4 requires it to record the final disposition of all 13 iteration-4 findings, and any finding fixed by no plan must appear there as an explicit operator-accepted risk rather than being quietly reconciled.

PRIOR (2026-08-10): **v3.7 Phase 117 GAP-CLOSURE PLANNED 2026-08-10 — 7 new plans (117-13..117-19) in 3 new waves (6/7/8), READY TO EXECUTE.** Ran `/gsd:plan-phase 117 --gaps` against `117-VERIFICATION.md` (`status: gaps_found`, 0/4 truths fully verified — SC1 partial, SC2/SC3/SC4 failed against their own literal wording). Research/pattern-mapper skipped (`--gaps`, artifacts already exist); UI gate matched only on the substrings `requirements`/`review` in the roadmap section — false positive on a Rust/Windows backend phase, treated as `--skip-ui`. Plans committed by the planner as `557837f4`; plans `117-01`..`117-12` untouched (all already have SUMMARY.md). **NO SDK STATE/ROADMAP WRITER VERB RUN** — `state.planned-phase` and `roadmap.annotate-dependencies` deliberately skipped again, STATE hand-edited and the ROADMAP Phase 117 section APPENDED (waves 6-8 added below wave 5, nothing overwritten); eighth consecutive phase using this deviation (113, 114, 115, 116, 117). **OPERATOR DECISION LOCKED THIS SESSION on SC3's bar:** automate what is automatable on an ordinary (non-elevated, unsigned-install) host, and record a per-row operator-accepted downgrade for the genuinely host-gated remainder — a manual step is never counted as a test, only as a named accepted gap. **THE FINDING THAT RESHAPED THE PLAN SET, AND IT CUTS AGAINST THE VERIFICATION REPORT:** `117-VERIFICATION.md` scored SC3 at 2/13 rows by counting `fn force_unavailable_*` in ONE file (`crates/nono-cli/tests/layer_force_unavailable.rs`, lines 160/189). The planner, and then the plan-checker independently against the live tree, found **8 more rows already have real, ordinary-host-runnable forced-unavailable tests** living under other names in other files — `restricted_token.rs:326`, `exec_strategy_windows/launch.rs:5069` and `:3662`, `dacl_guard.rs:980` and `:1083`, `nono-shell-broker/src/main.rs:1902`, `sandbox/windows.rs:3903` — plus one (`FirewallRulesEgress`) that plan `117-16` creates. So the real gap is 3 structurally/host-gated rows (`DaclSessionSidGrant`, `MinifilterAbsence`, `BrokerAuthenticodeTrustGate`), not 11. `117-18` therefore BROADENS the meta-test's discovery rather than writing 8 redundant tests. **This is the project's own standing lesson reproduced inside the verification of the phase built to catch it: a check that scans one file BY NAME is blind by construction — both the shipped meta-test and the verifier that graded it made the same mistake.** Plan map: `117-13` NR3-01 (the one open BLOCKER — CR-14's zero-ACE abort self-locks the next launch after abnormal exit / a concurrent second session; adds residue self-healing + the missing `NonoError::remediation()` arm for `LayerAttestationFailed`); `117-14` NR3-02 (the two DACL-ancestor rows derived `Applied` from `Option::is_some()` on an unconditionally-`Some` field — structurally could not deny); `117-15` NR3-03 + NR3-08 (`DaclSessionSidGrant` fail-open landmine whose doc comment cites a build-failing guard test that **does not exist anywhere in the tree**, plus the ~145-line citation drift deferred across three consecutive passes — converted to SYMBOL citations so it cannot recur); `117-16` NR3-04 (downgrade banner points at a channel carrying nothing on the success path); `117-17` NR3-05 (daemon's `ProceedDowngraded` declared and matched but never constructed — DELETED rather than implemented, with the CLI/daemon divergence RECORDED in the SPEC by `117-19` so SC4 is satisfied rather than evidence removed); `117-18` SC3 discovery broadening + NR-08 CI fix (the `windows-layer-fault-injection` job built no broker and set no `NONO_CI_HAS_WFP`, so broker/WFP-arm tests silently skipped); `117-19` SC4 SPEC ledger (port NR-04/05/06 + NR3-* rows, fix RF-14's stale pre-NR-06 prose, `git add -f` because `proj/` is gitignored). Checker verdict **VERIFICATION PASSED, 0 blockers, 2 warnings**; warning 1 fixed in place by the orchestrator (`117-18` task 2's `<automated>` was `gh workflow view ... || echo` — structurally could not fail; replaced with a real `python -c` YAML parse asserting both CI edits landed, using `python` not `python3` per host); warning 2 (`117-18`'s `depends_on` on `117-14` is conservative-but-unnecessary) accepted as-is. **KNOWN NON-BLOCKING, UNCHANGED FROM THE 2026-08-09 SESSION:** `check.decision-coverage-plan` again returned `passed:true skipped:true total:0` — the same vacuous green from CONTEXT.md's `- **D-01: ...**` bullet form; the new plans cite D-04/12/16/27/28/31/32/35, and full D-01..D-36 coverage remains carried by the shipped `117-01`..`117-12`, not by that gate. Requirements coverage checked by hand: CINT-01 (`117-15`, `117-19`), CINT-02 (`117-13`/`14`/`15`/`16`/`17`), CINT-03 (`117-16`, `117-18`). **STILL OPEN FROM 116, UNCHANGED:** the Uncovered-Window Finding (12 carve-out-touching commits dispositioned by no audit; proposed UPST13/FUT-08) NEEDS AN OPERATOR CALL, and Phase 120 planning stays operator-gated on ADR-116 ratification. **Next: `/gsd:execute-phase 117`** (waves 6-8 only — waves 1-5 are shipped).  ---- PRIOR SESSION ---- **v3.7 Phase 117 (Fail-Direction Contract + Startup Self-Attestation) PLANNED 2026-08-09 — 12 plans across 5 waves, READY TO EXECUTE.** Research (`117-RESEARCH.md`) + pattern map (`117-PATTERNS.md`) + `117-VALIDATION.md` + 12 plans; `gsd-plan-checker` ran TWO passes (11 findings -> 3 findings -> narrow pass 3), final state **0 blockers**. Commits `d1359a80` (research+validation), `441e5bec` (patterns), `b4ac7e50` (plans), `a8a3582c` (XML tag fix), `4cb9aad5` (pass-2 revision), `229d4bf6` (pass-3 revision), plus orchestrator hand-edits to VALIDATION/ROADMAP/STATE. **NO SDK STATE/ROADMAP WRITER VERB RUN** — `state.planned-phase` and `roadmap.annotate-dependencies` deliberately skipped, STATE + ROADMAP wave annotation hand-edited (seventh consecutive phase using this deviation: 113, 114, 115, 116, 117). Waves: 1 = `117-01` registry core / `117-02` library surfaces (diagnostic code + machine policy); 2 = `117-03` SPEC + drift-check / `117-04` `layer-fault-injection` feature + WFP toggle migration / `117-05` shared `crates/nono` attestation primitives; 3 = `117-06` token/label/DACL/Job hooks / `117-07` AppContainer hooks (broker + daemon) / `117-08` `attest_and_decide()` + broker wire contract / `117-09` D-27 channels; 4 = `117-10` + `117-11` gate insertion; 5 = `117-12` forced-unavailable tests + D-32 meta-test + cross-target gates + D-24 latency + SPEC close-out. All 3 requirements (CINT-01/02/03) in plan frontmatter; checker independently counted **all 36 D-01..D-36 cited in `must_haves.truths` in colon form**. **RESEARCH CORRECTED CONTEXT.md IN FOUR PLACES — these changed the plan:** (1) there are **THREE independent `CREATE_SUSPENDED` spawn-and-gate sites**, not one (`exec_strategy_windows/launch.rs`, `nono-shell-broker/src/main.rs`, `agent_daemon/launch.rs` — the daemon module's own doc comment states its non-dependency is deliberate), so D-21 is a pattern re-implemented at three sites, resolved as shared mechanical probes in `crates/nono` + centralized policy in nono-cli + an env-var wire contract to the broker; (2) on the broker arm nono-cli's own suspended window **never sees the real confined child** — it sees `nono-shell-broker.exe` (Medium-IL, unconfined), the AppContainer grandchild is spawned inside the broker's own second suspended window; (3) **WFP may already be effectively confirmed pre-spawn** via `assert_wfp_activation_installed_filters` aborting on `installed_filter_count == 0` before `CREATE_SUSPENDED` fires — this challenges D-18's stated accepted-consequence premise and was resolved in `117-08` rather than quietly promoting unobservable->confirmed; (4) AppContainer is present on `BrokerLaunchNoPty`, absent only on `BrokerLaunch` (PTY), so D-08's 'absent on the broker arm' was imprecise — the correction is itself evidence the per-arm matrix is load-bearing. Pattern-mapping added a fifth: the registry-enum analog is `NetworkAuditDenialCategory` (`crates/nono/src/undo/types.rs:291-377`, `const ALL` + exhaustive drift-guard match), **not** `diagnostic/codes.rs` which CONTEXT.md pointed at and which has no enumeration idiom — this is what makes D-32's meta-test able to ITERATE rows rather than source-scan. **THE CHECKER'S PASS-1 BLOCKER WORTH REMEMBERING:** the AppContainer registry row as first written would have made `117-10`'s gate probe `broker.exe`'s token for a package SID that lives on a grandchild — either spuriously aborting every `BrokerLaunchNoPty` session, or reporting `Confirmed` from a probe of a process that was never meant to carry that state. That is CINT-02's own failure mode, reproduced inside CINT-02's fix. Closed by rescoping expectancy to `EntryPath::Broker`/`Daemon` and adding a dispatch-level guard. **Pass 3 closed the follow-on structural hole:** `required_layers_for_broker()` filters to `ContractOutcome::Abort` rows only, so a future `EntryPath::Broker` row with a non-Abort outcome would be dropped by that filter AND excluded from `attest_and_decide` — going completely unattested. Now pinned by a discovery-based `broker_expected_rows_are_abort_only()` test and recorded in the SPEC as a structural constraint. Also settled in pass 3: the downgrade banner is **not silenceable** (`silent` param removed) but dedups per SESSION not per spawn, so the per-tool-call hook path does not re-print on every invocation; the HMAC-chained telemetry event is deliberately **not** deduplicated (complete forensic history). **KNOWN NON-BLOCKING:** the `check.decision-coverage-plan` gate returned `passed:true skipped:true total:0` — a **vacuous green**, because CONTEXT.md's `- **D-01: ...**` bullet form is not recognized as trackable; real coverage was verified by the checker's own count, not by that gate. `117-VALIDATION.md` frontmatter flipped to `status: planned` / `nyquist_compliant: true` with plan-time sign-off boxes checked and execution-time boxes deliberately left open. **STILL OPEN FROM 116, UNCHANGED:** the Uncovered-Window Finding (12 carve-out-touching commits dispositioned by no audit; proposed UPST13/FUT-08) NEEDS AN OPERATOR CALL, and Phase 120 planning stays operator-gated on ADR-116 ratification. **Next: `/gsd:execute-phase 117`.**  ---- PRIOR SESSION ---- **v3.7 Phase 117 (Fail-Direction Contract + Startup Self-Attestation) CONTEXT GATHERED 2026-08-09** via `/gsd:discuss-phase 117` (default interactive mode). All 8 offered gray areas discussed; 32 decisions captured as D-01..D-36 in `.planning/phases/117-fail-direction-contract-startup-self-attestation/117-CONTEXT.md`, audit trail in `117-DISCUSSION-LOG.md`, committed `308e5fb8`. **NO SDK STATE/ROADMAP WRITER VERB RUN** - `state.record-session` deliberately skipped, STATE hand-edited (sixth consecutive phase using this deviation: 113, 114, 115, 116, 117). Headline locks: a code-resident layer registry in `nono-cli` is the single source of truth with `proj/SPEC-windows-fail-direction-contract.md` generated/drift-checked from it (D-01/D-02/D-03); that registry is the SHARED type Phase 118 receipts consume (D-04); Windows-only with the CINT-01 six as a FLOOR not a ceiling, layer set derived from code (D-09/D-10); four-value outcome vocabulary that **settles ADR-116 Consequence 6** by naming `substitute-equivalent-mechanism` as first-class and requiring the substitute be ATTESTED not asserted (D-05/D-07); expectancy is per-execution-arm so the registry is a matrix, pre-building RCPT-03 (D-08); attestation is supervisor-side only against the real CREATE_SUSPENDED child then resume-or-terminate, observed-where-probeable with a distinct **"established but not independently observable"** fourth state that STILL downgrades (D-17..D-22); all confined-child paths gated incl. the per-tool-call hook, with a measured latency budget (D-23/D-24); per-row default posture tightenable via HKLM machine policy which a CLI flag may tighten but never loosen (D-25/D-26); test seam generalizes the shipped `set_windows_wfp_test_force_ready` toggle but is **compiled out of release, not runtime-gated** (D-29/D-30) with a discovery-based meta-test and loud gaps never silent skips (D-31/D-32). **ACCEPTED CONSEQUENCE STATED UP FRONT:** WFP filter state is unreadable from a non-elevated supervisor, so the WFP row will normally sit unobservable and normally downgrade the claim - D-25 per-row defaults are what keep that meaningful rather than mass-abort or noise. **FLAGGED FOR THE PLANNER:** the existing `set_windows_wfp_test_force_ready` runtime gate now violates D-30 and must either migrate or be recorded as an SC4 discrepancy row. **STILL OPEN FROM 116, UNCHANGED:** the Uncovered-Window Finding (12 carve-out-touching commits dispositioned by no audit; proposed UPST13/FUT-08) NEEDS AN OPERATOR CALL, and D-11 still binds - Phase 120 planning stays operator-gated on ADR-116 ratification. **Next: `/gsd:plan-phase 117`.**  ---- PRIOR SESSION ---- **v3.7 Phase 116 (Tool-Sandbox Divergence Audit + Disposition ADR) EXECUTED + VERIFIED PASSED 2026-08-09 - 6/6 plans, 18/18 must-haves, both requirements (TSBX-01, TSBX-02) satisfied.** Ran sequentially on the main tree (`workflow.use_worktrees=false`), branch `milestone/v2.13-carryforward-closeout`, pre-phase base `6509bcc9`. Waves 1 -> 2 -> 3 -> 4 -> 5. **NO SDK STATE/ROADMAP WRITER VERB WAS RUN AT ANY POINT** - `state.begin-phase` and `phase.complete` deliberately skipped, all six executors plus the verifier explicitly forbidden from touching STATE/ROADMAP/REQUIREMENTS via a CRITICAL_PROJECT_RULE block in every prompt; orchestrator hand-edited STATE/ROADMAP. Fifth consecutive phase using this deviation (113, 114, 115, 116). **D-20 HELD: zero source changes** - `git diff --name-only 6509bcc9..HEAD -- crates/ bindings/ Cargo.toml Cargo.lock` empty, confirmed independently by the verifier; the post-merge build/test gate and the regression gate were therefore SKIPPED WITH STATED REASON rather than run and reported green, and `/gsd:code-review` was skipped by operator choice because it reviews source changes and there were none. **THE VERDICT: formalize fork-native (Pole B)** - `proj/ADR-116-tool-sandbox-disposition.md`, Status: Accepted. Derived by reading the full D-09 table and the D-07 tally (5 implementable-on-fork-primitives / 4 implementable-but-new-work / 2 structurally-blocked, of 11) BEFORE drafting any verdict sentence. 3 of 7 D-09 criteria concretely favored Pole B (enforcement depth, fail-direction under layer failure, ongoing divergence cost); **1 concretely favored Pole A (engine-agnosticism) and is named honestly rather than smoothed** - the fork primitive `nono run --profile` is engine-neutral and Spike-003-validated, but its ENTRY POINT is Claude Code's PreToolUse contract, which is real undone integration work. 2 criteria are an explicit wash (ADR-86 boundary). D-12 supplies 5 named falsifiable reversal triggers plus a UPST13/FUT-08 re-test point; Phase 120 scope is 4 S/M/L-tagged deliverables, proposed only. **THE NON-BIAS CLAIM IS EVIDENCED, NOT ASSERTED:** the verifier confirmed it both by document order and by commit timestamps - 116-05's scoring commits land 18:27-18:28, 116-06's verdict commits 18:35-18:36. **D-01 NO-DELTA: every discussion-time hypothesis figure reproduced EXACTLY on live re-measurement** (38-commit union, 7 pre-fence, 11 post-fence, 12 files / 18,433 LOC / 74% Unix) - stated explicitly rather than silently, as CONTEXT.md required. D-03's 82-module `main.rs` sweep CONFIRMED rather than expanded Phase 108's inherited 3-module set; all 4 borderline candidates got explicit OUT calls with zero static or runtime coupling. **THE FIND THAT JUSTIFIED THE SYMBOL-LEVEL DISCIPLINE:** Plan 116-03 grep-verified all 31 of Phase 108's fenced-window absorb-marked paths against fork HEAD and caught a coincidental NAME COLLISION - `ea334d2b`'s claimed musl-fix symbols (`V9FS_MAGIC` / `fs_type_unsupported`) DO exist in the fork, but as an unrelated, independently-developed WSL2 9P-mount-detection feature. A presence-only grep scores that row `landed`; it is `not-landed`. That is the P112 failure class caught live, in the phase built to catch it. `8a4237f2`'s non-absorption is independently corroborated by a pre-existing Phase 112 test comment naming that exact SHA. **TWO OPERATOR-GATED FINDINGS, PROPOSED AND DELIBERATELY NOT APPLIED** (verifier confirmed zero ROADMAP/REQUIREMENTS diff): (1) **Uncovered-Window Finding - 12 commits touch a named fork-invariant carve-out surface but are dispositioned by NO audit to date**, because 108's ledger is scoped to the fenced window and 116's to the 38-commit tool-sandbox surface, and these fall between the two; 6 pre-fence / 6 post-fence, including `c5c7f56e` (#1538, platform enrollment + audit delivery -> CR-02 audit-bypass invariant), `ebdbe4c9` (#1428, per-route rate limiting -> 3 surfaces) and `9ce74e92` (#1210, seccomp IPC-fd sendmsg deadlock -> `linux.rs` restored invariants); proposed home UPST13/FUT-08, NEEDS AN OPERATOR CALL. (2) Post-Fence Residue Finding - 3 unresolved post-fence items are module-scoped refinements with no independent value, disposed `no new successor phase needed`. **THREE ITEMS ADJUDICATED BY THE VERIFIER, ALL ACCEPTED:** (a) 116-06 wrote a 4-deliverable ADR-111/113-style closing-boundary Phase 120 scope instead of the frontmatter must-have's row-by-row matrix sizing - acceptable, **the plan's own task action text authorized that branch for a Pole-B verdict and only the frontmatter wording lagged; a plan-authoring gap, not an executor defect**; (b) 116-01 landed 2 tasks in 1 commit (`5939cd9`) - acceptable, both acceptance-criteria grep sets reproduce against the combined commit; (c) orchestrator commit `7a031d7d` consolidated a duplicate `## References` heading (116-05 closed its half with one, 116-06 then appended Decision/Scope/Consequences after it, orphaning it mid-document) - clean, no citation lost, no other section moved. **UNRESOLVED CROSS-PHASE ITEM NAMED IN CONSEQUENCES:** a wording gap between this ADR's fail-direction finding and Phase 117's CINT-01 abort/downgrade framing, left for 117 to resolve rather than papered over. **NON-BLOCKING HYGIENE:** `116-VALIDATION.md` frontmatter still reads `status: draft` / `nyquist_compliant: false` - never flipped post-execution, though the evidence it protects independently checks out. **Next: `/gsd:discuss-phase 117`** - and D-11 still binds: Phase 120 planning stays operator-gated on ADR-116 ratification.

PRIOR (2026-08-09): **v3.7 Phase 115 (v3.6 Carry-Forward Drain) EXECUTED + CODE-REVIEWED + GAP-CLOSED + VERIFIED PASSED 2026-08-09 - 6/6 plans, all 6 requirements (DRAIN-01..06) Complete, verification 5/5.** Ran sequentially on the main tree (`workflow.use_worktrees=false`, required - Plans 115-05/06 reach `../nono-py` via `..`), branch `milestone/v2.13-carryforward-closeout`, pre-phase base `10a9b0e2`. Waves 1 -> 2 -> 3. **NO SDK STATE/ROADMAP WRITER VERB WAS RUN AT ANY POINT** - `state.begin-phase` deliberately skipped, all six executors plus the review-fixer and gap-closer explicitly forbidden from touching STATE.md/ROADMAP.md via a CRITICAL_PROJECT_OVERRIDE block in every prompt; orchestrator hand-edited STATE/ROADMAP/REQUIREMENTS. Third consecutive phase using this deviation (113, 114, 115). **WHAT SHIPPED:** `CustomCredentialDef.inject_mode`/`inject_header` are now `Option<T>` merged `.or(base)` with a `HookConfig` compile-time exhaustive-destructure guard (DRAIN-01, ACC-04 closed); `audit::log_denied` takes a REQUIRED denial category across 28 call sites with `InterceptHandshakeFailed` removed and a self-enumerating `ALL` const added (DRAIN-02/03); `aws_auth` and plain OAuth2 `client_credentials` are rejected at profile-validation time (DRAIN-04); both SPIFFE dispatch paths deny before minting (DRAIN-06); `../nono-py`'s denial-category codec is serde-driven with a round-trip test driven off `ALL`, and its `RouteConfig::new` exposes `spiffe`/`capture`/`endpoint_policy` via 8 new PyO3 wrapper types (DRAIN-02/05). **THE TWO DEFECTS THAT MATTERED WERE BOTH FOUND AFTER EVERY EXECUTOR SELF-CHECK PASSED - the fourth consecutive phase where that is true.** (1) `/gsd:code-review` (deep, 17 files, 2 repos) returned **1 BLOCKER + 6 Warning + 8 Info**. CR-01: Plan 115-06 opened `../nono-py`'s `SpiffeAuthConfig::new` as an infallible constructor taking `inject_header`/`credential_format` verbatim, while the CLI path validates the identical fields (`validate_header_name`, NET-02/Phase 113) and `reverse.rs` writes them with a raw `format!` into `\r\n`-delimited request bytes - so `inject_header` carrying a CRLF from a Python embedder smuggles an attacker-chosen header into a request bearing a live JWT-SVID. WR-03 was its sibling (`CaptureConfig` bypassed all four `validate_capture_config` gates including the 1 MiB D-05 ceiling, consumed unclamped at `reverse.rs:1913`). **The phase opened this surface in the act of closing DRAIN-05 - it is the phase's own defect class, two front-ends to one enforcement engine with divergent validation, reintroduced one layer out.** Operator chose the ENFORCEMENT-BOUNDARY fix over the review's primary suggestion: `SpiffeAuthConfig::validate`/`CaptureConfig::validate`/`RouteConfig::validate` now live in `nono-proxy/src/config.rs` and run from `RouteStore::load`, verified reachable from BOTH the CLI start path (`proxy_runtime.rs:604`) and the embedder path (`nono-py start_proxy` -> `nono_proxy::start`), which converge at `server.rs:601`; the CLI copies stay as defense-in-depth. The fixer correctly WIDENED scope by one field on its own initiative - `RouteConfig`'s own `inject_header`/`credential_format` feed the identical raw write for static-credential routes, so validating only `spiffe` would have made the unrepresentability claim false one field to the left. (2) The verifier then returned **gaps_found 4/5** on a defect neither the research, the plans, nor the code review had looked at: `reverse.rs:210-216` documents **TWO** independent SPIFFE-backed auth sources, and Plan 115-04 hoisted the host check on only one. `handle_spiffe_assertion_credential` (RFC 7523 jwt-bearer, reachable from a shipping profile since D-14 deliberately keeps `client_assertion` valid) still called `cache.get_or_refresh()` -> `exchange_jwt_assertion()` -> `fetch_token()` - minting an SVID AND presenting it to an external IdP - before `check_host`. No leak; the same ordering inversion DRAIN-06 names. **Two in-source comments were made false by the partial fix** (`:872` 'Mirrors handle_spiffe_route's structure exactly'; the new D-17 comment 'matching the check-first structure every other HostDenied site in this file already uses'), and the structural regression test scanned only `handle_spiffe_route`'s body so it was blind by construction. Operator chose fix-inline over a gap-closure replan. **THE DURABLE LESSON, one level up from Phase 114's CR-03:** verifying a fix's PLACEMENT is not verifying its COVERAGE ACROSS EVERY PATH IN THE CLASS. The closure replaced the name-scoped test with one that is **self-enumerating by behaviour** - it enumerates every top-level `async fn` in `reverse.rs`, classifies a fn as minting if it contains `managed_auth.acquire(`/`.get_or_refresh(`/`.fetch_token(`/`exchange_jwt_assertion(`, and asserts check-before-mint for each, panicking (not skipping) on a minting fn with no check, with `KNOWN_MINTING_DISPATCH_FNS` asserted a SUBSET of what discovery found so a renamed mint primitive surfaces as coverage loss rather than a vacuous green. A future third SPIFFE path is covered with no test edit. **RE-VERIFICATION PASSED 5/5, and the verifier did not take the self-report:** it re-implemented the guard test's algorithm in a scratch harness and mutation-tested copies of `reverse.rs` - 11 mutations, 11 detected, 0 false passes - independently reproducing the executor's reported byte offsets (2628/1657, 3255/2358), which corroborates the load-bearing reverts were real rather than narrated. It also confirmed the no-narrowing claim from the diff rather than the line count (deny block has zero +/- lines) and found the ordering STRICTLY WIDENED: previously a failing exchange returned 503 before the check ran, so a denied-host request with a failing exchange produced no `HostDenied` record at all. **ORCHESTRATOR-VERIFIED GATES (not executor self-report):** `cargo build --workspace --all-targets` exit 0; `cargo fmt --all -- --check` clean; `cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used` clean; `nono-sandbox-proxy` 307 lib + 6 integration, 0 failed; `nono-sandbox --lib` 826/826 **single-threaded**; `../nono-py` `cargo test` 77 passed + `maturin build` produced a real wheel (the 6th consecutive phase where the wheel build is the only honest binding gate). **REGRESSION GATE PASSED BY IDENTITY:** `nono-sandbox-cli --bin nono` = 1537 passed / 11 failed, and the 11 names are an EXACT match to the documented pre-existing Windows baseline - zero new. **TWO HOST FACTS WORTH CARRYING:** (a) `nono-sandbox --lib`'s `supervisor::aipc_sdk::tests::windows_loopback_tests::helper_stamps_session_token_from_env` fails ONLY under parallel run (an unrestored `NONO_SESSION_TOKEN` race - the exact hazard CLAUDE.md documents) and passes 826/826 with `--test-threads=1`; (b) **`../nono-py`'s pytest suite cannot collect at all on this Windows host** - 4 test modules import the Unix-only `sandboxed_exec` at module scope (conditionally exported at `python/nono_py/__init__.py:232`, Unix-only per `:78`), byte-identical at the phase base, so `cargo test` + `maturin build` are the real binding gates, not pytest. **OPEN BY OPERATOR DECISION (scoped out, NOT gaps - all re-confirmed present at HEAD):** WR-01 (the `ALL` const's drift guard is weaker than its own doc comment claims - adding a variant plus its guard arm leaves `ALL` at 10 and the hardcoded length assertion still passes, so D-11's recorded 'identical guarantee to strum' rationale is FALSE; SC2 still verified because encode/decode are now the same serde derive on the same type, making vocabulary drift unrepresentable for unit variants regardless of `ALL`), WR-02 (~55 lines of unreachable validation behind a tautological `client_assertion.is_none()`), WR-04 (`_nono_py.pyi` declares two nonexistent `RouteConfig` params, putting the three new ones at wrong positional indices), WR-05 (`PartialEq` semantics shift makes `nono profile diff` emit a false 'changed'), WR-06 (5 deny branches with zero audit record - three 413s, the AWS 501, and `external.rs:125`), IN-01..08, plus verifier findings V-02 (nono-py D-16 allowlist test's future-proofing claim over-stated), V-03 (115-VALIDATION.md's cross-target exemption PREMISE is false for the final file set - `proxy_runtime.rs` has 15 linux cfg gates - though the substantive risk is nil and no changed line is in a Unix-cfg branch; re-word, do not re-litigate) and V-04 (`strip_line_comments` handles whole-line `//` only, so guard tokens in trailing/block comments or string literals could bypass - swept, all 6 production occurrences are real code). **V-02, V-04 and WR-01 share one shape and should be read together before Phase 117/118 builds on them: each is a guard whose in-source documentation claims stronger future-proofing than the mechanism delivers.** **PHASE 116 CONTEXT GATHERED 2026-08-09** (`/gsd:discuss-phase 116`, commit `0ea1db66`; hand-edited STATE - no SDK state writer run). 20 locked decisions across 4 discussed areas. **THE FOUR THAT CHANGE DOWNSTREAM WORK:** (1) **D-01 window is `v0.64.1..v0.71.0`, NOT Phase 108's fenced `v0.66.0..v0.69.0`** - PR #1105 lands at v0.65.0 BEFORE the fence, so a fence-only ledger would not contain its own subject; 38 surface commits measured (7 pre-fence + 108's 20 + 11 post-fence), tip pinned at tag `v0.71.0` (`0055bf3c`) not `upstream/main` (`4ede9ccc`, 2026-08-05, recorded as a named boundary). All 38 get full dispositions - FUT-08 constrains absorb work, not knowledge. (2) **D-10 the verdict is GENUINELY OPEN and plans must not be written toward either pole** - a deliberate departure from Phase 108's D-11/D-12 "recommendation to argue for", because this phase exists precisely because the fork path was assumed inferior without examination. Executor lands a decided `Status: Accepted` ADR, but **Phase 120 planning is operator-gated on ratification (D-11)**. (3) **The decisive measured fact: 74% of upstream's subsystem is Unix platform drivers (`linux.rs` 6,586 + `macos.rs` 7,041 of 18,433 LOC) and there is NO `platform/windows.rs` upstream at all** (+ `command_policy.rs` 5,955). "Adopt" on the platform this milestone cares about means writing the subsystem's largest component from scratch - the ADR is a two-pole comparison (adopt+new-Windows-driver vs. formalize-fork-native), with Unix-only-adopt and policy-model-only-adopt recorded as considered-and-rejected. (4) **D-03 the module set is RE-DERIVED from `mod`/`use` edges at v0.71.0 and diffed against Phase 108's inherited 3** - D-06 was itself a correction (directory-only 18 vs. union 20, NOT equal) that warned in writing that the next sync must re-measure; this is that phase. Also locked: capability feasibility matrix marks minifilter-dependent rows `structurally-blocked` citing ADR-65 (feeds Phase 119's BOUND-02); both poles scored on ONE fixed criteria set incl. engine-agnosticism (the fork's honest weak spot - its entry point is Claude Code's PreToolUse contract); evidence is an inline literal-command + hit-count column whose greps DISCOVER their targets rather than confirming pre-named paths (the P112 + P115-V-01 lesson); deliverables are a NEW `116-DIVERGENCE-LEDGER.md` (108's closed ledger is not edited) + `proj/ADR-116-tool-sandbox-disposition.md` carrying a Phase 120 scope section and a PROPOSED, not applied, ROADMAP amendment. **D-20: this phase writes zero code** - no `.rs`, no `Cargo.toml`, `ROADMAP.md` untouched; `make ci` is not its gate. **PHASE 116 PLANNED 2026-08-09** (`/gsd:plan-phase 116`; hand-edited STATE - no SDK state writer run, fourth consecutive phase using this deviation). **6 plans in 5 waves**, both requirements claimed (TSBX-01 -> 01/03/04, TSBX-02 -> 02/05/06), all 20 locked decisions D-01..D-20 cited by at least one plan (verified by hand - the `check.decision-coverage-plan` verb returned the known vacuous `total:0` green on this repo's `**D-NN: text**` bullet form). Deliverables: `116-DIVERGENCE-LEDGER.md` (01 -> 03 -> 04), `116-FEASIBILITY-MATRIX.md` (02, a separate appendix the ADR cites - D-07's matrix was placed outside the ADR under Claude's Discretion), `proj/ADR-116-tool-sandbox-disposition.md` (05 -> 06). **SCOPED RESEARCH, not the standard run:** the researcher was fenced OUT of computing any commit count, module-set verdict, disposition or capability rating, because on an audit phase those ARE the deliverable and pre-computing them recreates the copy-forward failure D-01/D-03 exist to correct; it produced house-format extraction, live-verified git commands, and both symbol inventories only. It did incidentally reproduce D-01's hypothesis table exactly (38 commits / 12 files / 18,433 LOC / 74% Unix) - **this does NOT convert the hypothesis to fact; Plan 01 still re-measures and must state the no-delta case explicitly rather than silently.** **THREE RESEARCH FINDINGS THAT CHANGED THE PLANS:** (1) the four D-03 borderline candidates have ZERO static `mod`/`use` cross-reference to the tool-sandbox set and their wiring lives in `crates/nono-cli/src/main.rs`, so CONTEXT.md's literal "follow mod/use edges out of `tool-sandbox/mod.rs`" finds NOTHING without a second grep pass against `main.rs` - Plan 01 now carries that pass and discovers its module set rather than confirming the 4 pre-named ones; (2) `exec_strategy_windows/supervisor.rs` (241 KB, load-bearing) returns ZERO hits on a `pub|pub(crate)` grep because its entire API is `pub(super)` - a zero-hit is suspicious, not conclusive, and Plan 02's inventory greps are broadened accordingly; (3) the D-19 substring-glob hazard REPRODUCES in this window - `'*tool-sandbox*'` inflates the surface 35 -> 37 via `docs/cli/features/tool-sandbox.mdx` (commits `5a7447d3`, `ebd51cbb`). **PLANNER DIED MID-WRITE on an API connection error after 167k tokens of reading with zero plans on disk; recovered by resuming the same agent from transcript and writing ONE plan per turn** - context reuse plus chunking, cheaper than a re-spawn and immune to the same long-response failure. **PLAN-CHECKER RETURNED 3 BLOCKERS, all real, two of them sequencing defects the orchestrator's own file-collision check had missed because it checked WRITE collisions and not READ-before-WRITE:** 116-05 (then wave 2) read a `## Headline` section only 116-04 (wave 3) writes - guaranteed failure, not a race - and 116-06 read 116-04's ledger closing line with no dependency edge. Fixed by SERIALIZING the ADR chain behind full ledger closure (05 -> wave 4 deps [01,02,04]; 06 -> wave 5 deps [05,04]) rather than re-sourcing the figures from Plan 01, because 116-04 is where D-01's reconciliation is FINALIZED and citing pre-reconciliation numbers would ship a stale ADR beside a corrected ledger. Third blocker: `116-FEASIBILITY-MATRIX.md` carried no date anywhere, failing D-16's command+hit-count+DATE rule for the document holding most of TSBX-02's ratings. Re-verification PASSED, no new issues. **Gates skipped with stated reason:** the UI-SPEC gate false-positived on `reviewer` (substring `view`) in SC2 and the pattern-mapper was redundant against RESEARCH.md's house-format section. **Next: `/gsd:execute-phase 116`** - and note D-11: Phase 120 planning stays operator-gated on ADR ratification even after 116 lands.

PRIOR (2026-08-08): **v3.7 Phase 115 (v3.6 Carry-Forward Drain) PLANNED 2026-08-08 — 6 plans across 3 waves, READY TO EXECUTE.** `gsd-plan-checker` VERIFICATION PASSED at iteration 2 with 0 blockers. Waves: 1 = `115-01` DRAIN-01 (`CustomCredentialDef` inject fields -> `Option<T>`, exhaustive `.or(base)` merge test, `HookConfig` compile guard) + `115-02` DRAIN-02/03 (`log_denied` required-category signature across 28 call sites, `HostDenied`/`ExternalProxyRejected` wiring, `InterceptHandshakeFailed` removal) in parallel; 2 = `115-03` DRAIN-04 (reject `aws_auth` + plain OAuth2 `client_credentials` at validation), `115-04` DRAIN-06 (hoist `deny_domain` above `managed_auth.acquire()`), `115-05` DRAIN-02 (`../nono-py` codec -> serde-driven); 3 = `115-06` DRAIN-05 (`../nono-py` `RouteConfig` exposes `spiffe`/`capture`/`endpoint_policy` + D-16 allowlist test). All 6 requirements (DRAIN-01..06) and all 17 locked decisions (D-01..D-17) traced to plans BY HAND — `check.decision-coverage-plan` returned a **vacuous green** (`total: 0`, 'no trackable decisions') because CONTEXT.md uses the `- **D-01: ...**` bullet form the handler does not match. **Research findings that CHANGED the plan vs. CONTEXT.md's assumptions:** (1) **CONTRADICTION** — CONTEXT.md D-01 claimed `../nono-py`'s `RouteConfig::new` intersects the `CustomCredentialDef` shape change; `../nono-py` has **zero** references to that type, so DRAIN-01 has no nono-py footprint at all and the sequencing edge was dropped. (2) D-09's ledger precondition is **DISCHARGED** — zero production constructors of `InterceptHandshakeFailed` and zero occurrences of `intercept_handshake_failed` in any ledger, fixture or golden repo-wide, so no unknown-variant decode route and no investigation task were planned. (3) D-14's evidence is **DISCHARGED, verdict UNWIRED** — plain OAuth2 `client_credentials` routes enter none of `CredentialStore`'s three maps and reach upstream with **zero token injection** (a fail-open, worse than the 501 CONTEXT.md assumed); the rejection was planned directly. (4) `endpoint_policy` confirmed **live** (enforced by exhaustive match in `reverse.rs`, 3 dedicated tests) so D-15 exposes it. (5) **Cross-target clippy gates are NOT required this phase** — no in-scope file is cfg-gated Unix code, under `exec_strategy/`, or under `bindings/c/src/`; the exemption is evidence-based and recorded in `115-VALIDATION.md`, NOT a PARTIAL->CI fallback. (6) D-11 decided **against** `strum` (absent from the workspace) in favour of a zero-new-dependency `pub(crate) const ALL` paired with a private exhaustive-`match` guard. (7) DRAIN-06's test mechanism locked to the **structural** option (deny returns before `managed_auth.acquire()` is reachable), avoiding a SPIRE-agent-gated test. **The plan-checker's one blocker was real and live-verified:** D-03's exhaustive merge test as first written could never run — `validate_custom_credential` enforces pre-existing Phase-22/Phase-113 mutual-exclusion rules among `credential_key`/`auth`/`aws_auth`/`spiffe`, so a base fixture holding all four `Some(..)` fails validation before any inheritance assertion; split into Test A (through `finalize_profile`, one exclusive field) + Test B (direct `merge_custom_credential_def`, bypassing validation, all 14 non-`upstream` fields at once). **Next: `/gsd:execute-phase 115`.** `/gsd:code-review` is MANDATORY before this phase closes — executor self-check is not security evidence.

PRIOR (2026-08-08): **v3.7 OPENED 2026-08-08 — Composite Integrity + Tool-Sandbox Disposition. Phases 115+ (continuing from 114; no reset). Status: defining requirements.** Three workstreams, chosen by the operator from three standing candidates: (1) **Composite Integrity** — the trustworthiness slice of `.planning/NONO_WINDOWS_DESIGN_REVIEW_FUTURE_PHASE_v0.1.md`: a system-wide fail-direction contract covering every layer (WFP absent / token unrestricted / integrity label unapplied / AppContainer profile missing / minifilter unloaded) plus startup self-attestation that refuses to report 'enforcing' unless each expected layer confirms active (G-WIN-1); per-session content-free enforcement receipts recording which layers actually held (G-WIN-5); an explicit boundary statement that nono governs destination and credential, NOT payload (G-WIN-2); and a written ruled-in/ruled-out decision log for the six §4 state-of-the-art techniques (Windows Sandbox/HCS, PPL for protecting nono's own supervisor, WFP ALE layers beyond connect-time, ETW escape detection, AppContainer capability grants, WDAC exec gating). (2) **Tool-Sandbox Disposition** — an ADR-gated verdict on upstream's never-absorbed `tool-sandbox/` subsystem (PR #1105, v0.65.0) and the 7 refinement PRs Phase 108 fenced (#1280/#1322/#1325/#1384/#1394/#1413/#1417), weighed against the fork's own independently-built answer (PR #4: PreToolUse hook -> `nono run`, Low-IL primary-token broker). **The operator chose 'genuinely open' — the ADR is a gate and downstream scope is CONDITIONAL on its verdict**, so roadmap phases past the ADR are provisional by design, not by omission. (3) **v3.6 carry-forward drain** — NEW-01/03/05/06/07/08 from `milestones/v3.6-MILESTONE-AUDIT.md`. **THREE OPERATOR DECISIONS TAKEN AT MILESTONE OPEN:** **(a) ADR-65 STANDS** — the v2.10 production-minifilter verdict (No-go/Conditional-go) is NOT overturned, so design-review items G-WIN-3 (minifilter Phase 64 to production) and G-WIN-4 (driver lifecycle/fleet story) are explicitly OUT of v3.7. The load-bearing consequence, which is itself a deliverable and not a footnote: integrity levels and DACLs cannot express 'read `C:\\project\\src` but not `C:\\project\\.env`' — same directory, no integrity distinction — therefore **1.0's filesystem guarantee must be stated in terms of the shipped mechanism and per-file read policy is explicitly NOT claimed.** **(b) NEW-05 is a DEFECT, not presentation** — `merge_custom_credential_def` (`crates/nono-cli/src/profile/mod.rs:3564-3567`) takes `upstream`/`inject_mode`/`inject_header` from the child unconditionally; `inject_mode` is `#[serde(default)]` and `inject_header` is `#[serde(default = 'default_inject_header')]`, so a `platform_overrides.<os>` block that redefines a credential to change `upstream` and omits both **silently resets a `url_path`/`query_param` route to header-mode `Authorization` on that OS**. The operator rejected the fix's own doc-comment justification ('presentation fields that remove no security control') — where a credential is placed on the wire is not presentation, and this is the second removal-by-silence in the same function that NEW-02 was about. Fix with `.or(base)` and close ACC-04 (NEW-02's regression test sets `spiffe: None` on both sides and asserts only `capture`, so the `spiffe` arm at `:3577` is correct in code but has no assertion). **(c) MILESTONE-MARKER ONLY** — no crate leapfrog (tree stays `0.70.0`), no publish, tag `v3.7` local; this deliberately keeps v3.5's paused go-live as the single release decision point rather than stacking a second version bump behind it. UPST13 was offered as a fourth workstream and DECLINED — it is the leading candidate for the milestone after v3.7. **PARALLEL-MILESTONE HAZARD (unchanged and still live):** v3.5 owns 101-107 and remains open/paused at Phase 104; `phases.clear` MUST NOT run; `REQUIREMENTS.md` and `ROADMAP.md` carry BOTH milestones in separate sections so v3.7 content must be APPENDED, never written wholesale (the stock archive workflow's `git rm REQUIREMENTS.md` + ROADMAP overwrite was caught and worked around at the v3.6 close, commit `1fd559bf`); SDK STATE writers stay banned (`state.begin-phase` previously deleted the entire `parallel_milestone` v3.5 block; `state.advance-plan` previously deleted ~4,700 chars of this very field). No formal research — all three workstreams are internal (fork code, upstream diff, an already-written design review). **Phase 115 CONTEXT GATHERED 2026-08-08** (`115-CONTEXT.md`, commit `45a42fc9`) — 4 gray areas discussed, 17 decisions (D-01..D-17) locked. Scouting corrected two requirement assumptions the planner must start from: (a) **DRAIN-01's stated `.or(base)` fix is not directly expressible** — `inject_mode`/`inject_header` are non-`Option` serde-defaulted fields (`profile/mod.rs:963-972`), so 'omitted' and 'explicitly default' are the same value; D-01 changes both to `Option<T>` resolved at the use site, which ripples to the schema and `../nono-py`'s constructor. (b) **DRAIN-02 has a stronger fix than the requirement asks for** — `NetworkAuditDenialCategory` already derives `Serialize/Deserialize` + `rename_all = \"snake_case\"` (`undo/types.rs:242-244`), so D-10 deletes BOTH hand-written nono-py matches rather than adding two arms plus a test. Every decision resolved the same direction — **make the defect unrepresentable, not tested-for**: D-06 makes the denial category a required `log_denied` argument (28 call sites); D-04 guards `HookConfig` at compile time; D-09 REMOVES `InterceptHandshakeFailed` (permanently unconstructable under ADR-113 D-01) gated on a persisted-ledger decode check; D-11 derives `strum::EnumIter` on the core enum; D-16 pins the Python constructor with a no-unconditional-`None` allowlist test. Class breadth widened twice on evidence: **D-13 rejects ANY `aws_auth` route at validation** (SigV4 501s on every path; verified live that no shipped profile or fixture declares it — schema descriptions and `network_policy.rs` test constructors only, so no profile fallout), and **D-14 investigates plain OAuth2 `client_credentials`** (`credential.rs:296-300` says only the SPIFFE jwt-bearer arm is wired) and rejects it on the same rule if confirmed. **Forces `workflow.use_worktrees=false`** (D-12/D-15/D-16 reach `../nono-py` via `..`). `/gsd:code-review` is mandatory for this phase. **Next: `/gsd:plan-phase 115`.**

PRIOR (2026-08-07): **v3.6 Phase 114 EXECUTED + CODE-REVIEWED + CRITICALS FIXED 2026-08-07 — 11/11 plans, SEC-02 RESOLVED (ADAPTED-with-scope-limit).** Ran sequentially on the main tree (`workflow.use_worktrees=false`, required — 114-10 reaches `../nono-py`/`../nono-ts` via `..`), branch `milestone/v2.13-carryforward-closeout`, pre-phase base `b29211aa`. Waves 1→6. **WHAT SHIPPED:** a fork-native response buffer-and-rewrite enforcement point (`relay_response_with_capture()`, `nono-proxy/src/reverse.rs`) fail-closed at all 8 non-success branches, wired into all THREE relay sites; a zeroized in-memory session-scoped `CapturePhantomStore` (`capture.rs`, D-08 — upstream's `oauth_capture/persist.rs` NOT adopted); the mint→resolve loop closed with a live production chain (`handle_reverse_proxy` → `resolve_capture_request_body` → `resolve_request_nonce_fields` → `store.resolve`); a D-06 cross-path guard; the strict fork-only schema (D-13); ADR-114 + ledger carry-forward naming `149abde0`. **THE CENTRAL LESSON — THE SELF-CHECK/AUDIT GAP FIRED TWICE, AND BOTH TIMES SOMETHING OTHER THAN THE EXECUTOR CAUGHT IT.** (1) Plan 114-05's executor reported Self-Check PASSED while a route declaring BOTH `spiffe` and `capture` still relayed the REAL token unrewritten — dispatch on `has_spiffe_source()` (`reverse.rs:222`) returns BEFORE the capture branch (`:486`), and neither SPIFFE handler contained any `capture` reference. Only the orchestrator's hand control-flow audit found it; filed as Finding 3 in `114-05-SUMMARY.md`, closed by 114-06. (2) The `/gsd:code-review` gate then found **3 Criticals** across work that had already passed every executor self-check AND both cross-target gates: **CR-01** `decode_chunked_body` interpolated raw response bytes into an error (`\"malformed chunk size '{size_hex}'\"`) that flowed `deny()` → `warn!` → `audit::log_denied` → `NetworkAuditEvent.reason` → **session metadata on disk**, so a hostile/misframed upstream could write a REAL token to the audit ledger — falsifying D-08 and ADR-114, and INVISIBLE to `capture.rs`'s own self-enforcing source-scan test because the write happens in `reverse.rs`/`audit.rs`; **CR-02** the capture path terminated only on upstream EOF (no Content-Length/chunked framing, client `Connection:` forwarded), so against any keep-alive HTTP/1.1 token endpoint — i.e. every real provider — every capture request stalled to timeout then 502'd, with all six relay tests masking it by explicitly closing the upstream write half; **CR-03** the CONNECT guard evaluated `capture_declared_for_upstream()` INSIDE the exact-`host:port` `is_route_upstream()` gate, so D-06's host-only widening was DEAD there and only relabelled the audit category — `CONNECT capture-host:8443` against a route declaring `:9443` tunnelled through and the agent got the REAL token. **CR-03 was an orchestrator verification miss, recorded honestly:** when verifying 114-07 the orchestrator checked that the CONNECT block was not *conditioned* on anything else, but not that its *predicate* was wide enough — and CONNECT is the bypass that matters, since a token endpoint is `https` and the forward-HTTP guard that does apply the wide predicate cannot reach an https upstream at all. All 3 fixed on operator decision (`8b35a220`/`8d98b23c`/`02a469ce`): CR-01 structurally, by retyping the `deny` closure to `&'static str` so runtime-derived content is type-impossible to pass (plus redaction in `reject_unrewritten_token_fields`, whose JSON path is built from upstream-controlled object KEYS); CR-02 via RFC 9112 §6.3 framing-based termination + hop-by-hop header stripping + `Connection: close` on capture routes; CR-03 by hoisting all three predicates above the gate and making the gate their OR (nothing narrowed — `is_route_upstream` still denies every exact route upstream, so the old deny surface is a strict subset). The false in-source comment and ADR-114's CONNECT row were both corrected. Each fix carries a regression test **verified load-bearing** (reverting the fix makes exactly that test fail), including the module's only test that deliberately does NOT close the upstream write half. **9 Warning + 6 Info remain OPEN by operator scoping** (`114-REVIEW.md`) — notably the \"always held in Zeroizing\" claim being false at 5 points on the real data path, `reject_unrewritten_token_fields`'s `matches!` letting `accessToken` through, unbounded phantom-store growth, unvalidated shared-upstream capture config, and `nono-py`'s decoder raising `ValueError` on the two denial-category strings its own encoder emits. **D-14 CONFIRMED A SIXTH TIME:** 114-10 predicted 2 files/1 field each in `../nono-py`; the compiler reported **4 errors across 3 files** including an E0004 non-exhaustive match — `maturin build` produced a real wheel, `../nono-ts` immunity RE-CONFIRMED by running `napi build`, not assumed (`../nono-py` `21ab6c2`). Known residual: `capture_context` has no encoder in `audit_event_to_py_dict`, mirroring Phase 113's `spiffe_context` gap. **ORCHESTRATOR-VERIFIED GATES (not executor self-report):** both cross-target clippy gates GREEN locally, re-run AFTER the Critical fixes (linux-gnu `cross clippy` exit 0; apple-darwin `cargo-zigbuild` exit 0, `SDKROOT` unset) — no PARTIAL→CI; `cargo build --workspace --all-targets` exit 0; `cargo fmt --all --check` clean; `nono-sandbox-proxy` 300 lib + 5 integration, 0 failed; `nono-sandbox --lib` 825/825; CLI `profile::` 324, `network_policy::` 48, `proxy_runtime::` 31, all 0 failed. Test baseline is SCOPED, not a full sweep (the 36-binary CLI sweep stalls ~25 min) — stated honestly per the Phase 112 SC4 precedent. **No SDK STATE/ROADMAP writer verb was run at any point this phase** — `state.begin-phase` deliberately skipped, executors forbidden from touching either file, orchestrator hand-edited both. **Next: `/gsd:verify-work 114` or milestone close-out — v3.6's last phase is done.**

PRIOR (2026-08-07): **v3.6 Phase 114 Wave 1 COMPLETE — 2/11 plans (114-01, 114-02).** Running sequentially on the main tree (`workflow.use_worktrees=false`, required — Plan 114-10 reaches `../nono-py`/`../nono-ts` via `..`), branch `milestone/v2.13-carryforward-closeout`, pre-phase base `b29211aa`. **Plan 114-01** (`3345468c` feat / `273db64f` feat / `3fe78d29` fix / `fc3e935a`+`19ba0ce3` docs) added `CaptureAuditContext` + `NetworkAuditDenialCategory::CaptureUnsupportedPath` at `crates/nono/src/undo/types.rs:263` and threaded `EventContext.capture_context` / `NetworkAuditEvent.capture_context` through all 4 `push_event` sites in `nono-proxy/src/audit.rs`. **Plan 114-02** (`e2754cf1` feat / `c8007112` fix / `01ee45f4`+`a8cdc2e8` docs) added `CaptureConfig`/`CaptureResponseField`/`CaptureResponseFieldKind` (`nono-proxy/src/config.rs`, ~lines 637/692/704/719) and `RouteConfig.capture: Option<CaptureConfig>`. **THE EXECUTION-LEVEL FINDING SO FAR — the D-14 exhaustive-struct-literal class bit TWICE in Wave 1 alone, and both times the plan's own `files_modified` under-predicted it:** 114-01's `NetworkAuditEvent` has no `Default`, so `capture_context` forced `None,` additions at 5 literal sites *outside* the plan's declared list (`nono/src/audit.rs` x2, `nono-cli/src/audit_integrity.rs`, `exec_strategy/supervisor_linux.rs`, `proxy_command.rs`); 114-02's `RouteConfig.capture` forced **36** sites, not the 3 the plan named — one extra production literal (`network_policy.rs::partition_allow_domain`) plus 33 test literals across `credential.rs` x7, `reverse.rs` x1, `route.rs` x11, `server.rs` x14, `tests/spiffe_integration.rs` x1. **In BOTH cases neither task's own narrower verify command caught it — only `cargo build --workspace --all-targets` did.** This is direct, same-phase empirical confirmation that Plan 114-10's binding rebuild must be driven by real compiler output, never by prediction (D-14's stated rationale, now evidenced twice more). Note also that 114-02 already edited 11 `RouteConfig` literals in `route.rs`, which is Plan 114-03's own target file — later plans MUST re-read their files rather than trusting plan-time line numbers. **Orchestrator-verified post-wave gate (not executor self-report):** `cargo build --workspace --all-targets` exit 0, `cargo test -p nono-sandbox-proxy --lib` 245/245 passed, `cargo fmt --all -- --check` clean; both SUMMARYs committed, zero `Self-Check: FAILED` markers, working tree clean. **DELIBERATE WORKFLOW DEVIATION:** no SDK STATE/ROADMAP writer verb was run at any point — `state.begin-phase` was skipped and executors were explicitly forbidden from touching STATE.md/ROADMAP.md; the orchestrator hand-edits both (this repo has a documented history of `state.advance-plan` deleting ~4,700 chars of this field and `state.begin-phase` deleting an entire milestone block). One transient API error killed Plan 114-01's executor mid-Task-2; it was resumed from its own transcript via SendMessage after confirming Task 1's commit had landed and the tree was clean — no duplicate work. **Next: Wave 2 — `114-03` (route.rs capture predicate, D-06 host-only matching), `114-04` (capture.rs phantom store), `114-08` (CLI credential_provider.rs surface), sequentially.**

PRIOR (2026-08-06): **v3.6 Phase 114 PLANNED 2026-08-06 — 11 plans across 6 waves, READY TO EXECUTE.** Research (`114-RESEARCH.md`, 814 lines, HIGH confidence) + pattern map (13/13 analogs) + `114-VALIDATION.md` (`nyquist_compliant: true`, 16 required behaviors) + 11 plans + `gsd-plan-checker` **iteration 2 PASSED**. **THE ITERATION-1 BLOCKER IS THIS RECORD'S MOST CONSEQUENTIAL ITEM:** the first plan set built the inbound response buffer-and-rewrite enforcement point correctly, but left `CapturePhantomStore::resolve()` with **ZERO production call sites** — phantoms minted and handed to the sandboxed client could never be redeemed, so an agent completing an OAuth login would receive a token it could never use, and `CaptureConfig.request_nonce_fields` was documented as \"reserved / unused by this phase.\" The plans **described** a closed loop while leaving it open — functionally the exact shape the phase goal forbids verbatim (\"no reduced-scope half-feature ships\"). Operator ruled **close the loop**, rejecting the defer-and-document alternative. Revision wired `resolve_capture_request_body()` into `handle_reverse_proxy`'s real request-body path (Plan 114-06 Task 3), making `handle_reverse_proxy` → `resolve_capture_request_body` → `capture::resolve_request_nonce_fields` → `CapturePhantomStore::resolve` live production code. **Also corrected:** ADR-114's draft described the gap as \"egress phantom resolution *beyond same-route scope*,\" implying a same-route baseline that did not exist — fixed, with two grep acceptance criteria banning the old phrase, because `112-OAUTH-CAPTURE-DISPOSITION.md` misleading THIS phase's own discussion is the precise failure being avoided. **Iteration 2's 2 warnings were folded in by hand by the orchestrator, not deferred:** (1) the wiring proof was grep-only — a grep proves the call is *written*, not that it *fires with correct arguments* — so end-to-end test `capture_egress_resolution_reaches_upstream_on_live_dispatch_path` was added, driving a live request through `handle_reverse_proxy` against a fake upstream and asserting the REAL token (not the phantom) reaches upstream, reusing the existing `TcpListener` fixture pattern; (2) `capture_egress_fails_closed_for_unadmitted_consumer_in_request_body` renamed to `capture_egress_never_substitutes_real_token_for_unadmitted_consumer` across all 5 reference sites — the behavior is \"leave unchanged and forward,\" **NOT** a request denial, and a misleading test name on a security property is itself a defect in a codebase twice burned by inaccurate records. **Planner design calls the research deliberately left open:** D-05 cap = 256 KiB default / 1 MiB hard ceiling / per-route configurable; D-06 `capture_declared_for_upstream()` uses **host-only** matching, deliberately diverging from SPIFFE's exact `host:port` precedent per upstream's own `3c59c62e` hardening lesson (widens the fail-closed guard, does not narrow it); the schema round-trip test targets the `mod tests` block housing the second of `profile/mod.rs`'s **two duplicate** `validate_against_schema` helpers. **Structural findings:** the CONNECT path needs **NO new deny logic** (`is_route_upstream` already blocks it — regression test only); only `handle_forward_http` gets a genuinely new guard. Upstream's `9b692e07` diff hunks to `launch_runtime.rs`/`sandbox_prepare.rs`/`main.rs`/`policy.rs` reference `credential_capture`/`tls_intercept` fields that **do not exist anywhere in the fork's `ProxyLaunchOptions`** — those hunks describe a struct shape this fork never had, so `git apply` is not merely unusable but meaningless. D-14's break surface confirmed at **TWO** exhaustive `RouteConfig` literals (`../nono-py/src/proxy.rs:212-232`, `../nono-py/src/policy.rs:743-761`); `../nono-ts` structurally immune. PATTERNS.md corrected a RESEARCH.md line-range drift (`test_schema_validates_spiffe_custom_credential` is `mod.rs:7396-7415`, not -7434). All 12 locked decisions (D-01r, D-02r, D-05..D-14) verified cited in colon form **BY HAND** — `check.decision-coverage-plan` returned the known **vacuous green** (`passed:true skipped:true total:0`) on this CONTEXT.md's `- **D-NN: ...**` bullet form. `workflow.use_worktrees=false` already set (required — plans reach `../nono-py` via `..`). **Next: `/gsd:execute-phase 114`.**

PRIOR (2026-08-06): **v3.6 Phase 114 DISCUSSED 2026-08-06 — `114-CONTEXT.md` + `114-DISCUSSION-LOG.md` committed, 14 decisions locked, ready for `/gsd:plan-phase 114`.** **THE VERDICT WAS DECIDED, RETRACTED, AND RE-DECIDED — the retraction is the most consequential thing in this phase's record.** First pass locked D-01 = formally DECLINE (rationale: SC2's token-confinement property lives in `forward.rs`'s `ResponseRewrite` hook, created by `149abde0`, which IS the TLS-interception commit — it makes the proxy a terminating TLS endpoint, generates an ephemeral session CA, injects a trust bundle into the sandboxed child via `SSL_CERT_FILE`/`REQUESTS_CA_BUNDLE`, adds `CapabilitySet::allow_file_mut`, and pulls 7 new deps; the fork declines TLS interception by standing decision, and SC2 forbids a reduced-scope absorb). Operator then retracted it in free text — *'Phase 114 needs to resolve sec-02. let's revisit'* — and, asked which reading applied, chose **reopen the verdict: decline isn't enough, the CAPABILITY must be delivered.** **The investigation that retraction triggered overturned the premise:** the fork's reverse proxy **already has plaintext visibility into upstream responses** — the sandboxed client speaks plain HTTP to the proxy and the proxy holds its own `tls_stream` to upstream — so MITM was NEVER required for token endpoints reachable as configured routes. The real blocker is **buffering, not visibility**: the fork streams responses in 8 KiB chunks across **three** relay sites (static-credential + the two SPIFFE handlers Phase 113 added), deliberately, for SSE / MCP Streamable HTTP / A2A JSON-RPC. **This invalidates `112-OAUTH-CAPTURE-DISPOSITION.md`'s security conclusion**, which still reads as though `forward.rs` were the only possible enforcement point — ADR-114 must say so explicitly or a future reader will re-derive the same wrong premise. **BINDING VERDICT D-01r: ADAPT** — build a fork-native response buffer+rewrite on the reverse-proxy path; SC2 satisfied BY CONSTRUCTION (D-02r) rather than by importing upstream's hook; `149abde0`/TLS interception NOT absorbed, no-MITM stands. **Locked decisions:** D-05 tight buffer cap (~256 KiB-1 MiB) **failing CLOSED** on exceed — deny, never release unrewritten, since exceeding the cap means rewrite can't be guaranteed (the existing 16 MiB *request* cap deliberately NOT reused); D-06 buffering **opt-in per route only** (streaming stays default or SSE/MCP/A2A break) **plus a D-03-style request-time fail-closed guard** for capture-declared routes arriving via CONNECT / forward-HTTP / external-proxy chain — a **fork-original test obligation**, upstream ships none because upstream has interception there (WR-13 is the precedent defect shape); D-07 port upstream's config/JWT/token-field-mapping logic as a **design reference** but **rebuild the plumbing** against `reverse.rs` (Phase 113's proven pattern; `git apply` will fail here too); D-08 real tokens **in-memory session-scoped only, zeroized on drop** — upstream's `oauth_capture/persist.rs` disk persistence NOT adopted, accepted cost is re-login after restart; D-09 **SC3 is LIVE not vacuous** (the ADAPT ships real code, so ADR-86/ADR-111 must be confirmed non-regressed) and SC4's two cross-target gates are mandatory; D-10 file the scope limit as a carry-forward note in `108-DIVERGENCE-LEDGER.md`, flipping the deferred SEC-02 row to **ADAPTED-with-scope-limit** and naming `149abde0` as a tracked divergence (mirroring how Phase 113 filed `b1ecbc02`); D-11 **SEC-02's REQUIREMENTS.md checkbox resolves as satisfied-with-scope-limit citing ADR-114** — Phase 114 must genuinely resolve SEC-02, per explicit operator instruction; D-12 `profile/credential_provider.rs` ported **adapted** minus absent-subsystem coupling (SEC-02 is literally titled *declarative* OAuth capture); D-13 the fork-only strict `nono-profile.schema.json` is a **first-class task with a round-trip test** asserting a realistic profile both deserializes AND passes `validate_against_schema()` — do NOT port upstream's 265 schema lines wholesale, they advertise persistence/interception surfaces this fork isn't building, which is itself a fail-open shape; D-14 the `nono-py` exhaustive-struct-literal break gets its **own named task** with both bindings rebuilt (`maturin build` + `napi build`) — this will be the **5th consecutive** occurrence and Phase 113 proved static inspection under-predicts it (predicted 1 field, found 3 files), and it **forces `use_worktrees=false`**. **SCOPE LIMIT (load-bearing):** capture works only for token endpoints reachable as configured reverse-proxy routes — agent-initiated flows to arbitrary hosts via CONNECT remain out of reach without TLS interception, which is the named condition under which the limit could lift. **Next: `/gsd:plan-phase 114`.**

PRIOR (2026-08-06): **v3.6 Phase 113 EXECUTED + REVIEWED + VERIFIED 2026-08-06 — 8/8 plans, all 4 SCs verified, verdict `human_needed` on ONE item: the live SPIFFE/SPIRE path has been exercised ZERO times, anywhere.** Waves ran 1 → 2 → 3 → 4 → 5 sequentially on the main tree (`workflow.use_worktrees=false`, required because Plan 113-08 edits the sibling repo `../nono-py`). **The single open gate:** `.github/workflows/spire.yml` exists and is well-formed but `gh run list --workflow=spire.yml` returns HTTP 404 (never run on the default branch), and no SPIRE agent exists on this Windows host — so `handle_spiffe_route`'s and `handle_spiffe_assertion_credential`'s successful-forward paths are compile-and-unit-proven only. **D-07's loud-skip mechanism worked exactly as designed and is what made this visible:** 6 of 8 SPIFFE tests emit `SKIP[...]` markers locally (4 in `spiffe_integration`, 2 in `spiffe_run`), independently re-run by the orchestrator; without it `cargo test` reports 7 green and `spiffe_run` finishes in 0.00s with '2 passed' — the exact `socket_access_run.rs` false-evidence signature from Phase 112. Operator decision pending: push the branch so the CI lane runs against real SPIRE 1.9.6, run `bash scripts/spire-test.sh` on a Linux/macOS host with a live `SPIRE_AGENT_SOCKET`, or accept the residual and proceed. **RESEARCH FINDING THAT RESHAPED THE PHASE:** the OAuth2-route-wiring layer upstream's SPIFFE hunks build on (`CredentialStore.oauth2_routes`/`get_oauth2()`/`OAuth2Route`, `LoadedRoute.requires_managed_credential`, and the entire `crate::forward` module) is absent from the fork — NOT because of the missing `tls_intercept`, but tracing to unabsorbed upstream commit `b1ecbc02` (`v0.38.0-3-gb1ecbc02`, 974 lines, pre-`v0.66.0`, outside this milestone's sync window, never in any DIVERGENCE-LEDGER). So `reverse.rs`/`route.rs`/`credential.rs` shipped as from-scratch SPIFFE-scoped builds guided by the diff as a design reference, NOT ports (`git apply --check` fails, the SEC-07 precedent). Every LOCKED verdict (D-01/D-03/D-04/D-06/D-08/D-09) was independently re-verified at symbol level and all held; what was wrong was CONTEXT.md's `<code_context>` 'Reusable Assets' inventory. **OPERATOR DECISIONS:** OD-1 SPIFFE-only slice, DECLINING `b1ecbc02`'s general `client_credentials` wiring, recorded in ADR-113 as a named permanent scope boundary; OD-2 corrected `route.rs`'s comment misattributing the gap to `tls_intercept` (now names `b1ecbc02` ×4, old string greps to 0). **ORCHESTRATOR-VERIFIED EVIDENCE (not executor self-report):** both cross-target clippy gates re-run independently — `cross` linux-gnu exit 0, `cargo-zigbuild` apple-darwin exit 0; `cargo fmt --all --check` clean; native clippy 0 errors; `nono-sandbox-proxy` 245/245; `nono-sandbox` 825/825; ADR-113 confirmed tracked in git (survived the `.gitignore:16` bare-`proj/` trap that silently swallows NEW files under `proj/`); D-01 boundary verified by grep (all 11 `tls_intercept` hits are comments documenting its absence, zero module/struct/fn definitions; `crate::forward` refs comments-only; `log_reverse_proxy` still defined AND called); D-03's three paths verified at source (forward-HTTP guard unconditional and explicitly NOT gated on `require_auth`; the CONNECT guard sits before `use_external` is computed and before `handle_external_proxy` dispatches, covering the external-proxy chain by ordering). **REGRESSION GATE PASSED EMPIRICALLY:** `cargo test -p nono-sandbox-cli --bin nono` fails 11 post-phase vs **13 at the pre-phase base commit** (`f89c1753`, run in a throwaway worktree) — the 11 are a STRICT SUBSET, zero new names. Project memory had recorded only 4 of these as the known Windows baseline; the real figure is 11 (6 × `config::tests::*` HOME/LOCALAPPDATA-dependent, 3 × `protected_paths::tests::*`, `profile_cmd::test_init_allowed_when_pack_has_same_short_name`, `audit_session::discover_sessions_does_not_warn_when_legacy_audit_root_is_empty`), and they are NOT env-var parallelism (identical set single-threaded). **CODE REVIEW: 0 Critical, 3 Warning, 3 Info** — the reviewer specifically hunted the Phase-112 fail-open class and found none, going as far as reading vendored `url-2.5.8` source to confirm `Url::host_str()` returns bracketed IPv6 so the authority normalization has no bypass. **WR-01 FIXED** (`5cd5562c`): `CredentialStore::load` had no `route.spiffe` branch, so direct JWT-SVID routes never reached `loaded_prefixes()` — no phantom `{PREFIX}_API_KEY` env var (most official SDKs fail at client construction) and `route_diagnostics()` misreported `cred: none` for a live credential; fixed with `declared_spiffe_routes` + `ProxyHandle.spiffe_routes` + 3 tests, no secret exposed. **STILL OPEN by operator decision:** WR-02 (`handle_spiffe_assertion_credential`'s success path untested anywhere), WR-03 (Windows SPIRE-socket isolation is a log-only no-op relying on unverified AppContainer deny-by-omission), IN-01..03. ADR-113 corrected research's '19 new crates' to a proven 9-genuinely-new / 10-pre-existing split and discharged D-05(c)'s JNI cfg-gate proof on all 3 shipped targets. `../nono-py`'s real break surface was THREE files (`proxy.rs`, `policy.rs`, `undo.rs`), not the single anticipated field — fixed in `485821f`, `maturin build` green; `../nono-ts` confirmed structurally immune, `napi build` green. NET-02 flipped Complete; ROADMAP Phase 113 marked complete. **Next: operator call on the live-SPIRE gate, then `/gsd:plan-phase 114`** (OAuth Capture, SEC-02).

PRIOR (2026-08-06): **v3.6 Phase 113 PLANNED 2026-08-06 — 8 plans across 5 waves, READY TO EXECUTE.** Research (`113-RESEARCH.md`, 655 lines, MEDIUM-HIGH confidence, every fork claim backed by a live `git show`/`grep`/file:line) + pattern map (`113-PATTERNS.md`, 20/20 files mapped to fork analogs) + `113-VALIDATION.md` (Nyquist, reconciled against the finished plan set, `nyquist_compliant: true`, 21/21 tasks carry an automated verify) + plans + `gsd-plan-checker` (iteration 2: 1 blocker + 3 warnings raised and all closed; 1 cosmetic orphan-tag warning fixed by hand). Waves: 1 = `113-01` core SPIFFE audit vocabulary (D-08/SC3) + new `spiffe.rs`/`auth.rs` + the `spiffe 0.16` dep / `113-02` `config.rs` schema types + the ~63-site exhaustive-literal fixup + SC2 profile/schema surface + socket isolation; 2 = `113-03` SPIFFE-scoped OAuth2 jwt-bearer assertion machinery / `113-04` `LoadedRoute.managed_auth`/`has_spiffe_source` + async `RouteStore::load` (D-04) + OD-2 comment fix; 3 = `113-05` async call sites + `bound_port` + the D-03 fail-closed guard + `server.rs:235` rewrite / `113-06` `handle_spiffe_route` + `handle_spiffe_assertion_credential` from-scratch rewrite; 4 = `113-07` ported tests + D-07 loud-skip reporting + the fork-original D-03 test + the SPIRE CI lane; 5 = `113-08` ADR-113 + D-02 ledger carry-forward + D-05 dependency review + binding rebuilds + both cross-target clippy gates. All 18 tracked IDs (D-01..D-09, D-11, D-12, OD-1, OD-2, SC1..SC4, NET-02) verified cited in colon form across the plans **by hand** — `check.decision-coverage-plan` returned a **vacuous green** (`passed:true skipped:true total:0`) on this CONTEXT.md's `- **D-NN: ...**` bullet form, the known parser gap. **Research finding that materially changed the phase's shape vs. CONTEXT.md's assumptions:** the OAuth2-route-wiring layer upstream's SPIFFE hunks build on — `CredentialStore.oauth2_routes`/`get_oauth2()`/`OAuth2Route`, `LoadedRoute.requires_managed_credential`/`managed_auth_mechanism`/`managed_injection_mode`/`missing_managed_credential()`, and the **entire `crate::forward` module** — is **absent from this fork**, and NOT because of the missing `tls_intercept`: it traces to unabsorbed upstream commit `b1ecbc02` (`git describe` = `v0.38.0-3-gb1ecbc02`, 974 lines, pre-`v0.66.0`, outside this milestone's sync window, never in any DIVERGENCE-LEDGER). So `reverse.rs`/`route.rs`/`credential.rs` are **from-scratch SPIFFE-scoped builds guided by the diff as a design reference, not literal ports** (`git apply --check` fails, the SEC-07 precedent). CONTEXT.md's `<code_context>` 'Reusable Assets' inventory and D-01's own supporting text both understated this; the LOCKED VERDICTS of D-01/D-03/D-04/D-06/D-08/D-09 were each independently re-verified at symbol level and all held. **Two OPERATOR DECISIONS taken during planning:** **OD-1** — build only a narrow SPIFFE-only slice of the missing scaffolding and **DECLINE** `b1ecbc02`'s general `client_credentials` route wiring; ADR-113 must record this as a **named, permanent scope boundary** (ADR-111 shape) so a future `b1ecbc02` absorb has something to reconcile against, and the D-02 ledger note must file `b1ecbc02` as a newly-identified divergence alongside the `tls_intercept` carry-forward. **OD-2** — **correct** the fork's own comment at `route.rs:617-639`, which misattributes the missing machinery to *'the upstream tls_intercept module not present in this fork'* (disproved via `git log --all -S\"handle_oauth2_credential\"` on the `upstream` remote); leaving it wrong actively misdirects the next absorb. **Four landmines folded into explicit task steps with grep-verified acceptance criteria:** (1) let-chains at `spiffe.rs::check_nbf` and in `proxy_runtime.rs` pass `cargo build` but FAIL `cargo fmt --all --check` on this edition-2021 workspace (fix precedent `hook_runtime.rs:360-370`, identical to Plan 112-02's); (2) **`.gitignore:16`'s bare `proj/` pattern silently drops NEW files under `proj/`** — the SC1-required `proj/ADR-113-spiffe-disposition.md` needs `git add -f`, and upstream's `.gitignore` hunk must NOT be adopted; (3) upstream's diff DELETES `log_reverse_proxy` but the fork still CALLS it at `reverse.rs:460` — do not delete; (4) `server.rs:235`'s divergence note becomes 2-of-3-clauses false under D-04 and must be rewritten, not left stale. **Also corrected during planning:** `nono-proxy/audit.rs` has 4 `log_*` functions not 5 (`log_l7_policy_decision`/`log_credential_capture` don't exist here); `OAuth2Config.client_id`/`client_secret` currently lack `#[serde(default)]` (a real gap, not already-fixed as RESEARCH implied) and the profile schema wrongly lists both in `required`, which would hard-reject any `client_assertion` profile; `proxy_command.rs` (created by Phase 112, after the analog scan) has its own `NetworkAuditEvent` fixture site. **D-04's blast radius is far smaller than its own text feared** — 1 production `RouteStore::load` call site (`server.rs:557`, already async) + 7 test sites, all inside `nono-proxy`, zero cross-crate or cross-repo impact. **D-09's break site confirmed exact:** `../nono-py/src/proxy.rs:206-218`'s exhaustive `RustRouteConfig` literal needs `spiffe: None,` after `aws_auth: None,`; `../nono-ts` confirmed structurally immune (zero `nono_proxy` references). Toolchain confirmed on-host: Docker/`cross`, `zig`/`cargo-zigbuild`, `cargo-audit 0.22.1`; `make` still absent, so all `make ci`/`make test-spiffe` references are written into tasks as their constituent `cargo`/`bash` commands. **Next: `/gsd:execute-phase 113`.**

PRIOR (2026-08-06): **v3.6 Phase 113 DISCUSSED 2026-08-06 — `113-CONTEXT.md` + `113-DISCUSSION-LOG.md` committed, 7 decisions locked (D-01..D-07), ready for `/gsd:plan-phase 113`.** Also this session: the Phase 112 Criticals+Warnings fix pass landed (16 findings across 2 subsystem-scoped groups, 18 commits, both cross-target gates GREEN), and `../nono-py` was found broken by core-proxy struct drift and fixed (`252ffe3`). **Phase 113 pre-discussion re-measurement corrected two stale premises:** (1) **ROADMAP SC1 for Phase 113 is factually stale** — it directs ADR-113 to weigh 'the 545-deletion rewrite of the fork's divergent `tls_intercept`/`reverse.rs`', but the fork has **no `tls_intercept` module at all** (confirmed by `ls`, and independently recorded twice before in `109-AWS-SIGV4-TLS-INTERCEPT-FINDING.md` and `112-AWS-SIGV4-PROXY-AUTH-FINDING.md`); the `reverse.rs` half is live, the `tls_intercept` half is superseded by D-01. (2) `crates/nono-proxy/src/server.rs:235` already carries a deliberate Phase 109 note ('no TLS intercept, no SPIFFE, no async `RouteStore::load`'), making the RouteStore question an **overturn of a recorded decision**, not a fresh choice. **Locked decisions:** D-01 ADAPT-DOWN the ~478 `tls_intercept` lines, with ADR-113 owing a *positive* grep-backed proof no fork route type is left silently unauthenticated — motivated by the finding that `reverse.rs`'s `handle_spiffe_route` and `tls_intercept/handle.rs`'s `handle_spiffe_intercept_request` are **parallel sibling paths**, the inverse of SEC-02 where the missing hook WAS the sole enforcement point; D-02 (discretion) carry-forward note in the 108 ledger per the SEC-09 precedent; D-03 a SPIFFE-declared route arriving on a non-SPIFFE path **fails closed at request time** (directly the WR-13 shape); D-04 **ADOPT async `RouteStore::load`, overturning Phase 109** — the await on `SpiffeJwtSource::connect` is structurally required, and it sits inside the per-route SPIFFE branch so non-SPIFFE profiles gain no live-SPIRE startup dependency; the false `server.rs:235` note must be rewritten; D-05 accept the dependency surface **gated on an explicit review** — one direct dep (`spiffe 0.16`) pulling **19 crates** including a full gRPC/protobuf stack (`tonic`/`prost`) and **JNI bindings** via `rustls-platform-verifier`, with a proof obligation that the JNI path is cfg-gated out on all 3 shipped targets; D-06 absorb the fail-closed tests **and stand up the SPIRE CI lane** (upstream's split is favourable — fail-closed tests need no SPIRE, live ones gate on `SPIRE_AGENT_SOCKET`, and the mock server is a Rust `TcpListener` not the python script, so the local no-python3 gap does NOT bite the test bodies); D-07 **loud skip reporting is a hard acceptance criterion**, motivated by `socket_access_run.rs` reporting `ok` in ~0.01s while exercising nothing and nearly becoming evidence in Phase 112. Carried from 109 and NOT re-litigated: D-08 (core-library touches are audit-only, ADR-86 intact — but ADR-113 must confirm against the full diff, not inherit), D-09 rebuild both bindings, D-11 verify by behavior never identifier name, D-12 both cross-target gates mandatory. **Flagged for planning:** `RouteConfig` gains a `spiffe` field — the **4th consecutive drift** into nono-py's exhaustive struct literal. **Next: `/gsd:plan-phase 113`.**

PRIOR (2026-08-05): **v3.6 Phase 112 EXECUTED + REVIEWED + VERIFIED + CLOSED 2026-08-05 — 8/8 plans, all 10 owned requirements resolved, operator-approved at the `human_needed` gate.** Waves ran 1 → 2 (112-02/03/04) → 3 (112-05/06/07) → 4, sequentially on the main tree (`workflow.use_worktrees=false`). Phase completion was performed BY HAND — `gsd-sdk query phase.complete` was deliberately not run (it previously returned `is_last_phase: true` wrongly with later phases unbuilt, set `status: milestone_complete` and `percent: 125`, and flattened Current Position; 113/114 unbuilt reproduces exactly that condition). **Two execution-level findings that matter for future UPST phases:** (1) **`112-DISPOSITION-TABLE.md` was wrong in 3 of the ~6 dispositions re-checked at implementation time, always the same way — confidence assigned from file-presence evidence rather than symbol-level verification** (SEC-03 adopt→ADAPT, `4cc0af2c52` adapt-or-skip→SKIP, `9840a16f35` verbatim→adapted; all 3 recorded in the ledger addendum's planned-vs-shipped table). After the first miss, a symbol-level verification warning was injected into every subsequent executor prompt and it caught the second and third. **The defect is in how UPST reality-check passes are specified — a future UPST phase should require symbol-level verification before any confidence rating is assigned.** (2) **The phase introduced 4 Critical fail-open defects and then fixed them; all 8 executor self-checks reported PASSED and none caught any — the `/gsd:code-review` gate did.** CR-01 (`916d3bd7`): `nono proxy --profile X` hardcoded `deny_domain: Vec::new()`/`no_proxy: Vec::new()`/`strict_filter: false` while honouring the same profile's `allow_domain` + credentials, so `allow_domain:[\"*.corp.com\"] + deny_domain:[\"secrets.corp.com\"]` forwarded `secrets.corp.com` and `network.block:true` became allow-all — ambiently reachable via `NONO_PROFILE`; fixed + `--deny-domain` flag + deny-without-allow hard error + 5 regression tests. CR-02 (`892d2e01`): a predicate-less project `trust-policy.json` returned `Ok(TrustPolicy::default())` **before** the user-level policy was loaded, letting an untrusted repo neutralize the operator's global publishers/blocklist — and since no released version ever wrote a `predicate` field, this was the upgrade path for every existing deployment; fixed by content-based legacy detection (migration warning, deliberately not a hard error, to avoid bricking field policies) plus removal of the short-circuit return. CR-03 (`585f2743`): the orphan reaper's early return skipped `drain_pending_network_notifications()`, making the existing drain dead code on the normal exit path and silently losing end-of-run denial records. CR-04 (`20f9a1cd`): `SeccompOpts::external_tcp()` was a zero-caller fail-open hatch in the core library ignoring `CapabilitySet`'s `NetworkMode` — gated rather than deleted, since `112-02-PLAN.md`'s must_haves require the entry point to exist. CR-01 and CR-02 were orchestrator-verified at source before the fixer was dispatched. **12 Warning + 5 Info findings in `112-REVIEW.md` remain OPEN by deliberate operator scoping** — highest-value is **WR-06: `--allow-gpu` is now a hard error (`_exit(126)`) on non-NVIDIA hosts, in containers without `CAP_SYS_ADMIN`, and under WSL2, so `nono wrap --allow-gpu` no longer works there** (a real unfixed behaviour regression); also WR-01 (`waitpid(-1)` can steal unrelated `std::process::Child` statuses), WR-02/WR-03 (`discard_late_terminal_input` always eats the first byte of genuine type-ahead — the phase's own test asserts the loss — and does not retry `EINTR` on `poll()`, at teardown when `SIGCHLD` is near-certain), WR-05 (non-loopback `--listen` exposes the session token and injected credentials over plaintext HTTP). **Verification returned `human_needed`; 3 items persisted to `112-HUMAN-UAT.md` and operator-approved:** the full 36-binary `cargo test -p nono-sandbox-cli --tests` sweep stalled ~25min and was killed (only ~11 relevant binaries ran, so **ROADMAP SC4's 'make ci GREEN' is only PARTIALLY evidenced** — resolve on the GH Actions Linux lane), and SEC-06's orphan-reap test plus RES-02's tightened denial-marker assertion both short-circuit on `python3_available()` in the local `cross` container so neither was ever runtime-exercised. Both cross-target clippy gates ran GREEN with `--all-targets` on every code-touching plan and again after the Critical fix pass; `cargo fmt --all --check` clean; `crossbeam-epoch 0.9.20` + `cargo audit` exit 0 re-confirmed live. Artifacts: `112-REVIEW.md`, `112-VERIFICATION.md`, `112-HUMAN-UAT.md`. Commits `916d3bd7`/`892d2e01`/`585f2743`/`20f9a1cd`/`6706b18d`/`1722f027`. **Next: `/gsd:plan-phase 113`** (SPIFFE/SPIRE, NET-02) or `/gsd:plan-phase 114` (OAuth Capture, SEC-02) — operator picks.

PRIOR (2026-08-05): **v3.6 Phase 112 Plan 08 (close-out) COMPLETE 2026-08-05 — Phase 112 is fully CLOSED.** Appended the `## Phase 112 Security + Residual Sync Addendum` to `108-DIVERGENCE-LEDGER.md` recording all 18 SHA dispositions as shipped (superseding `112-DISPOSITION-TABLE.md`'s pre-execution text in 3 places: SEC-03 shipped ADAPT not the table's `adopt` — depends on the unabsorbed `fa21a004`/`8a4237f2` #1283 refactor; RES-02/`4cc0af2c52` shipped SKIP not `adapt-or-skip` — target `socket_test_dir()` confirmed absent from the fork entirely; RES-02/`9840a16f35` shipped adopt-adapted not verbatim — fork's diagnostic formatter never emits upstream's literal denial marker). Filed the "SEC-09 Carry-Forward Note (Phase 112, D-01)" directly in the ledger's tool-sandbox-surface cluster section, obligating a future v3.7 planner to consciously re-decide `f6f02751`'s relaxed guard rather than silently inherit it. Reconciled `REQUIREMENTS.md`: flipped `SEC-01`/`SEC-08`/`SEC-09`/`RES-01`/`RES-02` to complete with cited RESOLVED notes (`SEC-03..07` were already flipped by their own single-contributing plans); `SEC-02` explicitly left unchecked with a "-> CARVED OUT to Phase 114" note, mirroring the NET-02 -> Phase 113 precedent. `ROADMAP.md`'s Phase 112 checklist box, `112-08-PLAN.md` line, and Progress-table row all flipped to complete (the SDK's `roadmap.update-plan-progress` did not add the Progress row — added by hand); the pre-existing Amendment paragraph and Phase 114 section confirmed present and left unedited. Combined-surface verification: `git log ad8132e7..HEAD -- crates/` confirmed zero `crates/` commits since `112-07`'s own both-gates-GREEN confirmation, so the mandatory cross-target clippy gates were not re-run (zero-crates-diff plan, per this plan's own gate); `cargo fmt --all -- --check` ran fresh (clean) and the full `cargo test --workspace --no-fail-fast` sweep ran fresh — its 17-name failing set is byte-for-byte identical to `111-04-VERIFICATION-NOTES.md`'s own 17-name run, a confirmed strict subset of the 27-name baseline, zero new names. **HAZARD RE-CONFIRMED AGAIN 2026-08-05:** `gsd-sdk query state.advance-plan` deleted this `stopped_at` field's prior ~4,700-char content down to a bare 8-line diff (42 deletions/8 insertions) despite being a narrower verb than `phase.complete` — reverted via `git checkout -- .planning/STATE.md` before any commit; this file stays hand-maintained. Commits `e32f0192`(Task 1, docs)/`a3d69d48`(Task 3, docs)/`885f655b`(summary). See `112-08-SUMMARY.md`. **Phase 112 is CLOSED — 10 requirements Complete (SEC-01, SEC-03..SEC-09, RES-01, RES-02), SEC-02 carved out to Phase 114 with a full evidence trail.** Next: Phase 113 (SPIFFE/SPIRE, NET-02) or Phase 114 (OAuth Capture, SEC-02) — operator picks.

PRIOR: **v3.6 Phase 112 Plan 07 (SEC-07, standalone `nono proxy` command) COMPLETE 2026-08-05.** Absorbed upstream `2663e990` (#1261) as an ADAPT: upstream's `proxy_command.rs` imports `CredentialProxyIntent`/`DomainFilterIntent`/`EndpointFilterIntent`/`TlsInterceptIntent`/`UpstreamProxyIntent` from `launch_runtime.rs` — 0 grep hits in this fork — so built `ProxyArgs -> ProxyLaunchOptions -> proxy_runtime::build_proxy_config_from_flags -> nono_proxy::server::start` instead, reusing the fork's real post-ADR-98 proxy-launch API (the same path `nono run`'s proxy activation uses, inheriting ADR-108 deny-domain / D-06 no_proxy-overlap validation for free). `apply_tls_intercept_config` dropped entirely (0 grep hits; no `tls_intercept` module in this fork), with `proxy_command.rs`'s own module doc comment grep-clean of the banned type/function names per the plan's literal acceptance criterion. Task 1 wired the 4-site CLI surface (`Commands::Proxy(Box<ProxyArgs>)` in `cli.rs` mirroring `Commands::Why`'s flat-args shape, dispatch arm in `app_runtime.rs`, per-variant verbosity arm in `cli_bootstrap.rs`, `mod proxy_command;` in `main.rs`) — `ProxyArgs` omits upstream's `--pass` (custom session password; this fork's `nono_proxy::token` only supports random tokens) and `--proxy-ca-cert`/`--proxy-ca-key` (this fork has no TLS interception at all, confirmed `ProxyHandle::intercept_ca_path()` always returns `None`). **Task 2's disposition-verification pass surfaced a genuine, not-in-the-plan-text security gap and closed it (Rule 2):** `grep -n \"require_auth\" crates/nono-proxy/src/*.rs` returned 0 hits before this plan — the fork had NO `--no-auth` concept at all. `reverse.rs`'s two auth checks were unconditionally fatal (fine for sandboxed-run) and `connect.rs`'s CONNECT-tunnel check was unconditionally lenient (comment: \"Non-fatal for CONNECT: Node.js undici doesn't send Proxy-Authorization\" — also fine for sandboxed-run, where the OS sandbox is the real trust boundary), but for a standalone proxy with no OS sandbox behind it, the lenient CONNECT path meant a client on a non-loopback `--listen` with auth left ON (no `--no-auth`) could tunnel straight past the advertised session-token boundary. Added `ProxyConfig.require_auth` (default `true`, gates `reverse.rs`'s two checks + `external.rs`'s enterprise-proxy-chain check) and `ProxyConfig.strict_connect_auth` (default `false`, makes `connect.rs`'s CONNECT check fatal — 407 — instead of only logged); standalone sets both to `!args.no_auth`. Both defaults preserve pre-existing sandboxed-run behavior byte-for-byte — confirmed by all 218 pre-existing `nono-sandbox-proxy` unit tests passing unchanged after the change. The `--no-auth` + non-loopback `--listen` fail-secure guard (T-112-15) ported verbatim. Task 3's cross-target linux-gnu clippy gate (invisible to the Windows-host build, which cannot link `aws-lc-sys`/`ring`) caught a real `clippy::too_many_arguments` violation in `external::handle_external_proxy` (8 args after adding `require_auth`) — fixed via a new `ExternalProxyCtx` struct mirroring the existing `ReverseProxyCtx` pattern, dropping to 5 args. Added 2 Wave-0 integration tests (`crates/nono-cli/tests/proxy_command_run.rs`: `--help` usage line, `--no-auth`+non-loopback rejection), both pass. Both mandatory cross-target clippy gates GREEN with `--all-targets` (linux-gnu via `cross clippy`, apple-darwin via `cargo-zigbuild clippy`, `SDKROOT` unset); `cargo fmt --all --check` clean; `cargo build --workspace --all-targets` exits 0. `requirements.mark-complete SEC-07` applied by hand-edit (checkbox + traceability row) — this plan alone fully satisfies SEC-07 (sole contributing plan per D-04's \"own plan, own wave\" sizing). Commits `00d45c67`(Task 1, feat)/`14ed22e5`(Task 2, feat)/`ad8132e7`(Task 3, test)/`d83f9842`(summary). See `112-07-SUMMARY.md`. **Wave 3 of Phase 112 is now COMPLETE** (112-05/112-06/112-07 all done). Next: Wave 4 — `112-08` (D-05 ledger addendum + SEC-09 carry-forward note + combined verification), the final plan of Phase 112."

PRIOR (2026-08-05): **v3.6 Phase 112 Plan 06 (SEC-06, seccomp supervisor-ancestry orphan reaping) COMPLETE 2026-08-05.** Ported upstream `ac5ccd70` (#1401) into `crates/nono-cli/src/exec_strategy.rs` as an ADAPT: upstream's gate predicate (`tool_sandbox_runtime.is_some() || seccomp_policy.child_requires_dumpable()`) references two symbols absent from this fork (no `tool_sandbox_runtime` — standing v3.7 divergence per D-01/SEC-09; no `SeccompPolicy` struct — unabsorbed `fa21a004`/`8a4237f2`, #1283), so reused the fork's existing `linux_child_requires_dumpable()` predicate instead, dropping the `tool_sandbox_runtime` disjunct entirely. **Amendment to the plan's own `<interfaces>` section found during mandatory symbol-level re-verification:** the plan cited a stale 2-arg `linux_child_requires_dumpable(capability_elevation, network_notify)` call, but `112-02`'s same-day SEC-03 absorb (earlier in this wave-set) had already extended the predicate to 3 args (`+ proc_comm_notify`) before this plan executed — used the live 3-arg signature/call site so the new `PR_SET_CHILD_SUBREAPER` gate agrees exactly with the existing `PR_SET_DUMPABLE` gate. `PR_SET_CHILD_SUBREAPER` lands pre-fork (parent side, `execute_supervised`); `reap_reparented_orphans(child)` ported verbatim, placed between the two cfg-split `run_supervisor_loop` definitions (matching upstream's own placement) and wired ONLY into the `#[cfg(target_os = \"linux\")]` arm (confirmed via direct read of both cfg-gated definitions — resolves RESEARCH.md's Assumption A3), between `handle_pty_suspension` and the primary `waitpid(child, WNOHANG)`. **Genuine RED/GREEN TDD executed live**, not merely asserted: a temporary always-`None` stub of `reap_reparented_orphans` was compiled and run via `cross test --target x86_64-unknown-linux-gnu`, confirmed RED (\"expected primary child's exit status, got None\"), then the real implementation restored and confirmed GREEN together with the pre-existing, unmodified `test_linux_child_requires_dumpable_only_for_seccomp_driven_features`. Task 2 ported upstream's own integration regression test verbatim into `tests/socket_access_run.rs` (double-forked orphan + loopback TCP connect, `af_unix_mediation_pathname_allows_orphaned_child_tcp_connect`) — no fork field-name adaptation needed, `linux.af_unix_mediation`/`network.block` matched the fork's schema exactly. **Known limitation, not a regression:** all 3 python3-dependent tests in `socket_access_run.rs` (2 pre-existing + this plan's new one) short-circuit on \"skipping: python3 not available\" in the local `cross`-rs container (confirmed via `--nocapture`) — structural coverage is confirmed (compiles, matches schema, wired to the real CLI pipeline), but the new test's behavioral assertions were not end-to-end exercised locally; flagged for a live python3-equipped CI lane. Both mandatory cross-target clippy gates GREEN with `--all-targets` (linux-gnu via `cross clippy`, exit 0, 42s; apple-darwin via `cargo-zigbuild clippy`, exit 0, 16s — both fast, build already warm from the `cross test` runs in this same session); `cargo fmt --all --check` clean. `requirements.mark-complete SEC-06` applied by hand-edit (checkbox + traceability row) — this plan alone fully satisfies SEC-06. Commits `9aaba29e`(Task 1, feat)/`c0b5fab4`(Task 2, test)/`63193109`+`dd5e065f`(summary). See `112-06-SUMMARY.md`. Next: remaining Wave 3 plan `112-07` (SEC-07), then Wave 4 `112-08`.

PRIOR (2026-08-05): **v3.6 Phase 112 Plan 05 (SEC-05, Landlock execute-restriction Refer grant) COMPLETE 2026-08-05.** Ported upstream `d84b4818` (#1397) into `restrict_execute()` (`crates/nono/src/sandbox/linux.rs`): the stacked execute-restriction Landlock ruleset now also `.handle_access(AccessFs::Refer)` and, when `abi.has_refer()`, grants a bare `Refer` rule on `/` after the per-path `Execute` rules and before `restrict_self()` — fixing renames into subdirectories created after `restrict_execute()` is applied, which the narrower layer previously denied even though the main sandbox ruleset already permitted the rename. Symbol-level re-verification (per this phase's disposition-table-was-wrong-twice-already warning) confirmed the disposition table's **adopt, HIGH confidence** call was correct this time: every referenced symbol (`abi.has_refer()`, `PathFd`, `PathBeneath`, `AccessFs::Refer`, `FsCapability::new_dir`, `CapabilitySet::add_fs`) already exists with matching signatures, and the general `Refer` grant at `linux.rs:365` was independently confirmed to live in a separate, stacked `Ruleset`/`restrict_self()` layer from `restrict_execute()`'s own — an additive grant, not deny-within-allow. Ported the Wave-0 regression test `test_restrict_execute_does_not_break_rename_into_new_subdir` verbatim, with exactly one naming adaptation flagged in advance by the plan's own `read_first`: `apply_landlock(&caps)` -> `apply(&caps)` (no function named `apply_landlock` exists in this fork; its equivalent single entry point is `apply()`). Verified live via `cross test --target x86_64-unknown-linux-gnu -p nono-sandbox -- test_restrict_execute_does_not_break_rename_into_new_subdir`: `ok. 1 passed`. Both mandatory cross-target clippy gates GREEN with `--all-targets` (linux-gnu via `cross clippy`, apple-darwin via `cargo-zigbuild clippy`, `SDKROOT` unset); `cargo fmt --all --check` clean. `requirements.mark-complete SEC-05` applied by hand-edit (checkbox + traceability row) — this plan alone fully satisfies SEC-05. Commit `96398a3d` (fix, Task 1); Task 2 was pure verification with no code changes. See `112-05-SUMMARY.md`. Next: remaining Wave 3 plans `112-06` (SEC-06) / `112-07` (SEC-07), then Wave 4 `112-08`.

PRIOR (2026-08-05): **v3.6 Phase 112 Plan 04 (RES-02, PTY teardown drain + socket dispositions) COMPLETE 2026-08-05.** Verified-then-committed a prior interrupted executor's uncommitted Task 1 work (`CprReplyParse`/`discard_late_terminal_input`/`TERMINAL_QUERY_REPLY_TIMEOUT`, ported verbatim from upstream `503045801a`) after independently re-deriving the disposition and confirming the drain call sits only on the final `release_terminal_for_prompt` teardown path (not `pause_terminal_for_prompt` suspend/resume, not the `Drop` impl) and `restore_terminal()`'s own body is untouched. Live-verified all 38 `pty_proxy` unit tests (5 new) pass via `cross test --target x86_64-unknown-linux-gnu`. **Task 2 caught and corrected a disposition-table error at symbol level:** `4cc0af2c52`'s target `socket_test_dir()` tempdir-choice helper does not exist anywhere in this fork (`crates/nono/src/supervisor/socket.rs` — zero grep hits — nor `crates/nono-cli/src/open_url_runtime.rs`, which carries no test module at all; the commit's third file, `crates/nono-cli/tests/url_open_integration.rs`, doesn't exist in the fork). `112-DISPOSITION-TABLE.md`/`112-RESEARCH.md`'s MEDIUM-confidence 'adapt with caution, or skip' call was based on file-presence checks only; corrected to SKIP with a comment-only addition (`git diff --stat` confirms zero functional change) documenting the absence, superseding the disposition table's original note. `9840a16f35`'s test-quality tightening was adopted but **adapted, not ported verbatim**: `NONO_NO_SAVE_PROMPT=1` landed as-is, but the denial-marker assertion was rebuilt around the bare socket path rather than upstream's `\"send {socket_arg}\"` marker, since this fork's `diagnostic/formatter.rs` renders IPC denials as a block header + one denied-path-per-line, never that literal string — porting verbatim would have made the assertion vacuously pass. Both mandatory cross-target clippy gates (`--all-targets -D warnings -D clippy::unwrap_used`) run twice (Task 1 state, then combined Task 1+2 state) — all 4 runs GREEN (linux-gnu 14m08s/8m41s, apple-darwin 42s/18s); `cargo fmt --all --check` clean both times. `cross test` for `socket_access_run`'s tightened assertion reports 1 passed, but via the test's pre-existing python3-unavailable graceful-skip path (confirmed `python3` absent from the `cross-rs` base image) — the runtime denial-marker logic itself was not end-to-end exercised locally; documented as a known limitation, not a regression (unchanged guard, pre-existing behavior). RES-02 annotated inline in `REQUIREMENTS.md` (all 3 commits dispositioned); checkbox deliberately left unflipped, matching the SEC-01/SEC-08/SEC-09/RES-01 won't-sync/skip-biased convention — reconciled at the Phase 112 gate (`112-08`). Commits `f9199b37`(Task 1)/`f45ec8fc`(Task 2)/`f5b3b820`(summary). See `112-04-SUMMARY.md`. **Wave 2 of Phase 112 is now COMPLETE** (112-02/112-03/112-04 all done). Next: Wave 3 — `112-05` (SEC-05) / `112-06` (SEC-06) / `112-07` (SEC-07), parallel, all depend on `112-01`.

PRIOR (2026-08-05): **v3.6 Phase 112 Plan 03 (SEC-04 + SEC-08, batched per D-04) COMPLETE 2026-08-05.** Task 1 ported upstream `f943fb5a` verbatim: `TrustPolicy` gains a `predicate: Option<String>` field + `TRUST_POLICY_PREDICATE` const (`https://nono.sh/attestation/trust-policy/v1`); `TRUST_POLICY_VERSION` is `#[deprecated(since = \"0.66.0\")]` and dropped from the `crate::trust` re-export (kept internally for backward-compat parsing); a new `load_nono_policy()` peek-before-full-parse loader in `nono-cli`'s `trust_scan.rs` reads raw JSON and checks the `predicate` field before attempting full deserialization, so a foreign `trust-policy.json` (e.g. AWS IAM, same filename) is skipped with a warning suggesting `nono trust init --force` instead of crashing on a schema-parse error; `nono trust init` no longer emits `\"version\"`. `validate_version()`'s predicate check uses nested `if let`, not upstream's let-chain, staying MSRV-1.82/edition-2021-safe (same precedent Plan 112-02 hit). All 4 `trust-policy.json` examples in `docs/cli/features/trust.mdx` updated (including the fork-only GitLab-signed-files section upstream's own diff doesn't touch, for consistency). `cargo test -p nono-sandbox --lib -- trust::` 189 passed; `cargo test -p nono-sandbox-cli --bin nono -- trust_scan:: trust_cmd::` 57 passed. **Task 2 fired D-05's sole contingent ADR-112 escalation trigger** (an adopt that would loosen an existing fork guard): upstream `a5a441c2` re-types `EnvironmentConfig.allow_vars` `Vec<String>` -> `Option<Vec<String>>` so an *omitted* `allow_vars` key means \"inherit every parent env var\" (`None`) — the fork's existing `profile_runtime.rs:840-872` resolution (Plan 34-08a Task 3/5, D-20 manual replay of upstream `1b412a7`/`780965d7`) deliberately collapses *both* \"key absent\" and \"explicit `[]`\" to `Some(vec![])` (strip all, fail-closed). Wrote `proj/ADR-112-allow-vars-fail-closed-preserved.md` mirroring ADR-111's shape: **PRESERVE, do not adopt**, zero source-code change (`git diff -- crates/nono-cli/src/profile_runtime.rs` confirmed empty), `empty_allow_vars_fails_closed` (`profile_runtime.rs:989`) reconfirmed passing unchanged as the durable regression guard. `requirements.mark-complete SEC-04` applied by hand (checkbox + traceability row); **SEC-08's checkbox deliberately left unflipped** — per the SEC-01/SEC-09 won't-sync convention, a resolved-but-not-literally-\"absorbed\" item is annotated inline (`REQUIREMENTS.md:137`, \"RESOLVED 2026-08-05, not absorbed\" + ADR citation) rather than checked, reconciled at the Phase 112 gate (`112-08`). Neither task's files (`crates/nono/src/trust/*.rs`, `crates/nono-cli/src/trust_{cmd,scan}.rs`) contain `#[cfg(target_os = ...)]`/`exec_strategy/`/`bindings/c/src/` — mandatory cross-target clippy gates assessed NOT REQUIRED for this plan (documented in `112-03-SUMMARY.md`'s Verification section); ran Windows-host `cargo clippy --workspace --all-targets -D warnings -D clippy::unwrap_used` + `cargo fmt --all --check` as supplementary sanity, both clean. Commits `62d9e050`(Task 1)/`9cdef2f5`(Task 2)/`296b0a04`(summary)/`2f6cf5c0`(self-check). See `112-03-SUMMARY.md`. Next: remaining Wave 2 plan `112-04` (RES-02), then Wave 3.

PRIOR (2026-08-05): **v3.6 Phase 112 Plan 02 (SEC-03, NVIDIA procfs mediation hardening) COMPLETE 2026-08-05 — ADAPTED, not the disposition table's literal 'adopt'.** Live diff-forensics during Task 1's read_first (`git show a3243907 -- crates/nono/src/sandbox/mod.rs`, then symbol-level greps) proved upstream `a3243907` (#1284) depends on an antecedent, unabsorbed refactor: `fa21a004`/`8a4237f2` (2026-06-27/29, #1283, \"introduce SeccompPolicy struct and client-driven selection\", 21 files) — recorded in `108-DIVERGENCE-LEDGER.md` line ~1142 as a standing \"CORE-cluster/Phase-111 residual\" that Phase 111 never absorbed. Zero grep hits anywhere in the pre-absorb fork tree for `SeccompPolicy`/`LinuxSandboxPolicy`/`apply_landlock`/`apply_auto`/`apply_external`/`TcpNetworkEnforcement` confirmed the diff's own context lines assume symbols that simply do not exist; `112-DISPOSITION-TABLE.md`'s \"adopt, HIGH confidence\" call for SEC-03 was based on file-presence evidence only (`ls`/`Read`), not symbol-level verification. **Implemented as an ADAPT** (ADR-111 precedent: adapt not adopt when upstream pushes policy/enforcement into core `nono`) against the fork's existing simpler `apply()`/`apply_with_abi()` architecture: new `Sandbox::apply_seccomp`/`apply_seccomp_with_abi`/`apply_external` (backed by a new `SeccompOpts` + refactored `apply_with_abi_inner(caps, abi, handle_tcp: bool)`), `/proc/self/task` Landlock grant flipped `ReadWrite` -> `Read`, and a new supervisor seccomp-notify fast path (`is_proc_task_comm_for_tgid`/`proc_comm_notify_allows_access`/`open_proc_comm_for_access` in `supervisor_linux.rs`) that mediates `/proc/<tgid>/task/<tid>/comm` writes validated against the notifying child's own tgid. Required openat AND network seccomp-notify setup/fd-handoff failures are now fatal `NonoError::SandboxInit` (child `exit(126)`, supervisor kill+`Err`) instead of `warn!`+silent-continue, on both sides — closes a fail-open gap. `proc_comm_notify: bool` threaded end-to-end `PreparedSandbox -> ExecutionFlags -> ExecConfig -> SupervisorConfig` (flat-field pattern matching `capability_elevation`/`af_unix_mediation`, NOT upstream's nested struct); `nono wrap` rejects it (Direct-strategy can't run the supervisor). Fixed 1 latent bug surfaced by the refactor (Landlock NetPort-rule-add gate incorrectly keyed on `seccomp_net_fallback == None`, also true when `handle_tcp=false` — replaced with explicit `landlock_network_active` flag) and 1 edition-compat issue (a ported let-chain, invalid under this workspace's Rust 2021 edition — `cargo build` silently accepted it but `cargo fmt --all --check` correctly rejected it; rewrote as nested `if`/`if let`, matching the codebase's existing `hook_runtime.rs` precedent against let-chains). **Both mandatory cross-target clippy gates GREEN** (`cross clippy` linux-gnu exit 0 in 14m57s; `cargo-zigbuild clippy` apple-darwin exit 0 in 44s), `cargo fmt --all --check` clean, `cross test` confirms all 3 new/extended unit tests pass live on Linux. `requirements.mark-complete SEC-03` applied by hand-edit (checkbox + traceability row). **`cross test -p nono-sandbox-cli` (the plan's own `<verification>` command for the pre-existing `test_from_args_allow_gpu_sets_capability_on_unix` Unix-only wiring test) then caught a real E0063 compile error the canonical clippy gate structurally cannot see** (clippy's default target set is lib+bins, not `#[cfg(test)]` code): a 10th `SupervisorConfig` test-fixture construction site in `supervisor_linux.rs`'s own test module (`make_config()`) was missed by the Task 2 bulk-fix (which grep-anchored on `exec_strategy.rs`'s 9 sites + `supervised_runtime.rs`'s 1 production site, not `supervisor_linux.rs`'s own separate file). Fixed (`f1c35475`); re-ran BOTH mandatory gates with `--all-targets` added (supplementary, beyond the checklist's canonical command, specifically to close this gap class) — both GREEN (linux-gnu 52m45s, apple-darwin 2m04s); `cross test` re-confirmed the previously-failing test now passes. **Standing gap explicitly flagged (not silently absorbed):** `fa21a004`/`8a4237f2`'s full `LinuxSandboxPolicy`/`SeccompPolicy` CLI-policy-selection refactor (21 files, `--sandbox-policy auto|landlock|external` flag) remains unabsorbed — future Wave-3 plans (`112-05` SEC-05, `112-06` SEC-06) touching `linux.rs`/`exec_strategy.rs` should `read_first` this plan's diff before assuming a clean pre-`8a4237f2` baseline. Commits `2495f633`(library)/`85c88cd8`(CLI)/`f1c35475`(Task-3 fix)/`3fd4f29c`/`4fa9489a`/`186c529d`(summary). See `112-02-SUMMARY.md`. Next: remaining Wave 2 plans `112-03` (SEC-04+SEC-08+ADR-112) / `112-04` (RES-02), then Wave 3.

PRIOR (2026-08-05): **v3.6 Phase 112 Plan 01 (SEC-01, RES-01 partial — Wave 1 D-02 reality-check front gate) COMPLETE 2026-08-05.** Re-ran the highest-risk D-02 confirmatory commands live against the current fork tree (not copied from `112-RESEARCH.md`) and produced a single, finalized 18-row disposition table (`112-DISPOSITION-TABLE.md`): SEC-01 (`0ecc476b`) reconfirmed won't-sync (`aws/`/`tls_intercept/` absent, `credential.rs`'s placeholder + `reverse.rs`'s D-15 501 guard unchanged) with a dedicated finding document (`112-AWS-SIGV4-PROXY-AUTH-FINDING.md`) mirroring `109-AWS-SIGV4-TLS-INTERCEPT-FINDING.md`'s shape and correcting `112-CONTEXT.md`'s stale SEC-01 target-file listing; SEC-09 reconfirmed absent (zero grep hits for `command_policies`/`tool_sandbox_runtime`/`tool_sandbox_initial_shim`); SEC-05's `AccessFs::Refer` gap reconfirmed genuinely non-overlapping between the general grant (`linux.rs:365`) and `restrict_execute()`'s independent stacked ruleset (`:1105-1152`, read in full, zero `Refer` calls inside it); SEC-08's `empty_allow_vars_fails_closed` regression guard reconfirmed present at `profile_runtime.rs:989`; D-07 reconfirmed (`cargo audit` exits 0, 0 vulnerabilities, 6 unrelated allowed advisory warnings; `crossbeam-epoch 0.9.20` in `Cargo.lock`) — `373a67ae` NOT re-absorbed. **SEC-02a/b/c** (`9b692e07`/`3c59c62e`/`d033c631`) reality-check evidence captured in `112-OAUTH-CAPTURE-DISPOSITION.md`: confirmed `forward.rs`'s `ResponseRewrite` hook (doc comment: \"Used for OAuth token capture... replace real token fields with phantoms, and only then release the response to the sandboxed client\") is the mechanism keeping real tokens from reaching the sandbox, with no equivalent elsewhere in the fork (`oauth2.rs` is a different feature — `client_credentials` token exchange, not capture); recorded disposition as **deferred -> Phase 114** per the ROADMAP Amendment (2026-08-05) — no adopt/adapt/decline verdict made, that call belongs to Phase 114. RES-01's 4 SHAs (`f0506434`/`7fe0c828`/`0158d52f`/`762eb05b`) each recorded skip-with-reasoning per D-03 (HKLM machine-policy-spine collision, `grep -rln HKLM crates/nono-cli/src/*.rs` → 5 files), plus a flagged-but-out-of-scope UX-staleness finding for `f0506434`'s `always-further` pack-registry-namespace prefix (16 grep hits, UX-only per D-03's skip bias). Zero `crates/` source files touched (confirmed via `git diff --name-only` across all 3 task commits). Commits `a6a4f115`/`b1f17f61`/`a8a85d6f`/`83bcec31`/`c0ab5aca`. See `112-01-SUMMARY.md`. **Wave 1 of Phase 112 is now COMPLETE.** Next: Wave 2 — `112-02` (SEC-03) / `112-03` (SEC-04+SEC-08+ADR-112) / `112-04` (RES-02), parallel, all depend on `112-01`.

PRIOR (2026-08-05): **v3.6 Phase 112 PLANNED 2026-08-05 — 8 plans across 4 waves, READY TO EXECUTE.** Research (`112-RESEARCH.md`, HIGH confidence, every disposition backed by a live `git show`/`grep`/file:line citation rather than commit-subject inference) + pattern map + plans + `gsd-plan-checker` VERIFICATION PASSED (iteration 2; 1 blocker raised and operator-adjudicated, 2 warnings closed). Waves: 1 = `112-01` D-02 reality-check front gate (all 18 dispositions, SEC-01 won't-sync finding, RES-01 skip docs, D-07 confirmation); 2 = `112-02` SEC-03 / `112-03` SEC-04+SEC-08+ADR-112 / `112-04` RES-02 (parallel); 3 = `112-05` SEC-05 / `112-06` SEC-06 / `112-07` SEC-07 (parallel); 4 = `112-08` D-05 ledger addendum + SEC-09 carry-forward + combined verification. All 10 amended requirement IDs and all 8 locked decisions (D-01..D-08, colon form) traced to plans. **Research findings that changed the plan vs. CONTEXT.md's assumptions:** (1) **The three D-04 heavyweights collapse ASYMMETRICALLY.** SEC-07 (`2663e990`) is a genuine clean absorb and kept its own plan/wave; **SEC-01 (`0ecc476b`) collapses hard to won't-sync** — its target subsystems (`nono-proxy/src/aws/*`, `tls_intercept/*`) are 100% absent and `credential.rs`/`reverse.rs` already carry a fork-authored placeholder + 501 guard, so it needs a finding document (mirroring `109-AWS-SIGV4-TLS-INTERCEPT-FINDING.md`), not an absorb wave. This directly contradicts CONTEXT.md's `<code_context>` claim that SEC-09 was the only 'target subsystem absent' pass. (2) **SEC-08 (`a5a441c2`) would RELAX an existing fork guard if adopted verbatim** — the fork already has fail-closed `allow_vars` semantics (`profile_runtime.rs:840-872`, guarded by `empty_allow_vars_fails_closed` at `:989`) from a prior Phase 34 fix, and upstream reverses that default. This fired D-05's sole contingent ADR-112 escalation trigger; `112-03` writes `proj/ADR-112-*.md` preserving fail-closed. (3) **SEC-03 (`a3243907`) is materially larger than D-04's table sized it** — 13 source files including a new `Sandbox::apply_seccomp`/`apply_seccomp_with_abi` core-library API surface, so it was promoted out of the batched bucket into its own plan/wave. (4) **SEC-07 is not a clean drop-in** — `git apply --check` fails and upstream's `CredentialProxyIntent`/`DomainFilterIntent`/`EndpointFilterIntent`/`TlsInterceptIntent`/`UpstreamProxyIntent` imports do not exist in this fork; `112-07` adapts to the fork's real `ProxyLaunchOptions` API and explicitly drops `apply_tls_intercept_config` with a grep-verified acceptance criterion. (5) **SEC-05 confirmed genuinely needed** — the general `AccessFs::Refer` grant (`linux.rs:365`) and `restrict_execute()` (`:1105-1157`) are separate stacked Landlock rulesets, so the general grant does not propagate; this is an additional grant in a stacking layer, not a deny-within-allow expression. (6) Assumption A3 resolved during planning: the two `run_supervisor_loop` definitions are cfg-split (`:2897` non-Linux, `:3111` Linux) with the orphan-reap insertion point at `:3295`. **OPERATOR DECISION 2026-08-05 — SEC-02 CARVED OUT TO A NEW PHASE 114.** The plan-checker escalated a blocker: the plan set deferred SEC-02 (OAuth capture, `9b692e07`/`3c59c62e`/`d033c631`) out of the phase while ROADMAP SC1 read 'All nine security-relevant requirements (SEC-01..SEC-09) are absorbed' and CONTEXT.md D-04 had explicitly rejected a follow-on split — exceeding planner discretion. Rationale for the defer: ~9 of `9b692e07`'s 26 files depend on subsystems absent from the fork (`oauth_capture/` core, `tls_intercept/*`, and `forward.rs` from unabsorbed ancestor `149abde0`), and `forward.rs` carries the response-rewrite hook that keeps real OAuth tokens out of the sandboxed client — a reduced-scope absorb without an equivalent enforcement point ships an unsafe half-feature. Operator chose defer-to-own-phase + amend SC1, mirroring the NET-02/SPIFFE → Phase 113 split. ROADMAP Phase 112 `Requirements` drops SEC-02, SC1 amended to the eight remaining SEC ids with an explicit 'never silently drop it' clause, a dated Amendment line records the decision, and **new Phase 114 (OAuth Capture Absorb)** was added depending on Phases 112+113 with an SC2 forbidding a rewrite-hook-less reduced absorb. Phase 112 still records SEC-02's reality-check evidence and its `deferred → Phase 114` disposition in the D-05 ledger addendum. Commits `e535344d`(plans)/`6eed1b04`(carve-out)/`f374a8a2`(index). **Next: `/gsd:execute-phase 112`.**

PRIOR (2026-08-04): **v3.6 Phase 111 Plan 06 (RLS-14, sibling-repo half of the 0.70.0 version leapfrog) COMPLETE 2026-08-04 — PHASE 111 FULLY CLOSED.** Bumped `../nono-py` (Cargo.toml, pyproject.toml, Cargo.lock) and `../nono-ts` (Cargo.toml incl. correcting the live `nono` path-dep `version = \"0.66\"` -> `\"0.70\"` semver requirement, package.json incl. all 5 optionalDependencies platform pins, 4 existing npm/*/package.json files, Cargo.lock) to `0.70.0`, each committed with its own DCO sign-off in its own repo's git history (`c24b20b` nono-py, `6406096` nono-ts). Both bindings rebuild clean (`maturin build`, `npx napi build --platform --release`, both exit 0, no struct-drift fix required). **Found and fixed a real regression while running Task 3's release gate:** `scripts/release-dry-run.ps1`'s `PRE_PUBLISH_REGISTRY_BLOCKED` detection regex only matched cargo's pre-rename `failed to select a version for the requirement` phrasing; since the Phase 102/103 fork-owned rename to `nono-sandbox` (a name never published under any version), cargo now reports `no matching package named` instead — a phrasing the regex never matched, so the gate exited 1 (hard FAIL) on the very first live run since the rename. Widened the regex to match both phrasings (`be3d0ecc`); re-run confirmed the documented verdict: `PASS: Hard failures: 0. Blocked: 2 (pre-publish). Skipped: 1 (toolchain absent).` `pwsh -File scripts/verify-dark.ps1 -Gate release-readiness` re-confirmed `PASS` at `0.70.0`/`0.69.0`. D-07 upheld: no tag pushed (two pre-existing local `v0.7.0`/`v0.70.0` tags predate this session by months, unrelated), no live publish/upload command executed anywhere. `requirements.mark-complete RLS-14` applied via hand-edit of `.planning/REQUIREMENTS.md` (both checkbox and traceability row) — RLS-14 is now Complete, satisfied jointly by 111-05 (in-repo half) and this plan (sibling-repo half). Commits `c24b20b`(nono-py)/`6406096`(nono-ts)/`be3d0ecc`/`684e97d1`(this repo). See `111-06-SUMMARY.md`. **All 4 of Phase 111's requirements (CORE-01, CORE-02, VERIFY-01, RLS-14) are now Complete — Phase 111 is CLOSED.** Next: Phase 112 (Security + Residual) needs `/gsd:plan-phase 112`.

PRIOR (2026-08-05): **v3.6 Phase 111 Plan 05 (RLS-14, in-repo half of the 0.70.0 version leapfrog) COMPLETE 2026-08-05.** Bumped all 6 fork-owned workspace crates (`nono-sandbox`, `nono-sandbox-cli`, `nono-sandbox-proxy`, `nono-shell-broker`, `nono-fltmgr-client`, `nono-ffi`) + their 6 internal path-dependency version pins from `0.66.1` to `0.70.0`, regenerated `Cargo.lock` via `cargo build --workspace --all-targets` (exits 0; `git diff Cargo.lock` touches only the 6 bumped packages, zero third-party drift); `tools/sign-fixture` left untouched at its independent `0.1.0`. Corrected both hardcoded-version release-gate scripts — `scripts/gates/release-readiness.ps1`'s `$targetVersion`/`$upstreamHighest` now `0.70.0`/`0.69.0` (plus 2 stale inline comments describing the same assertions, Rule 1), and `scripts/release-dry-run.ps1`'s 4 cosmetic `0.66.1`/`0.66.0` references corrected — confirmed via `pwsh -File scripts/verify-dark.ps1 -Gate release-readiness` returning verdict `PASS` with `version_family` = \"0.70.0 confirmed for all 6 version-family crates\" and `leapfrog` = \"0.70.0 > 0.69.0 (leapfrog confirmed)\". D-07 upheld: no tag pushed, no live `cargo publish` executed. `requirements.mark-complete RLS-14` deliberately NOT run — RLS-14 is split across 111-05 (in-repo half, this plan) and 111-06 (sibling-repo half: `../nono-py` + `../nono-ts` bump, including correcting `../nono-ts`'s live `version = \"0.66\"` path-dep pin) — same split-requirement pattern as Phase 110's PROF-03 (4 sub-plans) and Phase 111's own CORE-02 (111-02/111-03); the SDK's auto-flip was caught and reverted via hand-edit of `.planning/REQUIREMENTS.md` before commit. Commits `502403ba`/`d1c0ad58`/`29729b46`/`5808b405`. See `111-05-SUMMARY.md`. **Wave 3 of Phase 111 is now COMPLETE.** Next: `111-06` (Wave 4, RLS-14 sibling-repo half + prepare-only release gate GREEN).

PRIOR (2026-08-04): **v3.6 Phase 111 Plan 04 (VERIFY-01, combined 108-111 fork-invariant pass) COMPLETE 2026-08-04.** Both mandatory cross-target clippy gates (linux-gnu via `cross clippy`, apple-darwin via `cargo-zigbuild clippy`) re-ran GREEN with zero findings over the tree with Wave 1 (111-01/02/03) merged, satisfying D-09's 'combined 108-111 surface' requirement. D-01 confirmed still holding (no `pub mod resource;` in `crates/nono/src/lib.rs`); ADR-86's Windows carve-out (`exec_strategy_windows/`) confirmed structurally unregressed via empty `git diff` since the pre-Wave-1 base SHA. `make ci` substitution set run (native clippy, `fmt --check`, `cargo audit`, `test -p nono-sandbox`/`nono-sandbox-cli`/`nono-ffi`) — all GREEN except `nono-sandbox-cli --bin nono`'s 11 pre-existing failures, matching the documented baseline exactly. **The first `cargo test --workspace --no-fail-fast` attempt was silently killed by this harness's timeout-auto-promotion-to-background behavior mid-`env_vars.rs`** (no process/log growth for ~39min) — redone explicitly with `run_in_background:true`, which completed end-to-end (`env_vars.rs` took 2702s/45min this run vs 986s/16.4min in `110-08`, consistent with that binary's already-documented host-state-contention flakiness). The full sweep's 17-name failing-test set is a confirmed **strict subset** of the documented 24-name baseline — zero new failure names, diffed name-by-name, honestly. All D-09 assertion-3 resource-flag regression guard tests pass. Both sibling bindings (`../nono-py` maturin build, `../nono-ts` napi build --platform --release) rebuild clean with no fix required at the current pre-bump `0.66.1` version. `111-04-VERIFICATION-NOTES.md` written, explicitly superseding `110-08-VERIFICATION-NOTES.md` per D-09. `requirements.mark-complete VERIFY-01` run. Commits `d7928c07`/`f027805e`/`ea3990ad`. See `111-04-SUMMARY.md`. **Wave 2 of Phase 111 is now COMPLETE.** Next: `111-05` (Wave 3, RLS-14 in-repo `0.70.0` version leapfrog).

PRIOR (2026-08-04): **v3.6 Phase 111 Plan 03 (CORE-02, other half) COMPLETE 2026-08-04.** Wrote `proj/ADR-111-resource-limits-boundary.md` settling D-01's ADAPT-not-adopt disposition of upstream `e6d26871`/#1269 (core `resource` module) and its sibling `34c2c975`/#1403, mirroring ADR-108's shape and explicitly answering (not merely asserting) why ADR-86's audit/diagnostics carve-out does not extend to resource-limit enforcement — audit/diagnostics are read-only observability primitives, resource limits are the enforcement mechanism itself. Appended a 'Phase 111 Standing Divergence Addendum' to the closed `108-DIVERGENCE-LEDGER.md` as a deliberate, locked exception to its own 'no further plan is expected to append' declaration, recording the divergence as **permanent** (not deferred), explicitly distinguished from the tool-sandbox-surface cluster's `DEFERRED->v3.7` treatment. `CORE-02` marked Complete in `.planning/REQUIREMENTS.md` now that both split halves (111-02 help-text, 111-03 ADR+ledger) have landed, per the Phase 110 PROF-03 split-requirement precedent 111-02 explicitly handed off. Commits `10194152`/`6bf65687`/`a56fbfb4`/`78072057`. See `111-03-SUMMARY.md`. Wave 1 of Phase 111 (111-01/02/03) is now fully COMPLETE. Next: `111-04` (Wave 2, VERIFY-01 combined 108-111 fork-invariant pass).

PRIOR (2026-08-04): **v3.6 Phase 111 PLANNED 2026-08-04 — 6 plans across 4 waves, READY TO EXECUTE.** Research (`111-RESEARCH.md`, 830 lines, HIGH confidence, every claim file:line or git-SHA grounded) + plans + `gsd-plan-checker` VERIFICATION PASSED with 0 blockers. Waves: 1 = `111-01` CORE-01 carry / `111-02` CORE-02 help-text correction / `111-03` ADR-111 + ledger addendum (parallel); 2 = `111-04` VERIFY-01 combined 108-111 fork-invariant pass; 3 = `111-05` in-repo `0.70.0` bump; 4 = `111-06` sibling-repo bump + prepare-only release gate. All 4 requirements (CORE-01/CORE-02/VERIFY-01/RLS-14) and all 10 locked decisions (D-01..D-10, colon form) traced to plans. **Research findings that changed the plan vs. CONTEXT.md's assumptions:** (1) #1378's real payload is a **single JSON line** in `policy.json`'s `user_caches_macos` group — 4 of upstream `ca888108`'s 6 touched files don't exist under those names in this fork (`macos_trust.rs`/`proxy_command.rs`/`tls_intercept/ca.rs`) or are unrelated clippy cosmetics (`learn.rs`), so a cherry-pick would fail or corrupt; the plan hand-edits instead. (2) The macOS memory caveat is **worse than D-06 knew** — `setrlimit(RLIMIT_AS)` frequently fails outright with `EINVAL` on Apple Silicon and the failure is silently swallowed, so the corrected help text must say **best-effort**, not merely 'address space not RSS'. (3) `scripts/gates/release-readiness.ps1` **hardcodes** `$targetVersion='0.66.1'`/`$upstreamHighest='0.66.0'` — left unedited the release gate asserts against the wrong version and **passes vacuously** (the exact Phase 110 defect class); `111-05` Task 2 edits it in the same plan as the bump. (4) `crates/nono-fltmgr-client` is the 6th version-family crate; `tools/sign-fixture` is a 7th workspace member that must **NOT** be bumped (independent `0.1.0`). (5) `../nono-ts/Cargo.toml`'s `nono` path-dep carries a **live** `version = \"0.66\"` semver requirement that breaks resolution at `0.70.0` — corrected distinctly from `../nono-py`'s version-key-less path-deps. (6) `cargo audit` already exits 0 (crossbeam-epoch RUSTSEC-2026-0204 closed out-of-band pre-phase). The checker's 3 non-blocking warnings were applied before commit (real build/grep commands wired into `111-04-03`/`111-02-02`/`111-03-01`/`111-03-02`; `111-VALIDATION.md` flipped `draft`→`approved`, `nyquist_compliant: true`, task map bound to real IDs). **Next: `/gsd:execute-phase 111`.**

PRIOR (2026-08-04): **v3.6 Phase 110 CLOSED — 8/8 plans, all 4 requirements complete.** Plan 06's `checkpoint:human-verify` (PROF-03e) was resolved live on an Administrator-elevated session: the port range reaches the kernel as **4 native `FWP_MATCH_RANGE` conditions at `49200..49210`** (one per layer, `ALE_USER_ID`-scoped to the child), **8 filters installed from a verified-zero baseline** where a per-port unroll would have installed 48, torn down to **0** on exit — full `0 → 8 → 0` lifecycle. Behavioural inside/outside connect probe **NOT RUN** (`0xC0000142`: neither `powershell` nor `curl.exe` starts under the AppContainer with this minimal profile, and an unconfined control returned 28/28 so the probe design could not have separated the cases); closure accepted on the filter-table proof, which is what Task 3 specifies. Verdict + limitations: `110-06-PROF-03e-VERDICT.md`; procedure: `110-06-CHECKPOINT-RUNBOOK.md`. **Running the checkpoint surfaced 4 real defects, all fixed, 3 proven live:** (1) `7c7a189c` `has_port_rules()` omitted `localhost_port_ranges` so `select_network_backend` fell to its catch-all and range-only profiles failed closed before reaching the service; (2) `ea26b5b2` `cmd_show`'s `has_net` omitted the range fields so `profile show` emitted no `Network:` section at all; (3) `6d7ef719` **version-skew fail-open** — a `nono-wfp-service` older than the CLI silently dropped `localhost_port_ranges`, built zero filter specs, installed nothing and returned `enforced-pending-cleanup` (run exited 0 unenforced); fixed by bumping `WFP_RUNTIME_PROTOCOL_VERSION` to 2 **and** adding `installed_filter_count` validation, since a matching version can still under-install; (4) `4aec1944` **the filter sweep never enumerated anything** — `FWPM_FILTER_ENUM_TEMPLATE0.layerKey` is a `GUID` **by value**, so the zeroed template enumerated the all-zero GUID rather than 'all layers', and `FWP_FILTER_ENUM_FULLY_CONTAINED` with zero conditions compounded it; neither `--purge-wfp-objects` nor the startup sweep could ever remove a real nono filter, and the sweep additionally short-circuited when the sublayer was absent, making survivors permanently unreachable. **Next: `/gsd:plan-phase 111`** (macOS/core carry, resource CLI, fork-invariant verify, `0.70.0` leapfrog). Note `110-08`'s phase-gate certification predates these 4 fixes — both cross-target clippy gates were re-run GREEN on every one of them, but the artifact itself describes a tree that no longer exists.

PRIOR (2026-07-30): **Phase 110 Plan 08 COMPLETE — phase-gate verification only, Phase 110 was then still OPEN.** Both mandatory cross-target clippy gates (linux-gnu via `cross clippy`, apple-darwin via `cargo-zigbuild clippy`) confirmed GREEN live, zero findings. Both sibling bindings (`../nono-py` maturin build, `../nono-ts` napi build --platform --release) rebuilt green with NO fix required (`localhost_port_ranges` is a private `CapabilitySet` field with builder/accessor-only surface, unlike Phase 109's pub-field `RouteConfig` E0063). All 3 fork-invariant checks confirmed with live grep/diff/test evidence: SC3 discrete-`Vec<u16>` back-compat unregressed, ADR-86 policy-free-library boundary unregressed (with a correction to the plan's own interfaces text — 110-05 does touch `crates/nono/src/manifest_convert.rs`, confirmed pure input validation not policy), and all 4 Wave-0 validation gaps closed with live passing test commands. `cargo test --workspace` does NOT exit 0 on this host — the 11 documented pre-existing `-bin nono` failures confirmed matching exactly, plus `--no-fail-fast` surfaced 13 MORE previously-unobserved-but-confirmed-pre-existing failures across `audit_attestation.rs`/`env_vars.rs`/`resl_nix_async_signal_safety.rs` (none intersect any 110-0X commit per `git log --all` per file; logged honestly to `deferred-items.md` § Plan 08, not fixed — Scope Boundary). Commits `b8c9de96`/`aa7def31`/`b8e2d790`/`397f389a`. See `110-08-SUMMARY.md`/`110-08-VERIFICATION-NOTES.md`. **PROF-03 NOT marked complete; Phase 110 checklist box NOT flipped.** Plan 06 Task 3 (`checkpoint:human-verify`, PROF-03e live-kernel FwpmFilterAdd0 proof) remains the sole blocker to closing Phase 110 — requires Administrator-elevated operator action not yet taken. Next: operator resolves 110-06's checkpoint, then run `requirements.mark-complete PROF-03` and flip the Phase 110 ROADMAP checklist box."
last_updated: "2026-08-11T00:00:00.000Z"
last_activity: 2026-08-09
resume_file: ".planning/phases/117-fail-direction-contract-startup-self-attestation/117-01-PLAN.md"
progress:
  total_phases: 6
  completed_phases: 2
  total_plans: 24
  completed_plans: 12
  percent: 33
parallel_milestone: v3.5
parallel_milestone_name: Trusted Signing Go-Live + First Distributed Release
parallel_milestone_status: paused-on-operator-tag-push-decision-and-red-board
parallel_stopped_at: "**v3.5 Phase 104 Plan 03 Task 2 FAILED (RED, diagnosed) — a NEW Azure blocker.** Sequence: (1) reconciled the stale 'held-on-external-azure-block' status after verifying the 2026-07-04 fix was real; (2) amended `104-03-PLAN.md` to retire the moot root-CTL pre-check gate (commit `8fb99f01`); (3) ran Task 1 pre-flight — release-readiness PASS, publish-selector PASS; (4) dispatched a fresh smoke run per Pitfall 104-B -> run `30460949560` **failed at Sign with HTTP 403**, a different step than any prior failure. Config and RBAC verified correct via ARM; the lead is a **lapsed identity validation** (cert rotation stopped 2026-07-15; newest 3-day cert expired 2026-07-18; none minted in 14 days). Needs an Azure Portal check the corporate host cannot perform. Evidence: `104-03-SMOKE-403-FINDING.md` (commit `28d950d2`). **Tag `v0.66.1` NOT pushed — correctly blocked.** Phases 101-105 dirs intact; Phase 105 fully planned (5 plans, 0 summaries) and waiting behind the tag. Phases 106/107 not yet built. **UPDATE 2026-08-16 — THE AZURE BLOCKER IS CLEARED.** Smoke run `31947685981` came back GREEN: `Status: Valid`, issuer `CN=Microsoft ID Verified CS EOC CA 04, O=Microsoft Corporation, C=US`, with `Pre-sign status: NotSigned` proving it actually signed. The operator's Portal-side identity-validation fix took; CI had simply not re-run since 2026-07-29. SIGN-03 is now Complete (see `REQUIREMENTS.md`). **v3.5 REMAINS PAUSED — but on different grounds:** (1) the irreversible `v0.66.1` tag push still needs explicit operator authorization, and (2) the board is red — Windows Packaging plus 5 Integration suites. Azure is no longer a gate. No Phase 104/105/106/107 checkbox or phase status was changed."
---

# Project State: nono — v3.7 OPENED 2026-08-08 (planning); v3.5 open + paused in parallel

> **v3.6 (UPST12 Upstream Sync v0.66.0 → v0.69.0, Phases 108-114) SHIPPED + ARCHIVED 2026-08-08.**
> 7 phases, 51 plans, 343 commits, 11 days. All 23 requirements Complete; milestone audit
> `passed_with_accepted_risk`. Tag `v3.6` created **LOCAL only** — deliberately not pushed.
> Archives: `milestones/v3.6-ROADMAP.md`, `v3.6-REQUIREMENTS.md`, `v3.6-MILESTONE-AUDIT.md`.
> Carried to v3.7: NEW-01, NEW-03, NEW-05, NEW-06, NEW-07, NEW-08 (see the audit).
> **The v3.6 execution narrative below is retained as historical record, not current position.**

## Project Reference

See: `.planning/PROJECT.md`. Roadmap: `.planning/ROADMAP.md` (**v3.7 = Phases 115+, ACTIVE**; v3.5 = Phases 101-107, open/paused; v3.6 = Phases 108-114, shipped + collapsed). Requirements: `.planning/REQUIREMENTS.md` — carries **both** the open v3.5 section and the new v3.7 section; the v3.6 section was excised to `milestones/v3.6-REQUIREMENTS.md` at close rather than the file being deleted, since the stock `git rm` would have destroyed active v3.5 tracking. **Append to these two files, never overwrite them** while two milestones are open. Phase numbering continues: v3.5 owns 101-107 → v3.6 owned **108-114** → v3.7 owns **115+** (no reset). Phases 112-114 were added after the original 12-requirement mapping: 112 + 113 by operator approval of the Phase 108 ledger's D-18/D-19 coverage-gap amendment and the NET-02 split; **114 by operator decision 2026-08-05 carving SEC-02 out of Phase 112**.

**Core Value (v3.7):** Observation of enforcement must be as trustworthy as the enforcement. The fork's Windows guarantee is deny-by-*composition* — the intersection of integrity level, AppContainer profile, DACL, and WFP — not upstream's single deny-by-construction kernel gate. A composite fails at its seams, has ambiguous fail-direction, and is harder to attest; v3.7 makes it **prove** it is enforcing rather than adding another layer.

**Current Focus:** **v3.7 OPENED 2026-08-08 — defining requirements.** Phases 115+. Three workstreams (Composite Integrity / Tool-Sandbox Disposition / v3.6 carry-forward drain). No phase planned yet.

**Historical (v3.6):** Core value was keeping the fork current with upstream `nolabs-ai/nono` without regressing the Windows security model or the ADR-86 boundary — a routine drain-then-sync (mirrors v3.1/v3.3/v3.4). Phase 114 completed 2026-08-07; all 7 phases CLOSED; SEC-02 resolved ADAPTED-with-scope-limit; 9 Warning + 6 Info findings left open in `114-REVIEW.md` by operator scoping.

> **Note on `progress.percent`:** it is **phase-based** (7 of 7 = 100%), not plan-based. `total_plans: 51` is now a genuine total for the first time — every v3.6 phase is planned (108=5, 109=5, 110=8, 111=6, 112=8, 113=8, 114=11). It was previously a floor while 113/114 were unbuilt, and it was also stale: Phase 113's 8 completed plans were never added at close.

**Phase 112 — Security + Residual Sync — CLOSED 2026-08-05** (SEC-01, SEC-03..SEC-09, RES-01, RES-02 — **SEC-02 carved out to Phase 114**). 8/8 plans, code-reviewed, verified `human_needed`, operator-approved. Execution history: **Wave 1 (112-01) + Wave 2 (112-02 SEC-03 / 112-03 SEC-04+SEC-08+ADR-112 / 112-04 RES-02) + Wave 3 (112-05 SEC-05 / 112-06 SEC-06 / 112-07 SEC-07) ALL COMPLETE 2026-08-05:** 8 plans, 4 waves, plan-checker VERIFICATION PASSED. Absorbs the 18-commit `security-residual-and-misc` cluster from the `v0.66.0..v0.69.0` window under a fork-invariant review kept distinct from any release-cut phase (v3.1 Phase 87 precedent). Wave 1's D-02 front gate finalized all 18 dispositions with live-re-run evidence; 112-02 delivered SEC-03 as an ADAPT (not the table's literal adopt — see stopped_at) after discovering `a3243907` depends on the unabsorbed `fa21a004`/`8a4237f2` refactor; 112-03 delivered SEC-04 as a clean adopt (trust-policy predicate discriminator) and closed SEC-08 by writing ADR-112 to explicitly PRESERVE the fork's fail-closed `allow_vars` default instead of adopting upstream's looser reinterpretation; 112-04 delivered RES-02's `503045801a` (adopt, PTY teardown drain) and `9840a16f35` (adopt-adapted, test tightening) verbatim/adapted, and corrected `112-DISPOSITION-TABLE.md`'s `4cc0af2c52` call from file-presence-only 'adapt with caution, or skip' to a symbol-level-verified SKIP (target `socket_test_dir()` absent from the fork entirely). **Wave 2 of Phase 112 is now COMPLETE.** 112-05 delivered SEC-05 as a clean adopt (Landlock `Refer` grant, live `cross test`-confirmed). 112-06 delivered SEC-06 as an ADAPT (seccomp supervisor-ancestry orphan reaping, `PR_SET_CHILD_SUBREAPER` + `reap_reparented_orphans`, RED/GREEN TDD live via `cross test`). 112-07 delivered SEC-07 as an ADAPT (standalone `nono proxy` command, built on the fork's real `ProxyLaunchOptions` API instead of upstream's absent Intent structs) and, via Task 2's mandatory disposition-verification pass, discovered and closed a genuine security gap not itemized in the plan text: the fork had no `--no-auth` concept at all before this plan, and the pre-existing CONNECT-tunnel auth check was unconditionally lenient — a real bypass for a standalone (no-OS-sandbox) proxy left on non-loopback with auth nominally enabled. Added `ProxyConfig.require_auth`/`strict_connect_auth` (both default to the fork's pre-existing behavior, confirmed byte-for-byte via 218/218 unchanged `nono-sandbox-proxy` unit tests). **Wave 3 of Phase 112 is now COMPLETE** (112-05/112-06/112-07 all done). Next: Wave 4 — `112-08` (D-05 ledger addendum + SEC-09 carry-forward note + combined verification), the final plan of Phase 112. Phase 111 is CLOSED (6/6 plans, all 4 requirements). Remaining after 112: Phase 113 (SPIFFE/SPIRE, NET-02) and Phase 114 (OAuth Capture, SEC-02). tool-sandbox subsystem remains EXCLUDED → v3.7, and Phase 112's SEC-09 carry-forward note (still owed by `112-08`) files a conscious-decision obligation against that future work.

## Parallel Milestone (PAUSED): v3.5 — Trusted Signing Go-Live

**Status corrected 2026-07-29.** v3.5 was previously recorded as *held on an external Azure root-propagation block*. **That block is resolved** — re-verified live this session:

| Evidence | Observed 2026-07-29 |
|---|---|
| `gh variable list --repo OscarMackJr/nono` | `TRUSTED_SIGNING_PROFILE = NonoPublicTrust`, updated **2026-07-04T02:28:46Z** |
| `gh run view 28692172728` | **success**, 2026-07-04T02:29:15Z, "Trusted Signing Smoke Test" |
| `git tag --list 'v0.66*'` | only `v0.66.0` (upstream) — **fork tag `v0.66.1` was never pushed** |

Root cause was never CTL propagation: `NonoCertProfile` was `profileType = PrivateTrustCIPolicy` (a WDAC/code-integrity private hierarchy that chains to `Microsoft Enterprise ID Root CA 2021` and is *by design* never in the public CTL), despite the portal labelling it PublicTrust. Fix applied 2026-07-04: a **new** profile `NonoPublicTrust` (`profileType = PublicTrust`) against the **public** identity validation `20cb70d3-2d17-4fdb-9121-963628df6b63` (the private `25e8c70f…` was rejected with *"PublicTrust certificate requires Public identity validation"*), then `TRUSTED_SIGNING_PROFILE` repointed. Smoke then went GREEN: `Status=Valid`, issuer `CN=Microsoft ID Verified CS EOC CA 03` (real public root), signer `CN=TWG Global`. Full detail: memory `azure_trusted_signing_golive`.

**Consequences for resuming v3.5:**

- `scripts/azure/check-trusted-signing-root.ps1` (the root-CTL pre-check) is **moot** — it probes the *private* root's thumbprint `991D364E…`, which will never appear in the public CTL and is no longer the chain in use. Do NOT gate Plan 104-03 Task 2 on it.
- The **only** remaining gate on Plan 104-03 is operator authorization for the irreversible `v0.66.1` tag push.
- Phase 105 (live multi-registry publish) is **fully planned** — 5 PLAN files, 0 SUMMARYs — and unblocks the moment the tag lands.
- Phases 106/107 not yet built.

v3.5 phases 101-105 remain live under `.planning/phases/`; `phases.clear` was deliberately NOT run. Runbook: `.planning/quick/260630-trusted-signing-golive/AZURE-TRUSTED-SIGNING-GOLIVE-COOKBOOK.md`.

## Current Position

Milestone: **v3.7 — Composite Integrity + Tool-Sandbox Disposition** (opened 2026-08-08)
Phase: **117 — Fail-Direction Contract + Startup Self-Attestation** — gap-closure waves 6-8 ALL EXECUTED 2026-08-10 (waves 1-5 shipped 12/12 plans; wave 6 `117-13`..`117-17` EXECUTED; wave 7 `117-18` EXECUTED; wave 8 `117-19`, the gap-closure round's final plan, EXECUTED this session — phase-level verification/close-out NOT run by this executor, left for the orchestrator)
Plan: `117-19` (wave 8, final gap-closure plan) — 3/3 tasks complete, sequentially on the main tree (`workflow.use_worktrees=false`). Closes SC4: the SPEC's own "Review-fix pass" table (`proj/SPEC-windows-fail-direction-contract.md`) — the standing artifact D-15 exists specifically to prevent "quietly reconciled" drift — had no row for 3 of 5 iteration-2 fixes and its RF-14 row described pre-NR-06 behavior. Task 1 ported NR-04/NR-05/NR-06 (each with its written Honest-limit caveat, read fresh from `117-REVIEW-FIX.md`) and corrected RF-14 to describe the shipped three-defense staging guard. Task 2 added NR3-01/02/03/04/05/08 rows sourced from `117-13`..`117-17`'s SUMMARY.md files (what actually shipped, not the review's literal suggestion — NR3-05's row states Plan 117-17's deviation, deletion over wiring a real downgrade path, and why), and converted the `## Layer registry` table's 4 flagged citations to the symbol-form strings Plan 117-15 landed in `layer_registry.rs` (read fresh from current source, not reused from the plan's own now-stale `<interfaces>` block). Task 3 reduced the manual-verification section from 11 to Plan 117-18's exact 3-row remainder (`DaclSessionSidGrant`, `MinifilterAbsence`, `BrokerAuthenticodeTrustGate`), each framed as an OPERATOR-ACCEPTED DOWNGRADE citing the session's locked SC3 decision, plus a stated 10-of-13 total. All 10 combined `layer_registry_selfcheck`/`layer_registry_meta_test` tests green against the fully-edited document. **CINT-01 left `Pending`, not flipped to Complete** — a spot-check of `restricted_token.rs:55` (a citation this plan did NOT touch, since NR3-08 flagged only 4 of 13 rows) found it, too, no longer points at the enforcing call site, so CINT-01's full citation-accuracy bar remains unmet across the whole registry even though this plan's own scoped rows are now accurate. CINT-03 left `Pending` per orchestrator instruction (no registry-row-count change this plan). Commits `dafae5c1` (Task 1), `215b611f` (Task 2), `c8fdf259` (Task 3), `2013e2b5` (docs: SUMMARY). See `117-19-SUMMARY.md`. **Next: orchestrator phase-level verification of 117's gap-closure round (waves 6-8 complete).**
Status: Milestone stands at 2/6 phases fully shipped (115, 116); Phase 117 originally shipped 12/12 plans (waves 1-5) then re-opened for gap-closure (waves 6-8, 7 new plans `117-13`..`117-19`) against `117-VERIFICATION.md` findings — waves 6-8 (all 7 gap-closure plans) now fully executed; phase-level completion/verification is pending the orchestrator's own pass, NOT flipped by this executor.
Last activity: 2026-09-06 — **Phase 118 close-out: Task 3's human-verify gate FAILED, and the failure was the point.** A confined child read receipt files on BOTH arms; root cause is that D-08's two guards are directory-scoped (`NO_INHERITANCE` + no `(OI)(CI)`) and neither reaches the FILES, plus the label is pinned to a LOW RID so a Low-IL child is EQUAL not below. Operator narrowed D-08 to write-integrity only. `/gsd:code-review` then found 6 Criticals — **all 6 fixed and independently re-verified, with 3 of them (CR-01, CR-03, CR-06) instances of the SAME unenumerated-terminal-exit class the phase had already hit once at 118-06**. CR-06 was the sharpest: broker receipts had been landing in `<workdir>\.nono-runtime\programdata\...` since 2026-08-16 because `broker_env_pairs` cloned the CHILD's sanitized env, in which `append_windows_runtime_env` had rewritten `PROGRAMDATA` — so RCPT-01 was violated on the broker arm for three weeks with no error, no warning and no missing file at the expected location. `make ci` is GREEN for the first time this phase. **Two things I got wrong and corrected in the record:** (1) I eliminated the correct CR-06 hypothesis on faulty evidence (assumed the child env was inherited-or-cleared; it was REWRITTEN, and my confirming sweep never covered the workdir); (2) I reported CR-05 "fixed and verified" from a 16→16 live measurement that only exercised the WriteRestricted arm — the broker producer is still unfixed. ⚠ **OPEN: CR-05 on `nono-shell-broker` (fix in flight), and the RCPT-02 requirement-text divergence needs an operator call.** Prior activity: 2026-09-05 — Quick task `260904-wkv`: consumer-side **spec-and-scaffold only** for the Nono↔Popeye Session Credential Contract v0.1 (design note + D-5 `endpoint-receipt/0.1` proposal + `crates/nono/src/session_credential.rs` + 6 REQ-named tests). D-1..D-5 left undecided; 4 conflicts recorded UNRESOLVED (the sharpest: `receipt.rs` D-20's "no mid-session field … or will be added" vs the contract's session window + renewal chain). ⚠️ Discovered along the way and **not caused by this task**: `make clippy`/`make ci` is **already RED on this branch** — 2 `dead_code` errors on `ReceiptWriter::session_id`/`file_path` in `crates/nono-cli/src/receipt_sink.rs`, a paused-**Phase-118** file (nothing outside `crates/nono` references `session_credential`; this task's commits touched only 2 core files). Phase 118's human-verify gate is UNTOUCHED and still open. Prior activity: 2026-08-15 — **CI-RED TRIAGE COMPLETE: fail-open gate fixed, all 19 failures diagnosed and fixed, NO product defect found.** Sequence: (1) `260815-f2x` fixed the fail-open gate — it reported green while 22 tests failed; (2) `/gsd:debug ci-windows-117-failures` proved 3 independent causes (15 elevated-runner ownership-gate artifacts, 3 hardcoded-`/tmp` test defects, 1 stale broker contract) with a per-test disposition table; (3) `260815-gfd` fixed all 19, test-code only, zero regressions (1694/12 = documented baseline). ⚠ **STILL OPEN — the two things that matter most:** (a) **cluster A is UNPROVEN until CI runs** — this host cannot construct the elevated condition, so local green proves only harmlessness, not efficacy; (b) **a NEW coverage gap** — normalising the `rb3_gate_tests` fixture means CI now covers the R-B3 gate's PASS branch while **nothing covers the elevated-workspace FAIL branch an elevated operator actually hits**; that plus the CR-01/WR-02 CI-darkness question is routed to planned work, NOT patched. ⚠ **ALSO STILL RED AND UNTRIAGED (item 3, not started):** Clippy on ALL THREE hosts + both Linux cross-compile gates + Integration Tests + Docs Checks — the cross-target debt CLAUDE.md's local-verification rule exists to prevent. Prior activity: 2026-08-15 — **FIRST CI RUN EVER ON THIS BRANCH (31888431118) — conclusion FAILURE, and it immediately paid for itself.** Pushed 687 commits (Phases 115/116/117; origin was at `899b1382` from 2026-07-29). ⚠ **The push triggered NO CI** — `ci.yml` fires only on push/PR to `main`, so every milestone branch here develops with ZERO CI coverage; had to `workflow_dispatch` manually. **GOOD NEWS: the WR-20 pin PASSED on the elevated runner** (`non_owned_path_with_a_foreign_label_is_exempt_not_a_coverage_gap ... ok`) — Phase 117's last human item is answered, and the `config::tests` env races all passed under `--test-threads=1` as predicted. **BAD NEWS, TWO ITEMS:** (1) `windows-layer-fault-injection` was GREEN while reporting 22 failures — a fail-open gate, fixed by quick task `260815-f2x` (see below), and the fix is UNEXERCISED so expect a correct RED next push; (2) **19 of the 22 failures are Phase 117's OWN tests** — `labels_guard`/`dacl_guard` residue, coverage and CR-01/WR-02 regression pins, plus `hook_runtime_windows` and `policy` — NOT the documented dev-host baseline (only the 3 `protected_paths` overlap). Representative: `labels_guard_mixed_owned_and_non_owned...` got `LabelCoverage { applied: 0 }`, expected 1. **HYPOTHESIS ONLY, UNPROVEN: CI runs as elevated `runneradmin`, so ownership/label semantics differ from the non-elevated domain account these tests were written against.** TRIAGE IS THE OPEN FOLLOW-UP — until it is answered we do not know whether these are environment artifacts or product defects. Separately RED and untriaged: Clippy on ALL THREE hosts + both Linux cross-compile gates + Integration Tests + Docs Checks — the cross-target debt CLAUDE.md's local-verification rule exists to prevent. Prior activity: 2026-08-15 — **PHASE 117 CLOSED OUT** via quick task `260815-d22`: `ROADMAP.md` Phase 117 flipped `[x]` (Progress row 34/34, Complete, 2026-08-15) and `REQUIREMENTS.md` CINT-01/02/03 flipped to Complete, with CINT-03 explicitly marked "via operator override … accepted, not met" so the table cannot be misread as a clean pass. Also corrected a misleading record: the WR-20 pin is **already covered by the existing `windows-layer-fault-injection` CI job by construction** (test is not feature-gated; the job's cargo invocation has no target filter) — it needs a RUN, not new wiring, and the run is blocked only on the branch being 684 commits unpushed. **THE ONE OPEN OPERATOR DECISION: whether to push this branch.** Nothing has been pushed; origin tip is still `899b1382` (2026-07-29), which predates all of Phases 115/116/117. Prior activity: 2026-08-15 — quick task `260815-b0s`: closed Phase 117's SC4 recording gap (all four `missing` bullets from `117-VERIFICATION.md`) and applied the operator-accepted SC3 override (`accepted_by: Oscar Mack Jr`, `2026-08-15`). `117-VERIFICATION.md` now reads `status: human_needed`, `overrides_applied: 1`, SC3 ACCEPTED-VIA-OVERRIDE (still factually 10/13 — the override accepts a disclosed shortfall, it does not erase it), SC4 VERIFIED-with-disclosed-limits. Two new gates: `module_doc_assigns_each_claimed_row_to_the_list_it_is_on` (sentence-scoped; 5 claims verified, 13 mentions equality-pinned) and `every_marker_carrying_source_file_is_in_marker_sources` (allow-list → discovered class). 4 `OPEN` markers now resolve, up from 2. Remaining human item is unchanged and pre-existing: the elevated/CI Windows run of the WR-20 pin. Prior activity: 2026-08-10 — Plan `117-19` executed (NR-04/05/06 + NR3-01/02/03/04/05/08 rows ported into the SPEC's standing review-fix ledger; RF-14 corrected; 4 registry-table citations converted to symbol form).

Phase numbering continues from 114 → **115+**. v3.5 (Phases 101-107) remains open and PAUSED in parallel at Phase 104 Plan 03. **The Azure blocker is CLEARED as of 2026-08-16:** smoke run `31947685981` came back GREEN (`Status: Valid`, issuer `CN=Microsoft ID Verified CS EOC CA 04`, `Pre-sign status: NotSigned`), resolving the 2026-07-29 403-at-Sign regression — the operator's Portal-side identity-validation fix took and CI had simply not re-run. SIGN-03 is now Complete. What v3.5 is paused on now is (a) the operator's authorization for the irreversible `v0.66.1` tag push, and (b) the red board (Windows Packaging + 5 Integration suites) — **not** Azure. See `parallel_stopped_at` in the frontmatter; `104-03-SMOKE-403-FINDING.md` is retained as the diagnosis of the now-resolved regression. Phase 105 is fully planned (5 plans, 0 summaries) and waiting behind the tag; 106/107 unbuilt.

---

**HISTORICAL — pre-gap-closure Phase 116/117 position (retained):**

Phase: **116 — Tool-Sandbox Divergence Audit + Disposition ADR** — **COMPLETE 2026-08-09** (6/6 plans, verified 18/18)
Plan: 6 of 6 — ALL COMPLETE (Wave 1: `116-01`, `116-02` · Wave 2: `116-03` · Wave 3: `116-04` · Wave 4: `116-05` · Wave 5: `116-06`)
Status: Executed + verified `passed`. Milestone stands at 2/6 phases (115, 116) and 12/12 planned plans.
Last activity: 2026-08-09 — Phase 116 executed and verified; ADR-116 accepted with verdict **formalize fork-native (Pole B)**

**Phase 116 deliverables:** `116-DIVERGENCE-LEDGER.md` (closed — Reproduction block with pinned SHAs `0551eba2..0055bf3c`, D-03 module-set re-derivation, pre/post-fence disposition tables, split-commit residue accounting with Σ_bucketed = Σ_touched = 161, grep-verified fenced-window reconciliation, carve-out re-touch check, finalized Headline); `116-FEASIBILITY-MATRIX.md` (151-symbol fork primitive inventory + 11-row capability rating); `proj/ADR-116-tool-sandbox-disposition.md` (Status: Accepted, verdict Pole B, 5 reversal triggers, Phase 120 scope proposal). Zero source files changed — D-20 held.

**TWO OPERATOR DECISIONS OUTSTANDING, both proposed inside the ledger and deliberately NOT applied to ROADMAP/REQUIREMENTS:**
1. **Uncovered-Window Finding** — 12 commits touch a named fork-invariant carve-out surface but are dispositioned by no audit to date (108's ledger is fenced-window-scoped; 116's is tool-sandbox-surface-scoped; these fall between). Proposed home: UPST13/FUT-08. Needs a call before the next upstream sync, since the set includes `c5c7f56e` (audit-bypass invariant surface) and `9ce74e92` (`linux.rs` restored invariants).
2. **ADR-116 ratification** — D-11 binds: Phase 120 planning stays operator-gated on ratifying the Pole B verdict.

**Next:** `/gsd:discuss-phase 117` (Fail-Direction Contract + Startup Self-Attestation) — depends on Phase 115's clean audit/denial spine, which is in place. Note ADR-116's Consequences names an unreconciled wording gap between its fail-direction finding and 117's CINT-01 abort/downgrade framing; 117 should resolve it rather than restate it. `workflow.use_worktrees` should stay `false`.

---

**HISTORICAL — v3.6 final position (retained):** Phase: 114 (OAuth Capture Absorb, SEC-02) — **COMPLETE 2026-08-07** (11/11 plans, code-reviewed, 3 Criticals fixed)
Plan: 11 of 11 — ALL COMPLETE (Wave 1: 114-01, 114-02 · Wave 2: 114-03, 114-04, 114-08 · Wave 3: 114-05, 114-09 · Wave 4: 114-06, 114-07 · Wave 5: 114-10 · Wave 6: 114-11)

**EXECUTION STATUS (2026-08-07):** Running sequentially on the main tree (`workflow.use_worktrees=false`), branch `milestone/v2.13-carryforward-closeout`, pre-phase base `b29211aa`. Wave 1 delivered the capture audit vocabulary (114-01) and the `CaptureConfig` declarative wire format (114-02). Orchestrator-verified post-wave gate: `cargo build --workspace --all-targets` exit 0, `cargo test -p nono-sandbox-proxy --lib` 245/245, `cargo fmt --all -- --check` clean. **The one finding so far: D-14's exhaustive-struct-literal class bit twice in Wave 1 alone and both plans under-predicted it** — 114-01's `capture_context` forced 5 extra literal sites, 114-02's `RouteConfig.capture` forced 36 (not the predicted 3), and in both cases only `cargo build --workspace --all-targets` caught it, never the task's own narrower verify. Same-phase evidence that Plan 114-10 must be driven by real compiler output. Note 114-02 already edited 11 `RouteConfig` literals inside `route.rs`, which is Plan 114-03's own target file. No SDK STATE/ROADMAP writer verb has been run at any point this phase — orchestrator hand-edits only.

**PLANNING RECORD (2026-08-06), retained:** **READY TO EXECUTE.** Research (`114-RESEARCH.md`, 814 lines, HIGH confidence) + pattern map (`114-PATTERNS.md`, 13/13 analogs) + `114-VALIDATION.md` (Nyquist, `nyquist_compliant: true`, 16 required behaviors) + 11 plans + `gsd-plan-checker` **iteration 2 PASSED**. **The plan-checker's iteration-1 BLOCKER is the most consequential thing in this phase's planning record:** the first plan set built the inbound response buffer-and-rewrite enforcement point correctly, but `CapturePhantomStore::resolve()` had **zero production call sites** — phantoms were minted and handed to the sandboxed client but could never be redeemed, so an agent completing an OAuth login would receive a token it could never use. The plans *described* a closed loop while leaving it open, and `CaptureConfig.request_nonce_fields` was documented as "reserved / unused by this phase." That is functionally the exact shape the phase goal names verbatim as forbidden ("no reduced-scope half-feature ships"). **Operator ruled: close the loop** (option (a)), rejecting the defer-and-document alternative. Revision added `resolve_capture_request_body()` wired into `handle_reverse_proxy`'s real request-body path (Plan 114-06 Task 3), making the chain `handle_reverse_proxy` → `resolve_capture_request_body` → `capture::resolve_request_nonce_fields` → `CapturePhantomStore::resolve` live production code. **Also caught during iteration 1:** Plan 114-11's ADR-114 draft described the gap as "egress phantom resolution *beyond same-route scope*," implying a same-route baseline existed — it did not; corrected, with two grep acceptance criteria banning the old phrase, because `112-OAUTH-CAPTURE-DISPOSITION.md` misleading this phase's own discussion is the precise failure being avoided. **Iteration 2 passed with 2 warnings, both folded in by hand by the orchestrator rather than deferred:** (1) the wiring proof was grep-only — a grep proves the call is *written*, not that it *fires with correct arguments* — so a third end-to-end test `capture_egress_resolution_reaches_upstream_on_live_dispatch_path` was added, driving a live request through `handle_reverse_proxy` against a fake upstream and asserting the REAL token (not the phantom) is what reaches upstream, reusing the existing `TcpListener` fixture pattern; (2) the test named `capture_egress_fails_closed_for_unadmitted_consumer_in_request_body` described behavior that is actually "leaves the field unchanged and forwards" — not a request denial — so it was renamed `capture_egress_never_substitutes_real_token_for_unadmitted_consumer` across all 5 reference sites, since a misleading test name on a security property is itself a defect in a codebase that has twice been burned by inaccurate records. **Planner design calls the research deliberately left open:** D-05 cap = 256 KiB default / 1 MiB hard ceiling / per-route configurable; D-06 `capture_declared_for_upstream()` uses **host-only** matching, deliberately diverging from SPIFFE's exact `host:port` precedent per upstream's own `3c59c62e` hardening lesson (widens the fail-closed guard, does not narrow it); the schema round-trip test goes in the `mod tests` block housing the second of `profile/mod.rs`'s **two duplicate** `validate_against_schema` helpers. **PATTERNS.md corrected a RESEARCH.md line-range drift** (`test_schema_validates_spiffe_custom_credential` is `mod.rs:7396-7415`, not -7434). **Verified by hand, not by tool:** all 12 locked decisions (D-01r, D-02r, D-05..D-14) cited in colon form across plan `must_haves` — `check.decision-coverage-plan` returned the known **vacuous green** (`passed:true skipped:true total:0`) on this CONTEXT.md's `- **D-NN: ...**` bullet form. **Structural notes:** the CONNECT path needs **no new deny logic** (`is_route_upstream` already blocks it — regression test only); only `handle_forward_http` gets a genuinely new guard. Upstream's `9b692e07` diff hunks to `launch_runtime.rs`/`sandbox_prepare.rs`/`main.rs`/`policy.rs` reference `credential_capture`/`tls_intercept` fields that **do not exist anywhere in the fork's `ProxyLaunchOptions`** — those hunks describe a struct shape this fork never had, so `git apply` is not merely unusable, it is meaningless. D-14's break surface confirmed at **two** exhaustive `RouteConfig` literals (`../nono-py/src/proxy.rs:212-232`, `../nono-py/src/policy.rs:743-761`); `../nono-ts` structurally immune. `workflow.use_worktrees=false` already set (required — plans reach `../nono-py` via `..`). **Next: `/gsd:execute-phase 114`.**

PRIOR position — Phase: 112 (Security + Residual Sync) — **COMPLETE 2026-08-05** (all 8 plans, 4 waves, closed)
Plan: 8 of 8 (Wave 1: 112-01 COMPLETE — Wave 2: 112-02 COMPLETE, 112-03 COMPLETE, 112-04 COMPLETE — Wave 3: 112-05 COMPLETE, 112-06 COMPLETE, 112-07 COMPLETE — Wave 4: 112-08 COMPLETE — all 4 waves fully closed)
Status: **CLOSED — executed, code-reviewed, verified, operator-approved 2026-08-05.** Post-112-08 close-out gates (owned by the orchestrator, not by any plan): `/gsd:code-review 112` over 34 changed source files at standard depth found **4 Critical fail-open defects introduced by this phase**, all fixed in `916d3bd7`(CR-01, `nono proxy` dropped the profile's deny layer)/`892d2e01`(CR-02, predicate-less trust policy neutralized operator enforcement on the upgrade path)/`585f2743`(CR-03, orphan-reaper early return lost end-of-run denial records)/`20f9a1cd`(CR-04, zero-caller fail-open seccomp hatch in the core library) — CR-01 and CR-02 orchestrator-verified at source before the fixer ran; both cross-target clippy gates re-ran GREEN with `--all-targets` after the fix pass. **12 Warning + 5 Info findings remain OPEN by deliberate operator scoping** (`112-REVIEW.md`) — most notably **WR-06: `--allow-gpu` is now a hard error on non-NVIDIA hosts / WSL2 / containers without `CAP_SYS_ADMIN`**. `gsd-verifier` returned **`human_needed`**, 3 items persisted to `112-HUMAN-UAT.md` and operator-approved: the 36-binary integration sweep stalled ~25min and was killed (**ROADMAP SC4's `make ci` GREEN is only PARTIALLY evidenced** — resolve on the GH Actions Linux lane), and SEC-06's orphan-reap test + RES-02's denial-marker assertion both short-circuit on absent python3 in the local `cross` container so neither was runtime-exercised. Phase marked complete BY HAND; `gsd-sdk query phase.complete` deliberately not run.

PRIOR status (Plan 112-08, close-out): Appended the Phase 112 Security + Residual Sync Addendum to `108-DIVERGENCE-LEDGER.md` (all 18 SHA dispositions as shipped, 3 corrections to `112-DISPOSITION-TABLE.md`'s pre-execution text — SEC-03 adopt->adapt, RES-02/`4cc0af2c52` adapt-or-skip->skip, RES-02/`9840a16f35` adopt->adopt-adapted) plus the SEC-09 Carry-Forward Note in the tool-sandbox-surface cluster section. Reconciled `REQUIREMENTS.md`/`ROADMAP.md`: 10 requirements Complete (SEC-01, SEC-03..SEC-09, RES-01, RES-02), SEC-02 explicitly carved out to Phase 114. Combined-surface verification via a zero-crates-diff evidence chain (no crates/ commits since 112-07's own both-gates-GREEN) plus fresh `cargo fmt --all -- --check` (clean) and `cargo test --workspace --no-fail-fast` (17-name failing set, byte-for-byte identical to `111-04`'s own run, confirmed strict subset of the 27-name baseline). Commits `e32f0192`/`a3d69d48`/`885f655b`. See `112-08-SUMMARY.md`. **Phase 112 is fully CLOSED.** Next: Phase 113 (SPIFFE/SPIRE, NET-02) or Phase 114 (OAuth Capture, SEC-02) — operator picks.

PRIOR (2026-08-05): Plan 112-06 (SEC-06, seccomp supervisor-ancestry orphan reaping) complete. Ported upstream `ac5ccd70` (#1401) as an ADAPT: reused the fork's `linux_child_requires_dumpable()` predicate in place of upstream's absent `tool_sandbox_runtime`/`SeccompPolicy` symbols. `PR_SET_CHILD_SUBREAPER` set pre-fork (parent side); `reap_reparented_orphans(child)` ported verbatim and wired ONLY into the Linux `run_supervisor_loop`. Caught and corrected a plan-interfaces staleness (cited 2-arg predicate signature, live tree already had 3 args from 112-02's same-day SEC-03 absorb) via mandatory symbol-level re-verification before implementing. Genuine RED/GREEN TDD executed live via `cross test` (temporary always-None stub confirmed RED, real implementation confirmed GREEN). New integration test ported verbatim from upstream's own regression test; all 3 python3-dependent tests in the file (2 pre-existing + this one) skip in the local `cross`-rs container (python3 absent) — pre-existing environment limitation, not a regression, flagged for live CI. Both mandatory cross-target clippy gates GREEN with `--all-targets`; `cargo fmt --all --check` clean. `requirements.mark-complete SEC-06` applied by hand. Commits `9aaba29e`/`c0b5fab4`. See `112-06-SUMMARY.md`.

PRIOR (2026-08-05): Plan 112-05 (SEC-05, Landlock execute-restriction Refer grant) complete. Ported upstream `d84b4818` (#1397) into `restrict_execute()`: `.handle_access(AccessFs::Refer)` added to the stacked ruleset, plus an `if abi.has_refer()` bare-Refer-on-`/` grant after the per-path `Execute` rules and before `restrict_self()` — fixes renames into subdirectories created after `restrict_execute()` is applied. Symbol-level re-verification confirmed the disposition table's adopt call was correct (unlike SEC-03/112-02 and `4cc0af2c52`/112-04, both of which the table got wrong on file-presence-only evidence): every referenced symbol already exists in the fork with matching signatures, and the general `Refer` grant at `linux.rs:365` was independently re-confirmed to live in a separate stacked ruleset from `restrict_execute()`'s own. Ported the Wave-0 regression test verbatim with one flagged adaptation (`apply_landlock` -> `apply`, since no `apply_landlock` function exists in this fork). Live-verified via `cross test`: `ok. 1 passed`. Both mandatory cross-target clippy gates GREEN with `--all-targets`; `cargo fmt --all --check` clean. `requirements.mark-complete SEC-05` applied by hand. Commit `96398a3d`. See `112-05-SUMMARY.md`.

PRIOR position — Phase: 111 (Core Carry + Resource CLI + Fork-Invariant Verify + Release Leapfrog) — **COMPLETE (EXECUTED + VERIFIED + CLOSED 2026-08-05)**
Plan: 6 of 6 (Wave 1: 111-01 COMPLETE · 111-02 COMPLETE · 111-03 COMPLETE · Wave 2: 111-04 COMPLETE · Wave 3: 111-05 COMPLETE · Wave 4: 111-06 COMPLETE — all 4 waves fully closed)
Status: Plan 111-01 (CORE-01) complete — both upstream carries absorbed (`~/.cache` policy grant #1378, `MAX_CRYPTO_THREADS` 7→12 #1424), both new Wave-0 tests passing (the crypto-threads test confirmed live via `cross test` on linux-gnu since `exec_strategy.rs` is Windows-excluded), both mandatory cross-target clippy gates GREEN. Commits `1d0c8eb5`/`74146367`/`7cd4569a`/`3010312f`. See `111-01-SUMMARY.md`.

Plan 111-02 (CORE-02, half) complete — corrected the false "accepted with a warning pending cross-platform follow-up" claim for `--memory`/`--timeout`/`--max-processes` in `crates/nono-cli/src/cli.rs` and `docs/cli/usage/flags.mdx`, derived from live reads of all three enforcement backends (Linux cgroup v2 fail-closed, macOS best-effort `RLIMIT_AS` with possible silent `EINVAL`, macOS UID-wide fail-closed `RLIMIT_NPROC`, Windows Job Object unchanged). D-04 flag-freeze confirmed (no upstream rename/re-range). Found docs/cli/usage/flags.mdx's `--cpu-percent` entry was also stale (unlike cli.rs's accurate one) — corrected under Rule 1. Zero test changes needed (no test asserts on help strings); existing regression guards pass unmodified. Both cross-target clippy gates re-confirmed GREEN. `requirements.mark-complete CORE-02` deliberately NOT run — CORE-02 is split across 111-02 (help-text) and 111-03 (ADR-111 + ledger addendum), complete only after both land. Commits `f378d68b`/`5d7761a1`/`224d2dd0`/`ae1c7e57`. See `111-02-SUMMARY.md`.

Plan 111-03 (CORE-02, other half) complete — wrote `proj/ADR-111-resource-limits-boundary.md` mirroring ADR-108's shape, settling D-01's ADAPT-not-adopt disposition of `e6d26871`/#1269 + `34c2c975`/#1403 with an explicit (not merely asserted) argument for why ADR-86's audit/diagnostics carve-out does not extend to resource-limit enforcement. Appended a "Phase 111 Standing Divergence Addendum" to `108-DIVERGENCE-LEDGER.md` as a deliberate, locked exception to its own "Ledger closed" declaration — recorded as a **permanent** divergence, explicitly distinguished from the tool-sandbox-surface cluster's `DEFERRED->v3.7` treatment. `requirements.mark-complete CORE-02` NOW run — both split halves landed. Commits `10194152`/`6bf65687`/`a56fbfb4`/`78072057`. See `111-03-SUMMARY.md`. **Wave 1 of Phase 111 is fully COMPLETE.**

Plan 111-04 (VERIFY-01, combined 108-111 fork-invariant pass) complete — both mandatory cross-target clippy gates re-ran GREEN over the combined 108-111 surface; D-01 and the ADR-86 Windows carve-out both confirmed structurally unregressed; `make ci` substitution set run (native clippy/`fmt --check`/`cargo audit`/`test -p nono-sandbox`/`nono-sandbox-cli`/`nono-ffi` all GREEN except the 11 documented pre-existing `--bin nono` failures); the full `cargo test --workspace --no-fail-fast` sweep's 17-name failing-test set confirmed a **strict subset** of the documented 24-name baseline (zero new names) after the first attempt was silently killed by harness timeout-auto-promotion and had to be redone explicitly with `run_in_background:true`; both sibling bindings rebuild clean at pre-bump `0.66.1`. `111-04-VERIFICATION-NOTES.md` explicitly supersedes `110-08-VERIFICATION-NOTES.md` per D-09. `requirements.mark-complete VERIFY-01` run. Commits `d7928c07`/`f027805e`/`ea3990ad`. See `111-04-SUMMARY.md`. **Wave 2 of Phase 111 is now COMPLETE.**

Plan 111-05 (RLS-14, in-repo half) complete — bumped all 6 fork-owned workspace crates (`nono-sandbox`, `nono-sandbox-cli`, `nono-sandbox-proxy`, `nono-shell-broker`, `nono-fltmgr-client`, `nono-ffi`) + their 6 internal path-dependency version pins from `0.66.1` to `0.70.0`, regenerated `Cargo.lock` with zero third-party drift (`cargo build --workspace --all-targets` exits 0); `tools/sign-fixture` untouched at `0.1.0`. Corrected both hardcoded-version release-gate scripts (`release-readiness.ps1`'s `$targetVersion`/`$upstreamHighest` → `0.70.0`/`0.69.0` plus 2 stale inline comments; `release-dry-run.ps1`'s 4 cosmetic references) — `pwsh -File scripts/verify-dark.ps1 -Gate release-readiness` returns `PASS`. D-07 upheld (no tag push, no live publish). `requirements.mark-complete RLS-14` deliberately NOT run — RLS-14 is split across this plan (in-repo half) and 111-06 (sibling-repo half: `../nono-py` + `../nono-ts`, including correcting `../nono-ts`'s live `version = "0.66"` path-dep pin); the SDK's auto-flip was caught and reverted by hand before commit. Commits `502403ba`/`d1c0ad58`/`29729b46`/`5808b405`. See `111-05-SUMMARY.md`. **Wave 3 of Phase 111 is now COMPLETE.** Next: `111-06` (Wave 4, RLS-14 sibling-repo half + prepare-only release gate GREEN).

Plan 111-06 (RLS-14, sibling-repo half) complete — bumped `../nono-py` and `../nono-ts` to `0.70.0` (each with its own DCO-signed commit in its own repo's git history, per D-08), including correcting `../nono-ts`'s live `nono` path-dep `version = "0.66"` -> `"0.70"` semver requirement; both `maturin build` and `npx napi build --platform --release` rebuild clean with no struct-drift fix needed. Found and fixed a real regression in `scripts/release-dry-run.ps1`: its `PRE_PUBLISH_REGISTRY_BLOCKED` detection regex predated the Phase 102/103 fork-owned rename and never matched cargo's post-rename "no matching package named" error text, so the gate had exited 1 (hard FAIL) on what should have been an expected pre-publish block — the first full live run of this exact script since the rename. Widened the regex to match both phrasings; re-run confirmed the documented `PASS: Hard failures: 0. Blocked: 2 (pre-publish).` verdict, and `verify-dark.ps1 -Gate release-readiness` re-confirmed `PASS`. D-07 upheld (no tag pushed, no live publish). `requirements.mark-complete RLS-14` applied by hand — RLS-14 now Complete (both split halves landed). Commits `c24b20b`(nono-py)/`6406096`(nono-ts)/`be3d0ecc`/`684e97d1`(this repo). See `111-06-SUMMARY.md`. **Wave 4 of Phase 111 is now COMPLETE. Phase 111 is fully CLOSED — all 4 requirements (CORE-01, CORE-02, VERIFY-01, RLS-14) Complete.** Next: `/gsd:plan-phase 112` (Security + Residual).

PRIOR position — Phase: 110 (Profile/Policy Absorb + platform_overrides) — **COMPLETE 2026-08-04**
Plan: 8 of 8 (Wave 1: 110-01/02/03 · Wave 2: 110-04/05/06 · Wave 3: 110-07 · Wave 4: 110-08)
Status: CLOSED. All 4 requirements (PROF-01..04) complete. Plan 06's Task 3 `checkpoint:human-verify` (PROF-03e) resolved live on an Administrator-elevated session — `0 -> 8 -> 0` filter lifecycle with 4 `FWP_MATCH_RANGE` conditions at `49200..49210`, one per layer, `ALE_USER_ID`-scoped to the child. Behavioural inside/outside connect probe NOT RUN (`0xC0000142`); closure accepted on the filter-table proof, which is what Task 3's own wording specifies. 4 defects found and fixed while running the checkpoint (`7c7a189c`, `ea26b5b2`, `6d7ef719`, `4aec1944`), 3 proven live; both cross-target clippy gates re-ran GREEN on every one.
**Phase 112 context gathered 2026-08-05** — `112-CONTEXT.md` + `112-DISCUSSION-LOG.md` committed (`2cead1d0`), 8 locked decisions (D-01..D-08). Phase 112 is DISCUSSED, not yet planned; next step is `/gsd:plan-phase 112`. Key locked calls: **D-02 reality-check pass FIRST** (Wave 1 diff-verifies every SEC target's disposition before any absorb is planned — chosen because SEC-09 was proven phantom during discussion); **D-01 SEC-09 (`f6f02751`) = won't-sync**, its guard is absent from this fork's `execution_runtime.rs` (no tool-sandbox runtime, no `command_policies`), with a mandatory v3.7 carry-forward note so the guard question is consciously re-decided when tool-sandbox lands; **D-03 RES-01/RES-02 skip-biased**; **D-04 heavyweights (SEC-01 +1,873 / SEC-02a +4,425 / SEC-07 +1,927 ≈ 8.2k of ~9.7k insertions) stay in 112**, one plan each, own waves, expect 8-10 plans; **D-05 dispositions recorded as a standing-divergence addendum in the 108 ledger** (Phase 111-03 precedent), ADR-112 contingent only; **D-06 Linux-only SEC-03/05/06 verified via both cross-target gates + `cross test`, no live-kernel UAT**; **D-07 ROADMAP SC3 already satisfied out-of-band** — `crossbeam-epoch` is 0.9.20 and `cargo audit` is clean, so `373a67ae` must NOT be re-absorbed; **D-08 the 18-commit cluster is fully itemized, no orphan bucket**.

Next phase: **112**. Phase 111 is **EXECUTED and CLOSED 2026-08-05** — all 6 plans complete, all 4 requirements (CORE-01, CORE-02, VERIFY-01, RLS-14) Complete, verifier scored 5/5 must-haves. v3.6 now stands at 24/24 plans across phases 108-111; the milestone is NOT complete — phases 112 (Security + Residual, carries the live `crossbeam-epoch` RUSTSEC-2026-0204 fix) and 113 (SPIFFE) are on the ROADMAP but unbuilt. Next step is `/gsd:plan-phase 112`.

**Phase 111 close-out artifacts:** `111-VERIFICATION.md` (status `human_needed`, 5/5 must-haves verified), `111-REVIEW.md` (1 Critical + 2 Warning + 3 Info), `111-HUMAN-UAT.md` (CR-01 RESOLVED in `5cf5e121`; WR-01 and WR-02 remain OPEN by operator decision). WR-01: `MAX_CRYPTO_THREADS` 7->12 applies to Linux as well as macOS though upstream #1424's rationale is macOS-only. WR-02: the corrected `--timeout` help text describes macOS's `setpgid`+`kill(-pgrp)` as if it applied to Linux, which actually uses `cgroup.kill`. Both are Warning-severity accuracy items; fixing them requires re-running both cross-target clippy gates.

**SDK HAZARD RE-CONFIRMED 2026-08-05:** `gsd-sdk query phase.complete 111` returned `is_last_phase: true` (wrong — 112/113 are unbuilt), then set `status: milestone_complete`, computed `percent: 125`, and flattened this Current Position block to "Plan: Not started". Reverted via `git checkout -- .planning/STATE.md` and hand-corrected. The `parallel_milestone` v3.5 block survived this time, but the standing rule holds: never let SDK state writers touch this file; commit STATE.md before running any phase/state verb so the revert is available.

**Phase 111 locked decisions (see `111-CONTEXT.md`):** D-01 **ADAPT not adopt** for upstream's core `resource` module (`e6d26871`/#1269) — the fork keeps resource limits CLI-side because its implementation is *more* complete than upstream's (kernel-enforced on all 3 platforms: Job Object / cgroup v2 / `RLIMIT_AS`+`RLIMIT_NPROC`) while upstream's core module carries no enforcement; D-02 record it as a **standing divergence** in the 108 ledger (tool-sandbox precedent); D-03 write `proj/ADR-111-resource-limits-boundary.md`; D-04 fork flag names **frozen**, upstream differences get an alias never a rename (Phase 110-01 precedent); D-05/D-06 **correct the stale Unix help text** — `cli.rs` falsely claims `--memory`/`--timeout`/`--max-processes` are unenforced on Linux/macOS when both enforce, but the rewrite must respect the macOS `RLIMIT_AS`=address-space-not-RSS caveat; D-07 **PREPARE-ONLY** (no `0.70.0` tag push — must not entangle with v3.5's still-blocked `v0.66.1`); D-08 bump `../nono-py` + `../nono-ts` in-phase (v3.4 lesson: binding drift is caught only by `maturin`/`napi` build); D-09 VERIFY-01 covers the **combined 108-111 surface** because `110-08` predates the 4 fixes of 2026-08-04 and `has_port_rules()` shipped broken straight through it; D-10 both cross-target clippy gates mandatory-local.
Last activity: 2026-08-06 -- Phase 114 PLANNED (research + pattern map + VALIDATION + 11 plans across 6 waves; checker iteration 1 raised 1 BLOCKER + 3 warnings, operator ruled "close the loop", revision landed, iteration 2 PASSED with 2 warnings folded in by hand)

## Performance Metrics

**Velocity:** (v3.5 — reset; populated as phases complete)

- Total plans completed: 0
- Average duration: —
- Total execution time: —

| Phase | Plan | Duration | Tasks | Files |
|-------|------|----------|-------|-------|

*Updated after each plan completion*
| Phase 101 P01 | 9min | 2 tasks | 2 files |
| Phase 101 P02 | 2min | 2 tasks | 2 files |
| Phase 101 P03 | 6min | 2 tasks | 2 files |
| Phase 101 P04 | 8min | 1 task (Task 1 deliberately skipped) | 4 files |
| Phase 102 P01 | 12min | 3 tasks | 6 files |
| Phase 102 P02 | 9min | 3 tasks | 5 files |
| Phase 102 P03 | 6min | 2 tasks | 3 files |
| Phase 102 P04 | 12min | 2 tasks | 7 files |
| Phase 102 P05 | 8min | 2 tasks | 0 files |
| Phase 103 P01 | 5min | 2 tasks | 3 files |
| Phase 103 P02 | 10min | 2 tasks | 2 files |
| Phase 103 P03 | 2min | 2 tasks | 0 files |
| Phase 104 P01 | 3min | 2 tasks | 2 files |
| Phase 104 P02 | 24min | 3 tasks | 5 files |
| Phase 110 P01 | 20min | 3 tasks | 3 files |
| Phase 110 P02 | 30min | 3 tasks | 4 files |
| Phase 110 P03 | 55min | 3 tasks | 6 files |
| Phase 110 P04 | 45min | 2 tasks | 3 files |
| Phase 110 P05 | 50min | 2 tasks | 6 files |
| Phase 110 P06 (Tasks 1-2 2026-07-30; Task 3 checkpoint resolved 2026-08-04) | 75min + ~3h checkpoint | 3 of 3 tasks | 6 files + 4 defect fixes |
| Phase 110 P07 | 20min | 2 tasks | 3 files |
| Phase 110 P08 | 36min | 2 tasks | 2 files (both repos: 0 files, verification-only) |
| Phase 111 P01 | 35min | 2 tasks | 3 files |
| Phase 111 P02 | 25min | 2 tasks | 2 files |
| Phase 111 P03 | 20min | 2 tasks | 3 files |
| Phase 111 P04 | 100min | 3 tasks | 1 file (verification-only) |
| Phase 111 P05 | 12min | 2 tasks | 9 files |
| Phase 111 P06 | 30min | 3 tasks | 12 files (9 sibling-repo + 1 in-repo script + 2 REQUIREMENTS/STATE) |
| Phase 112 P06 | ~150min | 2 tasks | 2 files |
| Phase 112 P08 | ~140min | 3 tasks | 3 files (docs-only: ledger + REQUIREMENTS + ROADMAP) |

## Accumulated Context

### Key Decisions (v3.6 roadmap — Phase 110)

| Decision | Phase | Rationale |
|----------|-------|-----------|
| D-08a preserved verbatim: windows_low_il_broker/windows_interpreters keep Phase 51/71 fail-secure OR/union merge_profiles semantics under platform_overrides.windows | 110-01 | apply_platform_overrides reuses merge_profiles unmodified — an override may tighten (add) but never silently loosen (disable/remove) either flag; Phase 51's merge_profiles_or_semantics_base_true_child_false test confirmed unmodified and passing |
| 4th Profile-shape exhaustive-literal site discovered beyond the plan's named 3: policy.rs::ProfileDef::to_raw_profile | 110-01 | Compiler-caught (E0063) immediately on first build after adding the platform_overrides field; fixed the same way environment: None was handled there previously (built-in policy.json profiles don't declare the field yet) |
| Fixed two real Windows-specific git-quoting bugs in the ported @git:* module (dynamic_tokens.rs): git --show-origin C-quotes backslash-containing origin paths; git-config ini values need doubled backslashes when hand-writing fixture files | 110-02 | This Windows dev host actually runs the ported 32-test suite; upstream never exercised this code on Windows, and the module is meant to become the v3.7 canonical implementation (D-02) — treated as real bugs, not test-skips |
| @git:* end-to-end test gated #[cfg(any(linux, macos))]; dynamic_tokens.rs given a module-level #![cfg_attr(not(any(linux, macos)), allow(dead_code))] | 110-02 | @git:* expansion is Unix-only by design (D-01) — the non-Unix fallback is an intentional no-op, so the ported git-worktree test would validate the wrong behavior on Windows; the dead_code allow mirrors the existing session.rs:1 idiom for a module only consumed from one cfg arm elsewhere in the crate |
| landlock v0.4.4 confirmed (vendored source, net.rs:109) to have no native range rule type — Linux emitter unrolls per-port with no cap, matching upstream d5803b99 exactly | 110-03 | Resolves the previously-UNVERIFIED question in 110-PATTERNS.md/110-RESEARCH.md by reading the actual vendored crate source rather than assuming |
| macOS cumulative-cap check computed and errored BEFORE any profile string is built, preserving the sandbox_init() SIGILL rationale (~17,770 rules) verbatim in both the error message and a code comment | 110-03 | Avoids wasted profile-string construction on the error path; keeps the crash rationale discoverable (CONTEXT.md: "a bare magic number invites removal") |
| SupervisorConfig.proxy_bind_port_ranges added as a Linux-only cfg-gated sibling field to proxy_bind_ports, paired into all 9 exhaustive test-fixture literals (self-consistent count-gate: 9 == 9) | 110-03 | Rule 1 auto-fix mid-task: the first mechanical insertion omitted the #[cfg(target_os = "linux")] guard, caught by the mandatory cargo-zigbuild clippy --all-targets apple-darwin gate (E0560 on 8 sites) before the task commit landed |
| Pre-existing, unrelated compile break in supervisor_linux.rs's mod tests::network_decision (stale ApprovalRequest/request_approval shape, traced to commit 1a804977) documented in deferred-items.md, not fixed | 110-03 | Out of scope per Scope Boundary — discovered only via `cross test` (beyond the plan's mandated `cross clippy` gate); blocks execution (not compilation) of this plan's 3 new supervisor_linux tests on Linux, while the production code is proven via clean cross clippy on both mandatory targets |
| `requirements.mark-complete PROF-03` deliberately NOT run despite PLAN.md frontmatter listing `requirements: [PROF-03]` — reverted the SDK's auto-flip via `git checkout -- .planning/REQUIREMENTS.md` | 110-03 | Plans 110-03/04/05/06 ALL declare `requirements: [PROF-03]` (confirmed by grepping all 4 PLAN.md frontmatters) — PROF-03 is a single requirement split across 4 sub-plans; this plan only satisfies the library+Unix-emitter mechanism half (per its own objective text), NOT the profile schema (110-04), capability_ext.rs/manifest wiring (110-05), or Windows WFP emitter (110-06). Flipping PROF-03 to Complete now would misrepresent phase progress until all 4 land. |
| NetworkConfig.open_port_range/listen_port_range added (Vec<[u16; 2]> array-of-pairs), wired through merge_profiles' 3rd exhaustive-literal site (dedup_append) and a fork-only nono-profile.schema.json companion edit (upstream never touched this strict-schema file) | 110-04 | Closes Pitfall 4 / T-110-12: without the schema edit, a profile using the new fields would serde-parse but fail validate_against_schema() |
| validate_port_ranges/build_listen_ports/check_macos_port_range_cap extracted as standalone fns above prepare_profile_with_context (not inlined in the Ok(PreparedProfile{...}) literal as the plan's interfaces section sketched); check_macos_port_range_cap gated #[cfg(any(target_os = "macos", test))] instead of a plain macOS-only gate | 110-04 | prepare_profile_with_context requires a full on-disk profile load to exercise, making direct unit testing of just the range-validation logic impractical; the cfg gate gives the "exceeds the macOS limit" error path direct unit coverage on this Windows dev host, per the plan's own allowance for an OS-independent test |
| `requirements.mark-complete PROF-03` again deliberately NOT run for 110-04 (same rationale as 110-03) — PROF-03 stays split across 110-03/04/05/06 | 110-04 | This plan satisfies the profile-schema + profile_runtime.rs validation half only; capability_ext.rs/manifest wiring (110-05) and the Windows WFP emitter (110-06) remain before open_port_range reaches a sandbox capability end-to-end |
| capability_ext.rs's `open_port_range` sibling loop calls `add_localhost_port_range` with no re-validation (already validated by 110-04's `profile_runtime.rs`); manifest_convert.rs's independent `ports.localhost_range` conversion duplicates the start<=end + macOS cumulative-cap validation shape (not by code reuse) since the manifest (`--config`) pathway never touches `profile_runtime.rs` | 110-05 | Matches the plan's own documented intentional dual-pathway design (ADR-86: mechanism validates only start==0; each CLI-facing/library-facing pathway owns its own input validation) |
| `requirements.mark-complete PROF-03` again deliberately NOT run for 110-05 (same rationale as 110-03/04) — PROF-03 stays split across 110-03/04/05/06, complete only after 110-06 | 110-05 | This plan wires `open_port_range` end-to-end on Linux+macOS via both the profile and manifest pathways, plus display support; only the Windows WFP-native range emitter (110-06) remains |
| Windows WFP emitter: `FWP_RANGE0.valueLow`/`valueHigh` confirmed INLINE `FWP_VALUE0` structs (not pointers) by reading the real vendored windows-sys 0.59 generated bindings; only `FWP_CONDITION_VALUE0_0.rangeValue` is a pointer (`*mut FWP_RANGE0`) — simpler than the plan's own `Box::into_raw` sketch anticipated | 110-06 | Plan's own `<interfaces>` section flagged this as UNVERIFIED and mandated reading the real bindings before committing to an allocation strategy; resolves the question in favor of the file's existing stack-local-then-borrow idiom (`sd_blob`), no heap allocation needed |
| `range: FWP_RANGE0` declared uninitialized (not `zeroed()`) at `add_policy_filter`'s function scope | 110-06 | A throwaway `zeroed()` default produced an `unused_assignments` warning (fails `-D warnings`); delayed declaration compiles cleanly via Rust's definite-assignment analysis while preserving the outlives-the-call memory-safety property the file's `sd_blob` idiom establishes |
| Scope extended to 2 files not named in the plan's `<interfaces>` (`exec_strategy_windows/network.rs`'s `make_blocked_policy` test fixture; `agent_daemon/launch.rs`'s 5 `WfpRuntimeActivationRequest` construction sites) | 110-06 | Rule 3 auto-fix: neither `WindowsNetworkPolicy` nor `WfpRuntimeActivationRequest` derives `Default`, so `cargo build --workspace --all-targets` (Task 1's own acceptance criterion) would not pass without updating every site — another instance of this milestone's D-14 "verify by behavior, not name" finding |
| `describe_windows_network_runtime_target` extended to surface `localhost_port_ranges` in the WFP-activation diagnostic string | 110-06 | Rule 2 auto-fix: same D-11 "schema key present but invisible to operator" gap this milestone repeatedly catches, this time in a diagnostic string rather than a profile-display surface |
| **Plan 06 is INCOMPLETE — Task 3 (`checkpoint:human-verify`, PROF-03e live-kernel proof) unresolved; `requirements.mark-complete PROF-03` NOT run** | 110-06 | `FwpmFilterAdd0` cannot be exercised from this non-elevated, non-Administrator dev session; Tasks 1-2 (library/IPC wiring + unit-tested filter-spec construction) are committed and fully verified, but the live-kernel enforcement proof requires operator action per `110-06-SUMMARY.md`'s "Pending Checkpoint" section |
| bun-dev/mise-dev absorbed with `~/.bunfig.toml`/`~/.config/bun/bunfig.toml` added to `deny_credentials`' `deny.access` list, not `deny.read` (which does not exist in the fork's `DenyOps` struct); both new profiles proven resolvable-by-name via dedicated `load_profile()` tests (D-11 closure), distinct from the pre-existing schema-only iteration test | 110-07 | Plan prose said `deny.read`, but `crates/nono-cli/src/policy.rs`'s `DenyOps` only has `access`/`unlink`/`unlink_override_for_user_writable`/`commands` at group level; every existing group (including `deny_credentials` itself) uses `deny.access` for this purpose, and the plan's own acceptance criteria only check for the `bunfig.toml` substring inside the `deny_credentials` block, not a specific sub-key name. **PROF-04 now fully satisfied — the last of Phase 110's 4 requirements.** |
| Both mandatory cross-target clippy gates confirmed GREEN live with zero findings; neither sibling binding repo (`../nono-py`, `../nono-ts`) required a fix because `localhost_port_ranges` is a private `CapabilitySet` field with builder/accessor-only surface | 110-08 | Read-first grep of both binding repos' `src/` for `CapabilitySet { ... }` struct literals and for `localhost_port\|PortConfig\|open_port_range\|listen_port_range` returned zero matches — both bindings construct `CapabilitySet` exclusively via `RustCapabilitySet::new()`, unlike Phase 109's pub-field `RouteConfig` `E0063` |
| `requirements.mark-complete PROF-03` again deliberately NOT run for 110-08 (same rationale as 110-03/04/05/06); Phase 110 ROADMAP checklist box NOT flipped | 110-08 | This plan is phase-gate verification only — it proves 110-01..07 together satisfy the phase's non-Windows-kernel success criteria, but Plan 110-06's Task 3 (`checkpoint:human-verify`, PROF-03e live-kernel `FwpmFilterAdd0` proof) remains unresolved pending Administrator-elevated operator action; flipping PROF-03/Phase 110 now would misrepresent the phase as closed |
| `cargo test --workspace` documented as non-green on this host with full honest accounting: 11 failures match the documented pre-existing baseline exactly; `--no-fail-fast` (needed to see past the first failing binary for the first time in this project's phase-gate history) surfaced 13 MORE failures across `audit_attestation.rs`/`env_vars.rs`/`resl_nix_async_signal_safety.rs`, all confirmed pre-existing and unrelated to any 110-0X commit via `git log --oneline --all` per file, none fixed (Scope Boundary), logged to `deferred-items.md` § Plan 08 | 110-08 | Plan Rule 5 mandates no fabricated GREEN; a truthful RED with full root-cause tracing is a successful execution. Root causes: hardcoded Unix `/bin/pwd` literal with no Windows fallback (`audit_attestation.rs`); live `windows_run_*` exit-code/env-expansion failures correlated with pre-existing host-state mandatory-label ACE contamination on real paths (`env_vars.rs`); a stale text-signature-match test expecting `std::io::Result<()>` where the source now spells the type via the crate's own `Result` alias (`resl_nix_async_signal_safety.rs`) |
| Plan's own interfaces-section claim corrected: 110-05 does touch `crates/nono/src/manifest_convert.rs` (not `crates/nono-cli/` only as the plan states), confirmed to be pure input validation not policy | 110-08 | `git show 4d635305 -- crates/nono/src/manifest_convert.rs` shows a `start<=end` bounds check + `#[cfg(target_os = "macos")]`-gated cumulative-port cap check, structurally identical to pre-existing validation in the same `impl TryFrom<&CapabilityManifest> for CapabilitySet` block — matches STATE.md's own 110-05 decision entry (dual-pathway design, ADR-86 intact); ADR-86 boundary CONFIRMED unregressed |
| Hand-edited `policy.json`'s single relevant line instead of `git cherry-pick ca888108` (#1378) | 111-01 | 4 of the upstream commit's 6 touched files don't exist under those names/paths in this fork (`macos_trust.rs`, `proxy_command.rs`, `tls_intercept/ca.rs`) or are unrelated clippy cosmetics (`learn.rs`) — a cherry-pick would fail or corrupt; matches `111-RESEARCH.md`'s pre-identified finding |
| `max_crypto_threads_raised_to_12` verified live via `cross test --target x86_64-unknown-linux-gnu`, not the plan's literal native `cargo test` command | 111-01 | `exec_strategy.rs` is `#[cfg(not(target_os = "windows"))]` (Windows uses `exec_strategy_windows/mod.rs`) — the native Windows filtered test run reports "0 tests" for this filter, which is expected-but-silent; the cross-target run gives a genuine `ok. 1 passed` beyond what clippy alone would confirm |
| `docs/cli/usage/flags.mdx`'s `--cpu-percent` entry corrected too, beyond the plan's literal action text | 111-02 | The plan characterized the docs-site `--cpu-percent` entry as "already correct" (mirroring `cli.rs`'s genuinely-accurate `--cpu-percent` doc comment), but the actual file content also carried the stale "accepted with a warning pending cross-platform follow-up" claim — the plan's own acceptance criteria required zero remaining occurrences of that substring anywhere in the file, so it was corrected under Rule 1 |
| `requirements.mark-complete CORE-02` deliberately NOT run for 111-02 | 111-02 | CORE-02 is split across 111-02 (help-text correction, this plan) and 111-03 (ADR-111 + 108-ledger standing-divergence addendum, the other Wave 1 parallel plan) — same split-requirement pattern as Phase 110's PROF-03 across 4 sub-plans; flipping now would misrepresent CORE-02 as fully satisfied |
| ADR-111's ADR-86-carve-out-non-extension argument grounded in function (does the code decide/enforce, or only observe/report?) not location | 111-03 | Matches ADR-86's own stated boundary invariant ("the library still applies ONLY what clients explicitly add to `CapabilitySet`"); audit/diagnostics are read-only observability primitives while resource limits are the enforcement mechanism itself (Job Object/cgroup v2/RLIMIT_AS+RLIMIT_NPROC), sitting on the policy side of the ADR-86 line |
| 108-DIVERGENCE-LEDGER.md's "Ledger closed" closing sentence amended in-place (not silently worked around) to name the one locked exception | 111-03 | Preserves the historical record's integrity per D-02's own framing ("an explicit, locked exception") — a future reader who lands on the closing sentence first sees the addendum's legitimacy is self-evident, not a violation of the prior close |
| `requirements.mark-complete CORE-02` run for 111-03 (both split halves now landed) | 111-03 | 111-02 (help-text) + 111-03 (ADR-111 + ledger addendum) together satisfy CORE-02 in full — same completion-trigger pattern as Phase 110's PROF-03 across its 4 sub-plans, where the last-landing plan flips the requirement |
| `cargo test --workspace --no-fail-fast` re-run explicitly with `run_in_background:true` after the first foreground attempt (auto-promoted to background by the harness on its 600s timeout) was silently killed mid-`env_vars.rs` with no exit marker written | 111-04 | Harness-level finding, not a code defect: `tasklist`/`ps` showed zero related processes and the log stopped growing for ~39min after auto-promotion — the explicit `run_in_background:true` invocation survived to completion (`EXIT=101` written, matching the expected non-zero exit for a workspace with pre-existing failures) |
| 111-04-VERIFICATION-NOTES.md's 24-name baseline diff reports a strict subset (17 of 24 names), not an exact-count match, and documents this as expected flake rather than re-running to chase a "cleaner" number | 111-04 | `env_vars.rs` only reproduced 3 of its 10 previously-documented failure names this run (2702s/45min vs 986s/16.4min in 110-08) — consistent with 110-08's own documented host-state-contention characterization of this binary; per the plan's own Pitfall-3 guidance, a subset match satisfies the acceptance criterion and re-running to force an exact match would be scope creep, not honesty |
| `requirements.mark-complete RLS-14` deliberately NOT run for 111-05 (reverted by hand after the SDK auto-flipped it) — RLS-14 stays split across 111-05/111-06, complete only after 111-06 | 111-05 | RLS-14's own text requires both the 6 in-repo crates AND both binding repos (`../nono-py`, `../nono-ts`) at `0.70.0` — this plan satisfies only the in-repo half; the SDK's `requirements.mark-complete` call auto-flipped it to Complete anyway (it only reads the plan's own `requirements:` frontmatter, not the requirement's full text), so it was caught via review and reverted in `.planning/REQUIREMENTS.md` before commit — same split-requirement pattern as Phase 110's PROF-03 and Phase 111's own CORE-02 |
| 2 stale inline comments in `release-readiness.ps1` (describing `ASSERTION (a)`/`ASSERTION (c)`) corrected beyond the plan's literal action text, which named only the 2 live variable lines | 111-05 | Same "not left stale" standard the plan already applies to `release-dry-run.ps1`'s 4 cosmetic references; found via a broader `grep -n "0.66"` sweep after the literal edit, classified Rule 1 (accuracy) |
| Widened `release-dry-run.ps1`'s `PRE_PUBLISH_REGISTRY_BLOCKED` regex to match cargo's post-rename `no matching package named` error text, not just the pre-rename `failed to select a version for the requirement` phrasing | 111-06 | Rule 1 bug fix, found while running Task 3's own acceptance-criterion command: the regex was written before the Phase 102/103 fork-owned rename to `nono-sandbox`, a name never published under any version, so cargo's error changed shape (package-not-found, not version-not-found) — the gate exited 1 on the first full live run since the rename instead of the documented GREEN |
| `../nono-ts/Cargo.lock` included in the Task 2 commit despite not being named in the plan's `<files>` tag | 111-06 | `npx napi build --platform --release` regenerates it as a build-side-effect, identical to how Task 1 explicitly required committing `../nono-py`'s regenerated `Cargo.lock`; leaving it uncommitted would leave the sibling repo dirty after this plan claims completion |
| `requirements.mark-complete RLS-14` run for 111-06 (both split halves now landed) | 111-06 | 111-05 (in-repo half) + 111-06 (sibling-repo half) together satisfy RLS-14 in full — same completion-trigger pattern as Phase 110's PROF-03 and Phase 111's own CORE-02, where the last-landing plan flips the requirement |

### Key Decisions (v3.3 roadmap — historical)

| Decision | Phase | Rationale |
|----------|-------|-----------|
| 4 phases (94-97), not 2-3 | all | Three distinct concerns (audit, absorb, cross-target, release) each have a clean delivery boundary and different risk profiles; collapsing absorb+release creates a dependency inversion (version bump must come after sync). |
| UPST10-04 (remote relocation) folded into Phase 94 | 94 | The `nolabs-ai/nono` rename is audit-setup work — done at audit-open when fetching commits; a separate phase would be artificial. |
| Version leapfrog (RLS-05) in Phase 97, after Phase 95 sync | 97 | Bump once, post-sync, to a clean ≥ 0.65.0; bumping mid-sync creates a rebasing treadmill and dirty Cargo.lock during cherry-picks. |
| Cross-target (Phase 96) sequenced after Phase 95 sync | 96 | XTGT clippy gates should run against the synced + post-sync tree, not a pre-sync snapshot that will change. |
| Release scope = PREPARE ONLY | 97 | Preserves LOCAL-ONLY posture; repo PUBLIC pending Microsoft minifilter-altitude approval; actual push/publish is operator-gated manual step outside this milestone. |
| D-02 confirmed: Cluster C (9b37dc52) structural no-op | 95-03 | Upstream CredentialProxyIntent refactor is structurally incompatible with fork's flat ProxyLaunchOptions; no code change; Phase 89 || !prepared.custom_credentials.is_empty() active predicate preserved |
| WR-01 gap closed: dynamic errno lost in post-fork static message | 95-06 | format!() heap allocation is unsafe in post-fork child; message type sufficient for operator diagnosis; errno inaccessible safely post-fork |
| CR-01 gap closed: evaluate() placed AFTER endpoint_rules check (additive) | 95-06 | Preserves backward compat for legacy routes while enforcing explicit deny rules; compile() wraps endpoint_rules as allow entries with deny-default |
| Cross-target clippy gate PARTIAL→CI (both Linux and macOS) | 95-07 | Rust targets installed; aws-lc-sys/ring require C cross-linker (x86_64-linux-gnu-gcc) absent; Docker Desktop not running; WSL absent; failure is C toolchain missing, not Rust clippy error in changed files; GH Actions decisive on HEAD be42a5af; Phase 96 resolution target |
| linux-gnu cross clippy gate GREEN locally (exit 0) — PARTIAL→CI retired for linux-gnu | 96-01 | `cross clippy` in pinned image `ghcr.io/cross-rs/x86_64-unknown-linux-gnu:0.2.5@sha256:9e5b39c0...`; first local run surfaced COMPILE errors (not lints) in cfg(linux) code: Phase 95 absorb of upstream ae77d198 (#1210) silently dropped fork invariants — SEC-01 AF_UNIX no-grant static-EPERM filter + cgroup v2 resource-enforcement module — and left stale audit/approval call sites. Restored verbatim from ae77d198^; aligned stale sites to converged API. All structural, no silencing allows. Native clippy+fmt still green. Windows clippy is structurally blind to cfg(linux) drift — this is the gate's whole value. |
| apple-darwin cross clippy gate LOCAL-RUNNABLE (exit 0) — PARTIAL→CI retired for apple-darwin too | 96-02 | zig 0.16.0 + cargo-zigbuild 0.23.0 (host installs); ONE bounded `cargo-zigbuild clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used` exited 0 with SDKROOT UNSET and no SDK extraction. The expected D-04(b) aws-lc-sys SDK-licensing wall did NOT materialize — zig's bundled macOS C target support satisfied the `aws-lc-sys 0.41.0`/`ring 0.17.14` build-dep probe (assumption A3 favorable branch). Working invocation is the direct-binary `cargo-zigbuild clippy …` form (NOT `cargo zigbuild clippy`, which mis-parses). Both cross-targets now provably local-runnable; XTGT-03 closed via D-04 clean-exit branch (not the hard-blocker). |
| XTGT-04 closed: verification protocol rewritten — both gates LOCAL-RUNNABLE, PARTIAL→CI demoted to documented-runner-failure fallback | 96-03 | Checklist (`.planning/templates/cross-target-verify-checklist.md`) rewritten as single source of truth (D-06): Q2 linux-gnu → `cross clippy` (bare `cargo clippy --target` removed); Q3 apple-darwin flipped to MUST-run-locally via direct-binary `cargo-zigbuild clippy` (SDKROOT unset), per 96-02 record. Auto-default-to-PARTIAL retired per-gate (D-07) — PARTIAL only on a *documented* runner failure (stopped daemon / absent-but-installable tool excluded). Added anti-patterns 5 (default-PARTIAL) + 6 (`cargo zigbuild clippy` mis-parse). CLAUDE.md bullet collapsed to a one-line pointer carrying both commands; security mandate + "Windows `cargo check` not a substitute" preserved. Docs-only, no source changes. |
| 3-crate publish set confirmed sufficient (nono, nono-proxy, nono-cli) | 97-02 | nono-shell-broker appears ONLY under `[target.'cfg(target_os = "windows")'.dev-dependencies]` in nono-cli/Cargo.toml — a version-pinned Windows dev-dep that cargo does not resolve during publish-verify and downstream consumers ignore entirely |
| release.yml homebrew download-url corrected to OscarMackJr/nono | 97-02 | T-97-07 mitigation: fork ships its own release tarball; always-further/nono is an abandoned org; nolabs-ai/nono is upstream — neither is the fork's release repo |
| cargo publish --dry-run PRE_PUBLISH_REGISTRY_BLOCKED for downstream workspace crates | 97-03 | cargo resolves deps from live crates.io index at package time; nono-proxy/shell-broker/cli exit 101 until nono 0.66.0 is published; only nono is always-runnable; downstream crates re-run after nono publish |
| nono-py RouteConfig missing endpoint_policy (hard blocker for PyPI release) | 97-03 | maturin build exits 1: endpoint_policy field added to nono-proxy RouteConfig in phase 95 absorb but nono-py src/policy.rs:743 and src/proxy.rs:206 were not updated; fix = add `endpoint_policy: None,` to both initializers |
| Release-readiness gate: policy violations return FAIL verdict; infrastructure failures throw | 97-04 | Enforces T-97-11/12/13 threat model — private-path leak is a FAIL verdict (operator can diagnose), not a harness error; command-not-found is a throw (harness-internal error, exit 4) |
| Publish set reconciled to 3 crates (nono → nono-proxy → nono-cli) — WR-02 | 97-04/close | Code review + verifier flagged a divergence: release.yml published 3 crates but the dry-run + runbook specified 4 (adding nono-shell-broker). Operator chose the 3-crate set — nono-shell-broker marked `publish=false` (internal Windows broker, ships inside the MSI, bin-only crate / no lib target), dropped from release-dry-run.ps1 + RELEASE-RUNBOOK.md; now consistent with release.yml's existing publish-crates job. Build clean; release-readiness gate re-PASS. Supersedes the 97-04 "4-crate set" entry. |

### Key Decisions (v3.5 roadmap)

| Decision | Phase | Rationale |
|----------|-------|-----------|
| Full go-live EXECUTE posture (operator-in-loop), not prepare-only | all | The Azure VM removes the clean-host access blocker (operator's corporate host is contaminated: POC cert imported + VC++ installed + corporate proxy/EDR/managed-trust confounds the chain-build/revocation failure being debugged). Milestone closes only when binaries are Verified-publisher + broker spawns on the VM. |
| Verify-gate debugging is CI-side (GitHub `windows-latest`), not user-host | all | Smoke test + `signtool /pa` re-verify run on GitHub's clean cloud runner; only the behavioral install/broker-spawn/no-VC++ tests need the Azure VM. Fork's autonomous deliverable = CI verify hardening + Azure-VM IaC + scripted gates. |
| Ship reproducible Azure-VM IaC (`az`/Bicep) + scripted `verify-dark.ps1` gates | — | Operator selected "Ship IaC + scripted gate" over runbook-only: a fresh Win11 VM (no POC cert, no VC++) stood up by script + unattended gates the operator runs over RDP, emitting machine-readable verdicts (`SKIP_HOST_UNAVAILABLE` when no VM). |
| FUT-01 live multi-registry publish IN scope | — | Operator broadened scope: actually publish `0.66.1` to crates.io + PyPI + npm in-milestone (v3.4 was PREPARE-ONLY). |
| 7 phases (101-107), spine 101→104→105/106→107, with 102 and 103 parallelizable | roadmap | Registry-ownership question is pre-resolved by the PUB-01 rename decision (fork publishes under owned `nono-sandbox` names, no upstream co-owner-grant preflight needed) — so the research's suggested "Phase 1: registry preflight" collapses into Phase 102 (the rename phase) rather than a separate go/no-go gate. Verify-gate hardening (101) must land before the release cut (104, "do not cut a release until smoke is green"); the rename (102) is independent of signing and parallelizable; Azure IaC + gates (103) reuse 101's shared verify helper but are otherwise independent infra work; live publish (105) needs both the rename (102) and a real release (104); clean-host UAT (106) needs the real release (104) but NOT the live registry publish (105) — the GitHub Release artifact is sufficient; close-out (107) is strictly gated on 106 PASS, never merely on 104 green (Pitfall 10). |
| CHOST-03 does not depend on PUB-02 | roadmap | Architecture research's suggested build order has Phase 8 (VM UAT) depend on Phases 2/3/6 (IaC, gates, release cut) but not Phase 7 (publish) — the VM stages GitHub Release artifacts directly, not registry-installed packages, so live publish is not a precondition for clean-host UAT. |
| Operator-in-loop checkpoints explicitly annotated per success criterion (not just per phase) | roadmap | Phases 104-107 each mix autonomous prep with operator-only actions (Azure profile config, tag push, registry publish commands, RDP session, secret deletion) — annotating at the success-criterion level (not phase level) keeps `/gsd:plan-phase` from mis-scoping an operator action as executor-automatable. |
| SIGN-02 helper: reused `Find-Signtool` verbatim from `scripts/sign-windows-artifacts.ps1` | 101-01 | Live, already-proven repo code for locating `signtool.exe` on `windows-latest`; avoids re-deriving the SDK-path probe |
| SIGN-02 helper: `Start-Process -PassThru -Wait` for signtool exit-code capture, not bare `& signtool ...; $LASTEXITCODE` | 101-01 | `Assert-TrustedSignature` interleaves GAS + signtool + diagnostic logic around the invocation — exactly the interleaving scenario that makes bare `$LASTEXITCODE` unsafe (101-RESEARCH.md Pitfall 3) |
| SIGN-02 helper: byte-for-byte preserved `if ($gas.Status -ne 'Valid')` as the sole Strict-mode fail-closed gate | 101-01 | Grep-verified single occurrence, guarding a `throw`; never weakened to accept `UnknownError` — matches `release.yml:269`/`:311` and `trusted-signing-smoke.yml:69` |
| knownGood test fixture trusts its throwaway cert via `Cert:\CurrentUser\Root`, not `Cert:\LocalMachine\Root` | 101-01 | Dev host not elevated; `LocalMachine\Root` requires administrator. GAS/`X509Chain.Build()` both consult CurrentUser Root for the current-user context, producing `Status=Valid` equivalently. Security-neutral, test-fixture-only. |
| knownGood test fixture imports the throwaway cert via the raw `X509Store` API, not `Import-Certificate` | 101-01 | `Import-Certificate -CertStoreLocation Cert:\CurrentUser\Root` triggers a blocking interactive "Security Warning" CryptUI dialog with no non-interactive suppression — hangs indefinitely; `X509Store.Add()`/`.Remove()` bypasses the trust-UI layer entirely (mirrors this repo's own `Add-TrustForVerify`/`Remove-TrustForVerify` pattern) |
| All 3 named verify sites (release.yml Site 1/Site 2, trusted-signing-smoke.yml) wired to `Assert-TrustedSignature`, plus a 4th plan-unenumerated inline gate | 101-02 | release.yml's "Verify signed binaries inside zip (Windows)" step (~L329) had its own duplicate `Get-AuthenticodeSignature`/`Status -ne 'Valid'` gate not named in the plan's `read_first`; wired it too (Rule 2) to satisfy the plan's own whole-file grep acceptance criterion (`grep -c "sig.Status -ne"` returns `0`) |
| SIGN-01 confirmed live: profileType == PublicTrust (account ArtifactNono / RG_Nono / identity 20cb70d3-2d17-4fdb-9121-963628df6b63); no fix applied | 101-03 | Confirmed via Azure Portal (not `az` CLI — `trustedsigning` extension failed on a corporate-TLS SSL error). This rules out the profile-type hypothesis for the 2026-06-30 `UnknownError`; root cause remains open, routed to Plan 01's D-04 chain diagnostics on a future smoke run — not guessed. Recorded verbatim in `101-SIGN01-FINDING.md`. |
| GitHub Trusted Signing config (vars + OIDC FIC) originally reported absent — CORRECTED 2026-07-02: config exists and works | 101-03, corrected post-verification | Plan 03's operator confirmation pass reported no `TRUSTED_SIGNING_ACCOUNT`/`_PROFILE`/`_ENDPOINT` variables or federated credential existed. Independent verification (`101-VERIFICATION.md`) found this false: repo-scoped variables (`ArtifactNono`/`NonoCertProfile`/`https://eus.codesigning.azure.net/`) and the OIDC FIC were both provisioned 2026-06-04 and confirmed working by run `28469674206` (2026-06-30, OIDC+Sign GREEN). Likely cause: the operator checked only the empty `Development` environment-scoped page, not the populated repo-scoped variables the workflow actually reads. See corrected `101-SIGN01-FINDING.md` / `101-SIGN03-SMOKE-VERDICT.md`. |
| SIGN-03 smoke dispatch NOT attempted; verdict recorded as BLOCKED (not FAIL, not PASS) — reasoning corrected 2026-07-02 | 101-04 | Original verdict cited 5 conditions (no OIDC FIC, no GitHub variables, workflow unpushed, `gh` wrong-repo, outward-facing posture) — 3 of these were false per independent verification (OIDC FIC exists/works, GitHub variables exist, `gh` correctly resolves to `OscarMackJr/nono`). The 2 genuine gates: Plan 02's hardened workflow is unpushed (all Phase 101 commits local-only), and dispatching is outward-facing/operator-gated under the fork's prepare-only posture — plus the prior run's Verify `UnknownError` remains unresolved. Recorded verbatim (with explicit retraction of the false premises) in the corrected `101-SIGN03-SMOKE-VERDICT.md`; SIGN-03 stays Blocked/Deferred in REQUIREMENTS.md; hands off to Phase 104, which needs push + operator authorization + re-verify — not fresh OIDC/variable re-provisioning. |
| SIGN-03 live smoke dispatch executed 2026-07-03 (operator-authorized push + dispatch); verdict rewritten BLOCKED → **FAILED (RED, diagnosed)** | 101-04 (live re-run) | Two runs: `28636000664` failed on a real Plan-02 wiring defect (no `actions/checkout` before the dot-source of `verify-authenticode.ps1`), fixed in `83eefe11`; authoritative `28636133664` ran the real hardened verify path and still returned `Status:UnknownError` on a genuinely-signed `PublicTrust` binary (`CN=TWGGLOBAL.onmicrosoft.com`, issuer `Microsoft Enterprise ID Verified Policy AOC CA 02`) — the fail-closed gate retried the transient case then correctly refused to pass; NOT loosened. Also **disproves** `101-RESEARCH.md`'s issuer-naming heuristic (`Enterprise ID Verified Policy` ≠ reliable `PublicTrustTest` tell — `PublicTrust` chains through it too), so any future gate must not resurrect that differentiator. A real D-04 diagnostic-flush gap was also found (chain-dump/`signtool /pa` output not visible in the CI log before the throw). Full evidence + 3 findings (A root cause, B heuristic disproven, C diagnostic gap) + Phase 104 hand-off in the rewritten `101-SIGN03-SMOKE-VERDICT.md`; `101-SIGN01-FINDING.md` updated with a corroborating 2026-07-03 note; REQUIREMENTS.md SIGN-03 now **Failed/Deferred** (not Blocked). |
| PUB-01 3-crate publish set renamed to fork-owned family: `nono`→`nono-sandbox`, `nono-proxy`→`nono-sandbox-proxy`, `nono-cli`→`nono-sandbox-cli`, live-availability-gated (all 5 registry names re-confirmed 404 on 2026-07-03) before any manifest edit | 102-01 | `[package] name` renamed via Cargo's `package =` dependency-key mechanism (table keys `nono`/`nono-proxy` left unchanged in all 5 in-workspace dependents, including publish=false `nono-shell-broker`/`nono-ffi`) — zero `use nono::`/`use nono_proxy::` import changed; `cargo build --workspace --all-targets` green with a Cargo.lock diff limited to exactly the 3 renamed packages |
| Rule 3 auto-fix: pinned explicit `[lib] name = "nono"` in `crates/nono/Cargo.toml` alongside the `[package]` rename | 102-01 | `crates/nono/tests/manifest_types.rs` (the crate's own integration test) links via Cargo's default-derived lib name, which silently became `nono_sandbox` once `[package] name` changed — a distinct failure mode from the well-documented dependency-key-vs-`package=` consumer pitfall, not covered by 102-RESEARCH.md's cross-crate scratch test. Pin has zero effect on the `package=` consumer mechanism (consumer extern names are controlled solely by their own dependency-table key, per 102-RESEARCH.md's own empirical finding) |
| Makefile + 4 permanent CI workflows (ci.yml, image-build.yml, release.yml build steps, phase-37-linux-resl.yml) reconciled to `-p nono-sandbox`/`-p nono-sandbox-cli` selectors; `nono-ffi`/`nono-shell-broker` selectors and release.yml's 3 `cargo publish` lines left untouched, with a new Phase 105 deferral comment added above the first publish step | 102-02 | PUB-01 SC4 names `make build` as its literal verification command — those selectors would otherwise error against the now-nonexistent old package names after Plan 102-01's rename; `cargo build --workspace --all-targets`, `make build`'s two constituent cargo invocations, and `cargo fmt --all -- --check` all verified green |
| `../nono-py`'s own Cargo.toml/pyproject.toml patched with `package = "nono-sandbox"`/`package = "nono-sandbox-proxy"` and `[project] name = "nono-sandbox"`; `maturin build` proven green; all edits landed in ONE DCO-signed commit (`787e2dd`) in the nono-py repo, separate from this repo's history | 102-03 | `../nono-py` is outside this Cargo workspace and path-deps in by relative path — invisible to `cargo build --workspace`; Plan 102-03's own acceptance criteria required Cargo.toml + pyproject.toml + regenerated Cargo.lock together in one commit, so Task 1/Task 2 were combined rather than committed separately; `cargo tree -p nono-py \| grep nono-sandbox` returned 3 matches confirming resolution against the renamed core crates via the correct relative path |
| Rule 3 auto-fix: force-added gitignored `../nono-py/Cargo.lock` (`git add -f`) to satisfy the plan's explicit requirement that it be present in the rename commit | 102-03 | `../nono-py/.gitignore` excludes `Cargo.lock` by default (standard for a Rust binding crate); the plan's own `files_modified`/acceptance criteria explicitly name this file, so the gitignore rule was treated as a staging-mechanism obstacle, not grounds to omit the file |
| `../nono-ts`'s own Cargo.toml patched with `package = "nono-sandbox"`; npm identity hand-rescoped to `@oscarmackjr/nono-ts` across package.json + all 4 platform subpackages + optionalDependencies (NOT via the official `napi rename` tool); `napi build --platform --release` proven green; all 7 files landed in ONE DCO-signed commit (`c2f5aaa`) in the nono-ts repo | 102-04 | The installed `@napi-rs/cli@3.6.0` `rename` subcommand's real implementation diverges from 102-RESEARCH.md's documentation-derived description: it cannot run non-interactively even with all documented flags supplied (real-TTY-only `@inquirer/prompts`), it contains NO code path that rewrites `npm/*/package.json` or `optionalDependencies` at all, and it OVERWRITES the crate's own `Cargo.toml [package].name` with a sanitized form of `--binary-name` — which would have corrupted nono-ts's required-unchanged `nono-node` identity. All 6 JSON manifests were hand-edited instead, with every optionalDependencies-key-to-subpackage-name cross-reference individually verified; `cargo tree \| grep nono-sandbox` confirmed resolution against the renamed core crate |
| Phase 102 CLOSED at the phase-gate: fresh 5-way registry re-check (crates.io x3, PyPI, npm) all 404 same-day as Plans 01-04, zero same-window squat; `cargo build --workspace --all-targets` + both `make build` constituent `cargo build -p` commands + `maturin build` (nono-py) + `napi build --platform --release` (nono-ts) all green; all 4 PUB-01 SC1-SC4 confirmed against live state across all 3 repos, each with a DCO-signed rename commit | 102-05 | `make` remains absent from this host's PATH (consistent with 102-02's own finding) — substituted its two constituent `cargo build -p nono-sandbox`/`-p nono-sandbox-cli` invocations, per this plan's explicit environment-substitution instruction; did not gate on `make ci`/`make test` (documented pre-existing Windows baseline test failures, unrelated to this rename). Zero source files touched (verification-only plan) |
| CHOST-01 authored: `main.bicep` self-contained (own VNet/Subnet/NSG/PublicIP/NIC), Gen2 + Trusted-Launch, `vmImageSku`/`operatorIpCidr` required with NO default; `deploy.ps1`/`teardown.ps1` default to dedicated ephemeral `RG_Nono_CleanHost` (never `RG_Nono`), resolve SKU/IP live (never hardcoded, never `az`'s REST passthrough, never a TLS-bypass flag); `az bicep build` exits 0; neither script invoked live | 103-01 | Bicep CLI 0.44.1 was already present at `~/.azure/bin/bicep.exe` from Phase 103 research — no re-install needed; comments describing forbidden literal patterns (wildcard CIDR, REST-passthrough command name, async-delete flag) were reworded to avoid containing the exact grep-checked substrings, since the plan's own acceptance criteria grep the whole file with zero tolerance |
| CHOST-02 authored: `scripts/gates/trusted-signed-assertion.ps1` (dot-sources `verify-authenticode.ps1`, gates solely on `Status='Valid'` via `Assert-TrustedSignature -Mode Strict`, catches its throw -> returned FAIL, issuer captured `detail.issuerInformational`-only never a branch condition) + `scripts/gates/broker-spawn-on-clean-host.ps1` (self-contained install -> `nono run --profile claude-code` -> uninstall cycle, zero dependency on the alphabetically-later install gate under `-All`); both confirmed SKIP_HOST_UNAVAILABLE (exit 3) on this dev host; zero edits to `scripts/verify-dark.ps1` | 103-02 | Split issuer-capture across separate variables/statements so no single line contains both an `if`-substring (e.g. inside `Certificate`) and `Issuer`, satisfying the plan's zero-match `if.*Issuer` grep while still capturing the issuer informational-only; reworded all prose references to the other gate file generically ("the Phase 80 INST-01 install-proof gate") rather than its literal filename, since the plan's own acceptance criteria grep the whole file for zero occurrences of `clean-host-install` |
| Phase 103 CLOSED at the phase-gate: `az bicep build` re-confirmed exit 0; both new gates independently re-confirmed exit 3 (SKIP_HOST_UNAVAILABLE); full `-All` sweep JSON programmatically parsed (not eyeballed) confirming both new gates present at `SKIP_HOST_UNAVAILABLE` and no gate besides the unrelated pre-existing `release-readiness` reporting FAIL/HARNESS_ERROR; `git diff --stat scripts/verify-dark.ps1` confirmed empty (zero harness edits across both plans); CHOST-01 and CHOST-02 both confirmed satisfied simultaneously | 103-03 | `-All` sweep's `overall: "FAIL"` is a pre-existing baseline condition (release-readiness gate's version-family cargo-metadata check, authored Phase 97/re-pointed Phase 100, zero file overlap with Plans 01/02), not a Phase 103 regression — independently re-traced (git history + file-overlap check), not merely re-stated from Plan 02's own prior observation of the same FAIL |
| REL-01 D-04 flush fix landed: `Write-Error -ErrorAction Continue` on both Strict-mode failure branches guarantees `Write-ChainDiagnostic` runs before the terminating `throw` under CI's forced `$ErrorActionPreference='Stop'`; `Merge-NoCheckOverride` (pure, one-directional) + a NoCheck-mode corroborating `X509Chain` build wired into `Get-ChainClassification` prevent a genuine untrusted root from being misclassified as a retry-able transient. Both fail-closed gate conditions (`Status -ne 'Valid'`, `ExitCode -ne 0`) grep-confirmed byte-for-byte unchanged; 6/6 harness cases pass; manual revert-then-restore proved the new `diagnosticFlushUnderStop` case would have caught the pre-fix bug (FAIL observed, then PASS after restore, file byte-identical after) | 104-01 | Purely additive legibility/classification hardening per `104-RESEARCH.md` Pattern 1/Pattern 2 — does not and cannot fix the external Microsoft root-certificate propagation gap (still open, tracked separately); makes the *next* verify failure fully legible on the first attempt regardless of cause |
| `publish-crates` job neutralized via job-level `if: false` (Pitfall 104-A closed) — 3 stale `cargo publish -p nono*` lines + `update-homebrew-core` left untouched; new `scripts/verify-release-yml-publish-selectors.ps1` regression-proven (FAIL-then-PASS observed via a throwaway substring injected into `update-homebrew-core`, then removed); new `scripts/azure/check-trusted-signing-root.ps1` live-run this session confirms Microsoft's root CTL still ABSENT the thumbprint (554 certs checked, exit 1) — external blocker unresolved, no code fallback exists; `REQUIREMENTS.md` REL-01 / `ROADMAP.md` Phase 104 SC3 wording corrected off the disproven issuer-substring heuristic to Status=Valid + non-test-signer, issuer informational-only (Finding B) | 104-02 | Closes the newly-discovered SC2 landmine before the operator's Plan 104-03 tag push; the local pre-check avoids burning CI-dispatch minutes polling for an external, unforceable propagation fix; the doc correction prevents a future reader from reintroducing the disproven heuristic as a gate condition |

*Further v3.5 decisions populated as phases complete.*

### Key Decisions (v3.4 roadmap — historical)

| Decision | Phase | Rationale |
|----------|-------|-----------|
| 3 phases (98-100), drain-then-sync shape | all | Audit (98) gates absorb (99), absorb completes before version bump (100) — same dependency-inversion protection as v3.3. #1225 HIGH-CONFLICT disposition settled in 98 before any cherry-pick begins. |
| #1225 ADR deferred to Phase 98 | 98 | The NetworkIntent refactor touches the core network-capability enum the Windows backends key off — adopt-vs-diverge decision must be deliberate and documented, not decided mid-absorb. Mirrors the v3.1 Phase 86 precedent. |
| Release reconcile in Phase 100, after sync | 100 | Version bump to 0.66.1 must follow the sync to avoid a dirty Cargo.lock during cherry-picks; PyPI blocker fixed in the same phase since it gates the release. |
| UPST11 window: 14 substantive + 6 noise = 20 total; 8 clusters A-H | 98-01 | D-12 closed: empirical accounting from drift tool (14) + git log enumeration (6 noise) = 20 total non-merge commits in v0.65.1..v0.66.0 |
| #1225 NetworkIntent refactor is CLI-side ONLY — no core library changes | 98-01 | actual-diff of 72bcfd66 shows 11 files all under crates/nono-cli/src/; preliminary 260629-toe scan suggested library touches but upstream chose CLI-side approach; conflict is fork CLI vs upstream CLI, not library-boundary issue |
| Cluster B (4 tool-sandbox commits) won't-sync: fork lacks tool-sandbox/ dir | 98-01 | Fork skipped tool-sandbox feature in Phase 94/95 Cluster B; all 4 commits (691e0f4f, 7011bc85, d2252225, 853d5236) patch absent files; carry-forward if feature adopted |
| Cluster A windows-touch=yes confirmed: 7 of 11 #1225-touched files have cfg(windows) | 98-01 | grep confirms capability_ext.rs, supervised_runtime.rs, command_runtime.rs, execution_runtime.rs, launch_runtime.rs, output.rs, terminal_approval.rs; cross-target clippy REQUIRED in Phase 99 if adopted |
| #1225 actual-diff confirmed CLI-side only; Cluster E verify→will-sync; ADR-98 sole Phase 99 blocking gate | 98-02 | git show on all 14 commits: no core library changes in window; c808f000 D-03 re-confirmed as will-sync (mechanical nolabs-ai URL update; package namespace identifiers preserved); per-cluster ADR risk matrix complete (Cluster A=H dominant; no TBD cells) |
| ADR-98 Decision: full-sync-adopt upstream #1225 NetworkIntent refactor (Option A) | 98-03 | CLI-only refactor; ADR-86 library boundary non-regressed (NetworkMode::ProxyOnly in crates/nono/src/ untouched); Windows WFP/AppContainer non-regressed (exec_strategy_windows/network.rs reads enforcement-time WindowsNetworkPolicyMode::ProxyOnly, not CLI intent type); Phase 86 convergence precedent applies; Phase 99 deviations: WSL2ProxyFallback preservation + CompiledEndpointPolicy compatibility in proxy_runtime.rs |
| Carve-out re-touch check finalized: 2 CLEAN, 3 HIT (with guard tests), 1 additive-HIT | 98-04 | CR-02+v3.2-override=clean; Cluster-F+endpoint-policy=HIT-expected-conflict-preserve-fork-expression; linux.rs=HIT-additive-only; guard tests named for all HITs; ADR-98 cross-referenced in endpoint-policy subsection (highest re-touch exposure) |
| Completeness sweep PASS: 14+6=20; no TBD; six verdicts; 0.66.1 floor anchored | 98-04 | All five assertions PASS; ledger is complete; Phase 99 gate document ready |
| Cluster D (5b8e94da): 9P warning via statfs/V9FS_MAGIC additive in linux.rs; fork GPU tests preserved | 99-04 | is_9p_path detection (statfs probe, V9FS_MAGIC 0x01021997) inserted before Landlock capability-add loop; warnings deduplicated per mount device ID; fork GPU tests unaffected |
| Cluster E (c808f000): always-further/nono→nolabs-ai GitHub URLs in 3 src/ files; package identifiers and production URLs preserved | 99-04 | profile/mod.rs, proxy_runtime.rs, route.rs had no matching lines in fork (test fixtures diverged); always-further/claude registry identifiers preserved; OscarMackJr/nono fork identity preserved |
| Cluster F (2e64798d): sigstore-trust-root =0.9.0 pinned; dual-version cascade (resolver kept sigstore-verify 0.8.0 + trust-root 0.8.0 for sigstore-verify; new trust-root 0.9.0 direct dep for nono); make build GREEN; cargo audit clean | 99-05 | sigstore-verify bump (upstream 9e084cbb) deferred to future sync phase; assumption A1 confirmed favorable: dual-version resolution is stable; no HIGH/CRITICAL advisories |
| Cluster G (a4d68189): X-Nono-Token stale claim removed from proxy README and token.rs; cli_bootstrap.rs deprecated flag warnings now 3-tuple with remove_by field (--proxy-credential → Will be removed in v1.0.0) | 99-05 | cli.rs ALIAS annotation absent in fork — skipped; behavioral change fully captured in cli_bootstrap.rs; 176/176 nono-proxy tests pass |
| RLS-10 leapfrog to 0.66.1: full 6-member set bumped (D-03 correction of stale 5-crate text) | 100-01 | nono-fltmgr-client IS one of the gate's 6 tracked crates and was included; 6 [package] versions + 6 internal path-dep pins → 0.66.1; tools/sign-fixture (0.1.0) + root repository/homepage (always-further/nono, Deferred Idea) untouched; Cargo.lock regenerated via `cargo build --workspace --all-targets` with exactly 6 workspace-member hunks, zero third-party drift; clippy/fmt/audit/nono+nono-ffi tests green |
| make ci not fully green on dev host — nono-cli test leg has 11 pre-existing env-state failures, NOT this plan's doing | 100-01 | Zero source files changed (Cargo.toml×6 + Cargo.lock only); failures are leftover dev-host state (my-agent.json dated May 26 → profile_cmd "already exists"; env-lock PoisonError cascade; 17-vs-1 session dirs) — documented in memory nono_cli_windows_baseline_test_failures.md as baseline; out of scope per deviation SCOPE BOUNDARY |
| ADR-100: #1245/#1251 ADAPTED not adopted verbatim — publish-crates idempotency clean 1:1 port; cross-compile job trigger rewritten from dead upstream 'chore: release v...' PR-title convention to workflow_dispatch | 100-02 | Fork's release model is manual tag-push/workflow_dispatch (release.yml), never a release-please-bot PR — verbatim adoption would add a permanently-false if: condition (silent false-assurance gap); #1251's pre-corrected quoted-string if: form applied inline from the start; release-readiness verify-dark gate + signed-MSI sign-before-harvest order (D-06) confirmed untouched by either hunk |
| Cross-repo binding bump finished 0.66.0→0.66.1 in both nono-py + nono-ts, one DCO-signed commit each (nono-py 84e8f18, nono-ts dd7d416); no 0.66.0 commit ever landed | 100-03 | D-01/D-02/D-07: both repos already had stale uncommitted 0.66.0 edits from a prior session — finished in place rather than starting from committed HEAD; publish stayed operator-gated (maturin build + npm publish --dry-run only, no push) |
| nono-py RouteConfig PyPI blocker (RLS-12) closed: endpoint_policy: None added at both RouteConfig construction sites (src/proxy.rs, src/policy.rs); Rule 3 auto-fix also added ProxyConfig.enable_h2: false (same-class binding drift, only surfaced by actually running maturin build, not by static grep) | 100-03 | maturin build now exits 0; both fields are no-op stubs matching nono-proxy's own defaults, not policy/business-logic changes; full endpoint_policy threading deferred to a named future phase per D-08 |
| RLS-13 closed: release-readiness gate re-pointed targetVersion=0.66.1/upstreamHighest=0.66.0; both mandatory pre-push gates re-run GREEN; RELEASE-RUNBOOK.md fully updated for the 0.66.1 tag | 100-04 | Rule 1 auto-fix: gate assertion (e) hardcoded a stale '0.66.0' Cargo.lock literal instead of referencing $targetVersion — Cargo.lock now contains zero 0.66.0 occurrences (all 6 crates at 0.66.1), so the old check would have false-FAILed the very re-green this plan exists to deliver; rewritten to reference $targetVersion |
| release-dry-run.ps1 twine-detection bug fixed: false-positived on Python's own "No module named twine" error text (contains the substring "twine") instead of checking $LASTEXITCODE, masking a real toolchain-absent SKIP as a hard FAIL | 100-04 | Discovered on first re-run after the version bump (pypi.twine_check FAILed with exit 1); switched detection to $LASTEXITCODE; re-run now correctly SKIPs with SKIP_HOST_UNAVAILABLE and the overall dry-run exits 0 with zero FAIL keys |
| Checkpoint clean-host-install gate verdict pre-resolved by orchestrator; SKIP_HOST_UNAVAILABLE/exit 3 operator-accepted ("Accept SKIP & close phase") and recorded verbatim on the msi-vcredist-prereq todo rather than re-run | 100-05 | Plan's `<how-to-verify>` treats SKIP_HOST_UNAVAILABLE/exit 3 as the expected, acceptable outcome per CONTEXT.md D-09; re-running would have been redundant |
| Neither host-gated todo marked resolved: msi-vcredist-prereq stays open pending a genuine clean Win11 VM run; poc-cert-broker-clean-host stays open pending both an actual 0.66.1 tag push (operator-gated) and a fix to the Azure Trusted Signing verify-gate UnknownError (quick 260630-trusted-signing-golive) | 100-05 | Fold-without-resolve pattern (mirrors v3.1 Phase 90 DRAIN) — a todo can be formally documented at milestone close without claiming false completion |

### Pending Todos

Two host-gated distribution todos are IN SCOPE for v3.5 (CHOST-03 / FUT-03 drain), to be run on the Azure Win11 VM (Phase 106):

- **`20260611-poc-cert-broker-clean-host.md`** — after a trusted-signed `0.66.1` release, confirm `nono run --profile claude-code` spawns the broker on a clean host with NO manual cert-trust step. Precondition (1) — the verify-gate `UnknownError` — is **CLEARED as of 2026-07-04**. Now blocked solely on (2): cutting a real trusted-signed `0.66.1` (Phase 104 Plan 03, operator-gated tag push).
- **`20260611-msi-vcredist-prereq.md`** — confirm the machine MSI installs on a fresh Win11 host with no VC++ redist (code fix `+crt-static` already shipped/verified; only the clean-VM empirical confirmation remains).

A third todo is v3.7-scoped and NOT host-gated — it is a design pass, sized accordingly:

- **`20260816-ci-gates-outlived-their-scope.md`** — three linked findings from the 2026-08-15 CI triage, grouped because each is *a gate that was correct when written and silently stopped covering reality*. (1) **libdbus in the Linux release binary** — both Linux cross-compile gates red from ONE cause: `keyring` v3 `sync-secret-service` (`nono-cli/Cargo.toml:154`) pulls `libdbus-sys`, present since Initial Commit and never observed because CI had never run this branch. ⚠ Adding `libdbus-1-dev` is NOT the fix — the portability gate runs on `runner.os == 'Linux'`, i.e. BOTH targets, so it would only move the failure and would weaken a probable forcing function. The real question is a product decision about **where Linux credentials live**. (2) **The Phase 102 rename tail** — live stale `-p nono-cli` in `phase-45`/`phase-46` workflows (11 sites) plus cosmetic script strings; `release.yml`'s are DELIBERATE (`if: false`, Phase 105/PUB-02 owns them) and must be left alone. (3) **CI toolchain floats** (`@stable` → 1.97.0) while the dev host trails (1.95.0), so a 1.97-only clippy lint failed CI while local clippy was clean — CLAUDE.md's rule pins a target set, not a toolchain version, and is structurally blind to this class. Plus the pattern fix: a gate asserting every `-p`/`--package` selector resolves against `cargo metadata`.

### Blockers/Concerns

- **Phase 110 Plan 06 (Windows WFP-native port-range emitter) BLOCKED on operator checkpoint — `checkpoint:human-verify`, PROF-03e.** Tasks 1-2 committed and fully verified (build/clippy/fmt clean, unit tests pass, `MACOS_PORT_RANGE_LIMIT` confirmed absent from the Windows backend). Task 3 requires an Administrator-elevated `nono-wfp-service.exe` session to prove the range filter enforces correctly at the kernel level (in-range connect succeeds, out-of-range blocked) and appears as exactly ONE `FWP_MATCH_RANGE` filter object (not per-port unrolled) — this cannot be exercised from the current non-elevated dev session. See `110-06-SUMMARY.md` "Pending Checkpoint" for the exact operator steps. **Phase 110 cannot close (110-08 phase gate) until this is resolved.**
- **✅ RESOLVED 2026-07-04 (verified live 2026-07-29) — Azure Trusted Signing verify-gate `UnknownError`.** Root cause was the certificate profile type, not the runner and not the code: `NonoCertProfile` was `profileType = PrivateTrustCIPolicy`, whose chain is never publicly trusted. Fixed by creating `NonoPublicTrust` (`PublicTrust`, public identity validation `20cb70d3-…`) and repointing `TRUSTED_SIGNING_PROFILE`; smoke run `28692172728` then returned `Status=Valid` via `CN=Microsoft ID Verified CS EOC CA 03`. The fail-closed `-ne 'Valid'` gate was never loosened — it correctly refused to pass a genuinely-untrusted chain for three weeks. **The historical narrative below is retained as the diagnostic record; every "still open" phrasing in it is superseded.** Do not re-open the root-CTL-propagation theory or re-run `check-trusted-signing-root.ps1` as a gate.
- **[HISTORICAL — superseded by the entry above] Azure Trusted Signing verify-gate `UnknownError`:** first live smoke run (`28467925298`, 2026-06-30) — OIDC login + Sign succeeded (signer `CN=TWGGLOBAL.onmicrosoft.com`) but Verify FAILED `Status: UnknownError`, issuer `CN=Microsoft Enterprise ID Verified Policy AOC CA 01`. Two causes were flagged to settle: (1) confirm the profile is **Public Trust**; (2) `UnknownError` ≠ `UntrustedRoot` → chain couldn't be *built* on `windows-latest`. **Update 2026-07-03 (live re-run, authoritative):** (1) is now CONFIRMED SETTLED — profile is `PublicTrust` (`101-SIGN01-FINDING.md`) — but the research's issuer-naming heuristic used to reason about (1) is **disproven**: `PublicTrust` chains through `…Enterprise ID Verified Policy AOC CA 02…` too (run `28636133664`), so issuer-string pattern-matching must not gate future checks. (2) remains the live open root cause: a runner-side chain-build/revocation-validation failure on GitHub's `windows-latest`, not resolved by re-verifying with `signtool verify /pa /v` yet (D-04 diagnostic output did not flush before the throw — a real gap, flagged for Phase 104). `release.yml:259` uses the same fail-closed `-ne 'Valid'` check, so a release tag aborts at verify until this is `Valid` — this gate must NOT be loosened. Full evidence in `101-SIGN03-SMOKE-VERDICT.md`. Runbook: `.planning/quick/260630-trusted-signing-golive/AZURE-TRUSTED-SIGNING-GOLIVE-COOKBOOK.md`. See [[azure_trusted_signing_golive]].
- **Correct GitHub OIDC FIC subject** (both workflows): `repo:OscarMackJr/nono:environment:Development` — the old `260603-i31` cookbook's `oscarmackjr-twg` + `ref:` subject is STALE and causes `AADSTS700213`.
- **Operator's corporate host is NOT a valid clean host** — POC cert previously imported + VC++ installed + corporate proxy/EDR/managed-trust-store confound the exact chain-build/revocation failure being debugged. Clean-host UAT must run on a fresh Azure Win11 VM (off corporate LAN, clean revocation egress, never trusted the POC cert, no VC++) — Phase 106.
- **Base crate `0.66.1` already prepared (v3.4)** — all 6 workspace crates + both binding repos at `0.66.1`; both mandatory pre-push gates GREEN; RELEASE-RUNBOOK.md current. v3.5 executes the actual go-live from this prepared state. Do NOT publish `0.66.0`.
- **Repo stays PUBLIC** (minifilter altitude 377813.5 received 2026-07-01; go-private retired). Verify no `build_notes/`/`.gsd/` staged before any push. Unlike prior prepare-only tags, v3.5's tag push is *intended* (go-live).
- **All commits DCO-signed**: `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>` on every commit.
- **Cross-target clippy MUST be GREEN** if any cfg-gated Unix code is touched: Docker `cross` (linux-gnu) + zig `cargo-zigbuild` (apple-darwin) exit 0 locally — PARTIAL→CI retired (v3.3 Phase 96). (v3.5 is primarily CI-yaml + PowerShell + IaC; likely low Unix-cfg exposure, but the rule stands.)
- **Registry ownership pre-resolved, not a live blocker** — crates.io `nono`/`nono-proxy`, PyPI `nono-py`, npm `nono-ts` are upstream-owned; the fork publishes under fork-owned `nono-sandbox` family names instead of requesting a co-owner grant (PUB-01, Phase 102). Each new name's availability must still be confirmed live before committing to the rename.
- **CLOSE-01 secret-retirement ordering is a structural gate, not a checklist item** — Phase 107 must NOT run until Phase 106 (CHOST-03) has a passed verdict; retiring POC secrets after only Phase 104 (release green) leaves no fallback signing path if clean-host UAT reveals a problem invisible on CI runners.
- **GitHub Trusted Signing config CORRECTION (2026-07-02)** — Phase 101 Plan 03 originally reported no `TRUSTED_SIGNING_ACCOUNT`/`_PROFILE`/`_ENDPOINT` variables or OIDC federated credential existed on the `OscarMackJr/nono` repo. Independent verification found this **false**: repo-scoped variables (account `ArtifactNono`, profile `NonoCertProfile`, endpoint `https://eus.codesigning.azure.net/`) and the OIDC FIC were both provisioned 2026-06-04 and confirmed working by smoke run `28469674206` (2026-06-30, OIDC+Sign GREEN; only Verify failed). Likely cause of the false report: the operator checked the empty `Development` **environment**-scoped variables page, not the populated **repo**-scoped variables the workflow actually reads (no `Development` environment exists on the repo at all). This reconciliation is pending explicit operator confirmation but is treated as authoritative absent contrary evidence. See corrected `101-SIGN01-FINDING.md` / `101-SIGN03-SMOKE-VERDICT.md`.
- **[HISTORICAL — WRONG, retracted 2026-07-03/04] Residual `UnknownError` root cause still open (profile-type hypothesis ruled out)** — this entry's premise is false. The portal-sourced "confirmed `PublicTrust`" was incorrect; an `az rest` management-plane GET showed `profileType: PrivateTrustCIPolicy`. The profile-type hypothesis was in fact **correct** and the chain-build/revocation theory was the dead end. Kept only to prevent someone re-deriving the retracted reasoning. Original text: the live Azure profile is confirmed `PublicTrust`, so the 2026-06-30 smoke-run `UnknownError` is NOT a profile-type issue. True cause (chain-build staleness or CRL/OCSP revocation transient) remains to be diagnosed via Plan 01's `Assert-TrustedSignature` D-04 chain-introspection output on the next smoke run. This is the one genuinely unresolved technical question left open by Phase 101.
- **✅ SIGN-03 GREEN 2026-07-04** — the hardened smoke workflow was pushed and dispatched; run `28692172728` succeeded against the new `NonoPublicTrust` profile (`Status=Valid`). SIGN-01/02/03 all satisfied; `REQUIREMENTS.md` should be reconciled from Failed/Deferred → Complete at the next v3.5 touch. **The entry below is the superseded 2026-07-02 record.**
- **[HISTORICAL — superseded] SIGN-03 BLOCKED/Deferred (Phase 101 Plan 04) — hands off to Phase 104, reasoning corrected 2026-07-02** — GitHub Trusted Signing config (OIDC FIC + variables) is confirmed to already exist and work (see correction above); `gh` correctly resolves to `OscarMackJr/nono`. The real remaining gates: (1) Plan 02's hardened smoke workflow is unpushed — all Phase 101 commits are local-only, so a dispatch today would run the pre-hardening workflow; (2) pushing + dispatching live Azure Trusted Signing CI is outward-facing and requires explicit operator authorization under the fork's prepare-only/push-operator-gated posture; (3) the prior run's Verify `UnknownError` is unresolved. See `101-SIGN03-SMOKE-VERDICT.md` for the corrected reasoning (with the false premises explicitly retracted) and unblock preconditions. Phase 104's success criterion #1 requires push + operator authorization + a hardened re-run — NOT fresh GitHub OIDC/variable re-provisioning.

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 260624-p1c | Cargo Audit: bump quinn-proto past RUSTSEC-2026-0185 (remote memory exhaustion) | 2026-06-24 | 78b50f04 | [260624-p1c-cargo-audit-bump-quinn-proto-past-rustse](./quick/260624-p1c-cargo-audit-bump-quinn-proto-past-rustse/) |
| 260624-q98 | Remove orphan audit_ledger.rs + dead state_paths helpers (never compiled) | 2026-06-24 | e350df23 | [260624-q98-remove-orphan-audit-ledger-rs-and-its-de](./quick/260624-q98-remove-orphan-audit-ledger-rs-and-its-de/) |
| 260624-q9j | Fix red Docs Checks: force-add already-in-nav windows-win-1706-option-1-workstream.mdx | 2026-06-24 | 3475b470 | [260624-q9j-exclude-docs-cli-development-from-docs-c](./quick/260624-q9j-exclude-docs-cli-development-from-docs-c/) |
| 260625-crs | Phase 83 deferred code-review findings: WR-02/03/04/05 + IN-01/IN-03 (interpreter PATH-hijack, GetWindowsDirectoryW, canonical expander, validate(), gate probe, SID regex) | 2026-06-25 | 4af1e8f9 | [260625-crs-address-phase-83-code-review-deferred-fi](./quick/260625-crs-address-phase-83-code-review-deferred-fi/) |
| 260629-toe | UPST11 v0.66.0 parity phase definition: reviewed upstream PR #1293, authored divergence ledger for v0.65.1→v0.66.0 (19 PRs) + fork-invariant gates + 3-wave structure; #1225 NetworkIntent flagged HIGH-CONFLICT; 0.66.0 version collision → next release ≥0.67.0 | 2026-06-29 | (docs) | [260629-toe-v066-parity](./quick/260629-toe-v066-parity/) |
| 20260701-fltmgr-altitude | Wire official Microsoft altitude 377813.5 for nono-fltmgr.sys: live artifacts (INF/DESIGN/README) already done in 2041fc62; reconciled ADR-65 §5 amendment + STATE approval-RECEIVED. PUBLIC→PRIVATE flip flagged as operator decision (un-taken) | 2026-07-01 | e8ea5508 | [20260701-fltmgr-altitude-assignment](./quick/20260701-fltmgr-altitude-assignment/) |
| 260630-trusted-signing-golive | Live smoke run `28467925298` on the Azure Trusted Signing path — OIDC login + Sign succeeded, Verify FAILED `UnknownError`; authored the go-live cookbook (`AZURE-TRUSTED-SIGNING-GOLIVE-COOKBOOK.md`) that seeds this milestone's Phase 101/104 gates | 2026-06-30 | (docs) | [260630-trusted-signing-golive](./quick/260630-trusted-signing-golive/) |
| 20260705-readme-fork-divergence | Rewrote README.md from inherited upstream copy to fork-accurate: hard-fork notice, `OscarMackJr/nono` links, Windows AppContainer/WFP + ZT-Infra sections, build-from-source install (registry publish in-progress), `nono-sandbox`/`@oscarmackjr/nono-ts` package names, workspace-layout + upstream-relationship tables | 2026-07-05 | (docs) | [20260705-readme-fork-divergence](./quick/20260705-readme-fork-divergence/) |
| 260729-nh4 | **Security:** closed live RUSTSEC-2026-0204 — bumped `crossbeam-epoch` 0.9.18→0.9.20 in Cargo.lock (mirrors upstream `373a67ae` #1369, byte-identical). Advisory reached the shipped binaries via `ignore`→`nono-sandbox`/`-cli`/`-proxy`/`nono-shell-broker`. Found by Phase 108's DEPS-cluster `cargo audit` — the payoff of D-16 treating dependency commits as security-relevant, not noise. Satisfies Phase 112 SC3 out-of-band. | 2026-07-29 | (see log) | [260729-nh4-crossbeam-epoch-rustsec-2026-0204](./quick/260729-nh4-crossbeam-epoch-rustsec-2026-0204/) |
| 260727-jkn | Mapped macOS 0.69 (upstream nolabs-ai) parity gap for the Windows fork: 2 inventory files (upstream v0.64→v0.69 features + fork HEAD coverage) → phase/milestone map. KEY: upstream `tool-sandbox/` subsystem (PR #1105, v0.65.0) NEVER absorbed (0 files) — standing divergence, not version lag. Proposed **v3.6 UPST12 sync v0.66→v0.69** (deny_domain, SPIFFE, platform_overrides, port-ranges; leapfrog 0.70.0) + **v3.7 Windows Tool-Sandbox Parity** (command_policies engine + `platform/windows.rs` driver, decision-gated). Fork AHEAD on Job-Object resource caps. | 2026-07-27 | (docs) | [260727-jkn-map-macos-0-69-parity-gap-phases-for-the](./quick/260727-jkn-map-macos-0-69-parity-gap-phases-for-the/) |
| 20260813-lib-test-parallel-races | Fixed the two process-global races making `-p nono-sandbox --lib` intermittently red (a different test failed in each of the last two full runs — the one thing that can mask a real regression in a baseline comparison). **(1)** `with_test_session_token` saved/restored `NONO_SESSION_TOKEN`/`NONO_SESSION_ID` but held no lock while 9 tests called it concurrently with overlapping values → one test read another's live value. **(2)** the four `machine_policy` tests use DISJOINT child subkeys but each deletes the SHARED parent `SOFTWARE\nono-test` on teardown → `Os` code 1018 "marked for deletion". Both serialized on poison-tolerant mutexes (`lock_log_target_is_private_test` idiom). Also fixed a latent defect the finding missed: env restore ran only on the normal path, so a panic in `f()` leaked the token — now a `Drop` guard. 8/8 clean runs; recorded that repeated green runs are supportive but NOT proof, the structural serialization is. Residual: neither lock is mechanically enforced. | 2026-08-13 | d72350dc | [20260813-lib-test-parallel-races](./quick/20260813-lib-test-parallel-races/) |
| 260815-b0s | Closed Phase 117's **SC4 recording gap** (all four `missing` bullets) and applied the operator-accepted **SC3 override**. (1) `layer_force_unavailable.rs`'s module doc claimed `RestrictedToken`/`JobObjectContainment` were on `MANUALLY_VERIFIED`; they are on `ALSO_AUTOMATED` — verbatim the class Iteration 4's WR-07 fixed once in this same file. (2) New **sentence-scoped** gate `module_doc_assigns_each_claimed_row_to_the_list_it_is_on` binds each `LayerId` to the nearest preceding list identifier *within its sentence*: 5 claims genuinely verified (3 of them inside the both-lists paragraph the first design would have skipped), 13 non-claiming mentions held by an equality pin. Chosen over the checker's suggested count-pin because it removes the escape hatch entirely — there is no ambiguity/skip case to exploit. **Key discovery: at HEAD the defective claim was anaphoric** ("Both rows are therefore on the …list", naming no row), so the rewrite had to put the list name FIRST and both rows AFTER it or the gate would have been decorative. (3) Refreshed the stale `TODO(117-12)`. (4) Scoped the D-15 ledger to contract-vs-code divergences + indexed rounds 4/6/8 — all 20 findings resolved to a compile region and classified verification-machinery; none swept out. (5) `CR-02`/`RF-13` `OPEN` markers (**RF-13, not WR-12** — an unrelated `WR-12` row exists and would have resolved wrong); `marker_sources()` hoisted to module scope + new `every_marker_carrying_source_file_is_in_marker_sources` closing the allow-list into a discovered class. (6) SC3 override applied, every disclosure preserved (10/13, the 3 remainder rows, the host-blocked WR-20 pin, `--bin nono` RED 1688/12). 4 independent perturbation proofs. Production edits provably comment-only. | 2026-08-15 | 511e2ab1, fba34606, 868cd5f4, c9037b91 | [260815-b0s-close-the-phase-117-sc4-recording-gap-an](./quick/260815-b0s-close-the-phase-117-sc4-recording-gap-an/) |
| 260815-d22 | Closed out Phase 117 in the shared tracking records and corrected the WR-20/CI framing. `REQUIREMENTS.md`: CINT-01/02/03 flipped `[ ]`→`[x]` and `Pending`→Complete — **CINT-03's cell says "Complete via operator override 2026-08-15 — 10/13 rows automated; 3 host/structurally-gated rows … accepted, not met"**, so a reader scanning only the table cannot mistake it for a clean pass. `ROADMAP.md`: Phase 117 bullet `[ ]`→`[x]` with the stale "Awaiting code review iteration 6" clause replaced, and the Progress row `26/34`→`34/34`, Complete, 2026-08-15. **THE SUBSTANTIVE FINDING:** the WR-20 pin was recorded everywhere as "needs an elevated/CI Windows runner", implying work to be done. It is actually **already covered by an existing CI job, by construction** — the test is NOT feature-gated (plain `#[test]` in `#[cfg(test)] mod tests`, no nested gated mod), and `windows-layer-fault-injection` (`ci.yml:387`) runs `cargo test -p nono-sandbox-cli --features layer-fault-injection` with **no `--bin`/`--test`/`--lib` filter**, so it runs every target including the `nono` bin's unit tests. What is missing is a RUN: the branch is 684 commits ahead of origin and the last CI run on it (2026-07-29) predates Phases 115/116/117 entirely. Also recorded: the `test` job matrix is `[ubuntu-latest, macos-latest]`, so that job is the ONLY Windows Rust-unit-test gate anywhere in CI. ci.yml gained a comment so the fact is discoverable at the job — proven comment-only by YAML structural equality against base, not just by grep. Diff exactly 6/6 REQUIREMENTS + 2/2 ROADMAP lines; no v3.5 row touched. ⚠ Follow-up flagged not fixed: TSBX-01/TSBX-02 (`REQUIREMENTS.md:184-185`) still read `Pending` though Phase 116 is Complete — same under-statement. NOTHING PUSHED. | 2026-08-15 | 8729fbe8, 0cd637d6 | [260815-d22-close-out-phase-117-tracking-records-and](./quick/260815-d22-close-out-phase-117-tracking-records-and/) |
| 260815-f2x | **Fixed a FAIL-OPEN CI GATE that hid 22 test failures.** Found by the first-ever CI run on this branch (31888431118): job `windows-layer-fault-injection` reported **conclusion: success** while its own log reported `test result: FAILED. 1684 passed; 22 failed`. Mechanism: the step had no `shell:` override → **pwsh** default on `windows-latest`, where GH Actions appends `exit $LASTEXITCODE`, so the step's status is the LAST native command's exit code — the failing `cargo test -p nono-sandbox-cli` was swallowed by the passing `-p nono-shell-broker` that followed it. **This is the gate Phase 117 leaned on for CINT-03, and three records asserted it worked** (ci.yml's CR-10 comment "this is the build", `117-VERIFICATION.md`'s SC3 evidence, and SPEC rows RF-11/RF-12) — a fail-open verification gate recorded as a working one, i.e. the exact defect class P117 exists to close, inside P117's own CI wiring. Fixed by CLASS not instance: parsed every Windows job and split BOTH multi-command non-bash `run:` blocks into one step per command (`windows-layer-fault-injection` confirmed fail-open; `windows-packaging`'s 2×`cargo build` had the same latent shape — a broken `nono.exe` build would have reached packaging). Non-vacuity proven with ONE script run against both trees: pre-fix **3** flagged, post-fix **1** (the allowlisted MSI-contract step, which has an explicit `exit 1` and its `.ps1` last). RUSTFLAGS `-Dwarnings -C target-feature=+crt-static` duplicated onto both new packaging steps (moving it would have silently changed the build). Comments 33→48 lines, zero removals/duplications. D-15 routing decided on precedent: earns a **row (CI-01)** on the RF-10/WR-19 shape (the SPEC's own cells were false), NOT the round-4/6/8 machinery treatment — the discriminator is "does this document say something false", not machinery severity; round 6's WR-04 was also fail-open inside a gate but got no row because the SPEC claimed nothing about it. SC3's verdict untouched — `status`/`score`/`overrides`/`overrides_applied` verified byte-EQUAL by frontmatter parse. ⚠ Also found: two SPEC/selfcheck **line-count parentheticals in the Required Artifacts table are stale** (SPEC cited 372, actual 400 — corrected; `layer_registry_selfcheck.rs` cited 2418, actual 2400 — left, out of scope). ⚠ **THE FIX IS UNEXERCISED** — expect `windows-layer-fault-injection` to go correctly RED on the next push; the 22-failure triage is the open follow-up. NOTHING PUSHED. | 2026-08-15 | de77b1fd, 0a34dc44 | [260815-f2x-fix-the-fail-open-windows-ci-gate-that-h](./quick/260815-f2x-fix-the-fail-open-windows-ci-gate-that-h/) |
| 260815-gfd | **Fixed the 19 CI-only test failures** triaged in `.planning/debug/ci-windows-117-failures.md` (diagnosis: 3 proven causes, **NO product defect**). **Cluster A (15, ci-environment-artifact):** `path_is_owned_by_current_user` compares owner SID to the token's USER SID via `EqualSid` — SID equality, NOT privilege — so on the elevated runner, objects owned by `BUILTIN\Administrators` make every ownership gate short-circuit and REFUSE to grant (`applied: 0`, the fail-SAFE direction). Proven by a natural control already in the run: the 5 `dacl_guard` tests calling `take_ownership_for_current_user` PASSED, the 2 omitting it FAILED — near-twins. The helper's OWN doc comment already documented this; the 15 tests just never called it. Promoted it out of `dacl_guard::tests` into crate-level `test_ownership_windows.rs` (`#[cfg(test)]`+`#[cfg(windows)]`) so all 15 route through one implementation, preserving the control. **Cluster B (3, test-defect):** hardcoded `/tmp`→`C:\tmp`, which exists on this host and not on CI; fixed selectively (parse-only JSON fixtures and Seatbelt-escaping literals deliberately left, listed with reasons) — incidentally fixing a latent macOS defect where `/tmp` canonicalizes to `/private/tmp`. **Cluster C (1, test-defect):** `--no-pty` without `--app-container-name`, rejected fail-closed since `cb341165` (2026-06-02); hidden for 2.5 months by a STALE `target/x86_64-pc-windows-msvc/release/nono-shell-broker.exe` dated 2026-06-01 (deleted). Fixing it exposed a SECOND stale contract — the harness passed a null `lpEnvironment`, so the broker refused to resume an unattested child (`NONO_BROKER_REQUIRED_LAYERS absent`); fixed by mirroring production and deriving the value from `required_layers_for_broker(...)` so it cannot drift from a hardcoded list. **1694 passed / 12 failed** = the documented baseline name-for-name, zero regressions; clippy clean ±feature; fmt exit 0; every hunk verified inside a `#[cfg(test)]` region — NO production line changed. ⚠ **LOCAL GREEN IS NECESSARY BUT NOT SUFFICIENT** — this non-elevated host cannot construct `owner == BUILTIN\Administrators`, so taking ownership of an already-owned path is a no-op here; cluster A stays UNPROVEN until `windows-layer-fault-injection` runs. ⚠ **NEW FINDING FOR THE FOLLOW-UP:** 14 of 15 now genuinely reach their subject (CR-01 and WR-02 pins recovered), but `rb3_gate_tests::workspace_owned_by_current_user_passes_write_owner_check` now normalises the very fixture whose un-normalised state IS the elevated-operator reality — CI now covers the PASS branch and **nothing covers the elevated-workspace FAIL branch**. NOTHING PUSHED. | 2026-08-15 | 81811391, e15184ee, 5647c6f2, b4f54036 | [260815-gfd-fix-the-19-ci-only-test-failures-from-th](./quick/260815-gfd-fix-the-19-ci-only-test-failures-from-th/) |
| 260815-jxi | Fixed 3 of the 4 causes behind the non-Windows CI red (the 4th, libdbus, routed to planning). **(1) Clippy on all 3 hosts** — a single lint, `useless_borrows_in_formatting` on `&*self.token` (a `Zeroizing<String>`) in a `format!` arg at `nono-proxy/src/server.rs:157`. **Root cause is a TOOLCHAIN VERSION GAP, not cross-target drift:** local clippy is **1.95.0**, CI's `dtolnay/rust-toolchain@stable` is **1.97.0**, and the lint does not exist in 1.95 — CLAUDE.md's cross-target rule pins a TARGET SET, not a toolchain version, so it is structurally blind to this class. Fixed by CLASS: the executor found 2 more identical sites at `nono-cli/src/proxy_command.rs:394,396` that CI had NOT reported, because `-D warnings` aborted at nono-proxy before nono-cli was ever linted — they would have failed the next run one crate later. 3 production lines / 2 files; 48 other `&*` occurrences correctly left (raw-pointer derefs in FFI/Windows code). **(2) Integration Tests** — `tests/run_integration_tests.sh:31` used `-p nono-cli`, renamed to `nono-sandbox-cli` in Phase 102; proven by perturbation (`cargo tree -p nono-cli` reproduces the exact CI error, `-p nono-sandbox-cli` resolves). **(3) Docs Checks** — `--allow-http2` undocumented; description derived from the code (`pool.rs:210` ALPN for the UPSTREAM pooled client only; `sandbox_prepare.rs:811` ORs with the profile setting and there is no negation flag) rather than from the flag name. ⚠ **BASELINE CLARIFICATION — do NOT "correct" these to a single number:** `--bin nono` is **1688 passed / 12 failed / 2 ignored** (population 1702); `--bin nono --features layer-fault-injection` is **1694 / 12 / 2** (population 1708). The 6-test delta IS the feature-gated seam tests. Both are correct for their own invocation; the executor briefly concluded the 1694 figure was stale after comparing a no-feature run against it — measured and disproven. ⚠ Local clippy CANNOT prove the 1.97 lint satisfied (only 1.95 toolchains installed); next CI run settles it. NOTHING PUSHED. | 2026-08-15 | 8397e5b2, b5d3b1c6, 90b36fc0 | [260815-jxi-fix-the-three-mechanical-ci-failures-cli](./quick/260815-jxi-fix-the-three-mechanical-ci-failures-cli/) |
| 260802-e9u | Cleared 3 stale `.continue-here.md` checkpoints (v2.10/v3.5/Phase-108 era, all untracked) and applied both operator-approved Phase 109 amendments: `108-DIVERGENCE-LEDGER.md` redispositions `6fb7ecbf` (#1430) / `23d93fc9` (#1437) from `adopt` → **`won't-sync (target subsystem absent)`** across all 5 disposition-bearing locations (NET per-commit table, NET summary, cluster rollup, DOCS row for `63c9589f`, global totals — arithmetic re-verified 19+3+2+20+18+38=100), and `REQUIREMENTS.md` NET-03 + `ROADMAP.md` Phase 109 SC3 reworded from "are absorbed" to "confirmed non-applicable". Neither `crates/nono-proxy/src/aws/` nor `.../tls_intercept/` exists in the fork — independently re-confirmed. ⚠️ NET-03's original "HTTP/2 injection" clause was intentionally DROPPED per the finding doc's rationale; restorable if the operator disagrees. No checkbox or status value flipped. | 2026-08-02 | c1186b20, d6470630 | [260802-e9u-clear-3-stale-continue-here-md-files-and](./quick/260802-e9u-clear-3-stale-continue-here-md-files-and/) |
| 260904-wkv | **Consumer-side SPEC-AND-SCAFFOLD ONLY for the Nono↔Popeye Session Credential Contract v0.1** (canonical: `fiskroad/contracts/NONO_POPEYE_SESSION_CREDENTIAL_CONTRACT_v0.1.md` — referenced by path, deliberately NOT copied into this repo). Serves the `NONO_AGENT_RUNTIME_CHARTER_v0.1` phase, whose §7 entry criteria are **UNMET** — so this builds nothing: no containment/WFP/driver work, no HTTP client, no schema registration, no emission wiring. Delivered: (1) design note (launch-sequence placement — issuance BEFORE containment finalize, fail-closed per contract §3/REQ-CRED-05; injection boundary naming the EXISTING `crates/nono-proxy/src/credential.rs` path as the mechanism candidate without touching it; renewal state-machine sketch for §4.2 covering BOTH branches since D-4 is undecided); (2) the **D-5 proposal**, banner-labeled `PROPOSAL FOR JOINT REVIEW — NOT DECIDED`, preferring a sibling **`endpoint-receipt/0.1`** over extending `endpoint-detection/0.1`, with a draft-2020-12 JSON Schema that nothing in `crates/` reads or validates against; (3) `crates/nono/src/session_credential.rs` — policy-free core vocabulary mirroring contract §2.1/§2.3 field-for-field, a `SessionCredentialKey(Zeroizing<String>)` secret newtype with a HAND-WRITTEN redacting `Debug` and **no `Display` and no `Serialize` impl at all** (leak becomes a compile error, not a review item), a `WorkspaceVisibleSessionCredential` that structurally has no `key` field, and the D-1 issuer-identity placeholder as a `todo!()` default trait method returning `!` — **verified uncalled anywhere** (`grep` outside its own definition: empty), so `cargo test` stays green. **D-1..D-5 all left undecided.** ⚠️ **FOUR CONFLICTS RECORDED, NONE RESOLVED** (in the design note with file:line): (a) `receipt.rs:19-26` D-20 says verbatim "No mid-session field exists on `EnforcementReceipt` or will be added" vs the contract's §5 session WINDOW and §4.2 renewal CHAIN, both inherently mid-session — this is the load-bearing reason the D-5 proposal prefers a sibling schema; (b) `receipt_content_free_scan.rs` mechanically rejects new `String` fields on `EnforcementReceipt` (`session_id` is the ONE allowlisted exception), so `key_refs[]`/`agent_id`/`on_behalf_of` cannot be added there; (c) **`endpoint-detection/0.1` does not exist anywhere in this repo** — only 2 prose mentions in the charter, one of them naming it as an unmet §7 entry criterion, so contract D-5's Option A is not locally expressible today; (d) REQ-CRED-08 ("no shared/static/cross-session endpoint credential") sits against the existing `keystore.rs` `env://` scheme and the proxy's configured-credential injection. ⚠️ **`make clippy`/`make ci` IS RED ON THIS BRANCH AND WAS ALREADY** — 2 `dead_code` errors on `ReceiptWriter::session_id`/`file_path` in `crates/nono-cli/src/receipt_sink.rs`, a **paused-Phase-118** file on this task's do-not-modify list. Independently confirmed unrelated, not taken on trust: `nono-cli`/`nono-proxy`/`nono-shell-broker`/`bindings` contain **zero** references to `session_credential`, this task's commits touched only `crates/nono/src/{lib.rs,session_credential.rs}`, and `receipt_sink.rs` was last written by Phase 118 commits (`f81a009d`/`ad8a3d11`/`717cf0b7`). Scoped `cargo clippy -p nono-sandbox` is clean. **Phase 118's human-verify gate is untouched.** Gates re-run by the orchestrator, not just self-reported: `cargo test -p nono-sandbox session_credential` = **6 passed / 0 failed, `running 6 tests`** (non-zero, not a silent-zero selector), build + `cargo fmt --all -- --check` clean. | 2026-09-05 | bf487686, d96e4b81 | [260904-wkv-consumer-side-scaffold-for-the-nono-pope](./quick/260904-wkv-consumer-side-scaffold-for-the-nono-pope/) |

## Deferred Items

Items acknowledged and deferred at **v3.6 close (2026-08-08)** — `gsd-sdk query audit-open` reported **57** open artifacts, operator acknowledged-all. None are v3.6 blockers:

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| Historical | 43 open quick-tasks (all `missing` — directories cleaned up, index stale) | Acknowledged | v3.6 close |
| Historical | 6 seeds SEED-001…006 (all dormant or consumed) | Acknowledged | v3.6 close |
| Host-gated | 2 todos — `20260611-msi-vcredist-prereq`, `20260611-poc-cert-broker-clean-host` (both need the clean Win11 VM; v3.5 Phase 106 scope) | Open (host-gated) | v3.6 close |
| UAT gap | `111-HUMAN-UAT.md` — 2 open scenarios | Open (partial) | v3.6 close |
| UAT gap | `112-HUMAN-UAT.md` — 3 open scenarios (full 36-binary integration sweep; python3-dependent socket tests) | Open (partial) | v3.6 close |
| Verification | `111`, `112`, `113` VERIFICATION.md = `human_needed` — each operator-approved at its own gate | Acknowledged | v3.6 close |
| Verification | `101-VERIFICATION.md` = `human_needed` — belongs to the **still-open v3.5**, not v3.6 | Open (v3.5 scope) | v3.6 close |

**v3.6 audit findings carried to v3.7** (from `milestones/v3.6-MILESTONE-AUDIT.md`): NEW-01 (3 denial sites emit no `denial_category`, incl. `connect.rs:86` = `deny_domain`'s HTTPS enforcement point; 2 spine variants with zero production constructors), NEW-03 (`aws_auth`+`capture` validates but is unreachable), NEW-05 (`inject_mode`/`inject_header` reset-by-silence in `merge_custom_credential_def`), NEW-06 (nono-py decoder rejects the two Phase 114 capture strings its own encoder emits), NEW-07 (nono-py cannot express `capture`/`spiffe`), NEW-08 (SPIRE mint before deny check). Plus per-phase review debt: 12 Warning + 5 Info from `112-REVIEW.md` (notably `--allow-gpu` now fatal on non-NVIDIA/WSL2), 9 Warning + 6 Info from `114-REVIEW.md`.

**Residual security risk consciously accepted at v3.6 close** (full reasoning in `114-VERIFICATION.md`): WR-02 (case-sensitive backstop), WR-03 (trailing-dot host), WR-05 (route-sharing), NEW-04 (`direct_connect_ports`). Each requires an unusual operator grant, a deliberately malformed request, or an ambiguous hand-authored profile — NEW-02 required none of those, which is why it was fixed and these were not.

---

Items acknowledged and deferred at **v3.2 close (2026-06-23)** — `gsd-sdk query audit-open` reported 47 open artifacts, user acknowledged-all. All historical or host-gated; none blockers:

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| Historical | 36 open quick-tasks (Mar–Apr 2026 dates, all `missing`/cleaned-up) | Acknowledged | v3.2 close |
| Historical | 6 seeds SEED-001…006 (all consumed or dormant; SEED-005 = v3.2 scope, delivered) | Acknowledged | v3.2 close |
| Historical | 4 empty/"None" todo parse artifacts | Acknowledged | v3.2 close |
| Host-gated | OVERRIDE-02 (DF-02) live allow/revoke proof — needs ZT-Infra provisioner + openssl + elevated session; SKIP_HOST_UNAVAILABLE by design | Open (host-gated) | v3.2 close |
| PARTIAL→CI | Cross-target clippy (linux-gnu + apple-darwin) for ZTL-04 `AWS_*` strip | Open (CI-decisive; may resolve in Phase 96) | v3.2 close |

Prior carry-forwards from v3.1 close (2026-06-21): SEC-01/SEC-02 AF_UNIX+procfs guards (PARTIAL→CI), DRAIN-01/02/03 live host-gated UAT, 2 env-sensitive Phase-74 DACL-guard tests.

Items acknowledged and deferred at **v3.3 close (2026-06-26)** — `gsd-sdk query audit-open` reported 48 open artifacts (after resolving both HUMAN-UAT gaps), user acknowledged-all. All historical or future-scoped; none v3.3 blockers:

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| Historical | 40 open quick-tasks (Mar–Apr 2026 dates, all `missing`/cleaned-up) | Acknowledged | v3.3 close |
| Historical | 6 seeds SEED-001…006 (all consumed; SEED-006 = UPST9 window, delivered v3.1) | Acknowledged | v3.3 close |
| Future-scoped | 2 todos FUT-02 (poc-cert-broker clean-host) + FUT-03 (msi-vcredist-prereq) — Azure Trusted Signing distribution milestone | Acknowledged | v3.3 close |

**Resolved at v3.3 close (not deferred):** 95-HUMAN-UAT cross-target clippy CI-lane confirmation → superseded by Phase 96 local cross-target toolchain (both gates GREEN locally); 97-HUMAN-UAT WR-02 publish-set divergence → operator chose the 3-crate set, reconciled. The v3.1/v3.2 PARTIAL→CI cross-target carry-forwards are likewise retired — Phase 96 made both Unix clippy gates locally runnable.

**Operator-action carry-forwards into v3.4 (PREPARE-ONLY — outside any prior milestone):** (1) nono-py `RouteConfig` missing `endpoint_policy` field (`src/policy.rs:743` + `src/proxy.rs:206`) — **CLOSED 2026-07-02, Phase 100 Plan 03 (RLS-12)**: `endpoint_policy: None` added at both sites, `maturin build` exits 0; (2) downstream crate `cargo publish --dry-run` stays PRE_PUBLISH_REGISTRY_BLOCKED until `nono 0.66.1` is on crates.io; (3) follow updated RELEASE-RUNBOOK.md (at 0.66.1) for the tag push + publish.

Items acknowledged and deferred at **v3.4 close (2026-07-02)** — `gsd-sdk query audit-open` reported 50 open artifacts, user acknowledged-all. All historical, dormant, or host-gated; none v3.4 blockers:

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| Historical | 41 open quick-tasks (Mar–Apr 2026 dates, all `missing`/cleaned-up) | Acknowledged | v3.4 close |
| Dormant | 6 seeds SEED-001…006 (all consumed/dormant; delivered across prior milestones) | Acknowledged | v3.4 close |
| Host-gated | 2 todos — `msi-vcredist-prereq` (clean Win11 VM MSI install) + `poc-cert-broker-clean-host` (trusted-signed release + broker spawns out-of-box) — FUT-02/03 Azure Trusted Signing distribution; folded-without-resolve in Plan 100-05; both now IN SCOPE for v3.5 CHOST-03 (Phase 106) | In progress (v3.5 Phase 106) | v3.4 close |
| Product-decision | `99-HUMAN-UAT` partial (3 open scenarios): WR-01 unused `sigstore-trust-root 0.9.0` direct-dep pin (keep-vs-remove), WR-03 `validate_block_net_conflicts` vs strict-filter semantics (fail-closed today, consistency not vuln), WR-02 `--allow-http2` runtime no-op (`UpstreamPool` absorbed but not yet wired — advertise-or-wire before H2 release notes) | Open (non-blocking) | v3.4 close |

**Blocked-external (now the v3.5 headline target, not merely tracked):** the signed `0.66.1` release was blocked on the open Azure Trusted Signing verify-gate `UnknownError` (quick `260630-trusted-signing-golive`) — signing works, verify fails; v3.5 Phase 101 exists specifically to resolve this before Phase 104 cuts the release.

## Session Continuity

Last session: 2026-08-16 — **v3.7 Phase 118 context gathered.** `118-CONTEXT.md` + `118-DISCUSSION-LOG.md` written (commit `aaff4459`), 24 decisions (D-01..D-24) across 4 gray areas: emission completeness & abort semantics, delivery channel + verification, chain choice + receipt shape, cross-binary coverage. Scouting established that **Phase 117 shipped far more of 118's foundation than the roadmap implies** — `LayerAttestationStatus` is already public/core/platform-neutral and its own doc names 118 as consumer; `LayerId` (13 variants), `EntryPath`, `LayerApplication`/`AppliedLayers`, `attest_and_decide`, `advance_and_emit`, and three discovery-based source-scan test suites all exist. Load-bearing decisions: receipts get their **own chain domain** (not the telemetry chain — interleaving two sinks on one HMAC chain makes the receipt sink structurally unverifiable on its own, so RCPT-02's "same terms as" must mean same *construction*, not same chain instance); the **receipt type + `LayerId` move into `crates/nono`** (only an exhaustive enum makes "all 13 rows covered" a compile-time property, and core is the one place all three binaries can reach — `nono-shell-broker` already depends on core, `nono-agentd` can reach core but not `exec_strategy_windows/`); **all four attestation states carried verbatim** (RCPT-03's three are a floor — collapsing would make the WFP row read as a failure on every ordinary session); a **dedicated operator-ACL'd sink** guarded by DENY ACE + `NO_READ_UP` (supervisor and child share a user identity, so a plain DACL cannot separate them, and `WRITE_RESTRICTED` tokens check restricting SIDs on writes only). **117's open Event-Log-readability verification item was RUN live** (`wevtutil gl Application`): `S-1-1-0` absent and `ALL APPLICATION PACKAGES` is write-only — so 117's stated worry measured clean — but `INTERACTIVE` and `WRITE RESTRICTED` both carry `0x3` (read+write) and there is no mandatory-label section, so **the assumption is not cleared; the plausible read path moved to `INTERACTIVE`**. Full SDDL recorded verbatim in `118-CONTEXT.md` `<specifics>`. Next session: `/gsd:plan-phase 118` (research-first was already chosen).

Previously: 2026-08-05 — executed Plan 112-08 (Phase 112 close-out, Wave 4) to completion. Appended the Phase 112 Security + Residual Sync Addendum to `108-DIVERGENCE-LEDGER.md` (18 SHA dispositions as shipped, 3 corrections to the pre-execution disposition table) + the SEC-09 Carry-Forward Note; reconciled `REQUIREMENTS.md`/`ROADMAP.md` (10 requirements Complete, SEC-02 carved out to Phase 114); combined-surface verification via a zero-crates-diff evidence chain + fresh `cargo fmt`/`cargo test --workspace` sweep (17-name failing set, strict subset of the 27-name baseline, zero new names). **Phase 112 is fully CLOSED — 8/8 plans, 10/11 requirements Complete (SEC-02 explicitly deferred to Phase 114).** Encountered the SDK STATE.md hazard again mid-session (`state.advance-plan` deleted this file's `stopped_at` content down to 8 lines) — reverted via `git checkout -- .planning/STATE.md` before any commit; all state updates this session were hand-edited. Next session: `/gsd:plan-phase 113` or `/gsd:plan-phase 114` (operator picks which runs first — 114 depends on 112 having landed `proxy_command.rs`, already true; 113 is independent).

Previously: 2026-07-31 — executed Plan 110-08 (phase gate) to completion. Both mandatory cross-target clippy gates confirmed GREEN live, both sibling bindings rebuilt green with no fix needed, SC3/ADR-86 fork invariants confirmed, all 4 Wave-0 gaps closed with live test evidence. `cargo test --workspace` honestly documented as non-green (24 pre-existing failures across 4 test binaries, none in Phase 110 files — see `deferred-items.md` § Plan 08). **110-08 deliberately did NOT mark PROF-03 complete or close Phase 110** — Plan 110-06's Task 3 `checkpoint:human-verify` (PROF-03e, live-kernel `FwpmFilterAdd0` proof) remains UNRESOLVED, pending an Administrator-elevated operator session. Next session: operator resolves that checkpoint, then run `requirements.mark-complete PROF-03` and flip the Phase 110 ROADMAP checklist box to actually close the phase.

Previously: **v3.6 Phase 110 Plan 06 PARTIAL — Tasks 1-2 COMPLETE, Task 3 checkpoint PENDING (2026-07-30).** Windows WFP-native port-range emitter: `WindowsNetworkPolicy.localhost_port_ranges`/`WfpRuntimeActivationRequest.localhost_port_ranges` threaded through `compile_network_policy` (via `merge_port_ranges`) and `build_wfp_runtime_activation_request` (Task 1, `ff10f52f`); `nono-wfp-service.rs`'s `PortCondition::RemoteRange`/`LocalRange` + `build_policy_filter_specs`'s two new sibling loops + `add_policy_filter`'s `FWP_MATCH_RANGE`/`FWP_RANGE0` construction arm, confirmed against the real vendored windows-sys 0.59 bindings (Task 2, `ca8b3f25`); Rule 2 follow-up surfacing ranges in the WFP activation diagnostic (`8fe69f43`). 7 new tests prove exactly-one-filter-per-range-per-layer (never per port, D-06), range-alone triggers the block fallback, and discrete+range coexistence is unregressed. `cargo build --workspace --release` succeeds (after clearing a stray leftover `nono-agentd.exe` process holding a file lock, unrelated host-state cleanup). **Task 3 is an unresolved `checkpoint:human-verify` (PROF-03e) — `FwpmFilterAdd0` requires Administrator elevation this dev session lacks. Plan 06 is NOT complete; PROF-03 is NOT marked complete; do not advance to 110-07/110-08 as if Phase 110 wave 2 finished.** See `110-06-SUMMARY.md` "Pending Checkpoint" for exact operator steps (start `nono-wfp-service.exe` elevated, run a range-profile `nono run`, confirm in-range connect succeeds / out-of-range blocked, confirm exactly ONE kernel filter object via `netsh wfp show filters`). Commits `ff10f52f`/`ca8b3f25`/`8fe69f43`/`3a8e58a8`.

Previously: **v3.6 Phase 110 Plan 05 COMPLETE (2026-07-30)** — `open_port_range` wired end-to-end to `CapabilitySet.localhost_port_ranges()` through both the profile pathway (`capability_ext.rs::CapabilitySet::from_profile`, a 3-line sibling loop next to `open_port` calling `add_localhost_port_range(start, end)?`, already validated by 110-04's `profile_runtime.rs`) and the independent manifest pathway (`manifest_convert.rs::TryFrom<&CapabilityManifest>`, consuming a NEW `PortConfig.localhost_range` typify-source schema property, with its OWN start<=end and macOS cumulative-cap validation since a `--config` launch never touches `profile_runtime.rs`). `profile_cmd.rs`'s `cmd_show` and `resolve_to_manifest`, plus `output.rs`'s `print_capabilities`, gained display support for the new fields. Both mandatory cross-target clippy gates (linux-gnu via `cross`, apple-darwin via `cargo-zigbuild`) ran clean; `cross test` additionally executed (not just compiled) both new manifest-pathway tests on real Linux (18/18 pass). Commits `4d635305`/`07a45e22`. See `110-05-SUMMARY.md`. Next: remaining Wave 2 plan (110-06 Windows WFP-native range emitter), then Wave 3 (110-07) and the phase-gate plan (110-08).

Previously: **v3.6 Phase 110 Plan 04 COMPLETE (2026-07-30)** — `NetworkConfig.open_port_range`/`listen_port_range` (`Vec<[u16; 2]>` array-of-pairs, matching upstream `d5803b99`'s JSON shape) added, wired through `merge_profiles`'s 3rd exhaustive-literal site (`dedup_append` union, same as `open_port`/`listen_port`) and both existing test-fixture literals, plus a fork-only `nono-profile.schema.json` companion edit (upstream's own commit never touched this strict-schema file) closing the Pitfall 4 / T-110-12 gap. `profile_runtime.rs`'s `prepare_profile_with_context` now validates both range fields via new standalone `validate_port_ranges`/`build_listen_ports`/`check_macos_port_range_cap` helpers: a hard parse-time error for any reversed `[start, end]` entry, a macOS-only cumulative cap pre-check against `nono::capability::MACOS_PORT_RANGE_LIMIT` (defense-in-depth duplicate of the Plan 110-03 sandbox-layer check), and `listen_port_range` unrolling into the existing discrete `PreparedProfile.listen_ports` carry-through (NOT a new `CapabilitySet` field) with a hard rejection for any range starting at port 0. `check_macos_port_range_cap` deliberately gated `#[cfg(any(target_os = "macos", test))]` so its error path is unit-tested on every host, not just macOS CI. Both mandatory cross-target clippy gates (linux-gnu via `cross`, apple-darwin via `cargo-zigbuild`) clean given the new `#[cfg(target_os = "macos")]` block. Commits `f7008b72`/`4fe0dcde`. See `110-04-SUMMARY.md`.

Then: **v3.6 Phase 110 Plan 03 COMPLETE (2026-07-30)** — `CapabilitySet` port-range mechanism (`MACOS_PORT_RANGE_LIMIT`, `merge_port_ranges`, `localhost_port_ranges` field/builders, ported from upstream `d5803b99`) plus both Unix sandbox-backend emitters: macOS Seatbelt unrolls ranges into per-port rules under a cumulative 16,384-port cap (checked before any profile string is built, SIGILL rationale preserved verbatim), Linux Landlock unrolls with NO cap (confirmed via the vendored `landlock-0.4.4` source at `net.rs:109` that no native range rule type exists — closes the previously-UNVERIFIED question from `110-PATTERNS.md`/`110-RESEARCH.md`). Linux seccomp proxy-only fallback (`supervisor_linux.rs`) gained `proxy_bind_port_ranges` (union semantics with the existing discrete `proxy_bind_ports` list). Rule 1 auto-fix: a missing `#[cfg(target_os = "linux")]` guard on 9 new test-fixture literals (caught by the mandatory apple-darwin cross-target clippy `--all-targets` gate, E0560 on 8 sites) was fixed before the task commit landed. Both mandatory cross-target clippy gates (linux-gnu via `cross`, apple-darwin via `cargo-zigbuild`) clean; Linux `cross test` additionally executed and passed both new `sandbox::linux` port-range tests (Wave-0 gap PROF-03c closed). Discovered but NOT fixed (out of scope, documented in `deferred-items.md`): a pre-existing dormant compile break in `supervisor_linux.rs`'s `mod tests::network_decision` predating this plan entirely (traced via `git log -L`/`git stash` to commit `1a804977`, 2026-06-26), which blocks execution (not compilation-via-clippy) of this plan's 3 new `supervisor_linux` tests on Linux. Commits `a0bf9076`/`0c747739`/`5d0c8e34`/`2102f538`/`a6ac7d46`. See `110-03-SUMMARY.md`. Next: Wave 2 (Plans 110-04/05/06, parallel — profile schema, capability_ext.rs/manifest wiring, Windows WFP emitter).

Then: **v3.6 Phase 110 Plan 01 COMPLETE (2026-07-30)** — `platform_overrides` per-OS profile-patch model (`ae1c513e`/`719975cf`) ported: `PlatformOverrides`/`PlatformOverride` types + custom nested-rejection `Deserialize`, `apply_platform_overrides` wired as the first step of `finalize_profile`, `merge_platform_overrides`/`merge_platform_override_slot` D-10 deep-merge (survives `extends` resolution intact), post-merge re-validation of custom_credentials/env_credentials/set_vars, and a `platform_overrides` schema property. D-08a preserved verbatim — `windows_low_il_broker`/`windows_interpreters` keep Phase 51/71 fail-secure OR/union `merge_profiles` semantics; `merge_profiles_or_semantics_base_true_child_false` confirmed unmodified+passing. 14 new tests (`platform_overrides_tests`), both cross-target clippy gates (linux-gnu via `cross`, apple-darwin via `cargo-zigbuild`) clean, `cargo fmt --all -- --check` clean. Commits `d44b5647`/`9872afa6`/`e8ce3a63`. See `110-01-SUMMARY.md`.

Then: **v3.6 Phase 108 context gathered** — `108-CONTEXT.md` + `108-DISCUSSION-LOG.md` written (commit `85874c01`), 23 decisions (D-01..D-23) across 4 gray areas. Live scouting contradicted several milestone planning assumptions: the window is **100 non-merge commits** (not the `260727-jkn` map's "~40"), **20** commits touch the tool-sandbox surface (not the 7 PRs named in REQUIREMENTS) with **12 entangled**, `deny_domain` #1374 touches core `net_filter.rs`, and **~28 code commits map to no v3.6 requirement** (several security-relevant). **Phase 108's SC4 is unsatisfiable as written** — the audit will report the gap and propose a new **Phase 112 (Security + Residual Sync)**, gated on operator approval before Phase 109 planning.

Then: **v3.6 Phase 108 PLANNED** — 5 plans across 4 waves (`de6998e9`), plan-checker returned NEEDS REVISION (2 blockers / 2 warnings), revision applied and verified (`19c6abbb`). Blockers were two corrupted commit SHAs (`d033c63c`/`a5a44107` — resolve to nothing; corrected to full 40-char `d033c631…`/`a5a441c2…`) and a Cluster Summary whose `TBD` disposition/windows-touch/security-relevant cells no plan ever finalized (breaking ROADMAP SC1's per-cluster rollup). Post-revision sweep: **all 89 distinct hex tokens across the 5 plans resolve to real commits**; XML tags balanced; tree clean. ROADMAP.md carries the corrected SC4 text citing D-19 but **no Phase 112 checklist row or detail section** — the amendment remains a proposal, operator-gated as required.

Then: **v3.6 Phase 108 EXECUTION — 4 of 5 plans complete.** `108-01` ledger foundation (`8cf5ed92`/`97450936`), `108-02` ADR-108 (`81255cc5`/`76ae7b10`), `108-03` NET/PROF/CORE tables (`fee56801`…`cc3549b3`), `108-04` tool-sandbox + DEPS/CI/DOCS (`43a00d2e`…`d95c8010`). Ledger is **1178 lines, 141 distinct SHAs all resolving**. `108-05` closeout (`6539030b`…`11e2f1ad`) after a first attempt died on a transient API 500 having written nothing. **Ledger final: 1642 lines, 176 distinct SHAs all resolving, all 100 window commits partitioned exactly once across 9 buckets, zero `TBD` cells.** Dispositions: adopt 21 / adapt 3 / DEFERRED→v3.7 20 / DEFERRED→proposed-Phase-112 18 + noise. Verifier re-derived the partition, SHA subjects, residue accounting, and carve-out checks from live git independently — `status: passed`, 8/8.

**Findings so far that outlive this phase:**

- **LIVE VULN:** `crossbeam-epoch 0.9.18` (RUSTSEC-2026-0204) is in the fork's `Cargo.lock`; its fix `373a67ae` (#1369) sits unabsorbed in this window's DEPS bucket. Independently confirmed. Priority absorb.
- **ADR-86 threat flag:** `e6d26871` (#1269) adds `pub mod resource;` + `pub use resource::ResourceLimits;` to the policy-free core `crates/nono/src/lib.rs`. Needs boundary review before Phase 111 absorbs it.
- **Phase 110 complication (RESOLVED by Plan 110-02):** PROF-02's absorb target `capability_ext.rs` called into upstream's DEFERRED `tool_sandbox::dynamic_providers`; resolved per D-01/D-02 by porting `expand_dynamic_tokens` into a new fork-owned `crates/nono-cli/src/dynamic_tokens.rs` module instead, with its own v3.7 reconciliation note.
- Two CONTEXT decisions corrected mid-execution by re-measurement (D-04 bucket split, D-06 directory-vs-union) — see `1332b657`.
- **New (Plan 110-03):** `supervisor_linux.rs`'s `mod tests::network_decision` has a pre-existing, dormant Linux-only compile break unrelated to PROF-03 (stale `ApprovalRequest`/`request_approval` shape predating a trait rename, plus a nonexistent `tool_sandbox_runtime` field reference), traced to commit `1a804977` (Phase 96, 2026-06-26). Documented in `.planning/phases/110-profile-policy-absorb-platform-overrides/deferred-items.md`; needs a fix before that test module can execute on Linux (`cross test`), though it does NOT block the mandatory `cross clippy` gate (which doesn't compile `#[cfg(test)]` code).

Resume file: `.planning/phases/118-per-session-enforcement-receipts/118-CONTEXT.md`

**Note on tracking:** SDK `state.begin-phase` / `roadmap.update-plan-progress` were deliberately NOT run for Phase 104 — `init.execute-phase` reports `milestone_version: v3.6`, so an SDK write would have clobbered v3.6's Current Position while v3.5 runs parallel. STATE.md stays hand-maintained for both milestones.

## Operator Next Steps

**v3.6 (active):** Phase 110 context gathered — run `/gsd:plan-phase 110`. PROF-01..04, 7 commits. Milestone is **phases 108-113**; 108 and 109 complete.

Key decisions: `expand_dynamic_tokens` is PORTED into a fork-owned module (PROF-02's `@git:*` import is `cfg`-gated to a `tool_sandbox` path the fork lacks — Windows absorbs cleanly, but the Unix arm would fail cross-target clippy); `d5803b99` (#1398) is absorbed WHOLE by Phase 110, and **Phase 111's CORE-01 has had its #1398 claim REMOVED** (amendment applied); the Windows port-range emitter uses WFP-native ranges with no unrolling and gets its own threat model — it is fork-original enforcement code, upstream built only macOS/Linux; `windows_*` flag migration accepts both forms with `platform_overrides.windows` winning, no deprecation warning yet.

Delivered: `deny_domain` (#1374) per ADR-108 ADAPT with the fail-closed guard at **both** entry points; profile-declared `no_proxy` (#1415) with all **six** D-06 overlap validators wired across direct / `extends`-inherited / group-expanded paths; `HTTP_PROXY` forward-proxy (#1335) enforcing the same `check_host` trust boundary as CONNECT. Tests: `nono-sandbox-proxy` **218/0** (from a 192/0 baseline, +26); `nono-sandbox-cli` 1411 passed / 11 failed (pre-existing environmental, unchanged). Both cross-target clippy gates GREEN. `cargo audit` clean.

**SC4 vindicated D-09:** `../nono-py` failed to compile (`E0063: missing fields denied_hosts and no_proxy`) — struct drift caught only by building. Fixed + DCO-committed there (`e24c1ff`).

📋 **Two operator amendments pending (non-blocking, both proposed-not-applied in `109-AWS-SIGV4-TLS-INTERCEPT-FINDING.md`):**

1. `108-DIVERGENCE-LEDGER.md` dispositions `6fb7ecbf`/`23d93fc9` as `adopt` — should become `won't-sync (target subsystem absent)`. Neither `crates/nono-proxy/src/aws/` nor `.../tls_intercept/` exists in the fork.
2. `REQUIREMENTS.md` NET-03 claims "#1430 and #1437 are absorbed" — contradicted by the verified N/A finding.

Phase 108 **COMPLETE — 5/5 plans, verification `status: passed` (8/8 must-haves)**.

⛔ **BLOCKING OPERATOR GATE before `/gsd:plan-phase 109`:** the ledger reports an exact, hand-verified **27 non-tool-sandbox CODE commits mapping to none of v3.6's 12 requirements**, and proposes a new **Phase 112 "Security + Residual Sync"** (11 draft requirement IDs) to absorb them. That amendment is **written but deliberately NOT applied** — `ROADMAP.md` has zero Phase 112 rows, by design (D-19). **Approve, modify, or reject the amendment before planning Phase 109.** See `108-DIVERGENCE-LEDGER.md` §"Proposed Phase 112: Security + Residual Sync".

**Three findings that must reach their downstream phase:**

1. **LIVE VULN (act soon):** `crossbeam-epoch 0.9.18` (RUSTSEC-2026-0204) is in the fork's `Cargo.lock`; the exact fix `373a67ae` (#1369) is unabsorbed in this window's DEPS bucket. One cherry-pick. Independently confirmed twice.
2. **ADR-86 flag → Phase 111:** `e6d26871` (#1269) adds `pub mod resource;` + `pub use resource::ResourceLimits;` to the policy-free core `crates/nono/src/lib.rs`. Boundary review required before absorb.
3. **Phase 110 complication:** PROF-02's absorb target `capability_ext.rs` calls `tool_sandbox::dynamic_providers::expand_dynamic_tokens` — a DEFERRED module. Verified in `d4927f95`'s diff.

**v3.5 (parallel, now unblocked):** resume Phase 104 Plan 03. Task 2's root pre-check is moot (see above) — go straight to confirming the smoke is green, then the operator-authorized `v0.66.1` tag push. Phase 105's 5 plans execute immediately after.

</content>
