---
phase: 37-linux-resl-backends-pkgs-auto-pull
plan: 06
type: execute
wave: 3
depends_on: ["37-04", "37-05"]
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/nono/src/trust/bundle.rs
autonomous: false
requirements: [REQ-PKGS-04]
requirements_addressed: [REQ-PKGS-04]
tags: [sigstore, tuf, trust-root, flake-triage, gate]

must_haves:
  truths:
    - "The 2 pre-existing trust-bundle test flakes are GREEN on Linux runner at Plan 37-06 close: `nono::trust::bundle::tests::load_production_trusted_root_succeeds` and `nono::trust::bundle::tests::verify_bundle_with_invalid_digest`"
    - "Decision rubric is recorded in SUMMARY: ground-truth `cargo test` output captured at start + `cargo update --dry-run` check + path (a)/(b) decision rationale"
    - "If path (a) — sigstore-rs version bump — was taken: Cargo.toml + Cargo.lock reflect the new version; cross-target clippy still clean; the tests pass"
    - "If path (b) — NONO_TEST_HOME test-only trust root fallback — was taken: the test infrastructure uses a fixture trust root via the Phase 27.1 seam; production trust root is NOT exercised in CI but the production code path remains unchanged; partial-D-15 disposition is documented with a tracking issue link per D-15 clause-1 carve-out"
    - "Plan 37-05's pkgs-auto-pull CI job is GREEN after Plan 37-06 lands (proof that the D-15 prerequisite is satisfied — fully if path a, partially with tracked issue if path b)"
  artifacts:
    - path: "Cargo.toml"
      provides: "Updated sigstore-rs version pin if path (a) taken; unchanged if path (b)"
    - path: "Cargo.lock"
      provides: "Updated resolution graph if path (a)"
    - path: "crates/nono/src/trust/bundle.rs"
      provides: "Test-only trust-root injection seam if path (b); unchanged if path (a)"
  key_links:
    - from: "trust::bundle::tests::load_production_trusted_root_succeeds"
      to: "production Sigstore TUF trust root"
      via: "nono::trust::bundle::verify_bundle network/trust-root fetch"
      pattern: "load_production_trusted_root_succeeds"
    - from: "Plan 37-05 pkgs-auto-pull job"
      to: "production trust root verification"
      via: "auto_pull_signature_failure_aborts integration test"
      pattern: "auto_pull_signature_failure_aborts"
---

<objective>
Triage and resolve the 2 pre-existing TUF-trust-root test flakes (`nono::trust::bundle::tests::load_production_trusted_root_succeeds` + `verify_bundle_with_invalid_digest`) that were carried in Plan 26-02 SUMMARY's "Deferred Items" and now block Plan 37-05's CI close gate (D-15 prerequisite per CONTEXT.md). At execution-time start, capture the ground-truth status via a single `cargo test` call + `cargo update --dry-run` for sigstore-rs; then choose path (a) sigstore-rs version bump OR path (b) NONO_TEST_HOME-based test-only trust root fallback. Path (c) — defer to a follow-up plan — is NOT acceptable because Phase 37's close gate requires GREEN on the Linux runner.

Purpose: This is the Wave 3 gate. Without it, Plan 37-05's `pkgs-auto-pull` job's "Run auto-pull e2e integration test" step would fail on signature verification due to the same TUF flakes (D-15 production trust root + GitHub Actions OIDC issuer pin), blocking REQ-PKGS-04 acceptance #1/#3 verification. The plan exists as a separate Wave 3 because (a) the decision can only be made AFTER capturing fresh ground-truth on a real Linux runner (which means Plans 37-04 + 37-05 must already land the workflow + test surfaces) and (b) the resolution may require external-repo coordination (sigstore-rs upstream).

**D-15 disposition:** Per the revision-1 CONTEXT clarification of D-15, BOTH clauses (production trust root + OIDC issuer pin) are LOCKED — partial-D-15 (test-only substitute) is permissible ONLY here in Plan 37-06 when path (a) cannot be achieved, and it MUST be documented with a tracking issue link. Plan 37-05 itself does NOT accept partial-D-15; that disposition lives exclusively in this plan's SUMMARY.

Output: depends on path chosen:
- **Path (a):** Cargo.toml/Cargo.lock updated; the 2 tests green on Linux runner; cross-target clippy clean.
- **Path (b):** `trust::bundle` adds a test-only trust-root injection seam keyed off `NONO_TEST_HOME` (Phase 27.1 seam reused); Plan 37-05's CI job switched to test trust root + test OIDC issuer pin; production trust root flakes documented as still-deferred (with an issue link).

Aligns with VALIDATION.md task: 37-06-01.

**Depends on Plans 37-04 + 37-05:** the CI workflow + auto-pull test must exist so this plan can verify its decision end-to-end (Plan 37-05's pkgs-auto-pull job is the proving ground for whichever path is taken).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-linux-resl-backends-pkgs-auto-pull/37-CONTEXT.md
@.planning/phases/37-linux-resl-backends-pkgs-auto-pull/37-RESEARCH.md
@.planning/phases/37-linux-resl-backends-pkgs-auto-pull/37-VALIDATION.md
@.planning/phases/26-pkg-streaming-followup/26-02-PKGS-STREAMING-SUMMARY.md
@.planning/phases/37-linux-resl-backends-pkgs-auto-pull/37-04-SUMMARY.md
@.planning/phases/37-linux-resl-backends-pkgs-auto-pull/37-05-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. -->

The 2 flaky tests (per Plan 26-02 SUMMARY's Deferred Items + CONTEXT.md D-15 prerequisite):

```
nono::trust::bundle::tests::load_production_trusted_root_succeeds
nono::trust::bundle::tests::verify_bundle_with_invalid_digest
```

Status at start of Plan 26-02 close: RED (environmental — suspected TUF spec compatibility issue per RESEARCH.md Sources Tertiary).

Decision rubric (per CONTEXT.md Claude's Discretion + researcher Open Question #4):

| Question | Source of truth | Outcome |
|----------|-----------------|---------|
| Are the 2 tests still RED on `ubuntu-24.04` runner? | `cargo test -p nono trust::bundle::tests::load_production_trusted_root_succeeds trust::bundle::tests::verify_bundle_with_invalid_digest --release` (run inside `phase-37-linux-resl.yml` resl-nix job ad-hoc) | If GREEN already → no work needed; close as informational |
| Is there a newer upstream sigstore-rs version? | `cargo update --dry-run -p sigstore` | If yes AND changelog mentions TUF spec fix → path (a) candidate |
| Does path (a) bump compile cleanly + tests green? | Local test + CI run | If yes → path (a) |
| Otherwise | — | Path (b) NONO_TEST_HOME-based test-only trust root |

NONO_TEST_HOME seam location (Phase 27.1 — referenced in CONTEXT.md "Integration Points"):
```rust
// crates/nono/src/... or crates/nono-cli/src/...
// The seam allows tests to redirect HOME-relative paths to a tmpdir
```

LOCKED test OIDC issuer pin (for path b if pinned to a test root): TBD by executor; suggestion: `https://oauth2.sigstore.dev/auth` (the staging Sigstore issuer) OR a self-issued mock issuer URL. Document choice in SUMMARY.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Capture ground-truth + choose path (a) or (b)</name>
  <decision>Which path resolves the 2 trust-bundle flakes for Phase 37's close gate?</decision>
  <context>
    The 2 flakes have been RED since v2.3 Plan 26-02 close. Phase 37 cannot close until they're GREEN on the Linux runner because Plan 37-05's pkgs-auto-pull job exercises the SAME production trust root through `auto_pull_signature_failure_aborts` and `auto_pull_happy_path`.

    Ground-truth capture (executor MUST run these BEFORE making the decision):
    1. `cargo test -p nono trust::bundle::tests::load_production_trusted_root_succeeds trust::bundle::tests::verify_bundle_with_invalid_digest --release 2>&1 | tail -40` — captures current status.
    2. `cargo update --dry-run -p sigstore 2>&1 | tail -20` — captures upstream availability.
    3. Read the changelog at https://github.com/sigstore/sigstore-rs/releases (or `cd vendor/sigstore-rs && git log --oneline` if vendored) — confirms whether any newer version mentions TUF spec / trust-root bundle compatibility.
    4. Push a one-commit branch invoking the 2 tests inside the `phase-37-linux-resl.yml::resl-nix` job ad-hoc to confirm the flake reproduces on `ubuntu-24.04` (not just locally on macOS / Windows-WSL).

    Capture all 4 outputs in the SUMMARY for audit trail.
  </context>
  <options>
    <option id="path-a">
      <name>Path (a) — sigstore-rs version bump</name>
      <pros>
        - Production code path unchanged (still uses production trust root + production OIDC issuer pin)
        - Aligns with upstream fix if one is available
        - REQ-PKGS-04 acceptance #3 verified end-to-end against the real verifier
        - Simplest if a fix-version exists
        - D-15 BOTH clauses fully satisfied
      </pros>
      <cons>
        - Requires upstream version exists AND addresses the flake (uncertain per RESEARCH.md Sources Tertiary)
        - May introduce other version-bump regressions (other sigstore-rs API surface changes)
        - Cargo.lock churn affects all crates that depend on sigstore
      </cons>
    </option>
    <option id="path-b">
      <name>Path (b) — NONO_TEST_HOME-based test-only trust root</name>
      <pros>
        - Works regardless of upstream sigstore-rs status
        - Hermetic: test trust root is checked into the repo (no network at test time, no Rekor TTL drift)
        - Reuses existing Phase 27.1 seam — minimal code-surface change
      </pros>
      <cons>
        - Production trust-root code path NOT exercised in CI (loses defense-in-depth)
        - The 2 original flakes remain RED — they get re-deferred to a future plan with an issue link
        - Plan 37-05's pkgs-auto-pull job needs an additional env-var setup (`NONO_TEST_HOME` + maybe `NONO_TRUST_OIDC_ISSUER`) to switch to test-trust-root mode
        - The test OIDC issuer pin is no longer the LOCKED D-15 `https://token.actions.githubusercontent.com`; D-15 acceptance is partial (this disposition is the LONE place partial-D-15 is permitted per the revision-1 CONTEXT clarification; tracking issue link mandatory in SUMMARY)
      </cons>
    </option>
  </options>
  <resume-signal>Select: `path-a` (preferred if upstream fix exists) or `path-b` (fallback). Document the 4 ground-truth captures + the choice rationale in the resumption message.</resume-signal>
  <files>(no files — decision-only checkpoint)</files>
  <action>
Execute the 4 ground-truth capture commands listed in `<context>` above, in order. Copy all 4 outputs verbatim into the resume message. Then evaluate the rubric in `<interfaces>` Decision rubric table and recommend `path-a` or `path-b` with rationale. WAIT for user confirmation before proceeding to Task 2.

Concretely:
1. Run `cargo test -p nono trust::bundle::tests::load_production_trusted_root_succeeds trust::bundle::tests::verify_bundle_with_invalid_digest --release 2>&1 | tail -40`.
2. Run `cargo update --dry-run -p sigstore 2>&1 | tail -20`.
3. Fetch the sigstore-rs changelog (Bash: `gh api /repos/sigstore/sigstore-rs/releases --jq '.[0:5] | .[] | {tag: .tag_name, name: .name}'` OR WebFetch the GitHub releases page).
4. Push a probe branch + trigger the workflow ad-hoc OR (cheaper) trust the local-host test result as ground-truth proxy if Linux is unavailable from the dev box. Document which proxy was used.

Then present a decision recommendation to the user using the resume-signal format.

**Note on Dimension 8a Nyquist exemption:** This task is `type="checkpoint:decision"`, which is exempt from the auto-verification rule per GSD Nyquist Compliance. The `<verify>` block intentionally omits an `<automated>` element (per revision-1 W6 fix; the prior `echo ... verified by user resume signal` was tautologically green and produced a false-positive CI signal). Completion is gated on user `resume-signal` AND on the precondition that the 4 ground-truth captures are recorded in SUMMARY before Task 2 begins.
  </action>
  <verify>
    <precondition>Ground-truth captures #1-4 recorded in the resume message (verified by human reading the resume content).</precondition>
    <human>Resume signal received from user with explicit `path-a` or `path-b` selection.</human>
  </verify>
  <done>4 ground-truth captures recorded in the resume message + path-a or path-b chosen + user has confirmed the choice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement chosen path</name>
  <read_first>
    - Output of Task 1's ground-truth captures (executor must have these in context after Task 1 resumes)
    - crates/nono/src/trust/bundle.rs (full file — locate `verify_bundle` + `load_trusted_root` entry points + the existing test module)
    - Cargo.toml workspace + crates/nono/Cargo.toml (sigstore dependency declaration)
    - Phase 27.1 seam implementation (grep `NONO_TEST_HOME` in crates/nono/src/ and crates/nono-cli/src/ to locate it)
    - .planning/phases/37-linux-resl-backends-pkgs-auto-pull/37-05-SUMMARY.md (confirm pkgs-auto-pull job's current trust-root + issuer-pin posture)
  </read_first>
  <behavior>
    **If path (a):**
    - Cargo.toml workspace / crate-level `sigstore = "<old>"` is updated to `"<new>"`.
    - `cargo build --workspace` succeeds with the new version.
    - `cargo test -p nono trust::bundle::tests::load_production_trusted_root_succeeds trust::bundle::tests::verify_bundle_with_invalid_digest --release` exits 0 on the Linux runner.
    - Plan 37-05's `auto_pull_happy_path` and `auto_pull_signature_failure_aborts` tests pass on the runner (proves end-to-end gate).
    - Any breaking-API call sites are updated (read the sigstore-rs changelog for the version-jump scope).

    **If path (b):**
    - `trust::bundle` exposes a test-only trust-root injection seam keyed off `NONO_TEST_HOME` (or a new env var `NONO_TRUST_ROOT_OVERRIDE`).
    - A test trust root fixture is checked into `crates/nono/tests/fixtures/` (or equivalent) — minimal Sigstore bundle generated against a test signing identity.
    - `Plan 37-05's pkgs-auto-pull` CI job is updated to set the trust-root override env var BEFORE running the auto-pull test.
    - The 2 original flake tests are marked `#[ignore = "tracked in <issue-link>; see Plan 37-06 SUMMARY"]` — NOT deleted, NOT moved, just ignored with an explicit issue link per CLAUDE.md "lazy use of dead code" rule.
    - SUMMARY records the partial-D-15 fulfillment with a follow-up issue link for re-pin.
  </behavior>
  <action>
**Execute the path chosen at Task 1.**

***

**PATH (a) STEPS:**

1. **Update version pin.** In workspace `Cargo.toml` (or in `crates/nono/Cargo.toml` depending on where sigstore is declared), update the `sigstore = "..."` version to the new version identified in Task 1 ground-truth.

2. **Run `cargo update -p sigstore`** to refresh `Cargo.lock`. Verify the new lockfile entry.

3. **Build + test.** `cargo build --workspace` + `cargo test -p nono trust::bundle::tests::load_production_trusted_root_succeeds trust::bundle::tests::verify_bundle_with_invalid_digest --release`. If compile errors due to API breaking changes, fix them (the version-bump scope determines the size of the fix; document in SUMMARY).

4. **Cross-target verification.** `cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used` exits 0.

5. **Push branch and trigger CI.** Confirm both `phase-37-linux-resl.yml` jobs are green: `resl-nix` AND `pkgs-auto-pull`. The pkgs-auto-pull job's `auto_pull_signature_failure_aborts` + `auto_pull_happy_path` tests pass — that's the proving ground that the trust-root verification path is now reliable.

6. **DO NOT add `#[ignore]` to the originally-flaky tests.** The whole point of path (a) is that they're now green.

***

**PATH (b) STEPS:**

1. **Locate the NONO_TEST_HOME seam.** `grep -rn "NONO_TEST_HOME" crates/nono/src/` to find the existing redirect mechanism. If the existing seam only covers HOME-relative paths (not trust-root selection), extend it to allow trust-root redirection.

2. **Add a test-only trust-root injection seam.** In `crates/nono/src/trust/bundle.rs`, add a function like:

   ```rust
   /// Phase 37 Plan 37-06 path (b): test-only trust root override.
   ///
   /// When `NONO_TRUST_ROOT_OVERRIDE` is set to a filesystem path, the
   /// trust-bundle verification path loads the TUF root from that file
   /// instead of the production Sigstore trust root. Tests use this seam
   /// to verify against a fixture trust root checked into the repo. The
   /// production code path (with the env var unset) is unchanged.
   ///
   /// SECURITY: This MUST NOT be honored in release builds outside of
   /// the test runner. Gated by `#[cfg(any(test, feature = "test-trust-root"))]`
   /// to prevent accidental exposure in production binaries.
   #[cfg(any(test, feature = "test-trust-root"))]
   pub fn trust_root_override_path() -> Option<std::path::PathBuf> {
       std::env::var("NONO_TRUST_ROOT_OVERRIDE")
           .ok()
           .map(std::path::PathBuf::from)
       // Validate as a file existing + readable; reject path-traversal per CLAUDE.md.
   }
   ```

   Then wire `verify_bundle` (or its trust-root loader) to consult the override BEFORE the production fetch when the cfg is active.

3. **Add a test trust-root fixture.** Generate a minimal Sigstore bundle against a test signing identity:
   ```bash
   mkdir -p crates/nono/tests/fixtures/trust-root/
   # Generate the bundle via cosign or sigstore-sign with --bundle output
   ```
   Check the fixture into `crates/nono/tests/fixtures/trust-root/test-root.json`. Document the test signing identity in SUMMARY.

4. **Update Plan 37-05's CI job** in `.github/workflows/phase-37-linux-resl.yml`. In the `pkgs-auto-pull` job's "Run auto-pull e2e integration test" step, set:
   ```yaml
   env:
     NONO_FIXTURE_PACK_DIR: ${{ github.workspace }}/target/fixture-pack
     NONO_TRUST_ROOT_OVERRIDE: ${{ github.workspace }}/crates/nono/tests/fixtures/trust-root/test-root.json
   ```
   And rebuild the workspace with the `test-trust-root` feature enabled (or `--cfg test_trust_root` via RUSTFLAGS) — whichever gating scheme is chosen in step 2.

5. **Mark the 2 original flakes `#[ignore]` with issue link.** In `crates/nono/src/trust/bundle.rs` test module:
   ```rust
   #[test]
   #[ignore = "tracked in #<issue-number>; see Phase 37 Plan 37-06 SUMMARY for triage rationale + path (b) test-trust-root fixture alternative"]
   fn load_production_trusted_root_succeeds() { /* ... */ }

   #[test]
   #[ignore = "tracked in #<issue-number>; see Phase 37 Plan 37-06 SUMMARY for triage rationale"]
   fn verify_bundle_with_invalid_digest() { /* ... */ }
   ```
   File the GitHub issue first (`gh issue create --title "TUF trust-root flakes (Phase 26-02 deferred)" --body "..."`) and use the issue number in the ignore message.

6. **Cross-target verification.** `cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used` exits 0.

7. **Push branch and trigger CI.** Confirm both jobs are green. The `pkgs-auto-pull` job now uses the test trust root via the env-var override; the production verifier path is exercised in production but NOT in CI (acceptable per CONTEXT.md fallback posture).

8. **SUMMARY must record** the partial-D-15 fulfillment: "Production Sigstore trust root + GitHub Actions OIDC issuer pin per D-15 is verified by Plan 37-05's integration test against a TEST trust root only; production trust root verification is deferred to <issue-link>. Acceptance #3 ('signature verification failure aborts') still verified end-to-end."

***

**BOTH PATHS — Cleanup:**

- `cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used` exits 0.
- DCO sign-off on commits.
- Plan 37-05's `pkgs-auto-pull` job runs GREEN end-to-end on the merge commit.
  </action>
  <verify>
    <automated>cargo test -p nono trust::bundle::tests::load_production_trusted_root_succeeds trust::bundle::tests::verify_bundle_with_invalid_digest --release 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY records all 4 ground-truth captures from Task 1 (cargo test output, cargo update --dry-run output, sigstore-rs changelog excerpt, runner reproduction confirmation).
    - SUMMARY records the chosen path (a/b) + rationale referencing the captures.
    - **If path (a):** `grep -nE "sigstore = " Cargo.toml crates/nono/Cargo.toml` shows the updated version; both originally-flaky tests pass (`cargo test ... 2>&1 | grep -E "2 passed"`); no `#[ignore]` added to the 2 tests.
    - **If path (b):** `grep -nE "NONO_TRUST_ROOT_OVERRIDE" crates/nono/src/trust/bundle.rs` returns ≥ 1 match; fixture file `crates/nono/tests/fixtures/trust-root/test-root.json` exists; both originally-flaky tests have `#[ignore = "tracked in #..."]` with a real issue number; `phase-37-linux-resl.yml::pkgs-auto-pull` env section has `NONO_TRUST_ROOT_OVERRIDE`.
    - Plan 37-05's CI job is GREEN on the merge commit: `gh run list -w "Phase 37 Linux RESL" --limit 1 --json conclusion -q '.[0].conclusion'` returns `success`.
    - `cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used` exits 0.
  </acceptance_criteria>
  <done>Path chosen, executed, verified end-to-end via Plan 37-05's CI job green; the D-15 prerequisite is closed (fully if path a, partially-with-issue-link if path b); cross-target clippy clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sigstore-rs upstream -> nono Cargo.lock | path (a): a new upstream version becomes a supply-chain trust anchor |
| test trust-root fixture -> verifier | path (b): the test fixture is a parallel trust root only honored under `cfg(test)` or feature flag — must NOT be exposed in production binaries |
| `NONO_TRUST_ROOT_OVERRIDE` env var -> trust-root loader | path (b): privilege boundary — env var must be unexpressible in production paths via `#[cfg]` gating |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05 | Repudiation / Spoofing | TUF trust-root flake masking real signature failures | mitigate | Path (a): version bump resolves the flake at the verifier layer. Path (b): test-only override seam keeps production code unchanged; the 2 flakes are explicitly tracked in a GitHub issue (no silent deferral). Severity: **high** (flake masking ANY signature failure is a critical-severity trust-anchor regression). |
| T-37-23 | Elevation of Privilege | Test trust-root override leaking into production binary | mitigate (path b only) | Task 2 path-b step 2 explicitly gates the override seam behind `#[cfg(any(test, feature = "test-trust-root"))]`. Release builds (`cargo build --release` without the feature) MUST NOT have the override function compiled in. Verify by `grep` after build and by `cargo expand` if needed. Severity: **critical** (override in production would bypass signature verification). |
| T-37-24 | Tampering | `NONO_TRUST_ROOT_OVERRIDE` path traversal | mitigate (path b only) | Task 2 path-b step 2 validates the override path: `.is_file()` + readable + (optional but recommended) `validate_path_within` against an expected directory like `${repo}/crates/nono/tests/fixtures/`. Severity: **medium** (test-only seam; defense-in-depth). |
| T-37-25 | Supply chain | sigstore-rs version bump introducing a malicious dependency | mitigate (path a only) | Cargo.lock pins the exact version + transitive hashes; review the diff (`git diff Cargo.lock` after `cargo update`) for unexpected new crates; cross-reference with sigstore-rs's released SHA. Severity: **medium** (standard supply-chain hardening). |
| T-37-26 | Repudiation | 2 flakes silently re-deferred to a "follow-up plan" with no tracking | mitigate (path b only) | Task 2 path-b step 5 requires a real GitHub issue number in the `#[ignore]` message; SUMMARY must include the issue link. CLAUDE.md "lazy use of dead code" rule explicitly forbids silent deferrals. Severity: **medium** (process hygiene). |
</threat_model>

<verification>
- Task 1 ground-truth captures recorded in SUMMARY (4 outputs).
- Task 1 decision recorded with rationale.
- `cargo test -p nono trust::bundle::tests::load_production_trusted_root_succeeds trust::bundle::tests::verify_bundle_with_invalid_digest --release` reports:
  - **Path (a):** `2 passed; 0 failed`
  - **Path (b):** `0 passed; 0 failed; 2 ignored` AND ignored messages contain a real issue number
- Plan 37-05's CI job GREEN on the merge commit (`gh run list -w "Phase 37 Linux RESL" --limit 1`).
- `cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used` exits 0.
- **Path (b) only:** `grep -nE "NONO_TRUST_ROOT_OVERRIDE" crates/nono/src/trust/bundle.rs` shows the gated seam; `cargo build --release` without the `test-trust-root` feature produces a binary without the override function (verify with `cargo expand` or strings-based inspection).
- DCO sign-off on commits.
</verification>

<success_criteria>
- The 2 trust-bundle tests are EITHER green (path a) OR explicitly ignored with issue-link (path b).
- Plan 37-05's `pkgs-auto-pull` job is GREEN end-to-end.
- D-15 prerequisite is satisfied (fully or partially-with-tracked-issue).
- No regression in production trust-root code path (path b's override is `cfg`-gated).
- Cross-target clippy clean.
- The Phase 37 close gate can now be requested without trust-bundle flake blocking it.
</success_criteria>

<output>
After completion, create `.planning/phases/37-linux-resl-backends-pkgs-auto-pull/37-06-SUMMARY.md` capturing:
- All 4 ground-truth captures from Task 1 verbatim (cargo test output, cargo update --dry-run, sigstore-rs changelog excerpt, runner reproduction confirmation).
- Chosen path (a or b) + decision rationale.
- **Path (a) details:** old version → new version; list of API breaking-change fixes if any; final tests-pass output.
- **Path (b) details:** GitHub issue number for the deferred TUF flakes; LOCKED test OIDC issuer pin chosen; fixture file path + signing identity used; `cfg`-gating mechanism (feature flag name or cfg key); explicit note that production-trust-root verification is NOT exercised in CI but production code path is unchanged.
- Plan 37-05's CI run URL (proving the green gate).
- Cross-target clippy result.
- Phase 37 close-gate readiness statement: "Phase 37 close gate is ready to be requested. All 4 requirements (REQ-RESL-NIX-01/02/03 + REQ-PKGS-04) are verified end-to-end on the ubuntu-24.04 runner."
</output>
