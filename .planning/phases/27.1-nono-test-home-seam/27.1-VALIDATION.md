---
phase: 27.1
slug: nono-test-home-seam
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-09
---

# Phase 27.1 — Validation Strategy

> Reconstructed retroactively (State B) from PLAN/SUMMARY artifacts plus a 3-gap Nyquist auditor pass. All requirement-bearing behaviors have automated verification. REQ-NTH-03 audit-attestation tests are explicitly manual-only per D-27.1-14 + Plan 03 SC #7 + the verifier's accepted override (see VERIFICATION.md).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Cargo + Rust built-in test runner |
| **Config file** | `Cargo.toml` (workspace + `crates/nono-cli/Cargo.toml`) |
| **Quick run command** | `cargo test -p nono-cli --bin nono config::tests` |
| **Full suite command** | `cargo test -p nono-cli --bin nono` |
| **Estimated runtime** | ~12 s build, <1 s test execution |
| **Crate type note** | `nono-cli` is binary-only (no `[lib]`); always use `--bin nono`, never `--lib`. Integration tests use `--test <name>`. |
| **Env-mutex pattern** | `crate::test_env::lock_env()` + `EnvVarGuard::set_all` (RAII restore on drop). Required for any test touching `NONO_TEST_HOME`, `HOME`, `USERPROFILE`, `TMPDIR`, `XDG_CONFIG_HOME`. |

---

## Sampling Rate

- **After every task commit:** `cargo test -p nono-cli --bin nono config::tests` (helper contract, ~1 s)
- **After every plan wave:** `cargo test -p nono-cli --bin nono` (full unit suite)
- **Before `/gsd-verify-work`:** `cargo build --workspace && cargo test -p nono-cli --bin nono && cargo clippy -p nono-cli -- -D warnings -D clippy::unwrap_used`
- **Max feedback latency:** ~12 s

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 27.1-01-T1 | 01 | 1 | REQ-NTH-01 | T-27.1-01 (Spoofing) | `nono_home_dir()` returns absolute override verbatim | unit | `cargo test -p nono-cli --bin nono config::tests::nono_home_dir_returns_override_when_set` | ✅ | ✅ green |
| 27.1-01-T1 | 01 | 1 | REQ-NTH-01 | T-27.1-01 (Spoofing) | `nono_home_dir()` fail-closes on relative override with `EnvVarValidation` | unit | `cargo test -p nono-cli --bin nono config::tests::nono_home_dir_rejects_non_absolute_override` | ✅ | ✅ green |
| 27.1-01-T1 | 01 | 1 | REQ-NTH-01 | — | `nono_home_dir()` falls through to `dirs::home_dir()` when env unset | unit | `cargo test -p nono-cli --bin nono config::tests::nono_home_dir_falls_through_when_unset` | ✅ | ✅ green |
| 27.1-01-T1 | 01 | 1 | REQ-NTH-01 | T-27.1-04 (DoS) | `warn_once_test_home` emits exactly one warn per process (OnceLock guard) | unit (transitive) | Verified by code path — `OnceLock<()>` guard at `config/mod.rs:135-143`; covered by `audit_root_honors_nono_test_home` exercise | ✅ | ✅ green |
| 27.1-01-T2 | 01 | 1 | REQ-NTH-02 | T-27.1-05 (EoP) | `user_state_dir()` Windows-only override returns `<NONO_TEST_HOME>/.nono` | unit (Windows-only) | `cargo test -p nono-cli --bin nono config::tests::user_state_dir_honors_nono_test_home` | ✅ | ✅ green |
| 27.1-01-T2 | 01 | 1 | REQ-NTH-02 | — | `rollback_root()` Windows arm derives from `user_state_dir()` | unit (Windows-only) | `cargo test -p nono-cli --bin nono rollback_session::tests::rollback_root_uses_windows_state_dir` | ✅ | ✅ green |
| 27.1-02-T1 | 02 | 2 | REQ-NTH-01 | T-27.1-06 (Tamper) | `audit_root()` honors override → `<abs>/.nono/audit` | unit (gap fill) | `cargo test -p nono-cli --bin nono audit_session::tests::audit_root_honors_nono_test_home` | ✅ | ✅ green |
| 27.1-02-T1 | 02 | 2 | REQ-NTH-01 | T-27.1-06 (Tamper) | `rollback_root()` Unix arm honors override → `<abs>/.nono/rollbacks` | unit (gap fill, Unix-only) | `cargo test -p nono-cli --bin nono rollback_session::tests::rollback_root_unix_honors_nono_test_home` | ✅ | ✅ green (cfg-skipped on Windows) |
| 27.1-02-T1 | 02 | 2 | REQ-NTH-01 | T-27.1-07 (Tamper) | `sessions_dir()` honors override → `<abs>/.nono/sessions` | unit (gap fill) | `cargo test -p nono-cli --bin nono session::tests::sessions_dir_returns_path_under_nono_test_home` | ✅ | ✅ green |
| 27.1-02-T1 | 02 | 2 | REQ-NTH-01 | T-27.1-07 (Tamper) | `sessions_dir()` deviation maps `EnvVarValidation` → `ConfigParse` with legacy message | unit (gap fill) | `cargo test -p nono-cli --bin nono session::tests::sessions_dir_maps_envvar_validation_to_config_parse` | ✅ | ✅ green |
| 27.1-02-T2 | 02 | 2 | REQ-NTH-01 | T-27.1-08 (Tamper) | Display-style `shorten_home()` survives migration | unit (existing) | `cargo test -p nono-cli --bin nono rollback_commands::tests::shorten_home_replaces_prefix` | ✅ | ✅ green |
| 27.1-02-T1/T2 (structural) | 02 | 2 | REQ-NTH-01 | T-27.1-06 (Tamper) | Workspace-wide invariant: zero non-comment `dirs::home_dir`/`xdg_home::home_dir` calls in `crates/nono-cli/src/` | grep gate | `cd C:/Users/OMack/Nono && rg -n 'dirs::home_dir' crates/nono-cli/src/` returns only the helper's own fall-through + 2 doc-comment lines + 1 test fall-through assertion (verified by VERIFICATION.md row 119) | ✅ | ✅ green |
| 27.1-02-T3 | 02 | 2 | REQ-NTH-01 | T-27.1-09 (Tamper) | `xdg-home` dependency removed from `Cargo.toml` + `Cargo.lock` | grep gate (build-time) | `! grep -q '^xdg-home' crates/nono-cli/Cargo.toml && ! grep -q 'name = "xdg-home"' Cargo.lock` | ✅ | ✅ green |
| 27.1-02-T3 | 02 | 2 | REQ-NTH-01 | — | Workspace builds clean across all targets | build | `cargo build --workspace` exits 0 | ✅ | ✅ green |
| 27.1-02-T3 | 02 | 2 | REQ-NTH-01 | — | No new clippy warnings introduced by the migration | lint | `cargo clippy -p nono-cli -- -D warnings -D clippy::unwrap_used` exits 0 (pre-existing supervisor.rs/manifest.rs warnings tracked in `deferred-items.md`) | ✅ | ✅ green |
| (cross-cutting) | 01–03 | all | D-19 invariant | — | `crates/nono/` byte-identical across the phase | structural gate | `git diff --stat 18e8e4ea..HEAD -- crates/nono/` returns empty | ✅ | ✅ green |
| 27.1-03-T1 | 03 | 3 | REQ-NTH-03 | T-27.1-14 (Blocker 3) | `audit_verify_reports_signed_attestation_with_pinned_public_key` runs to completion on Windows host | integration | `cargo test -p nono-cli --test audit_attestation audit_verify_reports_signed_attestation_with_pinned_public_key` | ✅ | ⚠️ **DEFERRED to v2.4-FU-1/FU-2/FU-3 — see Manual-Only** |
| 27.1-03-T2 | 03 | 3 | REQ-NTH-03 | T-27.1-14 (Blocker 3) | `rollback_signed_session_verifies_from_audit_dir_bundle` runs to completion on Windows host | integration | `cargo test -p nono-cli --test audit_attestation rollback_signed_session_verifies_from_audit_dir_bundle` | ✅ | ⚠️ **DEFERRED to v2.4-FU-1/FU-2/FU-3 — see Manual-Only** |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky/deferred*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements.* No Wave 0 stubs needed — the test framework (cargo + Rust unit tests + `EnvVarGuard`/`lock_env` env-mutex utilities) was already in place from prior phases.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `audit_verify_reports_signed_attestation_with_pinned_public_key` returns `2 passed; 0 failed; 0 ignored` | REQ-NTH-03 | Per D-27.1-14 large-fix branch + Plan 03 SC #7 + VERIFICATION.md override. The seam itself reaches the production callsites end-to-end (verified by `nono run --audit-integrity` writing to `<NONO_TEST_HOME>/.nono/audit/<id>/` on Windows host). Full test green requires v2.4 production fixes (audit-loader swap + bundle-target architecture decision) explicitly out of Phase 27.1 scope. | After v2.4-FU-1 (wire `audit_session::load_session` into `audit_commands.rs:12`) AND v2.4-FU-2 (mirror bundle to `audit_dir` OR dual-root verify OR test rewrite) AND v2.4-FU-3 (remove `#[ignore]` attributes), run `cargo test -p nono-cli --test audit_attestation` on a Windows host. Expect both tests green. Tracked in `.planning/phases/27.1-nono-test-home-seam/deferred-items.md` § v2.4-FU-1/FU-2/FU-3. |
| `rollback_signed_session_verifies_from_audit_dir_bundle` returns `0 failed` | REQ-NTH-03 | Same as above — Blocker 3 architectural gap surfaces the bundle-target mismatch (`session_dir = <rollback>/<id>/` writes; test asserts `<audit>/<id>/` lookup). WR-04 from REVIEW also flags JSON-shape mismatch (`json["attestation"]["present"]` vs production `attestation_present`); v2.4-FU-3's "15 min" estimate likely understated. | Same as above. v2.4-FU-3 estimate should be revised upward to account for JSON-shape rework or production `cmd_verify` enrichment. |
| WR-01 split-brain home resolution disposition (validated_home vs nono_home_dir) accepted as intentional | REQ-NTH-01 (cross-cutting) | Plan 01 SUMMARY T-27.1-05 disposition treats this as intentional; D-27.1-08 locks always-on-in-release-binaries. Production users do not set `NONO_TEST_HOME`, so the confused-deputy hazard is not triggered in the production threat model. **Adjudicated 2026-05-08 via /gsd-verify-work 27.1 — option (a) accepted.** | One-time human review documented in VERIFICATION.md `human_verification` block. Resolution recorded; no recurring manual test. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or are documented in Manual-Only with explicit plan-locked deferral
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (every task has at least a grep-gate or unit test)
- [x] Wave 0 covers all MISSING references — N/A (no Wave 0 needed)
- [x] No watch-mode flags
- [x] Feedback latency < 15 s (full unit suite ~6–12 s)
- [x] `nyquist_compliant: true` set in frontmatter (REQ-NTH-03 manual-only deferral is plan-authorized per D-27.1-14)

**Approval:** approved 2026-05-09 (retroactive State B reconstruction + 3-gap auditor pass)

---

## Validation Audit 2026-05-09

| Metric | Count |
|--------|-------|
| Gaps found | 3 (audit_root, rollback_root Unix, sessions_dir incl. ConfigParse deviation) |
| Resolved (auditor) | 4 tests (3 gaps; sessions_dir split into positive+negative) |
| Escalated to manual-only | 0 |
| Pre-existing manual-only (REQ-NTH-03 deferral) | 2 (audit-attestation Test 1 + Test 2) |

### Tests added by gap-fill

1. `audit_session::tests::audit_root_honors_nono_test_home` — pins Plan 02 Edit 1.1 contract
2. `rollback_session::tests::rollback_root_unix_honors_nono_test_home` — pins Plan 02 Edit 1.4 Unix-arm contract (cfg-gated; Windows arm covered by existing `rollback_root_uses_windows_state_dir` + `user_state_dir_honors_nono_test_home`)
3. `session::tests::sessions_dir_returns_path_under_nono_test_home` — pins Plan 02 Edit 1.8 positive contract
4. `session::tests::sessions_dir_maps_envvar_validation_to_config_parse` — pins Plan 02 Edit 1.8 ConfigParse-deviation contract (matches exact legacy message string; fail-asserts that `EnvVarValidation` does not leak through)

### Cross-platform verification

- Windows host (this machine): `cargo test -p nono-cli --bin nono` → 837 passed; 0 failed; 0 ignored
- Unix arms cfg-gated correctly (`rollback_root_unix_honors_nono_test_home` skipped on Windows; will run on Linux/macOS CI)
