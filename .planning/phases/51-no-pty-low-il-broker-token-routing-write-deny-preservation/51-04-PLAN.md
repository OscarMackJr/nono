---
phase: 51
plan: 04
type: execute
wave: 3
depends_on:
  - 51-03
files_modified: []
autonomous: false
requirements:
  - REQ-WSRH-05
must_haves:
  truths:
    - "cargo clippy --workspace --target x86_64-unknown-linux-gnu passes with -D warnings -D clippy::unwrap_used (or marked PARTIAL per cross-target-verify-checklist.md if toolchain unavailable)"
    - "cargo clippy --workspace --target x86_64-apple-darwin passes with -D warnings -D clippy::unwrap_used (or marked PARTIAL per cross-target-verify-checklist.md if toolchain unavailable)"
    - "Full Windows CI suite: cargo test -p nono-cli --target x86_64-pc-windows-msvc && cargo test -p nono-shell-broker --target x86_64-pc-windows-msvc both pass"
    - "nono run --profile claude-code -- cmd /c echo hi exits 0 (repro A non-regression confirmation)"
    - "Windows host cargo clippy --workspace (host-target) passes with -D warnings -D clippy::unwrap_used"
  artifacts: []
  key_links:
    - from: "profile/mod.rs windows_low_il_broker field"
      to: "x86_64-unknown-linux-gnu clippy"
      via: "cross-platform struct field must not produce dead_code or unused-variable lints on Linux/macOS targets"
      pattern: "windows_low_il_broker"
---

<objective>
Run the full verification sweep for Phase 51: Windows CI suite, host-target clippy, cross-target clippy (Linux + macOS), and the non-regression live test for repro A.

Purpose: REQ-WSRH-05 requires cross-target clippy clean per CLAUDE.md § Coding Standards MUST/NEVER. The only cross-platform file touched in this phase is `profile/mod.rs` (which gains `windows_low_il_broker: bool`). All other modified files are pure Windows-only. The cross-target clippy gate specifically validates that the new profile field produces no dead_code or unused-import warnings on Linux/macOS targets.

Output: Verification record with pass/partial/fail disposition for each of the 5 criteria above; cross-target clippy either VERIFIED (both targets clean) or PARTIAL (toolchain missing, live-CI deferral documented per cross-target-verify-checklist.md).
</objective>

<execution_context>
@C:/Users/OMack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OMack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/OMack/nono/.planning/PROJECT.md
@C:/Users/OMack/nono/.planning/ROADMAP.md
@C:/Users/OMack/nono/.planning/phases/51-no-pty-low-il-broker-token-routing-write-deny-preservation/51-CONTEXT.md
@C:/Users/OMack/nono/.planning/phases/51-no-pty-low-il-broker-token-routing-write-deny-preservation/51-VALIDATION.md
@C:/Users/OMack/nono/.planning/templates/cross-target-verify-checklist.md
@C:/Users/OMack/nono/.planning/phases/51-no-pty-low-il-broker-token-routing-write-deny-preservation/51-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Windows host-target clippy + full CI suite</name>
  <files></files>
  <read_first>
    - .planning/phases/51-no-pty-low-il-broker-token-routing-write-deny-preservation/51-03-SUMMARY.md (confirm all tests passed in prior plan)
  </read_first>
  <action>
    Run the following commands in sequence and record each result:

    1. Windows host-target clippy (host = x86_64-pc-windows-msvc):
       cargo clippy --workspace -- -D warnings -D clippy::unwrap_used

    2. Full nono-cli test suite:
       cargo test -p nono-cli --target x86_64-pc-windows-msvc

    3. Full nono-shell-broker test suite:
       cargo test -p nono-shell-broker --target x86_64-pc-windows-msvc

    4. Targeted regression tests (belt-and-suspenders after full suite):
       cargo test -p nono-cli pty_token_gate_tests
       cargo test -p nono-cli broker_dispatch_tests
       cargo test -p nono-cli write_deny_low_il_broker_no_pty_tests

    If any test fails: diagnose the failure, fix it in the appropriate file (launch.rs, main.rs, mod.rs, profile/mod.rs), and re-run until all pass. This is the only code-change-allowed task in this plan.

    If a clippy warning is found: fix it without using #[allow(...)] unless the allow attribute is in a test module (test modules may use #[allow(clippy::unwrap_used)]). Per CLAUDE.md § Coding Standards — do not introduce #[allow(dead_code)].
  </action>
  <verify>
    <automated>cargo test -p nono-cli --target x86_64-pc-windows-msvc 2>&1 | tail -5 && cargo test -p nono-shell-broker --target x86_64-pc-windows-msvc 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - cargo clippy --workspace -- -D warnings -D clippy::unwrap_used exits 0 (no warnings on Windows host target)
    - cargo test -p nono-cli --target x86_64-pc-windows-msvc exits 0
    - cargo test -p nono-shell-broker --target x86_64-pc-windows-msvc exits 0
    - cargo test -p nono-cli pty_token_gate_tests shows 8 tests all passing
    - cargo test -p nono-cli write_deny_low_il_broker_no_pty_tests shows test ok (not ignored, not failed)
  </acceptance_criteria>
  <done>Windows CI suite fully green; host-target clippy clean; all Phase 51 test modules pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All Phase 51 implementation complete (Plans 51-01 through 51-03). Windows host-target clippy clean and full test suite passing. Now running cross-target clippy for REQ-WSRH-05 compliance.
  </what-built>
  <how-to-verify>
    Run the cross-target clippy commands per .planning/templates/cross-target-verify-checklist.md:

    Step 1 — Check installed targets:
      rustup target list --installed

    Step 2a — If x86_64-unknown-linux-gnu is installed:
      cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used
      Record result: PASS or errors.

    Step 2b — If x86_64-unknown-linux-gnu is NOT installed:
      Record: PARTIAL — toolchain missing. Live GH Actions Linux Clippy lane on HEAD SHA is the decisive signal per .planning/templates/cross-target-verify-checklist.md.

    Step 3a — If x86_64-apple-darwin is installed:
      cargo clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used
      Record result: PASS or errors.

    Step 3b — If x86_64-apple-darwin is NOT installed:
      Record: PARTIAL — toolchain missing. Live GH Actions macOS Clippy lane on HEAD SHA is the decisive signal.

    Note: the ONLY cross-platform file modified in Phase 51 is crates/nono-cli/src/profile/mod.rs (windows_low_il_broker: bool field). All other modified files are pure Windows-only and exempt per the checklist § Scope. The cross-target check is specifically for the profile field not producing dead_code lint on Linux/macOS.

    Expected outcome if toolchains are available:
    - No lint warnings for windows_low_il_broker field (Rust does not warn on unused struct fields by default; the field is defined cross-platform and consumed under Windows cfg paths, same pattern as unsafe_macos_seatbelt_rules which compiles cleanly)

    If errors appear: fix them. Do NOT add #[allow(dead_code)] — see CLAUDE.md and checklist § Anti-Patterns.

    Record one of:
    A) "Both targets PASS — REQ-WSRH-05 cross-target clippy VERIFIED"
    B) "Linux PASS, macOS PARTIAL (toolchain missing) — REQ-WSRH-05 cross-target clippy PARTIAL"
    C) "Both PARTIAL (toolchains missing) — REQ-WSRH-05 cross-target clippy PARTIAL; CI deferral documented"

    Type "approved-verified", "approved-partial-linux", "approved-partial-both", or describe errors found.
  </how-to-verify>
  <resume-signal>Type "approved-verified", "approved-partial-linux", "approved-partial-both", or describe errors found that need fixes</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All implementation and clippy gates complete. Now verify the non-regression live test (repro A from the debug session) still passes on the Windows dev host.
  </what-built>
  <how-to-verify>
    Build the CLI binary in debug mode:
      cargo build -p nono-cli --target x86_64-pc-windows-msvc

    Run the non-regression command (repro A from .planning/debug/claude-exe-dll-init-failed.md):
      ./target/x86_64-pc-windows-msvc/debug/nono.exe run --profile claude-code -- cmd /c "echo hi"

    Expected: the child prints "hi" to stdout and exits 0 with no nono error diagnostic.

    This confirms:
    1. The profile field addition did not break profile loading for claude-code
    2. The BrokerLaunchNoPty cascade extension does not erroneously fire on plain console children (cmd /c "echo hi" should still route through the correct arm — note: cmd.exe does not use the session-SID path in nono run non-shell mode, so it may route through a different arm; the key assertion is that it exits 0)
    3. nono-cli binary builds and runs on Windows

    If repro A fails: investigate and fix. This is a hard non-regression gate per REQ-WSRH-05.

    Type "approved" when repro A passes, or describe the failure.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the failure observed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-host clippy → CI clippy | Windows-host clippy does not exercise Linux/macOS cfg branches; cross-target clippy is the only way to verify profile/mod.rs changes are lint-free on all platforms |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51D-01 | Tampering | Cross-target clippy gate skipped (Phase 41 anti-pattern recurrence) | mitigate | Checkpoint:human-verify for cross-target clippy results; PARTIAL disposition documented per cross-target-verify-checklist.md if toolchain unavailable; "approved-verified" vs "approved-partial-*" signal distinguishes |
| T-51D-02 | Elevation of Privilege | Repro A regression (cmd /c echo hi stops working) | mitigate | checkpoint:human-verify for live repro A run; hard non-regression gate per REQ-WSRH-05 |
| T-51D-03 | Tampering | Lint fixed with #[allow(...)] instead of structural fix | mitigate | Acceptance criteria explicitly prohibit #[allow(dead_code)] or new allows except in test modules (CLAUDE.md + checklist anti-pattern 2) |
| T-51D-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies in this plan (verification only) |
</threat_model>

<verification>
Phase 51 complete when:
- cargo clippy --workspace -- -D warnings -D clippy::unwrap_used exits 0 (Windows host)
- cargo test -p nono-cli --target x86_64-pc-windows-msvc exits 0 (full nono-cli suite)
- cargo test -p nono-shell-broker --target x86_64-pc-windows-msvc exits 0 (broker suite)
- Cross-target clippy: either both VERIFIED or PARTIAL with CI-deferral documented
- Repro A (nono run --profile claude-code -- cmd /c "echo hi") exits 0
</verification>

<success_criteria>
- All 4 REQ-WSRH-{01,02,03,05} verifications are either VERIFIED or PARTIAL (with CI deferral documented)
- Windows CI suite green (all tests pass)
- Host clippy clean
- Cross-target clippy: VERIFIED or PARTIAL disposition documented
- Repro A passes
- Phase 51 VERIFICATION.md ready for creation by /gsd:verify-work
</success_criteria>

<output>
Create .planning/phases/51-no-pty-low-il-broker-token-routing-write-deny-preservation/51-04-SUMMARY.md when done.
</output>
