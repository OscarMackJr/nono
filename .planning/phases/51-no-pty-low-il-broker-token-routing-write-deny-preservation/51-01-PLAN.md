---
phase: 51
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono-cli/src/profile/mod.rs
  - crates/nono-cli/data/nono-profile.schema.json
  - crates/nono-cli/data/policy.json
  - crates/nono-cli/src/exec_strategy_windows/mod.rs
  - crates/nono-cli/src/exec_strategy_windows/launch.rs
autonomous: true
requirements:
  - REQ-WSRH-02
  - REQ-WSRH-05
must_haves:
  truths:
    - "select_windows_token_arm returns BrokerLaunchNoPty when prefers_low_il_broker=true, !is_detached, !has_pty, has_session_sid (D-06: distinct variant)"
    - "select_windows_token_arm still returns WriteRestricted when prefers_low_il_broker=false — WriteRestricted branch is NOT removed (D-02: profile-only field threading, no CLI override)"
    - "PTY path still returns BrokerLaunch regardless of prefers_low_il_broker (Phase 31 regression-safe)"
    - "profile.windows_low_il_broker deserializes cross-platform without unknown-field error (D-01: profile-gated opt-in field)"
    - "ExecConfig.prefers_low_il_broker is wired from the profile field in the Windows construction path"
    - "All 7 existing pty_token_gate_tests pass with false as the new 5th argument"
  artifacts:
    - path: "crates/nono-cli/src/profile/mod.rs"
      provides: "windows_low_il_broker: bool field on Profile + ProfileDeserialize + merge_profiles"
      contains: "windows_low_il_broker"
    - path: "crates/nono-cli/data/policy.json"
      provides: "claude-code profile opt-in (D-03: only the claude-code built-in profile sets the field in v2.7)"
      contains: '"windows_low_il_broker": true'
    - path: "crates/nono-cli/data/nono-profile.schema.json"
      provides: "JSON schema entry for new field"
      contains: "windows_low_il_broker"
    - path: "crates/nono-cli/src/exec_strategy_windows/mod.rs"
      provides: "ExecConfig.prefers_low_il_broker: bool field"
      contains: "prefers_low_il_broker"
    - path: "crates/nono-cli/src/exec_strategy_windows/launch.rs"
      provides: "BrokerLaunchNoPty variant + select_windows_token_arm new param + unit tests"
      contains: "BrokerLaunchNoPty"
  key_links:
    - from: "policy.json claude-code profile"
      to: "profile/mod.rs ProfileDeserialize"
      via: "serde JSON deserialization at startup"
      pattern: "windows_low_il_broker"
    - from: "profile/mod.rs Profile"
      to: "exec_strategy_windows/mod.rs ExecConfig"
      via: "execution_runtime.rs Windows ExecConfig literal"
      pattern: "prefers_low_il_broker.*windows_low_il_broker"
    - from: "ExecConfig.prefers_low_il_broker"
      to: "select_windows_token_arm"
      via: "launch.rs call site (line ~1180)"
      pattern: "select_windows_token_arm.*prefers_low_il_broker"
---

<objective>
Add the profile-gated opt-in field and extend the token-arm cascade to dispatch non-PTY supervised launches to a new `WindowsTokenArm::BrokerLaunchNoPty` arm.

Purpose: This plan implements the structural prerequisites for the no-PTY Low-IL broker path — the profile field (D-01/D-02/D-03), the cascade enum variant (D-06), and the pure-function cascade extension (D-02). The spawn wiring that uses `BrokerLaunchNoPty` is in Plan 51-03. This plan is a hard dependency for Plan 51-03 because `BrokerLaunchNoPty` must be defined and all call sites must compile before the spawn arm can be written.

Output: New `WindowsTokenArm::BrokerLaunchNoPty` variant, extended `select_windows_token_arm` signature, `ExecConfig.prefers_low_il_broker` field, `Profile.windows_low_il_broker` field, updated `policy.json` + schema, and all 7 existing + 1 new unit tests passing.
</objective>

<execution_context>
@C:/Users/OMack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OMack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/OMack/nono/.planning/PROJECT.md
@C:/Users/OMack/nono/.planning/ROADMAP.md
@C:/Users/OMack/nono/.planning/phases/51-no-pty-low-il-broker-token-routing-write-deny-preservation/51-CONTEXT.md
@C:/Users/OMack/nono/.planning/phases/51-no-pty-low-il-broker-token-routing-write-deny-preservation/51-RESEARCH.md
@C:/Users/OMack/nono/.planning/phases/51-no-pty-low-il-broker-token-routing-write-deny-preservation/51-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From crates/nono-cli/src/exec_strategy_windows/launch.rs (lines 1073-1137):
Current WindowsTokenArm enum — add BrokerLaunchNoPty variant after BrokerLaunch:
  Null, WriteRestricted, LowIlPrimary, BrokerLaunch
Current select_windows_token_arm signature (4 params) — add prefers_low_il_broker: bool as 5th:
  pub(super) fn select_windows_token_arm(is_detached, has_pty, has_session_sid, caps_demand_low_il) -> WindowsTokenArm
Call site at line ~1180: let arm = select_windows_token_arm(is_windows_detached_launch, pty.is_some(), config.session_sid.is_some(), should_use_low_integrity_windows_launch(config.caps))
Token match arm for BrokerLaunchNoPty: mirrors BrokerLaunch — returns null h_token (lines 1214-1227)
7 existing pty_token_gate_tests at lines 1885-1977 — each must gain false as 5th argument

From crates/nono-cli/src/exec_strategy_windows/mod.rs (lines 129-166):
ExecConfig struct — add: pub prefers_low_il_broker: bool
Construction site in execution_runtime.rs (line 371-385, Windows cfg-gated):
  session_sid: Some(exec_strategy::generate_session_sid()),
  session_token: Some(windows_session_token.clone()),
  cap_pipe_rendezvous_path: Some(windows_cap_pipe_path.clone()),
  loaded_profile (Option<Profile>) is available as the binding `loaded_profile` at this scope.
  Add: prefers_low_il_broker: loaded_profile.as_ref().map_or(false, |p| p.windows_low_il_broker),

From crates/nono-cli/src/profile/mod.rs (lines 2059-2174, 3028-3030):
Profile struct has field unsafe_macos_seatbelt_rules at line ~2072 — add windows_low_il_broker: bool after it
ProfileDeserialize at line ~2091 has #[serde(deny_unknown_fields)] — MUST add windows_low_il_broker to BOTH structs
merge_profiles at line ~3028 — add: windows_low_il_broker: base.windows_low_il_broker || child.windows_low_il_broker

From crates/nono-cli/data/policy.json (line ~728):
claude-code profile has "interactive": true — add "windows_low_il_broker": true after it

From crates/nono-cli/data/nono-profile.schema.json (lines 99-103):
unsafe_macos_seatbelt_rules schema entry — add windows_low_il_broker: { "type": "boolean", "description": "..." }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Profile.windows_low_il_broker field + policy.json + schema</name>
  <files>
    crates/nono-cli/src/profile/mod.rs
    crates/nono-cli/data/policy.json
    crates/nono-cli/data/nono-profile.schema.json
  </files>
  <read_first>
    - crates/nono-cli/src/profile/mod.rs (full file — focus on lines 2059-2174 for Profile/ProfileDeserialize/From impl and lines 3020-3040 for merge_profiles)
    - crates/nono-cli/data/policy.json (lines 656-730 — claude-code profile block)
    - crates/nono-cli/data/nono-profile.schema.json (lines 95-110 — unsafe_macos_seatbelt_rules precedent)
  </read_first>
  <behavior>
    - Profile struct gains windows_low_il_broker: bool with #[serde(default)] doc comment (Windows-only, no-op on Linux/macOS)
    - ProfileDeserialize gains windows_low_il_broker: bool with #[serde(default)] — BOTH structs updated atomically or deny_unknown_fields crashes deserialization
    - impl From<ProfileDeserialize> for Profile mapping includes windows_low_il_broker: raw.windows_low_il_broker
    - merge_profiles adds windows_low_il_broker: base.windows_low_il_broker || child.windows_low_il_broker (OR semantics, same as interactive: base || child)
    - policy.json claude-code profile gains "windows_low_il_broker": true immediately after "interactive": true
    - nono-profile.schema.json gains windows_low_il_broker property entry (type: boolean) after unsafe_macos_seatbelt_rules
  </behavior>
  <action>
    In profile/mod.rs, add the following field to the Profile struct immediately after unsafe_macos_seatbelt_rules (line ~2072). Field name: windows_low_il_broker. Type: bool. Attribute: #[serde(default)]. Doc: "Windows-only. When true, routes non-PTY supervised launches through WindowsTokenArm::BrokerLaunchNoPty instead of WriteRestricted. Preserves mandatory-label NO_WRITE_UP write-deny while removing the restricting-SID double-gate that causes STATUS_DLL_INIT_FAILED in heavy-runtime children (Electron, CLR, Node SEA). Ignored on Linux and macOS (no-op; deserialize-only). Only set in the claude-code built-in profile for v2.7."

    In the ProfileDeserialize struct (line ~2132, which has #[serde(deny_unknown_fields)]), add the matching field: #[serde(default)] windows_low_il_broker: bool. This MUST be added to ProfileDeserialize in the same edit as the Profile struct change or policy.json deserialization will fail at startup with "unknown field: windows_low_il_broker".

    In impl From<ProfileDeserialize> for Profile (line ~2144-2173), add the mapping line windows_low_il_broker: raw.windows_low_il_broker after the allow_parent_of_protected line.

    In merge_profiles (line ~3028-3030, after unsafe_macos_seatbelt_rules dedup_append call), add: windows_low_il_broker: base.windows_low_il_broker || child.windows_low_il_broker. OR semantics: either base or child enables the opt-in (same pattern as interactive field at line ~3006).

    In policy.json, find the claude-code profile block (search for "claude-code" name; the block ends around line 729). After the "interactive": true entry (line ~728), add "windows_low_il_broker": true. Only the claude-code profile gets this field (D-03 — codex/opencode/openclaw/swival stay on WriteRestricted).

    In nono-profile.schema.json, add a new property entry for windows_low_il_broker after the unsafe_macos_seatbelt_rules block (lines ~99-103). Format: { "type": "boolean", "description": "Windows-only. Routes non-PTY supervised launches through the Low-IL broker arm instead of WRITE_RESTRICTED, eliminating STATUS_DLL_INIT_FAILED for heavy-runtime children (Electron, CLR, Node SEA). Ignored on Linux and macOS." }
  </action>
  <verify>
    <automated>cargo test -p nono-cli profile_tests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - profile/mod.rs contains the string "windows_low_il_broker" in the Profile struct, ProfileDeserialize struct, From impl, and merge_profiles function (4 distinct locations)
    - policy.json contains "windows_low_il_broker": true in the claude-code profile block and nowhere else
    - nono-profile.schema.json contains "windows_low_il_broker" as a property
    - cargo build -p nono-cli exits 0 (no deny_unknown_fields panic for the new field)
    - cargo test -p nono-cli profile_tests exits 0
  </acceptance_criteria>
  <done>profile/mod.rs, policy.json, and schema updated; all profile tests pass; field is deserializable cross-platform without unknown-field errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ExecConfig.prefers_low_il_broker + WindowsTokenArm::BrokerLaunchNoPty + cascade extension + unit tests</name>
  <files>
    crates/nono-cli/src/exec_strategy_windows/mod.rs
    crates/nono-cli/src/exec_strategy_windows/launch.rs
    crates/nono-cli/src/execution_runtime.rs
  </files>
  <read_first>
    - crates/nono-cli/src/exec_strategy_windows/mod.rs (lines 129-166 — ExecConfig struct; look for session_sid, session_token fields as structural precedents)
    - crates/nono-cli/src/exec_strategy_windows/launch.rs (lines 1057-1200 — WindowsTokenArm enum + select_windows_token_arm + call site; lines 1885-1977 — all 7 pty_token_gate_tests)
    - crates/nono-cli/src/execution_runtime.rs (lines 371-386 — Windows ExecConfig literal; lines 110-116 — loaded_profile binding)
  </read_first>
  <behavior>
    - ExecConfig gains pub prefers_low_il_broker: bool field (Windows-only-meaningful, lives on all platforms analogous to session_sid)
    - WindowsTokenArm gains BrokerLaunchNoPty variant after BrokerLaunch with doc explaining D-06 rationale
    - select_windows_token_arm gains prefers_low_il_broker: bool as 5th parameter; new branch inserted AFTER has_pty arm and BEFORE has_session_sid (WriteRestricted) arm: else if prefers_low_il_broker && has_session_sid returns BrokerLaunchNoPty
    - Token match arm for BrokerLaunchNoPty in spawn_windows_child returns null h_token identical to BrokerLaunch (no token construction — broker self-degrades)
    - Call site in spawn_windows_child passes config.prefers_low_il_broker as 5th argument
    - All 7 existing pty_token_gate_tests updated to pass false as 5th argument — they must still assert their original expected variants
    - New test pty_none_session_sid_with_broker_opt_in_selects_broker_launch_no_pty asserts BrokerLaunchNoPty when (false, false, true, false, true)
    - execution_runtime.rs Windows ExecConfig literal gains prefers_low_il_broker: loaded_profile.as_ref().map_or(false, |p| p.windows_low_il_broker)
    - Cascade ordering: is_detached → Null; has_pty → BrokerLaunch; prefers_low_il_broker && has_session_sid → BrokerLaunchNoPty; has_session_sid → WriteRestricted; caps_demand_low_il → LowIlPrimary; else → Null
  </behavior>
  <action>
    In exec_strategy_windows/mod.rs, add the field pub prefers_low_il_broker: bool to the ExecConfig struct after session_sid (or at end of struct before closing brace). Add doc comment: "Phase 51 D-02: when true, routes non-PTY supervised launches through WindowsTokenArm::BrokerLaunchNoPty instead of WriteRestricted. Sourced from profile.windows_low_il_broker. Windows-only-meaningful; the field exists on all platforms but is only consumed under cfg(target_os = "windows") paths (same pattern as session_sid)."

    In launch.rs, add BrokerLaunchNoPty variant to the WindowsTokenArm enum immediately after BrokerLaunch (line ~1100). Doc comment must explain D-06: distinct variant so PTY-path tests keep asserting BrokerLaunch (structurally proving Phase 31 PTY path is untouched); token construction is identical to BrokerLaunch (null h_token, broker self-degrades to Low IL); the variant signals downstream spawn wiring (anonymous-pipe stdio, no ConPTY).

    In launch.rs, add prefers_low_il_broker: bool as the 5th parameter to select_windows_token_arm (line ~1106). Insert the new branch as the THIRD arm (after has_pty, before has_session_sid): else if prefers_low_il_broker && has_session_sid { return WindowsTokenArm::BrokerLaunchNoPty; }. The has_session_sid → WriteRestricted arm MUST remain as the fourth arm (REQ-WSRH-02: WriteRestricted stays reachable when prefers_low_il_broker=false).

    In launch.rs, find the token h_token selection match block (line ~1214-1227) that matches on `arm`. Add a BrokerLaunchNoPty arm that sets _restricted_holder = None; _low_integrity_holder = None; and returns std::ptr::null_mut(). This is identical to BrokerLaunch — the token is null because the broker constructs its own Low-IL token internally via create_low_integrity_primary_token.

    In launch.rs, update the select_windows_token_arm call site (line ~1180-1185) to pass config.prefers_low_il_broker as the new 5th argument after should_use_low_integrity_windows_launch(config.caps).

    In launch.rs pty_token_gate_tests module (lines 1885-1977), update ALL 7 existing test calls to select_windows_token_arm to add false as the 5th argument (prefers_low_il_broker). Add the new test fn pty_none_session_sid_with_broker_opt_in_selects_broker_launch_no_pty that calls select_windows_token_arm(false, false, true, false, true) and asserts_eq!(arm, WindowsTokenArm::BrokerLaunchNoPty).

    In execution_runtime.rs, find the Windows-cfg-gated ExecConfig literal (line ~372-385). Add the field: prefers_low_il_broker: loaded_profile.as_ref().map_or(false, |p| p.windows_low_il_broker). The loaded_profile binding is already in scope at that point (see lines 114-116 where it is destructured from LaunchPlan).

    All new unsafe blocks in the new token match arm (if any) must have // SAFETY: comments.
  </action>
  <verify>
    <automated>cargo test -p nono-cli pty_token_gate_tests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p nono-cli pty_token_gate_tests exits 0 with all 8 tests passing (7 existing + 1 new pty_none_session_sid_with_broker_opt_in_selects_broker_launch_no_pty)
    - cargo test -p nono-cli pty_token_gate_tests::pty_none_with_session_sid_selects_write_restricted exits 0 (WriteRestricted still reachable, REQ-WSRH-02 regression-safe assertion)
    - cargo test -p nono-cli pty_token_gate_tests::pty_some_no_detach_selects_broker_launch exits 0 (Phase 31 PTY path untouched, REQ-WSRH-05)
    - cargo test -p nono-cli pty_token_gate_tests::pty_none_session_sid_with_broker_opt_in_selects_broker_launch_no_pty exits 0 (REQ-WSRH-02 positive case)
    - cargo build -p nono-cli --target x86_64-pc-windows-msvc exits 0
    - launch.rs contains BrokerLaunchNoPty variant in the WindowsTokenArm enum
    - mod.rs contains prefers_low_il_broker: bool in ExecConfig struct
  </acceptance_criteria>
  <done>ExecConfig.prefers_low_il_broker wired from profile; BrokerLaunchNoPty variant defined; cascade returns correct variant per input matrix; all 8 unit tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile JSON → ProfileDeserialize | Untrusted profile JSON deserialized with deny_unknown_fields; new field must be present in both structs |
| ExecConfig → select_windows_token_arm | Profile opt-in field flows as a plain bool; no user-supplied data crosses here (profile is locked to built-ins for v2.7) |
| cascade output → spawn wiring | The returned WindowsTokenArm variant determines which token and spawn mode is used; an incorrect variant selection would change the security envelope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51A-01 | Elevation of Privilege | select_windows_token_arm cascade ordering | mitigate | New BrokerLaunchNoPty branch inserted AFTER has_pty and BEFORE has_session_sid → WriteRestricted; WriteRestricted arm NOT removed; pty_none_with_session_sid_selects_write_restricted unit test structurally proves it (prefers_low_il_broker=false → WriteRestricted) |
| T-51A-02 | Elevation of Privilege | profile field widens to non-claude-code profiles | mitigate | Only claude-code profile gets "windows_low_il_broker": true in policy.json (D-03); merge_profiles OR semantics means a user-supplied profile without the field remains false; unit test only exercises the claude-code path |
| T-51A-03 | Tampering | ProfileDeserialize missing new field (deny_unknown_fields crash) | mitigate | Both Profile and ProfileDeserialize updated atomically; From impl mapping explicitly includes the field; rustc struct-literal completeness check catches missing field |
| T-51A-04 | Tampering | Phase 31 PTY path regressed by cascade change | mitigate | BrokerLaunchNoPty arm is inserted BEFORE WriteRestricted but AFTER has_pty; pty_some_no_detach_selects_broker_launch test keeps asserting BrokerLaunch (D-06 structural proof) |
| T-51A-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies introduced; all primitives confirmed in existing codebase |
</threat_model>

<verification>
After plan completion:
- cargo test -p nono-cli pty_token_gate_tests exits 0 (8 tests: 7 existing + 1 new)
- cargo test -p nono-cli profile_tests exits 0
- cargo build -p nono-cli --target x86_64-pc-windows-msvc exits 0 (no compile errors from new field/variant)
- grep "windows_low_il_broker" crates/nono-cli/src/profile/mod.rs returns 4+ matches (Profile struct, ProfileDeserialize, From impl, merge_profiles)
- grep "BrokerLaunchNoPty" crates/nono-cli/src/exec_strategy_windows/launch.rs returns 4+ matches (variant def, cascade branch, token arm, unit test)
- grep -v '^#' crates/nono-cli/data/policy.json | grep -c '"windows_low_il_broker": true' returns 1
</verification>

<success_criteria>
- WindowsTokenArm::BrokerLaunchNoPty variant exists in launch.rs
- select_windows_token_arm has 5 parameters; cascade ordering is: Null (detached) → BrokerLaunch (PTY) → BrokerLaunchNoPty (no-PTY + opt-in + session-SID) → WriteRestricted (session-SID, no opt-in) → LowIlPrimary → Null
- Profile.windows_low_il_broker: bool compiles on all platforms (cross-platform struct field)
- policy.json claude-code profile contains "windows_low_il_broker": true; no other profile has it
- ExecConfig.prefers_low_il_broker wired from loaded_profile in execution_runtime.rs Windows branch
- All 8 pty_token_gate_tests pass
</success_criteria>

<output>
Create .planning/phases/51-no-pty-low-il-broker-token-routing-write-deny-preservation/51-01-SUMMARY.md when done.
</output>
