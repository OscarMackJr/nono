---
phase: 51
plan: 03
type: execute
wave: 2
depends_on:
  - 51-01
  - 51-02
files_modified:
  - crates/nono-cli/src/exec_strategy_windows/launch.rs
autonomous: true
requirements:
  - REQ-WSRH-01
  - REQ-WSRH-03
  - REQ-WSRH-05
must_haves:
  truths:
    - "spawn_windows_child has a BrokerLaunchNoPty arm that creates DetachedStdioPipes, gates the 3 child-end handles with PROC_THREAD_ATTRIBUTE_HANDLE_LIST, and passes --no-pty + 3 --inherit-handle values to the broker command line"
    - "The supervisor relay receives child stdout/stderr through detached_stdio_pipes (confirmed valid by PATTERNS.md open-question verification: execute_supervised unconditionally wires detached_stdio)"
    - "nono-shell-broker.exe is pre-built before write_deny_low_il_broker_no_pty_tests runs (Wave 0 pre-build step)"
    - "write_deny_low_il_broker_no_pty_tests spawns the broker with --no-pty and a child that attempts a write to a %TEMP% file; the test asserts the write was denied (exit code non-zero OR file unmodified)"
    - "The test hard-fails (no #[ignore]) if the broker artifact is missing or the child cannot be spawned (D-08)"
    - "All existing broker_dispatch_tests still pass (no regression to Phase 31 paths)"
    - "Authenticode verification is applied to the broker on the BrokerLaunchNoPty path (same as BrokerLaunch)"
    - "Handle inheritance is gated: child-end pipe handles are flipped to inheritable, added to PROC_THREAD_ATTRIBUTE_HANDLE_LIST; parent-end handles are flipped back non-inheritable after CreateProcessW"
  artifacts:
    - path: "crates/nono-cli/src/exec_strategy_windows/launch.rs"
      provides: "BrokerLaunchNoPty spawn arm + write_deny_low_il_broker_no_pty_tests module"
      contains: "BrokerLaunchNoPty"
  key_links:
    - from: "BrokerLaunchNoPty arm in spawn_windows_child"
      to: "DetachedStdioPipes::create()"
      via: "create anonymous-pipe stdin/stdout/stderr pairs (Phase 17 machinery)"
      pattern: "DetachedStdioPipes::create"
    - from: "spawn_windows_child return value"
      to: "execute_supervised detached_stdio relay"
      via: "Some(pipes) returned from BrokerLaunchNoPty arm; supervisor.attach_detached_stdio unconditional"
      pattern: "detached_stdio.*Some"
    - from: "write_deny_low_il_broker_no_pty_tests"
      to: "nono-shell-broker.exe artifact"
      via: "two-candidate path lookup in CARGO_MANIFEST_DIR/../../target/"
      pattern: "nono-shell-broker.exe"
---

<objective>
Wire the `BrokerLaunchNoPty` arm in `spawn_windows_child` (anonymous-pipe spawn, broker command line with `--no-pty`) and add the write-deny real-spawn integration test (REQ-WSRH-03 / D-07).

Purpose: With the BrokerLaunchNoPty variant defined (Plan 51-01) and the broker accepting `--no-pty` (Plan 51-02), this plan implements the spawn side in nono-cli: creating the anonymous pipes, gating handle inheritance with PROC_THREAD_ATTRIBUTE_HANDLE_LIST, building the broker command line with `--no-pty` and three `--inherit-handle` values, and calling CreateProcessW. It also adds the real-spawn integration test that proves mandatory-label NO_WRITE_UP write-deny is enforced at the kernel level for a Low-IL child (REQ-WSRH-03, D-07).

Output: `spawn_windows_child` handles `BrokerLaunchNoPty`; supervisor relay wired via DetachedStdioPipes; `write_deny_low_il_broker_no_pty_tests` integration test module in launch.rs; pre-build step for nono-shell-broker.exe documented in the plan task.
</objective>

<execution_context>
@C:/Users/OMack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OMack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/OMack/nono/.planning/PROJECT.md
@C:/Users/OMack/nono/.planning/phases/51-no-pty-low-il-broker-token-routing-write-deny-preservation/51-CONTEXT.md
@C:/Users/OMack/nono/.planning/phases/51-no-pty-low-il-broker-token-routing-write-deny-preservation/51-RESEARCH.md
@C:/Users/OMack/nono/.planning/phases/51-no-pty-low-il-broker-token-routing-write-deny-preservation/51-PATTERNS.md
@C:/Users/OMack/nono/.planning/phases/51-no-pty-low-il-broker-token-routing-write-deny-preservation/51-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From crates/nono-cli/src/exec_strategy_windows/launch.rs (from Plan 51-01 output):

DetachedStdioPipes (lines 56-171):
  DetachedStdioPipes::create() -> Result<DetachedStdioPipes>
  Fields: stdin_read, stdin_write, stdout_read, stdout_write, stderr_read, stderr_write (all HANDLE)
  unsafe fn close_child_ends() — closes stdin_read, stdout_write, stderr_write
  create() sets parent ends non-inheritable; child ends START inheritable (see Pitfall 4 in RESEARCH.md for SetHandleInformation flip requirement)

BrokerLaunch arm in spawn_windows_child (lines 1261-1578) — copy structure for BrokerLaunchNoPty:
  1. Resolve broker path (nono_exe.parent() + "nono-shell-broker.exe")
  2. verify_broker_authenticode (skip if is_dev_build_layout)
  3. Create pipe pairs / conpty handles
  4. SetHandleInformation(child ends, HANDLE_FLAG_INHERIT, HANDLE_FLAG_INHERIT)
  5. PROC_THREAD_ATTRIBUTE_HANDLE_LIST with the handles
  6. build_broker_command_line + add --no-pty flag for BrokerLaunchNoPty
  7. CreateProcessW(CREATE_SUSPENDED + EXTENDED_STARTUPINFO_PRESENT + bInheritHandles=1)
  8. AssignProcessToJobObject + apply_resource_limits + ResumeThread
  9. DeleteProcThreadAttributeList + unflip handles + close_child_ends
  10. Return detached_stdio = Some(pipes)

broker_dispatch_tests module (lines 2344-2608) — precedent for write_deny test:
  Two-candidate broker path lookup (CARGO_MANIFEST_DIR/../../target/{triple}/release/ + ../../target/release/)
  Panics loudly if broker missing (D-08 no-silent-skip)
  CREATE_SUSPENDED → AssignProcessToJobObject → ResumeThread → WaitForSingleObject → GetExitCodeProcess

execute_supervised relay (PATTERNS.md open-question verification):
  execute_supervised unconditionally calls runtime.attach_detached_stdio(detached_stdio) regardless of is_windows_detached_launch.
  When BrokerLaunchNoPty returns Some(pipes), the relay picks up pipes.stdout_read correctly.
  NO gate-relaxation task needed.

For the write_deny test (RESEARCH.md Pattern 4):
  Fixture: std::env::temp_dir().join(format!("nono-test-write-deny-{}.tmp", std::process::id()))
  std::fs::write(&fixture, b"sentinel") to create with default Medium-IL label
  Spawn broker with: --no-pty + 3 pipe handles + --shell cmd.exe --shell-arg /c --shell-arg "echo x > <fixture_path>"
  cmd.exe exits 1 on "Access is denied." — assert GetExitCodeProcess != 0
  Robust fallback: read fixture and assert contents == b"sentinel" (not modified)
  cfg gate: #[cfg(all(test, target_os = "windows"))]
  #[allow(clippy::unwrap_used)] on module (same as broker_dispatch_tests)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-build nono-shell-broker.exe artifact for integration test</name>
  <files></files>
  <read_first>
    - crates/nono-shell-broker/Cargo.toml (verify package name is nono-shell-broker)
  </read_first>
  <action>
    Build the nono-shell-broker binary in release mode so the write_deny_low_il_broker_no_pty_tests integration test can find it. Run: cargo build -p nono-shell-broker --target x86_64-pc-windows-msvc --release

    The test's two-candidate path lookup checks:
      {workspace}/target/x86_64-pc-windows-msvc/release/nono-shell-broker.exe
      {workspace}/target/release/nono-shell-broker.exe

    If neither exists, the test panics loudly per D-08. Building now ensures the artifact is present before the integration test runs.

    This is a prerequisite step, not a code change. No files_modified.
  </action>
  <verify>
    <automated>cargo build -p nono-shell-broker --target x86_64-pc-windows-msvc --release 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - cargo build -p nono-shell-broker --target x86_64-pc-windows-msvc --release exits 0
    - File target/x86_64-pc-windows-msvc/release/nono-shell-broker.exe exists in the workspace
  </acceptance_criteria>
  <done>nono-shell-broker.exe built at release; write_deny integration test can resolve the artifact path</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: BrokerLaunchNoPty spawn arm in spawn_windows_child</name>
  <files>
    crates/nono-cli/src/exec_strategy_windows/launch.rs
  </files>
  <read_first>
    - crates/nono-cli/src/exec_strategy_windows/launch.rs (lines 56-171 — DetachedStdioPipes; lines 1261-1578 — BrokerLaunch arm to copy structure from; lines 1036-1055 — build_broker_command_line; lines 1570-1610 — pty-is-None path / existing else branch structure)
  </read_first>
  <behavior>
    - BrokerLaunchNoPty arm in spawn_windows_child creates DetachedStdioPipes via DetachedStdioPipes::create()
    - Child-end handles (stdin_read, stdout_write, stderr_write) are set to HANDLE_FLAG_INHERIT via SetHandleInformation before PROC_THREAD_ATTRIBUTE_HANDLE_LIST construction
    - PROC_THREAD_ATTRIBUTE_HANDLE_LIST contains exactly the 3 child-end handles (prevents handle leaking per Pitfall 3 in RESEARCH.md)
    - Broker command line includes --no-pty flag before the three --inherit-handle values
    - CreateProcessW called with CREATE_SUSPENDED + EXTENDED_STARTUPINFO_PRESENT + bInheritHandles=1
    - After CreateProcessW: DeleteProcThreadAttributeList; unflip child-end handles to non-inheritable; close_child_ends()
    - AssignProcessToJobObject + apply_resource_limits + ResumeThread (same order as BrokerLaunch arm)
    - detached_stdio = Some(pipes) returned from the arm (supervisor relay wires stdout_read for output forwarding)
    - Broker path is resolved via current_exe().parent() + "nono-shell-broker.exe" — same as BrokerLaunch arm
    - verify_broker_authenticode applied unless is_dev_build_layout (same security invariant as BrokerLaunch)
    - All error paths return Err(NonoError::SandboxInit(...)) — no silent fallback to WriteRestricted or Null
    - Every unsafe block has a // SAFETY: comment
  </behavior>
  <action>
    Read launch.rs lines 56-171 (DetachedStdioPipes), lines 1261-1578 (BrokerLaunch arm, the copy source), and lines 1570-1620 (the pty-is-None else branch where BrokerLaunchNoPty arm will be inserted).

    Add the BrokerLaunchNoPty arm to the match statement (or if-else chain) in spawn_windows_child that dispatches on `arm`. The BrokerLaunchNoPty arm lives in the pty.is_none() / else path alongside the existing non-PTY arms (WriteRestricted, LowIlPrimary, Null). The BrokerLaunch arm is in the Some(pty_pair) branch; BrokerLaunchNoPty is structurally its counterpart in the else (no-PTY) branch.

    Step 1 — Broker path resolution: copy lines 1267-1281 from BrokerLaunch arm verbatim (current_exe(), parent(), join("nono-shell-broker.exe"), exists() check).

    Step 2 — Authenticode: copy lines 1289-1297 verbatim (is_dev_build_layout check, verify_broker_authenticode, tracing::info on skip).

    Step 3 — Create pipes: let mut pipes = DetachedStdioPipes::create()?; Collect child-end handles: let inherit_handles: [HANDLE; 3] = [pipes.stdin_read, pipes.stdout_write, pipes.stderr_write];

    Step 4 — Flip child-end handles to inheritable: for each handle in inherit_handles, call SetHandleInformation(h, HANDLE_FLAG_INHERIT, HANDLE_FLAG_INHERIT). Return Err on failure after unflipping the ones already flipped. Mirror error handling from lines 1314-1337 (BrokerLaunch). SAFETY comment required.

    Step 5 — PROC_THREAD_ATTRIBUTE_HANDLE_LIST: copy lines 1339-1397 verbatim, substituting inherit_handles ([HANDLE; 3]) for the ConPTY handle array ([HANDLE; 2]). The size_of_val call in UpdateProcThreadAttribute must use inherit_handles not a 2-element array.

    Step 6 — Broker command line: use build_broker_command_line with broker_path and broker_args. Build broker_args: --shell <program>, --shell-arg for each cmd_arg, --no-pty (NEW), --inherit-handle 0x{:016x} for each of the 3 handles in inherit_handles, --cwd <current_dir>. See PATTERNS.md Pattern C step 6 for the exact OsString construction.

    Step 7 — CreateProcessW: copy lines 1421-1470 verbatim (CREATE_SUSPENDED + EXTENDED_STARTUPINFO_PRESENT + bInheritHandles=1).

    Step 8 — Post-spawn cleanup: DeleteProcThreadAttributeList (SAFETY comment); unflip all 3 child-end handles to non-inheritable via SetHandleInformation(h, HANDLE_FLAG_INHERIT, 0); unsafe { pipes.close_child_ends() } (SAFETY: child-end handles are no longer needed in this process after CreateProcessW; closing avoids handle leak and signals EOF to the child when the supervisor closes its write ends).

    Step 9 — Job + resource limits + resume: copy AssignProcessToJobObject + apply_resource_limits + ResumeThread from BrokerLaunch arm (lines 1480-1530 area).

    Step 10 — Set detached_stdio: detached_stdio = Some(pipes); and continue to the return path.

    Fail-closed invariant: every Win32 API failure returns Err(NonoError::SandboxInit(format!("... (error={last})"))); never a silent fallback.
  </action>
  <verify>
    <automated>cargo build -p nono-cli --target x86_64-pc-windows-msvc 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - cargo build -p nono-cli --target x86_64-pc-windows-msvc exits 0
    - cargo test -p nono-cli broker_dispatch_tests 2>&1 exits 0 (existing broker tests still pass — no regression to Phase 31 BrokerLaunch path)
    - launch.rs BrokerLaunchNoPty arm contains "DetachedStdioPipes::create" and "--no-pty" string in the broker_args construction
    - launch.rs BrokerLaunchNoPty arm contains "PROC_THREAD_ATTRIBUTE_HANDLE_LIST" (HANDLE_LIST gating active)
    - launch.rs BrokerLaunchNoPty arm contains "verify_broker_authenticode" (Authenticode check active)
    - All new unsafe blocks have // SAFETY: comments (check: grep -A1 "unsafe {" launch.rs — each must be preceded by a SAFETY comment)
  </acceptance_criteria>
  <done>BrokerLaunchNoPty spawn arm complete: anonymous pipes created, HANDLE_LIST gated, broker invoked with --no-pty, detached_stdio=Some(pipes) for supervisor relay; existing broker_dispatch_tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: write_deny_low_il_broker_no_pty_tests integration test module</name>
  <files>
    crates/nono-cli/src/exec_strategy_windows/launch.rs
  </files>
  <read_first>
    - crates/nono-cli/src/exec_strategy_windows/launch.rs (lines 2344-2608 — broker_dispatch_tests module, the structural precedent: two-candidate broker path lookup, CREATE_SUSPENDED pattern, panic! on missing artifact)
  </read_first>
  <behavior>
    - New module write_deny_low_il_broker_no_pty_tests with cfg gate #[cfg(all(test, target_os = "windows"))] and #[allow(clippy::unwrap_used)]
    - One test fn write_deny_low_il_broker_no_pty_prevents_child_write_to_medium_il_file
    - Test creates a temp file in std::env::temp_dir() with sentinel content b"sentinel" (Medium-IL by default — no explicit label needed per PATTERNS.md Pattern E)
    - Test resolves broker artifact via two-candidate lookup (same as broker_dispatch_tests lines 2430-2459); panics loudly if broker missing (D-08 no-silent-skip)
    - Test creates DetachedStdioPipes (or equivalent anonymous pipes for the test harness) and passes 3 --inherit-handle values to the broker command line
    - Broker invoked with --no-pty --shell cmd.exe --shell-arg /c --shell-arg "echo x > <fixture_path>" plus the 3 pipe handles and --cwd
    - Test waits for broker exit via WaitForSingleObject(INFINITE)
    - Test asserts write was denied: GetExitCodeProcess != 0 OR reads fixture file and asserts contents == b"sentinel" (not modified)
    - Test cleans up: closes handles, deletes fixture file (best-effort)
    - Hard-fail invariant: if fixture file creation fails, pipe creation fails, or broker spawn fails → test panics with a diagnostic message (not #[ignore], not silent pass)
  </behavior>
  <action>
    Add the write_deny_low_il_broker_no_pty_tests module at the end of launch.rs, after the broker_dispatch_tests module. Module header: #[cfg(all(test, target_os = "windows"))] #[allow(clippy::unwrap_used)] mod write_deny_low_il_broker_no_pty_tests { ... }

    Imports: windows_sys::Win32::Foundation::{CloseHandle, GetLastError, HANDLE, INVALID_HANDLE_VALUE, FALSE}, windows_sys::Win32::System::Threading::{CreateProcessW, GetExitCodeProcess, ResumeThread, TerminateProcess, WaitForSingleObject, CREATE_SUSPENDED, EXTENDED_STARTUPINFO_PRESENT, INFINITE, PROCESS_INFORMATION, STARTUPINFOW}, windows_sys::Win32::System::JobObjects::AssignProcessToJobObject, std::path::PathBuf, std::ffi::OsString.

    Broker artifact resolution: copy the two-candidate lookup from broker_dispatch_tests lines 2430-2459 verbatim. The two candidates are {CARGO_MANIFEST_DIR}/../../target/x86_64-pc-windows-msvc/release/nono-shell-broker.exe and {CARGO_MANIFEST_DIR}/../../target/release/nono-shell-broker.exe. If neither exists: panic!("nono-shell-broker.exe missing at [...] and [...] — cannot be silently skipped (Phase 41 CR-04 disposition). Pre-build with: cargo build -p nono-shell-broker --release --target x86_64-pc-windows-msvc").

    Temp fixture: use std::env::temp_dir().join(format!("nono-test-write-deny-{}.tmp", std::process::id())). Write b"sentinel" to it with std::fs::write; if this fails, panic! with a diagnostic.

    For the pipe handles, create 3 simple anonymous pipe pairs using CreatePipe from windows_sys::Win32::System::Pipes. Pass stdin_read, stdout_write, stderr_write as the 3 --inherit-handle values. The test harness only needs to pass valid non-null handle values; the broker will bind them as child stdio via STARTF_USESTDHANDLES.

    Build broker command: use build_broker_command_line (the same helper as the production path — import it in the test or duplicate its logic). Command: broker_path --shell cmd.exe --shell-arg /c --shell-arg "echo x > {fixture_path}" --no-pty --inherit-handle 0x{:016x} (×3) --cwd {temp_dir}. The STARTUPINFOW for the broker spawn itself (not the child's STARTUPINFOW) is zeroed. bInheritHandles=1 is NOT needed for the test harness CreateProcessW call if the pipe handles are all that matter — but use a PROC_THREAD_ATTRIBUTE_HANDLE_LIST gating just the 3 handles (security hygiene, same as production code).

    Actually for test simplicity: mirror the exact broker_dispatch_tests pattern (lines 2497-2608) which uses a simple STARTUPINFOW (no attribute list) for the broker spawn at test level. The HANDLE_LIST is applied by the production BrokerLaunchNoPty arm; the test is validating the child's write-deny behavior via the broker, not the handle-list construction. Use the simpler test harness shape.

    Post-spawn: WaitForSingleObject(pi.hProcess, INFINITE). GetExitCodeProcess. Assert exit_code != 0 (cmd.exe exits 1 on "Access is denied."). As a belt-and-suspenders check, also read the fixture file and assert contents == b"sentinel" (not modified by the child).

    Cleanup: CloseHandle(pi.hProcess); CloseHandle(pi.hThread); close pipe handles; std::fs::remove_file(&fixture) best-effort (ignore error).

    All unsafe blocks have // SAFETY: comments.
  </action>
  <verify>
    <automated>cargo test -p nono-cli --target x86_64-pc-windows-msvc write_deny_low_il_broker_no_pty_tests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p nono-cli --target x86_64-pc-windows-msvc write_deny_low_il_broker_no_pty_tests exits 0
    - Test output includes "test write_deny_low_il_broker_no_pty_tests::write_deny_low_il_broker_no_pty_prevents_child_write_to_medium_il_file ... ok"
    - Test does NOT have #[ignore] attribute
    - cargo test -p nono-cli --target x86_64-pc-windows-msvc broker_dispatch_tests exits 0 (existing integration tests unaffected)
    - cargo test -p nono-cli pty_token_gate_tests exits 0 (unit tests still pass after the file was modified in Task 1)
  </acceptance_criteria>
  <done>write_deny_low_il_broker_no_pty_tests integration test passes: Low-IL child's write to a Medium-IL %TEMP% file is denied by OS kernel MIC pre-DACL check; test hard-fails if broker missing; no #[ignore]</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nono-cli → nono-shell-broker (BrokerLaunchNoPty) | nono-cli passes 3 pipe handles + --no-pty to broker; HANDLE_LIST gates exactly these 3 handles (no other inheritable handles leak) |
| broker → Low-IL child process | Broker creates Low-IL token via create_low_integrity_primary_token, binds pipes via STARTF_USESTDHANDLES, calls CreateProcessAsUserW; OS kernel MIC enforces NO_WRITE_UP pre-DACL |
| supervisor relay ← detached_stdio_pipes | Supervisor reads child stdout/stderr from pipes.stdout_read/stderr_read; execute_supervised wires detached_stdio unconditionally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51C-01 | Elevation of Privilege | Low-IL child escapes NO_WRITE_UP write-deny | mitigate | Broker constructs Low-IL primary token (no restricting SID) via create_low_integrity_primary_token; OS kernel MIC pre-DACL enforces NO_WRITE_UP — the test write_deny_low_il_broker_no_pty_prevents_child_write_to_medium_il_file is the cryptographic proof (it must pass on Windows before this plan closes) |
| T-51C-02 | Elevation of Privilege | Silent fallback from BrokerLaunchNoPty to WriteRestricted or Null on error | mitigate | Every Win32 API failure in the BrokerLaunchNoPty arm returns Err(NonoError::SandboxInit(...)); no match arms fall through to less-secure alternatives |
| T-51C-03 | Tampering | Handle leaking: supervisor's capability-pipe handles inherited by broker | mitigate | PROC_THREAD_ATTRIBUTE_HANDLE_LIST contains exactly the 3 pipe handles (mirror of BrokerLaunch arm pattern); child-end handles are explicitly flipped to inheritable before the HANDLE_LIST and back to non-inheritable after CreateProcessW |
| T-51C-04 | Tampering | Broker binary substitution (rogue nono-shell-broker.exe) | mitigate | verify_broker_authenticode applied to the BrokerLaunchNoPty broker path resolution (same security invariant as BrokerLaunch arm, lines 1289-1297) |
| T-51C-05 | Tampering | Test fixture on drive root causing false-PASS (WRITE_OWNER limitation) | mitigate | Test fixture uses std::env::temp_dir() (maps to %TEMP%), NOT C:\poc\ or any drive-root path; per D-08 and memory feedback_windows_mandatory_label_write_owner |
| T-51C-06 | Tampering | Integration test silently skipped if broker missing | mitigate | Hard-fail: panic!(...) if nono-shell-broker.exe not found at either candidate path (D-08, mirrors Phase 41 BROKER-CR-04 policy) |
| T-51C-07 | Tampering | Phase 31 broker_dispatch_tests regressed | mitigate | BrokerLaunchNoPty arm is ONLY active when WindowsTokenArm::BrokerLaunchNoPty is returned by select_windows_token_arm; existing BrokerLaunch arm is structurally unchanged; broker_dispatch_tests must pass before plan closes (acceptance criterion) |
| T-51C-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies introduced in this plan |
</threat_model>

<verification>
After plan completion:
- cargo build -p nono-shell-broker --target x86_64-pc-windows-msvc --release exits 0 (pre-build)
- cargo test -p nono-cli --target x86_64-pc-windows-msvc write_deny_low_il_broker_no_pty_tests exits 0
- cargo test -p nono-cli --target x86_64-pc-windows-msvc broker_dispatch_tests exits 0
- cargo test -p nono-cli pty_token_gate_tests exits 0
- grep "verify_broker_authenticode" crates/nono-cli/src/exec_strategy_windows/launch.rs returns 2 matches (BrokerLaunch + BrokerLaunchNoPty arms)
- grep "PROC_THREAD_ATTRIBUTE_HANDLE_LIST" crates/nono-cli/src/exec_strategy_windows/launch.rs returns 2+ matches (BrokerLaunch + BrokerLaunchNoPty)
- grep "DetachedStdioPipes::create" crates/nono-cli/src/exec_strategy_windows/launch.rs returns 2+ matches
- No #[ignore] attribute in write_deny_low_il_broker_no_pty_tests module
</verification>

<success_criteria>
- BrokerLaunchNoPty spawn arm complete in spawn_windows_child; anonymous pipes created; HANDLE_LIST gated to 3 handles; --no-pty + 3 handles in broker command line; detached_stdio=Some(pipes) returned
- Authenticode check applies to BrokerLaunchNoPty path
- write_deny_low_il_broker_no_pty_tests passes: kernel MIC pre-DACL denies Low-IL child write to Medium-IL %TEMP% file
- All existing broker_dispatch_tests pass (Phase 31 no-regression)
- All pty_token_gate_tests pass (8 tests)
</success_criteria>

<output>
Create .planning/phases/51-no-pty-low-il-broker-token-routing-write-deny-preservation/51-03-SUMMARY.md when done.
</output>
