---
phase: 73-ai-agent-marker
plan: 03
type: execute
wave: 2
depends_on: [73-01, 73-02]
files_modified:
  - crates/nono-cli/src/classify_runtime.rs
  - crates/nono-cli/src/cli.rs
  - crates/nono-cli/src/app_runtime.rs
  - crates/nono-cli/src/main.rs
  - crates/nono-cli/src/execution_runtime.rs
  - proj/DESIGN-engine-abstraction.md
autonomous: false
requirements: [MARK-01]
user_setup: []

must_haves:
  truths:
    - "nono classify <pid> prints 'AI_AGENT' for a PID the launcher spawned"
    - "nono classify <pid> prints 'not an agent' for an unrelated PID"
    - "nono classify output includes a non-authoritative disclaimer line"
    - "A launched agent's package SID is inserted into the AgentRegistry at spawn time"
    - "Spoof AppContainer (registry-absent) classifies as NotAnAgent"
    - "SC5 adopted-agent best-effort/demote-only limitation is documented"
  artifacts:
    - path: "crates/nono-cli/src/classify_runtime.rs"
      provides: "run_classify(args: ClassifyArgs, registry: Arc<Mutex<AgentRegistry>>) -> Result<()>"
      min_lines: 40
    - path: "crates/nono-cli/src/cli.rs"
      provides: "ClassifyArgs struct + Commands::Classify variant"
      contains: "ClassifyArgs"
    - path: "crates/nono-cli/src/app_runtime.rs"
      provides: "dispatch arm for Commands::Classify"
      contains: "Commands::Classify"
    - path: "crates/nono-cli/src/execution_runtime.rs"
      provides: "registry.insert(windows_package_sid.clone()) at mint time"
      contains: "AgentRegistry"
    - path: "proj/DESIGN-engine-abstraction.md"
      provides: "SC5 adopted-agent best-effort documentation appended"
      contains: "adopted"
  key_links:
    - from: "crates/nono-cli/src/app_runtime.rs:dispatch_command"
      to: "crates/nono-cli/src/classify_runtime.rs:run_classify"
      via: "Commands::Classify(args) => run_classify(args, registry.clone())"
      pattern: "run_classify"
    - from: "crates/nono-cli/src/execution_runtime.rs"
      to: "nono::AgentRegistry"
      via: "registry.lock().map_err(...).insert(windows_package_sid.clone())"
      pattern: "AgentRegistry"
---

<objective>
Wire the AI_AGENT marker into the shipping code path and ship the operator-facing `nono classify <pid>` CLI verb. Three distinct deliverables:

1. **Mint→registry wiring** (`execution_runtime.rs`): insert the minted package SID into an `Arc<Mutex<AgentRegistry>>` immediately after `windows_package_sid` is derived at line 488 — this is what makes real launches register at spawn time (SC1 "genuinely satisfied on the SHIPPING path").

2. **CLI verb** (`classify_runtime.rs`, `cli.rs`, `app_runtime.rs`, `main.rs`): `nono classify <pid>` calls `registry.classify(pid)` and prints human-readable output with a non-authoritative disclaimer. Wired exactly like the existing `nono why` verb.

3. **SC4 in-process integration tests** (in `execution_runtime.rs` or `classify_runtime.rs` test module, `#[ignore]`): a real confined child → `AI_AGENT`; a self-made AppContainer (not inserted) → `NotAnAgent`. These require a real Win11 host and dev-layout broker.

4. **SC5 documentation** (`proj/DESIGN-engine-abstraction.md`): append a section on adopted/not-launched agents as best-effort/demote-only.

Purpose: Closes SC1 (launched agent marked on shipping path), SC4 (in-process integration proof), SC5 (best-effort documentation), and the operator `nono classify` surface (D-04).

Output: New `classify_runtime.rs`; modified `cli.rs`, `app_runtime.rs`, `main.rs`, `execution_runtime.rs`, `proj/DESIGN-engine-abstraction.md`.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\OMack\Nono\.planning\PROJECT.md
@C:\Users\OMack\Nono\.planning\ROADMAP.md
@C:\Users\OMack\Nono\.planning\phases\73-ai-agent-marker\73-CONTEXT.md
@C:\Users\OMack\Nono\.planning\phases\73-ai-agent-marker\73-RESEARCH.md
@C:\Users\OMack\Nono\.planning\phases\73-ai-agent-marker\73-PATTERNS.md
@C:\Users\OMack\Nono\.planning\phases\73-ai-agent-marker\73-VALIDATION.md
@C:\Users\OMack\Nono\.planning\phases\73-ai-agent-marker\73-01-SUMMARY.md
@C:\Users\OMack\Nono\.planning\phases\73-ai-agent-marker\73-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From crates/nono-cli/src/why_runtime.rs (the exact analog — read the full file):
```rust
// Full file structure: single pub(crate) fn run_why(args: WhyArgs) -> Result<()>
// Imports: use crate::cli::{WhyArgs, ...}; use nono::{...};
// Output: println!(...) for results, eprintln!(...) for internal errors
// Error propagation: expected-outcome "not found" → Ok(()) + print; program errors → Err(...)
```

From crates/nono-cli/src/cli.rs lines 725-743 (Commands::Why — exact template for Commands::Classify):
```rust
Why(Box<WhyArgs>),  // the Why variant template
```

From crates/nono-cli/src/cli.rs lines 3073-3088 (InspectArgs — exact template for ClassifyArgs struct):
```rust
#[derive(Parser, Debug)]
pub struct InspectArgs {
    pub session: String,  // positional
    #[arg(long)] pub json: bool,
    #[arg(long)] pub events: bool,
}
```

From crates/nono-cli/src/app_runtime.rs lines 41-58 (dispatch_command):
```rust
fn dispatch_command(command: Commands, silent: bool, internal_supervisor: bool,
                    update_handle: &mut Option<update_check::UpdateCheckHandle>) -> Result<()> {
    match command {
        Commands::Why(args) => run_command_with_update(update_handle, silent, || run_why(*args)),
        // Pattern for Classify:
        // Commands::Classify(args) => run_command_with_update(update_handle, silent, || {
        //     classify_runtime::run_classify(args, registry.clone())
        // }),
    }
}
```

From crates/nono-cli/src/main.rs lines 24-110 (module declarations):
```rust
// Platform-gated example:
#[cfg(target_os = "windows")] mod exec_identity_windows;
// classify_runtime is NOT platform-gated (compiles on all platforms via AgentRegistry stub):
mod classify_runtime;  // insert alphabetically
```

From crates/nono-cli/src/execution_runtime.rs lines 482-530 (the exact wiring point):
```rust
// Existing code (lines 482-488):
#[cfg(target_os = "windows")]
let windows_app_container_name = exec_strategy::generate_app_container_name();
#[cfg(target_os = "windows")]
let windows_package_sid = {
    let psid = nono::derive_app_container_sid(&windows_app_container_name)?;
    nono::package_sid_to_string(&psid)?
};
// INSERT registry insert AFTER line 488:
#[cfg(target_os = "windows")]
{
    registry
        .lock()
        .map_err(|_| nono::NonoError::SandboxInit("AgentRegistry mutex poisoned".into()))?
        .insert(windows_package_sid.clone());
}
// `registry` is Arc<Mutex<AgentRegistry>> — must be threaded into the function
// that contains lines 482-488. Read the current function signature to find the
// right threading approach (see RESEARCH.md Open Question 3).
```

From nono::AgentClassification (created in Plan 01):
```rust
pub enum AgentClassification {
    AiAgent { package_sid: String },
    NotAnAgent,
}
```

RESEARCH.md Example 3 (classify output format):
```
$ nono classify 1234
PID 1234: AI_AGENT
  Package SID: S-1-15-2-1234567890-...
  AppContainer: yes
  In job: yes

$ nono classify 5678
PID 5678: not an agent
  AppContainer: no
  (NOTE: This check is structural only — not an authorization decision)

$ nono classify 9999
Error: OpenProcess(pid=9999) failed (GLE=87) — process may not exist
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mint→registry wiring in execution_runtime.rs</name>
  <files>crates/nono-cli/src/execution_runtime.rs</files>
  <read_first>
    - crates/nono-cli/src/execution_runtime.rs (lines 460-540: the full context around lines 482-488 — find the enclosing function name and signature; understand how Arc<Mutex<AgentRegistry>> will be introduced; read the function that owns lines 482-488 to determine threading approach)
    - .planning/phases/73-ai-agent-marker/73-RESEARCH.md (Example 2: Wiring Insert; Open Question 3: Arc<Mutex<AgentRegistry>> threading — recommends passing as a separate parameter rather than through ExecConfig)
    - .planning/phases/73-ai-agent-marker/73-PATTERNS.md (execution_runtime.rs section: exact wiring point at lines 482-488, map_err on PoisonError pattern)
    - .planning/phases/73-ai-agent-marker/73-CONTEXT.md (D-04: "Wire mint→registry into the live launch path so real launches register the package SID at spawn-time")
    - crates/nono-cli/src/app_runtime.rs (how the registry Arc will be threaded from dispatch_command down into the function that calls execution_runtime — this determines the correct parameter threading approach)
  </read_first>
  <action>
    Wire the `AgentRegistry` insert into the live launch path at the exact point where `windows_package_sid` is derived.

    STEP 1 — Create the registry at the appropriate call-chain entry point:
    Read the function that owns lines 482-488 in execution_runtime.rs (likely `run_windows_supervised` or similar). Determine where this function is called from in app_runtime.rs or command_runtime.rs. Create an `Arc<Mutex<nono::AgentRegistry>>` at the top of the highest-level dispatch function that is per-run (not process-global). Pass it down through function signatures as a separate parameter (per RESEARCH.md Open Question 3 recommendation — analogous to how `cap_file` is threaded). Do NOT use a global OnceLock for Phase 73 — the Arc<Mutex> approach is testable and avoids global state that the SC4 test cannot isolate.

    STEP 2 — Insert after windows_package_sid is derived (line 488 in execution_runtime.rs):
    Inside a #[cfg(target_os = "windows")] block, add:
      registry
          .lock()
          .map_err(|_| nono::NonoError::SandboxInit("AgentRegistry mutex poisoned".into()))?
          .insert(windows_package_sid.clone());
    This uses the `.map_err` pattern on PoisonError (required by clippy::unwrap_used — never .unwrap() on Mutex::lock()).

    STEP 3 — Update function signatures throughout the call chain to accept Arc<Mutex<nono::AgentRegistry>>. Thread it consistently. Import nono::{AgentRegistry} and std::sync::{Arc, Mutex} where needed.

    CONSTRAINT: The registry is initialized once per `nono run` invocation, not per-process. Phase 73's registry is per-run, in-memory, single-launch (per CONTEXT.md "Deferred Ideas" — persistence is Phase 74). The Arc is created fresh at dispatch time and dropped when the run completes.

    The #[cfg(target_os = "windows")] gate on the insert block is mandatory — non-Windows targets do not have windows_package_sid in scope. On non-Windows the registry insert is a no-op (no call site exists).

    NO .unwrap() or .expect() — use ? and map_err.
    DCO sign-off on commit.
  </action>
  <verify>
    <automated>cargo build -p nono-cli --target x86_64-pc-windows-msvc 2>&amp;1</automated>
  </verify>
  <done>
    - cargo build -p nono-cli --target x86_64-pc-windows-msvc succeeds
    - The execution_runtime.rs file contains `AgentRegistry` and `registry.lock().map_err` inside a #[cfg(target_os = "windows")] block
    - grep for ".unwrap()" in the new registry insert code returns 0 matches
    - The registry Arc is created per-run (not global), passed through function parameters
  </done>
</task>

<task type="auto">
  <name>Task 2: CLI verb — classify_runtime.rs, cli.rs, app_runtime.rs, main.rs</name>
  <files>
    crates/nono-cli/src/classify_runtime.rs,
    crates/nono-cli/src/cli.rs,
    crates/nono-cli/src/app_runtime.rs,
    crates/nono-cli/src/main.rs
  </files>
  <read_first>
    - crates/nono-cli/src/why_runtime.rs (read the full file — 235 lines — this is the EXACT analog for classify_runtime.rs structure)
    - crates/nono-cli/src/cli.rs (lines 725-743: Why variant; lines 3073-3088: InspectArgs — exact templates for ClassifyArgs and Commands::Classify)
    - crates/nono-cli/src/app_runtime.rs (lines 1-140: full dispatch_command function — understand how the registry Arc from Task 1 is accessible at dispatch time; understand run_command_with_update pattern)
    - crates/nono-cli/src/main.rs (lines 1-50: module declarations — find the correct alphabetical position for mod classify_runtime)
    - .planning/phases/73-ai-agent-marker/73-PATTERNS.md (all sections: classify_runtime.rs, cli.rs, app_runtime.rs, main.rs — exact code shapes)
    - .planning/phases/73-ai-agent-marker/73-RESEARCH.md (Example 3: classify output format; Architecture Patterns §Classification path)
    - .planning/phases/73-ai-agent-marker/73-CONTEXT.md (D-04: "best-effort nono classify <pid> CLI verb, documented as NON-authoritative")
  </read_first>
  <action>
    Create and modify four files to ship the `nono classify <pid>` CLI verb.

    FILE 1: crates/nono-cli/src/classify_runtime.rs (NEW file — mirror why_runtime.rs structure):
    Single public entry point: pub(crate) fn run_classify(args: ClassifyArgs, registry: std::sync::Arc<std::sync::Mutex<nono::AgentRegistry>>) -> nono::Result<()>
    Implementation:
      1. Lock registry with .lock().map_err(|_| nono::NonoError::SandboxInit("AgentRegistry mutex poisoned".into()))?
      2. Call locked_registry.classify(args.pid)
      3. Match on AgentClassification:
         AiAgent { package_sid } → println!("PID {}: AI_AGENT", args.pid) then println!("  Package SID: {}", package_sid) then a NOTE line
         NotAnAgent → println!("PID {}: not an agent", args.pid) then println!("  (NOTE: This check is structural only — not an authorization decision)")
      4. If args.json: serialize to JSON using serde_json (follow why_runtime.rs JSON output pattern — use .map_err for serde_json errors)
      5. Return Ok(())
    Non-authoritative disclaimer MUST appear on every output path (both AI_AGENT and not-an-agent output).
    Fail-secure: NotAnAgent is not a program error — return Ok(()) after printing.
    If OpenProcess fails (classify internally returns NotAnAgent on error): print the output as "not an agent" plus error context. Do NOT propagate the error as Err() — classification errors are expected outcomes, not program errors.

    FILE 2: crates/nono-cli/src/cli.rs (MODIFY — add ClassifyArgs struct + Commands::Classify variant):
    a) Add ClassifyArgs struct (follow InspectArgs at lines 3073-3088 as the exact template):
       #[derive(Parser, Debug)] pub struct ClassifyArgs { pub pid: u32, #[arg(long)] pub json: bool }
       Add appropriate #[command(after_help = "...")] with EXAMPLES block and NOTE that this check is structural / non-authoritative (per PATTERNS.md cli.rs section).
    b) Add Commands::Classify(ClassifyArgs) variant in the Commands enum (follow Why(Box<WhyArgs>) at lines 725-743 as template). ClassifyArgs is small enough to NOT require Box<> (like InspectArgs). Insert alphabetically or after Why.

    FILE 3: crates/nono-cli/src/app_runtime.rs (MODIFY — add Classify arm to dispatch_command):
    a) Add import: use crate::classify_runtime;
    b) In dispatch_command, add arm (after the Why arm, in alphabetical order or follow existing ordering):
       Commands::Classify(args) => run_command_with_update(update_handle, silent, || classify_runtime::run_classify(args, registry.clone()))
    Note: `registry` is the Arc<Mutex<AgentRegistry>> threaded from Task 1. If dispatch_command currently does not accept the registry as a parameter (because Task 1 threads it differently), adjust to match Task 1's threading approach exactly.

    FILE 4: crates/nono-cli/src/main.rs (MODIFY — add mod classify_runtime):
    Add `mod classify_runtime;` in the module declarations block (alphabetically with other *_runtime.rs modules). NOT cfg-gated (classify_runtime compiles on all platforms via AgentRegistry stub).

    Verify clap correctly parses `nono classify 1234` and `nono classify 1234 --json`. Use the exact arg name `pid` as a positional u32.
  </action>
  <verify>
    <automated>cargo build -p nono-cli --target x86_64-pc-windows-msvc 2>&amp;1</automated>
  </verify>
  <done>
    - cargo build -p nono-cli succeeds
    - nono classify --help shows the command with PID positional arg and --json flag
    - classify_runtime.rs exists with run_classify entry point
    - ClassifyArgs struct exists in cli.rs with pub pid: u32
    - Commands::Classify(ClassifyArgs) exists in the Commands enum
    - dispatch_command has a Classify arm routing to classify_runtime::run_classify
    - mod classify_runtime appears in main.rs
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SC4 in-process integration tests + SC5 documentation</name>
  <files>
    crates/nono-cli/src/classify_runtime.rs,
    proj/DESIGN-engine-abstraction.md
  </files>
  <read_first>
    - crates/nono-cli/src/exec_strategy_windows/launch.rs (lines 2880-2960: broker_dispatch_tests module — the #[ignore] real-child integration test pattern; read how it spawns a real confined child via BrokerLaunchNoPty, observes child PIDs, uses IsProcessInJob; this is the base for sc4_classify_real_agent)
    - .planning/phases/73-ai-agent-marker/73-RESEARCH.md (Pitfall 5: SC4 integration test needs real confined agent; §Validation Architecture: exact test names and commands)
    - .planning/phases/73-ai-agent-marker/73-VALIDATION.md (Per-Task Verification Map: sc4_classify_real_agent + sc4_classify_spoof_not_agent)
    - .planning/phases/73-ai-agent-marker/73-CONTEXT.md (D-04: SC4 proof; Claude's Discretion for SC5)
    - proj/DESIGN-engine-abstraction.md (read current content — the SC5 section will be appended)
    - .planning/phases/73-ai-agent-marker/73-PATTERNS.md (Shared Patterns: fail-secure default)
  </read_first>
  <behavior>
    - sc4_classify_real_agent: a real BrokerLaunchNoPty confined child → registry.classify(child_pid) == AiAgent { package_sid: _ }
    - sc4_classify_spoof_not_agent: a real AppContainer process spawned WITHOUT inserting into registry → registry.classify(spoof_pid) == NotAnAgent
    - Both tests are #[ignore] and require real Win11 host + dev-layout nono.exe
    - DESIGN-engine-abstraction.md contains a section on adopted agents as best-effort/demote-only
  </behavior>
  <action>
    PART A — Add SC4 integration tests to classify_runtime.rs (or a new inner test module there):

    Add a #[cfg(all(test, target_os = "windows"))] test module at the bottom of classify_runtime.rs. Tests:

    sc4_classify_real_agent (marked #[test] #[ignore]):
      Comment: "// requires: real Win11 host, dev-layout nono.exe at target/release/nono.exe or nono-shell-broker.exe"
      Procedure:
        1. Create a fresh AgentRegistry wrapped in Arc<Mutex<...>>
        2. Invoke BrokerLaunchNoPty path to spawn a real confined child (e.g., `cmd.exe /c exit 0` or `ping -n 1 127.0.0.1`). This requires building the same ExecConfig that execution_runtime.rs builds. If this is too complex in a unit test module, spawn a confined child using the same pattern as the existing broker_dispatch_tests in launch.rs (lines ~2880-2960). Capture the child PID.
        3. Before the child exits: call `registry.lock().unwrap().insert(windows_package_sid_str)` where windows_package_sid_str is the SID minted for that child's AppContainer profile (requires calling generate_app_container_name + derive_app_container_sid + package_sid_to_string as in execution_runtime.rs lines 482-488).
        4. Call `registry.lock().unwrap().classify(child_pid)`.
        5. Assert result matches AgentClassification::AiAgent { .. }.
        6. Terminate the child, wait for exit.
      If spawning a full BrokerLaunchNoPty child is too invasive in classify_runtime.rs tests, place this test in the launch.rs broker_dispatch_tests module instead (alongside the existing real-child tests there). Choose the location where the necessary helpers are already in scope.

    sc4_classify_spoof_not_agent (marked #[test] #[ignore]):
      Comment: "// requires: real Win11 host, CreateAppContainerProfile permission (user-space)"
      Procedure:
        1. Create a fresh AgentRegistry (empty — do NOT insert).
        2. Derive a package SID for a fresh random name (nono.session.<uuid>) via generate_app_container_name + derive_app_container_sid + package_sid_to_string. Call CreateAppContainerProfile with this name.
        3. Spawn a real process inside this AppContainer using CreateProcess with SECURITY_CAPABILITIES. Capture its PID.
        4. Call registry.classify(spoof_pid).
        5. Assert == NotAnAgent (registry is empty — spoof SID is not in it).
        6. Clean up the AppContainer profile (DeleteAppContainerProfile).

    Both tests: #[allow(clippy::unwrap_used)] in the test mod only.
    Both tests must have explicit // requires: comments so UAT operators know the preconditions.

    PART B — Append SC5 documentation to proj/DESIGN-engine-abstraction.md:
    Read the current content of proj/DESIGN-engine-abstraction.md first. Append a new section (at the end, before any existing "## References" section or at the very end):

    ## AI_AGENT Marker: Scope and Adopted-Agent Limits

    The `AI_AGENT` marker is sound **only for agents the launcher spawned**. An agent is marked by inserting its per-run AppContainer package SID (minted at `BrokerLaunchNoPty` spawn time) into the launcher's in-memory `AgentRegistry`. The registry is the authorization predicate.

    ### Adopted agents (not launched by this nono instance)

    An "adopted" agent is a process nono did not spawn — for example, a pre-existing running AI agent that an operator wants to retroactively confine or classify.

    **Current behavior (best-effort only):**
    - `nono classify <pid>` performs a structural pre-filter: it reads the PID's AppContainer package SID and checks whether it follows the `nono.session.<uuid>` naming convention and whether the process is in a job object.
    - The structural pre-filter is **non-authoritative**: a self-created AppContainer with a correctly-formatted name passes the structural filter but is NOT in the `AgentRegistry` and therefore classifies as `NotAnAgent`.
    - There is no sound way to adopt a running agent and retroactively grant it `AI_AGENT` status — doing so would require adding its SID to the registry after the fact, which creates a forgery window.

    **Demote-only model (per spike-002 findings):**
    Post-hoc token IL-drop (demote) is an incident-response lever, not a confinement model. Demote is a one-way operation: reducing IL does not retroactively confine all prior operations. Adopted-agent demote is implemented in Phase 75 (SUPP-01) as a supplementary control layered on a proven spawn-time default.

    **Recommendation:** To confine a new agent session, always launch it through `nono run` (or the `nono-py`/`nono-ts` bindings). Do not rely on post-hoc adoption as a primary security boundary.

    The above wording may be adjusted for tone by the executor within the intent of SC5 (Claude's Discretion per CONTEXT.md).
  </action>
  <verify>
    <automated>cargo test -p nono-cli --target x86_64-pc-windows-msvc classify_runtime::tests 2>&amp;1</automated>
  </verify>
  <done>
    - sc4_classify_real_agent and sc4_classify_spoof_not_agent exist (either in classify_runtime.rs or launch.rs broker_dispatch_tests) and are marked #[ignore] with // requires: comments
    - cargo test -p nono-cli --target x86_64-pc-windows-msvc (without --ignored) passes (the #[ignore] tests are skipped by default)
    - proj/DESIGN-engine-abstraction.md contains a section on adopted agents mentioning "best-effort", "non-authoritative", "demote-only", and the AgentRegistry as the authoritative predicate
    - make test passes (all non-ignored tests green)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The complete Phase 73 AI_AGENT Marker feature:
    1. nono::AgentRegistry + AgentClassification + read_process_appcontainer_sid (Plan 01)
    2. Job object SDDL hardening + breakaway negative tests (Plan 02)
    3. Mint→registry wiring in execution_runtime.rs (Plan 03 Task 1)
    4. nono classify CLI verb (Plan 03 Task 2)
    5. SC4 in-process integration tests + SC5 docs (Plan 03 Task 3)
  </what-built>
  <how-to-verify>
    Automated pre-checks (run these first):
    a. `cargo test -p nono -p nono-cli --target x86_64-pc-windows-msvc 2>&1` — all non-ignored tests pass
    b. `cargo clippy -p nono -p nono-cli --target x86_64-pc-windows-msvc -- -D warnings -D clippy::unwrap_used 2>&1` — zero errors
    c. If cross-toolchain available: `cargo clippy -p nono --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used 2>&1` — zero errors

    Manual Win11 UAT (requires real Win11 host, dev-layout nono.exe from target/release/):
    Step 1: Launch a confined agent:
      target\release\nono.exe run --profile claude-code -- cmd /c "ping -n 3 127.0.0.1"
      (or any profiled engine from Phase 71 — e.g., aider)
      Note the child PID from the supervisor output or Task Manager.

    Step 2: In a second terminal, while the agent is running:
      target\release\nono.exe classify <child-pid>
      EXPECTED: "PID <child-pid>: AI_AGENT" + Package SID line + non-authoritative NOTE

    Step 3: On an unrelated process (e.g., cmd.exe PID from Task Manager):
      target\release\nono.exe classify <unrelated-pid>
      EXPECTED: "PID <unrelated-pid>: not an agent" + "(NOTE: This check is structural only...)"

    Step 4: On the current terminal PID:
      target\release\nono.exe classify <current-powershell-pid>
      EXPECTED: "not an agent"

    Step 5: Run SC4 integration tests manually (real Win11, dev-layout):
      cargo test -p nono-cli --target x86_64-pc-windows-msvc -- --ignored sc4_classify_real_agent
      cargo test -p nono-cli --target x86_64-pc-windows-msvc -- --ignored sc4_classify_spoof_not_agent
      EXPECTED: both pass

    Step 6: Check DESIGN-engine-abstraction.md contains the adopted-agent section:
      grep -c "adopted" proj/DESIGN-engine-abstraction.md
      EXPECTED: >= 3

    Step 7: Verify job SD hardening:
      cargo test -p nono-cli --target x86_64-pc-windows-msvc launch::tests::job_never_has_breakaway_ok
      cargo test -p nono-cli --target x86_64-pc-windows-msvc launch::tests::job_security_descriptor_denies_low_il
      EXPECTED: both pass
  </how-to-verify>
  <resume-signal>Type "approved" if all automated checks pass and Win11 UAT confirms AI_AGENT for a live confined agent. Type "issues: <description>" if any check fails.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → nono classify <pid> | PID is operator-supplied; must not be trusted as proof of agent identity |
| execution_runtime.rs (launcher) → AgentRegistry | The registry insert is in the trusted launcher; only the launcher inserts — the agent process never touches the registry |
| classify_runtime.rs output → operator | Output is labeled non-authoritative; the operator must not use CLI output as an authorization decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-11 | Spoofing | Operator passes a spoofed PID to nono classify | accept | classify reads the real kernel token for the supplied PID; a caller cannot lie about PID-to-token binding. If the PID resolves to a different process than intended (PID reuse race), the result is a false-positive-safe NotAnAgent. |
| T-73-12 | Spoofing | Output of nono classify used as authorization decision | mitigate | Non-authoritative disclaimer is emitted on EVERY output path; CLI docs state this is NON-authoritative; the registry check is the only sound authz boundary |
| T-73-13 | Tampering | Agent modifying AgentRegistry (in-process) | accept | AgentRegistry lives in the launcher process; the confined agent runs in a separate Low-IL AppContainer child process and cannot access the launcher's memory |
| T-73-14 | Elevation of Privilege | Cross-run registry pollution (stale SIDs from prior runs) | accept (by design) | Registry is per-run in-memory (CONTEXT.md Deferred: persistence is Phase 74). The registry is created fresh per nono run invocation and dropped at exit. No cross-run state. |
| T-73-15 | Denial of Service | AgentRegistry mutex poisoning in classify path | mitigate | PoisonError propagates as Err(NonoError::SandboxInit(...)) via .map_err; classify_runtime returns a non-zero exit code so the operator knows the classify call failed — never silently succeeds |
| T-73-SC | Tampering | npm/pip/cargo installs | accept | No new external package dependencies — zero new crate additions |
</threat_model>

<verification>
Phase-level verification (run before /gsd:verify-work):
1. `make test 2>&1` — all non-ignored tests green
2. `cargo clippy -p nono -p nono-cli --target x86_64-pc-windows-msvc -- -D warnings -D clippy::unwrap_used 2>&1` — zero errors
3. Cross-target: `cargo clippy -p nono --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used 2>&1` (PARTIAL if toolchain missing — defer to CI per cross-target-verify-checklist.md)
4. Real Win11 UAT: `nono classify <confined-pid>` outputs `AI_AGENT` with package SID; unrelated PID outputs "not an agent"
5. SC4 integration tests pass with `-- --ignored` on real Win11 host
6. `grep "adopted" proj/DESIGN-engine-abstraction.md` returns ≥ 3 matches
7. `grep "AgentRegistry" crates/nono-cli/src/execution_runtime.rs` returns ≥ 1 match in a #[cfg(target_os = "windows")] block
8. `grep "BREAKAWAY_OK" crates/nono-cli/src/exec_strategy_windows/launch.rs` returns ≥ 1 match (the negative test)
</verification>

<success_criteria>
SC1: A launched agent's package SID is inserted into AgentRegistry at spawn time in the shipping execution path (execution_runtime.rs).
SC2: A non-agent PID (not in registry) classifies as NotAnAgent — proven by unit tests and SC4 spoof integration test.
SC3: JOB_OBJECT_LIMIT_BREAKAWAY_OK is never set (negative test passes); job SD denies Low-IL (negative test passes).
SC4: In-process integration test proves real confined child → AI_AGENT, spoof AppContainer → NotAnAgent (real Win11 UAT).
SC5: proj/DESIGN-engine-abstraction.md documents adopted agents as best-effort/demote-only with the non-authoritative disclaimer.

make test green. nono classify works from the command line. All DCO sign-offs present.
</success_criteria>

<output>
Create `.planning/phases/73-ai-agent-marker/73-03-SUMMARY.md` when done.
</output>
