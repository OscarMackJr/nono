---
phase: 85-upst9-divergence-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02

must_haves:
  truths:
    - "85-DIVERGENCE-LEDGER.md exists in the phase directory with valid YAML frontmatter (13 keys including upstream_head_at_audit, refetch_date, total_unique_commits: 35, and drift_tool_sh_sha)"
    - "The ledger's Cluster Summary table has exactly 14 rows: clusters A through M plus a release/dep-bumps row, each with a disposition from the set {will-sync, fork-preserve, split, won't-sync}"
    - "Clusters A and B are dispositioned will-sync (D-03 locked); cluster C is dispositioned will-sync; clusters F and M are dispositioned split"
    - "Every one of the 35 substantive commits from the drift tool output appears in exactly one cluster's per-commit table OR in the noise section"
    - "Clusters A, B, and F each contain an actual-diff re-export check subsection citing git show output (not --name-only alone)"
    - "The ADR review section contains a 5-row table (security, windows, maintenance, divergence, contributor) with L/M/H verdicts"
    - "The Excluded as noise section documents 55 commits with the filter criteria and at least 23 merge SHAs enumerated"
    - "Cluster M's per-commit inventory for e54cf9cb is annotated: apply to hook_runtime.rs (Unix path only); hook_runtime_windows.rs retains env_clear per windows_hook_interpreter_spawn_gotchas"
    - "Theme B's windows-touch field is yes; cluster C's cross-target clippy note records: cfg(linux) edits require cross-target clippy in Phase 87"
    - "The D-07 tip-check result is recorded: re-fetch date, upstream_head_at_audit SHA, and result (no v0.65.0, window unchanged)"
  artifacts:
    - path: ".planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md"
      provides: "Complete UPST9 divergence ledger gating Phases 86-89 cherry-pick decisions"
      min_lines: 400
      contains: "## Cluster Summary"
  key_links:
    - from: "85-DIVERGENCE-LEDGER.md § Cluster A"
      to: "Phase 86 BND-01 (audit→core cherry-pick)"
      via: "will-sync disposition + per-commit inventory of a5b2a516, aed35bec, 0b27cfc2, e9529312"
      pattern: "will-sync"
    - from: "85-DIVERGENCE-LEDGER.md § Cluster B"
      to: "Phase 86 BND-02 (diagnostics→core+FFI cherry-pick)"
      via: "will-sync disposition + cross-cluster re-export check on 4ad8ba92, a6aa5995, f867aba2"
      pattern: "will-sync"
    - from: "85-DIVERGENCE-LEDGER.md § Cluster F"
      to: "Phase 89 PROXY-01/02 (proxy hardening)"
      via: "split disposition + TLS-intercept assessment section (Phase 34 C11 fork-preserve)"
      pattern: "split"
---

<objective>
Produce the complete UPST9 `85-DIVERGENCE-LEDGER.md` for upstream `always-further/nono`
`v0.62.0..v0.64.0` (SHA range `52809dda..01537570`), classifying all 35 substantive
commits into themed clusters A–M with dispositions, ADR risk verdicts, actual-diff
re-export inspection on clusters A/B/F, and a verifiable noise-exclusion section.

Purpose: This ledger is the gating input for all downstream cherry-pick phases (86→A,B /
87→C / 88→D,E,G,H,I,J,K,L,M+bumps / 89→F). Its cluster IDs, disposition vocabulary, and
downstream-phase pointers are the literal contracts those phases read.

Output: `.planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md`
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\OMack\Nono\.planning\PROJECT.md
@C:\Users\OMack\Nono\.planning\ROADMAP.md
@C:\Users\OMack\Nono\.planning\STATE.md
@C:\Users\OMack\Nono\.planning\phases\85-upst9-divergence-audit\85-RESEARCH.md
@C:\Users\OMack\Nono\.planning\seeds\SEED-006-upst9-v0.62-v0.64-sync-window.md
@C:\Users\OMack\Nono\.planning\phases\85-upst9-divergence-audit\85-CONTEXT.md

<interfaces>
<!-- Key facts the executor needs without codebase exploration. All VERIFIED at research time. -->

SHA COLLISION GUARD (CRITICAL):
  - Local fork tag v0.62.0 = 3c5e9025 (WRONG — fork release, divergent history)
  - Upstream v0.62.0     = 52809dda (CORRECT — always use this SHA)
  - Upstream v0.64.0     = 01537570 (always use this SHA; do NOT use tag name)
  - NEVER use --from v0.62.0 or --to v0.64.0 (tag name collision)

WINDOW AT RESEARCH TIME (D-07):
  - upstream/main = 0153757001d21805a8218213e32add462d3322a1 = v0.64.0
  - git ls-remote upstream 'refs/tags/v0.65*' returned empty → D-07 window extension does NOT fire
  - Re-fetch at execution open is still required; record result in frontmatter regardless

COMMIT COUNTS:
  - Total in window: ~90 (includes merges + docs + CI)
  - Merge commits: ~23
  - Non-merge: ~67
  - Substantive (drift-tool path filter): 35

DRIFT TOOL:
  - Script: scripts/check-upstream-drift.sh (PowerShell twin: check-upstream-drift.ps1)
  - SHA pin to assert: 0834aa664fbaf4c5e41af5debece292992211559
  - Invocation: bash scripts/check-upstream-drift.sh --from 52809dda --to 01537570 --format json
  - Output dir: ci-logs-local/drift/ (gitignored)

LEDGER FILE: .planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md
(Prior ledgers: 54-DIVERGENCE-LEDGER.md at .planning/phases/54-upst7-audit/,
               69-DIVERGENCE-LEDGER.md at .planning/phases/69-upst8-audit/)

35 SUBSTANTIVE COMMITS (authoritative from research — drift tool confirmed):
  01537570  chore: release v0.64.0 (#1201)
  bd4b6b7f  refactor(proxy): separate proxy intent from activation (#1199)
  4179ce03  fix(pty): ctrl-z hangs when running with a PTY (#1135)
  a6aa5995  feat(diagnostics): expose structured diagnostics for library and FFI clients (#1171)
  cc11b389  feat(update-check): discover ci environments on update (#1113)
  724bb207  fix: proxy should activate with customCredentials set (#1197)
  76b7b695  Refactor forward_inner_request (#1192)
  5bb098cd  feat: [aws] implement aws_auth config (#1166)
  8e0d94f9  fix(cli): use XDG config paths consistently (#1179)
  a0bba5eb  feat(output): show blocked macos grants in capability summary (#1178)
  0b27cfc2  refactor(audit): move attestation logic to core library (#1148)
  b0b2c743  fix(proxy): stop allow_domain endpoint route from shadowing credential catch-all (#1132)
  b5f8db5c  fix(proxy): respect upstream_proxy in TLS CONNECT intercept path (#1048) (#1091)
  7f319b9e  fix(diagnostic): replace deprecated nono learn with nono run (#1170)
  a5d623fd  fix(proxy): return 403 + audit for denied non-CONNECT requests (#1077)
  084638b7  chore: release v0.63.0 (#1161)
  4ad8ba92  refactor(diagnostic): move diagnostic UX out of core nono crate (#1155)
  e8293b36  feat(cli): move runtime state to XDG state dirs (#1152)
  7c9abd3b  fix(proxy): keep connection open for reactive proxy auth on CONNECT (#1151)
  6d452b00  chore(deps): bump typify from 0.6.2 to 0.7.0 (#1156)
  f867aba2  fix: report the actual blocked operation instead of the readable target path (#1150)
  e2086877  fix(linux): trap sendto/sendmsg to prevent AF_UNIX datagram bypass (#1096)
  42e5bf73  fix(cli): accept truthy env values for bool flags (#1136)
  7d274cf7  feat: add $PACK_DIR support to session_hooks for store pack support (#1073)
  7e076d2d  refactor(pull_ui): remove sigstore provenance display (#1144)
  c6b13345  feat(keyring): add NONO_KEYRING_TIMEOUT_SECS for keychain access (#977)
  d48aeb7b  feat(environment): add set_vars for static env injection (#1134)
  e9529312  fix(audit): address ledger review and clippy
  6d88638e  refactor(profiles): standardize profile names with namespace
  ee7a3bda  fix: replace stale nono.dev schema domains with nono.sh
  aed35bec  refactor(audit-ledger): move audit ledger logic to library crate
  a5b2a516  refactor(audit): move audit integrity logic to nono crate
  6b3eb013  fix: guard deduplicate() against inheriting procfs-remap originals (#1064)
  9800f307  feat(pack-verification): skip pack verification on dry runs
  e54cf9cb  Remove env_clear from the session_hook subprocess

CLUSTER DRAFT MAP (from research; executor validates and may adjust):
  A (Audit→core):            a5b2a516, aed35bec, 0b27cfc2, e9529312 → will-sync (D-03 locked)
  B (Diagnostics→core+FFI):  4ad8ba92, a6aa5995, f867aba2, 7f319b9e → will-sync (D-03 locked)
  C (AF_UNIX+procfs):        e2086877, 6b3eb013 → lean will-sync (D-04; validate by diff)
  D (set_vars):              d48aeb7b → lean will-sync
  E (XDG state dirs):        e8293b36, 8e0d94f9 → lean will-sync + Windows-path note
  F (Proxy hardening):       b0b2c743, a5d623fd, b5f8db5c, 7c9abd3b, 76b7b695, bd4b6b7f, 724bb207 → lean split
  G (AWS auth):              5bb098cd → lean will-sync
  H (Keyring timeout):       c6b13345 → lean will-sync
  I ($PACK_DIR hooks):       7d274cf7 → lean will-sync
  J (PTY ctrl-z):            4179ce03 → lean will-sync
  K (update-check CI):       cc11b389 → lean will-sync
  L (Profile namespace):     6d88638e → lean will-sync
  M (Misc fixes):            42e5bf73, a0bba5eb, ee7a3bda, 7e076d2d, 9800f307, e54cf9cb → lean split
  Release/dep-bumps:         084638b7, 01537570, 6d452b00 → won't-sync

NOTE on 7f319b9e: NOT in SEED-006; in drift tool output. Inspect git show 7f319b9e --name-only.
  If touches crates/nono-cli/src/ diagnostic surfaces → fold into B with inline note.
  If standalone CLI diagnostic string change → fold into M.
  Either way: classify it; do NOT silently drop it (AUDIT-01 requires every substantive SHA classified).

NOTE on 5413a0b3 (go_runtime policy.json): in SEED-006 theme M but NOT in drift output (touches
  crates/nono-cli/data/policy.json only, outside drift tool path filter) → noise section.

FORK STATE (VERIFIED at research time):
  - crates/nono/src/audit.rs: DOES NOT EXIST (audit is CLI-side in 4 files)
  - crates/nono/src/diagnostic.rs: EXISTS (DiagnosticFormatter, DenialRecord, etc.)
  - crates/nono-proxy/src/diagnostic.rs: DOES NOT EXIST
  - bindings/c/src/diagnostic.rs: DOES NOT EXIST
  - crates/nono-cli/src/state_paths.rs: DOES NOT EXIST
  - crates/nono-cli/src/hook_runtime.rs: EXISTS, has env_clear at line ~196

THEME M / e54cf9cb ANNOTATION (CRITICAL per D-02 + windows_hook_interpreter_spawn_gotchas):
  "apply to hook_runtime.rs (Unix path only); hook_runtime_windows.rs RETAINS env_clear() +
  SystemRoot/windir/SystemDrive baseline restore to prevent CLR init failure (0xFFFF0000 /
  -65536) — windows_hook_interpreter_spawn_gotchas"

THEME C CROSS-TARGET NOTE (for cluster C risk verdict):
  "e2086877 and 6b3eb013 touch cfg(linux) blocks; Phase 87 executor MUST run
  cargo clippy --workspace --target x86_64-unknown-linux-gnu AND --target x86_64-apple-darwin;
  Windows-host cargo check is not accepted as substitute (CLAUDE.md MUST/NEVER rule)"

DIFF-INSPECTION TARGETS (D-05 / AUDIT-02):
  Full actual-diff (git show <sha>) required for: A, B, F clusters
  --name-only sufficient for: C, D, E, G, H, I, J, K, L, M, release/dep-bumps
  Per-cluster, ledger must state which inspection depth was applied.

ADR RISK PRE-SCORED LEANINGS (5 dimensions; validate by diff — D-04):
  A: security=H windows=L maintenance=H divergence=H contributor=H → overall H
  B: security=M windows=H maintenance=H divergence=H contributor=H → overall H
  C: security=H windows=L maintenance=L divergence=L contributor=L → overall H (security dominant)
  D: all=L → overall L
  E: security=L windows=M maintenance=M divergence=L contributor=L → overall M
  F: security=M windows=L maintenance=M divergence=M contributor=M → overall M
  G/H/I/J/K/L: all=L → overall L
  M: security=L windows=M maintenance=L divergence=L contributor=L → overall M (env_clear collision)

LEDGER SECTION ORDER (matches 54-DIVERGENCE-LEDGER.md precedent):
  1. YAML frontmatter (13 keys)
  2. ## Headline
  3. ## Reproduction
  4. ## Cluster Summary (table)
  5. ### Cluster A: ... through ### Cluster M: ... + Release/dep-bumps row
  6. ## ADR review
  7. ## Empirical cross-check
  8. ## Cross-cluster re-export deps detected
  9. ## Excluded as noise
  10. ## TLS-intercept assessment (for cluster F, per UPST7 precedent)

PER-COMMIT TABLE COLUMNS:
  | sha | subject | upstream-tag | categories | files-changed | windows-touch |
  sha = 8-char abbreviated; upstream-tag = v0.63.0 or v0.64.0; categories = drift tool output;
  files-changed = from git show <sha> --stat | tail -1; windows-touch = yes/no per *_windows.rs check
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate commits, classify into clusters A–M, write per-cluster sections and noise section</name>
  <files>.planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md</files>
  <read_first>
    - .planning/phases/85-upst9-divergence-audit/85-RESEARCH.md (full file — commit list, noise SHAs, cluster draft map, ledger format, section order, per-commit table schema, diff-inspection pitfalls)
    - .planning/seeds/SEED-006-upst9-v0.62-v0.64-sync-window.md (theme A–M decomposition, function inventory per cluster, fork-conflict notes)
    - .planning/phases/85-upst9-divergence-audit/85-CONTEXT.md (locked decisions D-01 through D-07)
    - CLAUDE.md § Library vs CLI Boundary (the policy-free-library invariant that A & B deliberately change)
  </read_first>
  <action>
Step 1 — Re-fetch upstream and record the tip-check result (D-07):
  Run: git fetch upstream
  Run: git rev-parse upstream/main   (record the SHA)
  Run: git ls-remote upstream 'refs/tags/v0.65*'   (check for window extension)
  If no v0.65.0: window stays 52809dda..01537570. Record refetch_date (today's date) and upstream_head_at_audit SHA in ledger frontmatter.
  If v0.65.0 exists: extend window; note in range_note frontmatter key.

Step 2 — Assert SHA collision guard:
  Run: git cat-file -t 52809dda   (expect: commit)
  Run: git cat-file -t 01537570   (expect: commit)
  Run: git rev-parse v0.64.0 2>/dev/null  (check if local fork tag exists; if so, confirm it differs from 01537570)
  Assert drift tool sha: git log -1 --format=%H -- scripts/check-upstream-drift.sh  (expect: 0834aa664fbaf4c5e41af5debece292992211559)

Step 3 — Run the drift tool:
  Run: bash scripts/check-upstream-drift.sh --from 52809dda --to 01537570 --format json > ci-logs-local/drift/$(date -u +%Y%m%dT%H%M%SZ)-v062-v064-upst9.json
  Or via PowerShell twin if bash unavailable: pwsh -File scripts/check-upstream-drift.ps1 -From 52809dda -To 01537570 -Format json
  Verify output count = 35 substantive commits. If count differs from 35, reconcile before proceeding.

Step 4 — Validate total commit counts (for noise section):
  git log --oneline 52809dda..01537570 | wc -l   (expect ~90)
  git log --merges --oneline 52809dda..01537570 | wc -l   (expect ~23)

Step 5 — Cross-check 7f319b9e:
  Run: git show 7f319b9e --name-only | head -20
  If touches crates/nono-cli/src/ diagnostic surfaces → fold into cluster B with note "not in SEED-006; folded into B (diagnostic CLI surface)".
  If standalone CLI diagnostic string → fold into cluster M with note "not in SEED-006; folded into M (misc CLI fix)".

Step 6 — For each cluster, gather per-commit stats:
  For every SHA in the cluster draft map, run: git show <sha> --stat | tail -1
  Record files-changed count. Check if commit touches *_windows.rs or exec_strategy_windows/ to determine windows-touch.
  For upstream-tag: commits in 52809dda..084638b7 range = v0.63.0; commits in 084638b7..01537570 range = v0.64.0.

Step 7 — Write the ledger file using the Write tool with:
  a) YAML frontmatter (all 13 keys; see <interfaces> for schema)
  b) ## Headline — paragraph: 35 substantive commits, 13 active clusters (A–M excluding release/dep-bumps, or 14 rows including them), disposition breakdown (will-sync N / fork-preserve N / split N / won't-sync N), windows-touch clusters, re-export scan deferred to Task 2, ADR review deferred to Task 2
  c) ## Reproduction — verbatim invocation with explicit SHAs, JSON output path, upstream_head_at_audit, refetch_date, drift_tool_sh_sha, total_unique_commits: 35, SHA collision guard note, auditor-rerun instructions
  d) ## Cluster Summary — table: | cluster_id | theme | commits | disposition | windows-touch | rationale |  (one row per cluster A–M + release/dep-bumps)
  e) Per-cluster sections ### Cluster A through ### Cluster M and ### Release/Dep-Bumps, each containing:
     - **Commits:** N — comma-separated 8-char SHAs with subjects
     - **Disposition:** (per locked decisions and D-04 leanings)
     - **Windows-touch:** yes/no
     - **Rationale:** brief narrative referencing SEED-006 + fork-state grounding
     - **Cross-cluster re-export check:** "Deferred to Task 2 — actual-diff inspection pending" (for A, B, F); "--name-only inspection: [result]" (for others)
     - Per-commit table (all columns)
     - For cluster M, commit e54cf9cb row MUST include the annotation: "PARTIAL-SYNC: apply to hook_runtime.rs (Unix path only); hook_runtime_windows.rs retains env_clear() + SystemRoot/windir/SystemDrive baseline restore per windows_hook_interpreter_spawn_gotchas"
     - For cluster C, include note: "Phase 87 executor must run cross-target clippy on cfg(linux) edits (CLAUDE.md MUST/NEVER rule)"
  f) ## Excluded as noise — with filter criteria, counts (55 total = 23 merges + 32 out-of-filter non-merge), and SHA lists from 85-RESEARCH.md Research Priority 3. Include the note on 5413a0b3 (in SEED-006 M but noise per drift tool path filter) and the dep-bump SHAs (absorbed in Phase 88 DEPS-02).

Disposition assignments (implement exactly — D-03 locks A & B; D-04 leanings confirmed by diff for others):
  A → will-sync (D-03 locked; adopt-upstream boundary change; Phase 86)
  B → will-sync (D-03 locked; adopt-upstream; Phase 86)
  C → will-sync (D-04 lean; Linux security fix; Phase 87)
  D → will-sync (D-04 lean; additive, no conflict; Phase 88)
  E → will-sync (D-04 lean; Windows-path reconciliation note required; Phase 88)
  F → split (D-04 lean; additive route/403/reactive-auth bits sync; tls_intercept/ bits differ — fork has no tls_intercept/ dir; Phase 89)
  G → will-sync (D-04 lean; additive; Phase 88)
  H → will-sync (D-04 lean; additive; Phase 88)
  I → will-sync (D-04 lean; additive; Phase 88)
  J → will-sync (additive; Phase 88)
  K → will-sync (D-04 lean; additive; Phase 88)
  L → will-sync (D-04 lean; additive; Phase 88)
  M → split (D-04 lean; e54cf9cb env_clear Unix-only; other misc additive; Phase 88)
  Release/dep-bumps (084638b7, 01537570, 6d452b00) → won't-sync (version management; dep versions absorbed in Phase 88 DEPS-02)

If any diff inspection during Step 6 contradicts a D-04 lean, record the actual finding and adjust the disposition. The pre-lean is a start, not a constraint (only A & B are D-03 locked).
  </action>
  <verify>
    <automated>grep -c "### Cluster" "C:\Users\OMack\Nono\.planning\phases\85-upst9-divergence-audit\85-DIVERGENCE-LEDGER.md"</automated>
    The count must be at least 13 (clusters A through M; 14 if release/dep-bumps gets its own section header).

    Check every substantive SHA appears:
    For each of the 35 SHAs in the commit list above, run:
    grep -c "&lt;8-char-sha&gt;" ".planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md"
    Each must return >= 1. Any SHA with 0 matches is a gap (AUDIT-01 violation).

    Check D-03 locks honored:
    grep -A2 "### Cluster A" ".planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md" | grep "will-sync"
    grep -A2 "### Cluster B" ".planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md" | grep "will-sync"
    Both must match.

    Check noise section exists:
    grep -c "Excluded as noise" ".planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md"
    Must be >= 1.

    Check env_clear annotation:
    grep -c "hook_runtime_windows" ".planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md"
    Must be >= 1.
  </verify>
  <acceptance_criteria>
    - 85-DIVERGENCE-LEDGER.md exists at .planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md with valid YAML frontmatter including keys: phase, plan, ledger_type, range, upstream_head_at_audit, refetch_date, drift_tool_sh_sha, fork_baseline, total_unique_commits, date
    - total_unique_commits in frontmatter is 35
    - ## Cluster Summary table is present with rows for all clusters A through M (plus release/dep-bumps)
    - Each of the 35 substantive SHAs from the research commit list appears in at least one cluster's per-commit table (grep each 8-char SHA; count >= 1 each)
    - 7f319b9e is classified (either in cluster B or M) with an inline note "not in SEED-006"
    - Cluster A disposition is "will-sync"; Cluster B disposition is "will-sync" (D-03 locked)
    - Cluster F disposition is "split" with a rationale referencing tls_intercept/ absence in fork
    - Cluster M disposition is "split" with e54cf9cb annotated for Unix-path-only application
    - windows-touch for cluster B is "yes"; windows-touch for clusters A, C, D, G, H, I, J, K is "no"
    - ## Excluded as noise section exists and states "55" commits with "23 merge" sub-count
    - Cluster C includes a note that Phase 87 must run cross-target clippy on cfg(linux) edits
    - The D-07 re-fetch result is recorded (upstream_head_at_audit SHA + refetch_date in frontmatter; no v0.65.0 paragraph in ## Reproduction)
    - Cross-cluster re-export check field for clusters A, B, F says "Deferred to Task 2" or equivalent placeholder (actual-diff content from Task 2 will fill this in)
  </acceptance_criteria>
  <done>85-DIVERGENCE-LEDGER.md contains all 13+ cluster sections, the Cluster Summary table, the Excluded as noise section, and all 35 substantive SHAs are classified. Cluster A and B are will-sync. Cluster F and M are split. e54cf9cb is annotated Unix-only. Noise section states 55 / 23 merges.</done>
</task>

<task type="auto">
  <name>Task 2: Actual-diff re-export inspection (clusters A/B/F), ADR review, empirical cross-check, finalize ledger</name>
  <files>.planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md</files>
  <read_first>
    - .planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md (the draft from Task 1 — read the current state of each cluster section to know what to fill in)
    - .planning/phases/85-upst9-divergence-audit/85-RESEARCH.md § Research Priority 4 (diff-inspection commands, re-export hazard, cluster F TLS context, pitfall 3 on Theme B FFI)
    - .planning/phases/85-upst9-divergence-audit/85-RESEARCH.md § Research Priority 6 (five-dimension ADR risk rubric + pre-scored leanings table)
  </read_first>
  <action>
Step 1 — Actual-diff re-export inspection for Cluster A (commits a5b2a516, aed35bec, 0b27cfc2, e9529312):
  For each SHA, run: git show &lt;sha&gt; | grep '^+' | grep -E '^\+\s*(pub use|pub mod|extern crate|pub\(crate\))'
  Record finding per commit: "Clean — no cross-cluster re-export dep" OR "Detected: &lt;symbol&gt; from &lt;module&gt; ..."
  If any new pub use/pub mod in crates/nono/src/audit.rs adds cross-cluster symbols from B or other clusters, record as a prerequisite dependency.
  Also check: git show &lt;sha&gt; -- crates/nono/src/lib.rs   (does lib.rs gain a new pub use audit:: re-export line?)

Step 2 — Actual-diff re-export inspection for Cluster B (commits 4ad8ba92, a6aa5995, f867aba2, 7f319b9e if in B):
  For each SHA:
    git show &lt;sha&gt; | grep '^+' | grep -E '^\+\s*(pub use|pub mod|extern crate|pub\(crate\))'
  CRITICAL (Pitfall 3 from research): also inspect bindings/c/ explicitly:
    git show a6aa5995 -- bindings/c/src/
  Check if lib.rs adds 'last_diagnostic_code'/'last_remediation_json' extern "C" fns that reference the new diagnostic module types.
  Check error.rs for new NonoError::{diagnostic_code, remediation} fields — these cross-link error.rs (core) to diagnostic/ module.
  Record: new pub extern "C" functions, new type re-exports in lib.rs, new fields in NonoError.
  If any finding creates a prerequisite order for Phase 86 cherry-pick (e.g. "diagnostic module must land before error.rs changes"), record as ordering constraint.

Step 3 — Actual-diff re-export inspection for Cluster F (commits b0b2c743, a5d623fd, b5f8db5c, 7c9abd3b, 76b7b695, bd4b6b7f, 724bb207):
  For each SHA:
    git show &lt;sha&gt; --name-only   (which files touched?)
    For any file under tls_intercept/: note "touches tls_intercept/ (absent in fork — won't-apply)"
    For any file under server.rs, route.rs, credential.rs, connect.rs: note "shared surface — diff-inspect for conflict"
    git show &lt;sha&gt; | grep '^+' | grep -E '^\+\s*(pub use|pub mod|extern crate)'
  For b5f8db5c and 76b7b695 (tls_intercept/ changes): determine which lines touch fork-shared files vs tls_intercept/-only files. This defines which parts of the split sync vs don't sync.
  For 724bb207 (customCredentials / credential.rs): inspect conflict level against fork's Windows credential-injection rewrite. Does it touch the fork-divergent Windows credential path?
  Write the ## TLS-intercept assessment section with verdict: which F commits are "sync-safe" (server.rs additive) vs "requires reconciliation" (tls_intercept/) vs "fork-conflict-risk" (credential.rs Windows surface).

Step 4 — Update cluster sections in 85-DIVERGENCE-LEDGER.md:
  Replace the "Deferred to Task 2 — actual-diff inspection pending" placeholder in each of A, B, F's **Cross-cluster re-export check:** fields with the actual findings from Steps 1–3.
  Use the Edit tool for targeted replacements (do not rewrite the entire ledger; preserve Task 1 content).

Step 5 — Write the ## ADR review section:
  Per-cluster ADR table (one table per cluster, or a combined master table):
  | dimension | score | notes |
  |-----------|-------|-------|
  | security  | H/M/L | &lt;rationale&gt; |
  | windows   | H/M/L | &lt;rationale&gt; |
  | maintenance | H/M/L | &lt;rationale&gt; |
  | divergence | H/M/L | &lt;rationale&gt; |
  | contributor | H/M/L | &lt;rationale&gt; |

  Use the pre-scored leanings from 85-RESEARCH.md Research Priority 6, updated by the actual-diff findings from Steps 1–3.
  Clusters A and B should both have H overall verdicts (boundary-convergence, FFI-touching, high merge risk).
  Cluster C should have security=H overall (AF_UNIX bypass is a real security fix).
  Outcome line: "Confirm. proceed with drain-then-sync shape; Phase 86 ADR required for A/B boundary-convergence rationale."

Step 6 — Write the ## Empirical cross-check section:
  Spot-check ≥4 files with git log walk to confirm they appear in the correct cluster (not silently miscategorized).
  Suggested checks:
    git log --oneline 52809dda..01537570 -- crates/nono/src/audit.rs     (expect: A commits only)
    git log --oneline 52809dda..01537570 -- crates/nono/src/diagnostic/  (expect: B commits only)
    git log --oneline 52809dda..01537570 -- crates/nono/src/sandbox/linux.rs  (expect: C commits only)
    git log --oneline 52809dda..01537570 -- crates/nono-proxy/src/route.rs    (expect: F commits)
  Record each result. Any file showing commits NOT in the expected cluster is a classification error — fix the per-commit table.

Step 7 — Write the ## Cross-cluster re-export deps detected section:
  Summarize findings from Steps 1–3.
  If clean (no pub-use cross-cluster deps found in A or B): "Re-export scan clean — no cross-cluster pub-use/pub-mod additions detected in clusters A or B."
  If any found: list them with the prerequisite ordering implication for Phase 86.
  For F: summarize the tls_intercept/ vs server.rs split finding.

Step 8 — Update ## Headline:
  Replace the placeholder paragraph from Task 1 with the complete summary:
  "35 substantive commits, 14 ledger rows (clusters A–M + release/dep-bumps). Dispositions: will-sync N / split 2 (F, M) / won't-sync 1 (release/dep-bumps). Windows-touch clusters: B (diagnostics FFI + error.rs). Re-export scan: [result from Step 7]. ADR review outcome: [result from Step 5]. Downstream phase gates: A,B → 86; C → 87; F → 89; D,E,G,H,I,J,K,L,M,dep-bumps → 88."

Step 9 — Final consistency check before committing:
  Verify every SHA from the 35-commit list appears in the ledger body (not just in the cluster summary row).
  Verify clusters A and B both say "will-sync" (D-03 lock honored).
  Verify 7f319b9e is explicitly classified with "not in SEED-006" note.
  Verify e54cf9cb has the Windows CLR annotation.
  Verify ## Excluded as noise has "55" and "23 merge".
  Verify cluster B has windows-touch: yes.
  Verify cluster C has the cross-target clippy Phase 87 note.
  Verify the per-cluster "inspection depth" statement is present for every cluster (D-05 requirement: ledger states which depth was applied per cluster).
  </action>
  <verify>
    <automated>grep -c "## ADR review" "C:\Users\OMack\Nono\.planning\phases\85-upst9-divergence-audit\85-DIVERGENCE-LEDGER.md"</automated>
    Must return >= 1.

    Check actual-diff evidence in A:
    grep -c "pub use\|pub mod\|re-export" "C:\Users\OMack\Nono\.planning\phases\85-upst9-divergence-audit\85-DIVERGENCE-LEDGER.md"
    Must return >= 1 (the re-export check sections have real content, not placeholders).

    Check TLS-intercept assessment:
    grep -c "TLS-intercept" "C:\Users\OMack\Nono\.planning\phases\85-upst9-divergence-audit\85-DIVERGENCE-LEDGER.md"
    Must return >= 1.

    Check empirical cross-check:
    grep -c "Empirical cross-check" "C:\Users\OMack\Nono\.planning\phases\85-upst9-divergence-audit\85-DIVERGENCE-LEDGER.md"
    Must return >= 1.

    Check 5-dimension ADR table:
    grep -c "security\|windows\|maintenance\|divergence\|contributor" "C:\Users\OMack\Nono\.planning\phases\85-upst9-divergence-audit\85-DIVERGENCE-LEDGER.md"
    Must return >= 5 (one per dimension; likely more since the pre-scored table has 5 rows × 13 clusters).

    Check no residual "Deferred to Task 2" placeholders remain:
    grep -c "Deferred to Task 2" "C:\Users\OMack\Nono\.planning\phases\85-upst9-divergence-audit\85-DIVERGENCE-LEDGER.md"
    Must return 0.

    Final SHA completeness spot-check (spot 5 SHAs from across the distribution):
    grep -c "a5b2a51\|4ad8ba9\|e208687\|b5f8db5\|e54cf9c" "C:\Users\OMack\Nono\.planning\phases\85-upst9-divergence-audit\85-DIVERGENCE-LEDGER.md"
    Must return >= 5.
  </verify>
  <acceptance_criteria>
    - ## ADR review section exists with a table containing all 5 dimensions (security, windows, maintenance, divergence, contributor) for the overall phase assessment OR per-cluster assessments
    - Clusters A and B have H verdicts; cluster C has security=H verdict
    - ## Empirical cross-check section exists with ≥4 git log walk results showing files mapped to expected clusters
    - ## Cross-cluster re-export deps detected section exists with a verdict (clean or detected with prerequisite)
    - ## TLS-intercept assessment section exists with a split verdict for cluster F identifying which commits touch tls_intercept/ (absent in fork) vs shared server.rs/route.rs/credential.rs surface
    - No "Deferred to Task 2" placeholder text remains anywhere in the ledger
    - Cluster A's Cross-cluster re-export check field contains actual git show findings (not a placeholder)
    - Cluster B's Cross-cluster re-export check field contains actual git show findings, including the bindings/c/src/ diagnostic.rs surface result (Pitfall 3 closure)
    - Cluster F's Cross-cluster re-export check field contains actual diff findings per commit
    - Every cluster section states which inspection depth was applied (actual-diff or --name-only), per D-05
    - ## Headline is complete (not placeholder): states substantive count, cluster count, disposition breakdown, re-export result, ADR outcome, downstream phase pointers
    - 85-DIVERGENCE-LEDGER.md is ≥400 lines
    - The DCO-signed commit for this plan includes the ledger file (Signed-off-by: Oscar Mack Jr &lt;oscar.mack.jr@gmail.com&gt;)
  </acceptance_criteria>
  <done>85-DIVERGENCE-LEDGER.md is complete: all 35 substantive commits classified into clusters A–M, actual-diff re-export inspection done on A/B/F, ADR review with 5-dimension verdicts present, empirical cross-check done, TLS-intercept assessment present for cluster F, no placeholders remain, noise section documents 55 commits, e54cf9cb annotated Unix-only, all D-01 through D-07 decisions honored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream remote → local git history | git fetch upstream pulls commit objects from github.com/always-further/nono; executor trusts this as the canonical upstream source |
| drift tool output → ledger classification | The shell script parses git log output; its path filter determines which commits are "substantive" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-01 | Tampering | Ledger accuracy | mitigate | Classify from drift tool output (35 SHAs), not SEED-006 preview; cross-check every SHA appears in ledger exactly once; empirical cross-check section validates 4+ files |
| T-85-02 | Information Disclosure | Incorrect disposition misleads downstream cherry-pick | mitigate | D-03 locks A&B; D-04 leanings require diff-inspection before final commit; Task 2 actual-diff check can overturn any lean; ledger states inspection depth per cluster (D-05 auditable) |
| T-85-03 | Repudiation | SHA collision causes wrong commit range (local v0.62.0 ≠ upstream v0.62.0) | mitigate | Always use explicit SHAs 52809dda..01537570; SHA collision guard verified in Task 1 Step 2; collision guard note in ## Reproduction section |
| T-85-04 | Information Disclosure | Silently dropping 7f319b9e (not in SEED-006) creates incomplete ledger | mitigate | Task 1 Step 5 explicitly classifies 7f319b9e; acceptance_criteria requires "not in SEED-006" note present |
| T-85-05 | Tampering | env_clear annotation omitted from e54cf9cb → Phase 88 blindly removes Windows CLR baseline | mitigate | Task 1 acceptance_criteria requires grep for "hook_runtime_windows" ≥1; annotation wording is specified in plan action |
| T-85-SC | Tampering | npm/pip/cargo installs | accept | Phase 85 is documentation-only; no package manager invocations; no runtime attack surface added |

Note: This is a docs-only phase. The realistic risks are correctness/process risks (inaccurate ledger misleads downstream phases), not runtime security threats. No new source code is written, no attack surface is added.
</threat_model>

<verification>
After both tasks complete, verify the following against the ledger file at
.planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md:

1. AUDIT-01 checks:
   - grep -c "85-DIVERGENCE-LEDGER\|phase: 85" 85-DIVERGENCE-LEDGER.md  → ≥1
   - grep -c "total_unique_commits: 35" 85-DIVERGENCE-LEDGER.md  → 1
   - grep -c "## Cluster Summary" 85-DIVERGENCE-LEDGER.md  → ≥1
   - For each of the 35 SHAs: grep count ≥1 (can script this)
   - grep -c "will-sync\|fork-preserve\|split\|won't-sync" 85-DIVERGENCE-LEDGER.md  → ≥13
   - grep -c "upstream_head_at_audit" 85-DIVERGENCE-LEDGER.md  → ≥1 (D-07 tip-check recorded)
   - grep -c "Excluded as noise" 85-DIVERGENCE-LEDGER.md  → ≥1

2. AUDIT-02 checks:
   - grep -c "## ADR review" 85-DIVERGENCE-LEDGER.md  → ≥1
   - grep -c "security.*windows.*maintenance\|5 dimensions\|dimension.*score" 85-DIVERGENCE-LEDGER.md  → the 5-dim table exists
   - grep -c "Cross-cluster re-export" 85-DIVERGENCE-LEDGER.md  → ≥1
   - grep -c "## Cross-cluster re-export deps detected" 85-DIVERGENCE-LEDGER.md  → ≥1
   - grep -c "Deferred to Task 2" 85-DIVERGENCE-LEDGER.md  → 0 (no placeholders remain)
   - grep -c "git show" 85-DIVERGENCE-LEDGER.md  → ≥3 (actual-diff commands recorded in A/B/F)

3. Decision-fidelity checks:
   - D-01 (cluster-level, per-commit inventory): every ### Cluster section has a per-commit table
   - D-02 (split annotation): M and F both "split"; e54cf9cb annotated
   - D-03 (A&B locked will-sync): grep "Cluster A" context includes "will-sync"; same for B
   - D-05 (inspection depth stated per cluster): each cluster section says "actual-diff" or "--name-only"
   - D-06 (noise verifiable): "Excluded as noise" section states filter criteria, 55 count, SHA list
   - D-07 (re-fetch recorded): upstream_head_at_audit in frontmatter

4. Downstream contract:
   - "Phase 86" appears in clusters A and B rationale/downstream pointers
   - "Phase 87" appears in cluster C
   - "Phase 88" appears in clusters D, E, G, H, I, J, K, L, M
   - "Phase 89" appears in cluster F
</verification>

<success_criteria>
Phase 85 is complete when:

1. 85-DIVERGENCE-LEDGER.md exists with valid YAML frontmatter, total_unique_commits: 35, and upstream_head_at_audit recorded (AUDIT-01 / SC#1 + SC#2)
2. ## Cluster Summary table has ≥13 rows with dispositions from {will-sync, fork-preserve, split, won't-sync}; clusters A and B are will-sync; F and M are split (AUDIT-01 / SC#1)
3. All 35 substantive SHAs from the drift tool appear in the ledger body (AUDIT-01 completeness)
4. The D-07 re-fetch tip-check result is recorded (AUDIT-01 / SC#2)
5. Each cluster section contains an ADR-style L/M/H verdict across 5 dimensions (security, windows, maintenance, divergence, contributor) — can be in per-cluster subsections or combined in ## ADR review (AUDIT-02 / SC#3)
6. Clusters A, B, and F each have an actual-diff re-export check (not --name-only) with recorded findings; the ## Cross-cluster re-export deps detected section summarizes results (AUDIT-02 / SC#4 — feedback_cluster_isolation_invalid closed)
7. Cluster B's inspection explicitly covers bindings/c/src/ (Pitfall 3)
8. Cluster M's e54cf9cb entry is annotated Unix-path-only with hook_runtime_windows.rs retention note
9. Cluster C carries a note: Phase 87 must run cross-target clippy on cfg(linux) edits
10. ## Excluded as noise section documents 55 commits with filter criteria
11. The ledger is committed with DCO sign-off (Signed-off-by: Oscar Mack Jr &lt;oscar.mack.jr@gmail.com&gt;)
12. ROADMAP.md Phase 85 Plans field is updated from "TBD" to "1 plan"
</success_criteria>

<output>
Create `.planning/phases/85-upst9-divergence-audit/85-01-SUMMARY.md` when done.
Use the summary template at @C:\Users\OMack\.claude\get-shit-done\templates\summary.md.
Key fields to capture:
  - files_created: [.planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md]
  - decisions: [upstream_head_at_audit SHA, final disposition for each cluster A–M, re-export scan verdict for A/B/F, whether 7f319b9e went to B or M, final total_unique_commits count]
  - tech_stack: [git, scripts/check-upstream-drift.sh, upstream remote always-further/nono]
  - provides: [85-DIVERGENCE-LEDGER.md: cluster A–M dispositions + ADR risk verdicts + re-export scan results — gating input for Phases 86/87/88/89]
</output>
