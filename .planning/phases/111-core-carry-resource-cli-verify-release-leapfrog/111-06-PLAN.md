---
phase: 111-core-carry-resource-cli-verify-release-leapfrog
plan: 06
type: execute
wave: 4
depends_on: ["111-05"]
files_modified:
  - ../nono-py/Cargo.toml
  - ../nono-py/pyproject.toml
  - ../nono-py/Cargo.lock
  - ../nono-ts/Cargo.toml
  - ../nono-ts/package.json
  - ../nono-ts/npm/darwin-arm64/package.json
  - ../nono-ts/npm/darwin-x64/package.json
  - ../nono-ts/npm/linux-arm64-gnu/package.json
  - ../nono-ts/npm/linux-x64-gnu/package.json
autonomous: true
requirements: [RLS-14]

must_haves:
  truths:
    - "D-08: both sibling binding repos (../nono-py, ../nono-ts) are bumped to 0.70.0 in-phase, each with its own DCO-signed commit in its own repo's git history."
    - "Both maturin build (../nono-py) and napi build --platform --release (../nono-ts) rebuild clean at 0.70.0 — the only mechanism that catches binding struct drift, per the v3.4 lesson."
    - "D-07: the prepare-only release-dry-run.ps1 gate is GREEN (exit 0) with zero live upload — crates.io PRE_PUBLISH_REGISTRY_BLOCKED for the two downstream crates is an expected non-failure, not a FAIL."
    - "No tag was pushed, no cargo publish/twine upload/npm publish (without --dry-run) was executed anywhere in this plan."
  artifacts:
    - path: "../nono-py/Cargo.toml"
      provides: "version = \"0.70.0\" (own crate version; no path-dep version field exists to bump — confirmed live, nono/nono-proxy path deps carry no version= key here)"
    - path: "../nono-ts/Cargo.toml"
      provides: "version = \"0.70.0\" AND the nono path-dep version requirement corrected from \"0.66\" to \"0.70\" (a live semver constraint, not cosmetic)"
    - path: "../nono-ts/package.json"
      provides: "version 0.70.0 plus all 5 optionalDependencies platform-package pins bumped to 0.70.0"
  key_links:
    - from: "../nono-ts/Cargo.toml (nono path-dep version requirement)"
      to: "crates/nono/Cargo.toml (this repo's [package] version, bumped to 0.70.0 in Plan 111-05)"
      via: "Cargo path-dependency version-requirement compatibility (^0.70 must be satisfied by the actual 0.70.0 package)"
      pattern: "version = \"0.70"
    - from: "scripts/release-dry-run.ps1 (this repo)"
      to: "../nono-py, ../nono-ts"
      via: "maturin build / npm publish --dry-run invoked in the sibling repos' working directories"
      pattern: "NoonoPyDir|NonoTsDir"
---

<objective>
Bump both sibling binding repos (`../nono-py`, `../nono-ts`) to `0.70.0`, rebuild both to prove no
struct drift, then run the full prepare-only `scripts/release-dry-run.ps1` gate (which itself
exercises both sibling repos) and confirm GREEN — closing out RLS-14 and the whole VERIFY-01/RLS-14
half of Phase 111.

Purpose: D-08 requires both sibling repos bumped in-phase, each with its own DCO-signed commit in
its own repo's git history — binding-repo drift is caught only by `maturin build`/`napi build`,
never by `cargo build --workspace` (the sibling repos live outside this Cargo workspace, path-dep'd
in by relative path). This plan runs after Plan 111-05 because `../nono-ts`'s `Cargo.toml` pins an
explicit `version = "0.66"` requirement on the path-dependency to this repo's `nono` crate — that
requirement would stop resolving once this repo reports `0.70.0`, so it must be corrected here.
Output: Two DCO-signed sibling-repo commits; both bindings rebuilt green; the prepare-only release
gate GREEN with zero live upload.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-core-carry-resource-cli-verify-release-leapfrog/111-CONTEXT.md
@.planning/phases/111-core-carry-resource-cli-verify-release-leapfrog/111-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump ../nono-py to 0.70.0, rebuild, commit</name>
  <files>../nono-py/Cargo.toml, ../nono-py/pyproject.toml, ../nono-py/Cargo.lock</files>
  <read_first>
    - ../nono-py/Cargo.toml lines 1-20 (`name = "nono-py"`, `version = "0.66.1"` at line 3; the
      `nono`/`nono-proxy` path-dependency entries at lines 16-17 — confirmed live to carry NO
      `version =` key, only `path =`/`package =`, so no semver-requirement edit is needed here,
      unlike `../nono-ts`)
    - ../nono-py/pyproject.toml lines 6-7 (`name = "nono-sandbox"`, `version = "0.66.1"`)
    - .planning/phases/102-fork-owned-package-rename (Phase 102-03 precedent for this repo's own
      DCO-signed rename commit, including the `git add -f` needed for the gitignored-but-tracked
      `Cargo.lock`)
  </read_first>
  <action>
    In `../nono-py/Cargo.toml`, change line 3 `version = "0.66.1"` to `version = "0.70.0"`. Do NOT
    add a `version =` key to the `nono`/`nono-proxy` path-dependency lines (16-17) — they
    intentionally carry none today; adding one would be a new, unrequested constraint. In
    `../nono-py/pyproject.toml`, change lines 6-7's `version = "0.66.1"` to `version = "0.70.0"`
    (leave `name = "nono-sandbox"` untouched). Run `maturin build` in `../nono-py` and confirm exit
    0. Regenerate `../nono-py/Cargo.lock` (produced as a side effect of `maturin build`'s own
    `cargo build` invocation) and force-add it (`git add -f Cargo.lock`, per the Phase 102-03
    precedent — this file is gitignored-but-tracked in the nono-py repo). Commit all 3 files
    (`Cargo.toml`, `pyproject.toml`, `Cargo.lock`) together in ONE commit in the `../nono-py` git
    repository (a separate repo from this one — `cd ../nono-py && git commit`, not a commit in this
    repo), with a DCO sign-off: `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`.
  </action>
  <acceptance_criteria>
    - `../nono-py/Cargo.toml` reports `version = "0.70.0"`; its path-dependency lines are
      unchanged (still no `version =` key).
    - `../nono-py/pyproject.toml` reports `version = "0.70.0"`, `name = "nono-sandbox"` unchanged.
    - `maturin build` (run with cwd `../nono-py`) exits 0.
    - `../nono-py`'s git log shows exactly one new commit touching `Cargo.toml`, `pyproject.toml`,
      and `Cargo.lock`, DCO-signed.
  </acceptance_criteria>
  <verify>
    <automated>maturin build</automated>
  </verify>
  <done>../nono-py reports 0.70.0 everywhere it should, rebuilds clean, and is committed with DCO sign-off in its own repo.</done>
</task>

<task type="auto">
  <name>Task 2: Bump ../nono-ts to 0.70.0 (incl. the live path-dep requirement), rebuild, commit</name>
  <files>../nono-ts/Cargo.toml, ../nono-ts/package.json, ../nono-ts/npm/darwin-arm64/package.json, ../nono-ts/npm/darwin-x64/package.json, ../nono-ts/npm/linux-arm64-gnu/package.json, ../nono-ts/npm/linux-x64-gnu/package.json</files>
  <read_first>
    - ../nono-ts/Cargo.toml lines 1-18 (`name = "nono-node"`, `version = "0.66.1"` at line 3; the
      `nono` path-dependency at line 17 — confirmed live to read
      `nono = { path = "../Nono/crates/nono", package = "nono-sandbox", version = "0.66" }` — this
      `version = "0.66"` IS a live semver requirement (Cargo caret-parses it as `>=0.66.0,
      <0.67.0`), which will FAIL to resolve once this repo's `nono` crate reports `0.70.0` unless
      corrected here)
    - ../nono-ts/package.json lines 1-85 (top-level `"name": "@oscarmackjr/nono-ts"`,
      `"version": "0.66.1"` at lines 2-3; the `optionalDependencies` block at lines 77-83 pinning
      all 5 platform packages — `@oscarmackjr/nono-ts-darwin-x64`, `-darwin-arm64`,
      `-linux-x64-gnu`, `-linux-arm64-gnu`, `-win32-x64-msvc` — each at `"0.66.1"`)
    - ../nono-ts/npm/darwin-arm64/package.json, ../nono-ts/npm/darwin-x64/package.json,
      ../nono-ts/npm/linux-arm64-gnu/package.json, ../nono-ts/npm/linux-x64-gnu/package.json (each
      has its own top-level `"version": "0.66.1"`)
    - 111-RESEARCH.md § RLS-14 "`../nono-ts` (sibling repo, own git history)" (confirms
      `npm/win32-x64-msvc/package.json` does NOT exist locally yet — v3.5 Phase 105-03's
      unfinished platform-package gap — and that RLS-14 should still bump the
      `optionalDependencies` string reference for it without creating the missing directory)
  </read_first>
  <action>
    In `../nono-ts/Cargo.toml`, change line 3 `version = "0.66.1"` to `version = "0.70.0"`, AND
    change line 17's path-dependency `version = "0.66"` to `version = "0.70"` — this second edit
    is load-bearing (a live semver requirement), not cosmetic; without it the crate fails to
    resolve against this repo's now-`0.70.0` `nono` package. In `../nono-ts/package.json`, change
    the top-level `"version": "0.66.1"` (lines 2-3) to `"0.70.0"`, and change all 5
    `optionalDependencies` entries (lines 78-82) to `"0.70.0"` — including
    `"@oscarmackjr/nono-ts-win32-x64-msvc": "0.70.0"` even though
    `npm/win32-x64-msvc/package.json` does not exist locally (that gap is v3.5 Phase 105-03's
    scope; bump the string reference anyway, do NOT create the missing directory). In each of the
    4 existing `npm/{darwin-arm64,darwin-x64,linux-arm64-gnu,linux-x64-gnu}/package.json` files,
    change the top-level `"version"` field to `"0.70.0"`.

    Run `npx napi build --platform --release` in `../nono-ts` and confirm exit 0 (no global `napi`
    binary is on PATH; `npx` resolves it from the local `@napi-rs/cli` devDependency, per Phase
    110-08 precedent). Commit all 6 files together in ONE commit in the `../nono-ts` git repository
    (a separate repo — `cd ../nono-ts && git commit`), DCO-signed:
    `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`.
  </action>
  <acceptance_criteria>
    - `../nono-ts/Cargo.toml` reports `version = "0.70.0"` AND its `nono` path-dependency reads
      `version = "0.70"` (not left at `"0.66"`).
    - `../nono-ts/package.json`'s top-level version is `"0.70.0"`; all 5 `optionalDependencies`
      entries read `"0.70.0"`, including the still-nonexistent `win32-x64-msvc` entry.
    - The 4 existing `npm/*/package.json` files each report `"version": "0.70.0"`.
    - `npx napi build --platform --release` (run with cwd `../nono-ts`) exits 0.
    - `../nono-ts`'s git log shows exactly one new commit touching all 6 named files, DCO-signed.
  </acceptance_criteria>
  <verify>
    <automated>npx napi build --platform --release</automated>
  </verify>
  <done>../nono-ts reports 0.70.0 everywhere it should (including the live path-dep requirement), rebuilds clean, and is committed with DCO sign-off in its own repo.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full prepare-only release gate and confirm GREEN</name>
  <files>none (verification only)</files>
  <read_first>
    - scripts/release-dry-run.ps1 (full file, already read in Plan 111-05 — the 3-leg dry-run:
      crates.io `cargo publish --dry-run` in dependency order, PyPI `maturin build` + `twine
      check` in `../nono-py`, npm `npm publish --dry-run` in `../nono-ts`; GREEN = exit 0, where
      `PRE_PUBLISH_REGISTRY_BLOCKED` and toolchain-absent `SKIP` are explicitly non-failures)
    - 111-RESEARCH.md § RLS-14 "Prepare-only release gate — exact invocation and what GREEN looks
      like" (full text)
  </read_first>
  <action>
    Invoke `pwsh -File scripts\release-dry-run.ps1` (never `pwsh -Command "<bare path>"` — that
    form swallows non-zero exit codes, a documented repo-wide gotcha). Confirm exit 0. Expect:
    `cargo publish --dry-run -p nono-sandbox` to PASS; `cargo publish --dry-run -p
    nono-sandbox-proxy` and `-p nono-sandbox-cli` to report `PRE_PUBLISH_REGISTRY_BLOCKED` (an
    expected non-failure — `0.70.0` is not yet on crates.io, so the downstream crates' dependency
    resolution against the live index cannot succeed pre-publish); `maturin build` + `twine check`
    in `../nono-py` to PASS; `npm publish --dry-run` in `../nono-ts` to PASS with `index.js` +
    `index.d.ts` present in the tarball manifest.

    Then re-run `pwsh -File scripts\verify-dark.ps1 -Gate release-readiness` one final time as the
    phase-closing confirmation and confirm it still reports PASS against `0.70.0`/`0.69.0` (now
    that the sibling repos are also bumped — this re-run is the same gate Plan 111-05 already ran,
    repeated here to confirm nothing regressed during Tasks 1-2).

    Confirm, by inspection of shell history / task actions across this entire plan, that no
    `cargo publish` (without `--dry-run`), `twine upload`, `npm publish` (without `--dry-run`),
    `git tag`, or `git push --tags` command was ever executed (D-07) — `release-dry-run.ps1`
    itself contains no live-upload command at any point in the file, independently confirmed by a
    full read in 111-RESEARCH.md.
  </action>
  <acceptance_criteria>
    - `pwsh -File scripts\release-dry-run.ps1` exits 0.
    - The script's own final verdict line reads `PASS: Hard failures: 0. Blocked: 2 (pre-publish).`
      (or equivalent — 2 blocked crates: `nono-sandbox-proxy`, `nono-sandbox-cli`) — zero `FAIL`
      entries.
    - `pwsh -File scripts\verify-dark.ps1 -Gate release-readiness` returns PASS.
    - No live publish/upload/tag-push command was executed anywhere in Plan 111-06.
  </acceptance_criteria>
  <verify>
    <automated>pwsh -File scripts\release-dry-run.ps1</automated>
  </verify>
  <done>The prepare-only release gate is GREEN with zero hard failures; RLS-14 and the phase's release-leapfrog objective are both satisfied without any live push or publish.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This repo's release-dry-run script → sibling repos' working directories | `release-dry-run.ps1` runs `maturin build`/`npm publish --dry-run` with `Push-Location` into `../nono-py`/`../nono-ts` — both are local, developer-owned sibling checkouts, not a network-fetched or untrusted path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | Information Disclosure (incorrect security posture) | Not touched by this plan | accept | N/A to RLS-14. |
| T-111-02 | Denial of Service (resource exhaustion) | Not touched by this plan | accept | N/A to RLS-14. |
| T-111-03 | Spoofing (package-identity confusion during the multi-repo leapfrog) | `../nono-py/pyproject.toml` (`name = "nono-sandbox"`), `../nono-ts/package.json` (`"name": "@oscarmackjr/nono-ts"`) | mitigate | Both sibling identities are already fork-owned (Phase 102); this plan bumps only their version strings, introducing no new identity-confusion surface. D-07's prepare-only posture (no live publish) is itself a mitigation — no registry-visible artifact is created until an explicit, separate, operator-gated decision. |
</threat_model>

<verification>
- Both sibling repos report `0.70.0` (and, for `../nono-ts`, the corrected `"0.70"` path-dep
  requirement) with clean `maturin build` / `napi build --platform --release`.
- `pwsh -File scripts\release-dry-run.ps1` exits 0 with zero hard failures.
- `pwsh -File scripts\verify-dark.ps1 -Gate release-readiness` returns PASS.
- No tag pushed, no live publish/upload command executed anywhere in this plan.
</verification>

<success_criteria>
- RLS-14 is fully satisfied: all 6 in-repo crates (Plan 111-05) + both sibling repos (this plan)
  report `0.70.0`; `Cargo.lock` shows zero unexpected third-party drift; the prepare-only release
  gate is GREEN; no operator push occurred.
</success_criteria>

<output>
Create `.planning/phases/111-core-carry-resource-cli-verify-release-leapfrog/111-06-SUMMARY.md` when done
</output>
