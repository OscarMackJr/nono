---
phase: 111-core-carry-resource-cli-verify-release-leapfrog
plan: 04
type: execute
wave: 2
depends_on: ["111-01", "111-02", "111-03"]
files_modified:
  - .planning/phases/111-core-carry-resource-cli-verify-release-leapfrog/111-04-VERIFICATION-NOTES.md
autonomous: true
requirements: [VERIFY-01]

must_haves:
  truths:
    - "D-09: the fork-invariant pass covers the combined 108-111 surface, not just Phase 111's own diff — both cross-target clippy gates and both binding rebuilds are re-run fresh, superseding the now-stale 110-08 certification."
    - "D-10: both cross-target clippy gates (cross linux-gnu, cargo-zigbuild apple-darwin) ran LOCALLY and GREEN with zero findings — no PARTIAL→CI substitution, since no documented runner failure occurred."
    - "D-01 still holds after CORE-01/CORE-02: crates/nono/src/lib.rs has no `pub mod resource;` and no file matching resource* exists under crates/nono/src."
    - "The ADR-86 Windows carve-out (crates/nono-cli/src/exec_strategy_windows/) is confirmed structurally unregressed by CORE-01/CORE-02 (neither touches this directory)."
    - "cargo test --workspace --no-fail-fast's failing-test-name set is reproduced against the documented 24-name baseline (11 + 13) from 110-08 — no new failure name, no fabricated GREEN."
  artifacts:
    - path: ".planning/phases/111-core-carry-resource-cli-verify-release-leapfrog/111-04-VERIFICATION-NOTES.md"
      provides: "The combined-108-111 fork-invariant verification record superseding 110-08's stale certification"
  key_links:
    - from: "111-04-VERIFICATION-NOTES.md"
      to: ".planning/phases/110-profile-policy-absorb-platform-overrides/110-08-VERIFICATION-NOTES.md"
      via: "explicit supersession note — this record covers the combined 108-111 surface the prior one predates"
      pattern: "110-08"
---

<objective>
Run the full fork-invariant verification pass VERIFY-01 requires: both mandatory local
cross-target clippy gates over the combined 108-111 surface, the `make ci` constituent commands
(since `make` is absent from this host's PATH), a baseline-honest diff of `cargo test --workspace
--no-fail-fast` against the documented 24-failure list, the concrete D-09 grep/diff assertions
(D-01 still holds; ADR-86 Windows carve-out unregressed; the 4 resource-flag regression-guard
tests still pass), and a rebuild of both sibling bindings.

Purpose: Phase 110's own `110-08` phase-gate certification predates four library-behavior changes
landed 2026-08-04 and demonstrably missed a real defect (`has_port_rules()`) inside its own scope —
per D-09, this phase's certification must cover the combined 108-111 surface honestly, not just
Phase 111's own small diff.
Output: A new verification-notes record; both gates confirmed GREEN; the 24-name baseline
reproduced (not exceeded); both bindings confirmed green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-core-carry-resource-cli-verify-release-leapfrog/111-CONTEXT.md
@.planning/phases/111-core-carry-resource-cli-verify-release-leapfrog/111-RESEARCH.md
@.planning/templates/cross-target-verify-checklist.md
@.planning/phases/110-profile-policy-absorb-platform-overrides/110-08-VERIFICATION-NOTES.md
@.planning/phases/110-profile-policy-absorb-platform-overrides/deferred-items.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Both cross-target clippy gates + D-01/ADR-86 grep-diff assertions</name>
  <files>none (verification only; findings recorded in Task 3's output file)</files>
  <read_first>
    - .planning/templates/cross-target-verify-checklist.md (full file — exact gate invocations,
      pinned `cross` image, decision tree)
    - crates/nono/src/lib.rs (confirm no `pub mod resource;` among its `pub mod` lines)
    - crates/nono-cli/src/exec_strategy_windows/ directory listing (confirm CORE-01/CORE-02 did
      not touch it — neither plan's `files_modified` names this directory)
    - 111-RESEARCH.md § VERIFY-01 "Concrete fork-invariant assertions for D-09" (items 1-3)
  </read_first>
  <action>
    Run `cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used`
    (pinned image `ghcr.io/cross-rs/x86_64-unknown-linux-gnu:0.2.5@sha256:9e5b39c0...` per the
    checklist) against the tree with Plans 111-01/111-02/111-03's changes merged. Then, with
    `SDKROOT` unset, run the direct-binary form
    `cargo-zigbuild clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used`.
    Both must exit 0 with zero findings — this is a re-run over the combined 108-111 surface per
    D-09, not merely Phase 111's own diff, so treat any finding as a real regression requiring a
    structural fix (cfg-gates, visibility, restoring a dropped fork invariant), never an `#[allow]`
    silencing (per the checklist's Anti-Pattern 2).

    Then run the D-09 grep/diff assertions: confirm
    `grep -n "^pub mod resource" crates/nono/src/lib.rs` returns zero matches and no file under
    `crates/nono/src` matches `resource*` (D-01 still holds after CORE-01/CORE-02's edits); and
    confirm `git diff c5c8c5fc6e6b146ba5f27e6cd1d427b2da750ba2..HEAD --
    crates/nono-cli/src/exec_strategy_windows/` is empty (the ADR-86 Windows denial-rendering
    carve-out is unregressed, since neither Plan 111-01 nor 111-02 modifies this directory — only
    `crates/nono-cli/src/exec_strategy_windows/launch.rs` was read, not modified, per Plan 111-02's
    `read_first`).
  </action>
  <acceptance_criteria>
    - `cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used`
      exits 0, zero findings.
    - `cargo-zigbuild clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used`
      (SDKROOT unset) exits 0, zero findings.
    - `grep -n "^pub mod resource" crates/nono/src/lib.rs` returns zero matches.
    - No file under `crates/nono/src` matches the glob `**/resource*`.
    - `git diff c5c8c5fc6e6b146ba5f27e6cd1d427b2da750ba2..HEAD -- crates/nono-cli/src/exec_strategy_windows/`
      produces empty output.
  </acceptance_criteria>
  <verify>
    <automated>cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used</automated>
  </verify>
  <done>Both gates GREEN over the combined 108-111 surface; D-01 and the ADR-86 Windows carve-out both confirmed unregressed.</done>
</task>

<task type="auto">
  <name>Task 2: make ci substitution + 24-name baseline diff + resource-flag regression guard</name>
  <files>none (verification only; findings recorded in Task 3's output file)</files>
  <read_first>
    - Makefile lines ~1-150 (`make ci`'s constituent commands: `check` = clippy + fmt-check;
      `test` = test-lib + test-cli + test-ffi; `audit` = cargo audit)
    - .planning/phases/110-profile-policy-absorb-platform-overrides/110-08-VERIFICATION-NOTES.md
      (full file — the exact 24-name baseline: 11 `--bin nono` failures + 13 more surfaced only by
      `--no-fail-fast`, across `audit_attestation.rs`/`env_vars.rs`/`resl_nix_async_signal_safety.rs`)
    - 111-RESEARCH.md § VERIFY-01 "make ci substitution" and "The exact pre-existing cargo test
      --workspace baseline to reproduce (not exceed)" (full text, including the verbatim 24 names)
  </read_first>
  <action>
    `make` is confirmed absent from this host's PATH — run the constituent commands directly (do
    not emit a bare `make ci`): `cargo clippy --workspace --all-targets --all-features -- -D
    warnings -D clippy::unwrap_used`, `cargo fmt --all -- --check`, `cargo test -p nono-sandbox`,
    `cargo test -p nono-sandbox-cli` (fork-owned package selectors — NOT `-p nono`/`-p nono-cli`),
    `cargo test -p nono-ffi`, and `cargo audit`. `cargo audit` is already confirmed exit-0 on this
    host (RUSTSEC-2026-0204/`crossbeam-epoch` was closed pre-phase by quick task `260729-nh4`) — no
    action needed if it stays 0.

    Then run the full baseline sweep: `cargo test --workspace --no-fail-fast`. Collect the set of
    failing test names. Diff this set against the documented 24-name baseline (11 names from
    `--bin nono` + 13 names across `audit_attestation.rs`/`env_vars.rs`/`resl_nix_async_signal_safety.rs`,
    verbatim in 111-RESEARCH.md § VERIFY-01). The failing set for THIS run must be a subset of or
    identical to those 24 names, plus whatever new tests Plans 111-01/111-02 added (which must all
    PASS, not fail). Any failing name NOT in the 24-name list is a genuine regression requiring
    investigation — do not fix unrelated pre-existing failures to make the count look smaller (per
    Pitfall 3); do not report `cargo test --workspace` as exiting 0 (it will not, and that is
    expected and honest).

    Finally, run the D-09 assertion 3 regression guard:
    `cargo test -p nono-sandbox-cli --bin nono -- cpu_percent_range_enforced_by_clap
    max_processes_range_enforced_by_clap memory_zero_rejected_by_parser` plus the "Regression
    guard: Phase 16's --cpu-percent / --memory / --timeout / --max-processes remain parseable"
    tests at ~line 4133/4249 — all must pass unmodified after Plan 111-02's doc-comment rewrite.
  </action>
  <acceptance_criteria>
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings -D clippy::unwrap_used`
      exits 0.
    - `cargo fmt --all -- --check` exits 0.
    - `cargo test -p nono-sandbox` and `cargo test -p nono-ffi` both exit 0.
    - `cargo audit` exits 0.
    - `cargo test --workspace --no-fail-fast`'s failing-test-name set is a subset of the documented
      24-name baseline (no new failure name outside that list; the two Wave-0 tests from Plan
      111-01 pass).
    - `cargo test -p nono-sandbox-cli --bin nono -- cpu_percent_range_enforced_by_clap
      max_processes_range_enforced_by_clap memory_zero_rejected_by_parser` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --workspace --no-fail-fast</automated>
  </verify>
  <done>make ci's constituents are GREEN; the failure set matches the documented baseline exactly (no new regressions, no fabricated GREEN).</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild both sibling bindings + write the verification-notes record</name>
  <files>.planning/phases/111-core-carry-resource-cli-verify-release-leapfrog/111-04-VERIFICATION-NOTES.md</files>
  <read_first>
    - .planning/phases/110-profile-policy-absorb-platform-overrides/110-08-VERIFICATION-NOTES.md
      (the binding-rebuild procedure and evidence format to mirror)
  </read_first>
  <action>
    Run `maturin build` in `../nono-py` and confirm exit 0 (no `pub` struct field either binding
    constructs was touched by CORE-01/CORE-02 — CORE-01 is `policy.json` + a private constant;
    CORE-02 is CLI-only help text — so a clean, no-fix-required rebuild is expected, matching the
    110-08 precedent). Run `npx napi build --platform --release` in `../nono-ts` and confirm exit
    0. Both are run at the CURRENT (pre-RLS-14, still `0.66.1`) version — this task validates
    struct/API compatibility, not version numbers; RLS-14's own binding rebuild happens later in
    Plan 111-06 after the version bump.

    Write `.planning/phases/111-core-carry-resource-cli-verify-release-leapfrog/111-04-VERIFICATION-NOTES.md`
    recording: both cross-target clippy gate results (Task 1), the make ci substitution results and
    the full 24-name baseline diff with the actual observed failing-test-name list (Task 2), both
    binding-rebuild results (this task), and an explicit statement that this record supersedes
    `110-08-VERIFICATION-NOTES.md` per D-09 (that record predates the 4 defect-fix commits of
    2026-08-04 and this one covers the combined 108-111 surface).
  </action>
  <acceptance_criteria>
    - `maturin build` in `../nono-py` exits 0.
    - `npx napi build --platform --release` in `../nono-ts` exits 0.
    - `.planning/phases/111-core-carry-resource-cli-verify-release-leapfrog/111-04-VERIFICATION-NOTES.md`
      exists and documents all three tasks' evidence plus the explicit supersession note.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — binding builds are integration-level; verified by exit-code inspection per this task's own acceptance_criteria</automated>
  </verify>
  <done>Both bindings rebuild clean; the combined-surface verification record exists and explicitly supersedes 110-08's stale certification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A — this plan is verification/gate execution only | No new code path or input surface is introduced; this plan confirms existing boundaries are unregressed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | Information Disclosure (incorrect security posture) | N/A — no help text touched by this plan; this plan verifies Plan 111-02's fix landed correctly | accept | Verified via the regression-guard test re-run in Task 2. |
| T-111-02 | Denial of Service (resource exhaustion) | Kernel-enforced Job Object / cgroup v2 / `RLIMIT_NPROC` — this plan verifies these controls (and the ADR-86 boundary around them) are unregressed, but modifies none | accept | Verification, not modification — both cross-target gates + the regression-guard tests are the mitigation evidence. |
| T-111-03 | Spoofing (package-identity confusion) | Not touched — no version/package-identity change in this plan | accept | RLS-14's version leapfrog is Plan 111-05/111-06's scope. |
</threat_model>

<verification>
- Both cross-target clippy gates GREEN, zero findings, over the combined 108-111 surface.
- `make ci` substitution set GREEN.
- `cargo test --workspace --no-fail-fast` failure set is a subset of the documented 24-name
  baseline; zero new failures.
- Both sibling bindings rebuild clean.
- D-01 (no `pub mod resource;`) and the ADR-86 Windows carve-out both confirmed unregressed.
</verification>

<success_criteria>
- VERIFY-01 is satisfied: both gates + `make ci` GREEN locally, fork-invariant pass confirms the
  Windows security model + ADR-86 boundary unregressed, over the honest combined 108-111 surface
  (D-09), not merely Phase 111's own small diff.
</success_criteria>

<output>
Create `.planning/phases/111-core-carry-resource-cli-verify-release-leapfrog/111-04-SUMMARY.md` when done
</output>
