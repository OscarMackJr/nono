---
phase: 111-core-carry-resource-cli-verify-release-leapfrog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono-cli/data/policy.json
  - crates/nono-cli/src/policy.rs
  - crates/nono-cli/src/exec_strategy.rs
autonomous: true
requirements: [CORE-01]

must_haves:
  truths:
    - "macOS profiles carrying the `user_caches_macos` group grant readwrite access to `~/.cache`, matching upstream #1378's fix for tools (uv, Corepack) that write there despite the platform convention being `~/Library/Caches`."
    - "The macOS fork-safety crypto-thread budget is 12, not 7, matching upstream #1424's fix for the libdispatch workqueue-thread flake."
    - "Only the one relevant line from upstream commit ca888108 (#1378) was ported; the other 5 files in that commit (which don't exist under those names/paths in this fork, or are unrelated clippy cosmetics) were explicitly left untouched."
  artifacts:
    - path: "crates/nono-cli/data/policy.json"
      provides: "user_caches_macos.allow.readwrite now contains ~/.cache alongside the pre-existing ~/Library/Caches and ~/Library/Logs entries"
    - path: "crates/nono-cli/src/exec_strategy.rs"
      provides: "MAX_CRYPTO_THREADS constant raised from 7 to 12 with upstream's corrected doc comment"
    - path: "crates/nono-cli/src/policy.rs"
      provides: "New Wave-0 by-value test asserting ~/.cache is present in user_caches_macos"
  key_links:
    - from: "crates/nono-cli/data/policy.json"
      to: "crates/nono-cli/src/policy.rs::load_embedded_policy"
      via: "embedded build.rs data load"
      pattern: "user_caches_macos"
    - from: "crates/nono-cli/src/exec_strategy.rs::MAX_CRYPTO_THREADS"
      to: "crates/nono-cli/src/exec_strategy.rs::ThreadingContext::CryptoExpected arm"
      via: "constant reference by name (no other call sites)"
      pattern: "MAX_CRYPTO_THREADS"
---

<objective>
Absorb the two macOS/core carries CORE-01 requires: upstream #1378's one-line `~/.cache` grant
to the `user_caches_macos` policy group, and upstream #1424's `MAX_CRYPTO_THREADS` bump from 7 to
12. Both are small, independent, verified-live diffs — the actual payload of #1378 is a single
JSON array entry (the other 5 files in that upstream commit do not port to this fork), and #1424
is a single `usize` constant + its doc comment.

Purpose: Close the CORE-01 requirement without over-porting unrelated upstream cosmetic diffs,
and add the two by-value test assertions RESEARCH.md's Wave 0 gap analysis identified as missing
(existing tests only prove the policy group *loads*, not its contents; the crypto-thread count is
only exercised indirectly).
Output: `policy.json` gains one array entry; `exec_strategy.rs` gains a corrected constant + doc
comment; two new passing unit tests; both mandatory cross-target clippy gates re-confirmed GREEN
on `exec_strategy.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-core-carry-resource-cli-verify-release-leapfrog/111-CONTEXT.md
@.planning/phases/111-core-carry-resource-cli-verify-release-leapfrog/111-RESEARCH.md
@.planning/templates/cross-target-verify-checklist.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Absorb #1378 — add ~/.cache to user_caches_macos + Wave-0 test</name>
  <files>crates/nono-cli/data/policy.json, crates/nono-cli/src/policy.rs</files>
  <read_first>
    - crates/nono-cli/data/policy.json lines 320-336 (the `user_caches_macos` group: `allow.readwrite`
      currently `["~/Library/Caches", "~/Library/Logs"]`, `allow.read` currently
      `["~/Library/Preferences"]`)
    - crates/nono-cli/src/policy.rs lines 1661-1740 (`test_embedded_claude_code_platform_groups_have_expected_paths`
      — the exact test pattern to mirror: `load_embedded_policy()` -> `policy.groups.get(name)` ->
      `.allow.as_ref().expect(...).readwrite` -> `.contains(&"...".to_string())`)
    - 111-RESEARCH.md § CORE-01 "#1378 — ~/.cache on macOS" (lines 138-184) — the verbatim diff and
      the table of the other 5 files in upstream commit `ca888108fe5983be866e8d1a6eccf96edc2a8dd5`
      that do NOT apply to this fork
  </read_first>
  <behavior>
    - Test: `load_embedded_policy()` -> `policy.groups.get("user_caches_macos")` ->
      `.allow.as_ref().unwrap().readwrite` contains `"~/.cache"` AND still contains
      `"~/Library/Caches"` AND `"~/Library/Logs"` (regression guard on the pre-existing two).
  </behavior>
  <action>
    In `crates/nono-cli/data/policy.json`, inside the `user_caches_macos` group's `allow.readwrite`
    array (lines 328-331, currently `["~/Library/Caches", "~/Library/Logs"]`), append a third
    string element `"~/.cache"` after `"~/Library/Logs"` — this is the ONLY line to port from
    upstream commit `ca888108fe5983be866e8d1a6eccf96edc2a8dd5` (#1378, "fix: missing ~/.cache on
    macOS (e.g., uv, Corepack)"). Do NOT run `git cherry-pick ca888108` and do NOT attempt to port
    any of the commit's other 5 files (`crates/nono-cli/src/learn.rs` — exists but the touched
    lines are an unrelated `clippy::question_mark` cosmetic, not part of the `~/.cache` feature;
    `crates/nono-cli/src/macos_trust.rs`, `crates/nono-cli/src/proxy_command.rs` — do not exist
    under those names in this fork; `crates/nono-proxy/src/server.rs`'s `&*self.token` diff — the
    matching line was not found via search, the fork has diverged past it; `crates/nono-proxy/src/tls_intercept/ca.rs`
    — the `tls_intercept/` directory does not exist in this fork). Leave `allow.read` (the
    `~/Library/Preferences` entry) untouched.

    In `crates/nono-cli/src/policy.rs`, add a new `#[test]` named
    `test_user_caches_macos_includes_dot_cache` near the existing
    `test_embedded_claude_code_platform_groups_have_expected_paths` (~line 1661), following that
    test's exact pattern: call `load_embedded_policy().expect("embedded policy")`, get
    `policy.groups.get("user_caches_macos").expect("user_caches_macos group missing")`, assert
    `.platform.as_deref() == Some("macos")`, then assert its `.allow.as_ref().expect(...).readwrite`
    contains `"~/.cache"`, `"~/Library/Caches"`, and `"~/Library/Logs"` (all three, as a regression
    guard proving the addition is additive, not a replacement).
  </action>
  <acceptance_criteria>
    - `crates/nono-cli/data/policy.json`'s `user_caches_macos.allow.readwrite` array is exactly
      `["~/Library/Caches", "~/Library/Logs", "~/.cache"]` — no entries removed, order preserved,
      new entry last.
    - `cargo test -p nono-sandbox-cli --bin nono -- test_user_caches_macos_includes_dot_cache`
      passes.
    - A diff of this task's own commit touches exactly the 2 files named above — none of the other
      5 files from upstream `ca888108` appear in the diff.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-cli --bin nono -- test_user_caches_macos_includes_dot_cache</automated>
  </verify>
  <done>policy.json has the one-line addition; the new test passes; no unrelated upstream file was touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Absorb #1424 — MAX_CRYPTO_THREADS 7→12 + Wave-0 test + cross-target gates</name>
  <files>crates/nono-cli/src/exec_strategy.rs</files>
  <read_first>
    - crates/nono-cli/src/exec_strategy.rs lines 160-172 (the current `MAX_CRYPTO_THREADS: usize = 7`
      constant and its doc comment)
    - crates/nono-cli/src/exec_strategy.rs lines 895-943 (`ThreadingContext::CryptoExpected` arm
      and the error message that read the constant by name — neither needs editing)
    - crates/nono-cli/src/exec_strategy.rs lines 4367-4460 (the `mod tests` block — pattern to add
      the new test into)
    - 111-RESEARCH.md § CORE-01 "#1424 — MAX_CRYPTO_THREADS raised to 12" (lines 186-226) — the
      verbatim upstream target doc-comment text
    - .planning/templates/cross-target-verify-checklist.md (full file — exact gate invocations;
      this file's own `#[cfg(target_os = "macos")]` `RLIMIT_AS` pre_exec hook at ~line 1127 is why
      both gates are mandatory here even though the touched lines are not themselves inside a
      `cfg` block)
  </read_first>
  <behavior>
    - Test: `assert_eq!(MAX_CRYPTO_THREADS, 12);`
  </behavior>
  <action>
    In `crates/nono-cli/src/exec_strategy.rs` at line 168, change
    `const MAX_CRYPTO_THREADS: usize = 7;` to `const MAX_CRYPTO_THREADS: usize = 12;`, and replace
    its preceding doc comment with upstream commit `099237da94d33752ca5617db3f4902a216f9f84c`'s
    (#1424) exact target text: "Maximum threads allowed when crypto library thread pool is active.
    Main thread (1) + tokio proxy workers (2) + aws-lc-rs ECDSA pool (4), plus headroom for the
    OS-managed libdispatch workqueue threads that Security.framework spawns for
    `SecTrustSettings*` XPC during proxy CA setup on macOS (2-5 observed, scales with load). Those
    workqueue threads are unnamed, parked, and fork-safe, but a tighter budget intermittently
    tripped on them (issue: fork thread-count flake)." Do not touch the two consumer sites at
    lines 915 and 939 — both reference the constant by name and require no edit.

    Add a new `#[test] fn max_crypto_threads_raised_to_12()` inside the existing `mod tests` block
    (~line 4367, `use super::*;` already in scope) containing
    `assert_eq!(MAX_CRYPTO_THREADS, 12);`.

    Then run both mandatory cross-target clippy gates per
    `.planning/templates/cross-target-verify-checklist.md` (this touched file is in-scope per that
    checklist's own § Scope — it contains `#[cfg(target_os = "macos")]` blocks elsewhere):
    `cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used`
    and, with `SDKROOT` unset, the direct-binary form
    `cargo-zigbuild clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used`.
  </action>
  <acceptance_criteria>
    - `MAX_CRYPTO_THREADS: usize = 12` appears exactly once in `crates/nono-cli/src/exec_strategy.rs`.
    - `cargo test -p nono-sandbox-cli --bin nono -- max_crypto_threads_raised_to_12` passes.
    - `cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used`
      exits 0 with zero findings.
    - `cargo-zigbuild clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used`
      (SDKROOT unset) exits 0 with zero findings.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-cli --bin nono -- max_crypto_threads_raised_to_12</automated>
  </verify>
  <done>The constant reads 12 with the corrected doc comment; the new test passes; both cross-target gates are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Embedded policy data → sandbox capability compilation | `crates/nono-cli/data/policy.json` is CLI-owned, build-time-embedded static data (per CLAUDE.md's Library vs CLI table). No runtime/untrusted input crosses this boundary — this task only appends one hardcoded path string to an existing, already-trusted group. |
| macOS pre-fork thread budget | `MAX_CRYPTO_THREADS` gates a pre-fork safety check in CLI-side supervised-fork machinery, not a sandbox capability boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | Information Disclosure (incorrect security posture in docs) | N/A to this plan — no help text touched here | accept | Not applicable — CORE-02's help-text correction is Plan 111-02's scope, not this plan's. |
| T-111-02 | Denial of Service (resource exhaustion / fork-bomb) | Not touched — this plan edits a policy path grant and a pre-fork thread-count constant, neither of which changes any kernel-enforced resource limit | accept | No enforcement mechanism is modified by this plan; `--cpu-percent`/`--memory`/`--timeout`/`--max-processes` are untouched. |
| T-111-03 | Spoofing (package-identity confusion) | Not touched — no version/package-identity change in this plan | accept | RLS-14's version leapfrog is Plan 111-05/111-06's scope. |
</threat_model>

<verification>
- `cargo test -p nono-sandbox-cli --bin nono -- test_user_caches_macos_includes_dot_cache max_crypto_threads_raised_to_12` — both new tests pass.
- `cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used` exits 0.
- `cargo-zigbuild clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used` (SDKROOT unset) exits 0.
- `git diff --stat` on this plan's commits shows only `crates/nono-cli/data/policy.json`, `crates/nono-cli/src/policy.rs`, and `crates/nono-cli/src/exec_strategy.rs`.
</verification>

<success_criteria>
- `user_caches_macos.allow.readwrite` contains `~/.cache` alongside the two pre-existing entries.
- `MAX_CRYPTO_THREADS == 12` with the corrected doc comment.
- Both new Wave-0 tests pass; both mandatory cross-target clippy gates are GREEN.
- No file from upstream `ca888108` other than `policy.json` was touched.
</success_criteria>

<output>
Create `.planning/phases/111-core-carry-resource-cli-verify-release-leapfrog/111-01-SUMMARY.md` when done
</output>
