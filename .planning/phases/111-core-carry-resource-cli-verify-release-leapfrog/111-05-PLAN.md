---
phase: 111-core-carry-resource-cli-verify-release-leapfrog
plan: 05
type: execute
wave: 3
depends_on: ["111-04"]
files_modified:
  - crates/nono/Cargo.toml
  - crates/nono-cli/Cargo.toml
  - crates/nono-proxy/Cargo.toml
  - crates/nono-shell-broker/Cargo.toml
  - bindings/c/Cargo.toml
  - crates/nono-fltmgr-client/Cargo.toml
  - Cargo.lock
  - scripts/gates/release-readiness.ps1
  - scripts/release-dry-run.ps1
autonomous: true
requirements: [RLS-14]

must_haves:
  truths:
    - "All 6 in-repo version-family crates (nono-sandbox, nono-sandbox-cli, nono-sandbox-proxy, nono-shell-broker, nono-fltmgr-client, nono-ffi) report version 0.70.0 via cargo metadata."
    - "tools/sign-fixture keeps its independent 0.1.0 versioning, untouched by this bump."
    - "scripts/gates/release-readiness.ps1's $targetVersion/$upstreamHighest are updated to 0.70.0/0.69.0 in the same wave as the version bump (not left stale), so the gate asserts against the real target rather than passing vacuously against 0.66.1/0.66.0."
    - "D-07: no tag is pushed and no cargo publish (without --dry-run) is executed by this plan."
  artifacts:
    - path: "Cargo.lock"
      provides: "Regenerated lockfile with drift limited to the 6 bumped workspace crates"
    - path: "scripts/gates/release-readiness.ps1"
      provides: "Version-assertion inputs corrected to 0.70.0/0.69.0"
  key_links:
    - from: "crates/nono-cli/Cargo.toml (path-dep pins)"
      to: "crates/nono/Cargo.toml, crates/nono-proxy/Cargo.toml, crates/nono-shell-broker/Cargo.toml"
      via: "version = \"0.70.0\" path-dependency requirements, kept in lockstep with the depended-on crates' own [package] version"
      pattern: "version = \"0.70.0\""
---

<objective>
Bump the fork's own 6 workspace-member crates + their internal path-dependency version pins from
`0.66.1` to `0.70.0` (collision-free above upstream `v0.69.0`), regenerate `Cargo.lock`, and
correct the two gate scripts' hardcoded `0.66.1`/`0.66.0` version-string touchpoints so the
prepare-only release gate asserts against the real target rather than a stale one.

Purpose: This is the in-repo half of RLS-14's mechanical version leapfrog — the sibling binding
repos (`../nono-py`, `../nono-ts`) are bumped separately in Plan 111-06 because they depend on
this repo's crates being bumped first (nono-ts pins an explicit `version = "0.66"` requirement on
the path-dependency, which would stop resolving once this repo's `nono` crate reports `0.70.0`).
Output: 6 Cargo.toml files + Cargo.lock updated; `release-readiness.ps1`'s live assertion inputs
corrected; `release-dry-run.ps1`'s cosmetic references corrected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-core-carry-resource-cli-verify-release-leapfrog/111-CONTEXT.md
@.planning/phases/111-core-carry-resource-cli-verify-release-leapfrog/111-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump the 6 in-repo crate versions + path-dep pins + regenerate Cargo.lock</name>
  <files>crates/nono/Cargo.toml, crates/nono-cli/Cargo.toml, crates/nono-proxy/Cargo.toml, crates/nono-shell-broker/Cargo.toml, bindings/c/Cargo.toml, crates/nono-fltmgr-client/Cargo.toml, Cargo.lock</files>
  <read_first>
    - crates/nono/Cargo.toml line 3 (`version = "0.66.1"`); crates/nono-cli/Cargo.toml lines 3,
      47, 48, 180; crates/nono-proxy/Cargo.toml lines 3, 21; crates/nono-shell-broker/Cargo.toml
      lines 3, 20; bindings/c/Cargo.toml lines 3, 18; crates/nono-fltmgr-client/Cargo.toml line 3
      (all currently `0.66.1`, verified live)
    - tools/sign-fixture/Cargo.toml lines 2-3 (`name = "sign-fixture"`, `version = "0.1.0"` —
      confirm this is NOT touched)
    - 111-RESEARCH.md § RLS-14 "Complete version-bump file inventory" (full section) — the exact
      file-and-line inventory, confirming `nono-fltmgr-client` is the 6th crate in the bump set and
      `sign-fixture` is explicitly excluded
  </read_first>
  <action>
    Change `version = "0.66.1"` to `version = "0.70.0"` in the `[package]` section of all 6
    workspace-member `Cargo.toml` files: `crates/nono/Cargo.toml`, `crates/nono-cli/Cargo.toml`,
    `crates/nono-proxy/Cargo.toml`, `crates/nono-shell-broker/Cargo.toml`, `bindings/c/Cargo.toml`,
    `crates/nono-fltmgr-client/Cargo.toml`. Also change the 6 internal path-dependency version
    requirements that pin `0.66.1` (these live inside 4 of the same files, already listed):
    `crates/nono-cli/Cargo.toml:47` (`nono = { version = "0.66.1", ... }`),
    `crates/nono-cli/Cargo.toml:48` (`nono-proxy = { version = "0.66.1", ... }`),
    `crates/nono-cli/Cargo.toml:180` (`nono-shell-broker = { path = "../nono-shell-broker", version
    = "0.66.1" }`), `crates/nono-proxy/Cargo.toml:21` (`nono = { version = "0.66.1", ... }`),
    `crates/nono-shell-broker/Cargo.toml:20` (`nono = { version = "0.66.1", ... }`),
    `bindings/c/Cargo.toml:18` (`nono = { version = "0.66.1", ... }`) — all become `"0.70.0"`.

    Do NOT touch `tools/sign-fixture/Cargo.toml` — it keeps `name = "sign-fixture"`,
    `version = "0.1.0"`, an independent CI-tooling versioning scheme unrelated to the product
    release train.

    Regenerate `Cargo.lock` via `cargo build --workspace --all-targets` (this both validates the
    workspace still compiles and refreshes the lockfile to the new versions).
  </action>
  <acceptance_criteria>
    - `cargo metadata --format-version 1 --no-deps` reports `"version": "0.70.0"` for exactly the
      6 packages named `nono-sandbox`, `nono-sandbox-cli`, `nono-sandbox-proxy`,
      `nono-shell-broker`, `nono-fltmgr-client`, `nono-ffi`.
    - `cargo metadata --format-version 1 --no-deps` reports `"version": "0.1.0"` for `sign-fixture`
      (unchanged).
    - `cargo build --workspace --all-targets` exits 0.
    - `git diff Cargo.lock` shows version-field changes only for entries whose `name` matches one
      of the 6 bumped packages — no unrelated third-party dependency version drift.
  </acceptance_criteria>
  <verify>
    <automated>cargo build --workspace --all-targets</automated>
  </verify>
  <done>All 6 in-repo crates + their path-dep pins report 0.70.0; sign-fixture is untouched; Cargo.lock diff is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the two hardcoded-version gate scripts + confirm release-readiness GREEN</name>
  <files>scripts/gates/release-readiness.ps1, scripts/release-dry-run.ps1</files>
  <read_first>
    - scripts/gates/release-readiness.ps1 lines 60-176 (the `Invoke-Gate` function; line 76
      `$targetVersion = '0.66.1'`, line 77 `$upstreamHighest = '0.66.0'`, lines 81-88
      `$versionFamilyCrates` array — already correct, do not touch)
    - scripts/release-dry-run.ps1 lines 1-33 (the `.SYNOPSIS`/`.DESCRIPTION` comment block: lines
      3, 20 reference `0.66.1`; line 85 comment, line 91 status-message string also reference it —
      these 4 occurrences are the file's only `0.66.1`/`0.66.0` matches, confirmed live via grep)
    - .planning/config.json — confirm this repo's `verify-dark.ps1`/`-Gate` invocation contract
      (dot-sourced `Test-Precondition`/`Invoke-Gate`, never calls `exit` itself)
    - 111-RESEARCH.md § RLS-14 "Two hardcoded-version-string gate scripts" and Pitfall 4 (full
      text — why this edit must land in the SAME wave as Task 1's version bump, not a later phase)
  </read_first>
  <action>
    In `scripts/gates/release-readiness.ps1`, change line 76 to
    `$targetVersion       = '0.70.0'` and line 77 to `$upstreamHighest     = '0.69.0'`. Leave the
    `$versionFamilyCrates` array (lines 81-88) untouched — it already correctly enumerates the 6
    fork-owned crate names and requires no change.

    In `scripts/release-dry-run.ps1`, update the 4 cosmetic `0.66.1`/`0.66.0` references: line 3
    (`.SYNOPSIS` — "Dry-run validation for all three publish paths at v0.66.1." → v0.70.0), line 20
    (`.DESCRIPTION` prose referencing "nono 0.66.1 has been published" → 0.70.0), line 85 (inline
    comment referencing `nono = ^0.66.1`), and line 91 (the status message string
    `"nono ^0.66.1 not yet on crates.io; re-run after publishing nono"` → the equivalent 0.70.0
    string). The regex match at line 88 (`'failed to select a version for the requirement'`) is
    version-string-agnostic and needs no edit.

    Then invoke `pwsh -File scripts/verify-dark.ps1 -Gate release-readiness` and confirm the
    verdict is PASS, with the `version_family` detail reading "0.70.0 confirmed for all 6
    version-family crates" and the `leapfrog` detail reading "0.70.0 > 0.69.0 (leapfrog
    confirmed)". This confirms only the in-repo-crate assertions this wave is responsible for —
    the full `release-dry-run.ps1` 3-leg dry-run (which also builds the sibling repos) is deferred
    to Plan 111-06 after those repos are bumped.
  </action>
  <acceptance_criteria>
    - `scripts/gates/release-readiness.ps1` line 76 reads `$targetVersion       = '0.70.0'` and
      line 77 reads `$upstreamHighest     = '0.69.0'`.
    - `grep -c "0.66.1" scripts/release-dry-run.ps1` returns 0 after the edit (all 4 cosmetic
      references updated, none left stale).
    - `pwsh -File scripts/verify-dark.ps1 -Gate release-readiness` returns verdict `PASS` with
      `version_family` and `leapfrog` details both referencing `0.70.0`/`0.69.0`.
    - No `cargo publish` (without `--dry-run`), `git tag`, or `git push --tags` command was
      executed by this task (D-07).
  </acceptance_criteria>
  <verify>
    <automated>pwsh -File scripts/verify-dark.ps1 -Gate release-readiness</automated>
  </verify>
  <done>Both gate scripts assert against 0.70.0/0.69.0; release-readiness reports PASS for the in-repo crate set; no tag pushed, no live publish executed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Version-string edit → release-gate assertion correctness | If `release-readiness.ps1`'s hardcoded version inputs are left stale, the gate silently asserts against the wrong target and could report PASS vacuously — the exact defect class Phase 110 hit with `has_port_rules()`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | Information Disclosure (incorrect security posture) | Not touched by this plan | accept | N/A to RLS-14. |
| T-111-02 | Denial of Service (resource exhaustion) | Not touched by this plan | accept | N/A to RLS-14. |
| T-111-03 | Spoofing (package-identity confusion during the multi-repo leapfrog) | `Cargo.toml` `[package] name`/`version` fields, `scripts/gates/release-readiness.ps1` | mitigate | Version bump stays entirely within the already fork-owned `nono-sandbox`/`nono-sandbox-cli`/`nono-sandbox-proxy`/`nono-shell-broker`/`nono-fltmgr-client`/`nono-ffi` identities (Phase 102) — no new identity is introduced; `release-readiness.ps1`'s corrected assertion inputs are the structural check that the version bump landed everywhere it should. |
</threat_model>

<verification>
- `cargo metadata --format-version 1 --no-deps` reports `0.70.0` for the 6 named crates, `0.1.0`
  for `sign-fixture`.
- `cargo build --workspace --all-targets` exits 0; `Cargo.lock` diff limited to the 6 bumped
  crates.
- `pwsh -File scripts/verify-dark.ps1 -Gate release-readiness` returns PASS against 0.70.0/0.69.0.
- No tag pushed; no live `cargo publish` executed.
</verification>

<success_criteria>
- All 6 in-repo version-family crates report `0.70.0`; `sign-fixture` stays `0.1.0`.
- `release-readiness.ps1` asserts against the real target, not the stale `0.66.1`/`0.66.0`.
- D-07 upheld: prepare-only, no push, no live publish.
</success_criteria>

<output>
Create `.planning/phases/111-core-carry-resource-cli-verify-release-leapfrog/111-05-SUMMARY.md` when done
</output>
