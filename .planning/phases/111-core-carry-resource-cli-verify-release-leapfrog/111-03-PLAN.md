---
phase: 111-core-carry-resource-cli-verify-release-leapfrog
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - proj/ADR-111-resource-limits-boundary.md
  - .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md
autonomous: true
requirements: [CORE-02]

must_haves:
  truths:
    - "D-01: proj/ADR-111-resource-limits-boundary.md exists and settles ADAPT-not-adopt — the fork rejects upstream e6d26871's (#1269) core-module absorb (`pub mod resource;` + `pub use resource::ResourceLimits;` on crates/nono/src/lib.rs) and keeps all resource-limit code CLI-side, aligning only flag names/help/semantics."
    - "D-03: ADR-111 mirrors proj/ADR-108-deny-domain-posture.md's shape (Status/Phase/Date/Authors header, Context, Decision, Consequences, References) and explicitly answers why ADR-86's audit/diagnostics carve-out does NOT extend to resource limits."
    - "D-02: 108-DIVERGENCE-LEDGER.md carries an explicit standing-divergence entry for e6d26871 (#1269) and its sibling 34c2c975 (#1403), recording that this is a PERMANENT divergence (not a deferral to a future phase, unlike the tool-sandbox subsystem's v3.7 DEFERRED treatment)."
  artifacts:
    - path: "proj/ADR-111-resource-limits-boundary.md"
      provides: "The standalone, citable ADR settling CORE-02's library-boundary disposition"
    - path: ".planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md"
      provides: "A new Standing Divergence addendum section recording e6d26871/#1269 + 34c2c975/#1403 as permanently ADAPTed, not adopted"
  key_links:
    - from: "proj/ADR-111-resource-limits-boundary.md"
      to: "proj/ADR-86-library-boundary-convergence.md"
      via: "explicit argument for why the audit/diagnostics carve-out does not extend to resource limits"
      pattern: "ADR-86"
    - from: ".planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md (Standing Divergence addendum)"
      to: "proj/ADR-111-resource-limits-boundary.md"
      via: "cross-reference — the ledger entry points at the ADR as the authoritative disposition"
      pattern: "ADR-111"
---

<objective>
Write the two pure-documentation artifacts CORE-02's library-boundary decision requires:
`proj/ADR-111-resource-limits-boundary.md` (D-03), which settles and records the reasoning behind
D-01's ADAPT-not-adopt disposition of upstream's core `resource` module, and a standing-divergence
addendum in `108-DIVERGENCE-LEDGER.md` (D-02), so the next upstream sync finds a documented
decision instead of re-litigating this one from scratch.

Purpose: the Phase 108 ledger already flagged `e6d26871`'s `pub mod resource;` /
`pub use resource::ResourceLimits;` addition as a genuine cross-crate re-export crossing the
ADR-86 library boundary (Threat Flags table, "threat_flag: cross-crate-reexport"). Without a
durable artifact, that flag is a dangling TODO. This plan closes it.
Output: One new ADR file; one new section appended to an already-"closed" ledger (an explicit,
locked exception per D-02 — see Task 2's action for how this is reconciled with the ledger's own
"Ledger closed" declaration).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-core-carry-resource-cli-verify-release-leapfrog/111-CONTEXT.md
@.planning/phases/111-core-carry-resource-cli-verify-release-leapfrog/111-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write proj/ADR-111-resource-limits-boundary.md</name>
  <files>proj/ADR-111-resource-limits-boundary.md</files>
  <read_first>
    - proj/ADR-108-deny-domain-posture.md (full file) — the shape to mirror: `# ADR-NNN: Title`,
      Status/Phase/Date/Authors header, `## Context`, `## Fork Touchpoint Map` (optional),
      `## Options Considered` (Option A / Option B with Pros/Cons), `## Decision`,
      `## Consequences`, `## References`
    - proj/ADR-86-library-boundary-convergence.md lines 1-60 (the audit/diagnostics carve-out
      rationale — ADR-111 must explain why that same reasoning does NOT extend to resource limits)
    - .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md lines 767-800 (CORE
      Cluster per-commit table, the `e6d26871` row's "adapt" disposition and its
      "threat_flag: cross-crate-reexport" entry at ~line 805 — the exact upstream diff shape:
      `pub mod resource;` + `pub use resource::ResourceLimits;` added to `crates/nono/src/lib.rs`)
    - 111-CONTEXT.md § "CORE-02 — where ResourceLimits lives" (D-01 through D-04, full rationale
      already drafted by the user — do not re-derive from scratch, cite this reasoning directly)
  </read_first>
  <action>
    Create `proj/ADR-111-resource-limits-boundary.md` mirroring `ADR-108`'s structure:
    - Header: `# ADR-111: Resource-Limit CLI Surface — Reject the Core-Module Absorb`, Status
      Accepted, Phase 111, today's date, Authors "Phase 111 execution".
    - `## Context`: cite upstream `e6d26871f0498e7dc7a867e67af5c6136b84f91c` (#1269, "feat:
      resource limiting") adding `pub mod resource;` + `pub use resource::ResourceLimits;` to the
      policy-free core `crates/nono/src/lib.rs`, and its sibling `34c2c975d649844923cf1515be94de624c689c6c`
      (#1403, `--max-processes`) which extends the same module. State that the fork already ships
      all four resource limits (`--cpu-percent`/`--memory`/`--timeout`/`--max-processes`)
      kernel-enforced on three platforms (Job Object/Windows, cgroup v2/Linux,
      `RLIMIT_AS`+`RLIMIT_NPROC`/macOS) via CLI-side code, materially more complete than upstream's
      un-enforced core module.
    - `## Decision`: ADAPT, not adopt (D-01). `crates/nono/src/lib.rs` will NOT gain
      `pub mod resource;` or any resource-limit type; all resource-limit code stays
      `nono-cli`-side. Only flag names/help text/semantics are aligned with upstream where they
      match (D-04: no renames were needed this phase — both upstream commits spell the four flags
      identically to the fork).
    - `## Why the ADR-86 audit/diagnostics carve-out does NOT extend here` (this is the answer
      ADR-111 must give, per CONTEXT.md's "Specific Ideas"): ADR-86's Cluster A/B carve-out
      (audit ledger + structured diagnostics) was justified because those are pure observability
      primitives carrying no enforcement or policy semantics — the library only records/formats
      facts, it does not decide or enforce anything. Resource limits are the opposite: they ARE
      the enforcement mechanism (kernel resource caps), sitting on the policy side of the ADR-86
      boundary line, not the observability side. Relocating already-working, security-critical,
      tested enforcement code into the core library for cosmetic upstream-convergence buys nothing
      functional and risks introducing exactly the restructuring-induced regression CORE-02
      explicitly forbids.
    - `## Consequences`: (a) standing divergence recorded in `108-DIVERGENCE-LEDGER.md` (D-02,
      Task 2 of this plan); (b) every future upstream sync touching the `resource` module in
      `always-further/nolabs-ai`'s `nono` core crate must re-affirm this decision rather than treat
      the fork's non-adoption as a gap; (c) the corrected help text (Plan 111-02) is the only
      user-visible change this decision produces.
    - `## References`: this ADR's own upstream commits; `proj/ADR-86-library-boundary-convergence.md`;
      `proj/ADR-108-deny-domain-posture.md` (structural precedent); the ledger's CORE cluster table
      and Threat Flags entry.
  </action>
  <acceptance_criteria>
    - `proj/ADR-111-resource-limits-boundary.md` exists with all 5 sections named above.
    - The file explicitly states `crates/nono/src/lib.rs` will not gain `pub mod resource;`.
    - The file explicitly answers why ADR-86's audit/diagnostics carve-out does not extend to
      resource limits (not merely asserts the conclusion).
    - The file cites both upstream SHAs (`e6d26871f0498e7dc7a867e67af5c6136b84f91c`,
      `34c2c975d649844923cf1515be94de624c689c6c`) verbatim.
  </acceptance_criteria>
  <verify>
    <automated>test -f proj/ADR-111-resource-limits-boundary.md && grep -q 'e6d26871f0498e7dc7a867e67af5c6136b84f91c' proj/ADR-111-resource-limits-boundary.md && grep -q '34c2c975d649844923cf1515be94de624c689c6c' proj/ADR-111-resource-limits-boundary.md && grep -q 'pub mod resource' proj/ADR-111-resource-limits-boundary.md</automated>
    <manual>Whether the ADR actually ANSWERS why ADR-86's audit/diagnostics carve-out does not extend to resource limits — rather than merely asserting the conclusion — is editorial and must be read.</manual>
  </verify>
  <done>ADR-111 exists, mirrors ADR-108's shape, and settles the disposition with reasoning, not just a conclusion.</done>
</task>

<task type="auto">
  <name>Task 2: Record the standing divergence in 108-DIVERGENCE-LEDGER.md</name>
  <files>.planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</files>
  <read_first>
    - .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md — the final ~10 lines
      of the file (the "### ROADMAP.md Phase 108 Success Criteria — final disposition" section
      ending in the sentence "**Ledger closed.** All 5 plans in Phase 108 ... have now contributed
      to this document; no further plan is expected to append to `108-DIVERGENCE-LEDGER.md`.")
    - .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md lines 486-508 (the
      `tool-sandbox-surface` cluster's `DEFERRED->v3.7` treatment — the precedent style this new
      entry partially mirrors, while being explicit that THIS divergence is permanent, not a
      deferral)
    - Task 1's `proj/ADR-111-resource-limits-boundary.md` (produced by this same plan)
  </read_first>
  <action>
    At the very end of `108-DIVERGENCE-LEDGER.md`, immediately after the existing "**Ledger
    closed.**" sentence, append a new section `## Phase 111 Standing Divergence Addendum` stating:
    despite the "Ledger closed" declaration above, CONTEXT.md's Phase 111 D-02 directs one
    explicit exception. Record `e6d26871f0498e7dc7a867e67af5c6136b84f91c` (#1269, "feat: resource
    limiting") and its sibling `34c2c975d649844923cf1515be94de624c689c6c` (#1403, `--max-processes`)
    as a **STANDING DIVERGENCE**: the fork ADAPTS (does not ADOPT) upstream's core-module
    relocation, per `proj/ADR-111-resource-limits-boundary.md`'s Decision —
    `crates/nono/src/lib.rs` will never gain `pub mod resource;` / `pub use resource::ResourceLimits;`,
    and every future upstream sync touching this area must re-affirm this decision rather than
    treat the module's absence as an unabsorbed gap. Explicitly distinguish this from the
    `tool-sandbox-surface` cluster's `DEFERRED->v3.7` treatment elsewhere in this ledger: that is a
    deferral with a named future phase to pick it back up; this is a **permanent** divergence with
    no future phase this decision hands off to. Amend the closing sentence itself (or add a
    footnote immediately below it) to note this addendum is the one deliberate, locked exception to
    "no further plan is expected to append."
  </action>
  <acceptance_criteria>
    - `108-DIVERGENCE-LEDGER.md` contains a `## Phase 111 Standing Divergence Addendum` section
      after the "Ledger closed" sentence.
    - The new section names both upstream SHAs and cites `proj/ADR-111-resource-limits-boundary.md`
      by path.
    - The new section explicitly distinguishes "permanent divergence" from the tool-sandbox
      subsystem's "deferred to v3.7" treatment.
  </acceptance_criteria>
  <verify>
    <automated>grep -q '## Phase 111 Standing Divergence Addendum' .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md && grep -q 'proj/ADR-111-resource-limits-boundary.md' .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</automated>
    <manual>Whether the addendum genuinely distinguishes "permanent divergence" from the tool-sandbox cluster's "deferred to v3.7" treatment is editorial and must be read.</manual>
  </verify>
  <done>The ledger records the standing divergence, reconciled explicitly with its own "closed" declaration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A — this plan is pure documentation | No code, no runtime input, no build artifact is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | Information Disclosure (incorrect security posture) | N/A — no help text or user-facing behavior touched by this plan | accept | Plan 111-02 owns the actual help-text fix; this plan only documents the library-boundary decision. |
| T-111-02 | Denial of Service (resource exhaustion) | N/A — no enforcement code touched | accept | Documentation-only plan. |
| T-111-03 | Spoofing (package-identity confusion) | N/A — no version/package-identity change | accept | RLS-14's version leapfrog is Plan 111-05/111-06's scope. |
</threat_model>

<verification>
- `proj/ADR-111-resource-limits-boundary.md` exists and contains all required sections.
- `108-DIVERGENCE-LEDGER.md`'s new addendum section exists and cross-references the ADR.
</verification>

<success_criteria>
- D-01, D-02, and D-03 are all durably recorded in citable artifacts, not just in CONTEXT.md.
- The next upstream sync auditor who encounters `e6d26871`/`34c2c975` finds a documented decision,
  not a surprise.
</success_criteria>

<output>
Create `.planning/phases/111-core-carry-resource-cli-verify-release-leapfrog/111-03-SUMMARY.md` when done
</output>
