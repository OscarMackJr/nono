---
phase: 52-field-validation-closure-heavy-runtime-human-uat-doc-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/52-field-validation-closure-heavy-runtime-human-uat-doc-update/52-HUMAN-UAT.md
autonomous: false
requirements:
  - REQ-WSRH-04
  - REQ-WSRH-06

must_haves:
  truths:
    - "nono run --profile claude-code -- cmd /c 'echo hi' launches, prints 'hi', exits 0 on a Windows 11 host (repro A passes — no regression)"
    - "nono run --profile claude-code -- claude --version launches the ~234 MB self-contained claude.exe, prints the version string, exits 0, with no 0xC0000142 dialog (repro B passes — fix confirmed)"
    - "Both verdicts are recorded in 52-HUMAN-UAT.md with exact commands, full stdout, exit code, no-0xC0000142-dialog confirmation, binary identity, host Windows build number, and timestamps"
  artifacts:
    - path: ".planning/phases/52-field-validation-closure-heavy-runtime-human-uat-doc-update/52-HUMAN-UAT.md"
      provides: "Timestamped operator-attested evidence record for repro A and repro B"
      contains: "repro A: PASS, repro B: PASS"
  key_links:
    - from: "52-HUMAN-UAT.md repro B verdict"
      to: "52-02-PLAN.md doc-update gate"
      via: "D-52-03 hard-stop sequencing — doc update only runs if repro B: PASS"
      pattern: "repro B.*PASS"
---

<objective>
Execute the Phase 52 HUMAN-UAT reproduction matrix on a real Windows 11 host and record
timestamped verdicts in 52-HUMAN-UAT.md. This plan closes the ROADMAP SC-4 positive-spawn
deferral from Phase 51 and gates the entire doc-update and VERIFICATION close.

Purpose: Confirm on live hardware that the Phase 51 BrokerLaunchNoPty arm eliminates
0xC0000142 for the heavy self-contained claude.exe while leaving the plain console-app
path (cmd /c echo hi) fully intact.

Output: 52-HUMAN-UAT.md with PASS or FAIL verdicts for repro A (no-regression) and
repro B (heavy-runtime fix). If repro B records FAIL, this plan routes to /gsd:debug
and the milestone stays blocked — no doc claim is shipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-field-validation-closure-heavy-runtime-human-uat-doc-update/52-CONTEXT.md
@.planning/phases/51-no-pty-low-il-broker-token-routing-write-deny-preservation/51-HUMAN-UAT.md
@.planning/phases/51-no-pty-low-il-broker-token-routing-write-deny-preservation/51-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Prepare the UAT environment on the Windows 11 host</name>

  <read_first>
    .planning/phases/52-field-validation-closure-heavy-runtime-human-uat-doc-update/52-CONTEXT.md
    (read D-52-01, D-52-02, D-52-07 in full before touching the host)
    .planning/phases/51-no-pty-low-il-broker-token-routing-write-deny-preservation/51-HUMAN-UAT.md
    (read the Phase 27 launch-path-gate note — this is why working directory matters)
    docs/cli/development/windows-poc-handoff.mdx §§ "Working directory choice (Windows)", "Step 2 — Stage on the test machine"
  </read_first>

  <what-built>
    This task has no automation to run beforehand — it is a human setup step.
  </what-built>

  <how-to-verify>
    On the Windows 11 test host, in a fresh PowerShell window, perform the following
    pre-flight steps in order. Record the output of each command in the UAT note section.

    1. Confirm the nono binary is the Phase 51 build (0.57.0 or later):
         nono --version
       Expected: nono 0.57.x (or later). If the binary is pre-Phase-51, rebuild and
       re-stage before proceeding — a pre-fix binary will not exercise the BrokerLaunchNoPty arm.

    2. Confirm claude.exe is the heavy self-contained build (~234 MB, 2026-05-24 or newer):
         claude --version
         (Get-Item (Get-Command claude).Source).Length / 1MB
       Expected: version string printed; file size approximately 234 MB or larger. Record
       the exact version string and the exact file size in the UAT note — these are the
       binary identity fields required by D-52-02.

    3. Navigate to the USERPROFILE working directory (per D-52-01 — avoids the Phase 27
       launch-path policy gate that fires on C:\ drive-root paths):
         cd $env:USERPROFILE

    4. Confirm the working directory passes the label-apply gate:
         nono run --dry-run --profile claude-code -- cmd /c "echo preflight-ok"
       Expected output shape includes the capability list and the dry-run notice. If this
       prints a WRITE_OWNER error, the working directory is wrong — re-read the
       "Working directory choice" section of the handoff doc.
  </how-to-verify>

  <resume-signal>
    Type "preflight-ok" with the nono version, claude version, and claude.exe file size
    recorded, and confirm the dry-run ran without a WRITE_OWNER error.
  </resume-signal>

  <acceptance_criteria>
    - Operator confirms nono binary is Phase 51 build (0.57.0+)
    - Operator records the exact claude --version output string (binary identity per D-52-02)
    - Operator records the claude.exe file size in MB (must be approximately 234 MB or larger)
    - Operator is at $env:USERPROFILE as the working directory
    - dry-run of repro A shape prints the capability list without a WRITE_OWNER diagnostic
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Execute reproduction matrix and record verdicts in 52-HUMAN-UAT.md</name>

  <read_first>
    .planning/phases/52-field-validation-closure-heavy-runtime-human-uat-doc-update/52-CONTEXT.md
    (re-read D-52-01, D-52-03, D-52-04, D-52-07 — these define the exact commands,
    the strict pass boundary, the hard-stop on FAIL, and the evidence to capture)
    .planning/phases/51-no-pty-low-il-broker-token-routing-write-deny-preservation/51-HUMAN-UAT.md
    (the deferred SC-4 test this plan closes — repro B is that test)
  </read_first>

  <what-built>
    No automation precedes this checkpoint. The reproduction matrix is purely human-run
    on a Windows 11 host with the Phase 51 binary.
  </what-built>

  <how-to-verify>
    From $env:USERPROFILE on the Windows 11 host (pre-flighted in Task 1), execute
    the following two commands in order and observe all output. After both runs, create
    .planning/phases/52-field-validation-closure-heavy-runtime-human-uat-doc-update/52-HUMAN-UAT.md
    with the evidence schema below.

    --- REPRO A (no-regression: plain console app) ---
    Command (run exactly as written):
      nono run --profile claude-code -- cmd /c "echo hi"

    Pass criteria (all three must hold — D-52-04 strict boundary):
      1. "hi" is printed to stdout (exact word, no other errors)
      2. Process exits 0
      3. No 0xC0000142 dialog appears

    Record: exact command, full stdout, exit code ($LASTEXITCODE), and "no 0xC0000142
    dialog" or description of any dialog that appeared, plus timestamp.

    --- REPRO B (heavy-runtime fix: self-contained claude.exe) ---
    Command (run exactly as written, from the same $env:USERPROFILE working directory):
      nono run --profile claude-code -- claude --version

    Pass criteria (all three must hold — D-52-04 strict boundary; D-52-03 hard stop on any FAIL):
      1. The Claude version string is printed to stdout (e.g., "claude 1.x.x")
      2. Process exits 0
      3. No 0xC0000142 dialog appears (no "STATUS_DLL_INIT_FAILED" crash dialog, no
         Windows Error Reporting dialog for the child process)

    Record: exact command, full stdout, exit code ($LASTEXITCODE), explicit statement
    "no 0xC0000142 dialog" or description of any crash dialog that appeared,
    the binary identity from Task 1 (claude --version output + file size), the host
    Windows build number (run: (Get-CimInstance Win32_OperatingSystem).BuildNumber),
    and timestamp.

    --- IF REPRO B FAILS ---
    If any of the three repro B pass criteria does not hold, record FAIL with all
    available diagnostic context (full stdout, full stderr, exit code, any crash
    dialog text, ProcMon or Windows Event Viewer output if available), then STOP.
    Do NOT write a PASS verdict. Do NOT proceed to Plan 02. Open /gsd:debug — the
    Phase 51 implementation is insufficient and the milestone stays blocked.

    --- CREATING 52-HUMAN-UAT.md ---
    Create the file with this schema (fill all fields; use "PASS" or "FAIL" for verdict):

      ---
      status: complete
      phase: 52-field-validation-closure-heavy-runtime-human-uat-doc-update
      started: "<ISO-8601 timestamp>"
      updated: "<ISO-8601 timestamp>"
      ---

      ## Tests

      ### Repro A — plain console app (no-regression)

      command: nono run --profile claude-code -- cmd /c "echo hi"
      working_dir: $env:USERPROFILE
      stdout: <full stdout>
      exit_code: <N>
      no_0xC0000142_dialog: <yes / no — if no, describe what appeared>
      timestamp: <ISO-8601>
      verdict: PASS | FAIL

      ### Repro B — heavy-runtime (0xC0000142 fix confirmation)

      command: nono run --profile claude-code -- claude --version
      working_dir: $env:USERPROFILE
      stdout: <full stdout>
      exit_code: <N>
      no_0xC0000142_dialog: <yes / no — if no, describe what appeared>
      binary_identity:
        claude_version_string: <exact claude --version output>
        claude_exe_size_mb: <N> MB (confirmed >= 234 MB self-contained build per D-52-02)
      host_windows_build: <BuildNumber from (Get-CimInstance Win32_OperatingSystem).BuildNumber>
      timestamp: <ISO-8601>
      verdict: PASS | FAIL

      ## Summary

      total: 2
      passed: <N>
      failed: <N>
      deferred_sc4_closed: <true if repro B is PASS; false otherwise>

      ## Phase 51 SC-4 Closure Note

      [Record whether this plan closes the ROADMAP SC-4 deferral from 51-HUMAN-UAT.md.
      SC-4 is closed when repro B verdict is PASS. If FAIL, SC-4 remains open and
      /gsd:debug is required before proceeding.]
  </how-to-verify>

  <resume-signal>
    Type "uat-complete" and state the repro B verdict (PASS or FAIL). If FAIL, describe
    the failure mode observed. The plan coordinator will route to Plan 02 on PASS, or
    to /gsd:debug on FAIL per D-52-03.
  </resume-signal>

  <acceptance_criteria>
    - 52-HUMAN-UAT.md exists at .planning/phases/52-field-validation-closure-heavy-runtime-human-uat-doc-update/52-HUMAN-UAT.md
    - Repro A section records: exact command, full stdout containing "hi", exit_code 0, "no 0xC0000142 dialog: yes", ISO-8601 timestamp, verdict PASS
    - Repro B section records: exact command, full stdout containing the claude version string, exit_code 0, "no 0xC0000142 dialog: yes", binary identity (claude --version output + file size >= 234 MB), host Windows build number, ISO-8601 timestamp, verdict PASS
    - If repro B verdict is FAIL: file records FAIL verdict with full diagnostic context; plan routes to /gsd:debug; Plan 02 does NOT proceed
    - deferred_sc4_closed is true when repro B is PASS
    - Summary counts are accurate
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator attestation → UAT record | Human records evidence manually; fabricated verdicts would let a broken fix ship |
| binary identity | Validating against a thin/old claude.exe would yield a false positive (heavy DllMain not triggered) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52A-01 | Tampering | 52-HUMAN-UAT.md operator attestation | mitigate | Task 2 requires recording exact stdout + binary identity (D-52-02 ~234 MB size check); thin binary false positive is structurally prevented by the size gate |
| T-52A-02 | Information Disclosure | No new code or attack surface | accept | Phase ships no code; no new information-disclosure surface introduced |
| T-52A-03 | Denial of Service | UAT blocked by wrong working directory | mitigate | Task 1 preflight validates the working directory via dry-run before live repro runs; D-52-01 USERPROFILE requirement avoids Phase 27 launch-path gate |
| T-52A-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan; no slopcheck needed |
</threat_model>

<verification>
The plan is complete when 52-HUMAN-UAT.md exists with:
  - repro A verdict: PASS (no-regression confirmed)
  - repro B verdict: PASS (0xC0000142 fix confirmed on real Windows 11 host with ~234 MB claude.exe)
  - binary identity, host build number, and timestamps recorded per D-52-07
  - Phase 51 SC-4 closure note present

If repro B is FAIL, the plan ends here. Route to /gsd:debug. Milestone blocked.
</verification>

<success_criteria>
- 52-HUMAN-UAT.md records repro A: exact command, stdout "hi", exit 0, no 0xC0000142 dialog, timestamp, verdict PASS
- 52-HUMAN-UAT.md records repro B: exact command, claude version string in stdout, exit 0, no 0xC0000142 dialog, binary identity (claude --version + ~234 MB), host Windows build number, timestamp, verdict PASS
- The Phase 51 ROADMAP SC-4 positive-spawn deferral is closed (deferred_sc4_closed: true in the summary)
- Repro B FAIL → /gsd:debug route, Plan 02 blocked
</success_criteria>

<output>
Create .planning/phases/52-field-validation-closure-heavy-runtime-human-uat-doc-update/52-01-SUMMARY.md when done.
</output>
