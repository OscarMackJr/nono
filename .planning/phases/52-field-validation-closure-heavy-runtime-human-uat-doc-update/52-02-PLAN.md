---
phase: 52-field-validation-closure-heavy-runtime-human-uat-doc-update
plan: 02
type: execute
wave: 2
depends_on:
  - "52-01"
files_modified:
  - docs/cli/development/windows-poc-handoff.mdx
  - .planning/phases/52-field-validation-closure-heavy-runtime-human-uat-doc-update/52-VERIFICATION.md
autonomous: false
requirements:
  - REQ-WSRH-06

must_haves:
  truths:
    - "windows-poc-handoff.mdx no longer claims nono run is universally WRITE_RESTRICTED + per-session WFP — the doc is now profile-conditional"
    - "The doc states that nono run --profile claude-code routes through the Low-IL broker (NO_WRITE_UP mandatory label, NOT WRITE_RESTRICTED; per-session WFP differentiation waived, falls back to AppID-based filtering)"
    - "Other profiles (codex / opencode / openclaw / swival) are documented as keeping WRITE_RESTRICTED + per-session WFP"
    - "A new heavy-runtime children subsection explains that self-contained heavy executables (e.g. claude.exe) are now supported under the claude-code profile"
    - "A dated 'validated <date>, build <N>' line appears in the doc (D-52-08)"
    - "52-VERIFICATION.md is closed with status: pass"
    - "The doc commit uses git add -f docs/cli/development/windows-poc-handoff.mdx"
  artifacts:
    - path: "docs/cli/development/windows-poc-handoff.mdx"
      provides: "Updated POC handoff doc with profile-conditional security envelope and heavy-runtime subsection"
      contains: "Low-IL broker"
    - path: ".planning/phases/52-field-validation-closure-heavy-runtime-human-uat-doc-update/52-VERIFICATION.md"
      provides: "Phase 52 verification record"
      contains: "status: pass"
  key_links:
    - from: "52-HUMAN-UAT.md repro B verdict: PASS"
      to: "docs/cli/development/windows-poc-handoff.mdx heavy-runtime support claim"
      via: "D-52-03 gate — doc claim is published only after live confirmation"
      pattern: "heavy-runtime.*supported|Low-IL broker"
    - from: "launch.rs lines 1205, 1214-1215"
      to: "doc per-session WFP waiver claim"
      via: "D-52-05 factual basis — BrokerLaunchNoPty arm comment is the ground truth"
      pattern: "FWPM_CONDITION_ALE_USER_ID.*waived|per-session.*waived"
---

<objective>
Perform the D-52-05/D-52-06 consistency sweep of windows-poc-handoff.mdx, add the
heavy-runtime children subsection, and close 52-VERIFICATION.md with status: pass.
This plan runs ONLY when 52-HUMAN-UAT.md records repro B: PASS. If repro B failed,
stop — do not edit the doc or close verification.

Purpose: Make the operator doc accurate and honest about the security envelope after
the Phase 51 BrokerLaunchNoPty change. An operator who believes per-session WFP is
active for the claude-code profile when it is waived could make an unsafe trust decision —
therefore doc accuracy is a security property, not just a quality concern.

Output:
  - docs/cli/development/windows-poc-handoff.mdx updated (4 anchored edits + new subsection)
  - 52-VERIFICATION.md with status: pass
  - git commit (using git add -f for the gitignored-but-tracked doc path)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-field-validation-closure-heavy-runtime-human-uat-doc-update/52-CONTEXT.md
@.planning/phases/52-field-validation-closure-heavy-runtime-human-uat-doc-update/52-HUMAN-UAT.md
@docs/cli/development/windows-poc-handoff.mdx

<interfaces>
<!-- Key code-level fact the doc edit must reflect. Read-only — do not edit this file. -->
<!-- From crates/nono-cli/src/exec_strategy_windows/launch.rs -->
<!-- Lines 1101-1113: BrokerLaunchNoPty variant documentation -->
<!--   BrokerLaunchNoPty — Phase 51 D-06: non-PTY supervised launch via nono-shell-broker.exe -->
<!--   Token construction identical to BrokerLaunch: null h_token, broker self-degrades to Low IL -->
<!--   Mandatory-label NO_WRITE_UP write-deny preserved via MIC pre-DACL kernel check (REQ-WSRH-05) -->
<!-- Lines 1205, 1214-1215: per-session WFP waiver comment on the BrokerLaunchNoPty / detached path -->
<!--   "Per-session WFP differentiation via FWPM_CONDITION_ALE_USER_ID is waived on this path -->
<!--    (falls back to AppID-based filtering, same as Phase 15 detached-path waiver)" -->
<!-- Lines 1120-1131: cascade rule 3 — prefers_low_il_broker && has_session_sid → BrokerLaunchNoPty -->
<!--   Only fires when the profile sets windows_low_il_broker: true (currently: claude-code only) -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision">
  <name>Task 1: Gate check — confirm repro B PASS before editing</name>

  <read_first>
    .planning/phases/52-field-validation-closure-heavy-runtime-human-uat-doc-update/52-HUMAN-UAT.md
    (read the repro B verdict field; if it is not PASS, do not proceed)
  </read_first>

  <decision>
    Is 52-HUMAN-UAT.md present and does it record repro B verdict: PASS (all three of
    D-52-04 must hold: no 0xC0000142 dialog AND version string printed AND exit 0)?
  </decision>

  <context>
    D-52-03 hard-stop: if repro B did not pass all three criteria, this plan must not
    proceed. The "heavy-runtime supported" doc claim is gated on live confirmation.
    Shipping the claim without the evidence would misrepresent the security envelope.
  </context>

  <options>
    <option id="proceed">
      <name>Proceed — repro B is PASS in 52-HUMAN-UAT.md</name>
      <pros>Evidence exists; doc claim is grounded; VERIFICATION can close</pros>
      <cons>None</cons>
    </option>
    <option id="stop">
      <name>Stop — repro B is FAIL or 52-HUMAN-UAT.md is absent</name>
      <pros>Fail-secure; no misleading doc claim shipped</pros>
      <cons>Milestone remains blocked until /gsd:debug resolves the failure</cons>
    </option>
  </options>

  <resume-signal>
    Select: proceed (repro B PASS confirmed), or stop (repro B failed — route to /gsd:debug)
  </resume-signal>

  <acceptance_criteria>
    - If proceed: 52-HUMAN-UAT.md is confirmed to exist with repro B verdict: PASS
    - If stop: no edits are made; operator opens /gsd:debug; Plan 02 ends here
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Apply D-52-05/06 consistency sweep to windows-poc-handoff.mdx</name>

  <read_first>
    docs/cli/development/windows-poc-handoff.mdx (read in full before editing — line numbers are advisory; verify current anchors before editing)
    .planning/phases/52-field-validation-closure-heavy-runtime-human-uat-doc-update/52-CONTEXT.md (D-52-05, D-52-06, D-52-08)
    .planning/phases/52-field-validation-closure-heavy-runtime-human-uat-doc-update/52-HUMAN-UAT.md (the validated date and build number for D-52-08)
    crates/nono-cli/src/exec_strategy_windows/launch.rs (lines 1100-1131 and 1205-1216 — read-only; verify factual basis before writing any claim)
  </read_first>

  <action>
    Edit docs/cli/development/windows-poc-handoff.mdx to apply the four anchored fixes
    and add the new heavy-runtime subsection. All edits use the Edit tool. No fenced code
    blocks in this action.

    VERIFY CURRENT LINE NUMBERS BEFORE EDITING — the numbers below are advisory (from
    CONTEXT.md). Read the file and locate each anchor by content, not line number.

    --- ANCHOR 1: line ~594 (primary nono run envelope claim) ---
    Locate the sentence that reads approximately:
      "For non-TUI commands, nono run --profile claude-code -- <command> remains the
      supported invocation (anonymous-pipe stdio; WRITE_RESTRICTED + per-session SID;
      full WFP per-session differentiation)."

    Replace with a profile-conditional statement that:
      - States nono run behavior is now profile-dependent (not universal)
      - For the claude-code profile: anonymous-pipe stdio; Low-IL broker (NO_WRITE_UP
        mandatory label, NOT WRITE_RESTRICTED); per-session WFP differentiation waived
        (falls back to AppID-based filtering — same waiver as the nono shell broker path
        documented at line 573)
      - For other profiles (codex / opencode / openclaw / swival): WRITE_RESTRICTED +
        per-session SID; full WFP per-session differentiation (unchanged behavior)
      - Cross-reference the new heavy-runtime children subsection (use an anchor link)

    --- ANCHOR 2: line ~573 (shell-path cross-ref) ---
    Locate the sentence that reads approximately:
      "Network differentiation per-session via WFP FWPM_CONDITION_ALE_USER_ID is waived
      on this path (broker→Low-IL child); falls back to AppID-based filtering (same as
      Phase 15 detached-path waiver). For full per-session network filtering (granular
      --allow-domain differentiation across concurrent sessions of the same binary), use
      nono run (non-PTY supervised path retains WRITE_RESTRICTED + per-session SID)."

    Update the final parenthetical to be profile-aware: nono run with the claude-code
    profile now also uses the Low-IL broker path with the same per-session-WFP waiver;
    per-session SID + full WFP differentiation is retained for other profiles (codex /
    opencode / openclaw / swival).

    --- ANCHOR 3: line ~544 (claude --version "works fine through pipes" framing) ---
    Locate the sentence in the "Known limitation: nono run cannot host TUI agents" section
    that reads approximately:
      "Non-interactive children (cmd /c echo, cargo build, python -c '...', claude --version)
      work fine through pipes."

    Update to note that as of Phase 52, claude --version (the heavy self-contained build)
    now works via the nono run --profile claude-code path specifically, not just through
    generic anonymous pipes — this is the positive result the phase confirms. Do NOT weaken
    or remove the TUI-limitation section header or body (line 542 scope is out of bounds);
    only update the inline claude --version framing. A cross-reference to the new
    heavy-runtime subsection is appropriate.

    --- ANCHOR 4: smoke-checklist table rows (lines ~615-616) ---
    Locate the "Quick verification" and "Non-TUI commands" rows in the Step 6 POC user
    handoff table. Update:
      - "Quick verification" row: nono run --profile claude-code -- claude --version now
        exercises the Low-IL broker path and confirms heavy-runtime support, not just the
        generic sandbox + profile path. Update the description to reflect this.
      - "Non-TUI commands" row: note that nono run with the claude-code profile uses the
        Low-IL broker path; for other profiles, WRITE_RESTRICTED + per-session WFP applies.

    --- NEW SUBSECTION: heavy-runtime children ---
    Add a subsection within or immediately following the "Windows nono shell — security
    envelope" section (or as a new subsection of "Step 5 — Smoke checklist", whichever
    flows better editorially — this is Claude's discretion per the Decisions section).

    The subsection must convey:
      - What "heavy-runtime children" means: self-contained executables with embedded
        runtimes (e.g. the ~234 MB claude.exe single-exe build) whose DllMain/bootstrap
        performs WRITE-type operations (NtCreateSection SECTION_MAP_WRITE on named objects,
        temp DLL extraction) that the WriteRestricted synthetic restricting SID blocked.
      - How Phase 51 fixes this: the claude-code profile now sets windows_low_il_broker:
        true, routing nono run through the BrokerLaunchNoPty arm — a Low-IL primary token
        with no synthetic restricting SID — so the WRITE-type DllMain activity succeeds.
      - The security tradeoff (per D-52-05): write-deny is preserved via the NO_WRITE_UP
        mandatory label (kernel MIC pre-DACL check), not WRITE_RESTRICTED; per-session WFP
        differentiation is waived (falls back to AppID-based filtering, same as the nono
        shell broker path). Other profiles retain WRITE_RESTRICTED + per-session WFP.
      - Validated date and build number (per D-52-08): add a line in the style of the
        Phase 31 validation note — e.g., "validated <date from 52-HUMAN-UAT.md>,
        build <Windows BuildNumber from 52-HUMAN-UAT.md>".
      - The canonical invocation: nono run --profile claude-code -- claude --version
        (from $env:USERPROFILE or $env:TEMP, not C:\ drive root).

    Do NOT touch the "Known limitation: nono run cannot host TUI agents on Windows"
    section header or its core TUI-limitation body text (line 542 and surrounding
    ConPTY/TUI rendering explanation). A cross-reference from within the new subsection
    is permitted and encouraged for completeness.
  </action>

  <verify>
    After editing, run the following checks (all from the repo root):

    1. Confirm the four anchor areas no longer assert "nono run" is universally
       WRITE_RESTRICTED + per-session WFP:
         grep -n "WRITE_RESTRICTED" docs/cli/development/windows-poc-handoff.mdx
       The line ~594 sentence must NOT claim WRITE_RESTRICTED for the claude-code profile.
       Line ~573 cross-ref must be profile-aware.

    2. Confirm the new heavy-runtime subsection exists and contains the key claims:
         grep -n "heavy-runtime\|Low-IL broker\|BrokerLaunchNoPty\|NO_WRITE_UP\|AppID-based" docs/cli/development/windows-poc-handoff.mdx
       Expect multiple matching lines in the new subsection.

    3. Confirm the dated validation line exists (D-52-08):
         grep -n "validated.*build\|validated.*2026" docs/cli/development/windows-poc-handoff.mdx
       Expect at least one match with the Phase 52 validation date.

    4. Confirm the TUI-limitation section header is intact (must NOT be removed):
         grep -n "Known limitation.*nono run cannot host TUI" docs/cli/development/windows-poc-handoff.mdx
       Expect exactly one match at roughly the original line position.

    5. Confirm the doc still ends at a reasonable length (was 643 lines; should be ~660-700
       lines after additions):
         wc -l docs/cli/development/windows-poc-handoff.mdx
  </verify>

  <acceptance_criteria>
    - The line ~594 primary envelope claim is profile-conditional: claude-code → Low-IL broker + NO_WRITE_UP + per-session-WFP-waived; other profiles → WRITE_RESTRICTED + per-session WFP
    - The line ~573 shell-path cross-ref is updated to note claude-code nono run also uses the broker path with the same per-session-WFP waiver
    - The line ~544 "claude --version works fine through pipes" framing is updated to reflect the Phase 52 positive result under the claude-code profile
    - The smoke-checklist table rows (~615-616) are updated to be profile-aware
    - A new heavy-runtime children subsection exists with: the problem description, the Phase 51 fix, the security tradeoff (NO_WRITE_UP vs WRITE_RESTRICTED; per-session-WFP waiver), and the canonical invocation
    - A "validated <date>, build <N>" line appears in the new subsection (D-52-08)
    - The TUI-limitation section header (line ~542) is intact and unweakened
    - grep -n "WRITE_RESTRICTED" shows the line ~594 claim is profile-conditional, not universal
    - grep -n "Low-IL broker" finds at least one match in the new subsection
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create 52-VERIFICATION.md and commit all artifacts</name>

  <read_first>
    .planning/phases/52-field-validation-closure-heavy-runtime-human-uat-doc-update/52-HUMAN-UAT.md
    (confirm both verdicts are PASS before creating the VERIFICATION with status: pass)
    .planning/ROADMAP.md § Phase 52 Success Criteria
    (the four success criteria that VERIFICATION must confirm are met)
  </read_first>

  <action>
    Step 1 — Create 52-VERIFICATION.md. Use the Write tool to create the file at
    .planning/phases/52-field-validation-closure-heavy-runtime-human-uat-doc-update/52-VERIFICATION.md.

    The file must include:
      - status: pass (set ONLY if both repro A and repro B are PASS in 52-HUMAN-UAT.md;
        if either is FAIL this task should not be reached — see Task 1 gate)
      - References to all four ROADMAP Phase 52 Success Criteria and their verdict:
          SC-1: nono run --profile claude-code -- claude --version → no 0xC0000142,
                version printed, exit 0 (closed by repro B PASS)
          SC-2: nono run --profile claude-code -- cmd /c "echo hi" passes on same host
                (closed by repro A PASS)
          SC-3: Both verdicts recorded with timestamps in 52-HUMAN-UAT.md (PASS)
          SC-4: windows-poc-handoff.mdx updated; heavy-runtime supported per D-52-05/06
                (closed by Task 2 of this plan)
      - Phase 51 SC-4 deferral closed (positive reference to 51-HUMAN-UAT.md deferred test)
      - Requirements closure: REQ-WSRH-04 (repro B PASS), REQ-WSRH-06 (UAT + doc PASS)

    Step 2 — Stage and commit all Phase 52 artifacts.

    The doc file is in docs/cli/development/ which is gitignored-but-tracked. A plain
    git add exits 1 and silently breaks && chains. Use git add -f for the doc file.

    Run in order (do not chain with &&; run each separately to catch failures):
      git add -f docs/cli/development/windows-poc-handoff.mdx
      git add .planning/phases/52-field-validation-closure-heavy-runtime-human-uat-doc-update/52-HUMAN-UAT.md
      git add .planning/phases/52-field-validation-closure-heavy-runtime-human-uat-doc-update/52-VERIFICATION.md

    Verify staging succeeded (no untracked/modified files in the above paths):
      git status --short -- docs/cli/development/windows-poc-handoff.mdx .planning/phases/52-field-validation-closure-heavy-runtime-human-uat-doc-update/

    Commit with DCO sign-off (required by CLAUDE.md for all commits):
      git commit -m "$(cat <<'EOF'
docs(52): field-validation closure — heavy-runtime UAT + handoff doc update

Phase 52 HUMAN-UAT records repro A (cmd echo) and repro B (claude --version)
both PASS on Windows 11 with the Phase 51 BrokerLaunchNoPty binary, closing
the ROADMAP SC-4 positive-spawn deferral from 51-HUMAN-UAT.md.

windows-poc-handoff.mdx D-52-05/06 sweep: the nono run security envelope is
now profile-conditional — claude-code routes through the Low-IL broker (NO_WRITE_UP
mandatory label; per-session WFP waived → AppID-based filtering); other profiles
retain WRITE_RESTRICTED + per-session WFP. Adds heavy-runtime children subsection.

Closes REQ-WSRH-04, REQ-WSRH-06. Milestone v2.7 complete.

Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>
EOF
)"
  </action>

  <verify>
    1. 52-VERIFICATION.md exists and contains status: pass:
         grep "status:" .planning/phases/52-field-validation-closure-heavy-runtime-human-uat-doc-update/52-VERIFICATION.md
       Expected: "status: pass"

    2. The doc commit includes the gitignored-but-tracked doc file:
         git log --name-only -1
       Expected: docs/cli/development/windows-poc-handoff.mdx appears in the file list.

    3. The commit has the DCO sign-off line:
         git log -1 --format="%B" | grep "Signed-off-by"
       Expected: "Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>"

    4. Working tree is clean for Phase 52 artifacts:
         git status --short -- docs/cli/development/windows-poc-handoff.mdx .planning/phases/52-field-validation-closure-heavy-runtime-human-uat-doc-update/
       Expected: no modified or untracked lines (all committed).
  </verify>

  <acceptance_criteria>
    - 52-VERIFICATION.md exists with status: pass
    - 52-VERIFICATION.md references all four SC verdicts and both REQ IDs as closed
    - git add -f docs/cli/development/windows-poc-handoff.mdx exits 0 (not plain git add)
    - git commit includes the DCO sign-off "Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>"
    - git log --name-only -1 shows docs/cli/development/windows-poc-handoff.mdx in the committed file set
    - git status is clean for all Phase 52 artifacts after commit
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc claim → operator trust decision | If the doc overstates network isolation for claude-code (claims per-session WFP when it is waived), an operator could make an unsafe network-boundary decision |
| code comment → doc accuracy | launch.rs lines 1205, 1214-1215 are the ground truth for the WFP waiver claim; doc must match code, not the reverse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52B-01 | Information Disclosure | doc overstates claude-code network isolation | mitigate | Task 2 D-52-05 sweep explicitly forbids claiming per-session WFP for claude-code; executor must verify launch.rs lines 1205/1214-1215 before writing the WFP claim; acceptance criteria check "per-session.*waived" is present in the doc |
| T-52B-02 | Tampering | gitignored-but-tracked doc not committed | mitigate | Task 3 explicitly requires git add -f and validates the commit includes the doc file via git log --name-only; plain git add is forbidden in the action prose |
| T-52B-03 | Repudiation | doc claims heavy-runtime support without live evidence | mitigate | Task 1 gate check blocks all edits until 52-HUMAN-UAT.md repro B PASS is confirmed; D-52-03 hard-stop is encoded in the checkpoint:decision task |
| T-52B-04 | Tampering | TUI-limitation section weakened by doc sweep | mitigate | Task 2 acceptance criteria explicitly require the TUI-limitation header to be intact; grep -n "Known limitation.*nono run cannot host TUI" must return one match |
| T-52B-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan |
</threat_model>

<verification>
Overall phase checks (run after Task 3 commit):

1. ROADMAP SC-1 closed: 52-HUMAN-UAT.md repro B = PASS (no 0xC0000142, version printed, exit 0)
2. ROADMAP SC-2 closed: 52-HUMAN-UAT.md repro A = PASS (cmd echo hi, exit 0, no regression)
3. ROADMAP SC-3 closed: 52-HUMAN-UAT.md timestamps present; 52-VERIFICATION.md status: pass
4. ROADMAP SC-4 closed: windows-poc-handoff.mdx updated; profile-conditional envelope documented;
   heavy-runtime children subsection present with D-52-08 dated validation line
5. REQ-WSRH-04 closed: repro B PASS (live field confirmation of the 0xC0000142 fix)
6. REQ-WSRH-06 closed: HUMAN-UAT matrix executed with evidence + doc updated
7. Phase 51 SC-4 deferral closed: 51-HUMAN-UAT.md pending test resolved by repro B PASS
8. git log shows DCO sign-off on the commit
9. git log --name-only shows docs/cli/development/windows-poc-handoff.mdx committed
</verification>

<success_criteria>
- 52-VERIFICATION.md closes with status: pass
- windows-poc-handoff.mdx is profile-conditional and does not claim universal WRITE_RESTRICTED + per-session WFP
- The doc states the claude-code Low-IL broker tradeoff: NO_WRITE_UP preserved; per-session WFP waived; AppID-based fallback
- Heavy-runtime children subsection present with Phase 52 validation date and Windows build number
- git commit includes the doc (git add -f) and carries the DCO sign-off
- The milestone v2.7 is complete: REQ-WSRH-04 and REQ-WSRH-06 both closed
</success_criteria>

<output>
Create .planning/phases/52-field-validation-closure-heavy-runtime-human-uat-doc-update/52-02-SUMMARY.md when done.
</output>
