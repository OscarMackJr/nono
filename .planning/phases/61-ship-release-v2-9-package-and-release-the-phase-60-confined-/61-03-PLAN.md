---
phase: 61-ship-release-v2-9-package-and-release-the-phase-60-confined
plan: 03
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - .planning/phases/61-ship-release-v2-9-package-and-release-the-phase-60-confined-/61-PRETAG-READINESS.md
autonomous: true
requirements: [REQ-RLS-03]
user_setup: []

must_haves:
  truths:
    - "The 4 untagged v2.7 drain-fix commits are confirmed ancestors of the tag commit (D-08)"
    - "No superseded v0.57.4 GitHub release exposing unsigned-payload MSIs exists on the remote (D-07)"
    - "The CI signing secrets release.yml requires are accounted for as a pre-flight (present or surfaced as a release blocker), not silently assumed (D-02)"
    - "The checked-in nono-wfp-driver.sys exists so release.yml Package step does not fail closed"
  artifacts:
    - path: ".planning/phases/61-ship-release-v2-9-package-and-release-the-phase-60-confined-/61-PRETAG-READINESS.md"
      provides: "pre-tag readiness record: D-08 ancestry, D-07 v0.57.4 absence, D-02 signing-secret pre-flight, driver-sys presence"
      min_lines: 20
  key_links:
    - from: ".planning/phases/61-ship-release-v2-9-package-and-release-the-phase-60-confined-/61-PRETAG-READINESS.md"
      to: ".github/workflows/release.yml"
      via: "documents the signing-secret fail-closed pre-flight and the driver-sys Package gate"
      pattern: "release.yml"
---

<objective>
Produce the pre-tag readiness record that must be green BEFORE pushing the v0.58.0 tag: confirm
the untagged v2.7 drain fixes are present in the shipped tree (D-08), confirm no superseded
v0.57.4 GitHub release exposes unsigned-payload MSIs (D-07), and pre-flight the CI signing
material so D-02 is surfaced as a blocker rather than silently assumed.

Purpose: The tag IS the release trigger — once v0.58.0 is pushed, release.yml runs. These checks
catch the regressions that would either ship a broken release (missing drain fix), leave a
distribution hazard live (unsigned v0.57.4), or hard-fail the pipeline (missing signing secrets /
missing nono-wfp-driver.sys). Doing them as a readiness gate is cheaper than discovering them
mid-release.

Output: A 61-PRETAG-READINESS.md record with a go/no-go checklist for the Wave 3 tag plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/61-ship-release-v2-9-package-and-release-the-phase-60-confined-/61-CONTEXT.md
@.planning/phases/61-ship-release-v2-9-package-and-release-the-phase-60-confined-/61-RESEARCH.md

<interfaces>
<!-- D-08 drain-fix commits (RESEARCH confirmed all 4 are ancestors of HEAD 8da48ea7 at plan time): -->
<!--   d8b7ce00  broker CreateProcessAsUserW GLE=87 HANDLE_LIST dedup -->
<!--   005b4c9e  no-PTY relay stdout-echo -->
<!--   0cbeb3be  WFP service-stop -->
<!--   b852826b  WFP MSI-uninstall custom action -->
<!-- Ancestry check shape: git merge-base --is-ancestor <sha> HEAD  (exit 0 = ancestor) -->

<!-- D-07 (RESEARCH confirmed): `gh release view v0.57.4` → "release not found"; `gh release list` -->
<!-- shows only v0.57.5 (Latest). So v0.57.4 is ABSENT. Verify-absent + document; delete ONLY if found. -->
<!--   If a stale unsigned-payload release surfaces: gh release delete v0.57.4 --yes  (--cleanup-tag optional). -->

<!-- D-02 / release.yml signing pre-flight (release.yml read in full by RESEARCH): -->
<!--   release.yml has a fail-closed "Check signing secrets (Windows)" step (~:124) that fails the -->
<!--   build if WINDOWS_SIGNING_CERT or WINDOWS_SIGNING_CERT_PASSWORD is empty. These signed v0.57.5. -->
<!--   This plan cannot READ repo secrets from the CLI — pre-flight = confirm the fail-closed step -->
<!--   exists (so absence becomes a loud blocker, never a silent POC-cert fallback) and record that -->
<!--   the operator must confirm the secrets are still configured before/at the tag push (D-02). -->
<!--   gh secret list (if repo-admin) can corroborate the secret NAMES are present. -->

<!-- release.yml Package step (~:193) fails closed if crates/nono-cli/data/windows/nono-wfp-driver.sys -->
<!-- is absent. RESEARCH confirmed the file is present (checked-in pre-signed placeholder copy). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify D-08 drain-fix ancestry, D-07 v0.57.4 absence, and the nono-wfp-driver.sys Package-gate file</name>
  <files>(no source changes — verification only)</files>
  <action>
Run the three mechanical pre-tag checks. (1) D-08: for each of the 4 drain-fix commits
(d8b7ce00 broker GLE=87, 005b4c9e no-PTY stdout-echo, 0cbeb3be WFP service-stop, b852826b WFP
MSI-uninstall), run `git merge-base --is-ancestor <sha> HEAD` and confirm exit 0 (ancestor) for
all four — these must not regress out of the release. (2) D-07: run `gh release view v0.57.4` and
`gh release list` to confirm v0.57.4 is absent (expected "release not found"; only v0.57.5 as
Latest). If a v0.57.4 (or any release exposing unsigned-payload MSIs) is found, delete it with
`gh release delete v0.57.4 --yes` (it is a distribution hazard) and record the deletion; if
absent, record "verified absent". (3) Confirm crates/nono-cli/data/windows/nono-wfp-driver.sys
exists (release.yml Package step fails closed without it). Capture each command's output for the
readiness record.
  </action>
  <verify>
  <automated>for s in d8b7ce00 005b4c9e 0cbeb3be b852826b; do git merge-base --is-ancestor $s HEAD && echo "$s ok" || echo "$s MISSING"; done; test -f crates/nono-cli/data/windows/nono-wfp-driver.sys && echo "driver-sys ok"; gh release view v0.57.4 >/dev/null 2>&1 && echo "v0.57.4 PRESENT-investigate" || echo "v0.57.4 absent-ok"</automated>
  </verify>
  <done>
All 4 drain-fix commits report ancestor (exit 0). nono-wfp-driver.sys is present. `gh release
view v0.57.4` reports not-found (or, if it was found, it was deleted and that is recorded). Each
result is captured for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pre-flight the D-02 signing material and write the pre-tag readiness record</name>
  <files>.planning/phases/61-ship-release-v2-9-package-and-release-the-phase-60-confined-/61-PRETAG-READINESS.md</files>
  <action>
Confirm release.yml's fail-closed signing-secret guard exists by grepping the workflow for the
"Check signing secrets" step and the WINDOWS_SIGNING_CERT / WINDOWS_SIGNING_CERT_PASSWORD secret
references — this is the D-02 mechanism that turns missing signing material into a loud build
blocker rather than a silent fallback to the self-signed POC cert (which is forbidden for a
public release). If repo-admin access is available, optionally corroborate the secret NAMES via
`gh secret list`; the CLI cannot read secret VALUES, so the record must state that the operator
confirms the secrets are still configured at/before the tag push (D-02).

Write 61-PRETAG-READINESS.md as a go/no-go gate for the Wave 3 tag plan (61-04), recording:
the D-08 ancestry results (all 4 commits, with the SHAs), the D-07 finding (v0.57.4 verified
absent or deleted-if-found), the nono-wfp-driver.sys presence, and the D-02 signing pre-flight
(fail-closed guard present at release.yml; operator must confirm secrets configured — do NOT
fall back to the POC cert). End with a clear "READY TO TAG: yes/no" line gated on all four checks
green.
  </action>
  <verify>
  <automated>test -f .planning/phases/61-ship-release-v2-9-package-and-release-the-phase-60-confined-/61-PRETAG-READINESS.md && grep -ci "WINDOWS_SIGNING_CERT" .github/workflows/release.yml</automated>
  </verify>
  <done>
61-PRETAG-READINESS.md exists and records all four checks (D-08 ancestry, D-07 v0.57.4
absence/deletion, driver-sys presence, D-02 signing pre-flight with the no-POC-fallback note)
plus a READY-TO-TAG go/no-go line. The release.yml signing-secret guard is confirmed present
(WINDOWS_SIGNING_CERT referenced).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tag push → CI release | The tagged tree + CI signing material determine whether shipped MSIs are correct and signed |
| public GitHub releases → downloaders | A live release with unsigned-payload MSIs is a tampering hazard for anyone who installs it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-05 | Tampering | superseded v0.57.4 release (unsigned-payload MSIs) | mitigate | Verify-absent; delete-if-found via `gh release delete --yes` so the only public artifacts are the signed v0.58.0 ones (D-07) |
| T-61-06 | Spoofing | CI signing material | mitigate | Pre-flight the release.yml fail-closed signing-secret guard; if secrets are unavailable at release time it is a surfaced release blocker — NEVER a silent fallback to the self-signed POC cert (D-02) |
| T-61-07 | Tampering | dropped drain fix regressing out of the release | mitigate | git merge-base --is-ancestor gate on all 4 drain-fix commits before tagging (D-08) |
| T-61-08 | Denial of Service (release pipeline) | missing nono-wfp-driver.sys | mitigate | Confirm the checked-in driver-sys exists so the release.yml Package step does not fail closed mid-release |
</threat_model>

<verification>
- All 4 drain-fix commits are ancestors of HEAD (D-08).
- v0.57.4 release verified absent (or deleted-if-found) (D-07).
- release.yml fail-closed signing-secret guard confirmed present; operator-confirm note recorded (D-02).
- nono-wfp-driver.sys present.
- 61-PRETAG-READINESS.md carries a READY-TO-TAG go/no-go line.
</verification>

<success_criteria>
A pre-tag readiness record exists with all four checks green and a clear go/no-go signal that
the Wave 3 tag plan consumes.
</success_criteria>

<output>
Create `.planning/phases/61-ship-release-v2-9-package-and-release-the-phase-60-confined-/61-03-SUMMARY.md` when done.
Commit with a DCO sign-off line: `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`.
</output>
