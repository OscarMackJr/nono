---
phase: 61-ship-release-v2-9-package-and-release-the-phase-60-confined
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono/Cargo.toml
  - crates/nono-cli/Cargo.toml
  - crates/nono-proxy/Cargo.toml
  - crates/nono-shell-broker/Cargo.toml
  - bindings/c/Cargo.toml
  - Cargo.lock
  - CHANGELOG.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [REQ-RLS-03]
user_setup: []

must_haves:
  truths:
    - "Every nono workspace crate reports version 0.58.0 (D-04)"
    - "Every internal nono/nono-proxy/nono-shell-broker path-dep version pin reads 0.58.0 (D-04)"
    - "cargo build --workspace resolves at 0.58.0 with no version-pin mismatch (D-04)"
    - "CHANGELOG.md has a [0.58.0] / v2.9 section describing the Phase 60 + Phase 62 release (D-03)"
    - "REQUIREMENTS.md registers REQ-RLS-03 and REQ-RLS-04 mapped to Phase 61"
    - "ROADMAP Phase 61 Requirements line cites REQ-RLS-03, REQ-RLS-04 (no longer TBD)"
  artifacts:
    - path: "crates/nono/Cargo.toml"
      provides: "nono crate version 0.58.0"
      contains: "version = \"0.58.0\""
    - path: "crates/nono-cli/Cargo.toml"
      provides: "nono-cli version 0.58.0 + 3 bumped path-dep pins"
      contains: "version = \"0.58.0\""
    - path: "CHANGELOG.md"
      provides: "0.58.0 release section"
      contains: "## [0.58.0]"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-RLS-03 + REQ-RLS-04 registration"
      contains: "REQ-RLS-03"
  key_links:
    - from: "crates/nono-cli/Cargo.toml"
      to: "crates/nono/Cargo.toml"
      via: "path-dep version pin"
      pattern: "nono = \\{ version = \"0.58.0\""
---

<objective>
Lockstep-bump the nono workspace from 0.57.5 to 0.58.0 (D-04), add the v2.9 CHANGELOG
section (D-03), and register the two release requirements (REQ-RLS-03, REQ-RLS-04) in
REQUIREMENTS.md + ROADMAP.md so Phase 61 has tracked requirements.

Purpose: The v0.58.0 git tag is the build trigger for release.yml (D-01). The tagged commit
MUST already carry the 0.58.0 version across all crate manifests and the lockfile, or the
shipped binaries/MSI report the wrong version and `cargo publish` (release.yml publish-crates
job) version-resolution mismatches. This plan makes the version state correct BEFORE tagging.

Output: All 5 crate Cargo.toml versions + 6 internal path-dep pins at 0.58.0, regenerated
Cargo.lock, a CHANGELOG [0.58.0] section, and registered REQ-RLS-03/04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-ship-release-v2-9-package-and-release-the-phase-60-confined-/61-CONTEXT.md
@.planning/phases/61-ship-release-v2-9-package-and-release-the-phase-60-confined-/61-RESEARCH.md

<interfaces>
<!-- EXACT version-bump file set — verified by grep at HEAD 8da48ea7. All currently "0.57.5". -->
<!-- tools/sign-fixture is version 0.1.0, publish=false, has NO nono path-dep pin → OUT of this bump. -->
<!-- Root Cargo.toml has NO [workspace.package].version field → nothing changes there. -->

Crate version literals (line 3 in each):
  crates/nono/Cargo.toml:3                  version = "0.57.5"
  crates/nono-cli/Cargo.toml:3              version = "0.57.5"
  crates/nono-proxy/Cargo.toml:3            version = "0.57.5"
  crates/nono-shell-broker/Cargo.toml:3     version = "0.57.5"
  bindings/c/Cargo.toml:3                   version = "0.57.5"

Internal path-dep version pins (6 total — these MUST also bump or cargo publish/resolve mismatches):
  crates/nono-cli/Cargo.toml:43             nono = { version = "0.57.5", path = "../nono", ... }
  crates/nono-cli/Cargo.toml:44             nono-proxy = { version = "0.57.5", path = "../nono-proxy", ... }
  crates/nono-cli/Cargo.toml:158            nono-shell-broker = { path = "../nono-shell-broker", version = "0.57.5" }
  crates/nono-proxy/Cargo.toml:21           nono = { version = "0.57.5", path = "../nono", ... }
  crates/nono-shell-broker/Cargo.toml:19    nono = { version = "0.57.5", path = "../nono" }
  bindings/c/Cargo.toml:18                  nono = { version = "0.57.5", path = "../../crates/nono", ... }

CHANGELOG.md current top (stale, predates shipped versions):
  ## [Unreleased] - v2.6 Phase 45

MSI ProductVersion is NOT set here — build-windows-msi.ps1 derives it from the git tag via
ConvertTo-MsiVersion. Do NOT touch any .wxs file (generated artifact).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lockstep-bump all 5 crate versions + 6 path-dep pins to 0.58.0 and regenerate Cargo.lock</name>
  <files>crates/nono/Cargo.toml, crates/nono-cli/Cargo.toml, crates/nono-proxy/Cargo.toml, crates/nono-shell-broker/Cargo.toml, bindings/c/Cargo.toml, Cargo.lock</files>
  <action>
Bump the workspace 0.57.5 to 0.58.0 in lockstep per D-04. Edit each of the 5 crate Cargo.toml
files: change the line-3 `version = "0.57.5"` literal to `version = "0.58.0"`. Then edit the 6
internal path-dep version pins listed in the interfaces block — each `version = "0.57.5"` on a
nono / nono-proxy / nono-shell-broker `{ path = ... }` dependency becomes `version = "0.58.0"`
(cli has 3 such pins at lines 43/44/158, proxy 1 at line 21, broker 1 at line 19, bindings/c 1
at line 18). Do NOT bump tools/sign-fixture (independent 0.1.0, no nono pin) and do NOT add a
version to the root Cargo.toml (no [workspace.package].version exists). Do NOT edit any .wxs
file — MSI ProductVersion flows from the build tag, never hand-edited (per [[windows_msi_wxs_is_generated]]).
Then regenerate the lockfile by running a workspace build so Cargo.lock records 0.58.0 for all
five crates. On Windows, cross-target Unix clippy cannot run — that is deferred-to-CI and is not
in scope for this plan (cargo build --workspace is the bump-correctness gate).
  </action>
  <verify>
  <automated>cargo build --workspace 2>&1 | tail -5</automated>
  </verify>
  <done>
`cargo build --workspace` completes successfully. `grep -rn 'version = "0.57.5"' crates/ bindings/`
returns zero matches. All 5 crate Cargo.toml line-3 versions and all 6 path-dep pins read
"0.58.0". Cargo.lock records nono/nono-cli/nono-proxy/nono-shell-broker/nono-ffi at 0.58.0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CHANGELOG [0.58.0] section and register REQ-RLS-03/04 in REQUIREMENTS.md + ROADMAP.md</name>
  <files>CHANGELOG.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Add a new `## [0.58.0] - v2.9` section at the top of CHANGELOG.md (above the existing stale
`## [Unreleased] - v2.6 Phase 45` entry — leave that entry in place; do not delete history).
The section must describe the v2.9 release contents per D-03/D-05: the Phase 60 Windows confined
tool-mediation POC (confined Write/Edit/MultiEdit/NotebookEdit + PowerShell-runner shell story)
and the Phase 62 out-of-box WFP kernel network enforcement (machine MSI start=auto, in-run
auto-start-or-fail-closed). Frame honestly as a POC / defense-in-depth, not full isolation
(per the Phase 60 verdict). Note it bundles the untagged post-v2.7 drain fixes.

Register two requirements in REQUIREMENTS.md under a new `### Release & Distribution (RLS) — v2.9`
subsection (mirroring how v2.8 used REQ-RLS-01/02): REQ-RLS-03 = "The v2.9 milestone is published
as a CI-signed public GitHub release: lockstep 0.58.0 workspace bump, dual tag v2.9+v0.58.0 off
current main, release.yml produces signed machine+user MSIs (Authenticode-valid wrapper AND
payload), release notes written, superseded v0.57.4 release resolved." REQ-RLS-04 = "The shipped
claude-code tool runner cannot expose ~/.claude (credentials/session state) or a project .claude/
to a confined tool call regardless of --allow-cwd — verified by the hook-level CWD guard
(claude_code_hook.rs) and its unit tests; enforcement is hook-layer, documented as the v2.9
boundary." Add both to the REQUIREMENTS.md Traceability table mapped to Phase 61 with status
Pending, and bump the coverage counts. In ROADMAP.md, replace the Phase 61 `**Requirements**: TBD
(run /gsd-plan-phase 61)` line with `**Requirements**: REQ-RLS-03, REQ-RLS-04`.
  </action>
  <verify>
  <automated>grep -c "REQ-RLS-03" .planning/REQUIREMENTS.md && grep -c "REQ-RLS-04" .planning/REQUIREMENTS.md && grep -c "## \[0.58.0\]" CHANGELOG.md</automated>
  </verify>
  <done>
CHANGELOG.md contains a `## [0.58.0]` section describing Phase 60 + Phase 62 with honest POC
framing. REQUIREMENTS.md defines REQ-RLS-03 and REQ-RLS-04, maps both to Phase 61 in the
Traceability table, and updates coverage counts. ROADMAP Phase 61 Requirements line reads
"REQ-RLS-03, REQ-RLS-04" (no longer TBD).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo source → CI build | The version literals in committed manifests drive what version the released binaries/crates report |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Tampering | Cargo manifest version pins | mitigate | Bump ALL 5 crate versions AND all 6 internal path-dep version pins in lockstep; `cargo build --workspace` gates that no `=0.57.5` pin survives (prevents a sibling crate resolving to a stale version at publish/build time) |
| T-61-02 | Tampering | generated .wxs MSI ProductVersion | accept | This plan never touches .wxs; MSI version is derived from the git tag by build-windows-msi.ps1. Hand-editing the generated .wxs is prohibited per [[windows_msi_wxs_is_generated]] |
</threat_model>

<verification>
- `cargo build --workspace` succeeds (lockfile + pin consistency).
- `grep -rn 'version = "0.57.5"' crates/ bindings/` returns zero matches.
- CHANGELOG has `## [0.58.0]`; REQUIREMENTS.md has REQ-RLS-03 + REQ-RLS-04 mapped to Phase 61;
  ROADMAP Phase 61 Requirements line cites both REQ-IDs.
</verification>

<success_criteria>
All workspace crates and internal path-dep pins read 0.58.0, `cargo build --workspace` passes,
CHANGELOG carries the v2.9 [0.58.0] section, and REQ-RLS-03/04 are registered and mapped to
Phase 61 in both REQUIREMENTS.md and ROADMAP.md.
</success_criteria>

<output>
Create `.planning/phases/61-ship-release-v2-9-package-and-release-the-phase-60-confined-/61-01-SUMMARY.md` when done.
Commit with a DCO sign-off line: `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`.
</output>
