---
phase: 61-ship-release-v2-9-package-and-release-the-phase-60-confined
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/61-ship-release-v2-9-package-and-release-the-phase-60-confined-/61-D09-VERIFICATION.md
  - .planning/todos/done/2026-05-29-claude-tools-runner-deny-dotclaude-regardless-of-cwd.md
autonomous: true
requirements: [REQ-RLS-04]
user_setup: []

must_haves:
  truths:
    - "The hook-level CWD guard that denies wrapping Bash when CWD covers ~/.claude is present and its unit tests pass (D-09)"
    - "The deny-~/.claude protection is documented honestly as hook-layer enforcement, not an OS-label deny-within-allow (D-09)"
    - "The bare-CLI gap (nono run --allow-cwd ~/.claude outside the hooked loop still grants write on Windows) is recorded as a documented limitation, not a Phase 61 code task (D-09)"
    - "The pending deny-~/.claude todo is moved to resolved citing the Phase 60-03 fix (D-09)"
  artifacts:
    - path: ".planning/phases/61-ship-release-v2-9-package-and-release-the-phase-60-confined-/61-D09-VERIFICATION.md"
      provides: "D-09 verification record + honest scope/limitation note for release notes"
      min_lines: 20
    - path: ".planning/todos/done/2026-05-29-claude-tools-runner-deny-dotclaude-regardless-of-cwd.md"
      provides: "resolved todo citing Phase 60-03 commits"
  key_links:
    - from: ".planning/phases/61-ship-release-v2-9-package-and-release-the-phase-60-confined-/61-D09-VERIFICATION.md"
      to: "crates/nono-cli/src/claude_code_hook.rs"
      via: "cited enforcement location (cwd_self_disable_risk_reason at :204)"
      pattern: "claude_code_hook"
---

<objective>
Verify and document the D-09 deny-`~/.claude` security blocker. Per RESEARCH §D-09, the robust
fix is ALREADY IMPLEMENTED and committed on main (Phase 60-03): a hook-level CWD guard in
`crates/nono-cli/src/claude_code_hook.rs` that refuses to wrap Bash when the CWD covers
`~/.claude` / `~/.claude.json[.lock]` / a project-local `.claude/`, with passing unit tests.
This is a VERIFY-AND-DOCUMENT plan, NOT a build plan.

Purpose: D-09 is a pre-ship security blocker — shipping a confined-tool-mediation POC whose
runner could be coaxed into exposing `~/.claude` credentials via `--allow-cwd` would be a real
gap in exactly what v2.9 distributes. This plan proves the guard holds on the 0.58.0 build and
captures the HONEST scope note (hook-layer boundary; the bare-CLI path is a documented
limitation) for the release notes.

Output: A D-09 verification record with the release-notes scope/limitation language, and the
pending todo moved to resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<verify_runtime_note>
HOST IS WINDOWS (PowerShell default). Every `<automated>` verify in this plan is POSIX bash
(`cargo test ... | tail`, `test -f ... && echo`, `test ! -f`) — run them via the **Bash tool**,
NOT PowerShell. For any bash `git` operation (e.g. the `git mv` in Task 2), FIRST
`cd /c/Users/OMack/Nono` and confirm `pwd` + `git branch` before the git call (guards the known
Windows worktree-CWD-divergence footgun where a stale worktree's branch ref is implicitly
targeted after a failed `git worktree remove`).
</verify_runtime_note>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/61-ship-release-v2-9-package-and-release-the-phase-60-confined-/61-CONTEXT.md
@.planning/phases/61-ship-release-v2-9-package-and-release-the-phase-60-confined-/61-RESEARCH.md
@crates/nono-cli/src/claude_code_hook.rs
@.planning/todos/pending/2026-05-29-claude-tools-runner-deny-dotclaude-regardless-of-cwd.md

<interfaces>
<!-- D-09 enforcement is ALREADY on main (Phase 60-03). Verified locations: -->
<!--   claude_code_hook.rs:204  Bash arm calls cwd_self_disable_risk_reason()? and returns deny -->
<!--                            BEFORE emitting any --allow-cwd runner command -->
<!--   :272 cwd_self_disable_risk_reason()  /  :279 cwd_self_disable_risk_reason_for(cwd) -->
<!--   :301 cwd_covers_home_claude_state(cwd, home)  uses path_covers (component comparison, -->
<!--        not string starts_with — closes the CR-01 fail-open) -->
<!-- Tests present in the #[cfg(test)] module (run via the verify command below): -->
<!--   pre_tool_use_file_tools_deny (:553), plus cwd_covers_home_claude_state assertions at -->
<!--   :691 (home CWD covers ~/.claude → denied), :707 (.claudefoo NOT falsely matched — -->
<!--   component boundary), :722/:906 (repo CWD with project .claude/ child → denied). -->

<!-- Phase 60-03 fix commits to cite in the resolved todo: ddb711dc, fe832dfc, 309c94a4. -->

<!-- HONEST SCOPE (bake into the doc + release notes): the Windows label backend cannot -->
<!-- deny-within-allow (validate_deny_overlaps in policy.rs early-returns unless target_os=linux; -->
<!-- add_deny_access is a Windows no-op for the allow-overlap case). So the deny is enforced at -->
<!-- the HOOK layer, which only fires when Claude Code is the launcher. A bare -->
<!-- `nono run --profile claude-code-tools-windows-runner --allow-cwd ~/.claude` (no hook) would -->
<!-- still grant ~/.claude write on Windows. The hooked-loop boundary is THE documented v2.9 -->
<!-- boundary; the bare-CLI gap is a documented LIMITATION, not a Phase 61 code task. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the D-09 hook guard unit tests on the 0.58.0 build</name>
  <files>(no source changes — verification only)</files>
  <action>
Run the claude_code_hook test module to confirm the Phase 60-03 CWD guard still passes on the
current (0.58.0-bumped) tree. This proves: the home-CWD-covers-`~/.claude` case denies, the
`.claudefoo` component-boundary case does NOT falsely match, the project-local `.claude/` child
case denies, and non-Bash file tools deny. If any test fails, STOP and escalate — D-09 is a
pre-ship blocker and a regression here blocks the tag. Do NOT modify the hook or the tests; this
plan verifies an existing committed fix, it does not re-implement the design.
  </action>
  <verify>
  <!-- Run via the Bash tool (Windows host); see <verify_runtime_note>. -->
  <automated>cargo test -p nono-cli --lib claude_code_hook 2>&1 | tail -15</automated>
  </verify>
  <done>
`cargo test -p nono-cli --lib claude_code_hook` reports all tests passed (test result: ok),
including `pre_tool_use_file_tools_deny` and the `cwd_covers_home_claude_state` assertions.
</done>
</task>

<task type="auto">
  <name>Task 2: Write the D-09 verification record + release-notes scope note, and resolve the pending todo</name>
  <files>.planning/phases/61-ship-release-v2-9-package-and-release-the-phase-60-confined-/61-D09-VERIFICATION.md, .planning/todos/done/2026-05-29-claude-tools-runner-deny-dotclaude-regardless-of-cwd.md</files>
  <action>
Create 61-D09-VERIFICATION.md recording the D-09 closure. It must state: (1) the enforcement
location is the hook-level CWD guard at claude_code_hook.rs:204 (cwd_self_disable_risk_reason →
cwd_covers_home_claude_state at :301, using path_covers component comparison), implemented in
Phase 60-03; (2) the unit tests pass on the 0.58.0 build (cite the Task 1 result); (3) the
HONEST scope note for release notes — the Windows OS label backend cannot deny-within-allow
(add_deny_access is a Windows no-op for the allow-overlap case; validate_deny_overlaps is
Linux-only), so the deny is a HOOK-LAYER fail-closed guard that fires when Claude Code is the
launcher; (4) the documented LIMITATION — a bare `nono run --allow-cwd ~/.claude` invoked
OUTSIDE the hooked Claude loop would still grant write on Windows; the hooked-loop boundary is
THE documented v2.9 boundary (consistent with the Phase 60 "defense-in-depth, not full
isolation" verdict); the bare-CLI gap is a documented limitation/deferral, NOT a Phase 61 code
task. Provide a short ready-to-paste paragraph the release-notes plan (61-04) can reuse.

Move the pending todo file
.planning/todos/pending/2026-05-29-claude-tools-runner-deny-dotclaude-regardless-of-cwd.md to
.planning/todos/done/ (use git mv so history is preserved), and append a resolution note to the
moved file citing the Phase 60-03 fix commits ddb711dc, fe832dfc, 309c94a4 and pointing at
61-D09-VERIFICATION.md.
  </action>
  <verify>
  <!-- Run via the Bash tool (Windows host); see <verify_runtime_note>. The git mv needs the cd-to-root guard. -->
  <automated>test -f .planning/phases/61-ship-release-v2-9-package-and-release-the-phase-60-confined-/61-D09-VERIFICATION.md && test -f .planning/todos/done/2026-05-29-claude-tools-runner-deny-dotclaude-regardless-of-cwd.md && test ! -f .planning/todos/pending/2026-05-29-claude-tools-runner-deny-dotclaude-regardless-of-cwd.md && echo OK</automated>
  </verify>
  <done>
61-D09-VERIFICATION.md exists and documents the hook-layer enforcement, the test result, the
honest scope framing, AND the bare-CLI documented limitation, with a reusable release-notes
paragraph. The todo is moved from pending/ to done/ with a resolution note citing the Phase
60-03 commits.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| confined tool call → ~/.claude credential/session state | A Bash tool call wrapped via `nono run --allow-cwd` could write to the agent's own hook config and self-disable the jail |
| bare CLI invocation → runner profile | A direct `nono run` (no Claude Code hook) bypasses the hook-layer guard — the OS label cannot deny-within-allow on Windows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-03 | Elevation of Privilege / Tampering | claude_code_hook.rs Bash arm | mitigate | Hook refuses to wrap Bash when CWD covers ~/.claude or a project .claude/ (cwd_self_disable_risk_reason at :204), using path_covers component comparison (closes the CR-01 fail-open). Verified by unit tests on the 0.58.0 build |
| T-61-04 | Tampering | bare `nono run --allow-cwd ~/.claude` outside the hooked loop | accept | Windows label backend has no deny-within-allow; the hook is the load-bearing boundary. Documented limitation framed as the hooked-loop v2.9 boundary in 61-D09-VERIFICATION.md + release notes (consistent with the POC "defense-in-depth, not full isolation" verdict). Not a Phase 61 code task |
</threat_model>

<verification>
- `cargo test -p nono-cli --lib claude_code_hook` passes on the 0.58.0 tree.
- 61-D09-VERIFICATION.md documents hook-layer enforcement + the honest bare-CLI limitation +
  a reusable release-notes paragraph.
- The deny-`~/.claude` todo is in todos/done/ (not pending/) with a Phase-60-03 resolution note.
- (All verify commands run via the Bash tool — see <verify_runtime_note>.)
</verification>

<success_criteria>
D-09 is proven closed on the release build (hook tests pass), the enforcement and its honest
scope/limitation are documented for the release notes, and the originating todo is resolved.
</success_criteria>

<output>
Create `.planning/phases/61-ship-release-v2-9-package-and-release-the-phase-60-confined-/61-02-SUMMARY.md` when done.
Commit with a DCO sign-off line: `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`.
</output>
</content>
