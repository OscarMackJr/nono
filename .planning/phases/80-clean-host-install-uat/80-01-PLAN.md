---
phase: 80-clean-host-install-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .cargo/config.toml
  - scripts/build-windows-msi.ps1
  - scripts/validate-windows-msi-contract.ps1
autonomous: true
requirements:
  - INST-01
must_haves:
  truths:
    - "Building for x86_64-pc-windows-msvc produces binaries with no vcruntime140.dll import"
    - "A nono-wfp-service start failure during MSI install does not trigger a rollback (exit 0, not 1603)"
    - "The MSI contract validator asserts vital=no on ServiceControl and ErrorControl=ignore on ServiceInstall"
    - "Linux and macOS CI builds are unaffected by the .cargo/config.toml change"
  artifacts:
    - path: ".cargo/config.toml"
      provides: "Windows MSVC target-scoped +crt-static rustflag"
      contains: "[target.x86_64-pc-windows-msvc]"
    - path: "scripts/build-windows-msi.ps1"
      provides: "Generated WiX with vital=no and ErrorControl=ignore in service component"
      contains: "vital=\"no\""
    - path: "scripts/validate-windows-msi-contract.ps1"
      provides: "Contract assertions for non-fatal service attributes"
      contains: "vital"
  key_links:
    - from: ".cargo/config.toml"
      to: "x86_64-pc-windows-msvc target binaries"
      via: "Cargo target-scoped rustflags"
      pattern: "target\\.x86_64-pc-windows-msvc"
    - from: "scripts/build-windows-msi.ps1"
      to: "dist/windows/nono-machine.wxs"
      via: "Write-Utf8NoBomCompat at ~line 404 (generated from $serviceComponentXml here-string)"
      pattern: "vital=\"no\""
    - from: "scripts/validate-windows-msi-contract.ps1"
      to: "scripts/build-windows-msi.ps1"
      via: "Assert-Equal reads ServiceControl.vital attribute from the generated .wxs XML"
      pattern: "machineServiceControl\\.vital"
---

<objective>
Apply the two-part build fix (D-03 + D-04) that closes the Phase 67 carry-forward INST-01
failure mode on a clean Win11 host.

Purpose: A clean host without vcruntime140.dll causes msiexec exit 1603 because the nono
binaries fail to load (STATUS_DLL_NOT_FOUND), which triggers a full install rollback. Two
changes prevent this: (a) static CRT linkage eliminates the DLL dependency, and (b) making
the nono-wfp-service start non-fatal prevents a service SCM timeout from rolling back an
otherwise-successful install.

Output: `.cargo/config.toml` (new), patched `scripts/build-windows-msi.ps1` (two attribute
changes in the $serviceComponentXml here-string), and new Assert-Equal assertions in
`scripts/validate-windows-msi-contract.ps1` that lock this non-fatal contract going forward.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/80-clean-host-install-uat/80-CONTEXT.md
@.planning/phases/80-clean-host-install-uat/80-RESEARCH.md
@.planning/phases/80-clean-host-install-uat/80-PATTERNS.md

<interfaces>
<!-- .cargo/config.toml: does NOT exist today (confirmed). Create at workspace root. -->
<!-- Cargo.toml [profile.release] already sets panic = "abort" — no conflict with +crt-static. -->

<!-- scripts/build-windows-msi.ps1 — current $serviceComponentXml here-string (lines 226-247): -->
<!--   ErrorControl="normal"  on ServiceInstall  → CHANGE to ErrorControl="ignore" -->
<!--   No vital attribute on ServiceControl       → ADD vital="no" -->
<!-- DO NOT edit dist/windows/nono-machine.wxs — it is generated and overwritten by Write-Utf8NoBomCompat. -->

<!-- scripts/validate-windows-msi-contract.ps1 — existing service block ends ~line 215: -->
<!--   Assert-Equal on $machineServiceControl.Wait -Expected "yes"  (last existing assertion) -->
<!--   ADD after this line: -->
<!--     Assert-Equal -Actual $machineServiceInstall.ErrorControl -Expected "ignore" -Message "..." -->
<!--     Assert-Equal -Actual $machineServiceControl.vital -Expected "no" -Message "..." -->
<!--   Assert-Equal signature: param($Actual, $Expected, [string]$Message); throw on mismatch. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .cargo/config.toml with Windows MSVC static-CRT rustflag (D-03)</name>
  <files>.cargo/config.toml</files>
  <read_first>
    - .cargo/ directory contents (confirmed: only audit.toml exists — no config.toml today)
    - .planning/phases/80-clean-host-install-uat/80-RESEARCH.md § Q1 (exact stanza, pitfall 1 warning against [build] scope)
    - .planning/phases/80-clean-host-install-uat/80-PATTERNS.md § .cargo/config.toml section
  </read_first>
  <action>
    Create `.cargo/config.toml` at the workspace root (alongside `.cargo/audit.toml`).

    The file MUST contain a `[target.x86_64-pc-windows-msvc]` section (NOT `[build]`) with:
      rustflags = ["-C", "target-feature=+crt-static"]

    This is the ONLY section in the file — no `[build]` section, no `[net]` section, nothing
    else. The target scope ensures this flag applies exclusively to the Windows MSVC target;
    Linux (`x86_64-unknown-linux-gnu`, `aarch64-unknown-linux-gnu`) and macOS
    (`x86_64-apple-darwin`, `aarch64-apple-darwin`) targets are completely unaffected (per D-03).

    Add a concise header comment stating: "Applies to Windows MSVC target ONLY. Does NOT affect
    Linux or macOS builds. Eliminates vcruntime140.dll dependency (INST-01 / Phase 80 D-03)."

    Do NOT use `RUSTFLAGS` env var in the build script — the config.toml approach applies
    uniformly to all Windows MSVC builds including release.yml (per RESEARCH.md Q1 rationale).

    Assumption A2 (Cargo env-var + config-file merge order with CI's RUSTFLAGS: -Dwarnings) is
    flagged for empirical confirmation on the first Windows CI run. The flags are non-conflicting
    so merge should be additive, but executor should note this in the SUMMARY.
  </action>
  <verify>
    <automated>grep -c "target.x86_64-pc-windows-msvc" .cargo/config.toml && grep -c "crt-static" .cargo/config.toml</automated>
  </verify>
  <acceptance_criteria>
    - `.cargo/config.toml` exists at the workspace root.
    - File contains the section header `[target.x86_64-pc-windows-msvc]` (exact string).
    - File contains `rustflags = ["-C", "target-feature=+crt-static"]`.
    - File does NOT contain `[build]` (would be global and break Linux/macOS).
    - `grep -c "target.x86_64-pc-windows-msvc" .cargo/config.toml` returns 1.
    - `grep -c "crt-static" .cargo/config.toml` returns 1.
  </acceptance_criteria>
  <done>`.cargo/config.toml` created with Windows-only +crt-static stanza; grep gates pass.</done>
</task>

<task type="auto">
  <name>Task 2: Patch build-windows-msi.ps1 non-fatal service + add contract assertions (D-04)</name>
  <files>scripts/build-windows-msi.ps1, scripts/validate-windows-msi-contract.ps1</files>
  <read_first>
    - scripts/build-windows-msi.ps1 lines 220-260 (the $serviceComponentXml here-string; current state: ErrorControl="normal", no vital attribute)
    - scripts/validate-windows-msi-contract.ps1 lines 190-230 (existing Assert-Equal service block; ServiceInstall + ServiceControl assertions)
    - .planning/phases/80-clean-host-install-uat/80-PATTERNS.md § build-windows-msi.ps1 section (exact current vs target state diff)
    - .planning/phases/80-clean-host-install-uat/80-PATTERNS.md § validate-windows-msi-contract.ps1 section (exact new assertions to append)
    - .planning/phases/80-clean-host-install-uat/80-RESEARCH.md § Q2 (WiX v4 vital attribute semantics and pitfall 2 on casing)
  </read_first>
  <action>
    CHANGE 1 — `scripts/build-windows-msi.ps1` (edit the here-string; never edit the generated .wxs):
      In the `$serviceComponentXml` here-string (located at approximately lines 226-246):
      (a) On the `ServiceInstall` element: change `ErrorControl="normal"` to `ErrorControl="ignore"`.
          Rationale: SCM will not pop an error dialog or block boot on a service start failure.
      (b) On the `ServiceControl` element: add `vital="no"` as a new attribute on its own line,
          after `Wait="yes"` and before the closing ` />`. Use lowercase `vital` (WiX v4 schema,
          not the WiX v3 `Vital` with capital V — see RESEARCH.md pitfall 2 and assumption A3).
          The `Start="install"` attribute MUST be retained — the intent is "try to start, but do
          not roll back on failure."

    CHANGE 2 — `scripts/validate-windows-msi-contract.ps1` (add assertions after the existing
    ServiceControl block; insert after the last existing Assert-Equal on $machineServiceControl,
    approximately line 215):
      (a) Assert-Equal -Actual $machineServiceInstall.ErrorControl -Expected "ignore"
          -Message "Machine MSI ServiceInstall ErrorControl mismatch (must be ignore so SCM
          start failure is non-fatal per D-04)"
      (b) Assert-Equal -Actual $machineServiceControl.vital -Expected "no"
          -Message "Machine MSI ServiceControl vital mismatch (must be no so a service start
          failure does not roll back the install per D-04)"
      These assertions lock the non-fatal contract so a future refactor cannot silently revert
      to fatal-service behavior (RESEARCH.md Pitfall 5).

    Do NOT edit `dist/windows/nono-machine.wxs` directly — it is regenerated by
    Write-Utf8NoBomCompat and any direct edit is overwritten on the next build run.
    Do NOT modify anything outside the $serviceComponentXml here-string block in build-windows-msi.ps1.
  </action>
  <verify>
    <automated>grep -c 'vital="no"' scripts/build-windows-msi.ps1 && grep -c 'ErrorControl="ignore"' scripts/build-windows-msi.ps1 && grep -c 'machineServiceControl.vital' scripts/validate-windows-msi-contract.ps1 && grep -c 'machineServiceInstall.ErrorControl' scripts/validate-windows-msi-contract.ps1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'vital="no"' scripts/build-windows-msi.ps1` returns 1 (inside the $serviceComponentXml here-string).
    - `grep -c 'ErrorControl="ignore"' scripts/build-windows-msi.ps1` returns 1 (same block).
    - `grep -c 'ErrorControl="normal"' scripts/build-windows-msi.ps1` returns 0 (old value removed).
    - `grep -c 'machineServiceControl.vital' scripts/validate-windows-msi-contract.ps1` returns 1.
    - `grep -c 'machineServiceInstall.ErrorControl' scripts/validate-windows-msi-contract.ps1` returns 1.
    - `grep -c 'Start="install"' scripts/build-windows-msi.ps1` returns 1 (ServiceControl Start preserved).
    - The generated .wxs file (`dist/windows/nono-machine.wxs`) is NOT directly modified (verify with `git diff dist/windows/nono-machine.wxs` — should be empty if the script was run; or just confirm the .ps1 changes are in the here-string, not the .wxs).
  </acceptance_criteria>
  <done>build-windows-msi.ps1 has vital="no" + ErrorControl="ignore" in the service here-string; validate-windows-msi-contract.ps1 has two new Assert-Equal calls locking those values; all grep gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev host → MSI build script | `scripts/build-windows-msi.ps1` is a development tool run by the developer; inputs are file paths from the workspace, not external |
| runner → gate script | `scripts/verify-dark.ps1` dot-sources gate files; gate files are version-controlled repo content (trusted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01 | Tampering | `.cargo/config.toml` — per-target rustflags scope | mitigate | Use `[target.x86_64-pc-windows-msvc]` stanza only (NOT `[build]`); confirm Linux/macOS CI legs are unaffected after merge |
| T-80-02 | Tampering | `scripts/build-windows-msi.ps1` `$serviceComponentXml` here-string | mitigate | Edit only the PowerShell source here-string; assert in validate-windows-msi-contract.ps1 so any future revert is caught by contract validation |
| T-80-03 | Spoofing | WiX attribute casing (vital vs Vital) — WiX v4 schema assumption A3 | mitigate | Run `build-windows-msi.ps1 -EmitOnly` after the change and verify generated `.wxs` contains `vital="no"` (lowercase); then run `wix build` to confirm no schema error |
| T-80-SC | Tampering | No npm/pip/cargo installs introduced this plan | accept | No new packages; no slopcheck needed |
</threat_model>

<verification>
On the dev host (dirty — no clean VM needed for structural verification):
1. `grep -c "target.x86_64-pc-windows-msvc" .cargo/config.toml` → 1
2. `grep -c "crt-static" .cargo/config.toml` → 1
3. `grep -c 'vital="no"' scripts/build-windows-msi.ps1` → 1
4. `grep -c 'ErrorControl="ignore"' scripts/build-windows-msi.ps1` → 1
5. `grep -c 'ErrorControl="normal"' scripts/build-windows-msi.ps1` → 0
6. `grep -c 'machineServiceControl.vital' scripts/validate-windows-msi-contract.ps1` → 1
7. `grep -c 'machineServiceInstall.ErrorControl' scripts/validate-windows-msi-contract.ps1` → 1
8. `cargo build -p nono-cli` succeeds on the Windows dev host (confirms no config.toml syntax errors).

CI verification (after push):
- Ubuntu + macOS Clippy/Test CI legs remain green (confirms the `[target.x86_64-pc-windows-msvc]`
  scope does not affect Linux/macOS builds — assumption A1/A2 empirically confirmed).
- Windows CI build leg compiles with `+crt-static` (confirm via verbose flag or CI log inspection
  that `-C target-feature=+crt-static` appears in the effective rustflags).

Post-build (on dev host with WiX installed, optional):
- Run `.\scripts\build-windows-msi.ps1 ... -EmitOnly` and inspect the generated
  `dist/windows/nono-machine.wxs` for `vital="no"` (assumption A3 confirmation).
</verification>

<success_criteria>
- `.cargo/config.toml` exists with `[target.x86_64-pc-windows-msvc]` + `rustflags = ["-C", "target-feature=+crt-static"]`; no `[build]` scope.
- `scripts/build-windows-msi.ps1` emits `ErrorControl="ignore"` (was "normal") and `vital="no"` (was absent) in the $serviceComponentXml here-string.
- `scripts/validate-windows-msi-contract.ps1` contains Assert-Equal for both ErrorControl="ignore" and vital="no", ensuring no future refactor silently reverts these.
- All seven structural grep gates pass on the dev host.
- CI ubuntu/macos legs remain green after merge (A1/A2 empirical confirmation).
</success_criteria>

<output>
Create `.planning/phases/80-clean-host-install-uat/80-01-SUMMARY.md` when done.
</output>
