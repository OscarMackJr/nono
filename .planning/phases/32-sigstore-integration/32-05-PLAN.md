---
phase: 32
plan: 05
type: execute
wave: 3
depends_on: [02, 03, 04]
files_modified:
  - docs/architecture/broker-trust-anchor.md
  - docs/architecture/sigstore-tuf-cache.md
  - docs/cli/development/windows-poc-handoff.mdx
  - .planning/deferred-items.md
  - .planning/phases/32-sigstore-integration/32-CONTEXT.md   # P32-CHK-014 — backfill stale :2173+ anchor
autonomous: false
requirements_addressed:
  - D-32-10
tags: [sigstore, adr, cookbook, documentation]
nyquist_compliant: true

must_haves:
  truths:
    - "ADR docs/architecture/broker-trust-anchor.md exists, documents the D-32-13 self-trust-anchor pattern (why self-introspection over baked constants), Phase 31 Plan 04 release-pipeline assumption, dev-build skip mechanism, threat model"
    - "ADR docs/architecture/sigstore-tuf-cache.md exists, documents the D-32-01 cached-root design rationale (why offline verify, why per-user, why no admin)"
    - "Cookbook docs/cli/development/windows-poc-handoff.mdx has a new prereq section for `nono setup --refresh-trust-root` mirroring the 260509-stb block-net WFP-required prereq pattern"
    - "Cookbook documents the canonical --issuer / --identity examples: GitHub Actions release pattern + GitLab CI release pattern (per CONTEXT § Specifics)"
    - "Release-pipeline audit findings recorded: read .github/workflows/release.yml, document current signing posture (keyed vs keyless), note any migration recommendations as v2.4+ deferred items"
    - "v2.4+ deferred items in .planning/deferred-items.md updated with: keyless migration of release.yml (D-32-10 audit conclusion). NOTE: per Plan 03 revision (P32-CHK-005/008), mock-Fulcio/Rekor fixture capture and keyless_sign_then_verify_roundtrip graduation are NO LONGER deferred — they are CI-runnable via rcgen at-test-time generation. Per Plan 04 revision (P32-CHK-010/011), broker-mismatch-stub.exe fixture is also NO LONGER deferred — tempdir staging replaces it. The deferred-items entries should reflect these closures, NOT carry them forward."
    - "CONTEXT.md backfill (P32-CHK-014): Phase 32 CONTEXT.md no longer cites the stale `launch.rs:2173+` anchor; it cites the correct `launch.rs:1246-1438` range. Plan 05 Task 1 Step 3 records the correction."
  artifacts:
    - path: "docs/architecture/broker-trust-anchor.md"
      provides: "ADR for D-32-13 self-trust-anchor pattern"
      contains: "self-trust-anchor"
    - path: "docs/architecture/sigstore-tuf-cache.md"
      provides: "ADR for D-32-01 cached TUF root design"
      contains: "verify-is-offline"
    - path: "docs/cli/development/windows-poc-handoff.mdx"
      provides: "Updated cookbook with trust-root prereq + --issuer/--identity examples"
      contains: "nono setup --refresh-trust-root"
    - path: ".planning/deferred-items.md"
      provides: "v2.4+ follow-up entries for release.yml migration audit, fixture captures, roundtrip test graduation"
      contains: "Phase 32"
  key_links:
    - from: "docs/cli/development/windows-poc-handoff.mdx"
      to: "docs/architecture/broker-trust-anchor.md"
      via: "explicit cross-link in Sigstore Trust section"
      pattern: "broker-trust-anchor"
    - from: "docs/cli/development/windows-poc-handoff.mdx"
      to: "docs/architecture/sigstore-tuf-cache.md"
      via: "explicit cross-link"
      pattern: "sigstore-tuf-cache"
    - from: ".planning/deferred-items.md"
      to: "Phase 32 follow-up tracking"
      via: "Phase 32 row(s) in deferred-items table"
      pattern: "Phase 32"
---

<objective>
Land the documentation half of D-32-10 + the CONTEXT § Specifics cookbook updates. Two ADRs (broker-trust-anchor + sigstore-tuf-cache) document the novel decisions Phase 32 introduces for future maintainers. Cookbook prerequisite section mirrors the 260509-stb block-net WFP-required precedent so POC handoff users hit ZERO surprises. Release-pipeline audit findings recorded as v2.4+ deferred items (no migration in Phase 32 per D-32-10 explicit deferral). Audit step requires reading `.github/workflows/release.yml` — a checkpoint task because the conclusion (keep keyed vs migrate to keyless) is a human judgment call.

Purpose: Without this plan, Phase 32 ships working code but no operator-facing docs. Future readers (and the next person debugging a broker-Authenticode error) won't have an ADR to consult. The cookbook is the single piece of operator-facing material that's exercised end-to-end during POC handoff; it MUST be in sync with the Phase 32 fail-closed contracts.

Output: Two ADRs + cookbook updates + audit findings + deferred-items entries. Phase 32 closes; v2.3 milestone unblocks for `/gsd-complete-milestone`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-sigstore-integration/32-CONTEXT.md
@.planning/phases/32-sigstore-integration/32-RESEARCH.md
@.planning/phases/32-sigstore-integration/32-PATTERNS.md
@.planning/phases/32-sigstore-integration/32-VALIDATION.md
@.planning/phases/32-sigstore-integration/32-02-SUMMARY.md
@.planning/phases/32-sigstore-integration/32-03-SUMMARY.md
@.planning/phases/32-sigstore-integration/32-04-SUMMARY.md
@.planning/quick/260509-stb/260509-stb-SUMMARY.md
@docs/cli/development/windows-poc-handoff.mdx
@docs/architecture/audit-bundle-target.md
@docs/architecture/aipc-unix-futures.md
@.github/workflows/release.yml
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write the two ADRs (broker-trust-anchor + sigstore-tuf-cache)</name>
  <files>
    docs/architecture/broker-trust-anchor.md,
    docs/architecture/sigstore-tuf-cache.md
  </files>
  <read_first>
    - docs/architecture/audit-bundle-target.md (full file — Phase 27.2 ADR for format/length convention)
    - docs/architecture/aipc-unix-futures.md (full file — Phase 25-02 ADR for format)
    - .planning/phases/32-sigstore-integration/32-CONTEXT.md (D-32-01, D-32-11, D-32-13, D-32-14 — quote verbatim where the ADR records the decision)
    - .planning/phases/32-sigstore-integration/32-RESEARCH.md (Pattern 5 + Pitfalls 5, 6 + § Known Threat Patterns)
    - .planning/phases/32-sigstore-integration/32-04-SUMMARY.md (broker dispatch site insertion + dev-skip rationale)
    - .planning/phases/32-sigstore-integration/32-02-SUMMARY.md (cached-root rewrite + 6-caller migration + verify-is-offline test)
    - .planning/phases/31-broker-process-architecture-shell-01/31-04-SUMMARY.md (release pipeline contract: both binaries signed by same identity)
  </read_first>
  <action>
    Step 1 — Write `docs/architecture/broker-trust-anchor.md` (ADR for D-32-13).

    Match the format of existing ADRs (`docs/architecture/audit-bundle-target.md`, `docs/architecture/aipc-unix-futures.md`). Required sections:

    - Title: "ADR: Broker.exe Authenticode Self-Trust-Anchor"
    - Status: Accepted (2026-05-XX)
    - Context: Phase 31 broker dispatch arm spawns nono-shell-broker.exe with no signature verification. Naive options: (a) baked-in expected publisher CN constant; (b) config-file trust anchor; (c) Sigstore bundle alongside the binary.
    - Decision: Self-introspection. nono.exe extracts ITS OWN Authenticode signature at every broker dispatch via Phase 28's `query_authenticode_status` (called on `current_exe()`), then requires broker.exe's signature to match (subject AND thumbprint). Trust = "broker is signed by whoever signed me."
    - Consequences:
      - Pro: No baked-in constants. Naturally handles publisher-cert rotation (both binaries roll together via Phase 31 Plan 04 release pipeline). Self-bootstrapping (no chicken-and-egg: if nono.exe runs, the OS trusts it via CodeIntegrity).
      - Pro: Reuses Phase 28 chain-walker primitives unchanged.
      - Pro: ~30 lines of glue at the dispatch site, no new FFI.
      - Con: A compromised release pipeline that signs both binaries with the same hostile cert defeats this (acceptable: relies on Authenticode chain-of-trust + revocation pipeline).
      - Con: TOCTOU window between `query_authenticode_status(broker)` and `CreateProcessW(broker)` (acceptable: requires write-access to install dir = already admin).
      - Con: Dev-build broker is unsigned → must skip in dev (see Skip Mechanism below).
    - Skip Mechanism: install-layout detector (substring `\target\debug\` or `\target\release\` on `current_exe()`). NOT `#[cfg(debug_assertions)]` because `cargo test --release` would falsely trigger production checks against an unsigned dev broker. Tracing-info on every activation so any unexpected production hit shows in logs.
    - Threat Model: see Plan 04's threat-model block — STRIDE table covers spoofing (different signer, unsigned, no escape hatch), tampering (TOCTOU, false-positive detector), DoS (chain check on damaged PE).
    - References: 32-CONTEXT.md D-32-11..14; 32-RESEARCH.md Pattern 5 + Pitfalls 5, 6; Phase 28 chain-walker; Phase 31 Plan 04 release pipeline.

    Step 2 — Write `docs/architecture/sigstore-tuf-cache.md` (ADR for D-32-01).

    - Title: "ADR: Sigstore TUF Root Cache + Verify-Is-Offline Invariant"
    - Status: Accepted (2026-05-XX)
    - Context: sigstore-rs 0.6.5 ships a stale TUF root that fails the threshold-of-3 signature check on Windows ("0 valid signatures of required 3" — quick task 260509-s9m). `TrustedRoot::production().await` calls TUF protocol every time = Sigstore-uptime dependency on every verify + privacy leak (every verify phones home).
    - Decision: Cache the verified `TrustedRoot` JSON at `<nono_home_dir()>/.nono/trust-root/trusted_root.json`, refreshed via `nono setup --refresh-trust-root` (per-user, no admin). `crates/nono/src/trust/bundle.rs::load_production_trusted_root` rewritten as a sync cache-reader. Verify NEVER does inline network — verify-is-offline invariant.
    - Consequences:
      - Pro: Verify offline. Hermetic CI tests possible via frozen fixture (D-32-02). No Sigstore-uptime DoS vector.
      - Pro: Per-user (no admin required for refresh). Per-user-MSI install convention preserved.
      - Pro: Honors `NONO_TEST_HOME` test seam (Phase 27.1 D-27.1-08).
      - Con: User must run `nono setup --refresh-trust-root` once before first keyless verify (D-32-05 first-run UX). Fail-closed with clear recovery hint.
      - Con: Library API surface change: `pub async fn load_production_trusted_root() -> Result` → `pub fn` (D-19 deviation, enumerated in D-32-15, documented in `bundle.rs` doc-comment + `tests/integration/test_upstream_drift.sh:257` annotation).
    - Expiry Detection: string-prefix date comparison `"YYYY-MM-DD" < &valid_for_end[..10]` (D-19 invariant — no `chrono` dep added). Fail-closed when no active tlog has end > now. Pitfall 3 caveat: an EXPIRED RETIRED tlog is normal (kept for historical bundle verification); only the absence of any current key triggers fail-closed.
    - Caller-Site Cascade: 6 sites in `crates/nono-cli/` dropped their `rt.block_on(load_production_trusted_root())` wrapper (Plan 02 Task 1).
    - Test Strategy: frozen TUF fixture (D-32-06 indefinite pin) + `load_test_trusted_root()` helper (D-32-15 #2). Production-cache path NEVER read in tests.
    - References: 32-CONTEXT.md D-32-01..06, D-32-15; 32-RESEARCH.md Pattern 1, 2, 3 + Pitfalls 2, 3, 4, 7; quick task 260509-s9m for the failure that triggered the phase.

    Both ADRs MUST cross-reference each other (broker-trust-anchor.md mentions the TUF cache ADR explains where Sigstore primitives come from; sigstore-tuf-cache.md mentions the broker ADR for context on Phase 32's other deliverable).

    Step 3 — CONTEXT.md backfill (P32-CHK-014).

    The Phase 32 CONTEXT.md `<canonical_refs>` block at line ~103 cites
    `crates/nono-cli/src/exec_strategy_windows/launch.rs § WindowsTokenArm::BrokerLaunch (:2173+)`
    — this anchor was incorrect (`:2173+` points into the test module).
    The actual broker dispatch arm is at `launch.rs:1246-1438` (verified by
    grep during PATTERNS.md preparation).

    Edit `.planning/phases/32-sigstore-integration/32-CONTEXT.md` line 103
    (or whichever line currently cites `:2173+`):

    ```diff
    - `crates/nono-cli/src/exec_strategy_windows/launch.rs` § `WindowsTokenArm::BrokerLaunch` (:2173+) — broker dispatch arm. D-32-13 + D-32-14 add Authenticode verification here before the `CreateProcessAsUserW` call.
    + `crates/nono-cli/src/exec_strategy_windows/launch.rs` § `WindowsTokenArm::BrokerLaunch` (:1246-1438) — broker dispatch arm. D-32-13 + D-32-14 add Authenticode verification here before the `CreateProcessAsUserW` call. (Originally cited `:2173+` which is the test module; corrected during planning per PATTERNS.md verification — see 32-PATTERNS.md anchor-correction note.)
    ```

    This is a documentation-only edit; CONTEXT.md is project-internal planning
    state and does not affect production code. The canonical_refs block exists
    so downstream agents can locate the right code paths; an incorrect anchor
    misdirects them. P32-CHK-014 surfaced this as INFO-severity but worth
    fixing here while we're updating phase docs.

    Step 4 — Verify.
    - `test -f docs/architecture/broker-trust-anchor.md`
    - `test -f docs/architecture/sigstore-tuf-cache.md`
    - `grep -c ":2173+" .planning/phases/32-sigstore-integration/32-CONTEXT.md` returns 0 (P32-CHK-014 backfill complete).
    - `grep -c ":1246-1438" .planning/phases/32-sigstore-integration/32-CONTEXT.md` returns >= 1.
    - Manual review for completeness (no checkpoint required at this task — Task 2 is the audit checkpoint).
    - Both files have a `## References` section linking to CONTEXT.md, RESEARCH.md, the failing tests, and adjacent phases.
  </action>
  <verify>
    <automated>test -f docs/architecture/broker-trust-anchor.md &amp;&amp; test -f docs/architecture/sigstore-tuf-cache.md &amp;&amp; grep -c "self-trust-anchor" docs/architecture/broker-trust-anchor.md &amp;&amp; grep -c "verify-is-offline" docs/architecture/sigstore-tuf-cache.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/architecture/broker-trust-anchor.md` (file exists).
    - `test -f docs/architecture/sigstore-tuf-cache.md` (file exists).
    - `grep -c "self-trust-anchor" docs/architecture/broker-trust-anchor.md` returns ≥ 2.
    - `grep -c "verify-is-offline" docs/architecture/sigstore-tuf-cache.md` returns ≥ 1.
    - `grep -c "D-32-13" docs/architecture/broker-trust-anchor.md` returns ≥ 1 (decision ID traceability).
    - `grep -c "D-32-01" docs/architecture/sigstore-tuf-cache.md` returns ≥ 1.
    - Both ADRs have a `## Status` line containing `Accepted`.
    - Both ADRs have a `## References` section.
    - Cross-references: `grep "sigstore-tuf-cache" docs/architecture/broker-trust-anchor.md` returns ≥ 1 hit; `grep "broker-trust-anchor" docs/architecture/sigstore-tuf-cache.md` returns ≥ 1 hit.
    - File length: each ADR is at least 80 lines (substantive content, not a stub).
    - P32-CHK-014: `grep -c ":2173+" .planning/phases/32-sigstore-integration/32-CONTEXT.md` returns 0 (stale launch.rs:2173+ citation removed).
    - P32-CHK-014: `grep -c ":1246-1438" .planning/phases/32-sigstore-integration/32-CONTEXT.md` returns >= 1 (corrected anchor in place).
  </acceptance_criteria>
  <done>
    Two ADRs written matching existing format conventions; both decisions (D-32-01 cached TUF root + D-32-13 self-trust-anchor) recorded for future maintainers with rationale, threat model, and references.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Release-pipeline audit (read release.yml + decide keyed-vs-keyless posture)</name>
  <files>.planning/deferred-items.md</files>
  <read_first>
    - .github/workflows/release.yml (full file — current signing posture)
    - .planning/phases/32-sigstore-integration/32-CONTEXT.md (D-32-10 verbatim — "Phase 32 includes a release-pipeline audit task: read `.github/workflows/release.yml`, document current signing posture (keyed vs keyless), decide whether to migrate. Recommendation pending audit is to keep whatever current is and document it; if keyed, ship a baked-in `~/.nono/trust-policy.json` template wiring D-32-08's identity for the project's release pipeline. Migration to keyless is itself out of scope for Phase 32 — that's a separate decision recorded as a v2.4+ candidate.")
    - .planning/deferred-items.md (existing v2.4+ candidate format — match style)
    - .planning/phases/31-broker-process-architecture-shell-01/31-04-SUMMARY.md (Phase 31 Plan 04 release pipeline narrative)
  </read_first>
  <what-built>
    Plan 03 Task 2 already shipped the BAKED-IN `trust-policy.json` template at `docs/templates/trust-policy-keyless-template.json` with the canonical GitHub Actions identity pattern (issuer=token.actions.githubusercontent.com, repository=always-further/nono, workflow=.github/workflows/release.yml, ref_pattern=^refs/tags/v.*$). Plans 01-04 are code-complete. What remains for D-32-10 is the human judgment call: read release.yml, decide whether to migrate to keyless OR keep the current keyed posture, and record the verdict in `.planning/deferred-items.md` as a v2.4+ entry.
  </what-built>
  <how-to-verify>
    1. Read `.github/workflows/release.yml` end-to-end. Identify:
       - Current Windows .exe signing mechanism (the `scripts/sign-windows-artifacts.ps1` invocation per Phase 4 STATE — RFC 3161 timestamping, signtool /tr /td sha256).
       - Whether `cosign sign-blob` or `sigstore-rs --keyless` is invoked anywhere (probably NOT — Phase 4 + Phase 22 OAUTH-* deferred Sigstore for release artifacts).
       - The cert source: `WINDOWS_SIGNING_CERT` + `WINDOWS_SIGNING_CERT_PASSWORD` repo secrets (per Phase 4 STATE).

    2. Decide the verdict:
       - **Option A — Keep keyed (recommended default per D-32-10):** documents the existing posture; ships the baked-in `trust-policy.json` template (already done in Plan 03); records "migrate to keyless" as a v2.4+ deferred item with concrete entry criteria.
       - **Option B — Migrate to keyless now:** out of scope per D-32-10's explicit deferral. Reject this option unless evidence in release.yml suggests keyless is already wired and keyed is dead code.

    3. Write findings to `.planning/deferred-items.md` as a new v2.4+ entry (or extend existing Phase 32 entry). Required content:
       - **Title:** "v2.4+ candidate — keyless migration of release.yml signing"
       - **Trigger:** Phase 32 Plan 05 audit (this checkpoint)
       - **Current posture:** [from step 1 — name the script + cert sources]
       - **Why deferred:** D-32-10 explicit; migration entails (a) repo-secret rotation (id-token: write OIDC permissions on the workflow), (b) Sigstore Bundle artifact packaging, (c) consumer-side update to `nono trust verify --keyless` against tagged releases. Out of scope for Phase 32.
       - **Entry criteria for v2.4+:** [name 2-3 conditions, e.g., "if a customer needs to verify release artifacts via Sigstore for compliance reasons; if WINDOWS_SIGNING_CERT secret rotation becomes an operational pain point"]
       - **References:** D-32-10, this plan's SUMMARY, Phase 32 trust-policy template at `docs/templates/`.

    4. Also add deferred-items entries (or extend Phase 32 row) for the test-fixture follow-ups surfaced in Plans 03-04:
       - Mock Fulcio cert + Rekor entry capture (Plan 03 Task 2 — graduates the `keyless_sign_then_verify_roundtrip` test from `#[ignore]` to active).
       - Two-publisher `broker-mismatch-stub.exe` fixture (Plan 04 Task 2 — graduates `broker_signature_mismatch_refuses_spawn` and `broker_unsigned_release_refuses_spawn` from SKIP-when-fixture-missing to actively verified).

    Provide the deferred-items.md diff to the human reviewer for final sign-off, since the audit verdict is judgment-based.
  </how-to-verify>
  <action>This is a checkpoint:human-verify — see &lt;what-built&gt; and &lt;how-to-verify&gt; above for the operator's procedure. The executor reads `.github/workflows/release.yml`, drafts the deferred-items.md entry, then pauses for human sign-off on the audit verdict before committing.</action>
  <verify>
    <automated>grep -c "Phase 32" .planning/deferred-items.md &amp;&amp; grep -c "v2.4" .planning/deferred-items.md &amp;&amp; grep -ci "release.yml\|keyless migration" .planning/deferred-items.md</automated>
  </verify>
  <resume-signal>
    Type one of:
    - "audit-complete keyed-stays" if Option A (keep keyed, record migration as v2.4+ deferred): proceed and commit deferred-items.md.
    - "audit-complete keyless-migrate-now" if Option B (migration in scope after all): WARNING — this contradicts D-32-10. Confirm by replying again with "audit-confirm-keyless-now" to override.
    - "audit-blocked <reason>" if release.yml inspection reveals an issue requiring further investigation.
  </resume-signal>
  <acceptance_criteria>
    - `.planning/deferred-items.md` has a new entry (or extended existing Phase 32 entry) titled "v2.4+ candidate — keyless migration of release.yml signing" (or substantively equivalent).
    - The entry includes: trigger, current posture, why deferred, entry criteria, references.
    - Mock-Fulcio/Rekor capture and broker-mismatch-stub.exe fixture are recorded as deferred follow-ups (either as separate entries or as bullet items under the Phase 32 entry).
    - Human reviewer signed off on the audit verdict via the resume-signal.
  </acceptance_criteria>
  <done>
    Audit verdict recorded in deferred-items.md; human reviewer signed off; D-32-10 implementation half (template) + audit half (deferred entry) both complete.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update Windows POC handoff cookbook with --refresh-trust-root prereq + --issuer/--identity examples</name>
  <files>docs/cli/development/windows-poc-handoff.mdx</files>
  <read_first>
    - docs/cli/development/windows-poc-handoff.mdx (full file — confirm structure; locate the `260509-stb` block-net WFP-required prereq section that Phase 32 mirrors)
    - .planning/quick/260509-stb/260509-stb-SUMMARY.md (the precedent block-net cookbook addition)
    - .planning/phases/32-sigstore-integration/32-CONTEXT.md (§ Specifics — POC handoff cookbook update + documented --issuer / --identity examples)
    - .planning/phases/32-sigstore-integration/32-02-SUMMARY.md (cached-root + setup --refresh-trust-root surface)
    - .planning/phases/32-sigstore-integration/32-03-SUMMARY.md (--issuer / --identity flag surface; trust-policy template)
    - .planning/phases/32-sigstore-integration/32-04-SUMMARY.md (broker Authenticode error message text — match it verbatim)
  </read_first>
  <action>
    Step 1 — Locate the existing `260509-stb` block-net WFP-required prereq section in `docs/cli/development/windows-poc-handoff.mdx`. Use it as a structural template — the Sigstore additions should be in a parallel section with parallel formatting.

    Step 2 — Add a new section "Sigstore Trust Root Setup" near the existing block-net section. Match the precedent's tone (matter-of-fact, fail-closed-correct framing, no apology):

    ```mdx
    ## Sigstore Trust Root Setup (one-time per user)

    `nono trust verify --keyless` requires a refreshed Sigstore trusted root cached
    at `~/.nono/trust-root/trusted_root.json`. The cache is per-user (no admin)
    and refreshes when you explicitly run setup.

    Run once after install:

    ```powershell
    nono setup --refresh-trust-root
    ```

    The command fetches TUF metadata from `https://tuf-repo-cdn.sigstore.dev` and
    writes the verified trusted root to your per-user cache. Subsequent
    `nono trust verify --keyless` invocations run offline against that cache.

    **Fail-closed contract:** if the cache is absent or expired, `nono trust verify --keyless`
    fails with a recovery message naming the exact command — verify NEVER attempts
    inline network refresh. See [`docs/architecture/sigstore-tuf-cache.md`](../../architecture/sigstore-tuf-cache.md)
    for the design rationale (Phase 32 D-32-01).

    **Check status:**

    ```powershell
    nono setup --check-only
    ```

    Look for the `Trust root cache:` line — `OK` (with cache path), `STALE` (with diagnostic),
    or `NOT INITIALIZED` (run `--refresh-trust-root` to fix).
    ```

    Step 3 — Add a "Verifying Keyless-Signed Artifacts" subsection with the canonical examples per CONTEXT § Specifics:

    ```mdx
    ### Verifying Keyless-Signed Artifacts

    `nono trust verify --keyless` requires explicit `--issuer` (OIDC issuer URL,
    exact match) and `--identity` (regex against the bundle SAN). Both flags are
    fail-closed-mandatory — verify rejects bundles when either is missing.

    **GitHub Actions release** (canonical pattern):

    ```powershell
    nono trust verify --keyless `
      --issuer https://token.actions.githubusercontent.com `
      --identity '^https://github\.com/<org>/<repo>/\.github/workflows/release\.yml@refs/tags/v.*$' `
      <bundle-file>
    ```

    Example for the `nono` project itself:

    ```powershell
    nono trust verify --keyless `
      --issuer https://token.actions.githubusercontent.com `
      --identity '^https://github\.com/always-further/nono/\.github/workflows/release\.yml@refs/tags/v.*$' `
      instruction.md
    ```

    **GitLab CI release**:

    ```powershell
    nono trust verify --keyless `
      --issuer <gitlab-issuer-url> `
      --identity '<gitlab-identity-pattern>' `
      <bundle-file>
    ```

    A baked-in policy template lives at [`docs/templates/trust-policy-keyless-template.json`](../../templates/trust-policy-keyless-template.json) — copy to `~/.nono/trust-policy.json` to make `--policy` invocations match the project's release pipeline by default.

    **Local development:** keyless signing is CI-only (no interactive browser OAuth). For local dev, use:

    ```powershell
    nono trust sign --keyref <key-path> <file>
    nono trust verify <file>  # keyed verify; no --issuer/--identity needed
    ```
    ```

    Step 4 — Add a brief "Broker Authenticode Self-Trust-Anchor" note (D-32-11..14):

    ```mdx
    ### Broker.exe Verification at Launch

    `nono shell --profile <name>` on Windows spawns `nono-shell-broker.exe` (a sibling
    of `nono.exe` in the install directory). Phase 32 adds an Authenticode
    self-trust-anchor verification before the spawn: nono.exe extracts ITS OWN
    signature, then requires broker.exe's signature to match (subject + thumbprint).

    On a properly-installed release MSI, both binaries are signed by the same
    pipeline identity — verification passes silently.

    **Fail mode:** if a malicious sibling broker is dropped in, or if either
    binary's signature is unverifiable, `nono shell` fails with:

    ```
    Trust verification failed for <broker path>: broker.exe Authenticode
    signature does not match nono.exe — expected subject `<X>` thumbprint
    `<Y>`, got subject `<X'>` thumbprint `<Y'>`. Refusing to spawn.
    ```

    No escape-hatch flag exists by design (see [`docs/architecture/broker-trust-anchor.md`](../../architecture/broker-trust-anchor.md)).
    Recovery: reinstall from the official MSI.

    **Dev-build note:** `cargo build`-built `nono.exe` runs out of `target/(debug|release)/`
    — the install-layout detector activates and SKIPS verification (logged at info-level).
    Production installs (`Program Files\nono\` or `LocalAppData\Programs\nono\`)
    do not match this pattern and always enforce verification.
    ```

    Step 5 — Cross-reference the two new ADRs from the cookbook's "References" section (or equivalent footer):

    ```mdx
    ## References

    - ...existing references...
    - [Sigstore TUF Cache ADR](../../architecture/sigstore-tuf-cache.md) — Phase 32 D-32-01
    - [Broker Trust Anchor ADR](../../architecture/broker-trust-anchor.md) — Phase 32 D-32-13
    - Phase 32 CONTEXT — `.planning/phases/32-sigstore-integration/32-CONTEXT.md`
    ```

    Step 6 — Verify.
    - `test -f docs/cli/development/windows-poc-handoff.mdx` (file unchanged in path).
    - `grep -c "nono setup --refresh-trust-root" docs/cli/development/windows-poc-handoff.mdx` returns ≥ 2.
    - `grep -c "\-\-issuer" docs/cli/development/windows-poc-handoff.mdx` returns ≥ 3.
    - `grep -c "\-\-identity" docs/cli/development/windows-poc-handoff.mdx` returns ≥ 3.
    - `grep -c "always-further/nono" docs/cli/development/windows-poc-handoff.mdx` returns ≥ 1 (canonical example).
    - `grep -c "Authenticode signature does not match" docs/cli/development/windows-poc-handoff.mdx` returns 1 (matches Plan 04's error text verbatim).
    - `grep -c "broker-trust-anchor" docs/cli/development/windows-poc-handoff.mdx` returns ≥ 1.
    - `grep -c "sigstore-tuf-cache" docs/cli/development/windows-poc-handoff.mdx` returns ≥ 1.
    - Mintlify build (if available locally) renders without broken-link warnings: `mintlify dev` or equivalent.
  </action>
  <verify>
    <automated>grep -c "nono setup --refresh-trust-root" docs/cli/development/windows-poc-handoff.mdx &amp;&amp; grep -c -- "--issuer" docs/cli/development/windows-poc-handoff.mdx &amp;&amp; grep -c -- "--identity" docs/cli/development/windows-poc-handoff.mdx &amp;&amp; grep -c "Authenticode signature does not match" docs/cli/development/windows-poc-handoff.mdx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "nono setup --refresh-trust-root" docs/cli/development/windows-poc-handoff.mdx` returns ≥ 2.
    - `grep -E "always-further/nono" docs/cli/development/windows-poc-handoff.mdx` matches at least once (canonical GitHub Actions example).
    - `grep -c "token.actions.githubusercontent.com" docs/cli/development/windows-poc-handoff.mdx` returns ≥ 1 (canonical issuer).
    - `grep -c "Authenticode signature does not match" docs/cli/development/windows-poc-handoff.mdx` returns 1 (broker mismatch error text verbatim).
    - `grep -c "broker-trust-anchor" docs/cli/development/windows-poc-handoff.mdx` returns ≥ 1 (cross-link to ADR).
    - `grep -c "sigstore-tuf-cache" docs/cli/development/windows-poc-handoff.mdx` returns ≥ 1.
    - `grep -c "trust-policy-keyless-template" docs/cli/development/windows-poc-handoff.mdx` returns ≥ 1 (cross-link to template).
    - Mintlify build does not surface link-validation errors against the new content (manual verification on local Mintlify if available).
    - The `260509-stb` block-net WFP-required prereq section structure is matched (parallel section heading, parallel powershell code blocks, parallel fail-closed callouts).
  </acceptance_criteria>
  <done>
    Cookbook has a Sigstore Trust Root Setup section + Verifying Keyless-Signed Artifacts subsection + Broker.exe Verification note + cross-references to both new ADRs and the trust-policy template.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADR durability | ADRs are project-internal documentation; their content informs future maintainers but does not run in production. No runtime trust boundary; their accuracy gates whether future readers correctly understand the design. |
| Cookbook → user behavior | Cookbook directs operators on how to invoke `--issuer` / `--identity` correctly. An over-broad documented pattern (e.g., `--identity '.*'`) silently weakens user verifications. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05-01 | T (Tampering) | Cookbook documents over-broad `--identity` regex | mitigate | Canonical examples lock down to the project's specific GitHub Actions / GitLab CI patterns (per CONTEXT § Specifics). The `^https://github\.com/<org>/<repo>/\.github/workflows/release\.yml@refs/tags/v.*$` shape pins org, repo, workflow file, AND the tag prefix — narrow enough to reject unrelated runs in the same org. |
| T-32-05-02 | I (Information disclosure) | ADR exposes design details to attackers | accept | Public-good defense-in-depth design + we already document the threat model in the codebase (CLAUDE.md security considerations are project-internal but visible in any repo clone). Security through obscurity isn't part of the contract. |
| T-32-05-03 | T (Tampering) | Future migration to keyless silently changes verify expectations | mitigate | Audit verdict + entry criteria recorded in `.planning/deferred-items.md` under v2.4+ candidate. If migration happens, the corresponding cookbook + ADR updates are part of that future phase's deliverables. Phase 32's documentation is point-in-time correct. |
| T-32-05-04 | E (Elevation of privilege) | Cookbook accidentally documents the (non-existent) escape hatch | mitigate | Acceptance criteria enforces "No escape-hatch flag" wording in the broker section and refers users to "reinstall from the official MSI" as the recovery. No NONO_BROKER_VERIFY=off mention. |
</threat_model>

<verification>
- Both ADRs exist and pass content-grep acceptance criteria.
- Cookbook updates pass content-grep acceptance criteria.
- `.planning/deferred-items.md` has Phase 32 follow-up entries with audit verdict.
- Human reviewer signed off on the audit verdict (Task 2 checkpoint resolved).
- `make ci` still passes (no code changes in this plan, but doc-builds may surface link warnings — those are gating).
</verification>

<success_criteria>
- D-32-10 fully closed: trust-policy template (Plan 03 Task 2 implementation half) + release.yml audit (this plan's Task 2 documentation half) + v2.4+ deferred entry recorded.
- Two ADRs document the novel decisions for future maintainers (broker-trust-anchor, sigstore-tuf-cache).
- POC handoff cookbook is in sync with Phase 32's fail-closed contracts (trust-root prereq, --issuer/--identity examples, broker error text).
- v2.4+ follow-ups recorded for: keyless migration of release.yml, mock-Fulcio cert capture, broker-mismatch-stub.exe fixture, keyless_sign_then_verify_roundtrip graduation.
- Phase 32 closes; v2.3 milestone unblocks.
</success_criteria>

<output>
After completion, create `.planning/phases/32-sigstore-integration/32-05-SUMMARY.md` documenting:
- Both ADR file paths + a one-line summary of each decision
- Cookbook section names + cross-links added
- Release-pipeline audit verdict (Option A keyed-stays vs Option B keyless-migrate; in practice expected: A) and the corresponding deferred-items entry
- Three v2.4+ follow-ups recorded (release.yml migration, mock fixtures, broker-mismatch fixture)
- VALIDATION.md row transitions: D-32-10 ❌ → ✅ (both implementation halves complete after this plan + Plan 03 Task 2 trust-policy template)
- Phase 32 close-out checklist confirming all 16 D-32-XX decisions are addressed by Plans 01-05
- Pointer to next steps: `/gsd-verify-work 32` to gate phase close, then `/gsd-complete-milestone v2.3` if all v2.3 phases are green
</output>
