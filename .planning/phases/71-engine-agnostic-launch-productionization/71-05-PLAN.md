---
phase: 71-engine-agnostic-launch-productionization
plan: 05
type: execute
wave: 3
depends_on: ["71-01", "71-02", "71-03", "71-04"]
files_modified:
  - .planning/phases/71-engine-agnostic-launch-productionization/71-HUMAN-UAT.md
autonomous: false
requirements: [ENG-01]
must_haves:
  truths:
    - "An operator has a precise, runnable UAT script proving Aider is confined end-to-end on a real Win11 host"
    - "The script states the R-B4 (dev-layout/signed nono.exe), real-PowerShell-console, and user-owned-workspace preconditions"
    - "The script proves: write inside the granted absolute workspace lands; write outside is denied (NO_WRITE_UP); the engine's subprocess (python.exe) ops are confined transitively"
  artifacts:
    - path: ".planning/phases/71-engine-agnostic-launch-productionization/71-HUMAN-UAT.md"
      provides: "SC1 real-Win11 Aider end-to-end UAT script + per-engine fit notes + preconditions"
      min_lines: 60
  key_links:
    - from: "71-HUMAN-UAT.md"
      to: "nono run --profile aider --workspace <abs> -- aider.exe"
      via: "the engine-neutral launch command under test"
      pattern: "--profile aider"
---

<objective>
Author the `71-HUMAN-UAT.md` real-Win11 acceptance script for SC1 (ENG-01) — Aider confined end-to-end through the productionized engine-neutral launch path — and run it as a human-verify checkpoint. SC1 cannot be unit-tested (the broker arm requires a real host + real Aider install + the Low-IL relabel + AppContainer path), so it is gated here on the operator.

Purpose: SC1 is the milestone-foundation proof — every later v2.12 phase (72/73/74/75) sits on this launch path being demonstrably sound on a real machine.
Output: a precise UAT doc + an executed human-verify checkpoint capturing the result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-engine-agnostic-launch-productionization/71-CONTEXT.md
@.planning/phases/71-engine-agnostic-launch-productionization/71-RESEARCH.md
@.planning/phases/71-engine-agnostic-launch-productionization/71-VALIDATION.md
@.planning/phases/71-engine-agnostic-launch-productionization/71-01-SUMMARY.md
@.planning/phases/71-engine-agnostic-launch-productionization/71-04-SUMMARY.md

<interfaces>
<!-- Preconditions sourced from RESEARCH Pitfall 4 + Environment Availability + project memory: -->
- R-B4 broker-trust gate: BrokerLaunchNoPty calls verify_broker_authenticode UNLESS is_dev_build_layout
  (launch.rs:1375,1698). Unsigned Program-Files installs are correctly REFUSED. → Run SC1 from a
  dev-layout `target\release\nono.exe` (or a signed install).
- Real PowerShell console required (NOT git-bash/MSYS) — else CreateProcessAsUserW GLE=87.
- Workspace MUST be user-owned with WRITE_OWNER (R-B3) — use %USERPROFILE%\nono-work, NOT a drive-root
  C:\ subdir created elevated.
- Aider on the host: `pipx install aider-chat` (or `pip install aider-install && aider-install`). Verify
  on PyPI at UAT time ([ASSUMED] — engine-under-test, not a nono dependency).
- AppContainer SID: CreateAppContainerProfile (not derive-only, else ERROR_FILE_NOT_FOUND);
  preserve SystemRoot/windir/SystemDrive env baseline (else CLR 0xFFFF0000) — both already handled by
  the broker arm; re-assert in the doc as "what to watch if it fails".
- Fallback engine: if Aider install is problematic, raw `python.exe` via the `langchain-python` profile
  independently proves "engine is a variable" (spike-003 used python as the strongest proof).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 71-HUMAN-UAT.md</name>
  <read_first>
    - .planning/phases/71-engine-agnostic-launch-productionization/71-RESEARCH.md (Validation Architecture → manual SC1 row; Pitfall 4 R-B4; Environment Availability; Common Pitfalls 1-3 as "what to watch")
    - .planning/phases/71-engine-agnostic-launch-productionization/71-VALIDATION.md (Manual-Only Verifications table — the exact SC1 behavior + test instructions skeleton)
    - a prior phase HUMAN-UAT doc for format (search `.planning/phases/` for `*-HUMAN-UAT.md`, e.g. Phase 58 or 60) — mirror its structure (preconditions, numbered steps, expected output, pass/fail capture)
  </read_first>
  <action>
    Write `.planning/phases/71-engine-agnostic-launch-productionization/71-HUMAN-UAT.md` modeled on a prior phase HUMAN-UAT doc. It MUST contain:
    1. Preconditions block: dev-layout `target\release\nono.exe` (R-B4); real PowerShell console (not git-bash); user-owned workspace `%USERPROFILE%\nono-work` (R-B3 — WRITE_OWNER); Aider installed (`pipx install aider-chat`), with the `langchain-python` + raw `python.exe` fallback noted; the AppContainer/CLR gotchas to watch (CreateAppContainerProfile; SystemRoot/windir/SystemDrive baseline).
    2. SC1 primary test — the exact command `nono run --profile aider --workspace %USERPROFILE%\nono-work -- aider.exe <minimal-args>`, with numbered steps that prove: (a) a write INSIDE `%USERPROFILE%\nono-work` LANDS; (b) a write OUTSIDE it (e.g. a relative write that would resolve outside, and an explicit `..\outside.txt`) is DENIED with `NO_WRITE_UP`/access-denied; (c) the engine's SUBPROCESS (`python.exe` spawned by aider.exe) is confined transitively (a python-initiated write outside the workspace is denied) — proving nono is the PARENT, not a per-tool hook. Each step states the EXPECTED observable outcome.
    3. SC2/CWD assertion — confirm a RELATIVE write by the engine resolves INSIDE the absolute workspace (the PowerShell→C:\ trap is gone): show the file appears under `%USERPROFILE%\nono-work`, not `C:\`.
    4. Fail-secure spot-checks (optional but recommended, exercising ENG-02 live): (a) point `--workspace` at an admin-owned drive-root dir → expect the named R-B3 refusal BEFORE launch; (b) run an engine whose interpreter is uncovered → expect the named interpreter-coverage refusal naming the exact `python.exe`.
    5. Per-engine fit notes (SC5 doc deliverable): a short table — launch-and-confine (Aider, LangChain-Python, Copilot — engine-neutral parent-and-confine) vs per-tool hook (Claude Code PreToolUse, the legacy path) vs Cursor (WSL-only, engine limitation) — so SC5's "per-engine fit is documented" is satisfied.
    6. A pass/fail capture section the operator fills in (date, host build, Aider version, observed outcomes per step, NO_WRITE_UP confirmation).
    Do NOT claim SC1 passed — this task only AUTHORS the script. The checkpoint (Task 2) runs it.
  </action>
  <acceptance_criteria>
    - Artifact assertion: `.planning/phases/71-engine-agnostic-launch-productionization/71-HUMAN-UAT.md` exists with ≥ 60 lines.
    - Content assertion: `grep -n "nono run --profile aider --workspace" 71-HUMAN-UAT.md` shows the exact engine-neutral launch command.
    - Content assertion: the doc names all three preconditions (dev-layout/R-B4, real PowerShell console, user-owned %USERPROFILE% workspace) AND the NO_WRITE_UP outside-write expectation AND the python.exe transitive-subprocess proof AND the per-engine-fit table (SC5 doc deliverable).
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/71-engine-agnostic-launch-productionization/71-HUMAN-UAT.md && grep -q "NO_WRITE_UP" .planning/phases/71-engine-agnostic-launch-productionization/71-HUMAN-UAT.md && grep -q "\-\-profile aider \-\-workspace" .planning/phases/71-engine-agnostic-launch-productionization/71-HUMAN-UAT.md && echo OK</automated>
  </verify>
  <done>71-HUMAN-UAT.md exists with preconditions, the exact SC1 command, inside-lands/outside-denied/transitive-subprocess steps, the SC2 relative-write-CWD check, the ENG-02 live fail-secure spot-checks, the SC5 per-engine-fit table, and an operator pass/fail capture section.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: SC1 — run Aider end-to-end on a real Win11 host</name>
  <what-built>
    The productionized engine-neutral launch path (Plans 01-04): the `aider` profile + interpreter coverage gate + `--workspace` absolute-grant/child-CWD + R-B3 pre-launch gate + SC5 foreign-job diagnostic. Plus the `71-HUMAN-UAT.md` script (Task 1).
  </what-built>
  <how-to-verify>
    On a real Win11 host (NOT this dev environment), follow `71-HUMAN-UAT.md` exactly:
    1. Build dev-layout: `cargo build --release -p nono-cli` and use `target\release\nono.exe` (R-B4 — do NOT use a Program-Files install; it will be refused at the broker Authenticode gate).
    2. Open a REAL PowerShell console (NOT git-bash/MSYS — else CreateProcessAsUserW GLE=87).
    3. `pipx install aider-chat` (or the langchain-python + raw python.exe fallback if Aider install is problematic).
    4. Create the user-owned workspace: `mkdir %USERPROFILE%\nono-work` (NOT an elevated drive-root dir — R-B3).
    5. Run: `target\release\nono.exe run --profile aider --workspace %USERPROFILE%\nono-work -- aider.exe <minimal-args>` and execute the SC1 steps.
    EXPECTED: a write inside `%USERPROFILE%\nono-work` LANDS; a write outside (incl. a relative write and `..\outside.txt`) is DENIED (NO_WRITE_UP / access-denied); a python.exe-initiated write outside the workspace is also DENIED (transitive confinement); a relative write resolves INSIDE the workspace (no PowerShell→C:\ trap). Optionally run the two ENG-02 fail-secure spot-checks (admin-owned workspace → named R-B3 refusal; uncovered interpreter → named coverage refusal).
    Record the outcomes in the `71-HUMAN-UAT.md` pass/fail capture section (host build, Aider version, per-step result).
  </how-to-verify>
  <resume-signal>Type "approved" (with the captured outcomes pasted into 71-HUMAN-UAT.md) or describe which step failed and the observed behavior.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real Aider engine (Low-IL/AppContainer child) → host filesystem | The live proof that the OS-enforced boundary holds for a real non-Claude engine and its subprocesses |
| operator-run dev-layout nono.exe → broker self-trust (R-B4) | The Authenticode gate is bypassed only for dev-layout; a Program-Files unsigned install is correctly refused |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-14 | Elevation of Privilege | engine subprocess (python.exe) escapes confinement | mitigate | SC1 step (c) explicitly proves transitive confinement of the spawned python.exe (parent-and-confine, not per-tool hook). Acceptance: python-initiated outside-write DENIED |
| T-71-15 | Spoofing | unsigned broker spawn (trust-anchor bypass) | mitigate | R-B4: SC1 runs from dev-layout `target\release\nono.exe`; a Program-Files unsigned install is REFUSED by verify_broker_authenticode — documented as a precondition, not a code change. Acceptance: UAT run uses dev-layout per the doc |
| T-71-SC | Tampering | npm/pip/cargo installs | mitigate | Only `pipx install aider-chat` on the UAT HOST (engine-under-test, `[ASSUMED]`) — NOT a nono dependency; verify on PyPI at UAT time. No package added to the nono workspace |
</threat_model>

<verification>
- Doc existence + content checks (Task 1 automated verify).
- SC1 is operator-gated (Task 2 human-verify checkpoint) — the captured outcomes in 71-HUMAN-UAT.md are the acceptance record.
- No code changes in this plan → no cross-target gate.
</verification>

<success_criteria>
- ENG-01 / SC1: Aider confined end-to-end on a real Win11 host — inside-workspace write lands, outside denied (NO_WRITE_UP), subprocess confined transitively — captured in 71-HUMAN-UAT.md.
- SC5 doc deliverable: per-engine fit (launch-and-confine vs hook vs Cursor-WSL-only) is documented.
</success_criteria>

<output>
Create `.planning/phases/71-engine-agnostic-launch-productionization/71-05-SUMMARY.md` when done (referencing the captured UAT outcomes).
</output>
