---
phase: 71-engine-agnostic-launch-productionization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono-cli/src/profile/mod.rs
  - crates/nono-cli/data/nono-profile.schema.json
  - crates/nono-cli/data/policy.json
  - crates/nono-cli/src/profile/builtin.rs
autonomous: true
requirements: [ENG-03]
must_haves:
  truths:
    - "A user can resolve a built-in `aider` engine profile that sets windows_low_il_broker:true and declares python.exe interpreter coverage"
    - "A user can resolve a built-in `langchain-python` engine profile with the same shape"
    - "Every embedded profile (including the two new ones) extends `default`"
  artifacts:
    - path: "crates/nono-cli/src/profile/mod.rs"
      provides: "windows_interpreters Vec<String> field on Profile"
      contains: "windows_interpreters"
    - path: "crates/nono-cli/data/policy.json"
      provides: "aider + langchain-python engine profile entries"
      contains: "\"aider\""
    - path: "crates/nono-cli/data/nono-profile.schema.json"
      provides: "windows_interpreters schema field"
      contains: "windows_interpreters"
  key_links:
    - from: "crates/nono-cli/data/policy.json"
      to: "crates/nono-cli/src/profile/mod.rs"
      via: "serde deserialize of windows_interpreters into Profile"
      pattern: "windows_interpreters"
---

<objective>
Establish the engine-profile contract (D-01/D-02/D-03/ENG-03): add a Windows-only `windows_interpreters` field to the `Profile` struct and schema, then ship the two built-in engine profiles `aider` and `langchain-python` in `policy.json`. An "engine" IS a profile + an interpreter-coverage list — no new resolver, no new config surface.

Purpose: This is the foundational interface consumed by the coverage gate (Plan 03/04). Defining it first means downstream plans receive the contract, not a scavenger hunt.
Output: New profile field + two engine profiles + profile-load tests proving they resolve with the broker flag and interpreter coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-engine-agnostic-launch-productionization/71-CONTEXT.md
@.planning/phases/71-engine-agnostic-launch-productionization/71-PATTERNS.md

<interfaces>
<!-- The canonical Windows-only #[serde(default)] field pattern to mirror, from profile/mod.rs:2229-2237 -->
Profile struct is `#[derive(Debug, Clone, Default, Serialize)]` (mod.rs:2137). Existing analog field:

    /// Windows-only. When true, routes non-PTY supervised launches through
    /// `WindowsTokenArm::BrokerLaunchNoPty` instead of `WriteRestricted`.
    #[serde(default)]
    pub windows_low_il_broker: bool,

Vec<String> #[serde(default)] analogs already on the struct: command_args (mod.rs:2214), skipdirs (2192), packs (2203).

The "default" profile inheritance is enforced by test_embedded_profiles_extend_default (builtin.rs:340-353): iterates every embedded profile and asserts def.extends.as_deref() == Some("default"). list_builtin() (builtin.rs:14) returns all keys — no registration list to update.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add windows_interpreters field to Profile struct + schema</name>
  <read_first>
    - crates/nono-cli/src/profile/mod.rs (read the Profile struct around lines 2130-2240, especially the windows_low_il_broker field at 2229-2237 and the Vec<String> fields command_args/skipdirs/packs)
    - crates/nono-cli/data/nono-profile.schema.json (read the existing optional-field entries to mirror the array-of-string + default shape)
    - .planning/phases/71-engine-agnostic-launch-productionization/71-PATTERNS.md (the "windows_interpreters field" pattern section)
  </read_first>
  <action>
    Add `pub windows_interpreters: Vec<String>` to the `Profile` struct in profile/mod.rs, placed adjacent to `windows_low_il_broker` (mod.rs:2237), with `#[serde(default)]` and a doc-comment matching the Windows-only-runtime convention: bare exe names of the interpreter(s) this engine's launch program will spawn (e.g. `python.exe`), resolved to absolute paths at launch and coverage-checked by `validate_launch_paths`; ignored on Linux/macOS (deserialize-only); empty = no interpreter coverage required. The struct already derives `Default`, so the `Vec` default is free — do NOT add a custom Default impl.
    Mirror the field into crates/nono-cli/data/nono-profile.schema.json as an optional `windows_interpreters` property: `{ "type": "array", "items": { "type": "string" } }`, NOT in the `required` list, matching how `windows_low_il_broker` is declared in the schema. If typify-generated types regenerate from the schema, ensure they stay in sync (per RESEARCH Runtime State Inventory).
    NO `.unwrap()`/`.expect()` in non-test code (clippy::unwrap_used enforced).
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -n "windows_interpreters" crates/nono-cli/src/profile/mod.rs` shows a `#[serde(default)]` `pub windows_interpreters: Vec<String>` field on the Profile struct.
    - Source assertion: `grep -n "windows_interpreters" crates/nono-cli/data/nono-profile.schema.json` shows an array-of-string property NOT in `required`.
    - Build: `cargo build -p nono-cli` compiles clean (the new field deserializes on all platforms).
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p nono-cli</automated>
  </verify>
  <done>Profile carries a `windows_interpreters: Vec<String>` `#[serde(default)]` field; schema mirrors it as optional array-of-string; crate compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add aider + langchain-python engine profiles to policy.json</name>
  <read_first>
    - crates/nono-cli/data/policy.json (read the `"profiles"` object starting at policy.json:598; specifically the `"claude-code"` entry at 656-730 — the ONLY built-in with windows_low_il_broker:true, see the field at 728-729 — and the leaner `"codex"` entry at 796-840 to model shape on; note `"python-dev"` at 877-894 is the ANTI-PATTERN: do NOT retrofit it)
    - .planning/phases/71-engine-agnostic-launch-productionization/71-PATTERNS.md (the policy.json engine-profiles pattern section has the exact `aider` entry to write)
  </read_first>
  <action>
    Add two new entries inside the `"profiles"` object (policy.json:598), modeled on the `codex` shape plus the broker flag and the new interpreter field. Per D-03, ship BOTH:
    `"aider"`: extends "default"; meta name "aider" version "1.0.0" description "Aider AI pair-programming engine (Python entry point)" author "nono-project"; security.groups ["python_runtime", "git_config", "unlink_protection"], signal_mode "isolated"; filesystem {} (the workspace comes from --workspace/cwd, expressed absolutely by Plan 04 — do NOT bake a path here); network { "block": false } (file-confinement proof needs no WFP service per D); workdir { "access": "readwrite" }; windows_low_il_broker true (REQUIRED — routes to BrokerLaunchNoPty); windows_interpreters ["python.exe"].
    `"langchain-python"`: identical shape — same groups, signal_mode "isolated", filesystem {}, network.block false, workdir readwrite, windows_low_il_broker true, windows_interpreters ["python.exe"]; meta name "langchain-python" description "Generic LangChain-Python agent (Python interpreter engine)".
    Both MUST set `"extends": "default"` (enforced by test_embedded_profiles_extend_default, builtin.rs:340). Do NOT modify `python-dev`. No registration list to update — the resolver loads all keys (list_builtin, builtin.rs:14).
    Note: policy.json is embedded via build.rs — a rebuild is required for the entries to appear (`make build` / cargo build picks it up).
  </action>
  <acceptance_criteria>
    - Source assertion: `python -c "import json,sys; d=json.load(open('crates/nono-cli/data/policy.json')); p=d['profiles']; assert 'aider' in p and 'langchain-python' in p; assert p['aider']['windows_low_il_broker'] is True; assert p['aider']['windows_interpreters']==['python.exe']; assert p['aider']['extends']=='default'; assert p['langchain-python']['windows_low_il_broker'] is True; print('ok')"` prints `ok`.
    - Behavior assertion: `python-dev` is unchanged (no `windows_low_il_broker` added to it).
    - Build: `cargo build -p nono-cli` recompiles with the embedded policy.json containing both profiles.
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p nono-cli</automated>
  </verify>
  <done>policy.json `"profiles"` contains `aider` and `langchain-python`, both extending `default`, both with `windows_low_il_broker:true` and `windows_interpreters:["python.exe"]`; `python-dev` untouched; crate embeds them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Profile-load test assertions for both engine profiles</name>
  <read_first>
    - crates/nono-cli/src/profile/builtin.rs (read test_get_builtin_codex at 163-172 for the per-profile assertion pattern; test_embedded_profiles_extend_default at 340-353; test_all_profiles_signal_mode_resolves at 606; list_builtin at 14)
  </read_first>
  <behavior>
    - get_builtin("aider") resolves: meta.name == "aider", security.groups contains "python_runtime", windows_low_il_broker == true, windows_interpreters == ["python.exe"].
    - get_builtin("langchain-python") resolves: meta.name == "langchain-python", windows_low_il_broker == true, windows_interpreters == ["python.exe"].
    - Both extend "default" (already auto-covered by test_embedded_profiles_extend_default once added — no new test needed for that assertion, it sweeps all keys).
  </behavior>
  <action>
    Add `test_get_builtin_aider` and `test_get_builtin_langchain_python` to the test module in profile/builtin.rs, copying the construction style of `test_get_builtin_codex` (builtin.rs:163-172). Assert `meta.name`, the `python_runtime` group membership, `windows_low_il_broker == true`, and `windows_interpreters == vec!["python.exe".to_string()]`. Use `.expect("...")` is permitted in test modules only (clippy::unwrap_used is allowed in tests). Do NOT add a registration list — `list_builtin()` already returns all keys and the existing sweep tests (`test_embedded_profiles_extend_default`, `test_all_profiles_signal_mode_resolves`) exercise the new profiles automatically.
  </action>
  <acceptance_criteria>
    - Test command: `cargo test -p nono-cli --lib test_get_builtin_aider test_get_builtin_langchain_python` passes (both new tests green).
    - Test command: `cargo test -p nono-cli --lib test_embedded_profiles_extend_default` passes (sweep still green with the two new profiles).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-cli --lib test_get_builtin_aider test_get_builtin_langchain_python test_embedded_profiles_extend_default</automated>
  </verify>
  <done>Both new per-profile tests assert name + python_runtime group + broker flag + interpreter coverage and pass; the extend-default sweep stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| policy.json (build-embedded config) → resolved Profile | Declarative engine declaration crosses into the runtime resolver; a malformed/over-broad profile would weaken the gate downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01 | Tampering | `aider`/`langchain-python` profile grants | mitigate | Profiles declare `filesystem {}` (no baked path) + `network.block:false` only (no WFP exercised this phase); workspace grant is supplied absolutely by Plan 04 — the profile cannot smuggle a broad grant |
| T-71-02 | Elevation of Privilege | `windows_interpreters` field deserialization | accept | The field is data only; it NARROWS nothing and GRANTS nothing on its own — it is the input to the fail-secure coverage gate (Plan 03/04). An empty/missing value = no interpreter coverage required (deserialize default), which Plan 04's resolution + gate still validates against the actual program |
| T-71-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; only JSON + Rust field edits. No `[ASSUMED]`/`[SUS]` packages introduced |
</threat_model>

<verification>
- `cargo build -p nono-cli` clean (field + embedded profiles compile).
- `cargo test -p nono-cli --lib` green (new + existing sweep tests).
- Cross-target gate: this plan touches NO cfg-gated Unix code (profile/mod.rs field is deserialize-on-all-platforms; policy.json/schema are data). Cross-target clippy is N/A — but run `cargo clippy -p nono-cli -- -D warnings -D clippy::unwrap_used` on the Windows host.
</verification>

<success_criteria>
- ENG-03 (profile half): a user can declare/resolve a per-engine profile (exe via `binary`, interpreter via `windows_interpreters`, workspace via Plan 04, network via `network`) — proven by the two resolving built-in profiles.
- Both engine profiles resolve with broker flag + interpreter coverage + extend default.
</success_criteria>

<output>
Create `.planning/phases/71-engine-agnostic-launch-productionization/71-01-SUMMARY.md` when done.
</output>
