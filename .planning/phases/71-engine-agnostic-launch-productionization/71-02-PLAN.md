---
phase: 71-engine-agnostic-launch-productionization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono-cli/src/exec_strategy_windows/launch.rs
autonomous: true
requirements: [ENG-01]
must_haves:
  truths:
    - "When AssignProcessToJobObject fails with ERROR_ACCESS_DENIED (foreign-job collision), the error names the foreign-job cause specifically (not a generic assign failure)"
    - "On any assign failure the suspended child is terminated (fail-secure) before nono returns"
    - "The kill-group job sets NO UI limits and NO breakaway flag (kill-group/descendant-capture only)"
  artifacts:
    - path: "crates/nono-cli/src/exec_strategy_windows/launch.rs"
      provides: "named GLE-5 foreign-job diagnostic in apply_process_handle_to_containment"
      contains: "ERROR_ACCESS_DENIED"
  key_links:
    - from: "apply_process_handle_to_containment"
      to: "GetLastError"
      via: "GLE capture on AssignProcessToJobObject failure"
      pattern: "GetLastError"
---

<objective>
Harden the SC5 / P6 nested-job-collision path: the suspend→assign→resume sequence and fail-secure terminate already exist; add the missing NAMED diagnostic that distinguishes the foreign-job case (child already in a Job Object nono did not create, breakaway disallowed → `AssignProcessToJobObject` returns `ERROR_ACCESS_DENIED`/GLE 5) from a generic assign failure, plus a negative test proving fail-secure-on-assign-failure structurally.

Purpose: SC5 closes the silent-confinement-loss pitfall (P6) — an operator must be told WHY descendant capture could not be guaranteed, and the launch must refuse rather than run partially contained.
Output: GLE-aware named error in `apply_process_handle_to_containment` + a unit test for the GLE-5 branch / fail-secure terminate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-engine-agnostic-launch-productionization/71-CONTEXT.md
@.planning/phases/71-engine-agnostic-launch-productionization/71-PATTERNS.md

<interfaces>
<!-- Existing assign helper to extend (launch.rs:247-263). It currently does NOT capture GetLastError(). -->
pub(super) fn apply_process_handle_to_containment(
    containment: &ProcessContainment,
    process: HANDLE,
) -> Result<()>;
  // body: AssignProcessToJobObject(containment.job, process); if ok==0 → NonoError::SandboxInit(generic msg)

<!-- Existing fail-secure caller — ALREADY CORRECT, DO NOT CHANGE (launch.rs:2004-2015): -->
//   apply_process_handle_to_containment(...) Err → terminate_suspended_process(...) then return Err(err)
//   apply_resource_limits(...) Err → terminate_suspended_process(...) then return Err(err)
//   resume_contained_process(...)?

<!-- Job creation — ALREADY SC5-CORRECT, DO NOT add UI limits or breakaway (launch.rs:221-222): -->
//   limits.BasicLimitInformation.LimitFlags =
//       JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE | JOB_OBJECT_LIMIT_DIE_ON_UNHANDLED_EXCEPTION;

NonoError::SandboxInit(String) is the error family used by create_process_containment (launch.rs:208).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Named GLE-5 foreign-job diagnostic on assign failure</name>
  <read_first>
    - crates/nono-cli/src/exec_strategy_windows/launch.rs (read apply_process_handle_to_containment at 247-263; the suspend→assign→resume caller at 2004-2015; the job creation LimitFlags at 221-222; confirm GetLastError is already imported from windows_sys in this file — grep for GetLastError before adding the import)
    - .planning/phases/71-engine-agnostic-launch-productionization/71-RESEARCH.md (the "SC5 nested-job-aware diagnostic" Code Examples block + Open Question 2 + Pitfall 5)
    - .planning/phases/71-engine-agnostic-launch-productionization/71-PATTERNS.md (the launch.rs SC5 named-diagnostic pattern section)
  </read_first>
  <action>
    Extend the `ok == 0` failure branch of `apply_process_handle_to_containment` (launch.rs:247-263): after `AssignProcessToJobObject` returns 0, capture `let gle = unsafe { GetLastError() };`. Special-case `gle == ERROR_ACCESS_DENIED` (numeric `5` — use the windows_sys constant if imported, else the literal with a comment) with a message stating the child is already a member of a Job Object nono did not create (and that job disallows breakaway), so nono cannot guarantee descendant capture / kill-group for this launch and refuses to continue (fail-secure). For any other GLE, keep a generic "AssignProcessToJobObject failed" message but INCLUDE the GLE value. Return `NonoError::SandboxInit(format!("Job Object assignment failed (GLE={gle}): {detail}"))`.
    Do NOT change the caller at launch.rs:2004-2015 — it already does `terminate_suspended_process(...)` then propagates (fail-secure). Do NOT add `JOB_OBJECT_LIMIT_BREAKAWAY_OK`/`SILENT_BREAKAWAY_OK` or any `JOB_OBJECT_UILIMIT_*` (CONTEXT locks "no UI limits", kill-group only; the named/ACL'd/breakaway-denied job is Phase 73).
    NO `.unwrap()`/`.expect()` in non-test code. `GetLastError` is an `unsafe` FFI call — wrap with `// SAFETY:` per CLAUDE.md.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -n "GetLastError" crates/nono-cli/src/exec_strategy_windows/launch.rs` shows a capture inside the assign-failure branch of `apply_process_handle_to_containment`.
    - Source assertion: the error message for the GLE-5 branch contains the substring "did not create" (the foreign-job naming) and the generic branch includes `GLE=`.
    - Source assertion: `grep -n "JOB_OBJECT_UILIMIT\|BREAKAWAY_OK" crates/nono-cli/src/exec_strategy_windows/launch.rs` shows NO new UI-limit or breakaway flag added (job LimitFlags remains KILL_ON_JOB_CLOSE | DIE_ON_UNHANDLED_EXCEPTION).
    - Build: `cargo build -p nono-cli` clean.
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p nono-cli</automated>
  </verify>
  <done>`apply_process_handle_to_containment` captures GLE on assign failure and emits a named foreign-job diagnostic for GLE 5; the kill-group job stays UI-limit-free and breakaway-denied; the existing fail-secure terminate caller is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Negative test — assign failure fails secure with named GLE-5 diagnostic</name>
  <read_first>
    - crates/nono-cli/src/exec_strategy_windows/launch.rs (read the existing test module in this file — grep for `#[cfg(test)]` / `mod tests` — to find the established pattern for testing containment helpers; read apply_process_handle_to_containment again to see what is unit-testable without a live process)
    - .planning/phases/71-engine-agnostic-launch-productionization/71-VALIDATION.md (Wave 0: SC5 foreign-job-collision negative test)
  </read_first>
  <behavior>
    - Test 1 (GLE-5 message): the foreign-job diagnostic string produced for `ERROR_ACCESS_DENIED` contains "did not create" and is constructed as a `NonoError::SandboxInit`. If the message is built by a small pure helper, test the helper directly; otherwise assert the branch text via a deterministic seam.
    - Test 2 (fail-secure structural): assigning a process to an INVALID job handle (e.g. spawn a real short-lived suspended child via the existing test scaffolding, or pass a deliberately invalid `containment.job`) returns `Err` (never `Ok`) — proving the assign-failure path propagates an error rather than silently continuing.
  </behavior>
  <action>
    Add a `#[cfg(all(test, target_os = "windows"))]` test to launch.rs's test module. To keep the GLE-5 diagnostic deterministically testable, factor the message construction into a small pure helper (e.g. `fn assign_failure_message(gle: u32) -> String`) that the failure branch calls, then unit-test `assign_failure_message(ERROR_ACCESS_DENIED)` contains "did not create" and `assign_failure_message(1)` contains "GLE=1". For the structural fail-secure assertion, reuse whatever live-spawn scaffolding the existing test module already has (a CREATE_SUSPENDED child terminated at end of test) and assert `apply_process_handle_to_containment` against an invalid job returns `Err`; if no such scaffolding exists, restrict Task 2 to the pure-helper test (the message branch) and note the structural assertion is covered by the SC1 HUMAN-UAT (Plan 05). `.expect()` permitted in tests only.
    Cross-target note: this is `#[cfg(target_os = "windows")]` cfg-gated test code in `exec_strategy_windows/` — flag for the executor to run the cross-target clippy gate per `.planning/templates/cross-target-verify-checklist.md` if the cross-toolchain is available; if not, mark the related verification PARTIAL and defer to live CI.
  </action>
  <acceptance_criteria>
    - Test command (Windows host): `cargo test -p nono-cli --lib assign_failure` passes — the GLE-5 message test asserts the "did not create" substring and the generic test asserts "GLE=".
    - Behavior assertion: if the structural invalid-job test is included, it asserts `Err` (fail-secure), never `Ok`.
    - No watch-mode flags; test completes < 30s.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-cli --lib assign_failure</automated>
  </verify>
  <done>A negative test proves the foreign-job (GLE-5) branch produces the named "did not create" diagnostic and the generic branch reports the GLE; fail-secure-on-assign-failure is asserted structurally where live scaffolding exists, else deferred to SC1 UAT with a noted reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spawned child process → nono's kill-group Job Object | If the child escapes the job (foreign-job collision or breakaway), descendant capture / kill-group is lost — confinement silently weakens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-03 | Elevation of Privilege | `AssignProcessToJobObject` foreign-job collision (GLE 5) | mitigate | Spawn CREATE_SUSPENDED (already), assign BEFORE ResumeThread (already), fail-secure TERMINATE the suspended child on assign failure (already), and (this plan) NAME the foreign-job cause so the operator knows confinement was refused — never run partially contained. Acceptance: GLE-5 branch returns Err with "did not create"; suspended child terminated |
| T-71-04 | Elevation of Privilege | child breaks out of the kill-group job | mitigate | Do NOT set `JOB_OBJECT_LIMIT_BREAKAWAY_OK`/`SILENT_BREAKAWAY_OK` — breakaway stays default-deny so descendants are captured. Acceptance: grep shows no breakaway/UI-limit flag added |
| T-71-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; only a launch.rs diagnostic + test edit. No `[ASSUMED]`/`[SUS]` packages |
</threat_model>

<verification>
- `cargo build -p nono-cli` clean.
- `cargo test -p nono-cli --lib assign_failure` green on a Windows host.
- Cross-target gate: `exec_strategy_windows/` is cfg-gated Windows code; per CLAUDE.md run cross-target clippy (`--target x86_64-unknown-linux-gnu` and `--target x86_64-apple-darwin` with `-D warnings -D clippy::unwrap_used`) IF the cross-toolchain is installed; otherwise mark PARTIAL and defer to CI per the checklist. The change is inside `#[cfg(target_os="windows")]` so the Unix legs should compile unchanged — verify no shared symbol drift.
</verification>

<success_criteria>
- SC5 / P6: the job-assignment path is hardened — spawn suspended, assign before any code runs, fail-secure terminate on assign failure, NO UI limits, and now a NAMED foreign-job diagnostic distinguishing the GLE-5 collision. Proven by the negative test.
</success_criteria>

<output>
Create `.planning/phases/71-engine-agnostic-launch-productionization/71-02-SUMMARY.md` when done.
</output>
