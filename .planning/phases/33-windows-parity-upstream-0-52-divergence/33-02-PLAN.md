---
phase: 33-windows-parity-upstream-0-52-divergence
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - docs/architecture/upstream-parity-strategy.md
autonomous: false
requirements: [REQ-2]
tags: [upstream-parity, adr, strategic-decision]

must_haves:
  truths:
    - "docs/architecture/upstream-parity-strategy.md exists with plain-text `**Status:** Accepted` header (D-33-C4, NOT YAML frontmatter)"
    - "ADR Decision Table scores all 3 named options (continue / split-windows / freeze-at-v0.52) on exactly 5 criteria (D-33-C1: maintenance / security / clarity / velocity / optionality)"
    - "Each option×criterion cell carries Low/Med/High verdict + 1-2 sentence rationale (D-33-C2; no false-precision integer scale)"
    - "The chosen option's verdict cell reads `**Accepted**`; rejected options' verdict cells read `Rejected: <one-liner>`"
    - "Equal-weight scoring is documented as a weight choice; tiebreaker rationale explicit if needed (D-33-C3 — security-posture lean per PROJECT.md core value)"
    - "ADR has all six locked sections: Context, Goals, Non-goals, Decision, Consequences, Alternatives Considered"
    - "Decision section includes `### Fork-only surface area` subsection quoting ledger highlights as evidence (D-33-A3)"
    - "Consequences section includes `### Future audit cadence` subsection (CONTEXT Specifics §3)"
  artifacts:
    - path: "docs/architecture/upstream-parity-strategy.md"
      provides: "Scored strategic decision between three parity-strategy options; locks the picked option and consequences"
      contains: "**Status:** Accepted, ## Context, ## Goals, ## Non-goals, ## Decision Table, ## Decision, ## Consequences, ## Alternatives Considered, ### Fork-only surface area, ### Future audit cadence"
  key_links:
    - from: "upstream-parity-strategy.md Decision Table"
      to: "DIVERGENCE-LEDGER.md cluster counts + fork-only surface section"
      via: "scoring rationales cite ledger row counts (size evidence) + fork-only surface size (security-posture evidence)"
      pattern: "DIVERGENCE-LEDGER|cluster|fork-only surface"
    - from: "upstream-parity-strategy.md References section"
      to: ".planning/phases/33-windows-parity-upstream-0-52-divergence/{33-SPEC.md, 33-CONTEXT.md, DIVERGENCE-LEDGER.md}, .planning/phases/25-cross-platform-resl-aipc-unix-design/25-HUMAN-UAT.md"
      via: "ADR cites SPEC reqs, CONTEXT decision IDs, ledger evidence, and the gap that motivated the audit"
      pattern: "33-SPEC|33-CONTEXT|DIVERGENCE-LEDGER|G-25-DRIFT-01"
---

<objective>
Write the strategic ADR (`docs/architecture/upstream-parity-strategy.md`) — the scored decision artifact that picks one of three named parity-strategy options based on the audit evidence in `DIVERGENCE-LEDGER.md`. The ADR is the lock-point for the parity question that's been growing since v2.1 (Windows-only surface accumulation vs continued bidirectional parity).

Purpose: REQ-2 demands a scored, accepted ADR with three named options × five criteria × Low/Med/High verdicts. The maintainer's strategic judgment (picking option A / B / C) is the only remaining freedom; the scoring criteria, scale, header convention, and tiebreaker are all locked in CONTEXT D-33-C1..C4.

Output: ONE new file at `docs/architecture/upstream-parity-strategy.md` following the 4-ADR fork convention (plain-text header, six-section structure).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/33-windows-parity-upstream-0-52-divergence/33-CONTEXT.md
@.planning/phases/33-windows-parity-upstream-0-52-divergence/33-SPEC.md
@.planning/phases/33-windows-parity-upstream-0-52-divergence/33-RESEARCH.md
@.planning/phases/33-windows-parity-upstream-0-52-divergence/33-PATTERNS.md
@.planning/phases/33-windows-parity-upstream-0-52-divergence/33-VALIDATION.md
@.planning/phases/33-windows-parity-upstream-0-52-divergence/33-00-SUMMARY.md
@.planning/phases/33-windows-parity-upstream-0-52-divergence/DIVERGENCE-LEDGER.md
@docs/architecture/audit-bundle-target.md
@docs/architecture/broker-trust-anchor.md
@docs/architecture/sigstore-tuf-cache.md
@docs/architecture/aipc-unix-futures.md

<interfaces>
<!-- ADR header convention (D-33-C4 + RESEARCH Pitfall 4 — VERIFIED across 4 prior ADRs) -->
<!-- CRITICAL: plain-text **Status:** Accepted line, NOT YAML frontmatter -->
```markdown
# <Title>

**Status:** Accepted
**Date:** YYYY-MM-DD
**Phase:** N (vX.Y description)
[optional: **Requirement:** | **Decision IDs:** | **Supersedes:** | **Related artifact:**]
```

<!-- Six-section structure (PATTERNS §"ADR Section Structure" — verified across 4 prior ADRs) -->
- ## Context (prose narrative motivating the decision)
- ## Goals + ## Non-goals (bulleted commits + explicit non-commits)
- ## Decision Table (option × dimensions × verdict)
- ## Decision (prose for chosen option)
- ## Consequences (### Positive + ### Negative + ### Future audit cadence)
- ## Alternatives Considered (brief restatement of rejected options)
- ## References (internal + source code + related ADRs)

<!-- D-33-C1 locked criteria (exact order matters for the table headers): -->
1. Maintenance cost — per-sync labor, cherry-pick conflict count, manual-replay rate
2. Security posture — Windows-only hardening vs upstream's threat model coverage
3. User clarity — single-CLI-surface vs split-surface confusion
4. Contributor velocity — Windows-PR latency, cross-platform-PR latency, PR review burden
5. Roadmap optionality — which option keeps the most v2.4+ doors open

<!-- D-33-C3 tiebreaker — security-posture lean per PROJECT.md core value -->
<!-- "OS-enforced isolation, structurally impossible bypass" -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Score the three options against the ledger and pick the option</name>
  <read_first>
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/DIVERGENCE-LEDGER.md § Cluster Summary table + § Fork-only surface area (Wave 1 output — primary scoring evidence)
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-CONTEXT.md D-33-C1, D-33-C2, D-33-C3
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-RESEARCH.md § "Open Question 2" (review-cycle vs single-commit ADR pattern — already locked in 33-00-SUMMARY.md)
    - .planning/PROJECT.md L1-30 (Core Value statement — "OS-enforced isolation, structurally impossible bypass" — informs D-33-C3 tiebreaker)
  </read_first>
  <decision>
    Pick ONE of three named options for the parity strategy going forward, with scoring grounded in the audit evidence:

    **Option A — Continue bidirectional parity in this repo.** Every upstream release continues to be sync-audited and absorbed; Windows-only surface grows alongside upstream's cross-platform surface in the same workspace.

    **Option B — Split Windows-only surface into a separate downstream.** Move `crates/nono-shell-broker/`, `*_windows.rs` files, broker-process / Authenticode / WFP / NONO_TEST_HOME seams, and Sigstore broker self-trust-anchor into a dedicated `always-further/nono-windows` (or similar) downstream that periodically pulls from upstream rather than chasing parity.

    **Option C — Freeze fork at v0.52, stop chasing upstream.** Accept the v0.52 baseline as the upstream-parity terminus; future upstream releases are not absorbed; fork becomes its own thing.
  </decision>
  <context>
    Use the ledger evidence to ground each option×criterion cell:

    - **Maintenance cost** — informed by `total_unique_commits` from ledger (12 minor releases of unabsorbed divergence) and the cluster count's `will-sync` vs `fork-preserve` vs `won't-sync` mix. High `will-sync` count → high per-sync labor for option A. Option B externalizes the labor; option C eliminates it.
    - **Security posture** — informed by the fork-only surface size (broker / WFP / Authenticode / TUF cached-root / NONO_TEST_HOME). Larger fork-only surface → more Windows-only security mechanism that upstream cannot evolve. Option A: continues to evolve. Option B: continues to evolve, but in a separate repo. Option C: static fork-only surface; upstream security fixes don't flow in (Med, not High).
    - **User clarity** — single CLI surface across all platforms (option A, High) vs "which `nono` am I running?" confusion (option B, Low). Option C: divergence documented but expected (Med).
    - **Contributor velocity** — Windows-PR latency stays low under option A and option C (single repo). Option B: cross-platform PRs need 2 reviews / 2 commit chains (Low).
    - **Roadmap optionality** — Option A: high — all v2.4+ doors open. Option B: low — structurally hard to reverse (Med-Low). Option C: low — forecloses re-merge (Low).

    **Tiebreaker (D-33-C3):** PROJECT.md core value is "OS-enforced isolation, structurally impossible bypass" — security-posture column leans. If two options tie on the aggregate L/M/H shape, the security-posture column breaks the tie.

    The maintainer should:
    1. Score each option×criterion cell using ledger evidence (write the Low/Med/High verdict + 1-2 sentence rationale).
    2. Tally the L/M/H shape per option.
    3. Pick the option that maximizes the aggregate verdict, applying the D-33-C3 tiebreaker if needed.
  </context>
  <options>
    <option id="continue">
      <name>Option A — Continue bidirectional parity</name>
      <pros>
        - User clarity HIGH — single CLI surface
        - Roadmap optionality HIGH — all v2.4+ doors open
        - Windows-only hardening evolves alongside upstream's threat model
        - 78-commit Phase 22 UPST2 precedent shows sync labor is sustainable
      </pros>
      <cons>
        - Maintenance cost MED — per-sync labor sustains; every upstream release requires drift audit + cherry-pick chain
        - Cherry-pick conflicts will grow as fork-only surface grows
      </cons>
    </option>
    <option id="split-windows">
      <name>Option B — Split Windows into nono-windows fork</name>
      <pros>
        - Maintenance cost LOW for upstream sync — fork only pulls from upstream periodically
        - Windows-only hardening unchanged (just lives in a separate repo)
      </pros>
      <cons>
        - User clarity LOW — "which `nono` am I running?" confusion
        - Contributor velocity LOW — Windows PRs land in separate repo; cross-platform PRs need 2 reviews
        - Roadmap optionality LOW — structurally hard to reverse (workspace split is one-way)
        - Migration cost itself is non-trivial (split history, CI rewire, two release pipelines)
      </cons>
    </option>
    <option id="freeze-at-v0.52">
      <name>Option C — Freeze fork at v0.52, stop chasing upstream</name>
      <pros>
        - Maintenance cost LOW — zero sync labor
        - Contributor velocity HIGH — no upstream parity gate on Windows PRs
        - Fork becomes its own thing; no more cross-platform PR review burden
      </pros>
      <cons>
        - Security posture MED — Windows hardening static; upstream security fixes don't flow in
        - User clarity MED — divergence documented but expected
        - Roadmap optionality LOW — forecloses re-merge with upstream
      </cons>
    </option>
  </options>
  <resume-signal>
    Select: `continue` (option A), `split-windows` (option B), or `freeze-at-v0.52` (option C).

    Provide the per-cell L/M/H verdicts + 1-2 sentence rationales for the chosen option's row in the Decision Table (the other two options' rows can be filled in by Task 2's executor following the structure in `<options>` above).

    Record the picked option + scoring snapshot in this plan's eventual SUMMARY so Wave 3 (Plan 33-03) knows which option to write into the PROJECT.md row + G-25-DRIFT-01 Update + ROADMAP UPST3 title.

    **Default if no operator response within reasonable wait:** `continue` (option A) — RESEARCH §"Open Questions" notes this is the convergent recommendation under the security-posture tiebreaker (D-33-C3) given PROJECT.md core value, AND option A's aggregate L/M/H shape (Med, High, High, Med, High) dominates option B (Low, High, Low, Low, Low) and option C (Low, Med, Med, High, Low) on the 5-criterion sum.
  </resume-signal>
  <files>(no file edits in this checkpoint — captures the operator's chosen option into 33-02-SUMMARY.md via Task 2-3 and the ADR Decision Table verdict cell)</files>
  <action>This is a checkpoint:decision task. The executor presents the decision/options/resume-signal blocks above to the operator and BLOCKS until they respond. On response, the operator's chosen option (continue / split-windows / freeze-at-v0.52) plus the per-cell L/M/H verdicts and rationales for the chosen option's Decision Table row are captured for Task 2 to write into docs/architecture/upstream-parity-strategy.md.</action>
  <verify>
    <automated>true # checkpoint:decision — verification is operator response captured in the eventual 33-02-SUMMARY.md and the Wave 2 ADR Decision Table</automated>
  </verify>
  <done>Operator has selected one of continue / split-windows / freeze-at-v0.52; chosen option name + per-cell L/M/H verdicts + tiebreaker rationale (if applicable) are recorded for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Write the ADR with locked structure + scoring + chosen option</name>
  <files>docs/architecture/upstream-parity-strategy.md</files>
  <read_first>
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-PATTERNS.md L105-225 (verbatim ADR analog excerpts from audit-bundle-target.md)
    - docs/architecture/audit-bundle-target.md L1-93 (closest convention analog per D-33-C4)
    - docs/architecture/aipc-unix-futures.md L1-60 (alternative convention check — Phase 25 ADR)
    - docs/architecture/broker-trust-anchor.md L1-60 (alternative convention check — Phase 32 ADR)
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/DIVERGENCE-LEDGER.md (full read — for evidence quotes in Decision section)
    - Task 1 checkpoint output (chosen option + scoring snapshot)
  </read_first>
  <action>
    Write `docs/architecture/upstream-parity-strategy.md` using the EXACT structure below. The header is plain-text per D-33-C4 (CRITICAL — NOT YAML frontmatter; SPEC.md L80 wording was loose, all 4 prior ADRs use plain-text). The Decision Table follows the verbatim shape from PATTERNS L180-190 + RESEARCH L520-525.

    **Section 1 — Plain-text header (D-33-C4 + RESEARCH Pitfall 4 — verbatim shape from `audit-bundle-target.md` L1-7):**

    ```markdown
    # Upstream Parity Strategy (continue / split-windows / freeze-at-v0.52)

    **Status:** Accepted
    **Date:** <audit-date from 33-00-SUMMARY.md>
    **Phase:** 33 (v2.4 windows-parity-upstream-0-52-divergence)
    **Decision IDs:** D-33-A1..A3, B1..B3, C1..C4, D1..D2 (33-CONTEXT.md)
    **Related artifact:** [DIVERGENCE-LEDGER.md](../../.planning/phases/33-windows-parity-upstream-0-52-divergence/DIVERGENCE-LEDGER.md)
    ```

    Note: NO YAML `---` markers. The header is grep-discoverable via `grep -E "^\*\*Status:\*\* Accepted$"` — this is the structural acceptance gate per 33-VALIDATION.md REQ-2 validator block.

    **Section 2 — Context (prose narrative, 3-5 paragraphs):**

    ```markdown
    ## Context

    The fork's last upstream sync was Phase 22 UPST2 (v0.38–v0.40.1, shipped 2026-04-28). Upstream `always-further/nono` is at v0.52.0 and has accumulated 12 minor releases of feature divergence: `<total_unique_commits from DIVERGENCE-LEDGER.md>` commits, grouped into `<cluster count>` themed clusters per the Wave 1 audit. The audit's dispositions break down as `<N>` will-sync, `<M>` fork-preserve, `<K>` won't-sync clusters.

    Phase 25's HUMAN-UAT (2026-05-10) surfaced G-25-DRIFT-01: all four RESL flags (`--memory`, `--cpu-percent`, `--max-processes`, `--timeout`) shipped by Phase 25 are deprecated or renamed in upstream v0.52. Phase 25's source-level closure is intact (cgroup v2 / setrlimit backends correctly enforce against the values they receive — backend correctness is independent of flag naming), but the user-facing CLI surface diverges from upstream documentation. G-25-DRIFT-01 was the tip of an iceberg; the v0.41–v0.52 audit confirms `<N>` additional clusters of divergence.

    Beyond the parity gap, this ADR resolves a strategic question that's been growing since v2.1: the fork has accumulated significant Windows-only surface area — broker-process architecture (Phase 31), WFP service + filtering (Phases 6/9), ConPTY shell (Phase 8/30), Authenticode chain-walker (Phase 28), Sigstore broker self-trust-anchor (Phase 32), NONO_TEST_HOME seam (Phase 27.1) — none of which has an upstream analog. Every upstream sync from here on will hit fork-only files. The question is whether continued bidirectional parity is sustainable, or whether the Windows-specific work belongs in a separate downstream that periodically pulls from upstream rather than chasing parity.

    This ADR scores three options against five criteria using the audit data as evidence and picks one.
    ```

    Fill `<total_unique_commits>`, `<cluster count>`, `<N>`, `<M>`, `<K>` from the LIVE DIVERGENCE-LEDGER.md Cluster Summary table (read it back at write time).

    **Section 3 — Goals + Non-goals (analog L19-35 of audit-bundle-target.md):**

    ```markdown
    ## Goals

    This ADR commits to:

    - A scored, falsifiable strategic verdict among three named options: continue / split-windows / freeze-at-v0.52.
    - Per-option scoring across five equal-weighted criteria (D-33-C1): maintenance cost, security posture, user clarity, contributor velocity, roadmap optionality.
    - Qualitative Low/Med/High scoring (D-33-C2) with 1-2 sentence rationale per cell — no false-precision integer scale.
    - A documented tiebreaker (D-33-C3) grounded in PROJECT.md core value ("OS-enforced isolation, structurally impossible bypass") favoring the security-posture column.
    - A `Future audit cadence` consequence that downstream maintainers can act on (every-release / every-milestone / on-trigger).
    - A clear handoff to the UPST3-sync follow-up phase queued in ROADMAP for the actual cherry-pick / manual-replay work.

    ## Non-goals

    This ADR explicitly does NOT commit to:

    - Executing any cherry-picks, manual replays, or code changes that close divergences. — The UPST3-sync follow-up phase (`<TBD phase number — set by Wave 3>`) does that.
    - Closing G-25-DRIFT-01. — The RESL flag renames are sync work; the gap stays open until UPST3-sync lands the actual renames.
    - Per-row will-sync vs fork-preserve dispositions. — The ledger handles per-cluster dispositions; this ADR scores the strategic question on top.
    - Touching `crates/nono/`. — D-19 byte-identical invariant holds trivially for this docs-only phase.
    - Forecasting the v2.5+ milestone or any downstream sync's specific scope. — The ADR's consequences are the cadence rule; specific phase plans are their own work.
    ```

    **Section 4 — Decision Table (D-33-C1 + D-33-C2 + verbatim shape from PATTERNS L181-189):**

    ```markdown
    ## Decision Table

    | Option | Maint cost | Security posture | User clarity | Contributor velocity | Roadmap optionality | Verdict |
    |--------|-----------|------------------|--------------|---------------------|---------------------|---------|
    | **A (<chosen?>) — Continue bidirectional parity** | <Low/Med/High> — <1-2 sentence rationale grounded in ledger evidence: cluster count, will-sync ratio, fork-only surface size> | <L/M/H> — <rationale> | <L/M/H> — <rationale> | <L/M/H> — <rationale> | <L/M/H> — <rationale> | **<Accepted or Rejected: <one-liner>>** |
    | B (<chosen?>) — Split Windows into nono-windows fork | <L/M/H> — <rationale> | <L/M/H> — <rationale> | <L/M/H> — <rationale> | <L/M/H> — <rationale> | <L/M/H> — <rationale> | **<Accepted or Rejected: <one-liner>>** |
    | C (<chosen?>) — Freeze fork at v0.52, stop chasing upstream | <L/M/H> — <rationale> | <L/M/H> — <rationale> | <L/M/H> — <rationale> | <L/M/H> — <rationale> | <L/M/H> — <rationale> | **<Accepted or Rejected: <one-liner>>** |
    ```

    The chosen option's row gets `**Accepted**` in the Verdict cell. Rejected options get `Rejected: <one-sentence reason>` (mirror analog audit-bundle-target.md L42-43 verbatim wording). Each rationale cell is 1-2 sentences citing concrete ledger evidence (commit counts, fork-only surface items, etc.) — NOT generic prose.

    **Section 5 — Decision (prose for chosen option + fork-only surface subsection per D-33-A3):**

    ```markdown
    ## Decision

    <Picked option name from Task 1 checkpoint>: <3-5 sentence prose explaining the choice. Cite the aggregate L/M/H shape from the Decision Table. If a tiebreaker applies (D-33-C3), name it explicitly: "Per D-33-C3 tiebreaker (PROJECT.md core value — 'OS-enforced isolation, structurally impossible bypass' — favors security-posture column), [chosen option] wins despite tying [other option] on the M/H aggregate.">

    ### Fork-only surface area (D-33-A3 evidence)

    The decision rests in part on the size and ownership of the fork-only Windows surface area. Per [DIVERGENCE-LEDGER.md § Fork-only surface area](../../.planning/phases/33-windows-parity-upstream-0-52-divergence/DIVERGENCE-LEDGER.md#fork-only-surface-area):

    - `crates/nono-shell-broker/` crate (Phase 31 Low-IL broker process)
    - `*_windows.rs` files: 7+ files across `crates/nono-cli/src/` (Authenticode, ConPTY, learn, open_url, pty_proxy, session_commands, trust_intercept) + `crates/nono/src/sandbox/windows.rs`
    - Phase 31 broker dispatch (`WindowsTokenArm::BrokerLaunch` at `launch.rs:1246+`)
    - Phase 32 Sigstore TUF cached-root + broker self-trust-anchor
    - Phase 28 Authenticode chain-walker
    - Phase 27.1 NONO_TEST_HOME seam

    None of this surface has an upstream analog. <Explain how this fact supports the chosen option — e.g., for option A: "Continued parity preserves the option to evolve Windows-only hardening alongside upstream's threat model rather than letting either side drift."; for option B: "The size of fork-only surface justifies the structural separation cost."; for option C: "The fork-only surface is large enough that further upstream absorption costs exceed marginal security benefit.">
    ```

    **Section 6 — Consequences (### Positive + ### Negative + ### Future audit cadence per CONTEXT Specifics §3):**

    ```markdown
    ## Consequences

    ### Positive

    - <3-5 bullets specific to the chosen option's upside>

    ### Negative

    - <2-4 bullets specific to the chosen option's downside, frankly acknowledged>

    ### Future audit cadence

    <2-3 sentences explaining when `make check-upstream-drift` should run going forward under the chosen option. For option A: "Every upstream minor release triggers a drift audit + ledger update; UPST{N+1}-sync phases land between releases as needed." For option B: "Pre-sync audits only when the downstream pulls from upstream; cadence is downstream-owner-determined (target: every minor release)." For option C: "Drift audits are not required after v0.52 (the fork no longer chases upstream); the existing tooling is preserved for future re-evaluation but is not on a schedule."
    ```

    **Section 7 — Alternatives Considered (brief prose for rejected options):**

    ```markdown
    ## Alternatives Considered

    ### Option <X> — <rejected option name>

    <1-2 paragraphs: what it would have meant, why the Decision Table verdict rejected it, what it would have cost. Reference specific scoring cells (e.g., "User clarity scored Low because <reason>"). Don't just repeat the Verdict cell text; expand into rationale that a future maintainer re-evaluating could audit.>

    ### Option <Y> — <other rejected option name>

    <Same shape.>
    ```

    **Section 8 — References (analog L75-93 of audit-bundle-target.md):**

    ```markdown
    ## References

    ### Internal

    - [`33-SPEC.md`](../../.planning/phases/33-windows-parity-upstream-0-52-divergence/33-SPEC.md) — Locked requirements REQ-1..5
    - [`33-CONTEXT.md`](../../.planning/phases/33-windows-parity-upstream-0-52-divergence/33-CONTEXT.md) — Decisions D-33-A1..A3, B1..B3, C1..C4, D1..D2
    - [`DIVERGENCE-LEDGER.md`](../../.planning/phases/33-windows-parity-upstream-0-52-divergence/DIVERGENCE-LEDGER.md) — Wave 1 audit artifact; scoring evidence
    - [`25-HUMAN-UAT.md` § G-25-DRIFT-01](../../.planning/phases/25-cross-platform-resl-aipc-unix-design/25-HUMAN-UAT.md) — The gap that motivated the audit
    - [`PROJECT.md` § Key Decisions](../../.planning/PROJECT.md#key-decisions) — Where this decision's summary row lives (Wave 3 / Plan 33-03 writes the row)

    ### Related ADRs (convention references)

    - [`audit-bundle-target.md`](audit-bundle-target.md) — Phase 27.2 ADR (closest convention match per D-33-C4)
    - [`aipc-unix-futures.md`](aipc-unix-futures.md) — Phase 25 ADR
    - [`broker-trust-anchor.md`](broker-trust-anchor.md) — Phase 32 ADR
    - [`sigstore-tuf-cache.md`](sigstore-tuf-cache.md) — Phase 32 ADR
    ```

    Use the Write tool with the full assembled content. Do NOT use heredoc / `cat <<EOF`. Per Wave 0 Open Question 2 (locked in 33-00-SUMMARY.md as `Accepted-first-commit`), ship `**Status:** Accepted` from the first commit — no Proposed → Accepted two-step.

    **CRITICAL pre-commit checks** (FROM 33-VALIDATION.md REQ-2 validator block):
    1. `grep -E "^\*\*Status:\*\* Accepted$" docs/architecture/upstream-parity-strategy.md` — exactly one match (NO YAML frontmatter form).
    2. `grep -cE "(continue|split-windows|freeze-at-v0.52)" docs/architecture/upstream-parity-strategy.md` — at least 3 matches (all three options named).
    3. `grep -cE "(Maintenance cost|Security posture|User clarity|Contributor velocity|Roadmap optionality)" docs/architecture/upstream-parity-strategy.md` — at least 5 matches (all 5 criteria named).
    4. `grep -cE "^## (Context|Goals|Non-goals|Decision Table|Decision|Consequences|Alternatives Considered)" docs/architecture/upstream-parity-strategy.md` — at least 7 matches (all 6 main sections + the Decision-Table-vs-Decision distinction).
    5. `grep -E "^### Fork-only surface area" docs/architecture/upstream-parity-strategy.md` — one match (D-33-A3 evidence subsection).
    6. `grep -E "^### Future audit cadence" docs/architecture/upstream-parity-strategy.md` — one match (CONTEXT Specifics §3).
    7. `grep -E "\*\*Accepted\*\*" docs/architecture/upstream-parity-strategy.md` — at least one match (chosen option's verdict cell).
  </action>
  <verify>
    <automated>test -f docs/architecture/upstream-parity-strategy.md &amp;&amp; grep -cE "^\*\*Status:\*\* Accepted$" docs/architecture/upstream-parity-strategy.md | grep -q "^1$" &amp;&amp; test $(grep -cE "(Maintenance cost|Security posture|User clarity|Contributor velocity|Roadmap optionality)" docs/architecture/upstream-parity-strategy.md) -ge 5 &amp;&amp; test $(grep -cE "^## (Context|Goals|Non-goals|Decision Table|Decision|Consequences|Alternatives Considered)" docs/architecture/upstream-parity-strategy.md) -ge 7 &amp;&amp; grep -qE "^### Fork-only surface area" docs/architecture/upstream-parity-strategy.md &amp;&amp; grep -qE "^### Future audit cadence" docs/architecture/upstream-parity-strategy.md &amp;&amp; grep -qE "\*\*Accepted\*\*" docs/architecture/upstream-parity-strategy.md &amp;&amp; echo OK</automated>
  </verify>
  <done>
    - File exists at `docs/architecture/upstream-parity-strategy.md`
    - Plain-text `**Status:** Accepted` header (NOT YAML frontmatter — D-33-C4)
    - Decision Table has 3 option rows × 5 criterion columns + Verdict column; one row's Verdict is `**Accepted**`; two rows' Verdicts are `Rejected: <one-liner>`
    - Each Decision-Table cell carries Low/Med/High verdict + 1-2 sentence rationale citing ledger evidence
    - All 6 locked sections present (Context, Goals, Non-goals, Decision Table, Decision, Consequences, Alternatives Considered)
    - `### Fork-only surface area` subsection under Decision (D-33-A3 evidence)
    - `### Future audit cadence` subsection under Consequences (CONTEXT Specifics §3)
    - All References resolve as relative paths from `docs/architecture/`
  </done>
</task>

<task type="auto">
  <name>Task 3: Self-audit the ADR + commit</name>
  <files>docs/architecture/upstream-parity-strategy.md</files>
  <read_first>
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-VALIDATION.md § "REQ-2 — Strategic decision ADR" validator block
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-SPEC.md § "Acceptance Criteria" REQ-2 row
    - docs/architecture/upstream-parity-strategy.md (read back to self-audit)
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-00-SUMMARY.md (confirm `adr-commit-pattern` lock)
  </read_first>
  <action>
    Run the REQ-2 validator block + cross-cutting checks, then commit per the Wave 0 locked pattern (`Accepted-first-commit`).

    1. Run all 7 pre-commit greps from Task 2's CRITICAL checklist; all must pass.

    2. Confirm the chosen option matches the Task 1 checkpoint decision recorded in this plan's eventual SUMMARY (i.e., the **Accepted** verdict cell aligns with the operator's pick — `continue` / `split-windows` / `freeze-at-v0.52`).

    3. `crates/nono/` byte-identical D-19 invariant:
       ```bash
       git diff --name-only -- crates/nono/ | wc -l
       ```
       Expected: 0 (this plan touches only `docs/architecture/`).

    4. `make ci` smoke check (33-VALIDATION.md cross-cutting):
       ```bash
       make ci 2>&1 | tail -20
       ```
       Expected: green. Phase 33 ships only docs; clippy/fmt/tests should be uneventful.

    5. Commit the ADR with DCO sign-off (Accepted-first-commit pattern per 33-00-SUMMARY.md):
       ```
       docs(33): write upstream-parity-strategy ADR (Accepted)

       Produces docs/architecture/upstream-parity-strategy.md per REQ-2 / D-33-C1
       (5 equal-weighted criteria: maintenance / security / clarity / velocity /
       optionality) / D-33-C2 (Low/Med/High qualitative) / D-33-C3 (tiebreaker:
       security-posture lean per PROJECT.md core value) / D-33-C4 (plain-text
       **Status:** Accepted header, NOT YAML frontmatter — matches the 4 prior
       ADRs in docs/architecture/).

       Three options scored: (A) continue bidirectional parity, (B) split Windows
       into nono-windows fork, (C) freeze fork at v0.52. Chosen: <option>.

       Decision Table cells cite DIVERGENCE-LEDGER.md (Wave 1 / Plan 33-01) row
       counts and fork-only surface enumeration as scoring evidence. Decision
       section includes ### Fork-only surface area subsection (D-33-A3) and
       Consequences includes ### Future audit cadence (CONTEXT Specifics §3).

       Wave 3 (Plan 33-03) consumes this ADR to write:
         - PROJECT.md Key Decisions row (REQ-3)
         - G-25-DRIFT-01 Update section (REQ-4)
         - ROADMAP UPST3-sync placeholder (REQ-5; title flips per D-33-D1 if
           option B or C was chosen)

       Refs: REQ-2, D-33-C1, D-33-C2, D-33-C3, D-33-C4, D-33-A3
       Signed-off-by: <user>
       ```
  </action>
  <verify>
    <automated>test $(git diff --name-only -- crates/nono/ | wc -l) -eq 0 &amp;&amp; make ci &gt; /dev/null 2&gt;&amp;1 &amp;&amp; grep -qE "^\*\*Status:\*\* Accepted$" docs/architecture/upstream-parity-strategy.md &amp;&amp; echo OK</automated>
  </verify>
  <done>
    - All 7 Task 2 pre-commit greps pass
    - `crates/nono/` D-19 invariant: 0 files diff
    - `make ci` green
    - Commit lands with DCO sign-off and references all 6 decision IDs (C1..C4, A3)
    - Chosen option (A / B / C) is recorded in the plan SUMMARY for Wave 3 to consume
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer judgment → Decision Table cell verdicts | The L/M/H verdicts and the picked option are human strategic judgment grounded in ledger evidence — falsifiable in code review against the rationale cells |
| ADR text → downstream artifacts (PROJECT.md row, G-25-DRIFT-01 Update, ROADMAP UPST3 stub) | Wave 3 reads the ADR to fill its three downstream edits; tampering with the ADR text after Wave 2 commit would propagate silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-02-01 | T (Tampering) | Decision Table scoring manipulation | mitigate | Per D-33-C2, every cell carries a 1-2 sentence rationale citing concrete ledger evidence (cluster counts, fork-only surface items). Generic rationales fail code review. The L/M/H verdicts are checkable against the cited evidence; a tampered cell would surface a missing or inconsistent rationale-evidence link. |
| T-33-02-02 | I (Information Disclosure) | ADR contents | accept | ADR is intended to be public — it documents the project's parity strategy for future maintainers. No secrets. |
| T-33-02-03 | R (Repudiation) | Chosen option commitment | mitigate | Status: Accepted header + commit message naming the picked option + DCO sign-off creates a non-repudiable record. Wave 3 reads this directly; if the option needs to change, that requires a new ADR (Superseding the current one), not a silent edit. |
| T-33-02-04 | E (Elevation of Privilege) | (none — docs-only) | n/a | No code path is added or modified; Decision Table verdicts have no runtime effect. |
</threat_model>

<verification>
- `docs/architecture/upstream-parity-strategy.md` exists with plain-text `**Status:** Accepted` header
- All 3 options named in Decision Table; all 5 criteria as column headers
- Chosen option's verdict cell reads `**Accepted**`; other two read `Rejected: <one-liner>`
- 6 main sections + Fork-only surface + Future audit cadence subsections all present
- `crates/nono/` D-19 invariant holds (zero file diffs)
- `make ci` green
- Commit lands with all 6 D-33-C* / D-33-A3 decision IDs referenced
</verification>

<success_criteria>
- REQ-2 acceptance fully met: ADR exists at the locked path, `Status: Accepted`, 3 options × 5 criteria scored with rationale, Decision + Consequences + Alternatives sections all present
- Wave 3 (Plan 33-03) can read the picked option directly from the ADR's Decision section + Verdict column
- D-33-C4 plain-text header convention is preserved (NOT YAML frontmatter)
</success_criteria>

<output>
After completion, `docs/architecture/upstream-parity-strategy.md` is the lock-point for the parity question. The picked option drives Wave 3's three downstream edits (PROJECT.md row, G-25-DRIFT-01 Update, ROADMAP UPST3 stub). The Plan 33-02 SUMMARY records the picked option verbatim so Wave 3's executor reads it without re-deriving from the ADR.
</output>
