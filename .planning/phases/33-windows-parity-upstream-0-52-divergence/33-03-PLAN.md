---
phase: 33-windows-parity-upstream-0-52-divergence
plan: 03
type: execute
wave: 3
depends_on: ["33-02"]
files_modified:
  - .planning/PROJECT.md
  - .planning/phases/25-cross-platform-resl-aipc-unix-design/25-HUMAN-UAT.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [REQ-3, REQ-4, REQ-5]
tags: [upstream-parity, downstream-updates, key-decisions, gap-update, roadmap]

must_haves:
  truths:
    - "PROJECT.md Key Decisions table (L158-183, 3-column shape) gains one row for the parity-strategy decision; row's ADR link resolves to docs/architecture/upstream-parity-strategy.md (REQ-3 / RESEARCH OQ-1 locked target)"
    - "Phase 25's 25-HUMAN-UAT.md G-25-DRIFT-01 entry gains a `**Update (Phase 33, YYYY-MM-DD):**` section with all 4 subsections (D-33-D2); frontmatter `status: open` stays open (Phase 33 does NOT close the gap)"
    - "ROADMAP.md gains a new Phase 34 placeholder for UPST3-sync (or split/freeze variant per D-33-D1) with `Depends on: Phase 33` and `Plans: 0 plans` (REQ-5)"
    - "ROADMAP.md Phase 33's own entry is updated: Plans count and bullet list flip from TBD to the actual plans Phase 33 ships (PATTERNS Pitfall 8 — TWO ROADMAP edits required, not one)"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Discoverable key-decisions row for the parity-strategy decision"
      contains: "upstream-parity-strategy"
    - path: ".planning/phases/25-cross-platform-resl-aipc-unix-design/25-HUMAN-UAT.md"
      provides: "G-25-DRIFT-01 Update section with cross-references to Phase 33's outputs"
      contains: "**Update (Phase 33,"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 34 UPST3-sync placeholder + Phase 33 row marked complete with actual plan list"
      contains: "### Phase 34: UPST3"
  key_links:
    - from: "PROJECT.md Key Decisions row"
      to: "docs/architecture/upstream-parity-strategy.md"
      via: "relative-path link from .planning/PROJECT.md → ../docs/architecture/upstream-parity-strategy.md"
      pattern: "upstream-parity-strategy"
    - from: "25-HUMAN-UAT.md G-25-DRIFT-01 Update section"
      to: "DIVERGENCE-LEDGER.md + upstream-parity-strategy.md"
      via: "Update section cites both artifacts; gap stays open with explicit handoff to Phase 34"
      pattern: "DIVERGENCE-LEDGER|upstream-parity-strategy"
    - from: "ROADMAP.md Phase 34 stub"
      to: "Phase 33 outputs"
      via: "Depends on: Phase 33; Reference list cites DIVERGENCE-LEDGER + ADR"
      pattern: "Depends on.*Phase 33"
---

<objective>
Land the three downstream artifact updates that close Phase 33: (REQ-3) append the Key Decisions row to PROJECT.md; (REQ-4) append the `**Update (Phase 33, ...):**` section to G-25-DRIFT-01; (REQ-5) append the Phase 34 UPST3-sync placeholder to ROADMAP.md AND flip Phase 33's own ROADMAP entry from TBD to complete. All three edits are append-only/in-place and depend on the chosen option recorded in `33-02-SUMMARY.md`.

Purpose: REQ-3..5 are the discoverability + queueing surface for the strategic decision. Without these, the ADR is unreachable from project root navigation and the audit's `will-sync` clusters have no execution owner.

Output: Three modified files (PROJECT.md, 25-HUMAN-UAT.md, ROADMAP.md); zero new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/33-windows-parity-upstream-0-52-divergence/33-CONTEXT.md
@.planning/phases/33-windows-parity-upstream-0-52-divergence/33-SPEC.md
@.planning/phases/33-windows-parity-upstream-0-52-divergence/33-RESEARCH.md
@.planning/phases/33-windows-parity-upstream-0-52-divergence/33-PATTERNS.md
@.planning/phases/33-windows-parity-upstream-0-52-divergence/33-VALIDATION.md
@.planning/phases/33-windows-parity-upstream-0-52-divergence/33-00-SUMMARY.md
@.planning/phases/33-windows-parity-upstream-0-52-divergence/33-02-SUMMARY.md
@.planning/phases/33-windows-parity-upstream-0-52-divergence/DIVERGENCE-LEDGER.md
@docs/architecture/upstream-parity-strategy.md
@.planning/PROJECT.md
@.planning/phases/25-cross-platform-resl-aipc-unix-design/25-HUMAN-UAT.md
@.planning/ROADMAP.md

<interfaces>
<!-- REQ-3 target (locked in 33-00-SUMMARY.md per RESEARCH Open Question 1) -->
<!-- .planning/PROJECT.md L158-183: 3-column `| Decision | Rationale | Outcome |` Key Decisions table -->
<!-- Append one row AFTER current last row at L183, BEFORE `## Upstream Parity Process` heading at L185 -->

<!-- REQ-4 target -->
<!-- .planning/phases/25-cross-platform-resl-aipc-unix-design/25-HUMAN-UAT.md L62-87: G-25-DRIFT-01 entry -->
<!-- Append Update section AFTER current Cross-references bullet list (after L87) -->
<!-- Frontmatter `status: open` at L64 STAYS OPEN — Phase 33 does NOT close the gap -->

<!-- REQ-5 target (TWO edits per PATTERNS Pitfall 8) -->
<!-- Edit 1: .planning/ROADMAP.md L394-409: Phase 33 entry — update Plans count + flip TBD bullet to actual plan list -->
<!-- Edit 2: .planning/ROADMAP.md AFTER L409: append the Phase 34 UPST3-sync stub -->
<!--   - Title varies per D-33-D1 + chosen ADR option (continue / split / freeze) -->
<!--   - Required structure: ### Phase 34 + Goal + Trigger + Requirements + Depends on + Plans + (optional Reference) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REQ-3 — Append Key Decisions row to PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md L155-186 (the Key Decisions section — confirm current last row before appending; identify the `## Upstream Parity Process` heading position)
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-00-SUMMARY.md (confirm `project-md-target` lock = 3-column table at L158-183)
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-02-SUMMARY.md (read the chosen option name for the Outcome cell)
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-PATTERNS.md L228-258 (verbatim row shape)
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/DIVERGENCE-LEDGER.md § Cluster Summary (read cluster count + dispositions for the Rationale cell)
  </read_first>
  <action>
    Append ONE row to the 3-column `| Decision | Rationale | Outcome |` table at `.planning/PROJECT.md` L158-183. The target is locked in `33-00-SUMMARY.md` (RESEARCH Open Question 1 verdict = 3-column table, NOT Requirements bullets per RESEARCH Pitfall 6).

    **Verbatim row shape (from PATTERNS L249-251 — values filled at write time):**

    ```markdown
    | Phase 33 Upstream parity strategy (continue / split / freeze) | <chosen option's rationale: 2-4 sentences — 5-criteria L/M/H aggregate (e.g., "Med High High Med High"), tiebreaker if applicable per D-33-C3, evidence from DIVERGENCE-LEDGER cluster counts + fork-only surface size (cite actual numbers)> | ✔ Decided — [docs/architecture/upstream-parity-strategy.md](../docs/architecture/upstream-parity-strategy.md); <UPST3-sync follow-up / split-execution follow-up / freeze-bookkeeping follow-up> queued in ROADMAP § Phase 34 |
    ```

    **Fill rules:**
    - **Decision cell:** Verbatim `Phase 33 Upstream parity strategy (continue / split / freeze)` (the 3-option summary is the title; the picked option goes in the Rationale + Outcome).
    - **Rationale cell:** 2-4 sentences with chosen option summary + L/M/H aggregate + cluster-count evidence + fork-only-surface-size evidence + tiebreaker if applicable. Read actual numbers from `DIVERGENCE-LEDGER.md § Cluster Summary` (cluster count, will-sync vs fork-preserve vs won't-sync mix). Example skeleton:
      > "Chose <option name> (<aggregate L/M/H shape e.g. Med High High Med High>); ledger surfaced N clusters across 12 minor releases with <X> will-sync / <Y> fork-preserve / <Z> won't-sync dispositions, plus <K> fork-only surface seams without upstream analog. <If tiebreaker applied:> Per D-33-C3 tiebreaker (PROJECT.md core value — OS-enforced isolation, structurally impossible bypass — favors security-posture column), <option> wins. <Else:> Aggregate dominated without tiebreaker."
    - **Outcome cell:** Use `✔ Decided` glyph + ADR link. The follow-up phrase depends on the chosen option (D-33-D1):
      - Option A (continue): `UPST3-sync follow-up`
      - Option B (split-windows): `Phase 34 split-execution follow-up`
      - Option C (freeze-at-v0.52): `Phase 34 freeze-bookkeeping follow-up`
    - **Outcome cell link path:** Relative from `.planning/PROJECT.md` → `docs/architecture/upstream-parity-strategy.md` = `../docs/architecture/upstream-parity-strategy.md` (one `..` up from `.planning/` to repo root, then down).

    **Insertion point:** AFTER the current last row of the Key Decisions table (currently L183 = Phase 22 audit-integrity DSSE row), BEFORE the `## Upstream Parity Process` section heading at L185. Read the file at write time to confirm current line positions — concurrent edits may have shifted lines.

    Use the **Edit tool** with a precise old_string/new_string pair:
    - `old_string` = `<current last row line>\n\n## Upstream Parity Process`
    - `new_string` = `<current last row line>\n| Phase 33 Upstream parity strategy ... |\n\n## Upstream Parity Process`

    DO NOT use heredoc / `cat <<EOF`. DO NOT re-write the entire PROJECT.md (only Edit the targeted append point).
  </action>
  <verify>
    <automated>grep -qE "upstream-parity-strategy" .planning/PROJECT.md &amp;&amp; grep -qE "Phase 33 Upstream parity strategy.*continue.*split.*freeze" .planning/PROJECT.md &amp;&amp; grep -qE "✔ Decided.*upstream-parity-strategy\.md" .planning/PROJECT.md &amp;&amp; echo OK</automated>
  </verify>
  <done>
    - PROJECT.md has exactly one new row in the Key Decisions table referencing `upstream-parity-strategy.md`
    - Row link path is `../docs/architecture/upstream-parity-strategy.md` (resolves from `.planning/PROJECT.md` to the ADR file)
    - Rationale cell cites the chosen option + L/M/H aggregate + ledger evidence (cluster counts, fork-only surface size)
    - Outcome cell uses `✔ Decided` glyph
    - No rows BEFORE the new row were modified (append-only edit)
    - `## Upstream Parity Process` heading and following sections untouched
  </done>
</task>

<task type="auto">
  <name>Task 2: REQ-4 — Append G-25-DRIFT-01 Update section</name>
  <files>.planning/phases/25-cross-platform-resl-aipc-unix-design/25-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/25-cross-platform-resl-aipc-unix-design/25-HUMAN-UAT.md L62-90 (read the existing G-25-DRIFT-01 entry through its Cross-references bullet list; confirm frontmatter `status: open` at L64)
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-CONTEXT.md D-33-D2 (Update section spec — 4 required subsections)
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-PATTERNS.md L296-311 (verbatim append shape)
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-02-SUMMARY.md (chosen option name for subsection 2)
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/DIVERGENCE-LEDGER.md (find the cluster covering RESL flag renames for subsection 1 commit count + name)
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-00-SUMMARY.md (read `audit-date` for the Update section header)
  </read_first>
  <action>
    Append a `**Update (Phase 33, <audit-date>):**` section to G-25-DRIFT-01 in `25-HUMAN-UAT.md`. CRITICAL: this is a full appended section per D-33-D2 — NOT a frontmatter edit. The frontmatter `status: open` field at L64 STAYS OPEN.

    **Append target:** AFTER the existing Cross-references bullet list (after L87 — the current last line of the G-25-DRIFT-01 entry). Read the file to confirm the actual line number at write time (other concurrent edits may have shifted lines).

    **Verbatim section to append (from PATTERNS L299-305, D-33-D2 4-subsection spec):**

    ```markdown

    **Update (Phase 33, <audit-date from 33-00-SUMMARY>):**

    1. **Drift audit summary:** Confirmed <N> commits in upstream cluster `<cluster name covering RESL flags from DIVERGENCE-LEDGER.md>` (introduced in v0.<X>.0) covering the RESL-flag-rename surface. See [`DIVERGENCE-LEDGER.md`](../33-windows-parity-upstream-0-52-divergence/DIVERGENCE-LEDGER.md) for the full row table.
    2. **Parity-strategy ADR decision:** The strategic ADR landed at [`docs/architecture/upstream-parity-strategy.md`](../../../docs/architecture/upstream-parity-strategy.md) picked option <A/B/C>: <short option name>. Implication for this gap: <one-line per chosen option — pick exactly ONE bullet matching the chosen option; do not include all three>:
       - For option A (continue): "RESL renames will sync in UPST3 (Phase 34) following the ledger's `will-sync` disposition for this cluster."
       - For option B (split-windows): "RESL flags stay fork-named with the divergence documented in the split-windows execution phase."
       - For option C (freeze-at-v0.52): "RESL flags stay frozen at v0.40 verbatim; upstream's renamed flags are accepted as out-of-fork-scope going forward."
    3. **Closure handoff:** Gap stays `status: open` until Phase 34 (UPST3-sync follow-up — or whichever Phase 34 title D-33-D1 assigns based on the chosen option) lands the actual renames. **Phase 33 does NOT close G-25-DRIFT-01** — the audit + decision artifacts ship without the execution work per SPEC.md § Out of scope.
    4. **Audit-walk note:** <Conditional — resolve at write time:>
       - If the audit surfaced MORE than 4 RESL-flag-rename commits: "Audit surfaced <N - 4> additional RESL-flag-rename commits beyond the 4 originally suspected from Phase 25 HUMAN-UAT; see ledger cluster `<cluster name>`."
       - If the audit surfaced EXACTLY 4 RESL-flag-rename commits: "Audit confirmed exactly the 4 RESL-flag-rename commits originally suspected from Phase 25 HUMAN-UAT — no additional drift in this surface area."
       - If the audit surfaced FEWER than 4 RESL-flag-rename commits: "Audit surfaced only <N> RESL-flag-rename commits — fewer than the 4 originally suspected; cluster <name> contains the actual evidence."
    ```

    **Fill rules:**
    - `<audit-date>`: from `33-00-SUMMARY.md` `audit-date` field (YYYY-MM-DD UTC).
    - `<N>` + `<cluster name covering RESL flags>` + `<X>`: read `DIVERGENCE-LEDGER.md` and find the cluster whose rationale references G-25-DRIFT-01 or whose commit subjects contain RESL flag renames (`--memory`, `--cpu-percent`, `--max-processes`, `--timeout`). Cite the cluster's commit count, name, and introducing upstream tag.
    - `<A/B/C>` + `<short option name>`: from `33-02-SUMMARY.md` chosen-option record.
    - Subsection 2: Pick exactly ONE bullet matching the chosen option; remove the other two.
    - Subsection 4: Resolve the conditional at write time by counting RESL-flag-rename commits in the ledger.

    **Relative-path link rules (from PATTERNS § "Cross-file linking conventions"):**
    - From `25-HUMAN-UAT.md` (at `.planning/phases/25-.../`) → Phase 33's `DIVERGENCE-LEDGER.md` (at `.planning/phases/33-.../`): `../33-windows-parity-upstream-0-52-divergence/DIVERGENCE-LEDGER.md`
    - From `25-HUMAN-UAT.md` → `docs/architecture/upstream-parity-strategy.md`: `../../../docs/architecture/upstream-parity-strategy.md` (three `..` up from `phases/25-.../` to repo root, then down to `docs/`)

    Use the **Edit tool** with old_string = "<last 1-2 lines of the existing G-25-DRIFT-01 Cross-references bullet list>" and new_string = "<those same lines>\n\n<the appended Update section above>".

    **DO NOT touch the frontmatter `status: open` field at L64.** Verify after the edit:
    ```bash
    grep -E "^status: open$" .planning/phases/25-cross-platform-resl-aipc-unix-design/25-HUMAN-UAT.md
    ```
    Must still match.
  </action>
  <verify>
    <automated>grep -qE "^\*\*Update \(Phase 33, [0-9]{4}-[0-9]{2}-[0-9]{2}\):\*\*$" .planning/phases/25-cross-platform-resl-aipc-unix-design/25-HUMAN-UAT.md &amp;&amp; grep -qE "(DIVERGENCE-LEDGER\.md|upstream-parity-strategy\.md)" .planning/phases/25-cross-platform-resl-aipc-unix-design/25-HUMAN-UAT.md &amp;&amp; grep -qE "Phase 33 does NOT close G-25-DRIFT-01" .planning/phases/25-cross-platform-resl-aipc-unix-design/25-HUMAN-UAT.md &amp;&amp; grep -qE "^status: open$" .planning/phases/25-cross-platform-resl-aipc-unix-design/25-HUMAN-UAT.md &amp;&amp; echo OK</automated>
  </verify>
  <done>
    - G-25-DRIFT-01 entry has a new `**Update (Phase 33, YYYY-MM-DD):**` section appended after Cross-references
    - All 4 subsections present (drift audit summary, parity-strategy ADR decision, closure handoff, audit-walk note)
    - Subsection 2 names exactly ONE chosen option (not all three)
    - Both relative links resolve: `DIVERGENCE-LEDGER.md` and `upstream-parity-strategy.md`
    - Frontmatter `status: open` UNCHANGED at L64
    - Phrase `Phase 33 does NOT close G-25-DRIFT-01` appears verbatim in subsection 3
  </done>
</task>

<task type="auto">
  <name>Task 3: REQ-5 — Append Phase 34 UPST3 stub + flip Phase 33 entry to complete</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md L394-409 (current Phase 33 stub — the analog per D-33-D1 + the TARGET of Edit 1)
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-CONTEXT.md D-33-D1 (UPST3-sync title spec; title flips for B/C)
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-PATTERNS.md L317-362 (verbatim stub shape + PITFALL 8 — TWO ROADMAP edits required)
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-02-SUMMARY.md (chosen option dictates the Phase 34 title per D-33-D1)
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-00-SUMMARY.md (audit-date for Phase 33 complete marker)
  </read_first>
  <action>
    **CRITICAL — PATTERNS Pitfall 8:** This task requires TWO ROADMAP edits, not one. (1) Update Phase 33's own existing entry (L394-409) to mark plans complete. (2) Append the new Phase 34 stub AFTER L409.

    Before editing, re-check current ROADMAP state:
    ```bash
    grep -nE "^### Phase " .planning/ROADMAP.md
    ```
    Confirm Phase 33 is at L394 (or wherever it has shifted) and Phase 34 is the next free slot. If Phase 34 has been taken by a concurrent edit, use the next free integer (Phase 35, etc.) and document the slot bump in the Phase 33 SUMMARY.

    ---

    **Edit 1 — Update Phase 33's own entry (replace TBD plan list with actual shipped plans):**

    Current state (verbatim from L394-409):
    ```markdown
    ### Phase 33: Windows parity with upstream 0.52 features and divergence decision

    **Goal:** [To be planned] — close the v0.52 upstream-parity gap surfaced after v0.41 baseline (Phase 25's G-25-DRIFT-01 + RESL-flag rename in upstream v0.52) AND decide whether continued parity is sustainable in this repo or warrants splitting Windows off into a dedicated repo (`always-further/nono`).

    **Trigger:** Upstream baseline at v0.52 has accumulated feature divergence we have not yet absorbed; Phase 25 surfaced G-25-DRIFT-01 (RESL flags renamed in upstream v0.52) as an UPST3 follow-up. The repo-split question is a strategic decision, not just a code merge.

    **Requirements:** TBD — to be locked at `/gsd-spec-phase 33` / `/gsd-discuss-phase 33`.

    **Depends on:** Phase 25 (RESL Unix backends + G-25-DRIFT-01), Phase 32 (Sigstore Integration; closes Windows-only trust-anchor surface).

    **Plans:** 0 plans

    Plans:
    - [ ] TBD (run `/gsd-spec-phase 33` then `/gsd-plan-phase 33`)

    **Reference:** Upstream repo — https://github.com/always-further/nono
    ```

    Target state — three replacements within the Phase 33 entry:

    1. **Goal line:** Replace `[To be planned] — close the v0.52 upstream-parity gap ...` with the audit-shaped goal:
       > `Produce audited DIVERGENCE-LEDGER.md inventory of v0.40.1..v0.52.0 fork-vs-upstream divergence AND scored strategic ADR (upstream-parity-strategy.md) picking one of three options (continue / split-windows / freeze-at-v0.52). Sync execution deferred to Phase 34 (UPST3-sync) per SPEC.md § Out of scope.`

    2. **Requirements line:** Replace `TBD — to be locked at ...` with:
       > `REQ-1 (drift audit + DIVERGENCE-LEDGER.md), REQ-2 (strategic ADR with scored 3-option matrix), REQ-3 (PROJECT.md key-decisions row), REQ-4 (G-25-DRIFT-01 cross-reference), REQ-5 (UPST3-sync placeholder in ROADMAP) — locked in 33-SPEC.md 2026-05-10.`

    3. **Plans line + bullet list:** Replace `**Plans:** 0 plans` with `**Plans:** 4 plans` AND replace the single TBD bullet with the actual 4 plans Phase 33 shipped:
       > ```markdown
       > **Plans:** 4 plans
       >
       > Plans:
       > - [x] 33-00-PLAN.md — Wave 0 prep: fetch upstream tags, capture drift-tool / upstream HEAD shas for ledger provenance, lock RESEARCH Open Questions 1 + 2
       > - [x] 33-01-PLAN.md — REQ-1 drift audit: run `make check-upstream-drift ARGS="--from v0.40.1 --to v0.52.0 --format json"` and curate DIVERGENCE-LEDGER.md (two-tier cluster + nested row table) with manual fork-only surface enumeration
       > - [x] 33-02-PLAN.md — REQ-2 strategic ADR: write `docs/architecture/upstream-parity-strategy.md` (plain-text `**Status:** Accepted` header per D-33-C4; 3 options × 5 criteria L/M/H scoring)
       > - [x] 33-03-PLAN.md — REQ-3 + REQ-4 + REQ-5 downstream updates: PROJECT.md Key Decisions row + 25-HUMAN-UAT.md G-25-DRIFT-01 Update section + ROADMAP Phase 34 UPST3 stub + Phase 33 entry flipped to complete
       > ```

    Use 3 separate Edit tool calls with narrow old_string/new_string pairs, NOT a single rewrite of the whole entry.

    ---

    **Edit 2 — Append Phase 34 UPST3-sync stub AFTER Phase 33's `**Reference:**` line (verbatim shape from PATTERNS L340-356, D-33-D1):**

    The stub title varies per D-33-D1 based on the chosen ADR option (read from `33-02-SUMMARY.md`):
    - **Option A (continue):** `Phase 34: UPST3 — Upstream v0.41–v0.52 Sync Execution`
    - **Option B (split-windows):** `Phase 34: Windows-fork split execution`
    - **Option C (freeze-at-v0.52):** `Phase 34: v0.52 freeze-bookkeeping`

    Append shape (verbatim from PATTERNS L341-356 for option A; adapt Goal + Reference for B/C):

    ```markdown

    ### Phase 34: <title per D-33-D1 above>

    **Goal:** [To be planned] — <execute the cherry-picks and manual replays catalogued in Phase 33's `DIVERGENCE-LEDGER.md` per the parity-strategy ADR (`docs/architecture/upstream-parity-strategy.md`), closing G-25-DRIFT-01 once the RESL flag renames land FOR OPTION A | execute the workspace split per the parity-strategy ADR (`docs/architecture/upstream-parity-strategy.md`); G-25-DRIFT-01 stays open as a documented fork-vs-upstream divergence FOR OPTION B | document the v0.52 freeze posture per the parity-strategy ADR (`docs/architecture/upstream-parity-strategy.md`); G-25-DRIFT-01 stays open as accepted divergence FOR OPTION C>.

    **Trigger:** Phase 33 audit produced disposition-complete ledger with `will-sync` clusters queued for execution; ADR locked <continue-parity | split-windows | freeze-at-v0.52> option.

    **Requirements:** TBD — to be locked at `/gsd-spec-phase 34` / `/gsd-discuss-phase 34`.

    **Depends on:** Phase 33 (audit ledger + parity-strategy ADR).

    **Plans:** 0 plans

    Plans:
    - [ ] TBD (run `/gsd-spec-phase 34` then `/gsd-plan-phase 34`)

    **Reference:** `.planning/phases/33-windows-parity-upstream-0-52-divergence/DIVERGENCE-LEDGER.md`, `docs/architecture/upstream-parity-strategy.md`<, `.planning/templates/upstream-sync-quick.md` FOR OPTION A ONLY>
    ```

    Use **one Edit tool call** for Edit 2 with old_string = `<Phase 33 **Reference:** line + trailing blank lines>` and new_string = `<same lines + the new Phase 34 stub>`.

    ---

    **Pre-commit verification (per PATTERNS Pitfall 8 + 33-VALIDATION.md REQ-5 validator block):**

    ```bash
    # Phase 34 stub queued:
    grep -E "^### Phase 34: (UPST3.*Sync Execution|Windows-fork split execution|v0.52 freeze-bookkeeping)" .planning/ROADMAP.md
    # Depends on Phase 33:
    grep -A8 "^### Phase 34:" .planning/ROADMAP.md | grep "Depends on.*Phase 33"
    # Plans: 0 plans boilerplate (matches existing TBD-stub shape):
    grep -A10 "^### Phase 34:" .planning/ROADMAP.md | grep -E "Plans: 0 plans"
    # Phase 33 entry has been flipped:
    grep -A4 "^### Phase 33:" .planning/ROADMAP.md | grep -E "Plans:\*\* 4 plans"
    # Phase 33's bullet list now has 4 [x] entries (no TBD):
    sed -n '/^### Phase 33:/,/^### Phase 34:/p' .planning/ROADMAP.md | grep -cE "^- \[x\] 33-0[0-3]-PLAN\.md"  # expect 4
    sed -n '/^### Phase 33:/,/^### Phase 34:/p' .planning/ROADMAP.md | grep -cE "TBD \(run.*gsd-spec-phase 33"  # expect 0
    ```

    All 6 greps must pass before committing.
  </action>
  <verify>
    <automated>grep -qE "^### Phase 34: (UPST3.*Sync Execution|Windows-fork split execution|v0.52 freeze-bookkeeping)" .planning/ROADMAP.md &amp;&amp; grep -A8 "^### Phase 34:" .planning/ROADMAP.md | grep -q "Depends on.*Phase 33" &amp;&amp; grep -A10 "^### Phase 34:" .planning/ROADMAP.md | grep -qE "Plans: 0 plans" &amp;&amp; grep -A4 "^### Phase 33:" .planning/ROADMAP.md | grep -qE "Plans:\*\* 4 plans" &amp;&amp; test $(sed -n '/^### Phase 33:/,/^### Phase 34:/p' .planning/ROADMAP.md | grep -cE "^- \[x\] 33-0[0-3]-PLAN\.md") -eq 4 &amp;&amp; echo OK</automated>
  </verify>
  <done>
    - Phase 33's ROADMAP entry: Goal line updated (no more `[To be planned]`), Requirements line lists REQ-1..5, Plans count = 4, bullet list shows the 4 actual plans as `[x]` checked
    - Phase 34 stub appended after Phase 33's `**Reference:**` line
    - Phase 34 title matches the chosen option per D-33-D1
    - Phase 34 has `Depends on: Phase 33` AND `Plans: 0 plans` AND `Goal: [To be planned]` boilerplate matching the TBD-stub convention
    - Phase 34 Reference list cites DIVERGENCE-LEDGER + ADR + (for option A only) upstream-sync-quick.md template
  </done>
</task>

<task type="auto">
  <name>Task 4: Self-audit all three downstream edits + final commit</name>
  <files>(verification only — no edits in this task; final commit aggregates Tasks 1-3)</files>
  <read_first>
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-VALIDATION.md § REQ-3, REQ-4, REQ-5 validator blocks + Cross-cutting `make ci` block
    - .planning/PROJECT.md (read back to verify Task 1 row)
    - .planning/phases/25-cross-platform-resl-aipc-unix-design/25-HUMAN-UAT.md (read back to verify Task 2 Update section)
    - .planning/ROADMAP.md (read back to verify Task 3 edits)
  </read_first>
  <action>
    Run all REQ-3 / REQ-4 / REQ-5 validators from `33-VALIDATION.md` + cross-cutting checks, then commit all three edits together.

    **REQ-3 validators (verbatim from 33-VALIDATION.md):**
    ```bash
    grep -E "upstream-parity-strategy" .planning/PROJECT.md
    grep -B1 -A1 "upstream-parity-strategy" .planning/PROJECT.md  # confirms 3-column-table context
    ```
    Expected: at least one match; surrounding lines show pipe-delimited row shape (NOT bullet-list shape).

    **REQ-4 validators:**
    ```bash
    grep -E "^\*\*Update \(Phase 33, 2026-[0-9]{2}-[0-9]{2}\):\*\*" .planning/phases/25-cross-platform-resl-aipc-unix-design/25-HUMAN-UAT.md
    grep -E "(DIVERGENCE-LEDGER\.md|upstream-parity-strategy\.md)" .planning/phases/25-cross-platform-resl-aipc-unix-design/25-HUMAN-UAT.md
    grep -E "^status: open$" .planning/phases/25-cross-platform-resl-aipc-unix-design/25-HUMAN-UAT.md
    grep -E "Phase 33 does NOT close G-25-DRIFT-01" .planning/phases/25-cross-platform-resl-aipc-unix-design/25-HUMAN-UAT.md
    ```
    All 4 must return matches.

    **REQ-5 validators:**
    ```bash
    grep -E "^### Phase 34: (UPST3.*Sync Execution|Windows-fork split execution|v0.52 freeze-bookkeeping)" .planning/ROADMAP.md
    grep -A8 "^### Phase 34:" .planning/ROADMAP.md | grep "Depends on.*Phase 33"
    grep -A10 "^### Phase 34:" .planning/ROADMAP.md | grep -E "Plans: 0 plans"
    grep -A4 "^### Phase 33:" .planning/ROADMAP.md | grep -E "Plans:\*\* 4 plans"
    ```
    All 4 must return matches.

    **Cross-cutting (D-19 + make ci, from 33-VALIDATION.md):**
    ```bash
    # crates/nono/ byte-identical D-19 invariant:
    git diff --name-only -- crates/nono/ | wc -l   # expect 0
    # make ci green (docs-only phase — should be uneventful):
    make ci 2>&1 | tail -10
    echo "make-ci-exit=$?"
    ```
    Expected: 0 files diff in `crates/nono/`; `make ci` exit 0.

    **If all 12 validators pass, commit all three edits in one commit:**

    ```
    docs(33): land downstream artifacts — PROJECT.md row + G-25-DRIFT-01 Update + ROADMAP Phase 34 stub

    Closes REQ-3 / REQ-4 / REQ-5 for Phase 33.

    - REQ-3 (D-33 RESEARCH OQ-1 locked target): Append one row to PROJECT.md
      Key Decisions 3-column table (L158-183) for the parity-strategy decision;
      row's Outcome cell links to docs/architecture/upstream-parity-strategy.md
      with ✔ Decided glyph and cites the Phase 34 follow-up.

    - REQ-4 (D-33-D2): Append `**Update (Phase 33, <date>):**` section to
      G-25-DRIFT-01 in 25-HUMAN-UAT.md with all 4 D-33-D2 subsections (drift
      audit summary, parity-strategy ADR decision, closure handoff, audit-walk
      note). Frontmatter `status: open` UNCHANGED — Phase 33 does NOT close
      this gap; closure handoff is to Phase 34.

    - REQ-5 (D-33-D1 + PATTERNS Pitfall 8): TWO ROADMAP edits.
      (1) Flip Phase 33's own entry — Goal line updated, Requirements lists
          REQ-1..5, Plans count = 4, bullet list shows 4 [x] actual plans.
      (2) Append Phase 34 stub — title per D-33-D1 (UPST3 / split-execution
          / freeze-bookkeeping based on chosen ADR option), Depends on Phase
          33, Plans: 0 plans TBD-stub shape.

    crates/nono/ D-19 byte-identical invariant holds (zero diffs).
    make ci green (docs-only phase).

    Refs: REQ-3, REQ-4, REQ-5, D-33-D1, D-33-D2, RESEARCH OQ-1
    Signed-off-by: <user>
    ```

    Stage only the 3 modified files:
    ```bash
    git add .planning/PROJECT.md .planning/phases/25-cross-platform-resl-aipc-unix-design/25-HUMAN-UAT.md .planning/ROADMAP.md
    git commit -m "..."
    ```

    Do NOT include `.planning/phases/33-windows-parity-upstream-0-52-divergence/33-03-SUMMARY.md` in this commit — the SUMMARY is written and committed in the subsequent execute-phase wrap step, not here.
  </action>
  <verify>
    <automated>test $(git diff --name-only -- crates/nono/ | wc -l) -eq 0 &amp;&amp; make ci &gt; /dev/null 2&gt;&amp;1 &amp;&amp; grep -qE "upstream-parity-strategy" .planning/PROJECT.md &amp;&amp; grep -qE "^\*\*Update \(Phase 33, [0-9]{4}-[0-9]{2}-[0-9]{2}\):\*\*" .planning/phases/25-cross-platform-resl-aipc-unix-design/25-HUMAN-UAT.md &amp;&amp; grep -qE "^### Phase 34: (UPST3|Windows-fork split execution|v0.52 freeze-bookkeeping)" .planning/ROADMAP.md &amp;&amp; echo OK</automated>
  </verify>
  <done>
    - All 12 validators (4 REQ-3 + 4 REQ-4 + 4 REQ-5) pass
    - `crates/nono/` D-19 invariant: 0 file diffs
    - `make ci` green
    - Single commit aggregates the 3 file edits (PROJECT.md, 25-HUMAN-UAT.md, ROADMAP.md) with full DCO sign-off and references to REQ-3/4/5 + D-33-D1/D2
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADR text (Wave 2) → REQ-3/4/5 downstream artifacts | Wave 3 mirrors the chosen option into 3 downstream files; if those mirrorings drift from the ADR over time, the project's strategic intent becomes inconsistent across artifacts |
| G-25-DRIFT-01 closure status → Phase 33 wrap | Phase 33 explicitly does NOT close the gap (out of scope per SPEC); accidentally flipping `status: open` to `closed` would create a premature-closure threat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03-01 | T (Tampering) | G-25-DRIFT-01 premature closure | mitigate | Task 2 explicitly preserves `status: open`; Task 4 validator block greps `^status: open$` after the edit. CONTEXT D-33-D2 + SPEC.md § Out of scope both lock this. Per the security note in the planning context: "T-PROCESS: Premature G-25-DRIFT-01 closure — explicitly out-of-scope per SPEC and CONTEXT D-33-D2; gap stays status: open." |
| T-33-03-02 | T (Tampering) | PROJECT.md row placed in wrong table | mitigate | Wave 0 Task 3 checkpoint locked `project-md-target = 3-column Key Decisions table at L158-183` per RESEARCH Open Question 1 + PATTERNS Pitfall 6. Task 1 uses Edit tool with `## Upstream Parity Process` heading as anchor to ensure the row lands inside the Key Decisions table boundary. |
| T-33-03-03 | I (Information Disclosure) | ROADMAP.md content | accept | Public roadmap; no secrets. |
| T-33-03-04 | R (Repudiation) | Phase 34 title flip per chosen option | mitigate | D-33-D1 explicit conditional: title depends on chosen ADR option. Wave 2 SUMMARY records the picked option; Task 3 reads it and selects the matching title. Single source of truth for the chosen option = the Wave 2 ADR + its SUMMARY. |
| T-33-03-05 | E (Elevation of Privilege) | (none — docs-only) | n/a | No code path is added or modified. |
</threat_model>

<verification>
- `.planning/PROJECT.md` has a new row in the Key Decisions table linking to `upstream-parity-strategy.md`
- `.planning/phases/25-cross-platform-resl-aipc-unix-design/25-HUMAN-UAT.md` has a `**Update (Phase 33, ...):**` section appended to G-25-DRIFT-01 with all 4 D-33-D2 subsections; frontmatter `status: open` UNCHANGED
- `.planning/ROADMAP.md` has TWO updated sections: Phase 33's own entry flipped to complete (4 plans listed); new Phase 34 stub appended with title matching the chosen ADR option per D-33-D1, `Depends on: Phase 33`, `Plans: 0 plans`
- `crates/nono/` D-19 byte-identical invariant holds (zero file diffs in `crates/nono/`)
- `make ci` green (no clippy / fmt / test regressions — docs-only phase)
- Single commit aggregates the 3 downstream edits with full DCO sign-off and references to REQ-3/4/5 + D-33-D1/D2
</verification>

<success_criteria>
- REQ-3 acceptance fully met: PROJECT.md grep finds the row; row's ADR link resolves to the file written under REQ-2
- REQ-4 acceptance fully met: Grep for `G-25-DRIFT-01` in Phase 25's HUMAN-UAT finds cross-references; gap status remains `open`; closure handoff to Phase 34 is explicit
- REQ-5 acceptance fully met: ROADMAP.md grep finds the new Phase 34 placeholder; its `Depends on` line references Phase 33; ledger entries with disposition `will-sync` are explicitly assigned to Phase 34 (via the ADR's Future audit cadence subsection + Phase 34 Goal line)
- Phase 33's own ROADMAP entry flips from TBD to complete (PATTERNS Pitfall 8 satisfied)
</success_criteria>

<output>
After completion, the strategic decision is fully discoverable from project root navigation:
- `PROJECT.md` → Key Decisions row → `upstream-parity-strategy.md` ADR
- `ROADMAP.md` Phase 33 → complete (lists 4 plans); ROADMAP.md Phase 34 → queued (titled per chosen option, depends on Phase 33)
- `25-HUMAN-UAT.md` G-25-DRIFT-01 → Update section cross-references both ledger + ADR; gap stays open with explicit handoff to Phase 34

The Plan 33-03 SUMMARY (written in the execute-phase wrap step, NOT this task) records: which option was chosen, the L/M/H aggregate, the cluster count + dispositions cited in the PROJECT.md row, and the Phase 34 slot + title that the planner appended.
</output>
