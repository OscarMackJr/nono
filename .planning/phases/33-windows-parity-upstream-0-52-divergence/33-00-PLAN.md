---
phase: 33-windows-parity-upstream-0-52-divergence
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: false
requirements: [REQ-1]
tags: [upstream-parity, audit-prep, drift-tool]

must_haves:
  truths:
    - "Upstream tags v0.43.1..v0.52.0 are fetched locally (D-33-A1 prereq)"
    - "Drift-tool script sha is captured for ledger header (D-33-A2)"
    - "Upstream HEAD sha at audit time is captured for ledger header (D-33-A2)"
    - "Open Question 1 (PROJECT.md key-decisions target) is resolved and recorded in this plan's SUMMARY before Wave 3 runs"
  artifacts:
    - path: ".planning/phases/33-windows-parity-upstream-0-52-divergence/33-00-SUMMARY.md"
      provides: "Prep artifacts: drift-tool script sha, upstream HEAD sha at audit time, drift-tool JSON output path (gitignored / tmp), PROJECT.md key-decisions target resolution"
      contains: "drift-tool-sha:, upstream-head-sha:, audit-date:, project-md-target:"
  key_links:
    - from: "33-00-SUMMARY.md"
      to: "33-01-PLAN.md / 33-03-PLAN.md"
      via: "captured shas feed the ledger header (Wave 1) and the PROJECT.md target lock guides the row writer (Wave 3)"
      pattern: "drift-tool-sha|upstream-head-sha|project-md-target"
---

<objective>
Prepare the audit environment so Wave 1's drift-tool invocation succeeds and Wave 1's ledger header records reproducible provenance.

Purpose: Per RESEARCH §8, upstream tags v0.43.1..v0.52.0 are NOT currently fetched locally — `git fetch upstream --tags` is REQUIRED before `make check-upstream-drift ARGS="--from v0.40.1 --to v0.52.0 ..."` (D-33-A1) can succeed. Per D-33-A2, the ledger header must record the drift-tool script sha and upstream HEAD sha at audit time so the audit is reproducible against the historical tool. Per RESEARCH Open Question 1, the PROJECT.md "key-decisions row" target needs locking before Wave 3 writes the row.

Output: A short summary file capturing all three artifacts plus the checkpoint approval to proceed to Wave 1. NO code changes; NO committed files in this wave (the captured shas live in the SUMMARY artifact which Wave 1's ledger header references).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/33-windows-parity-upstream-0-52-divergence/33-CONTEXT.md
@.planning/phases/33-windows-parity-upstream-0-52-divergence/33-SPEC.md
@.planning/phases/33-windows-parity-upstream-0-52-divergence/33-RESEARCH.md
@.planning/phases/33-windows-parity-upstream-0-52-divergence/33-PATTERNS.md
@.planning/phases/33-windows-parity-upstream-0-52-divergence/33-VALIDATION.md

<interfaces>
<!-- Drift-tool invocation shape (verified from scripts/check-upstream-drift.sh L243-294 per RESEARCH Pattern 1) -->
<!-- D-33-A1 LOCKED invocation: -->
```
make check-upstream-drift ARGS="--from v0.40.1 --to v0.52.0 --format json"
```
<!-- Provenance-capture commands (D-33-A2 spec, RESEARCH §"Version verification"): -->
```bash
# drift-tool script sha:
git log -1 --format=%H scripts/check-upstream-drift.sh
# upstream HEAD sha at audit time:
git rev-parse upstream/main
# audit-date:
date -u +%Y-%m-%d
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch upstream tags + verify drift-tool prerequisites</name>
  <files>(none — read-only preparation)</files>
  <read_first>
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-RESEARCH.md §8 (Verification Gap — upstream tag fetch status)
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-VALIDATION.md § Wave 0 Requirements
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-CONTEXT.md D-33-A1
  </read_first>
  <action>
    Run the Wave 0 prerequisite checks in this exact order. Capture each command's output into a workspace scratch buffer (do NOT commit raw output, but include the captured shas in the Wave 0 SUMMARY in Task 3):

    1. Confirm `upstream` remote points at the right repo:
       ```bash
       git remote -v | grep upstream
       ```
       Expected: `upstream\s+https://github.com/always-further/nono.git` on both fetch and push lines.

    2. Fetch all upstream tags (RESEARCH §8 confirms v0.43.1..v0.52.0 are NOT currently local):
       ```bash
       git fetch upstream --tags
       ```
       Expected: at minimum tags `v0.43.1`, `v0.44.0`, ..., `v0.52.0` appear.

    3. Verify the v0.52.0 tag exists locally after fetch:
       ```bash
       git tag --list 'v0.5*' --sort=v:refname
       ```
       Expected: includes `v0.50.0`, `v0.50.1`, `v0.51.0`, `v0.52.0` (RESEARCH §"Installation" prediction).

    4. Drift-tool sanity check (Phase 24 D-04 artifact must exist + be invocable):
       ```bash
       make check-upstream-drift ARGS="--help" 2>&1 | head -20
       ```
       Expected: usage text from the Phase 24 twin script; non-zero exit but no `command not found`. The tool's `--help` is wired through both `scripts/check-upstream-drift.sh` and `scripts/check-upstream-drift.ps1`.

    5. If ANY of steps 1-4 fail, STOP and emit a checkpoint message — do not proceed to Task 2. The audit cannot run without these prerequisites.
  </action>
  <verify>
    <automated>git tag --list 'v0.52.0' | grep -q '^v0.52.0$' &amp;&amp; echo OK</automated>
  </verify>
  <done>
    - `git tag --list 'v0.5*'` shows v0.50.0 / v0.50.1 / v0.51.0 / v0.52.0
    - `git remote -v | grep upstream` shows `https://github.com/always-further/nono.git`
    - `make check-upstream-drift ARGS="--help"` exits non-zero but produces usage text (not "No rule to make target")
  </done>
</task>

<task type="auto">
  <name>Task 2: Capture provenance shas + dry-run drift tool</name>
  <files>(none — read-only capture)</files>
  <read_first>
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-CONTEXT.md D-33-A1, D-33-A2 (provenance requirements)
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-RESEARCH.md § "Version verification (drift tool)"
    - scripts/check-upstream-drift.sh (or .ps1 on Windows) — confirm it has not changed since Phase 24 ship
  </read_first>
  <action>
    Capture the three pieces of provenance that the Wave 1 ledger header MUST record (per D-33-A2). Each value goes into the Wave 0 SUMMARY (Task 3) verbatim — Wave 1's executor reads them from there.

    1. **Drift-tool script sha** (the commit sha of `scripts/check-upstream-drift.sh` at audit time — D-33-A2 requirement):
       ```bash
       git log -1 --format=%H scripts/check-upstream-drift.sh
       # Also capture the PowerShell twin sha:
       git log -1 --format=%H scripts/check-upstream-drift.ps1
       ```
       Record both shas verbatim.

    2. **Upstream HEAD sha at audit time** (D-33-A2 requirement — pinpoints what `upstream/main` resolved to when the audit ran):
       ```bash
       git rev-parse upstream/main
       ```
       Record this sha verbatim.

    3. **Audit date** (UTC, ISO short form):
       ```bash
       date -u +%Y-%m-%d
       ```
       Record verbatim.

    4. **Dry-run smoke test** of the actual drift-tool invocation (D-33-A1 — do NOT save the JSON output to a tracked file per D-33-A2; pipe to `/dev/null` and just confirm exit code 0):
       ```bash
       make check-upstream-drift ARGS="--from v0.40.1 --to v0.52.0 --format json" > /tmp/drift-smoke.json 2>&1
       echo "exit=$?"
       # Sanity-check shape:
       head -c 200 /tmp/drift-smoke.json
       grep -E '"total_unique_commits"\s*:' /tmp/drift-smoke.json | head -1
       grep -E '"range"\s*:' /tmp/drift-smoke.json | head -1
       ```
       Expected: exit=0; JSON output starts with `{` and contains `total_unique_commits` + `range` keys (per RESEARCH Pattern 1 verified shape). This proves the actual Wave 1 invocation will work without surprises.

    Per D-33-A2 the JSON file at `/tmp/drift-smoke.json` is NOT committed and may be deleted; only the captured shas and the smoke-test row count are recorded.
  </action>
  <verify>
    <automated>test -f /tmp/drift-smoke.json &amp;&amp; grep -q '"total_unique_commits"' /tmp/drift-smoke.json &amp;&amp; echo OK</automated>
  </verify>
  <done>
    - drift-tool-sh-sha + drift-tool-ps1-sha captured (40-char hex each)
    - upstream/main HEAD sha captured (40-char hex)
    - audit-date captured (YYYY-MM-DD UTC)
    - drift-tool dry-run exits 0 and emits well-formed JSON with `total_unique_commits` + `range` keys
    - The smoke-test commit count (`total_unique_commits` value) is noted for Wave 1 sizing
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Lock RESEARCH Open Question 1 (PROJECT.md key-decisions row target) + ADR commit pattern</name>
  <read_first>
    - .planning/PROJECT.md L158-183 (the 3-column `| Decision | Rationale | Outcome |` Key Decisions table — RESEARCH Open Question 1 target candidate A)
    - .planning/PROJECT.md L65-101 + L105-112 (the Requirements bullets — Open Question 1 target candidate B; RESEARCH recommends AGAINST this)
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-RESEARCH.md § Open Questions 1 + 2
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-PATTERNS.md § PROJECT.md analog (L158-183 verified as target)
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-CONTEXT.md "Claude's Discretion" (ADR Proposed-vs-Accepted commit pattern)
  </read_first>
  <decision>
    Lock two open questions before Wave 1 starts so Wave 3 has no further branches:

    (a) **PROJECT.md key-decisions row target**: Should the REQ-3 row be appended to the 3-column `| Decision | Rationale | Outcome |` table at L158-183 (RESEARCH-recommended; PATTERNS-verified) OR to the Requirements bullets at L65-101/L105-112 (loose SPEC.md / CONTEXT.md wording — "matches REQ-WRU-01 / SHELL-01 shape")?

    (b) **ADR commit pattern**: Should `upstream-parity-strategy.md` ship with `**Status:** Accepted` from the first commit, OR ship `**Status:** Proposed` first then a follow-up commit flips to `Accepted`?
  </decision>
  <context>
    (a) Three pieces of evidence say "the 3-column table at L158-183":
    - RESEARCH §"Three reality-check findings" #3: "The planner should treat the 3-column Key Decisions table as the canonical target (it's the more-discoverable structural artifact and matches 'key-decisions' naming)"
    - RESEARCH §Phase Requirements REQ-3 row: "3-column ... table at PROJECT.md:158-183; planner appends one row"
    - PATTERNS L228-258: verified-shape analog is the 3-column table; explicit "CRITICAL — RESEARCH Pitfall 6 + Assumption A3: The target IS the 3-column ... table at L158-183, NOT the Requirements bullets"

    The loose SPEC/CONTEXT wording ("REQ-WRU-01 / SHELL-01 shape") was a researcher misnaming that quoted Requirements-section identifiers; the structural intent of "key-decisions row" is the Key Decisions table.

    (b) CONTEXT § Claude's Discretion says both are valid:
    - `Accepted-first-commit`: the ADR ships in one commit; simpler chain; fits the "scope-lock has already happened in CONTEXT/SPEC" model.
    - `Proposed → Accepted` two-step: useful if the ADR needs review before acceptance.

    Phase 33's ADR is the locked product of a /gsd-discuss-phase + /gsd-spec-phase pipeline — the decision criteria (D-33-C1..C4) are already pinned; the option selection (A/B/C) is the only remaining freedom and happens during Wave 2 execution against the audit data. Once the maintainer picks the option in Wave 2, there's no review gate before commit; the `Accepted` status reflects the locked-in decision the moment Wave 2's commit lands.
  </context>
  <options>
    <option id="option-a">
      <name>(a) 3-column Key Decisions table at L158-183 + (b) Accepted-first-commit</name>
      <pros>
        - PATTERNS-verified target; matches existing Phase 22/23/32 Key-Decisions row precedent (each phase appends one row to this table)
        - Single commit for the ADR — simpler chain, easier to revert if Wave 2 surfaces a problem
        - "Accepted" status reflects the locked-in decision; no review gate exists between Wave 2 and the commit
        - Grep-discoverable from project root; PROJECT.md is the higher-traffic file vs Requirements bullets
      </pros>
      <cons>
        - None identified by RESEARCH / PATTERNS / CONTEXT — this is the convergent recommendation
      </cons>
    </option>
    <option id="option-b">
      <name>(a) Requirements bullets + (b) Proposed → Accepted two-step</name>
      <pros>
        - Two-step commit chain documents a "review window" between decision draft and acceptance
        - Requirements-bullet target reads SPEC.md wording literally (matches "REQ-WRU-01 / SHELL-01 shape")
      </pros>
      <cons>
        - Requirements-bullet target conflicts with PATTERNS verified analog (L228-258 PATTERNS file explicitly rejects this as a misread)
        - Two-step ADR commits add complexity with no review value (no human review gate exists between Wave 2 and Wave 3)
        - Requirements bullets are NOT what "key-decisions" names — naming inconsistency
      </cons>
    </option>
  </options>
  <resume-signal>
    Select: option-a or option-b (or provide a custom resolution if neither fits).

    **Default if no response within reasonable wait:** option-a (this is RESEARCH + PATTERNS + CONTEXT convergent recommendation; selecting option-a is the safe default and unblocks Wave 1).

    After approval, record the lock in `33-00-SUMMARY.md` so Wave 3's executor reads it directly:
    ```
    project-md-target: 3-column Key Decisions table at L158-183 (append one row)
    adr-commit-pattern: Accepted-first-commit (single commit for the ADR in Wave 2)
    ```
  </resume-signal>
  <files>(no file edits in this checkpoint — captures the operator's decisions into 33-00-SUMMARY.md via Task 4)</files>
  <action>This is a checkpoint:decision task. The executor presents the decision/options/resume-signal blocks above to the operator and BLOCKS until they respond. On response, the operator's selections (project-md-target and adr-commit-pattern) are recorded for Task 4 to write into 33-00-SUMMARY.md.</action>
  <verify>
    <automated>true # checkpoint:decision — verification is operator response captured in 33-00-SUMMARY.md by Task 4</automated>
  </verify>
  <done>Operator has responded with option-a or option-b (or a custom resolution); both project-md-target and adr-commit-pattern values are captured for Task 4.</done>
</task>

<task type="auto">
  <name>Task 4: Write Wave 0 SUMMARY with captured provenance + locked open questions</name>
  <files>.planning/phases/33-windows-parity-upstream-0-52-divergence/33-00-SUMMARY.md</files>
  <read_first>
    - .planning/phases/33-windows-parity-upstream-0-52-divergence/33-VALIDATION.md § Wave 0 Requirements (sign-off checklist)
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Create `33-00-SUMMARY.md` recording the Wave 0 outputs verbatim. This file is the canonical source of provenance + open-question resolutions that Waves 1 and 3 read from. Required content:

    ```markdown
    ---
    phase: 33-windows-parity-upstream-0-52-divergence
    plan: 00
    wave: 0
    status: complete
    completed: 2026-05-XX
    ---

    # Phase 33 Plan 00 — Wave 0 Prep SUMMARY

    ## Provenance captured (D-33-A2 inputs for Wave 1 ledger header)

    - **drift-tool-sh-sha:** `<40-char sha from Task 2 step 1>` (`scripts/check-upstream-drift.sh`)
    - **drift-tool-ps1-sha:** `<40-char sha from Task 2 step 1>` (`scripts/check-upstream-drift.ps1`)
    - **upstream-head-sha:** `<40-char sha from Task 2 step 2>` (`upstream/main` at audit time)
    - **audit-date:** `<YYYY-MM-DD from Task 2 step 3>` (UTC)
    - **drift-tool-invocation:** `make check-upstream-drift ARGS="--from v0.40.1 --to v0.52.0 --format json"` (D-33-A1 LOCKED verbatim)
    - **smoke-test total_unique_commits:** `<integer from Task 2 step 4>` (sets Wave 1 ledger sizing expectation; row count MUST be ≥ this number per REQ-1 acceptance)

    ## Wave 0 prerequisites verified (per 33-VALIDATION.md)

    - [x] `git fetch upstream --tags` — v0.43.1..v0.52.0 fetched locally
    - [x] `git remote -v | grep upstream` — points at `https://github.com/always-further/nono.git`
    - [x] `make check-upstream-drift --help` — succeeds
    - [x] Drift-tool dry-run with locked invocation — exits 0, well-formed JSON

    ## RESEARCH Open Questions resolved

    - **project-md-target:** `<option-a or option-b verdict from Task 3>` — Wave 3 Task X (REQ-3) writes the row at this target.
    - **adr-commit-pattern:** `<Accepted-first-commit or Proposed→Accepted verdict from Task 3>` — Wave 2 Task Y (REQ-2) commits the ADR using this pattern.

    ## Handoffs to downstream waves

    - **Wave 1 (Plan 33-01):** reads `drift-tool-sh-sha`, `upstream-head-sha`, `audit-date`, `drift-tool-invocation` into the DIVERGENCE-LEDGER.md header (D-33-A2). Reads `smoke-test total_unique_commits` to size the ledger walk.
    - **Wave 2 (Plan 33-02):** reads `adr-commit-pattern` to decide single-commit vs two-step ADR commit chain.
    - **Wave 3 (Plan 33-03):** reads `project-md-target` to lock REQ-3 row destination.
    ```

    Use the Write tool with the actual captured values from Tasks 1-3 (DO NOT leave `<placeholders>` in the committed file). The `audit-date` placeholder in the frontmatter `completed:` field MUST be filled with the actual date from Task 2.
  </action>
  <verify>
    <automated>test -f .planning/phases/33-windows-parity-upstream-0-52-divergence/33-00-SUMMARY.md &amp;&amp; grep -E '^- \*\*drift-tool-sh-sha:\*\* `[0-9a-f]{40}`' .planning/phases/33-windows-parity-upstream-0-52-divergence/33-00-SUMMARY.md &amp;&amp; grep -E '^- \*\*upstream-head-sha:\*\* `[0-9a-f]{40}`' .planning/phases/33-windows-parity-upstream-0-52-divergence/33-00-SUMMARY.md &amp;&amp; grep -E '^- \*\*project-md-target:\*\*' .planning/phases/33-windows-parity-upstream-0-52-divergence/33-00-SUMMARY.md &amp;&amp; grep -E '^- \*\*adr-commit-pattern:\*\*' .planning/phases/33-windows-parity-upstream-0-52-divergence/33-00-SUMMARY.md</automated>
  </verify>
  <done>
    - `33-00-SUMMARY.md` exists with all 6 provenance fields populated by real captured values (no `<placeholder>` strings)
    - Both Open Question resolutions recorded verbatim
    - Smoke-test commit count recorded for Wave 1 sizing
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `upstream/main` git refs → local working tree | Tags fetched from `always-further/nono`; remote-source data that the audit treats as the source of truth |
| Drift-tool JSON output → ledger header | The captured shas are what makes the audit reproducible; falsification would silently break reproducibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-00-01 | T (Tampering) | Drift-tool script sha capture | mitigate | Sha is captured via `git log -1 --format=%H scripts/check-upstream-drift.sh` — a deterministic git operation against the local repo; any tampering would also break `make ci` (the script is in tree). Recorded verbatim in `33-00-SUMMARY.md` so Wave 1 cannot silently change the value. |
| T-33-00-02 | I (Information Disclosure) | Drift-tool JSON output | accept | The smoke-test JSON goes to `/tmp/drift-smoke.json` (not committed per D-33-A2). Contains only public upstream commit data (subjects, shas, file paths) — no secrets. Acceptable per D-33-A2 "raw JSON regenerable from header." |
| T-33-00-03 | R (Repudiation) | Open Question resolution (project-md-target, adr-commit-pattern) | mitigate | Locked into `33-00-SUMMARY.md` with the maintainer's explicit selection from the checkpoint; downstream waves read from this file. No verbal/implicit handoff. |
</threat_model>

<verification>
- `33-00-SUMMARY.md` exists and contains both 40-char-hex shas, the audit-date, the invocation string, and both open-question resolutions
- `git tag --list 'v0.52.0'` returns `v0.52.0`
- Drift-tool dry-run JSON contains `total_unique_commits` and `range` keys
- No committed code/library files changed in this wave (`git diff --name-only HEAD~0 -- crates/` empty during Wave 0)
</verification>

<success_criteria>
- Upstream tags fetched + drift-tool dry-run succeeds
- Provenance shas captured for Wave 1 ledger header
- RESEARCH Open Questions 1 + 2 locked into Wave 0 SUMMARY
- Wave 1 can read all required inputs from `33-00-SUMMARY.md` without re-running any prep
</success_criteria>

<output>
After completion, the `.planning/phases/33-windows-parity-upstream-0-52-divergence/33-00-SUMMARY.md` file is the canonical Wave 0 artifact. Wave 1 (Plan 33-01) reads it for the ledger header and the smoke-test row count. Wave 2 (Plan 33-02) reads it for the ADR commit pattern. Wave 3 (Plan 33-03) reads it for the PROJECT.md target.
</output>
