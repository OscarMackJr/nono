---
phase: 93-live-zt-infra-integration-revocation-request-flow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/OMack/Nono/crates/nono-cli/src/exec_strategy/env_sanitization.rs
  - C:/Users/OMack/Nono/crates/nono-cli/src/cli.rs
  - C:/Users/OMack/Nono/crates/nono-cli/src/override_audit_emit.rs
  - C:/Users/OMack/Nono/crates/nono-cli/src/main.rs
autonomous: true
requirements: [ZTL-04, ZTL-02, ZTL-03]
must_haves:
  truths:
    - "The sandboxed child env contains no AWS_* variable; the strip is cfg-unconditional"
    - "nono.exe override audit-emit <meta> --kind rejected|revoked appends exactly one PolicyOverrideRejected (10008) or PolicyOverrideRevoked (10010) entry to the SecurityEventLayer HMAC chain and advances the chain hash"
    - "audit-emit carries no raw key material or secrets — only jti, kms_key_id, optional zt_audit_hash"
    - "The Windows SystemRoot/windir baseline re-add for CLR children is untouched by the AWS_* strip"
  artifacts:
    - path: "C:/Users/OMack/Nono/crates/nono-cli/src/exec_strategy/env_sanitization.rs"
      provides: "AWS_* prefix clause in is_dangerous_env_var"
      contains: "AWS_"
    - path: "C:/Users/OMack/Nono/crates/nono-cli/src/override_audit_emit.rs"
      provides: "override audit-emit subcommand runtime (live-reject HMAC emission, OQ-1 option a)"
      contains: "emit_override_event"
  key_links:
    - from: "C:/Users/OMack/Nono/crates/nono-cli/src/override_audit_emit.rs"
      to: "C:/Users/OMack/Nono/crates/nono-cli/src/telemetry/mod.rs"
      via: "SECURITY_LAYER.get().emit_override_event(...)"
      pattern: "emit_override_event"
    - from: "C:/Users/OMack/Nono/crates/nono-cli/src/exec_strategy/env_sanitization.rs"
      to: "child env"
      via: "is_dangerous_env_var drops AWS_* before spawn"
      pattern: "starts_with\\(\"AWS_\"\\)"
---

<objective>
Two nono-cli (Nono repo) additions that the Wave 2 Python live arm depends on: (1) the ZTL-04 `AWS_*` credential strip at the single env-sanitization chokepoint, and (2) a thin `nono override audit-emit` subcommand (OQ-1 option a) so the live-reject branch — which fails closed in nono-py BEFORE spawning nono.exe — can still land its REJECTED/REVOKED event in the authoritative nono-cli HMAC chain.

Purpose: Phase 92 wired the audit chain only at spawn-time via `--override-audit`. On a live deny/timeout nono-py never spawns, so EventID 10008/10010 had no emission path (RESEARCH Pitfall 3 / OQ-1). This subcommand provides the reject-path emission locus (the work is scoped under ZTL-02/ZTL-03 + the OQ-1 success criterion; it deliberately does NOT re-tag the Phase 92 `AUD-01` requirement, which is already Complete — see requirements note below); the AWS_* strip closes ZTL-04.

Output: modified `env_sanitization.rs`, new `override_audit_emit.rs` runtime, `cli.rs` subcommand wiring, `main.rs` dispatch.
</objective>

<requirements_note>
This plan's `requirements` field intentionally OMITS `AUD-01`. `AUD-01` reached Complete in Phase 92 (spawn-time `--override-audit` chain emission). The `override audit-emit` subcommand here EXTENDS that chain to the live-reject branch, but it is covered under this phase's `ZTL-02`/`ZTL-03` requirements plus the resolved OQ-1 success criterion. Tagging `AUD-01` would risk `phase.complete` re-opening or double-counting a prior-phase Complete requirement at closeout — so it is deliberately excluded.
</requirements_note>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-CONTEXT.md
@.planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-RESEARCH.md
@.planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-PATTERNS.md
@.planning/phases/92-runtime-capabilityset-mutation-audit-wiring/92-VERIFICATION.md

<interfaces>
<!-- Chokepoint to extend (env_sanitization.rs:16-77). Add ONE prefix clause next to LD_/DYLD_. -->
From crates/nono-cli/src/exec_strategy/env_sanitization.rs:16-19:
```rust
pub(crate) fn is_dangerous_env_var(key: &str) -> bool {
    key.starts_with("LD_")
        || key.starts_with("DYLD_")
```

<!-- Emission API (telemetry/mod.rs:321-365). #[must_use]; Err on poisoned mutex (AUD-04 fail-closed). -->
From crates/nono-cli/src/telemetry/mod.rs:319-321:
```rust
pub fn emit_override_event(&self, event_type: &SecurityEventType, jti: &str,
    kms_key_id: &str, zt_audit_hash: Option<&str>) -> Result<String, &'static str>
```

<!-- EventID constants (telemetry/event.rs:48-56). SecurityEventType variants at :81-90. -->
<!-- rejected -> PolicyOverrideRejected = 10008 ; revoked -> PolicyOverrideRevoked = 10010 -->

<!-- SECURITY_LAYER OnceLock (telemetry/mod.rs:48) set by init_tracing (cli_bootstrap.rs:127). -->
<!-- OverrideAuditMeta struct (cli.rs:1628-1648, serde deny_unknown_fields) carries jti/kms_key_id/zt_audit_hash. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strip AWS_* from the sandboxed child env (ZTL-04)</name>
  <files>C:/Users/OMack/Nono/crates/nono-cli/src/exec_strategy/env_sanitization.rs</files>
  <read_first>
    - crates/nono-cli/src/exec_strategy/env_sanitization.rs (lines 1-90: is_dangerous_env_var chokepoint, LD_/DYLD_ prefix precedent, Windows-gated PATH/SystemRoot block at :66-77, #[cfg(test)] mod)
    - .planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-PATTERNS.md (§ env_sanitization.rs MODIFY — correctness notes)
  </read_first>
  <behavior>
    - `is_dangerous_env_var("AWS_ACCESS_KEY_ID") == true`
    - `is_dangerous_env_var("AWS_SECRET_ACCESS_KEY") == true`
    - `is_dangerous_env_var("AWS_SESSION_TOKEN") == true`
    - `is_dangerous_env_var("AWS_REGION") == true`
    - `is_dangerous_env_var("SystemRoot") == true` only on Windows (existing behavior unchanged)
    - A non-AWS unrelated var (e.g. `HOME`) is still allowed (`false`)
  </behavior>
  <action>
    Add one clause `|| key.starts_with("AWS_")` immediately after the `key.starts_with("DYLD_")` line (lines 18-19) in `is_dangerous_env_var`. This is cfg-UNCONDITIONAL (AWS creds are dangerous on every platform) — do NOT wrap it in the Windows-gated `cfg!(target_os = "windows")` block at :66-77. `key.starts_with("AWS_")` is a string-prefix on an env-var NAME, not a path — this matches the existing `LD_`/`DYLD_` precedent and is NOT the CLAUDE.md path-`starts_with` footgun (note this in a one-line comment). Do NOT touch the Windows `SystemRoot`/`windir` baseline re-add (a separate code path for CLR children). Extend the existing `#[cfg(test)] mod tests` with the AWS_* cases from <behavior> plus an assertion that an unrelated var stays allowed.
  </action>
  <verify>
    <automated>cd C:/Users/OMack/Nono && cargo test -p nono-cli --lib exec_strategy::env_sanitization 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `env_sanitization.rs` contains `|| key.starts_with("AWS_")` outside any `cfg!(target_os = "windows")` guard
    - `cargo test -p nono-cli --lib exec_strategy::env_sanitization` exits 0 with AWS_* dangerous cases passing
  </acceptance_criteria>
  <done>AWS_* is stripped unconditionally; SystemRoot/windir baseline untouched; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: override audit-emit subcommand — live-reject HMAC emission (OQ-1 a)</name>
  <files>C:/Users/OMack/Nono/crates/nono-cli/src/override_audit_emit.rs, C:/Users/OMack/Nono/crates/nono-cli/src/cli.rs, C:/Users/OMack/Nono/crates/nono-cli/src/main.rs</files>
  <read_first>
    - crates/nono-cli/src/telemetry/mod.rs (lines 48-49 SECURITY_LAYER OnceLock; 287-365 emit_override_event, #[must_use], AUD-04 Err-on-poison)
    - crates/nono-cli/src/telemetry/event.rs (lines 48-110: EventID 10008/10010 constants, SecurityEventType PolicyOverrideRejected/PolicyOverrideRevoked variants, event_id_for exhaustive match)
    - crates/nono-cli/src/cli.rs (lines 644-1058 Commands enum + Profile help-template; 1628-1648 OverrideAuditMeta deny_unknown_fields; 2021-2032 --override-audit field)
    - crates/nono-cli/src/main.rs (command routing / dispatch)
    - .planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-PATTERNS.md (§ "kind -> EventID 1:1" + "HMAC chain integrity")
  </read_first>
  <behavior>
    - `nono override audit-emit <base64-meta> --kind rejected` emits a `PolicyOverrideRejected` event (EventID 10008) and advances the chain by exactly one
    - `nono override audit-emit <base64-meta> --kind revoked` emits a `PolicyOverrideRevoked` event (EventID 10010) and advances the chain by exactly one
    - The decoded meta supplies `jti`, `kms_key_id`, and optional `zt_audit_hash`; no raw key/secret bytes appear in the emitted event
    - A poisoned/uninitialized SECURITY_LAYER or an emit failure returns a non-zero exit (fail-closed; AUD-04 parity)
  </behavior>
  <action>
    This plan OWNS the `Override`/`OverrideCommands` clap skeleton in `cli.rs`: introduce `Override(OverrideArgs)` on the `Commands` enum and define the `OverrideCommands` subcommand enum with the `AuditEmit` variant. Plan 03 (Wave 2, strictly after this plan merges — see the file-ownership note) adds ONLY the `Request` variant onto this merged skeleton; it does not re-introduce the `Override`/`OverrideCommands` types. The subcommand takes a positional base64url `<meta>` (decoded into the existing `OverrideAuditMeta` struct, reusing its `serde(deny_unknown_fields)`) and a `--kind` arg accepting exactly `rejected` or `revoked`. Create `override_audit_emit.rs` with a runtime fn that: decodes the meta (base64url no-pad, matching the Phase 92 `--override-audit` decode at `launch_runtime.rs:420-436`), maps `--kind` to `SecurityEventType::PolicyOverrideRejected` (10008) or `SecurityEventType::PolicyOverrideRevoked` (10010), and calls `SECURITY_LAYER.get()...emit_override_event(&event_type, jti, kms_key_id, zt_audit_hash.as_deref())`. `emit_override_event` is `#[must_use]` — on `Err` (poisoned mutex) or `SECURITY_LAYER` unset, return a `NonoError` so the process exits non-zero (fail-closed, AUD-04 parity). Wire dispatch in `main.rs`. Carry NO raw key material. Add a `#[cfg(test)]` test asserting kind->EventID mapping (`rejected`->10008, `revoked`->10010) and a fresh-layer chain-advance-by-one (mirror the Phase 92 `emit_override_event_advances_chain_by_one` test).
  </action>
  <verify>
    <automated>cd C:/Users/OMack/Nono && cargo test -p nono-cli --lib override_audit_emit 2>&1 | tail -20 && cargo build -p nono-cli --bin nono 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `override_audit_emit.rs` exists and calls `emit_override_event` with the kind-mapped `SecurityEventType`
    - A unit test asserts `rejected`->10008 and `revoked`->10010 and chain-advance-by-one
    - `nono override audit-emit --help` lists the `--kind` arg accepting `rejected|revoked` (subcommand reachable)
    - `cargo build -p nono-cli --bin nono` exits 0
  </acceptance_criteria>
  <done>The reject-branch emission path exists, maps kinds 1:1 to EventIDs 10008/10010, advances the HMAC chain, and fails closed when the layer is unavailable.</done>
</task>

</tasks>

<file_ownership_note>
`cli.rs` is touched by both Plan 02 (this, Wave 1) and Plan 03 (override request, Wave 2). To eliminate the two-author `OverrideCommands`-enum merge hazard — the exact pattern that forced Phases 91/92 to run sequential/no-worktree under Windows worktree fragility — the following is a **HARD orchestration constraint, not advisory**:

- **Plan 02 OWNS the skeleton.** This plan is the sole author of `Override(OverrideArgs)` + the `OverrideCommands` enum (with the `AuditEmit` variant) in `cli.rs`.
- **Plan 03 MUST run strictly AFTER Plan 02 is merged.** Plan 03's `depends_on: ["93-02"]` already encodes this; the orchestrator MUST NOT run Plan 02 and Plan 03 in parallel worktrees on `cli.rs`. Plan 03 only ADDS the `Request` variant onto the already-merged skeleton.
- **Execution mode for this milestone: sequential / no-worktree** (established practice carried forward from Phases 91/92 — cross-repo split + Windows worktree fragility). Do not parallelize any two plans that both touch `cli.rs`.

Document the command-group asymmetry in code: `apply` is NOT a nono.exe variant (it lives in nono-py per D-07).
</file_ownership_note>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent env -> sandboxed child | AWS_* credentials in the parent must never cross into the child |
| nono-py reject branch -> nono-cli HMAC chain | the reject event must land in the tamper-evident chain without carrying secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-02-01 | Information Disclosure | AWS_* in child env | mitigate | `is_dangerous_env_var` drops all `AWS_*` at the single chokepoint feeding every exec path (ZTL-04); SC3 verified in Plan 04 child-env test |
| T-93-02-02 | Repudiation | live-reject not audited | mitigate | `override audit-emit` lands 10008/10010 in the HMAC chain (covered under ZTL-02/ZTL-03, OQ-1); chain advances by one |
| T-93-02-03 | Information Disclosure | secrets in audit event | mitigate | meta carries only jti/kms_key_id/optional zt_audit_hash; `deny_unknown_fields`; no raw key material |
| T-93-02-04 | EoP / Tampering | emit failure ignored | mitigate | `emit_override_event` is `#[must_use]`; `Err`/unset layer -> non-zero exit (fail-closed, AUD-04 parity) |
| T-93-02-05 | Spoofing | forged --kind value | mitigate | `--kind` is a closed enum `{rejected, revoked}`; clap rejects anything else |
</threat_model>

<verification>
- `cargo test -p nono-cli --lib exec_strategy::env_sanitization` green (AWS_* cases).
- `cargo test -p nono-cli --lib override_audit_emit` green (kind->EventID + chain advance).
- `cargo build -p nono-cli --bin nono` exits 0; `nono override audit-emit --help` reachable.
- Cross-target clippy per CLAUDE.md MUST/NEVER: the AWS_* clause is cfg-unconditional; run `--target x86_64-unknown-linux-gnu`/`--target x86_64-apple-darwin` if cross-toolchain present, else PARTIAL->CI per `.planning/templates/cross-target-verify-checklist.md` (watch for `unreachable_patterns` if any new match arm is added to telemetry — none expected, the SecurityEventType variants already exist from Phase 92).
</verification>

<success_criteria>
- AWS_* stripped unconditionally from the child env; SystemRoot/windir baseline untouched.
- `override audit-emit` lands 10008/10010 in the HMAC chain, advances by one, fails closed on emit error.
- Plan 02 is the sole author of the `OverrideCommands` skeleton; Plan 03 runs strictly after merge (sequential / no-worktree).
- No `.unwrap()`/`.expect()`; native build + tests green.
</success_criteria>

<output>
Create `.planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-02-SUMMARY.md` when done
</output>
</output>
