---
phase: 93-live-zt-infra-integration-revocation-request-flow
plan: 05
type: execute
wave: 3
depends_on: ["93-04"]
files_modified:
  - C:/Users/OMack/nono-py/python/nono_py/_cli_apply.py
  - C:/Users/OMack/nono-py/pyproject.toml
  - C:/Users/OMack/nono-py/tests/test_override_apply.py
autonomous: true
requirements: [CLI-02, VFY-01]
must_haves:
  truths:
    - "nono-override-apply <token-path> -- <command> runs the full fail-closed verification (Rust offline verify_override THEN _live.live_check) before any expansion, then runs the command confined in one shot"
    - "Any NonoOverrideError from offline or live verification propagates and blocks the run — no exec on verification failure"
    - "The apply entry is registered as a [project.scripts] console-script (nono-override-apply) — no nono.exe -> nono-py shell-back (D-07)"
  artifacts:
    - path: "C:/Users/OMack/nono-py/python/nono_py/_cli_apply.py"
      provides: "console-script: verify-then-run one-shot (D-08)"
      contains: "live_check"
    - path: "C:/Users/OMack/nono-py/pyproject.toml"
      provides: "[project.scripts] nono-override-apply entry"
      contains: "[project.scripts]"
  key_links:
    - from: "C:/Users/OMack/nono-py/python/nono_py/_cli_apply.py"
      to: "C:/Users/OMack/nono-py/python/nono_py/_live.py"
      via: "calls live_check after the Rust offline verify_override, before confined_run"
      pattern: "live_check"
    - from: "C:/Users/OMack/nono-py/pyproject.toml"
      to: "C:/Users/OMack/nono-py/python/nono_py/_cli_apply.py"
      via: "console-script entry points at _cli_apply:main"
      pattern: "_cli_apply:main"
---

<objective>
Ship the `nono override apply` developer affordance (CLI-02) as a nono-py console-script `nono-override-apply` (OQ-5, D-07/D-08): a one-shot `verify-then-run` that reads a signed token, runs the full fail-closed verification (Rust offline `verify_override` THEN the Plan 04 `_live.live_check`), and only on a complete pass executes the command confined via `confined_run`. No nono.exe -> nono-py shell-back (D-07).

Purpose: A developer who received a signed token needs to apply it. This is the CLI mirror of `confined_run` and the consumer-facing end of the two-key AND gate — verification before any expansion (CLI-02).

Output: `_cli_apply.py` (new console-script), `pyproject.toml` `[project.scripts]` table (new section), `test_override_apply.py` (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-CONTEXT.md
@.planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-RESEARCH.md
@.planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-PATTERNS.md

<interfaces>
<!-- Reused from Plan 04: nono_py._live.live_check(url, grant, *, timeout, extra_header) -->
<!-- Reused offline verify (Phase 91): nono_py.verify_override(...) -> OverrideGrant (raises NonoOverrideError) -->
<!-- Reused spawn (Phase 92): nono_py.confined_run(..., override_token=grant, ...) -->

<!-- pyproject.toml today has NO [project.scripts] section (verified). [tool.maturin] -->
<!-- (python-source="python", module-name="nono_py._nono_py") is unchanged. -->
<!-- After adding [project.scripts], `pip install -e .` (or maturin develop) must be re-run -->
<!-- for the console-script to appear in the venv Scripts/. -->

<!-- Flow (D-08): parse <token-path> -- <command> -> read token -> verify_override (offline) -->
<!--   -> _live.live_check(grant) (live, fail-closed) -> confined_run(..., override_token=grant). -->
<!-- Any NonoOverrideError propagates; no exec on failure. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _cli_apply.py — verify-then-run console-script (CLI-02 / D-08)</name>
  <files>C:/Users/OMack/nono-py/python/nono_py/_cli_apply.py, C:/Users/OMack/nono-py/tests/test_override_apply.py</files>
  <read_first>
    - C:/Users/OMack/nono-py/python/nono_py/__init__.py (exports: verify_override, confined_run, NonoOverrideError, the Plan 04 wrapper + live_check)
    - C:/Users/OMack/nono-py/python/nono_py/_live.py (live_check signature from Plan 04)
    - C:/Users/OMack/nono-py/src/windows_confined_run.rs (lines 367-420: confined_run shape this mirrors)
    - .planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-PATTERNS.md (§ _cli_apply.py NEW)
  </read_first>
  <behavior>
    - `main(["<token-path>", "--", "echo", "hi"])` reads the token, runs offline verify then live_check, and on full pass calls confined_run with the command
    - An offline verification failure (NonoOverrideError raised by verify_override) propagates and the command is never executed
    - A live failure (NonoOverrideError from live_check: LiveRevoked / LiveUnavailable) propagates and the command is never executed
    - Scope paths from the token are handled component-wise (never string starts_with) when passed through to confined_run
    - The argv split on `--` correctly separates the token-path/flags from the command
  </behavior>
  <action>
    Create `python/nono_py/_cli_apply.py` with a `main(argv=None)` entry. Parse `<token-path> [flags] -- <command...>` (split on the `--` separator). Read the token file, call the Rust `verify_override(...)` (offline; raises `NonoOverrideError` on any failure), then call `_live.live_check(url, grant)` (live; reads `NONO_ZT_ACTIONS_URL` + optional header from env per D-05 ops config), and only on a complete pass call `confined_run(..., override_token=grant, ...)` with the parsed command (the CLI mirror of `confined_run`, D-08). Any `NonoOverrideError` propagates and blocks the run — no `exec`/`confined_run` on verification failure. Use the existing `confined_run` (or the Plan 04 checked wrapper) so the live check is not duplicated. Never string-`starts_with` on scope paths (delegate to the existing component-wise handling). Print a redaction-safe summary; return a non-zero exit code on any verification failure. No `.unwrap()` equivalent silent-default in Python — surface errors. Write `tests/test_override_apply.py` mocking `_live.live_check` and the token read to cover: full-pass-runs-command (mock confined_run, assert called), offline-fail-blocks (verify_override raises -> confined_run NOT called), live-fail-blocks (live_check raises LiveRevoked -> confined_run NOT called), and argv `--` split correctness. Mark Windows-only where the compiled extension / confined_run is required; pure-logic argv-split and propagation tests can run anywhere with mocks.
  </action>
  <verify>
    <automated>cd C:/Users/OMack/nono-py && python -m pytest tests/test_override_apply.py -x -q 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `_cli_apply.py` defines `main` and calls `verify_override` then `live_check` before `confined_run`
    - `pytest tests/test_override_apply.py -x` exits 0; offline-fail and live-fail tests assert `confined_run` is NOT called
    - The `--` argv split test passes
  </acceptance_criteria>
  <done>The apply console-script runs full offline+live verification before any expansion and blocks the run on any failure, mirroring confined_run.</done>
</task>

<task type="auto">
  <name>Task 2: Register the [project.scripts] console-entry (OQ-5)</name>
  <files>C:/Users/OMack/nono-py/pyproject.toml</files>
  <read_first>
    - C:/Users/OMack/nono-py/pyproject.toml (full: [project] block, [tool.maturin] — confirm NO [project.scripts] exists)
    - .planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-PATTERNS.md (§ pyproject.toml MODIFY)
  </read_first>
  <action>
    Add a new `[project.scripts]` table to `pyproject.toml` with `nono-override-apply = "nono_py._cli_apply:main"` (OQ-5 console-script form — NO nono.exe -> nono-py shell-back per D-07). Leave `[tool.maturin]` (`python-source = "python"`, `module-name = "nono_py._nono_py"`) unchanged. Add a short comment near the table noting that `pip install -e .` (or `maturin develop`) must be re-run after adding `[project.scripts]` for the console-script to appear in the venv `Scripts/`. Verify the TOML parses and the entry resolves to `_cli_apply:main`.
  </action>
  <verify>
    <automated>cd C:/Users/OMack/nono-py && python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); print(d['project']['scripts']['nono-override-apply'])" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains a `[project.scripts]` table with `nono-override-apply = "nono_py._cli_apply:main"`
    - The TOML parses (the python `tomllib` check prints `nono_py._cli_apply:main`)
    - `[tool.maturin]` is unchanged
  </acceptance_criteria>
  <done>The console-script is registered and resolvable; maturin config untouched; re-install note present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| token file -> apply entry | an attacker-supplied token must pass the full two-key gate before any expansion |
| developer invocation -> confined child | the command runs confined only after offline+live verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-05-01 | EoP | run before verify | mitigate | `main` calls `verify_override` (offline) then `live_check` (live) BEFORE `confined_run`; any `NonoOverrideError` blocks exec (CLI-02 / D-08) |
| T-93-05-02 | EoP | live check skipped on apply | mitigate | The apply path reuses `_live.live_check` (Plan 04); no offline-only shortcut (REQUIREMENTS Out-of-Scope: cached verification defeats revocation) |
| T-93-05-03 | Tampering | scope path traversal | mitigate | Scope paths handled component-wise via existing `sanitize_override_path` in `confined_run`; never string `starts_with` |
| T-93-05-04 | Spoofing | nono.exe shell-back confusion | accept | No nono.exe -> nono-py shell-back (D-07); apply is a standalone console-script — documented asymmetry |
</threat_model>

<verification>
- `python -m pytest tests/test_override_apply.py -x` green (full-pass / offline-fail-blocks / live-fail-blocks / argv split).
- `pyproject.toml` `[project.scripts]` parses; entry resolves to `_cli_apply:main`.
- Full nono-py suite green at wave merge (Windows-marked tests skip off-Windows).
- The end-to-end live apply against a real provisioner is exercised by the OVERRIDE-02 gate (Plan 06) and is host-gated (SKIP_HOST_UNAVAILABLE).
</verification>

<success_criteria>
- `nono-override-apply` runs the full fail-closed verify (offline + live) before any expansion, then runs the command confined in one shot.
- Registered as a console-script; no nono.exe shell-back (D-07).
- Verification failures block exec; tests green.
</success_criteria>

<output>
Create `.planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-05-SUMMARY.md` when done
</output>
