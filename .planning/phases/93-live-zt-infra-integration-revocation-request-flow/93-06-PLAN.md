---
phase: 93-live-zt-infra-integration-revocation-request-flow
plan: 06
type: execute
wave: 3
depends_on: ["93-04", "93-05"]
files_modified:
  - C:/Users/OMack/Nono/scripts/gates/override-02.ps1
autonomous: true
requirements: [DF-02, ZTL-03, ZTL-01]
must_haves:
  truths:
    - "verify-dark.ps1 --gate override-02 emits SKIP_HOST_UNAVAILABLE (exit 3) when the local provisioner / NONO_ZT_ACTIONS_URL is absent — never FAIL"
    - "When the provisioner is present, the gate mints a token (Phase 91 keypair), seeds an allow rule override.apply*, verifies allow->grant, then seeds a deny rule override.apply:<jti> and verifies deny->NonoOverrideError(LiveRevoked) (ZTL-03 revocation proof)"
    - "The gate returns a verdict object; it never calls exit and never calls Persist-Verdict"
    - "DAAL anchoring is observed as async / non-blocking — the gate never sends flush_daal and the spawn path is not blocked on ledger finality (ZTL-05)"
  artifacts:
    - path: "C:/Users/OMack/Nono/scripts/gates/override-02.ps1"
      provides: "DF-02 live gate mirroring override-01 contract + local-provisioner precondition"
      contains: "Test-Precondition"
  key_links:
    - from: "C:/Users/OMack/Nono/scripts/gates/override-02.ps1"
      to: "C:/Users/OMack/Nono/scripts/verify-dark.ps1"
      via: "dot-sourced Test-Precondition / Invoke-Gate contract; verdict object only"
      pattern: "Invoke-Gate"
---

<objective>
Add the DF-02 Dark Factory gate `scripts/gates/override-02.ps1` (Nono repo): an unattended live gate that mirrors the Phase 92 `override-01.ps1` contract, adds a local-provisioner precondition probe, and proves the live two-key gate end-to-end — allow path and revocation path — against the local ZT-Infra provisioner. When the provisioner/AWS is absent it emits `SKIP_HOST_UNAVAILABLE` (exit 3), consistent with the Dark Factory mandate.

Purpose: This is the machine-readable verdict that gates Phase 93 — it exercises the live arm (Plan 04), the apply path (Plan 05), and the revocation enforcement point (ZTL-03) without a human, degrading to SKIP when the host lacks the live stack.

Output: `override-02.ps1` (new gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-CONTEXT.md
@.planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-RESEARCH.md
@.planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-PATTERNS.md

<interfaces>
<!-- Mirror EXACTLY: scripts/gates/override-01.ps1 (full file). -->
<!-- Contract: exports Test-Precondition + Invoke-Gate, dot-sourced by scripts/verify-dark.ps1. -->
<!--   Test-Precondition -> $null (run) | "reason" (SKIP_HOST_UNAVAILABLE exit 3) -->
<!--   Invoke-Gate -> [ordered]@{ gate; verdict; reason; detail; timestamp }; -->
<!--     verdict in { PASS | FAIL | SKIP_HOST_UNAVAILABLE } -->
<!--   NEVER call exit; NEVER call Persist-Verdict; a throw = harness-internal (exit 4) -->
<!-- Gate config vars (override-01.ps1:51-53): $script:GateName, $script:FixturesPath, $script:TestKmsArn -->
<!-- Phase 91 test keypair: C:\Users\OMack\nono-py\tests\fixtures\override_test_key.{pem,der} -->

<!-- Provisioner: server.js 127.0.0.1:3000; POST /actions 200 allow / 403 deny; /health probe. -->
<!-- policy.js evaluateAction keys on ACTION only; deny[] is the revocation enforcement point; -->
<!--   ACTION_POLICY_FILE (policy.js:24) is the seedable policy file (allow override.apply*, deny override.apply:<jti>). -->

<!-- Invocation rule (MEMORY durable): pwsh -File scripts\verify-dark.ps1 --gate override-02 -->
<!--   NEVER pwsh -Command "<bare path>" (swallows exit N -> 1). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: override-02.ps1 — precondition probe + allow/revoke live proof (DF-02 / ZTL-03)</name>
  <files>C:/Users/OMack/Nono/scripts/gates/override-02.ps1</files>
  <read_first>
    - scripts/gates/override-01.ps1 (full file: contract docblock :5-45, gate config :51-53, Assert-True, Test-Precondition :77-115, Invoke-Gate :117+, make_token / NONO_EXE stub pattern)
    - scripts/verify-dark.ps1 (harness: auto-discovery, exit-code mapping 0/2/3/4, Persist-Verdict ownership)
    - C:/Users/OMack/ZeroTrust2/ZERO_TRUST_V2/provisioner/src/server.js (lines 7-70: 127.0.0.1:3000, /actions, /health)
    - C:/Users/OMack/ZeroTrust2/ZERO_TRUST_V2/provisioner/src/policy.js (lines 14-59: evaluateAction action-only; deny[]; ACTION_POLICY_FILE at :24; defaultDecision deny)
    - .planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-PATTERNS.md (§ override-02.ps1 NEW — precondition delta + Invoke-Gate body)
    - .planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-RESEARCH.md (§ "OVERRIDE-02 gate precondition delta" + Pitfall 1 action-only matching + Pitfall 4 child-vs-gate env)
  </read_first>
  <action>
    Create `scripts/gates/override-02.ps1` mirroring `override-01.ps1` EXACTLY in contract: export exactly `Test-Precondition` and `Invoke-Gate`; set `$script:GateName = 'override-02'`, `$script:FixturesPath`, `$script:TestKmsArn` to the override-01 values; reuse the Phase 91 keypair at `C:\Users\OMack\nono-py\tests\fixtures\override_test_key.{pem,der}`. NEVER call `exit`; NEVER call `Persist-Verdict`; a `throw` is harness-internal (exit 4). In `Test-Precondition`, replicate the override-01 checks (Python, nono_py importable, override symbols, openssl, fixtures) THEN add: read `$env:NONO_ZT_ACTIONS_URL` — if unset return a SKIP reason naming the provisioner-start instructions; probe `($ztUrl -replace '/actions$','/health')` with `Invoke-WebRequest -TimeoutSec 2 -UseBasicParsing` — non-200 or unreachable returns a SKIP reason (SKIP_HOST_UNAVAILABLE). In `Invoke-Gate`: mint a token with the override-01 `make_token` pattern (Phase 91 keypair); point the provisioner `ACTION_POLICY_FILE` (policy.js:24) at a temp policy seeding an allow rule `override.apply*` (Pitfall 1 — revocation rides on `action` ONLY; do NOT build actor-based matching); run the apply/live path and assert allow -> `OverrideGrant`; then seed a deny rule `override.apply:<jti>` for the minted token's jti, re-run the live check, and assert deny -> `NonoOverrideError` with kind `LiveRevoked` (ZTL-03 revocation proof). Verify the request body the live arm sends carries NO `flush_daal` (ZTL-05). Strip `AWS_*` only from the CHILD env, never the gate/provisioner process (Pitfall 4). Return `[ordered]@{ gate; verdict; reason; detail; timestamp }` with `verdict` PASS on both sub-checks, FAIL if either behaves wrong, SKIP_HOST_UNAVAILABLE from the precondition. Invocation is `pwsh -File scripts\verify-dark.ps1 --gate override-02` — never `-Command "<bare path>"`.
  </action>
  <verify>
    <automated>cd C:/Users/OMack/Nono && pwsh -NoProfile -Command "& { . ./scripts/gates/override-02.ps1; if ((Get-Command Test-Precondition -ErrorAction SilentlyContinue) -and (Get-Command Invoke-Gate -ErrorAction SilentlyContinue)) { 'contract-ok' } else { 'contract-missing' } }" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/gates/override-02.ps1` exists; dot-sourcing it defines both `Test-Precondition` and `Invoke-Gate` (the verify command prints `contract-ok`)
    - The file contains NO bare `exit` and NO `Persist-Verdict` call in PowerShell code (comments excepted)
    - `Test-Precondition` returns a SKIP reason string when `NONO_ZT_ACTIONS_URL` is unset / provisioner unreachable
    - `Invoke-Gate` proves both the allow path and the deny->LiveRevoked revocation path (ZTL-03) and asserts the body omits `flush_daal`
    - Running `pwsh -File scripts/verify-dark.ps1 --gate override-02` on this host emits SKIP_HOST_UNAVAILABLE (exit 3), never FAIL, when the provisioner is absent
  </acceptance_criteria>
  <done>The override-02 gate mirrors the override-01 contract, adds the live-provisioner precondition, proves allow + revoke live paths, and SKIPs cleanly when the host lacks the provisioner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate -> local provisioner | the gate exercises the live decision endpoint; an absent endpoint must SKIP, not falsely PASS or FAIL |
| gate child env -> provisioner env | AWS_* must be stripped from the confined child only, never the provisioner's own process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-06-01 | Tampering | gate falsely PASSes without live stack | mitigate | `Test-Precondition` probes `/health`; absent -> SKIP_HOST_UNAVAILABLE (exit 3), never a vacuous PASS |
| T-93-06-02 | EoP | revocation not actually enforced | mitigate | `Invoke-Gate` seeds a `deny override.apply:<jti>` rule and asserts deny->`LiveRevoked` (ZTL-03 proof), keying on `action` only (Pitfall 1) |
| T-93-06-03 | Information Disclosure | AWS_* stripped from wrong process | mitigate | Strip only the confined child env, never the gate/provisioner (Pitfall 4); SC3 inspects the child env |
| T-93-06-04 | Denial of Service | gate blocks on DAAL anchoring | mitigate | Assert the live body omits `flush_daal` (ZTL-05); the spawn path is never awaited on ledger finality |
| T-93-06-05 | Repudiation | exit-code swallowing | mitigate | Gate returns a verdict object; never calls `exit`/`Persist-Verdict`; invoked via `-File` (MEMORY durable: never `-Command "<bare path>"`) |
</threat_model>

<verification>
- Dot-sourcing `override-02.ps1` defines `Test-Precondition` + `Invoke-Gate` (`contract-ok`).
- `pwsh -File scripts/verify-dark.ps1 --gate override-02` emits SKIP_HOST_UNAVAILABLE (exit 3) on this host (provisioner absent) — never FAIL.
- On a host with the provisioner running (`cd provisioner && npm install && npm start`, `NONO_ZT_ACTIONS_URL=http://127.0.0.1:3000/actions`) + Python/nono_py/openssl, the gate emits PASS proving allow + revoke (host-gated; manual per the Dark Factory mandate — see 93-VALIDATION Manual-Only).
</verification>

<success_criteria>
- DF-02 gate exists, mirrors the override-01 contract, never calls exit/Persist-Verdict.
- SKIP_HOST_UNAVAILABLE when the provisioner is absent; PASS proving allow + revocation when present.
- Honors action-only matching (Pitfall 1), child-only AWS_* strip (Pitfall 4), no flush_daal (ZTL-05).
</success_criteria>

<output>
Create `.planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-06-SUMMARY.md` when done
</output>
