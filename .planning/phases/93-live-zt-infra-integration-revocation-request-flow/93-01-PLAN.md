---
phase: 93-live-zt-infra-integration-revocation-request-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/OMack/nono-py/src/override_trust.rs
  - C:/Users/OMack/nono-py/src/override.rs
  - C:/Users/OMack/nono-py/src/lib.rs
  - C:/Users/OMack/nono-py/Cargo.toml
autonomous: true
requirements: [ZTL-01, VFY-01]
must_haves:
  truths:
    - "A #[cfg(windows)] HKLM reader sources the KMS pubkey DER + ARN allowlist from HKLM\\SOFTWARE\\Policies\\nono\\Override; absent trust material returns a fail-closed result, never a default-permit"
    - "OverrideErrorKind has LiveRevoked and LiveUnavailable variants exposed via as_str() with stable codes"
    - "verify_ecdsa_digest sources the pubkey from a per-key_id cache built from policy DER, not a test-injected param, on the production path"
    - "A non-Windows stub for the trust reader returns the same fail-closed Result shape (no unreachable_patterns / dead_code on Linux/macOS)"
  artifacts:
    - path: "C:/Users/OMack/nono-py/src/override_trust.rs"
      provides: "fail-secure HKLM trust-root reader (winreg) + non-Windows stub"
      contains: "windows_reader"
    - path: "C:/Users/OMack/nono-py/src/override.rs"
      provides: "LiveRevoked/LiveUnavailable error kinds + per-key_id VerificationKey cache"
      contains: "LiveUnavailable"
    - path: "C:/Users/OMack/nono-py/src/lib.rs"
      provides: "PyO3 registration of the trust-config reader function"
  key_links:
    - from: "C:/Users/OMack/nono-py/src/override.rs"
      to: "C:/Users/OMack/nono-py/src/override_trust.rs"
      via: "verify path sources pubkey DER from the HKLM reader keyed by key_id"
      pattern: "override_trust::"
    - from: "C:/Users/OMack/nono-py/src/override_trust.rs"
      to: "crates/nono/src/machine_policy.rs"
      via: "mirrors the fail-secure winreg taxonomy"
      pattern: "KEY_WOW64_64KEY"
---

<objective>
Build the production trust-root foundation for the live override path in the nono-py repo: a fail-secure `HKLM\SOFTWARE\Policies\nono\Override` reader (the policy-authoritative source per D-05/D-06), the two new live-arm error kinds (`LiveRevoked`/`LiveUnavailable`, D-02), and a per-`key_id` `VerificationKey` cache that closes the `[BLOCKING-93]` VFY-03a test-injected-pubkey seam.

Purpose: Phase 91/92 left `verify_ecdsa_digest` taking a test-injected `pubkey_der` and left the live-arm error kinds undefined. This plan moves trust roots to the policy spine and defines the error vocabulary the Wave 2 Python live arm raises — so the live arm has a fail-closed trust source and a 1:1 kind→EventID contract to map against.

Output: `override_trust.rs` (new), modified `override.rs` (kinds + cache), modified `lib.rs` (registration), and a `winreg` direct-dep promotion in `Cargo.toml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-CONTEXT.md
@.planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-RESEARCH.md
@.planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-PATTERNS.md
@.planning/templates/cross-target-verify-checklist.md

<interfaces>
<!-- Existing OverrideErrorKind enum (nono-py/src/override.rs:55-101) the executor extends. -->
<!-- Add two variants + their as_str() arms; Display delegates to as_str() (no change). -->

From nono-py/src/override.rs:56-101:
```rust
pub enum OverrideErrorKind {
    BadSignature, Expired, NotYetValid, OutOfScope, Replay,
    AlgorithmMismatch, KeyNotAllowlisted, Parse, MissingField,
}
impl OverrideErrorKind { pub fn as_str(&self) -> &'static str { /* 1:1 string codes */ } }
```

<!-- Fail-secure HKLM reader to MIRROR: crates/nono/src/machine_policy.rs:265-536 windows_reader mod. -->
<!-- Taxonomy: wrong-REG-type -> Err; absent sub-key (ERROR_FILE_NOT_FOUND=2) -> Ok(empty/None); -->
<!-- present-but-unreadable -> Err (NEVER fall through, machine_policy.rs:477-482); -->
<!-- always open KEY_READ | KEY_WOW64_64KEY. Non-Windows stub returns Ok(None)/fail-closed (:533-536). -->

<!-- winreg DEP STATUS (corrected): winreg is NOT yet a direct dep of nono-py. It is present only -->
<!-- TRANSITIVELY via the path-dep on core `nono` (winreg 0.56, target-gated, operator-approved at -->
<!-- crates/nono/Cargo.toml:65). nono-py/Cargo.toml [dependencies] has NO winreg line. Task 2 must -->
<!-- PROMOTE it to a direct nono-py dep (`winreg = "0.56"` under [dependencies]). No new external -->
<!-- package is introduced (same operator-approved 0.56 already in the workspace tree). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add LiveRevoked / LiveUnavailable error kinds (D-02)</name>
  <files>C:/Users/OMack/nono-py/src/override.rs</files>
  <read_first>
    - C:/Users/OMack/nono-py/src/override.rs (lines 50-130: OverrideErrorKind enum, as_str(), Display, override_err_to_py)
    - .planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-PATTERNS.md (§ "src/override.rs (MODIFY)" Analog A)
  </read_first>
  <behavior>
    - `OverrideErrorKind::LiveRevoked.as_str() == "LiveRevoked"`
    - `OverrideErrorKind::LiveUnavailable.as_str() == "LiveUnavailable"`
    - Display delegates to as_str() unchanged for both new variants (no separate Display arm needed)
    - `override_err_to_py(OverrideErrorKind::LiveRevoked, msg)` produces a `NonoOverrideError` whose args[0] == "LiveRevoked"
  </behavior>
  <action>
    Add two variants to the `OverrideErrorKind` enum: `LiveRevoked` (doc: "live POST /actions returned deny -> EventID 10010 REVOKED") and `LiveUnavailable` (doc: "timeout/unreachable/non-200/malformed live response -> EventID 10008 REJECTED"). Add the matching arms to `as_str()` returning the exact literals `"LiveRevoked"` and `"LiveUnavailable"`. Do NOT touch the `Display` impl (it delegates to `as_str()`). These stable codes are the 1:1 contract the nono-cli `override audit-emit` subcommand (Plan 02) and the Python `_live.py` (Plan 04) map to EventIDs 10010/10008 — do not invent message-string parsing. Add a `#[cfg(test)]` unit test asserting both `as_str()` literals and the `override_err_to_py` args[0] round-trip.
  </action>
  <verify>
    <automated>cd C:/Users/OMack/nono-py && cargo test -p nono-py --lib override:: 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `override.rs` source contains the token `LiveUnavailable` in both the enum and an `as_str()` arm returning `"LiveUnavailable"`
    - `cargo test -p nono-py --lib override::` exits 0 with the new kind test passing
  </acceptance_criteria>
  <done>Both new kinds compile, map to their exact string codes, and round-trip through override_err_to_py.</done>
</task>

<task type="auto">
  <name>Task 2: Fail-secure HKLM trust-root reader (override_trust.rs, D-05/D-06)</name>
  <files>C:/Users/OMack/nono-py/src/override_trust.rs, C:/Users/OMack/nono-py/src/lib.rs, C:/Users/OMack/nono-py/Cargo.toml</files>
  <read_first>
    - crates/nono/src/machine_policy.rs (lines 260-536: windows_reader mod, fail-secure taxonomy, KEY_WOW64_64KEY, read_list_subkey, non-Windows stub)
    - crates/nono/Cargo.toml (line 65: winreg 0.56 target-gated, operator-approved — the version to match)
    - C:/Users/OMack/nono-py/src/lib.rs (full: PyO3 module + class/function registration pattern)
    - C:/Users/OMack/nono-py/Cargo.toml ([dependencies] — confirm winreg is NOT yet a direct line; it is only transitive via core nono)
    - .planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-PATTERNS.md (§ "src/override_trust.rs (NEW)")
    - .planning/templates/cross-target-verify-checklist.md
  </read_first>
  <action>
    Create `src/override_trust.rs` with a `#[cfg(target_os = "windows")] mod windows_reader` mirroring `machine_policy.rs:265-536` EXACTLY in fail-secure taxonomy. Read sub-key `Override\AllowedKeyArns\` as an N×REG_SZ ADMX `<list>` (mirror `read_list_subkey`), and `Override\KmsPublicKeys\` as named values where value-name=`key_id` and data=DER+base64 REG_SZ. Open every key with `KEY_READ | KEY_WOW64_64KEY`. Taxonomy: wrong REG type -> `Err`; sub-key absent (`ERROR_FILE_NOT_FOUND` = 2) -> `Ok(Vec::new())`/`Ok(None)`; present-but-unreadable -> `Err` (NEVER fall through — mirror `machine_policy.rs:477-482`). Expose a function `read_override_trust()` returning a struct `{ allowed_arns: Vec<String>, kms_pubkeys: HashMap<String, Vec<u8>> }` (DER bytes per key_id, base64-decoded). Provide a `#[cfg(not(target_os = "windows"))]` stub returning the same fail-closed `Result` shape (mirror `machine_policy.rs:533-536` — `Ok` with empty trust = fail-closed deny downstream, NOT a permit). Add `mod override_trust;` and the PyO3 registration of a `#[pyfunction]` wrapper (e.g. `read_override_trust_py`) to `lib.rs` following the existing `m.add_function`/`m.add_class` pattern. DEP PROMOTION: `winreg` is currently transitive via the core `nono` path-dep (operator-approved 0.56, `crates/nono/Cargo.toml:65`); promote it to a DIRECT nono-py dep in this task by adding `winreg = "0.56"` (target-gated to Windows, mirroring core) under `[dependencies]` (or `[target.'cfg(windows)'.dependencies]`) in `nono-py/Cargo.toml`. No new external package is introduced — it is the same already-vetted 0.56 in the workspace tree; the `## Package Legitimacy Audit` gate is NOT triggered. Do NOT hand-roll raw `windows-sys` `RegGetValueW` (D-06 "own windows-sys call" is non-binding on the crate choice per RESEARCH Alternatives). No `.unwrap()`/`.expect()`.
  </action>
  <verify>
    <automated>cd C:/Users/OMack/nono-py && cargo build -p nono-py 2>&1 | tail -20 && cargo clippy -p nono-py -- -D warnings -D clippy::unwrap_used 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `override_trust.rs` exists with a `windows_reader` mod and a `#[cfg(not(target_os = "windows"))]` stub
    - `read_override_trust` returns DER bytes keyed by key_id and the ARN allowlist; absent `Override\` key yields empty trust (fail-closed), not a default-permit
    - `cargo build -p nono-py` exits 0; native `cargo clippy -p nono-py -- -D warnings -D clippy::unwrap_used` exits 0
    - `lib.rs` registers the reader `#[pyfunction]`; `Cargo.toml` now lists `winreg = "0.56"` as a DIRECT dep (promoted from transitive)
  </acceptance_criteria>
  <done>The HKLM reader compiles on Windows, has a fail-closed non-Windows stub, registers via PyO3, promotes winreg to a direct dep, and passes native clippy with no unwrap.</done>
</task>

<task type="auto">
  <name>Task 3: Per-key_id VerificationKey cache — close [BLOCKING-93] VFY-03a (D-06)</name>
  <files>C:/Users/OMack/nono-py/src/override.rs</files>
  <read_first>
    - C:/Users/OMack/nono-py/src/override.rs (lines 600-720, 760-868: verify_ecdsa_digest at :621 with the [BLOCKING-93] doc at :609-616; verify_override_impl at :762)
    - C:/Users/OMack/nono-py/src/override_trust.rs (the reader from Task 2)
    - .planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-PATTERNS.md (§ "Pattern 2: Per-key_id VerificationKey cache")
  </read_first>
  <action>
    Add a process-wide cache `static VK_CACHE: LazyLock<Mutex<HashMap<String, Vec<u8>>>>` keyed on `key_id` -> DER bytes (cache the DER bytes; `VerificationKey` is not cheap to clone). On the production verify path, source the pubkey DER from `override_trust::read_override_trust()` keyed by the token's `key_id`: cache-miss -> read policy -> if the `key_id` is absent from `kms_pubkeys` return `Err(OverrideErrorKind::KeyNotAllowlisted)` (fail-closed, D-05); also verify the signer ARN is in `allowed_arns` (preserve the existing VFY-04 allowlist check). Keep `verify_ecdsa_digest(pubkey_der: &[u8], ...)` callable with an explicit DER param so existing tests still inject test keys, but add the policy-sourced lookup in `verify_override_impl` (or a thin wrapper) so the public verify entry no longer requires a test-injected key on the production path. Update the `[BLOCKING-93]` doc-comment at :609-616 to state the seam is closed (policy-sourced, per-key_id cached). Env vars MUST NOT supply or widen pubkey/ARN trust roots (D-05). No `.unwrap()`/`.expect()`; keep `#[must_use]` on the verify Result.
  </action>
  <verify>
    <automated>cd C:/Users/OMack/nono-py && cargo test -p nono-py --lib override:: 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `override.rs` contains `VK_CACHE` (a `LazyLock<Mutex<HashMap<String, Vec<u8>>>>`)
    - A token whose `key_id` is absent from policy yields `Err(OverrideErrorKind::KeyNotAllowlisted)` (asserted by a unit test), never `Ok`
    - The `[BLOCKING-93]` doc-comment at the former seam now records closure (policy-sourced)
    - Existing Phase 91 override tests still pass (`cargo test -p nono-py --lib override::` exits 0)
  </acceptance_criteria>
  <done>The pubkey is policy-sourced and per-key_id cached; an unknown key_id fails closed; Phase 91 tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| environment -> trust roots | Tampered env must NOT be able to install a rogue KMS pubkey or widen the ARN allowlist (D-05) |
| HKLM policy -> nono-py reader | Registry read may be absent, wrong-typed, or unreadable; each maps to fail-closed deny, never a permit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01-01 | Tampering / EoP | env-supplied pubkey/ARN | mitigate | Trust roots ONLY from HKLM `Override\` (D-05); env supplies no pubkey/ARN; unknown key_id -> `Err(KeyNotAllowlisted)` |
| T-93-01-02 | EoP | unreadable HKLM key | mitigate | Mirror `machine_policy.rs:477-482` — present-but-unreadable -> `Err`, NEVER fall through to permit |
| T-93-01-03 | Spoofing | rogue signer ARN | mitigate | Preserve VFY-04 ARN allowlist check against policy `allowed_arns` (cryptographic validity alone never authorizes) |
| T-93-01-04 | Tampering | cross-target drift on cfg-gated reader | mitigate | Non-Windows stub returns fail-closed shape; cross-target clippy PARTIAL->CI per checklist if cross-toolchain absent |
| T-93-01-SC | Tampering | winreg promotion in Cargo.toml | accept | `winreg` is transitive via core `nono` (operator-approved 0.56, `crates/nono/Cargo.toml:65`); Task 2 promotes it to a DIRECT nono-py dep at the same 0.56 — no new external package, no install of an unvetted crate, so `## Package Legitimacy Audit` is not triggered |
</threat_model>

<verification>
- `cargo build -p nono-py` exits 0; native `cargo clippy -p nono-py -- -D warnings -D clippy::unwrap_used` exits 0.
- `cargo test -p nono-py --lib override::` green (new kind test + unknown-key_id fail-closed test + Phase 91 regressions).
- Cross-target clippy (`--target x86_64-unknown-linux-gnu` and `--target x86_64-apple-darwin`) per CLAUDE.md MUST/NEVER: run if the cross-toolchain is present; otherwise mark PARTIAL->CI per `.planning/templates/cross-target-verify-checklist.md` (the new `override_trust.rs` is cfg-gated — watch for `unreachable_patterns`/`dead_code` on the non-Windows stub).
</verification>

<success_criteria>
- LiveRevoked/LiveUnavailable kinds exist with stable string codes.
- HKLM trust reader is fail-secure and policy-authoritative; missing trust = deny.
- VFY-03a `[BLOCKING-93]` pubkey seam closed: pubkey policy-sourced + per-key_id cached.
- `winreg` promoted from transitive to a direct nono-py dep (0.56, operator-approved); no new external package.
- No `.unwrap()`/`.expect()`; native clippy clean; cross-target PARTIAL->CI noted if applicable.
</success_criteria>

<output>
Create `.planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-01-SUMMARY.md` when done
</output>
</output>
