---
phase: 93-live-zt-infra-integration-revocation-request-flow
plan: 04
type: execute
wave: 2
depends_on: ["93-01", "93-02"]
files_modified:
  - C:/Users/OMack/nono-py/python/nono_py/_live.py
  - C:/Users/OMack/nono-py/python/nono_py/__init__.py
  - C:/Users/OMack/nono-py/src/windows_confined_run.rs
  - C:/Users/OMack/nono-py/tests/test_live_arm.py
autonomous: true
requirements: [ZTL-01, ZTL-02, ZTL-03, ZTL-05, VFY-01]
must_haves:
  truths:
    - "live_check builds a POST /actions body of {actor=signer, action=override.apply:<jti>, resource=repo_context, correlation_id=jti} from the already-verified OverrideGrant — never re-parsing the token (TOCTOU closure)"
    - "A live deny (403 / decision!=allow) raises NonoOverrideError kind LiveRevoked; a timeout >2s / unreachable / non-200 / malformed raises kind LiveUnavailable — both block the run (fail-closed)"
    - "The request never sets flush_daal (ZTL-05) — authorization returns on the /actions decision without waiting on ledger finality"
    - "The urllib opener disables env-proxy (empty ProxyHandler) and does not follow cross-host redirects (endpoint is the trust anchor)"
    - "A Python wrapper runs live_check BEFORE the Rust confined_run/confine; on live allow it proceeds, on any failure it never spawns"
  artifacts:
    - path: "C:/Users/OMack/nono-py/python/nono_py/_live.py"
      provides: "live POST /actions arm — fail-closed decision mapping (D-01/D-02)"
      contains: "live_check"
    - path: "C:/Users/OMack/nono-py/tests/test_live_arm.py"
      provides: "mocked-urllib tests: allow/deny/timeout/malformed/no-flush_daal/body-shape"
      exports: ["test_timeout_fails_closed", "test_no_flush_daal", "test_post_body_shape"]
  key_links:
    - from: "C:/Users/OMack/nono-py/python/nono_py/_live.py"
      to: "ZT-Infra POST /actions"
      via: "urllib.request.urlopen(req, timeout=2.0) with empty ProxyHandler"
      pattern: "urlopen"
    - from: "C:/Users/OMack/nono-py/python/nono_py/__init__.py"
      to: "C:/Users/OMack/nono-py/python/nono_py/_live.py"
      via: "Python wrapper calls live_check before the Rust confined_run"
      pattern: "live_check"
---

<objective>
Build the live `POST /actions` AND-gate as a pure-Python module (`_live.py`, D-01) and wire it as a Python pre-step before the Rust `confined_run`/`confine` (OQ-2). This closes VFY-01 clause (b): an override is now accepted only when the KMS signature verifies offline AND a live ZT-Infra lookup returns `allow`. Revoked tokens are rejected on the next live check (ZTL-03, no new nono-side infra); DAAL anchoring stays async (ZTL-05); fail-closed is total (ZTL-02).

Purpose: This is the keystone of v3.2 — the second key of the two-key AND gate. Phase 92 left the seam at `windows_confined_run.rs:404-406`; this plan fills it from the Python side per D-01/OQ-2.

Output: `_live.py` (new), `__init__.py` wrapper + exports (modified), `windows_confined_run.rs` seam comment update (modified), `test_live_arm.py` (new, mocked urllib).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-CONTEXT.md
@.planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-RESEARCH.md
@.planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-PATTERNS.md

<interfaces>
<!-- OverrideGrant getters (override.rs:660-716, #[pyclass(frozen)]) the live arm consumes. -->
<!-- #[pyo3(get)]: signer, not_before, expires_at, jti, repo_context -->
<!-- #[getter] (clone): scope_paths(), scope_domains(), zt_audit_hash() -->

<!-- Provisioner POST /actions contract (ZeroTrust2/.../provisioner/src/server.js:25-70): -->
<!-- request {actor, action, resource, correlation_id?, cross_org?, flush_daal?} -->
<!-- response 200 -> {ok, decision:"allow", reason, audit:{...current_hash...}, daal_flush} -->
<!--          403 -> decision:"deny" -->
<!-- evaluateAction (policy.js:31-59) keys on ACTION ONLY; actor required-non-empty but never matched; -->
<!--   resource/correlation_id audit-only. defaultDecision:"deny". -->

<!-- Existing platform gate to extend (__init__.py:69-73, 113-117): -->
```python
if _sys.platform == "win32":
    from nono_py._nono_py import confined_run, confine
else:
    from nono_py._nono_py import sandboxed_exec
```

<!-- Error kinds added by Plan 01: OverrideErrorKind::LiveRevoked / LiveUnavailable -->
<!-- NonoOverrideError is raised with args[0]=kind string, args[1]=redaction-safe message. -->

<!-- Audit-emit reach-back added by Plan 02: nono.exe override audit-emit <base64-meta> --kind rejected|revoked -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _live.py — fail-closed POST /actions decision mapping (ZTL-01/02/03/05)</name>
  <files>C:/Users/OMack/nono-py/python/nono_py/_live.py, C:/Users/OMack/nono-py/tests/test_live_arm.py</files>
  <read_first>
    - C:/Users/OMack/nono-py/python/nono_py/__init__.py (existing exports, NonoOverrideError re-export)
    - C:/Users/OMack/nono-py/src/override.rs (lines 640-720: OverrideGrant getters signer/jti/repo_context, scope_paths()/scope_domains()/zt_audit_hash())
    - C:/Users/OMack/ZeroTrust2/ZERO_TRUST_V2/provisioner/src/server.js (lines 7-70: POST /actions contract, 200 allow / 403 deny)
    - C:/Users/OMack/ZeroTrust2/ZERO_TRUST_V2/provisioner/src/policy.js (lines 14-59: evaluateAction keys on action only; deny[] enforcement)
    - .planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-RESEARCH.md (§ "Pattern 1" full code + outcome->kind->EventID table + Pitfall 5)
    - .planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-PATTERNS.md (§ _live.py — verbatim mapping table + body shape)
  </read_first>
  <behavior>
    - `live_check(url, grant)` POST body == `{"actor": grant.signer, "action": "override.apply:<jti>", "resource": grant.repo_context or "", "correlation_id": grant.jti}` — and contains NO `flush_daal` key (ZTL-05)
    - HTTP 200 + `decision == "allow"` -> returns the response `audit.current_hash` (AUD-02 fresh hash); run proceeds
    - HTTP 403 (or 200 with `decision != "allow"`) -> raises NonoOverrideError with kind `LiveRevoked` (-> EventID 10010)
    - `urllib.error.URLError` / `TimeoutError` / `socket.timeout` / `json.JSONDecodeError` / non-200-non-403 HTTPError -> raises NonoOverrideError with kind `LiveUnavailable` (-> EventID 10008)
    - The opener is built with an empty `ProxyHandler({})` (env-proxy disabled) and does not follow cross-host redirects
    - The token object is never re-parsed: only `OverrideGrant` getters are read (TOCTOU closure)
  </behavior>
  <action>
    Create `python/nono_py/_live.py` implementing `live_check(actions_url, grant, *, timeout=2.0, extra_header=None)` per RESEARCH Pattern-1. Build the request body from the already-verified `OverrideGrant` getters ONLY — `grant.signer`, `f"override.apply:{grant.jti}"`, `grant.repo_context or ""`, `grant.jti` — and NEVER include `flush_daal` (ZTL-05). Build a urllib opener with an empty `ProxyHandler({})` to disable env-proxy and a redirect handler that does NOT follow cross-host redirects (Pitfall 5 — the `NONO_ZT_ACTIONS_URL` is the D-04 trust anchor). Call `urllib.request.urlopen(req, timeout=timeout)` (default 2.0s, ZTL-02 bounded). Map outcomes exactly per the verbatim table: 200+allow -> return `payload["audit"]["current_hash"]`; 403 or 200-not-allow -> raise `NonoOverrideError("LiveRevoked", ...)`; timeout/unreachable/non-200/malformed -> raise `NonoOverrideError("LiveUnavailable", ...)`. Raise via the same `NonoOverrideError` re-exported in `__init__.py`, passing the kind string as args[0] (matching the Plan 01 Rust kinds). Apply the optional `extra_header` passthrough (D-04, `NONO_ZT_ACTIONS_HEADER`, empty by default). The endpoint URL and timeout come from env (ops config, D-05) — never trust roots. No raw secrets in raised messages. Write `tests/test_live_arm.py` mocking `urllib.request.urlopen` (and the opener) to cover: `test_post_body_shape` (body keys + no flush_daal), `test_allow_returns_hash`, `test_deny_raises_live_revoked`, `test_timeout_fails_closed` (raise TimeoutError -> LiveUnavailable), `test_non_200_fails_closed`, `test_malformed_body_fails_closed`, `test_no_flush_daal`. Mark Windows-only only where the compiled extension is required; the pure-Python `live_check` mapping tests should run on any platform with a stub grant object.
  </action>
  <verify>
    <automated>cd C:/Users/OMack/nono-py && python -m pytest tests/test_live_arm.py -x -q 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `_live.py` contains `live_check` and `urlopen(req, timeout=` with an empty `ProxyHandler({})`
    - `pytest tests/test_live_arm.py -x` exits 0; `test_timeout_fails_closed`, `test_deny_raises_live_revoked`, `test_no_flush_daal`, `test_post_body_shape` all pass
    - The request body asserted in `test_post_body_shape` has exactly keys actor/action/resource/correlation_id and NO `flush_daal`
  </acceptance_criteria>
  <done>The live arm maps every outcome to allow-or-deny, fails closed on all failure modes, never sends flush_daal, never re-parses the token, and disables env-proxy.</done>
</task>

<task type="auto">
  <name>Task 2: Python pre-step wrapper + seam-comment update (VFY-01 clause b, OQ-2)</name>
  <files>C:/Users/OMack/nono-py/python/nono_py/__init__.py, C:/Users/OMack/nono-py/src/windows_confined_run.rs</files>
  <read_first>
    - C:/Users/OMack/nono-py/python/nono_py/__init__.py (lines 29-117: import block, platform gate, __all__, win32 __all__ extension)
    - C:/Users/OMack/nono-py/src/windows_confined_run.rs (lines 390-420 confined_run seam at :404-406; ~:505-515 confine seam)
    - C:/Users/OMack/nono-py/python/nono_py/_live.py (the live_check from Task 1)
    - .planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-PATTERNS.md (§ __init__.py MODIFY + windows_confined_run.rs MODIFY)
  </read_first>
  <behavior>
    - A Python wrapper (e.g. `confined_run_checked`) runs `_live.live_check(grant)` BEFORE calling the Rust `confined_run(..., override_token=grant)`
    - When `override_token` is None, the wrapper calls through with no live check (byte-identical to the no-override path, MUT-05 parity)
    - On a live failure the wrapper raises NonoOverrideError and the Rust spawn is never reached
    - On a live allow it proceeds to the Rust spawn; the fresh `audit.current_hash` is preferred for the audit metadata (AUD-02 refinement)
  </behavior>
  <action>
    Add a Python pre-step wrapper in `__init__.py` (per OQ-2 — a Rust `#[pyfunction]` cannot cleanly call `urllib` mid-body, so the live check is a Python-level pre-step). The wrapper reads `NONO_ZT_ACTIONS_URL` (+ optional `NONO_ZT_ACTIONS_HEADER`, timeout) from env, calls `_live.live_check(url, grant)` to obtain the fresh `current_hash`, then invokes the Rust `confined_run(..., override_token=grant)`. When `override_token` is None, skip the live check entirely (MUT-05 parity). Export the wrapper and `_live.live_check` in the `win32` import branch and add them to the `win32` `__all__` extension, following the existing alphabetical ordering. Do the equivalent for `confine` (Shape B). Update the seam comments in `windows_confined_run.rs` at :404-406 and ~:513-515 to state: "live check performed by the Python caller (OQ-2); this fn assumes the grant is live-verified (VFY-01 clause b closed Python-side)." Do NOT add HTTP to Rust. If the live-arm prefers the fresh response hash over the token's `zt_audit_hash` for the emitted audit metadata, thread it through the Python wrapper (AUD-02 lean: live hash on the live path, token hash offline).
  </action>
  <verify>
    <automated>cd C:/Users/OMack/nono-py && python -m pytest tests/test_live_arm.py -x -q 2>&1 | tail -10 && cd C:/Users/OMack/nono-py && cargo build -p nono-py 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `__init__.py` exports a live-checked wrapper and `live_check`; both appear in the `win32` `__all__`
    - The wrapper calls `live_check` before the Rust `confined_run` and skips it when `override_token is None`
    - `windows_confined_run.rs` seam comments at :404-406 / ~:513-515 record VFY-01 clause b closed Python-side (no `[BLOCKING-93]` remaining as an open seam)
    - `cargo build -p nono-py` exits 0
  </acceptance_criteria>
  <done>The live check runs as a Python pre-step before the Rust spawn, fails closed before spawn, preserves the no-override byte-identical path, and the Rust seam comments are updated to record closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nono-py -> ZT-Infra POST /actions | new outbound network boundary; the endpoint URL is the trust anchor (D-04) |
| verified grant -> live arm | the live arm must consume the immutable grant, never re-parse the token (TOCTOU) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-04-01 | EoP | fail-open on live-check failure | mitigate | Every urllib exception/timeout/non-200/malformed -> `LiveUnavailable` deny; 403/not-allow -> `LiveRevoked` deny; both block the run (ZTL-02) |
| T-93-04-02 | EoP | TOCTOU verify->live | mitigate | `live_check` reads only `OverrideGrant` getters (frozen pyclass); the token is never re-parsed between offline and live |
| T-93-04-03 | Tampering / Spoofing | endpoint re-pointing via proxy/redirect | mitigate | Empty `ProxyHandler({})`; no cross-host redirects (Pitfall 5); URL is the trust anchor |
| T-93-04-04 | Spoofing / EoP | replayed/revoked token | mitigate | live `deny[]` is the durable cross-process revocation point (ZTL-03); per-token via `action=override.apply:<jti>` |
| T-93-04-05 | Denial of Service | DAAL anchoring blocks spawn | mitigate | Body never sets `flush_daal` (ZTL-05); anchoring is the provisioner's async concern, never awaited |
| T-93-04-06 | Information Disclosure | secrets in raised message | mitigate | Raised `NonoOverrideError` messages are redaction-safe (no signature/key bytes) |
</threat_model>

<verification>
- `python -m pytest tests/test_live_arm.py -x` green (allow/deny/timeout/non-200/malformed/no-flush_daal/body-shape).
- `cargo build -p nono-py` exits 0 (seam comment update compiles).
- Full nono-py suite `python -m pytest tests -q` green at wave merge (Windows-marked tests skip off-Windows by design — per 92-VERIFICATION human-verification pattern).
- The live provisioner integration is exercised by the OVERRIDE-02 gate (Plan 06) — SKIP_HOST_UNAVAILABLE when absent.
</verification>

<success_criteria>
- VFY-01 clause b closed: offline + live AND gate operational Python-side.
- Fail-closed on deny/timeout/unreachable/non-200/malformed; never sends flush_daal; never re-parses the token; env-proxy disabled.
- No-override path unchanged (MUT-05 parity); Rust seam comments record closure.
</success_criteria>

<output>
Create `.planning/phases/93-live-zt-infra-integration-revocation-request-flow/93-04-SUMMARY.md` when done
</output>
