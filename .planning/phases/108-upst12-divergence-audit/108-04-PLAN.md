---
phase: 108-upst12-divergence-audit
plan: 04
type: execute
wave: 3
depends_on: ["108-03"]
files_modified:
  - .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md
autonomous: true
requirements: [UPST12-01]

must_haves:
  truths:
    - "All 20 tool-sandbox-surface commits are classified pure (DEFERRED to v3.7) or split (per-path residue accounted), with zero silent path loss."
    - "All 7 named tool-sandbox refinement PRs are explicitly recorded DEFERRED->v3.7 with the reason (base subsystem absent)."
    - "The DEPS (15), CI (11), and DOCS (6) clusters are individually reviewed, not blanket-adopted or blanket-skipped."
  artifacts:
    - path: ".planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md"
      provides: "tool-sandbox-pure/tool-sandbox-split residue tables + DEPS/CI/DOCS cluster tables"
      contains: "DEFERRED"
  key_links:
    - from: ".planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md"
      to: "v3.7 Windows Tool-Sandbox Parity milestone"
      via: "DEFERRED->v3.7 disposition on the 20-commit tool-sandbox surface"
      pattern: "DEFERRED.{0,10}v3\\.7"
---

<objective>
Append the full tool-sandbox-surface (20 commits) split/defer treatment and the DEPS/CI/DOCS
cluster tables to `108-DIVERGENCE-LEDGER.md`. This is the highest-risk classification call in the
phase (CONTEXT.md D-05): a blanket "defer anything touching tool-sandbox" rule would silently
drop four commits that must absorb elsewhere (port ranges = PROF-03, `@git:*` tokens = PROF-02, a
musl build fix, and an 18-file seccomp refactor touching `bindings/c`).

Purpose: Per-commit, per-path residue accounting (D-07) is the concrete mitigation for silent
loss — a path appearing in zero buckets is a ledger defect, not a passable gap. The 7 named
refinement PRs must be recorded DEFERRED->v3.7 so that milestone starts from real data instead
of the PR-level approximation (D-09).
Output: tool-sandbox-pure, tool-sandbox-split, DEPS, CI, and DOCS sections appended to
`108-DIVERGENCE-LEDGER.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/108-upst12-divergence-audit/108-CONTEXT.md
@.planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md
@.planning/milestones/v3.4-phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md

<interfaces>
<!-- The complete 20-commit tool-sandbox surface (verified live 2026-07-29 via
     `git log --no-merges --oneline <range> -- 'crates/nono-cli/src/tool-sandbox/'
     'crates/nono-cli/src/command_policy.rs' 'crates/nono-cli/src/lineage_cgroup.rs'`).
     Module set per D-06: tool-sandbox/, command_policy.rs, lineage_cgroup.rs — all three
     confirmed ABSENT from the fork (verify again with `ls crates/nono-cli/src/tool-sandbox
     crates/nono-cli/src/command_policy.rs crates/nono-cli/src/lineage_cgroup.rs`). -->

  a519ee62e6d571330caa6a9a7d033fb10bff5939  #1417  fix(tool-sandbox): attribute daemonized callers to their command (cgroup on linux, verified daemon-pid on macos)
  cafc39eb3104e789cee2b663dc04b8aa7ef0062a  #1391  fix(sandbox): allow exec in writable grant-dirs under command policies
  d5803b994b416ad07a73907143ca169c408917f3  #1398  feat: add port range support to sandbox profiles      [D-05 NAMED — entangled]
  ab93cf44e5706b49b1aa5f8ecc1b3b59f29596f8  #1413  fix(tool-sandbox): preserve argv[0] for symlink-dispatched commands
  bf7ea3cb7faa241de13fe549152e28333b1f0068  #1394  fix(tool-sandbox): grant env-shebang scripts their re-exec interpreter
  72a988309974f426f0da8f507a4f05df9738ce92  #1384  feat(tool-sandbox): add per-command exec_paths for multi-call binaries
  1f54f4ae91870de02c1fabfda554278d49c99bd9  #1373  fix(command-policy): resolve command_policies binaries once, in parallel, with caching
  7c20dc75f4f155d64601719437980a0e232c8bd8  #1339  fix(tool-sandbox): resolve command policy paths against the live cwd
  42161620ec68da9c4b09b2283e6584ecbf51d3a2  #1344  fix(cli): match intercept args after global options
  eb2d61a7abc5a354b8f8085b782cd11afa6babb8  #1322  feat(tool-sandbox): add exec intercept action
  5a7447d3ed30835bd9bd647b7812ee18ea80a155  (none) fix(tool-sandbox): strip untrusted unsafe_macos_seatbelt_rules before emission
  ebd51cbb9546aec872136301249d048074a05e38  (none) fix(tool-sandbox): warn on unsafe_macos_seatbelt_rules nested in command/intercept sandboxes
  e2c87fd5e82b1dd7c1cf9a70d9632f2b0310afb4  (none) feat(tool-sandbox): per-command unsafe_macos_seatbelt_rules escape hatch
  052b8374d2381ca4efdc6a51d9eee54d292a54da  (none) feat(tool-sandbox): per-intercept sandbox override
  ea334d2bbdcb332c3a1c4164843667b0d2153cb1  #1332  fix(linux): use u64 for fs_type_unsupported to fix musl build     [D-05 NAMED — entangled]
  d4927f95a37863cf0ba534b054e28f48f002ad21  #1298  feat(profile): expand @git:* dynamic tokens in top-level filesystem paths  [D-05 NAMED — entangled]
  676a042f3c1e3877f1d1770abd19b0fea0345750  #1325  fix(tool-sandbox): ack frame before SCM_RIGHTS send to prevent EMSGSIZE on macOS
  199bb26699661e8a36de141adc4767617e0a1036  #1280  feat(tool-sandbox): add git worktree tokens; fold include-files into @git:config-files
  de8a54b25c3c26f68f6d51f768be904cee0d0c41  #1313  fix(dynamic-providers): run git config from repo root to honour hasconfig: includeIf
  8a4237f2ee0dc33bc1e5afdd39c0db8ca6f5ed38  #1283  refactor(seccomp): introduce SeccompPolicy struct and client-driven selection     [D-05 NAMED — entangled]

<!-- The 7 named tool-sandbox refinement PRs (REQUIREMENTS.md / ROADMAP.md v3.6 Out-of-Scope
     table) map to these 7 of the 20 SHAs above: -->
  #1280 -> 199bb266   #1322 -> eb2d61a7   #1325 -> 676a042f   #1384 -> 72a98830
  #1394 -> bf7ea3cb   #1413 -> ab93cf44   #1417 -> a519ee62

<!-- Reconnaissance pure/split classification (Planner's own `git show --name-only` pass,
     2026-07-29) — treat as a STARTING HYPOTHESIS to independently re-derive and reconcile
     against CONTEXT.md's stated "8 pure, 12 entangled" figure. These two numbers already
     disagree by one commit (9 pure / 11 entangled found here) — resolving that exact
     discrepancy, by naming the disputed SHA and the reasoning, is part of this task's job,
     not an error to silently paper over:
       Planner-found PURE (9): cafc39eb, ab93cf44, bf7ea3cb, 42161620, e2c87fd5, 052b8374,
                                676a042f, 199bb266, de8a54b2
       Planner-found ENTANGLED (11): a519ee62, d5803b99, 72a98830, 1f54f4ae, 7c20dc75,
                                eb2d61a7, 5a7447d3, ebd51cbb, ea334d2b, d4927f95, 8a4237f2
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tool-sandbox-pure and tool-sandbox-split — full 20-commit residue accounting (D-05/D-06/D-07/D-09)</name>
  <files>.planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</files>
  <read_first>
    .planning/phases/108-upst12-divergence-audit/108-CONTEXT.md (D-05 through D-09 in full — the
    per-commit residue table is the load-bearing mechanism of this task)
    .planning/milestones/v3.4-phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md Cluster B
    section (won't-sync tool-sandbox precedent — the format to extend with D-07 residue tables,
    which Cluster B did not need since v3.4's window had zero entangled tool-sandbox commits)
  </read_first>
  <action>
    For each of the 20 SHAs listed in context, run `git show --name-only --format='' <sha>` to
    get the definitive touched-path list. Classify each commit as `pure` (every touched path is
    inside the D-06 module set — `crates/nono-cli/src/tool-sandbox/`,
    `crates/nono-cli/src/command_policy.rs`, `crates/nono-cli/src/lineage_cgroup.rs` — OR is a
    non-production path: `docs/`, `crates/nono-cli/data/`, `crates/nono-cli/tests/`) or `split`
    (touches the module set AND at least one other production source path — anything under
    `crates/*/src/` or `bindings/c/src/` outside the module set). Independently reconcile against
    the context section's "Planner-found 9 pure / 11 entangled" note and CONTEXT.md's stated "8
    pure, 12 entangled" hypothesis — name explicitly, by SHA, which commit(s) cause the
    discrepancy and which classification (pure or split) you land on for it, with the specific
    touched-path evidence. Write a `## tool-sandbox-pure` table (sha | subject | PR# | disposition
    "DEFERRED->v3.7 (base subsystem absent — D-06)") for every pure commit, explicitly marking
    each of the 7 named refinement PRs by number. Write a `## tool-sandbox-split` section with one
    D-07 residue table PER split commit: every path from that commit's `--name-only` output listed
    with a marker of exactly `absorb` (non-tool-sandbox production path — cite which Phase
    109/110/111 cluster it belongs to, e.g. `d5803b99`'s `crates/nono/src/sandbox/linux.rs` and
    `sandbox/macos.rs` hunks absorb via CORE-01/PROF-03), `defer` (tool-sandbox-module path,
    DEFERRED->v3.7), or `noise` (docs/data/test path). Explicitly work through the 4 D-05-named
    commits (`d5803b99`, `d4927f95`, `ea334d2b`, `8a4237f2`) as the worked examples, citing their
    absorb targets by name (PROF-03, PROF-02, "musl build fix — portable, no requirement ID",
    seccomp `SeccompPolicy` refactor touching `bindings/c/src/sandbox.rs`).
  </action>
  <acceptance_criteria>
    - tool-sandbox-pure table row count + tool-sandbox-split table count (counting each split
      commit once) sum to exactly 20.
    - All 7 named refinement PR numbers (#1280, #1322, #1325, #1384, #1394, #1413, #1417) appear
      verbatim in the tool-sandbox-pure table with disposition "DEFERRED->v3.7".
    - For every split commit, the sum of its residue table's row count equals the exact output
      count of `git show --name-only --format='' <sha> | wc -l` for that SHA (i.e., zero paths
      missing from all three buckets, zero paths double-counted) — this is spot-checked for at
      least the 4 D-05-named commits explicitly.
    - A dedicated paragraph or note names the pure/entangled-count reconciliation (planner's 9/11
      vs. CONTEXT's 8/12) and states the final resolved count with the disputed SHA identified by
      hash.
    - The 4 D-05-named commits (d5803b99, d4927f95, ea334d2b, 8a4237f2) each have at least one
      `absorb`-marked row in their residue table (none are 100% defer/noise).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "DEFERRED->v3.7" .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</automated>
  </verify>
  <done>All 20 tool-sandbox-surface commits classified pure/split with the 7 named refinement PRs marked DEFERRED->v3.7 and every split commit's residue table accounting for 100% of its touched paths (absorb/defer/noise), including the 4 D-05-named worked examples.</done>
</task>

<task type="auto">
  <name>Task 2: DEPS, CI, and DOCS clusters — individual review (D-15/D-16/D-17)</name>
  <files>.planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</files>
  <read_first>
    .planning/phases/108-upst12-divergence-audit/108-CONTEXT.md (D-15, D-16, D-17)
    .planning/milestones/v3.4-phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md Cluster F
    section (the sigstore-trust-root dependency-bump precedent for D-16's cargo-audit
    cross-reference) and its "Excluded as Noise" section (format for CI/docs entries)
    .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md (own DEPS/CI/DOCS
    bucket SHA lists from Plan 108-01 Task 1)
  </read_first>
  <action>
    Using the DEPS/CI/DOCS bucket SHA lists already recorded in Plan 108-01's Full Commit
    Accounting section, write three tables. `## DEPS Cluster` (D-16 — its own cluster, not noise):
    sha | subject | Cargo.toml/Cargo.lock delta summary (one line, e.g. "bumps X 0.a.b -> 0.c.d")
    | cargo-audit-flag. Attempt `cargo audit` against the fork's current `Cargo.lock` (or
    equivalent RUSTSEC cross-reference if `cargo-audit` is not installed — record the tool's
    absence explicitly, do not silently skip the check) and cross-reference any advisory against
    the DEPS commits' bumped crates. `## CI Cluster` (D-17 — adapt-by-default, never adopt
    verbatim): sha | subject | adapt-reasoning — for each of the 11 CI commits, state explicitly
    whether it is portable (like the musl build fix precedent, though note `ea334d2b` is itself a
    CODE-bucket tool-sandbox-split commit already handled in Task 1, not a CI-bucket commit — do
    not double-count it here) or fork-specific-conflicting (citing the ADR-100 precedent: upstream
    CI assuming a release-please bot the fork does not use). `## DOCS Cluster`: sha | subject |
    reconcile-note (safe-to-ignore / needs-doc-follow-up). Finally write a `## Bucket-Count
    Reconciliation` note: DEPS table rows, CI table rows, DOCS table rows, each compared against
    Plan 108-01's recorded bucket counts (15/11/6) — if the counts disagree after this task's more
    careful per-commit review, state the corrected numbers and which SHA(s) moved buckets and why
    (this is expected to happen occasionally per D-15's own finding that prefix-based and
    path-set-based classification disagree).
  </action>
  <acceptance_criteria>
    - DEPS table row count, CI table row count, and DOCS table row count are each stated
      explicitly and cross-checked against Plan 108-01's bucket counts in the
      Bucket-Count Reconciliation note (equal or explicitly reconciled with named SHAs).
    - Every CI cluster row's adapt-reasoning cell states one of "portable" or "fork-specific
      conflict" — never left as a bare subject-line restatement.
    - `ea334d2b` does NOT appear in the CI cluster table (it is a tool-sandbox-split CODE commit
      from Task 1, verified by its own touched-path list including `crates/nono/src/sandbox/
      linux.rs`).
    - The cargo-audit (or equivalent) invocation result is recorded verbatim, including an
      explicit statement if the tool was unavailable in this environment.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^## DEPS Cluster\|^## CI Cluster\|^## DOCS Cluster" .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</automated>
  </verify>
  <done>DEPS/CI/DOCS clusters each individually reviewed (not blanket adopted/skipped), cargo-audit cross-reference recorded, and bucket counts reconciled against Plan 108-01's totals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool-sandbox deferral boundary → v3.7 work-list | A blanket "defer anything touching tool-sandbox" classification (the exact anti-pattern D-05 forbids) would silently drop PROF-02/PROF-03 scope and a portable Linux musl fix from this milestone — the highest-severity classification risk in the phase. |
| DEPS cluster → supply-chain integrity | A dependency bump skipped as "noise" (D-16 explicitly rejects this) could hide an unaddressed RUSTSEC advisory the fork's Cargo.lock currently carries unpatched. |
| CI cluster → release pipeline integrity | Verbatim-adopting a CI commit that assumes upstream's bot-driven release process (the ADR-100 precedent) would silently add a permanently-false `if:` condition to the fork's release.yml. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-08 | Tampering | tool-sandbox-split residue accounting | mitigate | D-07 per-path bucket accounting with a countable row-count-equals-touched-path-count check (Task 1 acceptance criteria) makes silent loss structurally detectable. |
| T-108-09 | Repudiation | DEPS cluster cargo-audit cross-reference | mitigate | Verbatim recording of the cargo-audit invocation result (or explicit tool-absence note) — never silently skipped. |
| T-108-10 | Tampering | CI cluster verbatim-adoption risk | mitigate | Every CI commit gets an individual adapt-reasoning cell citing portable-vs-fork-specific-conflict, per D-17; no blanket adopt or blanket skip permitted. |
</threat_model>

<verification>
Confirm the tool-sandbox-pure + tool-sandbox-split commit counts sum to 20 and all 7 named
refinement PRs are present with DEFERRED->v3.7. Confirm DEPS/CI/DOCS table row counts reconcile
against Plan 108-01's bucket counts.
</verification>

<success_criteria>
All 20 tool-sandbox-surface commits are classified with zero silent path loss (D-07 residue
accounting complete); the 7 named refinement PRs are recorded DEFERRED->v3.7; DEPS/CI/DOCS
clusters are individually reviewed per D-15/D-16/D-17.
</success_criteria>

<output>
Create `.planning/phases/108-upst12-divergence-audit/108-04-SUMMARY.md` when done
</output>
