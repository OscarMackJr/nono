---
phase: 108-upst12-divergence-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md
autonomous: true
requirements: [UPST12-01]

must_haves:
  truths:
    - "A reviewer can independently re-run the reproduction commands and get the exact same commit counts this ledger records."
    - "Every one of the 100 non-merge commits in the v0.66.0..v0.69.0 window is accounted for exactly once (no double-count, no silent drop)."
    - "The noise/substantive split is derived from actual touched-path-sets, not merely trusted from the conventional-commit prefix."
  artifacts:
    - path: ".planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md"
      provides: "Reproduction block (pinned tip SHAs) + full 100-commit CODE/DEPS/CI/DOCS bucket accounting + Cluster Summary taxonomy skeleton"
      contains: "total_unique_commits: 100"
  key_links:
    - from: ".planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md"
      to: "upstream nolabs-ai/nono v0.66.0..v0.69.0 window"
      via: "pinned SHA reproduction block"
      pattern: "d817ed53663c6bba4669ee7a5bfb41b35971fd1b\\.\\.59bdace7e905c05c127f480dc6d2a8c3a3331392"
---

<objective>
Create `.planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md` and populate its
Reproduction block plus a full 100-commit noise/substantive accounting and a Cluster Summary
taxonomy skeleton. This is the foundation every later plan in this phase appends to — it fixes
the pinned SHAs, the total commit count, and the bucket/cluster scheme so nothing downstream
re-derives numbers that disagree with each other.

Purpose: Phases 109/110/111 need a reproducible, non-drifting starting point. Per CONTEXT.md
D-01..D-04, the 260727-jkn map's "~40 substantive commits" estimate is already known wrong
(measured: 100 non-merge commits, 68 CODE / 15 DEPS / 11 CI / 6 DOCS, 20 touching the
tool-sandbox surface) — this plan re-verifies that measurement live rather than copying it
forward.
Output: `108-DIVERGENCE-LEDGER.md` with a populated Reproduction block and Cluster Summary
table (dispositions still placeholders — filled in by Plans 108-03/108-04/108-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/108-upst12-divergence-audit/108-CONTEXT.md
@.planning/milestones/v3.4-phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

<interfaces>
<!-- Pre-resolved tag SHAs (verified live 2026-07-29 via `git ls-remote --tags upstream`).
     Re-verify at execution time — do not assume upstream hasn't moved. -->
v0.66.0 = d817ed53663c6bba4669ee7a5bfb41b35971fd1b  (fork's confirmed high-water mark, per D-01)
v0.67.0 = 19b43f300b9d2e257e161b808440f0573abfd422
v0.67.1 = 6118b79aeda1365da213d85457b4d3cf1201d575
v0.68.0 = 00692e8c7846c6ee00ad6239d1be3b9e9b8d5dea
v0.69.0 = 59bdace7e905c05c127f480dc6d2a8c3a3331392  (window tip)

Window range for all git commands in this plan and downstream plans:
d817ed53663c6bba4669ee7a5bfb41b35971fd1b..59bdace7e905c05c127f480dc6d2a8c3a3331392

Verified live 2026-07-29:
- `git log --no-merges --oneline <range> | wc -l` = 100
- `git log --merges --oneline <range> | wc -l` = 4 (note: unlike v3.4's UPST11 window, which had
  ZERO merge commits, this window has 4 — the drift-tool / manual accounting must explicitly
  exclude these 4 merge SHAs, not just assume there are none)
- `git log --no-merges --oneline <range> -- 'crates/nono-cli/src/tool-sandbox/' 'crates/nono-cli/src/command_policy.rs' 'crates/nono-cli/src/lineage_cgroup.rs' | wc -l` = 20
- `upstream` remote already points at `nolabs-ai/nono`; `upstream-legacy` retains `always-further/nono`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Reproduction block and full CODE/DEPS/CI/DOCS accounting</name>
  <files>.planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</files>
  <read_first>
    .planning/phases/108-upst12-divergence-audit/108-CONTEXT.md (D-01 through D-04, D-14, D-15 — the noise-classification methodology)
    .planning/milestones/v3.4-phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md (Reproduction block format — the frontmatter fields `range`, `upstream_head_at_audit`, `refetch_date`, `total_unique_commits`, and the fenced `Reproduction` section with SHA-guard `git cat-file -t` checks)
  </read_first>
  <action>
    Create the ledger file with YAML frontmatter mirroring 98-DIVERGENCE-LEDGER.md's field set
    (phase: 108-upst12-divergence-audit, plan: 01, ledger_type: upst12-audit, range using the
    pinned window SHAs above, upstream_head_at_audit: 59bdace7e905c05c127f480dc6d2a8c3a3331392,
    total_unique_commits: 100). Run `git fetch upstream --tags` and `git ls-remote --tags
    upstream v0.66.0 v0.67.0 v0.67.1 v0.68.0 v0.69.0`; record the live output verbatim in a
    `## Reproduction` section, confirming it matches the pinned SHAs given in context (flag
    explicitly, by name, if upstream has moved past v0.69.0 since 2026-07-29 — that changes the
    audit window and must be escalated, not silently absorbed). Add `git cat-file -t` guard
    checks for both window-tip SHAs. Run `git log --no-merges --oneline <range> | wc -l` and
    `git log --merges --oneline <range> | wc -l`, record both verbatim. Then classify all 100
    non-merge commits into exactly one of four buckets by touched-path-set (D-15): CODE (touches
    any path under `crates/*/src/` or `bindings/c/src/`), DEPS (touches ONLY `Cargo.toml` and/or
    `Cargo.lock` files, no src/), CI (touches ONLY `.github/`, `Makefile`, `scripts/`, no src/),
    DOCS (touches ONLY `docs/`, `CHANGELOG*`, `*.md`, `*.mdx`, no src/). Use `git show --name-only
    --format='' <sha>` per commit to get the touched-path list, then bucket. Also run the D-15
    conventional-commit-prefix cross-check: count commits whose subject starts with `chore:`,
    `ci:`, `docs:`, or `build:` and separately count commits classified non-CODE (DEPS+CI+DOCS);
    list every SHA where the prefix-based guess and the path-set-based bucket disagree (CONTEXT.md
    reports this hypothesis as 38 prefixed vs 32 non-CODE — re-measure, do not copy forward
    per D-04). Write four bucket tables (sha | subject | touched-path-summary) into the ledger
    under a `## Full Commit Accounting (100 non-merge commits)` section.
  </action>
  <acceptance_criteria>
    - The Reproduction section contains all 5 pinned tag SHAs verbatim, each matching the live
      `git ls-remote --tags upstream` output captured in this task.
    - `git log --no-merges --oneline <range> | wc -l` output is recorded verbatim in the
      Reproduction section and equals 100 (or, if upstream moved, the new number is recorded with
      an explicit escalation note — never silently substituted).
    - The four bucket tables' row counts sum to exactly the non-merge count recorded above; every
      one of those SHAs appears in exactly one bucket table (no SHA in zero or 2+ tables — spot
      checkable via `git log --no-merges --format='%H' <range> | sort > /tmp/all.txt` and a
      diff against the concatenated bucket SHA lists, sorted and deduped).
    - The prefix-vs-path-set disagreement section lists every disagreeing SHA by hash and states
      the final disagreement count as a number (not "several" or "some").
  </acceptance_criteria>
  <verify>
    <automated>grep -c "d817ed53663c6bba4669ee7a5bfb41b35971fd1b" .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md && grep -c "^## Full Commit Accounting" .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</automated>
  </verify>
  <done>Reproduction section pins all 5 tag SHAs with live-reconfirmed commit counts; Full Commit Accounting section buckets all 100 non-merge commits into CODE/DEPS/CI/DOCS with the prefix-cross-check disagreement list recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Define the Cluster Summary taxonomy skeleton (D-14)</name>
  <files>.planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</files>
  <read_first>
    .planning/phases/108-upst12-divergence-audit/108-CONTEXT.md (D-05, D-06, D-09, D-14, D-18, D-20 — cluster-first-per-commit-where-it-matters, tool-sandbox module set, security-relevant flag)
    .planning/milestones/v3.4-phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md (Cluster Summary table columns to mirror: cluster_id | theme | commits | disposition | windows-touch | rationale | phase-status)
  </read_first>
  <action>
    Append a `## Cluster Summary` section to the ledger. Define exactly these named clusters and
    assign every CODE-bucket SHA (from Task 1) into exactly one: NET (proxy/network — anchor
    commits already identified: 3b207eeb #1374 deny_domain, c831dade #1272 SPIFFE, 6fb7ecbf #1430
    SigV4, 23d93fc9 #1437 sibling-route), PROF (profile/policy — anchor commits: ae1c513e #1371
    platform_overrides, 719975cf #1380 extends, 2cbaa9a0 #1296 $VAR, b620ed8e #1305 bun, f016b2d5
    #1387 mise), CORE (macOS/resource-CLI — anchor commits: ca888108 #1378 ~/.cache, 099237da
    #1424 MAX_CRYPTO_THREADS, e6d26871 #1269 resource-limiting, 34c2c975 #1403 --max-processes),
    tool-sandbox-surface (the 20 SHAs already enumerated in context: a519ee62, cafc39eb, d5803b99,
    ab93cf44, bf7ea3cb, 72a98830, 1f54f4ae, 7c20dc75, 42161620, eb2d61a7, 5a7447d3, ebd51cbb,
    e2c87fd5, 052b8374, ea334d2b, d4927f95, 676a042f, 199bb266, de8a54b2, 8a4237f2 — do NOT
    subdivide pure/split yet, that is Plan 108-04's job), security-residual-and-misc (a holding
    cluster for every remaining CODE-bucket SHA not yet assigned — this MUST include, at minimum,
    the following named security-relevant commits: 0ecc476b #1195 AWS MiTM auth, 9b692e07 declarative
    OAuth capture, 3c59c62e OAuth capture-boundary hardening, d033c63111472711e242f4067eb4be04aeaf618a OAuth fixture test, a3243907
    #1284 NVIDIA procfs hardening, f943fb5a #1333 trust-policy predicate field, d84b4818 #1397 Linux
    Refer grant, ac5ccd70 #1401 seccomp supervisor-ancestry, 2663e990 #1261 standalone `nono proxy`
    command, a5a441c25769ecec663aed1d7038ab2b8814d43f #1204 allow_vars fix — plus any other CODE-bucket SHA not captured by NET/
    PROF/CORE/tool-sandbox-surface). Note two named commits (d5803b99 #1398 port-range, d4927f95
    #1298 @git:*) belong to BOTH a PROF theme and the tool-sandbox-surface set — assign them to
    tool-sandbox-surface in this summary table (their split/residue accounting happens there in
    Plan 108-04) and add a one-line cross-reference note in the PROF cluster's rationale cell.
    Produce the Cluster Summary table with columns: cluster_id | theme | commit_count |
    disposition (write "TBD — Plan 108-03/04/05" as placeholder) | windows-touch (write "TBD") |
    security-relevant (write "TBD") | phase-target (write intended routing: NET→109, PROF→110,
    CORE→111, tool-sandbox-surface→v3.7 DEFERRED (per-commit split in Plan 108-04), security-
    residual-and-misc→proposed Phase 112 pending operator approval).
  </action>
  <acceptance_criteria>
    - The Cluster Summary table has exactly 5 cluster rows (NET, PROF, CORE, tool-sandbox-surface,
      security-residual-and-misc).
    - The sum of the `commit_count` column across all 5 clusters equals the CODE-bucket row count
      established in Task 1 (a stated arithmetic check, e.g. "NET n1 + PROF n2 + CORE n3 +
      tool-sandbox-surface 20 + security-residual-and-misc n5 = <CODE total>").
    - All 10 named security-relevant anchor SHAs (0ecc476b, 9b692e07, 3c59c62e, d033c63111472711e242f4067eb4be04aeaf618a,
      a3243907, f943fb5a, d84b4818, ac5ccd70, 2663e990, a5a441c25769ecec663aed1d7038ab2b8814d43f) appear verbatim in the
      security-residual-and-misc cluster's commit list.
    - d5803b99 and d4927f95 appear in the tool-sandbox-surface cluster's commit list, not in NET
      or PROF, with the cross-reference note present in the PROF cluster's rationale cell.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^| NET \|tool-sandbox-surface\|security-residual-and-misc" .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</automated>
  </verify>
  <done>Cluster Summary table defines exactly 5 clusters whose commit_count sums to the CODE-bucket total, with the 10 named security-relevant SHAs filed under security-residual-and-misc and d5803b99/d4927f95 filed under tool-sandbox-surface with a PROF cross-reference note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live upstream fetch → pinned ledger record | The ledger's pinned SHAs are the sole reproducibility anchor for every downstream plan and Phase 109/110/111; a mismatch here silently invalidates all later per-commit work. |
| Path-set classification → noise/substantive split | A CODE commit misclassified as DEPS/CI/DOCS is silently dropped from all cluster analysis in this phase — the exact "cluster isolation can be empirically false" failure mode this phase exists to prevent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-01 | Tampering | Reproduction block SHA pins | mitigate | Live `git ls-remote --tags` cross-check against the given SHAs is a mandatory task step, with explicit escalation language if upstream has moved. |
| T-108-02 | Repudiation | 100-commit bucket accounting | mitigate | Every SHA must land in exactly one of 4 buckets, verified via a stated sum-equality check in acceptance criteria — not merely "looks complete". |
| T-108-03 | Information Disclosure | N/A | accept | This is an internal planning artifact; no secrets or credentials are read or written by this task. |
</threat_model>

<verification>
Re-run `git log --no-merges --oneline d817ed53663c6bba4669ee7a5bfb41b35971fd1b..59bdace7e905c05c127f480dc6d2a8c3a3331392 | wc -l` and confirm it matches the number recorded in the ledger's Reproduction section. Confirm the Cluster Summary table's 5 cluster_count values sum to the CODE bucket total from Task 1.
</verification>

<success_criteria>
`108-DIVERGENCE-LEDGER.md` exists with a Reproduction section (pinned SHAs, live-reconfirmed) and
a Full Commit Accounting section (100 SHAs bucketed into CODE/DEPS/CI/DOCS with zero
double-counts) and a Cluster Summary skeleton (5 clusters, dispositions still TBD, ready for
Plans 108-03/108-04/108-05 to fill in).
</success_criteria>

<output>
Create `.planning/phases/108-upst12-divergence-audit/108-01-SUMMARY.md` when done
</output>
