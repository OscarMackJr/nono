---
phase: 108-upst12-divergence-audit
plan: 05
type: execute
wave: 4
depends_on: ["108-04"]
files_modified:
  - .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md
autonomous: true
requirements: [UPST12-01]

must_haves:
  truths:
    - "Every fork-invariant carve-out surface has an explicit clean/HIT re-touch result -- silence is never treated as evidence (D-22)."
    - "The requirement-coverage gap (D-18) is visible with an exact commit count and every named security-relevant commit accounted for."
    - "A Phase 112 'Security + Residual Sync' roadmap amendment is proposed in writing and explicitly marked as requiring operator approval before Phase 109 planning begins -- it is NOT applied to ROADMAP.md."
    - "The ledger's completeness sweep proves every one of the 100 commits appears in exactly one cluster table, with zero duplicates."
  artifacts:
    - path: ".planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md"
      provides: "Carve-out re-touch check, D-18/D-19/D-20/D-21 gap report + Phase 112 proposal, and a finalized Headline + completeness sweep"
      contains: "Phase 112"
  key_links:
    - from: ".planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md"
      to: "ROADMAP.md Phase 108 SC4"
      via: "explicit statement that SC4 as written is unsatisfiable, with the Phase 112 amendment as the resolution path"
      pattern: "Phase 112|SC4"
---

<objective>
Close out `108-DIVERGENCE-LEDGER.md`: run the carve-out re-touch check against every fork
invariant surface, compile the requirement-coverage gap report (the commits mapping to none of
v3.6's 12 requirements), propose a new Phase 112 "Security + Residual Sync" as a written,
operator-gated roadmap amendment, and finalize the ledger's Headline with a full completeness
sweep proving no commit was silently dropped or double-counted across the whole document.

Purpose: ROADMAP.md Phase 108 SC4 ("the ledger maps each will-sync cluster onto Phase
109/110/111") is unsatisfiable as written (CONTEXT.md D-19) -- ~28 commits map to none of the 12
requirements. This is a legitimate audit finding, not a phase failure; surfacing it with a
concrete proposal IS the audit's purpose. This plan does not silently rewrite ROADMAP.md -- the
proposal is presented for explicit operator approval between this phase and Phase 109 planning.
Output: Carve-out re-touch check, Requirement Coverage Gap section, and finalized Headline
appended to `108-DIVERGENCE-LEDGER.md`. This closes the ledger file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/108-upst12-divergence-audit/108-CONTEXT.md
@.planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md
@.planning/milestones/v3.4-phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@proj/ADR-86-library-boundary-convergence.md

<interfaces>
Carve-out surfaces to re-touch-check (paths verified to exist in the current fork 2026-07-29;
window range for all git log commands:
d817ed53663c6bba4669ee7a5bfb41b35971fd1b..59bdace7e905c05c127f480dc6d2a8c3a3331392):

  CR-02 (audit bypass invariant):        crates/nono/src/audit.rs
  CR-01 (FFI clear_last_call_state):     bindings/c/src/lib.rs bindings/c/src/diagnostic.rs
                                          bindings/c/src/capability_set.rs bindings/c/src/fs_capability.rs
                                          bindings/c/src/sandbox.rs bindings/c/src/state.rs
                                          bindings/c/src/query.rs
  Proxy fork-preserve surface:           crates/nono-proxy/src/route.rs crates/nono-proxy/src/connect.rs
                                          crates/nono-proxy/src/reverse.rs crates/nono-proxy/src/server.rs
                                          crates/nono-cli/src/proxy_runtime.rs
  Endpoint-policy surface (Phase 95):    crates/nono-cli/src/network_policy.rs crates/nono-proxy/src/config.rs
                                          crates/nono-proxy/src/credential.rs crates/nono-proxy/src/route.rs
                                          crates/nono-proxy/src/server.rs
  linux.rs restored invariants:          crates/nono/src/sandbox/linux.rs
  ADR-86 Windows denial-rendering carve-out: crates/nono-cli/src/exec_strategy_windows/
  v3.2 signed-override surface (Rust core side only -- nono-py override logic lives outside this repo):
                                          crates/nono/src/audit.rs (PolicyOverrideApplied + EventIDs 10006-10010)

D-18-named security-relevant commits (verified live 2026-07-29 via git log --grep against the
window) -- the D-18 gap list MUST include all 10, cross-referenced against whatever Plan 108-01
Task 2 filed under security-residual-and-misc:

  0ecc476bf0db3c9509bcc2bd8efae43832c764b8  #1195  feat: implement aws authentication for the MiTM proxy
  9b692e07ee4dd156d85588b12b63691778d9e9be  (none) feat(oauth): add declarative sandboxed OAuth capture
  3c59c62e7ddcc2d3a1f1c161bd039b5f2b0b5f7b  (none) fix(oauth): harden capture security boundaries
  d033c63111472711e242f4067eb4be04aeaf618a  (none) test(oauth): consume provider stdin in header fixture
  a32439074a61eea21eb374a91fdbcebb91b0afe6  #1284  feat(gpu): harden NVIDIA procfs mediation
  f943fb5a0721352e85d92ef48e3e4d5b36747486  #1333  fix(trust): add predicate field to distinguish nono trust policies from foreign JSON
  d84b4818f824b7660d1a4d95ec2e56f322b6fa63  #1397  fix(sandbox): grant Refer in execute-restriction layer on Linux
  ac5ccd70712782f4a97c54916ee18b03c650f56f  #1401  fix(sandbox): keep orphaned descendants in supervisor ancestry for seccomp-notify mediation
  2663e9900fe1317cec0949c18b7a0e6b39321602  #1261  feat(cli): add standalone nono proxy command
  a5a441c25769ecec663aed1d7038ab2b8814d43f  #1204  fix(profile): empty allow_vars no longer strips all env vars
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Carve-out re-touch check (D-22/D-23) + security-relevant rollup (D-20)</name>
  <files>.planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</files>
  <read_first>
    .planning/phases/108-upst12-divergence-audit/108-CONTEXT.md (D-22, D-23)
    .planning/milestones/v3.4-phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md "Carve-out
    Re-touch Check" section (exact format: git log window -- exact path per surface, explicit
    "clean -- no re-touch in window" vs "HIT (N commits) -- disposition" result)
    proj/ADR-86-library-boundary-convergence.md (the D-02 Windows denial-rendering carve-out
    definition, to ground the ADR-86 surface row)
  </read_first>
  <action>
    Append a `## Carve-out Re-touch Check` section. For each of the 7 carve-out surfaces listed
    in context, run `git log --no-merges --oneline <range> -- <exact-path(s)>` and
    record the literal command plus its literal output. If the output is empty, write the exact
    phrase "clean -- no re-touch in window" (matching the 98-DIVERGENCE-LEDGER.md precedent's
    wording). If non-empty, write "HIT (N commits) -- disposition note" naming every returned SHA
    and cross-referencing which cluster/table (from Plans 108-03/108-04) already covers it -- do
    not re-analyze a HIT commit here if it was already classified elsewhere; just point to it.
    Then append a `## Security-Relevant Rollup (D-20)` section: scan every per-commit table
    produced in Plans 108-03 and 108-04 for rows flagged security-relevant=yes, and compile them
    into one consolidated table (sha | subject | source cluster | requirement-mapping-or-none).
    Confirm all 10 D-18-named security-relevant SHAs listed in context appear in this rollup.
  </action>
  <acceptance_criteria>
    - All 7 carve-out surfaces have an explicit result line -- either the literal phrase "clean --
      no re-touch in window" or a "HIT (N commits)" line naming every SHA -- never a surface left
      without a stated result.
    - Every HIT result's named SHA(s) are cross-referenced to a specific cluster/table already
      present in the ledger (from Plan 108-01/108-03/108-04), by section name -- no HIT is left
      un-routed.
    - The Security-Relevant Rollup table contains all 10 D-18-named SHAs (0ecc476b, 9b692e07,
      3c59c62e, d033c63c, a3243907, f943fb5a, d84b4818, ac5ccd70, 2663e990, a5a44107) verbatim.
    - The rollup table's row count is stated as an explicit number and is greater than or equal
      to 10.
  </acceptance_criteria>
  <verify>
    <automated>grep -cE "clean . no re-touch in window|HIT \(" .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</automated>
  </verify>
  <done>All 7 carve-out surfaces have explicit clean/HIT results (silence is never treated as evidence); the Security-Relevant Rollup table contains all 10 D-18-named SHAs.</done>
</task>

<task type="auto">
  <name>Task 2: D-18/D-19/D-21 requirement coverage gap report + Phase 112 proposal</name>
  <files>.planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</files>
  <read_first>
    .planning/phases/108-upst12-divergence-audit/108-CONTEXT.md (D-18, D-19, D-20, D-21 in full)
    .planning/ROADMAP.md (Phase 108's exact SC4 wording, and the v3.1 Phase 87 "Security Sync"
    entry as the structural precedent to mirror for the Phase 112 proposal's shape)
    .planning/REQUIREMENTS.md (v3.6 Traceability table, to see the exact 12 requirement IDs the
    gap list is measured against)
  </read_first>
  <action>
    Append a `## Requirement Coverage Gap (D-18/D-19)` section near the TOP of the document
    (immediately after the Headline placeholder, before the Reproduction block) -- not buried at
    the end. Compile the full list of CODE-bucket commits whose requirement-mapping cell (across
    every per-commit table in Plans 108-03/108-04) reads "none"; state the exact count. Confirm
    every one of the 10 D-18-named SHAs from context is in this list with security-relevant=yes.
    Write the literal sentence: "ROADMAP.md Phase 108 SC4 ('the ledger maps each will-sync
    cluster onto Phase 109/110/111') cannot be satisfied as written -- N commits map to none of
    v3.6's 12 requirements. This is a legitimate audit finding, not a phase failure." (fill in the
    exact N). Then write a `### Proposed Phase 112: Security + Residual Sync` subsection,
    mirroring the v3.1 Phase 87 "Security Sync" precedent's shape: a draft Goal line ("Absorb the
    security-relevant and residual commits from the v0.66.0..v0.69.0 window that no existing v3.6
    phase covers, without mixing security review into a release-cut phase -- mirrors the v3.1
    Phase 87 precedent"), a draft requirement-ID list (at minimum one ID per distinct theme found
    in the gap list -- e.g. SEC-01 AWS MiTM auth, SEC-02 OAuth capture hardening, SEC-03 NVIDIA
    procfs mediation, SEC-04 trust-policy predicate, SEC-05 Linux Refer grant, SEC-06 seccomp
    supervisor-ancestry, SEC-07 standalone proxy command, SEC-08 allow_vars fix, plus additional
    IDs for any non-security residual themes found), and draft Success Criteria bullets in the
    ROADMAP.md phase-detail style. Close the subsection with the literal sentence: "This proposal
    requires operator approval before Phase 109 planning begins -- Phase 108 does not apply this
    amendment to ROADMAP.md." Do NOT edit .planning/ROADMAP.md in this task.
  </action>
  <acceptance_criteria>
    - The gap list states an explicit commit count as a number (not "approximately" or "~28"
      copied verbatim from CONTEXT.md -- a freshly counted number from this ledger's own tables).
    - All 10 D-18-named SHAs appear in the gap list.
    - The literal string "Phase 112" appears at least twice: once in the section header and once
      in body prose.
    - A draft requirement-ID list of at least 8 IDs is present (grep-countable, one per line or
      bullet).
    - The literal sentence "requires operator approval before Phase 109 planning begins" (or an
      exact match up to trivial punctuation) is present.
    - A check of `.planning/ROADMAP.md` shows zero changes after this task (confirms ROADMAP.md
      was not edited by this task).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Phase 112" .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</automated>
  </verify>
  <done>Requirement Coverage Gap section states an exact unmapped-commit count including all 10 D-18-named SHAs, and proposes Phase 112 with a draft requirement-ID list and explicit operator-approval-required language, without editing ROADMAP.md.</done>
</task>

<task type="auto">
  <name>Task 3: Ledger completeness sweep + Headline finalization</name>
  <files>.planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</files>
  <read_first>
    .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md (the full accumulated
    document from Plans 108-01/108-03/108-04/108-05 Tasks 1-2 -- read the whole thing to perform
    the sweep)
    .planning/milestones/v3.4-phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md (Headline
    section format and the "Completeness verification: 14 substantive + 6 noise = 20 total"
    closing-arithmetic pattern to mirror at 5x scale)
  </read_first>
  <action>
    Rewrite the ledger's `## Headline` section (at the very top, after frontmatter) to state:
    total 100 non-merge commits across the named clusters (NET/PROF/CORE/tool-sandbox-pure/
    tool-sandbox-split/security-residual-and-misc/DEPS/CI/DOCS) with per-cluster counts and
    disposition tallies (how many adopt/adapt/skip/split/DEFERRED overall); a one-line pointer to
    the Requirement Coverage Gap section and the Phase 112 proposal; a one-line pointer to the 7
    tool-sandbox refinement PRs DEFERRED->v3.7; a relative-path cross-reference to
    `proj/ADR-108-deny-domain-posture.md`. Then append a final `## Completeness Verification`
    section performing the sweep: sum every cluster/bucket's commit_count (NET + PROF + CORE +
    tool-sandbox-pure + tool-sandbox-split + security-residual-and-misc + DEPS + CI + DOCS) and
    state the arithmetic explicitly, confirming it equals 100; extract every SHA-looking token
    referenced across the whole document as a row-defining identifier (not as a cross-reference
    mention), sort and deduplicate, and state the unique count -- it must equal 100 with zero
    unexplained duplicates (a SHA appearing both in a cluster table AND in a carve-out re-touch
    HIT cross-reference is not a duplicate -- only the same SHA appearing as a primary row in two
    different cluster tables counts as a defect). State explicitly whether SC1 (dispositions +
    windows-touch + ADR verdict per cluster), SC2 (actual-diff re-export scans, not name-only),
    and SC3 (7 refinement PRs DEFERRED->v3.7) from ROADMAP.md Phase 108 are satisfied, citing
    the specific ledger section that satisfies each.
  </action>
  <acceptance_criteria>
    - The Headline section, read alone, contains the number "100", the phrase "Phase 112", and a
      relative-path reference to `proj/ADR-108-deny-domain-posture.md`.
    - The Completeness Verification section shows the explicit per-cluster arithmetic summing to
      100 (e.g. "NET n1 + PROF n2 + CORE n3 + tool-sandbox-pure n4 + tool-sandbox-split n5 +
      security-residual-and-misc n6 + DEPS 15 + CI 11 + DOCS 6 = 100" with actual numbers, not
      variable names).
    - The unique-SHA count from the primary-row dedup pass is stated explicitly as a number and
      equals 100; if it does not, every discrepant SHA is named and explained (not silently
      rounded away).
    - SC1, SC2, and SC3 are each addressed with an explicit "satisfied -- see section X" statement
      (or an explicit gap statement if not satisfied -- never omitted).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^## Completeness Verification" .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</automated>
  </verify>
  <done>Ledger Headline finalized (100 commits, Phase 112 pointer, ADR-108 cross-reference) and Completeness Verification section proves the per-cluster arithmetic sums to 100 with zero unexplained duplicate SHAs, and explicitly confirms SC1/SC2/SC3 satisfaction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Carve-out re-touch silence → fork-invariant regression | D-22 explicitly forbids treating an unstated carve-out surface as evidence of safety; an omitted surface with an actual HIT would let Phase 109/110/111 absorb a conflicting commit without ever flagging the fork-expression-preservation requirement. |
| D-19 gap finding → roadmap amendment authority | This plan proposes Phase 112 in writing but must NOT apply it -- an executor that edits ROADMAP.md directly here would bypass the explicit operator-approval gate D-19 requires, silently expanding the milestone's scope without consent. |
| Ledger completeness → downstream trust | If the completeness sweep is skipped or fudged, Phase 109/110/111 planning inherits an unverified work-list and could silently miss a will-sync commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-11 | Repudiation | Carve-out re-touch check | mitigate | Every surface gets an explicit clean/HIT line with the literal git log command and output recorded (Task 1); "silence is not evidence" is enforced by the acceptance criteria requiring a stated result for all 7 surfaces. |
| T-108-12 | Elevation of Privilege | Phase 112 roadmap-amendment proposal | mitigate | Task 2 explicitly forbids editing ROADMAP.md and requires a zero-change confirmation as an acceptance criterion -- the proposal stays a written recommendation pending operator approval, never a unilateral scope change. |
| T-108-13 | Tampering | Ledger completeness sweep | mitigate | The sum-to-100 arithmetic and unique-SHA-count check (Task 3) make silent commit loss or duplication structurally detectable rather than dependent on reviewer attention. |
</threat_model>

<verification>
Confirm all 7 carve-out surfaces have explicit results. Confirm the Requirement Coverage Gap
section states an exact commit count and proposes Phase 112 without editing ROADMAP.md (a diff
check on .planning/ROADMAP.md shows no changes). Confirm the Completeness Verification section's
arithmetic sums to 100.
</verification>

<success_criteria>
`108-DIVERGENCE-LEDGER.md` is complete: carve-out re-touch check covers all 7 fork-invariant
surfaces with explicit clean/HIT results; the D-18/D-19 requirement-coverage gap is surfaced with
an exact count and a written, operator-gated Phase 112 proposal; the Headline and Completeness
Verification sections prove every one of the 100 window commits is accounted for exactly once.
</success_criteria>

<output>
Create `.planning/phases/108-upst12-divergence-audit/108-05-SUMMARY.md` when done
</output>
