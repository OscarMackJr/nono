---
phase: 108-upst12-divergence-audit
plan: 03
type: execute
wave: 2
depends_on: ["108-01"]
files_modified:
  - .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md
autonomous: true
requirements: [UPST12-01]

must_haves:
  truths:
    - "Every NET/PROF/CORE cluster commit has a hand-verified (not keyword-matched) requirement mapping to one of NET-01/02/03, PROF-01/02/03/04, CORE-01/02, or explicitly 'none'."
    - "Re-export/public-surface diffs for every NET/PROF/CORE commit are inspected via actual-diff (git show), not just git diff --name-only."
    - "The #1374 deny_domain row points to ADR-108 instead of re-deriving the posture analysis inline."
  artifacts:
    - path: ".planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md"
      provides: "NET, PROF, and CORE cluster per-commit tables with requirement mapping and re-export scan findings"
      contains: "see ADR-108"
  key_links:
    - from: ".planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md"
      to: ".planning/REQUIREMENTS.md"
      via: "requirement-mapping column citing NET-01/02/03, PROF-01/02/03/04, CORE-01/02"
      pattern: "NET-0[1-3]|PROF-0[1-4]|CORE-0[1-2]"
---

<objective>
Append per-commit tables for the NET, PROF, and CORE clusters to `108-DIVERGENCE-LEDGER.md`,
with hand-verified requirement mapping (per CONTEXT.md D-21 — the crude keyword-matcher's
42-mapped/28-unmapped split is a hypothesis only) and actual-diff re-export scans (per the
"cluster isolation can be empirically false" lesson — never `--name-only` alone).

Purpose: These three clusters carry the requirement IDs Phase 109 (NET-01/02/03) and Phase 110
(PROF-01/02/03/04) and Phase 111 (CORE-01/02) will consume directly — an unverified or
mis-mapped row here silently under-scopes an absorb phase.
Output: NET/PROF/CORE sections appended to `108-DIVERGENCE-LEDGER.md`, building on Plan 108-01's
Cluster Summary skeleton.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/108-upst12-divergence-audit/108-CONTEXT.md
@.planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md
@.planning/milestones/v3.4-phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md
@.planning/REQUIREMENTS.md
@proj/ADR-108-deny-domain-posture.md

<interfaces>
<!-- Anchor SHAs resolved live 2026-07-29 via `git log --grep` against the window
     d817ed53663c6bba4669ee7a5bfb41b35971fd1b..59bdace7e905c05c127f480dc6d2a8c3a3331392.
     Treat this as a starting set, not an exhaustive one — re-run the grep sweep below to catch
     any additional NET/PROF/CORE commit this list missed. -->

NET anchors:
  3b207eeb884bd71b0fc10f0123fa89fffe9f7955  feat(proxy): add deny_domain to block domains through the proxy (#1374)
  c831dade422f2bdf37d7429af0423cafa0a60c06  feat(proxy): add SPIFFE/SPIRE workload identity auth for upstream routes (#1272)
  6fb7ecbf36e5d18760d8084b7f4900e93582004c  bug: Fix SigV4 URI generation errors for uri's that have encoded characters in them (#1430)
  23d93fc96abf795d672c712e9c1834a8f97aa0aa  fix(proxy): don't cross-deny sibling routes sharing an upstream (#1437)

PROF anchors:
  ae1c513e6ed1f1a0628dbb690c3f1c984fb7a225  feat(profile): add platform_overrides field for per-OS profile patches (#1371)
  719975cf03ea303f835061089279eb6c50245ccf  fix(profile): preserve platform_overrides through extends resolution (#1380)
  2cbaa9a017b14ddc45219474b6eebf94727787ef  feat(profile): expand $VAR tokens from process env in profile paths and capture commands (#1296)
  b620ed8e4572359afa4ed9cff4d803c8e9064f26  feat(policy): add bun runtime preset (#1305)
  f016b2d56dcbe4aad2bb322acd7574d38b5c13f7  mise policy (#1387)
  <!-- these two are ALSO in the tool-sandbox-surface cluster (Plan 108-01 Task 2 assigned them
       there) — this plan gives them ONLY a pointer row here, full analysis is Plan 108-04's job -->
  d5803b994b416ad07a73907143ca169c408917f3  feat: add port range support to sandbox profiles (#1398)
  d4927f95a37863cf0ba534b054e28f48f002ad21  feat(profile): expand @git:* dynamic tokens in top-level filesystem paths (#1298)

CORE anchors:
  ca888108fe5983be866e8d1a6eccf96edc2a8dd5  fix: missing ~/.cache on macOS (#1378)
  099237da94d33752ca5617db3f4902a216f9f84c  fix(exec): raise MAX_CRYPTO_THREADS to 12 for macOS libdispatch workqueue threads (#1424)
  e6d26871f0498e7dc7a867e67af5c6136b84f91c  feat: resource limiting (#1269)
  34c2c975d649844923cf1515be94de624c689c6c  feat(resources): cap sandbox process count with --max-processes (cgroup v2 pids.max) (#1403)

Re-sweep command to catch anything missed (run against crates/nono-proxy/src/, crates/nono-cli/
src/network_policy.rs, crates/nono-cli/src/profile/, crates/nono/src/sandbox/macos.rs, crates/
nono-cli/src/resource_cgroup.rs paths for the window range above):
  git log --no-merges --oneline <range> -- crates/nono-proxy/src/ crates/nono-cli/src/network_policy.rs
  git log --no-merges --oneline <range> -- crates/nono-cli/src/profile/
  git log --no-merges --oneline <range> -- crates/nono/src/sandbox/macos.rs crates/nono-cli/src/resource_cgroup.rs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NET cluster per-commit table — hand-verified requirement mapping + re-export scan</name>
  <files>.planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</files>
  <read_first>
    .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md (own Cluster Summary
    from Plan 108-01, to confirm the NET cluster's commit_count before writing the per-commit
    table)
    .planning/milestones/v3.4-phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md Cluster A
    section (per-commit table columns, actual-diff re-export scan command format:
    `git show <sha> | grep '^+' | grep -E '^\+\s*(pub use|pub mod|extern crate|pub\(crate\))'`)
    .planning/REQUIREMENTS.md (NET-01/NET-02/NET-03 exact wording)
    proj/ADR-108-deny-domain-posture.md (to confirm what to cross-reference, not duplicate)
  </read_first>
  <action>
    Re-run the NET re-sweep commands from context against the window to confirm the 4 anchor
    SHAs are the complete NET set (or find more); append any newly found NET commit. For each
    NET-cluster SHA, write a per-commit row: sha | subject | files-changed (count + list) |
    windows-touch (yes/no, grep the changed files for `cfg(target_os = "windows")` /
    `cfg(windows)`) | security-relevant (yes/no) | requirement-mapping (exactly one of NET-01,
    NET-02, NET-03, none) | disposition. Hand-verify requirement mapping by reading the actual
    `git show <sha>` diff, not by matching keywords against the commit subject (D-21) — e.g.
    confirm SigV4 (#1430) and sibling-route (#1437) map to NET-03 by checking their diffs touch
    the SigV4 signing path / route cross-deny logic named in NET-03's wording, not merely because
    the requirement text happens to mention "SigV4". For every row, run the actual-diff re-export
    scan command and record its findings inline (either the listed `pub` items or the literal
    word "Clean" if none found) — per the cluster-isolation-can-be-empirically-false lesson, do
    NOT rely on `git diff --name-only`. For the `3b207eeb` (#1374) row specifically, set the
    disposition cell to the literal text "see ADR-108 (`proj/ADR-108-deny-domain-posture.md`) —
    ADAPT" and do not write a separate analysis paragraph for it.
  </action>
  <acceptance_criteria>
    - The NET per-commit table's row count exactly equals the NET cluster's `commit_count` from
      Plan 108-01's Cluster Summary table (after accounting for any additional commits found by
      the re-sweep, with the Cluster Summary count updated to match).
    - Every row's requirement-mapping cell contains exactly one of the literal strings `NET-01`,
      `NET-02`, `NET-03`, or `none` — never blank, never a comma-separated list.
    - Every row has a re-export-scan finding recorded (either at least one `pub` item or the
      literal word "Clean").
    - The `3b207eeb` row's disposition cell contains the literal substring "see ADR-108" and the
      table does not contain a paragraph re-deriving the strict/deny-only analysis (that
      analysis lives only in the ADR).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "see ADR-108" .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</automated>
  </verify>
  <done>NET cluster per-commit table complete with hand-verified requirement mapping, windows-touch flags, and actual-diff re-export scan findings for all 4+ anchor commits; the 3b207eeb row points to ADR-108 instead of duplicating its analysis.</done>
</task>

<task type="auto">
  <name>Task 2: PROF + CORE cluster per-commit tables — hand-verified requirement mapping</name>
  <files>.planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</files>
  <read_first>
    .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md (own PROF/CORE rows in
    the Cluster Summary from Plan 108-01)
    .planning/REQUIREMENTS.md (PROF-01/02/03/04 and CORE-01/02 exact wording)
    .planning/phases/108-upst12-divergence-audit/108-CONTEXT.md (D-06 — the exact tool-sandbox
    module set, to confirm d5803b99/d4927f95 stay pointer-only here)
  </read_first>
  <action>
    Re-run the PROF/CORE re-sweep commands from context to confirm anchor completeness. Write the
    PROF per-commit table with the same columns as Task 1, for `ae1c513e` (#1371 → PROF-01),
    `719975cf` (#1380 → PROF-01, extends-resolution preservation), `2cbaa9a0` (#1296 → PROF-02),
    `b620ed8e` (#1305 → PROF-04), `f016b2d5` (#1387 → PROF-04) as full rows with re-export scans,
    PLUS two pointer rows for `d5803b99` (#1398 → PROF-03) and `d4927f95` (#1298 → PROF-02) whose
    disposition cell contains the literal text "see tool-sandbox-split section (Plan 108-04) —
    entangled with tool-sandbox module set per D-05" instead of a duplicated analysis. Write the
    CORE per-commit table for `ca888108` (#1378 → CORE-01), `099237da` (#1424 → CORE-01),
    `e6d26871` (#1269 → CORE-02), `34c2c975` (#1403 → CORE-02) as full rows with re-export scans.
    Windows-touch for PROF-01 rows (platform_overrides, extends) MUST be verified specifically —
    run `git show <sha> --stat | grep -i windows` and also check whether the diff touches any
    `platform_overrides.windows`-shaped key, since this is the field the fork's own
    `windows_low_il_broker`/`windows_interpreters` migration (PROF-01) depends on.
  </action>
  <acceptance_criteria>
    - PROF per-commit table has exactly 7 rows total (5 full analysis rows + 2 pointer rows for
      d5803b99/d4927f95); CORE per-commit table has exactly 4 rows.
    - Every row's windows-touch cell contains exactly `yes` or `no` (never blank); the `ae1c513e`
      (#1371 platform_overrides) row's windows-touch determination is backed by a cited grep
      result in the row's rationale text, not asserted without evidence.
    - The two PROF pointer rows (d5803b99, d4927f95) each contain the literal phrase "see
      tool-sandbox-split section" and do not duplicate Plan 108-04's residue analysis.
    - Every PROF/CORE row's requirement-mapping cell is exactly one of PROF-01, PROF-02, PROF-03,
      PROF-04, CORE-01, CORE-02, or none — never blank.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "see tool-sandbox-split section" .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</automated>
  </verify>
  <done>PROF cluster table has 7 rows (5 full + 2 pointers) and CORE cluster table has 4 rows, all with hand-verified requirement mapping and windows-touch determination backed by evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ledger classification → Phase 109/110 requirement scoping | A commit hand-mapped to `none` when it actually implements NET-01/PROF-03 etc. silently drops scope from the absorb phase's plan; the inverse (mapping to a requirement it doesn't satisfy) causes the absorb phase to falsely believe a requirement is already covered. |
| Re-export scan completeness | A `pub mod`/`pub use` addition missed by relying on `--name-only` could introduce an undetected cross-crate surface change that regresses the ADR-86 boundary during Phase 109/110 absorb. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-06 | Tampering | Requirement-mapping cell (NET/PROF/CORE tables) | mitigate | D-21 hand-verification is a mandatory task step (read the actual diff, not the commit subject); acceptance criteria require every cell populated with an exact requirement ID or `none`, never blank. |
| T-108-07 | Information Disclosure | Re-export scan (`pub` surface changes) | mitigate | Actual-diff `git show \| grep` scan is mandatory per row, per the explicit "cluster isolation can be empirically false" precedent from 98-DIVERGENCE-LEDGER.md. |
</threat_model>

<verification>
Cross-check the NET/PROF/CORE per-commit table row counts against Plan 108-01's Cluster Summary
`commit_count` values (updating the Summary if the re-sweep found additional commits). Confirm no
requirement-mapping cell is blank.
</verification>

<success_criteria>
NET, PROF, and CORE clusters each have complete, hand-verified per-commit tables in
`108-DIVERGENCE-LEDGER.md` with requirement mapping, windows-touch flags, and actual-diff
re-export scan findings — ready for Phase 109/110/111 to consume directly.
</success_criteria>

<output>
Create `.planning/phases/108-upst12-divergence-audit/108-03-SUMMARY.md` when done
</output>
