---
phase: 46-windows-squash-merge-post-merge-ci-verifications-uat-backlog
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/phase-46-uat-backlog.yml
  - .planning/phases/35-upst3-closure-quick-wins/35-HUMAN-UAT.md
  - .planning/phases/35-upst3-closure-quick-wins/35-VERIFICATION.md
  - .planning/phases/36-upst3-deep-closure/36-HUMAN-UAT.md
  - .planning/phases/36-upst3-deep-closure/36-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-03-SUMMARY.md
autonomous: false
requirements:
  - REQ-UAT-BL-01
  - REQ-UAT-BL-02
tags:
  - uat-backlog
  - workflow-dispatch
  - phase-35
  - phase-36
  - no-test-fixture-waiver
  - phase-46
  - v2.6
user_setup:
  - service: github-actions
    why: "Plan dispatches the new phase-46-uat-backlog.yml workflow via `gh workflow run` — requires `gh auth status` to show an active token with `workflow` scope (same as Plan 46-02)."
    env_vars: []
    dashboard_config:
      - task: "Verify `gh auth status` shows logged-in account with `workflow` scope; refresh via `gh auth refresh -s workflow` if needed."
        location: "Local shell — `gh auth status` per memory `gh_available`"
must_haves:
  truths:
    - "A new `.github/workflows/phase-46-uat-backlog.yml` workflow exists with `workflow_dispatch`-only trigger and a matrix shape (`ubuntu-24.04` + `macos-latest` per D-46-C1) that mirrors `.github/workflows/phase-45-resl-native-host.yml`'s `gh_runner_os` choice input pattern per D-46-C2"
    - "The workflow runs the automatable subset of the canonical 11 UAT + 7 verification items (inventoried at plan-open via grep) and emits per-item pass/fail markers usable for the backfilled HUMAN-UAT.md / VERIFICATION.md verdicts"
    - "Phase 35 `35-HUMAN-UAT.md` + `35-VERIFICATION.md` are created (backfilled per D-46-C4) with per-item verdicts: pass / `no-test-fixture (waived per 46-03-SUMMARY § <heading>)` per D-46-C3 (target ≥8/11 pass, ≤3 waived)"
    - "Phase 36 `36-HUMAN-UAT.md` + `36-VERIFICATION.md` are created (backfilled per D-46-C4) with per-item verdicts: pass / no-test-fixture per D-46-C3 (target ≥5/7 pass, ≤2 waived)"
    - "Phase 35 + Phase 36 VERIFICATION.md frontmatter `status` flips out of `human_needed` to `passed` (literal verbatim SC#5 transition language)"
    - "`46-03-SUMMARY.md` records the canonical 11+7 item inventory, the per-item disposition table, the workflow run-id (if dispatched), and `no-test-fixture` waiver rationale per D-46-C3 per item"
    - "REQUIREMENTS.md REQ-UAT-BL-01 + REQ-UAT-BL-02 checkboxes both flip `[ ]` → `[x]`; Traceability rows update `Pending → Complete`"
  artifacts:
    - path: ".github/workflows/phase-46-uat-backlog.yml"
      provides: "workflow_dispatch-only matrix workflow running automatable subset of 35+36 UAT/verification items; mirrors phase-45-resl-native-host.yml layout; tactical/deletable in v3.0 per D-46-C2"
      contains: "workflow_dispatch"
    - path: ".planning/phases/35-upst3-closure-quick-wins/35-HUMAN-UAT.md"
      provides: "Backfilled UAT record for Phase 35: 11-item canonical list with per-item verdicts (pass / no-test-fixture / waived); `backfilled_in: phase-46-plan-46-03` provenance trailer"
      contains: "backfilled_in: phase-46-plan-46-03"
    - path: ".planning/phases/35-upst3-closure-quick-wins/35-VERIFICATION.md"
      provides: "Backfilled verification record for Phase 35; `status: passed`; `re_verification:` block records `human_needed → passed` transition"
      contains: "status: passed"
    - path: ".planning/phases/36-upst3-deep-closure/36-HUMAN-UAT.md"
      provides: "Backfilled UAT record for Phase 36: 7-item canonical list with per-item verdicts; same provenance trailer"
      contains: "backfilled_in: phase-46-plan-46-03"
    - path: ".planning/phases/36-upst3-deep-closure/36-VERIFICATION.md"
      provides: "Backfilled verification record for Phase 36; `status: passed`"
      contains: "status: passed"
    - path: ".planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-03-SUMMARY.md"
      provides: "Roll-up: 11+7 inventory + per-item disposition + waiver rationale per D-46-C3 + workflow run-id (if dispatched) + REQUIREMENTS.md flip record"
      contains: "no-test-fixture"
  key_links:
    - from: ".planning/phases/35-upst3-closure-quick-wins/35-VERIFICATION.md"
      to: ".planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-03-SUMMARY.md"
      via: "`backfilled_in: phase-46-plan-46-03` provenance + `re_verification.trigger:` field cite"
      pattern: "backfilled_in: phase-46-plan-46-03"
    - from: ".planning/phases/36-upst3-deep-closure/36-VERIFICATION.md"
      to: ".planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-03-SUMMARY.md"
      via: "same provenance + trigger cite"
      pattern: "backfilled_in: phase-46-plan-46-03"
    - from: ".github/workflows/phase-46-uat-backlog.yml"
      to: ".planning/phases/35-upst3-closure-quick-wins/35-HUMAN-UAT.md"
      via: "workflow run-id citation in HUMAN-UAT.md per-item verdict rows"
      pattern: "phase-46-uat-backlog"
---

<objective>
Drain the Phase 35 + Phase 36 human-UAT backlog (11 UAT scenarios + 7 verification items) deferred at v2.4 close. Per D-46-C2 lands a new `phase-46-uat-backlog.yml` workflow_dispatch-only workflow on a ubuntu-24.04 + macos-latest matrix that runs the automatable subset of items. Per D-46-C4 backfills the missing `35-HUMAN-UAT.md` + `35-VERIFICATION.md` + `36-HUMAN-UAT.md` + `36-VERIFICATION.md` with the canonical 11+7 item list and per-item verdicts. Per D-46-C3 non-automatable items waive as `no-test-fixture` with per-item rationale in `46-03-SUMMARY.md`. Per D-46-C4 Phase 35 + 36 VERIFICATION.md `status: human_needed` flips to `passed` (verbatim SC#5 transition language). Flips REQUIREMENTS.md REQ-UAT-BL-01 + REQ-UAT-BL-02 checkboxes.

Purpose: Closes the host-blocked v2.4 carry-forward. GH Actions runners replace the unavailable native Linux/macOS host (per D-46-C1). Per-item `no-test-fixture` waivers preserve audit clarity (SC#5 explicit allowance) instead of deferring non-automatable items to v3.0 (rejected at D-46-C3).

Output: New `.github/workflows/phase-46-uat-backlog.yml`; backfilled Phase 35 + 36 HUMAN-UAT.md + VERIFICATION.md files with `status: passed` post-execution; `46-03-SUMMARY.md` records inventory + waiver rationale; REQUIREMENTS.md REQ-UAT-BL-01 + 02 atomically flipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-CONTEXT.md
@.planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-PATTERNS.md
@.planning/milestones/v2.4-MILESTONE-AUDIT.md
@.planning/phases/35-upst3-closure-quick-wins/35-01-WIN-ENV-FILTER-SUMMARY.md
@.planning/phases/35-upst3-closure-quick-wins/35-02-LINUX-LANDLOCK-PROFILES-SUMMARY.md
@.planning/phases/35-upst3-closure-quick-wins/35-03-WIN-TEST-HYGIENE-SUMMARY.md
@.github/workflows/phase-45-resl-native-host.yml
@.github/workflows/phase-37-linux-resl.yml
@.planning/phases/37-linux-resl-backends-pkgs-auto-pull/37-HUMAN-UAT.md
@.planning/phases/37-linux-resl-backends-pkgs-auto-pull/37-VERIFICATION.md
@.planning/phases/44.1-oidc-fail-closed-remediation-req-review-fu-01-t-44-01-cr-01/44.1-VERIFICATION.md
@CLAUDE.md

<interfaces>
<!-- Phase 45 workflow_dispatch input pattern (verbatim from PATTERNS.md inline excerpt for phase-46-uat-backlog.yml): -->
<!-- on: -->
<!--   workflow_dispatch: -->
<!--     inputs: -->
<!--       gh_runner_os: -->
<!--         description: Which OS matrix to run -->
<!--         type: choice -->
<!--         options: -->
<!--           - ubuntu-24.04 -->
<!--           - macos-latest -->
<!--           - both -->
<!--         default: both -->

<!-- Top-level env + permissions (verbatim from PATTERNS.md analog excerpt — copy exactly): -->
<!-- env: -->
<!--   CARGO_TERM_COLOR: always -->
<!--   RUSTFLAGS: -Dwarnings -->
<!-- permissions: -->
<!--   contents: read -->

<!-- SHA-pinned actions (Phase 37 D-15 / WR-09 enforcement applies — reuse these exact pinned SHAs from PATTERNS.md inline excerpt): -->
<!-- - actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6 -->
<!-- - dtolnay/rust-toolchain@631a55b12751854ce901bb631d5902ceb48146f7 # stable -->
<!-- - actions/cache@668228422ae6a00e4ad889ee87cd7109ec5666a7 # v5 -->

<!-- Per-OS conditional job pattern (verbatim shape from PATTERNS.md analog excerpt — substitutions: name `Phase 46 UAT backlog (Linux)`/(macOS); cache key prefix `phase46-uat-backlog`; cargo invocations replace `cargo test -p nono-cli --test audit_attestation`): -->
<!-- jobs: -->
<!--   uat-backlog-linux: -->
<!--     if: ${{ inputs.gh_runner_os == 'ubuntu-24.04' || inputs.gh_runner_os == 'both' }} -->
<!--     name: Phase 46 UAT backlog (Linux) -->
<!--     runs-on: ubuntu-24.04 -->
<!--     timeout-minutes: 30 -->
<!--     continue-on-error: true -->
<!--     steps: -->
<!--       - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6 -->
<!--       - uses: dtolnay/rust-toolchain@631a55b12751854ce901bb631d5902ceb48146f7 # stable -->
<!--       - uses: actions/cache@668228422ae6a00e4ad889ee87cd7109ec5666a7 # v5 -->
<!--         with: -->
<!--           path: | -->
<!--             ~/.cargo/registry -->
<!--             ~/.cargo/git -->
<!--             target -->
<!--           key: ${{ runner.os }}-phase46-uat-backlog-${{ hashFiles('**/Cargo.lock') }} -->
<!--           restore-keys: | -->
<!--             ${{ runner.os }}-phase46-uat-backlog- -->
<!--       - name: Build workspace -->
<!--         run: cargo build --workspace --release --verbose -->
<!--       - name: Run automatable UAT subset (Linux) -->
<!--         run: <inventoried cargo test invocations — see Task 1 Step 1> -->

<!-- Backfill provenance trailer convention (from PATTERNS.md § "Shared Patterns" § "Backfill provenance trailer"): -->
<!-- backfilled_in: phase-46-plan-46-03 -->
<!-- backfill_rationale: "v2.4 close left <FILE>.md absent (human_needed deferred to v2.6 native host per memory project_v26_opened); Phase 46 Plan 46-03 backfills with verdicts from phase-46-uat-backlog.yml CI runs + no-test-fixture waivers per D-46-C3." -->

<!-- v2.4-MILESTONE-AUDIT rows 116-121 confirm pre-passed items at v2.4 close: -->
<!-- - Phase 35: env_filter_tests + profile_cli debug-syntax (2 of 11 UAT pre-passed) -->
<!-- - Phase 36: docs MDX bypass_protection (1 of 7 verification pre-passed) -->

<!-- v2.4-MILESTONE-AUDIT rows 273-274 confirm canonical totals: -->
<!-- - UAT gaps | 11 | Phases 35 + 36 human_uat -->
<!-- - Verification gaps | 7 -->

<!-- Phase 35 + Phase 36 source SUMMARYs for item inventory (do NOT touch per D-46-C4): -->
<!-- - 35-01-WIN-ENV-FILTER-SUMMARY.md (REQ-PORT-CLOSURE-01; Win env-filter wiring; 4 cfg-gated regression tests) -->
<!-- - 35-02-LINUX-LANDLOCK-PROFILES-SUMMARY.md (REQ-PORT-CLOSURE-06; Linux Landlock profiles-dir) -->
<!-- - 35-03-WIN-TEST-HYGIENE-SUMMARY.md (REQ-PORT-CLOSURE-07; profile_cli debug-syntax tests + UNC path flake fix) -->
<!-- - 36-{01a..03}-SUMMARY.md (REQ-PORT-CLOSURE-02 + 04 + 05; deprecated_schema port, yaml_merge wiring, ExecConfig refactor) -->

<!-- HUMAN-UAT.md / VERIFICATION.md target shapes — see PATTERNS.md § `35-HUMAN-UAT.md` + `35-VERIFICATION.md` inline excerpts. -->
<!-- For HUMAN-UAT.md frontmatter use post-execution shape (Phase 50 analog): status: passed + closed: 2026-05-DD + scenarios + result + recording_location + backfilled_in + backfill_rationale. -->
<!-- For VERIFICATION.md frontmatter use post-execution shape (Phase 44.1 + Phase 37 re_verification blend): phase + verified + status: passed + score + re_verification block (previous_status: human_needed, trigger, gaps_closed, gaps_remaining: [], regressions: []) + backfilled_in. -->

<!-- REQUIREMENTS.md current REQ-UAT-BL-01..02 lines (Task 5 single-char flip target at lines 26-27): -->
<!-- - [ ] **REQ-UAT-BL-01**: Phase 35 + 36 human-UAT backlog (11 scenarios) ... -->
<!-- - [ ] **REQ-UAT-BL-02**: Phase 35 + 36 verification backlog (7 items) ... -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory canonical 11 UAT + 7 verification items + create phase-46-uat-backlog.yml workflow</name>
  <files>
    .github/workflows/phase-46-uat-backlog.yml
  </files>
  <read_first>
    - .planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-CONTEXT.md § D-46-C1, C2, C3, C4 + § Claude's Discretion bullets 4 + 5 (workflow shape + UAT item inventory at plan-open + per-item categorization rules)
    - .planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-PATTERNS.md § `.github/workflows/phase-46-uat-backlog.yml` (inline analog excerpts — exact workflow_dispatch input shape + per-OS conditional job pattern + SHA-pinned actions + cache key namespace; do NOT re-read `.github/workflows/phase-45-resl-native-host.yml`)
    - .planning/milestones/v2.4-MILESTONE-AUDIT.md (rows 70-114 = per-REQ human_verify_blocker rationale; rows 116-121 = 3 pre-passed items; rows 273-274 = canonical 11+7 totals)
    - .planning/phases/35-upst3-closure-quick-wins/35-01-WIN-ENV-FILTER-SUMMARY.md (REQ-PORT-CLOSURE-01 item list + automatable test invocations)
    - .planning/phases/35-upst3-closure-quick-wins/35-02-LINUX-LANDLOCK-PROFILES-SUMMARY.md (REQ-PORT-CLOSURE-06 item list)
    - .planning/phases/35-upst3-closure-quick-wins/35-03-WIN-TEST-HYGIENE-SUMMARY.md (REQ-PORT-CLOSURE-07 item list)
    - .planning/phases/36-upst3-deep-closure/36-01a-SUMMARY.md (only if exists — read for REQ-PORT-CLOSURE-02 item list)
    - .planning/phases/36-upst3-deep-closure/36-02-SUMMARY.md (only if exists)
    - .planning/phases/36-upst3-deep-closure/36-03-SUMMARY.md (only if exists)
    - .github/workflows/phase-45-resl-native-host.yml (sanity-check the analog file still exists; do NOT exhaustively re-read — PATTERNS.md inline excerpts are authoritative)
  </read_first>
  <action>
**Step 0 — cwd guard:**

```bash
cd /c/Users/OMack/Nono && pwd && git branch --show-current
```

**Step 1 — Inventory canonical 11 UAT + 7 verification items.** Grep across the canonical sources:

```bash
# Confirm canonical totals from v2.4-MILESTONE-AUDIT.md
grep -E "(UAT gaps|Verification gaps)\s*\|\s*[0-9]+\s*\|" .planning/milestones/v2.4-MILESTONE-AUDIT.md

# Extract per-REQ blockers (rows 70-114)
sed -n '70,114p' .planning/milestones/v2.4-MILESTONE-AUDIT.md

# Pre-passed items at v2.4 close
sed -n '116,121p' .planning/milestones/v2.4-MILESTONE-AUDIT.md
```

Build an inventory table (this drives both the workflow's cargo invocations + the HUMAN-UAT.md item rows + the SUMMARY's disposition). Expected shape:

| # | Phase | UAT vs Verif | Description | Source SUMMARY | Pre-passed at v2.4? | Disposition (auto/no-test-fixture) |
|---|-------|--------------|-------------|----------------|---------------------|-----------------------------------|
| 1 | 35 | UAT | env_filter_tests | 35-01 | YES | pre-pass (skip workflow) |
| 2 | 35 | UAT | profile_cli debug-syntax | 35-03 | YES | pre-pass |
| ...| ...| ...| ... | ... | ... | ... |
| 11 | 35 | UAT | <last item> | ... | NO | auto (cargo test ...) OR no-test-fixture |
| 12 | 36 | VERIF | docs MDX bypass_protection | 36-xx | YES | pre-pass |
| ...| ...| ...| ... | ... | ... | ... |
| 18 | 36 | VERIF | <last item> | ... | NO | ... |

**Deviation check per CONTEXT.md Claude's Discretion bullet 5:** If the actual inventory differs from 11+7 by more than ±1, surface a `checkpoint:decision` so the user can confirm scope deviation BEFORE writing the workflow file.

**Step 2 — Write `.github/workflows/phase-46-uat-backlog.yml`** mirroring the Phase 45 analog per D-46-C2 + the inline PATTERNS.md excerpts. Structure:

```yaml
# Phase 46 — UAT backlog drain (REQ-UAT-BL-01..02)
#
# Tactical re-execution of Phase 35 + 36 HUMAN-UAT + VERIFICATION items
# deferred at v2.4 close. Per D-46-C2 workflow_dispatch-only, deletable
# in v3.0 once verdicts are recorded in 46-03-SUMMARY.md. Mirrors
# .github/workflows/phase-45-resl-native-host.yml layout.
#
# Per D-46-C1, GH Actions runners replace the unavailable native
# Linux/macOS host. Non-automatable items waive as `no-test-fixture`
# per D-46-C3 with per-item rationale in 46-03-SUMMARY.md.
#
# Invocation (Phase 46 orchestrator action):
#   gh workflow run phase-46-uat-backlog.yml -f gh_runner_os=both
#   gh run watch
#
# continue-on-error: true on both per-OS jobs — D-46-C3 maps cleanly:
# items that fail because the test fixture is intrinsically unavailable
# get a `no-test-fixture` waiver in 46-03-SUMMARY rather than blocking.

name: Phase 46 — UAT backlog drain

on:
  workflow_dispatch:
    inputs:
      gh_runner_os:
        description: Which OS matrix to run
        type: choice
        options:
          - ubuntu-24.04
          - macos-latest
          - both
        default: both

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -Dwarnings

permissions:
  contents: read

jobs:
  uat-backlog-linux:
    if: ${{ inputs.gh_runner_os == 'ubuntu-24.04' || inputs.gh_runner_os == 'both' }}
    name: Phase 46 UAT backlog (Linux)
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    continue-on-error: true
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@631a55b12751854ce901bb631d5902ceb48146f7 # stable
        with:
          toolchain: stable
      - name: Cache cargo registry + target
        uses: actions/cache@668228422ae6a00e4ad889ee87cd7109ec5666a7 # v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-phase46-uat-backlog-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-phase46-uat-backlog-
      - name: Build workspace
        run: cargo build --workspace --release --verbose
      - name: Run automatable Linux UAT subset
        run: |
          # Per inventory Step 1: each automatable Linux item gets a
          # `cargo test` (or smoke-test) invocation here. Linux-only
          # items: e.g. REQ-PORT-CLOSURE-06 Landlock profiles-dir.
          # Host-agnostic items run on both jobs.
          <inventoried cargo test invocations per Step 1 — Linux subset>

  uat-backlog-macos:
    if: ${{ inputs.gh_runner_os == 'macos-latest' || inputs.gh_runner_os == 'both' }}
    name: Phase 46 UAT backlog (macOS)
    runs-on: macos-latest
    timeout-minutes: 30
    continue-on-error: true
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@631a55b12751854ce901bb631d5902ceb48146f7 # stable
        with:
          toolchain: stable
      - name: Cache cargo registry + target
        uses: actions/cache@668228422ae6a00e4ad889ee87cd7109ec5666a7 # v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-phase46-uat-backlog-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-phase46-uat-backlog-
      - name: Build workspace
        run: cargo build --workspace --release --verbose
      - name: Run automatable macOS UAT subset
        run: |
          # Per inventory Step 1: macOS-only + host-agnostic items.
          <inventoried cargo test invocations per Step 1 — macOS subset>
```

The `<inventoried cargo test invocations per Step 1 — Linux subset>` placeholder MUST be replaced with the actual cargo invocations derived from the inventory table in Step 1. Examples (substitute by inventory):

- Linux Landlock profiles-dir (REQ-PORT-CLOSURE-06): `cargo test -p nono-cli --test landlock_profiles_dir -- --include-ignored`
- Linux env-filter cfg-gated regression tests (REQ-PORT-CLOSURE-01): `cargo test -p nono-cli --test env_filter -- --include-ignored`
- macOS Seatbelt host-specific test: similar pattern

For non-automatable items (interactive consent prompts, ETW capture, etc.) leave them OUT of the cargo invocations — they get the `no-test-fixture` waiver in Task 4's SUMMARY rather than appearing in the workflow.

**Step 3 — Commit the workflow file** with DCO sign-off:

```
feat(46-03): add phase-46-uat-backlog.yml workflow_dispatch matrix (REQ-UAT-BL-01..02)

Per D-46-C2, lands a tactical workflow_dispatch-only workflow on the
ubuntu-24.04 + macos-latest matrix (D-46-C1) that runs the automatable
subset of the canonical 11 UAT + 7 verification items deferred at v2.4
close. Mirrors .github/workflows/phase-45-resl-native-host.yml layout.
continue-on-error: true on both per-OS jobs so D-46-C3 no-test-fixture
waivers can apply per-item in 46-03-SUMMARY.md rather than block.

Deletable in v3.0 once verdicts recorded.

Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>
Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

**Step 4 — Dispatch the workflow + capture run-id** (this turns this task into `autonomous: false` — gh invocation against live external state):

```bash
gh workflow run phase-46-uat-backlog.yml -f gh_runner_os=both
sleep 5  # give GH Actions a moment to enqueue the run
UAT_RUN_ID=$(gh run list --workflow=phase-46-uat-backlog.yml -L 1 --json databaseId --jq '.[0].databaseId')
echo "UAT_RUN_ID=$UAT_RUN_ID"

# Wait for completion (allow continue-on-error per-job, so we don't --exit-status)
gh run watch "$UAT_RUN_ID"
```

Capture `$UAT_RUN_ID` for use in Tasks 2/3/4 verdict rows + SUMMARY frontmatter.

**Step 5 — cwd recovery** (per memory `feedback_windows_worktree_cwd`):

```bash
cd /c/Users/OMack/Nono && pwd && git branch --show-current
```

**Authentication gate:** Same as Plan 46-02 Task 1 — if `gh workflow run` fails on auth, emit a dynamic checkpoint for the user to run `gh auth refresh -s workflow`.
  </action>
  <verify>
    <automated>test -f .github/workflows/phase-46-uat-backlog.yml &amp;&amp; grep -q "^name: Phase 46 — UAT backlog drain$" .github/workflows/phase-46-uat-backlog.yml &amp;&amp; grep -q "^  workflow_dispatch:$" .github/workflows/phase-46-uat-backlog.yml &amp;&amp; grep -q "      gh_runner_os:" .github/workflows/phase-46-uat-backlog.yml &amp;&amp; grep -q "        type: choice" .github/workflows/phase-46-uat-backlog.yml &amp;&amp; grep -q "          - ubuntu-24.04" .github/workflows/phase-46-uat-backlog.yml &amp;&amp; grep -q "          - macos-latest" .github/workflows/phase-46-uat-backlog.yml &amp;&amp; grep -q "          - both" .github/workflows/phase-46-uat-backlog.yml &amp;&amp; grep -q "  uat-backlog-linux:" .github/workflows/phase-46-uat-backlog.yml &amp;&amp; grep -q "  uat-backlog-macos:" .github/workflows/phase-46-uat-backlog.yml &amp;&amp; grep -q "    continue-on-error: true" .github/workflows/phase-46-uat-backlog.yml &amp;&amp; grep -q "actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd" .github/workflows/phase-46-uat-backlog.yml &amp;&amp; grep -q "dtolnay/rust-toolchain@631a55b12751854ce901bb631d5902ceb48146f7" .github/workflows/phase-46-uat-backlog.yml &amp;&amp; grep -q "actions/cache@668228422ae6a00e4ad889ee87cd7109ec5666a7" .github/workflows/phase-46-uat-backlog.yml &amp;&amp; grep -q "phase46-uat-backlog-" .github/workflows/phase-46-uat-backlog.yml &amp;&amp; gh run list --workflow=phase-46-uat-backlog.yml -L 1 --json databaseId | grep -qE '"databaseId":[0-9]+' &amp;&amp; git log -1 --format="%s" | grep -q "^feat(46-03): add phase-46-uat-backlog.yml"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/phase-46-uat-backlog.yml` exists
    - File header comment block declares "Phase 46 — UAT backlog drain (REQ-UAT-BL-01..02)" + D-46-C2 + invocation example
    - YAML `name:` is exactly `Phase 46 — UAT backlog drain`
    - YAML trigger is `workflow_dispatch` only (no `push:` / `pull_request:` triggers)
    - `gh_runner_os` input is a `type: choice` with options `[ubuntu-24.04, macos-latest, both]` and default `both` (verbatim Phase 45 analog shape)
    - Two jobs exist: `uat-backlog-linux` (gated on `inputs.gh_runner_os == 'ubuntu-24.04' || 'both'`) and `uat-backlog-macos` (gated on `'macos-latest' || 'both'`)
    - Both jobs have `continue-on-error: true` (per D-46-C3 no-test-fixture mapping)
    - Both jobs use the 3 SHA-pinned actions verbatim: `actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6`, `dtolnay/rust-toolchain@631a55b12751854ce901bb631d5902ceb48146f7 # stable`, `actions/cache@668228422ae6a00e4ad889ee87cd7109ec5666a7 # v5` (Phase 37 D-15 / WR-09 enforcement)
    - Cache key prefix is `phase46-uat-backlog-` (per PATTERNS.md namespace substitution)
    - `gh run list --workflow=phase-46-uat-backlog.yml -L 1` returns a databaseId for a dispatched run
    - Latest commit subject is `feat(46-03): add phase-46-uat-backlog.yml workflow_dispatch matrix (REQ-UAT-BL-01..02)` with DCO sign-off
    - Inventory of 11 UAT + 7 verification items captured (used as source for Tasks 2/3); deviation > ±1 surfaced as a checkpoint
  </acceptance_criteria>
  <done>Workflow YAML lands with workflow_dispatch trigger + matrix + continue-on-error + SHA-pinned actions; workflow dispatched + run-id captured for downstream tasks; inventory of 11+7 items recorded for use in backfill tasks.</done>
</task>

<task type="auto">
  <name>Task 2: Backfill Phase 35 HUMAN-UAT.md + VERIFICATION.md (per D-46-C4)</name>
  <files>
    .planning/phases/35-upst3-closure-quick-wins/35-HUMAN-UAT.md
    .planning/phases/35-upst3-closure-quick-wins/35-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-CONTEXT.md § D-46-C4 (literal "HUMAN-UAT.md and VERIFICATION.md transition out of `human_needed` state" + verbatim SC#5 language)
    - .planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-PATTERNS.md § `35-HUMAN-UAT.md` + § `35-VERIFICATION.md` (inline target frontmatter shapes + body section structure + backfill provenance trailer + "no-test-fixture" verdict marker)
    - .planning/phases/37-linux-resl-backends-pkgs-auto-pull/37-HUMAN-UAT.md (analog confirmation — body shape)
    - .planning/phases/37-linux-resl-backends-pkgs-auto-pull/37-VERIFICATION.md (analog confirmation — six-section body structure)
    - .planning/phases/44.1-oidc-fail-closed-remediation-req-review-fu-01-t-44-01-cr-01/44.1-VERIFICATION.md (post-execution `status: passed` frontmatter shape)
    - Task 1 inventory table output (11 UAT items, which Phase = 35; reuse the Phase-35-scoped subset — typically all 11 are in Phase 35 per CONTEXT.md `<canonical_refs>` UAT-BL-01 sources)
    - Task 1 captured `$UAT_RUN_ID` (for run-id citations in verdict rows)
  </read_first>
  <action>
**Step 0 — cwd guard:**

```bash
cd /c/Users/OMack/Nono && pwd && git branch --show-current
```

**Step 1 — Determine per-item verdicts from Task 1 workflow run output:**

```bash
UAT_RUN_ID=$(gh run list --workflow=phase-46-uat-backlog.yml -L 1 --json databaseId --jq '.[0].databaseId')
gh run view "$UAT_RUN_ID" --log 2>/dev/null | grep -E "(test result|FAILED|ok|PASSED)" > /tmp/uat-run-log.txt
gh run view "$UAT_RUN_ID" --json jobs --jq '.jobs[] | {name, conclusion}'
```

For each of the 11 Phase 35 UAT items, determine the verdict:
- pass (cargo test invocation succeeded)
- pre-pass (already passed at v2.4 close per v2.4-MILESTONE-AUDIT rows 116-121 — applies to env_filter_tests + profile_cli debug-syntax)
- no-test-fixture (item is non-automatable per D-46-C3 — gets waiver in 46-03-SUMMARY)

Target per D-46-C3: at least 8/11 pass, ≤3 waived as no-test-fixture.

**Step 2 — Write `35-HUMAN-UAT.md`** at `.planning/phases/35-upst3-closure-quick-wins/35-HUMAN-UAT.md`:

```markdown
# ---
status: passed
phase: 35-upst3-closure-quick-wins
source: [35-VERIFICATION.md]
started: 2026-05-23T<HH:MM:SS>Z
updated: 2026-05-23T<HH:MM:SS>Z
closed: 2026-05-23
scenarios: 11
result: <N>/11 pass + <M>/11 no-test-fixture (waived per 46-03-SUMMARY)
recording_location: "Phase 46 Plan 46-03 (.github/workflows/phase-46-uat-backlog.yml run-id <UAT_RUN_ID>) + per-item waiver rationale in 46-03-SUMMARY.md"
backfilled_in: phase-46-plan-46-03
backfill_rationale: "v2.4 close left 35-HUMAN-UAT.md absent (human_needed deferred to v2.6 native host per memory project_v26_opened); Phase 46 Plan 46-03 backfills with verdicts from phase-46-uat-backlog.yml CI runs + no-test-fixture waivers per D-46-C3."
# ---

## Current Test

[all tests complete — backfilled at Phase 46 close]

## Tests

### 1. env_filter_tests (REQ-PORT-CLOSURE-01)
expected: All 4 cfg-gated Windows env-filter regression tests pass on Windows host (per 35-01-WIN-ENV-FILTER-SUMMARY.md). Validates `test_windows_empty_allow_denies_all_env_vars` + 3 sibling tests.
result: pass (pre-passed v2.4 per v2.4-MILESTONE-AUDIT.md rows 116-121)

### 2. profile_cli debug-syntax (REQ-PORT-CLOSURE-07)
expected: profile_cli debug-syntax tests pass on Windows host (per 35-03-WIN-TEST-HYGIENE-SUMMARY.md). Validates UNC path flake fix.
result: pass (pre-passed v2.4)

### 3. <item 3 from inventory>
expected: <verbatim from source SUMMARY>
result: <pass | no-test-fixture (waived per 46-03-SUMMARY § <heading>) | skipped>

[... continue for all 11 items ...]

## Summary

total: 11
passed: <N>
issues: 0
pending: 0
skipped: 0
blocked: 0
no-test-fixture: <M>

## Gaps

No goal-blocking gaps — REQ-UAT-BL-01 closed via Plan 46-03 with `<N>/11 pass + <M>/11 no-test-fixture` per D-46-C3 explicit allowance.
```

For each item that gets `no-test-fixture`, the `result:` line cites the section heading in `46-03-SUMMARY.md` where the per-item rationale lives (Task 4 writes those section headings).

**Step 3 — Write `35-VERIFICATION.md`** at `.planning/phases/35-upst3-closure-quick-wins/35-VERIFICATION.md`:

Use the post-execution shape blended from Phase 44.1 + Phase 37 re_verification per PATTERNS.md inline excerpt:

```markdown
# ---
phase: 35-upst3-closure-quick-wins
verified: 2026-05-23T<HH:MM:SS>Z
status: passed
score: <N>/11 UAT items verified (<M> items waived per `no-test-fixture` per D-46-C3)
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: n/a (v2.4 close did not produce 35-VERIFICATION.md)
  previous_verified: n/a
  trigger: "Phase 46 Plan 46-03 backfill per D-46-C4; phase-46-uat-backlog.yml CI run-id <UAT_RUN_ID> + no-test-fixture waivers in 46-03-SUMMARY close REQ-UAT-BL-01."
  gaps_closed:
    - "<verbatim item 1 description> → pass (pre-passed v2.4 per v2.4-MILESTONE-AUDIT.md rows 116-121)"
    - "<verbatim item 2 description> → pass (pre-passed v2.4)"
    - "<item 3> → <pass | no-test-fixture (waiver in 46-03-SUMMARY § <heading>)>"
    [... all 11 items ...]
  gaps_remaining: []
  regressions: []
backfilled_in: phase-46-plan-46-03
# ---

# Phase 35: upst3-closure-quick-wins Verification Report

**Phase Goal:** <copy from ROADMAP.md or original phase SUMMARY if present>

**Verified:** <ISO-8601 timestamp>
**Status:** passed
**Re-verification:** Yes (backfilled per Phase 46 Plan 46-03 D-46-C4)

## Goal Achievement

### Observable Truths

| #   | Truth (Success Criterion) | Status | Evidence |
| --- | ------------------------- | ------ | -------- |
| ... | <from original phase SUMMARYs> | VERIFIED | <reach back to 35-01/02/03-SUMMARY.md per PATTERNS.md "Planner discretion: option (b)" — reference SUMMARY artifacts as evidence source rather than re-do verification work> |

**Score:** N/N truths verified

### Deferred Items

No items deferred to later phases.

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| 35-01-WIN-ENV-FILTER-SUMMARY.md | exists at v2.4 close | VERIFIED | See SUMMARY |
| 35-02-LINUX-LANDLOCK-PROFILES-SUMMARY.md | exists | VERIFIED | See SUMMARY |
| 35-03-WIN-TEST-HYGIENE-SUMMARY.md | exists | VERIFIED | See SUMMARY |

### Key Link Verification

(reach-back to per-plan SUMMARYs per PATTERNS.md option (b))

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| REQ-PORT-CLOSURE-01 | 35-01 | Win env-filter wiring | SATISFIED | 35-01-WIN-ENV-FILTER-SUMMARY.md + Phase 46 Plan 46-03 backfill verdict |
| REQ-PORT-CLOSURE-06 | 35-02 | Linux Landlock profiles-dir | SATISFIED | 35-02-LINUX-LANDLOCK-PROFILES-SUMMARY.md + Phase 46 Plan 46-03 backfill verdict |
| REQ-PORT-CLOSURE-07 | 35-03 | profile_cli debug-syntax + UNC fix | SATISFIED | 35-03-WIN-TEST-HYGIENE-SUMMARY.md + Phase 46 Plan 46-03 backfill verdict |

**No orphaned requirements.**

### Human Verification Required

All HUMAN-UAT items closed via phase-46-uat-backlog.yml CI run-id <UAT_RUN_ID> + no-test-fixture waivers in 46-03-SUMMARY § <heading>. See 35-HUMAN-UAT.md for per-item verdicts.

### Gaps Summary

**No goal-blocking gaps.** All 11 Phase 35 UAT items reach pass or carry a documented `no-test-fixture` waiver per SC#5; the v2.4-close `human_needed` deferral closed via Phase 46 Plan 46-03 backfill per D-46-C4.

# ---

_Verified: <timestamp>_
_Verifier: Claude (gsd-verifier) — Phase 46 backfill_
```

**Step 4 — Commit both files** as a single atomic commit with DCO sign-off:

```
docs(46-03): backfill Phase 35 HUMAN-UAT + VERIFICATION (REQ-UAT-BL-01 partial)

Per D-46-C4, backfills the two files that were absent at v2.4 close
(human_needed deferred to v2.6 native host). Records per-item verdicts
from phase-46-uat-backlog.yml CI run-id <UAT_RUN_ID> + pre-passed
items from v2.4-MILESTONE-AUDIT rows 116-121 + no-test-fixture waivers
per D-46-C3 (rationale in 46-03-SUMMARY). Phase 35 VERIFICATION.md
status: human_needed → passed (verbatim SC#5 transition language).

Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>
Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```
  </action>
  <verify>
    <automated>test -f .planning/phases/35-upst3-closure-quick-wins/35-HUMAN-UAT.md &amp;&amp; test -f .planning/phases/35-upst3-closure-quick-wins/35-VERIFICATION.md &amp;&amp; grep -q "^status: passed$" .planning/phases/35-upst3-closure-quick-wins/35-HUMAN-UAT.md &amp;&amp; grep -q "^status: passed$" .planning/phases/35-upst3-closure-quick-wins/35-VERIFICATION.md &amp;&amp; grep -q "^backfilled_in: phase-46-plan-46-03$" .planning/phases/35-upst3-closure-quick-wins/35-HUMAN-UAT.md &amp;&amp; grep -q "^backfilled_in: phase-46-plan-46-03$" .planning/phases/35-upst3-closure-quick-wins/35-VERIFICATION.md &amp;&amp; grep -q "^scenarios: 11$" .planning/phases/35-upst3-closure-quick-wins/35-HUMAN-UAT.md &amp;&amp; grep -q "^total: 11$" .planning/phases/35-upst3-closure-quick-wins/35-HUMAN-UAT.md &amp;&amp; grep -q "  previous_status: human_needed$" .planning/phases/35-upst3-closure-quick-wins/35-VERIFICATION.md &amp;&amp; grep -qE "trigger: \"Phase 46 Plan 46-03 backfill" .planning/phases/35-upst3-closure-quick-wins/35-VERIFICATION.md &amp;&amp; git log -1 --format="%s" | grep -q "^docs(46-03): backfill Phase 35 HUMAN-UAT"</automated>
  </verify>
  <acceptance_criteria>
    - `35-HUMAN-UAT.md` exists with frontmatter `status: passed` + `scenarios: 11` + `backfilled_in: phase-46-plan-46-03` + `backfill_rationale:` referencing v2.4 close + D-46-C3
    - `35-HUMAN-UAT.md` body has `## Current Test`, `## Tests`, `## Summary`, `## Gaps` sections
    - `## Tests` section has 11 numbered `###` items (count via grep `^### [0-9]`)
    - `## Summary` block has `total: 11` and breakdown lines: passed, issues, pending, skipped, blocked, no-test-fixture
    - `35-VERIFICATION.md` exists with frontmatter `status: passed` + `re_verification:` block with `previous_status: human_needed` + `trigger:` citing Phase 46 Plan 46-03 + `gaps_closed:` list (~11 items) + `gaps_remaining: []` + `backfilled_in: phase-46-plan-46-03`
    - `35-VERIFICATION.md` body has H2 sections: Goal Achievement, Requirements Coverage, Human Verification Required, Gaps Summary
    - `35-VERIFICATION.md` Requirements Coverage table has rows for REQ-PORT-CLOSURE-01, REQ-PORT-CLOSURE-06, REQ-PORT-CLOSURE-07
    - Latest commit subject is `docs(46-03): backfill Phase 35 HUMAN-UAT + VERIFICATION (REQ-UAT-BL-01 partial)` with DCO sign-off
  </acceptance_criteria>
  <done>Phase 35 HUMAN-UAT.md + VERIFICATION.md backfilled with per-item verdicts from CI run + pre-pass markers + no-test-fixture waiver pointers; `status: human_needed → passed` transition recorded per D-46-C4; atomic commit with DCO sign-off.</done>
</task>

<task type="auto">
  <name>Task 3: Backfill Phase 36 HUMAN-UAT.md + VERIFICATION.md (per D-46-C4)</name>
  <files>
    .planning/phases/36-upst3-deep-closure/36-HUMAN-UAT.md
    .planning/phases/36-upst3-deep-closure/36-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-CONTEXT.md § D-46-C4
    - .planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-PATTERNS.md § `36-HUMAN-UAT.md` + § `36-VERIFICATION.md` (same pattern as 35 — substitutions called out: 7 verification items target, docs MDX bypass_protection pre-passed, REQ-PORT-CLOSURE-02/04/05 coverage)
    - .planning/phases/36-upst3-deep-closure/36-01a-SUMMARY.md (if exists — read for REQ-PORT-CLOSURE-02 evidence)
    - .planning/phases/36-upst3-deep-closure/36-02-SUMMARY.md (if exists)
    - .planning/phases/36-upst3-deep-closure/36-03-SUMMARY.md (if exists)
    - Task 1 inventory table output (the 7 Phase 36 verification items)
    - Task 1 captured `$UAT_RUN_ID`
  </read_first>
  <action>
**Step 0 — cwd guard:**

```bash
cd /c/Users/OMack/Nono && pwd && git branch --show-current
```

**Step 1 — Reuse Task 2 pattern with Phase 36 substitutions:**

For `36-HUMAN-UAT.md`:
- `phase: 36-upst3-deep-closure`
- `scenarios: 7`
- `total: 7`
- Source per-plan SUMMARYs: `36-01a-SUMMARY.md` through `36-03-SUMMARY.md`
- Pre-passed item (1 of 7 per v2.4-MILESTONE-AUDIT rows 116-121): docs MDX bypass_protection
- Remaining 6 items: derive verdicts from Task 1 workflow run + no-test-fixture waivers per D-46-C3

For `36-VERIFICATION.md`:
- `phase: 36-upst3-deep-closure`
- Requirements coverage: REQ-PORT-CLOSURE-02 (36-01a deprecated_schema port), REQ-PORT-CLOSURE-04 (36-02 yaml_merge wiring), REQ-PORT-CLOSURE-05 (36-03 ExecConfig refactor) — verify exact mapping by reading the 36-*-SUMMARY.md files
- `score: <N>/7 verification items verified (<M> items waived per `no-test-fixture` per D-46-C3)`

Target per D-46-C3: at least 5/7 pass, ≤2 waived.

Use the EXACT same template shapes as Task 2's 35-HUMAN-UAT.md / 35-VERIFICATION.md (substitutions only — no schema divergence).

**Step 2 — Commit both files** as a single atomic commit with DCO sign-off:

```
docs(46-03): backfill Phase 36 HUMAN-UAT + VERIFICATION (REQ-UAT-BL-02 partial)

Per D-46-C4, backfills the two files that were absent at v2.4 close
(human_needed deferred to v2.6 native host). Records per-item verdicts
from phase-46-uat-backlog.yml CI run-id <UAT_RUN_ID> + pre-passed item
(docs MDX bypass_protection per v2.4-MILESTONE-AUDIT rows 116-121) +
no-test-fixture waivers per D-46-C3 (rationale in 46-03-SUMMARY).
Phase 36 VERIFICATION.md status: human_needed → passed (verbatim SC#5
transition language).

Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>
Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```
  </action>
  <verify>
    <automated>test -f .planning/phases/36-upst3-deep-closure/36-HUMAN-UAT.md &amp;&amp; test -f .planning/phases/36-upst3-deep-closure/36-VERIFICATION.md &amp;&amp; grep -q "^status: passed$" .planning/phases/36-upst3-deep-closure/36-HUMAN-UAT.md &amp;&amp; grep -q "^status: passed$" .planning/phases/36-upst3-deep-closure/36-VERIFICATION.md &amp;&amp; grep -q "^backfilled_in: phase-46-plan-46-03$" .planning/phases/36-upst3-deep-closure/36-HUMAN-UAT.md &amp;&amp; grep -q "^backfilled_in: phase-46-plan-46-03$" .planning/phases/36-upst3-deep-closure/36-VERIFICATION.md &amp;&amp; grep -q "^scenarios: 7$" .planning/phases/36-upst3-deep-closure/36-HUMAN-UAT.md &amp;&amp; grep -q "^total: 7$" .planning/phases/36-upst3-deep-closure/36-HUMAN-UAT.md &amp;&amp; grep -q "  previous_status: human_needed$" .planning/phases/36-upst3-deep-closure/36-VERIFICATION.md &amp;&amp; git log -1 --format="%s" | grep -q "^docs(46-03): backfill Phase 36 HUMAN-UAT"</automated>
  </verify>
  <acceptance_criteria>
    - `36-HUMAN-UAT.md` exists with frontmatter `status: passed` + `scenarios: 7` + `backfilled_in: phase-46-plan-46-03`
    - `36-HUMAN-UAT.md` `## Tests` section has 7 numbered `###` items
    - `36-HUMAN-UAT.md` `## Summary` block has `total: 7`
    - `36-VERIFICATION.md` exists with frontmatter `status: passed` + `re_verification:` block with `previous_status: human_needed` + `gaps_closed:` list of ~7 items
    - `36-VERIFICATION.md` Requirements Coverage table has rows for REQ-PORT-CLOSURE-02, REQ-PORT-CLOSURE-04, REQ-PORT-CLOSURE-05
    - Latest commit subject is `docs(46-03): backfill Phase 36 HUMAN-UAT + VERIFICATION (REQ-UAT-BL-02 partial)` with DCO sign-off
  </acceptance_criteria>
  <done>Phase 36 HUMAN-UAT.md + VERIFICATION.md backfilled with same pattern as Task 2; `status: human_needed → passed`; atomic commit with DCO sign-off.</done>
</task>

<task type="auto">
  <name>Task 4: Write 46-03-SUMMARY.md with no-test-fixture waivers + flip REQUIREMENTS.md REQ-UAT-BL-01..02</name>
  <files>
    .planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-03-SUMMARY.md
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-CONTEXT.md § D-46-C3 (waiver shape + target counts)
    - .planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-PATTERNS.md § REQUIREMENTS.md (per-plan ownership — Plan 46-03 flips REQ-UAT-BL-01 + REQ-UAT-BL-02)
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY shape)
    - .planning/phases/35-upst3-closure-quick-wins/35-HUMAN-UAT.md (Task 2 output — needed for per-item verdict cross-reference)
    - .planning/phases/36-upst3-deep-closure/36-HUMAN-UAT.md (Task 3 output)
    - .planning/REQUIREMENTS.md (current state of REQ-UAT-BL-01..02 lines 26-27 + Traceability table)
    - Task 1 captured `$UAT_RUN_ID`
  </read_first>
  <action>
**Step 0 — cwd guard:**

```bash
cd /c/Users/OMack/Nono && pwd && git branch --show-current
```

**Step 1 — Write `46-03-SUMMARY.md`** at `.planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-03-SUMMARY.md`:

Frontmatter:

```yaml
# ---
phase: 46-windows-squash-merge-post-merge-ci-verifications-uat-backlog
plan: 03
closed: 2026-05-DD
requirements_closed: [REQ-UAT-BL-01, REQ-UAT-BL-02]
status: complete
commits: <to-be-filled-by-final-commit-sha>
workflow_run:
  workflow: .github/workflows/phase-46-uat-backlog.yml
  run_id: <UAT_RUN_ID>
  inputs: { gh_runner_os: both }
  conclusion: <green | mixed-with-no-test-fixture-waivers>
inventory:
  phase_35_uat_total: 11
  phase_35_uat_pass: <N>
  phase_35_uat_pre_passed: 2  # env_filter_tests + profile_cli debug-syntax per v2.4-MILESTONE-AUDIT rows 116-121
  phase_35_uat_no_test_fixture: <M>
  phase_36_verif_total: 7
  phase_36_verif_pass: <N>
  phase_36_verif_pre_passed: 1  # docs MDX bypass_protection
  phase_36_verif_no_test_fixture: <M>
files_created:
  - .github/workflows/phase-46-uat-backlog.yml
  - .planning/phases/35-upst3-closure-quick-wins/35-HUMAN-UAT.md
  - .planning/phases/35-upst3-closure-quick-wins/35-VERIFICATION.md
  - .planning/phases/36-upst3-deep-closure/36-HUMAN-UAT.md
  - .planning/phases/36-upst3-deep-closure/36-VERIFICATION.md
  - .planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-03-SUMMARY.md
files_modified:
  - .planning/REQUIREMENTS.md
# ---
```

Body sections:

- `## Outcome` — 2-3 sentences: tactical workflow_dispatch-only workflow shipped; 18 total items (11 UAT + 7 verif) inventoried; per-item verdicts recorded in backfilled `35/36-HUMAN-UAT.md` + `VERIFICATION.md`; `status: human_needed → passed` transition completed for both Phase 35 + 36 per D-46-C4.

- `## Inventory + Disposition Table` — 18-row table: # / Phase / UAT-or-Verif / Description / Source SUMMARY / Disposition (pass / pre-pass / no-test-fixture / waived-rationale-heading-link). Mirrors the inventory table built in Task 1.

- `## No-Test-Fixture Waivers (per D-46-C3)` — one H3 subsection per waived item (these are the headings cited in the verdict rows of `35/36-HUMAN-UAT.md` `result:` lines):
  - `### Item N — <description>`
    - **Source:** <Phase 35 or 36 source SUMMARY citation>
    - **Why not automatable:** <e.g., "Requires interactive consent prompt approval — no headless automation surface" / "ETW capture requires manual ETL trace start/stop sequence" / "Requires manual nono setup wizard re-run to validate diagnostic copy text changes">
    - **Audit verdict:** `no-test-fixture` per SC#5 explicit allowance
    - **Future re-execution:** <e.g., "Re-evaluate at v3.0 if EDR-instrumented runner ships (WR-02-EDR)" / "Defer to manual smoke-test next time user is at native Linux/macOS host">

- `## Workflow Run Attribution` — Per-job conclusion from Task 1's `$UAT_RUN_ID`: Linux job + macOS job conclusions, log excerpt for failed items (if any), evidence link to the run URL.

- `## Cross-References` — links to: ROADMAP § Phase 46 SC#5, REQUIREMENTS.md § REQ-UAT-BL-01..02, `35-HUMAN-UAT.md`, `35-VERIFICATION.md`, `36-HUMAN-UAT.md`, `36-VERIFICATION.md`, `.github/workflows/phase-46-uat-backlog.yml`.

**Step 2 — Flip REQUIREMENTS.md REQ-UAT-BL-01 + REQ-UAT-BL-02** (2 checkbox flips + 2 Traceability table updates per PATTERNS.md per-plan ownership):

Line 26: `- [ ] **REQ-UAT-BL-01**` → `- [x] **REQ-UAT-BL-01**`
Line 27: `- [ ] **REQ-UAT-BL-02**` → `- [x] **REQ-UAT-BL-02**`

Traceability table: 2 rows for REQ-UAT-BL-01 + REQ-UAT-BL-02 update `Pending` → `Complete`.

**Step 3 — Commit both files** as a single atomic commit with DCO sign-off:

```
docs(46-03): close REQ-UAT-BL-01..02 + roll up Phase 35/36 UAT drain

Per D-46-C3 + D-46-C4, writes 46-03-SUMMARY.md with the canonical 11+7
inventory + per-item disposition table + no-test-fixture waiver rationale
(one H3 subsection per waived item) + workflow_run attribution
(.github/workflows/phase-46-uat-backlog.yml run-id <UAT_RUN_ID>). Flips
REQUIREMENTS.md REQ-UAT-BL-01 + REQ-UAT-BL-02 [ ] → [x] per per-plan
atomic ownership.

Phase 35 + 36 VERIFICATION.md status: human_needed → passed transition
recorded in Tasks 2 + 3 (separate atomic commits); this commit closes
the REQ checkboxes and lands the roll-up.

Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>
Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

After commit, capture the commit SHA and back-fill `46-03-SUMMARY.md` frontmatter `commits:` field if it's still a placeholder, then `git commit --amend --no-edit`.
  </action>
  <verify>
    <automated>test -f .planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-03-SUMMARY.md &amp;&amp; grep -q "^requirements_closed: \[REQ-UAT-BL-01, REQ-UAT-BL-02\]$" .planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-03-SUMMARY.md &amp;&amp; grep -q "^  workflow: .github/workflows/phase-46-uat-backlog.yml$" .planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-03-SUMMARY.md &amp;&amp; grep -q "^  phase_35_uat_total: 11$" .planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-03-SUMMARY.md &amp;&amp; grep -q "^  phase_36_verif_total: 7$" .planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-03-SUMMARY.md &amp;&amp; grep -q "^## No-Test-Fixture Waivers (per D-46-C3)$" .planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-03-SUMMARY.md &amp;&amp; grep -q "^## Inventory + Disposition Table$" .planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-03-SUMMARY.md &amp;&amp; grep -E "^- \[x\] \*\*REQ-UAT-BL-01\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^- \[x\] \*\*REQ-UAT-BL-02\*\*" .planning/REQUIREMENTS.md &amp;&amp; ! grep -E "^- \[ \] \*\*REQ-UAT-BL-0[12]\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^\| REQ-UAT-BL-01 \| Phase 46 \| Complete" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^\| REQ-UAT-BL-02 \| Phase 46 \| Complete" .planning/REQUIREMENTS.md &amp;&amp; git log -1 --format="%s" | grep -q "^docs(46-03): close REQ-UAT-BL-01..02"</automated>
  </verify>
  <acceptance_criteria>
    - `46-03-SUMMARY.md` exists with required frontmatter (phase, plan, closed, requirements_closed, status, commits, workflow_run block, inventory block, files_created, files_modified)
    - `workflow_run.workflow` field is exactly `.github/workflows/phase-46-uat-backlog.yml`
    - `inventory.phase_35_uat_total: 11` and `inventory.phase_36_verif_total: 7` (canonical totals)
    - Body contains H2 sections: Outcome, Inventory + Disposition Table, No-Test-Fixture Waivers (per D-46-C3), Workflow Run Attribution, Cross-References
    - Inventory + Disposition Table has 18 data rows (11 Phase 35 UAT + 7 Phase 36 verification)
    - No-Test-Fixture Waivers section has one `### Item N —` subsection per waived item (count matches `phase_35_uat_no_test_fixture` + `phase_36_verif_no_test_fixture` from frontmatter)
    - REQUIREMENTS.md REQ-UAT-BL-01 + REQ-UAT-BL-02 both show `[x]`; no `[ ]` versions remain
    - Traceability table rows for REQ-UAT-BL-01 + REQ-UAT-BL-02 both show `Complete`
    - Latest commit subject is `docs(46-03): close REQ-UAT-BL-01..02 + roll up Phase 35/36 UAT drain` with DCO sign-off
    - Latest commit touches exactly 2 files (46-03-SUMMARY.md + REQUIREMENTS.md)
  </acceptance_criteria>
  <done>46-03-SUMMARY.md roll-up landed with inventory + disposition + waiver rationale + workflow run attribution; REQUIREMENTS.md REQ-UAT-BL-01..02 atomic flip committed; per-plan ownership semantics preserved.</done>
</task>

</tasks>

<verification>
- `.github/workflows/phase-46-uat-backlog.yml` exists with workflow_dispatch-only trigger + `gh_runner_os` choice input + 2 jobs (Linux + macOS) + continue-on-error + SHA-pinned actions.
- Phase 35 HUMAN-UAT.md + VERIFICATION.md exist; `status: passed`; `backfilled_in: phase-46-plan-46-03`; `scenarios: 11` + `total: 11`; 11 numbered `###` items.
- Phase 36 HUMAN-UAT.md + VERIFICATION.md exist; `status: passed`; `backfilled_in: phase-46-plan-46-03`; `scenarios: 7` + `total: 7`; 7 numbered `###` items.
- Both VERIFICATION.md files carry `re_verification:` block with `previous_status: human_needed` + `trigger:` citing Phase 46 Plan 46-03.
- `46-03-SUMMARY.md` exists with `inventory.phase_35_uat_total: 11` + `phase_36_verif_total: 7` + per-item disposition table + `## No-Test-Fixture Waivers (per D-46-C3)` H2 with per-item H3 subsections.
- REQUIREMENTS.md REQ-UAT-BL-01..02 both `[x]`; Traceability rows show `Complete`.
- 4 commits land with DCO sign-off (Task 1 workflow YAML, Task 2 Phase 35 backfill, Task 3 Phase 36 backfill, Task 4 SUMMARY + REQUIREMENTS flip).
- No source-tree edits (workflow YAML is not a source file per `.github/workflows/` convention); no cross-target clippy required per CONTEXT.md `<canonical_refs>`.
- `cd /c/Users/OMack/Nono && pwd && git branch --show-current` recovery dance applied between gh invocations per memory `feedback_windows_worktree_cwd`.
</verification>

<success_criteria>
ROADMAP SC#5: All 11 Phase 35 + 36 human-UAT scenarios (REQ-UAT-BL-01) reach `pass` or carry a documented `no-test-fixture` waiver; all 7 verification items (REQ-UAT-BL-02) land with a verdict; Phase 35 + 36 HUMAN-UAT.md and VERIFICATION.md transition out of `human_needed` state (verbatim SC#5 transition language). Target counts honored: ≥8/11 pass + ≤3 waived for UAT; ≥5/7 pass + ≤2 waived for verification. Per-plan REQ checkbox-flip atomic ownership preserved (Plan 46-03 flips only REQ-UAT-BL-01 + REQ-UAT-BL-02).
</success_criteria>

<output>
After completion, `.planning/phases/46-windows-squash-merge-post-merge-ci-verifications-uat-backlog/46-03-SUMMARY.md` exists with full 11+7 inventory, per-item disposition table, no-test-fixture waiver rationale, and workflow run attribution. Phase 35 + 36 dirs each carry HUMAN-UAT.md + VERIFICATION.md `status: passed` files backfilled per D-46-C4.
</output>
