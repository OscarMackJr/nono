---
phase: 77-copilot-cli-end-to-end-confinement
plan: 03
type: execute
wave: 3
depends_on: ["77-01", "77-02"]
files_modified:
  - scripts/gates/copilot-e2e.ps1
  - proj/DESIGN-engine-abstraction.md
autonomous: false
requirements: [CPLT-03]
must_haves:
  truths:
    - "scripts/verify-dark.ps1 --gate copilot-e2e emits PASS on a host where the one-time-admin grant has been run, Copilot is installed + authenticated + online, and a real confined copilot suggestion prints output with zero STATUS_ACCESS_DENIED and zero Node module-resolution crash."
    - "The gate emits SKIP_HOST_UNAVAILABLE (not FAIL) when Copilot is not installed, not authenticated, or offline (D-07)."
    - "The gate distinguishes a confinement FAIL from a Copilot/auth/network SKIP (D-08) and never masks a real STATUS_ACCESS_DENIED as a SKIP."
  artifacts:
    - path: "scripts/gates/copilot-e2e.ps1"
      provides: "Test-Precondition + Invoke-Gate two-function dark-factory gate for end-to-end Copilot confinement"
      contains: "function Invoke-Gate"
  key_links:
    - from: "scripts/gates/copilot-e2e.ps1 Invoke-Gate"
      to: "nono run --profile copilot-cli -- copilot <one-shot>"
      via: "the gate runs a confined one-shot Copilot task and asserts no STATUS_ACCESS_DENIED / module-resolution crash"
      pattern: "nono run --profile copilot-cli"
    - from: "scripts/verify-dark.ps1"
      to: "scripts/gates/copilot-e2e.ps1"
      via: "runner auto-discovers the gate by base name and owns emit/persist/exit mapping"
      pattern: "copilot-e2e"
---

<objective>
Deliver the CPLT-03 unattended scripted gate: `scripts/gates/copilot-e2e.ps1`, dot-sourced by
the Phase 76 runner `scripts/verify-dark.ps1 --gate copilot-e2e`, which proves the standalone
`@github/copilot` Node CLI completes a real task end-to-end under AppContainer confinement —
replacing the interactive v2.12 Phase 75 SC3 UAT.

The gate treats Copilot install + GitHub auth + network as a PRECONDITION (→ `SKIP_HOST_UNAVAILABLE`
if absent, D-07), and asserts confinement behavior — zero `STATUS_ACCESS_DENIED` and zero
Node module-resolution crash under AppContainer — as the PASS condition (D-08). It distinguishes
a confinement FAIL from an unrelated Copilot/auth/network SKIP.

Purpose: This is the dark-factory verdict that closes CPLT-03 and the whole phase: it exercises
the CPLT-01 runtime ancestor-RA grant and depends on the CPLT-02 one-time-admin grant having
been run on the host.
Output: The `copilot-e2e` gate file + a documentation note recording the permanent, non-destructive
admin grant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-copilot-cli-end-to-end-confinement/77-CONTEXT.md
@.planning/phases/77-copilot-cli-end-to-end-confinement/77-RESEARCH.md
@.planning/phases/77-copilot-cli-end-to-end-confinement/77-PATTERNS.md
@.planning/phases/77-copilot-cli-end-to-end-confinement/77-01-SUMMARY.md
@.planning/phases/77-copilot-cli-end-to-end-confinement/77-02-SUMMARY.md
@scripts/gates/harness-self-check.ps1
@scripts/verify-dark.ps1

<interfaces>
<!-- Contracts the executor uses directly — the Phase 76 gate contract; no exploration needed. -->

Runner contract (scripts/verify-dark.ps1):
- Auto-discovers gates from `scripts/gates/<name>.ps1` (no ValidateSet). `--gate copilot-e2e` dot-sources `copilot-e2e.ps1`.
- The gate exports exactly two functions: `Test-Precondition` and `Invoke-Gate`. The gate NEVER calls `exit` — the runner owns exit mapping.
- `Build-Verdict` (lines 29-44) — verdict object locked key order: `gate, verdict, reason, detail, timestamp`.
- Exit mapping: PASS=0, FAIL=2, SKIP_HOST_UNAVAILABLE=3, harness-internal error (thrown / unknown gate / persist failure)=4.
- `Persist-Verdict` (lines 52+) writes `.nono-runtime/verdicts/copilot-e2e.json` BEFORE the stdout emit (WR-04). The gate must NOT duplicate persist/exit logic.

Gate contract (scripts/gates/harness-self-check.ps1 — the reference contract for phases 77-80):
- `function Test-Precondition` — return `$null` → preconditions met, run Invoke-Gate; return a `"reason string"` → SKIP_HOST_UNAVAILABLE (exit 3), Invoke-Gate NEVER runs.
- `function Invoke-Gate` — return an `[ordered]@{ gate; verdict; reason; detail; timestamp }` dict; verdict ∈ { 'PASS' | 'FAIL' | 'SKIP_HOST_UNAVAILABLE' }; a `throw` here = harness-internal error (exit 4), never a silent PASS.
- Assertion helpers `Assert-Equal` / `Assert-True` (lines 25-54) — throw on failure.

CPLT-01 / CPLT-02 produced (this phase, prior plans):
- `copilot-cli` profile now declares `windows_interpreters: ["node.exe"]` (requires `make build` to embed — the gate host must run a build that includes the profile edit).
- The runtime ancestor-RA guard grants RA on user-owned ancestors at launch; `nono setup --grant-ancestors --profile copilot-cli` (run once, elevated) grants the well-known SID RA on `C:\` / `C:\Users`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/gates/copilot-e2e.ps1 (Test-Precondition + Invoke-Gate)</name>
  <files>scripts/gates/copilot-e2e.ps1</files>
  <read_first>
    - scripts/gates/harness-self-check.ps1 — the FULL file (the reference contract: the two-function shape, `Assert-Equal`/`Assert-True` at 25-54, the verdict-dict construction at ~73-79, no `exit`).
    - scripts/verify-dark.ps1 — `Build-Verdict` (29-44, key order), `Persist-Verdict` (52+), the exit-code mapping comment block (1-10, 46-50).
    - 77-PATTERNS.md § "scripts/gates/copilot-e2e.ps1 — NEW gate" (the skeleton, verdict key order, determinism note).
    - 77-RESEARCH.md § Pitfall 2 (shim coverage / %APPDATA%\npm), Pitfall 4 (non-interactive one-shot mode + timeout), and OQ-3 (settle the one-shot flag + shim coverage on the live host).
  </read_first>
  <action>
    Create `scripts/gates/copilot-e2e.ps1` copying the `harness-self-check.ps1` structure exactly.
    `Test-Precondition` (D-07): return a reason string (→ SKIP) if `Get-Command copilot` is absent; probe GitHub auth + network with a cheap authed status call and return a reason string if not authenticated/offline; return `$null` when all preconditions are met. NEVER conflate a missing/unauthenticated Copilot with a confinement FAIL.
    `Invoke-Gate` (D-08): run a SINGLE confined ONE-SHOT Copilot task under nono — `nono run --profile copilot-cli -- copilot <one-shot prompt flag> "list files"` (confirm Copilot's exact non-interactive/one-shot flag on the live host per OQ-3; apply a timeout so a REPL cannot hang the unattended harness). Capture stdout+stderr. Assert (FAIL on violation): no `STATUS_ACCESS_DENIED` / `Access is denied` in output; no Node module-resolution crash (`Cannot find module`, `ERR_MODULE_NOT_FOUND`, ENOENT on a module path); Copilot printed a real non-empty suggestion. If the nono invocation is refused at the executable-coverage gate for an uncovered `copilot` shim path (`%APPDATA%\npm` / `%APPDATA%\npm\node_modules`), settle the gate's nono `--allow` coverage empirically on-host per OQ-3 — prefer resolving the package entry and invoking `node <copilot-entry>` directly OR adding the shim dirs to the gate's nono invocation. Return `[ordered]@{ gate='copilot-e2e'; verdict=$verdict; reason=$reason; detail=$detail; timestamp=(Get-Date -Format 'yyyy-MM-ddTHH:mm:ss.fffZ') }` with the locked key order. Use `Assert-Equal`/`Assert-True`-style helpers that `throw` on failure (a throw = harness-internal error, exit 4 — never a silent PASS). Do NOT call `exit`; do NOT duplicate persist logic.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; . ./scripts/gates/copilot-e2e.ps1; if ((Get-Command Test-Precondition -ErrorAction SilentlyContinue) -and (Get-Command Invoke-Gate -ErrorAction SilentlyContinue)) { 'contract-ok' } else { throw 'missing required gate functions' }"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/gates/copilot-e2e.ps1` defines `function Test-Precondition` and `function Invoke-Gate`, and contains no `exit` statement.
    - Dot-sourcing the file exposes both functions (the `contract-ok` automated check passes).
    - The returned verdict dict uses the locked key order `gate, verdict, reason, detail, timestamp` and a verdict in `{PASS, FAIL, SKIP_HOST_UNAVAILABLE}`.
    - `Test-Precondition` returns a reason string (SKIP path) for missing copilot / no auth / offline; `Invoke-Gate` asserts no `STATUS_ACCESS_DENIED` and no Node module-resolution crash.
  </acceptance_criteria>
  <done>The gate file exists, exports the two-function contract, never calls exit, and produces a correctly-shaped verdict — the contract-load automated check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Document the permanent non-destructive admin grant</name>
  <files>proj/DESIGN-engine-abstraction.md</files>
  <read_first>
    - proj/DESIGN-engine-abstraction.md — find the section covering engine launch coverage / Windows confinement so the grant note lands in the right place.
    - 77-CONTEXT.md D-09 (permanent, documented, non-destructive, no --revoke this phase) and 77-RESEARCH.md § Runtime State Inventory (OS-registered state row — permanent allow-ACE persists after uninstall).
  </read_first>
  <action>
    Add a short documentation note to `proj/DESIGN-engine-abstraction.md` recording that `nono setup --grant-ancestors --profile <p>` adds a PERMANENT, NON-DESTRUCTIVE allow-ACE granting the well-known `ALL APPLICATION PACKAGES` SID (`S-1-15-2-1`) `FILE_READ_ATTRIBUTES` on `C:\` and `C:\Users`; that it is one-time-admin, idempotent, attribute-read only (not content), adds one allow-ACE per ancestor, alters/removes no existing ACE and no deny-ACE, persists after uninstall, and has no `--revoke` counterpart in this phase (D-09). State why it is required (Node-ESM `realpathSync`/`lstat` ancestor walk under AppContainer needs RA up to drive root; nono cannot ACL the system ancestors at runtime as a non-elevated user — the D-04 split). No fenced implementation code — directive prose only.
  </action>
  <verify>
    <automated>grep -F "S-1-15-2-1" proj/DESIGN-engine-abstraction.md && grep -iF "non-destructive" proj/DESIGN-engine-abstraction.md</automated>
  </verify>
  <acceptance_criteria>
    - `proj/DESIGN-engine-abstraction.md` documents the grant: well-known SID `S-1-15-2-1`, `FILE_READ_ATTRIBUTES`-only, `C:\`/`C:\Users` only, permanent + non-destructive, one-time-admin, idempotent, no `--revoke` this phase.
    - The grep automated check finds both the SID string and the non-destructive language.
  </acceptance_criteria>
  <done>The permanent admin grant is documented as non-destructive with its rationale and scope in the engine-abstraction design doc.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: [BLOCKING] Host-gated end-to-end Copilot confinement proof (elevated + Win11 + authenticated Copilot)</name>
  <action>Operator-run host-gated verification (NOT unattended / NOT non-elevated — cannot run in CI). On a provisioned Win11 host, run the steps in how-to-verify and record the verdict. This gate exercises the CPLT-01 runtime ancestor-RA grant and consumes the CPLT-02 one-time-admin grant as a precondition.</action>
  <what-built>
    The full Phase 77 confinement stack is in place: the CPLT-01 runtime ancestor-RA guard + node.exe profile coverage, the CPLT-02 one-time-admin `nono setup --grant-ancestors` command, and the CPLT-03 `copilot-e2e` gate. This checkpoint is the host-gated end-to-end proof that CANNOT run unattended/non-elevated in CI — it requires a real Win11 host, an elevated session for the one-time grant, an installed + authenticated `@github/copilot` CLI, and network.
  </what-built>
  <how-to-verify>
    On a real Win11 host:
    1. `make build` (embeds the updated copilot-cli profile with node.exe coverage).
    2. Install + authenticate Copilot: `npm install -g @github/copilot` and complete GitHub auth; confirm `copilot` is on PATH and authenticated.
    3. Run the one-time-admin grant ONCE in an ELEVATED session: `nono setup --grant-ancestors --profile copilot-cli` — expect success; re-run it to confirm idempotency (no duplicate ACE, no error).
    4. Run the gate from a normal (non-elevated) session: `pwsh scripts/verify-dark.ps1 --gate copilot-e2e`.
    Expected:
    - PASS (exit 0) — a real confined copilot suggestion printed output with zero `STATUS_ACCESS_DENIED` and zero Node module-resolution crash under AppContainer (CPLT-03 / D-08).
    - If Copilot is not installed / not authenticated / offline → SKIP_HOST_UNAVAILABLE (exit 3), NOT FAIL (D-07). A SKIP is acceptable to record but a real PASS on a provisioned host is required to fully close CPLT-03.
    - Confirm `.nono-runtime/verdicts/copilot-e2e.json` was written.
    If the gate FAILs on an uncovered copilot shim path (executable-coverage refusal) or a residual `STATUS_ACCESS_DENIED` on the ancestor walk, settle OQ-3 (shim/%APPDATA%\npm coverage) and OQ-2 (widen the RA mask only if the walk still denies) on-host, then re-run.
  </how-to-verify>
  <resume-signal>Type "approved" with the recorded verdict (PASS or SKIP_HOST_UNAVAILABLE + reason), or describe the FAIL for gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unattended gate verdict → milestone close signal | A false-PASS here would let a real confinement failure ship as "closed"; a mis-classified SKIP would hide it. The gate's verdict is the load-bearing dark-factory signal (DARK mandate). |
| confined Copilot child → host filesystem | The gate exercises the real AppContainer confinement; the assertion is that no STATUS_ACCESS_DENIED escapes and no module-resolution crash occurs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-03 | Repudiation / Spoofing (false-PASS) | gate verdict logic | mitigate | `Invoke-Gate` asserts a real non-empty suggestion AND zero `STATUS_ACCESS_DENIED` AND zero module-resolution crash before PASS; a `throw` maps to harness-internal error (exit 4), never a silent PASS; the runner persists-before-emit (WR-04). |
| T-77-03b | Tampering (masking a real denial as SKIP) | precondition vs assertion split | mitigate | `Test-Precondition` only detects missing copilot / no auth / offline (→ SKIP, D-07); a `STATUS_ACCESS_DENIED` during the confined run is asserted in `Invoke-Gate` as a FAIL (D-08) — the two paths are kept structurally separate per the contract. |
| T-77-03c | Denial of Service (unattended hang) | one-shot determinism | mitigate | Use Copilot's non-interactive one-shot mode + a timeout so a REPL cannot hang the harness (Pitfall 4). |
| T-77-03d | Elevation of Privilege | the one-time admin grant precondition | accept (handled in Plan 02) | The grant is admin-gated, attribute-read only, well-known-SID scoped, non-destructive (Plan 02 T-77-02); this gate consumes it as a host precondition and runs non-elevated. |
| T-77-SC | Tampering | npm install -g @github/copilot | accept | Copilot is installed by the operator as a host precondition, external to nono's build; nono never handles the GitHub token (precondition probe only). No package added to the nono workspace. |
</threat_model>

<verification>
- PowerShell contract load check: dot-sourcing `copilot-e2e.ps1` exposes `Test-Precondition` + `Invoke-Gate` and the file contains no `exit`.
- Documentation grep: the design doc records the well-known SID + non-destructive grant.
- **[BLOCKING / host-gated, not unattended in CI]** The real PASS/SKIP verdict requires the human-verify checkpoint above on a provisioned Win11 host (elevated grant + authenticated Copilot + network). `SKIP_HOST_UNAVAILABLE` is an acceptable recorded verdict where Copilot/auth/network is absent; a real PASS is required to fully close CPLT-03 (D-08). This plan is `autonomous: false`.
- No cfg-gated Rust is touched in this plan (PowerShell + docs only) — no cross-target clippy obligation here; the Rust obligations are discharged in Plans 01/02.
</verification>

<success_criteria>
- ROADMAP SC1 + SC4 satisfied: the standalone `copilot` suggestion completes under confinement with no `STATUS_ACCESS_DENIED` / module-resolution crash, proven by `scripts/verify-dark.ps1 --gate copilot-e2e` emitting PASS on a provisioned host (or a clearly-reasoned SKIP_HOST_UNAVAILABLE otherwise).
- The gate discriminates confinement FAIL from Copilot/auth/network SKIP (D-07/D-08) and never masks a real denial.
- The permanent admin grant is documented non-destructive (D-09).
</success_criteria>

<output>
Create `.planning/phases/77-copilot-cli-end-to-end-confinement/77-03-SUMMARY.md` when done
</output>
