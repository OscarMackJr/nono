---
phase: 94-upst10-divergence-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md
autonomous: true
requirements: [UPST10-01]
must_haves:
  truths:
    - "Decisions covered (CONTEXT.md): D-01: audit window 0153757001..1d1c88c9 (v0.64.0..v0.65.1) recorded in ledger header; D-02: clean-continuation byte-identical v0.64.0 anchor proof in Reproduction; D-03: drift tool run against explicit window SHAs not tag names; D-04: mandatory Carve-out Re-touch Check section; D-05: zero-hit carve-out results stated explicitly as clean; D-09: uniform actual-diff git show on every substantive commit plus re-export scan; D-10: release/version commits in won't-sync with one-line Phase-97 leapfrog-floor cross-ref; D-11: drift tool re-run plus ~6-file spot-check, tool SHA pin and invocation in Reproduction; D-12: fresh A,B,C cluster lettering independent of Phase 85; D-13: substantive plus noise equals total commit count reconciliation; D-14: per-cluster five-dimension ADR risk matrix, no bare TBD; D-15: cross-target clippy MUST note flagged for will-sync/split cfg(linux)/cfg(macos) commits."
    - "A DIVERGENCE-LEDGER exists for the nolabs-ai/nono v0.64.0..v0.65.1 window (covering v0.64.1, v0.65.0, v0.65.1) [SC1, UPST10-01]"
    - "Every substantive commit in the window is classified into exactly one of will-sync / fork-preserve / won't-sync / split, with a windows-touch flag and a per-cell ADR verdict [SC1, D-14]"
    - "The ledger records the D-02 clean-continuation byte-identical v0.64.0 anchor proof in its Reproduction block [D-02, specifics]"
    - "The drift tool was run against explicit window SHAs (0153757001..1d1c88c9), not tag names, with the tool SHA pin and exact invocation recorded [D-03, D-11]"
    - "A dedicated Carve-out Re-touch Check section records git log <window> -- <exact path> results for CR-02, CR-01, and Cluster F, with zero-hit results stated explicitly as clean [D-04, D-05]"
    - "Every substantive commit was inspected via git show actual-diff including a cross-cluster re-export scan [D-09]"
    - "Release/version commits are in won't-sync with a one-line Phase-97 leapfrog-floor (>= 0.65.0) cross-ref [D-10]"
    - "substantive + noise = total window commit count, with merge and out-of-filter commits enumerated [D-13]"
    - "Each cluster's disposition is justified by security impact, Windows-backend touch, or library-boundary relevance — no bare TBD verdict [SC3, D-14]"
    - "will-sync/split commits touching cfg(linux)/cfg(macos) blocks carry a CLAUDE.md cross-target clippy MUST note for the Phase 95 executor [D-15]"
  artifacts:
    - path: ".planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md"
      provides: "Complete divergence ledger for v0.64.0..v0.65.1 mirroring the Phase 85 ledger structure"
      contains: "## Carve-out Re-touch Check"
  key_links:
    - from: "94-DIVERGENCE-LEDGER.md Reproduction block"
      to: "scripts/check-upstream-drift.sh SHA 0834aa66"
      via: "drift_tool_sh_sha frontmatter + invocation line"
      pattern: "drift_tool_invocation"
---

<objective>
Author the `94-DIVERGENCE-LEDGER.md` for the `nolabs-ai/nono` `v0.64.0..v0.65.1`
window (releases v0.64.1, v0.65.0, v0.65.1), classifying every substantive
commit into will-sync / fork-preserve / won't-sync / split clusters with a
`windows-touch` flag and per-cell ADR verdict. The headline fork-specific
deliverable is the mandatory **Carve-out Re-touch Check** (D-04/D-05).

Purpose: Satisfies UPST10-01 and ROADMAP success criteria 1 and 3. The ledger
is the input contract for Phase 95 (absorb) and Phase 97 (leapfrog floor) —
without it those phases have nothing to cherry-pick against.

Output: `.planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md`,
a single markdown file mirroring the Phase 85 UPST9 ledger structure.

Scope guard: This plan AUDITS AND CLASSIFIES ONLY. Do NOT cherry-pick, edit
source/build/tests, stand up toolchains, or touch release engineering — those
are Phases 95/96/97 (CONTEXT.md `<deferred>`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-upst10-divergence-audit/94-CONTEXT.md
@.planning/REQUIREMENTS.md

# The structural template + carve-out source-of-truth (READ IN FULL — incl. all 3 addenda):
@.planning/milestones/v3.1-phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md

# The carve-out ADRs (CR-02 + library boundary):
@proj/ADR-87-cr02-audit-bypass.md
@proj/ADR-86-library-boundary-convergence.md

<reference_facts>
Grounded this session (CONTEXT.md D-01..D-03), reproduce in the ledger header:
- Window SHAs: v0.64.0 = 0153757001, v0.64.1 = 0551eba27e, v0.65.0 = 137bb15c56, v0.65.1 = 1d1c88c9f9 (tip).
- Audit range: 0153757001..1d1c88c9 (explicit SHAs — SHA-not-tag guard, D-03).
- D-02 clean-continuation: nolabs v0.64.0 SHA (0153757001) is byte-identical to the fork's
  last sync point (UPST9/Phase 85 endpoint, former always-further/nono head) — directly comparable,
  no history-translation step.
- Drift tool SHA pin (both .sh and .ps1): 0834aa664fbaf4c5e41af5debece292992211559.
- Drift tool path-inclusion filter: crates/nono/src/, crates/nono-cli/src/, crates/nono-proxy/src/,
  crates/nono/Cargo.toml. Exclusion patterns: *_windows.rs and crates/nono-cli/src/exec_strategy_windows/.
- The three carve-out paths (D-04, mandatory):
  CR-02 → crates/nono/src/audit.rs (records_verified: event_count > 0; ADR-87).
  CR-01 → bindings/c/src/ FFI files: diagnostic.rs, lib.rs, capability_set.rs, fs_capability.rs,
          sandbox.rs, state.rs, query.rs (clear_last_call_state() at entry; commit db0f221d).
  Cluster F → crates/nono-proxy/src/{route.rs,connect.rs,reverse.rs,server.rs},
          crates/nono-cli/src/proxy_runtime.rs, the ABSENT crates/nono-proxy/src/tls_intercept/ dir,
          and the EffectiveProxySettings model (Phase 89 reconciliation addendum).
</reference_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refetch upstream, prove the window, run the drift tool, write the ledger frontmatter + Reproduction + Headline</name>
  <read_first>
    - .planning/phases/94-upst10-divergence-audit/94-CONTEXT.md (D-01, D-02, D-03, D-09, D-11)
    - .planning/milestones/v3.1-phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md (frontmatter, Reproduction, Headline shape to mirror)
    - scripts/check-upstream-drift.sh (the tool being run; confirm its SHA matches the pin)
  </read_first>
  <files>.planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md</files>
  <action>
    Fetch the nolabs window non-destructively: `git fetch https://github.com/nolabs-ai/nono.git --tags`
    (do NOT yet rename or repoint the `upstream` remote — that is plan 94-02). Confirm the four
    window tip SHAs are reachable objects via `git cat-file -t <sha>` for 0153757001, 0551eba27e,
    137bb15c56, 1d1c88c9 (each must print `commit`). Record the D-02 clean-continuation proof:
    assert nolabs v0.64.0 = 0153757001 is byte-identical to the fork's UPST9 endpoint (the Phase 85
    range tip `upstream_head_at_audit: 0153757001d21805a8218213e32add462d3322a1`) — note this so a
    future auditor does not re-investigate the re-fork-vs-relocation question.
    Pin the drift tool SHA via `git log -1 --format=%H -- scripts/check-upstream-drift.sh` (expect
    0834aa664fbaf4c5e41af5debece292992211559) and the same for the .ps1.
    Run the drift tool against EXPLICIT window SHAs (D-03 SHA-not-tag guard), capturing JSON to
    ci-logs-local/drift/: `bash scripts/check-upstream-drift.sh --from 0153757001 --to 1d1c88c9
    --format json > ci-logs-local/drift/<timestamp>-v064-v0651-upst10.json`. Record the substantive
    commit count from the tool output.
    Compute the full-window commit accounting: `git log --oneline 0153757001..1d1c88c9 | wc -l`
    (total), `git log --merges --oneline 0153757001..1d1c88c9 | wc -l` (merges),
    `git log --no-merges --oneline 0153757001..1d1c88c9 | wc -l`.
    Create `.planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md` and write only:
    (a) the YAML frontmatter mirroring Phase 85's fields — `phase`, `plan: 01`, `ledger_type:
    upst10-audit`, `range: 0153757001..1d1c88c9 (v0.64.0..v0.65.1)`, `range_note`,
    `upstream_head_at_audit: 1d1c88c9...`, `refetch_date`, `drift_tool_sh_sha`, `drift_tool_ps1_sha`,
    `drift_tool_invocation` (the exact command above), `fork_baseline: v0.64.0 (SHA 0153757001;
    Phase 85 UPST9 endpoint)`, `total_unique_commits`, `date`;
    (b) the `## Headline` section (one paragraph: substantive-commit count, cluster letters + per-cluster
    dispositions, windows-touch clusters, and an explicit mention that the Carve-out Re-touch Check
    is the headline fork-specific deliverable);
    (c) the `## Reproduction` block — the exact commands above (fetch, cat-file guard, D-02 anchor proof,
    drift-tool SHA pin, drift-tool invocation, the three wc -l counts), plus an "Auditor rerun"
    note instructing a future auditor to re-run verbatim. Use the new canonical org `nolabs-ai/nono`
    in all URLs. The Headline and Cluster Summary cluster letters may be filled provisionally and
    finalized in Task 2; the count and reproduction commands must be concrete in this task.
  </action>
  <verify>
    <automated>test -f .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md && grep -q 'drift_tool_invocation' .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md && grep -q '0153757001' .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md && grep -q '1d1c88c9' .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md && grep -q 'nolabs-ai/nono' .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md && grep -q '0834aa664fbaf4c5e41af5debece292992211559' .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md</automated>
  </verify>
  <acceptance_criteria>
    - File `94-DIVERGENCE-LEDGER.md` exists with YAML frontmatter containing all Phase-85-shape fields,
      with `range: 0153757001..1d1c88c9 (v0.64.0..v0.65.1)` and `drift_tool_sh_sha:
      0834aa664fbaf4c5e41af5debece292992211559`.
    - A `## Reproduction` block contains: the `git cat-file -t` guard for all four window SHAs; an
      explicit D-02 byte-identical-anchor statement naming SHA 0153757001 as both the nolabs v0.64.0
      tip and the fork's UPST9 endpoint; the drift-tool SHA-pin command; the exact `--from 0153757001
      --to 1d1c88c9 --format json` invocation; and the three `git log ... | wc -l` accounting commands.
    - A `## Headline` paragraph states the substantive-commit count, the cluster dispositions, the
      windows-touch clusters, and names the Carve-out Re-touch Check as the headline deliverable.
    - All upstream URLs reference `nolabs-ai/nono` (zero `always-further` URLs in the Reproduction block).
    - No source code, build files, or tests were modified; no cherry-pick occurred.
  </acceptance_criteria>
  <done>
    Ledger file created with finalized frontmatter, Reproduction (incl. D-02 anchor proof + drift-tool
    SHA pin + explicit-SHA invocation), and Headline; drift-tool JSON captured under ci-logs-local/drift/.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author the Cluster Summary, per-cluster tables, the mandatory Carve-out Re-touch Check, and the per-cluster ADR risk matrix</name>
  <read_first>
    - .planning/phases/94-upst10-divergence-audit/94-CONTEXT.md (D-04, D-05, D-09, D-10, D-12, D-14, D-15)
    - .planning/milestones/v3.1-phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md (Cluster Summary, per-cluster table format, ADR Review matrix, AND the three addenda — Phase 87 CR-02, Phase 88 CR-01, Phase 89 Cluster F — which DEFINE the carve-out paths/lines/future-sync-note wording)
    - proj/ADR-87-cr02-audit-bypass.md (CR-02 records_verified detail)
    - proj/ADR-86-library-boundary-convergence.md (library-boundary carve-out, relevant if any window commit re-touches the core audit/diagnostic surface)
    - .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md (the file from Task 1)
  </read_first>
  <files>.planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md</files>
  <action>
    Append to `94-DIVERGENCE-LEDGER.md`. Use uniform actual-diff per D-09: run `git show <sha>` on
    EVERY substantive commit in the window (the drift-tool JSON from Task 1 enumerates them), including
    the cross-cluster re-export scan `git show <sha> | grep '^+' | grep -E '^\+\s*(pub use|pub mod|extern crate|pub\(crate\))'`.
    Do NOT risk-tier; the window is small (Phase 85 risk-tiered only because it had 90 commits).
    Write these sections:
    1. `## Cluster Summary` — a table with columns cluster_id | theme | commits | disposition |
       windows-touch | rationale. Letter clusters FRESH for this window (A, B, C…), independent of
       Phase 85's A–M (D-12). Dispositions drawn from the four buckets will-sync / fork-preserve /
       won't-sync / split.
    2. One `## Cluster X: <theme>` section per cluster mirroring the Phase 85 per-cluster shape:
       Commits list, Disposition, Windows-touch, Inspection depth (actual-diff per D-09), Rationale,
       the cross-cluster re-export findings, and a per-commit table (sha | subject | upstream-tag |
       categories | files-changed | windows-touch). For any will-sync/split commit whose diff touches
       a `#[cfg(target_os = "linux")]` or `#[cfg(target_os = "macos")]` block, add a "Phase 95 executor
       cross-target clippy note" citing the CLAUDE.md MUST/NEVER rule (D-15 — note only; the gate runs
       in 95/96).
    3. Release/version commits (Cargo.toml bumps, CHANGELOG, release metadata) go in a `won't-sync`
       cluster/row with a ONE-LINE cross-ref naming which window commits carry the upstream version
       metadata so Phase 97 knows the leapfrog floor is >= 0.65.0 (D-10). Do NOT add a standalone
       Release Readiness section (deferred — CONTEXT.md `<deferred>`).
    4. `## Carve-out Re-touch Check` (the headline fork-specific section, D-04/D-05) — for EACH of the
       three carve-outs run `git log 0153757001..1d1c88c9 -- <exact path>` and record the result:
       (a) CR-02: `git log <window> -- crates/nono/src/audit.rs`;
       (b) CR-01: `git log <window> -- bindings/c/src/diagnostic.rs bindings/c/src/lib.rs
           bindings/c/src/capability_set.rs bindings/c/src/fs_capability.rs bindings/c/src/sandbox.rs
           bindings/c/src/state.rs bindings/c/src/query.rs`;
       (c) Cluster F: `git log <window> -- crates/nono-proxy/src/route.rs crates/nono-proxy/src/connect.rs
           crates/nono-proxy/src/reverse.rs crates/nono-proxy/src/server.rs
           crates/nono-cli/src/proxy_runtime.rs` PLUS confirm the absent
           `crates/nono-proxy/src/tls_intercept/` dir via `ls crates/nono-proxy/src/` and note the
           EffectiveProxySettings model.
       Any hit is flagged "expected conflict — preserve fork expression" and carried into the Phase 95
       cherry-pick guidance (cite the relevant addendum + ADR + guard-test name from the Phase 85
       addenda). A ZERO-hit result is recorded EXPLICITLY as "clean — no re-touch in window" (silence
       is not evidence, D-05).
    5. `## ADR Review` — an overall five-dimension assessment table (security / windows / maintenance /
       divergence / contributor) AND a per-cluster risk matrix with one row per cluster scoring all five
       dimensions + an Overall. NO cluster may carry a bare `TBD` (D-14). Each disposition must be
       justified by >= 1 of: security impact, Windows-backend touch, or library-boundary relevance
       (ROADMAP SC3). End with an Outcome line (continue/escalate) and the downstream-phase routing
       (will-sync/split clusters → Phase 95).
  </action>
  <verify>
    <automated>grep -q '## Carve-out Re-touch Check' .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md && grep -q 'crates/nono/src/audit.rs' .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md && grep -q 'bindings/c/src/diagnostic.rs' .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md && grep -q 'tls_intercept' .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md && grep -q '## Cluster Summary' .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md && grep -q '## ADR Review' .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md && grep -Eqi '0\.65\.0' .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md && ! grep -qi 'bare TBD\| TBD ' .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md</automated>
  </verify>
  <acceptance_criteria>
    - `## Cluster Summary` table present with cluster_id / theme / commits / disposition / windows-touch /
      rationale columns; cluster letters start fresh at A; every disposition is one of will-sync /
      fork-preserve / won't-sync / split.
    - Each cluster has its own `## Cluster X:` section with a per-commit table (sha | subject |
      upstream-tag | categories | files-changed | windows-touch) and a cross-cluster re-export finding.
    - `## Carve-out Re-touch Check` present with all three carve-out `git log <window> -- <path>` results;
      each is either flagged "expected conflict — preserve fork expression" (with addendum/ADR/guard-test
      citation) OR explicitly "clean — no re-touch in window"; the absent `tls_intercept/` dir is confirmed.
    - Release/version commits are in a won't-sync row with a one-line >= 0.65.0 leapfrog-floor cross-ref
      for Phase 97; no standalone Release Readiness section exists.
    - `## ADR Review` contains a per-cluster matrix scoring security/windows/maintenance/divergence/
      contributor for every cluster with an Overall column; zero clusters carry a bare `TBD`; an Outcome
      line and Phase-95 routing are present.
    - Any will-sync/split commit touching cfg(linux)/cfg(macos) carries a Phase 95 cross-target clippy note.
    - No source/build/test files modified; no cherry-pick.
  </acceptance_criteria>
  <done>
    Cluster Summary, all per-cluster sections, the Carve-out Re-touch Check, the release/version won't-sync
    cross-ref, and the per-cluster ADR risk matrix are written; every commit is classified once; no bare TBD.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author the Empirical Cross-Check, the noise reconciliation, and finalize Headline counts</name>
  <read_first>
    - .planning/phases/94-upst10-divergence-audit/94-CONTEXT.md (D-11 spot-check, D-13 noise reconciliation)
    - .planning/milestones/v3.1-phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md (Empirical Cross-Check and Excluded as Noise section shapes)
    - .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md (the file from Tasks 1-2)
  </read_first>
  <files>.planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md</files>
  <action>
    Append to `94-DIVERGENCE-LEDGER.md`:
    1. `## Empirical Cross-Check` — a ~6-file `git log --oneline 0153757001..1d1c88c9 -- <file>` spot-check
       to validate cluster classification accuracy (D-11). MANDATORY files: the carve-out paths
       (crates/nono/src/audit.rs, a representative bindings/c/src/ FFI file, a crates/nono-proxy/src/
       file). Choose the remaining spot-check files at your discretion to cover the largest/most-touched
       clusters. For each, state the expected cluster and confirm CORRECT (or record + resolve any
       mismatch). State the drift tool was re-run + spot-check performed.
    2. `## Excluded as Noise` — the D-13 reconciliation. State the drift-tool path-inclusion filter
       (crates/nono/src/, crates/nono-cli/src/, crates/nono-proxy/src/, crates/nono/Cargo.toml) and
       exclusion patterns (*_windows.rs, crates/nono-cli/src/exec_strategy_windows/). Enumerate ALL
       merge commits (`git log --merges --oneline 0153757001..1d1c88c9`) and ALL non-merge out-of-filter
       commits so every commit is accounted for in exactly ONE place. End with a Completeness
       verification line: substantive + noise = total, matching `git log --oneline 0153757001..1d1c88c9
       | wc -l`. The arithmetic MUST balance.
    3. Finalize the `## Headline` and `## Cluster Summary` counts from Task 1 if they were provisional,
       so the substantive count, cluster letters, and dispositions are internally consistent across the
       Headline, Cluster Summary, per-cluster tables, and the noise reconciliation.
  </action>
  <verify>
    <automated>grep -q '## Empirical Cross-Check' .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md && grep -q '## Excluded as Noise' .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md && grep -Eqi 'substantive \+ noise|= [0-9]+ total|matches' .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md && grep -q 'crates/nono/src/audit.rs' .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md</automated>
  </verify>
  <acceptance_criteria>
    - `## Empirical Cross-Check` present with >= 6 `git log --oneline <window> -- <file>` spot-checks,
      including all three carve-out path families; each names an expected cluster and a CORRECT/resolved verdict.
    - `## Excluded as Noise` present, stating the path filter + exclusion patterns, enumerating every
      merge commit and every out-of-filter non-merge commit, with a Completeness line where
      substantive + noise equals the `git log --oneline 0153757001..1d1c88c9 | wc -l` total (arithmetic balances).
    - Headline / Cluster Summary / per-cluster tables / noise reconciliation are internally consistent
      on the substantive-commit count and cluster letters.
    - No source/build/test files modified; no cherry-pick.
  </acceptance_criteria>
  <done>
    Empirical Cross-Check and noise reconciliation written; total = substantive + noise balances; every
    window commit is accounted for in exactly one place; ledger counts internally consistent.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream git fetch → local repo | Commits fetched from `nolabs-ai/nono` are inspected (read-only) but NOT executed, cherry-picked, or built in this phase. |
| audit completeness → Phase 95 | The ledger is the sole contract Phase 95 absorbs against; a silently-dropped commit becomes an unabsorbed (potentially security-relevant) upstream change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01 | Tampering / wrong-source | git fetch URL | mitigate | Fetch only from the exact org `https://github.com/nolabs-ai/nono.git`; the four window tip SHAs are verified via `git cat-file -t` and the D-02 byte-identical v0.64.0 anchor proof confirms continuity from the known-good UPST9 endpoint (typosquat / wrong-org would not match the anchor SHA). |
| T-94-02 | Information disclosure / dropped commit | audit coverage | mitigate | Uniform actual-diff (D-09, `git show` every substantive commit) + the D-13 noise reconciliation (substantive + noise = total) structurally guarantees no commit is silently omitted; a security-impacting commit cannot hide in an additive-looking diff because every diff is read. |
| T-94-03 | Repudiation / unreproducible audit | drift tool run | mitigate | Drift-tool SHA pin + exact explicit-SHA invocation recorded in the Reproduction block (D-11), enabling a future auditor to re-run verbatim and detect any post-audit upstream push. |
| T-94-SC | Tampering | npm/pip/cargo installs | accept | No package installs occur in this docs/audit phase; no slopcheck applies. |
</threat_model>

<verification>
- `94-DIVERGENCE-LEDGER.md` exists and mirrors the Phase 85 ledger structure (frontmatter, Headline,
  Reproduction, Cluster Summary, per-cluster sections, Carve-out Re-touch Check, ADR Review, Empirical
  Cross-Check, Excluded as Noise).
- Every substantive commit is classified into exactly one cluster; substantive + noise = total
  (`git log --oneline 0153757001..1d1c88c9 | wc -l`).
- Carve-out Re-touch Check covers CR-02, CR-01, and Cluster F with explicit hit / clean verdicts.
- No cluster carries a bare TBD; each disposition is justified by security / windows / library-boundary.
- No source/build/test files were modified.
</verification>

<success_criteria>
- UPST10-01 satisfied: a DIVERGENCE-LEDGER classifies every commit into will-sync / fork-preserve /
  won't-sync / split with a windows-touch flag and per-cell ADR verdict (ROADMAP SC1).
- Each cluster's disposition is justified by security impact, Windows-backend touch, or library-boundary
  relevance — no bare TBD (ROADMAP SC3).
- D-01 through D-05, D-09 through D-15 are each traceable to a section of the ledger.
</success_criteria>

<output>
Create `.planning/phases/94-upst10-divergence-audit/94-01-SUMMARY.md` when done.
</output>
