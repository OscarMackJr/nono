---
phase: 94-upst10-divergence-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
autonomous: true
requirements: [UPST10-04]
must_haves:
  truths:
    - "Decisions covered (CONTEXT.md): D-06: repoint upstream to nolabs-ai/nono and retain former remote renamed upstream-legacy; D-07: PROJECT.md Upstream Parity Process references nolabs-ai/nono as canonical source; D-08: Future Cycles stub trigger = nolabs-ai/nono ships any v* tag past v0.65.1, next-v*-tag not drift-count not time-based."
    - "The git `upstream` remote points at https://github.com/nolabs-ai/nono.git [SC2, D-06, UPST10-04]"
    - "The former always-further/nono remote is retained, renamed `upstream-legacy`, for provenance/forensics [D-06]"
    - "PROJECT.md `## Upstream Parity Process` references nolabs-ai/nono as the canonical source (no remaining always-further/nono reference in that section) [SC2, D-07]"
    - "A Future Cycles stub records the next sync trigger = nolabs-ai/nono ships any `v*` tag past v0.65.1 (observable via git ls-remote --tags) [SC2, D-08]"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Upstream Parity Process updated to nolabs-ai/nono + Future Cycles stub"
      contains: "nolabs-ai/nono"
  key_links:
    - from: "git config remote.upstream.url"
      to: "https://github.com/nolabs-ai/nono.git"
      via: "git remote set-url"
      pattern: "nolabs-ai/nono"
---

<objective>
Record the upstream relocation `always-further/nono` → `nolabs-ai/nono`: repoint
the git `upstream` remote at the new canonical source while retaining the old
remote (renamed `upstream-legacy`) for provenance, update the PROJECT.md
`## Upstream Parity Process` references, and add a Future Cycles stub noting the
next sync trigger.

Purpose: Satisfies UPST10-04 and ROADMAP success criterion 2. Without the
repoint, every future drift run and sync would target the dead upstream org.

Output: updated git remotes (config-only) and an edited `.planning/PROJECT.md`.

Scope guard: This plan records the relocation ONLY. It does NOT fetch/inspect
commits for classification (that is plan 94-01), cherry-pick, build, or touch
release engineering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-upst10-divergence-audit/94-CONTEXT.md
@.planning/REQUIREMENTS.md

<reference_facts>
Current remotes (verified this session):
- origin   → https://github.com/OscarMackJr/nono.git
- upstream → https://github.com/always-further/nono.git   (TO BE RELOCATED)

Target state (D-06):
- upstream        → https://github.com/nolabs-ai/nono.git        (NEW canonical)
- upstream-legacy → https://github.com/always-further/nono.git   (retained, provenance only)

PROJECT.md `## Upstream Parity Process` is at ~line 602. The canonical-source
reference to replace is `always-further/nono` → `nolabs-ai/nono` (D-07).
Future Cycles stub trigger (D-08): nolabs-ai/nono ships any `v*` tag past v0.65.1
(observable via `git ls-remote --tags https://github.com/nolabs-ai/nono.git`).
NOT drift-count, NOT time-based. Matches the established drain-then-sync
per-tag-window cadence.
</reference_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint the upstream remote and retain the legacy remote</name>
  <read_first>
    - .planning/phases/94-upst10-divergence-audit/94-CONTEXT.md (D-06)
  </read_first>
  <files>(git remote config — no working-tree file)</files>
  <action>
    Verify the starting state with `git remote -v` (expect `upstream` →
    https://github.com/always-further/nono.git). Rename the existing remote to preserve provenance:
    `git remote rename upstream upstream-legacy`. Confirm `upstream-legacy` now carries the
    always-further URL (`git remote get-url upstream-legacy` → https://github.com/always-further/nono.git);
    if `git remote rename` did not preserve the URL, set it with
    `git remote set-url upstream-legacy https://github.com/always-further/nono.git`. Add the new canonical
    upstream: `git remote add upstream https://github.com/nolabs-ai/nono.git` (or, if an `upstream`
    remote still exists after the rename edge-case, `git remote set-url upstream
    https://github.com/nolabs-ai/nono.git`). Do NOT fetch or cherry-pick — config change only.
    Final `git remote -v` MUST show: `origin` → OscarMackJr/nono, `upstream` → nolabs-ai/nono,
    `upstream-legacy` → always-further/nono.
  </action>
  <verify>
    <automated>git remote get-url upstream | grep -q 'nolabs-ai/nono' && git remote get-url upstream-legacy | grep -q 'always-further/nono'</automated>
  </verify>
  <acceptance_criteria>
    - `git remote get-url upstream` prints `https://github.com/nolabs-ai/nono.git`.
    - `git remote get-url upstream-legacy` prints `https://github.com/always-further/nono.git`.
    - `git remote -v` shows exactly three remote names: origin, upstream, upstream-legacy (no orphaned
      or duplicate upstream entry).
    - No fetch, cherry-pick, build, or source edit occurred.
  </acceptance_criteria>
  <done>
    `upstream` points at nolabs-ai/nono; the former always-further/nono remote is retained as
    `upstream-legacy`; `git remote -v` is clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update PROJECT.md Upstream Parity Process to nolabs-ai/nono and add the Future Cycles stub</name>
  <read_first>
    - .planning/phases/94-upst10-divergence-audit/94-CONTEXT.md (D-07, D-08)
    - .planning/PROJECT.md (§ `## Upstream Parity Process`, ~line 602)
  </read_first>
  <files>.planning/PROJECT.md</files>
  <action>
    In `.planning/PROJECT.md`, edit the `## Upstream Parity Process` section so the canonical-source
    reference is `nolabs-ai/nono` (replace the `always-further/nono` mention in that section per D-07).
    Preserve the existing 4-step process, the drift-tool / cherry-pick / Windows-retrofit guidance, and
    the long-form runbook link — only the canonical-source org changes. Do NOT edit the historical
    milestone-footer prose at the bottom of PROJECT.md (those are point-in-time records and may legitimately
    still name always-further/nono).
    Add a `### Future Cycles` stub (subsection under Upstream Parity Process, or an adjacent clearly-labeled
    stub) recording the next sync trigger per D-08: the next sync window opens when **nolabs-ai/nono ships
    any `v*` tag past v0.65.1**, observable via `git ls-remote --tags https://github.com/nolabs-ai/nono.git`.
    State explicitly that the trigger is the next-`v*`-tag (NOT drift-count, NOT time-based) and that it
    matches the established drain-then-sync per-tag-window cadence. Record the current high-water mark =
    v0.65.1 (SHA 1d1c88c9) and that the v0.64.0..v0.65.1 window is audited by Phase 94's DIVERGENCE-LEDGER.
  </action>
  <verify>
    <automated>grep -q 'nolabs-ai/nono' .planning/PROJECT.md && grep -niq 'Future Cycles' .planning/PROJECT.md && grep -q 'v0.65.1' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - The `## Upstream Parity Process` section references `nolabs-ai/nono` as the canonical source and
      contains no `always-further/nono` reference within that section (historical footers untouched).
    - A `Future Cycles` stub exists naming the trigger as nolabs-ai/nono shipping any `v*` tag past
      v0.65.1, citing `git ls-remote --tags`, explicitly stating it is next-`v*`-tag (not drift-count,
      not time-based), and recording the v0.65.1 high-water mark.
    - The existing 4-step process and runbook link are preserved.
    - No source/build/test files modified; no cherry-pick.
  </acceptance_criteria>
  <done>
    PROJECT.md Upstream Parity Process points at nolabs-ai/nono and carries a D-08 Future Cycles stub;
    historical footers left intact.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git remote config → all future syncs | The `upstream` remote URL is the source of every future drift run and cherry-pick; a wrong-org repoint silently poisons all downstream sync work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-04 | Spoofing / typosquat | upstream remote URL | mitigate | Set the exact org string `https://github.com/nolabs-ai/nono.git` (D-06); the automated verify greps `nolabs-ai/nono` and the legacy retention preserves the verifiable prior `always-further/nono` URL for cross-check. Wrong-org/typosquat is the one security-relevant failure mode of this plan. |
| T-94-05 | Tampering / lost provenance | old remote deletion | mitigate | The former remote is renamed (not deleted) to `upstream-legacy`, retaining a forensic record of the relocation; continuity is byte-clean (94-01 D-02 anchor proof) so legacy is cheap insurance. |
| T-94-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this docs/config phase; no slopcheck applies. |
</threat_model>

<verification>
- `git remote get-url upstream` → https://github.com/nolabs-ai/nono.git.
- `git remote get-url upstream-legacy` → https://github.com/always-further/nono.git.
- PROJECT.md `## Upstream Parity Process` references nolabs-ai/nono; Future Cycles stub present with
  next-`v*`-tag trigger.
- No source/build/test files modified.
</verification>

<success_criteria>
- UPST10-04 satisfied: the upstream relocation is recorded — the git `upstream` remote and the PROJECT.md
  `## Upstream Parity Process` references point at `nolabs-ai/nono`, with a Future Cycles stub noting the
  next sync trigger past v0.65.1 (ROADMAP SC2).
- D-06, D-07, D-08 each traceable to a task here.
</success_criteria>

<output>
Create `.planning/phases/94-upst10-divergence-audit/94-02-SUMMARY.md` when done.
</output>
