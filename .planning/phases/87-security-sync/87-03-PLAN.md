---
phase: 87-security-sync
plan: 03
type: execute
wave: 3
depends_on:
  - 87-01
  - 87-02
files_modified:
  - .planning/phases/87-security-sync/87-VERIFICATION.md
autonomous: true
requirements:
  - SEC-01
  - SEC-02

must_haves:
  truths:
    - "make ci passes on Windows dev-host (clippy + fmt + tests for the Windows-target compilation path)"
    - "Cross-target clippy attempt is documented with outcome — either VERIFIED clean or PARTIAL→CI per cross-target-verify-checklist.md"
    - "87-VERIFICATION.md records all three SEC-01/SEC-02/CR-02 outcomes with PARTIAL→CI disposition for Linux-execution and cross-target clippy legs"
    - "All local-verifiable gates (cargo check, unit tests, fmt) pass"
    - "ROADMAP.md Phase 87 Plans field updated to 3 plans with plan list"
    - "STATE.md Phase 87 position updated to reflect plans complete"
  artifacts:
    - path: ".planning/phases/87-security-sync/87-VERIFICATION.md"
      provides: "Phase verification report with SEC-01/SEC-02/CR-02 dispositions and PARTIAL→CI deferral records"
      contains: "PARTIAL"
  key_links:
    - from: ".planning/phases/87-security-sync/87-VERIFICATION.md"
      to: ".planning/templates/cross-target-verify-checklist.md"
      via: "PARTIAL→CI deferral prose per template"
      pattern: "cross-target-verify-checklist"
---

<objective>
Run the full local verification suite, attempt cross-target clippy (expected PARTIAL on the
Windows dev-host), and produce the phase VERIFICATION.md documenting all outcomes with correct
PARTIAL→CI deferral dispositions.

Purpose: Phase 87's security fixes touch cfg-gated Unix code in three files
(`linux.rs`, `capability.rs`, `supervisor_linux.rs`) — cross-target clippy is MANDATORY per
CLAUDE.md and the cross-target-verify-checklist.md. The decisive gates for Linux-execution
(seccomp tests) and cross-target clippy run on GH Actions CI. This plan runs everything that
can run locally and creates the explicit deferral record for what cannot.

Output: A committed 87-VERIFICATION.md with per-requirement dispositions, PARTIAL entries for
cross-target and Linux-execution legs, and ROADMAP/STATE updated to reflect phase completion.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-security-sync/87-CONTEXT.md
@.planning/phases/87-security-sync/87-01-SUMMARY.md
@.planning/phases/87-security-sync/87-02-SUMMARY.md
@.planning/templates/cross-target-verify-checklist.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run local verification suite — make ci + unit tests + cross-target clippy attempt</name>
  <files>.planning/phases/87-security-sync/87-VERIFICATION.md</files>
  <read_first>
    - .planning/templates/cross-target-verify-checklist.md — full file; this is the authoritative
      procedure for PARTIAL→CI deferral; read before running any clippy command
    - 87-CONTEXT.md D-07, D-08 — the explicit PARTIAL→CI decisions for Linux-execution and
      cross-target clippy
    - .planning/phases/87-security-sync/87-01-SUMMARY.md — outcomes from SEC-01 commit
    - .planning/phases/87-security-sync/87-02-SUMMARY.md — outcomes from SEC-02 + CR-02 commits
    - .planning/ROADMAP.md — Phase 87 section (lines ~62-69) for success criteria text
  </read_first>
  <action>
    Run verification in this exact sequence. Record every command and its exit code/output for
    the VERIFICATION.md report.

    Gate 1 — Cargo fmt check:
    Run: make fmt-check (or cargo fmt --all -- --check)
    Record outcome. If any diff output: run make fmt, then re-run fmt-check.

    Gate 2 — Windows-host cargo clippy (local):
    Run: cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used
    This exercises only Windows cfg branches. Record exit code.
    This is NOT a substitute for cross-target clippy (CLAUDE.md MUST/NEVER rule) — record it
    as "Windows-host clippy: PASS/FAIL" in the report.

    Gate 3 — Unit tests (Windows-host, non-Linux-execution):
    Run: cargo test --workspace --all-targets 2>&1
    Most tests are platform-agnostic and run on Windows. The seccomp + Landlock tests are
    #[cfg(target_os = "linux")]-gated and will NOT execute — this is expected and not a failure.
    Record: total tests run, any failures.
    Specifically verify these tests pass (they are platform-agnostic or cfg-gated-but-pass-on-Windows):
    - cargo test -p nono -- remap_preserves_dev_null_when_deduped_with_dev_stdin (SEC-02 regression)
    - cargo test -p nono -- verify_empty_log_with_no_stored_metadata_is_not_valid (CR-02 regression)
    Note: BPF filter tests (test_build_seccomp_af_unix_filter_*) are #[cfg(target_os = "linux")]
    and will not run on Windows. Record as "PARTIAL→CI (Linux-execution-gated)".

    Gate 4 — Cross-target clippy attempt (expected PARTIAL):
    Run: rustup target add x86_64-unknown-linux-gnu x86_64-apple-darwin (ensure targets are added)
    Then: cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used 2>&1
    Expected outcome on this Windows dev-host: error about missing linker (x86_64-linux-gnu-gcc)
    or similar cross-toolchain error.
    If the command fails with a linker/toolchain error → record as PARTIAL→CI per the checklist:
      "Cross-target clippy gate SKIPPED on Windows dev-host due to missing toolchain
      (x86_64-unknown-linux-gnu). The live GH Actions Linux Clippy lane on the head SHA is the
      decisive signal per .planning/templates/cross-target-verify-checklist.md. SEC-01/SEC-02
      REQs marked PARTIAL pending CI confirmation."
    If the command actually succeeds (clean) → record as VERIFIED for the Linux target.
    Then attempt macOS: cargo clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used 2>&1
    Same recording approach.

    Gate 5 — make ci:
    Run: make ci
    This is the full CI equivalent (clippy + fmt + tests) on the Windows host. Record exit code.

    Gate 6 — Git log summary:
    Run: git log --oneline -5
    Record the 3 Phase 87 commits: SEC-01 cherry-pick, SEC-02 cherry-pick, CR-02 fork-hardening.
    Verify each has the correct provenance line and DCO trailer:
    git show HEAD~2 | grep -E "cherry picked|Signed-off-by"  (SEC-01)
    git show HEAD~1 | grep -E "cherry picked|Signed-off-by"  (SEC-02)
    git show HEAD   | grep "Signed-off-by"                   (CR-02)

    Write 87-VERIFICATION.md:
    Use the structure: ## Summary | ## Gate Results | ## Per-Requirement Disposition |
    ## PARTIAL→CI Deferral Record | ## Residual Risk

    Per-Requirement Disposition:
    - SEC-01: PARTIAL — Windows-host cargo check passes; cross-target clippy PARTIAL→CI (GH Actions
      Linux Clippy lane decisive); seccomp runtime tests PARTIAL→CI (Linux-execution-gated per D-07).
    - SEC-02: PARTIAL — regression test passes on Windows (remap_preserves_dev_null...); cross-target
      clippy PARTIAL→CI (capability.rs has #[cfg(target_os = "linux")] guards); seccomp dedup behavior
      only observable on Linux (runtime-gated per D-07).
    - CR-02: VERIFIED locally — audit.rs records_verified fix is platform-agnostic; test passes on
      Windows; no cfg-gating on the fix itself.

    PARTIAL→CI Deferral Record must use the exact prose from cross-target-verify-checklist.md §PARTIAL
    Disposition, substituting the actual toolchain error encountered.

    Commit 87-VERIFICATION.md:
    git add .planning/phases/87-security-sync/87-VERIFICATION.md
    git commit -m "docs(87): phase verification — SEC-01/SEC-02 PARTIAL→CI, CR-02 local pass

    Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>"
  </action>
  <verify>
    <automated>make ci 2>&1 | tail -10</automated>
    make ci must exit 0.
    <automated>cargo test -p nono -- remap_preserves_dev_null_when_deduped_with_dev_stdin verify_empty_log 2>&1</automated>
    Both tests must pass.
    <automated>ls .planning/phases/87-security-sync/87-VERIFICATION.md</automated>
    File must exist.
  </verify>
  <acceptance_criteria>
    - make ci exits 0 on Windows dev-host
    - Both platform-agnostic regression tests pass (SEC-02 + CR-02)
    - 87-VERIFICATION.md exists and contains:
      - Per-requirement disposition for SEC-01, SEC-02, CR-02
      - PARTIAL→CI deferral record using exact prose from the checklist template (mention the
        actual toolchain error encountered and the GH Actions decisive lane)
      - Gate results for Windows clippy, fmt-check, cargo test, cross-target clippy attempt
    - PARTIAL→CI record explicitly states: "Windows-host cargo check is not accepted as a substitute
      per CLAUDE.md §Coding Standards and cross-target-verify-checklist.md"
    - git log shows 4 commits for Phase 87: SEC-01 (cherry-pick), SEC-02 (cherry-pick), CR-02
      (fork-hardening), VERIFICATION docs
    - Each of the 3 code commits verified to have DCO Signed-off-by in body
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md + STATE.md to reflect Phase 87 plans complete</name>
  <files>
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/ROADMAP.md — Phase 87 section (lines ~62-69): the "Plans: TBD" field and the
      progress table row for Phase 87
    - .planning/STATE.md — Current Position section (Phase / Plan / Status fields)
    - .planning/phases/86-library-boundary-convergence/86-03-PLAN.md — how Phase 86 plan list
      was formatted (for consistency)
  </read_first>
  <action>
    Step 1 — Update ROADMAP.md Phase 87 section:
    Find "### Phase 87: Security Sync" and:
    (a) Replace `**Plans:** TBD` with `**Plans:** 3 plans`
    (b) Add a Plans list:
      Plans:
      - [x] 87-01-PLAN.md — SEC-01: AF_UNIX datagram bypass close (BPF filter expansion + send-family dispatch)
      - [x] 87-02-PLAN.md — SEC-02 procfs-remap dedup guard + CR-02 audit-integrity fix + divergence record
      - [x] 87-03-PLAN.md — Cross-target clippy verification + PARTIAL→CI deferral record

    (c) In the progress table at the bottom of ROADMAP.md, update Phase 87 row:
      | 87. Security Sync | 3/3 | Complete | 2026-06-20 |

    (d) Flip the Phase 87 checkbox in the top ## Phases list from `- [ ]` to `- [x]`.

    Step 2 — Update STATE.md:
    (a) Update `## Current Position` section: Phase: 88, Plan: Not started, Status: Ready to plan
    (b) Update `stopped_at` front-matter: "Phase 87 complete — PARTIAL→CI pending GH Actions"
    (c) Update `last_activity` to 2026-06-20

    Step 3 — Commit:
    git add .planning/ROADMAP.md .planning/STATE.md
    git commit -m "docs(state): Phase 87 security sync complete — 3 plans, PARTIAL→CI for SEC-01/SEC-02

    Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>"
  </action>
  <verify>
    <automated>grep -A3 "Phase 87" .planning/ROADMAP.md | head -10</automated>
    Must show "3 plans" and the [x] checkbox.
    <automated>grep "Phase: 88" .planning/STATE.md</automated>
    Must return one match.
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 87 section shows "**Plans:** 3 plans" with a plan list of 87-01/87-02/87-03
    - ROADMAP.md Phase 87 checkbox flipped to [x]
    - ROADMAP.md progress table shows Phase 87 Complete with date 2026-06-20
    - STATE.md Current Position shows Phase: 88, Status: Ready to plan
    - Commit carries DCO Signed-off-by trailer
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local verification → CI gate | Windows-host verification cannot exercise Linux cfg-gated code; GH Actions CI is the decisive gate for cross-target safety |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-09 | Tampering | Prematurely claiming SEC-01/SEC-02 VERIFIED on Windows-host-only evidence (cross-target anti-pattern 1 from checklist) | mitigate | Per the checklist §Anti-Patterns, REQs marked PARTIAL not VERIFIED until GH Actions Linux Clippy lane reports green. This plan explicitly records PARTIAL and does NOT flip to VERIFIED. |
| T-87-10 | Information Disclosure | 85-DIVERGENCE-LEDGER.md CR-02 addendum missing → future sync silently reverts hardening | mitigate | Already addressed in Plan 87-02 Task 2; this plan verifies the addendum exists (ls + grep in verification). |
| T-87-SC | Tampering | npm/pip/cargo installs | accept | No new packages. Package legitimacy gate N/A. |
</threat_model>

<verification>
## Phase 87 Final State

After all three plans complete:

1. git log --oneline -6 — shows 5 Phase 87 commits: SEC-01 cherry-pick, SEC-02 cherry-pick,
   CR-02 fork-hardening, VERIFICATION docs commit, STATE/ROADMAP update commit
2. make ci — exits 0 on Windows dev-host
3. cargo test -p nono -- remap_preserves_dev_null_when_deduped_with_dev_stdin — exits 0
4. cargo test -p nono -- verify_empty_log_with_no_stored_metadata_is_not_valid — exits 0
5. 87-VERIFICATION.md exists with PARTIAL→CI records for SEC-01 + SEC-02
6. ROADMAP.md shows Phase 87 complete with 3 plans
7. STATE.md shows Phase 88 as next

PARTIAL → CI gates pending (not blocking Phase 88 planning):
- GH Actions Linux lane must report green on the HEAD SHA for `cargo clippy --workspace
  --target x86_64-unknown-linux-gnu` (SEC-01/SEC-02 decisive gate)
- GH Actions Linux lane must run seccomp BPF filter tests (SEC-01 runtime execution gate)
</verification>

<success_criteria>
- make ci exits 0 (Windows-target compilation + tests + fmt)
- Both regression tests pass (SEC-02: remap_preserves_dev_null, CR-02: verify_empty_log)
- 87-VERIFICATION.md committed with PARTIAL→CI deferral using exact checklist prose
- ROADMAP.md and STATE.md updated: Phase 87 complete, Phase 88 next
- Phase 87 PARTIAL→CI disposition explicitly recorded — no false VERIFIED claims for Linux-execution or cross-target clippy legs
</success_criteria>

<output>
Create .planning/phases/87-security-sync/87-03-SUMMARY.md when done.
</output>
