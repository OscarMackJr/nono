---
phase: 99-upstream-absorb-fork-invariant-verify
plan: 02
type: execute
wave: 2
depends_on:
  - 99-01
files_modified:
  - crates/nono-cli/src/capability_ext.rs
  - crates/nono-cli/src/command_runtime.rs
  - crates/nono-cli/src/execution_runtime.rs
  - crates/nono-cli/src/launch_runtime.rs
  - crates/nono-cli/src/main.rs
  - crates/nono-cli/src/output.rs
  - crates/nono-cli/src/profile/mod.rs
  - crates/nono-cli/src/proxy_runtime.rs
  - crates/nono-cli/src/sandbox_prepare.rs
  - crates/nono-cli/src/supervised_runtime.rs
  - crates/nono-cli/src/terminal_approval.rs
autonomous: true
requirements:
  - UPST11-02

must_haves:
  truths:
    - "D-08 test for WSL2ProxyFallback: test function in profile/mod.rs or proxy_runtime.rs test module asserting Wsl2ProxyPolicy::Refuse/Allow variants exist and load from profile"
    - "D-08 test for CompiledEndpointPolicy compat: test function asserting CompiledEndpointPolicy::compile() -> evaluate() chain produces 403 for a denied endpoint (or confirmed covered by denied_endpoint_returns_403_and_audit)"
    - "72bcfd66 replay commit in git log with trailer '(cherry picked from commit 72bcfd66...)' and DCO sign-off 'Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>'"
    - "pub(crate) enum NetworkIntent { Unrestricted, BlockAll, ProxyFiltered(BoxedProxyLaunchOptions) } exists in sandbox_prepare.rs — NOT in crates/nono/ core"
    - "Wsl2ProxyPolicy enum at profile/mod.rs survives adoption (not removed or renamed by 72bcfd66 replay)"
    - "CompiledEndpointPolicy references in proxy_runtime.rs survive adoption (compile() -> evaluate() chain wired)"
    - "cargo test -p nono-proxy --lib exits 0 after adoption (denied_endpoint_returns_403_and_audit passes)"
    - "cargo test -p nono-cli --lib exits 0 after adoption (D-08 tests pass)"
  artifacts:
    - path: "crates/nono-cli/src/sandbox_prepare.rs"
      provides: "pub(crate) enum NetworkIntent definition (three variants)"
      contains: "pub(crate) enum NetworkIntent"
    - path: "crates/nono-cli/src/profile/mod.rs"
      provides: "Wsl2ProxyPolicy enum preserved (fork-only extension) + D-08 WSL2ProxyFallback test"
      contains: "Wsl2ProxyPolicy"
    - path: "crates/nono-cli/src/proxy_runtime.rs"
      provides: "NetworkIntent-based proxy launch + CompiledEndpointPolicy chain intact + D-08 CEP compat test"
      contains: "CompiledEndpointPolicy"
  key_links:
    - from: "crates/nono-cli/src/proxy_runtime.rs"
      to: "crates/nono-proxy/src/config.rs"
      via: "CompiledEndpointPolicy::compile() -> evaluate() — must not be dropped during NetworkIntent adoption"
      pattern: "CompiledEndpointPolicy"
    - from: "crates/nono-cli/src/profile/mod.rs"
      to: "Wsl2ProxyPolicy at line ~1983"
      via: "fork-only extension; 72bcfd66 modifies profile/mod.rs for NetworkIntent; must preserve the enum"
      pattern: "Wsl2ProxyPolicy"
    - from: "crates/nono-cli/src/sandbox_prepare.rs NetworkIntent"
      to: "crates/nono/src/capability.rs NetworkMode::ProxyOnly"
      via: "library type (unchanged by adoption); CLI intent != library enforcement type"
      pattern: "NetworkMode::ProxyOnly"
---

<objective>
Create the D-08 deviation tests (Wave 0 gap items from 99-VALIDATION.md) proving the two ADR-98-named Cluster A deviations are verifiable, then manually replay upstream commit 72bcfd66 (#1225 NetworkIntent) across 11 CLI files with the fork-specific deviations preserved. This is the highest-risk absorb in Phase 99: 11 files, 7 with cfg(windows) blocks, deep proxy intent refactor, CompiledEndpointPolicy overlap.

Purpose: Establish NetworkIntent as the CLI proxy-intent layer without regressing the CompiledEndpointPolicy enforce chain or the fork-only WSL2ProxyFallback extension, and commit one atomic DCO-signed replay of the upstream refactor.
Output: Two D-08 deviation test functions; one DCO-signed replay commit for 72bcfd66 with upstream SHA trailer.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/99-upstream-absorb-fork-invariant-verify/99-CONTEXT.md
@.planning/phases/99-upstream-absorb-fork-invariant-verify/99-RESEARCH.md
@.planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md
@proj/ADR-98-network-intent-disposition.md
@proj/ADR-86-library-boundary-convergence.md
@CLAUDE.md

<interfaces>
<!-- Key types the executor must preserve and test -->

From crates/nono-cli/src/profile/mod.rs (current fork — verified by grep):
  pub enum Wsl2ProxyPolicy { Refuse, Allow }  // at line ~1983
  wsl2_proxy_policy: Option<Wsl2ProxyPolicy>  // struct field at line ~2103

From crates/nono-proxy/src/config.rs (current fork — verified by grep):
  pub struct CompiledEndpointPolicy { default: EndpointPolicyDefault, ... }  // line ~272
  impl CompiledEndpointPolicy {
    pub fn compile(policy: Option<&EndpointPolicyConfig>) -> Self { ... }     // line ~350
    // pub fn evaluate(...) -> EndpointDecision                                // line ~486
  }
  // Used in route.rs:13/41/91, reverse.rs:121, network_policy.rs

From upstream 72bcfd66 — shape of the new NetworkIntent enum (per ADR-98 §Context):
  pub(crate) enum NetworkIntent { Unrestricted, BlockAll, ProxyFiltered(BoxedProxyLaunchOptions) }
  pub(crate) fn is_proxy_active(&self) -> bool
  pub(crate) fn proxy_options(&self) -> Option<&ProxyLaunchOptions>
  pub(crate) strict_filter: bool  // renamed from network_block on ProxyLaunchOptions

Commit trailer pattern (D-06 mandatory):
  (cherry picked from commit 72bcfd66f98f0a1f3ff79cff1509713aaec7cdb0)
  Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create D-08 deviation tests for WSL2ProxyFallback and CompiledEndpointPolicy</name>
  <files>crates/nono-cli/src/profile/mod.rs, crates/nono-cli/src/proxy_runtime.rs</files>
  <read_first>
    Read crates/nono-cli/src/profile/mod.rs (find the Wsl2ProxyPolicy enum near line 1983 and the wsl2_proxy_policy field near line 2103; find the existing test module at the bottom).
    Read crates/nono-cli/src/proxy_runtime.rs (find CompiledEndpointPolicy usage and the existing test module).
    Read .planning/phases/99-upstream-absorb-fork-invariant-verify/99-RESEARCH.md §"D-08 New Deviation Tests" subsection and §"Guard Test Map" subsection.
  </read_first>
  <behavior>
    - Test 1 (profile/mod.rs test module): assert Wsl2ProxyPolicy::Refuse and Wsl2ProxyPolicy::Allow variants exist and that a NetworkProfile with wsl2_proxy_policy: Some(Wsl2ProxyPolicy::Refuse) round-trips through serialization/deserialization without loss. Name the test: test_wsl2_proxy_policy_deviation_preserved
    - Test 2 (proxy_runtime.rs test module OR confirm via denied_endpoint_returns_403_and_audit): confirm that the CompiledEndpointPolicy::compile() -> evaluate() chain is present in proxy_runtime.rs. If denied_endpoint_returns_403_and_audit (in nono-proxy/src/reverse.rs:1343) fully covers this, document that in the test module and write a unit-level test in proxy_runtime.rs asserting that proxy_runtime builds a CompiledEndpointPolicy at launch time. Name the test: test_compiled_endpoint_policy_compat_deviation_preserved
  </behavior>
  <action>
    In profile/mod.rs test module (#[cfg(test)] block at the end of the file): add test_wsl2_proxy_policy_deviation_preserved. The test must:
    1. Construct a value of Wsl2ProxyPolicy::Refuse (just proving the variant compiles and is accessible)
    2. Assert the type exists — the simplest form: let _p: Wsl2ProxyPolicy = Wsl2ProxyPolicy::Refuse; assert that wsl2_proxy_policy field is present on the NetworkProfile struct (or whatever struct carries it — read the current code to find the exact struct name).
    3. Use no unwrap() per CLAUDE.md policy; use let _ = or match.

    In proxy_runtime.rs test module: add test_compiled_endpoint_policy_compat_deviation_preserved. The test must:
    1. Assert that proxy_runtime.rs currently compiles with a reference to CompiledEndpointPolicy (read the exact import and usage site first — do not invent symbols).
    2. The minimal form: write a test that constructs CompiledEndpointPolicy::compile(None) and confirms evaluate() can be called on the result with a dummy request path. Import from nono_proxy::config as it is currently done in the file.
    3. If denied_endpoint_returns_403_and_audit already covers the evaluate() chain completely (grep for it in reverse.rs), note that in a comment and write a simpler test that just proves the type is accessible from proxy_runtime.rs context.

    Run both tests before proceeding to Task 2:
    cargo test -p nono-cli --lib profile::tests::test_wsl2_proxy_policy_deviation_preserved
    cargo test -p nono-cli --lib proxy_runtime::tests::test_compiled_endpoint_policy_compat_deviation_preserved
    Both must exit 0 — these are the baseline state before Cluster A adoption.
  </action>
  <verify>
    <automated>cargo test -p nono-cli --lib 2>&1 | grep -E "test_wsl2_proxy_policy_deviation_preserved|test_compiled_endpoint_policy_compat_deviation_preserved" | grep "ok"</automated>
  </verify>
  <acceptance_criteria>
    - Both test functions exist in their respective test modules (grep confirms function names)
    - cargo test -p nono-cli --lib (scoped to the two test names) exits 0
    - No unwrap() or expect() in new test code
    - Test for WSL2ProxyFallback references Wsl2ProxyPolicy enum directly (not via string)
    - Test for CompiledEndpointPolicy references nono_proxy::config::CompiledEndpointPolicy (not a mock)
  </acceptance_criteria>
  <done>D-08 deviation tests exist and pass in baseline (pre-adoption) state. They document the two fork-specific deviations that must survive Cluster A absorption.</done>
</task>

<task type="auto">
  <name>Task 2: Manual replay 72bcfd66 (#1225 NetworkIntent) across 11 CLI files with fork deviations preserved</name>
  <files>
    crates/nono-cli/src/capability_ext.rs,
    crates/nono-cli/src/command_runtime.rs,
    crates/nono-cli/src/execution_runtime.rs,
    crates/nono-cli/src/launch_runtime.rs,
    crates/nono-cli/src/main.rs,
    crates/nono-cli/src/output.rs,
    crates/nono-cli/src/profile/mod.rs,
    crates/nono-cli/src/proxy_runtime.rs,
    crates/nono-cli/src/sandbox_prepare.rs,
    crates/nono-cli/src/supervised_runtime.rs,
    crates/nono-cli/src/terminal_approval.rs
  </files>
  <read_first>
    Run: git show 72bcfd66 (see the full upstream diff — 11 files, 184+/130-)
    Read: .planning/phases/99-upstream-absorb-fork-invariant-verify/99-RESEARCH.md §"Cluster A" subsection (per-file action table and fork-specific deviation callouts)
    Read: .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md §"Cluster A" (per-commit table and fork deviation notes)
    Read: proj/ADR-98-network-intent-disposition.md §"Consequences" (exact touchpoints to change, guard tests to preserve)
    Read each of the 11 fork files before editing (understand current state before applying changes).
  </read_first>
  <action>
    Manually replay upstream commit 72bcfd66 (#1225) by inspecting its diff and applying the corresponding changes to the fork's 11 files. The replay process, per file:

    sandbox_prepare.rs: Add pub(crate) enum NetworkIntent { Unrestricted, BlockAll, ProxyFiltered(BoxedProxyLaunchOptions) } and associated pub(crate) accessor methods is_proxy_active() and proxy_options(). This is where NetworkIntent is DEFINED. Rename the field name per d457ecc3's companion (profile_network_block → see what 72bcfd66 actually names it in the diff). Do NOT push NetworkIntent into crates/nono/src/ — it stays pub(crate) in crates/nono-cli/src/ only (ADR-86 policy-free boundary).

    capability_ext.rs: Remove the ProxyOnly { port: 0 } placeholder set on proxy-flag path (line ~1027 per ADR-98 §Category 2). The proxy intent is now carried by NetworkIntent::ProxyFiltered. Retain the ProxyOnly { port, bind_ports } constructor (real-port path, line ~1182) — this stays; only the port: 0 placeholder is removed. Follow the 72bcfd66 diff exactly for this file.

    execution_runtime.rs: Replace matches!(caps.network_mode(), NetworkMode::ProxyOnly { .. }) proxy-activation checks with NetworkIntent-based checks per the upstream diff. This is a KEY CONFLICT FILE — the fork's version diverges from upstream here.

    command_runtime.rs: Replace nono::NetworkMode::ProxyOnly { .. } match arm for the dry-run path per the upstream diff.

    launch_runtime.rs: Adapt port/bind_ports extraction to NetworkIntent-aware path per upstream diff.

    proxy_runtime.rs: Replace placeholder-based proxy launch logic with NetworkIntent-based launch per upstream diff. CRITICAL DEVIATION: after applying upstream changes, confirm that CompiledEndpointPolicy::compile() -> evaluate() chain is still present and called. The upstream 72bcfd66 diff rewrites proxy_runtime.rs; the fork's Phase 95 CompiledEndpointPolicy wiring passes through this file. The test test_compiled_endpoint_policy_compat_deviation_preserved must still pass after edits. If the upstream diff removes the CompiledEndpointPolicy wiring, restore it as a fork-specific deviation with a comment "// Fork deviation from 72bcfd66: CompiledEndpointPolicy chain preserved (Phase 95 / ADR-98 deviation 2)".

    profile/mod.rs: Apply NetworkIntent-related changes from 72bcfd66 diff. CRITICAL DEVIATION: preserve Wsl2ProxyPolicy enum (line ~1983) and wsl2_proxy_policy field (line ~2103) exactly as they are. If 72bcfd66 touches nearby lines, do not discard the WSL2 policy block. Add a comment "// Fork deviation from 72bcfd66: Wsl2ProxyPolicy preserved (ADR-98 deviation 1)".

    output.rs: Add proxy_pending parameter to print_capabilities per 72bcfd66 diff.

    supervised_runtime.rs: Adapt port/bind_ports extraction to NetworkIntent-aware path per upstream diff.

    terminal_approval.rs: Apply NetworkIntent awareness for approval display per upstream diff.

    main.rs: Apply NetworkIntent import/use per upstream diff (likely minor).

    After all 11 files are correct, run:
    cargo test -p nono-cli --lib (must exit 0, including D-08 deviation tests)
    cargo test -p nono-proxy --lib (must exit 0, including denied_endpoint_returns_403_and_audit)
    cargo build --workspace --all-targets (must exit 0)

    Then create ONE atomic DCO-signed commit with the commit message following this pattern:
    Subject: refactor(network): introduce NetworkIntent and remove ProxyOnly placeholders
    Body: Replay of upstream 72bcfd66 (#1225). Fork deviations from upstream:
    (1) Wsl2ProxyPolicy preserved in profile/mod.rs (ADR-98 deviation 1).
    (2) CompiledEndpointPolicy::compile() -> evaluate() chain preserved in proxy_runtime.rs (ADR-98 deviation 2).
    NetworkIntent is pub(crate) within crates/nono-cli/src/ only; NetworkMode::ProxyOnly in crates/nono/src/capability.rs is untouched (ADR-86 library boundary non-regressed).
    Trailer: (cherry picked from commit 72bcfd66f98f0a1f3ff79cff1509713aaec7cdb0)
    Sign-off: Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>

    NOTE: Do NOT add a cargo clippy --workspace call here — cross-target clippy is Plan 07. Run native cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used as a quick local sanity check but the authoritative cross-target gates run in Plan 07.
  </action>
  <verify>
    <automated>cargo test -p nono-proxy --lib reverse::tests::denied_endpoint_returns_403_and_audit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - git log --oneline -3 shows a commit with subject starting "refactor(network): introduce NetworkIntent"
    - git show HEAD shows trailer "(cherry picked from commit 72bcfd66f98f0a1f3ff79cff1509713aaec7cdb0)" present
    - git show HEAD shows "Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>" present
    - grep -rn "pub(crate) enum NetworkIntent" crates/nono-cli/src/ returns a match in sandbox_prepare.rs
    - grep -rn "NetworkIntent" crates/nono/src/ returns zero matches (library stays policy-free per ADR-86)
    - grep -n "Wsl2ProxyPolicy" crates/nono-cli/src/profile/mod.rs returns count >= 1 (enum preserved)
    - grep -n "CompiledEndpointPolicy" crates/nono-cli/src/proxy_runtime.rs returns count >= 1 (chain preserved)
    - cargo test -p nono-proxy --lib exits 0 (denied_endpoint_returns_403_and_audit passes)
    - cargo test -p nono-cli --lib 2>&1 | grep -E "test_wsl2_proxy_policy_deviation_preserved|test_compiled_endpoint_policy_compat_deviation_preserved" shows "ok" for both
    - cargo build --workspace --all-targets exits 0
    - grep -rn "tls_intercept" crates/ returns zero matches (no accidental introduction)
  </acceptance_criteria>
  <done>72bcfd66 NetworkIntent refactor absorbed into the fork as one atomic DCO-signed commit. WSL2ProxyFallback deviation preserved in profile/mod.rs. CompiledEndpointPolicy chain verified intact in proxy_runtime.rs. All existing proxy tests + D-08 deviation tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI proxy intent (NetworkIntent) → sandbox enforcement | NetworkIntent is CLI-only; NetworkMode::ProxyOnly in crates/nono/ must remain the enforcement type |
| proxy_runtime.rs → CompiledEndpointPolicy evaluate() | CompiledEndpointPolicy chain must not be bypassed by NetworkIntent adoption |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01 | Tampering | proxy_runtime.rs CompiledEndpointPolicy chain | mitigate | D-08 test_compiled_endpoint_policy_compat_deviation_preserved + denied_endpoint_returns_403_and_audit must pass after adoption; action explicitly requires chain preservation |
| T-99-02 | Tampering | ADR-86 policy-free-library boundary | mitigate | grep asserts NetworkIntent absent from crates/nono/src/; pub(crate) scope prevents cross-library export |
| T-99-03 | Elevation of Privilege | WSL2ProxyFallback bypass via adoption | mitigate | D-08 test_wsl2_proxy_policy_deviation_preserved + grep asserts Wsl2ProxyPolicy still present post-replay |
| T-99-04 | Tampering | cfg(windows) drift (7 files have cfg(windows) blocks) | defer-to-P07 | Cross-target clippy gates run in Plan 07 — this plan's cargo build catches compile errors but not lint drift |
| T-99-SC | Tampering | slopcheck — no new cargo installs in this plan | accept | No new dependencies introduced; 72bcfd66 is CLI-only refactor; Cargo.toml untouched |
</threat_model>

<verification>
After both tasks complete:
- git log --oneline shows commit with "cherry picked from commit 72bcfd66" in body
- git show HEAD | grep -c "Signed-off-by: Oscar Mack Jr" returns 1
- grep -rn "pub(crate) enum NetworkIntent" crates/ returns match in nono-cli/src/sandbox_prepare.rs only
- grep -rn "NetworkIntent" crates/nono/src/ returns 0 (library untouched)
- grep -n "Wsl2ProxyPolicy" crates/nono-cli/src/profile/mod.rs returns >= 1
- grep -n "CompiledEndpointPolicy" crates/nono-cli/src/proxy_runtime.rs returns >= 1
- cargo test -p nono-proxy --lib exits 0
- cargo test -p nono-cli --lib exits 0 (including D-08 tests)
- cargo build --workspace --all-targets exits 0
</verification>

<success_criteria>
72bcfd66 (#1225 NetworkIntent refactor) is present in the fork as a single DCO-signed atomic commit with upstream SHA trailer. Both D-08 deviation tests pass (WSL2ProxyFallback and CompiledEndpointPolicy compat). The Phase 89 proxy guard test denied_endpoint_returns_403_and_audit passes. The library boundary (NetworkIntent absent from crates/nono/) is verified by grep. The fork is at a clean, building, test-passing state ready for Plan 03.
</success_criteria>

<output>
Create `.planning/phases/99-upstream-absorb-fork-invariant-verify/99-02-SUMMARY.md` when done.
</output>
