---
phase: 99-upstream-absorb-fork-invariant-verify
plan: 06
type: execute
wave: 5
depends_on:
  - 99-04
  - 99-05
files_modified:
  - crates/nono-proxy/src/pool.rs
  - crates/nono-proxy/src/lib.rs
  - crates/nono-proxy/src/reverse.rs
  - crates/nono-proxy/src/route.rs
  - crates/nono-proxy/src/server.rs
  - crates/nono-proxy/src/credential.rs
  - crates/nono-proxy/src/config.rs
  - crates/nono-proxy/Cargo.toml
  - crates/nono-cli/src/cli.rs
  - crates/nono-cli/src/launch_runtime.rs
  - crates/nono-cli/src/main.rs
  - crates/nono-cli/src/profile/mod.rs
  - crates/nono-cli/src/proxy_runtime.rs
  - crates/nono-cli/src/sandbox_prepare.rs
  - crates/nono-cli/data/nono-profile.schema.json
autonomous: true
requirements:
  - UPST11-02

must_haves:
  truths:
    - "crates/nono-proxy/src/pool.rs exists as a new module (365 lines approx)"
    - "pub mod pool present in crates/nono-proxy/src/lib.rs"
    - "NO tls_intercept/ directory exists under crates/nono-proxy/src/ after Cluster C absorption"
    - "cdeeb5b9 replay commit in git log with upstream SHA trailer and DCO sign-off"
    - "endpoint_restrictions field present in ProxyLaunchOptions in proxy_runtime.rs after 46bcfbb9"
    - "46bcfbb9 replay commit in git log with upstream SHA trailer and DCO sign-off"
    - "wildcard matching in crates/nono-proxy/src/route.rs after 08ca21a8"
    - "08ca21a8 replay commit in git log with upstream SHA trailer and DCO sign-off"
    - "cargo test -p nono-proxy --lib exits 0 (denied_endpoint_returns_403_and_audit passes)"
    - "cargo test -p nono-proxy --lib exits 0 (allow_domain_endpoint_route_does_not_shadow_credential_route passes)"
    - "CompiledEndpointPolicy::compile() -> evaluate() chain still wired after endpoint_restrictions addition"
  artifacts:
    - path: "crates/nono-proxy/src/pool.rs"
      provides: "HTTP connection pooling module (new file)"
      min_lines: 300
    - path: "crates/nono-proxy/src/lib.rs"
      provides: "pub mod pool export"
      contains: "pub mod pool"
    - path: "crates/nono-cli/src/proxy_runtime.rs"
      provides: "endpoint_restrictions field on ProxyLaunchOptions (from 46bcfbb9)"
      contains: "endpoint_restrictions"
    - path: "crates/nono-proxy/src/route.rs"
      provides: "wildcard credential upstream route matching (from 08ca21a8)"
      contains: "wildcard"
  key_links:
    - from: "crates/nono-proxy/src/pool.rs"
      to: "crates/nono-proxy/src/reverse.rs"
      via: "pool-based request construction (applicable hunks from cdeeb5b9)"
      pattern: "pool"
    - from: "crates/nono-cli/src/proxy_runtime.rs endpoint_restrictions"
      to: "nono_proxy::config::EndpointRule"
      via: "46bcfbb9 endpoint wiring — existing cross-crate dep pattern; must not bypass CompiledEndpointPolicy::compile()"
      pattern: "endpoint_restrictions"
    - from: "crates/nono-proxy/src/route.rs wildcard"
      to: "_ep_ key namespace (Phase 89 Reconciliation Addendum)"
      via: "08ca21a8 wildcard fix must preserve _ep_ key prefix in RouteStore"
      pattern: "_ep_"
---

<objective>
Manually replay the three Cluster C commits as a split absorption: cdeeb5b9 (#983 HTTP/2 + connection pooling — skip all tls_intercept/ hunks), 46bcfbb9 (#1127 endpoint wiring through to credential routes), and 08ca21a8 (#1243 wildcard credential upstream route fix). This is the most complex plan in the phase because cdeeb5b9 is a 26-file commit requiring hunk-by-hunk selection to skip the tls_intercept/ additions.

Purpose: Add HTTP/2 connection pooling and endpoint-route wiring to the fork's proxy crate, while preserving the Phase 89 fail-secure proxy invariants (CompiledEndpointPolicy chain, deny-by-default, credential shadowing model, _ep_ namespace). The tls_intercept/ directory must remain absent.
Output: Three DCO-signed replay commits; crates/nono-proxy/src/pool.rs created; no tls_intercept/; all Phase 89 proxy guard tests GREEN.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/99-upstream-absorb-fork-invariant-verify/99-CONTEXT.md
@.planning/phases/99-upstream-absorb-fork-invariant-verify/99-RESEARCH.md
@.planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md
@CLAUDE.md

<interfaces>
<!-- Cluster C split extraction table (from 99-RESEARCH.md §Cluster C) -->

cdeeb5b9 (#983) — Apply vs Skip per file:
  crates/nono-proxy/src/pool.rs        — APPLY (new 365-line module, connection pooling)
  crates/nono-proxy/src/lib.rs         — APPLY (+1 line: pub mod pool)
  crates/nono-proxy/src/reverse.rs     — APPLY applicable hunks (pool-based request construction; skip tls h2_forward/h2_probe refs)
  crates/nono-proxy/src/route.rs       — APPLY applicable hunks (post Cluster F carve-out review; Phase 89 guard tests must pass)
  crates/nono-proxy/src/server.rs      — APPLY applicable hunks (--allow-http2 flag plumbing; skip tls_intercept/ wiring)
  crates/nono-proxy/src/credential.rs  — APPLY (small improvements, 6 changed)
  crates/nono-proxy/src/config.rs      — APPLY (small improvements, 7 changed)
  crates/nono-proxy/Cargo.toml         — APPLY applicable (hyper-util, h2 dep additions for pool; skip tls-specific deps if any)
  crates/nono-cli/src/cli.rs           — APPLY (--allow-http2 CLI flag, 18 changed)
  crates/nono-cli/src/launch_runtime.rs— APPLY (2 changed)
  crates/nono-cli/src/main.rs          — APPLY (2 changed)
  crates/nono-cli/src/profile/mod.rs   — APPLY (network.allow_http2 profile field, 8 changed)
  crates/nono-cli/src/proxy_runtime.rs — APPLY (3 changed)
  crates/nono-cli/src/sandbox_prepare.rs— APPLY (12 changed)
  crates/nono-cli/data/nono-profile.schema.json — APPLY (schema update for allow_http2)
  crates/nono-proxy/src/tls_intercept/acceptor.rs — SKIP (fork lacks tls_intercept/)
  crates/nono-proxy/src/tls_intercept/h2_forward.rs — SKIP (2607 lines, fork lacks tls_intercept/)
  crates/nono-proxy/src/tls_intercept/h2_probe.rs   — SKIP (352 lines, fork lacks tls_intercept/)
  crates/nono-proxy/src/tls_intercept/handle.rs     — SKIP (fork lacks tls_intercept/)
  crates/nono-proxy/src/tls_intercept/mod.rs        — SKIP (fork lacks tls_intercept/)

46bcfbb9 (#1127) — apply:
  crates/nono-cli/src/launch_runtime.rs (+3 lines)
  crates/nono-cli/src/proxy_runtime.rs (+225/-8)
  Adds: endpoint_restrictions: Vec<(String, nono_proxy::config::EndpointRule)> field to ProxyLaunchOptions
  Verify: CompiledEndpointPolicy::compile() -> evaluate() chain not bypassed

08ca21a8 (#1243) — apply:
  crates/nono-proxy/src/route.rs (+102/-4)
  Wildcard matching for credential upstream routes
  Verify: _ep_ key namespace preserved (Phase 89 Reconciliation Addendum)

Key pitfall (from 99-RESEARCH.md §Pitfall 3):
  NEVER use git apply on the full cdeeb5b9 diff — apply per-file using:
    git show cdeeb5b9 -- <file> | git apply --3way --index -
  Then review the result and revert any tls_intercept/ additions before staging.
  Or: extract applicable hunks manually by reading the diff and editing files directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Manual replay cdeeb5b9 split (HTTP/2 pooling — skip tls_intercept/)</name>
  <files>
    crates/nono-proxy/src/pool.rs,
    crates/nono-proxy/src/lib.rs,
    crates/nono-proxy/src/reverse.rs,
    crates/nono-proxy/src/route.rs,
    crates/nono-proxy/src/server.rs,
    crates/nono-proxy/src/credential.rs,
    crates/nono-proxy/src/config.rs,
    crates/nono-proxy/Cargo.toml,
    crates/nono-cli/src/cli.rs,
    crates/nono-cli/src/launch_runtime.rs,
    crates/nono-cli/src/main.rs,
    crates/nono-cli/src/profile/mod.rs,
    crates/nono-cli/src/proxy_runtime.rs,
    crates/nono-cli/src/sandbox_prepare.rs,
    crates/nono-cli/data/nono-profile.schema.json
  </files>
  <read_first>
    Run: git show cdeeb5b9 -- crates/nono-proxy/src/pool.rs (see new pool.rs module)
    Run: git show cdeeb5b9 -- crates/nono-proxy/src/lib.rs (see pub mod pool addition)
    Run: git show cdeeb5b9 -- crates/nono-proxy/src/reverse.rs (see applicable pool-based hunks vs tls ref hunks to skip)
    Run: git show cdeeb5b9 -- crates/nono-proxy/src/server.rs (see --allow-http2 hunks vs tls_intercept wiring to skip)
    Run: git show cdeeb5b9 -- crates/nono-proxy/Cargo.toml (see new deps — evaluate if any are tls-specific)
    Read: .planning/phases/99-upstream-absorb-fork-invariant-verify/99-RESEARCH.md §"Cluster C" subsection cdeeb5b9 table and Pitfall 3
    Read: crates/nono-proxy/src/reverse.rs (current fork state — understand existing structure before applying pool hunks)
    Read: crates/nono-proxy/src/route.rs (current fork state — Phase 89 guard tests touch this file)
  </read_first>
  <action>
    Extract and apply the split for cdeeb5b9 by reading each applicable file's diff and applying by hand:

    pool.rs (new file): Create crates/nono-proxy/src/pool.rs with the full content from `git show cdeeb5b9 -- crates/nono-proxy/src/pool.rs`. This module adds HTTP connection pooling independent of tls_intercept/. The file should compile cleanly once the required deps (hyper-util, h2) are added to Cargo.toml.

    lib.rs: Add pub mod pool to crates/nono-proxy/src/lib.rs at the appropriate location per `git show cdeeb5b9 -- crates/nono-proxy/src/lib.rs`.

    credential.rs: Apply the 6 changed lines per `git show cdeeb5b9 -- crates/nono-proxy/src/credential.rs`.

    config.rs: Apply the 7 changed lines per `git show cdeeb5b9 -- crates/nono-proxy/src/config.rs`.

    Cargo.toml (nono-proxy): Apply the applicable new deps (hyper-util, h2 for connection pooling). Do NOT add any tls-specific deps that only appear in tls_intercept/ context. If uncertain about a dep, check whether pool.rs or the applicable reverse.rs/server.rs hunks import it.

    reverse.rs: Apply the pool-based request construction hunks. Skip any hunks that reference h2_forward, h2_probe, or tls_intercept::{...}. If a hunk imports from crate::tls_intercept, skip the entire import hunk. Apply only hunks that reference crate::pool.

    server.rs: Apply the --allow-http2 flag plumbing hunks. Skip any tls_intercept/ wiring hunks.

    route.rs: Apply applicable hunks (small, 52 changed). Verify Phase 89 guard tests still pass after this change — the _ep_ namespace must not be disrupted.

    CLI files (cli.rs, launch_runtime.rs, main.rs, profile/mod.rs, proxy_runtime.rs, sandbox_prepare.rs, data/nono-profile.schema.json): Apply per `git show cdeeb5b9 -- <file>`. These are smaller changes for --allow-http2 CLI flag surfacing.

    After all changes: verify tls_intercept/ is NOT present:
    ls crates/nono-proxy/src/ (must NOT show tls_intercept/)

    Run: cargo build --workspace --all-targets (must exit 0)
    Run: cargo test -p nono-proxy --lib reverse::tests::denied_endpoint_returns_403_and_audit (must pass)

    Then create ONE atomic DCO-signed commit:
    Subject: feat(proxy): add HTTP/2 connection pooling and --allow-http2 flag
    Body: Split replay of upstream cdeeb5b9 (#983). Applied: pool.rs (new HTTP connection pooling module), applicable hunks in reverse.rs/route.rs/server.rs/credential.rs/config.rs, --allow-http2 CLI flag plumbing. SKIPPED: all crates/nono-proxy/src/tls_intercept/ files (fork has no TLS-interception surface; this is a deliberate fork divergence). The tls_intercept/ directory is absent and must remain absent.
    Trailer: (cherry picked from commit cdeeb5b9...)
    Sign-off: Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>
  </action>
  <verify>
    <automated>ls crates/nono-proxy/src/ && cargo test -p nono-proxy --lib 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ls crates/nono-proxy/src/ output does NOT contain tls_intercept
    - crates/nono-proxy/src/pool.rs exists (new file)
    - grep "pub mod pool" crates/nono-proxy/src/lib.rs returns >= 1 match
    - git log --oneline -1 shows commit with subject "feat(proxy): add HTTP/2 connection pooling and --allow-http2 flag"
    - git show HEAD | grep "(cherry picked from commit cdeeb5b9" returns a match
    - git show HEAD | grep "Signed-off-by: Oscar Mack Jr" returns a match
    - cargo build --workspace --all-targets exits 0
    - cargo test -p nono-proxy --lib exits 0 (denied_endpoint_returns_403_and_audit passes)
    - grep -rn "h2_forward\|h2_probe\|tls_intercept" crates/nono-proxy/src/ returns 0 (no tls imports accidentally introduced)
  </acceptance_criteria>
  <done>cdeeb5b9 split absorbed: pool.rs new module created, applicable proxy surface hunks applied, all tls_intercept/ files skipped. No tls_intercept/ dir. Phase 89 proxy guard tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Manual replay 46bcfbb9 (endpoint wiring) + 08ca21a8 (wildcard route) + guard test verification</name>
  <files>
    crates/nono-cli/src/launch_runtime.rs,
    crates/nono-cli/src/proxy_runtime.rs,
    crates/nono-proxy/src/route.rs
  </files>
  <read_first>
    Run: git show 46bcfbb9 (see the diff: launch_runtime.rs +3, proxy_runtime.rs +225/-8 — endpoint_restrictions field addition)
    Run: git show 08ca21a8 (see the diff: route.rs +102/-4 — wildcard matching)
    Read: crates/nono-cli/src/proxy_runtime.rs (current state post-Plans-02/03 Cluster A adoption; confirm CompiledEndpointPolicy chain location)
    Read: .planning/phases/99-upstream-absorb-fork-invariant-verify/99-RESEARCH.md §"Cluster C" subsections 46bcfbb9 and 08ca21a8
    Read: .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md §"Cluster C" for 46bcfbb9 compatibility notes
  </read_first>
  <action>
    Apply 46bcfbb9 (#1127 endpoint wiring):
    Inspect `git show 46bcfbb9`. This commit adds endpoint_restrictions: Vec<(String, nono_proxy::config::EndpointRule)> field to ProxyLaunchOptions in proxy_runtime.rs and wires it through to credential routes at prepare stage.

    In proxy_runtime.rs: Apply the +225/-8 changes. The key compatibility check: after applying these changes, verify the CompiledEndpointPolicy::compile() -> evaluate() chain is still wired in proxy_runtime.rs. The 46bcfbb9 diff adds endpoint_restrictions parsing (SERVICE:METHOD:PATH at prepare stage) and applies to matching credential routes before proxy config is built. This must NOT replace or bypass the fork's CompiledEndpointPolicy::compile() path. If they operate in different code paths, that is fine; if they conflict, preserve the fork's evaluate() chain and add endpoint_restrictions alongside it.

    In launch_runtime.rs: Apply the +3 line change (wires endpoint_restrictions from CLI args to ProxyLaunchOptions).

    After applying 46bcfbb9: run cargo test -p nono-proxy --lib reverse::tests::denied_endpoint_returns_403_and_audit (must pass). If it fails, the proxy_runtime.rs conflict resolution is broken — fix before committing.

    Commit ONE atomic DCO-signed commit for 46bcfbb9:
    Subject: fix(network): wire --allow-endpoint through to credential routes
    Body: Replay of upstream 46bcfbb9 (#1127). Adds endpoint_restrictions field to ProxyLaunchOptions; parses SERVICE:METHOD:PATH at prepare stage. Fork deviation preserved: CompiledEndpointPolicy::compile() -> evaluate() chain in proxy_runtime.rs not bypassed (D-04 compatibility check).
    Trailer: (cherry picked from commit 46bcfbb9...)
    Sign-off: Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>

    Apply 08ca21a8 (#1243 wildcard route fix):
    Inspect `git show 08ca21a8`. This is a targeted additive fix in route.rs (+102/-4): wildcard pattern matching for credential upstream routes. Apply the changes, then verify the _ep_ key namespace is preserved: grep -n "_ep_" crates/nono-proxy/src/route.rs returns same or more matches as before (Phase 89 Reconciliation Addendum).

    Commit ONE atomic DCO-signed commit for 08ca21a8:
    Subject: fix(proxy): match wildcard credential upstream routes
    Body: Replay of upstream 08ca21a8 (#1243). Adds wildcard pattern matching for credential upstream routes in route.rs. Verified: _ep_ key namespace preserved.
    Trailer: (cherry picked from commit 08ca21a8...)
    Sign-off: Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>

    Run full Phase 89 proxy guard test suite to confirm Cluster C absorption is complete and correct:
    cargo test -p nono-proxy --lib reverse::tests::denied_endpoint_returns_403_and_audit
    cargo test -p nono-proxy --lib route::tests::allow_domain_endpoint_route_does_not_shadow_credential_route
    cargo test -p nono-proxy --lib (full suite)
    cargo test -p nono-cli --lib (full suite including D-08 tests)
    cargo build --workspace --all-targets
  </action>
  <verify>
    <automated>cargo test -p nono-proxy --lib 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - git log --oneline -3 shows: 08ca21a8 replay (HEAD), 46bcfbb9 replay (HEAD~1), cdeeb5b9 replay (HEAD~2)
    - git show HEAD | grep "(cherry picked from commit 08ca21a8" returns a match
    - git show HEAD~1 | grep "(cherry picked from commit 46bcfbb9" returns a match
    - grep "endpoint_restrictions" crates/nono-cli/src/proxy_runtime.rs returns >= 1 match
    - grep "CompiledEndpointPolicy" crates/nono-cli/src/proxy_runtime.rs returns >= 1 match (chain not removed by 46bcfbb9)
    - grep "_ep_" crates/nono-proxy/src/route.rs returns >= 1 match (namespace preserved)
    - cargo test -p nono-proxy --lib exits 0 (all guard tests pass including denied_endpoint_returns_403_and_audit and allow_domain_endpoint_route_does_not_shadow_credential_route)
    - cargo test -p nono-cli --lib exits 0 (D-08 tests pass)
    - cargo build --workspace --all-targets exits 0
    - ls crates/nono-proxy/src/ does NOT contain tls_intercept (final confirmation post all Cluster C commits)
  </acceptance_criteria>
  <done>All 3 Cluster C commits absorbed as three atomic DCO-signed commits. pool.rs new module exists. tls_intercept/ absent. endpoint_restrictions wired without bypassing CompiledEndpointPolicy chain. Wildcard route fix applied with _ep_ namespace preserved. All Phase 89 proxy guard tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cluster C split application → tls_intercept/ absence | Must not create tls_intercept/ directory as a side effect |
| 46bcfbb9 endpoint_restrictions → CompiledEndpointPolicy chain | endpoint_restrictions must not bypass the evaluate() chain |
| route.rs wildcard fix → _ep_ key namespace | Wildcard pattern must not shadow or interfere with _ep_ credential routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-14 | Elevation of Privilege | tls_intercept/ accidentally created during cdeeb5b9 split | mitigate | ls crates/nono-proxy/src/ after commit: must not contain tls_intercept; grep -rn h2_forward crates/nono-proxy/src/ must return 0 |
| T-99-15 | Tampering | CompiledEndpointPolicy evaluate() bypassed by endpoint_restrictions | mitigate | denied_endpoint_returns_403_and_audit must pass after 46bcfbb9 replay; grep confirms both endpoint_restrictions AND CompiledEndpointPolicy in proxy_runtime.rs |
| T-99-16 | Tampering | _ep_ credential route namespace disrupted by wildcard fix | mitigate | grep _ep_ route.rs before and after: must be same count or more; allow_domain_endpoint_route_does_not_shadow_credential_route test must pass |
| T-99-SC | Tampering | slopcheck — Cargo.toml new deps (hyper-util, h2) | mitigate | cdeeb5b9 is an upstream commit from nolabs-ai/nono (trusted author); hyper-util and h2 are established Rust HTTP ecosystem crates (hyperium org); not [ASSUMED] packages; verify package names match expected orgs in crates.io before adding |
</threat_model>

<verification>
After both tasks:
- git log --oneline -3 shows 3 Cluster C replay commits (SHA trailers + DCO sign-offs)
- ls crates/nono-proxy/src/ does NOT contain tls_intercept
- crates/nono-proxy/src/pool.rs exists
- grep "pub mod pool" crates/nono-proxy/src/lib.rs: >= 1 match
- grep "endpoint_restrictions" crates/nono-cli/src/proxy_runtime.rs: >= 1 match
- grep "CompiledEndpointPolicy" crates/nono-cli/src/proxy_runtime.rs: >= 1 match
- grep "_ep_" crates/nono-proxy/src/route.rs: >= 1 match
- grep -rn "h2_forward\|h2_probe\|tls_intercept" crates/nono-proxy/src/: 0 matches
- cargo test -p nono-proxy --lib exits 0
- cargo test -p nono-cli --lib exits 0
- cargo build --workspace --all-targets exits 0
</verification>

<success_criteria>
Cluster C is fully absorbed: 3 commits in git history (cdeeb5b9, 46bcfbb9, 08ca21a8), all DCO-signed with upstream SHA trailers. pool.rs new module exists. tls_intercept/ is absent. endpoint_restrictions wiring is present without bypassing CompiledEndpointPolicy evaluate() chain. _ep_ namespace preserved. Phase 89 proxy guard tests all pass. The fork is ready for Plan 07 (fork-invariant verify gate).
</success_criteria>

<output>
Create `.planning/phases/99-upstream-absorb-fork-invariant-verify/99-06-SUMMARY.md` when done.
</output>
