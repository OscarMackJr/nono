---
phase: 99-upstream-absorb-fork-invariant-verify
plan: 04
type: execute
wave: 4
depends_on:
  - 99-03
files_modified:
  - crates/nono/src/sandbox/linux.rs
  - crates/nono-cli/src/profile/mod.rs
  - crates/nono-cli/src/proxy_runtime.rs
  - crates/nono-proxy/src/route.rs
  - crates/nono-cli/src/setup.rs
  - crates/nono-cli/src/test_env.rs
  - crates/nono-cli/src/update_check.rs
autonomous: true
requirements:
  - UPST11-02
  - UPST11-03

must_haves:
  truths:
    - "5b8e94da replay commit in git log with upstream SHA trailer and DCO sign-off"
    - "is_9p_path() function present in crates/nono/src/sandbox/linux.rs (additive, new function)"
    - "fork GPU tests (test_is_nvidia_compute_device_*) still present in linux.rs test module after 9P replay"
    - "cargo test -p nono --lib sandbox::linux exits 0 (seccomp/cgroup/GPU tests all pass)"
    - "c808f000 replay commit in git log with upstream SHA trailer and DCO sign-off"
    - "OscarMackJr/nono fork identity URLs preserved in production code (not rewritten to nolabs-ai)"
    - "always-further/nono upstream references rewritten to nolabs-ai/nono in drift-filter files"
    - "grep OscarMackJr crates/ returns results for fork identity (URLs not wiped)"
  artifacts:
    - path: "crates/nono/src/sandbox/linux.rs"
      provides: "9P filesystem detection warning (is_9p_path function + insertion before Landlock capability-add loop)"
      contains: "is_9p_path"
    - path: "crates/nono-cli/src/update_check.rs"
      provides: "always-further references migrated to nolabs-ai in test fixtures; NONO_UPDATE_URL constant stays OscarMackJr/nono"
      contains: "nolabs-ai"
  key_links:
    - from: "crates/nono/src/sandbox/linux.rs is_9p_path() call site"
      to: "Landlock capability-add loop (before the loop)"
      via: "additive insertion — does NOT touch AF_UNIX EPERM filter or cgroup module"
      pattern: "is_9p_path"
    - from: "crates/nono-cli/src/update_check.rs production NONO_UPDATE_URL"
      to: "OscarMackJr/nono (fork identity)"
      via: "Cluster E migration must not overwrite fork identity URL"
      pattern: "OscarMackJr"
---

<objective>
Manually replay two will-sync commits in sequence: 5b8e94da (Cluster D — 9P filesystem warning additive to linux.rs) and c808f000 (Cluster E — org-ref migration from always-further to nolabs-ai). These touch non-overlapping files and are independent of each other but both depend on Cluster A being complete (D-07 order).

Purpose: Close two simple will-sync rows in the ledger. Cluster D is an additive linux.rs diagnostic; Cluster E is a mechanical string-replacement. Both require care: D must not touch the GPU test section or the AF_UNIX/seccomp invariants; E must not overwrite OscarMackJr/nono fork identity URLs.
Output: Two DCO-signed replay commits (one per upstream SHA) with the linux.rs seccomp guard tests and full cli lib tests passing.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/99-upstream-absorb-fork-invariant-verify/99-CONTEXT.md
@.planning/phases/99-upstream-absorb-fork-invariant-verify/99-RESEARCH.md
@.planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md
@CLAUDE.md

<interfaces>
<!-- Cluster D — 5b8e94da (#1207): linux.rs additive 9P warning -->
<!-- From 99-RESEARCH.md §Cluster D and 98-DIVERGENCE-LEDGER.md §Cluster D -->
5b8e94da: +128 lines pure addition to crates/nono/src/sandbox/linux.rs
  New function: is_9p_path(path: &Path) -> bool  (uses statfs(2) checking f_type against V9FS_MAGIC 0x01021997)
  Insertion point: before the Landlock capability-add loop
  Pitfall: the fork has added test_is_nvidia_compute_device_* tests AFTER the upstream test section
            that 5b8e94da modifies — do NOT discard the GPU tests; they are fork-only additions

<!-- Cluster E — c808f000 (#1235): org-ref migration always-further → nolabs-ai -->
<!-- From 99-RESEARCH.md §Cluster E -->
Drift-filter relevant files: profile/mod.rs, proxy_runtime.rs, route.rs (nono-proxy), setup.rs, test_env.rs, update_check.rs
Files DELETED in fork (no action): command_policy.rs, migration.rs
PRESERVE: OscarMackJr/nono URLs in ALL production code paths
APPLY: always-further/nono.git → nolabs-ai/nono.git replacements only
Safe to apply: update_check.rs test fixture JSON value (not the production NONO_UPDATE_URL constant)
Safe to apply: route.rs test case fixtures using /always-further/nono.git/ paths → /nolabs-ai/nono.git/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Manual replay 5b8e94da (Cluster D — 9P filesystem warning in linux.rs)</name>
  <files>crates/nono/src/sandbox/linux.rs</files>
  <read_first>
    Run: git show 5b8e94da (see the upstream diff: +128 lines additive to linux.rs)
    Read: crates/nono/src/sandbox/linux.rs (current fork state; identify the Landlock capability-add loop where the 9P check inserts; find the test module at the end to confirm GPU tests are present)
    Read: .planning/phases/99-upstream-absorb-fork-invariant-verify/99-RESEARCH.md §"Cluster D" and "Pitfall 1: Leaving linux.rs GPU tests unresolved"
  </read_first>
  <action>
    Inspect `git show 5b8e94da` to extract the 9P warning changes:
    1. The new is_9p_path(path: &Path) -> bool function that checks statfs f_type against V9FS_MAGIC (0x01021997) — extract this function verbatim from the upstream diff
    2. The insertion point: the 9P check is inserted before the Landlock capability-add loop in apply_landlock() or equivalent function — find the exact location in the upstream diff and apply at the corresponding location in the fork file
    3. The deduplication mechanism (warnings per path) — apply per upstream diff

    Apply ONLY the is_9p_path function addition and the check insertion. Do NOT modify the test module section at the bottom of linux.rs. The fork's test_is_nvidia_compute_device_* GPU tests live after the upstream test section and must be preserved.

    After edits:
    cargo test -p nono --lib sandbox::linux exits 0 (all existing tests pass including GPU tests)

    Then create ONE atomic DCO-signed commit:
    Subject: fix(sandbox): warn when capability path is on a 9P filesystem
    Body: Replay of upstream 5b8e94da (#1207). Adds is_9p_path() check before Landlock capability-add loop; paths on 9P mounts (WSL2 /mnt/c, QEMU virtfs) emit a warning. Fork's GPU tests (test_is_nvidia_compute_device_*) preserved — they appear after the upstream test section that this commit modifies.
    Trailer: (cherry picked from commit 5b8e94da...)
    Sign-off: Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>
  </action>
  <verify>
    <automated>cargo test -p nono --lib sandbox::linux 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - git log --oneline -1 shows commit with subject "fix(sandbox): warn when capability path is on a 9P filesystem"
    - git show HEAD | grep "(cherry picked from commit 5b8e94da" returns a match
    - git show HEAD | grep "Signed-off-by: Oscar Mack Jr" returns a match
    - grep -n "is_9p_path" crates/nono/src/sandbox/linux.rs returns >= 1 (function added)
    - grep -n "test_is_nvidia_compute_device" crates/nono/src/sandbox/linux.rs returns >= 1 (fork GPU tests preserved)
    - cargo test -p nono --lib sandbox::linux exits 0 (ALL linux tests pass including GPU, AF_UNIX, seccomp, cgroup-v2)
    - cargo test -p nono --lib sandbox::linux::tests::test_seccomp_network_fallback_mode_proxy_only exits 0
    - cargo test -p nono --lib sandbox::linux::tests::test_proxy_only_with_landlock_v4_returns_no_fallback exits 0
  </acceptance_criteria>
  <done>5b8e94da (9P warning) replayed as one DCO-signed atomic commit. is_9p_path() present in linux.rs. Fork's GPU tests and AF_UNIX/seccomp/cgroup guard tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Manual replay c808f000 (Cluster E — org-ref migration, preserve OscarMackJr/nono)</name>
  <files>
    crates/nono-cli/src/profile/mod.rs,
    crates/nono-cli/src/proxy_runtime.rs,
    crates/nono-proxy/src/route.rs,
    crates/nono-cli/src/setup.rs,
    crates/nono-cli/src/test_env.rs,
    crates/nono-cli/src/update_check.rs
  </files>
  <read_first>
    Run: git show c808f000 (see the full diff; focus on the 8 drift-filter-relevant files)
    Read: .planning/phases/99-upstream-absorb-fork-invariant-verify/99-RESEARCH.md §"Cluster E" and "Pitfall 2: Deleting fork-identity URLs during Cluster E replay"
    Read: crates/nono-cli/src/update_check.rs (find the production NONO_UPDATE_URL constant to confirm it points to OscarMackJr/nono — this must NOT change)
    Run: grep -rn "OscarMackJr" crates/ (record the current fork-identity URLs before making any changes — these must survive)
  </read_first>
  <action>
    Inspect `git show c808f000` and for each drift-filter-relevant file that exists in the fork, apply ONLY the always-further/nono → nolabs-ai/nono string replacements. Files deleted in the fork (command_policy.rs, migration.rs) require NO action — they don't exist.

    For each file, apply only the lines that change always-further/nono references:
    - profile/mod.rs: apply the +2/-1 changes (profile loading references → nolabs-ai)
    - proxy_runtime.rs: apply the +4/-2 changes (proxy-related URL references → nolabs-ai)
    - route.rs (nono-proxy): apply changes — test fixtures using /always-further/nono.git/ paths → /nolabs-ai/nono.git/ (these are test fixtures in route.rs, safe to update)
    - setup.rs: apply the +4/-4 changes (GitHub URL references in setup instructions → nolabs-ai)
    - test_env.rs: apply the +2/-1 changes (test environment references → nolabs-ai)
    - update_check.rs: apply ONLY the test fixture JSON value change — the production NONO_UPDATE_URL constant (default URL pointing to OscarMackJr/nono releases) must NOT be changed

    After each file edit, immediately run: grep -n "OscarMackJr" <file> to confirm fork identity URLs are still present in production paths (not overwritten).

    Explicitly verify: grep -n "OscarMackJr" crates/nono-cli/src/update_check.rs returns >= 1 result pointing to the production URL constant.

    After all files edited:
    cargo test -p nono-cli --lib exits 0
    cargo test -p nono-proxy --lib exits 0

    Then create ONE atomic DCO-signed commit:
    Subject: chore: migrate GitHub org references from always-further to nolabs-ai
    Body: Replay of upstream c808f000 (#1235). Applies always-further/nono → nolabs-ai/nono string replacements in drift-filter files. command_policy.rs and migration.rs are absent in fork (deleted) — no action needed. OscarMackJr/nono fork identity URLs are preserved in all production code paths (not rewritten).
    Trailer: (cherry picked from commit c808f000...)
    Sign-off: Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>
  </action>
  <verify>
    <automated>grep -rn "OscarMackJr" crates/nono-cli/src/update_check.rs | head -5</automated>
  </verify>
  <acceptance_criteria>
    - git log --oneline -1 shows commit with subject "chore: migrate GitHub org references from always-further to nolabs-ai"
    - git show HEAD | grep "(cherry picked from commit c808f000" returns a match
    - git show HEAD | grep "Signed-off-by: Oscar Mack Jr" returns a match
    - grep -rn "OscarMackJr" crates/nono-cli/src/update_check.rs returns >= 1 (fork identity URL in production constant preserved)
    - grep -rn "always-further" crates/nono-cli/src/profile/mod.rs returns 0 (upstream ref migrated)
    - grep -rn "always-further" crates/nono-cli/src/setup.rs returns 0 (upstream ref migrated)
    - grep -rn "nolabs-ai" crates/nono-cli/src/setup.rs returns >= 1 (migration applied)
    - cargo test -p nono-cli --lib exits 0
    - cargo test -p nono-proxy --lib exits 0
    - ls crates/nono-cli/src/command_policy.rs 2>&1 shows file absent (not created as a side effect)
  </acceptance_criteria>
  <done>c808f000 (org-ref migration) replayed as one DCO-signed atomic commit. Fork-identity OscarMackJr/nono URLs preserved in production paths. always-further upstream references migrated to nolabs-ai in drift-filter files. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| linux.rs 9P check insertion → Landlock capability-add loop | 9P check must be additive only; must not affect AF_UNIX EPERM filter or cgroup enforcement |
| Org-ref migration → fork identity URLs | always-further replacements must not overwrite OscarMackJr/nono production URLs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-08 | Tampering | linux.rs AF_UNIX EPERM filter weakened by 9P insertion | mitigate | test_seccomp_network_fallback_mode_proxy_only + test_proxy_only_with_landlock_v4_returns_no_fallback must pass post-replay; is_9p_path insertion is additive (no modification of existing code) |
| T-99-09 | Information Disclosure | OscarMackJr/nono fork identity URL overwritten by Cluster E | mitigate | grep -n OscarMackJr update_check.rs after edit; action explicitly excludes NONO_UPDATE_URL constant from migration |
| T-99-10 | Tampering | GPU test_is_nvidia_compute_device_* deleted during linux.rs edit | mitigate | grep test_is_nvidia_compute_device after replay; action explicitly says not to touch test module |
| T-99-SC | Tampering | slopcheck — no new cargo installs | accept | No Cargo.toml changes in this plan; string replacements and code additions only |
</threat_model>

<verification>
After both tasks:
- git log --oneline -2 shows c808f000 replay (HEAD) and 5b8e94da replay (HEAD~1)
- grep -n "is_9p_path" crates/nono/src/sandbox/linux.rs: >= 1 match
- grep -n "test_is_nvidia_compute_device" crates/nono/src/sandbox/linux.rs: >= 1 match (GPU tests preserved)
- grep -rn "OscarMackJr" crates/nono-cli/src/update_check.rs: >= 1 match (fork identity preserved)
- grep -rn "always-further" crates/nono-cli/src/setup.rs: 0 matches (migration applied)
- cargo test -p nono --lib sandbox::linux exits 0
- cargo test -p nono-cli --lib exits 0
- cargo test -p nono-proxy --lib exits 0
</verification>

<success_criteria>
Clusters D and E are absorbed: 5b8e94da (9P warning) and c808f000 (org-ref migration) are in git history as two atomic DCO-signed commits. The fork's GPU tests and linux.rs invariants survive. OscarMackJr/nono fork identity URLs are preserved. The fork is ready for Plan 07 verify gate after Plans 05 and 06 complete.
</success_criteria>

<output>
Create `.planning/phases/99-upstream-absorb-fork-invariant-verify/99-04-SUMMARY.md` when done.
</output>
