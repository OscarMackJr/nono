---
phase: 99-upstream-absorb-fork-invariant-verify
plan: 03
type: execute
wave: 3
depends_on:
  - 99-02
files_modified:
  - crates/nono-cli/src/command_runtime.rs
  - crates/nono-cli/src/launch_runtime.rs
  - crates/nono-cli/src/sandbox_prepare.rs
autonomous: true
requirements:
  - UPST11-02

must_haves:
  truths:
    - "d457ecc3 replay commit in git log with '(cherry picked from commit d457ecc3...)' trailer and DCO sign-off"
    - "validate_block_net_conflicts() function present in sandbox_prepare.rs"
    - "validate_block_net_conflicts() called from command_runtime.rs (dry-run path) and launch_runtime.rs (main launch)"
    - "cargo test -p nono-proxy --lib exits 0 (all Phase 89 proxy guard tests pass)"
    - "cargo test -p nono-cli --lib exits 0 (D-08 deviation tests pass)"
    - "cargo test -p nono --lib sandbox::linux exits 0 (linux.rs invariants unaffected by A)"
  artifacts:
    - path: "crates/nono-cli/src/sandbox_prepare.rs"
      provides: "validate_block_net_conflicts() function using NetworkIntent types from Plan 02"
      contains: "validate_block_net_conflicts"
    - path: "crates/nono-cli/src/launch_runtime.rs"
      provides: "validate_block_net_conflicts() call before sandbox/proxy setup"
      contains: "validate_block_net_conflicts"
    - path: "crates/nono-cli/src/command_runtime.rs"
      provides: "validate_block_net_conflicts() call on dry-run path"
      contains: "validate_block_net_conflicts"
  key_links:
    - from: "launch_runtime.rs validate_block_net_conflicts() call site"
      to: "sandbox_prepare.rs validate_block_net_conflicts() definition"
      via: "pub(crate) fn — intra-CLI only, uses NetworkIntent types from 72bcfd66"
      pattern: "validate_block_net_conflicts"
    - from: "crates/nono-proxy/src/reverse.rs denied_endpoint_returns_403_and_audit"
      to: "crates/nono-cli/src/proxy_runtime.rs CompiledEndpointPolicy chain"
      via: "Phase 89 guard test — primary signal for endpoint-policy chain integrity"
      pattern: "denied_endpoint_returns_403_and_audit"
---

<objective>
Manually replay upstream commit d457ecc3 (#1263 "error early on contradictory network flag combinations") as the companion to 72bcfd66 absorbed in Plan 02. Then run the full set of Phase 89 proxy guard tests and D-08 deviation tests to confirm the endpoint-policy chain and deviation invariants are intact through the complete Cluster A adoption.

Purpose: d457ecc3 depends on the NetworkIntent type from 72bcfd66 and adds validate_block_net_conflicts() — early validation of --block-net + proxy/credential/network-profile/allow-domain flag conflicts. It also adds has_proxy_intent() for display accuracy. Both are additive safety logic that must apply before sandbox or proxy setup.
Output: One DCO-signed replay commit for d457ecc3; Phase 89 proxy guard tests GREEN.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/99-upstream-absorb-fork-invariant-verify/99-CONTEXT.md
@.planning/phases/99-upstream-absorb-fork-invariant-verify/99-RESEARCH.md
@.planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md
@proj/ADR-98-network-intent-disposition.md
@CLAUDE.md

<interfaces>
<!-- d457ecc3 adds these to the already-adopted NetworkIntent surface -->
<!-- From 99-RESEARCH.md §Cluster A and ADR-98 §Consequences -->

d457ecc3 (#1263) changes — 3 files, 254+/8-:
  sandbox_prepare.rs: adds pub(crate) fn validate_block_net_conflicts(args: &Args, network: &NetworkIntent) -> Result<()>
  launch_runtime.rs: calls validate_block_net_conflicts() before any sandbox or proxy setup (+256 lines of new logic)
  command_runtime.rs: calls validate_block_net_conflicts() on dry-run path

Guard tests that must pass (Phase 89 + D-08):
  cargo test -p nono-proxy --lib reverse::tests::denied_endpoint_returns_403_and_audit
  cargo test -p nono-proxy --lib route::tests::allow_domain_endpoint_route_does_not_shadow_credential_route
  cargo test -p nono-proxy --lib -- proxy_activates_with_custom_credentials_only  (in proxy_runtime.rs or nono-proxy)
  cargo test -p nono-cli --lib -- test_wsl2_proxy_policy_deviation_preserved
  cargo test -p nono-cli --lib -- test_compiled_endpoint_policy_compat_deviation_preserved
  cargo test -p nono --lib sandbox::linux::tests::test_proxy_only_with_landlock_v4_returns_no_fallback
  cargo test -p nono --lib sandbox::linux::tests::test_seccomp_network_fallback_mode_proxy_only

Commit trailer pattern (D-06):
  (cherry picked from commit d457ecc3...)
  Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Manual replay d457ecc3 (#1263 validate_block_net_conflicts)</name>
  <files>crates/nono-cli/src/sandbox_prepare.rs, crates/nono-cli/src/command_runtime.rs, crates/nono-cli/src/launch_runtime.rs</files>
  <read_first>
    Run: git show d457ecc3 (see the full diff: sandbox_prepare.rs +256 lines, command_runtime.rs and launch_runtime.rs call sites)
    Read: crates/nono-cli/src/sandbox_prepare.rs (current fork state post-Plan-02 adoption)
    Read: crates/nono-cli/src/command_runtime.rs (current state post-Plan-02)
    Read: crates/nono-cli/src/launch_runtime.rs (current state post-Plan-02)
    Read: .planning/phases/99-upstream-absorb-fork-invariant-verify/99-RESEARCH.md §"Cluster A" subsection for d457ecc3 replay notes.
  </read_first>
  <action>
    Inspect `git show d457ecc3` and apply the changes manually to the three files.

    sandbox_prepare.rs: Add pub(crate) fn validate_block_net_conflicts(args, network: &NetworkIntent) -> Result<()> from the upstream diff. This function validates: --block-net conflicts with custom credentials, network-profile, allow-domain flags; --proxy-port without proxy-triggering flags. It uses NetworkIntent type from 72bcfd66 (already present from Plan 02). Also add pub(crate) fn has_proxy_intent() if present in the diff.

    command_runtime.rs: Add the validate_block_net_conflicts() call on the dry-run path per the upstream diff. The call goes before any sandbox or proxy setup.

    launch_runtime.rs: Add the validate_block_net_conflicts() call on the main launch path per the upstream diff. The call goes before any sandbox or proxy setup.

    If the upstream diff references profile_network_block as a field that was renamed by 72bcfd66, use the name as it exists in the fork's post-Plan-02 state (read the current file to confirm the current field name).

    After edits, run cargo build --workspace --all-targets to confirm the three changed files compile without error.

    Then create ONE atomic DCO-signed commit with subject: fix(network): error early on contradictory network flag combinations
    Body: Replay of upstream d457ecc3 (#1263). Adds validate_block_net_conflicts() called before sandbox/proxy setup; catches --block-net combined with proxy/credential/network-profile/allow-domain flags. Depends on NetworkIntent type from 72bcfd66 (Plan 02).
    Trailer: (cherry picked from commit d457ecc3...)  [use full SHA from git show d457ecc3 output]
    Sign-off: Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>
  </action>
  <verify>
    <automated>cargo build --workspace --all-targets 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - git log --oneline -1 shows commit with subject "fix(network): error early on contradictory network flag combinations"
    - git show HEAD | grep "(cherry picked from commit d457ecc3" returns a match
    - git show HEAD | grep "Signed-off-by: Oscar Mack Jr" returns a match
    - grep -n "validate_block_net_conflicts" crates/nono-cli/src/sandbox_prepare.rs returns >= 1 (function defined)
    - grep -n "validate_block_net_conflicts" crates/nono-cli/src/launch_runtime.rs returns >= 1 (call site)
    - grep -n "validate_block_net_conflicts" crates/nono-cli/src/command_runtime.rs returns >= 1 (call site)
    - cargo build --workspace --all-targets exits 0
  </acceptance_criteria>
  <done>d457ecc3 replayed as one DCO-signed commit. validate_block_net_conflicts() present and called from both launch paths. Workspace builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Phase 89 proxy guard tests + D-08 deviation tests + linux.rs seccomp guard tests</name>
  <files></files>
  <read_first>
    Read: .planning/phases/99-upstream-absorb-fork-invariant-verify/99-RESEARCH.md §"Guard Test Map" (exact test names and file locations for all guard tests in D-09).
    No file edits in this task — verification only. If a test fails, read the relevant source file and fix the issue in that file before re-running.
  </read_first>
  <action>
    Run the complete set of D-09 guard tests verifying the Cluster A adoption is correct. All must pass:

    Phase 89 proxy guard tests:
    cargo test -p nono-proxy --lib reverse::tests::denied_endpoint_returns_403_and_audit
    cargo test -p nono-proxy --lib route::tests::allow_domain_endpoint_route_does_not_shadow_credential_route
    cargo test -p nono-proxy --lib (full proxy lib suite)

    D-08 deviation tests:
    cargo test -p nono-cli --lib -- test_wsl2_proxy_policy_deviation_preserved
    cargo test -p nono-cli --lib -- test_compiled_endpoint_policy_compat_deviation_preserved

    linux.rs seccomp guard tests (confirming Cluster A did not regress linux.rs invariants):
    cargo test -p nono --lib sandbox::linux::tests::test_proxy_only_with_landlock_v4_returns_no_fallback
    cargo test -p nono --lib sandbox::linux::tests::test_seccomp_network_fallback_mode_proxy_only
    cargo test -p nono --lib sandbox::linux::tests::test_seccomp_network_fallback_mode_proxy_only_with_bind

    Full lib suite to catch any other breakage:
    cargo test -p nono-cli --lib

    If any test fails: read the failing test + the source file it tests; diagnose whether the failure is in the replay resolution or a pre-existing issue. For proxy guard test failures, check that proxy_runtime.rs's CompiledEndpointPolicy wiring was not accidentally dropped during the 72bcfd66 replay. Fix inline if needed.
  </action>
  <verify>
    <automated>cargo test -p nono-proxy --lib 2>&1 | tail -5 && cargo test -p nono --lib sandbox::linux 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p nono-proxy --lib exits 0 (all proxy tests pass including denied_endpoint_returns_403_and_audit and allow_domain_endpoint_route_does_not_shadow_credential_route)
    - cargo test -p nono --lib sandbox::linux::tests::test_proxy_only_with_landlock_v4_returns_no_fallback exits 0
    - cargo test -p nono --lib sandbox::linux::tests::test_seccomp_network_fallback_mode_proxy_only exits 0
    - cargo test -p nono --lib sandbox::linux::tests::test_seccomp_network_fallback_mode_proxy_only_with_bind exits 0
    - cargo test -p nono-cli --lib 2>&1 | grep "test_wsl2_proxy_policy_deviation_preserved" shows "ok"
    - cargo test -p nono-cli --lib 2>&1 | grep "test_compiled_endpoint_policy_compat_deviation_preserved" shows "ok"
    - No test exits with FAILED status
  </acceptance_criteria>
  <done>Cluster A absorption is fully verified: both D-08 deviation tests pass, all Phase 89 proxy guard tests pass, all linux.rs seccomp guard tests pass. Cluster A (72bcfd66 + d457ecc3) is complete and correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arg validation → sandbox/proxy setup | validate_block_net_conflicts() must run before any security-sensitive setup |
| proxy guard tests → CompiledEndpointPolicy chain | Test failures reveal endpoint-policy regressions from the replay |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-05 | Tampering | validate_block_net_conflicts() call ordering in launch_runtime.rs | mitigate | Confirm call precedes any sandbox_prepare/proxy_launch calls in upstream diff and fork application |
| T-99-06 | Tampering | CompiledEndpointPolicy evaluate() bypass post-adoption | mitigate | denied_endpoint_returns_403_and_audit must pass (primary signal); D-08 test_compiled_endpoint_policy_compat_deviation_preserved also run |
| T-99-07 | Tampering | linux.rs seccomp/AF_UNIX invariants regressed by Cluster A (indirect) | mitigate | Run test_proxy_only_with_landlock_v4_returns_no_fallback + test_seccomp_network_fallback_mode_proxy_only — Cluster A is CLI-only so any failure here signals a different regression |
| T-99-SC | Tampering | slopcheck — no new package installs | accept | d457ecc3 adds only function code; no new Cargo.toml changes |
</threat_model>

<verification>
After both tasks:
- git log --oneline -2 shows 2 commits: d457ecc3 replay (HEAD) and 72bcfd66 replay (HEAD~1)
- cargo test -p nono-proxy --lib exits 0
- cargo test -p nono --lib sandbox::linux exits 0 (all three seccomp guard tests pass)
- cargo test -p nono-cli --lib exits 0 (including D-08 tests)
- grep "validate_block_net_conflicts" crates/nono-cli/src/sandbox_prepare.rs: >= 1 match
- grep "validate_block_net_conflicts" crates/nono-cli/src/launch_runtime.rs: >= 1 match
- Both commits in git log have "(cherry picked from commit ...)" trailer and DCO sign-off
</verification>

<success_criteria>
Cluster A is fully absorbed: 72bcfd66 (Plan 02) + d457ecc3 (this plan) are in git history as two atomic DCO-signed commits. validate_block_net_conflicts() is callable from both launch paths. All Phase 89 proxy guard tests pass. All linux.rs seccomp guard tests pass. Both D-08 deviation tests pass. The fork is ready for Clusters D/E (Plan 04) and F/G (Plan 05) in parallel.
</success_criteria>

<output>
Create `.planning/phases/99-upstream-absorb-fork-invariant-verify/99-03-SUMMARY.md` when done.
</output>
