---
phase: 99-upstream-absorb-fork-invariant-verify
plan: 07
type: execute
wave: 6
depends_on:
  - 99-06
files_modified: []
autonomous: false
requirements:
  - UPST11-04

must_haves:
  truths:
    - "cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used exits 0 (linux-gnu gate GREEN)"
    - "cargo-zigbuild clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used exits 0 (apple-darwin gate GREEN)"
    - "make ci (clippy + fmt + tests) exits 0 on the dev host"
    - "D-10 checklist entry for AppContainer/WFP/broker Windows backends: Verified unregressed"
    - "D-10 checklist entry for ADR-86 policy-free-library boundary: Verified unregressed"
    - "D-10 checklist entry for exec_strategy_windows/ denial-rendering carve-out: Verified unregressed"
    - "All 9 upstream SHA replay commits present in git log with DCO sign-offs and upstream SHA trailers"
    - "No will-sync row open in the 98-DIVERGENCE-LEDGER (every will-sync SHA has a corresponding fork commit)"
  artifacts:
    - path: ".planning/phases/99-upstream-absorb-fork-invariant-verify/99-07-SUMMARY.md"
      provides: "D-10 fork-invariant carve-out checklist with verdict lines"
      contains: "Verified unregressed"
  key_links:
    - from: "cross clippy linux-gnu gate exit 0"
      to: "Cluster A cfg(windows) branches in supervised_runtime.rs/execution_runtime.rs/etc."
      via: "Windows-host cargo clippy structurally cannot see cfg(linux) code; cross is mandatory"
      pattern: "cross clippy.*x86_64-unknown-linux-gnu"
    - from: "cargo-zigbuild clippy apple-darwin gate exit 0"
      to: "Cluster A cfg(macos) branches + Cluster D linux.rs"
      via: "apple-darwin gate covers cfg(target_os = macos) branches missed by Windows host"
      pattern: "cargo-zigbuild clippy.*x86_64-apple-darwin"
    - from: "exec_strategy_windows/network.rs WindowsNetworkPolicyMode::ProxyOnly"
      to: "Cluster A NetworkIntent adoption"
      via: "Windows enforcement reads enforcement-time type derived from library, not CLI intent type; no changes required to exec_strategy_windows/"
      pattern: "WindowsNetworkPolicyMode::ProxyOnly"
---

<objective>
Run the Phase 99 gate: both mandatory local cross-target clippy gates (linux-gnu + apple-darwin, no PARTIAL→CI), `make ci` (clippy + fmt + tests), then complete the D-10 fork-invariant carve-out checklist and submit the full verification record for human review. This is the terminal plan for Phase 99 — no code changes are made here unless a gate failure reveals an issue that must be fixed inline.

Purpose: Prove the complete Phase 99 absorption is correct and all fork invariants are unregressed. The cross-target gates are mandatory because Cluster A touches 7 files with cfg(windows) blocks and Cluster D touches the cfg(linux) linux.rs file — Windows-host cargo clippy cannot see these branches.
Output: Both cross-target clippy gates GREEN; make ci GREEN; D-10 checklist complete with three explicit "Verified unregressed" verdicts; human confirmation.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/99-upstream-absorb-fork-invariant-verify/99-CONTEXT.md
@.planning/phases/99-upstream-absorb-fork-invariant-verify/99-RESEARCH.md
@.planning/templates/cross-target-verify-checklist.md
@proj/ADR-86-library-boundary-convergence.md
@proj/ADR-98-network-intent-disposition.md
@CLAUDE.md

<interfaces>
<!-- Cross-target gate invocations (from cross-target-verify-checklist.md — canonical forms) -->

linux-gnu gate:
  Prerequisite: docker info 2>&1 | grep "Server Version"  (must show a version; start Docker if stopped)
  Command: cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used
  Pinned image: ghcr.io/cross-rs/x86_64-unknown-linux-gnu:0.2.5@sha256:9e5b39c09874bc1816c675ed11afca2c2ed6cee0c4ed2b3c1d5763c346c9ae3f

apple-darwin gate:
  Prerequisite: SDKROOT MUST stay UNSET
  Command: cargo-zigbuild clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used
  NOTE: Use direct-binary form "cargo-zigbuild clippy ..." NOT "cargo zigbuild clippy ..." (mis-parses)

Windows host local gate (supplementary — does NOT substitute for cross-target):
  cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used
  NOTE: --all-targets (not --bin nono) to catch nono-ffi exhaustive-match errors

D-10 fork-invariant carve-out checklist entries (from 99-RESEARCH.md §Fork-Invariant Carve-Out):
  1. AppContainer/WFP/broker Windows backends (exec_strategy_windows/launch.rs, network.rs, mod.rs, restricted_token.rs, dacl_guard.rs)
     Check: none of these files appear in any Phase 99 plan's files_modified; exec_strategy_windows/ excluded from all cluster diffs
  2. ADR-86 policy-free-library boundary (crates/nono/src/)
     Check: NetworkIntent is pub(crate) in nono-cli only; grep confirms no NetworkIntent in crates/nono/src/; NetworkMode::ProxyOnly in capability.rs untouched
  3. exec_strategy_windows/ denial-rendering carve-out (ADR-86 D-02)
     Check: WindowsNetworkPolicyMode::ProxyOnly in exec_strategy_windows/network.rs reads enforcement-time type (derived from library's NetworkMode::ProxyOnly at sandbox-apply time); not affected by NetworkIntent adoption; no changes to exec_strategy_windows/ in Phase 99
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Both cross-target clippy gates + make ci</name>
  <files></files>
  <read_first>
    Read: .planning/templates/cross-target-verify-checklist.md (full file — this is the single source of truth for gate invocations, prerequisites, and PARTIAL→CI fallback rules)
    Read: CLAUDE.md §"Cross-target clippy verification" bullet (confirms MUST/NEVER rules)
    Verify docker info 2>&1 | grep "Server Version" shows a running Docker engine before running linux-gnu gate.
  </read_first>
  <action>
    Step 1: Windows local gate (supplementary):
    Run cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used
    This exercises Windows cfg branches and catches nono-ffi exhaustive-match errors. Fix any issues before proceeding to cross-target gates.

    Step 2: linux-gnu cross-target gate:
    Confirm Docker engine is running: docker info 2>&1 | grep "Server Version" (must show "Server Version: X.Y.Z")
    If Docker is stopped: start it. A stopped daemon is NOT a documented gate failure per the checklist.
    Run: cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used
    Expected: exit 0. If errors: fix structurally (cfg-gates, visibility, code changes) — NEVER #[allow] silencing per CLAUDE.md. This gate exercises cfg(linux) branches that Windows-host clippy cannot see, including Cluster D's is_9p_path() addition to linux.rs and Cluster A's cfg(linux)/cfg(macos) blocks in the 11 modified CLI files.

    If the gate fails with a documented runner failure (verbatim Docker/cross image-pull error captured): document per cross-target-verify-checklist.md §PARTIAL Disposition. PARTIAL→CI is the FALLBACK for a documented failure only, not a default.

    Step 3: apple-darwin cross-target gate:
    Confirm SDKROOT is NOT set (unset SDKROOT if needed).
    Run: cargo-zigbuild clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used
    NOTE: Use the direct-binary form above (NOT "cargo zigbuild clippy" — that mis-parses).
    Expected: exit 0. If errors: fix structurally. This gate exercises cfg(macos) branches.

    Step 4: make ci (clippy + fmt + tests):
    Run: make ci
    This runs clippy (Windows target), rustfmt check, and the full test suite. All three must pass. If fmt check fails: run make fmt and re-run make ci. Fix any test failures by reading the failing test + source.

    If all four gates exit 0, proceed to Task 2. If any gate fails: fix the issue, then re-run the failing gate before proceeding.
  </action>
  <verify>
    <automated>make ci 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - cargo clippy --workspace --all-targets exits 0
    - cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used exits 0 (linux-gnu gate GREEN)
    - cargo-zigbuild clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used exits 0 (apple-darwin gate GREEN)
    - make ci exits 0 (clippy + fmt + tests all pass)
    - If any gate was PARTIAL (documented runner failure): verbatim failure signature captured in SUMMARY.md; status set to human_needed per checklist protocol
  </acceptance_criteria>
  <done>All four verification gates pass or are documented per PARTIAL protocol. Cross-target gates prove cfg(linux)/cfg(macos) branches are clean after Phase 99 absorptions. make ci confirms fmt + full test suite pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-10 fork-invariant carve-out checklist + human sign-off</name>
  <what-built>
    All 9 upstream SHA replay commits absorbed (Plans 02-06). Both cross-target clippy gates GREEN (Task 1). make ci GREEN (Task 1). The D-10 fork-invariant carve-out checklist completed with explicit verdicts for the three Windows security model + ADR-86 boundary invariants.

    D-10 checklist (to be completed and included in the SUMMARY.md):

    Invariant 1: AppContainer/WFP/broker Windows backends
    Files checked: crates/nono-cli/src/exec_strategy_windows/ (launch.rs, network.rs, mod.rs, restricted_token.rs, dacl_guard.rs)
    Evidence: None of these files appear in Plans 01-06 files_modified lists. exec_strategy_windows/ is explicitly excluded from the drift-filter scope (98-DIVERGENCE-LEDGER). git diff HEAD~9..HEAD -- crates/nono-cli/src/exec_strategy_windows/ returns empty (no changes).
    Verdict: [ ] Verified unregressed / [ ] Regressed: <what changed>

    Invariant 2: ADR-86 policy-free-library boundary
    Files checked: crates/nono/src/capability.rs, crates/nono/src/sandbox/
    Evidence: NetworkIntent is pub(crate) in crates/nono-cli/src/ only (grep confirms). NetworkMode::ProxyOnly in capability.rs untouched by any Phase 99 commit. No audit, diagnostic, or network-policy logic moved into/out of crates/nono/src/.
    Run: grep -rn "NetworkIntent" crates/nono/src/ — must return 0
    Run: git diff HEAD~9..HEAD -- crates/nono/src/capability.rs — must return empty
    Verdict: [ ] Verified unregressed / [ ] Regressed: <what changed>

    Invariant 3: exec_strategy_windows/ denial-rendering carve-out (ADR-86 D-02)
    Files checked: crates/nono-cli/src/exec_strategy_windows/network.rs
    Evidence: WindowsNetworkPolicyMode::ProxyOnly in network.rs reads the enforcement-time type derived from crates/nono/src/sandbox/windows.rs at sandbox-apply time — NOT the CLI intent type NetworkIntent. No changes to exec_strategy_windows/network.rs in Phase 99. The WFP filter registration path, ProxyOnly denial message, and AppContainer SID-scoped network policy are driven by enforcement-time type; NetworkIntent adoption in Plans 02-03 does not require any changes here.
    Run: git diff HEAD~9..HEAD -- crates/nono-cli/src/exec_strategy_windows/ — must return empty
    Verdict: [ ] Verified unregressed / [ ] Regressed: <what changed>

    Upstream SHA ledger closure (D-06 validation):
    Run: git log --oneline (confirm all 9 replay commits present with their upstream SHA trailers)
    Expected commits (in any order): 72bcfd66, d457ecc3, 5b8e94da, c808f000, 2e64798d, a4d68189, cdeeb5b9, 46bcfbb9, 08ca21a8
  </what-built>
  <how-to-verify>
    1. Run the three grep commands from the D-10 checklist above and confirm the expected results.
    2. Run: git log --oneline -12 (should show the 9 Phase 99 commits at the top)
    3. Run: git log --oneline | grep -c "cherry picked" or check the last 9 commit bodies for upstream SHA trailers
    4. Confirm: make ci still exits 0 (run again to confirm no accidental changes in Task 2)
    5. Review the D-10 checklist entries in the SUMMARY.md with explicit "Verified unregressed" verdicts for all three invariants
    6. Confirm no tls_intercept/ directory: ls crates/nono-proxy/src/ (must not contain tls_intercept)
    7. Confirm OscarMackJr/nono identity: grep -rn "OscarMackJr" crates/nono-cli/src/update_check.rs (must return >= 1)
  </how-to-verify>
  <resume-signal>Type "approved" if all D-10 verdicts are "Verified unregressed", both cross-target gates are GREEN, and make ci is GREEN. Or describe the specific invariant that is regressed and what changed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cross-target clippy gate → cfg(linux)/cfg(macos) branches | Only the cross-target gates can detect drift in cfg-gated Unix branches; Windows-host clippy is structurally blind |
| D-10 checklist → Windows security model | Manual review of exec_strategy_windows/ unchanged-ness proves Windows backends unregressed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-17 | Tampering | cfg(linux)/cfg(macos) drift introduced by Phase 99 absorbs | mitigate | linux-gnu cross clippy gate (Docker + cross) + apple-darwin cross clippy gate (zig + cargo-zigbuild) both must exit 0; no PARTIAL→CI without documented runner failure |
| T-99-18 | Tampering | AppContainer/WFP/broker Windows backends regressed by NetworkIntent adoption | mitigate | D-10 checklist: git diff HEAD~9..HEAD -- crates/nono-cli/src/exec_strategy_windows/ must return empty; WindowsNetworkPolicyMode::ProxyOnly in network.rs untouched |
| T-99-19 | Tampering | ADR-86 policy-free boundary breached (NetworkIntent pushed into library) | mitigate | grep -rn "NetworkIntent" crates/nono/src/ must return 0; D-10 checklist Invariant 2 |
| T-99-20 | Repudiation | Missing upstream SHA trailer on a replay commit | mitigate | git log --oneline shows all 9 commits; each commit body inspected for "(cherry picked from commit <sha>)" |
| T-99-SC | Tampering | slopcheck — no cargo installs in this plan | accept | Verify-only plan; no new packages introduced |
</threat_model>

<verification>
Gate checklist (all must be GREEN before human sign-off):
- [ ] cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used exits 0
- [ ] cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used exits 0
- [ ] cargo-zigbuild clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used exits 0
- [ ] make ci (clippy + fmt + tests) exits 0
- [ ] grep -rn "NetworkIntent" crates/nono/src/ returns 0
- [ ] git diff HEAD~9..HEAD -- crates/nono-cli/src/exec_strategy_windows/ returns empty
- [ ] ls crates/nono-proxy/src/ does NOT contain tls_intercept
- [ ] grep -rn "OscarMackJr" crates/nono-cli/src/update_check.rs returns >= 1
- [ ] D-10 checklist has three explicit "Verified unregressed" verdicts
- [ ] git log shows 9 replay commits with upstream SHA trailers and DCO sign-offs
</verification>

<success_criteria>
Phase 99 is complete: all 9 upstream SHA replay commits in git history with DCO sign-offs. Both cross-target clippy gates GREEN (no PARTIAL→CI). make ci GREEN. D-10 fork-invariant carve-out checklist complete with "Verified unregressed" for AppContainer/WFP/broker, ADR-86 boundary, and exec_strategy_windows/ carve-out. Human sign-off received. The fork is ready for Phase 100 (version leapfrog 0.66.1 + release pipeline reconcile).
</success_criteria>

<output>
Create `.planning/phases/99-upstream-absorb-fork-invariant-verify/99-07-SUMMARY.md` when done.
Include the completed D-10 fork-invariant carve-out checklist with all three verdict lines in the SUMMARY.md body.
</output>
