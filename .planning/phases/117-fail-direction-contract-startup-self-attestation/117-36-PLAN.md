---
phase: 117-fail-direction-contract-startup-self-attestation
plan: 36
type: execute
wave: 16
depends_on: ["117-35"]
gap_closure: true
files_modified:
  - proj/SPEC-windows-fail-direction-contract.md
  - crates/nono-cli/tests/layer_registry_selfcheck.rs
  - crates/nono-cli/src/exec_strategy_windows/layer_registry.rs
autonomous: true
requirements: [CINT-01]

must_haves:
  truths:
    - "`cargo test -p nono-sandbox-cli --test layer_registry_selfcheck --no-fail-fast` passes in full (CR-04 closed)."
    - "The illustrative `file.rs::Symbol` placeholder form is rejected by `looks_like_a_spec_citation` regardless of whether it is quoted — a structural rule, not a convention a future writer must remember."
    - "`layer_registry.rs`'s OWN doc comments describing the `call_sites`/`token_arm` citation convention use the current symbol form, not the pre-WR-08 `\"file:line\"` form (WR-32)."
  artifacts:
    - path: "crates/nono-cli/tests/layer_registry_selfcheck.rs"
      provides: "illustrative_format_example_is_not_treated_as_a_citation — a regression test proving the placeholder-rejection rule, independent of quoting"
  key_links:
    - from: "crates/nono-cli/tests/layer_registry_selfcheck.rs::looks_like_a_spec_citation"
      to: "proj/SPEC-windows-fail-direction-contract.md"
      via: "rejects any span whose file part is the literal placeholder token `file`, case-insensitive, regardless of quoting"
      pattern: "looks_like_a_spec_citation"
---

<objective>
Close CR-04 — the round-3 SPEC drift gate
(`every_spec_symbol_citation_resolves_to_a_real_definition`, built by Plan 117-32) fails on HEAD
right now because Plan 117-34 (a later wave in the SAME round) wrote a SPEC ledger row (line ~296)
containing an UNQUOTED illustrative `` `file.rs::Symbol` `` span, and the gate's exclusion for
illustrative examples relies entirely on the document's quoting convention (`` `"file.rs::Symbol"` ``
at lines 274/284), which nothing enforces structurally. Fix the instance (quote line 296 to match
convention) AND close the class (make the exclusion rule intrinsic to the matcher: no real source
file in this codebase is ever named literally `file.rs` or `file`, so `looks_like_a_spec_citation`
should reject that specific placeholder token regardless of quoting — a future writer who forgets
to quote an illustrative example a second time must not reopen this exact failure).

Also close WR-32 in the same plan, since it is the SAME citation-drift class one file over:
`layer_registry.rs`'s OWN doc comments (the `call_sites` field doc, the `token_arm_names` module
doc) still describe the pre-WR-08 `"file:line"` citation convention and a stale
`launch.rs:1237-1278` raw line-range citation — outside both `spec_call_site_cells_match_registry_
call_sites` (SPEC-vs-registry) and `every_spec_symbol_citation_resolves_to_a_real_definition`
(SPEC-only) coverage, because those two gates scan the SPEC document, never the registry source's
own prose.

Purpose: unblock the test gate that CR-05, WR-22, and every other round-4 plan's `--no-fail-fast`
verify depends on being green, and close two citation-drift findings from the same review
iteration together since they are the identical class.
Output: SPEC.md line 296 fixed; `looks_like_a_spec_citation` structurally hardened with a
regression test; `layer_registry.rs`'s own citation-convention prose corrected with a narrow
regression guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-REVIEW.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-CONTEXT.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-BASELINE-ROUND4.md
</context>

<interfaces>
From `crates/nono-cli/tests/layer_registry_selfcheck.rs:476-499`, the current
`looks_like_a_spec_citation(span: &str) -> bool` — finds `.rs::`, splits into `file_part`
(everything up to and including `.rs`) and `symbol_part`, and returns true only when both halves
pass a character-class check. It has NO exclusion for the placeholder token `file` today; the only
thing keeping the illustrative examples at SPEC lines 274/284 from matching is that they are
written as `` `"file.rs::Symbol"` `` — the literal doubled-quote characters inside the backtick span
make `file_part` start with `"`, which fails the `is_ascii_alphanumeric() || '_' || '-' || '.'`
character class. Line 296's span has no such quotes.

From `proj/SPEC-windows-fail-direction-contract.md:296` (the WR-19 ledger row, written by Plan
117-34), the exact offending text: "`every_spec_symbol_citation_resolves_to_a_real_definition`
content-verifies every `` `file.rs::Symbol` ``-shaped citation anywhere in the whole SPEC
document, ...". Compare against the CORRECT convention at lines 274 and 284, which both wrap the
illustrative form in an extra pair of literal double quotes inside the backticks.

From `crates/nono-cli/src/exec_strategy_windows/layer_registry.rs:531-544` (the `LayerRegistryEntry`
struct), the `call_sites` field's doc comment at line 536 reads: `` /// `"file:line"` citations of
the enforcing call site(s). Layers `` — stale; every real `call_sites` entry in this file has used
`file.rs::Symbol` form since Plan 117-24/117-15 (WR-08/NR3-08). From lines 546-550, the
`token_arm_names` module doc reads: `` /// `token_arm` string constants naming `WindowsTokenArm`
variants (`launch.rs:1237-1278`) by name, `` — a raw line-range citation; the real symbol is
`launch.rs::select_windows_token_arm` (confirmed present at `launch.rs:1350` by this planning
session's direct read).

From `crates/nono-cli/tests/layer_registry_selfcheck.rs:58-67` (`read_layer_registry()`), the
existing `CARGO_MANIFEST_DIR` + `std::fs::read_to_string` house pattern this plan's new narrow
regression tests reuse to read `layer_registry.rs`'s own source text fresh at test-run time.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix the SPEC line-296 instance</name>
  <files>proj/SPEC-windows-fail-direction-contract.md</files>
  <read_first>
    proj/SPEC-windows-fail-direction-contract.md (lines 270-300 — the full WR-08/WR-19 ledger rows,
    to see both the correct quoted convention at 274/284 and the offending row at 296 in context)
  </read_first>
  <action>
    In the WR-19 ledger row (around line 296), wrap the illustrative `file.rs::Symbol` span in the
    same doubled-quote convention already used at lines 274 and 284: `` `"file.rs::Symbol"` ``
    instead of `` `file.rs::Symbol` ``. Do not change any other text in that row.
  </action>
  <acceptance_criteria>
    - `grep -c '`file\.rs::Symbol`-shaped' proj/SPEC-windows-fail-direction-contract.md` returns 0
      (the unquoted form no longer appears).
    - `grep -c '"file\.rs::Symbol"' proj/SPEC-windows-fail-direction-contract.md` returns at least 3
      (lines 274, 284, and the now-fixed 296).
  </acceptance_criteria>
  <verify>
    <automated>grep -c '`file\.rs::Symbol`-shaped' proj/SPEC-windows-fail-direction-contract.md</automated>
  </verify>
  <done>Line 296's illustrative example matches the document's own established quoting convention.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden looks_like_a_spec_citation to reject the placeholder token structurally, independent of quoting</name>
  <files>crates/nono-cli/tests/layer_registry_selfcheck.rs</files>
  <read_first>
    crates/nono-cli/tests/layer_registry_selfcheck.rs (lines 456-526 in full — `looks_like_a_spec_
    citation` and `extract_spec_citations`, and the doc comments explaining the current
    quoting-convention exclusion)
  </read_first>
  <behavior>
    - `looks_like_a_spec_citation("file.rs::Symbol")` returns `false` (the bare placeholder form,
      unquoted — the exact CR-04 trigger).
    - `looks_like_a_spec_citation("\"file.rs::Symbol\"")` still returns `false` (the existing
      quoted-convention form must remain excluded too — this is a strengthening, not a
      replacement, of the existing rule).
    - `looks_like_a_spec_citation("launch.rs::is_dev_build_layout")` still returns `true` (a real
      citation naming a real file must not be caught by the new exclusion — "launch" is not "file").
    - `looks_like_a_spec_citation("File.rs::Symbol")` returns `false` (case-insensitive — a future
      writer capitalizing the placeholder must not slip through).
  </behavior>
  <action>
    In `looks_like_a_spec_citation`, after computing `file_part`, additionally reject the span when
    `file_part`'s final `/`-separated segment, with its `.rs` suffix stripped and lowercased, equals
    exactly `"file"` — no real source file in this workspace is ever named literally `file.rs` (this
    is the illustrative-placeholder convention used consistently across
    `proj/SPEC-windows-fail-direction-contract.md`'s Layer registry table documentation and its
    Review-fix pass ledger). This rule is independent of the quoting convention: it also strengthens
    the existing quoted-form exclusion rather than duplicating it. Add
    `#[test] fn illustrative_format_example_is_not_treated_as_a_citation()` asserting the four
    `<behavior>` cases above. Then re-run
    `every_spec_symbol_citation_resolves_to_a_real_definition` and confirm it now passes against the
    Task-1-fixed SPEC document.
  </action>
  <acceptance_criteria>
    - `cargo test -p nono-sandbox-cli --test layer_registry_selfcheck --no-fail-fast --
      illustrative_format_example_is_not_treated_as_a_citation` passes.
    - `cargo test -p nono-sandbox-cli --test layer_registry_selfcheck --no-fail-fast --
      every_spec_symbol_citation_resolves_to_a_real_definition` passes.
    - Perturbation proof (record in SUMMARY.md): temporarily revert Task 1 (reintroduce the unquoted
      `` `file.rs::Symbol` `` span at SPEC line 296) with Task 2's structural fix still in place;
      re-run `every_spec_symbol_citation_resolves_to_a_real_definition` and confirm it STILL passes
      (proving the structural fix, not just the quoting fix, is what protects the gate going
      forward); then re-apply Task 1's fix. Separately, temporarily remove ONLY the new `file_part
      == "file"` check (keep Task 1's SPEC fix reverted too) and confirm
      `every_spec_symbol_citation_resolves_to_a_real_definition` fails again, naming the reintroduced
      span — proving the new check is what the previous gate lacked, not merely coincidental with
      Task 1's text fix.
    - `cargo test -p nono-sandbox-cli --test layer_registry_selfcheck --no-fail-fast` passes in full.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-cli --test layer_registry_selfcheck --no-fail-fast</automated>
  </verify>
  <done>CR-04's gate passes, and the fix closes the class (placeholder-token rejection, quoting-independent) rather than only the instance.</done>
</task>

<task type="auto">
  <name>Task 3: Fix WR-32's two stale citation-convention doc comments in layer_registry.rs, with a narrow regression guard</name>
  <files>crates/nono-cli/src/exec_strategy_windows/layer_registry.rs, crates/nono-cli/tests/layer_registry_selfcheck.rs</files>
  <read_first>
    crates/nono-cli/src/exec_strategy_windows/layer_registry.rs (lines 528-560 — the
    `LayerRegistryEntry` struct's `call_sites` field doc and the `token_arm_names` module doc, in
    full context)
    crates/nono-cli/src/exec_strategy_windows/launch.rs (line 1350, confirming
    `pub(super) fn select_windows_token_arm` is the real declaration this doc comment should cite)
  </read_first>
  <action>
    In `layer_registry.rs`, change the `call_sites` field's doc comment (currently `` /// `"file:
    line"` citations of the enforcing call site(s). Layers `` at line 536) to describe the CURRENT
    convention: `` /// `file.rs::Symbol` citations of the enforcing call site(s), content-verified `` and
    keep the rest of the sentence. Change the `token_arm_names` module doc's `` (`launch.rs:1237-
    1278`) `` citation to `` (`launch.rs::select_windows_token_arm`) ``. Do NOT add a blanket
    file-wide "no raw `\w+\.rs:\d+` pattern anywhere in this file" test — `layer_registry.rs`
    legitimately cites other files' analogs elsewhere in its own module-level doc comments (e.g.
    `network.rs:1500`, `crates/nono/src/undo/types.rs:333`) in ordinary descriptive prose, not as
    call-site/token-arm citations, and a blanket ban would produce false positives against those.
    Instead add two NARROWLY-scoped regression tests to
    `crates/nono-cli/tests/layer_registry_selfcheck.rs` (reusing `read_layer_registry()`):
    `#[test] fn call_sites_field_doc_describes_the_symbol_form()` asserting the registry source
    text contains the doc substring `` file.rs::Symbol `` immediately preceding the `call_sites`
    field declaration and does NOT contain the literal `` "file:line" `` string anywhere; and
    `#[test] fn token_arm_doc_cites_a_symbol_not_a_raw_line_range()` asserting the source does NOT
    contain `` launch.rs:1237 `` and DOES contain `` launch.rs::select_windows_token_arm ``.
  </action>
  <acceptance_criteria>
    - `grep -c '"file:line"' crates/nono-cli/src/exec_strategy_windows/layer_registry.rs` returns 0.
    - `grep -c "launch.rs:1237" crates/nono-cli/src/exec_strategy_windows/layer_registry.rs` returns 0.
    - `grep -c "launch.rs::select_windows_token_arm" crates/nono-cli/src/exec_strategy_windows/layer_registry.rs` returns 1.
    - `cargo test -p nono-sandbox-cli --test layer_registry_selfcheck --no-fail-fast --
      call_sites_field_doc_describes_the_symbol_form
      token_arm_doc_cites_a_symbol_not_a_raw_line_range` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-cli --test layer_registry_selfcheck --no-fail-fast</automated>
  </verify>
  <done>WR-32's two stale citation-convention doc comments in layer_registry.rs's own prose are fixed and guarded, scoped narrowly so legitimate cross-file analog citations elsewhere in the same file are unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `proj/SPEC-windows-fail-direction-contract.md` and `layer_registry.rs`'s own prose ↔ the code they describe | An operator/auditor reading either document trusts a citation points at the real enforcing logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-36-01 | Repudiation | SPEC citation drift surviving inside the drift gate's own convention | mitigate | Structural placeholder-token rejection, quoting-independent, plus the instance fix and a perturbation proof (closes CR-04) |
| T-117-36-02 | Repudiation | `layer_registry.rs`'s own doc comments describing the citation convention | mitigate | Two narrow regression tests pin the corrected doc text without over-broad false-positive risk against legitimate cross-file prose (closes WR-32) |
</threat_model>

<verification>
`cargo test -p nono-sandbox-cli --test layer_registry_selfcheck --no-fail-fast` passes in full,
including all new/hardened tests and their perturbation proofs recorded in SUMMARY.md. If this
file contains no `#[cfg(target_os = "linux")]`/`#[cfg(target_os = "macos")]` blocks itself, confirm
via grep before skipping cross-target clippy for `layer_registry_selfcheck.rs`; `layer_registry.rs`
IS under `exec_strategy_windows/`, so per CLAUDE.md's explicit rule both local cross-target clippy
gates (linux-gnu `cross clippy`, apple-darwin `cargo-zigbuild clippy`) are REQUIRED for this plan —
run both and record pass/fail in SUMMARY.md.
</verification>

<success_criteria>
CR-04 is closed with a structural (not merely instance) fix, proven by perturbation. WR-32 is
closed narrowly, without introducing false positives against legitimate prose elsewhere in the
same file. The round's own citation-drift gate is green again, unblocking every later plan's
`--no-fail-fast` verify.
</success_criteria>

<output>
Create `.planning/phases/117-fail-direction-contract-startup-self-attestation/117-36-SUMMARY.md` when done
</output>
