---
phase: 117-fail-direction-contract-startup-self-attestation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono/src/diagnostic/codes.rs
  - crates/nono/src/error.rs
  - crates/nono/src/machine_policy.rs
autonomous: true
requirements: [CINT-02]

must_haves:
  truths:
    - "D-22: the abort path surfaces a typed NonoError naming the failed layer, not a generic error."
    - "D-26: the strictness knob is machine policy plus a CLI flag, with machine policy winning and the flag only able to tighten, never loosen."
  artifacts:
    - path: crates/nono/src/error.rs
      provides: "NonoError::LayerAttestationFailed variant + diagnostic_code() match arm"
    - path: crates/nono/src/diagnostic/codes.rs
      provides: "NonoDiagnosticCode::LayerAttestationFailed variant"
    - path: crates/nono/src/machine_policy.rs
      provides: "pub RequiredLayersPolicy sub-struct + MachineEgressPolicy.required_layers field"
  key_links:
    - from: crates/nono/src/error.rs
      to: crates/nono/src/diagnostic/codes.rs
      via: "NonoError::diagnostic_code() exhaustive match arm"
      pattern: "LayerAttestationFailed"
    - from: crates/nono/src/machine_policy.rs
      to: crates/nono-cli/src/exec_strategy_windows/attestation.rs
      via: "pub struct RequiredLayersPolicy consumed cross-crate by Plan 08"
      pattern: "pub struct RequiredLayersPolicy"
---

<objective>
Add the two library-side (`crates/nono`, policy-free) surfaces that CINT-02's self-attestation
needs before any gate-insertion work can compile against them: a typed error + diagnostic code
naming the failed layer (D-22), and a `required_layers` machine-policy field that lets an admin
tighten the fleet-wide strictness posture (D-26). Both are additive changes to existing,
well-precedented extension points — no new architecture.

Purpose: unblock every later plan that needs to construct or match on
`NonoError::LayerAttestationFailed`, and settle D-26's abort-vs-degrade posture question for the
new policy field explicitly rather than by default.
Output: one new `NonoError` variant + `NonoDiagnosticCode` variant + `RequiredLayersPolicy` struct.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-CONTEXT.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-PATTERNS.md
</context>

<interfaces>
Confirmed exact (2026-08-09) `NonoError` variant shape to model on, `crates/nono/src/error.rs:207-214`:
```
#[error("Failed to apply integrity label to {path}: {hint} (HRESULT: 0x{hresult:08X})")]
LabelApplyFailed { path: PathBuf, hresult: u32, hint: String },
```

Confirmed exact `diagnostic_code()` — exhaustive match, no wildcard, tail already has one
`#[cfg(target_os = "linux")]` arm — `crates/nono/src/error.rs:383-448`:
```
Self::LabelApplyFailed { .. }
| Self::DaclApplyFailed { .. }
| Self::PolicyLoadFailed { .. }
| Self::TelemetryUnavailable { .. }
| Self::TelemetryConfigInvalid { .. } => NonoDiagnosticCode::ConfigurationError,
...
#[cfg(target_os = "linux")]
Self::Landlock(_) | Self::LandlockPath(_) => NonoDiagnosticCode::SandboxDeniedPath,
```

Confirmed exact `NonoDiagnosticCode`, `crates/nono/src/diagnostic/codes.rs:17-38` (`#[non_exhaustive]`,
16 current variants, additive-safe):
```
#[non_exhaustive]
pub enum NonoDiagnosticCode {
    SandboxDeniedPath, SandboxDeniedNetwork, SandboxDeniedUnixSocket, CommandNotFound,
    CommandFailedLikelySandbox, CommandFailedApplication, CredentialNotFound,
    CredentialUnavailable, UnsupportedPlatformFeature, RollbackBudgetExceeded,
    CwdAccessRequired, ConfigurationError, TrustVerificationFailed, IoError, Cancelled, Other,
}
```

Confirmed exact `TelemetryConfig` sub-struct + `MachineEgressPolicy` nesting pattern (the D-26
extension precedent), `crates/nono/src/machine_policy.rs:90-183` — **note `TelemetryConfig` and
`MachineEgressPolicy` are both `pub struct`, not `pub(crate)`, precisely because this same file is
consumed cross-crate by `nono-cli` — `RequiredLayersPolicy` MUST match this visibility (Blocker-2
fix, checker pass 2): a `pub(crate)` type used as a `pub` field's type trips rustc's
`private_interfaces` lint and fails `cargo clippy -p nono -- -D warnings`**:
```
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct TelemetryConfig {
    #[serde(default = "default_telemetry_enabled")] pub enabled: bool,
    #[serde(default = "default_telemetry_channel")] pub channel: String,
    #[serde(default = "default_telemetry_min_severity")] pub min_severity: TelemetrySeverity,
}
// ...
pub struct MachineEgressPolicy {
    #[serde(default)] pub allowed_suffixes: Vec<String>,
    #[serde(default)] pub allowed_hosts: Vec<String>,
    #[serde(default)] pub preset_tokens: Vec<String>,
    /// **INVARIANT:** This field MUST NOT be counted in [`Self::is_unconfigured`].
    #[serde(default)] pub telemetry: TelemetryConfig,
}
```
Egress fields abort on present-but-unreadable (`PolicyLoadFailed`); `TelemetryConfig` degrades
(default + stderr warning, `TelemetryConfigInvalid`). This is the house "D-07 egress aborts, D-14
telemetry degrades" vocabulary (`machine_policy.rs:513-514`, `:594`, `:605`) — reuse these terms.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add NonoDiagnosticCode + NonoError::LayerAttestationFailed</name>
  <files>crates/nono/src/diagnostic/codes.rs, crates/nono/src/error.rs</files>
  <read_first>
    - crates/nono/src/diagnostic/codes.rs (full file — 39 lines, confirm current variant list before
      inserting)
    - crates/nono/src/error.rs lines 195-250 (surrounding variants) and 380-448 (`diagnostic_code()`
      full exhaustive match, confirm exact current arm text before editing)
  </read_first>
  <behavior>
    - Test 1: `NonoError::LayerAttestationFailed { layer: "AppContainerProfile".into(), reason:
      "TokenAppContainerSid query failed".into() }.diagnostic_code()` returns
      `NonoDiagnosticCode::LayerAttestationFailed`.
    - Test 2: `diagnostic_code()`'s match remains exhaustive after the addition — this is enforced
      by the compiler (no wildcard arm exists today), not by a runtime test; the acceptance
      criterion is that `cargo build -p nono` still compiles with zero added wildcard arms.
  </behavior>
  <action>
    Add `LayerAttestationFailed,` to `NonoDiagnosticCode` (`codes.rs`, inside the existing
    `#[non_exhaustive]` enum, additive). Add a new `NonoError` variant modeled on
    `LabelApplyFailed`'s shape: `#[error("Startup self-attestation failed for layer {layer}: {reason}")]
    LayerAttestationFailed { layer: String, reason: String }` (plain fields, no `PathBuf`/`hresult` —
    this error covers OS-probe failures, not path/ACE failures). Add
    `Self::LayerAttestationFailed { .. } => NonoDiagnosticCode::LayerAttestationFailed,` as its own
    match arm in `diagnostic_code()` (do NOT fold it into the existing `ConfigurationError` arm — D-22
    requires the operator to distinguish an attestation failure from generic misconfiguration). The
    variant is NOT `#[cfg]`-gated (constructed only on Windows today, but the type itself must
    compile on every platform per D-11's platform-neutral-type framing, matching the existing
    unconditional `LabelApplyFailed` arm's own precedent — that variant is also Windows-only in
    practice but carries no `cfg`).
  </action>
  <acceptance_criteria>
    - `grep -c "LayerAttestationFailed" crates/nono/src/diagnostic/codes.rs` returns 1
    - `grep -c "LayerAttestationFailed" crates/nono/src/error.rs` returns at least 2 (variant decl +
      match arm)
    - `cargo build -p nono` and `cargo clippy -p nono -- -D warnings -D clippy::unwrap_used` both
      exit 0
    - `cargo test -p nono --lib diagnostic_code` (or the nearest existing diagnostic-code test
      module) still passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add required_layers machine-policy field (D-26)</name>
  <files>crates/nono/src/machine_policy.rs</files>
  <read_first>
    - crates/nono/src/machine_policy.rs lines 1-183 (module doc, `TelemetryConfig`,
      `MachineEgressPolicy`, the `is_unconfigured` invariant comment — note `TelemetryConfig` is
      declared `pub struct`, not `pub(crate)`, per this plan's `<interfaces>` visibility note)
    - crates/nono/src/machine_policy.rs lines 500-610 (`parse_telemetry_config`'s degrade-not-abort
      lifecycle vs. the egress fields' abort-on-unreadable lifecycle — read both to make the
      abort-vs-degrade call explicit, not by default)
    - crates/nono/src/machine_policy.rs lines 690-710 (`read_machine_egress_policy` cfg(windows)
      split — the pattern to extend if this field needs its own registry sub-read)
  </read_first>
  <action>
    Add `pub struct RequiredLayersPolicy { #[serde(default)] pub required: Vec<String> }` —
    **`pub`, not `pub(crate)`** (Blocker-2 fix, checker pass 2: `MachineEgressPolicy` is a `pub`
    struct and `RequiredLayersPolicy` becomes one of its fields; a `pub` field whose type is
    `pub(crate)` trips rustc's `private_interfaces` lint and fails this task's own
    `cargo clippy -p nono -- -D warnings` acceptance criterion — match `TelemetryConfig`'s
    visibility exactly, which is `pub` for precisely this cross-crate reason) — plain
    `Vec<String>` of `LayerId` name strings — `machine_policy.rs` must NOT import `nono-cli`'s
    `LayerId` type per D-02's CLI-owns-registry boundary; the CLI-side attestation decision module,
    Plan 08, is responsible for parsing these strings against `LayerId::ALL` and fail-closed-
    rejecting an unrecognized name. Add `#[serde(default)] pub required_layers: RequiredLayersPolicy`
    to `MachineEgressPolicy`, documented with the same `/// **INVARIANT:** This field MUST NOT be
    counted in [Self::is_unconfigured]` comment as `telemetry` — an admin who has only set
    required-layers policy must not accidentally flip the daemon to strict deny-all egress. Resolve
    the tension PATTERNS.md flags explicitly in a doc comment on `RequiredLayersPolicy`: **decision —
    this field follows the telemetry (degrade-not-abort) read lifecycle, not the egress
    (abort-on-unreadable) lifecycle** — a present-but-malformed `RequiredLayers` registry value falls
    back to an empty `Vec` (nothing additionally required) and surfaces a warning, rather than
    aborting the whole policy read; the CLI flag half of D-26 ("can only tighten, never loosen") is
    enforced downstream in Plan 08 by taking the union of machine-policy-required and CLI-flag-
    required layers, never letting the CLI flag remove a machine-policy requirement — state this
    reasoning inline so a future reader does not have to re-derive it from the two competing
    precedents.
  </action>
  <acceptance_criteria>
    - `grep -c "^pub struct RequiredLayersPolicy" crates/nono/src/machine_policy.rs` equals 1
      (confirms `pub`, not `pub(crate)` — Blocker-2 fix)
    - `grep -c "required_layers" crates/nono/src/machine_policy.rs` returns at least 3 (struct decl,
      field, doc comment)
    - `grep -n "MUST NOT be counted in \[\`Self::is_unconfigured\`\]" crates/nono/src/machine_policy.rs`
      shows two hits (the existing `telemetry` one and the new `required_layers` one)
    - `cargo build -p nono` and `cargo clippy -p nono -- -D warnings -D clippy::unwrap_used` exit 0
      (this specifically exercises the `private_interfaces` lint the Blocker-2 fix avoids)
    - `cargo test -p nono --lib machine_policy` passes (existing `is_unconfigured` tests still hold —
      confirms the new field is correctly excluded)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `HKLM\SOFTWARE\Policies\nono\RequiredLayers` → `MachineEgressPolicy.required_layers` | Admin-controlled registry input; malformed values must degrade, not crash or silently grant more trust than intended |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-08 | Denial of Service | `required_layers` registry read | mitigate | Degrade-not-abort lifecycle (Task 2) — a malformed value falls back to empty, matching the existing `TelemetryConfig` precedent, so a corrupt registry value cannot brick every confined launch |
| T-117-09 | Elevation of Privilege | `is_unconfigured()` exclusion | mitigate | `required_layers` explicitly excluded from `is_unconfigured()`, mirroring the existing `telemetry` invariant comment, so setting only this field cannot accidentally flip the daemon to strict deny-all egress |
| T-117-SC | Tampering | supply chain | accept | No new crate dependencies added |
</threat_model>

<verification>
- `cargo build -p nono`, `cargo clippy -p nono -- -D warnings -D clippy::unwrap_used`, `cargo fmt --all -- --check` all exit 0
- `cargo test -p nono --lib` passes (no regression in existing `machine_policy`/`diagnostic`/`error` tests)
</verification>

<success_criteria>
- `NonoError::LayerAttestationFailed` exists and maps to its own distinct `NonoDiagnosticCode`
- `MachineEgressPolicy.required_layers: RequiredLayersPolicy` exists as a `pub` type, consumable
  cross-crate by Plan 08 without tripping `private_interfaces` (Blocker-2 closed), with an explicit,
  documented abort-vs-degrade decision
</success_criteria>

<output>
Create `.planning/phases/117-fail-direction-contract-startup-self-attestation/117-02-SUMMARY.md` when done
</output>
