---
phase: 117-fail-direction-contract-startup-self-attestation
plan: 39
type: execute
wave: 16
depends_on: ["117-35"]
gap_closure: true
files_modified:
  - crates/nono-cli/src/main.rs
  - crates/nono-cli/src/exec_strategy_windows/labels_guard.rs
autonomous: true
requirements: [CINT-02, CINT-03]

must_haves:
  truths:
    - "The MandatoryIntegrityLabel remediation text names a command that ACTUALLY clears the condition it describes, proven by a test that applies the prescribed remedy and re-runs snapshot_and_apply, not merely by prose (CR-06a)."
    - "The remediation text no longer attributes the abort to nono's own abnormal-exit residue — Plan 117-13 (NR3-01) already made that specific cause non-aborting; the text names the causes that CAN still fire (CR-06b)."
    - "The non-label remediation arm no longer points the operator at the Windows Application event log — no telemetry event is ever written on the Abort path, confirmed by enumerating every LayerAttestationFailed construction site (WR-27)."
    - "LabelCoverage::application() excludes the contract-exempt SkipNotOwned category from its denominator, mirroring DaclGrantCoverage::application()'s existing rule for its own exempt category — the same physical shape D-37 fixed one file over (WR-28)."
    - "WR-20's host-blocked discovery test pins the DOWNSTREAM classification (coverage.application()), not only the entry variant, and its unverified-on-this-host status is recorded explicitly rather than silently assumed passing (WR-20 disposition)."
  artifacts:
    - path: "crates/nono-cli/src/main.rs"
      provides: "render_error_for_operator with a working MandatoryIntegrityLabel remedy and a truthful non-label pointer"
    - path: "crates/nono-cli/src/exec_strategy_windows/labels_guard.rs"
      provides: "LabelCoverage::application()'s corrected denominator, a mixed-case test, WR-20's downstream pin, and a remedy-effectiveness test"
  key_links:
    - from: "crates/nono-cli/src/main.rs::render_error_for_operator"
      to: "crates/nono-cli/src/exec_strategy_windows/labels_guard.rs::clear_mandatory_label"
      via: "the prescribed remedy is proven, by test, to be the operation that actually flips LabelCoverage::application() away from NotApplied"
      pattern: "clear_mandatory_label"
    - from: "crates/nono-cli/src/exec_strategy_windows/labels_guard.rs::LabelCoverage::application"
      to: "crates/nono-cli/src/exec_strategy_windows/dacl_guard.rs::DaclGrantCoverage::application"
      via: "both exclude their own contract-exempt category from the denominator — the classification RULE, not just the variant name set"
      pattern: "non_exempt"
---

<objective>
Close four review findings that share the `labels_guard.rs`/`render_error_for_operator` surface:
CR-06 (the `MandatoryIntegrityLabel` remediation prescribes a command — `icacls <path>
/setintegritylevel Medium` — that WRITES a Medium mandatory-label ACE, which `AppliedLabelsGuard`'s
own D-02 rule then reads back as `SkipPreExistingLabel`, so the operator following the ONLY printed
guidance stays locked out identically; it also blames a cause, "a prior session that exited
abnormally," that Plan 117-13 already made non-aborting), WR-27 (the non-label remediation arm
points at the Windows Application event log, which — confirmed by enumerating every
`LayerAttestationFailed` construction site in the workspace — NEVER receives a record on the Abort
path; only `ProceedDowngraded` ever calls `emit_attestation_event`), WR-28 (`LabelCoverage::
application()` counts the contract-exempt `SkipNotOwned` category in its denominator while its
sibling `DaclGrantCoverage::application()` excludes its own exempt category — the identical
"exempt classified as gap" shape D-37/WR-12 was convened to fix, unfixed one file over, in a round
that was explicitly planned by class), and WR-20 (the host-blocked discovery test
`non_owned_path_with_a_foreign_label_is_exempt_not_a_coverage_gap` pins the entry variant but not
the downstream `coverage.application()` decision the reclassification actually changes, and its
unverified-on-this-host status needs an explicit disposition, not a silent assumption).

These four are grouped in one plan because CR-06's fix must be PROVEN correct by exercising
`LabelCoverage::application()` — the exact function WR-28 changes — so fixing them separately risks
either testing the old (wrong) arithmetic or re-doing the proof twice.

Purpose: give the operator a remediation that actually works, stop pointing at a channel with
nothing in it, fix the coverage-arithmetic contradiction the review calls "the same shape D-37 was
convened to fix," and give WR-20's authored-but-unverified test a real downstream assertion plus an
explicit, honest disposition.
Output: `main.rs`'s `render_error_for_operator` fixed and re-tested; `labels_guard.rs`'s
`LabelCoverage::application()` fixed, mirrored against its sibling, and proven by a
remedy-effectiveness test; WR-20's test extended and its host-blocked status recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-REVIEW.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-CONTEXT.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-BASELINE-ROUND4.md
</context>

<interfaces>
From `crates/nono-cli/src/main.rs:258-276` (`render_error_for_operator`), the current match on
`layer.as_str()`: `"MandatoryIntegrityLabel"` prints the broken `icacls .../setintegritylevel
Medium` remedy and the false "abnormal exit" cause; the `_` arm prints "see the Windows Application
event log (source `nono`) for the per-layer attestation record" for every other layer. Existing
tests at `main.rs`'s `#[cfg(test)] mod tests` (lines ~303-342):
`render_error_for_operator_adds_remediation_for_layer_attestation_failed` asserts
`lines[1].contains("icacls")` and `lines[1].contains("/setintegritylevel Medium")`;
`render_error_for_operator_names_the_event_log_for_non_label_layers` asserts
`lines[1].contains("Windows Application event log")` and `!lines[1].contains("nono setup
--check-only")`. Both must be updated to match the corrected text (do not simply delete them — they
pin real regressions).

From `crates/nono-cli/src/exec_strategy_windows/labels_guard.rs:439-475` (`clear_mandatory_label`,
`#[cfg(target_os = "windows")]`), the private function that ACTUALLY removes a mandatory-label ACE
via `SetNamedSecurityInfoW(..., LABEL_SECURITY_INFORMATION, ...)` with an empty `"S:"` SDDL — this
is the mechanism the CR-06 remedy text must point at (in operator-executable form, since this
function is private and not CLI-exposed), and the mechanism this plan's new remedy-effectiveness
test calls directly to simulate the operator following the guidance.

From `crates/nono-cli/src/exec_strategy_windows/labels_guard.rs:106-157` (`LabelCoverage`,
`application()`), the current arithmetic: `policy_paths` (includes `SkipNotOwned`) `== 0 =>
NotApplicable`; `applied == 0 => NotApplied`; `skipped_pre_existing_label > 0 => PartiallyApplied`;
else `Applied`. Compare against the sibling at `crates/nono-cli/src/exec_strategy_windows/
dacl_guard.rs:129-154` (`DaclGrantCoverage::application`), whose `writable_rules()` helper is
`self.applied.saturating_add(self.skipped_writable_not_owned)` — deliberately EXCLUDING the
read-only (contract-exempt) category, so `writable_rules() == 0` (not `policy_paths == 0`) gates
`NotApplicable`.

From `crates/nono-cli/src/exec_strategy_windows/labels_guard.rs:1064-1126`
(`guard_skips_path_not_owned_by_current_user`) and `:1128-1281`
(`non_owned_path_with_a_foreign_label_is_exempt_not_a_coverage_gap`, WR-20's test, Plan 117-30): the
FIRST test currently asserts `coverage.application() == NotApplied` for an all-`SkipNotOwned`,
single-rule policy (lines 1105-1112) — this assertion is WRONG once WR-28's fix lands (an
all-exempt policy becomes `NotApplicable`, mirroring `DaclGrantCoverage`'s own rule for the same
shape) and MUST be updated in the same change, not left stale. The SECOND test (WR-20's) ends at
line 1263 (`assert_eq!(coverage.skipped_not_owned, 1, ...)`) without ever asserting
`coverage.application()` — this plan adds that assertion.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix render_error_for_operator (CR-06, WR-27)</name>
  <files>crates/nono-cli/src/main.rs</files>
  <read_first>
    crates/nono-cli/src/main.rs (lines 220-370 in full — render_error_for_operator and its full test
    module, so the fix and its tests are read together, not edited half-blind)
    crates/nono/src/error.rs (grep this file, and the whole workspace, for every
    `LayerAttestationFailed` construction site to independently confirm WR-27's claim that only the
    Abort arm constructs it and no telemetry event is ever paired with it — do not merely trust this
    plan's prose)
  </read_first>
  <behavior>
    - For `layer == "MandatoryIntegrityLabel"`: the remediation line names `icacls` for INSPECTION
      only (never `/setintegritylevel Medium`), describes the real causes (a third-party label, a
      mask mismatch, or a structurally-inert `INHERIT_ONLY_ACE` — never "abnormal exit"), and names
      an operator-executable command that REMOVES the label (a PowerShell `Set-Acl` /
      `SetSecurityDescriptorSddlForm("S:", "Sacl")` snippet, or an equivalent this task's author
      confirms is actually correct against `clear_mandatory_label`'s own SDDL/API usage).
    - For every OTHER layer (`_` arm): the remediation line does NOT mention "Windows Application
      event log" (no record exists there for an Abort), and instead names a channel that DOES exist
      on the Abort path — the `tracing::warn!` the failing guard itself already emits — pointing the
      operator at `-vv --log-file <path>` and the layer name to search for.
  </behavior>
  <action>
    Independently enumerate every `NonoError::LayerAttestationFailed` construction site in the
    workspace (grep `LayerAttestationFailed`) and confirm none is paired with any telemetry/event-log
    write — record this enumeration in SUMMARY.md. Rewrite the `"MandatoryIntegrityLabel"` arm's
    format string to: name the real causes (third-party label / mask mismatch / `INHERIT_ONLY_ACE`),
    state nono never mutates a pre-existing label, and prescribe an operator-executable removal
    command — a PowerShell snippet using `Get-Acl`/`SetSecurityDescriptorSddlForm("S:", "Sacl")`/
    `Set-Acl` (elevated) is the concrete mechanism `clear_mandatory_label` itself uses; verify the
    exact cmdlet sequence is correct before writing it (do not invent a plausible-sounding but
    unverified snippet). Rewrite the `_` arm to point at `-vv --log-file <path>` and the layer name,
    removing the "Windows Application event log" claim entirely from this arm. Update
    `render_error_for_operator_adds_remediation_for_layer_attestation_failed` to assert the new
    text (`icacls` present for inspection, `/setintegritylevel Medium` ABSENT, the PowerShell remedy
    marker present). Update `render_error_for_operator_names_the_event_log_for_non_label_layers`
    (rename it to reflect the new claim, e.g.
    `render_error_for_operator_names_a_reachable_channel_for_non_label_layers`) to assert
    `--log-file` is present and "Windows Application event log" is ABSENT.
  </action>
  <acceptance_criteria>
    - `cargo test -p nono-sandbox-cli --bin nono --no-fail-fast -- --test-threads=1
      render_error_for_operator` passes (3 tests: the two updated plus the unchanged
      `render_error_for_operator_is_unchanged_for_other_variants`).
    - `grep -c "/setintegritylevel Medium" crates/nono-cli/src/main.rs` returns 0.
    - `grep -n "LayerAttestationFailed" crates/nono/src/error.rs crates/nono-cli/src/exec_strategy_windows/launch.rs` — SUMMARY.md states the full enumeration result and confirms no construction site is paired with a telemetry emit.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-cli --bin nono --no-fail-fast -- --test-threads=1 render_error_for_operator</automated>
  </verify>
  <done>The MandatoryIntegrityLabel remedy is real and inspection-vs-removal is unambiguous; the non-label arm points at a channel that genuinely exists on the Abort path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix LabelCoverage::application()'s denominator (WR-28), pin WR-20's downstream decision, and prove CR-06's remedy actually clears the condition</name>
  <files>crates/nono-cli/src/exec_strategy_windows/labels_guard.rs</files>
  <read_first>
    crates/nono-cli/src/exec_strategy_windows/labels_guard.rs (lines 106-157 for LabelCoverage;
    lines 1064-1281 for the two existing tests this task modifies/extends; lines 439-475 for
    clear_mandatory_label; lines 820-830 for plant_mandatory_label_with_flags)
    crates/nono-cli/src/exec_strategy_windows/dacl_guard.rs (lines 100-154, DaclGrantCoverage's
    sibling arithmetic — the RULE this task mirrors, not merely the variant names)
  </read_first>
  <behavior>
    - A policy composed ENTIRELY of `SkipNotOwned` paths (contract-exempt) classifies
      `NotApplicable`, mirroring `DaclGrantCoverage`'s rule for its own all-exempt case — NOT
      `NotApplied` (the current, WR-28-flagged behavior).
    - A mixed policy (1 owned+applied path, 2 non-owned/`SkipNotOwned` paths) classifies `Applied` —
      not `PartiallyApplied` (`SkipNotOwned` must not count toward partial-application either).
    - `SkipPreExistingLabel` (a genuine coverage gap, never exempt) still counts toward the
      denominator and can still produce `NotApplied`/`PartiallyApplied` exactly as today —
      WR-28's fix narrows only the `SkipNotOwned` treatment.
    - The combined non-owned + foreign-labeled condition (WR-20's scenario) classifies
      `NotApplicable` under the fixed arithmetic (its single policy entry is `SkipNotOwned`, the
      exempt category) — pin this explicitly.
    - Applying `clear_mandatory_label` to a path carrying a foreign/mismatched label, then re-running
      `snapshot_and_apply` against the same policy, results in `coverage.application() ==
      LayerApplication::Applied` — the CR-06 remedy's own effectiveness, proven mechanically.
  </behavior>
  <action>
    Add a `non_exempt_paths(self) -> usize` method to `LabelCoverage` returning
    `self.applied.saturating_add(self.skipped_pre_existing_label)` (excluding `skipped_not_owned`),
    and change `application()` to gate `NotApplicable` on `self.non_exempt_paths() == 0` instead of
    `self.policy_paths == 0`, leaving the rest of the match (`applied == 0 => NotApplied`,
    `skipped_pre_existing_label > 0 => PartiallyApplied`, else `Applied`) structurally unchanged.
    Update `guard_skips_path_not_owned_by_current_user`'s assertion (currently `NotApplied` at
    line ~1109) to `NotApplicable`, updating its surrounding comment to state the corrected rule and
    cite WR-28/D-37 explicitly. Add a new `#[test]` (e.g.
    `labels_guard_mixed_owned_and_non_owned_reports_applied_not_partially_applied`) constructing a
    two-rule policy — one applied-owned path, one non-owned path — asserting
    `coverage.application() == LayerApplication::Applied`. In WR-20's existing test
    (`non_owned_path_with_a_foreign_label_is_exempt_not_a_coverage_gap`), immediately after the
    existing `assert_eq!(coverage.skipped_not_owned, 1, ...)` line and before `drop(guard);`, add
    `assert_eq!(coverage.application(), layer_registry::LayerApplication::NotApplicable, "WR-28: an
    all-SkipNotOwned policy must classify NotApplicable, not NotApplied — the exempt category must
    never fabricate a coverage gap");` — pinning the downstream decision the reclassification
    actually changes, not only the entry variant. Add a new `#[test]`
    `cr_06_remediation_command_actually_clears_the_condition` that: plants a foreign/mismatched
    mandatory-label ACE via `plant_mandatory_label_with_flags` on a single-rule policy path (mirror
    the setup already used by the nearby mask-mismatch test), runs `snapshot_and_apply` and confirms
    `SkipPreExistingLabel`/`NotApplied` (the CR-06 broken state), drops the guard, calls
    `clear_mandatory_label` directly on the same path (the exact operation Task 1's PowerShell
    remedy text describes), confirms `low_integrity_label_and_mask` now returns `None`, then re-runs
    `snapshot_and_apply` against the SAME policy and asserts `coverage().application() ==
    LayerApplication::Applied` — proving the prescribed remedy genuinely clears the condition it
    names, not merely that it sounds plausible.
  </action>
  <acceptance_criteria>
    - `cargo test -p nono-sandbox-cli --bin nono --features layer-fault-injection --no-fail-fast --
      --test-threads=1 labels_guard::` passes in full (all pre-existing tests in this module still
      pass with the corrected arithmetic — check each one that references `application()` or
      `NotApplied`/`NotApplicable` for a stale expectation, not only the two named above).
    - Perturbation proof (record in SUMMARY.md): temporarily revert `application()`'s `NotApplicable`
      gate to `self.policy_paths == 0`, re-run
      `labels_guard_mixed_owned_and_non_owned_reports_applied_not_partially_applied` and the WR-20
      pinning assertion, confirm BOTH fail naming the wrong variant, then re-apply the fix.
    - `cr_06_remediation_command_actually_clears_the_condition` passes, proving Task 1's remedy text
      is mechanically correct, not merely well-worded.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-cli --bin nono --features layer-fault-injection --no-fail-fast -- --test-threads=1 labels_guard::</automated>
  </verify>
  <done>LabelCoverage::application() mirrors its sibling's exempt-category rule; WR-20's test pins the downstream decision; CR-06's remedy is proven, not merely asserted, to work. WR-20's host-blocked status is recorded in SUMMARY.md as: authored, structurally reasoned correct, unverifiable on this dev host (non-elevated, no SeTakeOwnershipPrivilege) — needs an elevated/CI Windows runner before being called proven; do not weaken to a silent skip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Operator remediation guidance ↔ the actual guard predicate it must satisfy | Guidance that cannot clear the condition it names is a hard availability failure — the operator cannot run nono over their own workspace |
| `LabelCoverage`/`DaclGrantCoverage`'s shared classification vocabulary | Two sibling implementations must apply the identical rule to the identical physical shape (contract-exempt category), or an auditor reading one cannot trust the other |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-39-01 | Denial of Service | `render_error_for_operator`'s MandatoryIntegrityLabel remedy | mitigate | Name a command proven, by test, to clear the condition (closes CR-06a) |
| T-117-39-02 | Repudiation | `render_error_for_operator`'s non-label channel pointer | mitigate | Point at a channel proven to receive a record on the Abort path (closes WR-27) |
| T-117-39-03 | Repudiation | `LabelCoverage::application()`'s exempt-category arithmetic | mitigate | Mirror `DaclGrantCoverage`'s existing rule; assert the classification RULE with a mixed-case test, not only variant names (closes WR-28) |
</threat_model>

<verification>
`cargo test -p nono-sandbox-cli --bin nono --no-fail-fast -- --test-threads=1
render_error_for_operator` passes. `cargo test -p nono-sandbox-cli --bin nono --features
layer-fault-injection --no-fail-fast -- --test-threads=1 labels_guard::` passes in full. Both
files are under `crates/nono-cli/src/` with `labels_guard.rs` under `exec_strategy_windows/` and
`main.rs` containing `#[cfg(target_os = "linux")]`/`#[cfg(target_os = "macos")]` blocks — per
CLAUDE.md's rule BOTH files trigger the cross-target clippy MUST; run both local gates
(linux-gnu `cross clippy`, apple-darwin `cargo-zigbuild clippy`) and record pass/fail in
SUMMARY.md.
</verification>

<success_criteria>
CR-06, WR-27, WR-28, and WR-20's disposition are all closed. The operator remedy is proven to work
mechanically; the classification rule is mirrored across siblings and asserted by a mixed case, not
merely a renamed variant.
</success_criteria>

<output>
Create `.planning/phases/117-fail-direction-contract-startup-self-attestation/117-39-SUMMARY.md` when done
</output>
