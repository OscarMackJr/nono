---
phase: 117-fail-direction-contract-startup-self-attestation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono-cli/src/exec_strategy_windows/layer_registry.rs
  - crates/nono-cli/src/exec_strategy_windows/mod.rs
autonomous: true
requirements: [CINT-01]

must_haves:
  truths:
    - "D-01: the registry is a code-resident Rust enum + per-row metadata, not a hand-authored table — the SPEC (Plan 03) is derived from or drift-checked against it."
    - "D-02: the registry lives in crates/nono-cli, not crates/nono — CLI owns Windows layer policy."
    - "D-05: the registry's ContractOutcome enum carries the four-value vocabulary (Abort / DegradeWithVisibleClaim / SubstituteEquivalentMechanism / FailOpen), plus the distinct FailOpenDefect value D-06 requires — every one of the 13 rows gets an explicit, spelled-out outcome, not an inferred one (Item-1 fix, checker pass 3)."
    - "D-06: FailOpen and FailOpenDefect are two distinct ContractOutcome values, never conflated — FailOpen carries a written justification, FailOpenDefect names a successor."
    - "D-08: every row carries a per-(entry_path, token-arm) expectancy matrix, not a flat expected/not-expected flag."
    - "D-08: AppContainerProfile is expected at (EntryPath::Broker, None) only — never at (EntryPath::DirectCli, ...) — because nono-cli's own DirectCli gate observes nono-shell-broker.exe, an unconfined Medium-IL process, never the real AppContainer-confined grandchild the broker spawns in its own separate suspended window (Blocker-1 fix, checker pass 2)."
    - "D-09: the registry and every row in it are Windows-only — no Linux/macOS layer is enumerated here (handed to Phase 119 per D-12)."
    - "D-10: the layer set is derived from following prepare_live_windows_launch/spawn_windows_child, with every inclusion and exclusion reasoned on the record — not just the six CINT-01 names."
    - "D-16: every discrepancy finding (SC4-3 and the cross-target cfg table) carries the literal grep command, its hit count, and the date measured, in the module doc comment."
    - "D-33: the minifilter row states structural absence citing ADR-65, in Phase 116 D-08's structurally-blocked form."
    - "Every EntryPath::Broker-expected row must have ContractOutcome::Abort — enforced mechanically by a discovery-based unit test (Task 3), not by comment alone, so a future row expected at the broker entry path without an Abort outcome fails the build rather than going silently unattested (Item-1 fix, checker pass 3)."
  artifacts:
    - path: crates/nono-cli/src/exec_strategy_windows/layer_registry.rs
      provides: "LayerId enum, const ALL, per-row LayerRegistryEntry data with every outcome spelled out, exhaustive drift-guard match, and the broker-abort-only invariant test"
      exports: ["LayerId", "LayerRegistryEntry", "ContractOutcome", "EntryPath", "ArmExpectancy"]
  key_links:
    - from: crates/nono-cli/src/exec_strategy_windows/mod.rs
      to: crates/nono-cli/src/exec_strategy_windows/layer_registry.rs
      via: "pub(crate) mod layer_registry;"
      pattern: "mod layer_registry"
---

<objective>
Build the code-resident Windows fail-direction layer registry: the single source of truth every
other Phase 117 deliverable (SPEC, attestation, per-layer tests) reads from. This plan does the
D-10 derivation work (which layers are in scope and why), resolves two of RESEARCH's open
questions with direct evidence, lands the `LayerId` enum with an exhaustive, drift-guarded
enumeration so a future layer added without registry coverage fails the build, spells out every
row's `ContractOutcome` explicitly, and mechanically enforces the invariant that every
`EntryPath::Broker`-expected row is abort-only.

Purpose: give every downstream plan (03 SPEC, 05 shared probes, 06/07 fault-injection hooks, 08
CLI decision logic, 09 channels, 10/11 gate insertion, 12 tests) one non-negotiable data source
instead of five independent guesses about what layers exist and what they're supposed to do.
Output: `crates/nono-cli/src/exec_strategy_windows/layer_registry.rs` (new), wired into `mod.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-CONTEXT.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-RESEARCH.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-PATTERNS.md
</context>

<interfaces>
<!-- The registry-enum shape to copy near-verbatim (PATTERNS.md, confirmed exact 2026-08-09) -->

From crates/nono/src/undo/types.rs:291-345 (`NetworkAuditDenialCategory`):
```
impl NetworkAuditDenialCategory {
    pub const ALL: &'static [NetworkAuditDenialCategory] = &[ /* one entry per variant */ ];
}
#[cfg_attr(not(test), allow(dead_code))]
fn assert_all_variants_covered(category: &NetworkAuditDenialCategory) {
    match category { /* exhaustive, no wildcard */ }
}
```

From crates/nono/src/machine_policy.rs:150-183, :696-705 (platform-neutral type, cfg(windows)
population — the D-11 split to copy for any Windows-only *population* function this file adds):
```
#[cfg(target_os = "windows")]
pub fn read_machine_egress_policy() -> Result<Option<MachineEgressPolicy>> { .. }

#[cfg(not(target_os = "windows"))]
pub fn read_machine_egress_policy() -> Result<Option<MachineEgressPolicy>> { Ok(None) }
```

Confirmed live (2026-08-09) `WindowsNetworkBackend` dispatch, resolving RESEARCH Open Question 2 —
`crates/nono-cli/src/exec_strategy_windows/network.rs:1500-1533`: `FirewallRulesNetworkBackend` is
selected (reachable, not dead code) when `(WindowsNetworkPolicyMode::Blocked, WindowsNetworkBackendKind::FirewallRules)`
or `(AllowAll, FirewallRules)`, and also as a fallback at `:1517-1525` when `preferred_backend` is
not `Wfp` in the `Blocked` arm. Its `install()` (`network.rs:1535-1595`) fails closed: any
`run_netsh_firewall` `Err` propagates after rolling back the rule it already added
(`:1567`, `:1583-1585`).

Confirmed cross-target `#[cfg(target_os = "linux"|"macos")]` presence (direct grep, 2026-08-09) —
use this table verbatim in the layer_registry.rs doc comment, do not re-grep:
| File | Has literal linux/macos cfg? |
|---|---|
| crates/nono/src/sandbox/mod.rs | YES |
| crates/nono/src/machine_policy.rs | NO |
| crates/nono-cli/src/telemetry/mod.rs, event.rs | NO |
| crates/nono-cli/src/command_runtime.rs | YES |
| crates/nono-cli/src/agent_daemon/launch.rs | NO |
| crates/nono-shell-broker/src/main.rs | NO |
| crates/nono-cli/src/exec_strategy_windows/*.rs (all files) | NO |
| crates/nono-cli/src/execution_runtime.rs, cli.rs, crates/nono/src/error.rs | YES (RESEARCH §I, unchanged) |

**Blocker-1 fix (checker pass 2, 2026-08-09) — the AppContainer cross-process attestation gap.**
RESEARCH §Summary-2 / §D: on the broker arms (`BrokerLaunch`/`BrokerLaunchNoPty`), the process
nono-cli's own `spawn_windows_child` gate (`launch.rs`, `EntryPath::DirectCli`) observes IS
`nono-shell-broker.exe` itself — Medium-IL and deliberately unconfined. The real
AppContainer-confined grandchild is spawned later, inside the broker's own separate
`CREATE_SUSPENDED` window (`nono-shell-broker/src/main.rs:536-661`). Probing `AppContainerProfile`
from `EntryPath::DirectCli` would inspect the wrong process before the real target process even
exists — reproducing CINT-02's own failure mode inside its own fix. **Resolution:** `AppContainerProfile`'s
expectancy is `expected: true` ONLY at `(EntryPath::Broker, None)` and `(EntryPath::Daemon, None)`
— never at any `(EntryPath::DirectCli, ...)` cell. The `(EntryPath::Broker, None)` cell is
genuinely attested, just not by this crate's `attest_and_decide` (Plan 08) — `nono-shell-broker` is
a separate binary that cannot call into `nono-cli`'s attestation module. It is attested by
`nono-shell-broker`'s own local probe-then-decide (Plan 11), driven by the
`NONO_BROKER_REQUIRED_LAYERS` env-var contract Plan 08's `required_layers_for_broker()` derives
directly from this row. This is the registry's way of expressing "expected at this arm, attested
at a different gate site" — the `EntryPath::Broker` variant itself IS that expression; document it
in the row's doc comment so a future reader does not need to rediscover the two-hop spawn shape.
**No other row shares this gap:** `DaclPackageSidGrant`/`WfpEgressFilters`/`JobObjectContainment`'s
dual `(DirectCli, ...)`/`(Daemon, ...)` citations are each attested by that SAME process's own
`attest_and_decide` call (the daemon spawns and probes its own child directly — no intermediary
process, per RESEARCH §B's confirmation that `agent_daemon/launch.rs` never delegates to a second
binary); `RestrictedToken`/`DaclSessionSidGrant` are `WriteRestricted`-arm-only and never present on
a broker arm at all.

**Item-1 fix (checker pass 3, 2026-08-09) — the broker-outcome invariant.** Blocker-1's fix means
`required_layers_for_broker()` (Plan 08) filters registry rows to those with an `ArmExpectancy`
matching `(EntryPath::Broker, expected: true)` AND `outcome: ContractOutcome::Abort` — any row
expected at `EntryPath::Broker` with a NON-`Abort` outcome is silently excluded from that filter
AND already excluded from `attest_and_decide`'s own dispatch (which never runs with
`EntryPath::Broker`), so such a row would go **completely unattested** on the broker arm — the
exact green-by-absence gap Blocker-1 just closed, re-armed for the next added row. Every row's
`ContractOutcome` must therefore be spelled out explicitly in Task 2 (not left inferred), and Task 3
adds a discovery-based unit test asserting `every EntryPath::Broker-expected row has
outcome: ContractOutcome::Abort` — iterating `all_entries()`, never naming `AppContainerProfile` by
string, so a future violating row fails the test on its own.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Resolve open D-10 derivation questions and record the evidence</name>
  <files>crates/nono-cli/src/exec_strategy_windows/layer_registry.rs</files>
  <read_first>
    - crates/nono-cli/src/exec_strategy_windows/network.rs (lines 1490-1600: `WindowsNetworkBackend`
      trait, `FirewallRulesNetworkBackend`/`WfpNetworkBackend`, dispatch, both already re-verified
      2026-08-09 — cite the line numbers above, do not re-derive from scratch)
    - .planning/phases/117-fail-direction-contract-startup-self-attestation/117-RESEARCH.md §A, §G
      (SC4-3), §I (blast radius), Summary points 1-2 and §D (the AppContainer two-hop spawn shape —
      this is Blocker-1's evidence base)
    - crates/nono-cli/src/agent_daemon/launch.rs lines 1-30 (module doc — confirms structural
      independence from exec_strategy_windows/)
    - crates/nono-shell-broker/src/main.rs lines 300-460, 536-661 (confirms the broker's own,
      separate `CREATE_SUSPENDED` window for the real AppContainer child — the Blocker-1 evidence)
  </read_first>
  <action>
    Write a module-level doc comment block at the top of the new `layer_registry.rs` (before any
    code) that records, in D-16 evidence form (command run, hit count, date measured — use
    2026-08-09 for the citations already re-verified in this plan's `<interfaces>` block, and run
    fresh greps for anything not already covered there): (1) the SC4-3 resolution —
    `FirewallRulesNetworkBackend` is reachable in production, is a distinct network-enforcement
    mechanism from WFP (program-path-scoped `netsh advfirewall` block rules vs. session/package-SID
    scoped WFP allow filters), and already fails closed — so it becomes its own registry row
    (`LayerId::FirewallRulesEgress`), not a WFP sub-case; (2) the cross-target cfg-presence table
    from this plan's `<interfaces>` block, verbatim, so Plans 04/09/10/12 can cite it instead of
    re-grepping; (3) the D-10 in/out call for every RESEARCH §A candidate row not already covered by
    CINT-01's six named layers: Job Object containment (IN — full row), env sanitization (OUT —
    data hygiene, not a kernel confinement guarantee a compromised child can defeat, state this
    reasoning inline), `validate_windows_launch_paths` interpreter coverage gate (IN, but as a
    pre-flight `probe: n/a — validated pre-spawn, not re-checked post-spawn` row, not a D-21
    post-spawn attestation row), broker Authenticode trust gate (IN, scoped to the `Broker`/
    `BrokerNoPty` entry paths only), proxy egress (OUT — cross-platform, BOUND-01/Phase 119
    territory, not a Windows-backend-composed layer); (4) the Blocker-1 cross-process attestation
    note verbatim from this plan's `<interfaces>` block — the AppContainer two-hop spawn shape and
    why `EntryPath::Broker` (not `DirectCli`) is the only cell where `AppContainerProfile` is
    `expected: true` on a broker arm; (5) the Item-1 broker-outcome invariant note verbatim from
    this plan's `<interfaces>` block — stated as a named structural constraint the registry data
    (Task 2) and the invariant test (Task 3) both satisfy.
  </action>
  <acceptance_criteria>
    - `layer_registry.rs` module doc comment contains the literal string "SC4-3" and cites
      `network.rs:1500` and `network.rs:1535`
    - Module doc comment contains a markdown table with all 7 files from the cross-target
      cfg-presence interface block above
    - Every RESEARCH §A candidate row (Job Object, env sanitization, interpreter coverage gate,
      broker Authenticode, proxy egress) appears in the doc comment with an explicit IN or OUT
      call and a one-sentence reason
    - Module doc comment contains the literal string "Blocker-1" and names both
      `nono-shell-broker.exe` and `CREATE_SUSPENDED`
    - Module doc comment contains the literal string "Item-1" and states the broker-outcome
      invariant ("every EntryPath::Broker-expected row must have ContractOutcome::Abort")
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Define LayerId, the expectancy matrix, and every row's explicit outcome</name>
  <files>crates/nono-cli/src/exec_strategy_windows/layer_registry.rs</files>
  <read_first>
    - crates/nono-cli/src/exec_strategy_windows/mod.rs lines 42-48 (top-level probe imports),
      139-145 (`ExecConfig.app_container_name` doc — AppContainer arm-precision correction),
      280-333 (`PreparedWindowsLaunch` field ordering), 501-513 (`WindowsNetworkBackend` trait +
      concrete types)
    - crates/nono-cli/src/exec_strategy_windows/launch.rs lines 1237-1339 (`WindowsTokenArm` enum +
      `select_windows_token_arm` cascade)
    - crates/nono-cli/src/exec_strategy_windows/{restricted_token,labels_guard,dacl_guard}.rs
      (enforcing call sites: restricted_token.rs:55, labels_guard.rs:83, dacl_guard.rs:92/236/401 —
      all fail-closed via `?` propagation before spawn, per RESEARCH §A's "what happens today"
      column for each)
    - crates/nono-cli/src/exec_strategy_windows/network.rs lines 1698-1800 (WFP's pre-spawn
      `assert_wfp_activation_installed_filters` — already fail-closed, the basis for its outcome)
    - crates/nono-cli/src/exec_strategy_windows/launch.rs lines 273-402 (Job Object containment —
      `apply_process_handle_to_containment`, fail-closed via terminate+Err)
    - crates/nono-cli/tests/broker_authenticode.rs and its production dispatch gate (fail-closed in
      production layouts, bypassed only under a compile-time-baked dev-layout check — never a
      runtime toggle)
    - crates/nono/src/sandbox/mod.rs lines 194-198 (`validate_windows_launch_paths`'s doc comment —
      "fail-secure: an uncovered interpreter causes the gate to refuse")
    - crates/nono-shell-broker/src/main.rs lines 536-661 (the broker's own suspended-spawn window —
      the site that genuinely attests the `(EntryPath::Broker, None)` cell, per this plan's
      `<interfaces>` Blocker-1 note)
    - crates/nono/src/undo/types.rs lines 280-345 (`NetworkAuditDenialCategory` — the enum shape to
      copy, per this plan's `<interfaces>` block)
  </read_first>
  <action>
    Define `pub(crate) enum LayerId` with variants: `RestrictedToken`, `MandatoryIntegrityLabel`,
    `AppContainerProfile`, `DaclSessionSidGrant`, `DaclPackageSidGrant`, `DaclAncestorTraverse`,
    `DaclAncestorReadAttrs`, `WfpEgressFilters`, `FirewallRulesEgress`, `MinifilterAbsence`,
    `JobObjectContainment`, `BrokerAuthenticodeTrustGate`, `InterpreterCoverageGate`, deriving at
    minimum `Debug, Clone, Copy, PartialEq, Eq` (`Debug`/`PartialEq` are load-bearing — Plan 08's
    `format!("{:?}", id)` convention and this plan's own Task 3 invariant test both depend on them).
    Define `pub(crate) enum EntryPath { DirectCli, Broker, Daemon }` (same derive set — the axis-2
    finding from RESEARCH §B — the daemon path does not go through `WindowsTokenArm` at all; `Broker`
    is a distinct entry path from `DirectCli` precisely because the broker attests its own child
    locally — see Blocker-1 note). Define `pub(crate) struct ArmExpectancy { pub entry_path:
    EntryPath, pub token_arm: Option<&'static str>, pub expected: bool }` (`Debug, Clone, Copy`;
    token_arm names the `WindowsTokenArm` variant as a string literal since `layer_registry.rs`
    should not need to import `launch.rs`'s enum and create a circular concern — state this as a
    deliberate decoupling choice in a doc comment). Define `pub(crate) enum ContractOutcome { Abort,
    DegradeWithVisibleClaim, SubstituteEquivalentMechanism { alternate: &'static str }, FailOpen {
    justification: &'static str }, FailOpenDefect { successor: &'static str } }` per D-05/D-06,
    deriving `Debug, Clone, PartialEq, Eq` (`PartialEq` is load-bearing — Task 3's invariant test
    `assert_eq!`s against `ContractOutcome::Abort`) — `FailOpen` and `FailOpenDefect` are two
    distinct values per the CONTEXT.md D-06/`<specifics>` warning not to conflate them. Define
    `pub(crate) enum ProbeKind { LiveTokenOrJobQuery, ConfirmedByEnforcingComponentReport,
    ConfiguredOnly, NotApplicable }` (same derive set; the `ConfirmedByEnforcingComponentReport`
    variant is this plan's resolution of RESEARCH Open Question 1 — see Task 3). Define
    `pub(crate) struct LayerRegistryEntry { pub id: LayerId, pub name: &'static str, pub call_sites:
    &'static [&'static str], pub expectancy: &'static [ArmExpectancy], pub outcome: ContractOutcome,
    pub probe: ProbeKind }`. Populate one `LayerRegistryEntry` per `LayerId` variant with an
    EXPLICIT, spelled-out `outcome` for every single row (Item-1 fix — do not leave any row's
    outcome to be inferred from its probe kind or call site alone). Per RESEARCH §A's "what happens
    today" column, every enforced layer in this composite is already fail-closed except the
    deliberate minifilter carve-out, so the outcome assignment is: `RestrictedToken: Abort` (fail-
    closed `?` propagation before `CreateProcess*`, `restricted_token.rs:55`/`launch.rs:1403`);
    `MandatoryIntegrityLabel: Abort` (fail-closed `?` propagation, `labels_guard.rs:83`/`mod.rs:425`,
    plus the WRITE_OWNER precheck); `AppContainerProfile: Abort` (fail-closed at parse time in the
    broker, `main.rs:191-205`, AND fail-closed at the daemon's own construction step — this is the
    row Item-1 exists to pin down explicitly, since it is the only row `expected: true` at
    `EntryPath::Broker`); `DaclSessionSidGrant: Abort`, `DaclPackageSidGrant: Abort`,
    `DaclAncestorTraverse: Abort`, `DaclAncestorReadAttrs: Abort` (all four fail-closed via `?`,
    `dacl_guard.rs:92/236/401`, applied `mod.rs:436-482`); `WfpEgressFilters: Abort` (already
    fail-closed and pre-confirmed before `CREATE_SUSPENDED`, per `network.rs:1698-1800` — this
    resolves Open Question 1/SC4-5's tension: the CODE's behavior is already `Abort`, so the
    contract's outcome matches the code rather than the "normally downgrades" framing CONTEXT.md's
    D-18 discussion assumed); `FirewallRulesEgress: Abort` (Task 1's SC4-3 resolution — fails
    closed via `run_netsh_firewall` `Err` propagation with partial-rule rollback);
    `MinifilterAbsence: FailOpen { justification: "ADR-65: no minifilter exists; per-file read
    policy inside one directory is explicitly not claimed" }` (the one deliberate non-Abort row, per
    ADR-65 and Phase 116 D-08's `structurally-blocked` citation); `JobObjectContainment: Abort`
    (fail-closed terminate+`Err`, `launch.rs:273-402`); `BrokerAuthenticodeTrustGate: Abort`
    (fail-closed in production layouts, bypassed only under a compile-time-baked dev-layout check,
    never a runtime toggle); `InterpreterCoverageGate: Abort` (fail-secure pre-flight refusal,
    `crates/nono/src/sandbox/mod.rs:194-198`). Note in a doc comment that no row in this initial
    13-row inventory uses `SubstituteEquivalentMechanism` — the `WindowsTokenArm` cascade
    (`select_windows_token_arm`) resolves the arm from configuration flags upfront, before any
    layer's post-spawn attestation runs, rather than reactively substituting after a specific
    layer's failure — so the vocabulary value exists for D-05 completeness and any future row that
    genuinely needs it, without being forced onto a row that does not fit it. Each `call_sites`
    entry is a `"file:line"` string citing the enforcing call site(s) verified in RESEARCH §A/PATTERNS
    (for layers present on both the `DirectCli` and `Daemon` entry paths — `WfpEgressFilters`,
    `DaclPackageSidGrant`, `JobObjectContainment` — cite BOTH the `exec_strategy_windows/` site and
    the `agent_daemon/launch.rs` site per RESEARCH Open Question 3's recommended dual-citation
    resolution; `AppContainerProfile` cites BOTH `nono-shell-broker/src/main.rs:322-336`/`:536-556`
    (the Broker attesting site) AND `agent_daemon/launch.rs`'s own AppContainer construction step
    (the Daemon attesting site) — never `exec_strategy_windows/launch.rs`, which does not attest
    this layer at all per Blocker-1). Populate `expectancy` per RESEARCH §A/§B's arm table, WITH the
    Blocker-1 correction applied: `RestrictedToken` expected only on `(DirectCli,
    Some("WriteRestricted"))`; `AppContainerProfile` expected ONLY on `(EntryPath::Broker, None)`
    and `(EntryPath::Daemon, None)` — explicitly NOT on any `(EntryPath::DirectCli, ...)` cell,
    including `Some("BrokerLaunchNoPty")` (superseding the original D-08 AppContainer correction
    from RESEARCH Summary point 4, which correctly identified the arm but not the attesting
    process — see this plan's `<interfaces>` Blocker-1 note for the full reasoning). Add a doc
    comment on the `AppContainerProfile` entry stating verbatim: "expected: true at (Broker, None)
    and (Daemon, None); attested locally inside nono-shell-broker (Plan 11) via the
    NONO_BROKER_REQUIRED_LAYERS env-var contract Plan 08's `required_layers_for_broker()` derives
    from this row — never dispatched through `attest_and_decide`, since that function only runs at
    gate sites inside the nono-cli crate (DirectCli, Daemon). outcome: Abort — see Task 3's
    broker-abort-only invariant test, which enforces this mechanically." `MinifilterAbsence`'s
    `expectancy` applies to every `(entry_path, None)` uniformly (the one row with no per-arm
    variation, per RESEARCH §A). Compile on every host per D-11: no `#[cfg]` on the enum/struct
    declarations themselves; the populated `ALL_ENTRIES` data (Task 3) is the one thing gated
    `#[cfg(target_os = "windows")]` with a `#[cfg(not(target_os = "windows"))]` stub returning
    `&[]`, matching the `read_machine_egress_policy` split in this plan's `<interfaces>` block.
  </action>
  <acceptance_criteria>
    - `grep -c "pub(crate) enum LayerId" crates/nono-cli/src/exec_strategy_windows/layer_registry.rs`
      returns 1 with exactly 13 variants
    - `grep -c "LayerId::AppContainerProfile" crates/nono-cli/src/exec_strategy_windows/layer_registry.rs`
      shows an entry whose `expectancy` includes `entry_path: EntryPath::Broker, token_arm: None,
      expected: true` and `entry_path: EntryPath::Daemon, token_arm: None, expected: true`, and does
      NOT map any `entry_path: EntryPath::DirectCli` cell to `expected: true` for this layer
      (Blocker-1 fix — the direct-cli gate observes broker.exe, never the real confined child), and
      whose `outcome` is explicitly `ContractOutcome::Abort` (Item-1 fix)
    - `grep -c "outcome: ContractOutcome::" crates/nono-cli/src/exec_strategy_windows/layer_registry.rs`
      equals 13 (every row has an explicit, spelled-out outcome — Item-1 fix, checker pass 3; none
      inferred or left as a default)
    - Every `LayerRegistryEntry.call_sites` string matches the pattern `\.rs:\d+`
    - `cargo build -p nono-cli` succeeds (compiles on the Windows host; no `.unwrap()`/`.expect()`
      outside test modules)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Exhaustive drift guard, broker-abort-only invariant test, platform-neutral/cfg(windows) split, and module wiring</name>
  <files>crates/nono-cli/src/exec_strategy_windows/layer_registry.rs, crates/nono-cli/src/exec_strategy_windows/mod.rs</files>
  <read_first>
    - crates/nono/src/undo/types.rs lines 347-377 (`assert_all_variants_covered` — the exhaustive
      no-wildcard drift-guard idiom to copy)
    - crates/nono-cli/src/exec_strategy_windows/mod.rs lines 1-80 (existing `mod` declarations —
      find the correct insertion point and existing visibility conventions)
    - Task 2's finished `layer_registry.rs` (this plan's own output so far — confirm `ContractOutcome`
      derives `PartialEq`/`Eq` before writing the invariant test's `assert_eq!`)
  </read_first>
  <action>
    Add `pub(crate) const ALL: &'static [LayerId] = &[ /* all 13 variants, declaration order */ ];`
    and `#[cfg_attr(not(test), allow(dead_code))] fn assert_all_layer_ids_covered(id: &LayerId) {
    match id { /* exhaustive, no wildcard arm, one arm per variant */ } }` following the
    `undo/types.rs` idiom exactly — this is the mechanism that fails the build when a `LayerId`
    variant is added without a corresponding `ALL` entry and match arm. Add a
    `#[cfg(target_os = "windows")] pub(crate) fn all_entries() -> &'static [LayerRegistryEntry]`
    returning the populated data from Task 2, and a
    `#[cfg(not(target_os = "windows"))] pub(crate) fn all_entries() -> &'static [LayerRegistryEntry] { &[] }`
    stub. Add `pub(crate) mod layer_registry;` to `crates/nono-cli/src/exec_strategy_windows/mod.rs`
    (single-line addition, alongside the existing `mod` declarations — do not reorder existing
    declarations). **Item-1 fix, checker pass 3:** add a `#[cfg(all(test, target_os = "windows"))]`
    test module to `layer_registry.rs` with `#[test] fn broker_expected_rows_are_abort_only()` that
    iterates `all_entries()`, and for each `LayerRegistryEntry` whose `expectancy` contains any
    `ArmExpectancy { entry_path: EntryPath::Broker, expected: true, .. }`, asserts
    `entry.outcome == ContractOutcome::Abort` with a named, actionable panic message citing the
    offending `entry.id` (e.g. `format!("{:?} is expected at EntryPath::Broker but its outcome is
    {:?}, not Abort — a non-Abort broker-expected row is silently excluded from both
    attest_and_decide (Blocker-1) and required_layers_for_broker (Plan 08), and goes completely
    unattested on the broker arm", entry.id, entry.outcome)`). This test MUST discover rows by
    iterating `all_entries()` and filtering on `expectancy` — it must NOT hardcode
    `LayerId::AppContainerProfile` or any other specific variant name (D-32's discovery rule, Phase
    115 V-01's "a test that names its targets is blind by construction" lesson) — a future row
    added with `EntryPath::Broker` expectancy and a non-`Abort` outcome must fail this test without
    the test itself being touched.
  </action>
  <acceptance_criteria>
    - `cargo build -p nono-cli` succeeds
    - `grep -c "fn assert_all_layer_ids_covered" crates/nono-cli/src/exec_strategy_windows/layer_registry.rs`
      equals 1, and the match inside has no `_ =>` wildcard arm (verify by reading the function body)
    - `grep -c "mod layer_registry" crates/nono-cli/src/exec_strategy_windows/mod.rs` equals 1
    - `grep -c "fn broker_expected_rows_are_abort_only" crates/nono-cli/src/exec_strategy_windows/layer_registry.rs`
      equals 1, and its body contains `all_entries()` and does NOT contain the literal string
      `LayerId::AppContainerProfile` (confirms discovery-based, not name-based — Item-1 fix)
    - `cargo test -p nono-cli --lib exec_strategy_windows::layer_registry -- --test-threads=1` passes
    - `cargo clippy -p nono-cli -- -D warnings -D clippy::unwrap_used` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry data → SPEC/attestation/tests (Plans 03/08/12) | Every downstream artifact treats this registry as ground truth; if it silently omits a layer, nothing downstream ever attests that layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-06 | Tampering | `layer_registry.rs`'s own drift-guard | mitigate | Exhaustive no-wildcard `assert_all_layer_ids_covered` match (Task 3) — a new `LayerId` variant without an `ALL`/match update fails to compile, not merely fails a test |
| T-117-07 | Repudiation | D-10 exclusion reasoning | mitigate | Task 1 requires every candidate layer (in RESEARCH §A) to carry an explicit IN/OUT call with a stated reason in the module doc comment — an omission-by-silence is treated as a ledger defect per D-10 |
| T-117-21 | Spoofing | AppContainer attestation probing the wrong process | mitigate | `AppContainerProfile`'s expectancy is corrected to `(EntryPath::Broker, None)`/`(EntryPath::Daemon, None)` only (Task 2) — `attest_and_decide` (Plan 08), when called with `EntryPath::DirectCli`, now sees this layer as `NotApplicable` and never probes `nono-shell-broker.exe`'s token, closing the honesty gap the phase exists to prevent from reappearing inside its own implementation |
| T-117-24 | Repudiation | a future EntryPath::Broker row with a non-Abort outcome going silently unattested | mitigate | `broker_expected_rows_are_abort_only` (Task 3) is a discovery-based unit test, not a comment — it iterates every row and fails the build the moment a broker-expected row's outcome is anything other than `Abort`, closing the exact re-armed green-by-absence gap Item-1 identified |
| T-117-SC | Tampering | supply chain | accept | No new crate dependencies added by this plan (pure data/enum construction using existing `windows_sys` features already declared in `crates/nono-cli/Cargo.toml`) |
</threat_model>

<verification>
- `cargo build -p nono-cli` and `cargo clippy -p nono-cli -- -D warnings -D clippy::unwrap_used` both exit 0
- `cargo test -p nono-cli --lib exec_strategy_windows::layer_registry -- --test-threads=1` passes
- `cargo fmt --all -- --check` exits 0
- The module doc comment in `layer_registry.rs` is readable evidence for Plan 03's SPEC authoring —
  no separate scratch document was created
</verification>

<success_criteria>
- `LayerId::ALL` enumerates all 13 layers with zero drift risk (compile-time enforced)
- SC4-3 (`FirewallRulesEgress` reachability) is resolved with cited evidence, not left as
  RESEARCH's "UNRESOLVED"
- Every D-10 candidate layer has a recorded in/out decision
- `AppContainerProfile`'s expectancy no longer causes `attest_and_decide` to probe the wrong process
  (Blocker-1 closed)
- Every row's `ContractOutcome` is spelled out explicitly, and the broker-abort-only invariant is
  mechanically enforced, not merely commented (Item-1 closed)
</success_criteria>

<output>
Create `.planning/phases/117-fail-direction-contract-startup-self-attestation/117-01-SUMMARY.md` when done
</output>
