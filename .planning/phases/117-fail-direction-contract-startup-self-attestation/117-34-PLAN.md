---
phase: 117-fail-direction-contract-startup-self-attestation
plan: 34
type: execute
wave: 14
depends_on: ["117-27", "117-28", "117-29", "117-30", "117-31", "117-32", "117-33"]
gap_closure: true
files_modified:
  - proj/SPEC-windows-fail-direction-contract.md
autonomous: true
requirements: [CINT-01, CINT-02, CINT-03]

must_haves:
  truths:
    - "SC4 — every iteration-5 finding (CR-03, WR-12 through WR-21) is recorded in the SPEC's standing Contract vs. code discrepancies / Review-fix pass ledger with re-run grep evidence, not left to exist only in 117-REVIEW.md, an ephemeral review artifact."
    - "Every iteration-4 row this round continued (CR-01, WR-01, WR-04, WR-06, WR-07, WR-08, WR-09, WR-11) carries an Iteration 5 update addendum pointing at the new row that closes what was left partial, matching this document's own established addendum style."
    - "A final full-suite run confirms no regression beyond the 11 documented pre-existing Windows-host baseline failures."
  artifacts:
    - path: "proj/SPEC-windows-fail-direction-contract.md"
      provides: "CR-03 and WR-12 through WR-21 ledger rows, each with re-run grep evidence dated at this plan's execution, plus Iteration 5 addenda on the 8 continuing prior rows"
---

<objective>
Record this entire gap-closure round (117-27 through 117-33) in the SPEC's standing
"Contract vs. code discrepancies" / "Review-fix pass" ledger — the document `117-VERIFICATION.md`
and `117-REVIEW.md` both name as the standing artifact SC4 requires, and which iteration 5's own
review found had NOT been updated for the round that produced it (reproducing, one round earlier,
the exact "quietly reconciled" failure pattern SC4 exists to prevent). This plan runs LAST, after
every code fix in this round has landed, so its grep evidence reflects the true final state rather
than a state some later plan in the same round might still change.

Per this round's `<plan_the_classes_not_just_the_findings>` instruction: "Where a fix changes a
security-relevant classification, the plan must require a ledger entry" — D-37's reclassification
(WR-12) and WR-20's reclassification both land ledger rows here, not merely in code comments.

Cite D-37 explicitly in the WR-12 row, per the instruction that every plan touching that area must.

Purpose: close SC4 for this round the same way iteration 3's NR3-08/iteration 4's WR-08 closed it
for their own rounds — a standing, re-runnable, dated ledger entry, not an ephemeral review artifact.
Output: 11 new ledger rows (CR-03, WR-12..WR-21) and 8 addenda on continuing prior rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-REVIEW.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-CONTEXT.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-27-SUMMARY.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-28-SUMMARY.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-29-SUMMARY.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-30-SUMMARY.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-31-SUMMARY.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-32-SUMMARY.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-33-SUMMARY.md
</context>

<interfaces>
From `proj/SPEC-windows-fail-direction-contract.md:118-134` (the `## Contract vs. code
discrepancies` table) and `:240-287` (the `## Review-fix pass` table, `RF-NN`/`NR-NN`/`CR-NN`/
`WR-NN` rows, most recently extended through WR-11 by iteration 4's gap-closure round), this plan
appends to the SECOND table — the established row shape is `| # | What was wrong | What changed |`
with dense prose citing exact `grep`/file:line evidence, re-run and dated at write time (D-16: the
literal command run, its hit count, the date measured — never file presence alone). Follow this
exact shape and density; do not invent a new table or format.

Each of the 8 continuing prior rows already ends with an inline `**Iteration 4 update:** ...`
sentence where iteration 4 found a residual (see CR-01, WR-01 has none needed since it stayed fully
resolved... verify per row which already carry an iteration-4-era addendum and which do not before
writing iteration-5 addenda, so this plan does not duplicate an existing sentence).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add the 11 new iteration-5 ledger rows with re-run grep evidence</name>
  <files>proj/SPEC-windows-fail-direction-contract.md</files>
  <read_first>
    proj/SPEC-windows-fail-direction-contract.md (lines 240-288, the full Review-fix pass table —
    read every existing row's evidence style before writing new ones in the same shape)
    .planning/phases/117-fail-direction-contract-startup-self-attestation/117-REVIEW.md (the full
    iteration-5 findings text — CR-03 and WR-12 through WR-21 — the source for what each new row
    describes)
    All 117-27 through 117-33 SUMMARY.md files (already listed in this plan's `<context>`) — the
    source for what each row's "What changed" column states, plan-attributed
  </read_first>
  <action>
    Append 11 new rows to the `## Review-fix pass` table, one per iteration-5 finding, each following
    the exact `| ID (Iteration 5, gap-closure Plan 117-NN) | What was wrong | What changed |` shape
    the existing CR-01/CR-02/WR-01..WR-11 rows use: `CR-03` (Plan 117-27) — the 1-of-3-sites gate and
    its widened text-scanning test; `WR-12` (Plan 117-28) — cite D-37 by name, the three-arm
    classification, and the classification-rule table replacing the name-set comparison; `WR-13`
    (Plan 117-31) — the shared boundary predicate and Type::method resolution; `WR-14` (Plan 117-28)
    — the daemon_enum_segment anchor helper and the `//`-comment filter; `WR-15` (Plan 117-27) — the
    corrected destination text (Windows Application event log, event id 10011); `WR-16` (Plan 117-27)
    — the granted-path validation; `WR-17` (Plan 117-29) — the layer-specific remediation branch;
    `WR-18` (Plan 117-29) — `low_integrity_label_rid`'s delegation to `low_integrity_label_ace`;
    `WR-19` (Plan 117-32) — the symbol-form Manual-verification citation and the two new drift gates;
    `WR-20` (Plan 117-30) — the non-owned + foreign-label combined-condition test; `WR-21` (Plan
    117-33) — `advance_and_emit`'s lock-scope parity and `inner`'s narrowed visibility. For EVERY row,
    re-run the specific `grep -n`/`grep -c` command that verifies the fix landed (per D-16: literal
    command, hit count, date measured — this plan's own execution date, not iteration 5's review
    date) rather than copying evidence text from the review or a SUMMARY.md verbatim without
    re-running it against the current tree.
  </action>
  <acceptance_criteria>
    - `grep -c "^| CR-03\|^| WR-1[2-9]\|^| WR-20\|^| WR-21" proj/SPEC-windows-fail-direction-contract.md`
      == 11 (all 11 new rows present as table rows, not prose elsewhere).
    - Every new row contains at least one literal `grep -n` or `grep -c` command string and a hit
      count, matching the document's existing D-16 evidence density (spot-check: no new row is
      shorter than the shortest existing WR-NN row's evidence sentence).
    - Every new row names the Plan number (`117-27` through `117-33`) that produced the fix.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^| CR-03\|^| WR-1[2-9]\|^| WR-20\|^| WR-21" proj/SPEC-windows-fail-direction-contract.md</automated>
  </verify>
  <done>All 11 iteration-5 findings are recorded in the standing ledger with re-run, dated evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Add Iteration 5 addenda to the 8 continuing prior rows; run the final full-suite consistency check</name>
  <files>proj/SPEC-windows-fail-direction-contract.md</files>
  <read_first>
    proj/SPEC-windows-fail-direction-contract.md (lines 275-287, the 8 rows this task appends to:
    CR-01, WR-01, WR-04, WR-06, WR-07, WR-08, WR-09, WR-11 — read each one's existing text in full
    before appending, so the addendum does not duplicate or contradict what is already there)
  </read_first>
  <action>
    Append one `**Iteration 5 update:**` sentence to each of the 8 rows, pointing forward to the new
    row that closed what iteration 5 found still partial: CR-01 -> points at WR-18 (the sibling
    reader `low_integrity_label_rid` also fixed); WR-01 -> points at WR-20 (the reclassification now
    pinned by a combined-condition test); WR-04 -> points at WR-17 (the remediation text is now
    layer-specific and names a command that can diagnose the failure); WR-06 -> points at WR-12/WR-14
    (the cross-mirror test now compares the classification rule, not names, and is anchor-hardened);
    WR-07 -> points at WR-12 (D-37's three-arm classification resolves the over-correction WR-07's
    fix introduced); WR-08 -> points at WR-13/WR-19 (the boundary/qualifier fix and the whole-document
    citation drift gate); WR-09 -> points at WR-21 (lock-scope parity restored, `inner` narrowed);
    WR-11 -> points at WR-14 (the sibling test now routes through the same anchor). Match the
    document's existing addendum style exactly (e.g. CR-01's row already ends with `**Iteration 4
    update:** this residue predicate was found to accept a structurally-ineffective INHERIT_ONLY_ACE
    (CR-01) ...` — this task's new sentence is a peer of that one, appended after it, not replacing
    it). After all addenda are written, run the full local verification suite to confirm this
    round produced no regression: `cargo build --workspace --all-targets`,
    `cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used`,
    `cargo fmt --all -- --check`, and `cargo test --workspace` (plus
    `cargo test -p nono-sandbox-cli --features layer-fault-injection` for the fault-injection suite
    this round's tests touch). Record the pass/fail counts in SUMMARY.md, cross-checking any Windows
    unit-test failures against the known 11-failure baseline (`config::tests`, `protected_paths::tests`,
    etc., HOME/env parallel-test races) named in `CLAUDE.md`/this session's project context — do not
    treat those 11 as new regressions, but name any COUNT beyond 11 explicitly as a real regression
    requiring investigation before this plan is considered done.
  </action>
  <acceptance_criteria>
    - `grep -c "Iteration 5 update" proj/SPEC-windows-fail-direction-contract.md` == 8.
    - `cargo build --workspace --all-targets` succeeds.
    - `cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used` is clean.
    - `cargo fmt --all -- --check` is clean.
    - `cargo test --workspace` total failure count is documented in SUMMARY.md and does not exceed
      the known 11-failure Windows-host baseline plus zero new failures.
    - Both cross-target clippy gates (per D-35, since this round touched `cli_bootstrap.rs`,
      `output.rs`, `main.rs`) were confirmed clean by their owning plans (117-27, 117-29) — this task
      does not re-run them, only confirms both SUMMARY.md files record a clean or documented-fallback
      result.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --workspace</automated>
  </verify>
  <done>All 8 continuing prior rows point forward to their iteration-5 closure; the full workspace builds, lints, formats, and tests clean (baseline-adjusted).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| The SPEC document ↔ its readers (Phase 118/119, future audits, this project's own SC4 gate) | An unrecorded discrepancy is invisible to everyone who trusts the document as authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-34-01 | Repudiation | SPEC's Review-fix pass ledger | mitigate | Every iteration-5 finding recorded with re-run, dated evidence before this phase's next verification pass (closes the round's own SC4 gap) |
</threat_model>

<verification>
`grep -c "^| CR-03\|^| WR-1[2-9]\|^| WR-20\|^| WR-21" proj/SPEC-windows-fail-direction-contract.md`
== 11. `grep -c "Iteration 5 update"` == 8. Full workspace build/clippy/fmt/test suite is clean,
baseline-adjusted for the 11 documented pre-existing Windows-host failures.
</verification>

<success_criteria>
SC4 is closed for this round: every iteration-5 finding is recorded in the standing ledger with
re-runnable evidence, and every continuing iteration-4 finding points forward to its closure — the
document does not stop at WR-11 while 11 more findings exist only in an ephemeral review artifact.
</success_criteria>

<output>
Create `.planning/phases/117-fail-direction-contract-startup-self-attestation/117-34-SUMMARY.md` when done
</output>
