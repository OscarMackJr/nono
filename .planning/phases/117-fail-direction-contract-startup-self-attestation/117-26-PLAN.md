---
phase: 117-fail-direction-contract-startup-self-attestation
plan: 26
type: execute
wave: 11
depends_on: ["117-20", "117-21", "117-22", "117-23", "117-24", "117-25"]
files_modified:
  - proj/SPEC-windows-fail-direction-contract.md
autonomous: true
requirements: [CINT-01, CINT-02, CINT-03]

must_haves:
  truths:
    - "The SPEC's discrepancy ledger records CR-01, CR-02, and every one of WR-01 through WR-11, each with a disposition (the fix that landed this round, or an explicit operator-accepted-risk entry) and re-runnable evidence per D-16 (closes the SC4 gap)."
    - "The SPEC's '## Layer registry' table's 'Enforcing call site(s)' column matches layer_registry.rs's current, post-117-24 symbol-form citations exactly for all 13 rows — no drift between the code (source of truth) and the transcribed table."
    - "The SPEC's pre-existing NR3-01, NR3-04, and NR3-05 rows are corrected to describe the ACTUAL, current (post-this-round) behavior, not the behavior that was true before this round's fixes landed."
    - "REQUIREMENTS.md's CINT-01/CINT-02/CINT-03 rows are NOT flipped to Complete by this plan — that determination belongs to re-verification, not to the plan that fixes the gaps re-verification found."
  artifacts:
    - path: "proj/SPEC-windows-fail-direction-contract.md"
      provides: "a discrepancy ledger with zero unrecorded findings from iteration 4, a citation table matching the code, and corrected prior-round rows"
  key_links:
    - from: "proj/SPEC-windows-fail-direction-contract.md (## Layer registry table)"
      to: "crates/nono-cli/src/exec_strategy_windows/layer_registry.rs (REGISTRY_ENTRIES)"
      via: "manual transcription, verified equal by direct comparison during this plan's execution"
      pattern: "Enforcing call site"
---

<objective>
Close SC4: the discrepancy ledger `proj/SPEC-windows-fail-direction-contract.md`'s "Review-fix pass"
table stops at NR3-08 (iteration 3). Neither CR-01, CR-02, nor any of WR-01 through WR-11 (iteration
4's code-review findings, closed or dispositioned by Plans 117-20 through 117-25) is recorded anywhere
in the standing contract document — they exist only in the ephemeral `117-REVIEW.md`. This is the exact
"quietly reconciled" failure pattern SC4 exists to prevent, reproduced inside the very gap-closure round
meant to close it.

This plan is deliberately the LAST plan in this round, run alone in the final wave, because it is the
only plan positioned to read the true, final, post-fix state of everything else this round changed and
record it accurately. It must never record a disposition that is not yet true — if a warning was
accepted rather than fixed, its row says so explicitly; nothing is recorded as "closed" that a prior
plan in this round did not actually close.

Purpose: the SPEC must be a truthful, current description of shipped behavior — CINT-01's own
requirement — with every known contract/code discrepancy from this round visible in the standing
document, not left in a review artifact that gets archived with the phase.
Output: `proj/SPEC-windows-fail-direction-contract.md` with 13 new discrepancy rows, a corrected
citation table, and corrected prior-round rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-REVIEW.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-20-SUMMARY.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-21-SUMMARY.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-22-SUMMARY.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-23-SUMMARY.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-24-SUMMARY.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-25-SUMMARY.md
</context>

<interfaces>
`proj/SPEC-windows-fail-direction-contract.md`'s two relevant existing structures, to extend rather
than restructure:

1. `## Layer registry` table (line ~50-64), 13 rows, columns `| LayerId | Name | Enforcing call
   site(s) | Expected on | Outcome | Probe |` — the "Enforcing call site(s)" column is what Plan 117-24
   changes in code; this plan transcribes the NEW citations here.

2. `## Review-fix pass (code review 2026-08-10)` table (line ~248-274), columns `| # | What was wrong
   | What changed |`, currently ending at row `NR3-08 (Iteration 3, gap-closure Plans 117-15 and
   117-19)`. This plan appends 13 new rows using the SAME format, labelled `CR-01 (Iteration 4,
   gap-closure Plan 117-20)`, `CR-02 (Iteration 4, gap-closure Plan 117-21)`, `WR-01` through `WR-11`
   (each `(Iteration 4, gap-closure Plan 117-2X)` citing the actual plan that dispositioned it, per the
   mapping in `<action>` below).

D-16's evidence discipline (already the house style in this table, see the NR3-01..NR3-08 rows for the
exact form to match): each new row's "What changed" cell must be backed by a literal, re-runnable
`grep`/`cargo test` command and its actual output/hit-count, dated to when this plan runs — not a
restated claim from `117-REVIEW.md` taken on faith. Re-run every cited command against the CURRENT tree
during this plan's execution.

Finding → disposing plan mapping (verify each is TRUE by reading that plan's SUMMARY before writing the
row — do not assume):
- CR-01, WR-01, WR-02, WR-03 → Plan 117-20 (labels_guard.rs residue predicate)
- CR-02, WR-04 → Plan 117-21 (downgrade channel + remediation wiring)
- WR-06, WR-11 → Plan 117-22 (daemon doc correction + cross-mirror + anchor test)
- WR-07 → Plan 117-23 (ancestor-guard predicate width)
- WR-08 (both the citation-drift half and the content-check half) → Plan 117-24 (citation conversion +
  selfcheck tightening); WR-10 → also Plan 117-24 (contains_fn_exact)
- WR-05, WR-09 → Plan 117-25 (ci.yml + telemetry accessor)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Sync the "## Layer registry" table's citations to the post-117-24 code</name>
  <files>proj/SPEC-windows-fail-direction-contract.md</files>
  <read_first>
    proj/SPEC-windows-fail-direction-contract.md (read lines 1-66 for the full "## Layer registry"
    table)
    crates/nono-cli/src/exec_strategy_windows/layer_registry.rs (read the full REGISTRY_ENTRIES const,
    lines 858-1055, to get the CURRENT, post-117-24 call_sites arrays verbatim)
  </read_first>
  <behavior>
    Every one of the 13 rows in the SPEC's "Enforcing call site(s)" column matches its corresponding
    `REGISTRY_ENTRIES` entry's `call_sites` array exactly (same symbols, same order, comma-joined in
    the same style the table already uses for the 4 rows that were already correct before this round).
  </behavior>
  <action>
    For each of the 9 rows Plan 117-24 converted (`RestrictedToken`, `MandatoryIntegrityLabel`,
    `AppContainerProfile`, `DaclPackageSidGrant`, `WfpEgressFilters`, `FirewallRulesEgress`,
    `JobObjectContainment`, `BrokerAuthenticodeTrustGate`, `InterpreterCoverageGate`), replace that
    row's "Enforcing call site(s)" cell with the exact, current `call_sites` array content from
    `layer_registry.rs`, comma-and-backtick-joined in the same Markdown-table style the other cells
    already use (e.g. `` `restricted_token.rs::create_restricted_token_with_sid`,
    `launch.rs::spawn_windows_child` ``). Leave the 4 already-correct rows
    (`DaclSessionSidGrant`, `DaclAncestorTraverse`, `DaclAncestorReadAttrs`, `MinifilterAbsence`)
    unchanged unless reading `layer_registry.rs` shows they also changed (they should not have — this
    plan's job is to confirm, not assume).
  </action>
  <acceptance_criteria>
    - For every one of the 13 rows, the SPEC table's "Enforcing call site(s)" cell content, compared
      symbol-by-symbol, equals that row's `call_sites` array in `layer_registry.rs` — verified by
      direct side-by-side read during this task, not by a partial spot-check.
    - `grep -c '\.rs:[0-9]' proj/SPEC-windows-fail-direction-contract.md` — every remaining
      `file:line`-shaped citation in the document (if any survive outside the "## Layer registry"
      table, e.g. in prose describing history) is verified to be historical/descriptive text, not a
      live citation cell.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "restricted_token.rs::create_restricted_token_with_sid" proj/SPEC-windows-fail-direction-contract.md</automated>
  </verify>
  <done>The SPEC's registry table citations match layer_registry.rs's current source exactly, for all 13 rows.</done>
</task>

<task type="auto">
  <name>Task 2: Append the 13 iteration-4 discrepancy rows to the "## Review-fix pass" table</name>
  <files>proj/SPEC-windows-fail-direction-contract.md</files>
  <read_first>
    proj/SPEC-windows-fail-direction-contract.md (read lines 240-275, the full existing "## Review-fix
    pass" table, to match its exact row format)
    .planning/phases/117-fail-direction-contract-startup-self-attestation/117-REVIEW.md (the full CR-01,
    CR-02, and all 11 WR-NN sections — the finding text each new row summarizes)
    the six 117-2X-SUMMARY.md files listed in this plan's `<context>` (to confirm what each plan
    ACTUALLY did, not what it was planned to do — record the true disposition)
  </read_first>
  <behavior>
    13 new rows appended after the existing `NR3-08` row, each with a re-runnable command + hit count
    + today's date, and a "What changed" cell describing the ACTUAL landed fix (or, if a finding was
    accepted rather than fixed, an explicit "Accepted, not fixed" statement with the reasoning) — never
    a restated claim without independently re-running its evidence command against the current tree.
  </behavior>
  <action>
    Append 13 rows to the table in this order: `CR-01`, `CR-02`, `WR-01` through `WR-11`. For each:
    write the `#` cell as `{ID} (Iteration 4, gap-closure Plan {plan-id})` per the mapping in
    `<interfaces>`; write "What was wrong" as a 1-3 sentence summary of the finding (condensed from
    `117-REVIEW.md`, in this document's own voice, not copy-pasted verbatim); write "What changed" as
    the concrete fix, citing at least one re-run command and its live output for the specific claim
    (e.g. for CR-01: re-run `grep -c "header.AceFlags" crates/nono/src/sandbox/windows.rs` and record
    the NEW non-zero count and today's date; for WR-05: re-run `grep -c "NONO_CI_HAS_WFP"
    .github/workflows/ci.yml` and record `0`; for WR-09: re-run `grep -n "pub(crate) chain:"
    crates/nono-cli/src/telemetry/mod.rs` and record zero matches). For any finding where the disposing
    plan's actual SUMMARY shows a different outcome than this plan's `<interfaces>` mapping assumed
    (e.g. a cross-target clippy gate fell back to PARTIAL→CI with a documented runner failure, or a
    WR-02-style "corrected the doc, did not implement a refcount" disposition), write that TRUE outcome,
    not the assumed one — this task's whole purpose is to prevent exactly the kind of assumed-not-verified
    claim SC4 exists to catch.
  </action>
  <acceptance_criteria>
    - `grep -n "CR-01 (Iteration 4\|CR-02 (Iteration 4" proj/SPEC-windows-fail-direction-contract.md`
      shows both rows present.
    - `grep -c "WR-0[1-9] (Iteration 4\|WR-1[01] (Iteration 4" proj/SPEC-windows-fail-direction-contract.md`
      equals 11.
    - Every one of the 13 new rows' "What changed" cell contains at least one backtick-quoted
      command and a literal result value (not a bare prose claim with no command shown) — spot-checked
      by reading at least 5 of the 13 rows in full.
    - No row claims a fix that this plan did not independently re-verify against the current tree
      during this task's own execution (self-check: for each row, the command shown was actually run
      by this task, not transcribed from `117-REVIEW.md`'s pre-fix findings).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Iteration 4, gap-closure Plan 117-2" proj/SPEC-windows-fail-direction-contract.md</automated>
  </verify>
  <done>All 13 iteration-4 findings are recorded in the standing SPEC ledger with re-runnable, dated evidence and their true disposition.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the pre-existing NR3-01, NR3-04, and NR3-05 rows to describe post-round reality; confirm REQUIREMENTS.md stays Pending</name>
  <files>proj/SPEC-windows-fail-direction-contract.md</files>
  <read_first>
    proj/SPEC-windows-fail-direction-contract.md (read the NR3-01 row at line ~269, the NR3-04 row at
    line ~272, and the NR3-05 row at line ~273 — the three pre-existing rows this round's fixes make
    stale)
    .planning/REQUIREMENTS.md (read the CINT-01/CINT-02/CINT-03 rows — confirm current status, do not
    modify this file from this plan)
  </read_first>
  <behavior>
    - NR3-01's row no longer implies the residue predicate is sound as originally shipped by 117-13 —
      it gains a forward-reference to the new CR-01/WR-01 rows this task's sibling Task 2 just added,
      noting the predicate was widened and reordered by Plan 117-20.
    - NR3-04's row no longer states the false "tracing routes to the log file / Event Log subscriber,
      never to the confined child's stderr" premise — it gains a forward-reference to CR-02, noting the
      warn is now gated on the private-channel check Plan 117-21 added.
    - NR3-05's row no longer states the false "no partial-success return" premise for
      `DaemonDaclGuard::apply` — it gains a forward-reference to WR-06, noting the TRUE
      under-granting-not-under-confining rationale Plan 117-22's doc correction states.
    - `.planning/REQUIREMENTS.md`'s CINT-01/CINT-02/CINT-03 rows are read and confirmed still `[ ]
      Pending` — this task does NOT edit that file.
  </behavior>
  <action>
    Append one sentence to the end of the NR3-01 row's "What changed" cell: "**Iteration 4 update:**
    this residue predicate was found to accept a structurally-ineffective `INHERIT_ONLY_ACE` (CR-01)
    and to run before the ownership gate (WR-01); see those rows above for the fix Plan 117-20 landed."
    Append one sentence to the end of the NR3-04 row's "What changed" cell: "**Iteration 4 update:**
    the unconditional warn this row describes was found to violate D-28 by carrying layer names onto
    the shared console by default (CR-02); see that row above for the gating fix Plan 117-21 landed."
    Append one sentence to the end of the NR3-05 row's "What changed" cell: "**Iteration 4 update:**
    this row's premise that `DaemonDaclGuard::apply` 'has no partial-success return' was found false
    (WR-06) — passes 1 and 3 do have skip/break arms, but their fail direction is under-granting, never
    under-confining; see that row above for the corrected rationale Plan 117-22 landed." Do not touch
    `.planning/REQUIREMENTS.md` in this plan — re-verification, not this plan, decides whether
    CINT-01/02/03 move to Complete.
  </action>
  <acceptance_criteria>
    - `grep -n "Iteration 4 update" proj/SPEC-windows-fail-direction-contract.md` shows exactly 3
      matches (NR3-01, NR3-04, NR3-05).
    - `git diff --stat .planning/REQUIREMENTS.md` shows no changes from this plan (file untouched).
    - `grep -n "CINT-01\|CINT-02\|CINT-03" .planning/REQUIREMENTS.md` still shows all three as `[ ]
      Pending` (confirmed, not modified).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Iteration 4 update" proj/SPEC-windows-fail-direction-contract.md</automated>
  </verify>
  <done>The three stale pre-existing rows now forward-reference this round's corrections; REQUIREMENTS.md is confirmed untouched and still Pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Standing contract document ↔ actual shipped code | The SPEC is read by Phase 118 (receipts) and Phase 119 (boundary statement) as ground truth; an inaccurate ledger propagates false confidence downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-26-01 | Repudiation | SPEC discrepancy ledger | mitigate | Record all 13 iteration-4 findings with re-runnable, dated evidence before the phase can close (closes the SC4 gap) |
| T-117-26-02 | Tampering | REQUIREMENTS.md completion status | mitigate | This plan explicitly does not flip CINT-01/02/03 to Complete — that is re-verification's call, not a plan's self-assessment |
</threat_model>

<verification>
`grep -c "Iteration 4, gap-closure Plan 117-2"` in the SPEC equals 13. The registry-table citations
match `layer_registry.rs` exactly for all 13 rows. `git diff --stat .planning/REQUIREMENTS.md` shows no
changes.
</verification>

<success_criteria>
SC4 is closed: every iteration-4 finding (CR-01, CR-02, WR-01 through WR-11) is recorded in the standing
SPEC with its true, re-verified disposition. The registry citation table matches the code. The three
prior-round rows this round's fixes made stale are corrected. REQUIREMENTS.md is untouched.
</success_criteria>

<output>
Create `.planning/phases/117-fail-direction-contract-startup-self-attestation/117-26-SUMMARY.md` when done
</output>
