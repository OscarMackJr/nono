---
phase: 117-fail-direction-contract-startup-self-attestation
plan: 45
type: execute
wave: 17
depends_on: ["117-35", "117-36"]
gap_closure: true
files_modified:
  - crates/nono-cli/src/agent_daemon/launch.rs
autonomous: true
requirements: [CINT-02, CINT-03]

must_haves:
  truths:
    - "`DaclAncestorTraverse` is attested from the vector that actually records ancestor-traverse grants (`traverse_applied`), not from the workspace write grant — so its negative is representable and a failure in it can be named (WR-31)."
    - "The two rows' inputs are DISTINCT EXPRESSIONS, enforced by extending the existing wiring test that already imposes exactly this rule on `wfp_filters_installed`/`network_scoping_required` — the rule is asserted, not just implemented."
    - "The daemon arm is swept as a CLASS: every `Abort`-outcome registry row expected at `(Daemon, None)` is checked for having its own input, not just the one WR-31 named."
    - "The D-37 daemon test cannot leave an inheritable write-class ACE behind on a world-writable shared directory, and cannot bind to a pre-planted junction (WR-33)."
  artifacts:
    - path: "crates/nono-cli/src/agent_daemon/launch.rs"
      provides: "daemon_attest_and_decide with one input per attested row, and a panic-safe exclusively-created D-37 fixture"
  key_links:
    - from: "crates/nono-cli/src/agent_daemon/launch.rs::daemon_attest_and_decide"
      to: "crates/nono-cli/src/exec_strategy_windows/layer_registry.rs"
      via: "DaclAncestorTraverse's (Daemon, None) expectancy row now has an input that can express its own negative"
      pattern: "green-by-absence"
---

<objective>
Close WR-31 and WR-33, both in the daemon launch path.

**WR-31 — green by absence.** `daemon_attest_and_decide` attests **two** registry rows —
`DaclPackageSidGrant` *and* `DaclAncestorTraverse`, both `Abort`-outcome, both declared expected at
`(Daemon, None)` — from a **single** input. Confirmed by direct read this planning session:
`granted_write_access()` at `:121-122` is `!self.write_applied.is_empty()`; `write_applied` is
pushed **only** by pass 2, the workspace write grant (`:210`). `traverse_applied` — the vector
passes 1 and 3 populate (`:175`, `:240`), i.e. everything `DaclAncestorTraverse` actually
describes — is never read by the gate. The caller takes `dacl_guard.granted_write_access()` at
`:813` and passes it as `dacl_guard_applied` at `:906`; the gate's only use is
`if !dacl_guard_applied` at `:1401-1403`, which returns `layer: "DaclPackageSidGrant"`.

So `DaclAncestorTraverse`'s negative is **unrepresentable** on the daemon arm, and when the
predicate does fire the abort always names the other row — a `DaclAncestorTraverse` failure is
unreportable even in principle. WR-06 accepted the pass-1/pass-3 skip arms as *"under-granting,
never under-confining"*, which justifies not **aborting**; it does not justify a distinct
`Abort`-outcome row sharing an unrelated row's input. This is the "green by absence" shape the
SPEC's Structural constraints section names as the failure mode this phase exists to close — one
row over from where D-37 looked.

**WR-33 — test fixture on a world-writable path.** The D-37 daemon test added by 117-28
(`daemon_dacl_guard_apply_succeeds_when_immediate_ancestor_is_non_owned`, `:2662`) builds its
fixture under `%PUBLIC%` (`:2667`) with `std::fs::create_dir_all` (`:2678`). `create_dir_all`
**succeeds on an already-existing path**, including a pre-planted directory junction or symlink,
and any local user can write to `C:\Users\Public`. The test then runs `DaemonDaclGuard::apply`,
which calls `grant_sid_write_on_path(workspace, TEST_PACKAGE_SID, /*inheritable=*/true)` — writing
an inheritable write-class ACE to whatever that path resolves to. Cleanup runs only on the
`Ok(guard)` and precondition-panic arms, so a panic inside `apply` leaves the directory and its ACE
behind on a shared location. The `<pid>-<nanos>` name makes this hard to win in practice, which is
why it is a Warning — but a security-test suite should not choose a world-writable shared directory
when the same D-37 condition is reachable with an exclusive create.

Purpose: give each attested row an input that can express its own negative, and stop the test that
proves D-37 from being the least safe filesystem operation in the suite.
Output: a `granted_ancestor_traverse()` accessor with its own gate arm and its own classification;
a wiring test extended to enforce input distinctness for the daemon rows as a class; an exclusively
created, panic-safe, reparse-point-checked D-37 fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-REVIEW.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-CONTEXT.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-BASELINE-ROUND4.md
</context>

<interfaces>
All line numbers confirmed by direct read/grep in this planning session, in
`crates/nono-cli/src/agent_daemon/launch.rs` unless stated.

`DaemonDaclGuard`:
- `:89-91` — doc comment distinguishing `write_applied` (paths given
  `nono::grant_sid_write_on_path`) from `traverse_applied` (paths given
  `nono::grant_sid_traverse_on_path`). **The distinction is already documented; only the gate
  ignores it.**
- `:103` / `:105` — the two fields. `:153-154` — both initialised empty.
- `:175` — pass 1 pushes to `traverse_applied`. `:210` — pass 2 pushes `workspace` to
  `write_applied`. `:240` — pass 3 pushes to `traverse_applied`.
- `:121-122` — `pub(crate) fn granted_write_access(&self) -> bool { !self.write_applied.is_empty() }`.
  The accessor to pair with a new sibling.
- `:272-282` — the revert loop, draining both vectors. Confirms both are real, populated state.

Gate and caller:
- `:813` — `let dacl_guard_applied = dacl_guard.granted_write_access();`
- `:892-906` — the `match daemon_attest_and_decide(..)` call; `dacl_guard_applied` is argument 4
  (`:906`).
- `:1319-1332` — `daemon_attest_and_decide`'s doc comment, which **explicitly documents both rows
  as driven by `dacl_guard_applied`** (`:1319-1321`) and describes the
  `ProceedDowngraded { downgraded: ["DaclPackageSidGrant", "DaclAncestorTraverse",
  "DaclAncestorReadAttrs"] }` shape (`:1331-1332`). Note the THIRD row named there —
  `DaclAncestorReadAttrs`. Task 2's class sweep must cover it too; this plan does not assume the
  class is exactly two.
- `:1356-1360` — the signature. `:1401-1406` — `if !dacl_guard_applied { return
  DaemonAttestationDecision::Abort { layer: "DaclPackageSidGrant", .. } }`.
- `:1246` — an existing doc note that `granted_write_access()` "cannot express" something; read it,
  it may already concede part of WR-31.

**Blast radius — every `daemon_attest_and_decide` call site** (adding a parameter breaks all of
them; they pass positional bools): `:900`, `:1812`, `:1874`, `:1880`, `:1884`, `:1887`, `:1963`.
`:1909-1911` asserts `assert_eq!(layer, "DaclPackageSidGrant")` — check whether that assertion is
still correct after the split, or whether it was passing for the wrong reason.

The wiring test to extend:
- `:2094` — `fn daemon_attestation_gate_is_wired_to_real_outcomes_not_the_gate_condition`. Read it
  in full. It already imposes the input-distinctness rule on
  `wfp_filters_installed`/`network_scoping_required`; the extension mirrors that mechanism rather
  than inventing a second one.

The WR-33 test:
- `:2641-2660` — the doc comment explaining why `%PUBLIC%` was chosen (it explains the D-37
  condition being exercised — preserve that rationale).
- `:2662` — the test fn. `:2667` — `std::env::var_os("PUBLIC")`. `:2678-2682` — the
  `create_dir_all` to replace. `:2683-2710` — the explicit precondition checks on `%PUBLIC%`
  ownership, which already fail loudly; keep that discipline.
- `:2556` — a second `create_dir_all(&workspace)` in a different daemon test. **Check whether it
  has the same exposure** before concluding WR-33 is a single site.

`crates/nono-cli/src/exec_strategy_windows/layer_registry.rs` — the `DaclAncestorTraverse`
expectancy row including `Daemon`. **Modified by 117-36 in the previous wave** (WR-32 doc-comment
citations); re-read as landed before citing it.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Give `DaclAncestorTraverse` its own input and its own classification (WR-31)</name>
  <files>crates/nono-cli/src/agent_daemon/launch.rs</files>
  <read_first>
    `:80-300` (the guard struct, all three passes, both accessors, the revert loop) and
    `:1230-1420` (`daemon_attest_and_decide`'s doc comment and body in full). Then every call site
    in the blast-radius list. Then `layer_registry.rs`'s `DaclAncestorTraverse` and
    `DaclAncestorReadAttrs` rows **as landed by 117-36**, to confirm each row's declared outcome
    and expectancy before deciding what its arm should do.
  </read_first>
  <behavior>
    - `granted_ancestor_traverse()` returns `!traverse_applied.is_empty()` and is the ONLY input to
      the `DaclAncestorTraverse` classification.
    - `granted_write_access()` remains the only input to `DaclPackageSidGrant`. Neither row reads
      the other's vector.
    - A run where pass 2 succeeded but passes 1 and 3 granted nothing classifies
      `DaclAncestorTraverse` distinctly from `DaclPackageSidGrant` — the state that is
      unrepresentable today.
    - **Outcome disposition preserved:** per WR-06, an absent ancestor traverse is under-granting,
      never under-confining, so the new arm does NOT abort. It classifies separately (per D-37's
      contract-exempt rule) so the two rows stop sharing one fact. Aborting here would be a
      behaviour change no finding asked for and would fail daemon launches that succeed today —
      if the code as landed by 117-40 makes non-aborting impossible to express, stop and record the
      conflict rather than changing the outcome.
    - Every existing daemon attestation test passes, with `:1909-1911`'s
      `assert_eq!(layer, "DaclPackageSidGrant")` either still correct or corrected with a recorded
      reason.
  </behavior>
  <action>
    Add `pub(crate) fn granted_ancestor_traverse(&self) -> bool { !self.traverse_applied.is_empty() }`
    beside `granted_write_access` at `:121-122`, with a doc comment naming WR-31 and stating which
    passes populate the vector it reads.

    Add an `ancestor_traverse_applied: bool` parameter to `daemon_attest_and_decide` (`:1356-1360`)
    and pass `dacl_guard.granted_ancestor_traverse()` from the caller at `:813`/`:906`. Give
    `DaclAncestorTraverse` its own arm naming `"DaclAncestorTraverse"`, with the non-aborting
    disposition described above. Update `:1319-1332`'s doc comment — it currently documents the
    shared-input behaviour as intended; that text is now wrong and is exactly the kind of stale
    claim this phase keeps finding.

    Update all seven call sites. For each test call site, decide deliberately what the new argument
    should be rather than defaulting it to match the old one — several exist to drive the
    `dacl_guard_applied == false` path, and blindly passing the same value would preserve the
    coupling this task exists to remove. Record the per-call-site decision in SUMMARY.md.

    Add `#[test] fn ancestor_traverse_and_package_sid_grant_classify_independently`: drive the
    four combinations of (write applied, traverse applied) and assert all four produce distinct,
    correct classifications. Two-of-four passing is what the current code already does; the test
    must fail if the inputs are recoupled.
  </action>
  <acceptance_criteria>
    - `cargo test -p nono-sandbox-cli --bin nono --no-fail-fast -- --test-threads=1 agent_daemon::`
      passes in full, reconciled against `117-BASELINE-ROUND4.md`.
    - Perturbation proof, recorded verbatim: pass `granted_write_access()` for both parameters,
      confirm `ancestor_traverse_and_package_sid_grant_classify_independently` fails, revert.
    - SUMMARY.md records the per-call-site argument decisions and the verdict on `:1909-1911`.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-cli --bin nono --no-fail-fast -- --test-threads=1 agent_daemon::</automated>
  </verify>
  <done>Each of the two rows is attested from the state it describes, all four input combinations classify distinctly, and the doc comment no longer documents the coupled behaviour as intended.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce input distinctness across the daemon row class</name>
  <files>crates/nono-cli/src/agent_daemon/launch.rs</files>
  <read_first>
    `:2094` — `daemon_attestation_gate_is_wired_to_real_outcomes_not_the_gate_condition` in full,
    including how it currently expresses the `wfp_filters_installed`/`network_scoping_required`
    distinctness rule. Then enumerate, from `layer_registry.rs` as landed by 117-36, EVERY registry
    row whose expectancy includes `Daemon` — not just the DACL ones. `:1331-1332` names a third
    (`DaclAncestorReadAttrs`); there may be more.
  </read_first>
  <behavior>
    - The wiring test requires that no two `Daemon`-expected rows in `daemon_attest_and_decide`
      derive their classification from the same input expression.
    - It fails, naming both rows, when two rows are wired to one input — proven by perturbation.
    - Its enumeration is derived from the registry rather than a hand-written list, so a row added
      later is covered without editing the test. If that is not achievable, the hand-written list
      carries a comment saying it must be updated when a `Daemon` row is added, and Task 2 records
      why the mechanical route was rejected.
  </behavior>
  <action>
    Extend the existing test rather than adding a parallel one — a second test asserting a similar
    rule is how contradictory rules get shipped (this phase has already done that once: two plans
    shipped opposite classification rules for one condition and every gate passed).

    Enumerate every `Daemon`-expected row and record the list in SUMMARY.md with each row's input
    expression, BEFORE extending the test. If the sweep finds a third row sharing an input —
    `DaclAncestorReadAttrs` is the candidate named in `:1331-1332` — that is a new instance of
    WR-31's class. Fix it in this task if it is in this same function; if it is not, record it as a
    finding with evidence and escalate rather than expanding scope silently.
  </action>
  <acceptance_criteria>
    - SUMMARY.md contains the row | input-expression table for every `Daemon`-expected row.
    - `cargo test -p nono-sandbox-cli --bin nono --no-fail-fast -- --test-threads=1
      daemon_attestation_gate_is_wired` passes.
    - Perturbation proof: rewire two rows to one input, confirm the test fails naming both, revert.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-cli --bin nono --no-fail-fast -- --test-threads=1 daemon_attestation_gate_is_wired</automated>
  </verify>
  <done>Input distinctness is enforced across the whole daemon row class by one extended test, with the enumeration recorded and any further instances named.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Exclusive create, panic-safe cleanup, and reparse-point check for the D-37 fixture (WR-33)</name>
  <files>crates/nono-cli/src/agent_daemon/launch.rs</files>
  <read_first>
    `:2641-2740` — the whole test including its `%PUBLIC%` rationale and precondition checks. Also
    `:2540-2570` — the other `create_dir_all(&workspace)` site, to determine whether it shares the
    exposure.
  </read_first>
  <behavior>
    - The fixture directory is created exclusively: if the path already exists — including as a
      pre-planted junction or symlink — the test fails loudly at setup instead of binding to it.
    - The directory and its ACE are removed on every exit path, including an unwind from inside
      `DaemonDaclGuard::apply`.
    - The created path is confirmed not to be a reparse point before any ACE is written to it.
    - The test still exercises the same D-37 condition (immediate ancestor not owned by the current
      user) and still fails loudly rather than skipping when the host assumption breaks.
  </behavior>
  <action>
    Replace `create_dir_all` at `:2678` with `std::fs::create_dir`, keeping the existing
    loud-failure `.expect(..)` style and message intent. Add a `Drop`-based cleanup guard bound
    immediately after creation so removal runs on unwind:

    ```rust
    struct Cleanup(PathBuf);
    impl Drop for Cleanup { fn drop(&mut self) { let _ = std::fs::remove_dir_all(&self.0); } }
    let _cleanup = Cleanup(workspace.clone());
    ```

    Between creation and the first `apply`, assert
    `!workspace.symlink_metadata()?.file_type().is_symlink()` (fail loudly), so an ACE is never
    written through a reparse point. Note that on Windows a directory junction is not always
    reported as a symlink by `is_symlink()` — check `FILE_ATTRIBUTE_REPARSE_POINT` via
    `std::os::windows::fs::MetadataExt::file_attributes()` instead, or in addition, and record
    which you used and why in SUMMARY.md. Getting this wrong would leave the stated defense
    non-existent, which is the WR-24/WR-27 pattern.

    Apply the same treatment to `:2556` if the `read_first` shows it shares the exposure; if it
    does not, record why in SUMMARY.md. Do not leave that determination implicit — WR-33 was filed
    against one site, and "the other one is probably fine" is how this phase's one-of-N fixes
    happened.

    Preserve `:2641-2660`'s doc comment explaining the `%PUBLIC%` choice, extended with the WR-33
    rationale for exclusive creation.
  </action>
  <acceptance_criteria>
    - `cargo test -p nono-sandbox-cli --bin nono --no-fail-fast -- --test-threads=1
      daemon_dacl_guard_apply_succeeds_when_immediate_ancestor_is_non_owned` passes (or fails loudly
      with its documented host-assumption message, recorded as such).
    - Manual proof recorded in SUMMARY.md: pre-create the fixture path, re-run, confirm the test
      fails at setup instead of proceeding.
    - After a full run, `%PUBLIC%` contains no leftover `nono-test-d37-daemon-dacl-*` directory —
      verified and recorded.
    - SUMMARY.md states which reparse-point detection method was used and the verdict on `:2556`.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-cli --bin nono --no-fail-fast -- --test-threads=1 daemon_dacl_guard_apply</automated>
  </verify>
  <done>The D-37 fixture cannot bind to a pre-planted path, cannot write an ACE through a reparse point, and cannot leak a directory or its ACE on any exit path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| daemon attestation claim ↔ the DACL state it reports on | A row attested from another row's input reports on state it never observed; its negative cannot occur, so the row is green by construction rather than by evidence. |
| test process ↔ `%PUBLIC%`, a world-writable shared directory | Any local user can pre-plant a junction at a predictable path; the test then writes an inheritable write-class ACE through it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-45-01 | Spoofing | `DaclAncestorTraverse`'s daemon attestation | mitigate | Attest from `traverse_applied` so the row's negative is representable and reportable (Task 1) |
| T-117-45-02 | Tampering | future daemon rows sharing an input | mitigate | Extend the existing wiring test to enforce input distinctness across the whole class (Task 2) |
| T-117-45-03 | Elevation of Privilege | the `%PUBLIC%` D-37 fixture | mitigate | Exclusive create, reparse-point check before any ACE write, `Drop`-based cleanup on unwind (Task 3) |
| T-117-45-04 | Tampering | leftover inheritable write ACE on a shared directory | mitigate | Cleanup guard verified by a post-run check that no fixture directory remains (Task 3) |
</threat_model>

<verification>
`cargo test -p nono-sandbox-cli --bin nono --no-fail-fast -- --test-threads=1 agent_daemon::`
passes in full, reconciled line-by-line against `117-BASELINE-ROUND4.md`. `--no-fail-fast` is
mandatory — a bare `cargo test --workspace` is what let CR-04 ship through two prior gates.

`agent_daemon/launch.rs` is Windows-cfg-gated, so CLAUDE.md's cross-target clippy rule MUST
applies: linux-gnu via `cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings
-D clippy::unwrap_used` and apple-darwin via `cargo-zigbuild clippy --workspace --target
x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used`. Record both verdicts in SUMMARY.md.

This plan reads `layer_registry.rs` as modified by 117-36 in wave 16. Confirm 117-36 and 117-35 are
merged before starting, and record in SUMMARY.md which `<interfaces>` citations had moved.
</verification>

<success_criteria>
WR-31 is closed and generalised: `DaclAncestorTraverse` is attested from the state it describes,
all four input combinations classify distinctly, and a single extended wiring test enforces input
distinctness across every `Daemon`-expected row — so the next row cannot repeat it. WR-06's
non-aborting disposition is preserved; only the classification is separated. WR-33 is closed: the
D-37 fixture creates exclusively, refuses a reparse point, and cleans up on unwind, with the
sibling `create_dir_all` site explicitly dispositioned rather than assumed safe.
</success_criteria>

<output>
Create `.planning/phases/117-fail-direction-contract-startup-self-attestation/117-45-SUMMARY.md` when done
</output>
