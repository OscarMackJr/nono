---
phase: 117-fail-direction-contract-startup-self-attestation
plan: 32
type: execute
wave: 13
depends_on: ["117-31"]
gap_closure: true
files_modified:
  - proj/SPEC-windows-fail-direction-contract.md
  - crates/nono-cli/tests/layer_registry_selfcheck.rs
autonomous: true
requirements: [CINT-01]

must_haves:
  truths:
    - "The SPEC's Manual verification row for BrokerAuthenticodeTrustGate cites the enforcing gate by symbol (launch.rs::is_dev_build_layout), not the stale launch.rs:2190/:2194 line numbers that land on unrelated buffer-sizing code."
    - "Every registry row's 'Enforcing call site(s)' cell in the SPEC's Layer registry table is asserted, by a new discovery-based test, to equal that row's own call_sites array in layer_registry.rs — not merely checked for LayerId name presence."
    - "Every file.rs::Symbol citation appearing anywhere in the SPEC document (not only inside the Layer registry table) is content-verified against the file it names."
  artifacts:
    - path: "crates/nono-cli/tests/layer_registry_selfcheck.rs"
      provides: "spec_call_site_cells_match_registry_call_sites, a discovery-based test comparing each SPEC row's citation cell against the registry entry's own call_sites"
  key_links:
    - from: "crates/nono-cli/tests/layer_registry_selfcheck.rs::spec_call_site_cells_match_registry_call_sites"
      to: "proj/SPEC-windows-fail-direction-contract.md"
      via: "per-LayerId citation-cell comparison against layer_registry.rs's own call_sites array"
      pattern: "spec_call_site_cells_match_registry_call_sites"
---

<objective>
Close WR-19: the SPEC's `BrokerAuthenticodeTrustGate` Manual-verification row still reads "The gate
is skipped entirely under `is_dev_build_layout()` (`launch.rs:2190`/`:2194`)" — those two lines are
`InitializeProcThreadAttributeList` buffer-sizing code, unrelated to the cited function. The real
call sites are `:1800` and `:2129`; the declaration is `:2515`. Plans 117-24/117-26 converted every
citation inside the `## Layer registry` table but the *Manual verification* section was out of
scope and was not swept — the same WR-08 citation-drift class, in the very document plan 117-26
claimed to re-sync, surviving because `spec_matches_registry` only compares `LayerId` NAMES, never
citations, so nothing in the test suite can see this document's citations drift at all.

Purpose: fix the one remaining stale citation, and close the structural hole that let it survive —
a test that verifies SPEC citations against the code, not merely LayerId name presence, covering
BOTH the Layer registry table AND every other `file.rs::Symbol` citation anywhere in the document.
Output: the corrected Manual-verification citation; a new drift gate comparing SPEC citation cells
against the registry's own `call_sites` arrays; `content_defines_symbol`'s scan widened to the whole
SPEC document.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-REVIEW.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-CONTEXT.md
</context>

<interfaces>
From `proj/SPEC-windows-fail-direction-contract.md:311-315` (the `LayerId`-scoped Manual
verification table), the stale row: `| \`BrokerAuthenticodeTrustGate\` | The gate is skipped
entirely under \`is_dev_build_layout()\` (\`launch.rs:2190\`/\`:2194\`) — active only in a signed,
production ... |`.

From `crates/nono-cli/src/exec_strategy_windows/launch.rs:1800`, `:2129` (the two real call sites of
`is_dev_build_layout`) and `:2515` (its declaration) — confirmed by direct read this planning
session.

From `crates/nono-cli/src/exec_strategy_windows/layer_registry.rs:533` (`LayerRegistryEntry` struct)
and the 13 entries starting `:860` (each shaped `LayerRegistryEntry { id: LayerId::X, ...,
call_sites: &[...], ... }`), the registry's own per-entry citations — the source of truth this
plan's new test compares the SPEC's `## Layer registry` table cells against.

From `crates/nono-cli/tests/layer_registry_selfcheck.rs` (as extended by Plan 117-31, this plan's
dependency), `content_defines_symbol(content: &str, symbol: &str) -> bool` now correctly
distinguishes renames and `Type::method` qualifiers — this plan's new whole-document scan reuses it
unchanged. `read_spec()` (existing) reads the SPEC file fresh via `CARGO_MANIFEST_DIR` +
`std::fs::read_to_string`. `resolve_citation_path(citation)` (existing) resolves a `"file.rs"` or
`"file.rs::Symbol"` string to a workspace-relative `PathBuf`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix the stale Manual-verification citation</name>
  <files>proj/SPEC-windows-fail-direction-contract.md</files>
  <read_first>
    proj/SPEC-windows-fail-direction-contract.md (lines 289-330 for the full Manual verification
    section)
    crates/nono-cli/src/exec_strategy_windows/launch.rs (lines 1795-1805, 2125-2133, and 2510-2520 to
    confirm the two real call sites and the declaration by direct read before citing them)
  </read_first>
  <action>
    In the `BrokerAuthenticodeTrustGate` Manual-verification row, replace
    `(`launch.rs:2190`/`:2194`)` with `(`launch.rs::is_dev_build_layout`)` — symbol form, matching the
    `## Layer registry` table's citation convention (`file.rs::Symbol`), immune to line drift.
  </action>
  <acceptance_criteria>
    - `grep -n "launch.rs:2190" proj/SPEC-windows-fail-direction-contract.md` returns zero matches.
    - `grep -n "launch.rs::is_dev_build_layout" proj/SPEC-windows-fail-direction-contract.md`
      non-empty.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "launch.rs:2190" proj/SPEC-windows-fail-direction-contract.md</automated>
  </verify>
  <done>The Manual-verification citation names the real symbol, not unrelated buffer-sizing code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the SPEC-vs-registry call-site drift gate and widen the symbol scan to the whole document</name>
  <files>crates/nono-cli/tests/layer_registry_selfcheck.rs</files>
  <read_first>
    crates/nono-cli/tests/layer_registry_selfcheck.rs (read in full — this task adds to the same test
    file Plan 117-31 already modified; in particular re-read extract_symbol_citations,
    resolve_citation_path, spec_matches_registry, and content_defines_symbol as extended by 117-31)
    crates/nono-cli/src/exec_strategy_windows/layer_registry.rs (lines 533-1050 for
    LayerRegistryEntry's shape and all 13 entries' id/call_sites pairs)
    proj/SPEC-windows-fail-direction-contract.md (lines 42-65 for the Layer registry table's exact
    column layout — the "Enforcing call site(s)" column this task parses)
  </read_first>
  <behavior>
    - For every `LayerId` row in the registry, the SPEC's `## Layer registry` table's "Enforcing call
      site(s)" cell, parsed as a comma-separated list of backtick-wrapped citations, equals (as a set)
      the registry entry's own `call_sites` array for that `LayerId` — a stale, added, or removed
      citation in either the SPEC or the registry fails this test, naming the specific `LayerId` and
      the mismatch.
    - Every `` `file.rs::Symbol` `` -shaped citation appearing ANYWHERE in the SPEC document's text
      (not only inside the Layer registry table) resolves, via `content_defines_symbol`, to a real
      definition in the file it names — a stale citation in the Manual verification section, the
      discrepancy ledger, or any other section now fails the build instead of being invisible to
      `spec_matches_registry`'s name-only check.
  </behavior>
  <action>
    Add a function that segments `layer_registry.rs`'s source per entry: split on occurrences of
    `"id: LayerId::"`, and within each segment locate the `call_sites: &[` ... `]` array content
    (reusing `extract_symbol_citations`'s/`extract_call_site_citations`'s existing per-literal parsing
    logic on that narrowed slice rather than the whole file), producing a `Vec<(String /* LayerId
    name */, Vec<String> /* this entry's own citations */)>`. Add a function that parses the SPEC's
    `## Layer registry` markdown table: locate the table by its header row, split each data row on
    `|`, take the first cell (backtick-wrapped `LayerId` name) and the third cell ("Enforcing call
    site(s)"), and extract every backtick-wrapped citation from that third cell (no regex — scan for
    matched backtick pairs, mirroring this file's existing no-regex house style). Add
    `#[test] fn spec_call_site_cells_match_registry_call_sites()` that builds both maps and asserts,
    per `LayerId`, the two citation sets are equal — for `DaclSessionSidGrant` and
    `MinifilterAbsence` (whose registry `call_sites` is empty per RF-03/D-33), assert the SPEC cell's
    parsed citation count is also zero (or contains only the documented `*(none ...)*` prose, not a
    real citation) rather than skipping those two rows. Separately, add
    `#[test] fn every_spec_symbol_citation_resolves_to_a_real_definition()` that scans the FULL
    `read_spec()` text (not scoped to the Layer registry table) for every
    `` `word(/word)*.rs::Word(::Word)?` ``-shaped backtick citation (reuse or generalize
    `extract_symbol_citations`'s parsing approach, applied to the SPEC text instead of the registry
    source), resolves each via `resolve_citation_path`, reads that file's content, and asserts
    `content_defines_symbol(&content, &symbol_part)` is `true` — proving this test would have caught
    WR-19's stale citation before this plan's Task 1 fixed it.
  </action>
  <acceptance_criteria>
    - `cargo test -p nono-sandbox-cli --test layer_registry_selfcheck --
      spec_call_site_cells_match_registry_call_sites` passes.
    - `cargo test -p nono-sandbox-cli --test layer_registry_selfcheck --
      every_spec_symbol_citation_resolves_to_a_real_definition` passes.
    - Perturbation proof (record in SUMMARY.md): temporarily reintroduce the stale
      `launch.rs:2190`/`:2194` citation into the SPEC's Manual verification row (undoing Task 1),
      re-run `every_spec_symbol_citation_resolves_to_a_real_definition` — if the reintroduced citation
      is line-form (not symbol-form) confirm the test's citation-shape scan either flags it as an
      unresolvable/malformed symbol citation or (if line-form citations are out of this test's scope
      by design) explicitly note that in SUMMARY.md and instead prove the gate via a synthetic
      malformed `file.rs::NoSuchSymbol` citation temporarily inserted anywhere in the SPEC text,
      confirming the test fails naming it; then revert. Separately, temporarily edit one registry
      entry's `call_sites` array (add or remove one citation) without updating the SPEC's
      corresponding table cell, confirm `spec_call_site_cells_match_registry_call_sites` fails naming
      the specific `LayerId`, then revert.
    - `cargo test -p nono-sandbox-cli --test layer_registry_selfcheck` passes in full.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-cli --test layer_registry_selfcheck</automated>
  </verify>
  <done>SPEC citations are content-verified against the registry (call-site cells) and against the tree (every symbol citation anywhere in the document), closing the structural hole that let three consecutive rounds of citation drift go uncaught.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `proj/SPEC-windows-fail-direction-contract.md`'s citations ↔ the code they describe | An operator/auditor reading the SPEC trusts a citation points at the real enforcing logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-32-01 | Repudiation | SPEC citation drift outside the Layer registry table | mitigate | New whole-document symbol-citation scan + per-row call-site-cell comparison against the registry's own source of truth (closes WR-19) |
</threat_model>

<verification>
`cargo test -p nono-sandbox-cli --test layer_registry_selfcheck` passes in full, including the two
new drift gates and their perturbation proofs recorded in SUMMARY.md.
</verification>

<success_criteria>
WR-19 is closed: the stale citation is fixed, and the structural hole that let it (and two prior
rounds of citation drift) go uncaught is closed by a citation-content drift gate covering the whole
document, not only the Layer registry table.
</success_criteria>

<output>
Create `.planning/phases/117-fail-direction-contract-startup-self-attestation/117-32-SUMMARY.md` when done
</output>
