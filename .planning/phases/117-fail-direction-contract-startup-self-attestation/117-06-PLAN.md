---
phase: 117-fail-direction-contract-startup-self-attestation
plan: 06
type: execute
wave: 3
depends_on: ["117-04"]
files_modified:
  - crates/nono-cli/src/exec_strategy_windows/restricted_token.rs
  - crates/nono-cli/src/exec_strategy_windows/labels_guard.rs
  - crates/nono-cli/src/exec_strategy_windows/dacl_guard.rs
  - crates/nono-cli/src/exec_strategy_windows/launch.rs
autonomous: true
requirements: [CINT-03]

must_haves:
  truths:
    - "D-29: restricted token, mandatory label, DACL grants, and Job Object containment each get their own force-unavailable hook driven off the same feature the WFP row already uses."
    - "D-30: every new hook is compiled out under the default feature set, never runtime-gated."
  artifacts:
    - path: crates/nono-cli/src/exec_strategy_windows/restricted_token.rs
      provides: "force-unavailable hook for CreateRestrictedToken, gated #[cfg(feature = \"layer-fault-injection\")]"
    - path: crates/nono-cli/src/exec_strategy_windows/labels_guard.rs
      provides: "force-unavailable hook for SetNamedSecurityInfoW(LABEL_SECURITY_INFORMATION)"
    - path: crates/nono-cli/src/exec_strategy_windows/dacl_guard.rs
      provides: "force-unavailable hook for the DACL grant apply calls"
    - path: crates/nono-cli/src/exec_strategy_windows/launch.rs
      provides: "force-unavailable hook for apply_process_handle_to_containment (Job Object)"
  key_links:
    - from: crates/nono-cli/src/exec_strategy_windows/{restricted_token,labels_guard,dacl_guard,launch}.rs
      to: crates/nono-cli/Cargo.toml
      via: "#[cfg(feature = \"layer-fault-injection\")]"
      pattern: "layer-fault-injection"
---

<objective>
Give each of the four remaining CLI-side confinement layers (restricted token, mandatory integrity
label, DACL grants, Job Object containment) a compiled-out force-unavailable hook, following the
exact `#[cfg(feature = "layer-fault-injection")]` / `#[cfg(not(...))]` paired-arms shape Plan 04
established for WFP. This is the seam CINT-03's per-layer tests (Plan 12) drive to exercise the
real launch path end-to-end, not a mock.

Purpose: D-29 says generalize the shipped precedent to every layer; these four are the CLI-side
ones not already covered by Plan 04 (WFP) or Plan 07 (AppContainer, which lives in the broker/
daemon).
Output: four hook additions, each a small, localized change to an existing apply function.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-CONTEXT.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-RESEARCH.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-PATTERNS.md
</context>

<interfaces>
Paired-arms shape established by Plan 04, `crates/nono-cli/Cargo.toml` `layer-fault-injection`
feature (do not re-add it — Plan 04 already landed it; this plan only consumes it).

Enforcing call sites to hook (all re-verified 2026-08-09):
- `restricted_token::create_restricted_token_with_sid` — `restricted_token.rs:55`, called
  `launch.rs:1403`
- `labels_guard::AppliedLabelsGuard::snapshot_and_apply` — `labels_guard.rs:83`, called `mod.rs:425`
- `dacl_guard::AppliedDaclGrantsGuard::snapshot_and_apply` — `dacl_guard.rs:92`, called
  `mod.rs:436-440` (session/package-SID grant); also `AppliedAncestorTraverseGuard::snapshot_and_apply`
  (`dacl_guard.rs:236`) and `AppliedAncestorReadAttributesGuard::snapshot_and_apply_targets`
  (`dacl_guard.rs:401`)
- `apply_process_handle_to_containment` — `launch.rs:273-348` (creation), called `launch.rs:2134`
  (Job Object assignment, immediately before the D-21 gate point Plan 10 will insert into)

RAII guard shape to preserve (do not disturb Drop-order), `labels_guard.rs:52-59`, `:188-192`:
```
#[derive(Debug, Default)]
pub(crate) struct AppliedLabelsGuard { entries: Vec<AppliedLabel> }
impl Drop for AppliedLabelsGuard { fn drop(&mut self) { self.revert_all(); } }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Force-unavailable hooks for restricted token and mandatory label</name>
  <files>crates/nono-cli/src/exec_strategy_windows/restricted_token.rs, crates/nono-cli/src/exec_strategy_windows/labels_guard.rs</files>
  <read_first>
    - crates/nono-cli/src/exec_strategy_windows/restricted_token.rs (full file — confirm
      `create_restricted_token_with_sid`'s exact signature and error paths before editing)
    - crates/nono-cli/src/exec_strategy_windows/labels_guard.rs (full file — confirm
      `snapshot_and_apply`'s exact signature and the `Drop` impl before editing)
    - crates/nono-cli/src/exec_strategy_windows/mod.rs lines 550-600 (Plan 04's finished
      `#[cfg(feature = "layer-fault-injection")]` toggle shape — copy this exact idiom)
  </read_first>
  <behavior>
    - Test (feature-on only): calling `create_restricted_token_with_sid` after
      `force_restricted_token_unavailable(true)` returns `Err` even though the real
      `CreateRestrictedToken` call would have succeeded.
    - Test (feature-on only): calling `AppliedLabelsGuard::snapshot_and_apply` after
      `force_mandatory_label_unavailable(true)` returns `Err` before any `SetNamedSecurityInfoW`
      call is attempted.
    - Test (default/feature-off build): neither `force_restricted_token_unavailable` nor
      `force_mandatory_label_unavailable` exists as a symbol — confirmed by the crate simply not
      compiling if test code outside `#[cfg(feature = "layer-fault-injection")]` references them.
  </behavior>
  <action>
    In `restricted_token.rs`, add `#[cfg(feature = "layer-fault-injection")] static
    RESTRICTED_TOKEN_FORCE_UNAVAILABLE: std::sync::atomic::AtomicBool =
    std::sync::atomic::AtomicBool::new(false);` and a `#[cfg(feature = "layer-fault-injection")]
    pub(crate) fn force_restricted_token_unavailable(unavailable: bool)` setter, then at the top of
    `create_restricted_token_with_sid`, add a `#[cfg(feature = "layer-fault-injection")]` block that
    checks the flag and short-circuits to `Err(NonoError::LayerAttestationFailed { layer:
    "RestrictedToken".into(), reason: "forced unavailable by test seam".into() })` before any real
    `CreateRestrictedToken` call — mirror this exact pattern in `labels_guard.rs` for
    `snapshot_and_apply` (flag name `MANDATORY_LABEL_FORCE_UNAVAILABLE`, setter
    `force_mandatory_label_unavailable`, layer name `"MandatoryIntegrityLabel"`). Neither the static,
    setter, nor the check-block compiles under the default feature set (no `#[cfg(not(...))]`
    fallback needed here — the checked block simply does not exist, unlike Plan 04's WFP toggle
    which needed a stub because it replaced always-present production logic; here the hook is purely
    additive so it can be omitted entirely rather than stubbed).
  </action>
  <acceptance_criteria>
    - `cargo build -p nono-cli` (default) succeeds and
      `grep -c "force_restricted_token_unavailable\|force_mandatory_label_unavailable"
      target/debug/nono.exe` is not a meaningful check (binary grep is unreliable) — instead verify
      via `cargo expand -p nono-cli --features "" restricted_token 2>/dev/null | grep -c
      force_restricted_token_unavailable` returning 0, OR simpler: confirm via source read that both
      functions are declared only under `#[cfg(feature = "layer-fault-injection")]`
    - `cargo build -p nono-cli --features layer-fault-injection` succeeds
    - `cargo test -p nono-cli --features layer-fault-injection --lib exec_strategy_windows::restricted_token exec_strategy_windows::labels_guard -- --test-threads=1`
      passes
    - `cargo clippy -p nono-cli --features layer-fault-injection -- -D warnings -D clippy::unwrap_used`
      exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Force-unavailable hooks for DACL grants and Job Object containment</name>
  <files>crates/nono-cli/src/exec_strategy_windows/dacl_guard.rs, crates/nono-cli/src/exec_strategy_windows/launch.rs</files>
  <read_first>
    - crates/nono-cli/src/exec_strategy_windows/dacl_guard.rs (full file — confirm
      `AppliedDaclGrantsGuard::snapshot_and_apply`, `AppliedAncestorTraverseGuard::snapshot_and_apply`,
      `AppliedAncestorReadAttributesGuard::snapshot_and_apply_targets` signatures)
    - crates/nono-cli/src/exec_strategy_windows/launch.rs lines 270-410 (`apply_process_handle_to_containment`,
      `terminate_suspended_process`, `assign_failure_message`)
    - Task 1's output in `restricted_token.rs`/`labels_guard.rs` (the exact idiom this task repeats)
  </read_first>
  <behavior>
    - Test (feature-on): each of the three DACL apply functions returns `Err` when its
      corresponding force-unavailable flag is set, before any real `SetNamedSecurityInfoW` call.
    - Test (feature-on): `apply_process_handle_to_containment` returns `Err` when
      `force_job_object_unavailable(true)` is set, before any real `AssignProcessToJobObject` call.
  </behavior>
  <action>
    Repeat Task 1's exact pattern for `dacl_guard.rs` (one shared flag
    `DACL_GRANT_FORCE_UNAVAILABLE` covering all three DACL apply functions is acceptable — document
    why: they share one `LayerId` family in the registry's `DaclSessionSidGrant`/
    `DaclPackageSidGrant`/`DaclAncestorTraverse`/`DaclAncestorReadAttrs` rows, and CINT-03's
    per-layer test for each can still assert distinct outcomes by calling the specific apply
    function directly) and `launch.rs` (`JOB_OBJECT_FORCE_UNAVAILABLE`,
    `force_job_object_unavailable`, layer name `"JobObjectContainment"`, short-circuit inside
    `apply_process_handle_to_containment` before the real `AssignProcessToJobObject` call at its
    current location). Preserve the existing `terminate_suspended_process` + `Err` propagation idiom
    unchanged — the force-unavailable hook only changes what makes the function return `Err`, not
    what happens after it does.
  </action>
  <acceptance_criteria>
    - `cargo build -p nono-cli` (default) and `cargo build -p nono-cli --features layer-fault-injection`
      both succeed
    - `cargo test -p nono-cli --features layer-fault-injection --lib exec_strategy_windows::dacl_guard exec_strategy_windows::launch -- --test-threads=1`
      passes
    - `cargo clippy -p nono-cli --features layer-fault-injection -- -D warnings -D clippy::unwrap_used`
      exits 0
    - `grep -c "force_job_object_unavailable" crates/nono-cli/src/exec_strategy_windows/launch.rs`
      is at least 2 (setter + call site)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `layer-fault-injection` feature | Same boundary as Plan 04 — these hooks must not exist in a release binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01 | Tampering | four new fault-injection hooks | mitigate | Every hook is additive-only under `#[cfg(feature = "layer-fault-injection")]` with no runtime fallback path, following Plan 04's established pattern — none compile into the default build |
| T-117-13 | Denial of Service | forced-unavailable path bypassing existing Drop-order unwind | mitigate | Hooks short-circuit to `Err` BEFORE the real OS call, so the existing RAII guard construction (and its Drop-based revert) never partially applies — no half-applied label/DACL state to clean up |
| T-117-SC | Tampering | supply chain | accept | No new crate dependencies |
</threat_model>

<verification>
- `cargo build -p nono-cli` (default) and `--features layer-fault-injection` both succeed
- `cargo clippy -p nono-cli --features layer-fault-injection -- -D warnings -D clippy::unwrap_used` exits 0
- `cargo test -p nono-cli --features layer-fault-injection --lib exec_strategy_windows -- --test-threads=1` passes
</verification>

<success_criteria>
- Four CLI-side layers now have compiled-out force-unavailable hooks callable by Plan 12's
  integration tests
</success_criteria>

<output>
Create `.planning/phases/117-fail-direction-contract-startup-self-attestation/117-06-SUMMARY.md` when done
</output>
