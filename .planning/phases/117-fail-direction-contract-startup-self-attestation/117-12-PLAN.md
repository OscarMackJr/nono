---
phase: 117-fail-direction-contract-startup-self-attestation
plan: 12
type: execute
wave: 5
depends_on: ["117-10", "117-11"]
files_modified:
  - crates/nono-cli/tests/layer_force_unavailable.rs
  - crates/nono-cli/tests/layer_registry_meta_test.rs
  - proj/SPEC-windows-fail-direction-contract.md
autonomous: true
requirements: [CINT-03]

must_haves:
  truths:
    - "D-32: a meta-test discovers registry rows (from LayerId::ALL, not a hand-written list) and asserts each has a forced-unavailable test — adding a layer without a test fails the build."
    - "D-31: a row untestable on an ordinary host is a loud, named, non-silent gap — never a bare #[ignore] or a quiet skip."
    - "D-23: coverage is verified, not merely claimed — this plan proves all four confined-child paths were actually exercised, closing the loop Plans 06/07/10/11 opened."
    - "D-24: a real measured latency number is recorded per gated spawn path, not \"negligible\"."
    - "D-35: both local cross-target clippy gates (linux-gnu via cross, apple-darwin via cargo-zigbuild) are run and green before the phase closes — MUST, no PARTIAL→CI."
  artifacts:
    - path: crates/nono-cli/tests/layer_force_unavailable.rs
      provides: "one forced-unavailable integration test per non-host-gated LayerId row, exercising the real launch path via the layer-fault-injection feature"
    - path: crates/nono-cli/tests/layer_registry_meta_test.rs
      provides: "every_registry_row_has_a_test (discovery-based) and host_gated_rows_are_loud"
    - path: proj/SPEC-windows-fail-direction-contract.md
      provides: "D-24 latency numbers filled in, all SC4 discrepancy rows closed or given a final disposition"
  key_links:
    - from: crates/nono-cli/tests/layer_registry_meta_test.rs
      to: crates/nono-cli/src/exec_strategy_windows/layer_registry.rs
      via: "CARGO_MANIFEST_DIR source-scan reading LayerId::ALL and cross-referencing test-file text"
      pattern: "CARGO_MANIFEST_DIR"
---

<objective>
Close CINT-03 and finish SC1-SC4: land a forced-unavailable integration test for every registry row
that can run on an ordinary (non-elevated, non-domain-joined) host, the D-32 discovery meta-test
that fails the build if any `LayerId` is added without one, and the D-31 loud-gap list for the rows
that genuinely cannot run here (WFP live-service, minifilter absence, and whichever daemon-path
cases Wave-0's Plan 01 investigation left open). Then run both cross-target clippy gates (D-35,
MUST), measure real attestation latency per gated spawn path (D-24), and fold both into the final
SPEC.

Purpose: this is the phase's closing gate. CINT-03's own text is "a contract entry with no such
test is not satisfied" — this plan is what makes that literally true, mechanically enforced, rather
than a claim in a document.
Output: two new test files; the SPEC's `TBD` placeholders replaced with measured numbers and closed
discrepancy dispositions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-CONTEXT.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-RESEARCH.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-VALIDATION.md
@.planning/templates/cross-target-verify-checklist.md
</context>

<interfaces>
House source-scan pattern (`resl_supervisor_drain.rs`, already used by Plan 03's
`layer_registry_selfcheck.rs` — reuse that file's helper functions where possible rather than
duplicating the `CARGO_MANIFEST_DIR` read boilerplate a third time):
`env!("CARGO_MANIFEST_DIR")` + `std::fs::read_to_string`, named `.unwrap_or_else(|e| panic!(...))`
messages, no `include_str!`, no new `regex` dependency.

Manual-only verifications already enumerated in 117-VALIDATION.md (D-31 candidates — this plan's
Task 2 must transcribe these into the loud-gap list, not silently drop them): WFP forced-unavailable
end-to-end under a live `nono-wfp-service` (needs admin-installed service); minifilter *absence* row
(structurally untestable, cite ADR-65, Phase 116 D-08 form); daemon-path forced-unavailable
(resolve using this plan's own Task 1 findings — if Plan 06/07/10's daemon-side hooks and gate
DO run cleanly in an ordinary `cargo test`, move this off the manual list into the automated
map; only keep it manual if it genuinely needs a live/elevated daemon process); broker-arm
confinement attestation (run from PowerShell, not git-bash — GLE=87 under MSYS, per project memory
`feedback_windows_supervised_needs_real_console.md`).

`.planning/templates/cross-target-verify-checklist.md` — the two MUST gates:
`cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used`
and `cargo-zigbuild clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used`.
Per RESEARCH §I (re-verified independently by this plan's own greps during Plan 01/04): files this
phase touched with literal linux/macos cfg (triggering the MUST) are `crates/nono/src/error.rs`,
`crates/nono-cli/src/output.rs`, `crates/nono-cli/src/cli.rs`, `crates/nono-cli/src/command_runtime.rs`
— all four were modified by Plans 02/04/09. Files touched WITHOUT literal linux/macos cfg
(`exec_strategy_windows/*.rs`, `agent_daemon/launch.rs`, `nono-shell-broker/src/main.rs`,
`crates/nono/src/machine_policy.rs`, `crates/nono/src/attestation.rs`,
`crates/nono-cli/src/telemetry/*.rs`) do not trigger the literal CLAUDE.md rule by content — run
both gates on the whole workspace regardless (the gate command is `--workspace`-scoped, not
per-file), so this distinction only matters for justifying that the gate is required at all, not for
scoping which files to check.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Forced-unavailable integration tests, one per automatable registry row</name>
  <files>crates/nono-cli/tests/layer_force_unavailable.rs</files>
  <read_first>
    - crates/nono-cli/src/exec_strategy_windows/layer_registry.rs (final state — all 13 rows)
    - crates/nono-cli/tests/*.rs (survey 2-3 existing integration tests that spawn
      `env!("CARGO_BIN_EXE_nono")` subprocesses, e.g. `broker_authenticode.rs`, to confirm the house
      convention for spawning the compiled binary under test with specific env vars/flags)
    - Plans 06/07's finished force-unavailable hooks (exact setter function names per layer)
    - Plans 10/11's finished gate call sites (exact abort/degrade observable behavior: process exit
      code, stderr content, `NonoDiagnosticCode`)
  </read_first>
  <behavior>
    For every `LayerId` row NOT on the D-31 manual list (Task 2 finalizes that list; use
    117-VALIDATION.md's current draft as the starting point): a test named
    `force_unavailable_<snake_case_layer_name>` that (a) builds/spawns the `nono` (or
    `nono-shell-broker`, for `AppContainerProfile`) binary with `--features layer-fault-injection`
    and that layer's specific force-unavailable env var/flag set, (b) attempts a minimal confined
    launch, (c) asserts the OUTCOME matches that row's `ContractOutcome` exactly: `Abort` → non-zero
    exit + the row's `NonoDiagnosticCode::LayerAttestationFailed` surfaced in stderr;
    `DegradeWithVisibleClaim`/`FailOpen` → zero exit + the coarse banner text present in stderr;
    `FailOpenDefect` → same as degrade but the test also asserts the SPEC's discrepancy section
    names a successor (read the SPEC file inline, matching Plan 03's `spec_matches_registry` house
    pattern).
  </behavior>
  <action>
    Create `crates/nono-cli/tests/layer_force_unavailable.rs` with one `#[test]` function per
    automatable `LayerId`, function names exactly `force_unavailable_<snake_case_name>` (e.g.
    `force_unavailable_restricted_token`, `force_unavailable_mandatory_integrity_label`,
    `force_unavailable_job_object_containment`) — this exact naming convention is load-bearing:
    Task 2's meta-test discovers these by substring-matching `LayerId` names against test function
    names, so a mismatch here silently breaks D-32's coverage guarantee. Gate the whole file (or
    each test individually) behind `#[cfg(all(target_os = "windows", feature =
    "layer-fault-injection"))]` so it compiles to nothing in a default build (consistent with D-30 —
    the tests themselves reference the fault-injection seam and must not exist in release-shaped
    builds either). Run with `--test-threads=1` per the project's standing env-var/global-state
    parallelism rule (CLAUDE.md, `117-VALIDATION.md`).
  </action>
  <acceptance_criteria>
    - `cargo test -p nono-cli --features layer-fault-injection --test layer_force_unavailable -- --test-threads=1`
      exits 0
    - `grep -c "^fn force_unavailable_" crates/nono-cli/tests/layer_force_unavailable.rs` equals the
      count of automatable (non-D-31-manual) `LayerId` rows
    - Every test function asserts on the specific `ContractOutcome` its row declares, not a generic
      "something happened" check (verify by reading each test body)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-32 discovery meta-test + D-31 loud-gap list</name>
  <files>crates/nono-cli/tests/layer_registry_meta_test.rs</files>
  <read_first>
    - crates/nono-cli/src/exec_strategy_windows/layer_registry.rs (`LayerId::ALL`)
    - Task 1's finished `layer_force_unavailable.rs` (the test function names it discovers against)
    - .planning/phases/117-fail-direction-contract-startup-self-attestation/117-VALIDATION.md
      "Manual-Only Verifications" table (the starting D-31 candidate list)
  </read_first>
  <behavior>
    - Test `every_registry_row_has_a_test`: for every name in `LayerId::ALL` NOT present in a
      finalized `MANUALLY_VERIFIED: &[&str]` allow-list (defined in this same test file, one entry
      per D-31 row, each with an adjacent doc comment naming WHY it's manual and where the manual
      steps are documented), assert a `force_unavailable_<snake_case_name>` function exists in
      `layer_force_unavailable.rs`'s source text (read via `CARGO_MANIFEST_DIR`, substring search —
      discovery-based, per V-01's "prefer tests that discover their targets" lesson: this test reads
      `LayerId::ALL` from source, it does NOT hardcode the 13 names itself).
    - Test `host_gated_rows_are_loud`: for every entry in `MANUALLY_VERIFIED`, assert its adjacent
      doc-comment reason string is non-empty and the row also appears in
      `proj/SPEC-windows-fail-direction-contract.md`'s text under a heading containing "manual" or
      "host-gated" (case-insensitive substring check) — this is what makes a host-gated row LOUD
      (printed/documented) instead of a silent `#[ignore]`.
  </behavior>
  <action>
    Create `crates/nono-cli/tests/layer_registry_meta_test.rs` following the
    `resl_supervisor_drain.rs`/Plan 03 pattern exactly (no `include_str!`, named panic messages).
    Finalize the `MANUALLY_VERIFIED` list by re-checking each 117-VALIDATION.md candidate against
    what Plans 06/07/10/11 actually shipped: if the daemon-path force-unavailable test from Task 1
    runs successfully in `cargo test` without a live/elevated daemon process, REMOVE it from
    `MANUALLY_VERIFIED` (it is not actually host-gated — update 117-VALIDATION.md's frontmatter
    status accordingly in this same task, since that file's manual list must not overstate what is
    still unautomatable). Keep `WfpEgressFilters` (needs a live `nono-wfp-service`) and
    `MinifilterAbsence` (structurally untestable, ADR-65) on the list at minimum, per the phase's own
    `<hard_constraints>`.
  </action>
  <acceptance_criteria>
    - `cargo test -p nono-cli --features layer-fault-injection --test layer_registry_meta_test -- --test-threads=1`
      exits 0
    - Manually verify the discovery property: temporarily add a 14th `LayerId` variant to
      `layer_registry.rs` (do not add a matching test), run `every_registry_row_has_a_test`, confirm
      it FAILS with a message naming the missing test, then revert the temporary variant
    - `grep -c "MANUALLY_VERIFIED" crates/nono-cli/tests/layer_registry_meta_test.rs` is at least 1,
      and its list contains at least `WfpEgressFilters` and `MinifilterAbsence`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Cross-target clippy gates, D-24 latency measurement, SPEC finalization</name>
  <files>proj/SPEC-windows-fail-direction-contract.md</files>
  <read_first>
    - .planning/templates/cross-target-verify-checklist.md (exact commands, pinned image/tool
      versions)
    - proj/SPEC-windows-fail-direction-contract.md (Plan 03's output — the `TBD` latency placeholders
      and the discrepancy section)
    - Task 1/2's finished test files (for measuring real attestation-gate overhead)
  </read_first>
  <action>
    Run both local cross-target clippy gates against the full workspace (not a per-file subset —
    the gate command is workspace-scoped): `cross clippy --workspace --target
    x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used` and `cargo-zigbuild clippy
    --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used` (per
    `.planning/templates/cross-target-verify-checklist.md`'s exact invocation and prerequisites —
    Docker+cross for the first, zig+cargo-zigbuild with `SDKROOT` unset for the second). Record the
    pass/fail result and the date in this task's SUMMARY. Measure D-24's real attestation cost: time
    `attest_and_decide()`'s wall-clock cost for a representative launch (wrap the call in
    `std::time::Instant`-based timing inside one of Task 1's integration tests, or add a small
    dedicated timing test) for at minimum the direct `nono run` path; record the measured
    millisecond figure (not "negligible") into `proj/SPEC-windows-fail-direction-contract.md`'s
    "Latency budget" section, replacing every `TBD` placeholder — if a path's real cost cannot be
    measured on this host (e.g. the broker/daemon paths require a live spawn this plan's automated
    tests already exercise in Task 1, so reuse those timings), state the measurement method inline.
    Close out the SPEC's "Contract vs. code discrepancies" section: mark SC4-1 through SC4-5 with
    their final disposition (fixed-here with the plan number that fixed it, or deferred-with-named-
    successor), confirming every row Plan 03 opened now has a closing state, not merely a stated
    intent.
  </action>
  <acceptance_criteria>
    - Both cross-target clippy commands were actually run (not skipped) and both exit 0 — if either
      is genuinely blocked by a documented runner failure (stopped daemon, absent tool — NOT
      merely "not run"), the plan's SUMMARY must name the specific failure per
      `.planning/templates/cross-target-verify-checklist.md`'s PARTIAL→CI fallback criteria, since a
      silent skip is prohibited
    - `grep -c "TBD" proj/SPEC-windows-fail-direction-contract.md` returns 0 (no placeholder numbers
      remain)
    - `grep -c "^| SC4-" proj/SPEC-windows-fail-direction-contract.md` (or equivalent row marker)
      shows all 5 discrepancy rows with a non-empty disposition column
    - `make ci` (clippy + fmt + full workspace tests) exits 0 on the Windows host as the final phase
      gate
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite ↔ shipped behavior | The whole point of this plan: a test asserting a contracted outcome IS the evidence the outcome holds, closing the gap between "the SPEC says X" and "the code does X" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-19 | Repudiation | green-by-absence (Phase 115 V-01 failure class) | mitigate | `every_registry_row_has_a_test` is discovery-based (reads `LayerId::ALL` from source, not a hand-written list) — the exact property that prevented V-01's blind-by-construction failure |
| T-117-20 | Denial of Service | host-gated rows silently skipped | mitigate | `host_gated_rows_are_loud` fails the build if a `MANUALLY_VERIFIED` entry lacks a reason string or SPEC citation — D-31's "never a silent #[ignore]" is mechanically enforced, not merely policy |
| T-117-01 | Tampering | test file itself compiled into release builds | mitigate | `layer_force_unavailable.rs` is gated `#[cfg(feature = "layer-fault-injection")]` — it does not exist in a default `cargo test` run, consistent with D-30 |
| T-117-SC | Tampering | supply chain | accept | No new crate dependencies added; `cross`/`cargo-zigbuild` are existing local toolchain, not new project dependencies |
</threat_model>

<verification>
- `cargo test -p nono-cli --features layer-fault-injection -- --test-threads=1` (full crate,
  featured) exits 0
- `cargo test -p nono --lib` exits 0 (library-side additions)
- Both cross-target clippy gates run and pass
- `make ci` exits 0
</verification>

<success_criteria>
- SC2, SC3, SC4 all closed: every automatable row has a passing forced-unavailable test; the
  meta-test discovers coverage rather than asserting it; every discrepancy has a final, cited
  disposition; both cross-target gates are green; D-24's budget is a real number
</success_criteria>

<output>
Create `.planning/phases/117-fail-direction-contract-startup-self-attestation/117-12-SUMMARY.md` when done
</output>
