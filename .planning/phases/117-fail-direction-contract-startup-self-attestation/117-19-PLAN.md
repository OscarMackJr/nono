---
phase: 117-fail-direction-contract-startup-self-attestation
plan: 19
type: execute
wave: 8
depends_on: ["117-13", "117-14", "117-15", "117-16", "117-17", "117-18"]
files_modified:
  - proj/SPEC-windows-fail-direction-contract.md
autonomous: true
requirements: [CINT-01]

must_haves:
  truths:
    - "The SPEC's Review-fix pass table has a row for every iteration-2 finding (NR-04, NR-05, NR-06) and every iteration-3 finding this gap-closure phase fixed (NR3-01, NR3-02, NR3-03, NR3-04, NR3-05, NR3-08) — none reads 'quietly reconciled'"
    - "RF-14's row describes the shipped three-defense staging guard (traversal-component rejection, component test, canonicalize-both-sides), not the pre-NR-06 single-defense guard"
    - "The Layer registry table's call-site citations for MandatoryIntegrityLabel/DaclPackageSidGrant/DaclAncestorTraverse/DaclAncestorReadAttrs match the symbol-form citations Plan 117-15 landed in the registry source, not stale line numbers"
    - "The manual-verification section lists only the 3 rows Plan 117-18 left in MANUALLY_VERIFIED, each explicitly citing this session's operator-locked automate-what's-automatable decision"
  artifacts:
    - path: "proj/SPEC-windows-fail-direction-contract.md"
      provides: "Complete, current review-fix ledger; corrected RF-14; symbol-form registry table citations; reduced, accurate manual-verification section"
  key_links:
    - from: "proj/SPEC-windows-fail-direction-contract.md ## Review-fix pass table"
      to: "crates/nono-cli/src/exec_strategy_windows/{labels_guard,dacl_guard,layer_registry}.rs, crates/nono-cli/src/{exec_strategy_windows/launch,agent_daemon/launch}.rs"
      via: "re-runnable grep/test evidence per finding, D-16 shape"
      pattern: "NR3-0"
---

<objective>
Close the SC4 gap this gap-closure round's Priority 4 named directly: the SPEC's own
"Contract vs. code discrepancies"/"Review-fix pass" ledger — the standing artifact that exists
SPECIFICALLY to prevent "quietly reconciled" drift — was itself not updated for 3 of the 5
iteration-2 fixes (NR-04, NR-05, NR-06 have no row), and its existing RF-14 row describes behavior
the shipped code no longer has (the pre-NR-06 single-defense guard, while the code has three ordered
defenses). This is the exact failure mode SC4 exists to prevent, reproduced one review cycle later,
inside the very artifact meant to catch it — VERIFICATION.md is explicit that this must not happen
a THIRD time.

This plan, run last (wave 8, after every code fix in this gap-closure round has landed), makes the
SPEC an accurate description of the FINAL shipped state: it ports the already-written-up NR-04/
NR-05/NR-06 findings (`117-REVIEW-FIX.md`) into the standing ledger, corrects RF-14, adds rows for
this phase's own NR3-01 through NR3-05 and NR3-08 fixes (Plans 117-13 through 117-17), updates the
registry table's call-site citations to match Plan 117-15's symbol-form conversion, and reduces the
manual-verification section to the 3 rows Plan 117-18 left genuinely un-automated — each explicitly
citing this session's operator-locked "automate what's automatable, downgrade the rest per-row"
decision rather than reading as an unexplained shrink.

Purpose: CINT-01 requires the contract to be "a description of behaviour rather than an assertion
about it" — a stale ledger and stale citations are exactly the gap between the two this plan closes.
Output: the SPEC document, brought current with every code change this gap-closure round made.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-REVIEW.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-REVIEW-FIX.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-13-SUMMARY.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-14-SUMMARY.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-15-SUMMARY.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-16-SUMMARY.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-17-SUMMARY.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-18-SUMMARY.md
</context>

<interfaces>
`proj/SPEC-windows-fail-direction-contract.md`'s current structure (unchanged section headings this
plan edits within, does not restructure):
```
## Layer registry                              (lines ~42-64 — a 13-row markdown table transcribed from the registry)
## Contract vs. code discrepancies              (lines ~118-134 — SC4-1..SC4-5, unrelated to this plan, DO NOT edit)
## Review-fix pass (code review 2026-08-10)     (lines ~240-266 — RF-01..RF-16 table; THIS is the table this plan extends)
## Manual verification (D-31)                   (lines ~267-314 — ### LayerId-scoped rows table + ### Cross-cutting security assumptions)
```
The existing `## Review-fix pass` table's row shape (columns: `#`, `What was wrong`, `What
changed`) — RF-15/RF-16 are the two existing iteration-2 rows already present (CR-14, NR-02); this
plan adds NR-04/NR-05/NR-06 alongside them plus the NR3-* rows, continuing the SAME table with new
`#` identifiers (`NR-04`, `NR-05`, `NR-06`, `NR3-01`, `NR3-02`, `NR3-03`, `NR3-04`, `NR3-05`,
`NR3-08`) rather than renumbering RF-01..RF-16.

Source material for the "What was wrong" / "What changed" text — read the cited sections directly,
do not paraphrase from memory:
- `117-REVIEW-FIX.md`'s `## NR-04`, `## NR-05`, `## NR-06` sections (the "What changed" tables and
  "Honest limits" subsections are the source for the NR-04/NR-05/NR-06 rows' "What changed" text —
  each MUST include its "Honest limit" caveat, matching how RF-15's honest-limits language already
  appears in the SPEC's own "Downgrade banner behavior" section).
- `117-REVIEW.md`'s `## Critical Issues` (NR3-01) and `## Warnings` (NR3-02 through NR3-10 — this
  plan uses only NR3-02, NR3-03, NR3-04, NR3-05, NR3-08) sections for the "What was wrong" text.
- The 117-13 through 117-17 SUMMARY.md files (once they exist, produced by the wave-6/7 plans this
  plan depends on) for the "What changed" text describing what THIS gap-closure round actually did,
  not what the review merely recommended.

The registry table's four drifted rows' CURRENT (stale) citations in the SPEC
(lines ~52, ~56-58 — `MandatoryIntegrityLabel`, `DaclPackageSidGrant`, `DaclAncestorTraverse`,
`DaclAncestorReadAttrs`) must be replaced with the exact symbol-form strings Plan 117-15 landed in
`layer_registry.rs`'s `REGISTRY_ENTRIES` — read that file fresh (do not reuse the stale strings
already in this plan's own text above, which predate 117-15) to get the authoritative current
values.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Port NR-04/NR-05/NR-06 (iteration 2) into the Review-fix pass table; fix RF-14</name>
  <files>proj/SPEC-windows-fail-direction-contract.md</files>
  <read_first>
    proj/SPEC-windows-fail-direction-contract.md (the `## Review-fix pass` table in full, lines
    ~240-266, including the existing RF-14 row's stale text)
    117-REVIEW-FIX.md (`## NR-04`, `## NR-05`, `## NR-06` sections in full — "What changed" tables
    and "Honest limits"/"Symlink / TOCTOU analysis" subsections)
  </read_first>
  <action>
    Add three new rows to the `## Review-fix pass` table, identified `NR-04`, `NR-05`, `NR-06`,
    positioned after the existing `RF-16` row (chronological/table-append order, matching how
    RF-15/RF-16 were themselves appended after RF-01..RF-13 rather than renumbering). Each row's
    "What was wrong" / "What changed" columns are sourced verbatim-in-substance from
    `117-REVIEW-FIX.md`'s corresponding section, and each MUST include the section's own stated
    "Honest limit" (NR-04: the negative still cannot occur on the shipped path because
    `assert_wfp_activation_installed_filters` already gates earlier and harder; NR-05: both values
    remain dynamically `true` on every reachable path today, the fix makes the negative
    REPRESENTABLE and its erasure LOUD; NR-06: TOCTOU between canonicalize and delete is NOT closed,
    named in the function's own doc comment, exposure bounded to an attacker who already holds this
    user's own `%TEMP%` write access).

    Correct the existing `RF-14` row's "What changed" column, which currently describes the guard
    as refusing "any path that is not a strict subdirectory of `%TEMP%/nono-net-block`, compared by
    path components" (the PRE-NR-06 single-defense description). Replace with the shipped
    three-defense description: (1) reject any `Component::ParentDir`/`Component::CurDir` outright,
    (2) keep the literal `Path::starts_with` component test, (3) canonicalize both sides and
    re-apply the subdirectory test, fail-secure on canonicalization error — cross-reference the new
    `NR-06` row rather than duplicating its full text.
  </action>
  <verify>
    <automated>grep -c "^| NR-04 \| NR-05 \| NR-06 " proj/SPEC-windows-fail-direction-contract.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "| NR-04 |" proj/SPEC-windows-fail-direction-contract.md` finds the new row; same for `NR-05`, `NR-06`
    - `grep -n "refuses any path that is not a strict subdirectory" proj/SPEC-windows-fail-direction-contract.md` returns ZERO matches (the stale RF-14 text is gone)
    - `grep -n "Component::ParentDir\|three ordered defen" proj/SPEC-windows-fail-direction-contract.md` finds the corrected RF-14 text and/or the new NR-06 row
    - Each of the 3 new rows contains the literal substring "Honest limit" (or equivalent explicit caveat text) — verified by reading the added rows directly
  </acceptance_criteria>
  <done>The Review-fix pass table has rows for all 5 iteration-2 findings (RF-15, RF-16 pre-existing; NR-04, NR-05, NR-06 added), and RF-14 describes the actually-shipped guard.</done>
</task>

<task type="auto">
  <name>Task 2: Add NR3-01..NR3-05/NR3-08 rows (this phase's own fixes); update registry table citations</name>
  <files>proj/SPEC-windows-fail-direction-contract.md</files>
  <read_first>
    117-REVIEW.md's `## Critical Issues` (NR3-01) and `## Warnings` (NR3-02, NR3-03, NR3-04, NR3-05,
    NR3-08) sections in full
    117-13-SUMMARY.md, 117-14-SUMMARY.md, 117-15-SUMMARY.md, 117-16-SUMMARY.md, 117-17-SUMMARY.md
    (what each plan actually did — the "What changed" source of truth, since a plan may have
    deviated from the review's literal suggested fix)
    crates/nono-cli/src/exec_strategy_windows/layer_registry.rs (the CURRENT, post-117-15
    `REGISTRY_ENTRIES` call_sites for `MandatoryIntegrityLabel`, `DaclPackageSidGrant`,
    `DaclAncestorTraverse`, `DaclAncestorReadAttrs` — read fresh, do not reuse any stale string from
    this plan's own `<interfaces>` block)
  </read_first>
  <action>
    Add six new rows to the `## Review-fix pass` table, identified `NR3-01`, `NR3-02`, `NR3-03`,
    `NR3-04`, `NR3-05`, `NR3-08`, each labelled `(Iteration 3, gap-closure Plan 117-1{3,4,5,6,7})` so
    a reader can trace which plan landed the fix. Source "What was wrong" from the review's exact
    finding text; source "What changed" from the corresponding plan's SUMMARY (what actually
    shipped, including any deviation from the review's suggested fix — e.g. if Plan 117-17 chose
    deletion over wiring a real downgrade path for `DaemonAttestationDecision`, say so explicitly
    and why, matching this document's own established honesty standard).

    Update the `## Layer registry` table's citations for the four affected rows
    (`MandatoryIntegrityLabel`, `DaclPackageSidGrant`, `DaclAncestorTraverse`,
    `DaclAncestorReadAttrs`) to the exact symbol-form strings now in `layer_registry.rs`'s source
    (read fresh per `<read_first>`), replacing the stale `"file:line"` citations. Leave every other
    row's citations in this table untouched.
  </action>
  <verify>
    <automated>cargo test -p nono-sandbox-cli --test layer_registry_selfcheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "| NR3-01 |\|| NR3-02 |\|| NR3-03 |\|| NR3-04 |\|| NR3-05 |\|| NR3-08 |" proj/SPEC-windows-fail-direction-contract.md` finds all 6 new rows
    - The `## Layer registry` table's four affected rows' citation cells contain the SAME symbol-form strings as `layer_registry.rs`'s current source (spot-checked by reading both side by side — e.g. `AppliedLabelsGuard::snapshot_and_apply`, `prepare_live_windows_launch`, `AppliedDaclGrantsGuard::snapshot_and_apply`, `AppliedAncestorTraverseGuard::snapshot_and_apply`, `AppliedAncestorReadAttributesGuard::snapshot_and_apply_targets` all appear in the SPEC table)
    - `cargo test -p nono-sandbox-cli --test layer_registry_selfcheck` passes — `spec_matches_registry` (every `LayerId` still named somewhere in the SPEC) and `registry_call_sites_exist`'s symbol-content check (from Plan 117-15) both still pass against this edited document, proving the new citations are not merely well-formatted but actually correct
  </acceptance_criteria>
  <done>The SPEC records this gap-closure round's own fixes with the same evidence discipline as every prior review-fix pass, and the registry table's citations match the current source exactly, re-verified by the selfcheck tests.</done>
</task>

<task type="auto">
  <name>Task 3: Reduce and re-frame the manual-verification section per the operator's locked decision</name>
  <files>proj/SPEC-windows-fail-direction-contract.md</files>
  <read_first>
    proj/SPEC-windows-fail-direction-contract.md's `## Manual verification (D-31)` section in full
    (lines ~267-314, both the `### LayerId-scoped rows` table and `### Cross-cutting security
    assumptions` — the second table is untouched by this task)
    117-18-SUMMARY.md (the final 3-row `MANUALLY_VERIFIED` list and the 8 rows moved to
    `ALSO_AUTOMATED`, to confirm this task's row removal matches exactly what Plan 117-18 shipped)
  </read_first>
  <action>
    Remove the 8 rows from the `### LayerId-scoped rows` table that Plan 117-18 moved into
    `ALSO_AUTOMATED` (`RestrictedToken`, `JobObjectContainment`, `DaclAncestorTraverse`,
    `DaclAncestorReadAttrs`, `WfpEgressFilters`, `FirewallRulesEgress`, `AppContainerProfile`,
    `InterpreterCoverageGate`). Keep the remaining 3 rows (`DaclSessionSidGrant`,
    `MinifilterAbsence`, `BrokerAuthenticodeTrustGate`) with their existing "Why manual" reasoning
    (already accurate, unchanged), but add one sentence to each explicitly framing it as an
    OPERATOR-ACCEPTED DOWNGRADE per this gap-closure session's locked decision (cite it by its
    substance: "automate what is automatable; for the genuinely host-gated/structural remainder,
    record an explicit per-row accepted downgrade naming why the row cannot be automated" —
    2026-08-10 gap-closure session), so a reader understands these 3 rows are not an oversight but a
    deliberate, reviewed, named boundary.

    Add one introductory sentence immediately above the `### LayerId-scoped rows` heading (or fold
    into the existing D-31 paragraph above it) stating the CURRENT count — "10 of 13 rows are
    automated (2 via `layer_force_unavailable.rs`'s external-subprocess convention, 8 via direct
    in-process/gate-level tests discovered by `layer_registry_meta_test.rs`'s `ALSO_AUTOMATED`
    mechanism); the 3 rows below are the operator-accepted, genuinely host-gated or structural
    remainder" — so the section's own framing matches D-32's discovery-based enforcement rather than
    reading as a leftover list with no stated total.

    Do NOT touch `### Cross-cutting security assumptions` or its `MANUAL_SECURITY_ASSUMPTIONS`
    cross-reference — unrelated to this gap-closure round.
  </action>
  <verify>
    <automated>cargo test -p nono-sandbox-cli --test layer_registry_meta_test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^| \`" proj/SPEC-windows-fail-direction-contract.md` (or an equivalent scoped read of the `### LayerId-scoped rows` table specifically) shows exactly 3 data rows remaining in that table
    - `grep -n "operator-accepted\|OPERATOR-ACCEPTED" proj/SPEC-windows-fail-direction-contract.md` finds the new framing language on all 3 remaining rows
    - `cargo test -p nono-sandbox-cli --test layer_registry_meta_test` passes — `host_gated_rows_are_loud` (every `MANUALLY_VERIFIED` entry from the Rust source must still appear in this SPEC section) and `manual_verification_section_excludes_the_registry_table` both still pass against the edited document, proving the 3 remaining rows are still correctly cross-referenced and the section boundary is still correctly scoped
    - `git add -f proj/SPEC-windows-fail-direction-contract.md` succeeds before commit (per the project's standing `proj/` gitignore-but-tracked convention — a plain `git add` exits 1 here and would silently drop this file from the commit)
  </acceptance_criteria>
  <done>The manual-verification section accurately lists only the 3 rows Plan 117-18 left un-automated, each explicitly framed as this session's operator-accepted decision rather than an unexplained residual list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| SPEC document → Phase 118/119 downstream consumers | `117-CONTEXT.md`'s D-04/D-12 name this document as the shared input Phase 118's receipts and Phase 119's boundary statement build on; a stale or incomplete ledger here propagates false confidence downstream, not merely a local documentation defect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-117-19-01 | Repudiation (the SC4 failure mode itself, recurring a third time) | `proj/SPEC-windows-fail-direction-contract.md` | mitigate | Every finding this gap-closure round fixed gets a ledger row with re-runnable evidence, closing the exact "fix shipped, ledger not updated" gap VERIFICATION.md found reproduced once already |
| T-117-19-02 | Tampering (this task itself introducing a NEW stale claim) | Task 2's registry-table citation update | mitigate | Task 2's acceptance criteria require re-running `layer_registry_selfcheck.rs`'s tests AGAINST the edited SPEC, so the new citations are proven correct by the same mechanical gate Plan 117-15 built, not merely asserted by the executor |
</threat_model>

<verification>
- All three tasks' automated verify commands pass.
- `cargo test -p nono-sandbox-cli --test layer_registry_selfcheck --test layer_registry_meta_test` green as a combined final check.
- `git add -f proj/SPEC-windows-fail-direction-contract.md` used for the commit (not plain `git add`).
- Cross-target clippy: N/A — this plan touches only a markdown file, no Rust source.
</verification>

<success_criteria>
- SC4 is satisfied for this gap-closure round: every discrepancy this phase found and fixed (iteration-2's NR-04/NR-05/NR-06, this round's NR3-01..NR3-05/NR3-08) is recorded in the standing ledger with re-runnable evidence.
- The registry table's citations match the current source exactly, machine-verified.
- The manual-verification section accurately reflects the final 3-row accepted-downgrade set, explicitly citing the operator's locked decision.
</success_criteria>

<output>
Create `.planning/phases/117-fail-direction-contract-startup-self-attestation/117-19-SUMMARY.md` when done
</output>
