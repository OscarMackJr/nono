---
phase: 117-fail-direction-contract-startup-self-attestation
plan: 24
type: execute
wave: 9
depends_on: []
files_modified:
  - crates/nono-cli/src/exec_strategy_windows/layer_registry.rs
  - crates/nono-cli/tests/layer_registry_selfcheck.rs
  - crates/nono-cli/tests/layer_registry_meta_test.rs
autonomous: true
requirements: [CINT-01, CINT-03]

must_haves:
  truths:
    - "All 13 registry rows' call_sites citations are symbol-form ('file.rs::Symbol'), verified content-accurate by direct code read during this plan's execution — not merely file-existence-checked (closes SC1 / CINT-01, the 9-of-13 gap)."
    - "registry_call_sites_exist's symbol-form content check requires the symbol's last segment to appear at a real definition line (fn/impl with a qualifier-only prefix), not merely anywhere in the file's text — a citation satisfied only by a doc comment or another string literal now fails (closes WR-08's secondary finding)."
    - "contains_fn_exact excludes a trailing '!' as a boundary character per its own documented rule, and requires the match to sit at a real definition-line prefix (not inside a comment or a macro/string literal) — closes WR-10."
  artifacts:
    - path: "crates/nono-cli/src/exec_strategy_windows/layer_registry.rs"
      provides: "9 previously-drifted rows converted to accurate, content-verified symbol-form call_sites"
    - path: "crates/nono-cli/tests/layer_registry_selfcheck.rs"
      provides: "content_defines_symbol helper replacing the bare content.contains(symbol) check"
    - path: "crates/nono-cli/tests/layer_registry_meta_test.rs"
      provides: "contains_fn_exact implementing its documented '!' boundary exclusion and a definition-line-prefix requirement"
  key_links:
    - from: "crates/nono-cli/src/exec_strategy_windows/layer_registry.rs"
      to: "crates/nono-cli/tests/layer_registry_selfcheck.rs::registry_call_sites_exist"
      via: "every call_sites citation resolves to a real definition site in the cited file"
      pattern: "content_defines_symbol"
---

<objective>
Close SC1 / CINT-01's remaining gap: 9 of 13 Windows layer-registry rows still cite raw `file:line`
call sites that have drifted from the code they describe. Two independently re-confirmed samples
(`restricted_token.rs:55`, `launch.rs:1403`) and 13 more sampled by the code review — 15 of 15 land on
unrelated code (doc comments, closing braces, unrelated statements). This plan converts all 9 rows to
the same content-verified `file.rs::Symbol` form Plan 117-15 already established for the other 4 rows,
using the exact enforcing call site each row's own registry comment already describes.

Also close two secondary findings from the same review pass: WR-08's observation that
`registry_call_sites_exist`'s symbol-form check only requires `content.contains(symbol)` — satisfied by
a doc-comment mention, not a real definition — and WR-10's finding that `contains_fn_exact` (the
CINT-03 discovery meta-test's function-existence check) does not implement its own documented `!`
boundary-character exclusion and can be satisfied by a commented-out or string-literal "test".

Purpose: every "Enforcing call site(s)" citation must name code that genuinely enforces the row it is
attached to, and the tests that verify citations/test-existence must themselves be unable to pass on a
citation or test that only exists in a comment or string.
Output: `layer_registry.rs` with 9 corrected rows, `layer_registry_selfcheck.rs` with a
definition-site-aware content check, `layer_registry_meta_test.rs` with a hardened `contains_fn_exact`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-REVIEW.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md
</context>

<interfaces>
The 9 rows and their EXACT replacement `call_sites` arrays (each verified during planning by direct
code read — the real enforcing/creating function each row's own existing registry comment already
describes):

1. `LayerId::RestrictedToken` (currently `&["restricted_token.rs:55", "launch.rs:1403"]`) →
   `&["restricted_token.rs::create_restricted_token_with_sid", "launch.rs::spawn_windows_child"]`.
   `create_restricted_token_with_sid` (restricted_token.rs, constructs the token via
   `CreateRestrictedToken` with `WRITE_RESTRICTED`) is called from `spawn_windows_child`'s
   `WindowsTokenArm::WriteRestricted` arm.

2. `LayerId::MandatoryIntegrityLabel` (currently 2 symbol-form entries plus 1 raw
   `"nono-shell-broker/src/main.rs:615-644"`) → replace ONLY the raw entry with
   `"nono-shell-broker/src/main.rs::broker_resume_gate"` (the function that re-reads the broker's
   grandchild token integrity RID and requires `<= SECURITY_MANDATORY_LOW_RID`, per this row's own
   existing comment "the broker DOES perform a real token-RID check"). Keep the two existing
   symbol-form entries (`labels_guard.rs::AppliedLabelsGuard::snapshot_and_apply`,
   `mod.rs::prepare_live_windows_launch`) unchanged.

3. `LayerId::AppContainerProfile` (currently `&["nono-shell-broker/src/main.rs:322-336",
   "nono-shell-broker/src/main.rs:536-556", "agent_daemon/launch.rs:636-650"]`) →
   `&["nono-shell-broker/src/main.rs::run", "nono-shell-broker/src/main.rs::broker_resume_gate",
   "agent_daemon/launch.rs::launch_agent"]`. `run` (the broker's `pub fn run(args: BrokerArgs)`)
   creates the profile via `nono::create_app_container_profile`; `broker_resume_gate` confirms it on
   resume; `launch_agent` (the daemon's `pub(crate) async fn launch_agent`) creates it on the daemon
   path.

4. `LayerId::DaclPackageSidGrant` (currently 2 symbol-form entries plus 2 raw
   `"agent_daemon/launch.rs:133"`, `"agent_daemon/launch.rs:764"`) → replace ONLY the two raw entries
   with `"agent_daemon/launch.rs::DaemonDaclGuard::apply"` and
   `"agent_daemon/launch.rs::launch_agent"`. Keep the two existing symbol-form entries
   (`dacl_guard.rs::AppliedDaclGrantsGuard::snapshot_and_apply`, `mod.rs::prepare_live_windows_launch`)
   unchanged.

5. `LayerId::WfpEgressFilters` (currently `&["network.rs:1602-1610", "network.rs:1730",
   "network.rs:1775-1800", "agent_daemon/launch.rs:444"]`) → `&["network.rs::WfpNetworkBackend::install",
   "network.rs::assert_wfp_activation_installed_filters", "agent_daemon/launch.rs::wfp_filter_add"]`.
   `assert_wfp_activation_installed_filters` is the pre-spawn fail-closed `installed_filter_count`
   check this row's own comment already describes.

6. `LayerId::FirewallRulesEgress` (currently `&["network.rs:1500-1533", "network.rs:1535-1594"]`) →
   `&["network.rs::FirewallRulesNetworkBackend::install", "network.rs::run_netsh_firewall"]`. (The
   previous raw citations actually pointed at `start_windows_wfp_service_with_runner`, unrelated WFP
   service-startup code, not the `netsh` backend this row describes.)

7. `LayerId::JobObjectContainment` (currently `&["launch.rs:375", "launch.rs:2134-2137",
   "agent_daemon/launch.rs:959", "agent_daemon/launch.rs:1049"]`) →
   `&["launch.rs::create_process_containment", "launch.rs::apply_process_handle_to_containment",
   "agent_daemon/launch.rs::create_agent_job", "agent_daemon/launch.rs::assign_process_to_agent_job"]`.

8. `LayerId::BrokerAuthenticodeTrustGate` (currently `&["launch.rs:1505-1506", "launch.rs:1828-1829",
   "launch.rs:2190", "launch.rs:2236"]`) → `&["launch.rs::verify_broker_authenticode",
   "launch.rs::spawn_windows_child"]`. `verify_broker_authenticode` is the real check; both of its two
   production call sites sit inside `spawn_windows_child`.

9. `LayerId::InterpreterCoverageGate` (currently `&["crates/nono/src/sandbox/windows.rs:2247",
   "crates/nono/src/sandbox/mod.rs:925", "mod.rs:357-362"]`) → `&["crates/nono/src/sandbox/windows.rs::validate_launch_paths",
   "crates/nono/src/sandbox/mod.rs::Sandbox::validate_windows_launch_paths",
   "mod.rs::prepare_live_windows_launch"]`. (The first two raw citations already pointed at the
   correct declaration lines; only the third, `"mod.rs:357-362"`, was wrong — it pointed at an
   unrelated ancestor-grant comment. The real call site of
   `Sandbox::validate_windows_launch_paths(...)` is inside `prepare_live_windows_launch`, line ~498.)

File-part resolution convention (already established in `layer_registry_selfcheck.rs`'s
`resolve_file_part`, mirror it exactly, do not invent a new convention): a bare filename (e.g.
`"launch.rs"`, `"network.rs"`) resolves relative to `exec_strategy_windows/`; a `"crates/..."`-prefixed
part resolves relative to the workspace root; a `"nono-shell-broker/..."` part resolves relative to
`crates/`; an `"agent_daemon/..."` part resolves relative to `crates/nono-cli/src/`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert all 9 drifted rows to verified symbol-form citations</name>
  <files>crates/nono-cli/src/exec_strategy_windows/layer_registry.rs</files>
  <read_first>
    crates/nono-cli/src/exec_strategy_windows/layer_registry.rs (read lines 855-1055, the full
    REGISTRY_ENTRIES const — this is the only region this task edits)
    crates/nono-cli/src/exec_strategy_windows/restricted_token.rs (confirm create_restricted_token_with_sid
    at its `pub(super) fn` declaration)
    crates/nono-cli/src/exec_strategy_windows/launch.rs (confirm spawn_windows_child,
    verify_broker_authenticode, create_process_containment, apply_process_handle_to_containment at
    their declarations)
    crates/nono-cli/src/exec_strategy_windows/network.rs (confirm WfpNetworkBackend::install,
    assert_wfp_activation_installed_filters, FirewallRulesNetworkBackend::install,
    run_netsh_firewall at their declarations)
    crates/nono-cli/src/agent_daemon/launch.rs (confirm launch_agent, DaemonDaclGuard::apply,
    wfp_filter_add, create_agent_job, assign_process_to_agent_job at their declarations)
    crates/nono-shell-broker/src/main.rs (confirm run, broker_resume_gate at their declarations)
    crates/nono/src/sandbox/windows.rs (confirm validate_launch_paths at its declaration)
    crates/nono/src/sandbox/mod.rs (confirm Sandbox::validate_windows_launch_paths at its declaration)
  </read_first>
  <behavior>
    - Every one of the 13 `REGISTRY_ENTRIES` rows' `call_sites` array contains only symbol-form
      (`"file.rs::Symbol"` or `"file.rs::Type::method"`) citations or is deliberately empty
      (`DaclSessionSidGrant`, `MinifilterAbsence` — both already justified `&[]` rows, unchanged by
      this task).
    - Every symbol named in every citation is the function/method that ACTUALLY performs the
      enforcement/creation/confirmation the row's own `outcome`/comment describes — verified by
      reading each cited file's declaration during this task's execution (the `<interfaces>` list
      above is the pre-verified result; this task applies it, and MUST re-confirm each declaration
      still exists exactly as named before writing the citation, in case the working tree has moved
      since planning).
  </behavior>
  <action>
    Apply exactly the 9 row replacements enumerated in `<interfaces>` above to the corresponding
    `LayerRegistryEntry { call_sites: &[...], .. }` literals in `REGISTRY_ENTRIES`. For rows with a
    mix of already-symbol and raw entries (`MandatoryIntegrityLabel`, `DaclPackageSidGrant`), replace
    ONLY the raw entries, preserving the existing correct symbol-form entries and their array order.
    For fully-raw rows, replace the entire `call_sites: &[...]` literal. Do not touch any other field
    (`expectancy`, `outcome`, `probe`, doc comments) on any of these rows except where a citation
    literal is embedded inside an existing doc comment referencing the OLD raw form (e.g. the
    `MandatoryIntegrityLabel` row's own comment already correctly describes the broker's real-token-RID
    check in prose — leave prose untouched, only the `call_sites` array literal changes).
  </action>
  <acceptance_criteria>
    - `grep -c '\.rs:[0-9]' crates/nono-cli/src/exec_strategy_windows/layer_registry.rs | grep -v '^#'`
      — every remaining `"file:line"`-shaped citation (a double-quoted string containing `.rs:` followed
      by a digit) is gone from the `REGISTRY_ENTRIES` array region; the only acceptable survivors are
      inside doc comments illustrating the citation SHAPE (not inside an actual `call_sites: &[...]`
      literal) — confirm by reading the diff, not by a blind grep count, since doc-comment prose is
      allowed to mention the old shape descriptively.
    - Every one of the 9 rows' `call_sites` arrays now matches the `<interfaces>` replacement list
      exactly.
    - `cargo build -p nono-sandbox-cli --lib` succeeds (the array is a `&'static [&'static str]`
      literal — a syntax error here fails the build immediately).
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p nono-sandbox-cli --lib</automated>
  </verify>
  <done>All 9 previously-drifted rows now cite accurate, symbol-form call sites matching the code they describe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tighten registry_call_sites_exist's content check to require a real definition site</name>
  <files>crates/nono-cli/tests/layer_registry_selfcheck.rs</files>
  <read_first>
    crates/nono-cli/tests/layer_registry_selfcheck.rs (read the full file, especially
    registry_call_sites_exist at lines 187-245, extract_symbol_citations at lines 144-165, and
    split_symbol_citation at lines 167-177 — this task adds one new helper and changes one match arm)
  </read_first>
  <behavior>
    - `content_defines_symbol(content, "AppliedLabelsGuard::snapshot_and_apply")` returns `true` for
      the real `labels_guard.rs` source (which has a line, trimmed, starting with `pub(crate) fn
      snapshot_and_apply(`).
    - `content_defines_symbol(content, "some_symbol")` returns `false` if `content` mentions
      `"fn some_symbol"` ONLY inside a doc comment (`/// see \`fn some_symbol(...)\``) or inside a
      string/macro literal (`format!("fn {name}")` where the text before "fn " on that line is not a
      bare qualifier-keyword sequence).
    - `registry_call_sites_exist` now calls `content_defines_symbol` instead of `content.contains(symbol)`
      for every symbol-form citation, and still passes for all 13 rows' now-corrected citations
      (Task 1's output).
  </behavior>
  <action>
    Add a new function `fn content_defines_symbol(content: &str, symbol: &str) -> bool` to this file:
    take `let last = symbol.rsplit("::").next().unwrap_or(symbol);`, search `content` for both `"fn
    {last}"` and `"impl {last}"` needles; for each match, find the line start (`rfind('\n')`), take the
    trimmed prefix before the match on that line, and accept the match only if that prefix, split on
    whitespace, consists entirely of words from the set `{"pub", "pub(crate)", "pub(super)", "async",
    "unsafe", "const"}` or words starting with `"pub("` (covers `pub(in path)` forms) — return `true`
    on the first accepted match, `false` if no match is ever accepted. In `registry_call_sites_exist`'s
    symbol-citation loop, replace `Ok(content) if content.contains(symbol) => {}` with `Ok(content) if
    content_defines_symbol(&content, symbol) => {}`, keeping the existing `Ok(_) =>
    missing_symbols.push(...)` and `Err(e) => missing_symbols.push(...)` arms unchanged (only the guard
    condition changes).
  </action>
  <acceptance_criteria>
    - `content_defines_symbol` exists as a standalone function (not inlined into the test body) so it
      is independently testable.
    - A new unit test `content_defines_symbol_rejects_a_doc_comment_mention` constructs a small string
      containing only `"/// see `fn foo(...)` for details\n"` (no real `fn foo(` definition line) and
      asserts `content_defines_symbol(src, "foo") == false`.
    - A new unit test `content_defines_symbol_accepts_a_real_definition` constructs
      `"pub(crate) fn foo(x: u32) -> u32 {\n    x\n}\n"` and asserts
      `content_defines_symbol(src, "foo") == true`.
    - `cargo test -p nono-sandbox-cli --test layer_registry_selfcheck` passes all tests, including
      `registry_call_sites_exist` against the real, now-corrected `layer_registry.rs` (proving Task
      1's citations are genuinely definition-accurate, not merely present-as-text).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-cli --test layer_registry_selfcheck</automated>
  </verify>
  <done>registry_call_sites_exist can no longer be satisfied by a doc-comment mention or an unrelated string literal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement contains_fn_exact's documented '!' boundary rule and definition-line-prefix requirement (WR-10)</name>
  <files>crates/nono-cli/tests/layer_registry_meta_test.rs</files>
  <read_first>
    crates/nono-cli/tests/layer_registry_meta_test.rs (read lines 250-277 for the current
    contains_fn_exact implementation and its doc comment's stated `!`-boundary claim, and lines
    288-352/360-391 for its two callers every_registry_row_has_a_test and
    also_automated_entries_are_non_vacuous — this task's change must not regress either)
  </read_first>
  <behavior>
    - `contains_fn_exact(src, "foo")` still matches `pub(crate) fn foo(`, `async fn foo(`, `fn foo<T>`,
      and still rejects `fn foo2`/`fn foobar` — unchanged from today's correct behavior.
    - `contains_fn_exact(src, "foo")` now ALSO rejects `fn foo!` (the documented but previously
      unimplemented `!` boundary exclusion).
    - `contains_fn_exact(src, "foo")` now ALSO rejects a match inside a doc comment (`` /// calls `fn
      foo(...)` internally ``) or inside a macro/format-string literal (`format!("fn {name}")` where
      `name == "foo"` at runtime is irrelevant — the SOURCE TEXT literal `"fn {name}"` does not itself
      spell `"fn foo"`, so this specific example does not trigger contains_fn_exact at all; the real
      risk case is a literal source string that DOES spell out `"fn foo("`, e.g. inside a `#[doc =
      "... fn foo(...) ..."]` attribute or a raw string constant — the fix must reject any occurrence
      whose line-prefix is not a bare qualifier-keyword sequence, matching Task 2's
      `content_defines_symbol` logic exactly).
    - `every_registry_row_has_a_test` and `also_automated_entries_are_non_vacuous` both still pass
      against the real, unmodified `layer_force_unavailable.rs` and the real `ALSO_AUTOMATED`-cited
      files (proving the tightened check does not produce false negatives on genuine, already-shipped
      test functions).
  </behavior>
  <action>
    Rewrite `contains_fn_exact` to combine the existing trailing-character boundary check (adding `||
    c == '!'` to its exclusion set, closing the documented-but-unimplemented `!` rule) with a
    definition-line-prefix check identical in shape to Task 2's `content_defines_symbol`: for each
    candidate match of `"fn {fn_name}"`, compute the trimmed prefix from the line start to the match,
    and require it to consist only of words from `{"pub", "pub(crate)", "pub(super)", "async",
    "unsafe", "const"}` or words starting with `"pub("` — reject (continue scanning past this match,
    do not return early) if either the trailing-character check or the prefix check fails. Keep the
    function's existing loop-and-continue structure (advance `search_start` by 1 past a rejected match
    rather than returning `false` immediately, so a later genuine match in the same file is still
    found).
  </action>
  <acceptance_criteria>
    - `grep -n "c == '!'" crates/nono-cli/tests/layer_registry_meta_test.rs` non-empty.
    - A new unit test `contains_fn_exact_rejects_doc_comment_mention` constructs a string containing
      only `"/// see `fn probe_target(...)`\n"` and asserts
      `contains_fn_exact(src, "probe_target") == false`.
    - A new unit test `contains_fn_exact_rejects_bang_suffix` constructs `"fn probe_target! "` (or an
      equivalent macro-shaped false positive) and asserts `contains_fn_exact(src, "probe_target") ==
      false`.
    - A new unit test `contains_fn_exact_accepts_real_definition_after_rejecting_a_false_positive`
      constructs a string with BOTH a doc-comment mention AND a real `pub(crate) fn probe_target(`
      definition later in the same string, and asserts `contains_fn_exact(...) == true` (proving the
      function keeps scanning past a rejected match instead of returning `false` early).
    - `cargo test -p nono-sandbox-cli --test layer_registry_meta_test` passes every existing test
      (`every_registry_row_has_a_test`, `also_automated_entries_are_non_vacuous`,
      `host_gated_rows_are_loud`, `security_assumptions_are_loud`) plus the three new ones, against the
      real, unmodified `layer_force_unavailable.rs` and `ALSO_AUTOMATED`-cited source files.
    - `cargo clippy -p nono-sandbox-cli --tests -- -D warnings -D clippy::unwrap_used` is clean.
    - `cargo fmt --check` is clean.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-cli --test layer_registry_meta_test</automated>
  </verify>
  <done>contains_fn_exact implements its documented boundary rule in full and cannot be satisfied by a comment or literal mention; all pre-existing coverage still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Documentation ↔ enforcement reality | The registry's citations are the only link between the human-readable contract and the actual enforcing code; a drifted citation is a silent documentation failure, not a live escape, but it is exactly the "green by absence" class this phase exists to close |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-24-01 | Repudiation | `layer_registry.rs`'s 9 drifted citations | mitigate | Replace with verified, symbol-form citations naming the real enforcing/creating function (Task 1) |
| T-117-24-02 | Tampering | `registry_call_sites_exist`'s content check | mitigate | Require a real definition-line match, not a bare substring, so a future drift cannot hide inside a comment (Task 2) |
| T-117-24-03 | Tampering | `contains_fn_exact`'s discovery check (CINT-03's own coverage mechanism) | mitigate | Implement the documented `!` boundary and definition-line-prefix requirement so a commented-out or string-literal "test" cannot satisfy coverage (Task 3) |
</threat_model>

<verification>
`cargo test -p nono-sandbox-cli --test layer_registry_selfcheck` and `cargo test -p nono-sandbox-cli
--test layer_registry_meta_test` both pass in full, including all new unit tests. `grep -c '"file:line"
citation shape survives'` is zero in the `REGISTRY_ENTRIES` array.
</verification>

<success_criteria>
SC1 / CINT-01 is closed: all 13 registry rows cite accurate, content-verified symbol-form call sites.
WR-08's secondary finding is closed: the content check requires a real definition site. WR-10 is
closed: `contains_fn_exact` implements its documented boundary rule and cannot be satisfied by a
comment or literal.
</success_criteria>

<output>
Create `.planning/phases/117-fail-direction-contract-startup-self-attestation/117-24-SUMMARY.md` when done
</output>
