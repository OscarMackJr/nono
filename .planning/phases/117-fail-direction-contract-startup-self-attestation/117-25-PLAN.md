---
phase: 117-fail-direction-contract-startup-self-attestation
plan: 25
type: execute
wave: 9
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - crates/nono-cli/src/telemetry/mod.rs
  - crates/nono-cli/src/exec_strategy_windows/attestation_downgrade_event.rs
autonomous: true
requirements: [CINT-02, CINT-03]

must_haves:
  truths:
    - "The windows-layer-fault-injection CI job carries no dead configuration — NONO_CI_HAS_WFP is removed (closes WR-05)."
    - "SecurityEventLayerInner's chain/session_id/config fields are private again; the only way any module outside telemetry::mod can advance the HMAC chain or read session/config state is through a dedicated accessor that always routes through advance_chain (closes WR-09)."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "windows-layer-fault-injection job with the dead env block removed"
    - path: "crates/nono-cli/src/telemetry/mod.rs"
      provides: "SecurityEventLayer::advance_and_snapshot, the sole cross-module chain-advancement accessor"
    - path: "crates/nono-cli/src/exec_strategy_windows/attestation_downgrade_event.rs"
      provides: "emit_attestation_event rewritten to use advance_and_snapshot instead of direct field access"
  key_links:
    - from: "crates/nono-cli/src/exec_strategy_windows/attestation_downgrade_event.rs::emit_attestation_event"
      to: "crates/nono-cli/src/telemetry/mod.rs::SecurityEventLayer::advance_and_snapshot"
      via: "direct method call, replacing self.inner.lock() + direct field mutation"
      pattern: "advance_and_snapshot\\("
---

<objective>
Close two small, independent, low-risk findings from the iteration-4 code review:

WR-05: the `windows-layer-fault-injection` CI job's `env: NONO_CI_HAS_WFP: true` block has exactly one
consumer in the entire tree (`scripts/windows-test-harness.ps1`), and that job never invokes that
script — the variable changes nothing. It is inert configuration that makes the job's intent look
broader than it is (the job's real, load-bearing addition — the `nono-shell-broker.exe` release
pre-build — is unaffected and stays).

WR-09: Plan 117-17's relocation of `emit_attestation_event` to a second `impl SecurityEventLayer` block
(needed because `nono-agentd` has no non-test call site for it after NR3-05's daemon-decision
simplification) widened `SecurityEventLayerInner`'s `chain`/`session_id`/`config` fields to `pub(crate)`
so the new file could read them directly — giving every module in the binary unmediated mutable access
to the tamper-evident HMAC chain, with no accessor discipline and no test guarding the boundary.

Purpose: CI configuration should not claim gating it does not perform, and the append-only HMAC chain's
only legitimate mutation path (`advance_chain`) should be the only path any external module can reach.
Output: `.github/workflows/ci.yml` with the dead env block removed, `telemetry/mod.rs` with a narrow
accessor, `attestation_downgrade_event.rs` rewritten to use it, and `SecurityEventLayerInner`'s fields
private again.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-REVIEW.md
</context>

<interfaces>
From `crates/nono-cli/src/telemetry/mod.rs` (current state, to be narrowed):

```
pub(crate) struct SecurityEventLayerInner {
    pub(crate) chain: ChainState,
    pub(crate) session_id: String,
    session_salt: [u8; 32],
    pub(crate) config: TelemetryConfig,
}

pub struct SecurityEventLayer {
    pub(crate) inner: std::sync::Arc<Mutex<SecurityEventLayerInner>>,
}
```

`pub(crate) fn advance_chain(chain: &mut ChainState, event_bytes: &[u8])` (line ~126) and `fn
chain_head_hex(head: &[u8; 32]) -> String` are the existing chain-advancement primitives already used
by every other emitter in this file.

From `crates/nono-cli/src/exec_strategy_windows/attestation_downgrade_event.rs`, the current
`emit_attestation_event` body (to be rewritten):

```
pub fn emit_attestation_event(&self, downgraded_layers: &[&str]) -> Result<String, &'static str> {
    let mut inner = self.inner.lock().map_err(|_| "mutex poisoned")?;
    let timestamp_unix_ms = SystemTime::now().duration_since(UNIX_EPOCH).map(|d| d.as_millis() as u64).unwrap_or(0);
    let downgraded_layers_value = downgraded_layers.join(",");
    let pre_chain_bytes = format!("{event_type:?}|{downgraded}|{session_id}|{ts}",
        event_type = SecurityEventType::LayerAttestationDowngraded,
        downgraded = downgraded_layers_value, session_id = inner.session_id, ts = timestamp_unix_ms);
    advance_chain(&mut inner.chain, pre_chain_bytes.as_bytes());
    let chain_head = chain_head_hex(&inner.chain.head);
    if inner.config.enabled {
        let security_event = SecurityEvent { event_type: SecurityEventType::LayerAttestationDowngraded,
            agent_pid: std::process::id(), path_hash: None, path_category: None, host: None,
            session_id: inner.session_id.clone(), chain_head: chain_head.clone(), timestamp_unix_ms,
            downgraded_layers: Some(downgraded_layers_value) };
        emit_security_event(&security_event);
    }
    Ok(chain_head)
}
```

Its own test module (bottom of the same file) includes
`emit_attestation_event_err_on_poisoned_mutex`, which does `layer_clone.inner.lock().unwrap()` then
panics WITHOUT touching any field of the guard — this test must keep compiling and passing, which means
`SecurityEventLayer::inner` (the `Arc<Mutex<SecurityEventLayerInner>>` field itself) MUST stay
`pub(crate)`; only `SecurityEventLayerInner`'s three named fields go back to private.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove the dead NONO_CI_HAS_WFP env block from ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .github/workflows/ci.yml (read lines 280-330 for the windows-layer-fault-injection job's full
    definition, including the env block at lines ~315-317 and the two cargo test steps that follow)
  </read_first>
  <behavior>
    N/A — CI configuration only, no runtime behavior change; the job's actual test steps
    (`cargo test -p nono-sandbox-cli --features layer-fault-injection` and `cargo test -p
    nono-shell-broker --features layer-fault-injection`) and the broker release pre-build step are
    unchanged.
  </behavior>
  <action>
    Delete the `env:` block (`env:` followed by `      NONO_CI_HAS_WFP: true   # windows-latest runners
    run as Administrator`) from the `windows-layer-fault-injection` job in
    `.github/workflows/ci.yml`. Do not change any other line in this job — the broker pre-build
    (`cargo build -p nono-shell-broker --release`) and both `cargo test` steps stay exactly as they
    are.
  </action>
  <acceptance_criteria>
    - `grep -n "NONO_CI_HAS_WFP" .github/workflows/ci.yml` returns zero matches.
    - `grep -rn "NONO_CI_HAS_WFP" crates/ tests/` still returns zero matches (unchanged — confirms this
      task did not need to touch any Rust source, since the variable never had a Rust-side consumer).
    - `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` (or an equivalent
      YAML parse check) succeeds, confirming the file is still valid YAML after the deletion.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "NONO_CI_HAS_WFP" .github/workflows/ci.yml</automated>
  </verify>
  <done>The dead env block is gone; the job's real (broker pre-build) fix from Plan 117-18 is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Narrow SecurityEventLayerInner back to private fields behind a chain-advancement accessor</name>
  <files>crates/nono-cli/src/telemetry/mod.rs, crates/nono-cli/src/exec_strategy_windows/attestation_downgrade_event.rs</files>
  <read_first>
    crates/nono-cli/src/telemetry/mod.rs (read lines 100-260 for advance_chain, chain_head_hex,
    SecurityEventLayerInner, and SecurityEventLayer's existing impl block, and skim the rest of the
    file for the on_event/emit_override_event pattern this task's new accessor should match in style)
    crates/nono-cli/src/exec_strategy_windows/attestation_downgrade_event.rs (read the full file — the
    only production caller and its own test module this task must keep passing unchanged)
  </read_first>
  <behavior>
    - `SecurityEventLayer::advance_and_snapshot(&self, build_event_bytes: impl FnOnce(&str) ->
      Vec&lt;u8&gt;) -> Result&lt;(String, String, bool), &'static str&gt;` locks `inner` ONCE, calls
      `build_event_bytes(&inner.session_id)` to obtain the bytes to chain (so the caller never needs
      its own separate lock to read `session_id` first), calls `advance_chain(&mut inner.chain,
      &event_bytes)`, and returns `Ok((inner.session_id.clone(), chain_head_hex(&inner.chain.head),
      inner.config.enabled))` — a 3-tuple of `(session_id, chain_head, telemetry_enabled)` — on
      success, `Err("mutex poisoned")` if the lock is poisoned.
    - `emit_attestation_event`'s three existing tests
      (`emit_attestation_event_advances_chain_by_one_enabled`,
      `emit_attestation_event_advances_chain_by_one_disabled`,
      `emit_attestation_event_err_on_poisoned_mutex`) all still pass unchanged, with unchanged
      assertions — this is a single-lock, single-call refactor with byte-identical externally-observable
      behavior, not a behavior change.
  </behavior>
  <action>
    In `telemetry/mod.rs`: remove `pub(crate)` from `SecurityEventLayerInner`'s `chain`, `session_id`,
    and `config` fields (revert to fully private — module-private to `telemetry::mod`; leave
    `session_salt` as-is, already private; leave the STRUCT's own `pub(crate)` visibility and the
    `SecurityEventLayer::inner` field's `pub(crate)` visibility UNCHANGED, per `<interfaces>`'s note
    that the poisoned-mutex test needs `.inner.lock()` to keep working). Add a new method to
    `SecurityEventLayer`'s existing `impl` block:

    `pub(crate) fn advance_and_snapshot(&self, build_event_bytes: impl FnOnce(&str) -> Vec<u8>) ->
    Result<(String, String, bool), &'static str> { let mut inner = self.inner.lock().map_err(|_|
    "mutex poisoned")?; let event_bytes = build_event_bytes(&inner.session_id); advance_chain(&mut
    inner.chain, &event_bytes); let chain_head = chain_head_hex(&inner.chain.head);
    Ok((inner.session_id.clone(), chain_head, inner.config.enabled)) }`

    In `attestation_downgrade_event.rs`'s `emit_attestation_event`: keep the existing
    `timestamp_unix_ms` and `downgraded_layers_value` computation unchanged (neither needs `inner`).
    Replace the `let mut inner = self.inner.lock()...` line through the `advance_chain`/`chain_head_hex`
    lines with a single call: `let (session_id, chain_head, enabled) =
    self.advance_and_snapshot(|session_id| { format!("{event_type:?}|{downgraded}|{session_id}|{ts}",
    event_type = SecurityEventType::LayerAttestationDowngraded, downgraded = downgraded_layers_value,
    ts = timestamp_unix_ms).into_bytes() })?;`. Replace the remaining `if inner.config.enabled { ...
    session_id: inner.session_id.clone() ... }` block's two `inner.*` reads with the `enabled` and
    `session_id` locals just returned by `advance_and_snapshot`. The function's final `Ok(chain_head)`
    becomes `Ok(chain_head)` using the `chain_head` local from the tuple (unchanged return type/value).
  </action>
  <acceptance_criteria>
    - `grep -n "pub(crate) chain:\|pub(crate) session_id:\|pub(crate) config:" crates/nono-cli/src/telemetry/mod.rs`
      returns zero matches inside `SecurityEventLayerInner`'s field list (fields are private again).
    - `grep -n "pub(crate) inner:" crates/nono-cli/src/telemetry/mod.rs` still matches (the
      `Arc<Mutex<...>>` field itself stays `pub(crate)`, needed by the poisoned-mutex test).
    - `grep -n "fn advance_and_snapshot" crates/nono-cli/src/telemetry/mod.rs` matches exactly once.
    - `grep -n "inner\.chain\|inner\.session_id\|inner\.config" crates/nono-cli/src/exec_strategy_windows/attestation_downgrade_event.rs`
      returns zero matches (no direct field access left outside `telemetry::mod`).
    - All three pre-existing tests in `attestation_downgrade_event.rs`'s test module pass unchanged:
      `cargo test -p nono-sandbox-cli --lib exec_strategy_windows::attestation_downgrade_event`.
    - `cargo build -p nono-sandbox-cli --bin nono-agentd` still succeeds — confirms the daemon binary's
      independent `#[path]`-copy of `telemetry/mod.rs` compiles with the narrowed fields (it never
      calls `advance_and_snapshot`, which lives in a file the daemon never includes, so this only
      needs the struct definition to still be internally consistent).
    - `cargo clippy -p nono-sandbox-cli --lib -- -D warnings -D clippy::unwrap_used` is clean.
    - `cargo fmt --check` is clean.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-cli --lib exec_strategy_windows::attestation_downgrade_event</automated>
  </verify>
  <done>SecurityEventLayerInner's chain/session_id/config fields are private again; the only way to advance the chain or read session/config state from outside telemetry::mod is through advance_and_snapshot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Tamper-evident HMAC chain state (`SecurityEventLayerInner::chain`) | Must only ever advance through `advance_chain`; any other write path is a forgery/reset vector for the audit trail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-25-01 | Tampering | `SecurityEventLayerInner::chain` | mitigate | Revert to private fields behind `advance_and_snapshot`, the sole cross-module chain-advancement path (WR-09) |
| T-117-25-02 | Information Disclosure | CI job configuration accuracy | accept | `NONO_CI_HAS_WFP` was inert (no consumer); removing it changes nothing about actual coverage, only removes misleading configuration (WR-05) |
</threat_model>

<verification>
`cargo test -p nono-sandbox-cli --lib exec_strategy_windows::attestation_downgrade_event` passes.
`grep -n "NONO_CI_HAS_WFP" .github/workflows/ci.yml` returns zero matches. `SecurityEventLayerInner`'s
three named fields are private.
</verification>

<success_criteria>
WR-05 is closed: the dead CI configuration is removed. WR-09 is closed: the HMAC chain's mutable state
is private again, reachable only through an accessor that always advances it through `advance_chain`.
</success_criteria>

<output>
Create `.planning/phases/117-fail-direction-contract-startup-self-attestation/117-25-SUMMARY.md` when done
</output>
