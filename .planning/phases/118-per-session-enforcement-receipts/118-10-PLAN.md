---
phase: 118-per-session-enforcement-receipts
plan: 10
type: execute
wave: 6
depends_on: ["118-06", "118-07", "118-08", "118-09"]
files_modified:
  - crates/nono-cli/src/receipt_sink.rs
  - crates/nono-cli/src/exec_strategy_windows/launch.rs
autonomous: false
requirements: [RCPT-01, RCPT-02, RCPT-03]

must_haves:
  truths:
    - "Both cross-target clippy gates (linux-gnu via cross, apple-darwin via cargo-zigbuild) pass clean on the full workspace, since LayerId's core promotion and the nono receipt cli.rs addition both touch files with Unix cfg branches — D-23, no PARTIAL->CI."
    - "The D-08 sink guard is verified against a REAL confined child on this host, not merely a unit test — a confined process cannot read or write the receipt sink, on both the restricted-token arm and the broker's Medium-IL arm."
    - "D-17's per-tool-call latency budget is a MEASURED number, not an asserted 'negligible' — recorded in the codebase, per 117-D-24's discipline."
    - "This phase's process compliance is on record: no SDK STATE/ROADMAP writers used (D-24), and /gsd:code-review is understood to run on this phase before it is considered done (D-22)."
  artifacts:
    - path: "crates/nono-cli/src/receipt_sink.rs"
      provides: "a module-doc note recording the discretionary decisions made across this phase: sink location, retention policy, Event Log pointer disposition (D-06)"
  key_links:
    - from: "this plan"
      to: ".planning/templates/cross-target-verify-checklist.md"
      via: "both gate commands run against the full workspace after Plan 118-09"
      pattern: "cross clippy"
---

<objective>
Close the phase: run both mandatory cross-target clippy gates (D-23) against the full set of
changes from Plans 118-01 through 118-09, measure D-17's per-tool-call latency budget, and verify
D-08's sink guard against a real confined child on this host (a property no unit test can prove,
since the supervisor and the confined child share a user). Record the discretionary decisions this
phase made (sink location, retention, Event Log pointer) as a durable module-doc note, and confirm
process compliance (D-22's code-review requirement, D-24's SDK-writer ban).

Purpose: this is the phase-gate plan — nothing here adds new receipt functionality; it proves what
Plans 118-01 through 118-09 built actually holds under the constraints CLAUDE.md and CONTEXT.md
impose, and it closes every remaining open discretionary/deferred item with a recorded decision
rather than silence.
Output: green cross-target clippy on the full workspace; a measured D-17 latency number; a
demonstrated confined-child sink-guard checkpoint result; a discretion-decisions doc note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/118-per-session-enforcement-receipts/118-CONTEXT.md
@.planning/phases/118-per-session-enforcement-receipts/118-VALIDATION.md
@.planning/templates/cross-target-verify-checklist.md
@.planning/phases/118-per-session-enforcement-receipts/118-05-SUMMARY.md
@.planning/phases/118-per-session-enforcement-receipts/118-07-SUMMARY.md
@.planning/phases/118-per-session-enforcement-receipts/118-08-SUMMARY.md
@.planning/phases/118-per-session-enforcement-receipts/118-09-SUMMARY.md
</context>

<interfaces>
From `.planning/templates/cross-target-verify-checklist.md` (single source of truth for the
runbook, D-23):
```
cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used
cargo-zigbuild clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used
```
Use `cross clippy` (NOT bare `cargo clippy --target ...` — no native gnu linker on this host) for
linux-gnu, and the DIRECT-BINARY `cargo-zigbuild clippy ...` form (NOT `cargo zigbuild clippy ...`,
which mis-parses) for apple-darwin, with `SDKROOT` unset. Neither a stopped Docker daemon nor an
absent tool is a "documented runner failure" that permits falling back to PARTIAL→CI — both gates
are provably local-runnable on this host per the checklist's own Phase 96 update.

From VALIDATION.md's Manual-Only Verifications table: the sink-guard checkpoint needs "a real
confined session on a real host... from inside the child, attempt to read and to write the receipt
sink; both must be denied. Repeat on the broker arm (Medium-IL child) and on an arm where no
session SID was minted." The `%PROGRAMDATA%\nono\receipts` labelability check ("Attempt
try_set_mandatory_label on the candidate sink path; record the result before committing to the
location") should already have been resolved by Plan 118-05's Task 1 — this plan's checkpoint
confirms the END-TO-END guard, not re-derives the location choice.

From CONTEXT.md's `<decisions>` Claude's Discretion list: sink location/layout, retention/rotation
policy, on-disk schema versioning, whether the Event Log pointer (D-06) earns its place at all,
and whether token-arm/entry-path are first-class fields — all resolved across Plans 118-01/05/06;
this plan's job is to consolidate the RECORD of those decisions in one place (the sink module doc)
so a future reader does not have to reconstruct them from SUMMARYs.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Cross-target clippy gates (D-23) + D-17 latency measurement</name>
  <files>crates/nono-cli/src/exec_strategy_windows/launch.rs</files>
  <read_first>
    .planning/templates/cross-target-verify-checklist.md (full file — the exact commands, the
    anti-patterns section, e.g. Anti-pattern 6's `cargo zigbuild` vs `cargo-zigbuild` distinction)
  </read_first>
  <action>
    Run `cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D
    clippy::unwrap_used` and `cargo-zigbuild clippy --workspace --target x86_64-apple-darwin -- -D
    warnings -D clippy::unwrap_used` (SDKROOT unset) against the full workspace. Fix any findings
    in the files this phase touched — do not suppress with `#[allow]`. Measure D-17/117-D-24's
    per-tool-call latency budget: invoke the `claude_code_hook.rs` path (or the closest available
    harness — e.g. `nono claude-code-hook` directly, or a scripted loop of `nono run` invocations
    representative of the hook's real spawn shape) N times (N ≥ 20 for a stable measurement) with
    receipt-emission enabled, and record wall-clock delta per invocation; compare against the
    project's "zero startup latency" constraint qualitatively (record the actual number — do not
    write "negligible" without the number beside it). Record the measured number as a doc comment
    on `apply_startup_attestation_gate` (extending the D-17 note Plan 118-07 Task 2 already added)
    or in this plan's SUMMARY.md, whichever is more durable — prefer the source doc comment so it
    survives independent of the SUMMARY.
  </action>
  <verify>
    <automated>cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used && cargo-zigbuild clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used</automated>
  </verify>
  <done>
    Both cross-target gates pass clean; a measured (not asserted) D-17 latency number is recorded
    in the source.
  </done>
  <acceptance_criteria>
    Both `cross clippy` and `cargo-zigbuild clippy` commands exit 0 against `--workspace`;
    `grep -n "D-17" crates/nono-cli/src/exec_strategy_windows/launch.rs` finds a line containing an
    actual numeric measurement (e.g. "measured Nms" or "measured N microseconds"), not the word
    "negligible" alone; if the measured number is materially bad (comparable to or exceeding
    existing per-tool-call overhead), the SUMMARY.md records this as a named trade-off with a
    fallback, per CONTEXT.md's explicit instruction ("If the measurement comes back bad, that
    becomes a recorded trade-off with a named fallback — not a silent design compromise").
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Record discretionary decisions (D-06 and others) in the sink module doc</name>
  <files>crates/nono-cli/src/receipt_sink.rs</files>
  <read_first>
    crates/nono-cli/src/receipt_sink.rs (Plan 118-05's output — the module this task's doc note
    extends)
  </read_first>
  <action>
    Extend `receipt_sink.rs`'s module doc with a "# Discretionary decisions (Phase 118 closeout)"
    section recording: (1) the sink location chosen (`%PROGRAMDATA%\nono\receipts` or
    `%LOCALAPPDATA%\nono\receipts`) and why, referencing the live labelability check Plan 118-05
    ran; (2) the retention/rotation policy chosen (or explicitly "none — unbounded growth,
    deferred" if none was implemented), with the invariant CONTEXT.md requires — rotation must
    never silently truncate a chain segment in a way that makes a retained receipt unverifiable —
    stated as a constraint on any FUTURE rotation implementation; (3) whether the coarse Windows
    Event Log pointer (D-06) was implemented at all, and if not, that its absence is a deliberate,
    justified choice (not an oversight) given its three conditional dependencies; (4) confirmation
    that `entry_path`/`token_arm` were included as first-class receipt fields (per Plan 118-01's
    design), closing that discretion item explicitly.
  </action>
  <verify>
    <automated>cargo doc -p nono-cli --no-deps 2>&1 | grep -i "error" | wc -l</automated>
  </verify>
  <done>
    The module doc contains all four recorded decisions; `cargo doc` builds without new errors.
  </done>
  <acceptance_criteria>
    `grep -n "Discretionary decisions" crates/nono-cli/src/receipt_sink.rs` finds the new section;
    it contains the literal string "D-06" and addresses the Event Log pointer question explicitly
    (either "implemented" or "deliberately not implemented" — not silence).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confined-child sink-guard verification (D-08) — real host, not a unit test</name>
  <files></files>
  <read_first>
    crates/nono-cli/src/receipt_sink.rs (Plan 118-05's ensure_sink_guarded — the guard under test)
    .planning/phases/118-per-session-enforcement-receipts/118-VALIDATION.md (Manual-Only
    Verifications table — the exact scope this checkpoint covers)
  </read_first>
  <action>
    Launch a confined session and, from inside the confined child (or via an equivalent
    token-impersonation check), attempt to read and write the receipt sink directory on every
    named arm (restricted-token, broker Medium-IL, no-session-SID) per the numbered steps in
    `<how-to-verify>` below, then confirm the supervisor itself can still read/write it normally.
    This cannot be proven by a unit test because the supervisor and the confined child run as the
    same Windows user (D-08) — only a real host, real confined-process check demonstrates the
    guard actually holds under the constraint that makes it hard in the first place.
  </action>
  <verify>
    <human-check>All five checks in how-to-verify pass; the operator's typed response is the
    pass/fail signal for this task, not an automated command.</human-check>
  </verify>
  <done>
    Read and write are both denied to a confined child on every named arm, and the supervisor's
    own read/write access to the sink is unaffected.
  </done>
  <acceptance_criteria>
    Read access to the sink from the confined child returns ACCESS_DENIED (or equivalent denial)
    on the restricted-token arm and on the broker Medium-IL arm; write access is likewise denied
    on both arms; the supervisor's own process can still read and write the sink directory
    without modification; the operator's typed resume-signal response is recorded verbatim in
    this plan's SUMMARY.md as the evidence of the check having run.
  </acceptance_criteria>
  <what-built>
    A receipt sink directory guarded by a DENY ACE (session SID + package SID) AND a NO_READ_UP
    mandatory label, applied on every launch by `receipt_sink.rs::ensure_sink_guarded` (nono.exe/
    nono-agentd.exe) and the broker's own hand-rolled equivalent (Plan 118-06).
  </what-built>
  <how-to-verify>
    1. Launch a confined session on this host (`nono run` with a simple command, e.g. `nono run --
       cmd /c echo hi` or the project's standard smoke-test invocation) and, from a script running
       AS the confined child (or by inspecting the child's effective token permissions against the
       sink path), attempt to READ a file in the receipt sink directory (e.g.
       `%PROGRAMDATA%\nono\receipts\<any-existing-session>.jsonl`) — this must be DENIED.
    2. From the same confined child, attempt to WRITE a new file into the receipt sink directory —
       this must be DENIED.
    3. Repeat steps 1-2 for the broker arm specifically (a Medium-IL child, spawned via the
       `--no-pty`/AppContainer broker path) — since the mandatory label alone does not cover a
       Medium-IL child, the DENY ACE must be what catches this arm.
    4. Repeat for an arm where no per-session SID was minted (if such an arm currently exists in
       this codebase's launch paths) — the mandatory label must be what catches this arm.
    5. Confirm the SUPERVISOR itself (this Claude session, or an operator shell) CAN still read and
       write the sink directory normally (the guard must not lock out the legitimate writer).
  </how-to-verify>
  <resume-signal>
    Type "approved" if all four denial checks (read-deny, write-deny, broker-arm-deny,
    no-session-SID-arm-deny) and the supervisor-can-still-write check pass, or describe which
    check failed and its exact error/behavior.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This plan's own claims → reality | A closeout plan that asserts "the sink is guarded" without a real confined-child test would be exactly the "reports success while structurally incapable of reporting failure" anti-pattern this phase exists to eliminate — hence Task 3 is a blocking human checkpoint, not an automated self-check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-30 | Information Disclosure | Sink guard failing silently on ONE arm (e.g. broker) while appearing to work on another | mitigate | Task 3 explicitly tests all named arms separately (restricted-token, broker Medium-IL, no-session-SID) rather than accepting a single passing case as sufficient — Finding 1's "predicate width, not just placement" lesson applied to verification, not just to code. |
| T-118-31 | Denial of Service | The guard also locking out the legitimate supervisor writer | mitigate | Task 3's step 5 explicitly checks the supervisor can still write — a guard that is TOO restrictive is also a defect, not just one that is too permissive. |
| T-118-32 | Tampering | Cross-target clippy findings suppressed with `#[allow]` instead of fixed, to make Task 1's gate pass under time pressure | mitigate | Task 1's action explicitly forbids `#[allow]` suppression; `/gsd:code-review` (D-22) is expected to run on this phase and would catch a suppressed finding that slipped through. |
</threat_model>

<verification>
Both cross-target clippy gates exit 0 against `--workspace`. `cargo fmt --check` clean across the
whole repo. `make ci` green **modulo the 11 documented pre-existing `-p nono-cli`/`-p nono`
test failures on this Windows host that also fail at phase-base** (per this phase's known
baseline, 118-VALIDATION.md's Test Infrastructure section) -- compare any red against
phase-base before treating it as a regression; no work in this phase is budgeted to fix
those 11 documented failures. The confined-child checkpoint is explicitly approved before
this plan (and the phase) is considered complete.
This phase should be followed by `/gsd:code-review`
per D-22 before verification — note this explicitly in the phase-completion handoff. No SDK
STATE.md/ROADMAP.md writers were invoked during this phase's planning or execution (D-24);
`REQUIREMENTS.md`/`ROADMAP.md` remain hand-appended, never overwritten, consistent with the two
parallel-open-milestone constraint.
</verification>

<success_criteria>
Both cross-target clippy gates pass clean on the full workspace. D-17's latency is a measured
number in the source, not an assertion. D-08's sink guard is verified against a real confined
child on every named arm, with an explicit human approval. All discretionary decisions from
CONTEXT.md are recorded in a durable doc comment. D-22 (code-review) and D-24 (no SDK writers) are
confirmed as process facts in this plan's verification section.
</success_criteria>

<output>
Create `.planning/phases/118-per-session-enforcement-receipts/118-10-SUMMARY.md` when done
</output>
