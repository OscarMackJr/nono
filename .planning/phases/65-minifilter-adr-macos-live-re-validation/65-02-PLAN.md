---
phase: 65-minifilter-adr-macos-live-re-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/65-minifilter-adr-macos-live-re-validation/65-MACOS-CI-EVIDENCE.md
autonomous: false
requirements: [MACOS-03]
must_haves:
  truths:
    - "The macOS deny-after-allow ordering invariant is re-confirmed by the existing sandbox::macos unit tests (read<write<deny, last-match-wins)"
    - "x86_64-apple-darwin clippy was attempted from the dev host; clean -> recorded, link/toolchain error -> marked PARTIAL with the green macos-latest CI SHA as the decisive signal (D-11a fallback authorized by the cross-target checklist)"
    - "The Phase-64 macOS cherry-picks were scanned for edition-2024 let-chains, E0716-class borrows, and canonical-path (/private/etc, /private/tmp) dual-path coverage (D-11b)"
    - "A green macos-latest CI run link/SHA is captured as LITERAL gate evidence; no release tag may be cut before it (D-11, D-11c — the HARD gate, not advisory)"
  artifacts:
    - path: ".planning/phases/65-minifilter-adr-macos-live-re-validation/65-MACOS-CI-EVIDENCE.md"
      provides: "Cross-target clippy disposition (clean/PARTIAL), cherry-pick checklist scan results, and the green macos-latest CI run link/SHA as the HARD-gate evidence"
      contains: "macos-latest"
  key_links:
    - from: "crates/nono/src/sandbox/macos.rs ordering tests"
      to: "macos-latest CI test job (ci.yml)"
      via: "cargo test --workspace on the macOS runner"
      pattern: "test_generate_profile_platform_rules_after_writes"
    - from: ".planning/templates/cross-target-verify-checklist.md"
      to: "65-MACOS-CI-EVIDENCE.md"
      via: "PARTIAL disposition + CI-SHA-as-evidence rule"
      pattern: "macos-latest"
---

<objective>
Re-validate the already-landed macOS Seatbelt code (Phase 63/64) through every channel that does NOT require a macOS host, and capture the green `macos-latest` CI SHA as the literal HARD-gate evidence (MACOS-03, D-10 automatable subset + D-11).

Purpose: The v2.9 regression shipped two cfg-gated compile errors to release tags because the Windows host never compiles macOS code. D-11 makes a green `macos-latest` CI run mandatory, evidenced, and tag-blocking. This plan covers everything provable without a macOS host; the live `sandbox_init()` assertions are gate 65-A (plan 65-04).
Output: A `65-MACOS-CI-EVIDENCE.md` artifact recording the cross-target clippy disposition, the cherry-pick checklist scan, and the green `macos-latest` CI run link/SHA. No source code change is expected — Phase 65 re-validates, it does not modify `macos.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/65-minifilter-adr-macos-live-re-validation/65-PATTERNS.md
@.planning/phases/65-minifilter-adr-macos-live-re-validation/65-RESEARCH.md
@crates/nono/src/sandbox/macos.rs
@.planning/templates/cross-target-verify-checklist.md
@.planning/phases/64-minifilter-spike-implementation-macos-p1-cherry-pick-wave/64-VERIFICATION.md
@CLAUDE.md

<interfaces>
<!-- Existing macOS ordering tests the macOS CI test job runs (no new test needed): -->
<!-- crates/nono/src/sandbox/macos.rs: -->
<!--   test_generate_profile_platform_rules_after_writes (line ~997) — read_pos < write_pos < deny_pos -->
<!--   test_platform_rules_after_write_allows (line ~1880) — same ordering invariant -->
<!--   test_platform_deny_symlink_and_canonical_path (line ~1918) — both /etc/passwd AND /private/etc/passwd deny rules -->
<!--   test_generate_profile_extensions_before_platform_deny_rules (line ~1128) — extension allows precede platform denies -->
<!-- ci.yml: test job matrix [ubuntu-latest, macos-latest] (lines 90-125); clippy job matrix (lines 404-427) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run sandbox::macos ordering tests + attempt x86_64-apple-darwin clippy + scan cherry-pick checklist</name>
  <read_first>
    - crates/nono/src/sandbox/macos.rs (the re-validation target — deny-after-allow ordering lines ~700-716, dual-path emission ~239-246, ordering tests ~997-1031 + 1880-1943; READ-ONLY, no edit expected)
    - .planning/templates/cross-target-verify-checklist.md (the decision tree: clean clippy -> may flip; link/toolchain error -> PARTIAL + require green macos-latest CI SHA)
    - .planning/phases/64-minifilter-spike-implementation-macos-p1-cherry-pick-wave/64-VERIFICATION.md (the macOS cherry-pick set 8f1b0b74, 362ada22, 8f84d454 — the commits to scan)
    - CLAUDE.md § Coding Standards (the cross-target clippy MUST/NEVER rule)
  </read_first>
  <action>
    Run the `sandbox::macos` ordering tests: `cargo test -p nono sandbox::macos` (on the Windows dev host the macOS-gated tests compile-skip, but record the result; the contract is in-tree and the macOS CI job runs them natively). Record whether `test_generate_profile_platform_rules_after_writes`, `test_platform_rules_after_write_allows`, `test_platform_deny_symlink_and_canonical_path`, and `test_generate_profile_extensions_before_platform_deny_rules` are present and pass on their native target.

    Attempt the cross-target clippy gate (D-11a): `rustup target add x86_64-apple-darwin` (one-time) then `cargo clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used`. If it exits clean, record CLEAN. If it fails to link (keyring/ring/aws-lc-sys C-linking from the Windows host — Open Question 2), mark the clippy leg PARTIAL per the cross-target checklist and rely on the green `macos-latest` CI SHA as the decisive signal (do NOT chase the link error — the checklist explicitly authorizes PARTIAL).

    Scan the Phase-64 macOS cherry-picks (8f84d454, 362ada22, 8f1b0b74) for the three drift classes the v2.9 regression proved: (a) edition-2024 let-chains (`if let ... &&`), (b) E0716-class temporary-borrow patterns, (c) canonical-path dual coverage — confirm both `/etc` AND `/private/etc`, both `/tmp` AND `/private/tmp` are emitted (lines ~239-246 in macos.rs). Record each scan result as pass/flag in the evidence artifact.

    Do NOT modify `macos.rs` — this is a re-validation, not a code change. If the scan surfaces a genuine drift defect, STOP and surface it (it would be a Phase-64 escape, not in-scope here).
  </action>
  <verify>
    <automated>cargo test -p nono sandbox::macos --no-run 2>&amp;1 | grep -v '^#' ; AND grep -c "test_generate_profile_platform_rules_after_writes\|test_platform_deny_symlink_and_canonical_path" crates/nono/src/sandbox/macos.rs (expect &gt;= 2 — the ordering + dual-path contracts are in-tree)</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p nono sandbox::macos` recorded (pass on native macOS target; compile-skip noted on Windows dev host)
    - `crates/nono/src/sandbox/macos.rs` contains both `test_generate_profile_platform_rules_after_writes` and `test_platform_deny_symlink_and_canonical_path` (ordering + dual-path contracts present)
    - x86_64-apple-darwin clippy result recorded as CLEAN or PARTIAL (PARTIAL is acceptable per the cross-target checklist when the Windows host cannot link)
    - The three cherry-pick scan classes (let-chains, E0716 borrows, /private dual-path) each recorded pass/flag
    - `crates/nono/src/sandbox/macos.rs` is unmodified (`git diff --quiet crates/nono/src/sandbox/macos.rs`)
  </acceptance_criteria>
  <done>Ordering tests confirmed present and green on their native target; cross-target clippy attempted and dispositioned; cherry-pick checklist scanned with no new drift defect.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Capture the green macos-latest CI run SHA as the HARD-gate evidence (D-11c)</name>
  <read_first>
    - .github/workflows/ci.yml (the `test` job matrix incl. macos-latest, lines 90-125; the `clippy` job matrix incl. macos-latest, lines 404-427 — the legs that must be green)
    - .planning/templates/cross-target-verify-checklist.md (the CI-SHA-as-evidence rule — the decisive signal when the dev host cannot cross-compile)
    - .planning/phases/64-minifilter-spike-implementation-macos-p1-cherry-pick-wave/64-VERIFICATION.md (the precedent for recording a close-gated CI/cross-target follow-up)
  </read_first>
  <action>Locate/trigger a CI run on the phase HEAD (`gh run list --workflow ci.yml`; `gh run view`), confirm the `Test (macos-latest)` and `clippy (macos-latest)` legs are both green (`conclusion: success`), and record the run URL + commit SHA into `65-MACOS-CI-EVIDENCE.md` as the literal D-11c HARD-gate evidence with an explicit "no release tag before this green SHA" note.</action>
  <what-built>Task 1 ran the local/cross-target checks. CI is the decisive signal for the macOS legs (the Windows host cannot compile macOS code — Pitfall 4 / the exact v2.9 regression). This task captures a green `macos-latest` `test` + `clippy` run link/SHA as the literal HARD-gate evidence. Push to a branch / use `gh run list`/`gh run view` to locate the run; `gh` is available in this environment.</what-built>
  <how-to-verify>
    1. Trigger / locate a CI run on the phase HEAD: `gh run list --workflow ci.yml --branch &lt;phase-branch&gt;` then `gh run view &lt;run-id&gt;`.
    2. Confirm the `Test (macos-latest)` job AND the `clippy (macos-latest)` matrix leg are both green (not skipped, not the Windows legs).
    3. Record the run URL + the commit SHA + the run conclusion (`success`) into `.planning/phases/65-minifilter-adr-macos-live-re-validation/65-MACOS-CI-EVIDENCE.md` as the literal gate evidence.
    4. Note explicitly: NO release tag may be cut before this green SHA (the HARD gate, D-11c).
  </how-to-verify>
  <resume-signal>Type "approved" with the green macos-latest run URL + SHA, or describe the red leg.</resume-signal>
  <acceptance_criteria>
    - `65-MACOS-CI-EVIDENCE.md` contains a `macos-latest` CI run URL + commit SHA + `conclusion: success` for BOTH the `test` and `clippy` legs
    - The artifact states "no release tag before this green SHA" (the D-11c HARD gate)
    - The artifact records the cross-target clippy disposition (CLEAN or PARTIAL) and the cherry-pick scan results from Task 1
  </acceptance_criteria>
  <done>A green `macos-latest` CI SHA is captured as literal, tag-blocking evidence; the macOS automatable subset of MACOS-03 is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CapabilitySet → Seatbelt profile string | `macos.rs` transforms capabilities into allow/deny rules; deny-ordering correctness is a sandbox-escape boundary |
| dev host → release tag | cross-target compile drift can ship broken security code to a tag (the v2.9 failure) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-ORDER | EoP (sandbox escape) | macos.rs deny-after-allow ordering | mitigate | Last-match-wins ordering (8f84d454) asserted by `test_generate_profile_platform_rules_after_writes` (read<write<deny); re-confirmed green on the macOS test job |
| T-65-DUALPATH | EoP (sandbox escape) | macos.rs `/etc` ↔ `/private/etc` dual emission | mitigate | `test_platform_deny_symlink_and_canonical_path` asserts both forms; cherry-pick scan confirms `/private/etc` + `/private/tmp` coverage |
| T-65-DRIFT | Tampering (ships broken security code) | cross-target compile to release tag | mitigate | Cross-target checklist scan (let-chains, E0716) + green `macos-latest` CI SHA captured as tag-blocking evidence (D-11) |
| T-65-FALSEGREEN | Tampering | Windows-host `cargo check` mistaken for macOS validation | mitigate | D-11c: only a green `macos-latest` CI SHA is decisive; Windows-host checks are recorded but NOT the gate (Pitfall 4) |
| T-65-SC | Tampering | npm/pip/cargo installs | accept | Only `rustup target add x86_64-apple-darwin` (toolchain component, not a registry package); no Package Legitimacy Audit required |
</threat_model>

<verification>
- `cargo test -p nono sandbox::macos` ordering tests present + green on the macOS target.
- x86_64-apple-darwin clippy dispositioned (CLEAN or PARTIAL per the cross-target checklist).
- Cherry-pick checklist scanned (let-chains, E0716, `/private` dual-path).
- Green `macos-latest` CI run URL + SHA captured in `65-MACOS-CI-EVIDENCE.md` (D-11c); no tag before it.
- `macos.rs` unmodified.
</verification>

<success_criteria>
- The macOS automatable subset of MACOS-03 is green and evidenced (D-10 automatable, D-11a/b/c).
- The HARD CI gate is captured as a literal tag-blocking SHA (D-11c).
- No source change to `macos.rs` (re-validation only).
</success_criteria>

<output>
Create `.planning/phases/65-minifilter-adr-macos-live-re-validation/65-02-SUMMARY.md` when done.
Also create the evidence artifact `.planning/phases/65-minifilter-adr-macos-live-re-validation/65-MACOS-CI-EVIDENCE.md`.
</output>
