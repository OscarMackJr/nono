---
phase: 65-minifilter-adr-macos-live-re-validation
plan: 04
type: execute
wave: 2
depends_on: ["65-02"]
files_modified:
  - .planning/phases/65-minifilter-adr-macos-live-re-validation/65-HUMAN-UAT.md
autonomous: false
requirements: [MACOS-03]
must_haves:
  truths:
    - "A close-blocking HUMAN-UAT checklist (gate 65-A) stages the four live sandbox_init() deny assertions for a real macOS host (D-10 live subset)"
    - "nono run --dry-run --profile claude-code emits a profile where deny rules appear AFTER the allow rules they override (deny-after-allow, last-match-wins)"
    - "nono run --profile claude-code -- cat ~/.ssh/id_rsa is blocked (not the key contents)"
    - "BOTH /etc/hosts AND /private/etc/hosts are blocked (dual-path canonical coverage)"
    - "make test-lib is green on the macOS host"
    - "The checklist stays OPEN/blocking until a real macOS host runs it; it references the green macos-latest CI SHA (plan 65-02) in its sign-off"
  artifacts:
    - path: ".planning/phases/65-minifilter-adr-macos-live-re-validation/65-HUMAN-UAT.md"
      provides: "Close-blocking gate-65-A checklist: four live deny assertions with per-item pass/blocked field + host/date stamp + final PASS/FAIL line"
      contains: "id_rsa"
  key_links:
    - from: ".planning/phases/65-minifilter-adr-macos-live-re-validation/65-HUMAN-UAT.md"
      to: "crates/nono/src/sandbox/macos.rs deny-after-allow + dual-path behavior"
      via: "the live assertions confirm at runtime what the unit tests assert at compile time"
      pattern: "private/etc/hosts"
    - from: ".planning/phases/65-minifilter-adr-macos-live-re-validation/65-HUMAN-UAT.md sign-off"
      to: ".planning/phases/65-minifilter-adr-macos-live-re-validation/65-MACOS-CI-EVIDENCE.md"
      via: "the gate references the green macos-latest CI SHA (D-11c) alongside the live assertions"
      pattern: "macos-latest"
---

<objective>
Stage and (host permitting) execute the macOS live `sandbox_init()` HUMAN-UAT — the host-gated subset of MACOS-03 that BLOCKS phase close (gate 65-A, D-10 live).

Purpose: The unit tests and CI prove the Seatbelt profile *generation* contract, but only a real macOS host proves the OS actually *enforces* the denies. The four live assertions (dry-run ordering, SSH-key block, `/etc/hosts` + `/private/etc/hosts` dual block, `make test-lib` green) are the runtime confirmation. No macOS host is confirmed available — the gate stays OPEN/blocking until one runs it.
Output: `.planning/phases/65-minifilter-adr-macos-live-re-validation/65-HUMAN-UAT.md` — a close-blocking checklist mirroring the prior Windows HUMAN-UAT pattern (per-item pass/blocked, host/date stamp, raw-output paste blocks, final PASS/FAIL line).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/65-minifilter-adr-macos-live-re-validation/65-PATTERNS.md
@.planning/phases/65-minifilter-adr-macos-live-re-validation/65-RESEARCH.md
@.planning/phases/65-minifilter-adr-macos-live-re-validation/65-MACOS-CI-EVIDENCE.md
@crates/nono/src/sandbox/macos.rs
@.planning/phases/64-minifilter-spike-implementation-macos-p1-cherry-pick-wave/64-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the gate-65-A HUMAN-UAT checklist (close-blocking)</name>
  <read_first>
    - .planning/phases/65-minifilter-adr-macos-live-re-validation/65-RESEARCH.md (§ macOS UAT Sequencing — the four exact assertions + expected outputs; § Code Examples — the live deny commands)
    - .planning/phases/65-minifilter-adr-macos-live-re-validation/65-PATTERNS.md (the HUMAN-UAT-gated-close pattern + the live-UAT→unit-test mapping + the 64-SC1 evidence-template shape)
    - .planning/phases/64-minifilter-spike-implementation-macos-p1-cherry-pick-wave/64-VERIFICATION.md (the `## Human-verification` precedent — prior UAT lived in verification artifacts; there is NO `*HUMAN-UAT*.md` precedent file, so this establishes the shape)
    - .planning/phases/65-minifilter-adr-macos-live-re-validation/65-MACOS-CI-EVIDENCE.md (the green macos-latest CI SHA from plan 65-02 to reference in the sign-off)
  </read_first>
  <action>
    Create `.planning/phases/65-minifilter-adr-macos-live-re-validation/65-HUMAN-UAT.md` as a close-blocking checklist (gate 65-A) with four assertion items, each with a command, an expected output, a `Result: [ ] pass / [ ] blocked` field, a host/date/macOS-version stamp slot, and a raw-output paste block (64-SC1 evidence-template shape):
    1. Dry-run ordering: `nono run --dry-run --profile claude-code` → expect `file-write*` allow rules emitted, THEN `platform (deny ...)` lines AFTER them (deny-after-allow, last-match-wins — mirrors `macos.rs` lines ~1026-1030).
    2. SSH key blocked: `nono run --profile claude-code -- cat ~/.ssh/id_rsa` → expect non-zero exit + "Operation not permitted" / sandbox deny (NOT the key contents).
    3. Dual-path block: `nono run --profile claude-code -- cat /etc/hosts` AND `nono run --profile claude-code -- cat /private/etc/hosts` → BOTH blocked (mirrors `test_platform_deny_symlink_and_canonical_path`).
    4. Library tests: `make test-lib` (`cargo test -p nono`) → expect all `sandbox::macos` tests pass on the host.

    Add a sign-off section: a final `PASS / FAIL` line, the host/date stamp, AND a reference to the green `macos-latest` CI SHA from `65-MACOS-CI-EVIDENCE.md` (the D-11c HARD gate runs alongside this live gate). State explicitly that this checklist BLOCKS phase close until run on a real macOS host, and that NO macOS host is confirmed available at authoring time (the gate ships OPEN). Do NOT mark any item `pass` without real host output.
  </action>
  <verify>
    <automated>grep -n "id_rsa\|/private/etc/hosts\|--dry-run --profile claude-code\|make test-lib\|gate 65-A\|macos-latest" .planning/phases/65-minifilter-adr-macos-live-re-validation/65-HUMAN-UAT.md (expect all four assertions + the CI-SHA reference present)</automated>
  </verify>
  <acceptance_criteria>
    - `65-HUMAN-UAT.md` exists with four assertion items (dry-run ordering, `~/.ssh/id_rsa` block, `/etc/hosts` + `/private/etc/hosts` dual block, `make test-lib`), each with a command, expected output, and a pass/blocked field
    - Item 3 contains BOTH `/etc/hosts` AND `/private/etc/hosts` (dual-path coverage)
    - The sign-off references the green `macos-latest` CI SHA from `65-MACOS-CI-EVIDENCE.md`
    - The artifact states it BLOCKS phase close and ships OPEN (no host confirmed); no item is pre-marked `pass`
  </acceptance_criteria>
  <done>The four live deny assertions are staged as a close-blocking, host-stamped checklist that references the CI HARD-gate evidence.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Execute the gate-65-A live assertions on a real macOS host</name>
  <read_first>
    - .planning/phases/65-minifilter-adr-macos-live-re-validation/65-HUMAN-UAT.md (the checklist authored in Task 1 — the commands + expected outputs to run)
    - .planning/phases/65-minifilter-adr-macos-live-re-validation/65-RESEARCH.md (§ Environment Availability — no macOS host confirmed; this gate has NO automatable substitute)
  </read_first>
  <action>On a real macOS host, run the four staged assertions (`nono run --dry-run --profile claude-code` deny-after-allow; `cat ~/.ssh/id_rsa` blocked; `cat /etc/hosts` AND `cat /private/etc/hosts` both blocked; `make test-lib` green), paste raw output into each item of `65-HUMAN-UAT.md`, fill the host/date/macOS-version stamp, and write the final PASS/FAIL line. If no macOS host is available, record gate 65-A as `blocked: no macOS host` (close-blocking — do NOT flip on Windows/CI evidence).</action>
  <what-built>Task 1 staged the checklist. This step requires a real macOS host running `sandbox_init()` — there is NO CI or Windows-host substitute (CI runs the unit tests but does NOT execute the live `nono run -- cat ~/.ssh/id_rsa` deny path). If no macOS host is available, this gate stays OPEN and BLOCKS phase close; record it as a host-availability blocker.</what-built>
  <how-to-verify>
    On a real macOS host with the workspace built:
    1. `nono run --dry-run --profile claude-code` → confirm deny rules appear AFTER the write-allows; paste output.
    2. `nono run --profile claude-code -- cat ~/.ssh/id_rsa` → confirm non-zero exit + deny (NOT the key); paste output.
    3. `nono run --profile claude-code -- cat /etc/hosts` AND `nono run --profile claude-code -- cat /private/etc/hosts` → confirm BOTH blocked; paste both outputs.
    4. `make test-lib` → confirm all `sandbox::macos` tests pass; paste the summary line.
    5. Fill each `Result` field + the host/date/macOS-version stamp in `65-HUMAN-UAT.md`; write the final PASS/FAIL line.
  </how-to-verify>
  <resume-signal>Type "approved" with the four pasted results + host/macOS-version stamp, or "blocked: no macOS host" to keep gate 65-A open.</resume-signal>
  <acceptance_criteria>
    - Each of the four assertions in `65-HUMAN-UAT.md` has a `Result: pass` with pasted raw output AND a host/date/macOS-version stamp — OR the gate is explicitly recorded as `blocked: no macOS host` (close-blocking)
    - `~/.ssh/id_rsa` output shows a deny, NOT the key contents
    - BOTH `/etc/hosts` and `/private/etc/hosts` show blocked
    - `make test-lib` summary line shows all `sandbox::macos` tests passing
  </acceptance_criteria>
  <done>The four live `sandbox_init()` deny assertions are confirmed on a real macOS host (gate 65-A PASS) — or the gate is recorded OPEN/blocked pending a host.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sandboxed `nono run` child → host filesystem | the live deny path; an enforcement gap here is a real sandbox escape (SSH key / hosts-file read) |
| profile generation (compile-time contract) → OS enforcement (runtime) | the unit tests prove generation; only the live host proves the OS enforces the denies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-LIVE-ORDER | EoP (sandbox escape) | live deny-after-allow enforcement | mitigate | Assertion 1 confirms `nono run --dry-run` emits deny-after-allow at runtime (mirrors `macos.rs` lines 1026-1030); blocks close until confirmed on a host |
| T-65-LIVE-SSH | Information disclosure | `~/.ssh/id_rsa` read | mitigate | Assertion 2 confirms the SSH key read is blocked at runtime (deny, not key contents) |
| T-65-LIVE-DUAL | EoP (sandbox escape via symlink) | `/etc` ↔ `/private/etc` canonical path | mitigate | Assertion 3 confirms BOTH `/etc/hosts` and `/private/etc/hosts` are blocked (dual-path; macOS `/etc` is a symlink to `/private/etc`) |
| T-65-NOHOST | Coverage gap (false VERIFIED) | gate 65-A | mitigate | The gate has NO automatable substitute; it BLOCKS close and ships OPEN if no host is available (not flipped on Windows/CI evidence — Pitfall 4) |
| T-65-SC | Tampering | npm/pip/cargo installs | accept | No package installs (uses existing `nono run` CLI + `make test-lib`) |
</threat_model>

<verification>
- `65-HUMAN-UAT.md` stages the four live deny assertions with pass/blocked fields + host stamp + final PASS/FAIL line.
- Item 3 covers BOTH `/etc/hosts` and `/private/etc/hosts`.
- Sign-off references the green `macos-latest` CI SHA (D-11c).
- Gate 65-A blocks phase close; ships OPEN until a real macOS host runs it (no host confirmed at authoring).
</verification>

<success_criteria>
- The host-gated subset of MACOS-03 is staged as a close-blocking checklist (gate 65-A, D-10 live).
- Live `sandbox_init()` enforcement confirmed on a real macOS host — or the gate recorded OPEN/blocked pending a host.
- The live gate is tied to the CI HARD gate (D-11c) in the sign-off.
</success_criteria>

<output>
Create `.planning/phases/65-minifilter-adr-macos-live-re-validation/65-04-SUMMARY.md` when done.
If no macOS host is available, record gate 65-A as OPEN/blocking in the SUMMARY (phase cannot close until it runs).
</output>
