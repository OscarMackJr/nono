---
phase: 65-minifilter-adr-macos-live-re-validation
plan: 03
type: execute
wave: 2
depends_on: ["65-01"]
files_modified:
  - .planning/architecture/adr-65-minifilter-go-no-go.md
  - .planning/architecture/adr-65-latency-appendix.md
autonomous: false
requirements: [DRV-04]
must_haves:
  truths:
    - "A single go/no-go ADR covers all SIX DRV-04 topics as sections: interception design, measured latency + finite-timeout fail-open, windows-drivers-rs-not-viable, FltMgr-vs-ETW rationale, chosen altitude 365678 + official-assignment status, explicit go/no-go recommendation (D-09)"
    - "The verdict is derived from evidence and NOT pre-committed (D-05); the recommendation is surfaced for Oscar's human review, not silently locked (D-06)"
    - "The ADR ships Status: Proposed until human sign-off (D-06)"
    - "The ADR lives in .planning/architecture/ (repo convention) NOT the SC2 literal .planning/adr/ — and the path deviation is noted so the close gate does not flag a wrong-path miss (D-07)"
    - "Raw latency tables live in a sibling appendix file; the core ADR references DESIGN.md + 64-SC1 evidence rather than duplicating them (D-08)"
  artifacts:
    - path: ".planning/architecture/adr-65-minifilter-go-no-go.md"
      provides: "Concise go/no-go decision ADR with the six DRV-04 sections + a per-option verdict scoring table"
      contains: "## Go/No-Go Recommendation"
    - path: ".planning/architecture/adr-65-latency-appendix.md"
      provides: "Raw per-span min/median/p99 latency tables, iteration counts, QPC frequency, VM context"
      contains: "SPAN-A"
  key_links:
    - from: ".planning/architecture/adr-65-minifilter-go-no-go.md Measured Latency section"
      to: ".planning/architecture/adr-65-latency-appendix.md"
      via: "the ADR links the appendix for raw data (D-08)"
      pattern: "adr-65-latency-appendix"
    - from: ".planning/architecture/adr-65-latency-appendix.md"
      to: ".planning/phases/65-minifilter-adr-macos-live-re-validation/65-SC1-latency-evidence.md"
      via: "appendix tables sourced from the VM latency-run evidence (plan 65-01)"
      pattern: "SPAN-[AB]"
---

<objective>
Author the go/no-go ADR formalizing the Phase-64 minifilter spike verdict, backed by the freshly measured latency data from plan 65-01 (DRV-04 / SC1 + SC2, D-05..D-09).

Purpose: DRV-04 requires a committed go/no-go ADR. The verdict is NOT pre-committed — the evidence (measured latency + 18 spike defects + EV/WHQL cert cost + official altitude assignment + kernel-version maintenance burden, weighed against the WFP+AppContainer security gap) drives a *recommended* direction that Oscar reviews before it is final.
Output: `.planning/architecture/adr-65-minifilter-go-no-go.md` (concise, six DRV-04 sections, `Status: Proposed`) + `.planning/architecture/adr-65-latency-appendix.md` (raw latency tables). The recommendation is surfaced for human review.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/65-minifilter-adr-macos-live-re-validation/65-PATTERNS.md
@.planning/phases/65-minifilter-adr-macos-live-re-validation/65-RESEARCH.md
@.planning/phases/65-minifilter-adr-macos-live-re-validation/65-SC1-latency-evidence.md
@.planning/architecture/adr-58-windows-hook-executor.md
@.planning/architecture/v2.6-upstream-merge-deferral-ADR.md
@drivers/nono-fltmgr/DESIGN.md
@.planning/phases/64-minifilter-spike-implementation-macos-p1-cherry-pick-wave/64-SC1-driver-evidence.md
@.planning/phases/64-minifilter-spike-implementation-macos-p1-cherry-pick-wave/64-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the latency appendix (adr-65-latency-appendix.md)</name>
  <read_first>
    - .planning/phases/65-minifilter-adr-macos-live-re-validation/65-SC1-latency-evidence.md (the raw VM-run data — the source of truth for the appendix tables)
    - .planning/phases/65-minifilter-adr-macos-live-re-validation/65-PATTERNS.md (the suggested appendix shape: SPAN-A / SPAN-B tables with iterations/min/median/p99, header with VM context + QPC frequency, fail-open-envelope note)
    - .planning/architecture/adr-58-windows-hook-executor.md (the `.planning/architecture/` file-path-note footer idiom to copy — tail of the file)
  </read_first>
  <action>
    Create `.planning/architecture/adr-65-latency-appendix.md` as the sibling data file the core ADR references (D-08). Header: capture date, VM context (`nono-fltmgr-vm`, rg `rg-nono-fltmgr-spike`, `20.51.161.15`, Win11 26200), altitude `365678`, the measured QPC frequency, and the driver build note (VM-local `.sys`, T-63-05 — never committed). Two tables sourced from `65-SC1-latency-evidence.md`: SPAN-A (kernel-IPC round-trip, D-02a) and SPAN-B (full pre-op→IRP-completion, D-02b), each with iterations (~100), min, median, p99 — present in milliseconds here (convert from the in-driver integer-microsecond values). Add a Notes section explaining SPAN-A excludes ring-buffer enqueue + worker wakeup while SPAN-B includes them (scheduling jitter), and that both medians sit well under the 500ms `FltSendMessage` fail-open envelope (T-63-02). Append the `**File path note:**` footer (copy the adr-58 footer idiom) documenting the `.planning/architecture/` location per D-46-A2.
  </action>
  <verify>
    <automated>grep -c "SPAN-A\|SPAN-B" .planning/architecture/adr-65-latency-appendix.md (expect &gt;= 2) AND grep -n "median\|p99\|365678\|File path note" .planning/architecture/adr-65-latency-appendix.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/architecture/adr-65-latency-appendix.md` exists with SPAN-A and SPAN-B tables (iterations/min/median/p99) populated from `65-SC1-latency-evidence.md`
    - Header records altitude `365678`, the QPC frequency, and the VM context
    - Contains the `**File path note:**` footer documenting the `.planning/architecture/` location (D-07 / D-46-A2)
    - Does NOT duplicate `DESIGN.md` or the 64-SC1 evidence — references them (D-08)
  </acceptance_criteria>
  <done>The raw latency data lives in a sibling appendix the core ADR can link, keeping the decision narrative concise.</done>
</task>

<task type="auto">
  <name>Task 2: Write the core go/no-go ADR (adr-65-minifilter-go-no-go.md), Status: Proposed</name>
  <read_first>
    - .planning/architecture/adr-58-windows-hook-executor.md (the header block shape: Status/Date/Phase/Decision-IDs/Related → Context → Goals → Decision Table → Alternatives; and the `.planning/architecture/` file-path-note footer)
    - .planning/architecture/v2.6-upstream-merge-deferral-ADR.md (the per-option scoring-table shape for the go/no-go verdict — Decision Table lines ~38-42)
    - .planning/phases/65-minifilter-adr-macos-live-re-validation/65-PATTERNS.md (the six-topic→section map + the verdict scoring-table columns + the Status:Proposed/D-06 gate + the fail-direction-deferral note)
    - drivers/nono-fltmgr/DESIGN.md (interception design + STRIDE + fail-open rationale — to REFERENCE, not duplicate)
    - .planning/phases/64-minifilter-spike-implementation-macos-p1-cherry-pick-wave/64-SC1-driver-evidence.md (18 spike defects + altitude 365678 non-collision + deny chain — the "what the spike proved" backbone)
    - .planning/phases/64-minifilter-spike-implementation-macos-p1-cherry-pick-wave/64-CONTEXT.md (windows-drivers-rs-not-viable rationale)
    - .planning/REQUIREMENTS.md (DRV-04 acceptance text + DRV-PROD-01 deferral + the Out-of-Scope rows)
  </read_first>
  <action>
    Create `.planning/architecture/adr-65-minifilter-go-no-go.md` in the adr-58 shape. Header block: title "Minifilter Spike Go/No-Go (Gap 6b FltMgr feasibility verdict)"; `**Status:** Proposed` (D-06 — Proposed until Oscar's review, then flip to Accepted; do NOT ship Accepted); `**Date:**`; `**Phase:** 65 (minifilter-adr-macos-live-re-validation)`; `**Decision IDs:** D-01..D-09 (Phase 65 CONTEXT)`; `**Related:** drivers/nono-fltmgr/DESIGN.md, 64-SC1-driver-evidence.md, .planning/architecture/adr-65-latency-appendix.md`.

    Cover ALL SIX DRV-04 topics as sections (D-09), each REFERENCING source material, not duplicating it:
    1. Interception design — Context + a "How the spike intercepts" section (ref DESIGN.md ring-buffer+worker; `nono-fltmgr.c` deny chain).
    2. Measured Latency + finite-timeout fail-open — a "Measured Latency" section citing the SPAN-A/SPAN-B median+p99 and LINKING `adr-65-latency-appendix.md`; note the 500ms `FltSendMessage` fail-open envelope (T-63-02).
    3. `windows-drivers-rs`-not-viable — a Decision-Table row / Alternatives entry (ref 64-CONTEXT.md early-stage/KMDF-only; REQUIREMENTS.md Out-of-Scope).
    4. FltMgr-vs-ETW rationale — Decision-Table row / Alternatives (FltMgr enforces/deny vs ETW observes-only; EDR-INTEG-01 deferral).
    5. Altitude — a short "Altitude" section: chosen `365678` (FSFilter Activity-Monitor band, non-colliding per 64-SC1 `fltmc filters`), and the official Microsoft assignment (fsfcomm@microsoft.com) status as PENDING/not-yet-requested.
    6. Go/No-Go Recommendation — a "Decision" section with a per-option scoring table (copy the v2.6-ADR Decision-Table shape) with columns: Security gap closed vs WFP+AppContainer | Cert cost (EV/WHQL) | Maintenance burden (kernel-version) | Spike-defect signal (18 defects) | Verdict; rows Go / No-go / Conditional-go. Fill from evidence (D-05) — do NOT invent a pass/fail latency threshold (the measured number is ONE input among five). Then a "## Go/No-Go Recommendation" subsection stating the evidence-derived recommendation and marking it a HUMAN-review gate (D-06): surface it for Oscar, do not silently lock.

    Add a short "Non-goals / Deferred" note (mirror adr-58's Non-goals section): the production fail-open-vs-fail-closed decision is a SEPARATE production-ADR concern — mention it, do NOT decide it (CONTEXT Deferred). Append the `**File path note:**` footer documenting the `.planning/architecture/` location and explicitly noting this is the D-07 deviation from the SC2 literal `.planning/adr/` shorthand (so the close gate does not flag a wrong-path miss).
  </action>
  <verify>
    <automated>grep -n "Status:\*\* Proposed\|## Go/No-Go Recommendation\|365678\|adr-65-latency-appendix\|windows-drivers-rs\|ETW\|File path note" .planning/architecture/adr-65-minifilter-go-no-go.md (expect all present) AND grep "^\*\*Status:\*\*" .planning/architecture/adr-65-minifilter-go-no-go.md | grep -qv Accepted (the Status LINE must read Proposed not Accepted — the word "Accepted" may legitimately appear in the D-06 explanatory note, so check the Status line specifically, not the whole document)</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/architecture/adr-65-minifilter-go-no-go.md` exists with `**Status:** Proposed` (NOT Accepted — D-06)
    - All six DRV-04 sections present: interception design, Measured Latency (links the appendix), windows-drivers-rs-not-viable, FltMgr-vs-ETW, Altitude (`365678` + assignment status), and a `## Go/No-Go Recommendation`
    - A per-option verdict scoring table (Go / No-go / Conditional-go rows) weighing the five D-05 inputs against the WFP+AppContainer security gap; no invented latency threshold
    - The recommendation is explicitly flagged as a human-review gate (surface for Oscar, not locked — D-06)
    - Contains the `**File path note:**` footer noting the D-07 `.planning/architecture/` deviation from the SC2 `.planning/adr/` shorthand
    - References (does not duplicate) DESIGN.md + 64-SC1 evidence (D-08)
  </acceptance_criteria>
  <done>The committed ADR covers all six DRV-04 topics, links the latency appendix, ships Status: Proposed, and surfaces an evidence-derived recommendation for Oscar's review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike evidence → governance decision | the ADR is the decision record that gates DRV-PROD-01 (production driver); a wrong/locked verdict mis-allocates a cert-and-maintenance-heavy milestone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-VERDICT | Repudiation / wrong governance | ADR go/no-go recommendation | mitigate | Verdict derived from five evidence inputs (D-05), NOT pre-committed; surfaced as a human-review gate (D-06); ships `Status: Proposed` until Oscar signs off |
| T-65-PATH | Information disclosure (close-gate false miss) | ADR file location | mitigate | Written to `.planning/architecture/` per repo convention (D-07); the deviation from the SC2 `.planning/adr/` shorthand is documented in a file-path-note footer + verification |
| T-65-FAILDIR | Out-of-scope decision creep | production fail-open-vs-fail-closed | accept | Explicitly deferred to a future production ADR; the ADR mentions it but does NOT decide it (CONTEXT Deferred) |
| T-65-SC | Tampering | npm/pip/cargo installs | accept | No package installs (documentation-only plan) |
</threat_model>

<verification>
- `adr-65-minifilter-go-no-go.md` exists in `.planning/architecture/` with all six DRV-04 sections + `## Go/No-Go Recommendation`, `Status: Proposed`.
- `adr-65-latency-appendix.md` exists with SPAN-A/SPAN-B median+p99 tables sourced from `65-SC1-latency-evidence.md`.
- Verdict scoring table weighs the five D-05 inputs; no invented latency threshold.
- D-07 path deviation noted in the file-path-note footer.
- Recommendation surfaced as a human-review gate (D-06); not marked Accepted.
</verification>

<success_criteria>
- DRV-04 ADR committed covering all six topics (D-09), backed by measured latency (D-01..D-03 via the appendix).
- Verdict evidence-derived (D-05), human-review-gated (D-06), `Status: Proposed`.
- Located per repo convention with the deviation documented (D-07); raw data in the sibling appendix (D-08).
</success_criteria>

<output>
Create `.planning/phases/65-minifilter-adr-macos-live-re-validation/65-03-SUMMARY.md` when done.
Surface the ADR's go/no-go recommendation to Oscar for the D-06 human-review gate (do not silently lock it).
</output>
