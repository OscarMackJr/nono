---
phase: 82-fleet-deployment-infrastructure
plan: 04
type: execute
wave: 3
depends_on: ["82-01", "82-02", "82-03"]
files_modified:
  - scripts/gates/deploy-silent-install.ps1
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-03, DEPLOY-05, DEPLOY-06]
user_setup: []

must_haves:
  truths:
    - "`verify-dark.ps1 --gate deploy-silent-install` runs the new gate and emits a PASS/FAIL/SKIP verdict"
    - "The gate asserts silent install under SYSTEM context exits 0 or 3010"
    - "The gate asserts the first-run scratch is owned by the TARGET USER, not SYSTEM"
    - "The gate asserts a degraded-service path produces a non-zero `nono health` exit"
    - "The gate verifies TLS-through-proxy trust from PowerShell (CryptoAPI), Node.js, and nono-cli (rustls/native-certs), with the Node leg proving the PROVISIONED NODE_EXTRA_CA_CERTS reached node (not gate-set env-plumbing)"
    - "The gate RETURNS a verdict object — it never calls exit or Persist-Verdict (runner owns those)"
    - "A documented host-gated partial (SKIP_HOST_UNAVAILABLE leg recorded in detail) is an accepted close signal — the executor does not block waiting for a clean PASS"
  artifacts:
    - path: "scripts/gates/deploy-silent-install.ps1"
      provides: "Unattended deploy-silent-install dark-factory gate (Phase 82 close signal)"
      contains: "Test-Precondition"
  key_links:
    - from: "scripts/verify-dark.ps1"
      to: "scripts/gates/deploy-silent-install.ps1"
      via: "auto-discovery glob of scripts/gates/*.ps1"
      pattern: "Invoke-Gate"
    - from: "scripts/gates/deploy-silent-install.ps1"
      to: "nono health"
      via: "degraded-service path -> assert non-zero exit"
      pattern: "health"
---

<objective>
Add the `deploy-silent-install` dark-factory gate — the Phase 82 close signal (Dark Factory mandate
carried from v2.13). The gate is an unattended `scripts/verify-dark.ps1 --gate deploy-silent-install`
verdict covering all four success-criterion-5 checks: (1) silent install under SYSTEM context exits
0/3010; (2) the first-run scratch is owned by the TARGET USER not SYSTEM (Pitfall 4); (3) a
degraded-service path produces a non-zero `nono health` (Pitfall 5); (4) TLS-through-proxy trust
verified across PowerShell (CryptoAPI), Node.js, and nono-cli (rustls/native-certs) (Pitfall 13).

Purpose: This is the machine-readable proof that the whole Phase 82 spine works end-to-end on a single
dev host. Live clean-VM/fleet UAT remains operator-gated tech-debt; the scripted gate is the close
signal (carried Dark Factory standard) and an honest documented partial is a legitimate close. The gate
follows the two-function contract verbatim and is auto-discovered by the runner.
Output: `scripts/gates/deploy-silent-install.ps1`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-fleet-deployment-infrastructure/82-CONTEXT.md
@.planning/phases/82-fleet-deployment-infrastructure/82-PATTERNS.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the deploy-silent-install gate following the two-function verify-dark contract</name>
  <files>scripts/gates/deploy-silent-install.ps1</files>
  <read_first>
    - scripts/gates/clean-host-install.ps1 (the NEAR-EXACT template: contract header :1-27; `param($MsiPath = ...)` default :38-40; `Assert-True` throw-helper :49-59; `Test-Precondition` elevation→dirty-host→MSI-staged ordering :65-98; `Invoke-Gate` install + new-pwsh-session PATH proof + service-state-non-fatal + msiexec /x cleanup :100-188; WR-01 named-variable discipline)
    - scripts/verify-dark.ps1 (the runner — auto-discovery glob of scripts/gates/, exit mapping PASS=0/FAIL=2/SKIP=3/harness=4, persist-before-emit; the gate must NOT call exit or Persist-Verdict)
    - scripts/gates/wfp-egress-isolation.ps1 (the elevation-check two-line form referenced by clean-host :112-115; a degraded/non-zero-assertion gate for shape reference)
    - crates/nono-cli/src/health.rs (Plan 03 — the tri-state exit 0/1/2 contract the gate asserts against)
    - crates/nono-cli/src/provision_windows.rs (Plan 02 — the %LOCALAPPDATA%\nono\ scratch path + ownership semantics the gate inspects, AND how NODE_EXTRA_CA_CERTS is persisted: user-scope setx pointing at %PROGRAMDATA%\nono\nono-poc-root.pem — the Node TLS leg relies on this)
    - .planning/phases/82-fleet-deployment-infrastructure/82-PATTERNS.md (§"deploy-silent-install.ps1" :149-170 — what the new gate must add beyond clean-host: non-admin-target-user scratch ownership, degraded-service non-zero health, three-client TLS trust)
    - .planning/phases/82-fleet-deployment-infrastructure/82-CONTEXT.md (specifics :200-210 — exact gate coverage list)
    - .planning/research/PITFALLS.md (Pitfall 4 :89-112 scratch ownership; Pitfall 5 :116-143 degraded health; Pitfall 13 :340-363 three-client TLS matrix incl. the Node PEM/NODE_EXTRA_CA_CERTS leg)
  </read_first>
  <action>
    Create `scripts/gates/deploy-silent-install.ps1` exporting exactly the two contract functions, cloned structurally from `clean-host-install.ps1`:
    `Test-Precondition` → `$null` when ready, else a reason string for `SKIP_HOST_UNAVAILABLE`. Check in order: elevation (machine MSI needs admin); MSI artifact staged at the default `-MsiPath`; and any extra host capability the TLS-trust checks need (e.g. `node` on PATH → SKIP with a clear reason if absent, since the Node TLS leg cannot run without it). NEVER throw from `Test-Precondition` (a throw = exit 4).
    `Invoke-Gate` → one ordered verdict dict `@{ gate; verdict; reason; detail; timestamp }`, `verdict in {PASS|FAIL|SKIP_HOST_UNAVAILABLE}`. It MUST NOT call `exit` or `Persist-Verdict` (the runner owns those). Set `$ErrorActionPreference='Continue'`; assign every `Start-Process` result to a named variable (WR-01); no stray pipeline output. Orchestrate, recording each step into `detail`:
    (1) Silent install: `msiexec /i $MsiPath /quiet /norestart /l*v <fixed-log>`; `$installOk = ($exit -eq 0 -or $exit -eq 3010)`. Run under a SYSTEM/non-admin-target-user context per the CONTEXT specifics (use the established pattern; if a non-admin local test account / SYSTEM context cannot be created in this environment, record that leg as a documented partial in `detail` and keep the gate honest rather than faking it).
    (2) PATH propagation: `nono --version` from a NEW pwsh session (proves machine PATH, DEPLOY-02).
    (3) Scratch ownership (Pitfall 4 / DEPLOY-03): trigger a first `nono run` in the target USER context, then assert the `%LOCALAPPDATA%\nono\` scratch path is owned by THAT USER, not SYSTEM (inspect the owner via `Get-Acl`/`icacls` and compare the owner SID to the target user, NOT SYSTEM `S-1-5-18`). FAIL if SYSTEM-owned.
    (4) Degraded-service path (Pitfall 5 / DEPLOY-06): stop/disable `nono-wfp-service` (or otherwise force a degraded subsystem), run `nono health`, assert the exit code is NON-ZERO (1 or 2). FAIL if `nono health` returns 0 while a subsystem is degraded.
    (5) Three-client TLS-through-proxy trust (Pitfall 13 / DEPLOY-05): open a TLS connection through the nono proxy from (a) PowerShell (CryptoAPI / `Invoke-WebRequest`), (b) Node.js, (c) nono-cli itself (rustls/native-certs path). All three must succeed without TLS errors. FAIL if any client gets a trust error.
        For leg (b) Node.js (W3 — the gate must prove TRUST, not env-plumbing): the Plan 02 provisioner persists `NODE_EXTRA_CA_CERTS` at USER scope via `setx` (pointing at `%PROGRAMDATA%\nono\nono-poc-root.pem`), so a bare `node` launched from a NEW pwsh session inherits it. Run the `node -e` HTTPS request from a fresh `Start-Process pwsh` session (so the inherited persisted env var is in effect, exercising NODE_EXTRA_CA_CERTS + CurrentUser\Root) — do NOT manually set the env var inline before the node call (that would prove env-plumbing, not that provisioning persisted trust). (Equivalent alternative: run the Node TLS leg through `nono run` so the confined child receives the in-process-injected env; pick whichever matches how Plan 02 actually wired persistence, and record the choice in `detail`.) The leg must demonstrate the var reached node via provisioning, not via the gate setting it.
    (6) Cleanup: `msiexec /x` for repeatability.
    Aggregate: PASS only if all runnable legs pass; FAIL on any hard failure; SKIP_HOST_UNAVAILABLE (from Test-Precondition) if the host cannot run it at all. Record host-gated partials in `detail` honestly (per the Dark Factory standard: the scripted gate is the close signal on a single dev host; live clean-VM/fleet UAT is acknowledged tech-debt). W4 — a documented partial (a leg recorded as SKIP_HOST_UNAVAILABLE in `detail` because a non-admin local test account or a clean-VM SYSTEM context cannot be created on the dev host) is a LEGITIMATE close signal: the executor must NOT block waiting for a clean PASS when the host genuinely cannot run a leg — record the honest partial verdict + reason in `detail` and let that stand as the close. The runner auto-discovers the gate by glob — no registration needed.
  </action>
  <verify>
    <automated>pwsh -File scripts/verify-dark.ps1 --gate deploy-silent-install 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/gates/deploy-silent-install.ps1` defines `Test-Precondition` and `Invoke-Gate` and NEITHER calls `exit` nor `Persist-Verdict`
    - `Invoke-Gate` returns an ordered dict with keys `gate, verdict, reason, detail, timestamp` and `verdict in {PASS, FAIL, SKIP_HOST_UNAVAILABLE}`
    - The gate asserts `msiexec` install exit is 0 or 3010
    - The gate asserts the `%LOCALAPPDATA%\nono\` scratch owner SID is the target user, NOT `S-1-5-18` (SYSTEM)
    - The gate asserts `nono health` exits non-zero when a subsystem is degraded
    - The gate exercises TLS-through-proxy from PowerShell, Node.js, AND nono-cli; the Node leg runs from a fresh session (or via `nono run`) so it proves the PROVISIONED NODE_EXTRA_CA_CERTS reached node, NOT a gate-set inline env var
    - All `Start-Process` results are assigned to named variables (WR-01; no stray pipeline output)
    - `pwsh -File scripts/verify-dark.ps1 --gate deploy-silent-install` runs the gate and the runner emits a verdict (PASS on a fully-capable host; an honest documented partial / SKIP_HOST_UNAVAILABLE with a clear reason in `detail` on a host that cannot run a leg — never a silent pass, and never blocked waiting for a clean PASS the dev host cannot produce)
  </acceptance_criteria>
  <done>
    The deploy-silent-install gate is auto-discovered by verify-dark.ps1 and proves the four success-criterion-5 checks (or honestly records SKIP/partial-with-reason in `detail` on a host-incapable leg, which is itself an accepted close signal), serving as the Phase 82 close signal.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate (elevated) → host install/uninstall + service state | The gate installs/uninstalls the MSI and toggles the WFP service to exercise the degraded path. |
| gate → target-user context (scratch ownership check) | The gate triggers a first run as a target user and inspects ACL ownership. |
| gate → nono proxy TLS (three clients) | The gate drives real TLS handshakes through the proxy to verify the trust matrix. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-30 | Tampering | log/MSI path built from operator input → path injection | mitigate | Use a fixed log-path suffix under `$env:TEMP` and `-LiteralPath` for all path tests (mirror clean-host T-80-04/T-80-05); MSI path comes from the `param` default, not interpolated operator strings. |
| T-82-31 | Spoofing | gate reports PASS while a leg silently did not run (false close signal) | mitigate | Host-incapable legs return `SKIP_HOST_UNAVAILABLE` (from Test-Precondition) or are recorded as honest partials in `detail`; the gate NEVER fakes a TLS/ownership leg to reach PASS (Dark Factory honesty standard). The Node leg specifically must NOT inline-set NODE_EXTRA_CA_CERTS to fake trust (W3). |
| T-82-32 | Tampering | scratch-ownership check matches a substring SID (SYSTEM passes as a user) | mitigate | Compare the owner SID exactly against `S-1-5-18` (SYSTEM) and the target-user SID — exact SID comparison, never substring/string-contains (CLAUDE.md path/identity comparison footgun). |
| T-82-33 | Denial of Service | gate leaves the host dirty (service disabled, MSI installed) on early failure | mitigate | `msiexec /x` cleanup + service re-enable in a finally-style block so a mid-gate failure still restores host state for repeatability. |
| T-82-SC | Tampering | npm/pip/cargo installs | accept | This gate adds NO package-manager installs (PowerShell + `node -e` against an already-installed Node + msiexec). No package-legitimacy gate required. |
</threat_model>

<verification>
- `pwsh -File scripts/verify-dark.ps1 --gate deploy-silent-install` runs the gate via auto-discovery and the runner emits a verdict.
- The gate file defines exactly `Test-Precondition` + `Invoke-Gate`, neither calling exit/Persist-Verdict.
- On a fully-capable elevated host: PASS covering install / PATH / scratch-ownership / degraded-health / three-client TLS (Node leg via inherited persisted env).
- On a host that cannot run a leg: an honest documented partial / SKIP_HOST_UNAVAILABLE with a clear reason in `detail` (never a silent pass; never blocked).
</verification>

<success_criteria>
- DEPLOY-01/02/03/05/06 (end-to-end close signal): `verify-dark.ps1 --gate deploy-silent-install` emits a verdict covering silent install under SYSTEM context, scratch owned by the target user not SYSTEM, degraded-service path → non-zero `nono health`, and TLS trust across all three client types (Node via provisioned NODE_EXTRA_CA_CERTS) — satisfying Phase 82 success criterion 5 and the Dark Factory close mandate. A documented host-gated partial recorded in `detail` is an accepted close.
</success_criteria>

<output>
Create `.planning/phases/82-fleet-deployment-infrastructure/82-04-SUMMARY.md` when done.
</output>
</output>
