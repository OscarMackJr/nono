---
phase: 59-supervisor-ipc-robustness
plan: 03
type: execute
wave: 2
depends_on: [59-01]
files_modified:
  - crates/nono/src/supervisor/socket_windows.rs
  - crates/nono-cli/src/exec_strategy_windows/supervisor.rs
  - crates/nono-cli/tests/supervisor_ipc_robustness.rs
autonomous: false
requirements: [REQ-IPC-01]

must_haves:
  truths:
    - "On Windows, the AIPC named-pipe read_frame is bounded by a PeekNamedPipe poll-until-data-or-deadline loop (SC2/SC4)"
    - "A transient capability-pipe close re-accepts the same handle instead of permanently disabling capability expansion (SC1/SC4)"
    - "The Windows work is documented as a translate-not-cherry-pick with the PeekNamedPipe-over-overlapped-IO rationale in the SUMMARY (SC4)"
    - "Re-accept re-verifies the session SID/token and preserves seen_request_ids replay protection (no trust cached across reconnect)"
  artifacts:
    - path: "crates/nono/src/supervisor/socket_windows.rs"
      provides: "PeekNamedPipe-bounded read_frame (read_exact_bounded helper) + disconnect-class error classification"
      contains: "PeekNamedPipe"
    - path: "crates/nono-cli/src/exec_strategy_windows/supervisor.rs"
      provides: "capability-pipe re-accept loop (DisconnectNamedPipe + ConnectNamedPipe) bounded by terminate_requested"
      contains: "DisconnectNamedPipe"
    - path: "crates/nono-cli/tests/supervisor_ipc_robustness.rs"
      provides: "Windows-gated AIPC bounded-read + re-accept integration tests"
      contains: "windows"
  key_links:
    - from: "socket_windows.rs::read_frame"
      to: "read_exact_bounded(deadline)"
      via: "PeekNamedPipe availability probe gated by an Instant deadline"
      pattern: "PeekNamedPipe"
    - from: "supervisor.rs cap-pipe recv loop"
      to: "DisconnectNamedPipe + ConnectNamedPipe re-arm"
      via: "on disconnect-class error, re-arm the same handle (reuse ERROR_PIPE_CONNECTED-is-success idiom) and continue"
      pattern: "DisconnectNamedPipe"
---

<objective>
Translate the Unix supervisor IPC hardening intent onto the fork's Windows Named-Pipe AIPC path (Phase 18) — SC1 + SC4. Replace the unbounded `read_exact` in `socket_windows.rs::read_frame` with a `PeekNamedPipe` poll-until-data-or-deadline bounded read (D-03), and convert the capability-pipe server loop in `exec_strategy_windows/supervisor.rs` from hard-break-on-close to a re-accept loop (`DisconnectNamedPipe` + `ConnectNamedPipe`) bounded by `terminate_requested`. Add Windows-gated integration tests and a documented live-repro UAT (operator-run on a real Win11 console).

Purpose: Windows AIPC pipes are `PIPE_WAIT` (blocking) and do not honor socket-style read timeouts, and a single transient capability-pipe close currently disables capability expansion for the whole session. This is a genuinely divergent transport (named pipe, not AF_UNIX) — D-03 mandates the conservative `PeekNamedPipe` poll over an overlapped-I/O rewrite (explicitly rejected as too large/risky), and SC4 REQUIRES the SUMMARY document this as a translate-not-cherry-pick with the rejected-alternative rationale.

Output: Modified `socket_windows.rs` (bounded `read_frame`), `exec_strategy_windows/supervisor.rs` (re-accept loop), Windows-gated tests in `supervisor_ipc_robustness.rs`, and a Windows live-repro UAT documented in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-supervisor-ipc-robustness/59-CONTEXT.md
@.planning/phases/59-supervisor-ipc-robustness/59-RESEARCH.md
@.planning/phases/59-supervisor-ipc-robustness/59-PATTERNS.md
@.planning/phases/59-supervisor-ipc-robustness/59-VALIDATION.md
@.planning/phases/59-supervisor-ipc-robustness/59-01-SUMMARY.md
@crates/nono/src/supervisor/socket_windows.rs
@crates/nono-cli/src/exec_strategy_windows/supervisor.rs

<interfaces>
<!-- Exact change sites + Win32 error codes from 59-PATTERNS.md / 59-RESEARCH.md. Use directly — no exploration. -->

CLI accessor (from 59-01): `crate::timeouts::supervisor_ipc_read_timeout() -> Duration` (Windows reads the same const value, D-02).

socket_windows.rs change sites:
- `read_frame` (321-339): two unbounded `read_exact` calls under `PIPE_WAIT` — the gap to fix.
- `bind_aipc_pipe` (~748-782): `PIPE_WAIT` (767), `PIPE_UNLIMITED_INSTANCES` (768) — AIPC pipe tolerates re-accepting a fresh instance.
- control pipes `create_low_integrity_named_pipe` (~1257), `create_named_pipe` (~1289): `1` instance → re-arm the SAME handle, do NOT create a fresh instance (Pitfall 3).
- `finalize_server_connection` (1307-1324): the `ERROR_PIPE_CONNECTED`-is-success idiom to REUSE for re-accept.
- Error consts vendored at 20-39. `PIPE_CONNECT_TIMEOUT_MS = 5000` (106) is for connect, NOT read.

Win32 error codes to classify (RESEARCH Pattern 4):
- ERROR_NO_DATA = 232 → disconnect
- ERROR_PIPE_NOT_CONNECTED = 233 → re-arm
- ERROR_BROKEN_PIPE = 109 → re-accept
- ERROR_PIPE_CONNECTED = 535 → treat as SUCCESS (client raced in; reuse finalize_server_connection idiom)

supervisor.rs change site:
- cap-pipe server loop (561-600): `match sock.recv_message() { Ok => handle, Err => break }`; break-on-close logged "Capability pipe closed" at 591-598. `terminate_requested` checked at 562. `seen_request_ids` replay set at 560.

windows-sys API surface: `PeekNamedPipe`, `ConnectNamedPipe`, `DisconnectNamedPipe` from Win32::System::Pipes (already vendored, windows-sys 0.59).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PeekNamedPipe-bounded read_frame in socket_windows.rs (SC2/SC4)</name>
  <files>crates/nono/src/supervisor/socket_windows.rs</files>
  <read_first>
    - crates/nono/src/supervisor/socket_windows.rs (the file being modified — read read_frame 321-339, bind_aipc_pipe 748-782, finalize_server_connection 1307-1324, error consts 20-39)
    - 59-PATTERNS.md §"crates/nono/src/supervisor/socket_windows.rs" (the read_exact_bounded sketch + ERROR_PIPE_CONNECTED idiom)
    - 59-RESEARCH.md §"Pattern 3" + §"Common Pitfalls" Pitfall 2 (busy-spin → MUST sleep) + §"Don't Hand-Roll" (PeekNamedPipe non-destructive probe)
  </read_first>
  <action>
    Add a `read_exact_bounded(reader, buf, deadline: Instant)` helper that loops: call `PeekNamedPipe(handle, NULL, 0, NULL, &mut avail, NULL)` (non-destructive availability probe); if it returns 0, classify the GLE — ERROR_BROKEN_PIPE (109) / ERROR_PIPE_NOT_CONNECTED (233) / ERROR_NO_DATA (232) surface as a disconnect-class `NonoError` for the caller's re-accept; if `avail == 0` and `Instant::now() >= deadline` return a bounded-timeout `NonoError` (non-fatal/keep-alive classification, distinguishable from disconnect); if `avail == 0` and not past deadline, `std::thread::sleep(POLL_INTERVAL)` (~10ms, Claude's discretion per D-03 — MUST sleep to avoid the busy-spin pitfall) and continue; once `avail > 0`, do a blocking read of `min(avail, remaining)` bytes (completes promptly since bytes are present). Compute the deadline as `Instant::now() + crate-level timeout value` — the Windows side reads the same 5s value as Unix (D-02); since the library is policy-free, pass the `Duration` in from the caller rather than reading env in the library.
    Rewire `read_frame` (321-339) to call `read_exact_bounded` for both the 4-byte length prefix and the payload, preserving the `MAX_MESSAGE_SIZE` (64 KiB) cap. Every `unsafe` Win32 call gets a `// SAFETY:` comment. No `.unwrap()`/`.expect()`; propagate via `?`/`NonoError`. Use `checked_`/`saturating_` for any deadline/offset arithmetic. The bounded-timeout and disconnect errors MUST be distinguishable by the caller (e.g. distinct NonoError context strings or a classifier) so the supervisor loop can treat timeout as keep-alive and disconnect as re-accept (fail-closed for the in-flight frame either way).
  </action>
  <verify>
    <automated>cargo build -p nono --target x86_64-pc-windows-msvc && cargo test -p nono --lib supervisor::socket</automated>
  </verify>
  <acceptance_criteria>
    - `socket_windows.rs` contains `PeekNamedPipe` and a `read_exact_bounded` (or equivalently-named) bounded helper; `read_frame` no longer calls unbounded `read_exact` directly.
    - The peek loop contains a `std::thread::sleep` on the `avail == 0` path (no busy-spin).
    - Disconnect-class GLEs (109/233/232) and the deadline-timeout are surfaced as DISTINGUISHABLE errors (source review: distinct error context/classifier).
    - `MAX_MESSAGE_SIZE` (64 KiB) cap is preserved in `read_frame`.
    - `cargo build -p nono` (Windows host) succeeds; every new `unsafe` block has a `// SAFETY:` comment; no `.unwrap()`/`.expect()` outside tests.
  </acceptance_criteria>
  <done>The Windows AIPC `read_frame` is bounded by a PeekNamedPipe deadline poll — a slow/silent child can no longer block the read indefinitely, and disconnect vs timeout are distinguishable for the loop's keep-alive logic.</done>
</task>

<task type="auto">
  <name>Task 2: Capability-pipe re-accept loop in supervisor.rs + Windows-gated tests (SC1/SC4)</name>
  <files>crates/nono-cli/src/exec_strategy_windows/supervisor.rs, crates/nono-cli/tests/supervisor_ipc_robustness.rs</files>
  <read_first>
    - crates/nono-cli/src/exec_strategy_windows/supervisor.rs (the file being modified — read the cap-pipe loop 561-600, the break-on-close 591-598, terminate_requested 562, seen_request_ids 560)
    - crates/nono/src/supervisor/socket_windows.rs::finalize_server_connection (1307-1324 — the ERROR_PIPE_CONNECTED-is-success idiom to reuse)
    - crates/nono-cli/tests/supervisor_ipc_robustness.rs (the Wave-0 scaffold from 59-01 with the labeled Windows insertion points)
    - 59-PATTERNS.md §"crates/nono-cli/src/exec_strategy_windows/supervisor.rs" + §"...supervisor_ipc_robustness.rs" (Win32 round-trip assertion shape)
    - 59-RESEARCH.md §"Pattern 4" + §"Common Pitfalls" Pitfall 3 (instance count) + §"Security Domain" (re-verify SID, preserve replay protection)
  </read_first>
  <action>
    Convert the cap-pipe server loop (561-600) from break-on-close to re-accept: on a disconnect-class error from `recv_message`/`read_frame` (ERROR_BROKEN_PIPE 109 / ERROR_PIPE_NOT_CONNECTED 233 / ERROR_NO_DATA 232), call `DisconnectNamedPipe(server_handle)` then `ConnectNamedPipe(server_handle, NULL)` to re-arm the SAME handle (reuse the `finalize_server_connection` ERROR_PIPE_CONNECTED-is-success idiom — GLE 535 is success), then `continue`. For the AIPC pipe (`PIPE_UNLIMITED_INSTANCES`) re-arming a fresh instance is structurally fine, but for the 1-instance control pipes you MUST re-arm the existing handle, NOT create a fresh instance (Pitfall 3). Bound the loop by the already-present `terminate_requested` check — no separate retry cap (RESEARCH Open Q2: liveness-bounded, fail-secure). Treat a BOUNDED-TIMEOUT error (distinguishable from disconnect per Task 1) as keep-alive (abandon the in-flight frame, continue the loop), NOT a re-accept and NOT a capability grant.
    Security invariants (do NOT regress): re-accept MUST re-verify the per-session SID/token on reconnect (do not cache trust across reconnect — V3) and MUST preserve the `seen_request_ids` replay-protection set (carried at 560) across re-accepts. Never widen capabilities on an IPC hiccup (fail-closed). Add `debug!`/`warn!` on disconnect/re-accept.
    In `crates/nono-cli/tests/supervisor_ipc_robustness.rs`, fill the Windows-gated (`#[cfg(target_os = "windows")]`) insertion points: (a) a bounded-read test that opens an AIPC pipe, writes a partial frame (length prefix only) then stalls, and asserts `read_frame`/`read_exact_bounded` returns within the deadline (override `NONO_SUPERVISOR_IPC_READ_TIMEOUT` to ~1s with save/restore); (b) a re-accept test that connects, closes, then reconnects and asserts the server re-accepts (capability pipe not permanently disabled). Use the `aipc_handle_brokering_integration.rs` Win32 assertion shape (`// SAFETY:` on every FFI call, `last_os_error()` in assert messages, close handles). Mark the file's Windows tests `#[allow(clippy::unwrap_used)]`.
  </action>
  <verify>
    <automated>cargo build -p nono-cli && cargo test -p nono-cli --test supervisor_ipc_robustness</automated>
  </verify>
  <acceptance_criteria>
    - `supervisor.rs` contains `DisconnectNamedPipe` + `ConnectNamedPipe` re-arm on disconnect-class errors and no longer `break`s the cap-pipe loop on a transient close.
    - Re-accept re-runs the session SID/token verification and preserves `seen_request_ids` (source review: replay set not reset on re-accept).
    - A bounded-timeout error is handled as keep-alive (continue), distinct from the disconnect→re-accept path.
    - `cargo test -p nono-cli --test supervisor_ipc_robustness` exits 0 with the Windows-gated bounded-read + re-accept tests passing on this Windows host.
    - `cargo test -p nono-cli --test aipc_handle_brokering_integration` still green (round-trip regression unbroken).
  </acceptance_criteria>
  <done>A transient capability-pipe close re-accepts the same handle (capability expansion survives), bounded reads are honored, and security invariants (SID re-verify, replay protection, fail-closed) are preserved — proven by Windows-gated integration tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Windows live-repro UAT (SC1 + SC2) — operator, real Win11 console</name>
  <action>Operator-run live-repro on a real Win11 console (CI cannot deterministically exercise PIPE_WAIT named-pipe timing, D-05). Run the slow-child (SC2) and reconnect (SC1) repros described below against a supervised `nono run` launched from `target\release\nono.exe` in a profile-covered cwd, and record the PASS/FAIL evidence in 59-03-SUMMARY.md. This task is autonomous: false — it requires a human operator.</action>
  <what-built>
    The Windows AIPC supervisor IPC path now (1) bounds reads via a PeekNamedPipe deadline poll and (2) re-accepts the capability pipe on a transient child close. CI integration tests pass, but named-pipe `PIPE_WAIT` timing cannot be deterministically exercised in CI (D-05) — a real Win11 console live-repro is required to confirm field behavior.
  </what-built>
  <how-to-verify>
    On a real Win11 console (NOT git-bash/MSYS — supervised runs need a real console per project gotcha; run `target\release\nono.exe` from a profile-covered cwd to skip the broker trust gate):
    1. Build the release binaries: `cargo build --release -p nono-cli`.
    2. SLOW-CHILD repro (SC2): start a supervised `nono run` whose child opens the AIPC pipe, sends a partial frame, then stalls. Observe the supervisor is NOT blocked past the bounded deadline (set `NONO_SUPERVISOR_IPC_READ_TIMEOUT` low, e.g. 2s, to observe quickly) — the supervisor stays responsive and does not hang.
    3. RECONNECT repro (SC1): have the child close its capability-pipe connection then reconnect. Confirm the capability pipe re-accepts and capability expansion still works after the transient close (it is NOT permanently disabled).
    4. Confirm no `Access is denied` / handle-leak / `ERROR_PIPE_BUSY` errors appear during reconnect.
    Record the exact commands, observed output, and PASS/FAIL for SC1 + SC2 in the plan SUMMARY.
  </how-to-verify>
  <resume-signal>Type "approved" with the live-repro PASS evidence, or describe the observed failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sandboxed child → supervisor AIPC named pipe | Untrusted child controls pipe frame timing/content + connect/disconnect cadence; the read + accept paths are the attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-03a | Denial of Service | `socket_windows.rs::read_frame` partial-frame hang under PIPE_WAIT | mitigate | `PeekNamedPipe` deadline poll bounds the read; a slow/silent child cannot block indefinitely (Task 1; verified by the Windows bounded-read test + live-repro SC2). |
| T-59-03b | Denial of Service | PeekNamedPipe busy-spin pegging a CPU core | mitigate | `std::thread::sleep(~10ms)` between peeks (Pitfall 2); bounded by the deadline. |
| T-59-03c | Denial of Service | reconnect thrash on the capability pipe | mitigate | Re-accept loop bounded by `terminate_requested` + child liveness — reconnect cannot outlive the child; no infinite tight loop, no separate retry cap needed (fail-secure). |
| T-59-03d | Spoofing | reconnect impersonation (different principal connects after re-accept) | mitigate | Re-accept re-verifies the per-session SID/token on every connect; trust is NOT cached across reconnect (V3); the restricting-SID DACL on the pipe is unchanged. |
| T-59-03e | Elevation / Tampering | timeout/disconnect induces fail-open (granting a capability on a partial request) | mitigate | A timed-out/partial/malformed frame is ABANDONED; `seen_request_ids` replay protection preserved across re-accept; no capability granted on a partial/timed-out request (fail-closed Security Domain invariant). |
| T-59-03f | Tampering | re-accepting wrong instance count on a 1-instance control pipe | mitigate | Re-arm the EXISTING handle (`DisconnectNamedPipe`+`ConnectNamedPipe`) for control pipes rather than creating a fresh instance (Pitfall 3); avoids ERROR_PIPE_BUSY/handle leak. |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | No package installs — RESEARCH.md §"Package Legitimacy Audit": zero new deps (vendored `windows-sys 0.59`, `std` only). No `[ASSUMED]`/`[SUS]` packages → no legitimacy checkpoint. |
</threat_model>

<verification>
- `cargo build -p nono-cli` + `cargo build -p nono` succeed on this Windows host.
- `cargo test -p nono-cli --test supervisor_ipc_robustness` green (Windows-gated bounded-read + re-accept).
- `cargo test -p nono-cli --test aipc_handle_brokering_integration` green (regression).
- Windows live-repro (SC1 + SC2) signed off by the operator with evidence in the SUMMARY.
- Cross-target clippy is N/A here (these are `#[cfg(windows)]` files exercised on the Windows host); note this in the SUMMARY.
</verification>

<success_criteria>
- SC4: Windows AIPC receives keep-alive + bounded-timeout + robust re-accept, documented in the SUMMARY as a translate-not-cherry-pick with the PeekNamedPipe-over-overlapped-I/O rationale (and the rejected overlapped-I/O alternative).
- SC1/SC2 (Windows): transient close → re-accept (capability pipe not disabled); partial frame → bounded read (no indefinite block) — test-proven + live-repro confirmed.
- Security invariants preserved: SID re-verify on reconnect, replay protection intact, fail-closed on any IPC error.
</success_criteria>

<output>
Create `.planning/phases/59-supervisor-ipc-robustness/59-03-SUMMARY.md` when done. The SUMMARY MUST (SC4 requirement) document the Windows work as a TRANSLATE-NOT-CHERRY-PICK: state that AF_UNIX is unix-only so no code was cherry-picked, explain the PeekNamedPipe poll-until-data-or-deadline translation chosen, and explicitly name the REJECTED alternative (full overlapped/async-I/O rewrite with ReadFile-overlapped + WaitForSingleObject + CancelIoEx) and why it was rejected (too large/risky for this phase per D-03). Include the operator's live-repro PASS evidence for SC1 + SC2.
</output>
