---
phase: 59-supervisor-ipc-robustness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono-cli/src/timeouts.rs
  - crates/nono-cli/tests/supervisor_ipc_robustness_unix.rs
  - crates/nono-cli/tests/supervisor_ipc_robustness_windows.rs
autonomous: true
requirements: [REQ-IPC-01]

must_haves:
  truths:
    - "A bounded supervisor IPC read timeout exists as a named constant (5s default), not a bare literal (D-01)"
    - "NONO_SUPERVISOR_IPC_READ_TIMEOUT env override is honored and clamped to MAX_TIMEOUT (D-01)"
    - "An invalid/unparseable env value falls back to the 5s default (never unbounded) (D-01)"
    - "The timeouts const + accessor are reachable in-crate and proven by in-crate unit tests (nono-cli is a bin-only crate — no [lib] target, so integration tests CANNOT reach nono_cli::timeouts::*)"
    - "Two per-platform net-new integration test scaffolds exist (Unix + Windows), each cfg-gated to an empty binary on the other platform, and each compiles on all 3 platforms — the Wave-0 substrate for the D-05 cross-platform integration tests (D-05)"
  artifacts:
    - path: "crates/nono-cli/src/timeouts.rs"
      provides: "SUPERVISOR_IPC_READ_TIMEOUT const + supervisor_ipc_read_timeout() accessor + IN-CRATE unit tests (default/override/clamp/invalid-fallback)"
      contains: "SUPERVISOR_IPC_READ_TIMEOUT"
    - path: "crates/nono-cli/tests/supervisor_ipc_robustness_unix.rs"
      provides: "Wave 0 Unix-gated integration test scaffold (#![cfg(unix)], empty binary on Windows) scoped to the nono-lib pub surface only (use nono::supervisor::...)"
      contains: "supervisor_ipc"
    - path: "crates/nono-cli/tests/supervisor_ipc_robustness_windows.rs"
      provides: "Wave 0 Windows-gated integration test scaffold (#![cfg(target_os = \"windows\")], empty binary on Unix) scoped to the nono-lib pub surface only"
      contains: "supervisor_ipc"
  key_links:
    - from: "crates/nono-cli/src/timeouts.rs::supervisor_ipc_read_timeout"
      to: "env_duration_secs"
      via: "delegates env parse + MAX_TIMEOUT clamp to the shared helper"
      pattern: "env_duration_secs\\(\"NONO_SUPERVISOR_IPC_READ_TIMEOUT\""
---

<objective>
Add the bounded supervisor-IPC read-timeout configuration knob (D-01) to the centralized `timeouts.rs` module — proven by IN-CRATE unit tests (the only reachable home for the bin-only `nono-cli` crate's private `timeouts` mod) — and create the two per-platform net-new integration test scaffolds (Wave 0) that 59-02 (Unix) and 59-03 (Windows) will fill in.

Purpose: Establishes the single dependency both platform plans consume — the un-cfg'd `SUPERVISOR_IPC_READ_TIMEOUT` constant (5s, matching upstream `d1851c9`) with a `NONO_*` env override + `MAX_TIMEOUT` clamp — plus the two empty-binary-on-the-other-platform test files. CRITICAL: `nono-cli` is a BIN-ONLY crate (`[[bin]]` only, no `[lib]` target, no `src/lib.rs`; `timeouts` is `mod timeouts;` in `main.rs`). Therefore integration tests under `crates/nono-cli/tests/` CANNOT reach `nono_cli::timeouts::*`, `run_supervisor_loop`, or `read_frame`. The timeout const/accessor unit tests MUST live in the in-crate `#[cfg(test)] mod tests` of `timeouts.rs`; the integration scaffolds may only reach the `nono` LIBRARY's pub surface (`nono::supervisor::...`), exactly like `aipc_handle_brokering_integration.rs`.

Splitting the test file per platform (Unix file vs Windows file) gives 59-02 and 59-03 exclusive file ownership in Wave 2 — they run in parallel with zero `files_modified` overlap.

Output: A modified `timeouts.rs` (const + accessor + in-crate unit tests) and two net-new per-platform scaffolds: `crates/nono-cli/tests/supervisor_ipc_robustness_unix.rs` and `crates/nono-cli/tests/supervisor_ipc_robustness_windows.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-supervisor-ipc-robustness/59-CONTEXT.md
@.planning/phases/59-supervisor-ipc-robustness/59-RESEARCH.md
@.planning/phases/59-supervisor-ipc-robustness/59-PATTERNS.md
@.planning/phases/59-supervisor-ipc-robustness/59-VALIDATION.md
@crates/nono-cli/src/timeouts.rs
@crates/nono-cli/Cargo.toml
@crates/nono-cli/tests/aipc_handle_brokering_integration.rs

<interfaces>
<!-- The exact in-file pattern this plan mirrors (from 59-PATTERNS.md). Use directly — no exploration needed. -->

CRATE SHAPE (root cause of the reachability constraint) — crates/nono-cli/Cargo.toml:
- `[[bin]] name = "nono" path = "src/main.rs"` is the ONLY target. There is NO `[lib]` and NO `src/lib.rs`. `timeouts` is `mod timeouts;` private to the bin. Integration tests in `tests/` link against the crate as an external dependency and therefore CANNOT see `nono_cli::timeouts`, `run_supervisor_loop`, or `read_frame`. Do NOT add a `[lib]` target (out of scope / higher risk).

From crates/nono-cli/src/timeouts.rs (existing precedent — DETACH_STARTUP_TIMEOUT pair, lines 37-39 / 72-76):
- `pub const DETACH_STARTUP_TIMEOUT: Duration = Duration::from_secs(30);`
- `pub fn detach_startup_timeout() -> Duration { env_duration_secs("NONO_DETACH_STARTUP_TIMEOUT", DETACH_STARTUP_TIMEOUT) }`
- The existing in-crate `#[cfg(test)] mod tests` in this file is the home for the new unit tests.

Shared clamp helper to REUSE AS-IS (timeouts.rs:105, NOT #[cfg(unix)] — compiles on Windows too):
- `fn env_duration_secs(var: &str, default: Duration) -> Duration` — parses u64 seconds, clamps to `MAX_TIMEOUT` (timeouts.rs:103, 3600s) with a warn, falls back to `default` on unparseable.

nono LIBRARY pub surface reachable from integration tests (crates/nono/src/supervisor/socket.rs, re-exported via `nono::supervisor::socket`):
- `nono::supervisor::socket::SupervisorSocket::pair() -> Result<(Self, Self)>` (Unix-only — socket.rs is `#[cfg(not(target_os = "windows"))]`)
- `SupervisorSocket::from_stream(UnixStream) -> Self` (Unix-only)
- `SupervisorSocket::set_read_timeout(&self, Option<Duration>) -> Result<()>` (Unix-only)
- `SupervisorSocket::recv_message(&mut self) -> Result<SupervisorMessage>` (drives the private `read_frame`)
- These are the ONLY robustness-relevant symbols an integration test can reach. `read_frame`/`run_supervisor_loop`/`timeouts` are NOT reachable.

Test cfg-gate idiom (aipc_handle_brokering_integration.rs:28 header):
- `#![cfg(target_os = "windows")]` produces an empty test binary on the other platforms. Use a FILE-LEVEL `#![cfg(unix)]` on the Unix scaffold and a FILE-LEVEL `#![cfg(target_os = "windows")]` on the Windows scaffold (one platform per file, since each file is owned by exactly one Wave-2 plan).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SUPERVISOR_IPC_READ_TIMEOUT const + accessor + IN-CRATE unit tests to timeouts.rs</name>
  <files>crates/nono-cli/src/timeouts.rs</files>
  <read_first>
    - crates/nono-cli/src/timeouts.rs (the file being modified — read the full module; note DETACH_STARTUP_TIMEOUT pair at 37-39/72-76, env_duration_secs at 105, MAX_TIMEOUT at 103, and the existing `#[cfg(test)] mod tests`)
    - crates/nono-cli/Cargo.toml (confirm bin-only: `[[bin]]` present, no `[lib]` — the reason unit tests for this const MUST be in-crate, not in `tests/`)
    - 59-PATTERNS.md §"crates/nono-cli/src/timeouts.rs" (the exact analog + the "do NOT cfg-gate" instruction)
    - 59-RESEARCH.md §"Add the timeout constant + accessor (timeouts.rs)" + §"Don't Hand-Roll" (reuse env_duration_secs)
  </read_first>
  <action>
    Add a new un-cfg'd const `SUPERVISOR_IPC_READ_TIMEOUT: Duration = Duration::from_secs(5)` (5s = upstream d1851c9 per D-01) and accessor `supervisor_ipc_read_timeout()` that delegates to `env_duration_secs("NONO_SUPERVISOR_IPC_READ_TIMEOUT", SUPERVISOR_IPC_READ_TIMEOUT)`. Mirror the `DETACH_STARTUP_TIMEOUT` / `detach_startup_timeout()` pair exactly. Do NOT cfg-gate the const or accessor — Windows reads the same value per D-02, and `env_duration_secs` is already non-cfg'd. Do NOT add a per-run CLI flag (D-01: internal knob, env override only). Do NOT use a bare literal at any call site.
    Add unit tests in the EXISTING in-crate `#[cfg(test)] mod tests` of `timeouts.rs` (this is the ONLY reachable home — `nono-cli` is bin-only, so a `tests/`-dir test cannot see `nono_cli::timeouts::*`). Cover: (a) default value is 5s when env absent; (b) `NONO_SUPERVISOR_IPC_READ_TIMEOUT=1` yields 1s; (c) an over-`MAX_TIMEOUT` value (e.g. 99999) clamps to MAX_TIMEOUT; (d) an unparseable value (e.g. "abc") falls back to the 5s default. Every test MUST save and restore the env var per the CLAUDE.md parallel-test env rule (save `std::env::var(...).ok()`, set, exercise, then restore via set_var/remove_var) and use the test module's existing serialization lock if one exists. Mark test module `#[allow(clippy::unwrap_used)]` only where already established in that module.
  </action>
  <verify>
    <automated>cargo test -p nono-cli --bin nono timeouts:: -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p nono-cli --bin nono timeouts::` exits 0 with the 4 new in-crate tests passing.
    - `timeouts.rs` contains the literal token `SUPERVISOR_IPC_READ_TIMEOUT` and `NONO_SUPERVISOR_IPC_READ_TIMEOUT`.
    - The new tests live in the in-crate `#[cfg(test)] mod tests` of `timeouts.rs` (NOT in `crates/nono-cli/tests/`).
    - The const is NOT wrapped in `#[cfg(unix)]` (grep confirms no cfg attribute directly above `pub const SUPERVISOR_IPC_READ_TIMEOUT`).
    - No bare `Duration::from_secs(5)` literal appears at any IPC call site — only the const/accessor.
    - `cargo build -p nono-cli` succeeds; no new `.unwrap()`/`.expect()` outside test modules.
  </acceptance_criteria>
  <done>The supervisor IPC read-timeout knob exists as a named const + env-override accessor in timeouts.rs with passing in-crate parse/clamp/default unit tests; both platform plans can call `crate::timeouts::supervisor_ipc_read_timeout()`.</done>
</task>

<task type="auto">
  <name>Task 2: Create the two per-platform integration test scaffolds (Unix + Windows)</name>
  <files>crates/nono-cli/tests/supervisor_ipc_robustness_unix.rs, crates/nono-cli/tests/supervisor_ipc_robustness_windows.rs</files>
  <read_first>
    - crates/nono-cli/tests/aipc_handle_brokering_integration.rs (file-level `#![cfg(target_os = "windows")]` + empty-binary-on-other-platforms idiom + `use nono::supervisor::...` LIBRARY-surface imports + SAFETY-on-unsafe + last_os_error-in-assert)
    - crates/nono-cli/Cargo.toml (confirm bin-only — the scaffolds may ONLY reach the `nono` lib pub surface, never `nono_cli::`)
    - 59-PATTERNS.md §"crates/nono-cli/tests/... (test, net-new — Wave 0)" (per-platform file split)
    - 59-VALIDATION.md §"Wave 0 Requirements" + §"Per-Task Verification Map"
  </read_first>
  <action>
    Create TWO net-new files, each owned by exactly one downstream Wave-2 plan so there is no shared-file write in Wave 2:
    (1) `crates/nono-cli/tests/supervisor_ipc_robustness_unix.rs` — file-level `#![cfg(unix)]` (empty test binary on Windows) + `#![allow(clippy::unwrap_used)]`. Populate with: a module-level doc comment stating purpose + the cross-platform-compile contract; one real always-running (under `cfg(unix)`) sanity test `fn scaffold_links_nono_lib()` that constructs a `nono::supervisor::socket::SupervisorSocket::pair()` and asserts both ends are obtained (proves the `nono` LIBRARY pub surface — the ONLY reachable robustness surface — links from the integration-test crate). Then leave clearly-labeled TODO placeholder comments naming `reconnect_survival` (SC1) and `bounded_read_timeout` (SC2) for 59-02 to fill.
    (2) `crates/nono-cli/tests/supervisor_ipc_robustness_windows.rs` — file-level `#![cfg(target_os = "windows")]` (empty test binary on Unix) + `#![allow(clippy::unwrap_used)]`. Populate with: a module-level doc comment; one real Windows sanity test `fn scaffold_links_nono_lib()` that touches a reachable `nono::supervisor::socket` Windows pub symbol (e.g. construct/bind via the AIPC pub surface used by `aipc_handle_brokering_integration.rs`, or assert a pub type is in scope) to prove the lib surface links. Then leave labeled TODO placeholders for the Windows AIPC `bounded_read` and `re_accept` tests (59-03).
    CRITICAL constraints (both files): Do NOT reference `nono_cli::` ANYWHERE (unreachable — bin-only crate). Do NOT reference `nono_cli::timeouts::...`, `run_supervisor_loop`, or `read_frame` (the timeout-const proof lives in the in-crate unit tests from Task 1; the behavioral timeout proof is driven via the pub `set_read_timeout()`/`recv_message()` path by 59-02). Do NOT add `#[ignore]`'d fake tests; the only real test in each file is `scaffold_links_nono_lib`.
  </action>
  <verify>
    <automated>cargo test -p nono-cli --test supervisor_ipc_robustness_unix && cargo test -p nono-cli --test supervisor_ipc_robustness_windows</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p nono-cli --tests` succeeds (both files produce valid binaries on all 3 platforms — empty on the non-matching platform).
    - On the host platform, the matching `scaffold_links_nono_lib` test passes; the other file compiles to an empty binary.
    - NEITHER file contains the token `nono_cli` (grep confirms absence) — they reach only `nono::supervisor::...`.
    - NEITHER file references `run_supervisor_loop`, `read_frame`, or `nono_cli::timeouts` (all unreachable).
    - TODO placeholders naming `reconnect_survival` + `bounded_read_timeout` (Unix file) and the Windows AIPC `bounded_read` + `re_accept` variants (Windows file) are present for the downstream plans.
  </acceptance_criteria>
  <done>Two per-platform integration scaffolds exist, compile on all platforms (empty on the non-matching one), prove the `nono` library pub surface links from the test crate, reference NO unreachable `nono_cli` symbols, and are staged with labeled insertion points for 59-02 (Unix) and 59-03 (Windows). Each file is owned by exactly one Wave-2 plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → CLI config | `NONO_SUPERVISOR_IPC_READ_TIMEOUT` is operator-supplied untrusted input parsed into a `Duration` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-02 | Denial of Service | `timeouts::supervisor_ipc_read_timeout()` env parse | mitigate | Reuse `env_duration_secs` which clamps to `MAX_TIMEOUT` (3600s) and falls back to the 5s default on unparseable input — an attacker-supplied env cannot set an unbounded or overflowing timeout; verified by the clamp + invalid-value IN-CRATE unit tests (Task 1). |
| T-59-02b | Tampering | bare timeout literal drift | accept | D-01 forbids bare literals; mitigated structurally by routing all reads through the single const/accessor (no per-call literal exists to tamper). |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — RESEARCH.md §"Package Legitimacy Audit" confirms zero new deps (only vendored `libc`, `windows-sys 0.59`, `std`). No `[ASSUMED]`/`[SUS]` packages → no legitimacy checkpoint required. |
</threat_model>

<verification>
- `cargo test -p nono-cli --bin nono timeouts::` green (4 new IN-CRATE parse/clamp/default tests).
- `cargo test -p nono-cli --test supervisor_ipc_robustness_unix` + `--test supervisor_ipc_robustness_windows` green (per-platform scaffold sanity tests; empty binary on the non-matching platform).
- `cargo build -p nono-cli --tests` succeeds on this Windows host.
- No bare `Duration::from_secs(5)` literal at any IPC site; const + accessor only.
- Neither scaffold references `nono_cli`, `run_supervisor_loop`, `read_frame`, or `nono_cli::timeouts` (grep confirms — all unreachable from a bin-only crate's `tests/`).
</verification>

<success_criteria>
- `SUPERVISOR_IPC_READ_TIMEOUT` (5s) + `supervisor_ipc_read_timeout()` exist, un-cfg'd, with env override + MAX_TIMEOUT clamp, proven by in-crate unit tests.
- Env override + clamp + invalid-fallback covered by passing in-crate unit tests with save/restore.
- Two per-platform scaffolds (`supervisor_ipc_robustness_unix.rs`, `supervisor_ipc_robustness_windows.rs`) exist, compile on all platforms, link the `nono` lib pub surface, and carry NO unreachable `nono_cli` references — giving 59-02/59-03 exclusive file ownership.
</success_criteria>

<output>
Create `.planning/phases/59-supervisor-ipc-robustness/59-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
