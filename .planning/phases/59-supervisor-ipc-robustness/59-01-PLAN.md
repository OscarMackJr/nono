---
phase: 59-supervisor-ipc-robustness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono-cli/src/timeouts.rs
  - crates/nono-cli/tests/supervisor_ipc_robustness.rs
autonomous: true
requirements: [REQ-IPC-01]

must_haves:
  truths:
    - "A bounded supervisor IPC read timeout exists as a named constant (5s default), not a bare literal"
    - "NONO_SUPERVISOR_IPC_READ_TIMEOUT env override is honored and clamped to MAX_TIMEOUT"
    - "An invalid/unparseable env value falls back to the 5s default (never unbounded)"
    - "A net-new cross-platform integration test file exists that compiles on all 3 platforms"
  artifacts:
    - path: "crates/nono-cli/src/timeouts.rs"
      provides: "SUPERVISOR_IPC_READ_TIMEOUT const + supervisor_ipc_read_timeout() accessor + unit tests"
      contains: "SUPERVISOR_IPC_READ_TIMEOUT"
    - path: "crates/nono-cli/tests/supervisor_ipc_robustness.rs"
      provides: "Wave 0 net-new integration test scaffold (cfg-structured, compiles on Unix + Windows)"
      contains: "supervisor_ipc"
  key_links:
    - from: "crates/nono-cli/src/timeouts.rs::supervisor_ipc_read_timeout"
      to: "env_duration_secs"
      via: "delegates env parse + MAX_TIMEOUT clamp to the shared helper"
      pattern: "env_duration_secs\\(\"NONO_SUPERVISOR_IPC_READ_TIMEOUT\""
---

<objective>
Add the bounded supervisor-IPC read-timeout configuration knob (D-01) to the centralized `timeouts.rs` module and create the net-new cross-platform integration test scaffold (Wave 0) that the two platform plans (59-02 Unix, 59-03 Windows) will fill in.

Purpose: Establishes the single dependency both platform plans consume — the un-cfg'd `SUPERVISOR_IPC_READ_TIMEOUT` constant (5s, matching upstream `d1851c9`) with a `NONO_*` env override + `MAX_TIMEOUT` clamp — and the test file that compiles on all 3 platforms so SC1/SC2/SC4 tests have a home. Without this Wave 1 artifact, the platform plans have no constant to wire and no test file to extend.

Output: A modified `timeouts.rs` (const + accessor + unit tests) and a net-new `crates/nono-cli/tests/supervisor_ipc_robustness.rs` scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-supervisor-ipc-robustness/59-CONTEXT.md
@.planning/phases/59-supervisor-ipc-robustness/59-RESEARCH.md
@.planning/phases/59-supervisor-ipc-robustness/59-PATTERNS.md
@.planning/phases/59-supervisor-ipc-robustness/59-VALIDATION.md
@crates/nono-cli/src/timeouts.rs
@crates/nono-cli/tests/aipc_handle_brokering_integration.rs

<interfaces>
<!-- The exact in-file pattern this plan mirrors (from 59-PATTERNS.md). Use directly — no exploration needed. -->

From crates/nono-cli/src/timeouts.rs (existing precedent — DETACH_STARTUP_TIMEOUT pair, lines 37-39 / 72-76):
- `pub const DETACH_STARTUP_TIMEOUT: Duration = Duration::from_secs(30);`
- `pub fn detach_startup_timeout() -> Duration { env_duration_secs("NONO_DETACH_STARTUP_TIMEOUT", DETACH_STARTUP_TIMEOUT) }`

Shared clamp helper to REUSE AS-IS (timeouts.rs:105, NOT #[cfg(unix)] — compiles on Windows too):
- `fn env_duration_secs(var: &str, default: Duration) -> Duration` — parses u64 seconds, clamps to `MAX_TIMEOUT` (timeouts.rs:103, 3600s) with a warn, falls back to `default` on unparseable.

Test cfg-gate idiom (aipc_handle_brokering_integration.rs header):
- `#![cfg(target_os = "windows")]` produces an empty test binary on Linux/macOS. THIS phase needs BOTH a Unix-gated and a Windows-gated test in one file → use per-test `#[cfg(...)]`, NOT a file-level `#![cfg(target_os = "windows")]`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SUPERVISOR_IPC_READ_TIMEOUT const + accessor + unit tests to timeouts.rs</name>
  <files>crates/nono-cli/src/timeouts.rs</files>
  <read_first>
    - crates/nono-cli/src/timeouts.rs (the file being modified — read the full module; note DETACH_STARTUP_TIMEOUT pair at 37-39/72-76, env_duration_secs at 105, MAX_TIMEOUT at 103, and the existing test module)
    - 59-PATTERNS.md §"crates/nono-cli/src/timeouts.rs" (the exact analog + the "do NOT cfg-gate" instruction)
    - 59-RESEARCH.md §"Add the timeout constant + accessor (timeouts.rs)" + §"Don't Hand-Roll" (reuse env_duration_secs)
  </read_first>
  <action>
    Add a new un-cfg'd const `SUPERVISOR_IPC_READ_TIMEOUT: Duration = Duration::from_secs(5)` (5s = upstream d1851c9 per D-01) and accessor `supervisor_ipc_read_timeout()` that delegates to `env_duration_secs("NONO_SUPERVISOR_IPC_READ_TIMEOUT", SUPERVISOR_IPC_READ_TIMEOUT)`. Mirror the `DETACH_STARTUP_TIMEOUT` / `detach_startup_timeout()` pair exactly. Do NOT cfg-gate the const or accessor — Windows reads the same value per D-02, and `env_duration_secs` is already non-cfg'd. Do NOT add a per-run CLI flag (D-01: internal knob, env override only). Do NOT use a bare literal at any call site.
    Add unit tests in the existing `timeouts.rs` test module covering: (a) default value is 5s when env absent; (b) `NONO_SUPERVISOR_IPC_READ_TIMEOUT=1` yields 1s; (c) an over-`MAX_TIMEOUT` value (e.g. 99999) clamps to MAX_TIMEOUT; (d) an unparseable value (e.g. "abc") falls back to the 5s default. Every test MUST save and restore the env var per the CLAUDE.md parallel-test env rule (save `std::env::var(...).ok()`, set, exercise, then restore via set_var/remove_var) and use the test module's existing serialization lock if one exists. Mark test module `#[allow(clippy::unwrap_used)]` only where already established in that module.
  </action>
  <verify>
    <automated>cargo test -p nono-cli timeouts:: -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p nono-cli timeouts::` exits 0 with the 4 new tests passing.
    - `timeouts.rs` contains the literal token `SUPERVISOR_IPC_READ_TIMEOUT` and `NONO_SUPERVISOR_IPC_READ_TIMEOUT`.
    - The const is NOT wrapped in `#[cfg(unix)]` (grep confirms no cfg attribute directly above `pub const SUPERVISOR_IPC_READ_TIMEOUT`).
    - No bare `Duration::from_secs(5)` literal appears at any IPC call site — only the const/accessor.
    - `cargo build -p nono-cli` succeeds; no new `.unwrap()`/`.expect()` outside test modules.
  </acceptance_criteria>
  <done>The supervisor IPC read-timeout knob exists as a named const + env-override accessor in timeouts.rs with passing parse/clamp/default unit tests; both platform plans can call `crate::timeouts::supervisor_ipc_read_timeout()`.</done>
</task>

<task type="auto">
  <name>Task 2: Create the net-new cross-platform integration test scaffold</name>
  <files>crates/nono-cli/tests/supervisor_ipc_robustness.rs</files>
  <read_first>
    - crates/nono-cli/tests/aipc_handle_brokering_integration.rs (cfg-gate structure + SAFETY-on-unsafe + last_os_error-in-assert idioms)
    - 59-PATTERNS.md §"crates/nono-cli/tests/supervisor_ipc_robustness.rs (test, net-new — Wave 0)" (cfg structure: per-test gates, NOT a file-level windows-only gate)
    - 59-VALIDATION.md §"Wave 0 Requirements" + §"Per-Task Verification Map"
  </read_first>
  <action>
    Create `crates/nono-cli/tests/supervisor_ipc_robustness.rs`. It MUST compile (produce a valid test binary) on all 3 platforms — Unix uses a socketpair-fork shape, Windows uses the AIPC pipe — so use per-test `#[cfg(unix)]` / `#[cfg(target_os = "windows")]` attributes rather than a file-level `#![cfg(target_os = "windows")]`. Add `#![allow(clippy::unwrap_used)]` (test file). In this Wave 1 plan, populate the file with: (1) a module-level doc comment stating the file's purpose and the cross-platform-compile contract; (2) one always-compiling sanity test `fn scaffold_compiles()` (no cfg gate) that asserts `nono_cli::timeouts::supervisor_ipc_read_timeout()` returns a non-zero `Duration` and respects a `NONO_SUPERVISOR_IPC_READ_TIMEOUT=1` override (with save/restore) — this proves the accessor is reachable from the integration-test crate and pins the contract. Leave clearly-labeled placeholder stubs (commented TODO markers naming `reconnect_survival` (SC1) and `bounded_read_timeout` (SC2) for Unix in 59-02, and the Windows AIPC variants for 59-03) so the platform plans know exactly where to add their cfg-gated tests. Do NOT add `#[ignore]`'d fake tests; the only real test here is `scaffold_compiles`.
    Verify the accessor is exported from the `nono_cli` crate root (it is `pub` in `timeouts.rs` and `timeouts` is a `pub mod`); if the integration test cannot reach it, surface that as a blocker rather than duplicating the constant.
  </action>
  <verify>
    <automated>cargo test -p nono-cli --test supervisor_ipc_robustness</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p nono-cli --test supervisor_ipc_robustness` exits 0 (the `scaffold_compiles` test passes).
    - `cargo build -p nono-cli --tests` succeeds (file produces a valid binary).
    - The file contains NO file-level `#![cfg(target_os = "windows")]` (grep confirms absence); platform tests will use per-test cfg attributes.
    - The file references `supervisor_ipc_read_timeout` (proves Task-1 accessor is reachable from the test crate).
    - TODO placeholders naming `reconnect_survival`, `bounded_read_timeout`, and the Windows AIPC variants are present for the downstream plans.
  </acceptance_criteria>
  <done>A net-new `supervisor_ipc_robustness.rs` integration test file exists, compiles on all platforms, proves the new accessor is reachable, and is staged with labeled insertion points for the Unix (59-02) and Windows (59-03) tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → CLI config | `NONO_SUPERVISOR_IPC_READ_TIMEOUT` is operator-supplied untrusted input parsed into a `Duration` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-02 | Denial of Service | `timeouts::supervisor_ipc_read_timeout()` env parse | mitigate | Reuse `env_duration_secs` which clamps to `MAX_TIMEOUT` (3600s) and falls back to the 5s default on unparseable input — an attacker-supplied env cannot set an unbounded or overflowing timeout; verified by the clamp + invalid-value unit tests (Task 1). |
| T-59-02b | Tampering | bare timeout literal drift | accept | D-01 forbids bare literals; mitigated structurally by routing all reads through the single const/accessor (no per-call literal exists to tamper). |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — RESEARCH.md §"Package Legitimacy Audit" confirms zero new deps (only vendored `libc`, `windows-sys 0.59`, `std`). No `[ASSUMED]`/`[SUS]` packages → no legitimacy checkpoint required. |
</threat_model>

<verification>
- `cargo test -p nono-cli timeouts::` green (4 new parse/clamp/default tests).
- `cargo test -p nono-cli --test supervisor_ipc_robustness` green (scaffold sanity test).
- `cargo build -p nono-cli --tests` succeeds on this Windows host.
- No bare `Duration::from_secs(5)` literal at any IPC site; const + accessor only.
</verification>

<success_criteria>
- `SUPERVISOR_IPC_READ_TIMEOUT` (5s) + `supervisor_ipc_read_timeout()` exist, un-cfg'd, with env override + MAX_TIMEOUT clamp.
- Env override + clamp + invalid-fallback covered by passing unit tests with save/restore.
- `crates/nono-cli/tests/supervisor_ipc_robustness.rs` exists, compiles on all platforms, and reaches the accessor.
</success_criteria>

<output>
Create `.planning/phases/59-supervisor-ipc-robustness/59-01-SUMMARY.md` when done.
</output>
