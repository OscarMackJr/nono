---
phase: 90-v3-0-host-gated-uat-drain
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/90-v3-0-host-gated-uat-drain/90-HUMAN-UAT.md
autonomous: false
requirements: [DRAIN-01, DRAIN-02, DRAIN-03]
must_haves:
  truths:
    - "Each of the 5 closeout gates is run via verify-dark.ps1 -File on this dev host and its verdict captured"
    - "90-HUMAN-UAT.md records a per-gate verdict (PASS/FAIL/SKIP_HOST_UNAVAILABLE) for all 5 gates"
    - "Each requirement's residual live step is explicitly recorded as operator-gated host-gated tech-debt"
    - "Verdict JSON persistence stays owned by verify-dark.ps1 (the doc references, does not re-implement)"
  artifacts:
    - path: ".planning/phases/90-v3-0-host-gated-uat-drain/90-HUMAN-UAT.md"
      provides: "Per-gate verdict + residual-live-step record (D-05) mirroring 88-HUMAN-UAT.md"
      contains: "## Tests"
  key_links:
    - from: ".planning/phases/90-v3-0-host-gated-uat-drain/90-HUMAN-UAT.md"
      to: ".nono-runtime/verdicts/<gate>.json"
      via: "references verify-dark.ps1-persisted verdicts (WR-04)"
      pattern: "verdict"
---

<objective>
Close out DRAIN-01/02/03 by running the EXISTING mature `verify-dark.ps1` scripted gates on
this dev host, capturing verdicts, and recording per-gate verdicts plus explicitly
host-gated residual live steps in a new `90-HUMAN-UAT.md` (D-05). No gate-script code
changes (D-04) — a broken gate is a debug finding, not planned work here.

Purpose: Each host-gated v3.0 UAT item collapses to its unattended scripted gate with the
residual live step explicitly operator-gated.
Output: `90-HUMAN-UAT.md` with verdict rows for all 5 gates (D-06 mapping) and their residual
live steps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-v3-0-host-gated-uat-drain/90-CONTEXT.md
@.planning/phases/90-v3-0-host-gated-uat-drain/90-RESEARCH.md
@.planning/phases/88-feature-dependency-cherry-pick-wave/88-HUMAN-UAT.md

<gate_map>
<!-- D-06 gate→requirement mapping; verdict classes + invocation contract from RESEARCH.md -->
- DRAIN-01 → scripts/gates/clean-host-install.ps1, scripts/gates/deploy-silent-install.ps1
- DRAIN-02 → scripts/gates/wfp-egress-isolation.ps1, scripts/gates/egress-policy-deny.ps1
- DRAIN-03 → scripts/gates/telemetry-event-emit.ps1

Invocation (MEMORY durable + RESEARCH Pitfall 5): `pwsh -File scripts/verify-dark.ps1 -Gate <name>`.
NEVER `pwsh -Command "<bare path>"` (swallows exit N → 1).
Exit-code mapping (single gate): PASS=0, FAIL=2, SKIP_HOST_UNAVAILABLE=3, harness-internal=4.
verify-dark.ps1 persists `.nono-runtime/verdicts/<gate>.json` (WR-04) BEFORE the stdout line.
Expected on THIS dev host (RESEARCH): clean-host-install + deploy-silent-install likely SKIP
(needs fresh Win11 VM); egress-policy-deny may PASS (proxy-layer, in-process);
wfp-egress-isolation likely SKIP (needs admin + WFP service + 2nd host); telemetry-event-emit
SKIP unless run elevated with nono on PATH.
</gate_map>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The 5 existing verify-dark.ps1 gates (no code changes) to be run on this dev host.</what-built>
  <read_first>
    - scripts/verify-dark.ps1 (verdict classes, exit-code mapping, WR-04 persistence, -Gate/-All)
    - scripts/gates/telemetry-event-emit.ps1 (admin Test-Precondition; the -File invocation rule in its header)
  </read_first>
  <how-to-verify>
    Run each gate from a PowerShell 7 prompt (elevate the prompt to Administrator so
    telemetry-event-emit / wfp-egress-isolation preconditions can be evaluated rather than
    auto-skipped), using the -File form (NEVER -Command with a bare path):
      pwsh -File scripts/verify-dark.ps1 -Gate clean-host-install
      pwsh -File scripts/verify-dark.ps1 -Gate deploy-silent-install
      pwsh -File scripts/verify-dark.ps1 -Gate wfp-egress-isolation
      pwsh -File scripts/verify-dark.ps1 -Gate egress-policy-deny
      pwsh -File scripts/verify-dark.ps1 -Gate telemetry-event-emit
    For each, capture: the stdout verdict line, the process exit code (0/2/3/4), and the
    persisted `.nono-runtime/verdicts/<gate>.json`. A SKIP_HOST_UNAVAILABLE (exit 3) is the
    EXPECTED, acceptable drained-to-tech-debt outcome for the live-host steps — it is NOT a
    failure. Only a verdict FAIL (exit 2) on a gate that should run here is a real issue; if a
    gate is found broken, stop and report it as a debug finding (D-04 — do NOT patch gate code
    in this plan).
  </how-to-verify>
  <resume-signal>Paste the 5 verdict lines + exit codes (and note which required elevation), or describe any broken gate.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Author 90-HUMAN-UAT.md with per-gate verdicts and host-gated residuals (D-05)</name>
  <files>.planning/phases/90-v3-0-host-gated-uat-drain/90-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/88-feature-dependency-cherry-pick-wave/88-HUMAN-UAT.md (exact doc shape to mirror: frontmatter, ## Current Test, ## Tests with expected/why_human/result, ## Summary totals, ## Gaps)
    - scripts/verify-dark.ps1 (to confirm the verdict JSON path referenced)
  </read_first>
  <action>
    Create 90-HUMAN-UAT.md mirroring the 88-HUMAN-UAT.md structure. Frontmatter: `status: partial`,
    `phase: 90-v3-0-host-gated-uat-drain`, `source: [90-RESEARCH.md, v3.0-MILESTONE-AUDIT.md]`,
    `started`/`updated` timestamps. Under `## Tests`, add one `###` block per gate (5 blocks),
    each grouped under its requirement (DRAIN-01: clean-host-install + deploy-silent-install;
    DRAIN-02: wfp-egress-isolation + egress-policy-deny; DRAIN-03: telemetry-event-emit). For
    each block record: `expected:` (what the gate asserts), `why_human:` (the host-gated reason —
    e.g. needs fresh Win11 VM / needs admin + WFP service + 2nd host / needs live SIEM ingestion),
    and `result:` (the actual verdict captured in the checkpoint: PASS / FAIL / SKIP_HOST_UNAVAILABLE,
    with the exit code, referencing the persisted `.nono-runtime/verdicts/<gate>.json` — do NOT
    re-implement persistence, only reference it). Add an explicit residual-live-step note per
    requirement (DRAIN-01 live silent-MSI on clean VM; DRAIN-02 live dual-layer proxy+kernel-WFP
    block on a 2nd host; DRAIN-03 live SIEM ingestion + admin opt-out/min_severity HKLM→emit),
    each marked operator-gated host-gated tech-debt. Fill `## Summary` totals
    (total/passed/issues/pending/skipped/blocked) from the captured verdicts. Add a `## Gaps`
    section noting any FAIL (none expected) and the operator-gated residuals.
  </action>
  <verify>
    <automated>grep -c '^### ' .planning/phases/90-v3-0-host-gated-uat-drain/90-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - 90-HUMAN-UAT.md exists with YAML frontmatter (status, phase, source, started, updated)
    - It contains a `### ` verdict block for each of the 5 gates (grep count >= 5)
    - Each block has `expected:`, `why_human:`, and `result:` with the captured verdict + exit code
    - Each of DRAIN-01/02/03 has an explicit operator-gated residual-live-step note
    - The doc references `.nono-runtime/verdicts/<gate>.json` and does NOT re-implement persistence
    - `## Summary` totals reflect the captured verdicts
  </acceptance_criteria>
  <done>90-HUMAN-UAT.md records all 5 gate verdicts and the 3 operator-gated residual live steps, mirroring the 88-HUMAN-UAT.md pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/dev host → gate scripts | Gate scripts evaluate security-control state (install integrity, egress block, telemetry emit) on the host |
| gate verdict → closeout record | Verdicts cross from verify-dark.ps1 persistence into the human-UAT doc |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-06 | Repudiation | Gate verdict provenance | mitigate | verify-dark.ps1 persists `.nono-runtime/verdicts/<gate>.json` (WR-04) before stdout; the doc references the persisted verdict rather than re-asserting it |
| T-90-07 | Spoofing / false-PASS | Swallowed exit code | mitigate | Invoke gates via `-File` only (never `-Command "<bare path>"`) so PASS/FAIL/SKIP exit distinctions survive (RESEARCH Pitfall 5 / MEMORY durable) |
| T-90-08 | Tampering | Gate-script edits during closeout | accept | D-04: no gate-script code changes in this phase; a broken gate is escalated as a debug finding, not patched here |
| T-90-09 | Denial / false-confidence | Treating SKIP as drained-complete | accept | Residual live steps recorded explicitly as operator-gated host-gated tech-debt — closeout asserts the scripted-gate collapse, not live-host proof |
| T-90-SC | Tampering | npm/pip/cargo installs | accept | No package installs; no Rust toolchain needed for this plan |
</threat_model>

<verification>
- All 5 gates run via `-File` with verdicts + exit codes captured.
- 90-HUMAN-UAT.md has a verdict block per gate (>= 5) and an operator-gated residual per DRAIN-01/02/03.
- No gate-script files modified (D-04) — files_modified is the doc only.
</verification>

<success_criteria>
- DRAIN-01/02/03 each collapse to their unattended scripted gate(s) with the residual live step explicitly host-gated, recorded in 90-HUMAN-UAT.md.
</success_criteria>

<output>
Create `.planning/phases/90-v3-0-host-gated-uat-drain/90-02-SUMMARY.md` when done.
</output>
