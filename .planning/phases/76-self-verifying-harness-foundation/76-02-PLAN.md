---
phase: 76-self-verifying-harness-foundation
plan: 02
type: execute
wave: 2
depends_on: ["76-01"]
files_modified:
  - scripts/gates/harness-self-check.ps1
autonomous: true
requirements: [DARK-01]

must_haves:
  truths:
    - "scripts/gates/harness-self-check.ps1 defines a Test-Precondition that always returns $null (runs on any Win11 host) and an Invoke-Gate that returns a PASS verdict (ROADMAP SC5, D-11)"
    - "scripts/verify-dark.ps1 --gate harness-self-check exits 0 with a JSON PASS verdict on a Win11 host — THE Phase 76 acceptance gate (ROADMAP SC5)"
    - "The self-check gate trivially exercises framework wiring: emit a verdict object, persist its file, and round-trip the JSON (D-11)"
    - "The gate returns its verdict to the runner and never calls exit itself — only the runner maps verdict->exit (D-02, PATTERNS exit/return convention)"
  artifacts:
    - path: "scripts/gates/harness-self-check.ps1"
      provides: "First gate file; Test-Precondition + Invoke-Gate contract reference for phases 77-80"
      min_lines: 30
      contains: "function Invoke-Gate"
  key_links:
    - from: "scripts/verify-dark.ps1"
      to: "scripts/gates/harness-self-check.ps1"
      via: "--gate harness-self-check dot-sources the file and calls Test-Precondition then Invoke-Gate"
      pattern: "harness-self-check"
    - from: "scripts/gates/harness-self-check.ps1::Invoke-Gate"
      to: ".nono-runtime/verdicts/harness-self-check.json"
      via: "round-trip self-check reads back the persisted verdict file"
      pattern: "verdicts.*harness-self-check"
---

<objective>
Deliver the first gate file `scripts/gates/harness-self-check.ps1` and prove the framework
end-to-end: `scripts/verify-dark.ps1 --gate harness-self-check` exits 0 with a JSON `PASS` verdict
on a Win11 host. This gate's `Test-Precondition` always returns `$null` (runs on any Win11 host —
ROADMAP SC5) and its `Invoke-Gate` trivially verifies framework wiring (emit + persist + JSON
round-trip) and returns `PASS` (D-11).

Purpose: This IS the Phase 76 acceptance gate. It proves the harness functions before any feature
gate exists, and it is the canonical reference implementation of the `Test-Precondition` +
`Invoke-Gate` contract that phases 77-80 copy.

Output: `scripts/gates/harness-self-check.ps1` (creating the `scripts/gates/` directory) and a
verified passing run of the unattended Phase 76 gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/76-self-verifying-harness-foundation/76-CONTEXT.md
@.planning/phases/76-self-verifying-harness-foundation/76-PATTERNS.md
@.planning/phases/76-self-verifying-harness-foundation/76-01-SUMMARY.md
@scripts/verify-dark.ps1

<interfaces>
<!-- The runner from plan 76-01 dot-sources this gate and calls these two functions.
     The gate RETURNS its verdict object; it MUST NOT call exit (the runner owns exit mapping). -->

Required gate-file exports (D-05):
  function Test-Precondition  # harness-self-check ALWAYS returns $null (runs on any Win11 host, D-11)
  function Invoke-Gate        # returns the verdict object: PASS on success

Verdict object the runner expects (D-01, [ordered]@{} key order):
  gate="harness-self-check", verdict="PASS", reason="framework functional", detail=@{}, timestamp=<ISO>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the harness-self-check gate file</name>
  <files>scripts/gates/harness-self-check.ps1</files>
  <behavior>
    - Test-Precondition returns $null unconditionally (D-11: runs on any Win11 host)
    - Invoke-Gate, on a wired framework, returns a verdict object with verdict="PASS" and reason="framework functional"
    - Invoke-Gate exercises: (a) a verdict object serializes via ConvertTo-Json -Depth 5; (b) ConvertFrom-Json round-trips matching gate/verdict fields; (c) a persisted file under .nono-runtime/verdicts/ round-trips
    - A failed self-check assertion throws (surfacing to the runner as a harness-internal error per D-07), and NEVER returns PASS on a broken framework
  </behavior>
  <read_first>
    - scripts/verify-dark.ps1 (the runner from 76-01 — confirm exactly how it dot-sources the gate, what it expects Test-Precondition / Invoke-Gate to return, and the verdict object key order it emits, so this gate's return shape matches)
    - scripts/check-upstream-drift.ps1 (lines 224-243: `[ordered]@{}` + `ConvertTo-Json -Depth N -Compress` house JSON style to mirror for the round-trip assertion)
    - scripts/validate-windows-msi-contract.ps1 (lines 100-129: `Assert-Equal` / `Assert-True` throw-on-failure assertion-helper idiom)
    - scripts/test-windows-shell-write-deny.ps1 (line ~60: ISO timestamp `Get-Date -Format 'yyyy-MM-ddTHH:mm:ss.fffZ'`)
    - .planning/phases/76-self-verifying-harness-foundation/76-CONTEXT.md (D-05 contract, D-11 self-check gate, "specifics" verdict JSON example: gate="harness-self-check", verdict="PASS", reason="framework functional", detail={})
    - .planning/phases/76-self-verifying-harness-foundation/76-PATTERNS.md (§ `scripts/gates/harness-self-check.ps1` — exact contract shape + round-trip assertion idiom + exit/return convention: the gate RETURNS, never calls exit)
  </read_first>
  <action>
    Create `scripts/gates/harness-self-check.ps1` (creating the `scripts/gates/` directory). Export
    exactly two functions per D-05.

    `function Test-Precondition`: return `$null` unconditionally (D-11 — the self-check always runs
    on any Win11 host; this satisfies ROADMAP SC5). No host probing.

    `function Invoke-Gate`: trivially verify the framework wiring (D-11) and return the verdict
    object. Build a candidate verdict with `[ordered]@{}` keyed `gate="harness-self-check"`,
    `verdict="PASS"`, `reason="framework functional"`, `detail=@{}`,
    `timestamp = Get-Date -Format 'yyyy-MM-ddTHH:mm:ss.fffZ'`. Self-check assertions (use local
    `Assert-Equal`/`Assert-True` throw-on-failure helpers mirroring validate-windows-msi-contract.ps1):
    (a) serialize the verdict via `ConvertTo-Json -Depth 5` and assert it is a non-empty string;
    (b) `ConvertFrom-Json` it back and assert the round-tripped `gate` and `verdict` fields equal the
    originals; (c) verify the persistence file path the runner will write
    (`.nono-runtime/verdicts/harness-self-check.json`, resolved relative to repo root the same way
    the runner does) is writable/round-trips — keep this lightweight and consistent with how the
    runner persists. Return the verdict object on success. A failing assertion MUST `throw` (it
    surfaces to the runner as a harness-internal error per D-07) — NEVER return PASS on a broken
    framework, NEVER swallow the assertion.

    Convention (D-02): this gate RETURNS the verdict object to the runner and does NOT call `exit`
    itself — only the runner maps verdict→exit. This diverges from the standalone
    test-windows-shell-write-deny.ps1 which calls `exit` directly; this file is a dot-sourced plugin.
  </action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/verify-dark.ps1 -Gate harness-self-check; if ($LASTEXITCODE -eq 0) { exit 0 } else { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/gates/harness-self-check.ps1` exists and defines `function Test-Precondition` AND `function Invoke-Gate`
    - The file contains NO top-level `exit` call (the gate returns; the runner maps verdict->exit)
    - `pwsh -NoProfile -File scripts/verify-dark.ps1 -Gate harness-self-check` prints a single JSON object whose `verdict` field is `"PASS"` and whose `gate` field is `"harness-self-check"`
    - That same invocation exits 0
    - After that invocation, `.nono-runtime/verdicts/harness-self-check.json` exists and parses as JSON with `verdict` = `"PASS"`
    - `Test-Precondition` returns `$null` (verifiable by dot-sourcing the file and calling it: `. scripts/gates/harness-self-check.ps1; ($null -eq (Test-Precondition))` is `$true`)
  </acceptance_criteria>
  <done>
    The harness-self-check gate exists, returns a PASS verdict through the runner, persists its
    verdict file, and round-trips its JSON. It is the contract reference for phases 77-80.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove the five ROADMAP success criteria end-to-end (acceptance run)</name>
  <files>scripts/gates/harness-self-check.ps1</files>
  <read_first>
    - scripts/verify-dark.ps1 (the runner)
    - scripts/gates/harness-self-check.ps1 (the gate from Task 1)
    - .planning/ROADMAP.md (§ Phase 76 — the 5 Success Criteria this run must satisfy)
    - .planning/phases/76-self-verifying-harness-foundation/76-CONTEXT.md (D-02 exit codes, D-04 --gate isolation, D-06 SKIP path)
  </read_first>
  <action>
    No new production file — this task runs the harness on the Win11 host and records the evidence
    that all five ROADMAP SC are satisfied. Run each acceptance assertion and capture exit codes +
    JSON output into the SUMMARY:
    (SC1) `verify-dark.ps1 --gate harness-self-check` runs with no interactive prompts and prints a
    machine-readable JSON verdict + structured exit code.
    (SC2) the printed verdict is one of PASS/FAIL/SKIP_HOST_UNAVAILABLE (here PASS) — the typed gate
    contract.
    (SC3) `--gate harness-self-check` exercises exactly one gate in isolation (no other gate runs).
    (SC4) construct a TEMPORARY stub gate `scripts/gates/_skip-probe.ps1` whose `Test-Precondition`
    returns a reason string; confirm `--gate _skip-probe` emits `SKIP_HOST_UNAVAILABLE` and exits 3
    (not a crash, not ambiguous); then DELETE the stub so only `harness-self-check.ps1` remains.
    (SC5) `--gate harness-self-check` exits 0 with a PASS verdict.
    Also confirm the negative: `--gate does-not-exist` exits 1 (or >=4), proving unknown-gate is a
    harness-internal error, not a FAIL. Record all observed exit codes and the emitted JSON in the
    SUMMARY's verification section. If any assertion fails, fix the runner/gate before declaring done
    (do not record a passing result you did not observe).
  </action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/verify-dark.ps1 -Gate harness-self-check; $a=$LASTEXITCODE; pwsh -NoProfile -File scripts/verify-dark.ps1 -Gate does-not-exist; $b=$LASTEXITCODE; if ($a -eq 0 -and ($b -eq 1 -or $b -ge 4)) { exit 0 } else { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - SC5: `pwsh -NoProfile -File scripts/verify-dark.ps1 -Gate harness-self-check` exits 0 and prints `"verdict":"PASS"`
    - SC4: a temporary stub gate whose Test-Precondition returns a reason string yields `"verdict":"SKIP_HOST_UNAVAILABLE"` and exit 3 — the stub is DELETED after the check (only `harness-self-check.ps1` remains in `scripts/gates/`)
    - Negative: `pwsh -NoProfile -File scripts/verify-dark.ps1 -Gate does-not-exist` exits 1 or >=4 (harness-internal error), NOT 2
    - `scripts/gates/` after this task contains exactly one file: `harness-self-check.ps1` (no leftover stub)
    - The SUMMARY records the observed exit codes and JSON for SC1-SC5 plus the negative case
  </acceptance_criteria>
  <done>
    All five ROADMAP Phase 76 success criteria are demonstrated on the Win11 host with recorded
    evidence; the unknown-gate harness-internal-error path is confirmed; no stub files remain.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| self-check verdict → "framework is functional" claim | A false PASS here would let every downstream feature gate (77-80) run on a broken harness, masking real failures. |
| gate file → runner dot-source | `harness-self-check.ps1` runs in the runner's scope; a gate that calls `exit` or returns a malformed verdict could corrupt the runner's exit mapping. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-05 | Tampering/Elevation | self-check returns PASS on a broken framework | mitigate | D-11/D-07: `Invoke-Gate` runs real emit + persist + round-trip assertions that `throw` on mismatch; a thrown assertion surfaces to the runner as a harness-internal error, never PASS. The gate cannot return PASS without the round-trip succeeding. |
| T-76-06 | Tampering | gate calls `exit` and bypasses the runner's verdict→exit mapping | mitigate | Contract (D-02, PATTERNS exit/return convention): the gate RETURNS its verdict; acceptance criterion asserts the file contains NO top-level `exit`. Only the runner owns exit mapping. |
| T-76-07 | Spoofing | leftover stub gate (`_skip-probe.ps1`) persists and pollutes `--all` later | mitigate | Task 2 acceptance criterion requires the stub be deleted and `scripts/gates/` contain exactly `harness-self-check.ps1` afterward — verified by directory listing. |
| T-76-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (pure PowerShell). No `[ASSUMED]`/`[SUS]` packages introduced. |
</threat_model>

<verification>
- `verify-dark.ps1 --gate harness-self-check` exits 0 with `"verdict":"PASS"` (ROADMAP SC5).
- A temporary SKIP stub yields exit 3 + `SKIP_HOST_UNAVAILABLE` (ROADMAP SC4), then is deleted.
- `--gate does-not-exist` exits 1 (or >=4) — harness-internal error, not FAIL (D-05/D-07).
- `.nono-runtime/verdicts/harness-self-check.json` exists and round-trips with `verdict=PASS`.
- `scripts/gates/` contains exactly `harness-self-check.ps1` after the acceptance run.
</verification>

<success_criteria>
- All five ROADMAP Phase 76 success criteria demonstrated on the Win11 host.
- The harness-self-check gate is the canonical Test-Precondition + Invoke-Gate contract reference.
- DARK-01 satisfied: a single-invocation unattended script emits a machine-readable PASS verdict.
</success_criteria>

<output>
Create `.planning/phases/76-self-verifying-harness-foundation/76-02-SUMMARY.md` when done.
</output>
