---
phase: "79"
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - crates/nono-ts/src/windows_confined_run.rs
  - "C:/Users/OMack/nono-ts/tests/test_confined_run_default.js"  # sibling repo
  - "C:/Users/OMack/nono-ts/package.json"  # sibling repo
autonomous: false
requirements:
  - TSRG-01
user_setup:
  - service: nono-ts napi build
    why: "Every Rust change to windows_confined_run.rs requires napi build --platform --release on the Win11 host before npm test exercises new behavior; the rebuilt .node binary regenerates index.d.ts. Stale .node = SC4 passes against old code."
    env_vars:
      - name: NONO_EXE
        source: "Set to the dev-layout nono.exe path (e.g. C:\\Users\\OMack\\Nono\\target\\release\\nono.exe) before running npm test on the host"
must_haves:
  truths:
    - "confinedRun with no profile argument reaches the Low-IL broker arm (windows_low_il_broker: true via nono-ts-default) instead of the WriteRestricted arm"
    - "The D-03 default profile injection is the FIRST statement in confined_run, before the validation guard"
    - "confinedRun auto-covers only the resolved target exe's parent directory (no cwd, no ancestors)"
    - "A napi integration test confirms confinedRun with no profile/no allow flags exits 0 on Windows"
    - "Existing callers that pass an explicit profile are not affected"
  artifacts:
    - path: "crates/nono-ts/src/windows_confined_run.rs"
      provides: "D-03 default profile injection + D-04 exe-dir auto-cover"
      contains: "nono-ts-default"
    - path: "nono-ts/tests/test_confined_run_default.js"
      provides: "SC4 napi integration test"
      contains: "confinedRun"
    - path: "nono-ts/package.json"
      provides: "npm test wired to the new integration test"
  key_links:
    - from: "crates/nono-ts/src/windows_confined_run.rs"
      to: "crates/nono-cli/data/policy.json (via nono.exe --profile nono-ts-default)"
      via: "build_nono_run_args emits --profile nono-ts-default when profile=None after D-03"
      pattern: "nono-ts-default"
    - from: "nono-ts/tests/test_confined_run_default.js"
      to: "crates/nono-ts/src/windows_confined_run.rs"
      via: "require('../index.js').confinedRun (loaded from nono.win32-x64-msvc.node)"
      pattern: "confinedRun"
---

<objective>
Wire D-03 (default profile injection) and D-04 (auto-cover exe-dir) into confined_run in nono-ts, add a napi integration test confirming the no-profile path reaches the Low-IL broker arm, and update npm test wiring. The nono-ts-default profile was added to policy.json in Plan 01 — this plan wires the nono-ts side that selects it.

Purpose: Satisfy TSRG-01 — confinedRun defaults to Low-IL broker arm and auto-covers the target executable's directory so a caller gets a working confined run with no manual profile or coverage flags.

Output: Modified crates/nono-ts/src/windows_confined_run.rs (D-03/D-04), new tests/test_confined_run_default.js (SC4), updated package.json test script. Requires napi rebuild on Win11 host before verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/79-wfp-egress-isolation-nono-ts-ergonomics/79-CONTEXT.md
@.planning/phases/79-wfp-egress-isolation-nono-ts-ergonomics/79-RESEARCH.md
@.planning/phases/79-wfp-egress-isolation-nono-ts-ergonomics/79-PATTERNS.md

<interfaces>
<!-- Two-repo note: this plan modifies files in the nono-ts sibling repo      -->
<!--   at C:\Users\OMack\nono-ts\ — NOT in C:\Users\OMack\Nono\.               -->
<!--   git commits for nono-ts files must be made in C:\Users\OMack\nono-ts.   -->
<!--   The nono-ts-default profile (added to policy.json in Plan 01) is        -->
<!--   consumed via nono.exe --profile nono-ts-default (EMBEDDED_POLICY_JSON). -->
<!--   Plan 02 depends on Plan 01's profile being in the rebuilt nono.exe.     -->

<!-- From crates/nono-ts/src/windows_confined_run.rs (verified current, lines 76-144): -->
<!--                                                                            -->
<!--   fn build_nono_run_args(                                                  -->
<!--     cmd: &mut Command, profile: Option<&str>,                              -->
<!--     allow: Option<&[String]>, cwd_allow: Option<&str>,                    -->
<!--   ) {                                                                      -->
<!--     if let Some(p) = profile { cmd.arg("--profile").arg(p); }             -->
<!--     if let Some(paths) = allow { for path in paths { cmd.arg("--allow").arg(path); } } -->
<!--     if let Some(cwd) = cwd_allow { cmd.arg("--allow").arg(cwd).arg("--allow-cwd"); } -->
<!--   }                                                                        -->
<!--   // build_nono_run_args does NOT change — D-03/D-04 go into confined_run -->
<!--                                                                            -->
<!--   pub(crate) fn confined_run(                                              -->
<!--     exe: String, args: Vec<String>,                                        -->
<!--     allow: Option<Vec<String>>, profile: Option<String>,                   -->
<!--     cwd: Option<String>, timeout_secs: Option<f64>,                        -->
<!--   ) -> napi::Result<JsExecResult> {                                        -->
<!--     // [CURRENT] Validation guard fires BEFORE any default injection:      -->
<!--     if profile.is_none() && allow.as_ref().map_or(true, |v| v.is_empty()) { -->
<!--       return Err(Error::new(Status::InvalidArg, "..."));                   -->
<!--     }                                                                      -->
<!--     let nono_path = find_nono_exe()?;                                      -->
<!--     let mut cmd = Command::new(&nono_path);                                -->
<!--     cmd.arg("run");                                                         -->
<!--     build_nono_run_args(&mut cmd, profile.as_deref(), allow.as_deref(), cwd.as_deref()); -->
<!--     ...                                                                    -->
<!--   }                                                                        -->

<!-- From crates/nono-ts/src/windows_confined_run.rs lines 34-66 (find_nono_exe pattern): -->
<!--   fn find_nono_exe() -> napi::Result<PathBuf> {                           -->
<!--     if let Some(val) = std::env::var_os("NONO_EXE") { ... return Ok(path); } -->
<!--     if let Some(path_var) = std::env::var_os("PATH") {                    -->
<!--       for dir in std::env::split_paths(&path_var) {                       -->
<!--         let candidate = dir.join("nono.exe");                             -->
<!--         if candidate.is_file() { return Ok(candidate); }                  -->
<!--       }                                                                    -->
<!--     }                                                                      -->
<!--     Err(Error::new(Status::GenericFailure, "nono.exe not found..."))       -->
<!--   }                                                                        -->
<!--   // resolve_exe_dir follows the same pattern but returns Ok(Option<String>) -->
<!--   // (best-effort: resolution failure → Ok(None), not Err)                 -->

<!-- From crates/nono-ts/src/lib.rs lines 375-403 (confinedRun napi export): -->
<!--   #[napi] pub fn confined_run(                                             -->
<!--     exe: String, args: Vec<String>,                                        -->
<!--     allow: Option<Vec<String>>, profile: Option<String>,                   -->
<!--     cwd: Option<String>, timeout_secs: Option<f64>,                        -->
<!--   ) -> napi::Result<JsExecResult>                                          -->
<!--   // Adding lowIl/autoCoverTarget booleans requires matching napi export   -->

<!-- Module-level cfg gate: crates/nono-ts/src/windows_confined_run.rs line 12: -->
<!--   #![cfg(windows)]                                                         -->
<!--   // All helpers added to this file are auto-Windows-gated. No per-fn cfg needed. -->
<!--   // Cross-target clippy: non-Windows stub in lib.rs returns NotSupported; -->
<!--   //   if a non-Windows stub path is touched, mark cross-target verify PARTIAL -->
<!--   //   per .planning/templates/cross-target-verify-checklist.md            -->

<!-- From nono-ts/tests/test_broker_ffi_mapping.js lines 41-57 (assert helper): -->
<!--   let failures = 0;                                                        -->
<!--   function assert(condition, msg) {                                        -->
<!--     if (!condition) { failures += 1; console.error('FAIL', msg); }        -->
<!--     else { console.log('*', msg); }                                        -->
<!--   }                                                                        -->
<!--   // Exit pattern (lines 162-172): process.exit(failures === 0 ? 0 : 1)   -->

<!-- CRITICAL D-03 ordering constraint (Pitfall 5, RESEARCH.md §"Pitfall 5"):  -->
<!--   let profile = profile.or_else(|| Some("nono-ts-default".to_string()));   -->
<!--   // ^^^ THIS MUST BE THE FIRST STATEMENT IN confined_run, before:         -->
<!--   //   if profile.is_none() && allow.is_empty() { return Err(...) }        -->
<!--   // Otherwise the default injection is unreachable.                       -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire D-03/D-04 in windows_confined_run.rs + unit tests</name>
  <files>crates/nono-ts/src/windows_confined_run.rs</files>
  <read_first>
    - C:\Users\OMack\nono-ts\src\windows_confined_run.rs — full file; note line 12 (#![cfg(windows)]), lines 34-66 (find_nono_exe PATH pattern — copy for resolve_exe_dir), lines 76-94 (build_nono_run_args — does NOT change), lines 108-144 (confined_run body — D-03/D-04 slot in here), lines 349-502 (existing unit tests — copy the Mutex/ENV_LOCK + save/restore pattern for new tests)
    - .planning/phases/79-wfp-egress-isolation-nono-ts-ergonomics/79-RESEARCH.md — §"The Fix: Two-Part Change" §"Exact wiring plan (in confined_run)" and §"resolve_exe_dir helper"
    - .planning/phases/79-wfp-egress-isolation-nono-ts-ergonomics/79-PATTERNS.md — §"crates/nono-ts/src/windows_confined_run.rs — D-03/D-04 wiring" (find_nono_exe pattern, confined_run current body, unit test pattern)
  </read_first>
  <behavior>
    - test_confined_run_default_profile_injected: call confined_run with profile=None, allow=None; after D-03 the validation guard must NOT fire (profile is now Some("nono-ts-default")); the command built contains "--profile nono-ts-default"
    - test_resolve_exe_dir_absolute: given an absolute path like "C:\Windows\System32\curl.exe", resolve_exe_dir returns Ok(Some("C:\Windows\System32"))
    - test_resolve_exe_dir_not_found_returns_none: given "nonexistent-binary-xyz.exe", returns Ok(None) (not Err — best-effort)
    - test_confined_run_validation_still_fires_when_explicitly_opted_out: if a caller later explicitly passes profile=None + lowIl=false + allow=None (future opt-out), validation still fires — but this is not testable with current signature; confirm the existing test test_confined_run_requires_profile_or_allow now passes because D-03 changes the condition (it is now unreachable on the no-profile path)
  </behavior>
  <action>
In C:\Users\OMack\nono-ts\src\windows_confined_run.rs, make the following changes (working directory for git: C:\Users\OMack\nono-ts):

1. D-03 — default profile injection: Add a new `resolve_exe_dir` helper function (private, following the find_nono_exe PATH-walk pattern) that takes `exe: &str` and returns `napi::Result<Option<String>>`. It resolves the exe to an absolute path via PATH search (same for-dir-in-split-paths loop as find_nono_exe) then returns `Ok(abs.parent().map(|p| p.to_string_lossy().into_owned()))`. On resolution failure (no PATH match, not absolute), return `Ok(None)` — NOT Err (auto-cover is best-effort per D-04).

2. In confined_run: insert `let profile = profile.or_else(|| Some("nono-ts-default".to_string()));` as the FIRST statement in the function body, before ANYTHING else — including before the validation guard `if profile.is_none() && allow.is_empty()`. This is Pitfall 5 — the ordering is non-negotiable.

3. D-04 — auto-cover exe-dir: After the D-03 line and before calling build_nono_run_args, resolve the target exe directory and append to the allow list. Use a let-binding to shadow `allow`:
   a. Call `resolve_exe_dir(&exe)` → `let exe_dir = resolve_exe_dir(&exe)?;`
   b. `let mut allow_paths = allow.unwrap_or_default();`
   c. If `exe_dir` is Some and the directory string is not already in allow_paths, push it.
   d. `let allow = if allow_paths.is_empty() { None } else { Some(allow_paths) };`
   e. Pass the new `allow` (not the original) to build_nono_run_args.

4. The validation guard remains after the D-03/D-04 block. Since profile is now always Some after D-03, the `profile.is_none()` branch is unreachable on the no-profile path. The guard still fires if (in a future API version) a caller opts out of the default — keep it for safety.

5. build_nono_run_args itself does NOT change (per PATTERNS.md).

6. Unit tests: add to the existing `#[cfg(test)]` block following the Mutex/ENV_LOCK + save/restore pattern:
   - test_resolve_exe_dir_absolute: construct a temp path, confirm parent dir returned
   - test_resolve_exe_dir_not_found_returns_none: pass "nonexistent-binary-xyz-abc.exe", confirm Ok(None)
   - test_confined_run_default_profile_not_required: call confined_run("nono.exe".into(), vec![], None, None, None, None) — since NONO_EXE/PATH will not find nono.exe in test, the call returns Err from find_nono_exe — but crucially it must NOT return the old InvalidArg validation error. Check the error message does NOT contain "at least one of 'profile' or 'allow'"

No .unwrap()/.expect() — propagate via ? / return Err(Error::new(Status::GenericFailure, ...)).
The entire module is gated #![cfg(windows)] so no per-function cfg needed for new code.
Cross-target clippy: this file is Windows-only; any non-Windows stub changes in lib.rs (if any) must be deferred to CI per .planning/templates/cross-target-verify-checklist.md. Mark the cross-target verify REQ PARTIAL in the SUMMARY if non-Windows stubs are not touched.

All commits in C:\Users\OMack\nono-ts must include: Signed-off-by: Oscar Mack Jr &lt;oscar.mack.jr@gmail.com&gt;

IMPORTANT: After committing the Rust change, do NOT run npm test yet — the napi binary is still stale. The rebuild + test happens in Task 2.
  </action>
  <verify>
    <automated>cd /c/Users/OMack/nono-ts && cargo test 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - windows_confined_run.rs: the first statement of confined_run is `let profile = profile.or_else(|| Some("nono-ts-default".to_string()));` — appears before the validation guard
    - grep -n "nono-ts-default" src/windows_confined_run.rs returns a match on the first line of confined_run body
    - cargo test (in nono-ts) exits 0; all existing tests pass plus the 3 new unit tests
    - No .unwrap() or .expect() in the new code (clippy::unwrap_used)
    - The existing test test_confined_run_requires_profile_or_allow now exercises the "always-Some profile" path without the InvalidArg error — update or annotate the test if its assertion must change (the guard is no longer reachable with no-profile + no-allow, since profile is now always injected)
  </acceptance_criteria>
  <done>D-03/D-04 wired in confined_run; resolve_exe_dir helper added; unit tests green</done>
</task>

<task type="auto">
  <name>Task 2: Add integration test + update package.json (nono-ts repo)</name>
  <files>nono-ts/tests/test_confined_run_default.js, nono-ts/package.json</files>
  <read_first>
    - C:\Users\OMack\nono-ts\tests\test_broker_ffi_mapping.js — lines 1-57 (require/import pattern, assert helper, failures counter), lines 162-172 (exit pattern with process.exit)
    - C:\Users\OMack\nono-ts\package.json — line 53 ("test" script: currently "node test.js" which references a non-existent file); confirm test.js does NOT exist
    - .planning/phases/79-wfp-egress-isolation-nono-ts-ergonomics/79-RESEARCH.md — §"nono-ts Integration Test (SC4)" (full test code including the workspace path under os.homedir(), the confinedRun call with undefined/undefined/ws/30, the exit-code assertion)
    - .planning/phases/79-wfp-egress-isolation-nono-ts-ergonomics/79-PATTERNS.md — §"C:\Users\OMack\nono-ts\tests\test_confined_run_default.js" (require pattern, platform-skip pattern, assert helpers, summary/exit pattern, the confinedRun integration test core)
  </read_first>
  <action>
Working directory for both files and git: C:\Users\OMack\nono-ts.

1. Create C:\Users\OMack\nono-ts\tests\test_confined_run_default.js:
   - File header comment: "Integration test: confinedRun with no profile flag on Windows. SC4 (TSRG-01). Requires: NONO_EXE set or nono.exe on PATH; Win11 host; nono-ts rebuilt via napi build --platform --release."
   - Platform skip: if (process.platform !== 'win32') { console.log('SKIP: confinedRun integration test is Windows-only'); process.exit(0); }
   - Require: const { confinedRun } = require('../index.js'); const os = require('os'); const path = require('path'); const fs = require('fs');
   - Use the failures counter + assert helper pattern from test_broker_ffi_mapping.js (not immediate-exit, accumulate failures, print PASS/FAIL summary at the end)
   - Workspace: const ws = path.join(os.homedir(), 'nono-ts-default-gate-ws'); fs.mkdirSync(ws, { recursive: true }); — NOTE: ws MUST be under os.homedir() (i.e. %USERPROFILE%), NOT a drive-root path (C:\poc\* or similar) — the AppContainer R-B3 label grant fails on drive-root dirs. This is a CRITICAL constraint from memory feedback_windows_mandatory_label_write_owner.
   - Main assertion: call confinedRun('node.exe', ['-e', 'process.exit(0)'], undefined, undefined, ws, 30) — undefined for both allow and profile triggers D-03 (profile=nono-ts-default) and D-04 (auto-cover node.exe dir).
   - Assert result.exitCode === 0; if not, log the stderr bytes decoded as UTF-8 for diagnosis.
   - Section header console.log before the assertion: "--- SC4: confinedRun with no profile flags ---"
   - Exit with process.exit(failures === 0 ? 0 : 1).

2. Update C:\Users\OMack\nono-ts\package.json: change the "test" script (line 53, currently "node test.js") to "node tests/test_confined_run_default.js". This is the primary wiring change. Do NOT create a test.js dispatcher — point directly at the integration test.

3. After both file changes, commit in nono-ts with DCO sign-off: Signed-off-by: Oscar Mack Jr &lt;oscar.mack.jr@gmail.com&gt;

THEN: run the napi rebuild on the Win11 host (this is the critical Pitfall 4 step):
   napi build --platform --release
   (in C:\Users\OMack\nono-ts)
This regenerates nono.win32-x64-msvc.node AND index.d.ts. Without this step, npm test exercises the OLD binary with the OLD confined_run (no D-03/D-04) and the test will fail.

FINALLY: run npm test. Expected: "PASS: confinedRun default-broker-arm path succeeded (exit 0)" and process exits 0.

If npm test fails with a non-zero exitCode from confinedRun:
- First check: is NONO_EXE pointing at the REBUILT nono.exe (from target\release\nono.exe in the Nono workspace)? The rebuilt nono.exe must contain the nono-ts-default profile (from Plan 01's policy.json change embedded at build time).
- Second check: does the confinedRun stderr mention "Profile not found: nono-ts-default"? If so, the nono.exe on PATH is a stale build without the new profile.
- Third check: is the workspace ws under %USERPROFILE%? If it is at C:\ or any drive root, the label grant fails.

All commits in C:\Users\OMack\nono-ts must include: Signed-off-by: Oscar Mack Jr &lt;oscar.mack.jr@gmail.com&gt;
  </action>
  <verify>
    <automated>MISSING — napi build + npm test requires Win11 host action. See human-verify checkpoint in Task 3.</automated>
    <!-- Nyquist note: The MISSING tag here is intentional. Task 2 creates test artifacts that
         require a live Win11 host + napi rebuild to exercise. The verification gate is Task 3
         (checkpoint:human-verify). nyquist_compliant: true in 79-VALIDATION.md is set only
         AFTER Task 3's live-host checkpoint passes (napi build + npm test green on Win11). -->
  </verify>
  <acceptance_criteria>
    - C:\Users\OMack\nono-ts\tests\test_confined_run_default.js exists, begins with platform-skip, calls confinedRun with undefined/undefined/ws/30
    - package.json "test" script points to "node tests/test_confined_run_default.js"
    - The ws path is under os.homedir() (NOT a drive-root path)
    - Test file does NOT call process.exit() mid-test (only at the end via the failures summary)
  </acceptance_criteria>
  <done>Integration test and package.json wiring committed to nono-ts repo</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: napi rebuild + npm test live host proof (SC4)</name>
  <read_first>
    - .planning/phases/79-wfp-egress-isolation-nono-ts-ergonomics/79-RESEARCH.md — §"napi Rebuild Requirement" (Pitfall 4) and §"Environment Availability" (NONO_EXE must point at the dev-layout nono.exe with nono-ts-default profile embedded)
  </read_first>
  <what-built>
    Tasks 1 and 2 have wired D-03/D-04 into windows_confined_run.rs and added the integration test. The napi binary at C:\Users\OMack\nono-ts\nono.win32-x64-msvc.node is still the pre-change build.

    The executor has already committed the Rust changes. The following sequence must run on the Win11 host:

    PRECONDITION CHECK: Before running npm test, confirm the rebuilt nono.exe exposes
    the nono-ts-default profile (added in Plan 01). Run:
      C:/Users/OMack/Nono/target/release/nono.exe run --profile nono-ts-default --dry-run
    If this prints "Profile not found: nono-ts-default", the nono.exe build is stale or
    Plan 01 has not been executed yet. Do not proceed to npm test until this passes.
    1. cd C:\Users\OMack\Nono && cargo build --release  (builds nono.exe with the new nono-ts-default profile embedded via policy.json)
    2. cd C:\Users\OMack\nono-ts && napi build --platform --release  (rebuilds the .node binary with D-03/D-04 wired in windows_confined_run.rs)
    3. $env:NONO_EXE = "C:\Users\OMack\Nono\target\release\nono.exe"  (point at the freshly rebuilt nono.exe — must contain nono-ts-default profile)
    4. npm test  (exercises the integration test via test_confined_run_default.js)
  </what-built>
  <how-to-verify>
    On the Win11 dev host:

    1. Build nono.exe with new profiles: cd C:\Users\OMack\Nono; cargo build --release (the nono-ts-default profile is embedded via build.rs from data/policy.json)
    2. Rebuild napi binary: cd C:\Users\OMack\nono-ts; napi build --platform --release
    3. Set NONO_EXE: $env:NONO_EXE = "C:\Users\OMack\Nono\target\release\nono.exe"
    4. Run test: npm test

    Expected output:
       --- SC4: confinedRun with no profile flags ---
       * confinedRun default-broker-arm path succeeded (exit 0)
       PASS all SC4 assertions pass.
    Expected exit code: 0

    If npm test prints "Profile not found: nono-ts-default": nono.exe on PATH or NONO_EXE is a stale build. Rebuild step 1 first.
    If npm test prints error about workspace path: ensure ws is under %USERPROFILE% (e.g. C:\Users\OMack\nono-ts-default-gate-ws).
    If confinedRun returns exitCode=0xC0000142 (3221225858): the Low-IL broker arm was not reached — check nono-ts-default profile has windows_low_il_broker: true and the nono.exe build is current.
  </how-to-verify>
  <resume-signal>Type "sc4-pass" if npm test exits 0 with PASS output. Type "sc4-fail" followed by the error output if the test fails.</resume-signal>
  <acceptance_criteria>
    - napi build --platform --release completes without error (produces nono.win32-x64-msvc.node)
    - npm test exits 0
    - Test output includes "PASS: confinedRun default-broker-arm path succeeded (exit 0)" or the equivalent pass summary
    - confinedRun result.exitCode is 0 (node.exe -e process.exit(0) ran inside the Low-IL AppContainer)
    - No "Profile not found" or "0xC0000142" error in test output
  </acceptance_criteria>
  <done>napi rebuild complete; npm test exits 0; SC4 PASS confirmed on Win11 host</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nono-ts caller (JS) → confined_run (Rust/napi) | Caller-supplied exe string and args cross the napi boundary into Rust |
| confined_run → nono.exe subprocess | build_nono_run_args constructs args passed to nono.exe; nono.exe is a trusted binary from the nono workspace |
| nono.exe → AppContainer child (node.exe) | nono.exe applies sandbox policy; node.exe runs at Low-IL inside AppContainer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-01 | Spoofing | (see Plan 01 — WFP gate) | n/a | Covered in Plan 01. |
| T-79-02 | Spoofing | (see Plan 01 — SID collision) | n/a | Covered in Plan 01. |
| T-79-03 | Denial of Service | confined_run no-profile path / WriteRestricted arm | mitigate | D-03 injection is the FIRST statement in confined_run (before validation guard, per Pitfall 5). nono-ts-default sets windows_low_il_broker: true → broker arm. Unit test confirms profile injection order. napi binary must be rebuilt after Rust change (Pitfall 4 mitigation: Task 3 forces the rebuild). |
| T-79-04 | Elevation of Privilege | auto-cover exe-dir grant (D-04) | mitigate | resolve_exe_dir returns ONLY the parent directory of the resolved target executable. No cwd, no ancestors, no broader grants. Resolution is best-effort (Ok(None) on failure, not a promotion to a wider grant). The returned path is passed as --allow to nono.exe which applies it as a read-only grant (nono's sandbox defaults). Unit test test_resolve_exe_dir_absolute confirms exactly the parent dir is returned. |
| T-79-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced. napi-rs and node are pre-existing dependencies. Package legitimacy audit confirms no new packages. |
</threat_model>

<verification>
## Phase-Level Checks for TSRG-01

1. `cargo test` (in C:\Users\OMack\nono-ts) — Rust unit tests pass including the 3 new D-03/D-04 tests
2. `grep -n "nono-ts-default" src/windows_confined_run.rs` — appears on the first statement line of confined_run
3. `napi build --platform --release` (Win11 host) — exits 0, regenerates .node and index.d.ts
4. `npm test` (Win11 host, NONO_EXE pointing at rebuilt nono.exe) — exits 0, prints "PASS"
5. Cross-target clippy: windows_confined_run.rs is #![cfg(windows)] — module is Windows-only. If no non-Windows stub changes are made in lib.rs, cross-target verification is not triggered. Mark as PARTIAL in SUMMARY if any non-stub change was made, per .planning/templates/cross-target-verify-checklist.md.
</verification>

<success_criteria>
- SC2 (ROADMAP): confinedRun({ target: "node" }) (no profile flags) produces a confined child at Low IL out of the box — the broker arm is reached, node.exe starts cleanly (no 0xC0000142)
- SC3 (ROADMAP): confinedRun auto-covers the target executable's directory — resolve_exe_dir adds the parent dir of the resolved exe to --allow; no cwd or ancestor paths are added
- SC4 (ROADMAP): npm test exits 0 on the Win11 dev host after napi build --platform --release — the integration test confirms the default-broker-arm path works end-to-end
- TSRG-01 (REQUIREMENTS): confinedRun defaults to Low-IL broker arm (windows_low_il_broker: true via nono-ts-default) and auto-covers the target executable's directory; a caller gets a working confined run with no manual profile or coverage flags
</success_criteria>

<output>
Create `.planning/phases/79-wfp-egress-isolation-nono-ts-ergonomics/79-02-SUMMARY.md` when done.

Note: the nono-ts commits (tasks 1 and 2) go to C:\Users\OMack\nono-ts. The plan SUMMARY should note which commits went to which repo and which branch.
</output>
