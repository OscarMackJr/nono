---
phase: 98-upst11-divergence-audit
plan: 02
type: execute
wave: 2
depends_on: [98-01]
files_modified:
  - .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md
autonomous: true
requirements: [UPST11-01]

must_haves:
  truths:
    - "D-03: the ledger resolves to commits, not PRs — each 260629-toe per-PR disposition is re-confirmed at commit granularity as a starting hypothesis, never copied as ground truth"
    - "Every substantive commit in the window is inspected by actual-diff (git show), not by CHANGELOG/PR summary (D-09 uniform inspection)"
    - "Every substantive commit lands in exactly one fresh-letter cluster with a per-commit table row carrying a windows-touch flag (D-11)"
    - "Each cluster has a 5-dimension ADR risk matrix row with no bare TBD verdict (D-13)"
    - "Release/version commits are in the won't-sync cluster with a 0.66.1 leapfrog-floor cross-ref for Phase 100 (D-15)"
    - "Any will-sync/split commit touching cfg(linux)/cfg(macos) blocks is flagged for the Phase 99 cross-target clippy MUST (D-14)"
  artifacts:
    - path: ".planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md"
      provides: "Per-cluster bodies, per-commit tables, re-export scan, ADR risk matrix, empirical cross-check, finalized headline"
      contains: "## ADR Review"
  key_links:
    - from: "Cluster bodies"
      to: "drift JSON substantive commit list (Plan 01)"
      via: "every substantive SHA appears in exactly one per-commit table"
      pattern: "Per-commit table"
    - from: "won't-sync cluster"
      to: "Phase 100 leapfrog floor 0.66.1"
      via: "D-15 one-line cross-ref"
      pattern: "0.66.1"
---

<objective>
Perform the core divergence classification: uniform actual-diff (`git show`) inspection of every
substantive commit in the window, assign each to a fresh-letter cluster, write the per-cluster
bodies and per-commit tables (with windows-touch flags, categories, files-changed, cross-cluster
re-export scan), flag cfg-gated commits for Phase 99, place release commits in the won't-sync
cluster with the 0.66.1 floor cross-ref, and render the per-cluster ADR risk matrix + empirical
cross-check + finalized headline.

Purpose: This is the audit's substance — the per-commit dispositions that gate Phase 99 absorb.
Per D-09 inspection is UNIFORM (not risk-tiered): the window is small enough that every
substantive commit gets a `git show`, eliminating the risk of an additive-looking commit hiding a
boundary/FFI/security change. Per D-03 each 260629-toe disposition is re-confirmed at commit
granularity rather than copied.
Output: the populated cluster bodies, ADR Review matrix, and Empirical Cross-Check sections of
`98-DIVERGENCE-LEDGER.md` (carve-out re-touch check is Plan 04; #1225 ADR is Plan 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/98-upst11-divergence-audit/98-CONTEXT.md
@.planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md

# The DIRECT structural template — copy the per-cluster body, per-commit table, ADR Review
# matrix, and Empirical Cross-Check section shapes exactly:
@.planning/milestones/v3.3-phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md
# The preliminary per-PR dispositions to RE-CONFIRM at commit granularity (D-03):
@.planning/quick/260629-toe-v066-parity/PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Uniform git-show inspection, fresh-letter clusters, per-commit tables + re-export scan</name>
  <files>.planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md</files>
  <read_first>
    - .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md (the Cluster Summary scaffold + Reproduction SHAs from Plan 01)
    - .planning/milestones/v3.3-phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md (per-cluster body format: Commits / Disposition / Windows-touch / Inspection depth / Rationale / cross-cluster re-export check / per-commit table)
    - .planning/quick/260629-toe-v066-parity/PLAN.md (per-PR hypothesis: #1268/#1271/#983/#1213 features, #1263/#1127/#1207/#1253/#1249/#1243 fixes, #1251/#1245 CI, #1229/#1232 deps, #1247/#1246 docs, #1235 org-rename, #1225 refactor)
    - .planning/phases/98-upst11-divergence-audit/98-CONTEXT.md (D-03, D-09, D-11, D-14, D-15)
  </read_first>
  <action>
    For EVERY substantive commit from the Plan 01 drift JSON, run `git show <sha>` (uniform
    actual-diff per D-09 — no risk-tiering). For each, capture subject, upstream tag, drift-tool
    categories, files-changed summary, and a windows-touch determination (touches `*_windows.rs`,
    `exec_strategy_windows/`, or WFP/AppContainer/broker code = yes; else no). Run the cross-cluster
    re-export scan per commit: `git show <sha> | grep '^+' | grep -E '^\+\s*(pub use|pub mod|extern crate|pub\(crate\))'`
    and record findings + any intra-cluster ordering constraint. Assign each commit to a FRESH
    letter cluster (A, B, C..., independent of Phase 85/94 lettering, per D-11) by theme; re-confirm
    each 260629-toe disposition at commit granularity (per D-03 — the PR hypothesis is a starting
    point, the git-show diff is ground truth). Write each cluster body in the Phase 94 format
    (Commits / Disposition will-sync|fork-preserve|won't-sync|split / Windows-touch / Inspection
    depth: actual-diff / Rationale / cross-cluster re-export check / per-commit table with columns
    `sha | subject | upstream-tag | categories | files-changed | windows-touch`). Place all
    release/version commits (Cargo.toml bumps, CHANGELOG, the #1293 release-cut, dep-version bumps
    that are substantive) into the `won't-sync` cluster and add a one-line cross-ref recording the
    `0.66.1` leapfrog floor for Phase 100 (NOT 0.67 — D-15 supersedes the 260629-toe "≥0.67.0"
    note); do NOT add a standalone release section. For any will-sync/split commit whose diff
    touches `#[cfg(target_os = "linux")]` or `#[cfg(target_os = "macos")]` blocks, add a
    "Phase 99 executor cross-target clippy note" (D-14) naming the CLAUDE.md MUST and the affected
    files (note only — the gate runs in Phase 99). Update the Cluster Summary table cells to match
    the confirmed dispositions. Edit the existing ledger file.
  </action>
  <acceptance_criteria>
    - Every substantive SHA from the Plan 01 drift JSON appears in exactly one per-cluster per-commit table row.
    - Every per-commit row carries an explicit windows-touch value (yes/no), never blank.
    - Every cluster body has a cross-cluster re-export scan subsection (with findings, even if "Clean").
    - The won't-sync cluster contains the release/version commits and includes a one-line `0.66.1` leapfrog-floor cross-ref for Phase 100; the string `0.67` does not appear as the floor.
    - Every commit whose diff touches a cfg(linux)/cfg(macos) block has a Phase-99 cross-target clippy note.
    - Inspection depth is recorded as actual-diff (git show) for every cluster (no risk-tiered shortcut).
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'won.t-sync' .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md && grep -Eq 'floor[^0-9]*0\.66\.1|0\.66\.1[^0-9]*floor' .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md && grep -qi 're-export' .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md && grep -qi 'windows-touch' .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md
  </verify>
</task>

<task type="auto">
  <name>Task 2: ADR Review risk matrix, Empirical Cross-Check, finalized Headline + downstream routing</name>
  <files>.planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md</files>
  <read_first>
    - .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md (the cluster bodies written in Task 1)
    - .planning/milestones/v3.3-phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md (the "ADR Review" 5-dimension table + per-cluster risk matrix + "Empirical Cross-Check" + "Headline" formats)
    - .planning/phases/98-upst11-divergence-audit/98-CONTEXT.md (D-13)
  </read_first>
  <action>
    Write the `## ADR Review` section: an overall 5-dimension assessment table
    (security / windows / maintenance / divergence / contributor, each H/M/L with notes) AND a
    per-cluster risk matrix table with one row per cluster scoring all five dimensions plus an
    Overall column (D-13) — NO cell may carry a bare `TBD`; every disposition must be justified by
    at least one of: security impact, Windows-backend touch, or library-boundary relevance. Add a
    `## Empirical Cross-Check` section: spot-check 6-8 files via
    `git log --oneline <from>..<to> -- <file>` to validate cluster classification, choosing files
    that include the #1225 surface (`crates/nono/src/capability.rs`,
    `crates/nono/src/manifest_convert.rs`, `crates/nono/src/sandbox/linux.rs`,
    `crates/nono-cli/src/network_policy.rs`) plus `crates/nono/Cargo.toml` (won't-sync check) and
    any tool-sandbox/proxy surfaces; record each result as expected-cluster CORRECT/incorrect.
    Write/finalize the `## Headline` at the top: substantive count, cluster letters + dispositions,
    windows-touch clusters, the #1225 cluster called out explicitly (its full disposition is
    settled in ADR-98, Plan 03 — cross-reference, do not inline), and the downstream routing list
    (each cluster → Phase 99 absorb action or won't-sync→Phase 100). Add an Outcome paragraph
    (continue vs escalate). Edit the existing ledger file.
  </action>
  <acceptance_criteria>
    - `## ADR Review` present with an overall 5-dimension table AND a per-cluster risk matrix; every cluster has a row scoring all five dimensions + Overall.
    - No risk-matrix verdict cell contains a bare `TBD`.
    - `## Empirical Cross-Check` present with 6+ `git log --oneline ... -- <file>` spot-checks including the four #1225-surface files, each marked CORRECT against its expected cluster.
    - `## Headline` present with substantive count, per-cluster dispositions, windows-touch clusters, an explicit #1225 callout cross-referencing ADR-98, and a downstream routing list.
    - An Outcome paragraph records continue-vs-escalate per the established criteria.
  </acceptance_criteria>
  <verify>
    <automated>grep -q '## ADR Review' .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md && grep -q '## Empirical Cross-Check' .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md && grep -q '## Headline' .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md && grep -q 'capability.rs' .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md && ! grep -nE '\| *TBD *\|' .planning/phases/98-upst11-divergence-audit/98-DIVERGENCE-LEDGER.md</automated>
  </verify>
</task>

</tasks>

<threat_model>
N/A — documentation/audit phase. No production code, attack surface, or data handling is
introduced; the deliverable is the classification body of a markdown analysis artifact. The
security DIMENSION of each cluster is scored in the ADR Review risk matrix as analysis, not as an
enforced code change.
</threat_model>

<verification>
- Every substantive commit classified into exactly one cluster with a windows-touch flag.
- Per-cluster ADR risk matrix complete across five dimensions with no bare TBD.
- Empirical cross-check validates classification against the #1225 surface files.
- Headline + downstream routing finalized; won't-sync cluster carries the 0.66.1 floor cross-ref.
</verification>

<success_criteria>
- ROADMAP SC1 satisfied: every commit classified into will-sync/fork-preserve/won't-sync/split with windows-touch + per-cell ADR verdict.
- ROADMAP SC3 satisfied: no cluster carries a bare TBD; each disposition justified by the established criteria.
- D-03/D-09/D-11/D-13/D-14/D-15 all traceably satisfied in the ledger body.
</success_criteria>

<output>
Create `.planning/phases/98-upst11-divergence-audit/98-02-SUMMARY.md` when done.
</output>
