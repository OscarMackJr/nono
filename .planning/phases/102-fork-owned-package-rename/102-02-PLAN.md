---
phase: 102
plan: 02
type: execute
wave: 2
depends_on: ["102-01"]
files_modified:
  - Makefile
  - .github/workflows/ci.yml
  - .github/workflows/image-build.yml
  - .github/workflows/release.yml
  - .github/workflows/phase-37-linux-resl.yml
autonomous: true
requirements: [PUB-01]

must_haves:
  truths:
    - "make build (build-lib + build-cli) succeeds by invoking cargo build -p nono-sandbox / -p nono-sandbox-cli, not the now-nonexistent -p nono / -p nono-cli"
    - "make test and make ci resolve via the renamed package selectors"
    - "the always-on, required-check phase-37-linux-resl.yml workflow (D-03: runs on every PR/push to main) invokes the renamed package selectors"
    - "release.yml's build steps (compiling nono-sandbox-cli/nono-shell-broker binaries for signing) use -p nono-sandbox-cli; its 3 cargo publish -p nono/-nono-proxy/-nono-cli lines are DELIBERATELY left unchanged, with an in-file comment noting the deferral to Phase 105 (PUB-02), which owns the live-publish rewrite with dependency-ordered index-polling"
    - "phase-45-resl-native-host.yml and phase-46-uat-backlog.yml are explicitly NOT touched -- both are workflow_dispatch-only, self-documented as deletable tactical/historical workflows, not permanent CI lanes"
  artifacts:
    - path: "Makefile"
      provides: "all -p nono / -p nono-cli selectors renamed to -p nono-sandbox / -p nono-sandbox-cli; -p nono-ffi selectors unchanged"
    - path: ".github/workflows/ci.yml"
      provides: "build-step and FFI-header-check selectors renamed where the underlying package was renamed"
    - path: ".github/workflows/release.yml"
      provides: "build steps renamed; publish steps explicitly untouched + commented as a Phase 105 deferral"
  key_links:
    - from: "Makefile build-lib/build-cli/test-lib/test-cli targets"
      to: "crates/nono/Cargo.toml + crates/nono-cli/Cargo.toml [package] name (Plan 102-01)"
      via: "cargo -p selector string match"
      pattern: "cargo (build|test|run|watch) .*-p nono-sandbox(-cli)?"
---

<objective>
Reconcile every `cargo -p <name>` package selector in this repo's build automation (Makefile + the permanent/build-facing GitHub Actions workflows) with the Plan 102-01 rename, so `make build`/`make ci` and the always-on CI lane compile against the new package names instead of erroring on the now-nonexistent old ones.

Purpose: `crates/nono`'s `[package] name` no longer exists as `nono` after Plan 102-01 lands -- any automation still invoking `cargo build -p nono` or `-p nono-cli` will fail outright (`error: package ID specification ... did not match any packages`). PUB-01 SC4 names `make build` explicitly as its literal verification command; this plan is what makes that command actually pass. Explicitly excludes `release.yml`'s live `cargo publish` steps, which stay pointed at the old names until Phase 105 (PUB-02) rewrites them with dependency-ordered, index-polled publish logic (per ROADMAP Phase 105 SC1) -- rewriting them here would be premature and duplicated work.
Output: Makefile + 4 GitHub Actions workflow files with reconciled package selectors; a green `make ci`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/102-fork-owned-package-rename/102-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile Makefile package selectors</name>
  <files>Makefile</files>
  <read_first>Makefile, 102-RESEARCH.md (confirms nono-ffi/nono-shell-broker package names are NOT renamed by Plan 102-01)</read_first>
  <action>
In Makefile, change every bare `-p nono` selector to `-p nono-sandbox` and every `-p nono-cli` selector to `-p nono-sandbox-cli`, across these targets: build-lib, build-release-lib, test-lib, doc-lib (bare `-p nono` occurrences); build-cli, build-release-cli, build-arm64 (inside `cross build ... -p nono-cli`), test-cli, run, run-setup, run-dry, watch (inside `cargo watch -x 'build -p nono-cli'`) (the `-p nono-cli` occurrences). Leave build-ffi/test-ffi (`-p nono-ffi`) completely unchanged -- nono-ffi's own package name is not renamed by this phase. Leave every `--workspace`-scoped target (clippy, fmt, fmt-check, audit, ci, check-upstream-drift) unchanged -- they don't reference a package name.
  </action>
  <acceptance_criteria>
    - grep -cE '\-p nono($| )' Makefile returns 0 (no bare unrenamed -p nono selector remains)
    - grep -cE '\-p nono-cli\b' Makefile returns 0 (no unrenamed -p nono-cli selector remains)
    - grep -c '\-p nono-sandbox\b' Makefile returns 4 (build-lib, build-release-lib, test-lib, doc-lib)
    - grep -c '\-p nono-sandbox-cli\b' Makefile returns 8 (build-cli, build-release-cli, build-arm64, test-cli, run, run-setup, run-dry, watch)
    - grep -c '\-p nono-ffi\b' Makefile still returns 2 (build-ffi, test-ffi -- unchanged)
  </acceptance_criteria>
  <verify>
    <automated>grep -cE '\-p nono($| |-cli\b)' Makefile | grep -qx 0 && echo OK</automated>
  </verify>
  <done>Makefile contains zero references to the retired -p nono / -p nono-cli selectors; all renamed targets point at -p nono-sandbox / -p nono-sandbox-cli; nono-ffi selectors untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile CI workflow build/test package selectors (release.yml build steps only -- NOT publish)</name>
  <files>.github/workflows/ci.yml, .github/workflows/image-build.yml, .github/workflows/release.yml, .github/workflows/phase-37-linux-resl.yml</files>
  <read_first>.github/workflows/ci.yml (lines 335-410, 520-535), .github/workflows/image-build.yml (lines 95-115), .github/workflows/release.yml (lines 75-100, 480-525), .github/workflows/phase-37-linux-resl.yml (lines 115-145, 295-310), 102-RESEARCH.md Pitfall 3 (publish dry-run deferred to Phase 105)</read_first>
  <action>
In .github/workflows/ci.yml, change the `-p nono-cli` occurrences (the "Build (release, cli)" step and both matrix-target build steps around lines 340, 525, 529) to `-p nono-sandbox-cli`; leave the `-p nono-shell-broker` occurrence (line 341) and the two `-p nono-ffi` occurrences (FFI header regen check, lines 397/403) unchanged.
In .github/workflows/image-build.yml, change both `-p nono-cli` occurrences (matrix-target build + cross-build, lines 104/108) to `-p nono-sandbox-cli`.
In .github/workflows/release.yml, change ONLY the BUILD steps' `-p nono-cli` occurrences (lines 82, 88, 98) to `-p nono-sandbox-cli`; leave the `-p nono-shell-broker` build step (line 94) unchanged; do NOT touch the three `cargo publish -p nono` / `-p nono-proxy` / `-p nono-cli` lines (around lines 494, 508, 522) -- those are the live-publish invocations, deliberately deferred to Phase 105 (PUB-02), which rewrites them with dependency-ordered, index-visibility-polled publish logic per ROADMAP Phase 105 SC1. Add a one-line comment directly above the first publish step noting that Phase 105 (PUB-02) rewrites these publish targets to nono-sandbox/nono-sandbox-proxy/nono-sandbox-cli with index-visibility polling, so the deferral is visible in-repo, not silent.
In .github/workflows/phase-37-linux-resl.yml (the permanent "Required check ... Always-on trigger" workflow per its own D-03 header comment), change the `-p nono` and `-p nono-cli` occurrences (lines 119, 121, 126, 143, 303) to `-p nono-sandbox` / `-p nono-sandbox-cli`; leave the `-p nono-ffi` occurrence (line 120) and the `--bin nono` arguments (lines 121, 126 -- the [[bin]] name is not renamed) unchanged.
Do NOT touch .github/workflows/phase-45-resl-native-host.yml or .github/workflows/phase-46-uat-backlog.yml -- both are workflow_dispatch-only, explicitly self-documented as "deletable" tactical/historical workflows in their own header comments, not permanent CI lanes; this exclusion is intentional, not an oversight.
  </action>
  <acceptance_criteria>
    - grep -cE '\-p nono-cli\b' .github/workflows/ci.yml .github/workflows/image-build.yml .github/workflows/release.yml .github/workflows/phase-37-linux-resl.yml combined returns 0
    - grep -c '\-p nono-sandbox-cli\b' .github/workflows/ci.yml returns 2; .github/workflows/image-build.yml returns 2; .github/workflows/release.yml returns 3; .github/workflows/phase-37-linux-resl.yml returns 4
    - grep -c '\-p nono-sandbox\b' .github/workflows/phase-37-linux-resl.yml returns 1 (line 119 only, not double-counting the -cli variant)
    - grep -cE 'cargo publish -p nono(-proxy|-cli)?\b' .github/workflows/release.yml still returns 3 (untouched, deliberately deferred)
    - grep -c 'Phase 105' .github/workflows/release.yml returns at least 1 (the added deferral comment)
    - grep -c '\-p nono-ffi\b' .github/workflows/ci.yml still returns 2
    - grep -c '\-p nono-shell-broker\b' .github/workflows/ci.yml returns 1 and .github/workflows/release.yml returns 1 (both unchanged)
  </acceptance_criteria>
  <verify>
    <automated>! grep -rE '\-p nono-cli\b' .github/workflows/ci.yml .github/workflows/image-build.yml .github/workflows/release.yml .github/workflows/phase-37-linux-resl.yml && echo OK</automated>
  </verify>
  <done>All 4 permanent/build-facing CI workflow files reference the renamed nono-sandbox/nono-sandbox-cli package selectors for build/test steps; release.yml's 3 publish steps are explicitly untouched and commented as a Phase 105 deferral; phase-45/46 workflow_dispatch-only historical workflows are explicitly excluded.</done>
</task>

<task type="auto">
  <name>Task 3: Full make ci verification</name>
  <files>None (verification only)</files>
  <read_first>Makefile (post-edit), .github/workflows/*.yml (post-edit)</read_first>
  <action>
Run `make ci` (which chains `check` [clippy --workspace --all-targets --all-features -D warnings -D clippy::unwrap_used, then fmt --all -- --check], `test` [test-lib/test-cli/test-ffi, now resolving via the renamed -p selectors], and `audit`) from the repo root and confirm it exits 0, proving Plan 102-01's manifest rename and this plan's automation reconciliation work together end-to-end. Also run `make build` independently since SC4 names it explicitly. Per CLAUDE.md, the cross-target clippy protocol applies only when cfg-gated Unix source is touched -- this plan edits only Makefile/YAML, zero Rust source, so it does not apply here.
  </action>
  <acceptance_criteria>
    - make ci exits 0
    - make build exits 0 independently
  </acceptance_criteria>
  <verify>
    <automated>make ci && make build</automated>
  </verify>
  <done>make ci and make build both exit 0 under the fully-reconciled new package names.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Build automation (Makefile, CI workflows) to renamed Cargo packages | A stale `-p <old-name>` selector fails closed (cargo errors "no matching package"), it does not silently build the wrong crate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-102-02 | Tampering | Stale/incorrect -p package selectors in Makefile + CI workflows | mitigate | Task 3's `make ci` + `make build` green run is the authoritative check; any remaining stale selector fails the build loudly rather than silently resolving elsewhere |
</threat_model>

<verification>
- grep checks confirm every renamed target's -p selector matches the new package names, and every untouched target (nono-ffi, nono-shell-broker, the 3 release.yml publish lines, phase-45/46) is unchanged
- `make ci` and `make build` both exit 0
</verification>

<success_criteria>
- The `make build`/`make ci` portion of PUB-01 SC4 is fully satisfied
- release.yml's publish-step deferral to Phase 105 is documented in-repo, not silent
</success_criteria>

<output>
Create `.planning/phases/102-fork-owned-package-rename/102-02-SUMMARY.md` when done
</output>
