---
phase: 102
plan: 05
type: execute
wave: 3
depends_on: ["102-02", "102-03", "102-04"]
files_modified: []
autonomous: true
requirements: [PUB-01]

must_haves:
  truths:
    - "A fresh, in-window re-check of all 5 registry names still returns 404 at phase close, guarding against a same-window squat during Phase 102's own execution, ahead of Phase 105's own pre-publish re-check"
    - "cargo build --workspace --all-targets + make build are green in this repo under the fully reconciled names (crates + Makefile + CI automation); make test's known Windows baseline failures are documented and unrelated to the rename"
    - "maturin build (nono-py) and napi build --platform --release (nono-ts) are both re-confirmed green after this repo's Wave-2 Makefile/CI edits"
    - "all three repos (this repo, nono-py, nono-ts) show DCO-signed commits for the rename"
    - "all 4 of PUB-01's success criteria are checked against live repo state, not merely inferred from prior plans' SUMMARYs"
  artifacts: []
  key_links: []
---

<objective>
Close out Phase 102 with the phase-gate verification 102-VALIDATION.md prescribes: a fresh live re-check of all 5 registry names (in case of a same-window squat), a full `make ci` run in this repo, and a re-confirmation that both sibling-repo builds are still green -- consolidating proof for all 4 of PUB-01's success criteria before Phase 105 depends on this rename.

Purpose: 102-VALIDATION.md's Sampling Rate explicitly calls for "a fresh live re-check of the three registry availability endpoints" and "all three full builds green" at the phase gate, distinct from Plan 102-01 Task 1's pre-rename gate. This plan is that closing check -- it produces no new source edits, only verification evidence that PUB-01 is genuinely, currently true.
Output: A phase-gate verification record (this plan's SUMMARY) confirming SC1-SC4 all hold against live repo + registry state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/102-fork-owned-package-rename/102-RESEARCH.md
@.planning/phases/102-fork-owned-package-rename/102-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh registry-availability re-check + full make ci</name>
  <files>None (verification only)</files>
  <read_first>102-RESEARCH.md Code Examples (registry availability checks), 102-VALIDATION.md Sampling Rate (phase gate), auto-memory `nono_cli_windows_baseline_test_failures.md` (the documented phase-unrelated Windows baseline test failures — do NOT chase them as rename regressions)</read_first>
  <action>
Re-run the exact same 5 live registry-availability checks from 102-RESEARCH.md's Code Examples section (crates.io x3 with User-Agent, PyPI x1, npm x1) now that all renames have landed, to confirm none of the 5 names was claimed by a third party during this phase's own execution window -- this is the 102-VALIDATION.md-mandated phase-gate re-check, distinct from Plan 102-01 Task 1's pre-rename gate. If any of the 5 no longer returns 404, treat it as a phase-blocking finding and do not close the phase -- surface it for an operator decision rather than silently proceeding (Phase 105 would otherwise attempt to publish into a now-contested name). Then verify BUILD-GREEN under the reconciled names: run `cargo build --workspace --all-targets` and `make build` and confirm both exit 0. Do NOT gate on `make ci`/`make test` -- those chain `cargo test` and trip the pre-existing, documented, phase-unrelated Windows baseline test failures (see read_first memory) that reproduce at every phase base and are not caused by this rename; gating on them would force a false-regression chase or a fail-closed override.
  </action>
  <acceptance_criteria>
    - all 5 fresh curl checks (crates.io nono-sandbox/-proxy/-cli, PyPI nono-sandbox, npm @oscarmackjr%2Fnono-ts) return 404
    - cargo build --workspace --all-targets exits 0 (whole workspace compiles under the renamed packages)
    - make build exits 0 (SC4's literal named command)
  </acceptance_criteria>
  <verify>
    <automated>for u in "https://crates.io/api/v1/crates/nono-sandbox" "https://crates.io/api/v1/crates/nono-sandbox-proxy" "https://crates.io/api/v1/crates/nono-sandbox-cli"; do c=$(curl -s -A "nono-plan/1.0 (oscar.mack.jr@gmail.com)" -o /dev/null -w "%{http_code}" "$u"); [ "$c" = "404" ] || { echo "FAIL $u -> $c"; exit 1; }; done; c=$(curl -s -o /dev/null -w "%{http_code}" https://pypi.org/pypi/nono-sandbox/json); [ "$c" = "404" ] || { echo "FAIL pypi -> $c"; exit 1; }; c=$(curl -s -o /dev/null -w "%{http_code}" "https://registry.npmjs.org/@oscarmackjr%2Fnono-ts"); [ "$c" = "404" ] || { echo "FAIL npm -> $c"; exit 1; }; cargo build --workspace --all-targets && make build && echo "PHASE GATE PART 1 GREEN"</automated>
  </verify>
  <done>Fresh availability re-check confirms no name was squatted during Phase 102's execution; cargo build --workspace --all-targets and make build are both green (make test's baseline failures are the documented pre-existing set, not rename regressions).</done>
</task>

<task type="auto">
  <name>Task 2: Re-confirm both sibling-repo builds + consolidated SC1-SC4 checklist</name>
  <files>None (verification only)</files>
  <read_first>none new -- relies on Plan 102-01/02/03/04 artifacts already in repo state</read_first>
  <action>
From ../nono-py, re-run `maturin build`; from ../nono-ts, re-run `npx napi build --platform --release` -- confirming both remain green after this repo's own Wave-2 Makefile/CI edits (which don't touch the sibling repos, but re-confirming closes the phase-gate loop per 102-VALIDATION.md's "all three full builds green" rule). Then walk the 4 PUB-01 success criteria one more time against actual repo state: (SC1) confirm crates/nono/Cargo.toml, crates/nono-proxy/Cargo.toml, crates/nono-cli/Cargo.toml show the 3 new [package] names and crates/nono-cli/Cargo.toml still shows both [[bin]] name = "nono" / "nono-agentd" lines unchanged; (SC2) confirm ../nono-py/pyproject.toml shows [project] name = "nono-sandbox" and ../nono-ts/package.json shows name = "@oscarmackjr/nono-ts"; (SC3) confirmed by Task 1; (SC4) confirmed by this task's two build re-runs plus Task 1's cargo build --workspace --all-targets/make build.
  </action>
  <acceptance_criteria>
    - maturin build exits 0 in ../nono-py
    - npx napi build --platform --release exits 0 in ../nono-ts
    - grep '^name' crates/nono/Cargo.toml crates/nono-proxy/Cargo.toml crates/nono-cli/Cargo.toml shows nono-sandbox / nono-sandbox-proxy / nono-sandbox-cli
    - grep -A1 '\[\[bin\]\]' crates/nono-cli/Cargo.toml still shows name = "nono" and name = "nono-agentd"
    - grep '^name' ../nono-py/pyproject.toml shows nono-sandbox; node -e "console.log(require('../nono-ts/package.json').name)" shows @oscarmackjr/nono-ts
    - git -C ../nono-py log -1 --format=%B and git -C ../nono-ts log -1 --format=%B both contain "Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>"
  </acceptance_criteria>
  <verify>
    <automated>cd ../nono-py && maturin build && cd ../nono-ts && npx napi build --platform --release</automated>
  </verify>
  <done>Both sibling-repo builds are re-confirmed green; all 4 PUB-01 success criteria are checked against live repo state and pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| This phase's own execution window vs. live public registries | A name confirmed available at Plan 102-01's start could theoretically be claimed by a third party before Phase 102 closes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-102-01 | Spoofing | crates.io/PyPI/npm registry names (nono-sandbox family) | mitigate | Task 1's fresh re-check closes the same-window squat risk for Phase 102 itself; RESEARCH.md's Security Domain guidance additionally requires one more re-check immediately before Phase 105's actual publish -- this plan's check is a checkpoint, not a substitute for that one |
| T-102-02 | Tampering | Cross-repo dependency graph consistency (this repo + nono-py + nono-ts) | mitigate | Task 2's consolidated SC1-SC4 walk re-confirms no drift crept in across the 3 repos since Plan 102-01/03/04 landed |
</threat_model>

<verification>
- Fresh 5-way registry availability re-check all return 404
- `cargo build --workspace --all-targets`, `make build`, `maturin build`, `napi build --platform --release` all exit 0 (build-green is the rename gate; `make test`'s known baseline failures are documented and unrelated)
- All 4 PUB-01 success criteria hold against live repo state
</verification>

<success_criteria>
- PUB-01 SC1, SC2, SC3, SC4 are all confirmed true against live repo + registry state, closing Phase 102
</success_criteria>

<output>
Create `.planning/phases/102-fork-owned-package-rename/102-05-SUMMARY.md` when done
</output>
