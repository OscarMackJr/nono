---
phase: 102
plan: 03
type: execute
wave: 2
depends_on: ["102-01"]
files_modified:
  - ../nono-py/Cargo.toml
  - ../nono-py/pyproject.toml
  - ../nono-py/Cargo.lock
autonomous: true
requirements: [PUB-01]

must_haves:
  truths:
    - "../nono-py/Cargo.toml's nono and nono-proxy path-dependencies carry package = \"nono-sandbox\" / package = \"nono-sandbox-proxy\"; the dependency table keys remain nono/nono-proxy"
    - "../nono-py/pyproject.toml [project] name is nono-sandbox; [tool.maturin] module-name and the python/nono_py/ import surface are unchanged"
    - "../nono-py's own [package] name (nono-py, its Rust crate identity) is unchanged -- it is not in the 3-crate publish set and is never published to crates.io"
    - "maturin build succeeds in ../nono-py under the new names"
    - "the rename commit in ../nono-py is DCO-signed, and is a SEPARATE commit from this repo's Plan 102-01/102-02 commits"
  artifacts:
    - path: "../nono-py/Cargo.toml"
      provides: "nono/nono-proxy dependencies carry package= keys pointing at the renamed core crates"
    - path: "../nono-py/pyproject.toml"
      provides: "[project] name = \"nono-sandbox\""
  key_links:
    - from: "../nono-py/Cargo.toml [dependencies].nono"
      to: "crates/nono/Cargo.toml [package].name (this repo, Plan 102-01)"
      via: "package = \"nono-sandbox\" key, path = \"../Nono/crates/nono\""
      pattern: "package\\s*=\\s*\"nono-sandbox\""
---

<objective>
Patch the nono-py sibling repo's own Cargo.toml path-dependency declarations (outside this Cargo workspace) with the matching `package =` keys, rename its PyPI-facing `pyproject.toml [project] name` to `nono-sandbox`, and prove `maturin build` still succeeds -- without touching the unrelated `nono_py` importable module name or `[tool.maturin] module-name`.

Purpose: `../nono-py` is a separate git repository that path-deps into this workspace by relative path, entirely outside Cargo's workspace-member graph -- Plan 102-01's rename is invisible to it until this plan patches its own manifests. PUB-01 SC2/SC4 explicitly require the PyPI identity renamed and `maturin build` green; skipping this plan would leave Phase 102 silently incomplete despite `make build` (this repo) reporting success (per RESEARCH.md Pitfall 2).
Output: `../nono-py/Cargo.toml` + `../nono-py/pyproject.toml` patched; `../nono-py/Cargo.lock` regenerated; a green `maturin build`; one DCO-signed commit in the nono-py repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/102-fork-owned-package-rename/102-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch nono-py's Cargo.toml + pyproject.toml identity</name>
  <files>../nono-py/Cargo.toml, ../nono-py/pyproject.toml</files>
  <read_first>../nono-py/Cargo.toml (lines 1-20), ../nono-py/pyproject.toml, 102-RESEARCH.md Standard Stack (sibling-repo section) + Anti-Patterns (don't touch module-name/python dir)</read_first>
  <action>
In ../nono-py/Cargo.toml, add package = "nono-sandbox" to the existing nono = { path = "../Nono/crates/nono" } dependency entry and package = "nono-sandbox-proxy" to the existing nono-proxy = { path = "../Nono/crates/nono-proxy" } entry -- keys stay nono/nono-proxy. Do not touch [package] name = "nono-py" -- nono-py's own Rust crate identity is not part of the 3-crate publish set and is never published to crates.io.
In ../nono-py/pyproject.toml, change [project] name = "nono-py" to name = "nono-sandbox". Do NOT touch [tool.maturin] module-name = "nono_py._nono_py" or any file under python/nono_py/ -- the importable Python module stays nono_py per RESEARCH.md's explicit correction (PUB-01 only renames the PyPI distribution identity, not the import surface).
  </action>
  <acceptance_criteria>
    - grep -c 'package = "nono-sandbox"' ../nono-py/Cargo.toml returns 1
    - grep -c 'package = "nono-sandbox-proxy"' ../nono-py/Cargo.toml returns 1
    - grep -c '^name = "nono-py"' ../nono-py/Cargo.toml still returns 1 (own crate name untouched)
    - grep '^name' ../nono-py/pyproject.toml returns name = "nono-sandbox"
    - grep -c 'module-name = "nono_py._nono_py"' ../nono-py/pyproject.toml still returns 1 (untouched)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'package = "nono-sandbox"' ../nono-py/Cargo.toml && grep -q 'package = "nono-sandbox-proxy"' ../nono-py/Cargo.toml && grep -q '^name = "nono-sandbox"$' ../nono-py/pyproject.toml && echo OK</automated>
  </verify>
  <done>nono-py's Cargo.toml path-deps resolve via package= keys against the renamed core crates; its PyPI project name is nono-sandbox; its own crate name and Python import surface are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Prove maturin build green + DCO-signed commit in nono-py</name>
  <files>../nono-py/Cargo.lock</files>
  <read_first>../nono-py/Cargo.toml (post-edit), ../nono-py/pyproject.toml (post-edit)</read_first>
  <action>
From the ../nono-py repo root, run `maturin build` (or `uv run maturin build` per its own CLAUDE.md uv-managed venv convention) to regenerate ../nono-py/Cargo.lock and prove the PyPI-facing build compiles against the renamed nono-sandbox/nono-sandbox-proxy core crates. Then, still inside ../nono-py, run `cargo tree -p nono-py 2>&1 | grep nono-sandbox` to confirm the resolved dependency graph points at the renamed crates via the correct relative path, not a stray registry hit (T-102-02). Stage exactly Cargo.toml, pyproject.toml, and the regenerated Cargo.lock, and commit with a DCO sign-off line `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>` -- this is a SEPARATE commit in the nono-py git repository, not part of this repo's own commit history.
  </action>
  <acceptance_criteria>
    - maturin build exits 0 from ../nono-py
    - cargo tree -p nono-py in ../nono-py, grepped for nono-sandbox, returns at least 2 matches (confirms the resolved dependency graph points at the renamed crates)
    - git -C ../nono-py log -1 --format=%B contains the exact line "Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>"
    - git -C ../nono-py show --stat -1 includes Cargo.toml, pyproject.toml, and Cargo.lock
  </acceptance_criteria>
  <verify>
    <automated>cd ../nono-py && maturin build</automated>
  </verify>
  <done>maturin build is green under the new names, the resolved dependency graph is confirmed to point at nono-sandbox, and the rename is committed in nono-py with a DCO sign-off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| ../nono-py's relative-path dependency to this repo's renamed crates | Outside Cargo's workspace-member graph; a wrong path/casing/version could silently resolve to an unintended location if not verified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-102-02 | Tampering | ../nono-py/Cargo.toml relative-path dependency resolution | mitigate | Task 2's `cargo tree -p nono-py` explicitly confirms the resolved graph points at nono-sandbox/nono-sandbox-proxy via the expected local path, plus `maturin build` success as a second independent confirmation |
| T-102-01 | Spoofing | PyPI nono-sandbox identity | accept (inherited) | Already gated live in Plan 102-01 Task 1 (404 confirmed); re-verified fresh at the Plan 102-05 phase gate -- no redundant re-check needed in this plan |
</threat_model>

<verification>
- grep checks confirm the exact package= additions and the pyproject.toml name change
- `maturin build` exits 0; `cargo tree` confirms resolution against nono-sandbox
- the nono-py commit is DCO-signed and contains exactly the 3 expected files
</verification>

<success_criteria>
- PUB-01 SC2 (nono-py PyPI project name renamed) is satisfied
- The nono-py half of PUB-01 SC4 (maturin build green) is satisfied
</success_criteria>

<output>
Create `.planning/phases/102-fork-owned-package-rename/102-03-SUMMARY.md` when done
</output>
