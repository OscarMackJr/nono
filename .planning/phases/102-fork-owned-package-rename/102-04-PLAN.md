---
phase: 102
plan: 04
type: execute
wave: 2
depends_on: ["102-01"]
files_modified:
  - ../nono-ts/Cargo.toml
  - ../nono-ts/package.json
  - ../nono-ts/npm/darwin-arm64/package.json
  - ../nono-ts/npm/darwin-x64/package.json
  - ../nono-ts/npm/linux-arm64-gnu/package.json
  - ../nono-ts/npm/linux-x64-gnu/package.json
  - ../nono-ts/Cargo.lock
autonomous: true
requirements: [PUB-01]

must_haves:
  truths:
    - "../nono-ts/Cargo.toml's nono path-dependency carries package = \"nono-sandbox\"; the dependency key remains nono"
    - "../nono-ts/package.json name is @oscarmackjr/nono-ts; napi.binaryName stays \"nono\"; the 4 existing npm/*/package.json platform subpackages + optionalDependencies are consistently renamed by the official napi rename tool"
    - "../nono-ts's own [package] name (nono-node, its Rust crate identity) is unchanged -- it is not in the 3-crate publish set and is never published to crates.io"
    - "napi build --platform --release succeeds in ../nono-ts under the new names"
    - "the rename commit in ../nono-ts is DCO-signed, and is a SEPARATE commit from this repo's and nono-py's commits"
  artifacts:
    - path: "../nono-ts/Cargo.toml"
      provides: "nono dependency carries package = \"nono-sandbox\""
    - path: "../nono-ts/package.json"
      provides: "name = \"@oscarmackjr/nono-ts\"; napi.binaryName unchanged; optionalDependencies renamed"
  key_links:
    - from: "../nono-ts/Cargo.toml [dependencies].nono"
      to: "crates/nono/Cargo.toml [package].name (this repo, Plan 102-01)"
      via: "package = \"nono-sandbox\" key, path = \"../Nono/crates/nono\""
      pattern: "package\\s*=\\s*\"nono-sandbox\""
---

<objective>
Patch the nono-ts sibling repo's Cargo.toml path-dependency with the matching `package =` key, then use the official `napi rename` tool to re-identify the npm package as the scoped `@oscarmackjr/nono-ts` (propagating to all 4 existing platform subpackages and `optionalDependencies` in one pass), and prove `napi build --platform --release` still succeeds -- without touching `napi.binaryName` or any generated `.node` artifact naming.

Purpose: `../nono-ts` is a separate git repository that path-deps into this workspace by relative path, entirely outside Cargo's workspace-member graph -- Plan 102-01's rename is invisible to it until this plan patches its own Cargo.toml. PUB-01 SC2/SC4 explicitly require the scoped npm identity renamed and `napi build` green (per RESEARCH.md Pitfall 2, `make build` in this repo never surfaces this repo's breakage).
Output: `../nono-ts/Cargo.toml` patched; `package.json` + all 4 platform subpackages + `optionalDependencies` renamed via the official tool; `../nono-ts/Cargo.lock` regenerated; a green `napi build --platform --release`; one DCO-signed commit in the nono-ts repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/102-fork-owned-package-rename/102-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch nono-ts's Cargo.toml dependency + run the official npm rename tool</name>
  <files>../nono-ts/Cargo.toml, ../nono-ts/package.json, ../nono-ts/npm/darwin-arm64/package.json, ../nono-ts/npm/darwin-x64/package.json, ../nono-ts/npm/linux-arm64-gnu/package.json, ../nono-ts/npm/linux-x64-gnu/package.json</files>
  <read_first>../nono-ts/Cargo.toml, ../nono-ts/package.json, 102-RESEARCH.md napi rename invocation Code Example + Anti-Patterns (leave --binary-name unset, don't hand-edit npm/* files)</read_first>
  <action>
In ../nono-ts/Cargo.toml, add package = "nono-sandbox" to the existing nono = { path = "../Nono/crates/nono", version = "0.66" } dependency entry -- key stays nono. Do not touch [package] name = "nono-node" -- nono-ts's own Rust crate identity is unaffected, not in the 3-crate publish set.
Then, from the ../nono-ts repo root, run `npx napi rename --package-name @oscarmackjr/nono-ts` -- deliberately do NOT pass --name or --binary-name, so napi.binaryName (currently "nono") and the generated .node artifact prefix stay unchanged. This single invocation is expected to rewrite package.json's name field, all 4 existing npm/*/package.json name fields (darwin-arm64, darwin-x64, linux-arm64-gnu, linux-x64-gnu), and the optionalDependencies block in the main package.json to reference the new scoped subpackage names. Note the pre-existing win32-x64-msvc subpackage directory gap (referenced in optionalDependencies/napi.targets but with no generated npm/win32-x64-msvc/ folder) is a pre-existing condition, not introduced by this rename -- do not attempt to create it.
  </action>
  <acceptance_criteria>
    - grep -c 'package = "nono-sandbox"' ../nono-ts/Cargo.toml returns 1
    - grep -c '^name = "nono-node"' ../nono-ts/Cargo.toml still returns 1 (untouched)
    - node -e "console.log(require('../nono-ts/package.json').name)" prints @oscarmackjr/nono-ts
    - node -e "console.log(require('../nono-ts/package.json').napi.binaryName)" still prints nono (unset flag preserved it)
    - node -e "console.log(Object.keys(require('../nono-ts/package.json').optionalDependencies).length)" prints 5 (all 5 platform entries present, renamed to the new scoped pattern)
    - node -e "console.log(require('../nono-ts/npm/darwin-x64/package.json').name)" no longer equals the old nono-ts-darwin-x64 literal (renamed by the tool); same check holds for the other 3 existing platform dirs
  </acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('../nono-ts/package.json'); if(p.name!=='@oscarmackjr/nono-ts') process.exit(1); if(p.napi.binaryName!=='nono') process.exit(1); console.log('OK')"</automated>
  </verify>
  <done>nono-ts's Cargo.toml resolves via package= against the renamed core crate; package.json + all 4 platform subpackages + optionalDependencies are consistently renamed by the official tool; binaryName is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Prove napi build green + DCO-signed commit in nono-ts</name>
  <files>../nono-ts/Cargo.lock</files>
  <read_first>../nono-ts/Cargo.toml (post-edit), ../nono-ts/package.json (post-edit)</read_first>
  <action>
From ../nono-ts, run `npx napi build --platform --release` to regenerate ../nono-ts/Cargo.lock and prove the npm-facing native build compiles against the renamed nono-sandbox core crate and the renamed package identity. Then run `cargo tree 2>&1 | grep nono-sandbox` in ../nono-ts to confirm the resolved dependency graph points at the renamed crate via the expected relative path (T-102-02). Stage the edited Cargo.toml, package.json, all touched npm/*/package.json files, and the regenerated Cargo.lock, and commit with a DCO sign-off line `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>` -- a SEPARATE commit in the nono-ts git repository.
  </action>
  <acceptance_criteria>
    - npx napi build --platform --release exits 0 from ../nono-ts
    - cargo tree in ../nono-ts, grepped for nono-sandbox, returns at least 1 match
    - git -C ../nono-ts log -1 --format=%B contains the exact line "Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>"
  </acceptance_criteria>
  <verify>
    <automated>cd ../nono-ts && npx napi build --platform --release</automated>
  </verify>
  <done>napi build is green under the new names, resolved dependency graph confirmed against nono-sandbox, and the rename is committed in nono-ts with a DCO sign-off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| ../nono-ts's relative-path dependency to this repo's renamed crates | Outside Cargo's workspace-member graph; a wrong path/casing/version could silently resolve to an unintended location if not verified |
| napi rename tool's rewrite of 6 JSON manifests in one pass | A partial/incomplete rewrite (e.g. missing a platform subpackage) would leave optionalDependencies referencing a stale name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-102-02 | Tampering | ../nono-ts/Cargo.toml relative-path dependency resolution + napi rename manifest rewrite | mitigate | Task 2's `cargo tree` confirms resolution against nono-sandbox; Task 1's per-file grep/node checks confirm all 4 platform subpackages + optionalDependencies were actually rewritten, not just the top-level package.json |
| T-102-01 | Spoofing | npm @oscarmackjr/nono-ts scoped identity | accept (inherited) | Already gated live in Plan 102-01 Task 1 (404 confirmed); re-verified fresh at the Plan 102-05 phase gate -- no redundant re-check needed in this plan. Note: the @oscarmackjr npm scope itself has no owning account yet (Pitfall 4) -- a non-blocking Phase 105 precondition, not this plan's concern |
</threat_model>

<verification>
- grep/node checks confirm the Cargo.toml package= addition and the full 6-file napi rename propagation
- `napi build --platform --release` exits 0; `cargo tree` confirms resolution against nono-sandbox
- the nono-ts commit is DCO-signed
</verification>

<success_criteria>
- PUB-01 SC2 (nono-ts npm package name renamed to the scoped @oscarmackjr/nono-ts) is satisfied
- The nono-ts half of PUB-01 SC4 (napi build green) is satisfied
</success_criteria>

<output>
Create `.planning/phases/102-fork-owned-package-rename/102-04-SUMMARY.md` when done
</output>
