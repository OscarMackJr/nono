---
phase: 102
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono/Cargo.toml
  - crates/nono-proxy/Cargo.toml
  - crates/nono-cli/Cargo.toml
  - crates/nono-shell-broker/Cargo.toml
  - bindings/c/Cargo.toml
  - Cargo.lock
autonomous: true
requirements: [PUB-01]

must_haves:
  truths:
    - "crates.io names nono-sandbox, nono-sandbox-proxy, nono-sandbox-cli are live-confirmed available (HTTP 404) before any Cargo.toml is edited"
    - "crates/nono/Cargo.toml [package] name is nono-sandbox; crates/nono-proxy/Cargo.toml [package] name is nono-sandbox-proxy; crates/nono-cli/Cargo.toml [package] name is nono-sandbox-cli"
    - "Every in-workspace dependent (nono-proxy, nono-cli, nono-shell-broker, bindings/c/nono-ffi) resolves its nono / nono-proxy path-dependency via an added package= key, with the dependency table key left as nono/nono-proxy — never the table key itself renamed"
    - "crates/nono-cli/Cargo.toml [[bin]] name = \"nono\" and [[bin]] name = \"nono-agentd\" are byte-for-byte unchanged; bindings/c/Cargo.toml [lib] name = \"nono_ffi\" and [package] name = \"nono-ffi\" are unchanged; crates/nono-shell-broker/Cargo.toml [package] name = \"nono-shell-broker\" is unchanged"
    - "cargo build --workspace --all-targets succeeds with exactly 3 renamed [[package]] name hunks in Cargo.lock and zero third-party dependency drift"
  artifacts:
    - path: "crates/nono/Cargo.toml"
      provides: "[package] name = \"nono-sandbox\""
    - path: "crates/nono-proxy/Cargo.toml"
      provides: "[package] name = \"nono-sandbox-proxy\"; nono dependency carries package = \"nono-sandbox\""
    - path: "crates/nono-cli/Cargo.toml"
      provides: "[package] name = \"nono-sandbox-cli\"; nono/nono-proxy dependencies carry package= keys; [[bin]] names unchanged"
    - path: "crates/nono-shell-broker/Cargo.toml"
      provides: "nono dependency carries package = \"nono-sandbox\"; own package name unchanged (not in the 3-crate publish set, but must still resolve for cargo build --workspace)"
    - path: "bindings/c/Cargo.toml"
      provides: "nono dependency carries package = \"nono-sandbox\"; own package name (nono-ffi) and [lib] name unchanged"
    - path: "Cargo.lock"
      provides: "regenerated lockfile reflecting the 3 renamed packages, zero third-party drift"
  key_links:
    - from: "crates/nono-proxy/Cargo.toml [dependencies].nono"
      to: "crates/nono/Cargo.toml [package].name"
      via: "package = \"nono-sandbox\" key; dependency table key (nono) unchanged"
      pattern: "package\\s*=\\s*\"nono-sandbox\""
    - from: "crates/nono-cli/Cargo.toml [dependencies].nono / .nono-proxy"
      to: "crates/nono/Cargo.toml + crates/nono-proxy/Cargo.toml [package].name"
      via: "package= keys"
      pattern: "package\\s*=\\s*\"nono-sandbox(-proxy)?\""
    - from: "crates/nono-shell-broker/Cargo.toml + bindings/c/Cargo.toml [dependencies].nono"
      to: "crates/nono/Cargo.toml [package].name"
      via: "package = \"nono-sandbox\" key (publish=false crates still must resolve for cargo build --workspace)"
      pattern: "package\\s*=\\s*\"nono-sandbox\""
---

<objective>
Live-verify all 5 new fork-owned registry names are unclaimed, then rename the 3-crate publish set's `[package] name` (nono, nono-proxy, nono-cli) to the fork-owned `nono-sandbox` family and reconcile every in-workspace path-dependency via Cargo's `package =` rename key -- leaving `[[bin]]`/`[lib]` names and every `use nono::`/`use nono_proxy::` import completely untouched.

Purpose: PUB-01 requires the fork to publish under owned identities without breaking the internal import surface or any dependent crate's build. This plan lands the mechanical core of that rename -- the single most consequential edit in Phase 102, on which every other plan (Makefile/CI reconciliation, both sibling-repo renames, the final phase gate) depends.
Output: 5 renamed/patched Cargo.toml manifests + a regenerated Cargo.lock; a green `cargo build --workspace --all-targets`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-fork-owned-package-rename/102-RESEARCH.md
@.planning/phases/102-fork-owned-package-rename/102-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live registry availability gate (SC3)</name>
  <files>None (read-only network verification; no files created or modified)</files>
  <read_first>102-RESEARCH.md "Registry availability checks" Code Example (crates.io/PyPI/npm curl commands, including the crates.io User-Agent requirement)</read_first>
  <action>
Run the 5 live registry-availability checks exactly as documented in 102-RESEARCH.md's Code Examples section: three crates.io API calls (nono-sandbox, nono-sandbox-proxy, nono-sandbox-cli), each WITH a User-Agent header (crates.io returns a misleading 403 without one -- never read that as "taken"); one PyPI JSON-endpoint call (nono-sandbox); one npm registry call for the percent-encoded scoped name (@oscarmackjr%2Fnono-ts). Confirm every one of the 5 returns HTTP 404 (available). This is a hard gate: if ANY of the 5 returns anything other than 404 (e.g. 200 = already claimed by a third party since the 2026-07-02 research pass), STOP immediately -- do not proceed to Task 2 or Task 3, and do not let any downstream plan (102-02/03/04/05) run. Report the collision instead of renaming into a now-contested name (T-102-01).
  </action>
  <acceptance_criteria>
    - crates.io: 3 curl calls (nono-sandbox, nono-sandbox-proxy, nono-sandbox-cli), each with a User-Agent header, each return exactly 404
    - PyPI: curl -s -o /dev/null -w "%{http_code}" https://pypi.org/pypi/nono-sandbox/json returns 404
    - npm: curl -s -o /dev/null -w "%{http_code}" https://registry.npmjs.org/@oscarmackjr%2Fnono-ts returns 404
    - All 5 checks and their returned status codes are logged in the task's own output before any Cargo.toml is touched
  </acceptance_criteria>
  <verify>
    <automated>for u in "https://crates.io/api/v1/crates/nono-sandbox" "https://crates.io/api/v1/crates/nono-sandbox-proxy" "https://crates.io/api/v1/crates/nono-sandbox-cli"; do c=$(curl -s -A "nono-plan/1.0 (oscar.mack.jr@gmail.com)" -o /dev/null -w "%{http_code}" "$u"); [ "$c" = "404" ] || { echo "FAIL $u -> $c"; exit 1; }; done; c=$(curl -s -o /dev/null -w "%{http_code}" https://pypi.org/pypi/nono-sandbox/json); [ "$c" = "404" ] || { echo "FAIL pypi -> $c"; exit 1; }; c=$(curl -s -o /dev/null -w "%{http_code}" "https://registry.npmjs.org/@oscarmackjr%2Fnono-ts"); [ "$c" = "404" ] || { echo "FAIL npm -> $c"; exit 1; }; echo "ALL 5 AVAILABLE"</automated>
  </verify>
  <done>All 5 registry names (crates.io x3, PyPI, npm-scoped) confirmed available (404) live, before any manifest edit.</done>
</task>

<task type="auto">
  <name>Task 2: Rename the 3-crate publish set + wire package= keys</name>
  <files>crates/nono/Cargo.toml, crates/nono-proxy/Cargo.toml, crates/nono-cli/Cargo.toml</files>
  <read_first>crates/nono/Cargo.toml, crates/nono-proxy/Cargo.toml, crates/nono-cli/Cargo.toml, 102-RESEARCH.md Standard Stack table + Code Examples + Pitfall 1 (dependency-key rename silently breaks use nono::)</read_first>
  <action>
In crates/nono/Cargo.toml, change the [package] name = "nono" line to name = "nono-sandbox" -- no other line in this file changes (no new [lib] section, no dependency edits).
In crates/nono-proxy/Cargo.toml, change [package] name = "nono-proxy" to name = "nono-sandbox-proxy"; in its [dependencies] table, add package = "nono-sandbox" to the existing nono = { version = "0.66.1", path = "../nono", default-features = false } entry -- the table key stays nono. Do not touch the system-keyring = ["nono/system-keyring"] feature-forwarding line (it references the dependency KEY, not the package name, per Cargo's documented feature-forwarding behavior -- unaffected by this rename).
In crates/nono-cli/Cargo.toml, change [package] name = "nono-cli" to name = "nono-sandbox-cli"; add package = "nono-sandbox" to the nono = { version = "0.66.1", path = "../nono", default-features = false } dependency entry and package = "nono-sandbox-proxy" to the nono-proxy = { version = "0.66.1", path = "../nono-proxy", default-features = false } entry -- both table keys stay nono/nono-proxy. Leave completely untouched: both [[bin]] sections (name = "nono", name = "nono-agentd"), the system-keyring = ["dep:keyring", "nono/system-keyring", "nono-proxy/system-keyring"] feature-forwarding line, the entire [package.metadata.deb] block, and the Windows-only dev-dependency nono-shell-broker = { path = "../nono-shell-broker", version = "0.66.1" } (nono-shell-broker's own name is not part of this rename).
  </action>
  <acceptance_criteria>
    - grep -c '^name = "nono-sandbox"$' crates/nono/Cargo.toml returns 1
    - grep -c '^name = "nono-sandbox-proxy"$' crates/nono-proxy/Cargo.toml returns 1
    - grep -c '^name = "nono-sandbox-cli"$' crates/nono-cli/Cargo.toml returns 1
    - grep -c 'package = "nono-sandbox"' crates/nono-proxy/Cargo.toml returns 1
    - grep -c 'package = "nono-sandbox"' crates/nono-cli/Cargo.toml returns 1 AND grep -c 'package = "nono-sandbox-proxy"' crates/nono-cli/Cargo.toml returns 1
    - grep -c 'name = "nono"' crates/nono-cli/Cargo.toml returns exactly 1 (only the surviving [[bin]] name = "nono" line remains referencing bare "nono")
    - grep -c 'nono/system-keyring' crates/nono-cli/Cargo.toml still returns 1 (feature-forwarding untouched)
  </acceptance_criteria>
  <verify>
    <automated>grep -q '^name = "nono-sandbox"$' crates/nono/Cargo.toml && grep -q '^name = "nono-sandbox-proxy"$' crates/nono-proxy/Cargo.toml && grep -q '^name = "nono-sandbox-cli"$' crates/nono-cli/Cargo.toml && grep -q 'package = "nono-sandbox"' crates/nono-proxy/Cargo.toml && grep -q 'package = "nono-sandbox"' crates/nono-cli/Cargo.toml && grep -q 'package = "nono-sandbox-proxy"' crates/nono-cli/Cargo.toml && echo OK</automated>
  </verify>
  <done>All 3 publish-set manifests carry their new [package] name; all path-dependencies among them carry the matching package= key; [[bin]] names and feature-forwarding syntax unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Patch remaining in-workspace dependents + regenerate Cargo.lock + verify workspace compiles</name>
  <files>crates/nono-shell-broker/Cargo.toml, bindings/c/Cargo.toml, Cargo.lock</files>
  <read_first>crates/nono-shell-broker/Cargo.toml, bindings/c/Cargo.toml, 102-RESEARCH.md Pitfall 2 (publish=false crates still must resolve their path-dep for cargo build --workspace)</read_first>
  <action>
In crates/nono-shell-broker/Cargo.toml, add package = "nono-sandbox" to its nono = { version = "0.66.1", path = "../nono" } dependency entry (key stays nono; its own [package] name = "nono-shell-broker" is UNCHANGED -- it is not in the 3-crate publish set, but cargo build --workspace still needs the path-dep to resolve, per Pitfall 2).
In bindings/c/Cargo.toml, add package = "nono-sandbox" to its nono = { version = "0.66.1", path = "../../crates/nono", default-features = false } dependency entry (key stays nono; its own [package] name = "nono-ffi" and [lib] name = "nono_ffi" are UNCHANGED).
Then run `cargo build --workspace --all-targets` from the repo root to regenerate Cargo.lock and prove the entire workspace (all 7 members: nono, nono-cli, nono-fltmgr-client, nono-proxy, nono-shell-broker, bindings/c, tools/sign-fixture) compiles under the new names -- this single command is what would surface a Pitfall-1-style E0433 if any dependency-key rename had been mistakenly applied instead of a package= key.
  </action>
  <acceptance_criteria>
    - grep -c 'package = "nono-sandbox"' crates/nono-shell-broker/Cargo.toml returns 1
    - grep -c 'package = "nono-sandbox"' bindings/c/Cargo.toml returns 1
    - cargo build --workspace --all-targets exits 0
    - git diff Cargo.lock shows exactly 3 renamed [[package]] name hunks (nono->nono-sandbox, nono-proxy->nono-sandbox-proxy, nono-cli->nono-sandbox-cli) and no other package's name/version/checksum fields changed
  </acceptance_criteria>
  <verify>
    <automated>cargo build --workspace --all-targets</automated>
  </verify>
  <done>Workspace builds green under the new names with a Cargo.lock diff containing exactly 3 renamed package hunks and no third-party drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local cargo/curl tooling to live crates.io/PyPI/npm registries | Availability check consumes untrusted third-party registry state; a name can be claimed by anyone at any time |
| This repo's renamed [package] name to 5 in-workspace dependent manifests | A path-dependency package= key must exactly match the producer's new name or the workspace fails to compile -- fail-closed (cargo errors loudly), never silently degrades |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-102-01 | Spoofing | crates.io/PyPI/npm registry names (nono-sandbox family) | mitigate | Task 1's live 404-check gates all renames in this plan; re-checked fresh at the Plan 102-05 phase gate, and again immediately before Phase 105's actual publish per RESEARCH.md's Security Domain guidance |
| T-102-02 | Tampering | Cargo dependency-table package= keys across 5 manifests | mitigate | Task 3's `cargo build --workspace --all-targets` is the authoritative check -- any misapplied key rename (vs. a correct package= addition) fails loudly as E0433/"no matching package found", never silently resolves to an unintended crate |
</threat_model>

<verification>
- `curl` availability gate (Task 1) returns 404 for all 5 names before any file is touched
- `grep` checks confirm the 3 renamed [package] names and all package= dependency additions land exactly as specified
- `cargo build --workspace --all-targets` exits 0 with a Cargo.lock diff limited to the 3 renamed packages
</verification>

<success_criteria>
- PUB-01 SC1 (crates.io [package] name renamed on the 3-crate publish set; internal path-deps reconciled; [[bin]]/[lib] unchanged) is fully satisfied by this plan
- PUB-01 SC3 (availability confirmed live before committing) is satisfied for this plan's own commit by Task 1
- The workspace half of PUB-01 SC4 (make build green) is set up for the Plan 102-02 Makefile/CI reconciliation to complete
</success_criteria>

<output>
Create `.planning/phases/102-fork-owned-package-rename/102-01-SUMMARY.md` when done
</output>
