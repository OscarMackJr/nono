---
phase: 89-proxy-hardening-sync
plan: 04
type: execute
wave: 2
depends_on: ["89-01", "89-02", "89-03"]
files_modified:
  - .planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md
autonomous: true
requirements: [PROXY-01, PROXY-02]
must_haves:
  truths:
    - "All Cluster F equivalence findings (D-09 #1077, D-01 #1048/#1091, D-02 #1151, D-10 #1132) are recorded in the Phase-85 ledger with the actual test fn names so future syncs expect the divergence and never blind cherry-pick (D-11)"
    - "Both won't-sync findings (D-05 #1192 forward_inner_request in absent tls_intercept/; D-04 #1199 intent/activation refactor + TlsInterceptIntent the fork cannot back) are recorded as won't-apply / won't-sync (architecture divergence)"
    - "The D-07 customCredentials activation fix is recorded as a deliberate fork-divergence with its commit SHA and guard test, naming 724bb207 (#1197) as the upstream reference"
  artifacts:
    - path: ".planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md"
      provides: "Phase 89 Cluster F Reconciliation Addendum appended after the Phase 88 CR-01 Addendum (after line 856)"
      contains: "Phase 89 Cluster F Reconciliation Addendum"
  key_links:
    - from: ".planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md addendum"
      to: "the regression/equivalence test fn names from 89-01/02/03 SUMMARYs"
      via: "Future sync note naming the guard tests"
      pattern: "Future sync note"
---

<objective>
Record all Phase 89 Cluster F reconciliation findings as a Phase-85 ledger addendum (D-11),
mirroring the Phase 87 CR-02 and Phase 88 CR-01 addendum format exactly, so future upstream syncs
expect the Cluster F divergences and never attempt blind cherry-picks.

Purpose: This phase reconciles a `split`-disposition cluster by behavioral test, not by applying
upstream code. The ledger is the durable record: which commits are equivalence (fork delivers the
intent differently, with a guard test), which are won't-sync (architecture divergence), and the one
deliberate fork-divergence that landed (the D-07 activation fix). Without this addendum, the next
sync window blind-cherry-picks the tls_intercept/RouteSelection/TlsInterceptIntent hunks again.

Output: A new `## Phase 89 Cluster F Reconciliation Addendum` section appended to
`85-DIVERGENCE-LEDGER.md` after the Phase 88 CR-01 Addendum.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-proxy-hardening-sync/89-CONTEXT.md
@.planning/phases/89-proxy-hardening-sync/89-RESEARCH.md
@.planning/phases/89-proxy-hardening-sync/89-PATTERNS.md
@.planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md
@.planning/phases/89-proxy-hardening-sync/89-01-SUMMARY.md
@.planning/phases/89-proxy-hardening-sync/89-02-SUMMARY.md
@.planning/phases/89-proxy-hardening-sync/89-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the Phase 89 Cluster F Reconciliation Addendum to the divergence ledger</name>
  <files>.planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md</files>
  <read_first>
    - .planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md lines 807-856 — the Phase 87 CR-02 Addendum and Phase 88 CR-01 Addendum: the EXACT `| Field | Value |` table + `**Future sync note:**` closing-paragraph format to mirror; the file currently ends at line 856 (append AFTER it with a `---` separator)
    - .planning/phases/89-proxy-hardening-sync/89-01-SUMMARY.md — the D-07 fix commit SHA, the three test names (proxy_activates_with_custom_credentials_only, block_net_overrides_custom_credentials_activation, build_proxy_config_maps_upstream_proxy_to_external_proxy), and cross-target verify status
    - .planning/phases/89-proxy-hardening-sync/89-02-SUMMARY.md — the D-02 and D-09 test names (connect_keeps_open_on_missing_proxy_auth, denied_endpoint_returns_403_and_audit) and whether either reproduced a gap
    - .planning/phases/89-proxy-hardening-sync/89-03-SUMMARY.md — the D-10 disproof test name (allow_domain_endpoint_route_does_not_shadow_credential_route) and confirmation the shadow did not reproduce
    - 89-RESEARCH.md lines 430-468 — the full target addendum template (Equivalence findings table, Won't-sync findings table, Deliberate fork-divergence | Field | Value | table, Future sync note paragraph) — use it verbatim, filling the test-name columns from the SUMMARYs
    - 89-PATTERNS.md lines 238-258 — the append-point (after line 856), the four required sub-sections, and the field rows for the D-07 fork-divergence table
  </read_first>
  <action>
    Append (do not overwrite) to `85-DIVERGENCE-LEDGER.md` after the Phase 88 CR-01 Addendum
    (current EOF ~line 856): a `---` separator then a `## Phase 89 Cluster F Reconciliation Addendum`
    section with an `**Added:** 2026-06-20 (Phase 89 execution — Cluster F proxy hardening
    reconciliation)` line and FOUR sub-sections, mirroring the 89-RESEARCH.md template (lines 430-468):
    (1) Equivalence findings table `| Commit (issue) | Intent | Fork delivers via | Equivalence test |`
    with rows for a5d623fd (#1077, D-09 → reverse.rs:96-115 endpoint default-deny → test
    `denied_endpoint_returns_403_and_audit`), b5f8db5c (#1048/#1091, D-01 → server.rs:534-561
    external_proxy honoring + proxy_runtime mapping → test
    `build_proxy_config_maps_upstream_proxy_to_external_proxy`), 7c9abd3b (#1151, D-02 →
    connect.rs:46-48 lenient undici-compat auth → test `connect_keeps_open_on_missing_proxy_auth`),
    b0b2c743 (#1132, D-10 → exact-prefix RouteStore + `_ep_` key namespace → test
    `allow_domain_endpoint_route_does_not_shadow_credential_route`).
    (2) Won't-sync findings table `| Commit (issue) | Reason | Classification |` with 76b7b695
    (#1192, D-05: forward_inner_request lives entirely in the absent tls_intercept/ → won't-apply)
    and bd4b6b7f (#1199, D-04: intent/activation refactor + TlsInterceptIntent the fork cannot back,
    conflicts with EffectiveProxySettings → won't-sync (arch divergence)).
    (3) Deliberate fork-divergence `| Field | Value |` table for the D-07 landed change — copy the
    exact field rows from the Phase 88 CR-01 Addendum: File = crates/nono-cli/src/proxy_runtime.rs;
    Fork lines = activation predicate (WARN branch ~95, ACTIVE branch ~116); Upstream reference
    commit = 724bb207 (#1197); Fork behavior after Phase 89 = activation predicate includes
    `!prepared.custom_credentials.is_empty()`; Reason = #1197 proxy did not start when only
    customCredentials was set; Classification = Behavioral fix (intent ported; upstream's
    intent/activation refactor NOT ported); Commit = the SHA from 89-01-SUMMARY.
    (4) A closing `**Future sync note:**` paragraph naming the regression/equivalence tests as
    reversion guards and reiterating: preserve the fork's exact-prefix RouteStore + EffectiveProxySettings;
    do NOT import RouteSelection or TlsInterceptIntent.
    Pull all test names + the D-07 commit SHA from the 89-01/02/03 SUMMARYs. If any SUMMARY reports
    that a test reproduced a real gap (against expectation), move that commit from the Equivalence
    table to the Deliberate fork-divergence section with its guard test and commit SHA.
    Commit with DCO sign-off (`Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`).
  </action>
  <verify>
    <automated>grep -c "Phase 89 Cluster F Reconciliation Addendum" .planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "Phase 89 Cluster F Reconciliation Addendum" .planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md` returns 1.
    - Source assertion: the addendum contains all four upstream equivalence commit SHAs (a5d623fd, b5f8db5c, 7c9abd3b, b0b2c743), both won't-sync SHAs (76b7b695, bd4b6b7f), and the D-07 upstream reference 724bb207.
    - Source assertion: the addendum names all five test fns from the 89-01/02/03 SUMMARYs (grep each: proxy_activates_with_custom_credentials_only or the D-07 guard, build_proxy_config_maps_upstream_proxy_to_external_proxy, connect_keeps_open_on_missing_proxy_auth, denied_endpoint_returns_403_and_audit, allow_domain_endpoint_route_does_not_shadow_credential_route).
    - Source assertion: the addendum contains a `**Future sync note:**` paragraph and the strings `RouteSelection` and `TlsInterceptIntent` in the "do not import" context.
    - The prior Phase 87/88 addenda content is intact (append-only; grep both "Phase 87 CR-02 Addendum" and "Phase 88 CR-01 Addendum" still present).
    - Commit carries the DCO sign-off line.
  </acceptance_criteria>
  <done>The Phase 89 reconciliation addendum is appended to the ledger with all equivalence/won't-sync/fork-divergence findings and guard-test names; prior addenda intact; committed with DCO.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| current sync provenance → future sync window | The ledger is the durable record that prevents a future sync from blind-cherry-picking the Cluster F divergent hunks (re-introducing the upstream shadow class / an unbackable TlsInterceptIntent) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-09 | Tampering | future blind cherry-pick of Cluster F | mitigate | The addendum records every equivalence/won't-sync finding with its guard test, so a future sync expects the divergence and does not blind-apply tls_intercept/RouteSelection/TlsInterceptIntent hunks (the documented attack surface for re-introducing the upstream shadow class). Docs-only change; no runtime code path. |
| T-89-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this phase. Docs-only edit. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `grep -c "Phase 89 Cluster F Reconciliation Addendum" .planning/phases/85-upst9-divergence-audit/85-DIVERGENCE-LEDGER.md` returns 1.
- Both prior addenda ("Phase 87 CR-02 Addendum", "Phase 88 CR-01 Addendum") still present (append-only).
- All five guard-test fn names and all seven Cluster F commit SHAs present in the addendum.
- This is a docs-only change — no code build/test required; the guard tests themselves are verified by 89-01/02/03.
</verification>

<success_criteria>
- The Phase 89 Cluster F Reconciliation Addendum is appended after the Phase 88 CR-01 Addendum (D-11).
- Equivalence findings (D-09, D-01, D-02, D-10) recorded with their guard-test names.
- Won't-sync findings (D-05 #1192, D-04 #1199) recorded as won't-apply / won't-sync.
- D-07 deliberate fork-divergence recorded with commit SHA, 724bb207 reference, and guard test.
- Future sync note reiterates: do not import RouteSelection / TlsInterceptIntent; preserve exact-prefix RouteStore + EffectiveProxySettings.
</success_criteria>

<output>
Create `.planning/phases/89-proxy-hardening-sync/89-04-SUMMARY.md` when done. Record: the addendum
section title, the commit SHA, and confirmation that prior addenda are intact and the DCO sign-off
is present.
</output>
