---
phase: 95-upstream-absorb-fork-invariant-verify
plan: 03
type: execute
wave: 2
depends_on:
  - 95-02
files_modified:
  - crates/nono-cli/src/proxy_runtime.rs
autonomous: true
requirements:
  - UPST10-02

must_haves:
  truths:
    - "D-02: fork's Phase 89 fail-secure proxy-activation divergence is preserved; ONLY the credentials_intent bug-fix block from 9b37dc52 is applied (upstream's explicit-activation refactor NOT adopted wholesale)"
    - "proxy_runtime.rs active predicate still includes the || !prepared.custom_credentials.is_empty() disjunct (Phase 89 fail-secure divergence preserved)"
    - "proxy_activates_with_custom_credentials_only test exists with opts.active == true assertion (NOT inverted)"
    - "Test is NOT renamed to test_proxy_is_inactive_when_only_custom_credentials_are_set"
    - "Upstream SHA 9b37dc52 is referenced in the commit message body (manual -x equivalent)"
    - "DCO Signed-off-by is present on the commit"
    - "make ci exits 0 after Cluster C absorb with no new failures beyond 5 baseline reds"
  artifacts:
    - path: "crates/nono-cli/src/proxy_runtime.rs"
      provides: "Cluster C credentials_intent fix applied; Phase 89 active predicate preserved"
      contains: "proxy_activates_with_custom_credentials_only"
  key_links:
    - from: "proxy_runtime.rs active predicate"
      to: "proxy_activates_with_custom_credentials_only test"
      via: "opts.active == true assertion guards the Phase 89 fail-secure invariant"
      pattern: "proxy_activates_with_custom_credentials_only"
    - from: "Cluster C 9b37dc52"
      to: "git log HEAD"
      via: "upstream SHA in commit message body + Signed-off-by"
      pattern: "9b37dc52"
---

<objective>
Absorb Cluster C (SHA 9b37dc52 — refactor(credentials): require explicit activation for custom
credentials #1215) as a surgical partial apply. Upstream's change REVERSES the fork's Phase 89
deliberate fail-secure divergence (0c08e5d2): the upstream removes the custom_credentials disjunct
from the proxy active predicate, while the fork deliberately kept it. This plan applies ONLY the
compatible credentials_intent fix-equivalent, preserves the fork's active predicate, and confirms
the Phase 89 regression sentinel test remains intact.

Purpose: D-02 locks the approach — the fork's fail-secure behavior (custom credentials
auto-activating the proxy so credentials are injected via proxy, not left exposed) is
non-negotiable. The upstream's structural refactor (CredentialProxyIntent wrapper) is
incompatible with the fork's flat ProxyLaunchOptions shape. The fork's proxy_runtime.rs
must not adopt the upstream's test inversion.

The applicable "fix" from 9b37dc52 is scoped to the credentials_intent block logic. Per
RESEARCH, the fork's ProxyLaunchOptions already propagates custom_credentials correctly
(line 126: custom_credentials: prepared.custom_credentials.clone()). The fix may be a
structural no-op in the fork but must be explicitly confirmed and the commit recorded.

Output: One absorb commit in git log referencing 9b37dc52; proxy_runtime.rs with active
predicate intact; proxy_activates_with_custom_credentials_only test green; make ci green.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-upstream-absorb-fork-invariant-verify/95-CONTEXT.md
@.planning/phases/95-upstream-absorb-fork-invariant-verify/95-RESEARCH.md
@.planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md
@.planning/phases/95-upstream-absorb-fork-invariant-verify/95-02-SUMMARY.md

<interfaces>
<!-- Read crates/nono-cli/src/proxy_runtime.rs (current fork HEAD after Cluster B) before touching anything. -->

From 94-DIVERGENCE-LEDGER.md Cluster C + 95-RESEARCH.md § Cluster C:

Files touched by 9b37dc52 (3 files):
  1. crates/nono-cli/src/proxy_runtime.rs — the conflict file; this is the ONLY file to change
  2. crates/nono-cli/src/network_policy.rs — SKIP entirely (fork already returns early on
     service_names.is_empty(); upstream's change would be a no-op or could break resolution)
  3. crates/nono-cli/src/launch_runtime.rs — SKIP entirely (fork uses active: bool flat field,
     not CredentialProxyIntent wrapper; structural mismatch — hunk does not apply to fork shape)

APPLY from proxy_runtime.rs (D-02):
  The credentials_intent block fix equivalent: confirm that custom_credentials propagation
  is correct in the fork. The upstream's change: `if has_credentials || !prepared.custom_credentials.is_empty()`
  for the credentials_intent block. In the fork, the structural equivalent is already present
  (custom_credentials: prepared.custom_credentials.clone() at line 126). If the upstream diff
  contains a hunk applicable to the fork's shape, apply it; if not, the commit records the
  explicit analysis with rationale (structural no-op confirmed).

PRESERVE (non-negotiable per D-02 + Phase 89 0c08e5d2):
  - The active predicate in prepare_proxy_launch_options: must retain
    || !prepared.custom_credentials.is_empty() at lines 95 and 118 of fork proxy_runtime.rs
  - Test proxy_activates_with_custom_credentials_only: assertion opts.active == true must remain
    (upstream inverts to opts.active == false with renamed test — DO NOT apply)

SKIP from 9b37dc52:
  - network_policy.rs hunk (all_names change — no-op/risk in fork)
  - launch_runtime.rs hunk (CredentialProxyIntent struct — absent in fork)
  - Test rename (test_proxy_is_active_when_only_custom_credentials_are_set → test_proxy_is_inactive_when_only_custom_credentials_are_set)
  - Test assertion inversion (opts.active == true → opts.active == false)

From crates/nono-cli/src/proxy_runtime.rs lines 90-119 (fork HEAD):
  The active computation includes the custom_credentials disjunct. This is the Phase 89
  deliberate divergence (0c08e5d2). Do not remove or weaken this.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply Cluster C credentials_intent fix to proxy_runtime.rs (preserve active predicate)</name>
  <files>crates/nono-cli/src/proxy_runtime.rs</files>
  <read_first>
    - crates/nono-cli/src/proxy_runtime.rs (current fork state after Cluster B Plan 95-02) — read the ENTIRE prepare_proxy_launch_options function; note lines defining active computation (~90-119), note custom_credentials propagation (~line 126), note proxy_activates_with_custom_credentials_only test (~line 503)
    - .planning/phases/95-upstream-absorb-fork-invariant-verify/95-RESEARCH.md § "Cluster C (9b37dc52) — Custom Credentials Explicit-Activation Refactor" — full APPLY/PRESERVE/SKIP instructions
    - .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md § "Cluster C: Custom Credentials Explicit-Activation Refactor" — fork conflict rationale + guard test reference
    - git show 9b37dc52 -- crates/nono-cli/src/proxy_runtime.rs — to see exact upstream diff for this file
  </read_first>
  <action>
    STEP 1 — Read the upstream diff for proxy_runtime.rs:
      git show 9b37dc52 -- crates/nono-cli/src/proxy_runtime.rs

    STEP 2 — Run git cherry-pick --no-commit 9b37dc52:
      git cherry-pick --no-commit 9b37dc52

    STEP 3 — Inspect what was applied to the working tree:
      git diff HEAD -- crates/nono-cli/src/proxy_runtime.rs
      git diff HEAD -- crates/nono-cli/src/network_policy.rs
      git diff HEAD -- crates/nono-cli/src/launch_runtime.rs

    STEP 4 — Reset the files that must be SKIPPED:
      git checkout -- crates/nono-cli/src/network_policy.rs
      git checkout -- crates/nono-cli/src/launch_runtime.rs

    STEP 5 — For proxy_runtime.rs, carefully inspect the applied diff. Two outcomes:

    Outcome A (hunk applies and is compatible): The upstream added a credentials_intent block
    that IS compatible with the fork's shape. In that case, keep the applied hunk but manually
    restore the active predicate lines if the upstream removed the custom_credentials disjunct.
    Specifically:
      - If the diff removed || !prepared.custom_credentials.is_empty() from the active computation:
        restore it. The active predicate MUST retain the custom_credentials disjunct.
      - If the diff renamed proxy_activates_with_custom_credentials_only to
        test_proxy_is_inactive_when_only_custom_credentials_are_set: revert the rename.
        Keep the original test name and opts.active == true assertion.

    Outcome B (hunk does not apply cleanly or is structurally incompatible): The upstream
    uses CredentialProxyIntent struct shape incompatible with fork's ProxyLaunchOptions.
    In that case, reset proxy_runtime.rs entirely:
      git checkout -- crates/nono-cli/src/proxy_runtime.rs
    And proceed to Step 6 with a "structural no-op" commit that records the analysis.

    STEP 6 — Verify the preserved invariants in proxy_runtime.rs:
      grep -n "custom_credentials.is_empty\|proxy_activates_with_custom_credentials_only" \
        crates/nono-cli/src/proxy_runtime.rs

    Both must be present:
      - custom_credentials.is_empty() in the active predicate (the disjunct || !prepared.custom_credentials.is_empty())
      - proxy_activates_with_custom_credentials_only as the test name

    STEP 7 — Stage the result:
    If Outcome A: stage the proxy_runtime.rs with the applied-and-patched diff:
      git add crates/nono-cli/src/proxy_runtime.rs

    If Outcome B: there are no staged changes (proxy_runtime.rs was reset).
    Still proceed to commit with the "structural no-op confirmed" message.

    STEP 8 — Run make fmt to normalize any formatting from manual edits:
      make fmt
      git add crates/nono-cli/src/proxy_runtime.rs 2>/dev/null || true

    STEP 9 — Commit with DCO and upstream SHA reference:

    For Outcome A (partial apply):
      git commit -s -m "refactor(proxy): absorb Cluster C credentials_intent fix from 9b37dc52

    Applies the compatible credentials_intent fix from upstream 9b37dc52 (refactor(credentials):
    require explicit activation for custom credentials #1215).

    Preserved (non-negotiable per D-02 + Phase 89 0c08e5d2 fork invariant):
    - active predicate retains || !prepared.custom_credentials.is_empty() disjunct
    - proxy_activates_with_custom_credentials_only test name and opts.active == true assertion

    Skipped (structurally incompatible with fork's ProxyLaunchOptions shape):
    - network_policy.rs hunk (fork already returns early on service_names.is_empty())
    - launch_runtime.rs hunk (CredentialProxyIntent struct absent in fork)
    - Test rename and assertion inversion

    Cherry-picked from upstream nolabs-ai/nono 9b37dc52"

    For Outcome B (structural no-op):
      git commit -s --allow-empty -m "refactor(proxy): record Cluster C 9b37dc52 as structural no-op in fork

    Upstream 9b37dc52 (refactor(credentials): require explicit activation for custom
    credentials #1215) refactors proxy activation using CredentialProxyIntent struct
    (Option<CredentialProxyIntent> in ProxyLaunchOptions.credentials). The fork uses a
    different structural shape: ProxyLaunchOptions has active: bool and
    custom_credentials: HashMap as flat fields. The upstream hunks are structurally
    incompatible and do not apply to the fork.

    The fork's equivalent behavior (custom_credentials auto-activates the proxy) is already
    correct per the Phase 89 fail-secure divergence (0c08e5d2). No code change needed.
    The proxy_activates_with_custom_credentials_only guard test continues to serve as
    the regression sentinel.

    Cherry-picked from upstream nolabs-ai/nono 9b37dc52"

    In either outcome, Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com> is mandatory.
  </action>
  <verify>
    <automated>cargo test -p nono-cli --lib -- proxy_runtime::tests::proxy_activates_with_custom_credentials_only</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p nono-cli --lib -- proxy_runtime::tests::proxy_activates_with_custom_credentials_only exits 0 (test ... ok)
    - grep "opts.active ==" in proxy_runtime.rs (around the proxy_activates test) shows "== true" (not "== false")
    - grep -n "proxy_activates_with_custom_credentials_only" crates/nono-cli/src/proxy_runtime.rs returns a match (test NOT renamed)
    - grep -n "custom_credentials.is_empty" crates/nono-cli/src/proxy_runtime.rs returns at least 1 match (active predicate preserved)
    - Test "test_proxy_is_inactive_when_only_custom_credentials_are_set" does NOT exist: grep "test_proxy_is_inactive" crates/nono-cli/src/proxy_runtime.rs returns 0 matches
    - git log --format="%B" HEAD | grep "9b37dc52" returns 1 match (upstream SHA in message)
    - git log --format="%B" HEAD | grep "Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>" returns 1 match
    - grep "CredentialProxyIntent" crates/nono-cli/src/proxy_runtime.rs returns 0 matches (upstream type absent)
  </acceptance_criteria>
  <done>Cluster C absorb committed; Phase 89 active predicate and guard test intact.</done>
</task>

<task type="auto">
  <name>Task 2: Run make ci gate after Cluster C and confirm no new failures</name>
  <files></files>
  <read_first>
    - ci-logs-local/baseline-95/baseline-before-cherry-picks.txt — the D-04 baseline (5 known reds)
    - .planning/phases/95-upstream-absorb-fork-invariant-verify/95-RESEARCH.md § "Pitfall 4" — make fmt before make ci
    - .planning/phases/95-upstream-absorb-fork-invariant-verify/95-RESEARCH.md § "Gate command" — make ci definition
  </read_first>
  <action>
    Run the full CI gate:
      make ci

    make ci runs: cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used,
    then cargo fmt --check, then cargo test --workspace.

    If make ci fails:
    - clippy failure: diagnose and fix the specific warning (no new allow attributes — use
      structural fixes per CLAUDE.md unwrap policy)
    - fmt failure: run make fmt then re-run make ci
    - New test failure (not in the 5-red baseline): this is a blocker; investigate and fix before
      proceeding to Plan 95-04

    Compare failure set to baseline:
      cargo test --workspace 2>&1 | grep "FAILED" > /tmp/post-cluster-c-failures.txt
      diff ci-logs-local/baseline-95/baseline-before-cherry-picks.txt /tmp/post-cluster-c-failures.txt

    Expected diff: empty or shows only the pre-existing 5 red tests. Any NEW "FAILED" line = blocker.

    After make ci passes, run the full Cluster F guard suite to confirm the Phase 89 Cluster F
    carve-out is intact (all guard tests from the 94-DIVERGENCE-LEDGER Carve-out Re-touch Check):
      cargo test -p nono-cli --lib -- proxy_runtime::tests::proxy_activates_with_custom_credentials_only
      cargo test -p nono-cli --lib -- proxy_runtime::tests::block_net_overrides_custom_credentials_activation
      cargo test -p nono-cli --lib -- proxy_runtime::tests::build_proxy_config_maps_upstream_proxy_to_external_proxy

    All three must pass (test ... ok). Note: if block_net_overrides_custom_credentials_activation
    or build_proxy_config_maps_upstream_proxy_to_external_proxy don't exist with those exact names,
    look for close name matches with cargo test -p nono-cli --lib -- proxy_runtime::tests 2>&1.

    Record results in SUMMARY.md.
  </action>
  <verify>
    <automated>make ci</automated>
  </verify>
  <acceptance_criteria>
    - make ci exits 0 (clippy + fmt + tests all pass)
    - cargo test --workspace shows the same 5 FAILED tests from baseline; no new failures introduced by Clusters A, B, or C
    - cargo test -p nono-cli --lib -- proxy_runtime::tests::proxy_activates_with_custom_credentials_only exits 0
    - git log --oneline -6 shows: [baseline] → [9ce74e92 Cluster A] → [Cluster B shared-surface] → [Cluster C absorb] (in that order)
    - All 3 will-sync/split commits (9ce74e92, 11fd10e0-hunks, 9b37dc52) have Signed-off-by in their messages: git log --format="%B" HEAD~3..HEAD | grep -c "Signed-off-by" returns at least 3 (one per commit or more)
  </acceptance_criteria>
  <done>All three Cluster absorbs committed; make ci green; Phase 89 Cluster F proxy guard tests pass; no new failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 9b37dc52 upstream diff → fork proxy_runtime.rs | Upstream INVERTS the fork's fail-secure proxy activation; must not apply the inversion |
| Phase 89 fork divergence (0c08e5d2) | The non-negotiable fork invariant: custom credentials auto-activate the proxy for credential injection; reverting this would expose credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-11 | Security Feature Bypass | Phase 89 proxy active predicate inverted — custom_credentials no longer auto-activate proxy, credentials bypass injection | mitigate | D-02 + PRESERVE rule: restore || !prepared.custom_credentials.is_empty() if removed by upstream diff; proxy_activates_with_custom_credentials_only test with opts.active == true is the automated gate |
| T-95-12 | Security Feature Bypass | test_proxy_is_inactive_... replaces the guard test — regression sentinel gone | mitigate | Acceptance criteria explicitly checks the test name has NOT been renamed; opts.active == true assertion must remain |
| T-95-13 | Repudiation | Cluster C commit has no upstream SHA reference — absorb lineage lost | mitigate | Commit message template includes "Cherry-picked from upstream nolabs-ai/nono 9b37dc52"; verified via git log grep |
| T-95-14 | Denial of Service | CredentialProxyIntent type introduced in fork — compile-time dep on upstream-only type | mitigate | SKIP launch_runtime.rs hunk entirely; post-commit grep "CredentialProxyIntent" returns 0 matches |
| T-95-SC | Tampering | No new package installs in this plan | accept | No Cargo.toml dep changes; no slopcheck required |
</threat_model>

<verification>
After Plan 95-03 completes:
- git log --oneline -6 shows 3 absorb commits on top of baseline commit
- make ci exits 0
- cargo test --workspace: exactly 5 FAILED (same set as baseline — try_set_mandatory_label + 3 protected_paths + profile_cmd)
- cargo test -p nono-cli --lib -- proxy_runtime::tests::proxy_activates_with_custom_credentials_only: test ... ok
- grep "opts.active == true" in proxy_activates_with_custom_credentials_only test body
- All 3 absorb commits: git log --format="%B" HEAD~3..HEAD | grep "Signed-off-by" shows 3+ lines
</verification>

<success_criteria>
- Cluster C absorb committed (partial apply or structural no-op, both are valid outcomes)
- Phase 89 active predicate (|| !prepared.custom_credentials.is_empty()) intact in proxy_runtime.rs
- proxy_activates_with_custom_credentials_only guard test passes (opts.active == true)
- Upstream SHA 9b37dc52 in commit message body + DCO sign-off
- make ci green; zero new test failures beyond D-04 baseline
</success_criteria>

<output>
Create .planning/phases/95-upstream-absorb-fork-invariant-verify/95-03-SUMMARY.md when done.
Include "## Cluster C Outcome" section: state whether Outcome A (partial apply) or Outcome B
(structural no-op) was the result and WHY, with the structural evidence.
</output>
