---
phase: 95-upstream-absorb-fork-invariant-verify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ci-logs-local/baseline-95/baseline-before-cherry-picks.txt
  - crates/nono/src/sandbox/linux.rs
  - crates/nono/src/supervisor/socket.rs
  - crates/nono-cli/src/exec_strategy.rs
  - crates/nono-cli/src/exec_strategy/supervisor_linux.rs
  - crates/nono-cli/tests/socket_access_run.rs
  - crates/nono-cli/data/profile-authoring-guide.md
autonomous: true
requirements:
  - UPST10-02

must_haves:
  truths:
    - "Baseline test snapshot exists at ci-logs-local/baseline-95/baseline-before-cherry-picks.txt before any cherry-pick is applied"
    - "Exactly 5 red tests are recorded in the baseline (try_set_mandatory_label + 3 protected_paths + profile_cmd test)"
    - "Commit 9ce74e92 (Cluster A AF_UNIX deadlock fix) is present in git log with -x trailer and Signed-off-by DCO"
    - "make ci exits 0 on the post-Cluster-A tree (clippy + fmt + tests; no new failures)"
  artifacts:
    - path: "ci-logs-local/baseline-95/baseline-before-cherry-picks.txt"
      provides: "D-04 baseline red-set proof — required before any cherry-pick"
      contains: "FAILED"
    - path: "crates/nono/src/sandbox/linux.rs"
      provides: "AF_UNIX BPF filter install ordering fix (dup2 bypass closed)"
    - path: "crates/nono/src/supervisor/socket.rs"
      provides: "new read_fd_number() IPC socket helper"
  key_links:
    - from: "ci-logs-local/baseline-95/baseline-before-cherry-picks.txt"
      to: "D-04 gate"
      via: "grep FAILED | count == 5"
      pattern: "try_set_mandatory_label|protected_paths|test_init_allowed"
    - from: "9ce74e92 cherry-pick"
      to: "git log HEAD"
      via: "-x trailer + Signed-off-by"
      pattern: "Cherry-picked from commit 9ce74e92"
---

<objective>
Capture the D-04 Windows baseline red set before any cherry-pick, then absorb Cluster A
(SHA 9ce74e92 — AF_UNIX mediation deadlock fix) into the fork via `git cherry-pick -x`.

Purpose: Cluster A is the sole will-sync commit in this window and is security-critical: it
closes 4 bugs in the Phase 87 SEC-01 AF_UNIX pathname mediation code path (deadlock, wrong jt
offsets, rate-limiter starvation, dup2 bypass). The baseline capture before this absorb is
mandatory per D-04 — it is the only way to prove that later cherry-picks introduce zero new
failures. Absorbing the security fix first (before the split commits) keeps the commit ordering
correct (9ce74e92 precedes 11fd10e0 in the upstream history).

Output: Baseline file in ci-logs-local/baseline-95/, one cherry-pick commit in git log (9ce74e92
with -x trailer + DCO), and make ci green on the post-absorb tree.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-upstream-absorb-fork-invariant-verify/95-CONTEXT.md
@.planning/phases/95-upstream-absorb-fork-invariant-verify/95-RESEARCH.md
@.planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md

<interfaces>
<!-- Key files the executor will modify. Read current state before touching anything. -->

From crates/nono/src/sandbox/linux.rs: Landlock implementation + BPF filter install for
AF_UNIX mediation (Phase 87 SEC-01). Cluster A's changes move IPC handshake BEFORE BPF
filter install, eliminating fd-based exemptions.

From crates/nono-cli/src/exec_strategy.rs: supervisor loop containing execute_supervised().
Cluster A touches the IPC handshake timing around line 1029. This file is also touched by
Cluster B (Plan 95-02) so the order matters: apply Cluster A first (correct upstream order).

Expected conflict site: exec_strategy.rs execute_supervised() — the fork's current HEAD has
pre-fix IPC logic. Cluster A's change is the fix. Accept the upstream change on all conflict
hunks (fork-preserve rule for Cluster A: none — this IS the fix).

Cluster A file inventory (6 files, SHA 9ce74e92):
  - crates/nono/src/sandbox/linux.rs (BPF filter ordering)
  - crates/nono/src/supervisor/socket.rs (new read_fd_number() helper — additive)
  - crates/nono-cli/src/exec_strategy.rs (supervisor loop IPC handshake timing)
  - crates/nono-cli/src/exec_strategy/supervisor_linux.rs (notify fd via pidfd_getfd)
  - crates/nono-cli/tests/socket_access_run.rs (new Linux integration test — additive)
  - crates/nono-cli/data/profile-authoring-guide.md (docs — additive)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture D-04 Windows baseline red set before cherry-picks</name>
  <files>ci-logs-local/baseline-95/baseline-before-cherry-picks.txt</files>
  <read_first>
    - .planning/phases/95-upstream-absorb-fork-invariant-verify/95-RESEARCH.md § "Baseline Capture Mechanics (D-04)" — exact command + expected 5-failure set
    - .planning/phases/95-upstream-absorb-fork-invariant-verify/95-CONTEXT.md § D-04 decision — gate semantics
    - Confirm current branch HEAD is the phase-base commit (0e3d1a68 or later post-quick-task commits) — run: git log --oneline -3
  </read_first>
  <action>
    Create the ci-logs-local/baseline-95/ directory. Run cargo test --workspace and tee the full output to ci-logs-local/baseline-95/baseline-before-cherry-picks.txt (per D-04; exact command in RESEARCH.md § Baseline Capture). After the run, grep for "FAILED" and "test result" lines and confirm exactly 5 failures appear with these test names:
      1. sandbox::windows::tests::try_set_mandatory_label_surfaces_directive_when_user_owned_apply_fails (crate: nono)
      2. profile_cmd::tests::test_init_allowed_when_pack_has_same_short_name (crate: nono-cli)
      3. protected_paths::tests::blocks_child_directory_capability (crate: nono-cli)
      4. protected_paths::tests::blocks_parent_directory_capability (crate: nono-cli)
      5. protected_paths::tests::requested_path_blocks_nonexistent_child_under_protected_root (crate: nono-cli)
    If the failure count or names differ from the expected 5, STOP and report before cherry-picking. Do not proceed until the baseline matches D-04 expectations.

    Add the baseline file to git and commit it with DCO sign-off:
      git add ci-logs-local/baseline-95/baseline-before-cherry-picks.txt
      git commit -s -m "chore(phase-95): capture D-04 Windows baseline red set before cherry-picks"

    The Signed-off-by trailer must be: "Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>"
  </action>
  <verify>
    <automated>grep -c "FAILED" ci-logs-local/baseline-95/baseline-before-cherry-picks.txt</automated>
  </verify>
  <acceptance_criteria>
    - File ci-logs-local/baseline-95/baseline-before-cherry-picks.txt exists and is non-empty
    - grep -c "FAILED" returns exactly 5
    - All 5 named tests match D-04 documented set (try_set_mandatory_label + 3 protected_paths + profile_cmd)
    - git log --format="%B" HEAD | grep "Signed-off-by" returns "Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>"
    - No cherry-pick has been applied yet (git log --oneline -5 shows only the baseline commit on top of phase-base)
  </acceptance_criteria>
  <done>Baseline file committed; D-04 gate anchored to the pre-cherry-pick state.</done>
</task>

<task type="auto">
  <name>Task 2: Cherry-pick Cluster A (9ce74e92) with conflict resolution and DCO sign-off</name>
  <files>
    crates/nono/src/sandbox/linux.rs
    crates/nono/src/supervisor/socket.rs
    crates/nono-cli/src/exec_strategy.rs
    crates/nono-cli/src/exec_strategy/supervisor_linux.rs
    crates/nono-cli/tests/socket_access_run.rs
    crates/nono-cli/data/profile-authoring-guide.md
  </files>
  <read_first>
    - .planning/phases/95-upstream-absorb-fork-invariant-verify/95-RESEARCH.md § "Cluster A (9ce74e92) — AF_UNIX Mediation Deadlock Fix" — full conflict surface map
    - .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md § "Cluster A: AF_UNIX Mediation Deadlock Fix" — per-commit table, fork-preserve rule, cross-target note
    - .planning/phases/95-upstream-absorb-fork-invariant-verify/95-RESEARCH.md § "Cherry-pick with -x and DCO trailer (Cluster A)" — exact commands
    - Current state of crates/nono-cli/src/exec_strategy.rs (fork HEAD — read the execute_supervised() function around line 1029 to understand the conflict site before cherry-picking)
    - proj/ADR-87-cr02-audit-bypass.md — confirm Cluster A does NOT touch audit.rs (it doesn't; this is a sanity check)
  </read_first>
  <action>
    Run: git cherry-pick -x 9ce74e92

    Expected outcome: conflict in crates/nono-cli/src/exec_strategy.rs around execute_supervised() (the supervisor loop IPC handshake timing). Possibly also in sandbox/linux.rs (BPF filter install ordering). The fork's current IPC logic is the pre-fix version; Cluster A's change IS the security fix.

    Conflict resolution rule for Cluster A (per RESEARCH and CONTEXT D-01 fork-preserve override): there is NO fork-specific behavior to preserve in this conflict. Accept the upstream change on all conflict hunks in exec_strategy.rs (the IPC ordering is the fix). If `git cherry-pick -x 9ce74e92` leaves CONFLICT markers, open each file and resolve by taking the upstream (theirs) version of the IPC handshake timing code. The exec_strategy_windows/ directory is NOT touched by this commit (windows-touch: no per ledger).

    After resolving conflicts, stage the resolved files with `git add <file>` for each conflict file. Then continue the cherry-pick:
      git cherry-pick --continue --no-edit

    After the cherry-pick commits, check whether the Signed-off-by DCO trailer is present:
      git log --format="%B" HEAD | grep "Signed-off-by"

    If missing, amend to add it:
      git commit --amend --trailer "Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>" --no-edit

    Verify the -x trailer is present:
      git log --format="%B" HEAD | grep "Cherry-picked from commit"

    The commit message must contain both:
      Cherry-picked from commit 9ce74e92...
      Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>

    Cross-target clippy note (D-03): sandbox/linux.rs and exec_strategy/supervisor_linux.rs contain
    #[cfg(target_os = "linux")] blocks. Per CLAUDE.md cross-target rule, the full cross-target
    clippy gates are DEFERRED to Phase 96 per D-03. Do NOT attempt cross-target clippy in this task.
    Record in the plan SUMMARY under "## PARTIAL Deferrals":
      Commit 9ce74e92 touches sandbox/linux.rs and exec_strategy/supervisor_linux.rs (#[cfg(linux)] blocks).
      Cross-target clippy DEFERRED to Phase 96 per D-03 and cross-target-verify-checklist.md.

    After the cherry-pick commits cleanly, run the full gate:
      make fmt
      make ci

    make ci = cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used + cargo fmt --check + cargo test --workspace. All must pass. New failures (beyond the 5 documented in ci-logs-local/baseline-95/) = blocker; fix before recording done.
  </action>
  <verify>
    <automated>git log --format="%B" HEAD | grep -c "Cherry-picked from commit 9ce74e92" && git log --format="%B" HEAD | grep "Signed-off-by: Oscar Mack Jr"</automated>
  </verify>
  <acceptance_criteria>
    - git log --oneline HEAD shows the 9ce74e92 absorb commit on top
    - git log --format="%B" HEAD | grep "Cherry-picked from commit 9ce74e92" returns 1 line (the -x trailer is present)
    - git log --format="%B" HEAD | grep "Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>" returns 1 line
    - make ci exits 0 (clippy + fmt + tests — no new failures beyond the 5 baseline reds)
    - cargo test --workspace shows the same 5 reds and no new ones: git diff ORIG_HEAD..HEAD -- crates/nono-cli/src/exec_strategy_windows/ is empty (exec_strategy_windows/ untouched)
    - grep -n "records_verified" crates/nono/src/audit.rs | grep "event_count" shows "event_count > 0" (CR-02 intact; audit.rs not touched by Cluster A)
  </acceptance_criteria>
  <done>Cluster A absorbed; 9ce74e92 present in git log with -x and DCO; make ci green; no new test failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream remote → fork HEAD | Cherry-picked content from nolabs-ai/nono (external, previously audited by Phase 94 ledger) |
| git cherry-pick resolution | Conflict resolution is manual — risk of incorrectly accepting/rejecting hunks |
| Baseline file | D-04 anchor; if the baseline is captured AFTER a cherry-pick, the gate is meaningless |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01 | Elevation of Privilege | Cluster A dup2 bypass fix | mitigate | 9ce74e92 IS the fix: moves IPC handshake before BPF filter; accept all upstream conflict hunks in exec_strategy.rs (no fork-preserve override for this commit) |
| T-95-02 | Denial of Service | Rate-limiter starvation (Cluster A bug 3) | mitigate | Fixed by 9ce74e92: non-AF_UNIX calls bypass rate limiter in AfUnixOnly mode; verify make ci passes post-absorb |
| T-95-03 | Tampering | D-04 baseline captured post-cherry-pick | accept with mitigation | Task ordering enforces baseline capture BEFORE git cherry-pick; if new failures appear post-absorb, executor must report STOP rather than proceed |
| T-95-04 | Tampering | DCO sign-off missing on cherry-pick commit | mitigate | Verification step: git log --format="%B" HEAD | grep "Signed-off-by" — required before plan is complete; amend if missing |
| T-95-05 | Elevation of Privilege | exec_strategy_windows/ accidentally modified during conflict resolution | mitigate | Post-cherry-pick check: git diff ORIG_HEAD..HEAD -- crates/nono-cli/src/exec_strategy_windows/ must be empty |
| T-95-SC | Tampering | No npm/pip/cargo new package installs in this plan | accept | Phase 95 installs zero new packages; no slopcheck required |
</threat_model>

<verification>
After Task 2 completes, the post-Wave-1 state must satisfy:
- git log --oneline -5 shows: [baseline commit] then [9ce74e92 absorb commit]
- make ci exits 0
- cargo test --workspace failure count == 5 (same as baseline)
- git diff HEAD~2 -- crates/nono-cli/src/exec_strategy_windows/ is empty
- grep "event_count > 0" crates/nono/src/audit.rs returns a match (CR-02 intact)
- ci-logs-local/baseline-95/baseline-before-cherry-picks.txt exists with exactly 5 FAILED lines
</verification>

<success_criteria>
- D-04 baseline file committed at the phase-base commit (before any cherry-pick)
- Cluster A (9ce74e92) absorbed with -x trailer and DCO sign-off
- make ci green on the post-absorb tree
- exec_strategy_windows/ byte-unchanged relative to Wave 1 start
</success_criteria>

<output>
Create .planning/phases/95-upstream-absorb-fork-invariant-verify/95-01-SUMMARY.md when done.
Include "## PARTIAL Deferrals" section noting:
  Cross-target clippy gate SKIPPED on Windows dev host due to missing toolchain
  (x86_64-unknown-linux-gnu, x86_64-apple-darwin). Commit 9ce74e92 touches
  crates/nono/src/sandbox/linux.rs and crates/nono-cli/src/exec_strategy/supervisor_linux.rs
  (#[cfg(target_os = "linux")] blocks). The live GH Actions Linux Clippy / macOS Clippy lanes
  on the head SHA are the decisive signals per .planning/templates/cross-target-verify-checklist.md.
  UPST10-02 marked PARTIAL pending Phase 96 XTGT verification.
</output>
