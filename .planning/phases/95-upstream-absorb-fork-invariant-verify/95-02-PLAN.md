---
phase: 95-upstream-absorb-fork-invariant-verify
plan: 02
type: execute
wave: 2
depends_on:
  - 95-01
files_modified:
  - crates/nono/src/audit.rs
  - crates/nono/src/sandbox/linux.rs
  - crates/nono/src/sandbox/mod.rs
  - crates/nono/src/keystore.rs
  - crates/nono/src/scrub.rs
  - crates/nono-proxy/src/route.rs
  - crates/nono-proxy/src/server.rs
autonomous: true
requirements:
  - UPST10-02

must_haves:
  truths:
    - "SandboxRuntimeAuditEvent and CommandPolicyAuditEvent structs are added to crates/nono/src/audit.rs (additive only)"
    - "records_verified: event_count > 0 is byte-intact after the audit.rs changes (CR-02 not violated)"
    - "restrict_execute() is exported via crates/nono/src/sandbox/mod.rs"
    - "cmd:// URI prefix guard is present in crates/nono/src/keystore.rs"
    - "SENSITIVE_ENV_VARS additions are present in crates/nono/src/scrub.rs"
    - "RouteConfig has endpoint_policy field in crates/nono-proxy/src/route.rs (no tls_intercept refs)"
    - "All AuditEventPayload::Network match arms and record_network_event call sites are updated to Box<NetworkAuditEvent>"
    - "supervisor/types.rs ApprovalRequest enum is absent (SKIP confirmed)"
    - "exec_strategy.rs wait_for_child_with_startup_timeout is absent-or-unchanged (SKIP confirmed)"
    - "make ci exits 0 on the post-Cluster-B tree (no new failures beyond 5 baseline reds)"
  artifacts:
    - path: "crates/nono/src/audit.rs"
      provides: "SandboxRuntimeAuditEvent + CommandPolicyAuditEvent + record_sandbox_runtime_event() + record_command_policy_event()"
      contains: "SandboxRuntimeAuditEvent"
    - path: "crates/nono/src/sandbox/linux.rs"
      provides: "has_execute() + restrict_execute() pub fns"
      contains: "restrict_execute"
    - path: "crates/nono/src/keystore.rs"
      provides: "cmd:// URI error guard"
      contains: "CMD_URI_PREFIX"
  key_links:
    - from: "crates/nono/src/audit.rs SandboxRuntimeAuditEvent"
      to: "AuditEventPayload enum"
      via: "additive variant — does not touch records_verified"
      pattern: "records_verified: event_count > 0"
    - from: "crates/nono-proxy/src/route.rs endpoint_policy"
      to: "RouteConfig struct literals"
      via: "grep RouteConfig { — all construction sites updated with endpoint_policy: None"
      pattern: "endpoint_policy"
---

<objective>
Absorb Cluster B (SHA 11fd10e0 — feat(sandbox): tool sandbox #1105) as a surgical manual
hunk extraction. This is the largest and most complex commit in the window: a 50+ file squash
that introduces an entire tool-sandbox subsystem absent in the fork. The plan extracts ONLY
the approved shared-surface additions (per D-01) and commits them as a fork-local absorb commit.

Purpose: Cluster B contains genuinely valuable additions to the shared library surface —
new audit event types, restrict_execute(), cmd:// keystore guard, SENSITIVE_ENV_VARS expansion,
and minimal proxy route/server wiring. These are cleanly separable from the tool-sandbox
machinery (ApprovalRequest enum, tls_intercept/ dir, exec_strategy.rs timeout refactor) which
is absent in the fork and cannot be applied without compile errors.

D-01 locks the approach: `git cherry-pick --no-commit 11fd10e0` then selectively stage only
the 7 approved files; reset everything else. The commit carries the upstream SHA reference in
its message (manual -x equivalent: "Cherry-picked from upstream nolabs-ai/nono 11fd10e0")
plus DCO sign-off.

Output: One absorb commit in git log with the Cluster B shared-surface additions; all skipped
tool-sandbox hunks absent; make ci green; CR-02 (records_verified: event_count > 0) intact.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-upstream-absorb-fork-invariant-verify/95-CONTEXT.md
@.planning/phases/95-upstream-absorb-fork-invariant-verify/95-RESEARCH.md
@.planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md
@.planning/phases/95-upstream-absorb-fork-invariant-verify/95-01-SUMMARY.md
@proj/ADR-87-cr02-audit-bypass.md

<interfaces>
<!-- Key apply/skip decisions. Read RESEARCH.md § Cluster B before touching anything. -->

APPLY from 11fd10e0 — 7 files:
  1. crates/nono/src/audit.rs
     APPLY hunks: SandboxRuntimeAuditEvent and CommandPolicyAuditEvent struct additions;
       record_sandbox_runtime_event() and record_command_policy_event() method additions;
       record_network_event() signature change: event: Box<NetworkAuditEvent>
     SKIP hunks: @@ -1390, @@ -1428, @@ -1602 test section (CapabilityRequest→ApprovalRequest
       rename + struct-literal fields endpoint_policy_action/approval_backend/credential_capture_* —
       all require ApprovalRequest enum which is tool-sandbox machinery not in fork)

  2. crates/nono/src/sandbox/linux.rs
     APPLY hunks: has_execute() method on DetectedAbi; restrict_execute() pub fn
     SKIP: nothing in these hunks; both are additive

  3. crates/nono/src/sandbox/mod.rs
     APPLY hunks: pub use linux::restrict_execute re-export; Sandbox::restrict_execute() method
     SKIP: nothing

  4. crates/nono/src/keystore.rs
     APPLY hunks: CMD_URI_PREFIX const + is_cmd_uri() fn + early-return error for cmd:// in load_secrets()
     SKIP: nothing — these hunks are self-contained

  5. crates/nono/src/scrub.rs
     APPLY hunks: SENSITIVE_ENV_VARS list additions (ANTHROPIC_API_KEY etc.); doc comment update
     SKIP: nothing

  6. crates/nono-proxy/src/route.rs
     APPLY hunks: endpoint_policy field on RouteConfig; CompiledEndpointPolicy import; allows_all_without_l7() call
     SKIP: all tls_intercept refs; #[cfg(test)] struct literals adding endpoint_policy: None may
       apply cleanly or may require manual fixup if RouteConfig { struct construction sites exist
     PRE-CHECK: grep -rn "RouteConfig {" crates/nono-proxy/src/ before applying — update all
       struct literal sites to include endpoint_policy: None

  7. crates/nono-proxy/src/server.rs
     APPLY hunks: shutdown_tx.send(true) call
     SKIP: approval_backends, credential_capture_backend, nonce_resolver fields;
       start_with_approval() and start_with_approval_registry() fns — require types absent in fork

SKIP entirely — do NOT stage any hunks from:
  - crates/nono-cli/src/tool-sandbox/ (entire dir — absent in fork)
  - crates/nono-proxy/src/tls_intercept/ (entire dir — fork has no tls_intercept/)
  - crates/nono-cli/src/exec_strategy.rs (timeout refactor — wait_for_child_with_startup_timeout removal entangled with tool-sandbox)
  - crates/nono/src/supervisor/types.rs (ApprovalRequest enum addition — tool-sandbox plumbing)
  - crates/nono-proxy/src/credential.rs (if touched — Cluster F carve-out; check before staging)
  - crates/nono-proxy/src/config.rs (if touched — Cluster F carve-out; check before staging)
  - crates/nono-proxy/src/reverse.rs (if touched — Cluster F carve-out; check before staging)

CRITICAL — audit.rs record_network_event boxing:
  Changing record_network_event() to box the event internally changes AuditEventPayload::Network
  variant from NetworkAuditEvent to Box<NetworkAuditEvent>. ALL match arms on
  AuditEventPayload::Network and all record_network_event() call sites must be updated
  atomically in the same commit. Pre-check:
    grep -rn "AuditEventPayload::Network\|record_network_event" crates/ --include="*.rs"
  Count call sites; update all before committing.

CR-02 invariant: grep -n "records_verified" crates/nono/src/audit.rs | grep "event_count"
  Must show: records_verified: event_count > 0
  This line must remain unchanged. The new event type structs are additive and do NOT touch
  the verify_audit_log return block.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage the cherry-pick --no-commit and reset skipped files</name>
  <files>
    crates/nono/src/audit.rs
    crates/nono/src/sandbox/linux.rs
    crates/nono/src/sandbox/mod.rs
    crates/nono/src/keystore.rs
    crates/nono/src/scrub.rs
    crates/nono-proxy/src/route.rs
    crates/nono-proxy/src/server.rs
  </files>
  <read_first>
    - .planning/phases/95-upstream-absorb-fork-invariant-verify/95-RESEARCH.md §§ "Cluster B (11fd10e0)" + "Pitfall 1" + "Pitfall 5" + "Pitfall 6" (the three Cluster B pitfalls)
    - .planning/phases/94-upst10-divergence-audit/94-DIVERGENCE-LEDGER.md § "Cluster B: Tool Sandbox Feature" — APPLY/SKIP tables + Cross-cluster re-export check
    - crates/nono/src/audit.rs (current fork state) — locate: records_verified line (~1435), AuditEventPayload enum, record_network_event signature
    - crates/nono-proxy/src/route.rs (current fork state) — confirm RouteConfig struct shape; count RouteConfig { construction sites
    - crates/nono-proxy/src/server.rs (current fork state) — confirm shutdown_tx pattern
  </read_first>
  <action>
    STEP 1 — Pre-flight checks (run BEFORE git cherry-pick --no-commit):

    Run each pre-check; note results before proceeding:
      grep -rn "AuditEventPayload::Network\|record_network_event" crates/ --include="*.rs"
      grep -rn "RouteConfig {" crates/nono-proxy/src/ --include="*.rs"
      grep -n "records_verified" crates/nono/src/audit.rs | grep "event_count"

    Expected: record_network_event appears in audit.rs definition + 1-3 call sites; RouteConfig {
    appears in 1-3 places in proxy crates; records_verified: event_count > 0 on ~line 1435.

    STEP 2 — Stage the cherry-pick without committing:

      git cherry-pick --no-commit 11fd10e0

    This will apply the full 50+ file upstream diff to the working tree (un-staged). The working
    tree will have files from the tool-sandbox dir and tls_intercept/ dir that must NOT be staged.

    STEP 3 — Reset all skipped files to HEAD (un-apply the upstream diff for these files):

    Reset tool-sandbox dir (entire directory — absent in fork):
      git checkout -- crates/nono-cli/src/tool-sandbox/ 2>/dev/null || true
      # (may error if git says "pathspec did not match" — that is fine; dir doesn't exist in HEAD)
      # If upstream staged new files under tool-sandbox/, remove them:
      git rm -rf --cached crates/nono-cli/src/tool-sandbox/ 2>/dev/null || true

    Reset tls_intercept/ dir (absent in fork):
      git checkout -- crates/nono-proxy/src/tls_intercept/ 2>/dev/null || true
      git rm -rf --cached crates/nono-proxy/src/tls_intercept/ 2>/dev/null || true

    Reset exec_strategy.rs (entangled with tool-sandbox timeout refactor — SKIP entire file):
      git checkout -- crates/nono-cli/src/exec_strategy.rs

    Reset supervisor/types.rs (ApprovalRequest enum addition — tool-sandbox plumbing):
      git checkout -- crates/nono/src/supervisor/types.rs

    For any other files touched by 11fd10e0 that are NOT in the 7-file APPLY list above (e.g.,
    credential.rs, config.rs, reverse.rs if modified): reset them too:
      git checkout -- crates/nono-proxy/src/credential.rs 2>/dev/null || true
      git checkout -- crates/nono-proxy/src/config.rs 2>/dev/null || true
      git checkout -- crates/nono-proxy/src/reverse.rs 2>/dev/null || true

    Run git status to enumerate what remains staged. Only the 7 approved files should have changes.
    If any other file appears staged, reset it.

    STEP 4 — audit.rs: remove the test section hunks via git add -p:

    The audit.rs changes that must be APPLIED are the new struct additions (around @@ -81 and @@ -357
    in the upstream diff). The test section changes (@@ -1390, @@ -1428, @@ -1602) must be DISCARDED
    because they reference ApprovalRequest (not in fork) and CapabilityDecision struct fields not in fork.

    Use `git add -p crates/nono/src/audit.rs` to interactively select only the correct hunks.
    Accept (y) the struct addition hunks and the recorder method hunks.
    Reject (n) any test section hunks that reference ApprovalRequest or add fields like
    endpoint_policy_action/approval_backend/credential_capture_backend to struct literals.

    Verify after staging: grep -n "records_verified" crates/nono/src/audit.rs | grep "event_count"
    Must still show: records_verified: event_count > 0 (CR-02 preserved)

    STEP 5 — Fix all AuditEventPayload::Network call sites and match arms (if boxing hunk was applied):

    If the record_network_event signature was changed to use Box<NetworkAuditEvent>, run:
      grep -rn "AuditEventPayload::Network\|record_network_event" crates/ --include="*.rs"
    For each match arm that pattern-matches on AuditEventPayload::Network { event }, update
    the binding to dereference the Box: AuditEventPayload::Network { event } => { ... *event ... }
    or match on Box<NetworkAuditEvent> appropriately. Stage all changes atomically.

    STEP 6 — route.rs: add endpoint_policy: None to all existing RouteConfig struct literals:

    After applying the endpoint_policy field hunk, run:
      grep -rn "RouteConfig {" crates/ --include="*.rs"
    For each struct construction site that does NOT yet have endpoint_policy, add:
      endpoint_policy: None,
    Stage the updated files.

    Verify route.rs does NOT contain any tls_intercept reference:
      grep "tls_intercept\|TlsIntercept" crates/nono-proxy/src/route.rs
    Expected: no output (the tls_intercept hunks must have been reset)
  </action>
  <verify>
    <automated>git status --short | grep -v "^??" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - git status shows ONLY changes to the 7 approved files (no tool-sandbox/ entries, no tls_intercept/ entries, no exec_strategy.rs, no supervisor/types.rs)
    - grep -n "records_verified" crates/nono/src/audit.rs shows "event_count > 0" (CR-02 intact)
    - grep "SandboxRuntimeAuditEvent\|CommandPolicyAuditEvent" crates/nono/src/audit.rs returns matches (new structs applied)
    - grep "tls_intercept\|TlsIntercept\|ApprovalRequest\|tool_sandbox\|tool-sandbox" crates/nono-proxy/src/route.rs crates/nono-proxy/src/server.rs crates/nono/src/audit.rs returns 0 matches (skip confirmed)
    - grep "CMD_URI_PREFIX" crates/nono/src/keystore.rs returns a match (keystore hunk applied)
    - cargo check --workspace exits 0 (staged changes compile before committing)
  </acceptance_criteria>
  <done>Working tree has only the 7 approved file changes staged; tool-sandbox machinery absent; ready for commit.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the Cluster B shared-surface extraction and run make ci gate</name>
  <files>
    crates/nono/src/audit.rs
    crates/nono/src/sandbox/linux.rs
    crates/nono/src/sandbox/mod.rs
    crates/nono/src/keystore.rs
    crates/nono/src/scrub.rs
    crates/nono-proxy/src/route.rs
    crates/nono-proxy/src/server.rs
  </files>
  <read_first>
    - .planning/phases/95-upstream-absorb-fork-invariant-verify/95-RESEARCH.md § "Manual hunk extraction for Cluster B" — exact commit message template
    - .planning/phases/95-upstream-absorb-fork-invariant-verify/95-RESEARCH.md § "Pitfall 4" — make fmt before make ci
    - proj/ADR-87-cr02-audit-bypass.md — CR-02 guard test name for the post-commit verification
    - Current git status after Task 1 (confirmed: only 7 approved files staged)
  </read_first>
  <action>
    Run make fmt first to ensure conflict resolution edits haven't introduced formatting issues:
      make fmt

    Stage any auto-formatting changes:
      git add crates/nono/src/audit.rs crates/nono/src/sandbox/linux.rs crates/nono/src/sandbox/mod.rs crates/nono/src/keystore.rs crates/nono/src/scrub.rs crates/nono-proxy/src/route.rs crates/nono-proxy/src/server.rs

    Commit with the exact message below (manual -x equivalent including the upstream SHA reference),
    using `git commit -s` for DCO auto-injection:

      git commit -s -m "feat(audit,sandbox): absorb Cluster B shared-surface additions from 11fd10e0

    Extracts additive shared-surface hunks from upstream 11fd10e0 feat(sandbox): tool sandbox (#1105).
    Skips the tool-sandbox subsystem directory (absent in fork), tls_intercept/ hunks (Cluster F
    carve-out), exec_strategy.rs timeout refactor (entangled with tool-sandbox), and ApprovalRequest
    type plumbing.

    Applied:
    - crates/nono/src/audit.rs: SandboxRuntimeAuditEvent + CommandPolicyAuditEvent structs;
      record_sandbox_runtime_event() + record_command_policy_event() methods;
      record_network_event() signature: Box<NetworkAuditEvent> (updated all call sites)
    - crates/nono/src/sandbox/linux.rs: has_execute() + restrict_execute() pub fns
    - crates/nono/src/sandbox/mod.rs: pub use linux::restrict_execute re-export
    - crates/nono/src/keystore.rs: CMD_URI_PREFIX const + cmd:// early-return guard
    - crates/nono/src/scrub.rs: SENSITIVE_ENV_VARS expansion
    - crates/nono-proxy/src/route.rs: endpoint_policy field (no tls_intercept refs)
    - crates/nono-proxy/src/server.rs: shutdown_tx.send(true) call

    Skipped: tool-sandbox/ dir, tls_intercept/, ApprovalRequest enum, exec_strategy.rs
    timeout refactor. CR-02 records_verified: event_count > 0 preserved intact.

    Cherry-picked from upstream nolabs-ai/nono 11fd10e0"

    Verify the commit includes both the upstream SHA reference and DCO:
      git log --format="%B" HEAD | grep "11fd10e0"
      git log --format="%B" HEAD | grep "Signed-off-by: Oscar Mack Jr"

    Now run the full gate:
      make ci

    make ci = clippy + fmt-check + tests. All must pass. If any NEW failures appear (beyond the 5
    in ci-logs-local/baseline-95/), investigate and fix before declaring done.

    Additionally, run the CR-02 guard test explicitly to confirm the audit invariant is intact:
      cargo test -p nono --lib -- audit::tests::verify_empty_log_with_no_stored_metadata_is_not_valid

    And confirm the sandbox/linux.rs cfg-gated Unix blocks are noted for Phase 96:
    In the SUMMARY.md "## PARTIAL Deferrals" section add:
      Commit (Cluster B applied hunks) touches crates/nono/src/sandbox/linux.rs and
      crates/nono/src/sandbox/mod.rs (#[cfg(target_os = "linux")] blocks).
      Cross-target clippy DEFERRED to Phase 96 per D-03.
  </action>
  <verify>
    <automated>cargo test -p nono --lib -- audit::tests::verify_empty_log_with_no_stored_metadata_is_not_valid</automated>
  </verify>
  <acceptance_criteria>
    - git log --format="%B" HEAD | grep "11fd10e0" returns 1 match (upstream SHA in message)
    - git log --format="%B" HEAD | grep "Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>" returns 1 match
    - make ci exits 0 (clippy + fmt + tests)
    - cargo test -p nono --lib -- audit::tests::verify_empty_log_with_no_stored_metadata_is_not_valid exits 0 (test ... ok)
    - grep -n "records_verified" crates/nono/src/audit.rs shows "event_count > 0" (CR-02 byte-intact)
    - grep "SandboxRuntimeAuditEvent" crates/nono/src/audit.rs returns a match (new type present)
    - grep "CMD_URI_PREFIX" crates/nono/src/keystore.rs returns a match
    - grep "restrict_execute" crates/nono/src/sandbox/mod.rs returns a match
    - ls crates/nono-cli/src/tool-sandbox 2>&1 | grep "No such file\|cannot find" confirms tool-sandbox dir absent
    - cargo test --workspace shows same 5 known-red failures and no new failures
  </acceptance_criteria>
  <done>Cluster B shared-surface extracted and committed; make ci green; CR-02 guard test passes; no new failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream 11fd10e0 (50+ file squash) → fork selective hunk apply | Most of this commit is tool-sandbox machinery absent in fork; manual staging controls what lands |
| audit.rs test section | ApprovalRequest enum references in tests would introduce compile-time deps on tool-sandbox types if applied; skipping preserves fork isolation |
| CR-02 carve-out | records_verified: event_count > 0 is a deliberate security divergence from upstream (ADR-87); must not be overwritten by additive struct additions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-06 | Tampering | CR-02 overwrite — records_verified reverted to true by additive audit.rs hunk | mitigate | Pre-check grep before staging; post-commit CR-02 guard test (verify_empty_log_with_no_stored_metadata_is_not_valid must pass); additive struct additions confirmed non-overlapping with verify_audit_log return block in RESEARCH |
| T-95-07 | Elevation of Privilege | ApprovalRequest type landing in fork — tool-sandbox approval machinery that bypasses CapabilityRequest flow | mitigate | supervisor/types.rs is in the SKIP list; post-staging check: grep -rn "ApprovalRequest" crates/ must return 0 matches |
| T-95-08 | Security Feature Bypass | tls_intercept/ hunks landing via route.rs or server.rs — would introduce TLS intercept path that is a Cluster F carve-out | mitigate | Post-staging check: grep "tls_intercept\|TlsIntercept" crates/nono-proxy/src/route.rs crates/nono-proxy/src/server.rs must return 0 matches |
| T-95-09 | Tampering | NetworkAuditEvent boxing breaks existing match arms — partial update leaves audit events mis-routed | mitigate | Pre-check grep -rn "AuditEventPayload::Network" crates/ --include="*.rs"; all match sites updated atomically in same commit per RESEARCH Pitfall 6 |
| T-95-10 | Elevation of Privilege | cmd:// URI loaded directly from keystore bypassing supervisor path | mitigate | Keystore hunk is the FIX: adds early-return error for cmd:// in load_secrets(); accepted wholesale per APPLY list |
| T-95-SC | Tampering | No new package installs in this plan | accept | No Cargo.toml dep changes; no slopcheck required |
</threat_model>

<verification>
Post-Wave-2a (after Plan 95-02 commits):
- git log --oneline -5 shows: [baseline] → [9ce74e92 absorb] → [Cluster B shared-surface absorb]
- grep "11fd10e0" .git/COMMIT_EDITMSG is stale; check: git log --format="%B" HEAD | grep "11fd10e0"
- make ci exits 0
- cargo test -p nono --lib -- audit::tests::verify_empty_log_with_no_stored_metadata_is_not_valid exits 0
- grep "records_verified" crates/nono/src/audit.rs shows "event_count > 0"
- cargo test --workspace shows exactly 5 FAILED (same as baseline)
</verification>

<success_criteria>
- Cluster B shared-surface additions committed (7 approved files; tool-sandbox/tls_intercept/ absent)
- Upstream SHA 11fd10e0 referenced in commit message body (manual -x equivalent)
- DCO Signed-off-by present on the commit
- CR-02 invariant (records_verified: event_count > 0) byte-intact and guard test green
- make ci exits 0; no new test failures beyond D-04 baseline of 5
</success_criteria>

<output>
Create .planning/phases/95-upstream-absorb-fork-invariant-verify/95-02-SUMMARY.md when done.
Include "## PARTIAL Deferrals" section noting:
  Cross-target clippy gate SKIPPED on Windows dev host due to missing toolchain
  (x86_64-unknown-linux-gnu, x86_64-apple-darwin). Cluster B applied hunks touch
  crates/nono/src/sandbox/linux.rs and crates/nono/src/sandbox/mod.rs
  (#[cfg(target_os = "linux")] blocks). The live GH Actions Linux Clippy / macOS Clippy lanes
  on the head SHA are the decisive signals per .planning/templates/cross-target-verify-checklist.md.
  UPST10-02 marked PARTIAL pending Phase 96 XTGT verification.
</output>
