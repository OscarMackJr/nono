---
phase: 95-upstream-absorb-fork-invariant-verify
plan: 05
type: execute
wave: 5
depends_on: []
files_modified:
  - crates/nono/src/sandbox/linux.rs
autonomous: true
requirements:
  - UPST10-02
  - UPST10-03
gap_closure: true

must_haves:
  truths:
    - "WR-02 closed: arch-portable offset_of! msghdr derivation + compile-time assertions + #[must_use] restored"
    - "WR-03 closed: --allow-gpu Linux Landlock enforcement (collect_linux_gpu_paths, is_nvidia_compute_device, caps.gpu() branch) restored"
  artifacts:
    - path: "crates/nono/src/sandbox/linux.rs"
      provides: "WR-02 msghdr hardening restored from phase-base ed6cdde1 (arch-portable offset_of!, compile-time assertions, #[must_use])"
      contains: "core::mem::offset_of!(libc::msghdr, msg_name)"
    - path: "crates/nono/src/sandbox/linux.rs"
      provides: "GPU enforcement"
      contains: "collect_linux_gpu_paths"
  key_links:
    - from: "read_msghdr_dest (linux.rs)"
      to: "core::mem::offset_of!(libc::msghdr, msg_name)"
      via: "MSG_NAME_OFFSET const"
      pattern: "MSG_NAME_OFFSET.*offset_of"
    - from: "caps.gpu() branch (linux.rs apply path)"
      to: "collect_linux_gpu_paths()"
      via: "if caps.gpu() { let (gpu_paths, nvidia_present) = collect_linux_gpu_paths();"
      pattern: "collect_linux_gpu_paths"
---

<objective>
Restore two fork-invariants in linux.rs that were silently reverted by the Cluster A
cherry-pick (ae77d198) conflict resolution: the WR-04 arch-portable msghdr offset
derivation (GAP: WR-02) and the Linux GPU enforcement path (GAP: WR-03).

Purpose: Both items are fork-only additions absent in upstream. The conflict resolution
accepted upstream's version of the conflicting linux.rs sections, which did not have
either the WR-04 offset hardening or the GPU enforcement. This is security-critical:
the msghdr offset derivation is on the AF_UNIX sendmsg mediation path, and the GPU
enforcement is required for --allow-gpu on Linux to do anything (currently silent fail-open).

Recovery source: git show ed6cdde1:crates/nono/src/sandbox/linux.rs — the phase-base
commit before any cherry-pick. The restore must NOT regress the Cluster A AF_UNIX
deadlock fix that legitimately landed in ae77d198.

Output: linux.rs with (a) read_msghdr_dest using MSG_NAME_OFFSET/MSG_NAME_LEN_OFFSET/
PTR_SIZE derived via core::mem::offset_of!/size_of, compile-time layout assertions,
and #[must_use] on both read_msghdr_dest and read_mmsghdr_dests; (b) is_nvidia_compute_device,
collect_linux_gpu_paths, and caps.gpu() Landlock allowlist branch restored; (c) associated
GPU unit tests restored; (d) make ci (native Windows) exits 0.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-upstream-absorb-fork-invariant-verify/95-CONTEXT.md
@.planning/phases/95-upstream-absorb-fork-invariant-verify/95-VERIFICATION.md

<interfaces>
Recovery source for BOTH tasks: git show ed6cdde1:crates/nono/src/sandbox/linux.rs

CURRENT STATE of crates/nono/src/sandbox/linux.rs (post-ae77d198 regressions):

WR-02 regression site (~line 2427):
  const MSGHDR_MIN_READ: usize = 12;  ← hardcoded LP64 value, WR-04 derivation reverted

read_msghdr_dest function (~lines 2419-2455) currently lacks:
  - const MSG_NAME_OFFSET: usize = core::mem::offset_of!(libc::msghdr, msg_name);
  - const MSG_NAME_LEN_OFFSET: usize = core::mem::offset_of!(libc::msghdr, msg_namelen);
  - const PTR_SIZE: usize = core::mem::size_of::<usize>();
  - const _: () = assert!(MSG_NAME_OFFSET < MSG_NAME_LEN_OFFSET, "...");
  - const _: () = assert!(MSG_NAME_OFFSET + PTR_SIZE <= MSG_NAME_LEN_OFFSET, "...");
  - The pointer-sized-aware read using MSG_NAME_OFFSET..MSG_NAME_OFFSET + PTR_SIZE
  - The WR-01 TOCTOU comment at the msg_name == 0 branch
  - #[must_use = "Result must be checked — None means msg_name was NULL (connected socket)"]

read_mmsghdr_dests (~line 2470) currently lacks:
  - #[must_use = "Result must be checked — empty Vec or all-None means all msg_name fields were NULL"]

WR-03 regression sites (all absent — zero GPU references in current linux.rs):
  - is_nvidia_compute_device function (~phase-base line 428): #[cfg(target_os = "linux")] fn
  - collect_linux_gpu_paths function (~phase-base line 476): #[cfg(target_os = "linux")] fn
  - caps.gpu() Landlock allowlist branch in apply path (~phase-base line 915):
      if caps.gpu() { let (gpu_paths, nvidia_present) = collect_linux_gpu_paths(); ... }
  - GPU unit tests (~phase-base lines 4442-4520): 4 tests

Phase-base GPU function locations (use git show ed6cdde1:... to view exact code):
  - is_nvidia_compute_device: phase-base lines 417-449
  - collect_linux_gpu_paths: phase-base lines 451-576 (approximately)
  - caps.gpu() apply branch: phase-base lines 915-957 (approximately)
  - GPU tests: phase-base lines 4440-4530

Cluster A AF_UNIX deadlock fix (MUST NOT be regressed):
  The ae77d198 cherry-pick legitimately moved IPC handshake BEFORE BPF filter install.
  The conflict touched linux.rs. When restoring fork-only code, preserve the Cluster A
  changes — do NOT revert ae77d198's functional changes to the BPF filter ordering.
  The WR-04 restore only replaces the const derivation inside read_msghdr_dest and
  adds #[must_use]; it does NOT change the function signature or its call sites.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore WR-02 — arch-portable msghdr offset derivation in read_msghdr_dest</name>
  <files>crates/nono/src/sandbox/linux.rs</files>
  <read_first>
    1. Run: git show ed6cdde1:crates/nono/src/sandbox/linux.rs | sed -n '2660,2800p'
       This shows the complete WR-04 hardened read_msghdr_dest + read_mmsghdr_dests from phase-base.
    2. Read current crates/nono/src/sandbox/linux.rs lines 2410-2510 to see the exact regression sites.
    3. Cross-reference: git show ed6cdde1:crates/nono/src/sandbox/linux.rs | grep -n "must_use.*read_msghdr\|must_use.*read_mmsghdr\|MSG_NAME_OFFSET\|MSG_NAME_LEN_OFFSET\|PTR_SIZE\|compile-time\|TOCTOU"
  </read_first>
  <action>
    In crates/nono/src/sandbox/linux.rs, perform the following targeted edits to restore WR-04
    arch-portable msghdr offset derivation. Do NOT change function signatures or call sites.

    Step 1 — Add #[must_use] to read_msghdr_dest (~current line 2419):
    Replace the function signature line:
      pub fn read_msghdr_dest(pid: u32, msghdr_ptr: u64) -> Result<Option<(u64, u64)>> {
    With the WR-04 attributed version (per ed6cdde1 line 2685):
      #[must_use = "Result must be checked — None means msg_name was NULL (connected socket)"]
      pub fn read_msghdr_dest(pid: u32, msghdr_ptr: u64) -> Result<Option<(u64, u64)>> {

    Step 2 — Replace the hardcoded const MSGHDR_MIN_READ block (~current lines 2422-2427):
    Remove:
      // struct msghdr (x86_64 Linux):
      //   void         *msg_name;      // offset 0,  8 bytes
      //   socklen_t     msg_namelen;   // offset 8,  4 bytes
      //
      // We only need the first 12 bytes.
      const MSGHDR_MIN_READ: usize = 12;
    Replace with the WR-04 derivation block (per ed6cdde1 lines 2690-2714):
      // Derive field layout from libc::msghdr at compile time (WR-04: arch-portable).
      //   msg_name:    *mut c_void — pointer-sized, at offset_of!(libc::msghdr, msg_name)
      //   msg_namelen: socklen_t  — u32, at offset_of!(libc::msghdr, msg_namelen)
      //
      // On LP64 (x86_64, aarch64): msg_name @ 0 (8 bytes), msg_namelen @ 8 (4 bytes) → 12.
      // On ILP32 (32-bit Linux):  msg_name @ 0 (4 bytes), msg_namelen @ 4 (4 bytes) → 8.
      // Using offset_of! / size_of ensures correctness on both, replacing the
      // original hard-coded MSGHDR_MIN_READ = 12 that was only valid on LP64.
      const MSG_NAME_OFFSET: usize = core::mem::offset_of!(libc::msghdr, msg_name);
      const MSG_NAME_LEN_OFFSET: usize = core::mem::offset_of!(libc::msghdr, msg_namelen);
      // size_of::<usize>() == pointer size on all Rust targets.
      const PTR_SIZE: usize = core::mem::size_of::<usize>();
      // We must read enough bytes to cover msg_namelen (u32 = 4 bytes) after its offset.
      const MSGHDR_MIN_READ: usize = MSG_NAME_LEN_OFFSET + 4;

      // Compile-time sanity assertions.
      const _: () = assert!(
          MSG_NAME_OFFSET < MSG_NAME_LEN_OFFSET,
          "msg_name must precede msg_namelen"
      );
      const _: () = assert!(
          MSG_NAME_OFFSET + PTR_SIZE <= MSG_NAME_LEN_OFFSET,
          "msg_name must not overlap msg_namelen"
      );

    Step 3 — Replace the hardcoded buf extraction block (~current lines 2441-2446):
    Remove the LP64-only extraction:
      // msg_name is a pointer (8 bytes, native endian on Linux)
      let msg_name = u64::from_ne_bytes([
          buf[0], buf[1], buf[2], buf[3], buf[4], buf[5], buf[6], buf[7],
      ]);
      // msg_namelen is socklen_t (4 bytes, native endian), cast to u64 for read_notif_sockaddr
      let msg_namelen = u32::from_ne_bytes([buf[8], buf[9], buf[10], buf[11]]) as u64;
    Replace with the arch-portable extraction (per ed6cdde1 lines 2726-2741):
      // msg_name: read pointer-sized value at MSG_NAME_OFFSET (native endian).
      // Copy the pointer bytes into a zero-padded u64 buffer (handles both
      // LP64/8-byte and ILP32/4-byte pointer sizes correctly).
      let mut name_buf = [0u8; 8];
      name_buf[..PTR_SIZE].copy_from_slice(&buf[MSG_NAME_OFFSET..MSG_NAME_OFFSET + PTR_SIZE]);
      let msg_name = u64::from_ne_bytes(name_buf);

      // msg_namelen: socklen_t is always u32 (4 bytes, native endian).
      let namelen_bytes: [u8; 4] = buf[MSG_NAME_LEN_OFFSET..MSG_NAME_LEN_OFFSET + 4]
          .try_into()
          .map_err(|_| {
              NonoError::SandboxInit(
                  "failed to extract msg_namelen bytes from msghdr buf".to_string(),
              )
          })?;
      let msg_namelen = u32::from_ne_bytes(namelen_bytes) as u64;

    Step 4 — Add WR-01 TOCTOU comment at the msg_name == 0 branch (~current line 2448):
    The current file has:
      if msg_name == 0 {
          // No destination address: the socket is connected, sendmsg just
          // sends data to the already-connected peer. No mediation needed.
          return Ok(None);
      }
    Replace the comment block (preserving the logic) with the WR-01 annotated version
    (per ed6cdde1 lines 2743-2755):
      if msg_name == 0 {
          // No destination address: the socket is connected, sendmsg just
          // sends data to the already-connected peer. No mediation needed.
          //
          // WR-01 accepted limitation: on CONTINUE the kernel re-reads the
          // destination from child memory; a multi-threaded child could swap
          // msg_name after we read it (TOCTOU). This window is inherited from
          // the upstream design and is documented at the call site in
          // supervisor_linux.rs. See Phase 87 REVIEW WR-01.
          return Ok(None);
      }

    Step 5 — Add #[must_use] to read_mmsghdr_dests (~current line 2470):
    Replace:
      pub fn read_mmsghdr_dests(pid: u32, msgvec_ptr: u64, vlen: u64) -> Result<Vec<Option<(u64, u64)>>> {
    With (per ed6cdde1 line 2771):
      #[must_use = "Result must be checked — empty Vec or all-None means all msg_name fields were NULL"]
      pub fn read_mmsghdr_dests(pid: u32, msgvec_ptr: u64, vlen: u64) -> Result<Vec<Option<(u64, u64)>>> {

    After all edits, run cargo test -p nono on the Windows host to verify no new failures.
    The test suite must compile and run with the same 1-red baseline (try_set_mandatory_label).
  </action>
  <verify>
    <automated>grep -n "MSG_NAME_OFFSET\|MSG_NAME_LEN_OFFSET\|PTR_SIZE\|offset_of!(libc::msghdr" crates/nono/src/sandbox/linux.rs</automated>
  </verify>
  <acceptance_criteria>
    - grep "const MSGHDR_MIN_READ: usize = 12" crates/nono/src/sandbox/linux.rs returns NO MATCH (hardcoded value gone)
    - grep "core::mem::offset_of!(libc::msghdr, msg_name)" crates/nono/src/sandbox/linux.rs returns a match
    - grep "core::mem::offset_of!(libc::msghdr, msg_namelen)" crates/nono/src/sandbox/linux.rs returns a match
    - grep "const PTR_SIZE: usize = core::mem::size_of::<usize>()" crates/nono/src/sandbox/linux.rs returns a match
    - grep "const _: () = assert!" crates/nono/src/sandbox/linux.rs returns at least 2 matches (the two compile-time assertions)
    - grep "must_use.*msg_name was NULL" crates/nono/src/sandbox/linux.rs returns a match (#[must_use] on read_msghdr_dest)
    - grep "must_use.*all msg_name fields were NULL" crates/nono/src/sandbox/linux.rs returns a match (#[must_use] on read_mmsghdr_dests)
    - grep "TOCTOU" crates/nono/src/sandbox/linux.rs returns a match (WR-01 comment restored)
    - grep "namelen_bytes.*try_into" crates/nono/src/sandbox/linux.rs returns a match (arch-portable namelen extraction)
    - cargo test -p nono exits with the same baseline (1 red: try_set_mandatory_label only; no new failures)
  </acceptance_criteria>
  <done>read_msghdr_dest uses offset_of!/size_of derivation, compile-time assertions present, #[must_use] on both functions, WR-01 TOCTOU comment restored. Hardcoded LP64 const gone.</done>
</task>

<task type="auto">
  <name>Task 2: Restore WR-03 — is_nvidia_compute_device, collect_linux_gpu_paths, caps.gpu() Landlock branch, and GPU tests</name>
  <files>crates/nono/src/sandbox/linux.rs</files>
  <read_first>
    1. Run: git show ed6cdde1:crates/nono/src/sandbox/linux.rs | sed -n '415,580p'
       Shows is_nvidia_compute_device + collect_linux_gpu_paths full text.
    2. Run: git show ed6cdde1:crates/nono/src/sandbox/linux.rs | sed -n '910,960p'
       Shows caps.gpu() Landlock allowlist branch in the apply path.
    3. Run: git show ed6cdde1:crates/nono/src/sandbox/linux.rs | sed -n '4440,4535p'
       Shows all 4 GPU unit tests.
    4. Read current linux.rs to find the correct insertion points:
       - Function insertion: find the section ~line 415 of phase-base where GPU helpers lived
         (after path-validation helpers, before the apply function). Grep current file:
         grep -n "fn is_device_path\|fn is_device_directory\|pub fn apply\|fn collect_linux\|fn is_nvidia" crates/nono/src/sandbox/linux.rs
       - Apply-path insertion: find where the Landlock ruleset is built before restrict_self().
         Grep: grep -n "restrict_self\|caps\.gpu\|ioctl_dev_available\|add_rule.*PathBeneath" crates/nono/src/sandbox/linux.rs | tail -20
       - Test section insertion: find the GPU test block comment or the end of the test module.
         Grep: grep -n "allow-gpu Linux Landlock\|test_is_nvidia\|test_collect_linux_gpu" crates/nono/src/sandbox/linux.rs
    5. Verify exec_strategy_windows/ is NOT touched: this task touches only linux.rs.
  </read_first>
  <action>
    In crates/nono/src/sandbox/linux.rs, restore the three deleted GPU enforcement regions.
    All restored code must be gated with #[cfg(target_os = "linux")] exactly as in ed6cdde1.

    Step 1 — Restore helper functions is_nvidia_compute_device and collect_linux_gpu_paths.
    Find the correct insertion point in the current file: these belong with the other
    Linux-specific helper functions, before the main apply() function. Use the phase-base
    location as the reference: ed6cdde1 placed them at ~lines 417-576 (after path-security
    helpers, before the apply function). In the current file, find a nearby anchor
    (e.g., fn is_device_path or fn is_device_directory) and insert after it.

    Restore is_nvidia_compute_device verbatim from ed6cdde1 lines 415-449:
    - Must include doc comment (/// Predicate matching NVIDIA compute device...)
    - #[cfg(target_os = "linux")] attribute
    - #[must_use] attribute
    - Full match logic: nvidiactl, nvidia-uvm, nvidia-uvm-tools, and nvidia[0-9]+ suffix check

    Restore collect_linux_gpu_paths verbatim from ed6cdde1 lines 451-576 (approximately):
    - Must include full doc comment (/// Enumerate GPU-related filesystem paths...)
    - #[cfg(target_os = "linux")] attribute (NOT #[must_use] — the function has side effects via probing)
    - Full body: /dev/dri/renderD*, /dev/nvidia* (via is_nvidia_compute_device), /dev/nvidia-caps/*,
      /dev/kfd, /dev/dxg, /usr/lib/wsl/lib, NVIDIA procfs paths gated on nvidia_present,
      /proc/self, /proc/self/task, /usr/share/vulkan, /etc/vulkan, /sys/class/drm
    - Returns (Vec<(PathBuf, AccessMode, bool)>, bool)

    Step 2 — Restore the caps.gpu() Landlock allowlist branch in the apply path.
    Find the insertion point: immediately before the final ruleset.restrict_self() call,
    after all other Landlock rule adds. Grep: grep -n "restrict_self" crates/nono/src/sandbox/linux.rs
    The phase-base had this block at ~line 915 (after exec/restrict_execute path adds, before restrict_self).

    Restore the caps.gpu() block verbatim from ed6cdde1 lines 915-957:
    - if caps.gpu() { ... } — no additional cfg gate needed (the whole fn is already Linux-only)
    - let (gpu_paths, nvidia_present) = collect_linux_gpu_paths();
    - Loop: for (gpu_path, gpu_access, is_file) in &gpu_paths
    - Inside loop: access_to_landlock, IoctlDev augmentation gated on ioctl_dev_available,
      debug! log, PathFd::new(gpu_path)?, ruleset.add_rule(PathBeneath::new(path_fd, access)),
      nvidia_device_count saturation count
    - After loop: info! log with gpu_paths.len(), nvidia_device_count, nvidia_present

    Step 3 — Restore GPU unit tests.
    Find the test module in linux.rs and add the 4 GPU tests from ed6cdde1 lines 4440-4530
    (approximately). They live in the existing #[cfg(test)] mod tests block.
    Insert them in a sub-section labeled // --allow-gpu Linux Landlock path list.
    The 4 tests:
    - test_is_nvidia_compute_device_accepts_upstream_list
    - test_is_nvidia_compute_device_rejects_non_compute
    - test_collect_linux_gpu_paths_is_callable_without_panic
    - test_collect_linux_gpu_paths_nvidia_procfs_gated_on_nvidia_presence

    After all edits, run make fmt and then cargo test -p nono on the Windows host.
    The test suite must compile; the 4 new GPU tests will NOT run on Windows (they are
    cfg(target_os = "linux") gated) but must not introduce compile errors on the Windows host.
    Run make ci. Only the pre-existing 1-red baseline failure (try_set_mandatory_label) is acceptable.
  </action>
  <verify>
    <automated>grep -c "collect_linux_gpu_paths\|is_nvidia_compute_device" crates/nono/src/sandbox/linux.rs</automated>
  </verify>
  <acceptance_criteria>
    - grep "fn is_nvidia_compute_device" crates/nono/src/sandbox/linux.rs returns a match
    - grep "fn collect_linux_gpu_paths" crates/nono/src/sandbox/linux.rs returns a match
    - grep "caps\.gpu()" crates/nono/src/sandbox/linux.rs returns a match (the if caps.gpu() branch in apply path)
    - grep "test_is_nvidia_compute_device_accepts_upstream_list" crates/nono/src/sandbox/linux.rs returns a match
    - grep "test_is_nvidia_compute_device_rejects_non_compute" crates/nono/src/sandbox/linux.rs returns a match
    - grep "test_collect_linux_gpu_paths_is_callable_without_panic" crates/nono/src/sandbox/linux.rs returns a match
    - grep "test_collect_linux_gpu_paths_nvidia_procfs_gated_on_nvidia_presence" crates/nono/src/sandbox/linux.rs returns a match
    - grep "nvidia_present" crates/nono/src/sandbox/linux.rs returns at least 3 matches (collect fn, apply branch, tests)
    - make ci exits 0 — only the pre-existing 1-red try_set_mandatory_label is acceptable; no new failures
    - git diff HEAD -- crates/nono-cli/src/exec_strategy_windows/ is empty (Windows backend untouched)
    - git log --format="%B" HEAD shows DCO sign-off: Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>
  </acceptance_criteria>
  <done>is_nvidia_compute_device + collect_linux_gpu_paths present; caps.gpu() Landlock branch wired; 4 GPU unit tests restored; make ci green on Windows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase-base ed6cdde1 → current HEAD | Recovery source; executor must use git show ed6cdde1 verbatim, not re-implement |
| ae77d198 cherry-pick changes | Cluster A AF_UNIX deadlock fix must be preserved; WR-04 restore must not revert it |
| linux.rs cfg-gated code | #[cfg(target_os = "linux")] gates must be correct; Windows host cannot compile-test them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-05-01 | Elevation of Privilege | read_msghdr_dest hardcoded LP64 offset | mitigate | Restore offset_of!/size_of derivation; compile-time assertions prove no overlap |
| T-95-05-02 | Elevation of Privilege | --allow-gpu flag accepted with zero Landlock enforcement on Linux | mitigate | Restore caps.gpu() branch + collect_linux_gpu_paths; unit tests verify function is callable |
| T-95-05-03 | Tampering | Conflict with ae77d198 AF_UNIX deadlock fix during WR-04 restore | mitigate | WR-04 restore is additive (replaces const derivation, adds #[must_use], adds TOCTOU comment); does NOT change function call sites or IPC ordering |
| T-95-05-SC | Tampering | No npm/pip/cargo new package installs | accept | No new packages; slopcheck not required |
</threat_model>

<verification>
After both tasks complete:
- grep "const MSGHDR_MIN_READ: usize = 12" crates/nono/src/sandbox/linux.rs returns NO MATCH
- grep "core::mem::offset_of!(libc::msghdr, msg_name)" crates/nono/src/sandbox/linux.rs returns a match
- grep "fn collect_linux_gpu_paths" crates/nono/src/sandbox/linux.rs returns a match
- grep "caps\.gpu()" crates/nono/src/sandbox/linux.rs returns a match
- make ci exits 0 on Windows host
- git diff HEAD -- crates/nono-cli/src/exec_strategy_windows/ is empty
</verification>

<success_criteria>
- WR-02 closed: read_msghdr_dest uses core::mem::offset_of!/size_of derivation; compile-time assertions present; #[must_use] on both functions; TOCTOU comment restored
- WR-03 closed: is_nvidia_compute_device + collect_linux_gpu_paths present; caps.gpu() Landlock branch wired in apply path; 4 GPU unit tests committed
- make ci exits 0 (only the pre-existing try_set_mandatory_label baseline red permitted)
- Cluster A AF_UNIX deadlock fix from ae77d198 unregressed (IPC ordering intact)
- DCO Signed-off-by on all commits
</success_criteria>

<output>
Create .planning/phases/95-upstream-absorb-fork-invariant-verify/95-05-SUMMARY.md when done.
</output>
