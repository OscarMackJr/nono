---
phase: 116-tool-sandbox-divergence-audit-disposition-adr
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-FEASIBILITY-MATRIX.md
autonomous: true
requirements: [TSBX-02]

must_haves:
  truths:
    - "Every fork Windows-confinement symbol cited in the matrix is independently re-greppable at fork HEAD, including pub(super)-only surfaces that a naive pub|pub(crate) grep would miss."
    - "Every upstream capability row is rated implementable-on-fork-primitives / implementable-but-new-work / structurally-blocked, with a citation to either a specific re-greppable fork symbol or ADR-65's standing verdict -- never to file presence alone."
    - "No matrix row states or implies a preference for Pole A or Pole B; every rating is phrased as a neutral capability fact, not an argument."
    - "The document states, once and prominently, the date every cited grep/command was run -- satisfying D-16's 'command + hit count + date' evidence discipline document-wide."
  artifacts:
    - path: ".planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-FEASIBILITY-MATRIX.md"
      provides: "Fork Windows-confinement primitive inventory + upstream tool-sandbox capability rating table, cited by proj/ADR-116-tool-sandbox-disposition.md and consumed by Plan 116-05's D-09 scoring and Plan 116-06's Phase 120 sizing"
      contains: "structurally-blocked"
  key_links:
    - from: ".planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-FEASIBILITY-MATRIX.md"
      to: "crates/nono-cli/src/exec_strategy_windows/"
      via: "fork primitive citations in the capability rating table"
      pattern: "exec_strategy_windows"
    - from: ".planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-FEASIBILITY-MATRIX.md"
      to: ".planning/architecture/adr-65-minifilter-go-no-go.md"
      via: "structurally-blocked rows citing the standing No-go/Conditional-go verdict"
      pattern: "ADR-65"
---

<objective>
Build `116-FEASIBILITY-MATRIX.md`: an inventory of the fork's existing Windows confinement
primitives, plus a per-capability rating of what upstream's `tool-sandbox/platform/{linux,macos}.rs`
drivers actually do, scored implementable-on-fork-primitives / implementable-but-new-work /
structurally-blocked. This is factual grounding only -- it answers "is it possible", not "should
we build it". The scoring (D-09) and verdict (D-10/D-11) are separate plans (116-05, 116-06); this
plan supplies their evidence, and must not pre-argue either side.

Purpose: D-07 requires every capability rating to cite a specific fork symbol or ADR-65's verdict,
never file presence alone (D-16). Running independently of the ledger (Plan 116-01) since it reads
fork HEAD and upstream `v0.71.0` source directly, not commit history -- genuine Wave 1 parallelism,
no file overlap with `116-DIVERGENCE-LEDGER.md`.
Output: `116-FEASIBILITY-MATRIX.md`, an appendix the ADR (`proj/ADR-116-tool-sandbox-disposition.md`)
cites rather than duplicates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-CONTEXT.md
@.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-RESEARCH.md
@.planning/architecture/adr-65-minifilter-go-no-go.md

<interfaces>
<!-- Fork symbol inventory, live-verified 2026-08-09 (RESEARCH.md §3a/§3b) -- re-verify at
     execution time, fork HEAD may have moved. The Pitfall 2 finding is load-bearing: a naive
     `pub|pub(crate)` grep returns ZERO hits on exec_strategy_windows/supervisor.rs (241 KB,
     load-bearing -- WindowsSupervisorRuntime, WindowsSupervisedChild, etc.) because its entire
     public API is `pub(super)`. Any grep pattern used for this plan's evidence column MUST also
     match `pub(super)` and `pub(in ...)`, not just `pub`/`pub(crate)`. -->

  restricted_token.rs -> generate_session_sid(), generate_app_container_name()
  labels_guard.rs -> AppliedLabelsGuard::snapshot_and_apply()
  dacl_guard.rs -> AppliedDaclGrantsGuard / AppliedAncestorTraverseGuard / AppliedAncestorReadAttributesGuard, each ::snapshot_and_apply[_targets]()
  network.rs -> install/start/uninstall_windows_wfp_{service,driver}(), probe_windows_wfp_readiness()
  launch.rs -> create_process_containment(), apply_process_handle_to_containment(), terminate_job_object(), verify_broker_authenticode(), JobObjectHandle::create()
  supervisor.rs (pub(super) ONLY) -> SendableHandle, WindowsSupervisorLifecycleState, WindowsSupervisedChild, WindowsSupervisorRuntime, initialize(), run_child_event_loop(), startup_failure()/command_failure(), open_windows_supervisor_path() -- 23 items total, all invisible to a pub|pub(crate)-only grep
  mod.rs -> ExecStrategy/ThreadingContext enums, ExecConfig/SupervisorConfig, WindowsWfp*Report structs, execute_direct()/execute_supervised(), probe_job_object_permissions(), probe_integrity_level_support(), probe_bfe_service_status()

  claude_code_hook.rs -> pub(crate) fn run()
  hooks.rs -> HookInstallResult, install_hooks(), install_profile_hooks(), NONO_HOOK_SH / NONO_TOOL_HOOK_PS1 consts
  execution_runtime.rs -> execution_start_dir(), execute_sandboxed() -- also the BrokerLaunchNoPty XOR WriteRestricted token-arm split (the WRITE_RESTRICTED worked example, D-07/D-08)
  crates/nono-shell-broker/src/main.rs -> BrokerArgs, parse_args(), build_command_line(), run()

<!-- Upstream capability surface to rate, from platform/linux.rs + platform/macos.rs at v0.71.0
     (RESEARCH.md §2f/§2g/§4): per-command filesystem grants, exec shims, url_shim, token_broker,
     dynamic_providers, env scrubbing, per-command open-port mediation, sealed-shim runtime dir.
     `platform/mod.rs`'s non-cfg-gated top defines a no-op stub for non-linux/macos targets --
     upstream's own crate already compiles cleanly on Windows today, subsystem inert; this is a
     neutral factual grounding point, not an argument for either pole. -->

  RANGE (for any re-derivation needed) = 0551eba27ea53b0eda20f4f796f757dd168adb92..0055bf3c686de6fa665decd9831dc5686b282cb0
  v0.71.0 = 0055bf3c686de6fa665decd9831dc5686b282cb0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fork Windows confinement primitive inventory (re-greep at HEAD, catch pub(super))</name>
  <files>.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-FEASIBILITY-MATRIX.md</files>
  <read_first>
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-CONTEXT.md (D-07, D-08, D-16 in full)
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-RESEARCH.md §3 (fork symbol inventory; Pitfall 2 -- the pub(super) under-report finding)
    Skill("spike-findings-nono") -- read `.claude/skills/spike-findings-nono/SKILL.md` and both reference files (`references/windows-confinement-model.md`, `references/engine-agnostic-confinement.md`) in full before writing any capability grounding note
    crates/nono-cli/src/exec_strategy_windows/{mod.rs,restricted_token.rs,labels_guard.rs,dacl_guard.rs,network.rs,launch.rs,supervisor.rs}
    crates/nono-cli/src/claude_code_hook.rs, crates/nono-cli/src/hooks.rs, crates/nono-cli/src/execution_runtime.rs
    crates/nono-shell-broker/src/main.rs
  </read_first>
  <action>
    Create `116-FEASIBILITY-MATRIX.md` with a document header: a title line, immediately followed
    by a `**Date measured:** <today's date>` field, and directly beneath it a prominent statement:
    "All grep commands, hit counts, and symbol citations in this document were run and verified on
    the date above; every cell's evidence traces to that single date rather than being repeated
    per-row (D-16 -- command + hit count + date). A reader re-running any command after this date
    should expect drift and re-verify against fork HEAD / upstream `v0.71.0` at that time." This
    header satisfies D-16's date requirement once, document-wide, rather than duplicating a date
    column on every row. For each file in `crates/nono-cli/src/exec_strategy_windows/` plus
    `claude_code_hook.rs`, `hooks.rs`, `execution_runtime.rs`, and
    `crates/nono-shell-broker/src/main.rs`, run a visibility-broad grep:
    `grep -nE "^\s*pub(\(crate\)|\(super\)|\(in [^)]+\))?\s+(fn|struct|enum|const|type)" <file>`.
    Before trusting any zero-hit result, test the same pattern against a file already known to
    export symbols (e.g. `dacl_guard.rs`) to confirm the pattern itself works -- a 200+ KB file
    (specifically `supervisor.rs`, 241 KB) reporting zero symbols is a signal to widen the pattern,
    not a conclusion that the file exports nothing (Pitfall 2). Confirm `supervisor.rs` returns a
    nonzero hit count under this broadened pattern and record the literal hit count. Build a `##
    Fork Primitive Inventory` table: symbol | file | visibility (`pub`/`pub(crate)`/`pub(super)`) |
    one-line capability description, covering every symbol the grep surfaces (cross-check against
    the interfaces block above but do not stop at that list -- record any additional symbol the
    live grep finds that the interfaces block omitted). Add a `## Engine-Agnosticism Grounding
    (from spike-findings-nono)` section citing, by exact skill-file name, the "sandbox-the-tools,
    not sandbox-the-TUI" finding, the "daemon-as-launcher is the sound primary model" (Spike 003,
    VALIDATED) finding, and the "primitive vs. entry point" distinction from
    `engine-agnostic-confinement.md` -- record these as neutral grounding facts for Plan 116-05's
    D-09 scoring to cite; do not score or rank them here. Add a `## WRITE_RESTRICTED Worked Example`
    section: run `grep -n "BrokerLaunchNoPty\|WriteRestricted" crates/nono-cli/src/
    execution_runtime.rs` and record the hit lines, then confirm the `claude_code_hook.rs:1072`
    citation is still accurate by reading the surrounding lines at that location in the current
    file (line numbers drift -- record the actual current line number if it has moved, noting the
    original citation for traceability). State the underlying OS fact plainly: .NET/PowerShell CLR
    cannot start a process under `WRITE_RESTRICTED`, which is why the fork's token-arm split exists.
  </action>
  <acceptance_criteria>
    - The document header contains a literal `**Date measured:**` field followed by an explicit
      date, and the prominent re-verification statement described above appears directly beneath
      it (not buried elsewhere in the document).
    - The `## Fork Primitive Inventory` table has a nonzero row count for `supervisor.rs` (grep for
      rows whose file cell is `supervisor.rs` returns >= 1).
    - The visibility-broad grep pattern and its literal hit count are recorded for every one of the
      9 files listed in read_first (7 `exec_strategy_windows/` files + `claude_code_hook.rs` +
      `hooks.rs` + `execution_runtime.rs` + the shell-broker's `main.rs` -- 10 files total).
    - The Engine-Agnosticism Grounding section cites all three named findings by the exact skill
      reference-file name they came from.
    - The WRITE_RESTRICTED Worked Example section contains the literal strings `BrokerLaunchNoPty`,
      `WriteRestricted`, `execution_runtime.rs`, and `claude_code_hook.rs`, with the current (not
      merely the originally-cited) line number for the `claude_code_hook.rs` reference.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Date measured" .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-FEASIBILITY-MATRIX.md && grep -c "supervisor.rs" .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-FEASIBILITY-MATRIX.md && grep -c "BrokerLaunchNoPty" .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-FEASIBILITY-MATRIX.md && grep -c "spike-findings-nono\|windows-confinement-model\|engine-agnostic-confinement" .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-FEASIBILITY-MATRIX.md</automated>
  </verify>
  <done>Document header states Date measured plus the prominent re-verification statement; Fork Primitive Inventory table complete with a broadened pub-visibility grep (pub(super) captured for supervisor.rs) run and recorded for all 10 files; Engine-Agnosticism Grounding section cites all three spike-findings-nono findings by file name; WRITE_RESTRICTED Worked Example cites both execution_runtime.rs and the current claude_code_hook.rs line.</done>
</task>

<task type="auto">
  <name>Task 2: Upstream capability enumeration + neutral D-07/D-08 feasibility rating</name>
  <files>.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-FEASIBILITY-MATRIX.md</files>
  <read_first>
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-CONTEXT.md (D-07, D-08, and D-10 -- re-read D-10 immediately before writing any rating cell; this is the highest D-10-violation-risk task in the phase)
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-RESEARCH.md §2f-§2h, §4 (upstream tool-sandbox inventory, the platform/ cfg-gate no-op-stub finding)
    .planning/architecture/adr-65-minifilter-go-no-go.md (§6 Decision -- Go/No-Go verdict, in full; cite its literal verdict text, not a paraphrase)
    116-FEASIBILITY-MATRIX.md (this plan's own Task 1 output -- the header's `Date measured` field and the Fork Primitive Inventory table every rating cell must cite from)
  </read_first>
  <action>
    Run `git show v0.71.0:crates/nono-cli/src/tool-sandbox/platform/linux.rs` and `git show
    v0.71.0:crates/nono-cli/src/tool-sandbox/platform/macos.rs`; enumerate driver capabilities via
    a `grep -nE "^\s*(pub\s+)?(fn|struct|enum)\s"` pass on each plus a manual read for the named
    capability classes: per-command filesystem grants, exec shims, `url_shim`, `token_broker`,
    `dynamic_providers`, env scrubbing, per-command open-port mediation, sealed-shim runtime dir --
    plus any additional capability class the grep/read surfaces beyond this named list (discover,
    don't just confirm the eight). For each capability, add one row to a `## D-07 Feasibility
    Matrix` table: capability | upstream mechanism (file:symbol at v0.71.0) | rating
    (implementable-on-fork-primitives / implementable-but-new-work / structurally-blocked) |
    citation. Every citation MUST be either a literal fork symbol name copied verbatim from Task
    1's Fork Primitive Inventory table, or the literal string "ADR-65" followed by a one-clause
    quote of its §6 verdict text. Phrase every rating as a capability fact, not an argument for a
    pole -- e.g. "requires kernel-level file-create interception; the fork has no minifilter
    (ADR-65: 'No-go/Conditional-go'); rated structurally-blocked" is acceptable; "this makes Pole A
    a bad choice" or "this favors the fork-native approach" is NOT -- do not write sentences that
    compare the two poles or recommend one. Any minifilter-dependent capability is rated
    structurally-blocked citing ADR-65's standing verdict -- never "expensive", never "future
    work", and never phrased as "available if ADR-65 were reversed" (these three phrasings are
    explicitly forbidden by D-08). Insert the WRITE_RESTRICTED capability as its own row: state the
    OS fact (.NET/PowerShell CLR cannot start under WRITE_RESTRICTED) as upstream-independent
    ground truth, cite the fork's own `execution_runtime.rs` BrokerLaunchNoPty/WriteRestricted
    split from Task 1 as the fork's existing routing-around, and rate it as the matrix's worked
    example of "structurally-blocked" (an OS behavior) versus "merely expensive" (a build-cost
    judgment) -- state this distinction explicitly since it is the matrix's canonical
    disambiguating example per D-07. All of these ratings share the same `Date measured` header
    Task 1 wrote -- do not add a second, conflicting date anywhere in this task's output. Add a
    closing `## Neutrality Self-Check` note stating the total row count and confirming (by
    re-reading the table) that zero rows contain comparative or recommending language.
  </action>
  <acceptance_criteria>
    - The `## D-07 Feasibility Matrix` table has at least 8 capability rows (per D-07's named list,
      plus any additional discovered capability).
    - Every row's citation cell contains either a symbol name that also appears verbatim in Task
      1's Fork Primitive Inventory table, or the literal string "ADR-65".
    - `grep -icE "expensive|if ADR-65 (were|is) reversed|future work"` against the D-07 table
      section returns 0.
    - `grep -icE "favors (Pole|pole)|argues for|makes Pole [AB]|Pole [AB] (wins|loses|is better)"`
      against the whole file returns 0.
    - A row exists whose capability cell contains the literal string `WRITE_RESTRICTED`, whose
      citation cell contains `execution_runtime.rs`, and whose rating explicitly distinguishes
      "structurally-blocked" from "merely expensive".
    - No second `Date measured` field is introduced by this task (grep count of `Date measured`
      across the whole document remains exactly 1, from Task 1's header).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "structurally-blocked" .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-FEASIBILITY-MATRIX.md && grep -ic "expensive\|if ADR-65 were reversed\|if ADR-65 is reversed" .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-FEASIBILITY-MATRIX.md && grep -c "Date measured" .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-FEASIBILITY-MATRIX.md</automated>
  </verify>
  <done>D-07 Feasibility Matrix table has >=8 capability rows, each rated and cited to a Task-1 fork symbol or ADR-65's literal verdict; minifilter-dependent rows read structurally-blocked with zero forbidden D-08 phrasing; the WRITE_RESTRICTED row is present as the worked example; the Neutrality Self-Check confirms zero pole-comparative language in the file; exactly one Date measured field exists in the document.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Feasibility rating -> Phase 120 sizing | An unfalsifiable or file-presence-only rating here silently corrupts Plan 116-06's D-15 sizing bands two plans downstream, without the corruption being visible at that point. |
| Neutral-fact framing -> D-10 verdict independence | A matrix that leans toward a pole while presenting itself as neutral evidence corrupts Plan 116-05's D-09 scoring and Plan 116-06's verdict without the bias being visible in either of those documents. |
| Undated evidence -> unfalsifiable confidence rating | A rating with no recorded date cannot be told apart from a stale one; D-16's evidence-discipline requirement (command + hit count + date) is unmet without it, silently weakening every downstream citation of this document. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-05 | Repudiation | Feasibility matrix confidence ratings | mitigate | Every cell cites a re-greppable fork symbol or the literal string "ADR-65"; the pub-visibility grep pattern is tested against a known-good file before a zero-hit result is trusted; a document-level `Date measured` header anchors every cited command to a checkable date (Task 1, Task 2). |
| T-116-07 | Tampering | D-10 neutral-framing requirement | mitigate | Task 2's action forbids comparative/recommending language explicitly; a Neutrality Self-Check and two negative grep scans (forbidden D-08 phrasing, forbidden pole-comparative phrasing) are acceptance criteria, not just prose reminders. |
| T-116-08 | Information Disclosure | N/A | accept | Internal planning artifact; no secrets or credentials are read or written by this task. |
</threat_model>

<verification>
Re-run the visibility-broad grep against `exec_strategy_windows/supervisor.rs` and confirm the
recorded nonzero hit count reproduces. Re-run the two negative grep scans (forbidden D-08 phrasing,
forbidden pole-comparative phrasing) and confirm both still return 0. Confirm the document's
`Date measured` header is present exactly once.
</verification>

<success_criteria>
`116-FEASIBILITY-MATRIX.md` exists with a `Date measured` header satisfying D-16's date-discipline
requirement document-wide, a Fork Primitive Inventory table that correctly captures `pub(super)`-only
surfaces (not just `pub`/`pub(crate)`), a D-07 capability rating table of at least 8 rows each
citing a fork symbol or ADR-65's literal verdict, zero forbidden D-08 phrasing on minifilter-dependent
rows, and zero pole-comparative language anywhere in the document.
</success_criteria>

<output>
Create `.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-02-SUMMARY.md` when done
</output>
