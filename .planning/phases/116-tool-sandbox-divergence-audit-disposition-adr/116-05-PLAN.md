---
phase: 116-tool-sandbox-divergence-audit-disposition-adr
plan: 05
type: execute
wave: 4
depends_on: ["116-01", "116-02", "116-04"]
files_modified:
  - proj/ADR-116-tool-sandbox-disposition.md
autonomous: true
requirements: [TSBX-02]

must_haves:
  truths:
    - "Both poles are described with equal structural depth in the Context and Pole-framing sections, with the 74%-Unix-drivers/no-platform-windows.rs fact stated as neutral framing applying to both poles' comparison, not as an argument for either."
    - "Every one of the D-09 criteria set's 7 criteria has both a Pole A cell and a Pole B cell populated in the scoring table -- a criterion scored for one pole only is a defect."
    - "The two named intermediate shapes (adopt Unix-only; adopt the policy model only) are recorded as considered-and-rejected, one paragraph each, not scored as full options."
  artifacts:
    - path: "proj/ADR-116-tool-sandbox-disposition.md"
      provides: "ADR header (Status: Proposed) + Context citing the ledger's finalized stats + two-pole framing (D-05) + D-06 rejected intermediates + D-09 symmetric scoring table"
      contains: "## D-09"
  key_links:
    - from: "proj/ADR-116-tool-sandbox-disposition.md"
      to: ".planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md"
      via: "Context section citing the ledger's finalized Headline stats"
      pattern: "116-DIVERGENCE-LEDGER.md"
    - from: "proj/ADR-116-tool-sandbox-disposition.md"
      to: ".planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-FEASIBILITY-MATRIX.md"
      via: "D-09 scoring table cells citing feasibility-matrix rows and fork symbols"
      pattern: "116-FEASIBILITY-MATRIX.md"
---

<objective>
Write the first half of `proj/ADR-116-tool-sandbox-disposition.md`: the header (`Status: Proposed`
-- Plan 116-06 flips it to `Accepted` per D-11), the Context section citing the ledger's finalized
stats, the two-pole framing (D-05), the two considered-and-rejected intermediate shapes (D-06), and
the D-09 symmetric scoring table. This plan does NOT write a verdict, a Decision section, triggers,
or Phase 120 sizing -- those are Plan 116-06's job. Document order (matrix and scoring before
verdict) is itself the checkable evidence of non-bias 116-VALIDATION.md's manual-verification row
names; this plan must not compromise that order by previewing a conclusion.

Purpose: D-10 is at its highest risk of silent failure in exactly this plan -- a scoring table can
be structurally symmetric (7 criteria x 2 poles) while still smuggling a lean into cell prose. Every
task below is built to make that failure grep-detectable, not just discouraged in prose.
Output: `proj/ADR-116-tool-sandbox-disposition.md` through its D-09 scoring table. `depends_on:
["116-01", "116-02", "116-04"]`, `wave: 4`: this plan's Task 1 Context section cites
`116-DIVERGENCE-LEDGER.md`'s *finalized* `## Headline` section -- which is written only by Plan
116-04 Task 2 (the ledger's D-01 hypothesis-vs-measurement reconciliation is not final until then).
Citing the ledger before it closes risks the ADR shipping pre-reconciliation figures while the
ledger next to it later records a corrected delta -- the exact defect D-17's "reconciled in place,
superseded figure retained" discipline exists to prevent. Correct, closed-ledger figures in the ADR
are worth more here than wave-2 parallelism, since every plan in this phase is document authoring
with no execution-time cost pressure comparable to a code phase. This plan therefore runs after the
entire ledger chain closes (116-01 -> 116-03 -> 116-04), not alongside it. Plan 116-06 (`wave: 5`,
`depends_on: ["116-05", "116-04"]`) continues in the same ADR file sequentially.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-CONTEXT.md
@.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-RESEARCH.md
@.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md
@.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-FEASIBILITY-MATRIX.md
@proj/ADR-113-spiffe-disposition.md
@proj/ADR-86-library-boundary-convergence.md
@proj/DESIGN-engine-abstraction.md

<interfaces>
<!-- Pole descriptions per CONTEXT.md D-05 -- neither may be softened or sharpened relative to the
     other; both get equal structural depth (what it requires + one cost sentence + one capability
     sentence), no comparison, no recommendation. -->
  Pole A = adopt + build platform/windows.rs from scratch, on the fork's existing AppContainer /
           Low-IL / WFP / broker primitives (crates/nono-cli/src/exec_strategy_windows/,
           crates/nono-shell-broker/). Model: upstream's tool-sandbox/ subsystem (12 files, 18,433
           LOC at v0.71.0 per the ledger).
  Pole B = formalize fork-native: PR #4's PreToolUse hook -> `nono run` + Low-IL primary-token
           broker becomes a permanent, named scope boundary in the ADR-111/ADR-113 shape.
           Design doc: .planning/quick/260528-sch-spec-the-sandbox-the-tools-windows-tool-/260528-sch-SPEC.md
           Shipped hook: crates/nono-cli/data/hooks/nono-tool-hook.ps1

<!-- D-09's 7 criteria, in this exact order, no weighting column: -->
  1. per-command granularity
  2. engine-agnosticism (fork's named weak spot -- entry point is Claude Code's PreToolUse
     contract; upstream's subsystem is engine-neutral)
  3. enforcement depth (fork's named strongest claim -- kernel-enforced Windows; the Unix drivers
     make no Windows kernel-enforcement claim at all, since platform/windows.rs does not exist)
  4. fail-direction under layer failure
  5. platform coverage (Windows / Linux / macOS)
  6. ADR-86 library-vs-CLI boundary impact
  7. ongoing divergence + maintenance cost

<!-- D-06's two named intermediate shapes, considered-and-rejected, one paragraph each -- not
     scored in the D-09 table: -->
  (i)  Adopt Unix-only -- absorb for Linux/macOS where upstream's drivers land as-is, Windows
       stays fork-native. Rejection reason: leaves two tool-confinement architectures permanently
       split by OS.
  (ii) Adopt the policy model only -- command_policy.rs's schema + the command_policies profile
       surface the fork documents (crates/nono-cli/data/profile-authoring-guide.md) but has never
       wired into any .rs file (docs-only per Phase 108 D-06 -- RE-VERIFY here, do not inherit).
       Rejection reason: a docs-only surface enforced through the fork's existing Windows path is
       a narrower absorb than either full pole, not a genuine third option.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ADR header (Status: Proposed) + Context + two-pole framing (D-05) + D-06 rejected intermediates</name>
  <files>proj/ADR-116-tool-sandbox-disposition.md</files>
  <read_first>
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-CONTEXT.md (D-05, D-06, D-09, D-10 in full -- re-read D-10 immediately before writing anything)
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md (read the finalized `## Headline` section specifically -- Plan 116-04's output, which by this plan's `wave: 4` position has already closed -- for the actual measured numbers to cite; do not cite 116-CONTEXT.md's un-reconciled hypothesis table)
    proj/ADR-113-spiffe-disposition.md (header block format; its OD-1 section as the precedent shape for D-06's named-and-rejected paragraphs)
    crates/nono-cli/data/profile-authoring-guide.md (the fork's only command_policies reference -- re-verify the docs-only, zero-.rs-wiring claim live, per D-16, rather than inheriting Phase 108's finding)
    .planning/quick/260528-sch-spec-the-sandbox-the-tools-windows-tool-/260528-sch-SPEC.md (PR#4 design, for Pole B's description)
  </read_first>
  <action>
    Create `proj/ADR-116-tool-sandbox-disposition.md` with the header block: `# ADR-116:
    Tool-Sandbox Divergence -- Adopt vs. Formalize Fork-Native`, `**Status:** Proposed`, `**Phase:**
    116 -- Tool-Sandbox Divergence Audit + Disposition ADR`, `**Date:** <today's date>`,
    `**Authors:** Phase 116 execution`. Add an HTML comment immediately below the header: `<!-- Status
    flips to Accepted only in Plan 116-06's Decision task, per D-11. This document is not final
    until then. -->`. Write `## Context`: read `116-DIVERGENCE-LEDGER.md`'s finalized `## Headline`
    section (this is Plan 116-04's output, which has already closed by the time this plan runs) and
    cite its actual recorded figures verbatim (total dispositioned commit count, the
    12-file/18,433-LOC breakdown, the 74% Unix-driver figure) -- these are the ledger's own
    reconciled numbers, cite them from there even if they match CONTEXT.md's original hypothesis
    exactly (in which case note "confirmed, no delta" as the ledger itself states). State the single
    most decision-relevant framing fact neutrally: 74% of the subsystem (`platform/linux.rs` +
    `platform/macos.rs`) is Unix platform drivers, and upstream ships no `platform/windows.rs` at
    all -- so "adopt" on the platform this milestone cares about means "take upstream's model and
    write its largest component from scratch," not "take upstream's code as-is." Immediately follow
    with the literal sentence: "This framing applies equally to evaluating both poles below; it is
    not evidence for either." Write `## Pole A: Adopt + Build platform/windows.rs` and `## Pole B:
    Formalize Fork-Native` as two one-paragraph sections of equal structural depth (what each
    requires; the fork primitives or upstream files it builds on, cited by name) -- Pole A cites
    upstream's 12-file tool-sandbox/ inventory and the `platform/` cfg-gate no-op-stub finding (from
    `116-RESEARCH.md` §4); Pole B cites the SPEC doc and `crates/nono-cli/data/hooks/
    nono-tool-hook.ps1` by name. Neither paragraph may contain a comparative or recommending
    sentence. Write `## D-06: Considered and Rejected Intermediate Shapes` with `### (i) Adopt
    Unix-Only` (state the rejection reason: leaves two tool-confinement architectures permanently
    split by OS) and `### (ii) Adopt the Policy Model Only` -- before writing this subsection, run
    `grep -rn "command_policies" crates/nono-cli/src/ crates/nono-cli/data/` and confirm zero hits
    inside `crates/nono-cli/src/` and at least one hit inside
    `crates/nono-cli/data/profile-authoring-guide.md`; record this grep command and its literal
    output inline, then state the rejection reason (a docs-only surface enforced through the fork's
    existing Windows path is a narrower absorb than either full pole, not a genuine third option),
    naming ADR-113's OD-1 as the precedent shape for recording a permanently-considered-and-rejected
    option. Do NOT write a `## Decision` section, a bolded verdict sentence, or any D-09 scoring
    content in this task -- that is Task 2 and Plan 116-06's job respectively.
  </action>
  <acceptance_criteria>
    - Header contains the literal string "Status: Proposed" (not "Accepted").
    - `## Context` cites at least 3 numeric figures pulled from `116-DIVERGENCE-LEDGER.md`'s actual
      finalized Headline section, and contains the literal sentence "This framing applies equally
      to evaluating both poles below; it is not evidence for either."
    - Exactly 2 sections match `^## Pole` (grep count == 2), and neither section's text matches
      `is better|preferable|makes Pole [AB]|favors (Pole|pole)` (grep -icE returns 0 across both).
    - `## D-06` section contains exactly 2 named subsections (`### (i)` and `### (ii)`), and the
      `grep -rn "command_policies" ...` command plus its literal live output are recorded inline in
      subsection (ii), not asserted without the command shown.
    - The document contains zero `## Decision` sections (grep count == 0) and no standalone bolded
      verdict sentence (`**Adopt**` or `**Formalize fork-native**` as a sentence-opening bold
      phrase does not appear).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Status: Proposed" proj/ADR-116-tool-sandbox-disposition.md && grep -c "^## Pole" proj/ADR-116-tool-sandbox-disposition.md && grep -c "^## D-06" proj/ADR-116-tool-sandbox-disposition.md && grep -c "^## Decision" proj/ADR-116-tool-sandbox-disposition.md</automated>
  </verify>
  <done>ADR header written with Status: Proposed and a comment noting Plan 116-06 flips it; Context cites the ledger's finalized figures with the neutral 74%-framing sentence present verbatim; exactly 2 non-comparative Pole sections; D-06's two rejected intermediates recorded with the command_policies grep re-verified live; zero Decision section or verdict sentence present.</done>
</task>

<task type="auto">
  <name>Task 2: D-09 symmetric scoring table (7 criteria x 2 poles, no weighting)</name>
  <files>proj/ADR-116-tool-sandbox-disposition.md</files>
  <read_first>
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-CONTEXT.md (D-09 in full)
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-FEASIBILITY-MATRIX.md (full -- especially the Engine-Agnosticism Grounding section and the WRITE_RESTRICTED Worked Example)
    .claude/skills/spike-findings-nono/SKILL.md and references/engine-agnostic-confinement.md (the "primitive vs. entry point" distinction, in full)
    proj/ADR-86-library-boundary-convergence.md (for the ADR-86 boundary-impact criterion)
    crates/nono-cli/src/claude_code_hook.rs, crates/nono-cli/data/hooks/nono-tool-hook.ps1 (for the engine-agnosticism citation)
    proj/ADR-116-tool-sandbox-disposition.md (this plan's own Task 1 output, to append to)
  </read_first>
  <action>
    Append `## D-09: Symmetric Scoring` with a table of exactly 7 rows, columns `Criterion | Pole A
    | Pole B` -- no weighting, score, or total column. Populate in this exact order: (1)
    per-command granularity -- cite what each side's mechanism actually grants at (upstream:
    per-command filesystem grants/exec shims per the feasibility matrix; fork: `nono run --profile`
    per-invocation grants via the hook, citing `claude_code_hook.rs`). (2) engine-agnosticism --
    apply the "primitive vs. entry point" distinction verbatim: Pole B's cell states plainly the
    fork's underlying `nono run` primitive is engine-neutral (cite `nono run --profile ... --
    <engine.exe>`, Spike 003 VALIDATED) but its ENTRY POINT is Claude Code's PreToolUse contract
    (cite `claude_code_hook.rs` and `nono-tool-hook.ps1` by name) -- state this as the fork's named
    weak spot, not softened. Pole A's cell states upstream's subsystem is engine-neutral by design,
    citing the upstream entrypoint shape from the feasibility matrix / RESEARCH §4. (3) enforcement
    depth -- Pole B's cell states the fork's kernel-enforced claim (AppContainer + WFP + Job
    Object, citing exec_strategy_windows/ symbols from the Fork Primitive Inventory) as its
    strongest claim, stated without softening. Pole A's cell states upstream's Unix drivers make no
    Windows kernel-enforcement claim at all today (no `platform/windows.rs` exists), so a
    hypothetical Pole-A driver's Windows enforcement depth is unbuilt/unproven -- phrase as an
    honest gap, not disqualifying language. (4) fail-direction under layer failure -- Pole B cites
    whatever fail-closed behavior is visible in `execution_runtime.rs`/`launch.rs` per the
    feasibility matrix; Pole A states "not yet built -- no driver exists to test" as an honest gap.
    (5) platform coverage -- Pole A: Linux+macOS proven since v0.65.0 upstream, Windows net-new
    (cite the ledger's confirmed absence of `platform/windows.rs`). Pole B: Windows proven via PR#4,
    Linux/macOS untested/no host module (cite `116-RESEARCH.md` §3c's confirmed absence of
    tool-sandbox/, command_policy.rs, lineage_cgroup.rs in the fork). (6) ADR-86 library-vs-CLI
    boundary impact -- cite the Architectural Responsibility Map fact that both poles would live
    entirely in `nono-cli`, library untouched either way; state plainly this is a wash between the
    poles on this criterion, citing `proj/ADR-86-library-boundary-convergence.md` by name. (7)
    ongoing divergence + maintenance cost -- Pole A cites the ledger's own commit velocity (the
    dispositioned commit count from the ledger's Headline) as the rate a Pole-A-absorbed subsystem
    would need continued syncing against; Pole B cites that formalizing removes tool-sandbox/ from
    future sync scope entirely, but leaves the fork's own Windows-specific code as its own
    maintenance surface. Append `## Neutrality Self-Check`: state the literal count "7 criteria x 2
    poles = 14 populated cells" and confirm it by re-reading the table; re-scan the whole document
    (not just this section) for comparative/recommending language and state the scan's result
    explicitly.
  </action>
  <acceptance_criteria>
    - The `## D-09` table has exactly 7 rows matching the named criteria list verbatim, in the
      given order.
    - Every row has a non-empty Pole A cell and a non-empty Pole B cell; the Neutrality Self-Check
      states the literal count "14 populated cells" (7 x 2).
    - No column header in the table matches `score|weight|total` (grep -i returns 0).
    - The engine-agnosticism row contains the literal phrase "primitive vs. entry point" (or
      "primitive vs entry point") and cites both `claude_code_hook.rs` and `nono-tool-hook.ps1`.
    - The enforcement-depth row states the fork's kernel-enforced claim as its strongest claim
      without softening language, and states Pole A's Windows enforcement as unbuilt/unproven
      without disqualifying language (no "impossible", "cannot", or "fails" applied to Pole A here
      -- "unbuilt"/"unproven"/"not yet built" only).
    - `grep -icE "is better|preferable|favors (Pole|pole)|makes Pole [AB]|Pole [AB] (wins|loses)"`
      against the whole document returns 0.
    - The document still contains zero `## Decision` sections after this task (grep count == 0),
      confirming scoring precedes verdict in document order.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^## D-09" proj/ADR-116-tool-sandbox-disposition.md && grep -c "primitive vs" proj/ADR-116-tool-sandbox-disposition.md && grep -c "^## Decision" proj/ADR-116-tool-sandbox-disposition.md</automated>
  </verify>
  <done>D-09 scoring table has exactly 7 criteria rows with 14 populated cells (7x2), no weighting column, the engine-agnosticism and enforcement-depth rows stated per CONTEXT.md's named asymmetries without softening, zero comparative/recommending language anywhere in the document, and zero Decision section -- document order keeps the matrix and scoring strictly before any verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| D-10 bias -> scoring table cell prose | A structurally symmetric table (7 criteria x 2 poles) can still smuggle a lean into cell wording; this is the exact failure mode this plan's negative grep scans exist to catch, since a human skim would likely miss subtle phrasing bias. |
| Document order -> non-bias evidence | 116-VALIDATION.md's own manual-verification row treats "matrix and scoring precede the verdict section" as the checkable proxy for absence-of-bias; if this plan previewed a verdict, that proxy would be corrupted before Plan 116-06 even runs. |
| Ledger-not-yet-closed -> stale figures in the ADR | Citing `116-DIVERGENCE-LEDGER.md`'s Headline before Plan 116-04 has finalized it would risk the ADR shipping pre-reconciliation numbers while the ledger later records a corrected delta next to it -- resolved structurally here by `depends_on: ["116-01", "116-02", "116-04"]` rather than left to task-ordering discipline alone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-07 | Tampering | D-09 scoring table cell prose | mitigate | Negative grep scans for comparative/recommending language across the whole document; the two named asymmetries (engine-agnosticism weak spot, enforcement-depth strong spot) are stated in the direction CONTEXT.md names, not softened (Task 2). |
| T-116-07 | Repudiation | Document order (matrix/scoring before verdict) | mitigate | Task 1 explicitly forbids a `## Decision` section or bolded verdict sentence; both tasks' acceptance criteria re-confirm zero Decision section present after each task. |
| T-116-05 | Repudiation | Context section's ledger-derived figures | mitigate | Context cites the ledger's own finalized Headline numbers (Plan 116-04's output, guaranteed closed by this plan's `depends_on`), not CONTEXT.md's un-reconciled hypothesis table, so a stale number cannot silently propagate into the ADR. |
</threat_model>

<verification>
Confirm `proj/ADR-116-tool-sandbox-disposition.md` has `Status: Proposed`, exactly 2 Pole sections,
exactly 1 D-06 section with 2 rejected-shape subsections, exactly 1 D-09 section with 7 scoring
rows and 14 populated cells, and zero Decision sections. Re-run the two negative grep scans
(comparative/recommending language; forbidden disqualifying language on Pole A's enforcement-depth
cell) and confirm both return 0.
</verification>

<success_criteria>
`proj/ADR-116-tool-sandbox-disposition.md` exists through its D-09 scoring table: header
(`Status: Proposed`), Context citing the ledger's finalized figures with the neutral 74%-framing
sentence, two equally-weighted Pole sections, D-06's two rejected intermediates (with the
command_policies claim re-verified live, not inherited), and a symmetric 7-criteria x 2-pole D-09
scoring table with zero weighting and zero comparative language -- ready for Plan 116-06 to append
the verdict.
</success_criteria>

<output>
Create `.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-05-SUMMARY.md` when done
</output>
