---
phase: 116-tool-sandbox-divergence-audit-disposition-adr
plan: 06
type: execute
wave: 5
depends_on: ["116-05", "116-04"]
files_modified:
  - proj/ADR-116-tool-sandbox-disposition.md
autonomous: true
requirements: [TSBX-02]

must_haves:
  truths:
    - "The ADR states exactly one unambiguous verdict with Status: Accepted, derived from reading the full D-09 scoring table and the feasibility matrix's row tally -- never pre-selected before that evidence is read."
    - "Reversal is governed by >=3 named falsifiable trigger conditions a future reader can check, plus one named re-test checkpoint (the next UPST sync) -- not a reasoning paragraph nobody is obliged to revisit."
    - "Phase 120's scope is sized row-by-row from the feasibility matrix's non-blocked rows, each tagged S/M/L and never sized from commit counts, proposed inside the ADR and never applied to ROADMAP.md."
  artifacts:
    - path: "proj/ADR-116-tool-sandbox-disposition.md"
      provides: "Decision/verdict (Status: Accepted) + D-07/D-08 feasibility citation summary + D-12 triggers/re-test point + D-14/D-15 Phase 120 sizing proposal + Consequences + References"
      contains: "Status: Accepted"
  key_links:
    - from: "proj/ADR-116-tool-sandbox-disposition.md"
      to: ".planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-FEASIBILITY-MATRIX.md"
      via: "row-by-row Phase 120 Scope sizing, each deliverable tagged S/M/L"
      pattern: "\\(S\\)|\\(M\\)|\\(L\\)"
    - from: "proj/ADR-116-tool-sandbox-disposition.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 120 Scope section proposes an amendment, explicitly never applied -- operator-gated"
      pattern: "operator"
---

<objective>
Finish `proj/ADR-116-tool-sandbox-disposition.md`: append the Decision (verdict, `Status:
Accepted`, D-11), a self-contained D-07/D-08 feasibility citation summary, D-12's reversal triggers
and re-test point, D-14/D-15's row-by-row Phase 120 sizing proposal, Consequences, and References.

Purpose: This is the last plan in the phase and the single highest-stakes point for D-10 --
Plan 116-05 deliberately withheld the verdict so it would fall out of the D-09 scoring table and
the feasibility matrix's tally, not be chosen in advance. This plan's Task 1 must read that
evidence before writing anything, not after.
Output: `proj/ADR-116-tool-sandbox-disposition.md` complete end-to-end, `Status: Accepted`, with a
proposed (never-applied) Phase 120 Scope section. `depends_on: ["116-05", "116-04"]`, `wave: 5`:
the edge to `116-05` (`wave: 4`) is the direct same-file predecessor and would place this plan at
`wave: 5` on its own; the edge to `116-04` is stated explicitly rather than left implicit, because
Task 2's `## Consequences` cites `116-DIVERGENCE-LEDGER.md`'s "closed except for named exceptions"
closing line -- written only by Plan 116-04 Task 2. That edge already holds transitively through
116-05's own dependency on 116-04, but a real dependency should be legible without transitive
reasoning, so it is declared directly here too. This keeps the ADR file's two writing plans
strictly sequential -- they never touch the file concurrently -- the same mechanism used for the
ledger's 116-01 -> 116-03 -> 116-04 chain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-CONTEXT.md
@.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-RESEARCH.md
@proj/ADR-116-tool-sandbox-disposition.md
@.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-FEASIBILITY-MATRIX.md
@.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md
@proj/ADR-111-resource-limits-boundary.md
@proj/ADR-113-spiffe-disposition.md
@.planning/architecture/adr-65-minifilter-go-no-go.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- D-12's floor of 4 named falsifiable triggers (add any further trigger the D-09 table itself
     surfaces as a live uncertainty; do not drop any of these four without stating why): -->
  1. upstream ships platform/windows.rs
  2. a second agent engine needs confinement
  3. ADR-65's minifilter verdict is revisited
  4. the Unix drivers stop growing
  Named re-test checkpoint: the next UPST sync (UPST13/FUT-08).

<!-- D-15's banding rule (state verbatim in the Phase 120 Scope section): -->
  S = wraps/extends one existing fork primitive with no new OS surface
  M = a new fork primitive is needed but no new kernel/OS mechanism
  L = a new OS-mechanism integration is required
  Commit counts are explicitly NOT the sizing basis.

<!-- FUT-09 (REQUIREMENTS.md): conditional -- only exists as future work if TSBX-02 returns adopt
     AND TSBX-03 cannot fully land inside v3.7. Sized from the verdict, never pre-committed. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decision (verdict, derived from evidence) + Status: Accepted + D-07/D-08 feasibility citation summary</name>
  <files>proj/ADR-116-tool-sandbox-disposition.md</files>
  <read_first>
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-CONTEXT.md (D-10, D-11 in full -- re-read immediately before this task; this is the single highest D-10-violation-risk task in the entire phase)
    proj/ADR-116-tool-sandbox-disposition.md (this plan's own file -- read Plan 116-05's full output: Context, both Pole sections, D-06, and every cell of the `## D-09` scoring table plus its Neutrality Self-Check)
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-FEASIBILITY-MATRIX.md (full -- especially the `## D-07 Feasibility Matrix` table, to compute its row tally)
    .planning/architecture/adr-65-minifilter-go-no-go.md (§6 Decision -- Go/No-Go verdict, for the ADR-65 citation)
  </read_first>
  <action>
    Do not write a verdict sentence until you have read every cell of the `## D-09` scoring table
    (Plan 116-05) and computed `116-FEASIBILITY-MATRIX.md`'s `## D-07 Feasibility Matrix` row tally
    (count of implementable-on-fork-primitives rows, implementable-but-new-work rows, and
    structurally-blocked rows, out of the table's total row count). The verdict is whatever this
    evidence supports; it is not chosen in advance. Once both pieces of evidence are read, append
    `## Decision` with a first line stating, in bold, exactly one of "**Adopt** (Pole A)." or
    "**Formalize fork-native** (Pole B)." Follow with a rationale paragraph that cites at least 3 of
    the D-09 table's 7 criterion names verbatim, quoting or closely paraphrasing their actual Pole A
    and Pole B cell content (not a fresh argument disconnected from the table), plus at least one
    numeric tally from the feasibility matrix (e.g. "N of M capabilities rated
    implementable-on-fork-primitives; K rated structurally-blocked"). Update the document header:
    change `**Status:** Proposed` to `**Status:** Accepted`. Replace the HTML comment Plan 116-05
    left below the header with: `<!-- Status: Accepted, set by Plan 116-06 per D-11, derived from
    the D-09 scoring table and the D-07 feasibility tally above. -->`. Append a `## D-07/D-08:
    Feasibility Matrix Summary` section: state the feasibility matrix's top-line tally explicitly
    (the three counts computed above), cite `116-FEASIBILITY-MATRIX.md` by relative path as the
    full detail (do not duplicate its rows here), and restate inline -- for this document's own
    self-containment -- the WRITE_RESTRICTED worked example (citing both `execution_runtime.rs` and
    `claude_code_hook.rs`) and the fact that every minifilter-dependent row is rated
    structurally-blocked citing the literal string "ADR-65", confirming the same forbidden-phrasing
    discipline (never "expensive", never "if ADR-65 were reversed") holds in this restatement, not
    only in the appendix.
  </action>
  <acceptance_criteria>
    - Header contains the literal string "Status: Accepted" (the literal string "Status: Proposed"
      no longer appears anywhere in the document).
    - `## Decision` section's first line matches exactly one of the two literal bolded verdict
      strings ("**Adopt** (Pole A)." or "**Formalize fork-native** (Pole B)."), and no second,
      conflicting bolded verdict sentence appears anywhere else in the document (combined grep
      count of both verdict-string patterns across the whole file == 1).
    - The rationale paragraph cites at least 3 distinct D-09 criterion names verbatim (from the 7
      named in Plan 116-05) and at least 1 explicit numeric tally from the feasibility matrix.
    - `## D-07/D-08: Feasibility Matrix Summary` section contains the literal string "ADR-65" and
      `grep -icE "expensive|if ADR-65 (were|is) reversed"` against that section returns 0.
    - The WRITE_RESTRICTED restatement contains both `execution_runtime.rs` and `claude_code_hook.rs`.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Status: Accepted" proj/ADR-116-tool-sandbox-disposition.md && grep -c "Status: Proposed" proj/ADR-116-tool-sandbox-disposition.md && grep -c "^## Decision" proj/ADR-116-tool-sandbox-disposition.md && grep -c "^## D-07/D-08" proj/ADR-116-tool-sandbox-disposition.md</automated>
  </verify>
  <done>Header reads Status: Accepted (Proposed no longer present); exactly one bolded verdict sentence appears, derived by reading the full D-09 table and the feasibility matrix's tally, with a rationale citing >=3 criteria and a numeric tally; the D-07/D-08 summary restates the WRITE_RESTRICTED example and ADR-65 citation with zero forbidden phrasing.</done>
</task>

<task type="auto">
  <name>Task 2: D-12 reversal triggers/re-test point, D-14/D-15 Phase 120 sizing proposal, Consequences, References</name>
  <files>proj/ADR-116-tool-sandbox-disposition.md</files>
  <read_first>
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-CONTEXT.md (D-12, D-14, D-15 in full)
    proj/ADR-111-resource-limits-boundary.md, proj/ADR-113-spiffe-disposition.md (Consequences/OD-N section shape -- use whichever precedent matches this plan's own Task 1 verdict: ADR-111/113's permanent-boundary shape if Pole B, a build-out sizing shape if Pole A)
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-FEASIBILITY-MATRIX.md (the `## D-07 Feasibility Matrix` table -- the row-by-row sizing source)
    .planning/ROADMAP.md (Phase 120's current provisional entry, and Phase 117's entry for CINT-01's fail-direction wording)
    .planning/REQUIREMENTS.md (TSBX-03, FUT-09 exact wording)
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md (read the finalized `## Headline` and its "closed except for named exceptions" closing line -- Plan 116-04's output, which this plan's `depends_on` guarantees is already closed -- for the Consequences citation)
  </read_first>
  <action>
    Append `## D-12: Reversal Triggers + Re-Test Point`. Write at least 3 bulleted, individually
    checkable trigger conditions -- use the 4 named in the interfaces block above as the floor
    ("upstream ships platform/windows.rs"; "a second agent engine needs confinement"; "ADR-65's
    minifilter verdict is revisited"; "the Unix drivers stop growing") and add any further trigger
    the D-09 scoring table itself surfaced as a live uncertainty; if any of the 4 floor triggers is
    dropped, state explicitly why the D-09 evidence supersedes it. Close with exactly one named
    re-test checkpoint sentence: "Re-tested at the next UPST sync (UPST13/FUT-08)." Add the
    rationale sentence: "A trigger list nobody is obliged to look at is how this subsystem became a
    standing unexamined divergence in the first place -- naming the next UPST sync as the re-test
    checkpoint keeps this obligation live, not implicit." Append `## Phase 120 Scope (D-14/D-15)`.
    Read `116-FEASIBILITY-MATRIX.md`'s `## D-07 Feasibility Matrix` table row by row; for every row
    NOT rated structurally-blocked, write a named-deliverable bullet: deliverable name | the fork
    primitive it builds on (cited verbatim from the matrix) | a coarse band tag `(S)`, `(M)`, or
    `(L)`. State the banding rule verbatim from the interfaces block above, including the literal
    sentence "Commit counts are explicitly not the sizing basis -- one commit in the audited window
    is a 6,586-line Landlock-equivalent driver, another is a one-line fix; LOC arithmetic off either
    would be false precision." State plainly which deliverables land inside v3.7 and which become
    FUT-09 (per REQUIREMENTS.md's exact FUT-09 wording). If Task 1's verdict is Pole B (formalize),
    replace the build-out deliverable list with the smaller "close the divergence by naming the
    boundary" shape instead, mirroring ADR-111/ADR-113's Consequences section -- state explicitly
    that this is the smaller alternative shape and why the feasibility matrix's rows are not sized
    as deliverables in that case. Write the literal sentence: "This section is a proposal for
    ROADMAP.md's Phase 120 entry -- it is not applied by this phase; ratification and
    `.planning/ROADMAP.md` edits happen only after operator sign-off (D-11 gate)." Do NOT edit
    `.planning/ROADMAP.md` or `.planning/REQUIREMENTS.md` in this task. Append `## Consequences` as
    a numbered list: (1) cite `116-DIVERGENCE-LEDGER.md`'s "closed except for named exceptions"
    closing line; (2) state what's permanent vs. deferred under the verdict; (3) state what future
    syncs must re-affirm (the D-12 triggers); (4) state explicitly that the Windows security model
    (AppContainer/Low-IL/WFP/Job Object) and ADR-86's policy-free library boundary are unaffected by
    either pole (D-18); (5) state explicitly that Phase 120 planning does not begin until the
    operator ratifies this ADR (D-11). Read `.planning/ROADMAP.md`'s Phase 117 entry (CINT-01,
    fail-direction contract) and compare its wording against this ADR's own D-09 fail-direction
    criterion cell content (Plan 116-05); if the two documents characterize fail-direction behavior
    differently in any way, add a Consequences bullet flagging the disagreement explicitly by name
    (state what each document says) -- do not silently reconcile it by editing either document; if
    no disagreement is found, state that explicitly too. Append `## References`: a bulleted list
    citing the pinned SHAs (`0551eba2` / `0055bf3c`), `116-DIVERGENCE-LEDGER.md`,
    `116-FEASIBILITY-MATRIX.md`, every sibling ADR cited earlier in this document, `116-CONTEXT.md`,
    and `116-RESEARCH.md`.
  </action>
  <acceptance_criteria>
    - `## D-12` section has at least 3 bulleted trigger conditions (all 4 floor triggers present,
      or an explicit stated reason for dropping any one) plus exactly one re-test checkpoint
      sentence containing "UPST13" or "next UPST sync".
    - `## Phase 120 Scope` section's deliverable bullets each carry a literal `(S)`, `(M)`, or `(L)`
      tag; the tagged-bullet count is stated explicitly, and (if the verdict is Pole A) matches the
      count of non-structurally-blocked feasibility-matrix rows, or (if Pole B) the smaller
      closing-boundary list is used with that substitution stated explicitly.
    - The literal sentence about the ROADMAP.md Phase 120 proposal being unapplied and
      operator-gated is present, containing both "not applied" and "operator sign-off".
    - `git diff --stat -- .planning/ROADMAP.md .planning/REQUIREMENTS.md` shows zero changes after
      this task.
    - `## Consequences` states the Windows-security-model/ADR-86 non-regression explicitly, states
      the Phase 120 operator-gate explicitly, and either flags a fail-direction disagreement with
      Phase 117's CINT-01 by name (quoting or closely citing both documents' wording) or explicitly
      states none was found.
    - `## References` cites both `116-DIVERGENCE-LEDGER.md` and `116-FEASIBILITY-MATRIX.md` by
      relative path.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^## D-12" proj/ADR-116-tool-sandbox-disposition.md && grep -c "^## Phase 120 Scope" proj/ADR-116-tool-sandbox-disposition.md && grep -c "^## Consequences" proj/ADR-116-tool-sandbox-disposition.md && grep -c "^## References" proj/ADR-116-tool-sandbox-disposition.md</automated>
  </verify>
  <done>D-12 has >=3 checkable triggers plus the named UPST-sync re-test point; Phase 120 Scope is sized row-by-row from the feasibility matrix with S/M/L tags (or the smaller closing-boundary alternative if Pole B), explicitly proposed and never applied to ROADMAP.md/REQUIREMENTS.md; Consequences states non-regression and the operator gate and addresses the Phase 117 CINT-01 fail-direction cross-check; References cites the ledger and the feasibility matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Verdict derivation vs. pre-selection | This is the single highest-stakes D-10 risk point in the entire phase -- a verdict chosen before the D-09/D-07 evidence is read would invalidate the audit's entire premise, silently, since the resulting document would look identical in shape to a properly-derived one. |
| Silent trigger list -> future unexamined divergence | An unobliged, vague trigger list reproduces exactly the failure mode this phase exists to close: a standing divergence nobody is prompted to re-examine. |
| Phase 120 Scope proposal -> ROADMAP.md scope pressure | A sizing section that reads as already-decided (rather than explicitly proposed and operator-gated) risks the same silent-scope-expansion failure Phase 108's D-19 amendment was built to prevent. |
| Ledger closing-line citation -> premature read | Task 2's Consequences cites `116-DIVERGENCE-LEDGER.md`'s closing statement, which only exists once Plan 116-04 has run; resolved structurally by this plan's explicit `depends_on: ["116-05", "116-04"]` rather than left to transitive inference. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-06 | Tampering | Decision verdict selection | mitigate | The action explicitly requires reading the full D-09 table and the feasibility-matrix tally before writing the verdict sentence; the rationale must cite specific evidence, not merely assert a conclusion (Task 1). |
| T-116-06 | Repudiation | D-12 trigger list | mitigate | At least 3 named, individually checkable conditions plus one named re-test checkpoint are required acceptance criteria, not left as free prose a future reader could skip (Task 2). |
| T-116-08 | Elevation of Privilege (scope) | Phase 120 Scope proposal -> ROADMAP.md | mitigate | An explicit "not applied, operator sign-off" sentence is a required acceptance criterion, plus a `git diff --stat` check confirming ROADMAP.md/REQUIREMENTS.md are unedited (Task 2). |
| T-116-09 | Tampering | Phase-wide no-code-changes boundary (D-20) | mitigate | This plan's own tasks touch only the ADR markdown file; the phase-wide `git diff --stat` check for zero `.rs`/`.toml`/`ROADMAP.md` changes is confirmed in this plan's `<verification>` section as the phase's closing gate. |
</threat_model>

<verification>
Confirm `proj/ADR-116-tool-sandbox-disposition.md` has `Status: Accepted`, exactly one verdict
sentence, a D-12 section with >=3 triggers and the named re-test point, a Phase 120 Scope section
with every deliverable tagged S/M/L (or the smaller closing-boundary alternative), and zero changes
to `.planning/ROADMAP.md`/`.planning/REQUIREMENTS.md`. As the phase's final gate, also run
`git diff --stat -- '*.rs' '*.toml' .planning/ROADMAP.md` across the whole phase's changes and
confirm it is empty (D-20).

This plan's Task 2 Phase 120 Scope section directly answers 116-RESEARCH.md's Open Question 2
("does upstream's platform/ cfg-gate stub simplify Pole A's actual build, or is it a red
herring?") by explicitly deferring the "who builds this and where it lives" question to Phase 120's
sizing, consistent with the research's own recommendation. Treat Open Question 2 as (RESOLVED,
deferred-to-Phase-120) by this plan's Task 2 output.
</verification>

<success_criteria>
`proj/ADR-116-tool-sandbox-disposition.md` is complete end-to-end: `Status: Accepted`, exactly one
verdict derived from the D-09 scoring table and the feasibility matrix's tally, a self-contained
D-07/D-08 summary, D-12's reversal triggers and named re-test point, D-14/D-15's row-by-row Phase
120 sizing proposal (S/M/L-tagged, operator-gated, never applied to ROADMAP.md), Consequences
(including the Phase 117 CINT-01 fail-direction cross-check), and References. The phase as a whole
produces zero changes to any `.rs`, `Cargo.toml`, or `.planning/ROADMAP.md` file.
</success_criteria>

<output>
Create `.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-06-SUMMARY.md` when done
</output>
