---
phase: 116-tool-sandbox-divergence-audit-disposition-adr
plan: 04
type: execute
wave: 3
depends_on: ["116-03"]
files_modified:
  - .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md
autonomous: true
requirements: [TSBX-01]

must_haves:
  truths:
    - "Every accumulated fork-invariant carve-out surface has an explicit clean/HIT re-touch result against the full 116 window -- silence is never treated as evidence."
    - "The bucket-count reconciliation shows both summed totals (bucketed paths, touched paths) as explicit numbers and their equality, rather than asserting reconciliation without arithmetic."
    - "The finalized Headline restates the D-01 hypothesis-vs-measurement reconciliation explicitly, including the no-delta case, so a reader sampling only the Headline is not misled."
  artifacts:
    - path: ".planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md"
      provides: "Carve-out re-touch check + bucket-count reconciliation + Security-Relevant Rollup + Completeness Verification + finalized Headline"
      contains: "clean -- no re-touch in window"
  key_links:
    - from: ".planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md"
      to: "fork-invariant carve-out surfaces (exec_strategy_windows/, audit.rs, net_filter.rs, bindings/c/, ADR-named boundaries)"
      via: "per-surface git log re-touch check against the full v0.64.1..v0.71.0 window"
      pattern: "clean -- no re-touch in window|HIT \\("
---

<objective>
Close out `116-DIVERGENCE-LEDGER.md`: run the carve-out re-touch check against every accumulated
fork-invariant surface (Phase 108's original 7 plus any new named boundaries from ADR-108/111/
113/114), reconcile the bucket-count arithmetic with the totals shown rather than asserted, and
finalize the ledger's Headline -- including an explicit restatement of D-01's hypothesis-vs-
measurement reconciliation -- with a full completeness sweep.

Purpose: D-19 forbids treating silence as evidence -- every carve-out surface needs a stated
result. D-16 forbids asserting reconciliation without showing the arithmetic. This plan closes the
ledger the same way Phase 108's own closing plan did (108-05), adapted to this window and this
ledger's own accumulated content from Plans 116-01/116-03.
Output: Carve-out Re-touch Check, Bucket-Count Reconciliation, Security-Relevant Rollup,
Completeness Verification, and a finalized `## Headline` appended/prepended to
`116-DIVERGENCE-LEDGER.md`. This closes the ledger file. `depends_on: ["116-03"]` plus this plan's
own `wave: 3` (Plan 116-03 is `wave: 2`) keeps the ledger's writes strictly sequential across all
three ledger-owning plans (116-01 -> 116-03 -> 116-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-CONTEXT.md
@.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-RESEARCH.md
@.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md
@proj/ADR-108-deny-domain-posture.md
@proj/ADR-111-resource-limits-boundary.md
@proj/ADR-113-spiffe-disposition.md
@proj/ADR-114-oauth-capture-disposition.md

<interfaces>
<!-- Phase 108's original 7 carve-out surfaces (verified to exist in the fork as of Phase 108,
     2026-07-29 -- re-verify with `ls <path>` before using, since Phases 109-115 have since
     touched some of these files and paths may have moved). Full window for ALL re-touch checks
     in this plan (NOT Phase 108's fenced window): -->
  RANGE = 0551eba27ea53b0eda20f4f796f757dd168adb92..0055bf3c686de6fa665decd9831dc5686b282cb0

  CR-02 (audit bypass invariant):        crates/nono/src/audit.rs
  CR-01 (FFI clear_last_call_state):     bindings/c/src/lib.rs bindings/c/src/diagnostic.rs
                                          bindings/c/src/capability_set.rs bindings/c/src/fs_capability.rs
                                          bindings/c/src/sandbox.rs bindings/c/src/state.rs
                                          bindings/c/src/query.rs
  Proxy fork-preserve surface:           crates/nono-proxy/src/route.rs crates/nono-proxy/src/connect.rs
                                          crates/nono-proxy/src/reverse.rs crates/nono-proxy/src/server.rs
                                          crates/nono-cli/src/proxy_runtime.rs
  Endpoint-policy surface (Phase 95):    crates/nono-cli/src/network_policy.rs crates/nono-proxy/src/config.rs
                                          crates/nono-proxy/src/credential.rs crates/nono-proxy/src/route.rs
                                          crates/nono-proxy/src/server.rs
  linux.rs restored invariants:          crates/nono/src/sandbox/linux.rs
  ADR-86 Windows denial-rendering carve-out: crates/nono-cli/src/exec_strategy_windows/
  v3.2 signed-override surface:          crates/nono/src/audit.rs (PolicyOverrideApplied + EventIDs 10006-10010)

<!-- RESEARCH.md §2i's exact verified form (re-use verbatim -- trailing-slash on directory paths,
     single-quoted, never a `*substring*` glob): -->
  git log --no-merges --oneline $RANGE -- 'crates/nono-cli/src/exec_strategy_windows/'
  # -> (no output) -- record as "clean -- no re-touch in window; upstream has no Windows-specific
  #    exec-strategy directory at all" (the stronger framing RESEARCH.md §2i specifies, since the
  #    zero-hit reason here is upstream never having the surface, not merely not touching it).

<!-- The pathspec hazard this window reproduces live (RESEARCH.md §2d): a `'*tool-sandbox*'` glob
     over-counts 35 -> 37 via commits 5a7447d3 and ebd51cbb, whose only match is
     `docs/cli/features/tool-sandbox.mdx`. Never use a `*substring*` form for any carve-out check
     in this plan -- directory paths get a trailing slash, file paths are exact and single-quoted. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Carve-out re-touch check against the full 116 window (D-19)</name>
  <files>.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md</files>
  <read_first>
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-CONTEXT.md (D-19 in full)
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-RESEARCH.md §2i (the exact verified command form) and Pitfall 1 (the substring-glob hazard, live in this exact window)
    proj/ADR-108-deny-domain-posture.md, proj/ADR-111-resource-limits-boundary.md, proj/ADR-113-spiffe-disposition.md, proj/ADR-114-oauth-capture-disposition.md (read each in full for its named carve-out/permanent-boundary/rejected-absorb-surface file path(s) -- these are the "any new carve-outs named since Phase 108" this task must add)
    .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md (sample structurally via `grep -n "^## Carve-out" ...` for the exact result-line wording precedent: "clean -- no re-touch in window" / "HIT (N commits) -- disposition note")
  </read_first>
  <action>
    Compile the accumulated carve-out surface list: start with the 7 named in the interfaces block
    above, confirming each path still exists in the fork with `ls <path>` (paths may have moved
    since Phase 108's 2026-07-29 audit -- if a path no longer exists, record that explicitly and
    locate its successor path via `git log --follow --oneline -- <old-path>` before dropping it).
    Then read `proj/ADR-108-deny-domain-posture.md`, `proj/ADR-111-resource-limits-boundary.md`,
    `proj/ADR-113-spiffe-disposition.md`, and `proj/ADR-114-oauth-capture-disposition.md` in full,
    and extract each ADR's own named carve-out / permanent-boundary / rejected-absorb-surface file
    path(s) (grep each for its Fork Touchpoint Map, OD-N, or "does NOT extend" sections) -- add
    every one found to the accumulated list, citing which ADR it came from. For every surface in
    the final accumulated list, run `git log --no-merges --oneline $RANGE -- <exact-path(s)>`
    against the full 116 window (`RANGE=0551eba27ea53b0eda20f4f796f757dd168adb92..0055bf3c686de6fa665decd9831dc5686b282cb0`)
    -- directory paths get a trailing slash and single quotes, file paths are exact and
    single-quoted, never a `*substring*` glob (Pitfall 1 is live in this exact window: a
    `'*tool-sandbox*'` glob over-counts via `5a7447d3`/`ebd51cbb`, whose only match is a `docs/`
    path -- do not repeat that mistake for any carve-out path). If the output is empty, record the
    literal phrase "clean -- no re-touch in window" (or, for `exec_strategy_windows/` specifically,
    the RESEARCH.md §2i stronger form: "clean -- no re-touch in window; upstream has no
    Windows-specific exec-strategy directory at all"). If non-empty, record "HIT (N commits)"
    naming every SHA and cross-referencing which existing table already covers it (this ledger's
    own pre-fence/post-fence tables, Plan 116-03's residue tables, or `108-DIVERGENCE-LEDGER.md`) --
    do not re-analyze a HIT commit here, just point to where it is already classified. Build a `##
    Carve-out Re-touch Check` table: carve-out name | path(s) | source (Phase 108 original / named
    in ADR-108 / ADR-111 / ADR-113 / ADR-114) | git log command | result.
  </action>
  <acceptance_criteria>
    - The table has one row per accumulated carve-out surface (at least the 7 Phase 108 originals,
      plus every new surface found in ADR-108/111/113/114's own text) with a non-empty result cell
      for every single row -- no carve-out surface omitted from the table, no result cell blank.
    - Every result cell contains either the exact literal phrase "clean -- no re-touch in window"
      (or the `exec_strategy_windows/` stronger variant) or the form "HIT (" followed by a commit
      count and a routing citation to an existing table/section.
    - The `exec_strategy_windows/` row uses the RESEARCH.md §2i command form verbatim (trailing
      slash, single-quoted) and, since it is clean, the stronger "upstream has no Windows-specific
      exec-strategy directory at all" phrasing.
    - Zero rows in the table use a `*substring*` glob form -- grep the table's recorded git log
      commands for the pattern `'\*[A-Za-z]` (an asterisk immediately followed by a letter inside
      single quotes) and confirm zero matches.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^## Carve-out Re-touch Check" .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md && grep -c "clean -- no re-touch in window" .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md</automated>
  </verify>
  <done>Every accumulated carve-out surface (Phase 108's 7 plus every new boundary named in ADR-108/111/113/114) has an explicit clean/HIT result against the full 116 window, using exact literal pathspecs throughout, with the exec_strategy_windows/ row using RESEARCH.md §2i's verbatim command and stronger clean-result phrasing.</done>
</task>

<task type="auto">
  <name>Task 2: Bucket-count reconciliation (arithmetic shown) + Security-Relevant Rollup + Completeness Verification + finalized Headline (D-01, D-04, D-16, D-17)</name>
  <files>.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md</files>
  <read_first>
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-CONTEXT.md (D-01, D-04, D-16, D-17 in full)
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md (the full accumulated document from Plans 116-01/116-03 -- read the `## Reproduction` section's D-01 reconciliation statement, the Pre-Fence/Post-Fence Commits tables, and the `## Split-Commit Residue Accounting` tables from Plan 116-03 Task 1)
    .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md (sample structurally via `grep -n "^## Completeness Verification\|^## Security-Relevant Rollup\|^## Headline" ...` for the arithmetic-statement and rollup format precedent -- read only those sections)
  </read_first>
  <action>
    Bucket-count reconciliation: sum, across every pre-fence and post-fence split commit's residue
    table from Plan 116-03 Task 1, the total row count bucketed `absorb`+`defer`+`noise` (call this
    Σ_bucketed), and separately sum each of those same commits' `git show --name-only --format=''
    <sha> | wc -l` counts already recorded in those tables (call this Σ_touched). State both sums as
    explicit numbers in a `## Bucket-Count Reconciliation` section with the literal arithmetic shown
    per commit (e.g. "d5803b99: 5 bucketed / 5 touched; ea334d2b: 3 bucketed / 3 touched; ... ; Σ
    bucketed = 23, Σ touched = 23, 23 == 23") -- note this reconciliation is scoped to THIS ledger's
    own pre-fence/post-fence split commits; the fenced-window's split-commit residue is Phase 108's
    own already-reconciled arithmetic, cross-referenced not re-summed here, and state that scoping
    explicitly. Then, since this ledger did not carry Phase 108's `security-relevant` flag forward
    as a dedicated per-row column (Claude's Discretion, CONTEXT.md leans yes but does not require
    it -- state this exact reasoning in the ledger), perform a `## Security-Relevant Rollup`
    substitute pass instead: scan every pre-fence and post-fence commit's subject line and, where
    the subject is ambiguous, its diff (`git show <sha>`) for security-relevant themes -- auth,
    credential, token, trust, permission, deny/escape-relevant behavior -- and list every match in
    a table: sha | subject | reason. This makes the D-18 security subset queryable without a
    dedicated column, per the discretion note. Next, `## Completeness Verification`: state the
    arithmetic pre-fence-count + 20 (fenced, cross-referenced) + post-fence-count == full-window
    union total (reconfirming Plan 116-01 Task 2's own check), with the actual numbers, not
    variable names. Sweep every SHA appearing as a primary row in the Pre-Fence and Post-Fence
    tables (the fenced 20 are cross-referenced, not primary rows in this ledger), sort/dedupe, and
    state the unique count -- it must equal pre-fence-count + post-fence-count with zero
    unexplained duplicates; if a SHA duplicates, name it and explain (a SHA appearing once as a
    primary row and again inside a carve-out HIT cross-reference is not a duplicate -- only the
    same SHA as a primary row in two different tables counts as a defect). Finally, rewrite/prepend
    the ledger's `## Headline` section at the very top (immediately after the YAML frontmatter,
    before `## Reproduction`): restate the D-01 hypothesis-vs-measurement reconciliation from Plan
    116-01 Task 1 explicitly and verbatim-equivalent -- "measured 38, hypothesis 38, no delta" (or
    the actual recorded delta if one exists) -- so a reader sampling only the Headline sees this
    without opening the Reproduction section. State the total commit count breakdown (pre-fence /
    fenced-cross-ref-20 / post-fence), the disposition tally (count of adopt/adapt/skip/split
    across all dispositioned commits), the windows-touch tally, a relative-path pointer to
    `108-DIVERGENCE-LEDGER.md`, a relative-path pointer to `116-FEASIBILITY-MATRIX.md`, and a
    relative-path pointer to `proj/ADR-116-tool-sandbox-disposition.md`. Close the Headline with
    the phrase "Ledger closed except for named exceptions" (mirroring the precedent 108/98 ledgers
    use, per RESEARCH.md §1c) rather than a bare "closed", leaving room for a future addendum.
  </action>
  <acceptance_criteria>
    - `## Bucket-Count Reconciliation` states Σ_bucketed and Σ_touched as explicit numbers and
      states their equality (or names the discrepant commit if unequal) -- "the buckets reconcile"
      alone, without the two numbers shown, does not satisfy this.
    - `## Security-Relevant Rollup` section is present, contains the literal phrase "Claude's
      Discretion", and lists at least one commit with sha|subject|reason (or explicitly states zero
      security-relevant commits were found in the pre-fence/post-fence surface, if that is what the
      scan returns).
    - `## Completeness Verification` states the pre-fence + 20 + post-fence arithmetic with actual
      numbers summing to the recorded total, and states the unique primary-row SHA count explicitly
      with zero unexplained duplicates (or names every duplicate).
    - The `## Headline` section, read alone (i.e. without needing to open `## Reproduction`),
      contains the D-01 reconciliation restated with either the literal phrase "no delta" or a
      stated numeric delta, the total commit count, and relative-path pointers to
      `108-DIVERGENCE-LEDGER.md`, `116-FEASIBILITY-MATRIX.md`, and the ADR-116 filename.
    - The Headline's closing statement contains the literal phrase "closed except for named
      exceptions" (not a bare "Ledger closed").
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^## Bucket-Count Reconciliation" .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md && grep -c "^## Completeness Verification" .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md && grep -c "closed except for named exceptions" .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md</automated>
  </verify>
  <done>Bucket-count reconciliation shows both summed totals and their equality explicitly; the discretion note and Security-Relevant Rollup substitute for a dedicated column; Completeness Verification proves the per-segment arithmetic and unique-SHA count; the finalized Headline restates the D-01 reconciliation (including the no-delta case), the total counts, and pointers to the ledger's sibling documents, closing with "closed except for named exceptions".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Carve-out silence -> fork-invariant regression | An omitted or blank-result carve-out surface would let a conflicting commit slip through this audit unflagged -- the exact D-19 failure mode this task's acceptance criteria are built to make structurally impossible to miss. |
| Bucket arithmetic assertion vs. shown | An unshown "the buckets reconcile" claim hides silent path loss the same way an omitted carve-out hides a regression -- both fail the same way: an assertion standing in for evidence. |
| Headline-only readers -> stale D-01 read | RESEARCH.md's own Validation Architecture recommends Headline-level sampling for quick checks; if the Headline omits the D-01 reconciliation, a reader who only samples the Headline could believe the hypothesis was never re-tested. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-04 | Repudiation | Carve-out re-touch check | mitigate | Every surface gets an explicit clean/HIT line using the literal RESEARCH §2i command form against the 116 window, with a table-wide zero-substring-glob check as an acceptance criterion (Task 1). |
| T-116-03 | Tampering | Bucket-count reconciliation | mitigate | Both sums (Σ_bucketed, Σ_touched) are computed and shown explicitly per commit, not merely asserted equal (Task 2). |
| T-116-02 | Repudiation | Headline D-01 restatement | mitigate | The hypothesis-vs-measurement reconciliation is restated in the Headline itself, including the no-delta case, so a Headline-only reader is not misled (Task 2). |
</threat_model>

<verification>
Re-run one carve-out re-touch check command per accumulated surface and confirm the recorded
clean/HIT result reproduces. Re-sum the Bucket-Count Reconciliation's per-commit numbers and
confirm they match the stated Σ_bucketed and Σ_touched totals. Confirm the Headline, read alone,
states the D-01 reconciliation.
</verification>

<success_criteria>
`116-DIVERGENCE-LEDGER.md` is complete: every accumulated carve-out surface has an explicit
clean/HIT result against the full window; the bucket-count arithmetic is shown and reconciles; the
Security-Relevant Rollup substitutes for the (not-taken) dedicated column with its discretion
reasoning on the record; the Completeness Verification proves zero silent commit loss; and the
finalized Headline restates the D-01 reconciliation, states the total counts, and points to
`108-DIVERGENCE-LEDGER.md`, `116-FEASIBILITY-MATRIX.md`, and `proj/ADR-116-tool-sandbox-disposition.md`.
</success_criteria>

<output>
Create `.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-04-SUMMARY.md` when done
</output>
