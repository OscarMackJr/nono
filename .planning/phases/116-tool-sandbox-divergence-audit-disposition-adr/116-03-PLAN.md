---
phase: 116-tool-sandbox-divergence-audit-disposition-adr
plan: 03
type: execute
wave: 2
depends_on: ["116-01"]
files_modified:
  - .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md
autonomous: true
requirements: [TSBX-01]

must_haves:
  truths:
    - "Every path in every split commit (pre-fence, post-fence, and the fenced-window commits this plan reconciles) lands in exactly one bucket -- absorb, defer, or noise -- with zero paths left unbucketed."
    - "Every fenced-window absorb-marked path's landing status is verified by a live grep against fork HEAD, not read off Phase 108's routing-note prose as though prose were an outcome."
    - "Any post-fence residue mapping to no existing phase is named as a finding with a proposed successor, explicitly gated on operator approval and never applied to ROADMAP.md/REQUIREMENTS.md by this phase."
  artifacts:
    - path: ".planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md"
      provides: "Split-commit residue tables (pre-fence/post-fence) + grep-verified fenced-window reconciliation + Post-Fence Residue Finding (if any)"
      contains: "absorb"
  key_links:
    - from: ".planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md"
      to: "fork HEAD (crates/, bindings/)"
      via: "grep-verified absorb-target reconciliation, not routing-note prose"
      pattern: "landed|not-landed|partially-landed"
---

<objective>
Append split-commit residue accounting to `116-DIVERGENCE-LEDGER.md` for the pre-fence and
post-fence commits Plan 116-01 flagged `split`, then reconcile Phase 108's fenced-window
absorb-marked residue against fork HEAD by live grep (not by reading 108's routing notes as fact),
and surface any post-fence residue mapping to no existing phase as a named, operator-gated finding.

Purpose: D-04's residue accounting must have zero unbucketed paths (D-07 inherited discipline),
and its fenced-window reconciliation must be an outcome verified in code, not an intent copied
from prose -- Phase 112's disposition table was wrong in 3 of ~6 re-checked rows from exactly this
"file/claim exists" != "symbol exists" gap.
Output: Residue tables for every pre-fence/post-fence split commit, a grep-verified fenced-window
reconciliation table, and a Post-Fence Residue Finding section (naming a proposed successor, or
stating explicitly that none is needed) appended to `116-DIVERGENCE-LEDGER.md`. This plan writes
into the same ledger file Plan 116-01 created; `depends_on: ["116-01"]` plus this plan's own
`wave: 2` (Plan 116-01 is `wave: 1`) makes the two writes strictly sequential -- they never touch
the file concurrently.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-CONTEXT.md
@.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-RESEARCH.md
@.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md

<interfaces>
<!-- Phase 108's fenced-window tool-sandbox-split commits (11 of the 20-commit fenced surface).
     The 4 named worked examples from Phase 108's own planning-time reconnaissance (verify each
     against 108-DIVERGENCE-LEDGER.md's ACTUAL finalized residue tables, not this list, since this
     list is what Phase 108 planned to find, not necessarily its final recorded routing): -->
  d5803b99  #1398  port-range support -> claimed absorb target: crates/nono/src/sandbox/{linux,macos}.rs (CORE-01/PROF-03 per 108's plan-time note)
  d4927f95  #1298  @git:* dynamic token expansion -> claimed absorb target: PROF-02 per 108's plan-time note
  ea334d2b  #1332  musl build fix (fs_type_unsupported u64) -> claimed: portable fix, no requirement ID
  8a4237f2  #1283  SeccompPolicy struct refactor -> claimed absorb target: bindings/c/src/sandbox.rs (18 files touched)
  (7 further tool-sandbox-split commits exist in 108's ledger beyond these 4 -- read
  `108-DIVERGENCE-LEDGER.md`'s own tool-sandbox-split section directly for the complete list and
  its ACTUAL recorded routing notes; the above is planning-time context only, not ground truth.)

RANGE (full window) = 0551eba27ea53b0eda20f4f796f757dd168adb92..0055bf3c686de6fa665decd9831dc5686b282cb0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split-commit residue accounting -- pre-fence + post-fence (D-04, D-07 inherited scheme)</name>
  <files>.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md</files>
  <read_first>
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-CONTEXT.md (D-04 in full)
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md (own file -- read the `## Pre-Fence Commits` and `## Post-Fence Commits` tables Plan 116-01 wrote, to collect every row flagged `split`)
    .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md (sample structurally via `grep -n "^## \|^#### " ...` to find the tool-sandbox-split per-commit residue table format: `path | marker | note` per commit -- read only that section, not the full 180KB file)
  </read_first>
  <action>
    From `116-DIVERGENCE-LEDGER.md`'s Pre-Fence and Post-Fence Commits tables, collect every sha
    flagged `split`. For each: run `git show --name-only --format='' <sha>` to get the definitive
    touched-path list (the authoritative source, not the tables' summary column). Bucket every
    path exactly once: `defer` if the path is inside the module set finalized in Plan 116-01 Task 1
    (module-scoped -- the fork has no host module for it, so it always defers; record the literal
    note "fork has no host module for this path"); `noise` if the path is under `docs/`,
    `crates/nono-cli/data/`, `crates/nono-cli/tests/`, or another non-production path; `absorb` if
    the path is a production path outside the module set (e.g. `crates/nono/src/...`,
    `crates/nono-cli/src/<non-module-set-file>.rs`, `bindings/c/src/...`) -- for `absorb` rows,
    state where the path *would* land if ever absorbed (a description, not an assumption that it
    already has -- these are pre-fence/post-fence commits Phase 108 never routed). For every split
    commit, sum-check: the residue table's row count must equal `git show --name-only --format=''
    <sha> | wc -l` for that sha -- record both numbers and confirm equality explicitly. Build a
    `## Split-Commit Residue Accounting (Pre-Fence + Post-Fence)` section with one table per split
    commit: sha + subject as a heading, then `path | marker | note` rows.
  </action>
  <acceptance_criteria>
    - Every commit flagged `split` in Plan 116-01's Pre-Fence or Post-Fence tables has its own
      residue table in this section (a 1:1 match, verifiable by comparing the split-flagged sha
      list against this section's table headings).
    - For every residue table, the stated row count equals the stated `git show --name-only --
      format='' <sha> | wc -l` output for that sha (both numbers recorded explicitly, not just
      asserted equal).
    - Every row has exactly one marker (`absorb`, `defer`, or `noise`) -- no row left unmarked, no
      path appearing in two rows for the same commit.
    - Every `defer`-marked row's note contains the literal phrase "fork has no host module for
      this path".
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^## Split-Commit Residue Accounting" .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md && grep -c "fork has no host module for this path" .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md</automated>
  </verify>
  <done>Every pre-fence/post-fence split commit has a residue table whose row count matches its touched-path count exactly, every path is bucketed absorb/defer/noise with zero omissions, and every defer row states the fork has no host module for that path.</done>
</task>

<task type="auto">
  <name>Task 2: Fenced-window residue reconciliation by grep against fork HEAD + Post-Fence Residue Finding (D-04)</name>
  <files>.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md</files>
  <read_first>
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-CONTEXT.md (D-04 in full -- re-read immediately before this task; the grep-verified-outcome-vs-routing-note-prose distinction is the entire point of this task)
    .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md (read the ACTUAL finalized `## tool-sandbox-split` section directly -- every one of its 11 split commits' `absorb`-marked residue rows and their routing notes; this is ground truth for what Phase 108 claimed, not this plan's own hypothesis of it)
    .planning/phases/108-upst12-divergence-audit/108-CONTEXT.md (D-05 through D-09 for the residue-accounting convention 108 used, if the routing notes' phrasing needs interpretation)
  </read_first>
  <action>
    For every one of Phase 108's tool-sandbox-split commits (11 total, from `108-DIVERGENCE-LEDGER.md`'s
    own `## tool-sandbox-split` section), extract every `absorb`-marked path's routing note
    verbatim -- the phase/plan it claims to land in and the fork symbol, file, or feature it names.
    For each such absorb-marked path, grep fork HEAD for the specific symbol or feature the routing
    note names (e.g. if the note claims "port-range support absorbed via CORE-01/PROF-03 into
    `crates/nono/src/sandbox/linux.rs`", run `grep -n "<the specific symbol the note names>"
    crates/nono/src/sandbox/linux.rs`). Do NOT accept the routing note's phase-number citation as
    sufficient by itself -- a live grep hit (or a differently-named-but-functionally-equivalent
    symbol, explicitly noted as such) is required before recording "landed". Record, per
    absorb-marked path: the routing note's claim (quoted verbatim from 108's ledger), the grep
    command used to verify it, its hit count, and the verdict -- `landed` / `not-landed` /
    `partially-landed` (a `not-landed` or `partially-landed` result is a legitimate finding to
    record, not an error to paper over). Build a `## Fenced-Window Residue Reconciliation
    (grep-verified against fork HEAD)` table: absorb path | 108's routing-note claim | grep command
    | hit count | verdict. Then, using Task 1's post-fence split-commit residue tables, check every
    post-fence `absorb`-marked path the same way -- grep fork HEAD for a matching symbol/feature. If
    a post-fence absorb-marked path does NOT map to any existing phase (108-120) or landed symbol,
    name it under a `## Post-Fence Residue Finding` section with a proposed successor -- state the
    natural home is UPST13/FUT-08 per CONTEXT.md's own framing, not v3.7 scope pressure -- in the
    same "proposed, not applied" shape as Phase 108's own D-19 Phase 112 proposal. Close that
    section with the literal sentence: "This finding is a proposal; it is not applied to
    `.planning/ROADMAP.md` or `.planning/REQUIREMENTS.md` by this phase -- successor phase or FUT-item
    creation requires operator approval." If every post-fence absorb-marked path DOES map cleanly to
    an existing phase or a landed fork symbol, state that explicitly instead: "All N post-fence
    split commits' absorb-marked paths map to an existing phase or a grep-confirmed landed symbol
    -- no Post-Fence Residue Finding is needed" (silence is not an acceptable substitute for either
    outcome). Do not edit `.planning/ROADMAP.md` or `.planning/REQUIREMENTS.md` in this task.
  </action>
  <acceptance_criteria>
    - Every one of Phase 108's fenced-window `absorb`-marked paths has an explicit row in the
      Fenced-Window Residue Reconciliation table with a recorded grep command, hit count, and one
      of the three verdicts -- never a bare citation of the routing note's claim alone.
    - At least the 4 named worked-example commits (d5803b99, d4927f95, ea334d2b, 8a4237f2) appear
      in the reconciliation table with their actual (not planning-time-hypothesized) routing notes
      quoted from `108-DIVERGENCE-LEDGER.md`.
    - The document contains either a `## Post-Fence Residue Finding` section with a named proposed
      successor and the literal sentence "requires operator approval", OR the explicit "no
      Post-Fence Residue Finding is needed" statement with its stated N -- never neither.
    - `git diff --stat -- .planning/ROADMAP.md .planning/REQUIREMENTS.md` shows zero changes after
      this task.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^## Fenced-Window Residue Reconciliation" .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md && grep -icE "landed|not-landed|partially-landed" .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md</automated>
  </verify>
  <done>Every one of Phase 108's fenced-window absorb-marked paths is reconciled against fork HEAD by a recorded live grep (not routing-note prose), with an explicit landed/not-landed/partially-landed verdict; post-fence residue is either named as an operator-gated finding with a proposed successor or explicitly confirmed to need none; ROADMAP.md and REQUIREMENTS.md are unedited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Routing-note prose -> grep-verified outcome | Treating Phase 108's routing note as proof the code actually landed (rather than as an intent to verify) is the exact class of error that made Phase 112's disposition table wrong in 3 of ~6 re-checked rows. |
| Post-fence unmapped residue -> scope pressure | Naming a residue finding without the explicit "proposal only, operator-gated, zero ROADMAP.md edits" language risks silently expanding v3.7 scope the same way an unguarded finding would. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-03 | Repudiation | Split-commit residue accounting (pre-fence/post-fence) | mitigate | Per-commit sum-check (residue row count == touched-path count) makes silent path loss structurally detectable (Task 1). |
| T-116-03 | Tampering | Fenced-window absorb-target claims | mitigate | Every claim from Phase 108's routing notes is re-verified via a live grep against fork HEAD with a recorded command and hit count, not accepted from prose (Task 2). |
| T-116-08 | Elevation of Privilege (scope) | Post-fence residue finding -> proposed successor | mitigate | The finding is explicitly gated on operator approval with a literal required sentence, and a `git diff --stat` check confirms ROADMAP.md/REQUIREMENTS.md are unedited (Task 2). |
</threat_model>

<verification>
Re-run one Fenced-Window Residue Reconciliation grep command per named worked-example commit and
confirm the recorded hit count and verdict reproduce. Confirm `git diff --stat -- .planning/ROADMAP.md
.planning/REQUIREMENTS.md` is empty after this plan's tasks.
</verification>

<success_criteria>
`116-DIVERGENCE-LEDGER.md` gains complete split-commit residue tables for every pre-fence/post-fence
split commit with zero unbucketed paths, a fenced-window reconciliation table whose every verdict is
grep-verified against fork HEAD rather than assumed from Phase 108's prose, and an explicit
post-fence residue disposition (named finding or explicit non-finding) -- with zero edits to
ROADMAP.md or REQUIREMENTS.md.
</success_criteria>

<output>
Create `.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-03-SUMMARY.md` when done
</output>
