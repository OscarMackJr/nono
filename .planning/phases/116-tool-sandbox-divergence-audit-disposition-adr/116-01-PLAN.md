---
phase: 116-tool-sandbox-divergence-audit-disposition-adr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md
autonomous: true
requirements: [TSBX-01]

must_haves:
  truths:
    - "A reviewer can independently re-run the Reproduction block's commands and get the exact tag SHAs and 3-path-union commit count this ledger records."
    - "The module set participating in the tool-sandbox subsystem is re-derived from live grep evidence at v0.71.0, not inherited from Phase 108 without re-verification, and each of the 4 named candidate modules receives an explicit in-or-out call with cited evidence."
    - "Every disputed number (D-01's hypothesis table vs. this session's live measurement) is reconciled in place, with the superseded figure retained so the delta stays visible -- including the case where they agree."
  artifacts:
    - path: ".planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md"
      provides: "Reproduction block (pinned SHAs) + D-03 module-set re-derivation + full pre-fence/post-fence per-commit disposition tables with windows-touch flags"
      contains: "## Reproduction"
  key_links:
    - from: ".planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md"
      to: "upstream nolabs-ai/nono v0.64.1..v0.71.0"
      via: "pinned SHA reproduction block"
      pattern: "0551eba27ea53b0eda20f4f796f757dd168adb92\\.\\.0055bf3c686de6fa665decd9831dc5686b282cb0"
---

<objective>
Create `116-DIVERGENCE-LEDGER.md` and populate its Reproduction block, its D-03 module-set
re-derivation, and full per-commit disposition tables for the pre-fence (`v0.64.1..v0.66.0`) and
post-fence (`v0.69.0..v0.71.0`) portions of the audit window -- the two portions Phase 108's
ledger never touched. This is the foundation Plans 116-03/116-04/116-05/116-06 build on.

Purpose: Every prior audit's discussion-time hypothesis was wrong somewhere (D-01/D-03). This plan
re-measures live rather than copying CONTEXT.md's table forward, and re-derives the module set
from grep evidence rather than inheriting Phase 108's D-06 set unexamined.
Output: `116-DIVERGENCE-LEDGER.md` with a populated Reproduction block, a finalized module-set
derivation, and pre-fence/post-fence disposition tables (residue accounting for `split` commits
deferred to Plan 116-03; carve-out check and completeness sweep deferred to Plan 116-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-CONTEXT.md
@.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-RESEARCH.md
@.planning/phases/108-upst12-divergence-audit/108-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

<interfaces>
<!-- Pinned SHAs (verified live 2026-08-09 via `git ls-remote --tags upstream v0.64.1 v0.71.0`,
     matching 116-CONTEXT.md D-01 exactly). Re-verify at execution time -- do not assume upstream
     has not moved. -->
v0.64.1 = 0551eba27ea53b0eda20f4f796f757dd168adb92  (window start, PR #1105 pre-date)
v0.66.0 = d817ed53663c6bba4669ee7a5bfb41b35971fd1b  (Phase 108's fence start)
v0.69.0 = 59bdace7e905c05c127f480dc6d2a8c3a3331392  (Phase 108's fence end)
v0.71.0 = 0055bf3c686de6fa665decd9831dc5686b282cb0  (window tip)

Full window: 0551eba27ea53b0eda20f4f796f757dd168adb92..0055bf3c686de6fa665decd9831dc5686b282cb0
Pre-fence:   0551eba27ea53b0eda20f4f796f757dd168adb92..d817ed53663c6bba4669ee7a5bfb41b35971fd1b
Post-fence:  59bdace7e905c05c127f480dc6d2a8c3a3331392..0055bf3c686de6fa665decd9831dc5686b282cb0

3-path union pathspec (D-06 inherited set, D-03's starting point -- re-derive, do not assume final):
  'crates/nono-cli/src/tool-sandbox/' 'crates/nono-cli/src/command_policy.rs' 'crates/nono-cli/src/lineage_cgroup.rs'

Verified live 2026-08-09 (RESEARCH.md §2c/§2d/§2e -- treat as a hypothesis to re-confirm, not a
fact to copy forward, per D-01):
- Full-window 3-path-union count: 38
- Substring-hazard form (`'*tool-sandbox*'`) over-counts to 37 vs. directory-only 35 -- same two
  commits (5a7447d3, ebd51cbb) Phase 108 already flagged, both touching only `docs/cli/features/
  tool-sandbox.mdx`.
- `upstream` remote already points at `nolabs-ai/nono`; `upstream/main` was at `4ede9ccc` on
  2026-08-05 (re-confirm live -- it will have moved further by execution time).
- The 7 named refinement PR-to-SHA mappings inside Phase 108's fenced window (D-13, do not
  re-derive, cross-reference only):
  #1280->199bb266  #1322->eb2d61a7  #1325->676a042f  #1384->72a98830
  #1394->bf7ea3cb  #1413->ab93cf44  #1417->a519ee62
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduction block + D-03 module-set re-derivation</name>
  <files>.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md</files>
  <read_first>
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-CONTEXT.md (D-01, D-03, D-17 in full)
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-RESEARCH.md §2a-2h (live-verified commands; Pitfall 1 substring hazard; Pitfall 3 main.rs control-flow caveat)
    .planning/phases/108-upst12-divergence-audit/108-CONTEXT.md (D-06 -- the module-set correction this task's D-03 re-derivation mirrors)
    .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md (sample structurally via `grep -n "^## " ...` for the Reproduction section's field layout -- do NOT read the full 180KB file)
  </read_first>
  <action>
    Run `git fetch upstream --tags` then `git ls-remote --tags upstream v0.64.1 v0.71.0`; confirm
    the output matches `0551eba27ea53b0eda20f4f796f757dd168adb92  refs/tags/v0.64.1` and
    `0055bf3c686de6fa665decd9831dc5686b282cb0  refs/tags/v0.71.0` verbatim. If either SHA differs,
    stop and record an explicit escalation note -- never silently substitute a moved tag. Run `git
    rev-parse upstream/main` and record it, dated, as a named boundary (not an audited SHA). Create
    the ledger file with YAML frontmatter mirroring 108-DIVERGENCE-LEDGER.md's field set: phase,
    plan, ledger_type: tool-sandbox-audit, range: "0551eba27ea53b0eda20f4f796f757dd168adb92..0055bf3c686de6fa665decd9831dc5686b282cb0",
    upstream_head_at_audit, refetch_date, fork_baseline (`git rev-parse HEAD`), total_unique_commits,
    date. Set RANGE to the full-window string above and run `git log --no-merges --oneline $RANGE
    -- 'crates/nono-cli/src/tool-sandbox/' 'crates/nono-cli/src/command_policy.rs'
    'crates/nono-cli/src/lineage_cgroup.rs' | wc -l`; record the literal output and reconcile
    explicitly against D-01's hypothesis of 38 -- state "measured 38, hypothesis 38, no delta" if
    it matches, or the numeric delta with the causing SHA(s) named if it does not. Reproduce the
    D-06-style pathspec hazard (Pitfall 1) as documented evidence: run `git log --no-merges
    --oneline $RANGE -- '*tool-sandbox*' | wc -l` against `git log --no-merges --oneline $RANGE --
    'crates/nono-cli/src/tool-sandbox/' | wc -l`, then `comm -23 <(git log --no-merges
    --format='%H' $RANGE -- '*tool-sandbox*' | sort) <(git log --no-merges --format='%H' $RANGE --
    'crates/nono-cli/src/tool-sandbox/' | sort)` to isolate the over-counted SHA(s), and confirm
    via `git show --name-only --format='' <sha> | grep -i tool-sandbox` that each one's only match
    is a `docs/` path. Write this as a `### Pathspec Hazard Reproduction (Pitfall 1)` subsection --
    the reusable evidence every later `git log -- <pattern>` invocation in this ledger must avoid
    repeating. For D-03's module-set re-derivation: run `git show v0.71.0:crates/nono-cli/src/
    tool-sandbox/mod.rs | grep -n "^\s*\(pub(crate)\s*\)\?mod \|^\s*pub mod "` to list
    `tool-sandbox/mod.rs`'s own declared children, then run `git show v0.71.0:crates/nono-cli/src/
    main.rs | grep -n "^mod \|^use \|#\[path"` to list ALL of `main.rs`'s top-level module
    declarations and use-imports -- the FULL list, not only the four named candidates -- so the
    derivation discovers what participates rather than confirming four pre-named guesses (D-16's
    "discover, don't confirm" rule). From that full list, identify every sibling module beyond the
    D-06 inherited three (`tool_sandbox`, `command_policy`, `lineage_cgroup`) and grep each
    candidate's own file content for cross-references to the tool-sandbox set (`git show
    v0.71.0:crates/nono-cli/src/<candidate>.rs | grep -nE "tool_sandbox|command_policy|
    lineage_cgroup"`) and the reverse direction (grep all 12 tool-sandbox files +
    command_policy.rs + lineage_cgroup.rs for each candidate's own name). Record the full result
    table: module | declared in main.rs mod/use? | cross-references tool-sandbox set (fwd)? |
    referenced BY tool-sandbox set (rev)? | in-or-out call | reasoning. For the four modules
    CONTEXT.md names (`command_blocking_deprecation.rs`, `instruction_deny.rs`,
    `open_url_runtime.rs`, `hook_runtime.rs`) plus any additional sibling modules the discovery
    pass surfaced, run their own commit-history count within the full window: `git log --no-merges
    --oneline $RANGE -- crates/nono-cli/src/command_blocking_deprecation.rs
    crates/nono-cli/src/instruction_deny.rs crates/nono-cli/src/open_url_runtime.rs
    crates/nono-cli/src/hook_runtime.rs | wc -l` -- record whether it is zero (the in-or-out call
    is moot for the commit-surface count) or nonzero (the call is live). Per Pitfall 3, also read
    `main.rs`'s function body (`git show v0.71.0:crates/nono-cli/src/main.rs`, not just its
    mod/use header) to check whether any candidate's function is called adjacent to or gated by
    `tool_sandbox::maybe_run_internal_tool_sandbox_entrypoint()` in the same early-dispatch flow,
    even without a direct `use` edge -- record this control-flow finding separately from the
    static mod/use finding. State the final in-or-out call for the re-derived module set, with the
    reasoning on the record, not implied by omission.
  </action>
  <acceptance_criteria>
    - Ledger file exists with YAML frontmatter containing literal `range: "0551eba27ea53b0eda20f4f796f757dd168adb92..0055bf3c686de6fa665decd9831dc5686b282cb0"`.
    - `## Reproduction` section contains both live-reconfirmed tag SHAs verbatim and the
      `upstream/main` boundary SHA with its measurement date.
    - The 3-path-union commit count is recorded with an explicit reconciliation statement against
      D-01's "38" hypothesis containing either the literal phrase "no delta" or a stated numeric
      delta with named SHA(s).
    - `### Pathspec Hazard Reproduction` subsection contains both counts (hazard form and
      directory-only form), the diffing SHA(s) from the `comm -23` output, and confirmation each
      over-counted SHA's only match is under `docs/`.
    - The module-set derivation table lists ALL of `main.rs`'s top-level `mod`/`use` declarations
      (not only the four named candidates), with an explicit in-or-out call and cited grep
      evidence for each of the four named candidates at minimum.
    - The four candidates' own commit-history count is recorded as an explicit number, with a
      stated conclusion on whether it is zero (moot) or nonzero (live).
    - A dedicated paragraph records the Pitfall-3 control-flow finding for each of the four
      candidates, separate from the static mod/use finding.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^## Reproduction" .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md && grep -c "0551eba27ea53b0eda20f4f796f757dd168adb92" .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md && grep -c "Pathspec Hazard Reproduction" .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md</automated>
  </verify>
  <done>Ledger file created with a live-reconfirmed Reproduction block, the 3-path-union count reconciled against D-01's hypothesis, the substring-hazard reproduced with named SHAs, and the D-03 module-set re-derivation table complete with an explicit in-or-out call, commit-history count, and main.rs control-flow finding for all four named candidates.</done>
</task>

<task type="auto">
  <name>Task 2: Pre-fence + post-fence per-commit disposition tables + fenced-window cross-reference stub (D-02, D-13, D-17)</name>
  <files>.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md</files>
  <read_first>
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-CONTEXT.md (D-02, D-13, D-17 in full)
    .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md (sample structurally via `grep -n "^## " ...` to locate the "tool-sandbox-pure and tool-sandbox-split" section, then read only that section -- not the full 180KB file)
    .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-RESEARCH.md §2e (union arithmetic) and Pitfall 4 (conflating the 187-commit full window with the 38-commit dispositioned surface)
  </read_first>
  <action>
    Using the module-set pathspec finalized in Task 1 (the 3-path union unless Task 1's
    derivation changed it -- if changed, use the corrected pathspec and note the change), compute:
    Pre-fence -- `git log --no-merges --oneline 0551eba27ea53b0eda20f4f796f757dd168adb92..d817ed53663c6bba4669ee7a5bfb41b35971fd1b -- <module-set-paths>`;
    Post-fence -- `git log --no-merges --oneline 59bdace7e905c05c127f480dc6d2a8c3a3331392..0055bf3c686de6fa665decd9831dc5686b282cb0 -- <module-set-paths>`.
    Record both counts. State the arithmetic check: pre-fence count + 20 (Phase 108's fenced-window
    total) + post-fence count == Task 1's full-window union total; name any discrepancy by SHA, do
    not silently absorb it. Before the tables, write the disposition vocabulary paragraph verbatim:
    "adopt/adapt/skip/split describe what each commit's content would require IF a future absorb
    executes -- a descriptive classification of the commit itself, not a decision about whether to
    execute an absorb. adopt = would land on the re-derived module set verbatim if ever absorbed.
    adapt = would require modification to fit fork conventions. skip = purely upstream-process
    content with no fork-relevant substance. split = touches the module set AND at least one other
    production path outside it (residue accounting handled in a later plan). This classification
    takes no position on Pole A vs. Pole B." For every pre-fence and post-fence commit: run `git
    show --name-only --format='' <sha>` for touched paths, and `git show <sha> | grep -icE
    'cfg\(target_os = "windows"\)|cfg\(windows\)'` for windows-touch (yes if count > 0, no if 0).
    Classify disposition per the vocabulary above. Build `## Pre-Fence Commits (v0.64.1..v0.66.0)`
    and `## Post-Fence Commits (v0.69.0..v0.71.0)` tables: sha | subject | PR# | touched-path
    summary | windows-touch | disposition, flagging every `split` row distinctly (Plan 116-03
    handles residue). Cross-check the six named post-fence PR numbers from D-01's hypothesis table
    (#1476, #1492, #1467, #1453, #1521, #1550) against the live post-fence table's subjects --
    state explicitly which PR numbers match and which don't; treat the hypothesis list as
    unconfirmed, not as ground truth. Add a `## Fenced-Window Cross-Reference (v0.66.0..v0.69.0,
    D-13)` section: state the 20-commit total (9 pure / 11 split) and the 7 named refinement
    PR-to-SHA mappings from the interfaces block above, each as a one-line pointer ("see
    `108-DIVERGENCE-LEDGER.md` § tool-sandbox-pure / tool-sandbox-split") -- do not reproduce
    108's per-commit tables. Spot-verify (not re-derive) that the fenced window still yields 20
    commits under the Task-1-finalized pathspec: `git log --no-merges --oneline
    d817ed53663c6bba4669ee7a5bfb41b35971fd1b..59bdace7e905c05c127f480dc6d2a8c3a3331392 --
    <module-set-paths> | wc -l`, and name any delta if the re-derived module set changed it.
  </action>
  <acceptance_criteria>
    - Pre-fence and post-fence tables each have one row per commit found by the literal git log
      commands, with sha|subject|PR#|touched-paths|windows-touch|disposition all populated (no
      blank windows-touch or disposition cells).
    - The arithmetic check (pre-fence + 20 + post-fence == Task 1's union total) is stated
      explicitly with actual numbers, and any discrepancy is named by SHA.
    - The disposition vocabulary paragraph appears verbatim before the tables and contains the
      literal sentence "This classification takes no position on Pole A vs. Pole B."
    - The six named post-fence PR numbers from D-01's hypothesis table are each explicitly
      confirmed-present or confirmed-absent against the live post-fence table.
    - The Fenced-Window Cross-Reference section contains zero per-commit table rows (a
      `| sha | subject |`-shaped table header inside that section returns no grep match) and
      cites `108-DIVERGENCE-LEDGER.md` by relative path, with the spot-verification command's
      output recorded.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^## Pre-Fence Commits" .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md && grep -c "^## Post-Fence Commits" .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md && grep -c "108-DIVERGENCE-LEDGER.md" .planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-DIVERGENCE-LEDGER.md</automated>
  </verify>
  <done>Pre-fence and post-fence commit tables complete with windows-touch + disposition for every commit, the disposition vocabulary defined pole-neutrally, the six named post-fence PRs reconciled against live data, and the fenced-window 20-commit surface cross-referenced (not duplicated) to Phase 108's ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Live upstream fetch -> pinned ledger record | The ledger's pinned SHAs anchor every downstream plan (03/04/05/06) and the ADR's Context section; a mismatch here silently invalidates all later evidence in the phase. |
| D-03 module-set derivation -> disposition scope | An under-derived module set silently drops commits from the dispositioned surface, handing Phase 120 an undersized work-list -- the exact D-02 failure mode CONTEXT.md names. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-01 | Tampering | Reproduction block SHA pins + 3-path-union count | mitigate | Live `git ls-remote`/`git log` re-run is a mandatory task step with explicit escalation language if upstream has moved (Task 1). |
| T-116-01 | Repudiation | D-03 module-set derivation | mitigate | Discovery-based grep of the FULL `main.rs` mod/use list (not just the 4 named candidates) plus a control-flow read, per D-16's "discover, don't confirm" rule (Task 1). |
| T-116-02 | Repudiation | D-01 hypothesis vs. live measurement | mitigate | Every count is reconciled in place with the superseded figure retained, including the "no delta" case (Task 1, Task 2). |
| T-116-05 | Information Disclosure | N/A | accept | Internal planning artifact; no secrets or credentials are read or written by this task. |
</threat_model>

<verification>
Re-run the Reproduction block's `git ls-remote`/`git log` commands and confirm the recorded tag
SHAs and 3-path-union count match. Confirm the pre-fence + 20 (fenced) + post-fence arithmetic
holds against Task 1's full-window union total.

This plan's Task 1 (the four-candidate commit-history computation, Step 7) directly answers
116-RESEARCH.md's Open Question 1 ("does the D-03 candidate-module in-or-out call affect the
D-01 hypothesis table's 38-commit figure?") -- the ledger records the live commit count for the
four candidates and states whether the in-or-out call is moot or live. Treat Open Question 1 as
(RESOLVED) by this plan's Task 1 output.
</verification>

<success_criteria>
`116-DIVERGENCE-LEDGER.md` exists with a live-reconfirmed Reproduction block, a complete D-03
module-set re-derivation with explicit in-or-out calls for all four named candidates, and full
pre-fence/post-fence disposition tables (windows-touch + disposition per commit) that
cross-reference -- rather than duplicate -- Phase 108's fenced-window surface.
</success_criteria>

<output>
Create `.planning/phases/116-tool-sandbox-divergence-audit-disposition-adr/116-01-SUMMARY.md` when done
</output>
