---
phase: 97-release-engineering-leapfrog-pipeline-runbook
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono/Cargo.toml
  - crates/nono-cli/Cargo.toml
  - crates/nono-proxy/Cargo.toml
  - crates/nono-shell-broker/Cargo.toml
  - crates/nono-fltmgr-client/Cargo.toml
  - bindings/c/Cargo.toml
  - Cargo.lock
  - ../nono-py/Cargo.toml
  - ../nono-py/pyproject.toml
  - ../nono-ts/package.json
  - ../nono-ts/Cargo.toml
autonomous: true
requirements: [RLS-05]
must_haves:
  truths:
    - "All 6 version-family workspace crates report version 0.66.0 via cargo metadata"
    - "No internal path-dep still pins 0.62.2 (or the looser 0.62) anywhere in the workspace"
    - "Both binding manifests (nono-py, nono-ts) carry 0.66.0 and their path-dep pins resolve"
    - "Cargo.lock is regenerated and `cargo build --workspace --all-targets` is green"
  artifacts:
    - path: "Cargo.lock"
      provides: "Regenerated lockfile with 0.66.0 workspace package entries"
      contains: "0.66.0"
    - path: "crates/nono/Cargo.toml"
      provides: "Core crate at 0.66.0 (leapfrog past upstream 0.65.1)"
      contains: 'version = "0.66.0"'
  key_links:
    - from: "crates/nono-cli/Cargo.toml"
      to: "crates/nono"
      via: "internal path-dep version pin"
      pattern: 'nono = \{ version = "0.66.0"'
    - from: "../nono-ts/Cargo.toml"
      to: "crates/nono"
      via: "cross-repo path-dep version pin"
      pattern: 'version = "0.66"'
---

<objective>
Leapfrog the entire version family to 0.66.0 — the first version that clears the relocated
upstream `nolabs-ai/nono` highest tag `v0.65.1` (the fork's hard rule is to tag PAST upstream's
highest; `0.65.0` would collide, so the concrete target is `0.66.0`). Move every workspace
crate's `[package]` version AND every internal path-dep `version =` pin together, regenerate
`Cargo.lock`, and unify both sibling binding repos (`nono-py`, `nono-ts`) onto the same release
version.

Purpose: A version/path-dep mismatch produces a non-reproducible or registry-rejected release.
This plan makes the whole tree internally consistent at one release version so every downstream
build, dry-run, and signing step (plans 02-04) operates on a coherent snapshot.
Output: 11 edited manifests + a regenerated `Cargo.lock`, all building clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Version-bump surface, extracted from the manifests. Use these directly; no exploration needed. -->

Workspace member version-family crates currently at 0.62.2 (bump ALL SIX [package].version):
  crates/nono/Cargo.toml            line 3   version = "0.62.2"   (publish)
  crates/nono-cli/Cargo.toml        line 3   version = "0.62.2"   (publish)
  crates/nono-proxy/Cargo.toml      line 3   version = "0.62.2"   (publish)
  crates/nono-shell-broker/Cargo.toml line 3 version = "0.62.2"   (publish)
  crates/nono-fltmgr-client/Cargo.toml line 3 version = "0.62.2"  (publish = false — bump anyway for consistency)
  bindings/c/Cargo.toml (nono-ffi)  line 3   version = "0.62.2"   (publish = false — bump anyway)

EXCLUDED from the leapfrog (independently versioned CI tool — leave as-is):
  tools/sign-fixture/Cargo.toml     version = "0.1.0"   (publish = false; NOT a release artifact)

Internal path-dep version pins to move 0.62.2 -> 0.66.0:
  crates/nono-cli/Cargo.toml:47   nono = { version = "0.62.2", path = "../nono", default-features = false }
  crates/nono-cli/Cargo.toml:48   nono-proxy = { version = "0.62.2", path = "../nono-proxy", default-features = false }
  crates/nono-cli/Cargo.toml:180  nono-shell-broker = { path = "../nono-shell-broker", version = "0.62.2" }   (windows dev-dep)
  crates/nono-proxy/Cargo.toml:21 nono = { version = "0.62.2", path = "../nono", default-features = false }
  crates/nono-shell-broker/Cargo.toml:19 nono = { version = "0.62.2", path = "../nono" }
  bindings/c/Cargo.toml:18         nono = { version = "0.62.2", path = "../../crates/nono", default-features = false }

Binding repos (SEPARATE sibling directories — independent versioning, deliberately unified here):
  ../nono-py/Cargo.toml      line 3   version = "0.9.0"   -> 0.66.0  (path-deps have NO version pin, no pin to move)
  ../nono-py/pyproject.toml  line 7   version = "0.9.0"   -> 0.66.0
  ../nono-ts/package.json    line 3   "version": "0.4.0"  -> "0.66.0"
  ../nono-ts/package.json    optionalDependencies block: 4 entries "0.4.0" -> "0.66.0"
  ../nono-ts/Cargo.toml      line 3   version = "0.4.0"   -> 0.66.0
  ../nono-ts/Cargo.toml      line 17  nono = { path = "../Nono/crates/nono", version = "0.62" }  -> version = "0.66"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Leapfrog the workspace crates + regenerate Cargo.lock</name>
  <files>crates/nono/Cargo.toml, crates/nono-cli/Cargo.toml, crates/nono-proxy/Cargo.toml, crates/nono-shell-broker/Cargo.toml, crates/nono-fltmgr-client/Cargo.toml, bindings/c/Cargo.toml, Cargo.lock</files>
  <read_first>
    - crates/nono/Cargo.toml (the file being modified — confirm the [package].version line)
    - crates/nono-cli/Cargo.toml (3 internal pins live here: nono, nono-proxy, and the windows dev-dep nono-shell-broker)
    - Cargo.toml (workspace root — confirm there is NO single workspace.package.version; each crate owns its version)
    - tools/sign-fixture/Cargo.toml (confirm it stays at 0.1.0 — do NOT touch it)
  </read_first>
  <action>
    Set the `[package].version` to "0.66.0" in all SIX version-family workspace manifests listed in
    the interfaces block (nono, nono-cli, nono-proxy, nono-shell-broker, nono-fltmgr-client,
    bindings/c). Move every internal path-dep `version =` pin from "0.62.2" to "0.66.0" at the six
    pin sites enumerated above (three of them inside nono-cli/Cargo.toml, including the windows-only
    dev-dependency `nono-shell-broker` pin). Do NOT touch tools/sign-fixture (stays 0.1.0). Do NOT
    add any new dependency — this is a version-string-only change, no `cargo add`. After editing,
    regenerate the lockfile with `cargo update --workspace --offline` if offline resolution
    succeeds, otherwise `cargo update --workspace` (this rewrites only the workspace package entries;
    it must not bump external crates). Then build the whole tree. Target 0.66.0 chosen because it is
    the first SemVer release strictly greater than upstream `nolabs-ai/nono` v0.65.1 (fork must tag
    past upstream's highest; 0.65.0 collides).
  </action>
  <acceptance_criteria>
    - `grep -rn '0.62.2' crates/ bindings/c/Cargo.toml` returns nothing (no stale version or pin remains).
    - `cargo metadata --no-deps --format-version 1` shows nono, nono-cli, nono-proxy, nono-shell-broker, nono-fltmgr-client, nono-ffi each at "0.66.0" (and sign-fixture still at "0.1.0").
    - `grep -c '0.66.0' Cargo.lock` returns a count of at least 6 (the workspace package entries).
    - `cargo build --workspace --all-targets` exits 0 (use --all-targets, NOT --bin nono — the bin-scoped gate has historically hidden nono-ffi exhaustive-match breaks).
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/OMack/Nono" && grep -rn '0.62.2' crates/ bindings/c/Cargo.toml; test -z "$(grep -rn '0.62.2' crates/ bindings/c/Cargo.toml)" && cargo build --workspace --all-targets</automated>
  </verify>
  <done>Six workspace crates + six path-dep pins at 0.66.0, sign-fixture untouched, Cargo.lock regenerated, workspace builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Unify the sibling binding repos onto 0.66.0</name>
  <files>../nono-py/Cargo.toml, ../nono-py/pyproject.toml, ../nono-ts/package.json, ../nono-ts/Cargo.toml</files>
  <read_first>
    - ../nono-py/Cargo.toml (line 3 version 0.9.0; path-deps are path-only, no version pin to move)
    - ../nono-py/pyproject.toml (line 7 project.version 0.9.0 — must match the Cargo version for maturin)
    - ../nono-ts/package.json (line 3 version 0.4.0; optionalDependencies block lists 4 platform sub-packages at 0.4.0)
    - ../nono-ts/Cargo.toml (line 3 package version 0.4.0; line 17 path-dep `nono` pinned to the looser "0.62")
  </read_first>
  <action>
    Set nono-py to 0.66.0 in BOTH ../nono-py/Cargo.toml ([package].version) and
    ../nono-py/pyproject.toml ([project].version) — maturin requires the two to agree. Set nono-ts
    to 0.66.0 in ../nono-ts/package.json (top-level "version") AND update all four
    optionalDependencies entries (nono-ts-darwin-x64, nono-ts-darwin-arm64, nono-ts-linux-x64-gnu,
    nono-ts-linux-arm64-gnu) from "0.4.0" to "0.66.0" so the platform sub-packages stay in lockstep.
    In ../nono-ts/Cargo.toml set [package].version to 0.66.0 and move the `nono` path-dep pin from
    `version = "0.62"` to `version = "0.66"` (matches the leapfrogged core minor). This is a
    deliberate version unification: nono-py jumps 0.9.0->0.66.0 and nono-ts jumps 0.4.0->0.66.0 to
    align the bindings with the core release version per RLS-05 — it is NOT a typo. Do not introduce
    any new dependency.
  </action>
  <acceptance_criteria>
    - `grep -n '0.9.0' ../nono-py/Cargo.toml ../nono-py/pyproject.toml` returns nothing.
    - `grep -rn '"0.4.0"' ../nono-ts/package.json` returns nothing (top-level version + all 4 optionalDependencies bumped).
    - `grep -n '"0.62"' ../nono-ts/Cargo.toml` returns nothing (path-dep pin moved to 0.66).
    - `cargo metadata --no-deps --format-version 1 --manifest-path ../nono-ts/Cargo.toml` reports nono-node at 0.66.0 (or, if the napi build toolchain is unavailable, `grep -c '0.66.0' ../nono-ts/Cargo.toml` returns at least 2: package version + path-dep pin).
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/OMack/Nono" && test -z "$(grep -n '0.9.0' ../nono-py/Cargo.toml ../nono-py/pyproject.toml)" && test -z "$(grep -rn '\"0.4.0\"' ../nono-ts/package.json)" && test -z "$(grep -n '\"0.62\"' ../nono-ts/Cargo.toml)" && echo BINDINGS_BUMPED</automated>
  </verify>
  <done>nono-py and nono-ts manifests (4 files) report 0.66.0; nono-ts platform sub-packages and the nono path-dep pin moved in lockstep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fork tree → crates.io / PyPI / npm registries | A version-string snapshot here becomes the immutable identity of a published artifact later; an inconsistency is uncorrectable post-publish (yank only). |
| Nono repo → sibling binding repos | Cross-repo version drift between core and bindings produces a binding that pins a non-existent core version. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | Tampering | version + path-dep pins across 11 manifests | mitigate | `grep -rn '0.62.2'` must return empty AND `cargo build --workspace --all-targets` exits 0 — both gate the task; partial bump cannot pass. |
| T-97-02 | Tampering | Cargo.lock regeneration | mitigate | `cargo update --workspace` only; assert no external crate moved (lockfile diff limited to workspace package entries). |
| T-97-03 | Repudiation | leapfrog target choice | accept | Target 0.66.0 documented in-action as "first SemVer > upstream 0.65.1"; rationale auditable in the SUMMARY. |
| T-97-01-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install in this plan — version strings only, existing lockfile reused; no new package enters the tree. |
</threat_model>

<verification>
- `cargo build --workspace --all-targets` exits 0 on the Windows dev host.
- No occurrence of `0.62.2` (or the looser `0.62` pin) remains in any tracked manifest.
- `cargo metadata` confirms all six version-family crates at 0.66.0; sign-fixture unchanged at 0.1.0.
</verification>

<success_criteria>
The workspace + both bindings are internally consistent at release version 0.66.0, the lockfile is
regenerated, and the tree builds clean — the coherent snapshot every later plan depends on.
</success_criteria>

<output>
Create `.planning/phases/97-release-engineering-leapfrog-pipeline-runbook/97-01-SUMMARY.md` when done.
</output>
