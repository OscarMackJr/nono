---
phase: 97-release-engineering-leapfrog-pipeline-runbook
plan: 02
type: execute
wave: 2
depends_on: ["97-01"]
files_modified:
  - .github/workflows/release.yml
  - scripts/build-windows-msi.ps1
  - scripts/validate-windows-msi-contract.ps1
autonomous: true
requirements: [RLS-06, RLS-08]
must_haves:
  truths:
    - "build-windows-msi.ps1 produces correctly-named machine + user MSIs locally from a 0.66.0 release build"
    - "release.yml signs payload binaries BEFORE the WiX harvest and has an admin-extract payload-signature verify gate"
    - "release.yml has NO reusable-workflow-call job (the chronic 0s startup_failure cause) and all five build matrix legs are present"
    - "release.yml asset names and the crates.io publish set are reconciled with the 0.66.0 leapfrog and the publishable crate set"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Tag-triggered build+sign+release pipeline with preserved signing order"
      contains: "Verify MSI payload signatures"
    - path: "scripts/build-windows-msi.ps1"
      provides: "Generated-wxs MSI builder (machine + user scope)"
      contains: "VersionTag"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "scripts/build-windows-msi.ps1"
      via: "Package (Windows) step invokes the generator AFTER pre-package signing"
      pattern: "build-windows-msi.ps1"
    - from: "release.yml Sign Windows binaries (pre-package)"
      to: "release.yml Package (Windows)"
      via: "sign-before-harvest ordering (Phase 53 UAT-B fix)"
      pattern: "pre-package"
---

<objective>
Verify the release pipeline builds and signs all Windows release artifacts reproducibly from a
single tag at the new 0.66.0 version, and that release.yml is structurally sound — preserving the
Phase 53 sign-before-harvest order + the admin-extract payload-signature gate, carrying no
reusable-workflow-call job (the chronic 0s startup_failure), and reconciling asset names + the
crates.io publish set with the leapfrog.

Purpose: The signing-ORDER class of bug (MSI ships unsigned payload binaries under a signed
wrapper) and the 0s startup_failure are both prior production incidents in THIS file. This plan
re-asserts those invariants against the bumped tree so the operator's eventual tag push produces a
fully-signed GitHub Release.
Output: A locally-built, structurally-validated MSI pair + an audited release.yml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- release.yml structural invariants the executor must preserve (do NOT regress these). -->

Signing ORDER (Phase 53 UAT-B — MUST stay in this sequence):
  1. "Sign Windows binaries (pre-package) — Azure Trusted Signing"  signs target/<t>/release/*.exe in place
  2. "Package (Windows)"   build-windows-msi.ps1 harvests the now-signed binaries into the MSIs
  3. "Sign Windows MSIs — Azure Trusted Signing"  signs the two MSI wrappers
  4. "Verify Authenticode signatures (Windows)"   wrapper + loose exe must be Valid
  5. "Verify MSI payload signatures (Windows)"     msiexec /a admin-extract; every payload .exe must be Valid (the gate that was missing when the unsigned-payload MSI shipped)

0s startup_failure prevention (release.yml NOTE block ~lines 474-481): there is intentionally NO
docker job that does `uses: ./.github/workflows/image-build.yml`. image-build.yml has no
`on: workflow_call:` trigger, so a reusable call fails the WHOLE workflow at startup validation.
Do NOT re-add such a job.

Asset-name templates use the RELEASE_TAG env (e.g. nono-<tag>-x86_64-pc-windows-msvc-machine.msi) —
version-derived, so they track the tag automatically; verify they still resolve for a v0.66.0 tag.

crates.io publish-crates job (~lines 483-519) publishes ONLY nono, nono-proxy, nono-cli (in that
order, sleep 30 between). Publishable workspace crates are actually FOUR: nono, nono-proxy, nono-cli,
nono-shell-broker (none carry publish=false). nono-cli has a windows dev-dep on nono-shell-broker
with a version pin — reconcile whether nono-shell-broker must join the publish set (plan 97-03's
dry-run proves the dependency-resolution requirement; record the finding here and align the job).

Stale upstream reference: update-homebrew-core job download-url points at always-further/nono
(~line 532) — the fork relocated; this is the wrong source repo for the release tarball.

build-windows-msi.ps1 params (from the Package step): -VersionTag, -BinaryPath, -BrokerPath,
-ServiceBinaryPath, -DriverBinaryPath, -Scope {machine|user}, -OutputDir. The .wxs under
dist/windows/ is GENERATED by this script's here-strings — edit the SCRIPT (and
validate-windows-msi-contract.ps1), never the .wxs. The script throws fail-closed if -Scope user is
combined with the driver/service flags.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Locally build + structurally validate the 0.66.0 MSI pair and preserve the signing-order invariant</name>
  <files>scripts/build-windows-msi.ps1, scripts/validate-windows-msi-contract.ps1</files>
  <read_first>
    - scripts/build-windows-msi.ps1 (the generator — confirm the here-string .wxs templates embed -VersionTag, the machine/user scope branching, and the fail-closed throw on driver+user combination)
    - scripts/validate-windows-msi-contract.ps1 (the existing contract checker — reuse its assertions; do not duplicate the runbook)
    - .github/workflows/release.yml (read the five signing/packaging steps named in the interfaces block to confirm the order is intact)
  </read_first>
  <action>
    Produce a release build of the workspace binaries the MSI harvests (nono.exe and
    nono-shell-broker.exe; the WFP service if it builds) via `cargo build --release -p nono-cli` and
    `cargo build --release -p nono-shell-broker`, then drive scripts/build-windows-msi.ps1 with
    -VersionTag v0.66.0 for BOTH -Scope machine and -Scope user into a scratch -OutputDir, mirroring the
    release.yml Package (Windows) invocation (machine scope also passes -ServiceBinaryPath +
    -DriverBinaryPath using the checked-in pre-signed driver under crates/nono-cli/data/windows/; user
    scope passes NEITHER, per the script's fail-closed contract). Run the produced MSIs through
    validate-windows-msi-contract.ps1 to confirm structure + that the version-derived file names resolve
    for v0.66.0. These local MSIs are UNSIGNED (Azure Trusted Signing is CI-only / OIDC-gated and is NOT
    run locally — PREPARE-ONLY); the signing assurance is structural: confirm in release.yml that the
    pre-package signing step runs BEFORE the Package step and that the Verify MSI payload signatures
    admin-extract gate exists and fails closed on a non-Valid payload. Do not edit the .wxs directly; any
    fix goes in the .ps1 here-strings. Invoke every PowerShell script via `pwsh -File <path>` (never
    `pwsh -Command "<bare path>"`, which swallows the exit code).
  </action>
  <acceptance_criteria>
    - `pwsh -File scripts/build-windows-msi.ps1 -VersionTag v0.66.0 -Scope user -OutputDir <scratch>` exits 0 and emits a file named nono-v0.66.0-x86_64-pc-windows-msvc-user.msi.
    - The machine-scope invocation (with -ServiceBinaryPath + -DriverBinaryPath) exits 0 and emits the matching -machine.msi.
    - `pwsh -File scripts/validate-windows-msi-contract.ps1` against the produced MSIs reports PASS.
    - The pre-package signing step precedes the Package step in release.yml, and a Verify MSI payload signatures step exists.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/OMack/Nono" && grep -n 'pre-package' .github/workflows/release.yml && grep -n 'Verify MSI payload signatures' .github/workflows/release.yml</automated>
  </verify>
  <done>0.66.0 machine + user MSIs build locally and pass the contract checker; release.yml sign-before-harvest order and admin-extract payload gate are confirmed present.</done>
</task>

<task type="auto">
  <name>Task 2: Audit release.yml — no 0s startup, all legs present, publish set + stale references reconciled</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    - .github/workflows/release.yml (read in full — the build matrix, the NOTE block explaining why there is no docker job, the publish-crates job, the update-homebrew-core job)
    - crates/nono-shell-broker/Cargo.toml (confirm it has NO publish=false — it is a publishable crate)
    - crates/nono-cli/Cargo.toml (confirm the windows dev-dep nono-shell-broker version pin that forces the publish-set reconciliation)
  </read_first>
  <action>
    Confirm release.yml parses and contains NO `uses: ./.github/workflows/image-build.yml` reusable
    call (the removed-docker-job invariant that prevents the 0s startup_failure) and that all five build
    matrix legs remain (x86_64/aarch64 linux-gnu, x86_64/aarch64 apple-darwin, x86_64 windows-msvc).
    Reconcile the crates.io publish set: because nono-cli pins nono-shell-broker with a version (windows
    dev-dependency), the published nono-cli manifest references nono-shell-broker at 0.66.0 — add
    nono-shell-broker to the publish-crates job in dependency order (nono, then nono-shell-broker, then
    nono-proxy, then nono-cli) with the same sleep-30 indexing-wait pattern, ONLY if plan 97-03's dry-run
    proves the dependency requires it; otherwise record in the SUMMARY why the existing 3-crate set is
    sufficient (do not add speculative jobs). Fix the stale always-further/nono tarball download-url in
    the update-homebrew-core job to the fork's canonical source repo. Do NOT add live-publish behavior,
    do NOT remove the alpha/beta/rc publish guard, and do NOT touch the OIDC/signing wiring. PREPARE-ONLY:
    release.yml stays tag-triggered; no task here pushes a tag.
  </action>
  <acceptance_criteria>
    - `grep -c 'image-build.yml' .github/workflows/release.yml` confirms no `uses:` reusable-workflow call remains (the only matches, if any, are in the explanatory NOTE comment).
    - `grep -c 'always-further/nono' .github/workflows/release.yml` returns 0.
    - `grep -c 'target:' .github/workflows/release.yml` returns 5.
    - release.yml is valid YAML (parser exits 0).
    - The publish-crates reconciliation decision is recorded in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/OMack/Nono" && test "$(grep -c 'always-further/nono' .github/workflows/release.yml)" = "0" && echo NO_STALE_UPSTREAM</automated>
  </verify>
  <done>release.yml has no reusable-workflow-call job, all five legs present, valid YAML, stale upstream URL removed, and the publish-set decision recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → released MSI/binary assets | Signing happens here; a wrong order ships unsigned payloads to end users who trust the installer. |
| GitHub Actions startup validator → workflow graph | A malformed reusable-workflow call fails the whole release at 0s, silently shipping nothing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-04 | Tampering | MSI payload binaries (signing order) | mitigate | Assert pre-package signing step precedes Package step + the admin-extract Verify MSI payload signatures gate exists and fails closed (Phase 53 UAT-B regression guard). |
| T-97-05 | Spoofing | MSI wrapper vs payload signature | mitigate | Both wrapper (Verify Authenticode) and payload (admin-extract) verification steps retained; neither removed. |
| T-97-06 | Denial of Service | release.yml startup validation | mitigate | No `uses: ./.github/workflows/image-build.yml` reusable call; YAML parse gate. |
| T-97-07 | Tampering | homebrew tarball source URL | mitigate | Stale always-further/nono download-url corrected to the fork's canonical repo so the formula fetches the real release tarball. |
| T-97-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install task here; CI action pins and local cargo build reuse the existing lockfile. |
</threat_model>

<verification>
- release.yml is valid YAML, carries all five build legs, and has no reusable-workflow-call job.
- The signing-order steps and the admin-extract payload gate are present in the documented sequence.
- 0.66.0 machine + user MSIs build locally and pass validate-windows-msi-contract.ps1.
- No stale always-further/nono reference remains in release.yml.
</verification>

<success_criteria>
The pipeline reproducibly builds + signs all Windows release artifacts from a single tag, release.yml
is structurally sound (no 0s startup, all legs green, signing order preserved), and the publish set is
reconciled with the leapfrog.
</success_criteria>

<output>
Create `.planning/phases/97-release-engineering-leapfrog-pipeline-runbook/97-02-SUMMARY.md` when done.
</output>
