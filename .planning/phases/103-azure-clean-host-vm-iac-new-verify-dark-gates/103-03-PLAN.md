---
phase: 103
plan: 03
type: execute
wave: 2
depends_on: ["103-01", "103-02"]
files_modified: []
autonomous: true
requirements: [CHOST-01, CHOST-02]

must_haves:
  truths:
    - "az bicep build against main.bicep still exits 0 at phase-gate time (no regression from the two plans landing independently)"
    - "Both new gates independently return SKIP_HOST_UNAVAILABLE (exit 3) when invoked singly via -Gate"
    - "A full -All sweep includes both new gates in its gates[] array with verdict SKIP_HOST_UNAVAILABLE, and overall is PASS or PASS_WITH_SKIPS, never FAIL or HARNESS_ERROR"
    - "scripts/verify-dark.ps1 is byte-for-byte unchanged from before this phase started — zero harness code changes"
    - "broker-spawn-on-clean-host.ps1 has no code-level dependency on clean-host-install.ps1, confirmed by direct inspection at phase-gate time (not just inferred from Plan 02's own acceptance criteria)"
  artifacts: []
  key_links:
    - from: "scripts/verify-dark.ps1 -All sweep"
      to: "scripts/gates/trusted-signed-assertion.ps1 + scripts/gates/broker-spawn-on-clean-host.ps1"
      via: "filename-based auto-discovery"
      pattern: "SKIP_HOST_UNAVAILABLE"
---

<objective>
Confirm, at the phase-gate boundary, that all four Phase 103 success criteria hold simultaneously
against the live dev-host state produced by Plans 01 and 02: `main.bicep` still compiles, both new
gates still independently SKIP, a full `-All` sweep is clean (no FAIL/HARNESS_ERROR anywhere, both
new gates present with SKIP_HOST_UNAVAILABLE), and the harness itself received zero edits.

Purpose: CHOST-01/SC1 and CHOST-02/SC2-SC4 must be verified together, not just per-plan in
isolation, because SC4 (the full sweep) and the harness-regression check are phase-wide properties
that only make sense once both plans have landed.

Output: no new files — this plan is verification-only. It produces the phase-gate evidence that
CHOST-01 and CHOST-02 both hold before `/gsd:verify-work` runs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/103-azure-clean-host-vm-iac-new-verify-dark-gates/103-RESEARCH.md
@.planning/phases/103-azure-clean-host-vm-iac-new-verify-dark-gates/103-VALIDATION.md
@.planning/phases/103-azure-clean-host-vm-iac-new-verify-dark-gates/103-01-SUMMARY.md
@.planning/phases/103-azure-clean-host-vm-iac-new-verify-dark-gates/103-02-SUMMARY.md
@scripts/verify-dark.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bicep lint + both single-gate SKIP checks</name>
  <files></files>
  <read_first>
    103-VALIDATION.md Per-Task Verification Map (103-SC1a, 103-SC2, 103-SC3 rows) for the exact
    automated commands and expected exit codes. 103-RESEARCH.md "Validation Architecture" section
    for the same command set with rationale.
  </read_first>
  <action>
    Run `az bicep build -f scripts/azure/clean-vm/main.bicep --stdout` and confirm it exits 0 —
    this re-confirms CHOST-01/SC1's static shape did not regress after Plan 01 landed (no
    intervening edits from Plan 02, since the two plans touch disjoint file sets). Run
    `pwsh -File scripts/verify-dark.ps1 -Gate trusted-signed-assertion` and confirm it exits 3
    (SKIP_HOST_UNAVAILABLE). Run `pwsh -File scripts/verify-dark.ps1 -Gate
    broker-spawn-on-clean-host` and confirm it also exits 3. For the broker-spawn gate, also
    directly inspect the file's own source (not just re-trust Plan 02's acceptance criteria) to
    confirm it contains no reference to `clean-host-install` anywhere and that its own
    `Test-Precondition`/`Invoke-Gate` pair installs and uninstalls its own MSI copy — this is the
    phase-gate's independent confirmation of the order-safety requirement, not a re-statement of
    Plan 02's own claim.
  </action>
  <verify>
    <automated>az bicep build -f scripts/azure/clean-vm/main.bicep --stdout</automated>
  </verify>
  <acceptance_criteria>
    - `az bicep build -f scripts/azure/clean-vm/main.bicep --stdout` exits 0.
    - `pwsh -File scripts/verify-dark.ps1 -Gate trusted-signed-assertion` exits 3.
    - `pwsh -File scripts/verify-dark.ps1 -Gate broker-spawn-on-clean-host` exits 3.
    - `grep -c "clean-host-install" scripts/gates/broker-spawn-on-clean-host.ps1` returns 0,
      independently re-confirmed at phase-gate time.
  </acceptance_criteria>
  <done>
    All three commands above produce the exact exit codes listed, and the independent
    order-safety inspection confirms zero cross-gate dependency.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full -All sweep + harness-diff regression + consolidated SC1-SC4 checklist</name>
  <files></files>
  <read_first>
    scripts/verify-dark.ps1 lines 356-386 (the aggregator rollup: `overall` precedence is
    HARNESS_ERROR &gt; FAIL &gt; PASS_WITH_SKIPS &gt; PASS) so the exact JSON shape being parsed is
    understood before asserting against it. 103-VALIDATION.md "103-SC4" and "103-REG" rows for the
    exact expected shape and the `git diff --stat` regression command.
  </read_first>
  <action>
    Run `pwsh -File scripts/verify-dark.ps1 -All` and capture its single-line JSON output. Parse it
    (e.g. via `ConvertFrom-Json` in a `pwsh -Command` invocation, or `python -c "import json,sys"`
    against the captured line) and assert programmatically — not by eyeballing — that: the `gates`
    array contains an entry with `gate == 'trusted-signed-assertion'` and `verdict ==
    'SKIP_HOST_UNAVAILABLE'`; the `gates` array contains an entry with `gate ==
    'broker-spawn-on-clean-host'` and `verdict == 'SKIP_HOST_UNAVAILABLE'`; and the top-level
    `overall` field is either `'PASS'` or `'PASS_WITH_SKIPS'` (never `'FAIL'` or `'HARNESS_ERROR'`).
    Then run `git diff --stat scripts/verify-dark.ps1` and confirm its output is completely empty —
    this is the phase-wide proof that zero harness code changes occurred across both plans,
    satisfying CHOST-02/SC2's "plugs into the existing verify-dark.ps1 gate-discovery harness with
    zero harness code changes" requirement. Assemble a final consolidated checklist confirming all
    four Phase 103 success criteria (ROADMAP.md Phase 103 section) hold: SC1 (main.bicep + deploy/
    teardown scripts), SC2 (trusted-signed-assertion.ps1 wired with zero harness changes), SC3
    (broker-spawn-on-clean-host.ps1 self-contained), SC4 (both gates SKIP under a full sweep).
  </action>
  <verify>
    <automated>pwsh -File scripts/verify-dark.ps1 -All</automated>
  </verify>
  <acceptance_criteria>
    - `pwsh -File scripts/verify-dark.ps1 -All` exits 0 (PASS or PASS_WITH_SKIPS both map to exit 0
      per the harness's own SC3 exit-code contract at verify-dark.ps1 lines 382-385).
    - Programmatic JSON assertion confirms both new gate names appear in `gates[]` with
      `verdict: "SKIP_HOST_UNAVAILABLE"`.
    - Programmatic JSON assertion confirms the top-level `overall` field is `"PASS"` or
      `"PASS_WITH_SKIPS"` — never `"FAIL"` or `"HARNESS_ERROR"`.
    - `git diff --stat scripts/verify-dark.ps1` produces empty output.
  </acceptance_criteria>
  <done>
    The full `-All` sweep is clean, both new gates are confirmed present with
    `SKIP_HOST_UNAVAILABLE`, `overall` is never `FAIL`/`HARNESS_ERROR`, and
    `scripts/verify-dark.ps1` is confirmed byte-for-byte unchanged — all four Phase 103 success
    criteria (CHOST-01/SC1, CHOST-02/SC2, SC3, SC4) are satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase-gate verification -> the two independently-authored plans' outputs | This plan is the single point where CHOST-01 and CHOST-02 are proven to hold together, not merely in isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-09 | Tampering | scripts/verify-dark.ps1 | mitigate | `git diff --stat scripts/verify-dark.ps1` is asserted empty at phase-gate time — an inadvertent harness edit introduced while wiring either gate would be caught here, not discovered later |
| T-103-10 | Repudiation (unverified claim of correctness) | Plan 01 / Plan 02 acceptance criteria | mitigate | This plan re-runs the concrete commands independently rather than trusting the prior plans' own self-reported acceptance criteria, and parses the `-All` sweep's JSON programmatically rather than visually inspecting it |

</threat_model>

<verification>
`az bicep build` exits 0; both single-gate invocations exit 3; the full `-All` sweep exits 0 with
both new gates present at `SKIP_HOST_UNAVAILABLE` and `overall` never `FAIL`/`HARNESS_ERROR`;
`git diff --stat scripts/verify-dark.ps1` is empty.
</verification>

<success_criteria>
All four Phase 103 ROADMAP success criteria (CHOST-01/SC1, CHOST-02/SC2, SC3, SC4) are confirmed
true simultaneously against the live dev-host state, with an independently-parsed JSON assertion
(not visual inspection) and a confirmed-empty harness diff.
</success_criteria>

<output>
Create .planning/phases/103-azure-clean-host-vm-iac-new-verify-dark-gates/103-03-SUMMARY.md when done
</output>
