---
phase: 115-v3-6-carry-forward-drain
plan: 05
type: execute
wave: 2
depends_on: ["115-02"]
files_modified:
  - ../nono-py/src/proxy.rs
  - ../nono-py/src/undo.rs
autonomous: true
requirements: [DRAIN-02]
user_setup: []

must_haves:
  truths:
    - "Every denial category nono-py's encoder can emit decodes without raising (D-10)."
    - "The round-trip proof is self-enumerating from the core enum's own ALL const (added in 115-02), not a second hand-written list (D-11 via D-12)."
    - "maturin build succeeds against the new codec, proving the binding actually compiles — only a real wheel build catches binding struct drift (5th consecutive occurrence of this lesson)."
  artifacts:
    - path: "../nono-py/src/proxy.rs"
      provides: "serde-driven denial_category encoder (replaces the hand-written match)"
    - path: "../nono-py/src/undo.rs"
      provides: "serde-driven denial_category decoder + the crate's first #[cfg(test)] mod tests block"
  key_links:
    - from: "../nono-py/src/undo.rs test module"
      to: "nono::undo::NetworkAuditDenialCategory::ALL"
      via: "round-trip iteration over every core-enum variant"
      pattern: "NetworkAuditDenialCategory::ALL"
---

<objective>
Close DRAIN-02 (NEW-06, blocker-class): delete both of `../nono-py`'s hand-written
`NetworkAuditDenialCategory` encode/decode matches and replace them with conversion through the
enum's own `#[serde(rename_all = "snake_case")]` derive — closing the class of drift that let
`capture_unsupported_path` and `capture_buffer_or_rewrite_failed` raise `ValueError` on decode
even though the encoder already emitted them. Pin the fix with the crate's first Rust-level test:
a round trip driven from `NetworkAuditDenialCategory::ALL` (the self-enumerating const added in
Plan 115-02), so a future variant is covered automatically rather than requiring a second
hand-maintained list.

**Cross-repo ordering (must run after Plan 115-02):** this plan needs the core enum in its final,
post-`InterceptHandshakeFailed`-removal shape with the `ALL` const already present — writing this
plan's decoder against the pre-removal enum would reference a variant about to disappear.
`../nono-py` is a separate git repository, reached via `..` (`workflow.use_worktrees=false`
confirmed already set) — its commits need their own DCO sign-off, separate from `nono`'s.

Purpose: the binding must never hold a second copy of the denial-category vocabulary — this is
the fix DRAIN-02 actually calls for, not two additional hand-written arms.
Output: both hand-written matches deleted, a serde-driven encoder/decoder, a self-enumerating
round-trip test, and a green `maturin build`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-v3-6-carry-forward-drain/115-CONTEXT.md
@.planning/phases/115-v3-6-carry-forward-drain/115-RESEARCH.md
@.planning/phases/115-v3-6-carry-forward-drain/115-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
`../nono-py/Cargo.toml` (confirmed live): `nono = { path = "../Nono/crates/nono", package =
"nono-sandbox" }` — a real path dependency, so local core-enum edits from Plan 115-02 are picked
up immediately by `cargo build -p nono-py` / `maturin build`, no publish step needed.
`[dev-dependencies] pyo3 = { features = ["extension-module", "auto-initialize"] }` already exists
specifically so `cargo test` can start the Python interpreter without an explicit
`Python::initialize()` call — this crate's own pre-existing groundwork for this plan's first test.

Encoder to delete (`../nono-py/src/proxy.rs`, grep `denial_category`, full current shape):
```rust
dict.set_item(
    "denial_category",
    event.denial_category.as_ref().map(|c| match c {
        nono::undo::NetworkAuditDenialCategory::AuthenticationFailed => "authentication_failed",
        nono::undo::NetworkAuditDenialCategory::EndpointPolicy => "endpoint_policy",
        // ... 9 more arms, post-115-02 minus InterceptHandshakeFailed ...
    }),
)?;
```

Decoder to delete (`../nono-py/src/undo.rs`, grep `denial_category`, current shape — 9 arms after
115-02's removal, plus a fallback):
```rust
denial_category: dict
    .get_item("denial_category")?
    .and_then(|v| v.extract::<String>().ok())
    .map(|s| match s.as_str() {
        "authentication_failed" => { Ok(...AuthenticationFailed) }
        // ... other arms ...
        other => Err(PyValueError::new_err(format!("invalid denial_category: {}", other))),
    })
    .transpose()?,
```
RESEARCH Q8 empirically confirmed byte-identity between every hand-written string and serde's
`rename_all = "snake_case"` output, including the "awkward" `ConnectBypassesL7` ->
`connect_bypasses_l7` — no manual string massaging is needed in the replacement.

Test-module preamble precedent (`crates/nono-cli/src/profile/mod.rs:4440-4442`):
```rust
#[cfg(test)]
#[allow(clippy::unwrap_used)]
mod tests {
    use super::*;
    ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete hand-written matches; replace with serde-driven conversion (D-10)</name>
  <files>../nono-py/src/proxy.rs, ../nono-py/src/undo.rs</files>
  <read_first>
    ../nono-py/src/proxy.rs — grep `denial_category` for the encoder match; ../nono-py/src/undo.rs
    — grep `denial_category` for the decoder match; crates/nono/src/undo/types.rs (post-115-02
    shape — confirm exactly 10 variants remain, `InterceptHandshakeFailed` removed, `ALL` const
    present); 115-RESEARCH.md Q8 (byte-identity proof); 115-PATTERNS.md "D-10 encoder delete" and
    "D-10 decoder delete" sections.
  </read_first>
  <behavior>
    - Test (compile-level): the encoder/decoder no longer reference `InterceptHandshakeFailed` —
      any stale reference is a compile error since Plan 115-02 already removed the variant.
  </behavior>
  <action>
    In `../nono-py/src/proxy.rs`, delete the entire hand-written `match c { ... }` block building
    the `"denial_category"` dict entry. Replace with conversion through the enum's own serde
    derive — e.g. `event.denial_category.as_ref().map(|c| serde_json::to_value(c)).transpose()?`
    (or `serde_json::to_string(c)` with surrounding quotes trimmed) — do not hand-list strings.
    In `../nono-py/src/undo.rs`, delete the entire hand-written decoder `match s.as_str() { ... }`
    block (arms + `other => Err(...)` fallback). Replace with
    `serde_json::from_value::<nono::undo::NetworkAuditDenialCategory>(...)` (or an equivalent
    `from_str` on the quoted string), mapping any deserialize error to
    `PyValueError::new_err(format!("invalid denial_category: {}", ...))` — preserve this exact
    error-message shape for continuity even though the matching mechanism changes.
  </action>
  <acceptance_criteria>
    - `grep -c "InterceptHandshakeFailed" ../nono-py/src/proxy.rs ../nono-py/src/undo.rs` returns 0.
    - `grep -c "\"authentication_failed\" =>" ../nono-py/src/undo.rs` returns 0 (hand-written
      decoder match fully deleted).
    - `cargo build -p nono-py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p nono-py</automated>
  </verify>
  <done>Both hand-written matches deleted; encode/decode now flow entirely through the core enum's own serde derive.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Self-enumerating round-trip test (D-12) + maturin build</name>
  <files>../nono-py/src/undo.rs</files>
  <read_first>
    crates/nono-cli/src/profile/mod.rs — grep the `#[cfg(test)] #[allow(clippy::unwrap_used)] mod
    tests { use super::*; ...` preamble (`:4440-4442`) as the workspace-standard test-module
    shape; crates/nono/src/undo/types.rs — confirm the `ALL` const's exact name/visibility added
    in Plan 115-02; 115-PATTERNS.md "D-12's first Rust #[cfg(test)] mod tests block" section;
    115-RESEARCH.md "maturin build requirement (D-12)" (Q9).
  </read_first>
  <behavior>
    - Test: for every variant in `nono::undo::NetworkAuditDenialCategory::ALL`, encoding via
      Task 1's serde path and then decoding the result via Task 1's serde path returns the
      original variant unchanged (lossless round trip), with zero `ValueError`/decode failures.
  </behavior>
  <action>
    Add the first `#[cfg(test)] #[allow(clippy::unwrap_used)] mod tests { use super::*; ... }`
    block to `../nono-py/src/undo.rs` (this is the crate's first Rust-level test — its
    `Cargo.toml` already has `pyo3` with `auto-initialize` in `[dev-dependencies]` for exactly
    this purpose, no further interpreter bootstrap plumbing needed). Write a round-trip test that
    iterates `nono::undo::NetworkAuditDenialCategory::ALL` (the self-enumerating const from Plan
    115-02) rather than a hand-written list of variant names: for each variant, encode it through
    the same `serde_json`-based path Task 1 wired, then decode that value back through the same
    path, and assert the round trip is lossless. This makes the test automatically extend to any
    future variant the moment `ALL` grows.
    Then run `cargo test -p nono-py --lib` to confirm it passes, and run `maturin build` from
    `../nono-py` and confirm exit 0 — per Phase 114 D-14 precedent (this is the 5th consecutive
    occurrence of the lesson), only a real wheel build catches binding struct drift;
    `cargo test -p nono-py` alone is not sufficient evidence. Commit `../nono-py`'s changes
    separately from `nono`'s, with its own DCO sign-off trailer
    (`Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`).
  </action>
  <acceptance_criteria>
    - The round-trip test iterates `NetworkAuditDenialCategory::ALL`, not a hand-written list.
    - `cargo test -p nono-py --lib` exits 0.
    - `maturin build` (run from `../nono-py`) exits 0.
    - `../nono-py`'s commit for this plan carries a `Signed-off-by:` trailer.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-py --lib && cd ../nono-py && maturin build</automated>
  </verify>
  <done>First Rust test in nono-py proves every category the encoder can emit decodes without raising, self-enumerated from the core enum; wheel builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core `nono` crate (source of truth for audit vocabulary) <-> nono-py binding (Python embedder-facing) | the binding must never diverge from the core enum's own serialization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-13 | Tampering / drift | hand-maintained decoder list falling out of sync with the encoder (DRAIN-02's root cause) | mitigate | D-10 deletes both hand-written lists; D-12's round-trip test is self-enumerating from the core enum's own `ALL`, so the class cannot recur without the test catching it |
| T-115-14 | Denial of Service (of the binding's usability, not the sandbox) | a Python embedder receiving `ValueError` on a legitimate denial-category string it should be able to decode (the literal NEW-06 defect) | mitigate | closed by construction — decode now derives from the same serde the encoder uses, not a second copy |
| T-115-SC | Tampering (supply chain) | no new package installs in this plan (`serde_json` already a workspace dependency in both repos) | accept | no legitimacy-gate task required |
</threat_model>

<verification>
Full-plan gate: `cargo test -p nono-py --lib && (cd ../nono-py && maturin build) && cargo fmt --all -- --check` (run from `../nono-py` for fmt if its own fmt config differs)
</verification>

<success_criteria>
- Every denial category the encoder can emit decodes without raising.
- The round-trip proof is self-enumerating from the core enum, not a second hand-written list.
- `maturin build` succeeds.
</success_criteria>

<output>
Create `.planning/phases/115-v3-6-carry-forward-drain/115-05-SUMMARY.md` when done
</output>
