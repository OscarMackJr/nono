---
phase: 83-machine-policy-spine-egress-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono/Cargo.toml
  - crates/nono/src/machine_policy.rs
  - crates/nono/src/error.rs
  - crates/nono/src/lib.rs
  - crates/nono/src/net_filter.rs
autonomous: false
requirements: [POLICY-01, POLICY-02, EGRESS-03]
user_setup: []

must_haves:
  truths:
    - "A present-but-absent HKLM policy key makes the reader return Ok(None) (fall through to per-user)"
    - "A present-but-unreadable (ACCESS_DENIED) or malformed key makes the reader return Err(NonoError::PolicyLoadFailed)"
    - "The reader opens the 64-bit registry view (KEY_WOW64_64KEY) regardless of process bitness"
    - "MachineEgressPolicy is a platform-neutral serde type that compiles on Linux and macOS"
    - "api.anthropic.com matches *.anthropic.com; anthropic.com, evilanthropic.com, anthropic.com.evil.com are all rejected"
  artifacts:
    - path: "crates/nono/src/machine_policy.rs"
      provides: "MachineEgressPolicy type + read_machine_egress_policy() fail-secure reader"
      contains: "pub struct MachineEgressPolicy"
    - path: "crates/nono/src/error.rs"
      provides: "NonoError::PolicyLoadFailed variant"
      contains: "PolicyLoadFailed"
    - path: "crates/nono/src/net_filter.rs"
      provides: "SC-4 DNS-component reject-matrix test"
      contains: "sc4_dns_component_matrix"
  key_links:
    - from: "crates/nono/src/lib.rs"
      to: "crates/nono/src/machine_policy.rs"
      via: "pub mod + pub use re-export"
      pattern: "pub use machine_policy::"
    - from: "crates/nono/src/machine_policy.rs"
      to: "winreg RegKey (Windows arm only)"
      via: "open_subkey_with_flags(KEY_READ | KEY_WOW64_64KEY)"
      pattern: "KEY_WOW64_64KEY"
---

<objective>
Build the core-library spine that every other Phase 83 plan reads from: the platform-neutral `MachineEgressPolicy` type, the `NonoError::PolicyLoadFailed` fail-secure error variant, the Windows-cfg-gated `winreg` registry reader implementing the D-07 absent/unreadable/malformed taxonomy, and the SC-4 DNS-component reject-matrix test that codifies EGRESS-03.

Purpose: This is the single source of truth (D-04/D-05) that Plan 02 wires into both the proxy and WFP layers. The type must be platform-neutral so the workspace cross-compiles on Linux/macOS (Pitfall 5); only the reader is cfg-gated.

Output: `crates/nono/src/machine_policy.rs` (new), `NonoError::PolicyLoadFailed` (error.rs), lib.rs re-export, `winreg` Windows-only dependency, and the named EGRESS-03 four-case test in net_filter.rs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-machine-policy-spine-egress-control/83-CONTEXT.md
@.planning/phases/83-machine-policy-spine-egress-control/83-RESEARCH.md
@.planning/phases/83-machine-policy-spine-egress-control/83-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase — use directly, no exploration needed. -->

From crates/nono/src/net_filter.rs (the matcher Plan 02 will feed; already SC-4-safe):
```
pub enum FilterResult { Allow, DenyHost{host}, DenyLinkLocal{ip}, DenyNotAllowed{host} }
impl FilterResult { pub fn is_allowed(&self) -> bool; }
impl HostFilter {
    pub fn new(allowed_hosts: &[String]) -> Self;          // "*.x.com" stored as ".x.com" suffix
    pub fn new_strict(allowed_hosts: &[String]) -> Self;   // empty allowlist => deny all
    pub fn check_host(&self, host: &str, resolved_ips: &[IpAddr]) -> FilterResult;
}
// wildcard match (net_filter.rs:221): lower_host.ends_with(suffix) && lower_host.len() > suffix.len()
// existing test helper net_filter.rs:248: fn public_ip() -> Vec<IpAddr>
```

From crates/nono-cli/src/health.rs (Phase-82 reference taxonomy on the SAME HKLM key):
```
// reg query stderr-matched: success => Ok; "access denied" => unreadable; else => not_configured.
// Phase 83 does this TYPED via winreg raw_os_error(): 2 (ERROR_FILE_NOT_FOUND) => Ok(None);
// 5 (ERROR_ACCESS_DENIED) / wrong REG type => Err(PolicyLoadFailed).
```

From crates/nono/src/error.rs (existing struct-variant pattern to mirror — LabelApplyFailed at error.rs:206):
```
#[error("Failed to apply integrity label to {path}: {hint} (HRESULT: 0x{hresult:08X})")]
LabelApplyFailed { path: PathBuf, hresult: u32, hint: String },
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify winreg package legitimacy before adding the dependency</name>
  <read_first>
    - .planning/phases/83-machine-policy-spine-egress-control/83-RESEARCH.md (## Package Legitimacy Audit — winreg is [ASSUMED], slopcheck not run)
  </read_first>
  <files>crates/nono/Cargo.toml (the winreg dependency this checkpoint gates — added in Task 2)</files>
  <what-built>RESEARCH.md tags `winreg = "0.56"` as `[ASSUMED]` (slopcheck unavailable). This checkpoint gates the dependency add per the package-legitimacy protocol — it is never auto-approvable.</what-built>
  <action>Pause before any winreg dependency add. Present the verification steps below to the operator and wait for explicit approval. Do NOT add winreg to Cargo.toml until the operator confirms the crate provenance. This is a blocking-human gate (never auto-approvable, workflow.auto_advance ignored).</action>
  <how-to-verify>
    1. Open https://crates.io/crates/winreg in a browser.
    2. Confirm the source repository resolves to `github.com/gentoo90/winreg-rs`.
    3. Confirm the current published version is `0.56.x` and the crate has high download counts (millions, transitive dep of many crates).
    4. Confirm it is not a recently-published typosquat (account age, version history present).
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms winreg @ crates.io resolves to gentoo90/winreg-rs at version 0.56.x with established download history.
    - Operator types "approved" (or describes the discrepancy, which blocks the dependency add).
  </acceptance_criteria>
  <verify>Operator has typed "approved" confirming winreg @ crates.io = gentoo90/winreg-rs @ 0.56.x with established download history.</verify>
  <done>winreg provenance confirmed by the operator; Task 2 may add the dependency. If not approved, the dependency add is blocked and the discrepancy is recorded.</done>
  <resume-signal>Type "approved" to proceed with adding winreg, or describe the issue found.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MachineEgressPolicy type + PolicyLoadFailed error + winreg fail-secure reader</name>
  <files>crates/nono/Cargo.toml, crates/nono/src/error.rs, crates/nono/src/machine_policy.rs, crates/nono/src/lib.rs</files>
  <read_first>
    - crates/nono/src/error.rs (LabelApplyFailed struct-variant pattern at ~line 206; tests at ~line 398)
    - crates/nono/src/lib.rs (existing `pub mod net_filter;` + `pub use net_filter::{FilterResult, HostFilter};` re-export lines ~55, ~81)
    - crates/nono-cli/src/health.rs (probe_machine_policy_windows ~line 375-417 — present/unreadable/absent reference semantics)
    - crates/nono-cli/src/windows_wfp_contract.rs (WfpRuntimeActivationRequest — platform-neutral serde-derive struct shape to copy)
    - .planning/phases/83-machine-policy-spine-egress-control/83-RESEARCH.md (Pattern 1 winreg arm + Code Examples read_list_subkey + Pitfall 1 ADMX list shape)
  </read_first>
  <behavior>
    - Test: deserializing an empty `MachineEgressPolicy` (no suffixes/hosts/presets) succeeds and yields an empty expanded allowlist.
    - Test: `MachineEgressPolicy` round-trips through serde_json (serialize then deserialize equals original).
    - Test: the non-Windows stub `read_machine_egress_policy()` returns `Ok(None)` (compiles + runs on Linux/macOS CI).
    - Test: `NonoError::PolicyLoadFailed { reason }` Display contains the reason string; `matches!(err, NonoError::PolicyLoadFailed { .. })` holds; it propagates through the `Result<T>` alias.
    - Test (Windows-gated, `#[cfg(target_os = "windows")]`): seed a temp subkey under a writable hive (HKEY_CURRENT_USER redirect, since HKLM\Policies needs admin) with one REG_SZ list entry; the list-subkey enumerator returns it. Seed a wrong-REG-type value; the enumerator returns Err mapped to PolicyLoadFailed.
  </behavior>
  <action>
    Add `winreg = "0.56"` ONLY under the `[target.'cfg(windows)'.dependencies]` table in crates/nono/Cargo.toml (D-10, Pitfall 5) — never an unconditional dependency. Add `NonoError::PolicyLoadFailed { reason: String }` to error.rs mirroring the LabelApplyFailed struct-variant shape, with `#[error("Machine policy load failed: {reason}")]` and a fail-closed doc comment stating that once the HKLM key exists ANY read/parse error aborts (never fall through), distinct from absent.

    Create crates/nono/src/machine_policy.rs. Define `pub struct MachineEgressPolicy` deriving `Debug, Clone, PartialEq, Eq, Serialize, Deserialize` with PLATFORM-NEUTRAL fields only (Vec<String> for allowed suffixes, Vec<String> for allowed hosts, Vec<String> for preset tokens) — no Windows types in the struct (D-05, Pitfall 5). Structure it so Phase 84 can later add a telemetry section to the SAME read without re-architecting (leave the type extensible). Add a method that returns the raw allowlist entries (suffixes + hosts) as `Vec<String>`; preset-token expansion happens in the CLI (Plan 03), so this lib type only carries the tokens — do NOT embed the token→FQDN map here.

    Implement the Windows arm of `pub fn read_machine_egress_policy() -> Result<Option<MachineEgressPolicy>>` behind `#[cfg(target_os = "windows")]`: open `HKEY_LOCAL_MACHINE` subkey `r"SOFTWARE\Policies\nono"` with flags `KEY_READ | KEY_WOW64_64KEY` (D-09 — 64-bit view regardless of bitness). Map `e.raw_os_error() == Some(2)` (ERROR_FILE_NOT_FOUND) to `Ok(None)` (absent → fall through, D-07). Every OTHER open error (ACCESS_DENIED=5, etc.) returns `Err(NonoError::PolicyLoadFailed { reason })`. Enumerate the `AllowedSuffixes\` and `AllowedHosts\` subkeys as N×REG_SZ values via `open_subkey_with_flags(name, KEY_READ | KEY_WOW64_64KEY).enum_values()` (D-13 Option A — ADMX `<list>` materializes as separate REG_SZ values, NOT REG_MULTI_SZ; Pitfall 1). A list subkey being absent yields an empty Vec (the parent key existing is what gates enforcement). Any value with a non-REG_SZ type or bad UTF-16 is MALFORMED → `Err(PolicyLoadFailed)` (D-07 — malformed == unreadable == abort). Read preset toggle values likewise. Never use `unwrap_or`/`unwrap_or_default`/`.ok()` on any read — propagate with `?` (Pitfall 3, CLAUDE.md footgun #2). Add the `#[cfg(not(target_os = "windows"))]` stub returning `Ok(None)`.

    In lib.rs add `pub mod machine_policy;` and `pub use machine_policy::{MachineEgressPolicy, read_machine_egress_policy};` mirroring the existing net_filter re-export.
  </action>
  <verify>
    <automated>cargo test -p nono machine_policy && cargo test -p nono --lib error</automated>
  </verify>
  <acceptance_criteria>
    - `crates/nono/src/machine_policy.rs` contains `pub struct MachineEgressPolicy` and `pub fn read_machine_egress_policy`.
    - `crates/nono/src/error.rs` contains `PolicyLoadFailed`.
    - `crates/nono/Cargo.toml` has `winreg` ONLY under `[target.'cfg(windows)'.dependencies]` (grep `winreg` shows no unconditional dependency line).
    - `crates/nono/src/lib.rs` contains `pub use machine_policy::`.
    - `cargo test -p nono machine_policy` exits 0 (the non-Windows stub + serde round-trip + error tests run on the dev host).
    - No `.unwrap()`/`.expect()`/`unwrap_or` in machine_policy.rs outside `#[cfg(test)]`.
  </acceptance_criteria>
  <done>The MachineEgressPolicy type, PolicyLoadFailed error, fail-secure winreg reader (Windows arm + non-Windows stub), and lib re-export all exist; lib tests pass on the dev host.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SC-4 DNS-component reject-matrix test (EGRESS-03)</name>
  <files>crates/nono/src/net_filter.rs</files>
  <read_first>
    - crates/nono/src/net_filter.rs (HostFilter::check_host ~line 188-231; existing wildcard tests test_wildcard_does_not_match_bare_domain ~line 288; public_ip helper ~line 248)
    - .planning/phases/83-machine-policy-spine-egress-control/83-RESEARCH.md (Code Examples — test_sc4_dns_component_matrix)
  </read_first>
  <behavior>
    - Test `sc4_dns_component_matrix` with allowlist `["*.anthropic.com"]`: `api.anthropic.com` is allowed; `anthropic.com` is rejected; `evilanthropic.com` is rejected; `anthropic.com.evil.com` is rejected.
  </behavior>
  <action>
    Add a `#[test] fn sc4_dns_component_matrix()` inside the existing `#[cfg(test)] mod tests` in net_filter.rs, using `HostFilter::new_strict(&["*.anthropic.com".to_string()])` and the existing `public_ip()` helper. Assert `check_host("api.anthropic.com", &public_ip()).is_allowed()` is true, and that `anthropic.com`, `evilanthropic.com`, and `anthropic.com.evil.com` each return `!is_allowed()`. Do NOT rebuild the matcher (D-14) — the existing leading-dot `ends_with` + `len >` form already passes; this codifies the EGRESS-03 contract as a named test. (Claude's discretion D-14: optionally harden check_host to explicit `.split('.')` label comparison, but only if this matrix still passes and the fail-secure default is preserved.)
  </action>
  <verify>
    <automated>cargo test -p nono net_filter::tests::sc4_dns_component_matrix</automated>
  </verify>
  <acceptance_criteria>
    - `crates/nono/src/net_filter.rs` contains a test named `sc4_dns_component_matrix`.
    - `cargo test -p nono net_filter::tests::sc4_dns_component_matrix` exits 0.
    - The test asserts exactly: api.anthropic.com allowed; anthropic.com / evilanthropic.com / anthropic.com.evil.com rejected.
  </acceptance_criteria>
  <done>The named EGRESS-03 four-case reject matrix exists as a passing test in net_filter.rs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HKLM machine policy → nono process | Admin-controlled registry data crosses into the security-decision path; malformed/corrupt input must fail secure |
| WOW64 32-bit deployment → 64-bit read | A 32-bit Intune MDM write could land in WOW6432Node; the read must force the 64-bit view |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-malformed-failopen | Tampering / EoP | read_machine_egress_policy() | mitigate (block-level) | D-07: ERROR_FILE_NOT_FOUND(2)→Ok(None); every other error (ACCESS_DENIED=5, wrong REG type, bad UTF-16)→Err(PolicyLoadFailed). No unwrap_or/.ok() on the read path (Pitfall 3). |
| T-83-wow64-redirect | Tampering | winreg open_subkey_with_flags | mitigate | open with KEY_WOW64_64KEY (D-09) so a 32-bit-deployed policy in WOW6432Node cannot silently make the read see "absent" (Pitfall 2). |
| T-83-dns-component | Spoofing | HostFilter wildcard match (EGRESS-03) | mitigate | Leading-dot suffix + length guard rejects evilanthropic.com / anthropic.com.evil.com; codified by sc4_dns_component_matrix. Ambiguity fails secure (deny). |
| T-83-SC | Tampering | winreg crate install | mitigate | Task 1 blocking-human checkpoint verifies winreg @ crates.io = gentoo90/winreg-rs (slopcheck not run; [ASSUMED]). |
</threat_model>

<verification>
- `cargo test -p nono machine_policy` exits 0 (taxonomy + serde round-trip + non-Windows stub).
- `cargo test -p nono net_filter::tests::sc4_dns_component_matrix` exits 0 (EGRESS-03).
- `grep -n winreg crates/nono/Cargo.toml` shows the dep ONLY under `[target.'cfg(windows)'.dependencies]`.
- Cross-target clippy is verified centrally in Plan 04 (this plan adds the cfg-gated winreg/reader code that Plan 04's gate covers).
</verification>

<success_criteria>
- POLICY-01: reader opens HKLM with KEY_WOW64_64KEY; absent→Ok(None), present→Some.
- POLICY-02: present-but-unreadable/malformed→Err(NonoError::PolicyLoadFailed), never fall-through.
- EGRESS-03: sc4_dns_component_matrix passes (api.anthropic.com allowed; bare/evil/suffix-injection rejected).
- MachineEgressPolicy is platform-neutral and compiles on non-Windows targets (stub returns Ok(None)).
</success_criteria>

<output>
Create `.planning/phases/83-machine-policy-spine-egress-control/83-01-SUMMARY.md` when done.
</output>
