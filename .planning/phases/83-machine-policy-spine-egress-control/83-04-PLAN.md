---
phase: 83-machine-policy-spine-egress-control
plan: 04
type: execute
wave: 3
depends_on: ["83-02"]
files_modified:
  - scripts/gates/egress-policy-deny.ps1
  - scripts/verify-dark.ps1
autonomous: true
requirements: [POLICY-02, EGRESS-02]
user_setup: []

must_haves:
  truths:
    - "verify-dark.ps1 --gate egress-policy-deny runs the new gate and emits a machine-readable verdict (PASS/FAIL/SKIP_HOST_UNAVAILABLE)"
    - "On the corrupted/permission-denied machine-policy key, the gate asserts a NON-ZERO exit from the startup path (SC-2 fail-secure proof)"
    - "Under a machine policy of only *.anthropic.com, the gate asserts dual-layer deny: proxy rejects an out-of-list host AND the per-SID WFP block filter is present (SC-3)"
    - "On a host without admin / nono-wfp-control / nono-agentd-control, the gate cleanly returns SKIP_HOST_UNAVAILABLE (never a false PASS)"
    - "Cross-target clippy (Linux + macOS) is run for the Phase-83 cfg-gated Unix-touching code, or the verify REQ is marked PARTIAL and deferred to live CI per the checklist"
  artifacts:
    - path: "scripts/gates/egress-policy-deny.ps1"
      provides: "Dark Factory gate (Test-Precondition + Invoke-Gate two-function contract)"
      contains: "Invoke-Gate"
    - path: "scripts/verify-dark.ps1"
      provides: "egress-policy-deny registered in the gate dispatch"
      contains: "egress-policy-deny"
  key_links:
    - from: "scripts/verify-dark.ps1"
      to: "scripts/gates/egress-policy-deny.ps1"
      via: "--gate egress-policy-deny dispatch"
      pattern: "egress-policy-deny"
    - from: "egress-policy-deny.ps1 Invoke-Gate"
      to: "the nono startup path under a corrupted key"
      via: "asserts non-zero exit (SC-2)"
      pattern: "exit"
---

<objective>
Ship the `egress-policy-deny` Dark Factory gate (the SC-2 + SC-3 dark proof that fail-secure and dual-layer deny are wired, not just coded) and run the mandatory cross-target clippy verification for the Phase-83 cfg-gated Unix-touching code.

Purpose: Per the Dark Factory mandate, every phase ships an unattended `verify-dark.ps1` gate that collapses the host-gated proof to a single machine-readable verdict; the milestone closes on the aggregator. The cross-target clippy task closes the recurring v2.x trap where Windows-host `cargo check` masks cfg-gated Linux/macOS drift (CLAUDE.md MUST rule).

Output: `scripts/gates/egress-policy-deny.ps1` (new, cloned from the WFP-01 gate's two-function contract), its registration in `verify-dark.ps1`, and a recorded cross-target clippy result (PASS or PARTIAL-deferred).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-machine-policy-spine-egress-control/83-CONTEXT.md
@.planning/phases/83-machine-policy-spine-egress-control/83-RESEARCH.md
@.planning/phases/83-machine-policy-spine-egress-control/83-PATTERNS.md
@.planning/phases/83-machine-policy-spine-egress-control/83-02-SUMMARY.md
@.planning/templates/cross-target-verify-checklist.md

<interfaces>
<!-- Gate contract — clone from scripts/gates/wfp-egress-isolation.ps1 (structural twin). -->
```
# Two-function contract (the runner verify-dark.ps1 owns exit mapping: PASS=0/FAIL=2/SKIP=3/internal=4):
function Test-Precondition {
    # return $null when met; return a reason STRING -> SKIP_HOST_UNAVAILABLE
    #   1. admin check (netsh wfp show filters needs elevation)
    #   2. \\.\pipe\nono-wfp-control reachable
    #   3. \\.\pipe\nono-agentd-control reachable
}
function Invoke-Gate {
    # returns [ordered]@{ gate; verdict; reason; detail; timestamp }
    # verdict in { 'PASS' | 'FAIL' | 'SKIP_HOST_UNAVAILABLE' }
    # `throw` = harness-internal error (exit 4), never a silent PASS
}
# SID helpers to clone from wfp-egress-isolation.ps1:
#   Get-NonoBlockSids  (parses `netsh wfp show filters` for ALE_USER_ID block filters)
#   Get-LaunchSid      (parses sid=S-1-15-2-... from the launch response)
```
INVOCATION RULE (MEMORY durable): run via `pwsh -File scripts/verify-dark.ps1 --gate egress-policy-deny`,
NEVER `pwsh -Command "<bare path>"` (swallows exit N -> 1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: egress-policy-deny Dark Factory gate (SC-2 + SC-3)</name>
  <files>scripts/gates/egress-policy-deny.ps1, scripts/verify-dark.ps1</files>
  <read_first>
    - scripts/gates/wfp-egress-isolation.ps1 (the WFP-01 structural twin: Test-Precondition + Invoke-Gate; Get-NonoBlockSids; Get-LaunchSid; verdict object shape)
    - scripts/verify-dark.ps1 (how gates are dispatched/registered by name; the exit mapping PASS=0/FAIL=2/SKIP=3/internal=4)
    - .planning/phases/83-machine-policy-spine-egress-control/83-RESEARCH.md (Validation Architecture SC-2/SC-3 rows; Pitfall 3 corrupted-key non-zero exit)
    - .planning/phases/83-machine-policy-spine-egress-control/83-PATTERNS.md (egress-policy-deny.ps1 gate section)
  </read_first>
  <action>
    Create scripts/gates/egress-policy-deny.ps1 exporting EXACTLY `Test-Precondition` and `Invoke-Gate`, returning a verdict object — never calling `exit` or persisting the verdict (the runner owns that). `Test-Precondition` returns `$null` when met, else a reason string → SKIP_HOST_UNAVAILABLE: check (1) admin elevation (needed for `netsh wfp show filters`), (2) `\\.\pipe\nono-wfp-control` reachable, (3) `\\.\pipe\nono-agentd-control` reachable. `Invoke-Gate` performs two assertions:
    - SC-2 (fail-secure non-zero exit): seed a present-but-corrupted machine-policy key (either an ACCESS_DENIED ACL or a wrong-REG-type value under a test-scoped `HKLM\SOFTWARE\Policies\nono` location), run the nono startup path against it, and assert a NON-ZERO exit (the PolicyLoadFailed abort, Pitfall 3). A clean (zero) exit here is a FAIL verdict. Restore/clean up the seeded key afterward.
    - SC-3 (dual-layer deny): install a machine policy of only `*.anthropic.com`, launch a confined agent through the daemon, then assert BOTH (a) the proxy denies a request to an out-of-list host AND (b) `netsh wfp show filters` shows the per-SID block filter for the launched AppContainer SID (clone Get-NonoBlockSids + Get-LaunchSid). Both must hold for PASS.
    Use `throw` only for harness-internal errors (exit 4), never as a silent PASS. Register `egress-policy-deny` in verify-dark.ps1's gate dispatch alongside the existing gates (deploy-silent-install, wfp-egress-isolation, etc.). Add the gate to the no-flag aggregator set so the milestone close picks it up.
  </action>
  <verify>
    <automated>pwsh -File scripts/verify-dark.ps1 --gate egress-policy-deny</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/gates/egress-policy-deny.ps1` defines `Test-Precondition` and `Invoke-Gate` and never calls `exit`/persist directly.
    - `scripts/verify-dark.ps1` dispatches `--gate egress-policy-deny` (grep shows the registration).
    - On the dev host without the services/admin, `pwsh -File scripts/verify-dark.ps1 --gate egress-policy-deny` returns SKIP_HOST_UNAVAILABLE with a clean (non-error) exit — NOT a false PASS and NOT exit 4.
    - The gate body contains the SC-2 corrupted-key non-zero-exit assertion and the SC-3 dual-layer (proxy deny + WFP block-SID) assertion.
  </acceptance_criteria>
  <done>The egress-policy-deny gate exists with the two-function contract, asserts SC-2 + SC-3, is registered in verify-dark.ps1, and SKIPs cleanly on a host without prerequisites.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-target clippy verification for cfg-gated Unix code</name>
  <files>scripts/gates/egress-policy-deny.ps1</files>
  <read_first>
    - .planning/templates/cross-target-verify-checklist.md (the mandatory Linux+macOS clippy protocol and the PARTIAL-deferred fallback)
    - CLAUDE.md (Cross-target clippy verification bullet — Windows-host cargo check is NOT a substitute)
  </read_first>
  <action>
    Run the mandatory cross-target clippy for the Phase-83 cfg-gated Unix-touching code (the new winreg/reader in crates/nono with its `#[cfg(not(target_os = "windows"))]` stub, plus the daemon edits): `cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used` AND `cargo clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used`, from the dev host. If both pass, record PASS in the SUMMARY. If the cross-toolchain is not installed on the Windows dev host (expected per MEMORY — the host cannot cross-compile non-Windows), mark the cross-target verification REQ PARTIAL and defer to live CI per .planning/templates/cross-target-verify-checklist.md, recording the deferral explicitly in the SUMMARY (this is the documented, acceptable outcome on a Windows-only host — Windows `cargo check` is NOT a substitute). Confirm the non-Windows stub `read_machine_egress_policy()` returns `Ok(None)` so the workspace compiles on Linux/macOS (this is what CI will exercise).
  </action>
  <verify>
    <automated>cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used; echo "exit=$? (PARTIAL-deferred acceptable if toolchain absent — record in SUMMARY)"</automated>
  </verify>
  <acceptance_criteria>
    - Either: both `--target x86_64-unknown-linux-gnu` and `--target x86_64-apple-darwin` clippy runs pass with `-D warnings -D clippy::unwrap_used` (recorded PASS), OR the cross-toolchain is absent and the SUMMARY records the REQ as PARTIAL-deferred-to-CI per the checklist.
    - The SUMMARY explicitly states the cross-target outcome (PASS or PARTIAL-deferred) — never silently skipped.
    - The non-Windows stub path is confirmed to return Ok(None) (the workspace compiles for Linux/macOS in CI).
  </acceptance_criteria>
  <done>Cross-target clippy is run (or explicitly PARTIAL-deferred to CI per the checklist) and the outcome is recorded; the Windows-host cargo check is not treated as a substitute.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Corrupted HKLM key → startup path | The gate must prove that a corrupt key produces a non-zero exit (no silent permissive startup) |
| Host without services/admin → gate verdict | The gate must SKIP cleanly, never emit a false PASS |
| Windows-host build → Linux/macOS CI | cfg-gated Unix code must be lint-verified cross-target, not just on the Windows host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-malformed-failopen | Tampering / EoP | egress-policy-deny SC-2 assertion | mitigate (block-level) | Gate seeds a corrupted key and asserts non-zero exit; a zero exit is a FAIL verdict — the dark proof that fail-secure is wired, not just coded (Pitfall 3). |
| T-83-proxy-bypass | Bypass | egress-policy-deny SC-3 assertion | mitigate (block-level) | Gate asserts both proxy deny AND per-SID WFP block-filter presence under a single-domain policy (dual-layer). |
| T-83-false-pass | Repudiation | gate verdict | mitigate | Test-Precondition returns SKIP_HOST_UNAVAILABLE when admin/pipes absent; `throw`→exit 4 for harness errors; never a silent PASS. |
| T-83-crosstarget-drift | Tampering | cfg-gated winreg/reader | mitigate | Cross-target clippy (Linux+macOS) or explicit PARTIAL-deferred-to-CI; Windows cargo check is not a substitute (CLAUDE.md MUST). |
</threat_model>

<verification>
- `pwsh -File scripts/verify-dark.ps1 --gate egress-policy-deny` returns a verdict (SKIP_HOST_UNAVAILABLE on the dev host without services; PASS on a fully provisioned host-gated box).
- `grep -n egress-policy-deny scripts/verify-dark.ps1` shows the dispatch registration.
- Cross-target clippy run recorded (PASS or PARTIAL-deferred) in the SUMMARY.
</verification>

<success_criteria>
- POLICY-02: SC-2 gate asserts non-zero exit on the corrupted/permission-denied key.
- EGRESS-02: SC-3 gate asserts dual-layer deny (proxy + per-SID WFP block) from the same policy.
- Cross-target clippy verified or explicitly deferred to CI per the checklist.
</success_criteria>

<output>
Create `.planning/phases/83-machine-policy-spine-egress-control/83-04-SUMMARY.md` when done.
</output>
