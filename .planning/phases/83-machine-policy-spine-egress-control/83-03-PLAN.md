---
phase: 83-machine-policy-spine-egress-control
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono-cli/data/network-policy.json
  - crates/nono-cli/src/policy.rs
  - scripts/build-windows-msi.ps1
autonomous: true
requirements: [EGRESS-04]
decisions_covered: [D-11, D-12]
user_setup: []

must_haves:
  truths:
    - "network-policy.json carries AI-provider preset groups whose hosts include *.anthropic.com, *.openai.com, and api.github.com"
    - "A preset group TOKEN (e.g. anthropic) expands to its wildcard-FQDN host list via the embedded network-policy.json map"
    - "The generated ADMX exposes named preset toggles that write a stable group TOKEN (not literal FQDNs) into machine policy"
    - "Covers D-11: ADMX named toggles write group TOKENS (anthropic/openai/github-api), not literal FQDNs; nono expands token->FQDN"
    - "Covers D-12: preset token->FQDN map lives in the embedded network-policy.json group mechanism (CLI-side, not the core lib)"
  artifacts:
    - path: "crates/nono-cli/data/network-policy.json"
      provides: "egress preset groups (anthropic / openai / github-api)"
      contains: "*.anthropic.com"
    - path: "crates/nono-cli/src/policy.rs"
      provides: "preset token -> FQDN expansion from embedded network-policy.json"
      contains: "expand"
    - path: "scripts/build-windows-msi.ps1"
      provides: "ADMX named-toggle preset policies (here-string source)"
      contains: "Anthropic"
  key_links:
    - from: "ADMX named toggle"
      to: "network-policy.json group token"
      via: "toggle writes a group TOKEN value into HKLM\\SOFTWARE\\Policies\\nono"
      pattern: "anthropic"
    - from: "crates/nono-cli/src/policy.rs"
      to: "crates/nono-cli/data/network-policy.json"
      via: "embedded_network_policy_json() group lookup"
      pattern: "embedded_network_policy_json"
---

<objective>
Ship the AI-provider egress presets (EGRESS-04): add `anthropic` / `openai` / `github-api` preset groups to the embedded `network-policy.json`, implement token→FQDN expansion that Plan 02 will call, and add named ADMX toggles (in the generated-from-here-string `build-windows-msi.ps1` source) that write a stable group TOKEN — not literal FQDNs — so provider FQDN lists can be updated in nono without re-issuing the ADMX fleet-wide (D-11/D-12).

Purpose: Default-deny egress must not brick the agent's required provider traffic. The token-indirection (D-11) decouples the fleet-pushed ADMX template from the FQDN lists nono owns.

Output: preset groups in network-policy.json, an expansion function in policy.rs, and ADMX named-toggle here-strings + a contract assertion.

CRITICAL CORRECTION (from 83-PATTERNS.md): the token→FQDN home is `crates/nono-cli/data/network-policy.json` (domain `hosts[]` groups), NOT the filesystem `crates/nono-cli/data/policy.json`. D-12's reference to policy.json is corrected here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-machine-policy-spine-egress-control/83-CONTEXT.md
@.planning/phases/83-machine-policy-spine-egress-control/83-RESEARCH.md
@.planning/phases/83-machine-policy-spine-egress-control/83-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase — use directly. -->

From crates/nono-cli/data/network-policy.json (existing domain-group schema to extend):
```
{
  "meta": { "version": 1, "schema_version": "1.0" },
  "groups": {
    "llm_apis": { "description": "...", "hosts": ["api.openai.com", "api.anthropic.com", ...] },
    "package_registries": { "description": "...", "hosts": [...] }
  },
  "profiles": { ... }
}
```

From crates/nono-cli/src/config/embedded.rs:
```
pub fn embedded_network_policy_json() -> &'static str;  // network-policy.json, include_str! from OUT_DIR
```

From scripts/build-windows-msi.ps1 (ADMX is GENERATED from here-strings — edit the script, not dist/windows/nono.admx):
```
# shipped Phase-82 <list> policies: AllowedSuffixes, AllowedHosts under HKLM\SOFTWARE\Policies\nono
# new named toggles go alongside these in the same here-string source
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AI-provider preset groups + token expansion (EGRESS-04)</name>
  <files>crates/nono-cli/data/network-policy.json, crates/nono-cli/src/policy.rs</files>
  <read_first>
    - crates/nono-cli/data/network-policy.json (existing `groups` schema with `description` + `hosts[]`)
    - crates/nono-cli/src/config/embedded.rs (embedded_network_policy_json loader ~line 24)
    - crates/nono-cli/src/policy.rs (existing group resolver: parse embedded JSON, filter, expand — copy its serde_json group-lookup pattern)
    - .planning/phases/83-machine-policy-spine-egress-control/83-PATTERNS.md (network-policy.json group-schema correction to D-12)
  </read_first>
  <behavior>
    - Test `policy_egress_groups`: the embedded network-policy.json deserializes and contains groups whose union of hosts includes `*.anthropic.com`, `*.openai.com`, and `api.github.com`.
    - Test token expansion: expanding the token `anthropic` returns a host list containing `*.anthropic.com`; expanding `openai` returns `*.openai.com`; expanding `github-api` returns `api.github.com`.
    - Test: expanding an unknown token returns an empty list (or an error per the resolver convention) — it never silently widens to all hosts.
  </behavior>
  <action>
    Add three new preset groups to the `groups` object in network-policy.json — `anthropic` (hosts: `*.anthropic.com`), `openai` (hosts: `*.openai.com`), `github-api` (hosts: `api.github.com`) — each with a `description`. These are wildcard-FQDN entries (EGRESS-01 shape) authored exactly as the HostFilter expects (`*.` prefix). Keep the existing `llm_apis` / `package_registries` groups intact.

    In policy.rs add a function that takes a slice of group TOKENS (the `&str` values an ADMX toggle writes) and returns the expanded `Vec<String>` of FQDN hosts by looking each token up in the embedded network-policy.json `groups` map (parse via `embedded_network_policy_json()` + serde_json, mirroring the existing group-resolver parse). Unknown tokens expand to nothing (never to all hosts — fail-secure/least-privilege). This function is what Plan 02 calls to turn `MachineEgressPolicy` preset tokens into the proxy allowlist (D-11/D-12). Do NOT put the token→FQDN map in the core nono lib (library is policy-free; CLAUDE.md boundary).
  </action>
  <verify>
    <automated>cargo test -p nono-cli policy_egress_groups</automated>
  </verify>
  <acceptance_criteria>
    - `network-policy.json` `groups` contains `anthropic`, `openai`, `github-api` with hosts `*.anthropic.com`, `*.openai.com`, `api.github.com` respectively.
    - `policy.rs` contains a token-expansion function reading from `embedded_network_policy_json()`.
    - `cargo test -p nono-cli policy_egress_groups` exits 0.
    - Unknown-token expansion returns an empty list (asserted by a test), never all hosts.
  </acceptance_criteria>
  <done>The three AI-provider preset groups exist in network-policy.json and token→FQDN expansion is implemented + tested.</done>
</task>

<task type="auto">
  <name>Task 2: ADMX named-toggle preset policies + contract assertion (EGRESS-04)</name>
  <files>scripts/build-windows-msi.ps1</files>
  <read_first>
    - scripts/build-windows-msi.ps1 (the ADMX here-string source — find the shipped Phase-82 AllowedSuffixes/AllowedHosts `<list>` policies and the .adml display-string here-string)
    - .planning/phases/83-machine-policy-spine-egress-control/83-PATTERNS.md (nono.admx named-toggle pattern + validate-windows-msi-contract.ps1 assertion style)
    - .planning/phases/83-machine-policy-spine-egress-control/83-RESEARCH.md (Pitfall 1 — keep `<list>` as N×REG_SZ; do NOT switch to REG_MULTI_SZ)
  </read_first>
  <action>
    In the ADMX here-string source inside build-windows-msi.ps1, add three named-toggle `<policy>` elements alongside the existing AllowedSuffixes/AllowedHosts `<list>` policies: "Allow Anthropic", "Allow OpenAI", "Allow GitHub API". Each is an enable/disable toggle (boolean `<enabledValue>`/`<disabledValue>` or a simple presence policy) that writes a stable group TOKEN value (`anthropic`, `openai`, `github-api`) into `HKLM\SOFTWARE\Policies\nono` when enabled (D-11 — token, NOT literal FQDNs). Add the matching `<string>` display entries to the .adml here-string. Do NOT edit dist/windows/nono.admx or .adml directly — they are regenerated from these here-strings on every build (MEMORY: windows_msi_wxs_is_generated). Keep the existing `<list>` policies as N×REG_SZ (Pitfall 1) — do not convert them to REG_MULTI_SZ. Add or extend a contract assertion (validate-windows-msi-contract.ps1 style) confirming the generated ADMX contains the three new toggle policy names and that the AllowedSuffixes/AllowedHosts list policies are still present.
  </action>
  <verify>
    <automated>grep -Eq 'Allow Anthropic' scripts/build-windows-msi.ps1 && grep -Eq 'Allow OpenAI' scripts/build-windows-msi.ps1 && grep -Eq 'Allow GitHub API' scripts/build-windows-msi.ps1 && grep -Eq '"anthropic"|>anthropic<' scripts/build-windows-msi.ps1</automated>
    <note>Dev-host-runnable source assertion (no Windows/PowerShell required): exits 0 only if all three toggle names AND the token value are present in the ADMX here-string. The richer generated-ADMX contract check (validate-windows-msi-contract.ps1, extended per the action) runs in Plan 04's gate cross-check on a Windows host.</note>
  </verify>
  <acceptance_criteria>
    - `scripts/build-windows-msi.ps1` here-string source contains policy elements named for "Anthropic", "OpenAI", and "GitHub API" toggles.
    - Each toggle writes a group TOKEN (`anthropic`/`openai`/`github-api`), not a literal FQDN (source assertion: the enabledValue/registry value is the token string).
    - The existing AllowedSuffixes/AllowedHosts `<list>` policies remain unchanged (still N×REG_SZ).
    - A contract assertion (validate-windows-msi-contract.ps1 style) checks for the three new toggle names.
  </acceptance_criteria>
  <done>Three named AI-provider toggles writing group tokens are present in the ADMX here-string source with a contract assertion; the shipped list policies are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADMX toggle → HKLM token value | Admin enabling a named toggle writes a TOKEN, not raw FQDNs; nono owns the token→FQDN mapping |
| Preset token → expanded allowlist | An unknown/typo token must not silently widen the allowlist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-token-widen | EoP / Tampering | policy.rs token expansion | mitigate | Unknown tokens expand to an empty host list (least-privilege), never to all hosts; asserted by test. |
| T-83-admx-shape | Tampering | ADMX `<list>` materialization | mitigate | Keep `<list>` as N×REG_SZ (Pitfall 1) so the Plan-01 reader's enumeration matches the shipped, fleet-pushable template — no silent empty-allowlist. |
| T-83-token-indirection | Spoofing | ADMX toggle value | accept | Toggle writes a stable token; provider FQDN lists live in nono (D-11), so a fleet ADMX cannot be coerced into arbitrary FQDNs — only the known preset tokens resolve. |
</threat_model>

<verification>
- `cargo test -p nono-cli policy_egress_groups` exits 0 (preset groups present + token expansion correct).
- `grep -c -E "Anthropic|OpenAI|GitHub" scripts/build-windows-msi.ps1` shows the three new toggles (use `grep -v '^#'` to avoid counting comments).
- ADMX generation + contract assertion run on Windows; cross-checked in Plan 04.
</verification>

<success_criteria>
- EGRESS-04: network-policy.json ships `*.anthropic.com` / `*.openai.com` / `api.github.com` as named preset groups; token→FQDN expansion works; ADMX exposes named toggles writing group tokens.
</success_criteria>

<output>
Create `.planning/phases/83-machine-policy-spine-egress-control/83-03-SUMMARY.md` when done.
</output>
