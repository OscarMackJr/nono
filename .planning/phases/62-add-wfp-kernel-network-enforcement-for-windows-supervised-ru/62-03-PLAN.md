---
phase: 62
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - REQ-WFP-01

must_haves:
  truths:
    - "REQ-WFP-01 is formally defined in REQUIREMENTS.md with acceptance criteria matching the 5 ROADMAP success criteria"
    - "ROADMAP.md Phase 62 section no longer says 'TBD' for Requirements or Plans"
    - "ROADMAP.md Coverage table maps REQ-WFP-01 to Phase 62"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-WFP-01 entry in a v2.9 WFP section"
      contains: "REQ-WFP-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 62 plan list (4 plans) and Requirements line"
      contains: "REQ-WFP-01"
  key_links:
    - from: "REQUIREMENTS.md REQ-WFP-01"
      to: "ROADMAP.md Phase 62 Requirements field"
      via: "Requirement ID reference"
      pattern: "REQ-WFP-01"
---

<objective>
Record the formal requirement introduced by D-07 and update the ROADMAP Phase 62 entry to reflect
the planned 4-plan structure. No code changes; this is a planning-artifact synchronization plan.

Purpose: REQ-WFP-01 must exist in REQUIREMENTS.md so the phase gate checker can verify it is
satisfied. ROADMAP.md Phase 62 currently says "Requirements: TBD" and "Plans: 0" — both must be
updated so orchestrator state is consistent with the actual plan structure.

Output: REQUIREMENTS.md with REQ-WFP-01; ROADMAP.md Phase 62 with Requirements, Plan count,
and plan list showing all 4 plans (62-01..62-04).
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\OMack\Nono\.planning\REQUIREMENTS.md
@C:\Users\OMack\Nono\.planning\ROADMAP.md
@C:\Users\OMack\Nono\.planning\phases\62-add-wfp-kernel-network-enforcement-for-windows-supervised-ru\62-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add REQ-WFP-01 to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    1. .planning/REQUIREMENTS.md — read the full file to understand the existing section structure (v1 Requirements, v2 Requirements, etc.) and find the right insertion point. The file is for v2.8 but REQ-WFP-01 is a v2.9-track requirement — append a new section after the last v1 requirement section.
    2. .planning/phases/62-add-wfp-kernel-network-enforcement-for-windows-supervised-ru/62-CONTEXT.md — §D-07 for the requirement title and §Decisions for what REQ-WFP-01 must cover.
    3. .planning/ROADMAP.md — Phase 62 §Success Criteria (lines ~164-169) — these are the 5 criteria REQ-WFP-01 must reference.
  </read_first>
  <action>
    Append a new section "### Windows Network Enforcement (WFP)" to REQUIREMENTS.md after the
    existing "### Supervisor IPC (IPC)" section (the last v1 requirement). Do NOT insert into
    the "## v2 Requirements (Deferred)" section.

    The new entry:

    - [ ] **REQ-WFP-01**: Out-of-box WFP operational enforcement for supervised runs on a
      machine-MSI-installed Windows host. A `network.block:true` supervised `nono run` enforces
      WFP kernel network filtering without any manual `nono setup --start-wfp-service` step.
      Specifically: (1) the machine MSI registers `nono-wfp-service` with `start=auto` so the
      SCM boot-starts it as SYSTEM; (2) the control-pipe SDDL grants non-elevated supervised-run
      sessions (Interactive Users) read+write access; (3) when the service is not running at
      enforcement time, nono attempts an auto-start and if that fails returns a fail-closed error
      naming the exact remediation command — never passes through unenforced; (4) clean
      uninstall via `msiexec /x` still leaves nothing behind. Closes Phase 60's F-60-UAT-03.
      (v2.9 track, Phase 62)
  </action>
  <verify>
    <automated>powershell -Command "if ((Get-Content .planning/REQUIREMENTS.md | Select-String 'REQ-WFP-01').Count -ge 1) { exit 0 } else { Write-Error 'REQ-WFP-01 not found'; exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md contains "REQ-WFP-01" as a checkbox item.
    - The entry describes out-of-box WFP enforcement, boot-start, non-elevated pipe access, fail-closed, and clean-uninstall.
    - The entry is NOT in the "## v2 Requirements (Deferred)" section.
  </acceptance_criteria>
  <done>
    REQ-WFP-01 appears in REQUIREMENTS.md with full acceptance criteria. The existing requirement entries are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md Phase 62 entry with Requirements, Plans count, and plan list</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    1. .planning/ROADMAP.md — find the Phase 62 section (around line 160-173 based on the ROADMAP read above). Read the full Phase 62 block including the current "Requirements: TBD" and "Plans: 0" placeholders to understand exactly what to replace.
    2. .planning/ROADMAP.md — find the Progress table (around line 175+) to check if Phase 62 has an entry that also needs updating.
  </read_first>
  <action>
    In .planning/ROADMAP.md, update the Phase 62 detail section:

    1. Replace "Requirements: TBD" (or "**Requirements:** TBD") with:
       **Requirements**: REQ-WFP-01

    2. Replace "Plans: 0" and the placeholder plan list with:
       **Plans**: 4 plans
       Plans:
       **Wave 1** (parallel — no file overlap)
       - [ ] 62-01-PLAN.md — D-03 auto-start hook + start=auto in build_wfp_service_create_args + 3 unit tests (network.rs)
       - [ ] 62-02-PLAN.md — ServiceInstall start=auto (nono-machine.wxs) + PIPE_SDDL IU ACE + SDDL test (nono-wfp-service.rs)
       - [ ] 62-03-PLAN.md — REQ-WFP-01 in REQUIREMENTS.md + ROADMAP Phase 62 plan list (planning artifacts)

       **Wave 2** (blocked on Wave 1 — requires code + MSI complete)
       - [ ] 62-04-PLAN.md — HUMAN-UAT: machine-MSI install, reboot, out-of-box enforced block, clean uninstall (REQ-WFP-01 SC1-SC5)

    3. Do NOT change the Phase 62 Goal text, Success Criteria, Out-of-scope, or Depends-on fields —
       those were set by the discuss-phase session and are authoritative per CONTEXT.md.

    4. If the Progress table at the bottom of the file has a Phase 62 row, update it to show
       "0/4 | Planning". If no row exists, add one following the existing table format.
  </action>
  <verify>
    <automated>powershell -Command "if ((Get-Content .planning/ROADMAP.md | Select-String 'REQ-WFP-01').Count -ge 1) { exit 0 } else { Write-Error 'REQ-WFP-01 not found in ROADMAP'; exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 62 section contains "REQ-WFP-01" in the Requirements line.
    - ROADMAP.md Phase 62 section shows "4 plans" and lists 62-01 through 62-04.
    - The Phase 62 Goal, Success Criteria, and Out-of-scope blocks are unchanged.
    - The Progress table has a Phase 62 row.
  </acceptance_criteria>
  <done>
    ROADMAP.md Phase 62 entry has Requirements: REQ-WFP-01 and a 4-plan list with correct wave assignments.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning artifacts | No code; no trust boundary changes in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-PA | Tampering | Planning artifacts (REQUIREMENTS.md, ROADMAP.md) | accept | Planning artifacts are version-controlled; no security impact from miswording; any error is caught at phase verification |
| T-62-SC | Tampering | npm/pip/cargo installs | accept | No packages installed |
</threat_model>

<verification>
After both tasks complete:

1. powershell -Command "Select-String 'REQ-WFP-01' .planning/REQUIREMENTS.md"
   Must return a match.

2. powershell -Command "Select-String 'REQ-WFP-01' .planning/ROADMAP.md"
   Must return a match in the Phase 62 section.

3. powershell -Command "Select-String '62-04-PLAN' .planning/ROADMAP.md"
   Must return a match (4th plan listed).
</verification>

<success_criteria>
- REQUIREMENTS.md contains REQ-WFP-01 entry with full acceptance criteria.
- ROADMAP.md Phase 62 has Requirements: REQ-WFP-01 and plans list (62-01..62-04).
- No other sections of either file are modified.
- All commits include DCO sign-off: Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>
</success_criteria>

<output>
Create .planning/phases/62-add-wfp-kernel-network-enforcement-for-windows-supervised-ru/62-03-SUMMARY.md when done.
</output>
