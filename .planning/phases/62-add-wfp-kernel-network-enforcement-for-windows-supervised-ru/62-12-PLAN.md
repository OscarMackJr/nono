---
phase: 62
plan: 12
type: execute
wave: 1
gap_closure: true
depends_on:
  - 62-10
files_modified:
  - crates/nono/src/sandbox/windows.rs
  - crates/nono/Cargo.toml
  - crates/nono-shell-broker/src/main.rs
  - crates/nono-shell-broker/Cargo.toml
  - crates/nono-cli/src/exec_strategy_windows/launch.rs
  - crates/nono-cli/src/exec_strategy_windows/network.rs
  - crates/nono-cli/src/exec_strategy_windows/dacl_guard.rs
requirements:
  - REQ-WFP-01

must_haves:
  truths:
    - "The broker spawns the confined child as a per-run AppContainer (lowbox) — NOT a WRITE_RESTRICTED restricted token. The package SID is derived from a per-run AppContainer NAME `nono.session.<uuid>` via DeriveAppContainerSidFromAppContainerName (deterministic from the name, no profile registration needed), and the child is spawned with STARTUPINFOEX carrying PROC_THREAD_ATTRIBUTE_SECURITY_CAPABILITIES = SECURITY_CAPABILITIES{AppContainerSid, CapabilityCount:0}. This starts cleanly (private AppContainerNamedObjects namespace) — eliminating the 0xC0000142 STATUS_DLL_INIT_FAILED that the 62-10 WRITE_RESTRICTED token caused (debug wfp-write-restricted-0142, D1/D2)."
    - "62-10's create_low_integrity_primary_token_with_sid (WRITE_RESTRICTED restricting SID) is NO LONGER used on the broker-no-PTY arm — it is replaced by the AppContainer spawn. (Leave the parameterless create_low_integrity_primary_token intact; decide whether to retire _with_sid or keep it dead-code-free per CLAUDE.md — if unused after this change, REMOVE it and its tests.)"
    - "The WFP filter is scoped by the PACKAGE SID (the AppContainer SID), reusing the ALREADY-PROVEN ALE_USER_ID + security-descriptor `D:(A;;CC;;;<packageSid>)` path UNCHANGED (62-07/62-08 marshaling) — just fed the package SID string instead of the synthetic S-1-5-117-* session_sid. The package SID is a NORMAL access-check participant (S-1-15-2-*, a real OS SID class), so the WFP CC-probe matches AND the child starts. (ALE_PACKAGE_ID FWP_SID condition is the documented fallback if ALE_USER_ID does not match an AppContainer connection on the live host — D5 unknown #2; do NOT pre-build it, reuse ALE_USER_ID first.)"
    - "SINGLE-SOURCE invariant preserved: ONE per-run AppContainer name (derived from the same per-run UUID that seeds the run today) flows to BOTH the broker token build (SECURITY_CAPABILITIES) AND the WFP service request (the package SID string fed to the existing request.session_sid plumbing, network.rs:542). Both sides derive the SAME package SID deterministically from the name; never generate two identifiers."
    - "AppliedDaclGrantsGuard is RETARGETED from the synthetic session_sid to the PACKAGE SID — confined writes to the cwd grant paths grant the package SID (grant_sid_write_on_path), scoped to already-user-owned grant-set paths, FILE_GENERIC_WRITE|DELETE, (OI)(CI), revoked on Drop (Phase 60 F-60-UAT-04 mechanism, same primitive, package SID as the grantee)."
    - "Low-IL NO_WRITE_UP mandatory label is preserved on the AppContainer child (apply the existing Low-IL label to the child token, or verify AppContainer's own write-up isolation suffices — set Low explicitly for parity/defence-in-depth; re-apply if the lowbox build resets it — D1 step 3 / D5 unknown #4)."
    - "FAIL-CLOSED CONTRACT (unchanged invariant): when network.block is set, ANY failure deriving the package SID, building SECURITY_CAPABILITIES, spawning the lowbox child, threading the package SID to the WFP request, or applying the Low-IL label MUST return NonoError::SandboxInit — NEVER fall back to a plain/SID-less/non-AppContainer token, NEVER spawn the child. Enforce at launch (name present on this arm), broker argv (--app-container-name required under --no-pty), broker spawn (FFI failure propagates), and WFP request (must carry the same package SID)."
    - "No privilege escalation, net tightening: AppContainer with an EMPTY capability set is strictly more confined than the prior Low-IL token (capability-gated, private namespace, deny-by-default). No SeTcbPrivilege / credentials / logon-session change (debug D4a)."
  artifacts:
    - path: "crates/nono-shell-broker/src/main.rs"
      provides: "AppContainer spawn: --app-container-name -> DeriveAppContainerSid -> SECURITY_CAPABILITIES -> STARTUPINFOEX (2 attrs) -> spawn"
      contains: "SECURITY_CAPABILITIES"
    - path: "crates/nono/src/sandbox/windows.rs"
      provides: "package SID derivation + SECURITY_CAPABILITIES build helper (+ Low-IL label apply on child)"
      contains: "DeriveAppContainerSidFromAppContainerName"
    - path: "crates/nono-cli/src/exec_strategy_windows/launch.rs"
      provides: "per-run AppContainer name generation + --app-container-name threading on the no-PTY arm, fail-closed"
      contains: "--app-container-name"
  key_links:
    - from: "per-run AppContainer name nono.session.<uuid> (single source)"
      to: "broker SECURITY_CAPABILITIES AppContainerSid  AND  WFP ALE_USER_ID SD package SID"
      via: "DeriveAppContainerSidFromAppContainerName (deterministic, both sides)"
      pattern: "DeriveAppContainerSidFromAppContainerName"
---

<objective>
Gap-closure replacing the FALSIFIED 62-10 approach. Live Win11 UAT (2026-06-02, installed v0.57.9 SHA-confirmed)
showed the 62-10 fix breaks confined child startup: the WRITE_RESTRICTED broker token exits 0xC0000142
(STATUS_DLL_INIT_FAILED) — curl never runs. Root-caused + REDESIGNED in debug wfp-write-restricted-0142
(design_complete): CreateRestrictedToken(WRITE_RESTRICTED, restricting SID = synthetic S-1-5-117-* absent from
every DACL) denies the loader's WRITE-class operations (image-section copy-on-write mapping +
BaseNamedObjects/section creation) → DLL-init fails. Un-narrowable; hits NATIVE binaries (curl), not just CLR.
The prior premise ("restricting SID is the ONLY Win32 way to put a WFP-matchable SID on a token") is FALSE.

VIABLE FIX (debug D1–D4): spawn the confined child as a per-run **AppContainer** (lowbox). Derive a per-run
package SID (S-1-15-2-*) from an AppContainer name `nono.session.<uuid>`, spawn via STARTUPINFOEX +
SECURITY_CAPABILITIES{AppContainerSid, 0 caps} — which starts cleanly (private writable AppContainerNamedObjects
namespace) AND is per-run-unique. Scope the WFP filter by the package SID, REUSING the proven ALE_USER_ID +
security-descriptor path (62-07/62-08) fed the package SID instead of the synthetic SID. Retarget the DACL guard
to the package SID. Preserve Low-IL NO_WRITE_UP, the broker-spawn model, fail-closed, and the single-source invariant.

SCOPE: closes the SC1 enforcement gap with a child that ACTUALLY STARTS. The four residual unknowns (does the
AppContainer child start under the broker spawn; does ALE_USER_ID match an AppContainer connection or is
ALE_PACKAGE_ID needed; do confined writes work with the package-SID DACL grant; does the Low-IL label survive the
lowbox build) are validated in the follow-up live Win11 UAT (debug D5) — NOT here. 62-11 (uninstall purge) is
independent and unaffected.

Output: confined --block-net child spawns as a per-run AppContainer, starts cleanly, and its outbound TCP is
WFP-blocked via the package-SID-scoped filter.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\OMack\Nono\.planning\debug\wfp-write-restricted-0142.md

<design_authority>
The debug doc wfp-write-restricted-0142 sections D1 (recommended fix), D2 (concrete Win32 mechanics — the exact
DeriveAppContainerSidFromAppContainerName / SECURITY_CAPABILITIES / STARTUPINFOEX / UpdateProcThreadAttribute call
sequence), D3 (alternatives weighed), D4 (threat review + fail-closed contract), and D5 (the four live-UAT unknowns)
are the DESIGN AUTHORITY for this plan. Read them first and follow them. This plan gives the file anchors,
single-source wiring, and task ordering; the debug doc gives the FFI specifics.
</design_authority>

<interfaces>
<!-- Verified live-source extracts (2026-06-02). -->

== crates/nono-shell-broker/src/main.rs (the spawn to change) ==
Token build (L307): `let low_il_token = match args.session_sid.as_deref() { Some(sid) => create_low_integrity_primary_token_with_sid(sid)?, None => create_low_integrity_primary_token()? };`  <-- AppContainer path replaces the _with_sid branch.
Attr list (L313-377): InitializeProcThreadAttributeList(.., COUNT=1, ..) then ONE UpdateProcThreadAttribute(PROC_THREAD_ATTRIBUTE_HANDLE_LIST). <-- becomes COUNT=2: keep HANDLE_LIST, ADD PROC_THREAD_ATTRIBUTE_SECURITY_CAPABILITIES.
Spawn (L447): `CreateProcessAsUserW(low_il_token.raw(), .., EXTENDED_STARTUPINFO_PRESENT, .., &si.StartupInfo, &pi)`.
  DESIGN DECISION (verify against debug D2): AppContainer is normally launched with CreateProcessW (caller token + SECURITY_CAPABILITIES derives the lowbox). Decide AsUserW-with-token vs CreateProcessW: D2 shows CreateProcessW with SECURITY_CAPABILITIES. If switching to CreateProcessW, the broker (Medium IL) is the caller and the lowbox+caps produce the child token; then OPTIONALLY open the child token and apply the Low-IL label for parity. Keep bInheritHandles=1 (HANDLE_LIST still gates stdio). Document the chosen shape + why in the SUMMARY.
Current windows-sys imports (L46-49): CreateProcessAsUserW, *ProcThreadAttribute*, EXTENDED_STARTUPINFO_PRESENT, PROC_THREAD_ATTRIBUTE_HANDLE_LIST, STARTUPINFOEXW, ...
BrokerArgs (L54+) + argv parser (L85-140): has --session-sid (62-10). ADD --app-container-name.

== crates/nono-shell-broker/Cargo.toml ==
windows-sys 0.59 features: Win32_Foundation, Win32_Security, Win32_System_Threading, Win32_System_Console, Win32_System_SystemServices.
ADD: Win32_Security_Isolation (DeriveAppContainerSidFromAppContainerName, SECURITY_CAPABILITIES is under Win32_Security), Win32_Security_Authorization (ConvertSidToStringSidW/ConvertStringSidToSidW). Confirm exact module paths during impl; if a symbol is absent from windows-sys 0.59, declare a minimal `extern "system"` shim per the project's existing targeted-FFI pattern (debug D2 note).

== crates/nono/src/sandbox/windows.rs ==
create_low_integrity_primary_token (L536, KEEP — the plain Low-IL path), create_low_integrity_primary_token_with_sid (L702, the 62-10 fn to RETIRE from the broker path), apply_low_il_label helper (factored in 62-10 — REUSE to label the AppContainer child).
DACL primitives: grant_sid_write_on_path (L1446), revoke_sid_on_path, edit_dacl_for_sid (L1310). These already take a SID string — feed them the package SID.
This crate's windows-sys 0.59 already has many Win32_Security features; ADD Win32_Security_Isolation if the SID-derivation helper lives here.

== crates/nono-cli/src/exec_strategy_windows/restricted_token.rs ==
generate_session_sid() (L21): `S-1-5-117-{u32}-{u32}-{u32}-{u32}` from Uuid::new_v4(). The per-run UUID source. ADD a sibling that produces the per-run AppContainer NAME `nono.session.<uuid>` (or expose the UUID so launch.rs builds the name). The synthetic-SID fn may remain for the non-broker WriteRestricted arm OR be retired — decide during impl (it is independent of this fix).

== crates/nono-cli/src/exec_strategy_windows/launch.rs ==
BrokerLaunchNoPty no-PTY broker_args (L1816+): pushes --session-sid (62-10). REPLACE/AUGMENT with --app-container-name <name>, fail-closed via ok_or (this arm requires the per-run identifier). config.session_sid origin + the single-source flow.

== crates/nono-cli/src/exec_strategy_windows/network.rs ==
build_wfp_*_request: request.session_sid = session_sid.map(...) (L542). FEED this the PACKAGE SID STRING (derived from the per-run name) so the WFP service builds the ALE_USER_ID SD `D:(A;;CC;;;<packageSid>)` UNCHANGED. (Rename to package_sid optional; reusing the field is fine and minimises churn — but make the SUMMARY clear the value is now a package SID.)

== crates/nono-cli/src/exec_strategy_windows/mod.rs (L332) + dacl_guard.rs ==
AppliedDaclGrantsGuard::snapshot_and_apply(fs_policy, session_sid) at mod.rs:332 — pass the PACKAGE SID. dacl_guard.rs best_effort_revert(&path, &self.session_sid) (L168) — the guard's stored SID becomes the package SID.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-run AppContainer name/SID derivation + nono lib SECURITY_CAPABILITIES support</name>
  <files>crates/nono/src/sandbox/windows.rs, crates/nono/Cargo.toml, crates/nono-cli/src/exec_strategy_windows/restricted_token.rs</files>
  <read_first>
    1. debug wfp-write-restricted-0142 D1 (step 1 — Derive vs CreateAppContainerProfile; PREFER Derive-only, no cleanup) + D2 (exact FFI).
    2. restricted_token.rs generate_session_sid (L21) — the per-run UUID source to mirror.
    3. windows.rs apply_low_il_label + grant_sid_write_on_path (L1446) — reused as-is with the package SID.
  </read_first>
  <action>
    1. Add a per-run AppContainer NAME generator (in restricted_token.rs or alongside generate_session_sid):
       `pub(crate) fn generate_app_container_name() -> String` → `format!("nono.session.{}", Uuid::new_v4().simple())`
       (or reuse the existing per-run UUID if one is already threaded — keep ONE source). The name must be a valid
       AppContainer moniker (≤ the documented length, ASCII). This NAME is the single per-run identifier.
    2. In nono windows.rs, add a helper to derive the package SID + build the capabilities, e.g.:
       `pub fn derive_app_container_sid(name: &str) -> Result<OwnedPsid>` wrapping
       DeriveAppContainerSidFromAppContainerName(name_wide, &mut psid) — fail-closed on non-zero HRESULT / null psid;
       RAII-free the PSID (FreeSid) via an OwnedPsid guard (mirror OwnedHandle). Optionally a
       `package_sid_to_string(psid) -> Result<String>` via ConvertSidToStringSidW for the WFP request side.
       Add Win32_Security_Isolation (+ Win32_Security_Authorization if needed) to crates/nono/Cargo.toml windows-sys
       features. If a symbol is missing from windows-sys 0.59, declare a minimal extern "system" shim with // SAFETY.
       No .unwrap()/.expect(); // SAFETY on every unsafe block.
    3. Unit tests (#[cfg(windows)], #[allow(clippy::unwrap_used)] in test mod):
       - generate_app_container_name produces `nono.session.<hex>` and two calls differ.
       - derive_app_container_sid("nono.session.deadbeef...") returns Ok with a non-null PSID whose string form starts
         with "S-1-15-2-" (deterministic from the name; no elevation needed). Malformed/empty name → Err.
  </action>
  <verify>
    <automated>cargo test -p nono app_container 2>&1 | tail -15 || cargo build -p nono</automated>
  </verify>
  <done>
    - Per-run AppContainer name generator + derive_app_container_sid (+ string form) exist, fail-closed, RAII-freed.
    - windows-sys Isolation/Authorization features added; build green on Windows host.
    - Tests: name uniqueness + S-1-15-2-* derivation + malformed-name rejection pass (or documented CI limitation).
  </done>
</task>

<task type="auto">
  <name>Task 2: Broker — spawn the child as a per-run AppContainer</name>
  <files>crates/nono-shell-broker/src/main.rs, crates/nono-shell-broker/Cargo.toml</files>
  <read_first>
    1. debug D2 (the exact SECURITY_CAPABILITIES + STARTUPINFOEX + UpdateProcThreadAttribute sequence) + D4c (fail-closed).
    2. main.rs L307 (token branch), L313-377 (attr list, COUNT=1), L447 (CreateProcessAsUserW). The HANDLE_LIST attr MUST be preserved.
    3. main.rs argv parser (L85-140) + the --no-pty fail-closed precedent (62-10 added --session-sid).
  </read_first>
  <action>
    1. Cargo.toml: add Win32_Security_Isolation + Win32_Security_Authorization to the broker's windows-sys features.
       Import SECURITY_CAPABILITIES, PROC_THREAD_ATTRIBUTE_SECURITY_CAPABILITIES, DeriveAppContainerSidFromAppContainerName
       (or call the nono-lib helper from Task 1 — PREFER reusing nono::derive_app_container_sid to keep one code path).
    2. BrokerArgs: add `app_container_name: Option<String>`. Argv: add `--app-container-name <value>`. Under --no-pty,
       --app-container-name is REQUIRED (fail-closed: missing → NonoError::SandboxInit, never spawn). Keep --session-sid
       parse only if the synthetic path is retained elsewhere; otherwise remove it (and its 62-10 plumbing) to avoid dead code.
    3. Replace the token/spawn so that when app_container_name is present:
       - derive the package SID (nono::derive_app_container_sid).
       - build SECURITY_CAPABILITIES{ AppContainerSid: psid, Capabilities: null, CapabilityCount: 0, Reserved: 0 }.
       - GROW the proc-thread attribute list to COUNT=2: keep the existing PROC_THREAD_ATTRIBUTE_HANDLE_LIST entry AND
         add PROC_THREAD_ATTRIBUTE_SECURITY_CAPABILITIES (re-probe InitializeProcThreadAttributeList size with count 2;
         two UpdateProcThreadAttribute calls). DeleteProcThreadAttributeList on all paths.
       - spawn per debug D2 (CreateProcessW with EXTENDED_STARTUPINFO_PRESENT, or AsUserW — document the choice). Keep
         bInheritHandles=1. After spawn, OPTIONALLY open the child token and apply nono::apply_low_il_label for Low-IL
         parity (D1 step 3 / D5 #4) — if AppContainer's own isolation is deemed sufficient, note that decision instead.
       - FAIL-CLOSED: any FFI failure (derive, attr-list, spawn) → propagate Err; NEVER fall back to a plain token or
         spawn without the AppContainer.
    4. Tests: argv parse of --app-container-name; --no-pty without --app-container-name → Err. (The lowbox FFI needs a
       live host; the SUMMARY defers the spawn proof to the UAT.)
  </action>
  <verify>
    <automated>cargo test -p nono-shell-broker 2>&1 | tail -12 || cargo build -p nono-shell-broker</automated>
  </verify>
  <done>
    - Broker derives the package SID, builds SECURITY_CAPABILITIES, spawns the child as an AppContainer with BOTH the
      HANDLE_LIST and SECURITY_CAPABILITIES proc-thread attributes; HANDLE_LIST stdio gating preserved.
    - --no-pty requires --app-container-name (fail-closed); any FFI failure propagates (no plain-token fallback).
    - cargo build -p nono-shell-broker green; argv tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: launch.rs — generate the per-run name + thread it fail-closed; retire the --session-sid broker push</name>
  <files>crates/nono-cli/src/exec_strategy_windows/launch.rs</files>
  <read_first>
    1. launch.rs no-PTY broker_args (L1816+, where 62-10 pushed --session-sid) + select_windows_token_arm (L1166).
    2. How config.session_sid is populated today (mod.rs / runtime) — the single-source point to also carry the AppContainer name.
    3. debug D4c/D4d (fail-closed + single-source).
  </read_first>
  <action>
    1. Establish the per-run AppContainer name as the single-source identifier for the broker-no-PTY arm: generate it
       once (Task 1 generator) alongside the existing per-run setup, store it on config (e.g. config.app_container_name)
       so BOTH this arm and the WFP request (Task 4) read the SAME value. Derive it from the SAME per-run UUID if one
       already exists — do not introduce a second random source.
    2. In the no-PTY broker_args block, replace the `--session-sid <session_sid>` push with
       `--app-container-name <config.app_container_name>`, fail-closed via ok_or (this arm requires it; missing → Err,
       never spawn). Leave the PTY broker_args unchanged (PTY waives per-session WFP).
    3. No .unwrap()/.expect().
  </action>
  <verify>
    <automated>cargo build -p nono-cli 2>&1 | tail -12</automated>
  </verify>
  <done>
    - The no-PTY broker_args carry --app-container-name <single-source name>, fail-closed.
    - PTY path unchanged; cargo build -p nono-cli green.
  </done>
</task>

<task type="auto">
  <name>Task 4: WFP request scoped by the package SID (reuse ALE_USER_ID SD) + DACL guard retarget</name>
  <files>crates/nono-cli/src/exec_strategy_windows/network.rs, crates/nono-cli/src/exec_strategy_windows/mod.rs, crates/nono-cli/src/exec_strategy_windows/dacl_guard.rs</files>
  <read_first>
    1. network.rs build_wfp_*_request (request.session_sid = ... L542) — the value fed to the WFP service ALE_USER_ID SD.
    2. mod.rs:332 AppliedDaclGrantsGuard::snapshot_and_apply(fs_policy, session_sid) + dacl_guard.rs (L168).
    3. debug D1 step 4 (REUSE ALE_USER_ID SD fed the package SID first; ALE_PACKAGE_ID is the deferred fallback) + D4b (DACL parity).
  </read_first>
  <action>
    1. Compute the per-run PACKAGE SID STRING from the single-source AppContainer name (nono::derive_app_container_sid
       → package_sid_to_string) at the same point the run computes its WFP request. Feed that string into the EXISTING
       request.session_sid plumbing (network.rs:542) so the WFP service builds `D:(A;;CC;;;<packageSid>)` via the
       UNCHANGED 62-07/62-08 ALE_USER_ID + FWP_BYTE_BLOB path. Do NOT add ALE_PACKAGE_ID in this plan (deferred fallback;
       only if the live UAT shows ALE_USER_ID does not match the AppContainer connection — debug D5 #2). Keep the WFP
       service binary UNCHANGED in this plan.
    2. Retarget AppliedDaclGrantsGuard: at mod.rs:332 pass the PACKAGE SID (not the synthetic session_sid). The guard's
       grant_sid_write_on_path / revoke target the package SID; everything else (user-owned-path gate, FILE_GENERIC_WRITE|
       DELETE, (OI)(CI), Drop-revoke) is unchanged. This makes confined cwd writes work for the AppContainer child.
    3. SINGLE-SOURCE + FAIL-CLOSED: the package SID used here MUST derive from the SAME per-run name threaded to the
       broker in Task 3. If derivation fails when network.block is set → Err (do not request an unscoped filter, do not
       spawn). Update any session_sid-named tests/fixtures to reflect a package SID value (S-1-15-2-*) where they assert
       the request payload.
  </action>
  <verify>
    <automated>cargo test -p nono-cli 2>&1 | tail -20 || cargo build -p nono-cli</automated>
  </verify>
  <done>
    - The WFP request carries the package SID via the unchanged ALE_USER_ID SD path; WFP service binary untouched.
    - AppliedDaclGrantsGuard grants/revokes the package SID; single-source from the per-run name; fail-closed on derive failure.
    - cargo test -p nono-cli green (fixtures updated to package-SID values).
  </done>
</task>

<task type="auto">
  <name>Task 5: Retire dead 62-10 WRITE_RESTRICTED broker code + workspace verify</name>
  <files>crates/nono/src/sandbox/windows.rs, crates/nono-shell-broker/src/main.rs, crates/nono-cli/src/exec_strategy_windows/launch.rs</files>
  <read_first>
    1. CLAUDE.md "Lazy use of dead code" (remove unused; no #[allow(dead_code)]).
    2. Grep create_low_integrity_primary_token_with_sid + --session-sid usages after Tasks 1-4.
  </read_first>
  <action>
    1. If create_low_integrity_primary_token_with_sid (and its --session-sid broker plumbing) is unused after the
       AppContainer switch, REMOVE it and its tests (it was the falsified 62-10 path). If the non-broker WriteRestricted
       arm still legitimately uses session_sid, KEEP only that and document the boundary. Either way: NO dead code, NO
       #[allow(dead_code)]. Keep the parameterless create_low_integrity_primary_token (still used by LowIlPrimary/legacy).
    2. Full workspace build + the touched-crate tests + Windows-host clippy.
  </action>
  <verify>
    <automated>cargo build -p nono -p nono-shell-broker -p nono-cli 2>&1 | tail -8 && cargo clippy -p nono -p nono-shell-broker -p nono-cli -- -D warnings -D clippy::unwrap_used 2>&1 | tail -8</automated>
  </verify>
  <done>
    - No dead 62-10 WRITE_RESTRICTED code remains (removed or scoped to a still-live arm); no #[allow(dead_code)].
    - Workspace build + touched-crate tests + Windows-host clippy all green; cross-target Unix clippy PARTIAL/deferred-to-CI (cfg(windows)-only) noted in SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nono-cli (Medium IL) → broker (Medium IL) via argv | --app-container-name is a per-run moniker; broker derives the PSID itself (no trust of an inbound SID blob) |
| broker → AppContainer child | child runs in a per-run lowbox with EMPTY capability set + Low-IL label — strictly more confined than the prior token |
| AppContainer child connection → BFE WFP ALE_USER_ID check | package SID is a normal access-check participant → the BLOCK filter matches AND the child starts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-62-26 | Denial of Service (self) | confined child crashes at startup (0xC0000142) | mitigate | AppContainer spawn replaces WRITE_RESTRICTED — the child gets a private writable AppContainerNamedObjects namespace, so loader section/named-object writes SUCCEED. Closes the 62-10 regression (debug root cause). |
| T-62-27 | Spoofing / silent non-enforcement | child spawned without the per-run package SID | mitigate | FAIL-CLOSED at launch/broker/WFP-request; single-source name → same package SID on both sides; any derive/spawn failure → Err, never spawn (debug D4c/D4d). |
| T-62-28 | Elevation of Privilege | AppContainer SID widening access | mitigate | Empty capability set + Low-IL label = net tightening vs the prior Low-IL token; S-1-15-2-* names no real account; no SeTcb/credentials/logon-session (debug D4a). |
| T-62-29 | Tampering (write surface) | DACL grants retargeted to package SID | accept (intended parity) | Same grant_sid_write_on_path primitive, package SID grantee, only on already-user-owned grant-set paths, FILE_GENERIC_WRITE|DELETE, revoked on Drop (Phase 60 F-60-UAT-04 reused; debug D4b). |
| T-62-SC | Tampering | npm/pip/cargo installs | accept | No new packages (windows-sys feature flags only). |
</threat_model>

<verification>
1. `cargo build -p nono -p nono-shell-broker -p nono-cli` (Windows host) — exit 0.
2. `cargo test -p nono -p nono-shell-broker -p nono-cli` — new tests pass (name uniqueness, S-1-15-2-* derivation, malformed-name reject, argv --app-container-name + --no-pty-requires-it, updated WFP-request package-SID fixtures).
3. Windows-host `cargo clippy -p nono -p nono-shell-broker -p nono-cli -- -D warnings -D clippy::unwrap_used` — clean; no dead code, no #[allow(dead_code)]. Cross-target Linux/macOS clippy PARTIAL/deferred-to-CI per CLAUDE.md (cfg(windows)-only) — note in SUMMARY.
4. grep: DeriveAppContainerSidFromAppContainerName + SECURITY_CAPABILITIES present in the broker/lib path; --app-container-name in launch.rs (push) AND broker (parse); WFP request fed the package SID; AppliedDaclGrantsGuard uses the package SID. create_low_integrity_primary_token_with_sid removed (or scoped to a still-live arm).
5. LIVE acceptance (orchestrator rebuilds nono.exe + broker + version-bumped MSI v0.57.10+; operator on real Win11 — validates debug D5's four unknowns):
   - D5#1 PRIMARY: confined child STARTS (no 0xC0000142) — `nono run --profile claude-code --block-net --allow-cwd -- curl.exe -sS -m 5 https://api.ipify.org` runs curl.
   - SC1: outbound BLOCKED (no external IP; "BLOCKED"/timeout), exit != 0.
   - D5#2: if curl starts but is NOT blocked, the WFP ALE_USER_ID SD did not match the AppContainer connection → implement the ALE_PACKAGE_ID fallback (debug D1 step 4) in a follow-up.
   - D5#3: confined WRITE to a cwd grant path succeeds (DACL guard retargeted to the package SID).
   - D5#4: confirm the Low-IL label is present on the child (or AppContainer isolation deemed sufficient).
   Record all four explicitly in the SUMMARY.
</verification>

<success_criteria>
- The confined --block-net child spawns as a per-run AppContainer, STARTS cleanly (no 0xC0000142), and is WFP-blocked via the package-SID-scoped ALE_USER_ID filter (live SC1).
- Single-source per-run name → same package SID on the broker token AND the WFP request; fail-closed at every site; net-tightening, no escalation.
- 62-10's WRITE_RESTRICTED broker path removed (no dead code); 62-11 uninstall purge unaffected.
- SUMMARY documents the AppContainer design, the AsUserW-vs-CreateProcessW spawn choice, the four D5 UAT unknowns and which remain open, the cross-target clippy deferral, and the windows-sys symbol/feature resolution.
- All commits include DCO sign-off: Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>
</success_criteria>

<output>
Create .planning/phases/62-add-wfp-kernel-network-enforcement-for-windows-supervised-ru/62-12-SUMMARY.md when done.
</output>
