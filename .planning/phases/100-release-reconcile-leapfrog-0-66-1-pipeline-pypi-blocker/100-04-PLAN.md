---
phase: 100-release-reconcile-leapfrog-0-66-1-pipeline-pypi-blocker
plan: 04
type: execute
wave: 2
depends_on: ["100-01", "100-02", "100-03"]
files_modified:
  - scripts/gates/release-readiness.ps1
  - scripts/release-dry-run.ps1
  - .planning/milestones/v3.3-phases/97-release-engineering-leapfrog-pipeline-runbook/RELEASE-RUNBOOK.md
autonomous: true
requirements: [RLS-13]
must_haves:
  truths:
    - "release-readiness gate PASSes with $targetVersion=0.66.1 and $upstreamHighest=0.66.0"
    - "release-dry-run.ps1 exits 0 (crates.nono PASS; nono-proxy/nono-cli PRE_PUBLISH_REGISTRY_BLOCKED-expected; pypi.maturin_build PASS now that Plan 03's fix landed; npm.dry_run PASS)"
    - "RELEASE-RUNBOOK.md's version-family table, push sequence, and known-blockers table all reference 0.66.1 / upstream 0.66.0, with blocker 1 (nono-py RouteConfig) marked resolved"
    - "The actual tag push + registry publish remain operator-gated — this plan runs no live push or publish command"
    - "D-09: Phase 100 is PREPARE ONLY — release-dry-run.ps1 + release-readiness re-run GREEN at 0.66.1 and RELEASE-RUNBOOK.md is updated with the PUBLIC-repo pre-push checklist, but the actual tag push + registry publish stay operator-gated"
  artifacts:
    - path: "scripts/gates/release-readiness.ps1"
      provides: "gate constants updated to the 0.66.1 leapfrog"
      contains: "$targetVersion       = '0.66.1'"
    - path: ".planning/milestones/v3.3-phases/97-release-engineering-leapfrog-pipeline-runbook/RELEASE-RUNBOOK.md"
      provides: "0.66.1 operator push guide with PUBLIC-repo pre-push checklist"
      contains: "0.66.1"
  key_links:
    - from: "scripts/gates/release-readiness.ps1"
      to: ".nono-runtime/verdicts/release-readiness.json"
      via: "verify-dark.ps1 persisted verdict (WR-04)"
      pattern: "release-readiness"
    - from: "RELEASE-RUNBOOK.md"
      to: "scripts/release-dry-run.ps1"
      via: "Step 1 of the mandatory pre-push gates"
      pattern: "release-dry-run\\.ps1"
---

<objective>
Re-point the two hard-coded version constants in the existing `release-readiness` gate,
re-run both mandatory pre-push gates GREEN at `0.66.1`, and update `RELEASE-RUNBOOK.md` for
the `0.66.1` tag — closing out RLS-13. This plan makes the release genuinely one-step-push
ready; it depends on Plans 01-03 because the gate and dry-run scripts assert directly
against the version bump (Plan 01) and the PyPI/npm binding state (Plan 03).

Purpose: the pipeline, gate script, and dry-run orchestrator already exist and already
passed once at `0.66.0` (v3.3 Phase 97) — this is a re-green at the new version, not new
infrastructure.
Output: `release-readiness.ps1` PASSes at `0.66.1`; `release-dry-run.ps1` exits 0;
`RELEASE-RUNBOOK.md` reflects the `0.66.1` tag and the PUBLIC-repo pre-push checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-release-reconcile-leapfrog-0-66-1-pipeline-pypi-blocker/100-CONTEXT.md
@.planning/phases/100-release-reconcile-leapfrog-0-66-1-pipeline-pypi-blocker/100-RESEARCH.md
@.planning/phases/100-release-reconcile-leapfrog-0-66-1-pipeline-pypi-blocker/100-01-SUMMARY.md
@.planning/phases/100-release-reconcile-leapfrog-0-66-1-pipeline-pypi-blocker/100-03-SUMMARY.md

<interfaces>
<!-- Exact constants + doc locations, verified by direct read. -->

scripts/gates/release-readiness.ps1:76-84 — the ONLY edit site in this gate script:
```
$targetVersion       = '0.66.0'          # -> '0.66.1'
$upstreamHighest     = '0.65.1'          # -> '0.66.0'
$versionFamilyCrates = @(
    'nono', 'nono-cli', 'nono-proxy', 'nono-shell-broker', 'nono-fltmgr-client', 'nono-ffi'
)                                          # unchanged — already the 6-crate D-03 list
```
(Rendered as prose per deep_work_rules: two string literals change; the
`$versionFamilyCrates` array is already correct and untouched.) ASSERTION (b)'s
`no-stale-0.62.2` check is a historical guard, unaffected — no new `no-stale-0.66.0`
assertion is required to pass (optional defense-in-depth, not RLS-13's literal ask).

scripts/release-dry-run.ps1 — the crates.io/PyPI/npm dry-run logic is unchanged (no
version constant to edit; `$PublishableCrates = @("nono", "nono-proxy", "nono-cli")` at
line 71 is name-only, no version pin). Its header comment (lines 3, 20-22) references
"v0.66.0" / "nono = ^0.66.0" in human-readable prose only — not gate-enforced, but update
for accuracy.

RELEASE-RUNBOOK.md (current path, archived under v3.3's phase directory — updated in
place per the planner's discretion, not relocated, to keep a single canonical runbook
rather than forking two copies): every `0.66.0` occurrence (title, pre-push checklist,
version-family gate table, git tag in the push sequence, "What release.yml Does" section,
"Known Pre-Release Blockers" table, closing summary) needs the parallel `0.66.1` value;
the leapfrog assertion changes from ">upstream highest 0.65.1" to ">upstream highest
0.66.0"; "Known Pre-Release Blockers" row 1 (nono-py RouteConfig) is now resolved by Plan
03 — mark it done, citing this phase; row 2 (twine absence) stays open, non-blocking.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point release-readiness gate constants and confirm PASS at 0.66.1</name>
  <files>scripts/gates/release-readiness.ps1</files>
  <read_first>
    - scripts/gates/release-readiness.ps1:76-84 (the two constants), :88-172 (all 5 assertions this gate runs — version-family, no-stale-0.62.2, leapfrog, no-private-paths, cargo-lock)
  </read_first>
  <action>
    Edit `$targetVersion` from `'0.66.0'` to `'0.66.1'` and `$upstreamHighest` from
    `'0.65.1'` to `'0.66.0'` at release-readiness.ps1:76-77. Leave `$versionFamilyCrates`
    (the 6-crate D-03 list) unchanged — it already matches Plan 01's version-bump surface.
    Do not add a new assertion in this task (the existing `no-stale-0.62.2` guard is
    unaffected; a `no-stale-0.66.0` guard is optional defense-in-depth, not required).
    Re-run the gate via verify-dark.ps1's `-File` invocation (never `-Command` with a bare
    path — swallows exit N->1) and confirm PASS.
  </action>
  <verify>
    <automated>pwsh -File scripts/verify-dark.ps1 -Gate release-readiness</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits `0` (PASS)
    - `.nono-runtime/verdicts/release-readiness.json` shows `verdict: PASS` with `detail.version_family` confirming all 6 crates at `0.66.1` and `detail.leapfrog` confirming `0.66.1 > 0.66.0`
    - `grep "targetVersion.*0.66.1" scripts/gates/release-readiness.ps1` matches
  </acceptance_criteria>
  <done>release-readiness gate PASSes with both constants pointed at the 0.66.1 leapfrog over upstream's 0.66.0.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run release-dry-run.ps1 and confirm exit 0 at 0.66.1</name>
  <files>scripts/release-dry-run.ps1</files>
  <read_first>
    - scripts/release-dry-run.ps1:1-32 (header docstring — the 3 legs: crates.io/PyPI/npm; PRE_PUBLISH_REGISTRY_BLOCKED semantics)
    - .planning/phases/100-release-reconcile-leapfrog-0-66-1-pipeline-pypi-blocker/100-03-SUMMARY.md (confirms nono-py/nono-ts state this dry-run now exercises)
  </read_first>
  <action>
    Update the header docstring's human-readable "v0.66.0" references (lines 3, 20-22) to
    "v0.66.1" for accuracy — these are prose only, not gate-enforced assertions. Re-run the
    script and confirm exit 0. Expected leg outcomes at this point: `crates.nono` PASS;
    `crates.nono-proxy`/`crates.nono-cli` PRE_PUBLISH_REGISTRY_BLOCKED (expected — nono
    0.66.1 is not yet on crates.io, per the documented pre-publish state, not a failure);
    `pypi.maturin_build` and `pypi.twine_check` now PASS (or twine SKIP if absent on this
    host) now that Plan 03 closed the nono-py RouteConfig blocker; `npm.dry_run` PASS.
  </action>
  <verify>
    <automated>pwsh -File scripts/release-dry-run.ps1</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits `0`
    - Output shows `crates.nono` as PASS and `crates.nono-proxy`/`crates.nono-cli` as `PRE_PUBLISH_REGISTRY_BLOCKED` (not FAIL)
    - Output shows `pypi.maturin_build` as PASS (no longer FAIL)
    - Output shows `npm.dry_run` as PASS
    - No result key shows `FAIL`
  </acceptance_criteria>
  <done>release-dry-run.ps1 exits 0 at 0.66.1 with the PyPI leg now passing (blocker closed) and only the expected pre-publish-registry BLOCKED statuses on downstream crates.</done>
</task>

<task type="auto">
  <name>Task 3: Update RELEASE-RUNBOOK.md for the 0.66.1 tag</name>
  <files>.planning/milestones/v3.3-phases/97-release-engineering-leapfrog-pipeline-runbook/RELEASE-RUNBOOK.md</files>
  <read_first>
    - .planning/milestones/v3.3-phases/97-release-engineering-leapfrog-pipeline-runbook/RELEASE-RUNBOOK.md (full file — every 0.66.0 occurrence needs review)
    - .planning/STATE.md § Blockers/Concerns (repo stays PUBLIC — go-private cancelled 2026-07-01 — for the pre-push checklist language)
  </read_first>
  <action>
    Update RELEASE-RUNBOOK.md in place (this planner's call — keep the single canonical
    runbook at its existing v3.3 archived path rather than forking a duplicate at a new
    Phase 100 location). Replace every `0.66.0` occurrence with `0.66.1` (title, pre-push
    checklist's "Leapfrog version confirmed" line, the version-family gate table row, the
    git tag in the Step 3 push sequence, the "What release.yml Does" section, the closing
    "Reminder" summary). Update the leapfrog assertion text from ">upstream highest 0.65.1"
    to ">upstream highest 0.66.0". In the "Known Pre-Release Blockers" table, mark row 1
    (nono-py RouteConfig missing endpoint_policy) as resolved, citing Phase 100 Plan 03;
    leave row 2 (twine absence) open and non-blocking, unchanged. Confirm the PUBLIC-repo
    pre-push checklist language (no `build_notes/`/`.gsd/` staged; repo stays PUBLIC per
    the 2026-07-01 operator decision recorded in STATE.md) is present and accurate — the
    go-private idea is retired, not merely deferred, so do not reintroduce
    altitude-approval-gated language.
  </action>
  <verify>
    <automated>grep -c '0.66.0' .planning/milestones/v3.3-phases/97-release-engineering-leapfrog-pipeline-runbook/RELEASE-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - The grep-count verify command for `0.66.0` returns `1` (only the "upstream highest 0.66.0" leapfrog-comparison reference should remain; every other occurrence is now `0.66.1`)
    - `grep -c '0.66.1' RELEASE-RUNBOOK.md` (same path) returns a count consistent with title + checklist + gate table + push sequence + "What release.yml Does" + closing summary all updated
    - The "Known Pre-Release Blockers" table's row 1 is marked resolved/closed, citing Phase 100
    - The PUBLIC-repo pre-push checklist item is present and does not reference altitude-approval as a pending gate
  </acceptance_criteria>
  <done>RELEASE-RUNBOOK.md is fully updated for the 0.66.1 tag, with the nono-py blocker marked resolved and the PUBLIC-repo pre-push checklist accurate to the current operator decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local gate/dry-run scripts → operator go/no-go push decision | These scripts are the sole automated signal the operator relies on before pushing a tag to a PUBLIC repo |
| Runbook accuracy → operator action | A stale runbook (wrong version, wrong blocker status) could mislead the operator into pushing the wrong tag or skipping a still-open blocker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-09 | Tampering (private-path leak) | release-readiness gate ASSERTION (d) | mitigate | Unmodified by this plan — re-run confirms PASS at 0.66.1, structurally enforcing no `build_notes/`/`.gsd/` in the staging area before any push |
| T-100-10 | Repudiation (stale operator guidance) | RELEASE-RUNBOOK.md | mitigate | Every `0.66.0` reference updated to `0.66.1` except the intentional "upstream highest 0.66.0" comparison; verified by the grep-count checks in Task 3 |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — this plan edits 2 existing scripts' constants/comments and one runbook document |
</threat_model>

<verification>
- `pwsh -File scripts/verify-dark.ps1 -Gate release-readiness` exits 0 (PASS) at 0.66.1.
- `pwsh -File scripts/release-dry-run.ps1` exits 0, with the PyPI leg now PASS.
- RELEASE-RUNBOOK.md's only remaining `0.66.0` occurrence is the intentional upstream-comparison reference.
</verification>

<success_criteria>
Both `scripts/release-dry-run.ps1` and the `release-readiness` gate re-run GREEN at
`0.66.1`; `RELEASE-RUNBOOK.md` is updated for the `0.66.1` tag with the PUBLIC-repo
pre-push checklist. The actual tag push + registry publish remain operator-gated, outside
this plan.
</success_criteria>

<output>
Create `.planning/phases/100-release-reconcile-leapfrog-0-66-1-pipeline-pypi-blocker/100-04-SUMMARY.md` when done.
</output>
