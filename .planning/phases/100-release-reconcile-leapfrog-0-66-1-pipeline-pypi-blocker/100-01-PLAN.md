---
phase: 100-release-reconcile-leapfrog-0-66-1-pipeline-pypi-blocker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono/Cargo.toml
  - crates/nono-cli/Cargo.toml
  - crates/nono-proxy/Cargo.toml
  - crates/nono-shell-broker/Cargo.toml
  - crates/nono-fltmgr-client/Cargo.toml
  - bindings/c/Cargo.toml
  - Cargo.lock
autonomous: true
requirements: [RLS-10]
must_haves:
  truths:
    - "cargo metadata reports nono, nono-cli, nono-proxy, nono-shell-broker, nono-fltmgr-client, and nono-ffi all at version 0.66.1"
    - "tools/sign-fixture remains independently versioned at 0.1.0, untouched by the bump"
    - "cargo build --workspace --all-targets exits 0 with the bumped versions"
    - "Cargo.lock's diff touches only the 6 workspace-member version entries, no third-party crate version drift"
  artifacts:
    - path: "crates/nono/Cargo.toml"
      provides: "nono crate at 0.66.1"
      contains: "version = \"0.66.1\""
    - path: "crates/nono-cli/Cargo.toml"
      provides: "nono-cli crate + 3 internal path-dep pins (nono, nono-proxy, nono-shell-broker) at 0.66.1"
      contains: "version = \"0.66.1\""
    - path: "crates/nono-proxy/Cargo.toml"
      provides: "nono-proxy crate + nono path-dep pin at 0.66.1"
      contains: "version = \"0.66.1\""
    - path: "crates/nono-shell-broker/Cargo.toml"
      provides: "nono-shell-broker crate + nono path-dep pin at 0.66.1"
      contains: "version = \"0.66.1\""
    - path: "crates/nono-fltmgr-client/Cargo.toml"
      provides: "nono-fltmgr-client crate at 0.66.1 (no internal path-dep edges)"
      contains: "version = \"0.66.1\""
    - path: "bindings/c/Cargo.toml"
      provides: "nono-ffi crate + nono path-dep pin at 0.66.1"
      contains: "version = \"0.66.1\""
    - path: "Cargo.lock"
      provides: "regenerated lockfile with 0.66.1 workspace-member entries, no third-party drift"
  key_links:
    - from: "crates/nono-cli/Cargo.toml"
      to: "crates/nono/Cargo.toml"
      via: "internal path-dep version pin"
      pattern: "nono = \\{ version = \"0\\.66\\.1\""
    - from: "crates/nono-cli/Cargo.toml"
      to: "crates/nono-shell-broker/Cargo.toml"
      via: "Windows-only dev-dependency version pin"
      pattern: "nono-shell-broker = \\{ path = \"\\.\\./nono-shell-broker\", version = \"0\\.66\\.1\" \\}"
---

<objective>
Bump every workspace member currently carrying `0.66.0` to `0.66.1` — the minimal
collision-free version above upstream's own `0.66.0` (Phase 98 ledger anchor) — and keep
every internal path-dependency `version` pin consistent, per D-03/D-04. Regenerate
`Cargo.lock` and confirm the workspace still builds clean.

Purpose: `0.66.1` is the root dependency for every other Phase 100 concern (RLS-11 CI
reconcile references this version; RLS-13's release-dry-run + release-readiness gate both
assert against it). Nothing else in the phase can be verified against the wrong version.
Output: 6 `[package] version` lines + 6 internal path-dep pins updated to `0.66.1`;
regenerated `Cargo.lock`; clean `cargo build --workspace --all-targets`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-release-reconcile-leapfrog-0-66-1-pipeline-pypi-blocker/100-CONTEXT.md
@.planning/phases/100-release-reconcile-leapfrog-0-66-1-pipeline-pypi-blocker/100-RESEARCH.md

<interfaces>
<!-- Exact version-surface enumeration, verified exhaustive by RESEARCH.md. No codebase
     exploration needed — these are the only 12 edit sites in the nono workspace. -->

Six `[package] version = "0.66.0"` lines (workspace members — bump all six):
- crates/nono/Cargo.toml:3
- crates/nono-cli/Cargo.toml:3
- crates/nono-proxy/Cargo.toml:3
- crates/nono-shell-broker/Cargo.toml:3
- crates/nono-fltmgr-client/Cargo.toml:3   (omitted from RLS-10's stale text — D-03 correction; IS one of the gate's 6 tracked crates)
- bindings/c/Cargo.toml:3   (crate name nono-ffi)

Six internal path-dep `version = "0.66.0"` pins (bump all six, same file family):
- bindings/c/Cargo.toml:18                      — nono-ffi -> nono
- crates/nono-shell-broker/Cargo.toml:20         — nono-shell-broker -> nono
- crates/nono-cli/Cargo.toml:47                  — nono-cli -> nono
- crates/nono-cli/Cargo.toml:48                  — nono-cli -> nono-proxy
- crates/nono-cli/Cargo.toml:180                 — nono-cli -> nono-shell-broker (Windows-only [target.'cfg(target_os = "windows")'.dev-dependencies], Phase 41 CR-04)
- crates/nono-proxy/Cargo.toml:21                — nono-proxy -> nono

DO NOT TOUCH:
- tools/sign-fixture/Cargo.toml:3 — version = "0.1.0". Independently versioned CI fixture
  tool, never tracked the workspace release version (confirmed via git history c8b3eacf,
  4aaa0508). Bumping it would be wrong, not merely optional.
- Cargo.toml:18-19 (root workspace) — `[workspace.package] repository`/`homepage` still
  read `https://github.com/always-further/nono` (stale, matches upstream org before the
  fork's `OscarMackJr/nono` identity). This is flagged by CONTEXT.md/RESEARCH.md as an
  explicit Deferred Idea — capture as a follow-up note in the SUMMARY, do NOT edit it in
  this plan (avoids expanding RLS-10's literal scope).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump 6 crate versions + 6 internal path-dep pins to 0.66.1</name>
  <files>crates/nono/Cargo.toml, crates/nono-cli/Cargo.toml, crates/nono-proxy/Cargo.toml, crates/nono-shell-broker/Cargo.toml, crates/nono-fltmgr-client/Cargo.toml, bindings/c/Cargo.toml</files>
  <read_first>
    - .planning/phases/100-release-reconcile-leapfrog-0-66-1-pipeline-pypi-blocker/100-RESEARCH.md § "Architecture Patterns / RLS-10 — Version Bump Surface" (the exhaustive verified table this task replays)
    - Each of the 6 files listed above, at the exact line numbers given in <interfaces>
  </read_first>
  <action>
    Edit each of the 6 `[package] version = "0.66.0"` lines (crates/nono/Cargo.toml:3,
    crates/nono-cli/Cargo.toml:3, crates/nono-proxy/Cargo.toml:3,
    crates/nono-shell-broker/Cargo.toml:3, crates/nono-fltmgr-client/Cargo.toml:3,
    bindings/c/Cargo.toml:3) to `version = "0.66.1"`. Edit each of the 6 internal path-dep
    pins (bindings/c/Cargo.toml:18, crates/nono-shell-broker/Cargo.toml:20,
    crates/nono-cli/Cargo.toml:47, crates/nono-cli/Cargo.toml:48,
    crates/nono-cli/Cargo.toml:180, crates/nono-proxy/Cargo.toml:21) from
    `version = "0.66.0"` to `version = "0.66.1"`, preserving the surrounding `path = "..."`
    and feature-flag fields verbatim — change only the version string. Do not touch
    tools/sign-fixture/Cargo.toml (independent 0.1.0 versioning) or the root
    `Cargo.toml`'s `[workspace.package] repository`/`homepage` fields (Deferred Idea; note
    as a follow-up in the SUMMARY instead).
  </action>
  <verify>
    <automated>grep -rn 'version = "0.66.0"' crates/nono/Cargo.toml crates/nono-cli/Cargo.toml crates/nono-proxy/Cargo.toml crates/nono-shell-broker/Cargo.toml crates/nono-fltmgr-client/Cargo.toml bindings/c/Cargo.toml | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The grep-count verify command above returns exactly `0` (no remaining 0.66.0 in the 6 files)
    - `grep -c '0\.66\.1' crates/nono-cli/Cargo.toml` returns `4` (1 package version + 3 path-dep pins: nono, nono-proxy, nono-shell-broker)
    - `grep '^version' tools/sign-fixture/Cargo.toml` still shows `version = "0.1.0"` (unchanged)
    - Root `Cargo.toml:18-19` `repository`/`homepage` are unchanged (still `always-further/nono`) — confirmed unedited, not silently fixed
  </acceptance_criteria>
  <done>All 6 workspace-member `[package] version` lines and all 6 internal path-dep pins read `0.66.1`; tools/sign-fixture and the workspace `repository`/`homepage` fields are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate Cargo.lock and verify a clean, scoped build</name>
  <files>Cargo.lock</files>
  <read_first>
    - .planning/phases/100-release-reconcile-leapfrog-0-66-1-pipeline-pypi-blocker/100-RESEARCH.md § "Common Pitfalls / Pitfall 5" (regen via build, not `cargo update`, to avoid pulling unrelated third-party dependency changes)
    - Makefile:143 (`ci: check test audit` — the `make ci` target this task's final check invokes)
  </read_first>
  <action>
    Regenerate the lockfile via `cargo build --workspace --all-targets` (NOT
    `cargo update`, which could pull newer semver-compatible third-party releases and
    silently broaden the diff beyond the version bump — CLAUDE.md `--workspace
    --all-targets` is also the real local gate per project convention, unlike a bare
    `--bin nono` build which hides nono-ffi exhaustive-match breaks). After the build,
    diff `Cargo.lock` and confirm the only changed lines are the 6 workspace-member
    `version = "0.66.0"` -> `version = "0.66.1"` entries — investigate (do not silently
    accept) any third-party crate version change in the diff. Then run `make ci`
    (clippy + fmt-check + test + audit) to confirm the workspace is clean at the new
    version. This is a pure manifest edit — no cfg-gated Unix source code is touched, so
    the cross-target clippy gate (Docker `cross` / `cargo-zigbuild`) is not required for
    this plan per CLAUDE.md's touch-based trigger.
  </action>
  <verify>
    <automated>cargo build --workspace --all-targets</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace --all-targets` exits 0
    - `git diff Cargo.lock | grep -c '^-version = "0.66.0"'` equals `6`
    - `git diff Cargo.lock | grep -c '^+version = "0.66.1"'` equals `6`
    - No line in `git diff Cargo.lock` shows a version change for any crate name outside {nono, nono-cli, nono-proxy, nono-shell-broker, nono-fltmgr-client, nono-ffi}
    - `make ci` exits 0
  </acceptance_criteria>
  <done>Cargo.lock is regenerated with exactly the 6 workspace-member version bumps (no third-party drift); `cargo build --workspace --all-targets` and `make ci` both exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cargo.toml/Cargo.lock version metadata → crates.io publish surface | This plan's edits are the literal input to the future (operator-gated) `cargo publish` — a wrong value here propagates directly to the public registry |
| `cargo build` lockfile regen → third-party dependency graph | Regenerating Cargo.lock is an opportunity to accidentally widen the dependency graph beyond the intended version-string edit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01 | Tampering | 6 `[package] version` fields | mitigate | This plan performs the exact `0.66.0` → `0.66.1` bump (RLS-10); collision with upstream's real `0.66.0` is structurally avoided by never landing `0.66.0` — verified by the grep-zero check in Task 1 |
| T-100-02 | Tampering | Cargo.lock regeneration | mitigate | Regen via `cargo build --workspace --all-targets`, never `cargo update`; diff scoped to exactly 6 workspace-member hunks, verified in Task 2 |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — pure version-string edit of already-vetted workspace members plus a lockfile regen of already-resolved dependencies |
</threat_model>

<verification>
- `grep -rn 'version = "0.66.0"'` across the 6 workspace-family Cargo.toml files returns 0 matches.
- `cargo build --workspace --all-targets` and `make ci` both exit 0.
- `git diff Cargo.lock` shows exactly 6 workspace-member version hunks, no third-party drift.
</verification>

<success_criteria>
All 6 workspace-family crates (nono, nono-cli, nono-proxy, nono-shell-broker,
nono-fltmgr-client, nono-ffi) report `0.66.1` via their Cargo.toml and via `cargo metadata`;
internal path-dep pins are consistent; `Cargo.lock` is regenerated; `make build`/`make ci`
pass clean.
</success_criteria>

<output>
Create `.planning/phases/100-release-reconcile-leapfrog-0-66-1-pipeline-pypi-blocker/100-01-SUMMARY.md` when done.
</output>
