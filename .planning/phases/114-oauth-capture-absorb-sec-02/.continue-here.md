---
context: phase
phase: 114-oauth-capture-absorb-sec-02
task: 9
total_tasks: 11
status: in_progress
last_updated: 2026-08-07T07:49:55.799Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each was discovered through failure — two of them repeatedly,
> during this phase. Acknowledge each explicitly before proceeding.

- [ ] CONSTRAINT: No SDK state writers — `gsd-sdk query state.*`, `roadmap.*`, and `phase.complete` destroy this repo's hand-maintained STATE.md. The orchestrator hand-edits both tracking files; executors are forbidden from touching them.
- [ ] CONSTRAINT: Narrow verify commands lie — a plan's `files_modified` has under-predicted its real blast radius in **every plan of this phase**, and only `cargo build --workspace --all-targets` has ever caught it.
- [ ] CONSTRAINT: Executor Self-Check is not security evidence — the orchestrator must personally re-audit security claims at source. Doing this on 114-05 is what found a **live token leak** that the executor's own PASSED self-check did not surface.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| SDK state-writer corruption | `state.advance-plan` previously deleted ~4,700 chars of STATE.md's `stopped_at`; `state.begin-phase` deleted an entire `parallel_milestone` block. Documented repeat failures. | blocking | Orchestrator hand-Edits only. Every executor prompt carries an explicit prohibition. `state.begin-phase` was deliberately skipped at phase start. |
| Under-predicted blast radius | Adding one field to an exhaustively-constructed struct breaks literal sites the plan never named. Hit in every plan: 114-01 → 5 extra sites; 114-02 → 36 vs 3 predicted; 114-03 → 2 more; 114-08 → a missing `PartialEq` + 2 more. Each time the task's own narrower verify passed while the workspace was broken. | blocking | Every plan ends with `cargo build --workspace --all-targets` exit 0, never a `-p <crate>` command. Written into every executor prompt as a success criterion. |
| Self-check as security evidence | Phase 112 shipped 4 Critical fail-open defects; all 8 executor self-checks reported PASSED and the code-review gate caught them. **This phase reproduced the pattern:** 114-05's executor reported success while a `spiffe`+`capture` route still leaked the real token through relay sites 2/3. Only the orchestrator's hand audit of `reverse.rs` found it. | blocking | Orchestrator personally re-reads control flow and greps claimed symbols for every security-critical plan. The `code_review_gate` before phase verification is mandatory and must not be skipped. |
| Stale plan line numbers | Plans cite `file:line`, but earlier plans in the SAME phase edit those files first. 114-05/114-06 heavily rewrote `reverse.rs`; 114-06 also found one of its own plan's literal grep-count criteria had gone stale (`[0u8; 8192]` count went 3 → 4 legitimately). | advisory | Re-locate every symbol by NAME. If a literal grep count in a plan disagrees with reality, verify by content inspection and record it rather than forcing the number. |
| Bash CWD drift | A `cd` in one Bash call persists into later calls. A spot-check grep silently reported "No such file" for files that exist, briefly looking like an executor had failed. | advisory | Prefix orchestrator spot-checks with `cd /c/Users/OMack/nono &&`, or use absolute paths. |
| Interrupted executor leaves partial work | 114-03, 114-05 and 114-07 were each interrupted mid-plan, leaving work uncommitted (114-03's was non-compiling) or committed-but-unsummarised. | advisory | `git status` BEFORE dispatching any executor. If the target file is dirty or plan commits already exist, tell the successor explicitly — verify-then-adopt, don't blind-`checkout`. Precedent: Phase 112 Plan 04. |

<current_state>
Phase 114 (OAuth Capture Absorb, SEC-02) — the final phase of milestone v3.6 — is nearly done.

**8 of 11 plans complete; 114-07 is ~90% done and its work is committed with a clean tree.**

Executing sequentially on the main working tree (`workflow.use_worktrees=false`, required because
Plan 114-10 reaches `../nono-py` and `../nono-ts` via `..`).
Branch: `milestone/v2.13-carryforward-closeout`. Pre-phase base: `b29211aa`. HEAD: `5131556f`.

**The security story is complete on the code path.** All three response-relay sites are guarded by
the fail-closed enforcement point, the mint-to-resolve loop is closed with a live production call
chain, and the cross-path guard makes capture-declared upstreams unreachable via forward-HTTP and
CONNECT. What remains is one test, one SUMMARY, the binding rebuild, and the ADR/phase-gate plan.

**Repo state is green right now:** `cargo build --workspace --all-targets` exit 0,
`cargo fmt --all -- --check` clean, `cargo clippy --workspace --all-targets -D warnings
-D clippy::unwrap_used` exit 0, `cargo test -p nono-sandbox-proxy --lib` 295/295.

**⚠ TRACKING GAP — do not trust STATE.md or ROADMAP.md.** Both were hand-updated after Wave 1 only
(commit `80d87512`, recording 2/11). They are stale by **seven plans**. Real position is 8/11
complete + 1 nearly done.
</current_state>

<completed_work>

Completed plans (each with a committed SUMMARY.md, orchestrator-spot-checked at symbol level):

- **114-01** — `CaptureAuditContext` + `NetworkAuditDenialCategory::CaptureUnsupportedPath` (`crates/nono/src/undo/types.rs`); `EventContext.capture_context` / `NetworkAuditEvent.capture_context` through all 4 `push_event` sites.
- **114-02** — `CaptureConfig` / `CaptureResponseField` / `CaptureResponseFieldKind` (`nono-proxy/src/config.rs`), `RouteConfig.capture`.
- **114-03** — `LoadedRoute.declares_capture`, `has_capture_source()`, `RouteStore::capture_declared_for_upstream()` (**host-only** per D-06), `host_only_matches()`. Widening proven at matcher and store level.
- **114-04** — `nono-proxy/src/capture.rs`: zeroized session-scoped `CapturePhantomStore`, `rewrite_response_fields`, `reject_unrewritten_token_fields`, `jwt_shaped_phantom`, `resolve_request_nonce_fields`. 17 tests, including a **self-enforcing** source scan that fails the build if production code ever gains `std::fs::` / `File::create` / `.write(` — D-08 enforced structurally.
- **114-05** — `relay_response_with_capture()` (`reverse.rs:1671`), wired into relay site 1. Fail-closed at all 8 non-success branches. Its SUMMARY carries the orchestrator's hand control-flow audit.
- **114-08** — `profile/credential_provider.rs` validation, `CustomCredentialDef.capture`, real `network_policy.rs` conversion.
- **114-09** — `$defs.CaptureConfig` + `$defs.CaptureResponseField` in the strict fork-only `nono-profile.schema.json`, `capture` on `$defs.CustomCredentialDef`, round-trip + 2 negative tests. D-13 verified by diffing schema properties against the real Rust structs — no property without a Rust field, `required: ["path"]` correct (only `path` lacks `#[serde(default)]`).
- **114-06** — **closed the live leak.** Sites 2 and 3 wired via a shared `relay_capture_if_declared()` guard, each proven by its own test (WR-13). **Task 3 closed the mint-to-resolve loop.**

In progress:

- **114-07** — commits `0bd472cb` (guard + CONNECT audit refinement) and `5131556f` (fmt fix). Working tree clean. Forward-HTTP guard at `server.rs:1096` using `capture_declared_for_upstream()` + `CaptureUnsupportedPath`, explicitly **unconditional** (documented in-source as NOT gated on `require_auth`). CONNECT `denial_category` refined at `server.rs:1397-1401`. Three tests: `forward_http_denies_capture_declared_route_upstream`, `forward_http_non_capture_route_still_forwards`, `connect_denies_capture_declared_route_upstream`.

### The security verification trail (the most important thing in this record)

This phase reproduced Phase 112's defect pattern and caught it:

1. 114-05's executor reported success. The orchestrator's **hand audit** of `reverse.rs` then found
   that dispatch on `has_spiffe_source()` at `:222` returns **before** the capture branch at `:486`,
   and neither SPIFFE handler contained any `capture` reference — so a route with both `spiffe` and
   `capture` **relayed the real OAuth token unrewritten**. Recorded openly as Finding 3 in
   `114-05-SUMMARY.md` rather than deferred silently.
2. 114-06 closed it. Re-verified at source: both sites now do
   `if let Some(result) = relay_capture_if_declared(...).await { return result; }` before their
   `[0u8; 8192]` buffers, and the guard returns `None` only via `let capture = capture?;` — so
   capture-declared unconditionally returns, and non-capture still streams (D-06 intact).
3. The mint-to-resolve chain was verified to be **live production code**, not just written:
   `handle_reverse_proxy` (`reverse.rs:428`) → `resolve_capture_request_body` (`:2007`) →
   `capture::resolve_request_nonce_fields` (`:2022`) → `store.resolve()` (`capture.rs:320`).
   The e2e test mints a phantom, drives a real request through `handle_reverse_proxy` against a
   hermetic TLS upstream, and asserts upstream receives the REAL token and NOT the phantom.
</completed_work>

<remaining_work>

- **114-07 (finish)** — two things:
  1. **The missing host-only-widening test at the guard layer.** D-06's core security property is
     that `capture_declared_for_upstream()` matches HOST-ONLY, so a capture-declared host reached
     on a *different port* is still denied. A grep of `server.rs` finds no such test. The property
     *is* proven one layer down (114-03's `test_capture_declared_for_upstream_true_across_different_port`),
     but not at the guard itself. Add it.
  2. Write and commit `114-07-SUMMARY.md`, recording the CONNECT finding: verify at source whether
     `is_route_upstream()` really does block CONNECT unconditionally (the plan asserts it; confirm
     rather than inherit).
- **114-10** (Wave 5) — rebuild `../nono-py` (`maturin build`) and `../nono-ts`
  (`napi build --platform --release`). **D-14: drive this from real compiler output, never
  prediction.** This phase has now under-predicted blast radius five times; the plan predicts two
  `RouteConfig` literals in nono-py (`proxy.rs`, `policy.rs`) and claims nono-ts is structurally
  immune. Phase 113 predicted 1 field and found 3 files. Expect more. Note `RouteConfig` gained
  `capture` AND `NetworkAuditEvent`/`EventContext` gained `capture_context`.
- **114-11** (Wave 6) — `proj/ADR-114-oauth-capture-disposition.md` (needs `git add -f`, see
  Infrastructure), ledger carry-forward note naming `149abde0`, `REQUIREMENTS.md` SEC-02 checkbox,
  and the combined verification gate: **both cross-target clippy gates**, `cargo fmt --all --check`,
  and a workspace test-baseline diff.

Then the orchestrator still owes, after all plans land:
- Reconcile STATE.md + ROADMAP.md for plans 114-03 through 114-11 (stale by seven plans).
- `code_review_gate` (`/gsd:code-review 114`) — **mandatory**; this is the gate that caught Phase
  112's 4 Criticals.
- `regression_gate`, then `verify_phase_goal` via `gsd-verifier`.
</remaining_work>

<decisions_made>

- **Skip every SDK state/roadmap writer; hand-edit instead.** Deliberate, documented deviation from the execute-phase workflow's `validate_phase` step.
- **The orchestrator personally re-audits every security-critical claim at source.** This is what found the 114-05 leak. Do not relax it for 114-11's ADR claims either — ADR-114 must only assert SC2 "satisfied by construction" against verified code.
- **Adopt-then-verify interrupted executor work** rather than discarding (114-03, 114-05, 114-07). Precedent: Phase 112 Plan 04.
- **Post-wave gates scoped to touched crates.** Full-surface verification is 114-11's job.
- **Accepted 114-06's shared `relay_capture_if_declared()`** over the plan's literal per-site inline duplication — `ManagedUpstreamAuth`/`SpiffeAssertionTokenCache` have no test-only constructor bypassing a live SPIRE connection, so inline wiring could not be driven end-to-end. WR-13 independence is preserved because each site still has its own test.
- **Accepted 114-04's `mint() -> Result<String>`** over the plan's `-> String` — fails closed on RNG failure.
- **`capture.rs` helpers are `pub`, not `pub(crate)`** — under `-D warnings` a `pub(crate)` fn with only `#[cfg(test)]` callers is `dead_code`, and `#[allow(dead_code)]` is banned by CLAUDE.md. Now moot for most of them since 114-06 gave them production callers.
</decisions_made>

<blockers>
- None. No external dependency, no human action pending, no failing gate. Paused by operator choice.
</blockers>

## Required Reading (in order)

1. `.planning/phases/114-oauth-capture-absorb-sec-02/114-05-SUMMARY.md` — the control-flow audit. The single most useful document for understanding what this phase actually guarantees.
2. `.planning/phases/114-oauth-capture-absorb-sec-02/114-CONTEXT.md` — the 12 LOCKED decisions (D-01r, D-02r, D-05..D-14). D-06 governs the remaining 114-07 work; D-14 governs 114-10.
3. `.planning/phases/114-oauth-capture-absorb-sec-02/114-07-PLAN.md` — the plan being finished.
4. `crates/nono-proxy/src/server.rs` around `:1096` (forward-HTTP guard) and `:1397` (CONNECT refinement).
5. `./CLAUDE.md` — coding standards and non-negotiable security rules.
6. `.planning/STATE.md` — read the tracking-gap warning above first; it is stale by seven plans.

## Infrastructure State

- **Host:** Windows 11. `make` is NOT installed — use `cargo build --workspace --all-targets`, `cargo test -p <crate>`, `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used`.
- **Crate names are fork-renamed:** `nono-sandbox`, `nono-sandbox-proxy`, `nono-sandbox-cli` for `-p`; `use nono::` / `use nono_proxy::` import paths unchanged.
- **Never run** the full `cargo test -p nono-sandbox-cli --tests` sweep — 36 binaries, stalls ~25 minutes.
- **Cross-target clippy toolchain is live on this host** and is 114-11's obligation: `cross` (Docker) for `x86_64-unknown-linux-gnu`, `cargo-zigbuild` for `x86_64-apple-darwin` with `SDKROOT` unset.
- **`rcgen` was added as a `nono-proxy` dev-dependency** by 114-06 (already vetted in-workspace via `nono-cli`) to build the hermetic TLS upstream for the e2e proof.
- **Known pre-existing test failures — not regressions, do not chase:** 11 in `cargo test -p nono-sandbox-cli --bin nono` (6× `config::tests::*`, 3× `protected_paths::tests::*`, `profile_cmd::test_init_allowed_when_pack_has_same_short_name`, `audit_session::discover_sessions_does_not_warn_when_legacy_audit_root_is_empty`) plus `try_set_mandatory_label` in the core lib.
- **`.gitignore:16`'s bare `proj/`** silently swallows NEW files under `proj/` — 114-11's `proj/ADR-114-*.md` needs `git add -f`. Likewise `.gitignore:34`/`:36` ignore `.continue-here.md` and `HANDOFF.json`, so these handoff files also need `git add -f` (matching prior handoff commits).
- **Untracked, out of scope:** `.planning/NONO_WINDOWS_DESIGN_REVIEW_FUTURE_PHASE_v0.1.md` predates this session. Leave it alone.
- No background processes or servers running.

<context>
The strategic point: operator decision D-01r retracted a formal DECLINE of upstream's OAuth capture
and replaced it with an ADAPT, on the promise that the fork builds its OWN response
buffer-and-rewrite enforcement point. The investigation behind that retraction overturned the
original premise — the fork's reverse proxy already has plaintext visibility into upstream
responses, so TLS interception was never required for token endpoints reachable as configured
routes. The real blocker was buffering, not visibility.

That promise is now kept in code: the enforcement point exists, all three relay sites use it, the
mint-to-resolve loop is closed with a live production chain, and cross-path access is denied. The
remaining plans are about proving and recording it, not building it.

Two things to carry into 114-11 specifically. First, ADR-114 must correct
`112-OAUTH-CAPTURE-DISPOSITION.md`'s still-reads-as-`forward.rs`-only framing — CONTEXT.md is
explicit that a future reader will otherwise re-derive the same wrong premise, and there are grep
acceptance criteria banning the old "beyond same-route scope" phrasing. Second, ADR-114 may only
claim SC2 "satisfied by construction" against code that has actually been audited — this phase
already produced one artifact-level near-miss where an executor's PASSED self-check coexisted with
a live token leak.
</context>

<next_action>
Start with: finish Plan 114-07. Add the missing host-only-widening test at the guard layer — a
capture-declared route whose upstream is reached on a DIFFERENT port must still be denied by
`handle_forward_http` (D-06's core property; currently proven only one layer down in `route.rs`).
Then verify at source that `is_route_upstream()` blocks CONNECT unconditionally, and write and
commit `114-07-SUMMARY.md` recording both. Then run `/gsd:execute-phase 114` — plan discovery will
skip the completed plans and resume at 114-10.
</next_action>
