---
phase: 114-oauth-capture-absorb-sec-02
plan: 06
type: execute
wave: 4
depends_on: ["114-05"]
files_modified:
  - crates/nono-proxy/src/reverse.rs
autonomous: true
requirements: [SEC-02]

must_haves:
  truths:
    - "D-06: all three reverse.rs relay sites (static-credential, handle_spiffe_route, handle_spiffe_assertion_credential) apply the same buffer-and-rewrite helper when their route/cred declares capture — proven independently per site, per the WR-13 lesson that one path of several ignoring a gate is the defect shape"
    - "A grep for [0u8; 8192] in reverse.rs still returns exactly 3 distinct relay loops after this plan — no fourth hand-duplicated buffering implementation was introduced"
    - "D-02r: the mint-to-resolve loop is closed end-to-end — CapturePhantomStore::resolve() is reachable from a real production request-dispatch path in reverse.rs, not only from capture.rs's own unit tests, closing the SC2 half-feature gap the plan-checker found"
    - "D-06: resolution fails closed for a non-admitted consumer — an unresolved/unadmitted phantom in a request_nonce_fields path is left unchanged (never substituted with a real token), proven by an independent test"
  artifacts:
    - path: "crates/nono-proxy/src/reverse.rs"
      provides: "relay_response_with_capture() call sites at handle_spiffe_route and handle_spiffe_assertion_credential; resolve_capture_request_body() wired into handle_reverse_proxy's outbound request-body path"
      contains: "relay_response_with_capture"
  key_links:
    - from: "crates/nono-proxy/src/reverse.rs"
      to: "crates/nono-proxy/src/reverse.rs"
      via: "handle_spiffe_route / handle_spiffe_assertion_credential each call relay_response_with_capture when their route/cred declares capture"
      pattern: "relay_response_with_capture"
    - from: "crates/nono-proxy/src/reverse.rs"
      to: "crates/nono-proxy/src/capture.rs"
      via: "resolve_capture_request_body() calls capture::resolve_request_nonce_fields(), which calls CapturePhantomStore::resolve() — the production call site the plan-checker found missing"
      pattern: "resolve_request_nonce_fields"
---

<objective>
Wire Plan 114-05's `relay_response_with_capture()` helper into the remaining two of the three
`reverse.rs` response-relay sites — `handle_spiffe_route` (site 2, direct JWT-SVID bearer path)
and `handle_spiffe_assertion_credential` (site 3, RFC 7523 jwt-bearer exchange path) — and prove
each independently, per WR-13's lesson that "one path of six/three ignoring a gate" is the exact
defect shape a shared assertion across sites would mask.

Purpose: a capture-declared route's SPIFFE-authenticated request-side auth mechanism must not
determine whether its RESPONSE gets buffered and rewritten — the enforcement point applies
regardless of which of the three credential-dispatch paths served the request. Without this
plan, a route configured with BOTH `spiffe` (request-side auth) AND `capture` (response-side
rewrite) would leak the real token through sites 2 or 3 even though site 1 is protected.

This plan also closes the mint-to-resolve loop (Task 3): Plan 114-04 built
`CapturePhantomStore::resolve()` and `capture::resolve_request_nonce_fields()`, but as of Wave 3
neither has a caller outside `capture.rs`'s own tests — a minted phantom could never actually be
redeemed by the sandboxed agent on a later request. Task 3 wires `resolve_request_nonce_fields()`
into `handle_reverse_proxy`'s outbound request-body path, giving `resolve()` a real production
call site and making `CaptureConfig.request_nonce_fields` live, consumed configuration instead of
reserved-and-unused. Without Task 3, SEC-02 would ship exactly the forbidden half-feature the
ROADMAP goal names verbatim: real tokens blinded on the way in, but never usable on the way back
out.

Output: sites 2 and 3 both call `relay_response_with_capture()`; 2 new independent tests (one per
site) proving the rewrite fires; a regression grep proving no 4th hand-duplicated loop exists;
`resolve_capture_request_body()` wired into `handle_reverse_proxy` with two independent tests
proving admitted-resolves and non-admitted-fails-closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-oauth-capture-absorb-sec-02/114-CONTEXT.md
@.planning/phases/114-oauth-capture-absorb-sec-02/114-RESEARCH.md
@.planning/phases/114-oauth-capture-absorb-sec-02/114-PATTERNS.md
@.planning/phases/112-security-residual-sync/112-REVIEW.md
</context>

<interfaces>
<!-- Exact live code the executor must mirror. Verified live this session (site line numbers
confirmed unchanged after Plan 114-05 lands, since 114-05 only edits site 1 and adds new private
helpers above/below the existing sites, not between them — re-grep before editing per this
phase's "verify by symbol, never by line number" rule).

GROUND-TRUTH CAVEAT: every line number cited anywhere in this plan (this file's own `<interfaces>`
sections, both for sites 2/3 and for Task 3 below) was verified live at planning time, BEFORE
Plans 114-05 and this plan's own Tasks 1-2 land their edits to `reverse.rs`. By the time Task 3
runs, Tasks 1 and 2 in THIS SAME plan will already have inserted new dispatch checks and (per Task
2's own action) possibly a new parameter into `handle_spiffe_assertion_credential`'s signature —
every line number below WILL have shifted. Locate every insertion point by symbol
(`grep -n "let cred = ctx.credential_store.get"`, `grep -n "Content-Length for body"`, etc.), never
by the line numbers this plan cites. The compiler and grep are ground truth. -->

Site 2 — `handle_spiffe_route` (`crates/nono-proxy/src/reverse.rs:529` function start,
`:763-785` the relay loop, verified live this session, structurally identical to site 1 except
variable names):
```rust
async fn handle_spiffe_route(
    first_line: &str,
    stream: &mut TcpStream,
    remaining_header: &[u8],
    ctx: &ReverseProxyCtx<'_>,
    buffered_body: &[u8],
    service: &str,
    upstream_path: &str,
    route: &LoadedRoute,
) -> Result<()> {
    // ... acquires a fresh JWT-SVID, builds + sends the outbound request ...
    let mut response_buf = [0u8; 8192];
    // ... same [0u8; 8192] read/write loop as site 1 ...
}
```
`route: &LoadedRoute` is already a parameter here — `route.capture` is directly accessible, no new
parameter threading needed. Insert the SAME dispatch check used at site 1: `if let Some(capture) =
&route.capture { return relay_response_with_capture(&mut tls_stream, stream, capture,
ctx.capture_store, service, ctx.audit_log).await; }` immediately before this site's relay loop.

Site 3 — `handle_spiffe_assertion_credential` (`crates/nono-proxy/src/reverse.rs:831` function
start, `:1019-1040` the relay loop, verified live this session). This function does NOT take a
`route: &LoadedRoute` parameter (it dispatches off `ctx.credential_store.get_spiffe_assertion(&service)`,
a `SpiffeAssertionRoute` from `credential.rs`, not a `LoadedRoute`) — read the function's FULL
current parameter list and the `SpiffeAssertionRoute` type before writing this task's action, to
determine whether capture applies here via `ctx.route_store.get(service)` (looking the route back
up by service name, mirroring how the dispatch site at `reverse.rs:216-241` already has both
`route` and `service` in scope before branching to either SPIFFE handler) or via a field this
plan must add to `SpiffeAssertionRoute` itself — this is a genuine "read first, decide, then act"
task, not a copy-paste of site 2's pattern; do not assume the same call shape works verbatim.

Task 3 — the mint-to-resolve loop closure. Symbols to locate in `handle_reverse_proxy` (the
site-1 parent function, NOT `handle_spiffe_route`/`handle_spiffe_assertion_credential` — Task 3
is scoped to this one function, the simplest real production path, per this phase's discretion
grant on "how resolution composes"):
- `let cred = ctx.credential_store.get(&service);` — the existing credential lookup, unaffected.
- `let body = if let Some(len) = content_length { ... } else { Vec::new() };` — the existing
  request-body read block. Insert the new resolution step immediately AFTER this block completes
  (so `body` holds the full, already-capped request body) and BEFORE the `Content-Length for
  body` comment / the `tls_stream.write_all(&body)` call.
- `capture::resolve_request_nonce_fields(body: &mut Value, fields: &[String], store:
  &CapturePhantomStore, consumer: &str) -> usize` — Plan 114-04's already-built, already-tested
  request-side resolution function (`pub(crate)`, `crate::capture` module). This task's new
  `resolve_capture_request_body` is a thin JSON-parse/serialize wrapper around it — do NOT
  reimplement the dot-path traversal or the admission check a second time in `reverse.rs`.
- Why wiring through `capture::resolve_request_nonce_fields` (rather than calling
  `ctx.capture_store.resolve(...)` directly from `reverse.rs`) satisfies the blocker: the call
  chain `reverse.rs::resolve_capture_request_body` -> `capture::resolve_request_nonce_fields` ->
  `CapturePhantomStore::resolve` is now reachable from a live, production request-dispatch path
  (grep-provable — see Task 3's acceptance criteria), not just from `capture.rs`'s own test
  module. This is the exact gap the plan-checker's blocker named.
- Zeroize note (mirrors the already-accepted precedent in Plan 114-04's `rewrite_response_fields`
  and Plan 114-05's response-body reassembly): the real token exists as a `serde_json::Value`
  inside the parsed request body tree for the brief window between resolution and
  re-serialization, and the final re-serialized `Vec<u8>` is written to the wire and dropped
  immediately — the SAME accepted tradeoff already used for the response-side rewrite, not a new
  one. Do not attempt to wrap the `serde_json::Value` tree itself in `Zeroizing` (the type does
  not support it); the discipline that DOES apply: `CapturePhantomStore::resolve()`'s own return
  value stays `Zeroizing<Vec<u8>>` right up until it is written into the `Value` tree — never
  clone it into a second, non-`Zeroizing` binding first.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire site 2 (handle_spiffe_route) + independent proof</name>
  <files>crates/nono-proxy/src/reverse.rs</files>
  <read_first>
    crates/nono-proxy/src/reverse.rs (handle_spiffe_route in full, including its call site at
    lines 216-228 in handle_reverse_proxy)
  </read_first>
  <behavior>
    - A route with BOTH `spiffe` (or the equivalent SPIFFE-JWT dispatch condition) AND `capture`
      set: a request through `handle_spiffe_route` still gets its response buffered and
      rewritten — proven by a test independent of site 1's own test (different test function,
      different name, exercising `handle_spiffe_route`'s own dispatch path specifically) —
      `all_three_relay_sites_apply_capture_rewrite` (site 2 half) or a discretely-named
      `capture_rewrites_via_spiffe_route_site`.
  </behavior>
  <action>
    Insert the dispatch check shown in this plan's `<interfaces>` section immediately before site
    2's relay loop in `handle_spiffe_route`. Add the behavior-listed test to `reverse.rs`'s
    existing `mod tests` block, reusing whatever SPIFFE-route test fixture pattern
    `handle_spiffe_route`'s own existing tests (from Phase 113) already establish, extended with a
    `capture: Some(...)` config on the fixture route.
  </action>
  <acceptance_criteria>
    `grep -c "relay_response_with_capture" crates/nono-proxy/src/reverse.rs` returns at least 2
    after this task (site 1's call from Plan 114-05 + site 2's new call).
    `cargo test -p nono-sandbox-proxy --lib reverse:: -- --nocapture` exits 0, including the new
    site-2 test by name.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-proxy --lib reverse:: -- --nocapture 2>&1 | tail -30</automated>
  </verify>
  <done>Site 2 (handle_spiffe_route) applies the capture rewrite when its route declares it,
  proven by an independent test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire site 3 (handle_spiffe_assertion_credential) + independent proof + regression grep</name>
  <files>crates/nono-proxy/src/reverse.rs</files>
  <read_first>
    crates/nono-proxy/src/reverse.rs (handle_spiffe_assertion_credential in full, its call site at
    lines 229-241, and credential.rs's SpiffeAssertionRoute type definition — read
    crates/nono-proxy/src/credential.rs for the type)
  </read_first>
  <behavior>
    - A route reachable via `handle_spiffe_assertion_credential`'s dispatch condition (an
      OAuth2 jwt-bearer assertion route) that ALSO declares `capture` gets its response buffered
      and rewritten — proven by a third independent test, distinct from sites 1 and 2's tests —
      `capture_rewrites_via_spiffe_assertion_site` (or the third of 3 discrete tests satisfying
      `all_three_relay_sites_apply_capture_rewrite`).
    - After this task, `grep -c "\[0u8; 8192\]" crates/nono-proxy/src/reverse.rs` still returns
      exactly 3 — confirming no fourth hand-duplicated relay loop was introduced (Pitfall 1,
      RESEARCH.md — the WR-13-class defect this shared-helper design exists to prevent).
  </behavior>
  <action>
    Determine (by reading `handle_spiffe_assertion_credential`'s full current signature and the
    `SpiffeAssertionRoute` type) the correct way to reach the capture config for this dispatch
    path: EITHER thread an additional `route: Option<&LoadedRoute>` (looked up via
    `ctx.route_store.get(service)` at the call site, `reverse.rs:229-241`, before this handler is
    invoked) so the handler can check `route.and_then(|r| r.capture.as_ref())`, OR add a
    `capture: Option<crate::config::CaptureConfig>` field to `SpiffeAssertionRoute` itself if that
    type is constructed with direct access to the route's config already — pick whichever requires
    the smaller, more consistent diff against the existing dispatch chain (favor NOT adding a new
    parameter if the route is already cheaply re-derivable from `service`, since `ctx.route_store`
    is already in scope). Insert the dispatch check immediately before site 3's relay loop,
    mirroring sites 1 and 2's exact call shape. Add the behavior-listed test. Add a final
    regression test (or a documented grep-based CI assertion, per this plan's acceptance criteria)
    proving exactly 3 `[0u8; 8192]` occurrences remain.
  </action>
  <acceptance_criteria>
    `grep -c "relay_response_with_capture" crates/nono-proxy/src/reverse.rs` returns at least 3
    after this task (all three sites).
    `grep -c "\[0u8; 8192\]" crates/nono-proxy/src/reverse.rs` returns exactly 3.
    `cargo test -p nono-sandbox-proxy --lib reverse:: -- --nocapture` exits 0, including the new
    site-3 test by name, and all three site-specific capture tests (site 1 from Plan 114-05, sites
    2 and 3 from this plan) present and passing independently.
    `cargo clippy -p nono-sandbox-proxy --lib -- -D warnings -D clippy::unwrap_used` exits 0.
    `cargo fmt --all --check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-proxy --lib reverse:: -- --nocapture 2>&1 | tail -30 && cargo clippy -p nono-sandbox-proxy --lib -- -D warnings -D clippy::unwrap_used 2>&1 | tail -20</automated>
  </verify>
  <done>All three reverse.rs relay sites apply the capture rewrite when their route/cred declares
  it, each proven by an independent test; exactly 3 [0u8;8192] loops exist (no 4th duplicate);
  cross-target-relevant clippy clean on the Windows host (cross-target gates run in Plan
  114-11).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close the mint-to-resolve loop — wire resolve_request_nonce_fields into a real outbound dispatch path</name>
  <files>crates/nono-proxy/src/reverse.rs</files>
  <read_first>
    crates/nono-proxy/src/reverse.rs (Tasks 1 and 2's output in THIS plan, current state —
    re-grep for `let cred = ctx.credential_store.get`, the request-body read block, and the
    `Content-Length for body` comment; do not trust this plan's line numbers, per the
    ground-truth caveat in this plan's `<interfaces>` section)
    crates/nono-proxy/src/capture.rs (Plan 114-04's `resolve_request_nonce_fields` — exact
    signature, exact fail-closed behavior on an unresolved/unadmitted path)
    crates/nono-proxy/src/config.rs (`CaptureConfig.request_nonce_fields` — Plan 114-02's field)
  </read_first>
  <behavior>
    - `resolve_capture_request_body(body: &[u8], capture: Option<&CaptureConfig>, capture_store:
      &CapturePhantomStore, consumer: &str) -> Vec<u8>` returns `body` UNCHANGED (byte-for-byte)
      when `capture` is `None`, or when `capture.request_nonce_fields` is empty, or when `body` is
      not valid JSON — this is the safe, structural default (no capture-declared route with no
      configured nonce fields is ever affected; a malformed body is forwarded as-is, matching the
      fork's existing behavior for every other route).
    - Given a JSON body containing a phantom value (previously minted and admitted for `consumer`)
      at a path listed in `capture.request_nonce_fields`, the returned bytes are the
      re-serialized body with that path's value replaced by the REAL captured token — proving (i)
      required by the revision: "a minted phantom resolves to the real token for an admitted
      consumer on the real request path" (`capture_egress_resolves_admitted_phantom_in_request_body`).
    - Given the SAME JSON body but a phantom that is either unknown to the store OR known but NOT
      admitted for `consumer`, the returned bytes leave that path's value UNCHANGED (still the
      phantom, never a real token) — proving (ii) required by the revision: "resolution fails
      closed for a non-admitted consumer"
      (`capture_egress_fails_closed_for_unadmitted_consumer_in_request_body`).
    - No call in this function's path ever logs, audits, or otherwise records the real resolved
      value — `CaptureAuditContext` (Plan 114-01) carries route/field identifiers only, and this
      function has no audit call at all.
  </behavior>
  <action>
    Add a new private function `resolve_capture_request_body(body: &[u8], capture:
    Option<&crate::config::CaptureConfig>, capture_store: &crate::capture::CapturePhantomStore,
    consumer: &str) -> Vec<u8>` to `reverse.rs`: return `body.to_vec()` immediately if `capture`
    is `None` or its `request_nonce_fields` is empty; otherwise attempt
    `serde_json::from_slice::<serde_json::Value>(body)`, on `Err` return `body.to_vec()`
    unchanged; on `Ok(mut value)` call `crate::capture::resolve_request_nonce_fields(&mut value,
    &capture.request_nonce_fields, capture_store, consumer)`, and if the returned count is 0
    return `body.to_vec()` unchanged (avoids a needless re-serialize round-trip that could
    reformat whitespace/field-order for a body nothing was resolved in), else return
    `serde_json::to_vec(&value).unwrap_or_else(|_| body.to_vec())` (fail-safe: a serialize error
    falls back to the original bytes, never panics, never drops the request). Wire one call site
    into `handle_reverse_proxy`: immediately after the existing request-body read block completes
    (`let body = if let Some(len) = content_length { ... } else { Vec::new() };`), add `let body =
    resolve_capture_request_body(&body, route.capture.as_ref(), ctx.capture_store, &service);` —
    shadowing the existing `body` binding, so every downstream use (the `Content-Length: {}`
    header computation and the `tls_stream.write_all(&body)` call) automatically sees the
    resolved bytes with zero further changes needed downstream. Add the two behavior-listed tests
    directly against `resolve_capture_request_body` (pure function, no network I/O required) to
    `reverse.rs`'s existing `mod tests` block, using `CapturePhantomStore::mint(...)` to seed a
    real phantom-to-token mapping with a known `admitted_consumers` set before asserting resolve
    or non-resolve behavior.
  </action>
  <acceptance_criteria>
    `grep -c "fn resolve_capture_request_body" crates/nono-proxy/src/reverse.rs` returns 1.
    `grep -c "resolve_capture_request_body(&body" crates/nono-proxy/src/reverse.rs` returns 1
    (the production call site inside `handle_reverse_proxy` — this is the grep proof the
    plan-checker's blocker demanded: `resolve()` is no longer reachable only from
    `capture.rs`'s own tests).
    `grep -c "resolve_request_nonce_fields" crates/nono-proxy/src/reverse.rs` returns at least 1.
    `cargo test -p nono-sandbox-proxy --lib reverse:: -- --nocapture` exits 0, including
    `capture_egress_resolves_admitted_phantom_in_request_body` and
    `capture_egress_fails_closed_for_unadmitted_consumer_in_request_body` by name in the test
    output.
    `cargo clippy -p nono-sandbox-proxy --lib -- -D warnings -D clippy::unwrap_used` exits 0.
    `cargo fmt --all --check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-proxy --lib reverse:: -- --nocapture 2>&1 | tail -40</automated>
  </verify>
  <done>resolve_capture_request_body exists, is wired into handle_reverse_proxy's real request-body
  path (grep-proven production call site), and both required behaviors (admitted-resolves,
  non-admitted-fails-closed) pass as independent tests — CapturePhantomStore::resolve() now has a
  live caller outside capture.rs's own test module, closing the SC2 half-feature gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Upstream OAuth token endpoint -> proxy, reached via any of 3 credential-dispatch mechanisms | The response-side enforcement point must not depend on which request-side auth mechanism served the request |
| Sandboxed client -> proxy, outbound request body carrying a previously-minted phantom | A presented phantom must resolve to the real token ONLY for the consumer it was admitted to — never for any other route/consumer, and never when the phantom is unknown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-18 | Information Disclosure | `handle_spiffe_route` / `handle_spiffe_assertion_credential` | mitigate | Both now call the SAME shared `relay_response_with_capture` helper Plan 114-05 built and proved — WR-13's "one path of several missing a gate" defect shape is prevented by construction (one helper, three call sites) and proven by construction (3 independent tests, not 1 shared assertion) |
| T-114-19b | Information Disclosure | `resolve_capture_request_body` | mitigate | The mint-to-resolve loop is closed end-to-end (D-02r) — CapturePhantomStore::resolve()'s existing per-consumer admission scoping (Plan 114-04) is the enforcement; this task only wires a real caller, it adds no new trust decision |
| T-114-19c | Elevation of Privilege | Non-admitted or unknown phantom in a request_nonce_fields path | mitigate | resolve_request_nonce_fields leaves the value UNCHANGED (never substitutes a real token) when resolution fails — proven by an independent test (capture_egress_fails_closed_for_unadmitted_consumer_in_request_body), not prose |
</threat_model>

<verification>
`cargo test -p nono-sandbox-proxy --lib reverse::` green, 3 independent per-site capture tests
pass. `grep -c "\[0u8; 8192\]" crates/nono-proxy/src/reverse.rs` returns exactly 3. `cargo clippy`
and `cargo fmt --all --check` clean.
</verification>

<success_criteria>
All three reverse.rs relay sites apply the buffer-and-rewrite enforcement point identically,
proven independently per WR-13's lesson, with no fourth hand-duplicated relay loop. The
mint-to-resolve loop is closed: CapturePhantomStore::resolve() has a real, grep-provable
production call site in handle_reverse_proxy's outbound request-body path, and both required
behaviors (admitted resolves, non-admitted fails closed) are proven by independent tests.
</success_criteria>

<output>
Create `.planning/phases/114-oauth-capture-absorb-sec-02/114-06-SUMMARY.md` when done
</output>
