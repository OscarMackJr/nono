---
phase: 114-oauth-capture-absorb-sec-02
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono-proxy/src/config.rs
  - crates/nono-cli/src/network_policy.rs
  - crates/nono-cli/src/proxy_runtime.rs
autonomous: true
requirements: [SEC-02]

must_haves:
  truths:
    - "D-05: a tight, configurable response-buffer cap constant exists (default 256 KiB, hard ceiling 1 MiB) for later plans to enforce fail-closed"
    - "D-07: RouteConfig gains a capture: Option<CaptureConfig> declarative field following the exact serde idiom of the existing spiffe field, giving later plans a wire format to dispatch on"
    - "Every exhaustive RouteConfig struct literal in the workspace (production and test) compiles after the new field lands — no plan after this one discovers a hidden fifth/sixth break site"
  artifacts:
    - path: "crates/nono-proxy/src/config.rs"
      provides: "CaptureConfig, CaptureResponseField, CaptureResponseFieldKind types + RouteConfig.capture field"
      contains: "pub struct CaptureConfig"
    - path: "crates/nono-cli/src/network_policy.rs"
      provides: "capture: None literal at both RouteConfig construction sites (custom + built-in)"
      contains: "capture: None"
  key_links:
    - from: "crates/nono-proxy/src/config.rs"
      to: "crates/nono-cli/src/network_policy.rs"
      via: "RouteConfig { ..., capture: None, ... } exhaustive literal"
      pattern: "capture:"
---

<objective>
Define the declarative wire-format types for OAuth capture (`CaptureConfig` and friends) and add
`RouteConfig.capture: Option<CaptureConfig>`, then close out every resulting compile break across
the workspace so `cargo build --workspace --all-targets` is green at the end of this plan. This is
the Wave-1 contract that every later plan (route.rs dispatch, capture.rs's rewrite logic,
reverse.rs's enforcement point, the CLI profile layer, and the schema) builds against.

Purpose: give every downstream plan a stable, already-compiling declarative surface instead of
each discovering compile breaks independently — the "interface-first" ordering this phase's task
breakdown requires.
Output: `CaptureConfig`/`CaptureResponseField`/`CaptureResponseFieldKind` in `config.rs`,
`RouteConfig.capture`, a fully green `cargo build --workspace --all-targets`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-oauth-capture-absorb-sec-02/114-CONTEXT.md
@.planning/phases/114-oauth-capture-absorb-sec-02/114-RESEARCH.md
@.planning/phases/114-oauth-capture-absorb-sec-02/114-PATTERNS.md
</context>

<interfaces>
<!-- Exact live code the executor must mirror. Verified live this session. -->

From `crates/nono-proxy/src/config.rs:619-622` (the field pattern to mirror exactly, one field
lower in the same struct):
```rust
    /// SPIFFE/SPIRE workload identity auth. Mutually exclusive with
    /// `credential_key`, `oauth2`, and `aws_auth`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub spiffe: Option<SpiffeAuthConfig>,
```
`RouteConfig` (`config.rs:528`) has NO `#[derive(Default)]` — every field must be given a value
at every construction site in the workspace; there is no struct-update-syntax escape hatch.

From `crates/nono-cli/src/network_policy.rs:222-253` and `:267-284` (the TWO exhaustive
`RouteConfig { ... }` production literals in this workspace — both need a `capture: None,` line
added directly after their existing `spiffe: ...,` line):
```rust
            routes.push(RouteConfig {
                prefix: name.clone(),
                upstream: cred.upstream.clone(),
                credential_key: cred.credential_key.clone(),
                inject_mode: cred.inject_mode.clone(),
                inject_header: cred.inject_header.clone(),
                credential_format: cred.credential_format.clone(),
                path_pattern: cred.path_pattern.clone(),
                path_replacement: cred.path_replacement.clone(),
                query_param_name: cred.query_param_name.clone(),
                env_var: cred.env_var.clone(),
                endpoint_rules: cred.endpoint_rules.clone(),
                tls_ca: cred.tls_ca.as_deref().map(...).transpose()?,
                oauth2: cred.auth.clone(),
                aws_auth: cred.aws_auth.clone(),
                spiffe: cred.spiffe.clone(),
                endpoint_policy: None,
            });
            // ... and the second, built-in-credential branch, which ends:
                oauth2: None,
                aws_auth: None,
                spiffe: None, // Built-in credentials don't support SPIFFE
                endpoint_policy: None,
            });
```
This plan sets BOTH sites to `capture: None,` (a stub — CustomCredentialDef has no `capture`
field yet, that lands in Plan 114-08, which will replace this site's `capture: None,` with the
real conversion). Do NOT attempt to read `cred.capture` here — it does not exist yet.

A THIRD exhaustive `RouteConfig { ... }` literal exists inside this workspace's own test code,
`crates/nono-cli/src/proxy_runtime.rs:1401` (`fn spiffe_route(socket: &str) -> RouteConfig`) —
also needs `capture: None,` added.

There may be additional exhaustive `RouteConfig { ... }` literals inside `nono-proxy`'s own test
modules (`server.rs`, `credential.rs`, `reverse.rs`, `route.rs`, `config.rs`, and
`tests/spiffe_integration.rs`) — `grep -rn "RouteConfig {" crates/ --include=*.rs` before starting
Task 2 to get the authoritative, current list; do not trust this plan's file list as exhaustive,
the compiler is the source of truth (Task 2's acceptance criteria is a clean workspace build, not
a specific file count).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Declarative CaptureConfig types + RouteConfig.capture field</name>
  <files>crates/nono-proxy/src/config.rs</files>
  <read_first>
    crates/nono-proxy/src/config.rs (read lines 500-720 in full — RouteConfig's complete field
    list, OAuth2Config's struct shape for the sibling-optional-config-block precedent, and
    SpiffeAuthConfig's serde attribute idiom)
    .planning/phases/114-oauth-capture-absorb-sec-02/114-RESEARCH.md (Open Question 2 — buffer cap
    value discussion)
  </read_first>
  <action>
    Add three new types to `crates/nono-proxy/src/config.rs`, placed near `SpiffeAuthConfig`:
    (1) `CaptureResponseFieldKind` — a `#[derive(Debug, Clone, Copy, Default, PartialEq, Eq,
    Serialize, Deserialize)]` enum with `#[serde(rename_all = "snake_case")]`, two variants
    `Opaque` (`#[default]`) and `Jwt` — mirrors upstream's `CredentialProviderResponseFieldKind`
    (design reference, D-07). (2) `CaptureResponseField` — a `#[derive(Debug, Clone, PartialEq,
    Eq, Serialize, Deserialize)]` struct with `pub path: String` (dot-separated JSON path into the
    response body) and `#[serde(default)] pub kind: CaptureResponseFieldKind`. (3) `CaptureConfig`
    — a `#[derive(Debug, Clone, Serialize, Deserialize)]` struct with `#[serde(default)] pub
    response_fields: Vec<CaptureResponseField>` (which response JSON fields carry the real token
    and must be rewritten to phantoms), `#[serde(default)] pub request_nonce_fields: Vec<String>`
    (dot-paths in request bodies where a presented phantom must be resolved back to the real
    captured value before forwarding outbound — LIVE, consumed configuration: Plan 114-06 Task 3
    wires this into reverse.rs's egress-resolution dispatch via
    capture::resolve_request_nonce_fields(), the request-side mirror of
    rewrite_response_fields()), and `#[serde(default, skip_serializing_if = "Option::is_none")] pub
    max_response_bytes: Option<usize>` (D-05 per-route override of the default cap; `None` means
    use the default). Add two `pub const` values near `CaptureConfig`:
    `DEFAULT_CAPTURE_MAX_RESPONSE_BYTES: usize = 256 * 1024` (256 KiB — D-05's chosen default,
    picked as "a defensible, generous default for a JSON object holding a handful of JWTs" per
    RESEARCH.md Open Question 2) and `CAPTURE_MAX_RESPONSE_BYTES_CEILING: usize = 1024 * 1024` (1
    MiB — the hard upper bound `max_response_bytes` may not exceed; enforced at profile-validation
    time by Plan 114-08, not here). Finally, add `#[serde(default, skip_serializing_if =
    "Option::is_none")] pub capture: Option<CaptureConfig>` to `RouteConfig`, positioned
    immediately after the existing `spiffe` field, with a doc comment noting it is response-
    direction (unlike `credential_key`/`oauth2`/`aws_auth`/`spiffe`, which are request-direction)
    and therefore composes with them rather than being mutually exclusive — this is a deliberate
    design decision (RESEARCH.md's open Assumption A3), record it in the doc comment so a future
    reader does not "fix" it into the mutual-exclusion block by reflexive analogy.
  </action>
  <acceptance_criteria>
    `grep -c "pub struct CaptureConfig" crates/nono-proxy/src/config.rs` returns 1.
    `grep -c "pub capture: Option<CaptureConfig>" crates/nono-proxy/src/config.rs` returns 1.
    `grep -c "DEFAULT_CAPTURE_MAX_RESPONSE_BYTES" crates/nono-proxy/src/config.rs` returns at
    least 2 (definition + doc reference).
    `cargo build -p nono-sandbox-proxy --lib` FAILS with missing-field errors pointing at
    `RouteConfig` literals — this is EXPECTED at this point in the task and confirms the field
    landed; Task 2 closes those errors.
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p nono-sandbox-proxy --lib 2>&1 | grep -c "missing field"</automated>
  </verify>
  <done>CaptureConfig/CaptureResponseField/CaptureResponseFieldKind exist with the exact field
  shape above; RouteConfig.capture field exists; the crate does not yet compile (expected, closed
  by Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Close every RouteConfig exhaustive-literal break; green workspace build</name>
  <files>crates/nono-cli/src/network_policy.rs, crates/nono-cli/src/proxy_runtime.rs</files>
  <read_first>
    crates/nono-cli/src/network_policy.rs (both RouteConfig construction sites, full function)
    crates/nono-cli/src/proxy_runtime.rs (the `spiffe_route()` test helper, lines ~1395-1425)
  </read_first>
  <action>
    Run `cargo build --workspace --all-targets 2>&1 | grep -B2 "missing field \`capture\`"` to get
    the authoritative, current list of every `RouteConfig { ... }` literal that fails to compile
    (this supersedes any file list in this plan's context — the compiler is ground truth). For
    each reported site, add a `capture: None,` line directly after that literal's existing
    `spiffe: ...,` line (or, if a literal has no `spiffe` line yet, after `aws_auth:`). Do this for
    every reported site — expected sites include (but are not limited to)
    `network_policy.rs`'s two production literals and `proxy_runtime.rs`'s `spiffe_route()` test
    helper; nono-proxy's own test files may also need `capture: None,` added if they construct
    `RouteConfig` exhaustively rather than via `..Default::default()` (check each compiler error
    individually — do not assume a fixed count). Repeat `cargo build --workspace --all-targets`
    until it exits 0 with zero `missing field` errors related to `capture`.
  </action>
  <acceptance_criteria>
    `cargo build --workspace --all-targets` exits 0.
    `grep -c "capture: None," crates/nono-cli/src/network_policy.rs` returns 2.
    `grep -c "capture: None," crates/nono-cli/src/proxy_runtime.rs` returns at least 1.
    `cargo test -p nono-sandbox-proxy --lib` exits 0 (no regressions from the new field).
    `cargo test -p nono-sandbox-cli --lib network_policy::` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo build --workspace --all-targets 2>&1 | tail -20 && cargo test -p nono-sandbox-proxy --lib 2>&1 | tail -10</automated>
  </verify>
  <done>Workspace builds clean with zero RouteConfig-related compile errors; existing tests pass
  unchanged; every construction site sets capture: None as a deliberate stub pending Plan 114-08's
  real conversion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Profile config -> proxy runtime config | Declarative CaptureConfig is untrusted-input-shaped (comes from a profile file) but not yet parsed/validated in this plan — validation lands in Plan 114-08 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-03 | Denial of Service | `CaptureConfig.max_response_bytes` | mitigate | Hard ceiling constant (`CAPTURE_MAX_RESPONSE_BYTES_CEILING`) defined here; enforced at profile-validation time in Plan 114-08 and at buffer-read time in Plan 114-05 — this plan only defines the bound, does not yet enforce it |
| T-114-04 | Tampering | Exhaustive `RouteConfig` literals across the workspace | accept | No `#[derive(Default)]` on `RouteConfig` is a deliberate existing fork pattern (forces every construction site to make an explicit, compiler-checked choice for every field, including new security-relevant ones) — this plan preserves that pattern rather than adding `..Default::default()` as a shortcut |
</threat_model>

<verification>
`cargo build --workspace --all-targets` exits 0. `cargo test -p nono-sandbox-proxy --lib` and
`cargo test -p nono-sandbox-cli --lib` both green. No behavior change yet (capture: None
everywhere) — this plan is pure interface groundwork.
</verification>

<success_criteria>
CaptureConfig/CaptureResponseField/CaptureResponseFieldKind exist in config.rs; RouteConfig.capture
field exists; every exhaustive RouteConfig literal in the workspace compiles; cargo build
--workspace --all-targets is green.
</success_criteria>

<output>
Create `.planning/phases/114-oauth-capture-absorb-sec-02/114-02-SUMMARY.md` when done
</output>
