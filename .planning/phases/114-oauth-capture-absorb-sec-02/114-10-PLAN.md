---
phase: 114-oauth-capture-absorb-sec-02
plan: 10
type: execute
wave: 5
depends_on: ["114-01", "114-02", "114-03", "114-04", "114-05", "114-06", "114-07", "114-08", "114-09"]
files_modified:
  - ../nono-py/src/proxy.rs
  - ../nono-py/src/policy.rs
autonomous: true
requirements: [SEC-02]
user_setup: []

must_haves:
  truths:
    - "D-14: this is its own named task — both ../nono-py and ../nono-ts are rebuilt with real results reported, not predicted"
    - "maturin build (or maturin develop) succeeds in ../nono-py after RouteConfig gains capture and any other struct changes from this phase's plans"
    - "napi build --platform --release succeeds in ../nono-ts, confirming the previously-established structural immunity (zero nono_proxy references) still holds after this phase"
  artifacts:
    - path: "../nono-py/src/proxy.rs"
      provides: "capture: None literal in the RustRouteConfig exhaustive constructor"
      contains: "capture: None"
    - path: "../nono-py/src/policy.rs"
      provides: "capture: None literal in the PolicyRouteConfig -> RustRouteConfig conversion"
      contains: "capture: None"
---

<objective>
Rebuild both sibling binding repos (`../nono-py`, `../nono-ts`) against this phase's final
`nono-proxy`/`nono` struct surface, fixing whatever exhaustive struct-literal breaks the build
compiler actually reports — not the breaks this plan predicts. D-14 names this as its own task
because static prediction has under-predicted the real break surface every time this class of
change has landed: Phase 113 predicted 1 field, found 3 files (two `RouteConfig` literals plus a
`NetworkAuditEvent` field). This is the fifth consecutive occurrence.

Purpose: `RouteConfig` gained `capture` (Plan 114-02) and `NetworkAuditEvent`/`EventContext`
gained `capture_context` (Plan 114-01) — both exhaustively-constructed types in `../nono-py`.
Only running the actual build catches the true break surface.
Output: `maturin build` green in `../nono-py`, `napi build --platform --release` green in
`../nono-ts`, both confirmed by real command output in this plan's SUMMARY, not prediction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-oauth-capture-absorb-sec-02/114-CONTEXT.md
@.planning/phases/113-spiffe-spire-workload-identity/113-08-SUMMARY.md
</context>

<interfaces>
<!-- Exact live code the executor must mirror, PLUS a hard warning about trusting it. -->

**This plan's own `<interfaces>` section is a STARTING POINT, not a checklist to blindly follow.**
The actual break surface must be discovered by running `maturin build` (or `cargo check -p
nono_proxy_py` / the sibling repo's own build entry point) and reading its compiler errors — per
D-14 and the Phase 113 precedent (predicted 1 field, found 3 files: two `RouteConfig` literals
AND a `NetworkAuditEvent` struct-literal field this plan's own author did not anticipate either).

Known, already-verified-live break sites as of this session (BEFORE this phase's other plans
land — re-verify after, since Plans 114-01 through 114-09 may have changed exact line numbers):

`../nono-py/src/proxy.rs:211-232` (verified live this session, full exhaustive literal):
```rust
        Self {
            inner: RustRouteConfig {
                prefix, upstream, credential_key, inject_mode: inject_mode.into(), inject_header,
                credential_format, path_pattern, path_replacement, query_param_name, env_var,
                oauth2: None, aws_auth: None, spiffe: None,
                endpoint_rules: endpoint_rules.into_iter().map(...).collect(),
                tls_ca, endpoint_policy: None,
            },
        }
```

`../nono-py/src/policy.rs:733-754` (verified live this session, full exhaustive literal):
```rust
impl From<PolicyRouteConfig> for RustRouteConfig {
    fn from(route: PolicyRouteConfig) -> Self {
        Self {
            prefix: route.prefix, upstream: route.upstream, credential_key: route.credential_key,
            inject_mode: route.inject_mode.into(), inject_header: route.inject_header,
            credential_format: Some(route.credential_format), path_pattern: route.path_pattern,
            path_replacement: route.path_replacement, query_param_name: route.query_param_name,
            env_var: route.env_var, oauth2: None, aws_auth: None, spiffe: None,
            endpoint_rules: route.endpoint_rules.into_iter().map(Into::into).collect(),
            tls_ca: route.tls_ca, endpoint_policy: None,
        }
    }
}
```
Both need a `capture: None,` line directly below `spiffe: None,`.

Confirmed live this session: `../nono-ts/src/` has ZERO `nono_proxy` references anywhere (grep
returns no files) — matches Phase 113's finding that `nono-ts` is structurally immune to this
class of break. Still run its build per D-14 to report a REAL (expected-green) result, not to fix
anything.

Also check (not confirmed live this session — Plan 114-01 added `NetworkAuditEvent.capture_context`
and `EventContext.capture_context` AFTER this research pass; `113-08-SUMMARY.md` records that
Phase 113's equivalent field addition broke a THIRD file via an exhaustive `NetworkAuditEvent`
struct literal in `../nono-py` — grep `../nono-py/src` for `NetworkAuditEvent {` and
`EventContext {` before assuming only `proxy.rs`/`policy.rs` need fixing).

`workflow.use_worktrees=false` is already set project-wide — this plan reaches `../nono-py` and
`../nono-ts` directly via relative path, no worktree isolation concern.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild ../nono-py — fix every real compiler-reported break, report real results</name>
  <files>../nono-py/src/proxy.rs, ../nono-py/src/policy.rs</files>
  <read_first>
    ../nono-py/src/proxy.rs (the RustRouteConfig exhaustive literal, current state)
    ../nono-py/src/policy.rs (the PolicyRouteConfig -> RustRouteConfig conversion, current state)
    .planning/phases/113-spiffe-spire-workload-identity/113-08-SUMMARY.md (the 3-file break
      surface precedent — do not assume this phase's break surface is smaller)
  </read_first>
  <action>
    Run `cd ../nono-py && cargo build 2>&1 | tee /tmp/nono-py-build.log` (or the repo's documented
    build entry point if `cargo build` alone is insufficient — check `../nono-py/README.md` or
    `pyproject.toml` for the canonical local build command first). For EVERY compiler error
    reported (missing field `capture`, missing field `capture_context`, non-exhaustive match on
    any enum this phase touched, or anything else the compiler surfaces — do not assume the error
    list is limited to what this plan's `<interfaces>` section predicted), fix it by adding the
    minimal correct value: `capture: None,` for `RouteConfig`/`RustRouteConfig` literals,
    `capture_context: None,` for `NetworkAuditEvent`/`EventContext` literals, and an appropriate
    match arm for any new enum variant (`CaptureUnsupportedPath` on
    `NetworkAuditDenialCategory`, if a `match` on that enum exists anywhere in `../nono-py`).
    Repeat until the build is clean. Then run `uv run maturin build` (or `maturin develop` — use
    whichever this repo's own documented workflow specifies) and confirm it succeeds. Report the
    REAL list of files touched and errors fixed in this plan's SUMMARY — do not report only the
    files predicted in `<interfaces>` if the real list differs.
  </action>
  <acceptance_criteria>
    `cd ../nono-py && cargo build` exits 0.
    `cd ../nono-py && uv run maturin build` (or the repo's documented build command) exits 0, real
    command output captured (not predicted).
    Every fixed file is listed in this plan's SUMMARY with the specific compiler error it
    resolved.
  </acceptance_criteria>
  <verify>
    <automated>cd ../nono-py && cargo build 2>&1 | tail -30</automated>
  </verify>
  <done>../nono-py builds clean against this phase's final struct surface; maturin build succeeds;
  the real break surface (whatever it turns out to be) is documented, not predicted.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild ../nono-ts — confirm structural immunity still holds, report real results</name>
  <files>../nono-ts (build verification only — no source edits anticipated)</files>
  <read_first>
    .planning/phases/113-spiffe-spire-workload-identity/113-08-SUMMARY.md (the ../nono-ts
    structural-immunity finding this task re-confirms, not assumes)
  </read_first>
  <action>
    Run `grep -rln "nono_proxy" ../nono-ts/src` to re-confirm zero references (the structural
    immunity claim) BEFORE running the build — if this grep returns any files, STOP and treat this
    as a genuine new finding requiring investigation, not a predicted-and-dismissed result. If
    zero references confirmed, run `cd ../nono-ts && napi build --platform --release` (or the
    repo's documented build command) and confirm it succeeds. Report the real command output in
    this plan's SUMMARY.
  </action>
  <acceptance_criteria>
    `grep -rln "nono_proxy" ../nono-ts/src` returns zero files (or, if non-zero, this plan's
    SUMMARY documents the new finding and what was fixed).
    `cd ../nono-ts && napi build --platform --release` (or documented equivalent) exits 0, real
    command output captured.
  </acceptance_criteria>
  <verify>
    <automated>cd ../nono-ts && napi build --platform --release 2>&1 | tail -30</automated>
  </verify>
  <done>../nono-ts's structural immunity to this phase's struct changes is re-confirmed (not
  assumed) and its build is green with real, captured output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| nono-proxy/nono core struct surface -> Python/Node binding crates | Struct drift between the fork's own workspace and the sibling binding repos is a build-time (not runtime) risk — a stale binding would silently construct an incomplete RouteConfig if it somehow still compiled, but exhaustive literals make that structurally impossible (compile error, not silent drift) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-21 | Tampering | Sibling-repo struct drift | mitigate | Exhaustive Rust struct literals (no `..Default::default()`) in both binding repos make any missed field a compile error, not a silent behavior gap — this plan closes every one the compiler reports |
</threat_model>

<verification>
`cargo build` clean in `../nono-py`; `maturin build` green; `napi build --platform --release`
green in `../nono-ts`; all results are real captured command output, not prediction.
</verification>

<success_criteria>
Both sibling binding repos build green against this phase's final struct surface, with the real
(not predicted) break surface documented in this plan's SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/114-oauth-capture-absorb-sec-02/114-10-SUMMARY.md` when done
</output>
