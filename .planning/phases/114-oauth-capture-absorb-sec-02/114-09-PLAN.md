---
phase: 114-oauth-capture-absorb-sec-02
plan: 09
type: execute
wave: 3
depends_on: ["114-08"]
files_modified:
  - crates/nono-cli/data/nono-profile.schema.json
  - crates/nono-cli/src/profile/mod.rs
autonomous: true
requirements: [SEC-02]

must_haves:
  truths:
    - "D-13: the profile schema hand-ports only the CaptureConfig fields the fork actually implements (response_fields, request_nonce_fields, max_response_bytes) — upstream's 265-line schema (persistence/interception surfaces) is NOT ported wholesale"
    - "D-13: a realistic capture-provider profile both deserializes into CustomCredentialDef AND passes validate_against_schema() — a round-trip test proves this, not prose"
  artifacts:
    - path: "crates/nono-cli/data/nono-profile.schema.json"
      provides: "CaptureConfig $defs entry + capture property on CustomCredentialDef"
      contains: "\"CaptureConfig\""
    - path: "crates/nono-cli/src/profile/mod.rs"
      provides: "test_schema_validates_capture_custom_credential round-trip test"
      contains: "fn test_schema_validates_capture_custom_credential"
---

<objective>
Hand-port the `CaptureConfig` shape into `crates/nono-cli/data/nono-profile.schema.json` — the
fork-only, strict (`additionalProperties: false`) companion schema upstream never touches — and
add a round-trip test proving a realistic capture profile deserializes AND validates against it.

Purpose: D-13 is explicit that porting upstream's full 265-line OAuth-capture schema section would
advertise persistence/interception configuration this fork does not implement, which is itself a
fail-open shape (a schema promising capability the code doesn't have). This plan ports only what
Plan 114-02/114-08 actually built.
Output: `$defs.CaptureConfig` (+ `$defs.CaptureResponseField` if needed for the array item shape),
`capture` property on `$defs.CustomCredentialDef`, and
`test_schema_validates_capture_custom_credential`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-oauth-capture-absorb-sec-02/114-CONTEXT.md
@.planning/phases/114-oauth-capture-absorb-sec-02/114-PATTERNS.md
</context>

<interfaces>
<!-- Exact live code the executor must mirror. Verified live this session. -->

GROUND-TRUTH CAVEAT: this plan depends on Plan 114-08, which edits
`crates/nono-cli/src/profile/mod.rs` (adding `CustomCredentialDef.capture` and validation) in
the SAME file this plan's round-trip test lands in — every `profile/mod.rs` line number cited
below will have shifted. Locate the correct `mod tests` block by symbol
(`grep -n "fn test_schema_validates_spiffe_custom_credential"`, `grep -n "fn
validate_against_schema"`), never by the line numbers cited below. The compiler and grep are
ground truth.

`CustomCredentialDef`'s schema entry (`crates/nono-cli/data/nono-profile.schema.json:614-713`,
verified live — the `spiffe` property block to mirror, at lines 645-651, insert a `capture`
property directly after it):
```json
        "spiffe": {
          "oneOf": [
            { "$ref": "#/$defs/SpiffeAuthConfig" },
            { "type": "null" }
          ],
          "description": "SPIFFE/SPIRE Workload API auth (NET-02, Phase 113). ..."
        },
```

`$defs.SpiffeAuthConfig`'s entry shape to mirror (`nono-profile.schema.json:754-758+`, verified
live, opening):
```json
    "SpiffeAuthConfig": {
      "type": "object",
      "description": "NET-02 (Phase 113) SPIFFE/SPIRE Workload API auth ...",
      "additionalProperties": false,
      "required": ["type", "workload_api_socket", "audience"],
      "properties": { ... }
    },
```
`additionalProperties: false` is the enforced posture (D-13's hard requirement) — do not omit it
on either the new `CaptureConfig` or `CaptureResponseField` `$defs` entries.

The Rust types this schema must match exactly (Plan 114-02's `crates/nono-proxy/src/config.rs`
output — read the live file for the final field list/types before writing JSON, do not trust this
plan's paraphrase): `CaptureConfig { response_fields: Vec<CaptureResponseField>,
request_nonce_fields: Vec<String>, max_response_bytes: Option<usize> }`,
`CaptureResponseField { path: String, kind: CaptureResponseFieldKind }`,
`CaptureResponseFieldKind` (enum, `"opaque"` | `"jwt"`, snake_case).

The exact round-trip test template to copy (`crates/nono-cli/src/profile/mod.rs:7396-7415`,
verified live, full test — same `mod tests` block, whose local `validate_against_schema` helper
starts at line 7249; TWO copies of `validate_against_schema` exist in this file (the other at
~9465-9482) — use the SAME `mod tests` block as this existing SPIFFE test, per PATTERNS.md's
finding, to keep capture's schema test co-located with its closest sibling):
```rust
    #[test]
    fn test_schema_validates_spiffe_custom_credential() {
        let json = r#"{
            "meta": { "name": "spiffe-test" },
            "network": {
                "custom_credentials": {
                    "inventory": {
                        "upstream": "https://inventory.internal.example",
                        "spiffe": {
                            "type": "jwt",
                            "workload_api_socket": "/run/spire/agent.sock",
                            "audience": ["x"]
                        }
                    }
                }
            }
        }"#;
        validate_against_schema(json)
            .expect("custom credential with a valid spiffe block should pass schema validation");
    }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CaptureConfig $defs entry + capture property + round-trip test</name>
  <files>crates/nono-cli/data/nono-profile.schema.json, crates/nono-cli/src/profile/mod.rs</files>
  <read_first>
    crates/nono-cli/data/nono-profile.schema.json (the full CustomCredentialDef entry and the full
    SpiffeAuthConfig $defs entry, current state)
    crates/nono-proxy/src/config.rs (Plan 114-02's final CaptureConfig/CaptureResponseField/
    CaptureResponseFieldKind field list and types, read live — do not trust any prior plan's
    paraphrase since Phase 110/113 both hit real schema/struct mismatches this way)
    crates/nono-cli/src/profile/mod.rs (the exact `mod tests` block containing
    test_schema_validates_spiffe_custom_credential, lines ~7249-7420, to place the new test inside
    the SAME block)
  </read_first>
  <behavior>
    - A profile JSON with a `custom_credentials.<name>.capture` block containing a non-empty
      `response_fields` array with one entry (`path: "access_token"`, `kind: "opaque"`) both
      deserializes into `CustomCredentialDef` (via the profile's normal parse path) AND passes
      `validate_against_schema()` — `test_schema_validates_capture_custom_credential`.
    - A profile JSON with `custom_credentials.<name>.capture.response_fields` OMITTED entirely
      (capture present but empty) FAILS `validate_against_schema()` if `response_fields` is marked
      `required` in the `$defs.CaptureConfig` entry — OR is caught by
      `validate_capture_config` (Plan 114-08) if the schema itself allows an empty array; pick ONE
      enforcement layer and make it consistent with Plan 114-08's Rust-level validation (do not
      silently allow a schema-valid-but-Rust-invalid profile).
    - A profile JSON with a `capture.response_fields` entry whose `kind` is an unrecognized string
      (e.g. `"invalid_kind"`) fails `validate_against_schema()` (schema-level enum enforcement,
      independent of the Rust-level `CaptureResponseFieldKind` deserialize failure it would also
      hit).
  </behavior>
  <action>
    Add a `$defs.CaptureConfig` entry to `nono-profile.schema.json` (object,
    `additionalProperties: false`) with properties `response_fields` (array of `{ "$ref":
    "#/$defs/CaptureResponseField" }`, non-empty per the Rust-level check — schema-level `minItems:
    1` is acceptable and recommended for defense-in-depth), `request_nonce_fields` (array of
    strings, default `[]`), `max_response_bytes` (integer or null, with a description naming the
    256 KiB default / 1 MiB ceiling). Add a `$defs.CaptureResponseField` entry (object,
    `additionalProperties: false`, `required: ["path"]`, properties `path` (string) and `kind`
    (string enum `["opaque", "jwt"]`, default `"opaque"`)). Add a `capture` property to
    `CustomCredentialDef`'s schema properties (mirroring the `spiffe` property block shape shown
    in this plan's `<interfaces>` section, `oneOf` referencing `$defs/CaptureConfig` or `null`),
    with a description naming D-13/Phase 114 and explicitly noting what was NOT ported (disk
    persistence, credential-store detection, login-flow helpers) and why (fork scope decision, see
    ADR-114). Add `test_schema_validates_capture_custom_credential` to the SAME `mod tests` block
    as `test_schema_validates_spiffe_custom_credential` (the one whose `validate_against_schema`
    helper is at line ~7249), covering the first behavior case. Add the two negative-case tests
    covering the remaining behavior cases.
  </action>
  <acceptance_criteria>
    `grep -c "\"CaptureConfig\": {" crates/nono-cli/data/nono-profile.schema.json` returns 1.
    `grep -c "\"CaptureResponseField\": {" crates/nono-cli/data/nono-profile.schema.json` returns 1.
    `grep -c "additionalProperties.: false" crates/nono-cli/data/nono-profile.schema.json | tail -1`
    (spot-check both new $defs entries include this — read the file directly to confirm placement,
    grep count alone is not proof of correct placement).
    `cargo test -p nono-sandbox-cli --lib profile:: test_schema_validates_capture_custom_credential -- --nocapture`
    exits 0.
    `cargo test -p nono-sandbox-cli --lib profile:: -- --nocapture` exits 0 (no regression to
    existing schema tests, including `test_schema_validates_spiffe_custom_credential`).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-cli --lib profile:: -- --nocapture 2>&1 | tail -40</automated>
  </verify>
  <done>CaptureConfig and CaptureResponseField exist as strict, additionalProperties:false $defs
  entries; capture is a valid CustomCredentialDef property; the round-trip test and both negative
  cases pass; no upstream persistence/interception schema surface was ported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Profile file (operator-authored, on-disk) -> schema validation | The schema is the first gate against malformed capture configuration before Rust-level parsing/validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-19 | Tampering | `nono-profile.schema.json` | mitigate | `additionalProperties: false` on both new $defs entries — an operator (or a compromised profile source) cannot add undeclared fields the schema silently ignores |
| T-114-20 | Tampering | Schema advertises unimplemented capability | mitigate | Only `response_fields`/`request_nonce_fields`/`max_response_bytes` are exposed — upstream's persistence/credential-store/helper-command surface is deliberately absent, per D-13's "a schema advertising configuration nothing implements is itself a fail-open shape" |
</threat_model>

<verification>
`cargo test -p nono-sandbox-cli --lib profile::` green, including the new round-trip test and both
negative cases, with zero regression to the existing SPIFFE schema test.
</verification>

<success_criteria>
A realistic capture-provider profile both deserializes and validates against the strict, fork-only
schema; the schema exposes only what the fork implements.
</success_criteria>

<output>
Create `.planning/phases/114-oauth-capture-absorb-sec-02/114-09-SUMMARY.md` when done
</output>
