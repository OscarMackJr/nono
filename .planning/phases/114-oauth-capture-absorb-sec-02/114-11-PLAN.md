---
phase: 114-oauth-capture-absorb-sec-02
plan: 11
type: execute
wave: 6
depends_on: ["114-10"]
files_modified:
  - proj/ADR-114-oauth-capture-disposition.md
  - .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SEC-02]

must_haves:
  truths:
    - "D-01r: ADR-114 records that a formal DECLINE was considered and retracted, and states explicitly why the decline reasoning was wrong (conflating response visibility with response buffering)"
    - "D-09: ADR-114 confirms the ADR-86 / ADR-111 boundary is non-regressed with a grep-backed proof, not prose assurance"
    - "D-10: 108-DIVERGENCE-LEDGER.md's SEC-02 rows are updated from deferred to ADAPTED-with-scope-limit, and 149abde0 is named as a tracked divergence in a new Carry-Forward Note"
    - "D-11: REQUIREMENTS.md's SEC-02 checkbox resolves as satisfied-with-scope-limit citing ADR-114, not left open"
    - "D-02r: ADR-114's Consequences section accurately states the mint-to-resolve loop is closed (request_nonce_fields JSON-body resolution, Plan 114-06 Task 3) and names precisely what egress-resolution surface is NOT shipped (header-based Authorization Bearer resolution; SPIFFE-dispatched routes) — no phrasing implying a broader baseline than what shipped"
  artifacts:
    - path: "proj/ADR-114-oauth-capture-disposition.md"
      provides: "The formal disposition record for SEC-02"
      contains: "D-01r"
    - path: ".planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md"
      provides: "SEC-02 Carry-Forward Note (Phase 114, D-10) + updated SEC-02a/b/c disposition rows"
      contains: "SEC-02 Carry-Forward Note (Phase 114, D-10)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SEC-02 checkbox flipped to complete, citing ADR-114"
      contains: "ADR-114"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "proj/ADR-114-oauth-capture-disposition.md"
      via: "SEC-02's resolution note cites ADR-114"
      pattern: "ADR-114"
---

<objective>
Close out Phase 114: write `proj/ADR-114-oauth-capture-disposition.md` recording the full
disposition (including the retracted decline, per CONTEXT.md's explicit instruction that the
retraction is part of the record), update the ledger's SEC-02 rows and add a Carry-Forward Note
naming `149abde0` as a tracked divergence, flip `REQUIREMENTS.md`'s SEC-02 checkbox, and run the
combined verification gate — both cross-target clippy gates, `cargo fmt --all --check`, and a
workspace test-suite diff against the current documented baseline.

Purpose: this is the phase-gate plan. Every prior plan's work must be provably non-regressing and
the phase's disposition record must be genuinely useful to a future reader — CONTEXT.md's
`<specifics>` block is explicit that ADR-114 must correct `112-OAUTH-CAPTURE-DISPOSITION.md`'s
still-reads-as-forward.rs-only framing, or a future reader will re-derive the same wrong premise.
Output: `proj/ADR-114-oauth-capture-disposition.md`, updated
`108-DIVERGENCE-LEDGER.md`, updated `REQUIREMENTS.md`, both cross-target clippy gates GREEN,
`cargo fmt --all --check` clean, test baseline diff recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-oauth-capture-absorb-sec-02/114-CONTEXT.md
@.planning/phases/114-oauth-capture-absorb-sec-02/114-RESEARCH.md
@proj/ADR-113-spiffe-disposition.md
@proj/ADR-86-library-boundary-convergence.md
@.planning/phases/112-security-residual-sync/112-OAUTH-CAPTURE-DISPOSITION.md
@.planning/templates/cross-target-verify-checklist.md
</context>

<interfaces>
<!-- Exact live locations to edit. Verified live this session. -->

GROUND-TRUTH CAVEAT: the ledger and REQUIREMENTS.md line numbers below were verified live at
planning time; neither file is touched by any earlier plan in this phase (only source code is),
so drift risk here is lower than for the code-editing plans — but re-grep
(`grep -n "SEC-02"`) before editing anyway, since this plan runs last (Wave 6) and other
operator/executor activity may have touched these shared project-wide docs in the interim. The
compiler and grep (for source-code claims cited from prior plans' SUMMARYs) are ground truth,
not the line numbers below.

`108-DIVERGENCE-LEDGER.md`'s current SEC-02 rows to update (`108-DIVERGENCE-LEDGER.md:1800-1802`,
verified live this session):
```
| `9b692e07` | SEC-02a | deferred -> Phase 114 (ROADMAP Amendment framing) | **deferred -> Phase 114 — CONFIRMED** | ... |
| `3c59c62e` | SEC-02b | deferred -> Phase 114 | **deferred -> Phase 114 — CONFIRMED** | ... |
| `d033c631` | SEC-02c | deferred -> Phase 114 | **deferred -> Phase 114 — CONFIRMED** | ... |
```
Update each row's rightmost (evidence) column to `ADAPTED-with-scope-limit — see "SEC-02
Carry-Forward Note (Phase 114, D-10)"` (or similar), citing `proj/ADR-114-oauth-capture-
disposition.md`. Also update the ledger's summary bullet list (near
`- **deferred -> Phase 114 (evidence captured, no verdict made):** SEC-02a/b/c (3)`,
`108-DIVERGENCE-LEDGER.md:1885`) to move SEC-02a/b/c out of the "deferred" bucket into an "adapt"
bucket.

The `### SPIFFE Carry-Forward Note (Phase 113, D-02)` section
(`108-DIVERGENCE-LEDGER.md:659`) is the structural precedent for the new
`### SEC-02 Carry-Forward Note (Phase 114, D-10)` section this plan adds (place it as a new
subsection near the SPIFFE note, or immediately after the SEC-02 rows table — either placement is
acceptable as long as it is discoverable, per D-10's "the ledger is the canonical work-list a
future UPST planner actually reads").

`REQUIREMENTS.md`'s current SEC-02 line (`REQUIREMENTS.md:131`, verified live this session):
```
- [ ] **SEC-02**: Declarative sandboxed OAuth capture (`9b692e07`), capture-boundary hardening
  (`3c59c62e`), and the stdin fixture test (`d033c631`) are absorbed. **→ CARVED OUT to Phase 114
  (2026-08-05, ROADMAP Amendment):** ... Not resolved by Phase 112; checkbox intentionally left
  unchecked, not silently dropped.
```
Flip the leading `- [ ]` to `- [x]` and REPLACE the trailing note with a resolution note in the
style of the existing `- [x] **SEC-01**: ... **RESOLVED 2026-08-05 ...**` pattern
(`REQUIREMENTS.md:130`) — state the disposition (ADAPT, fork-native enforcement point), the scope
limit (reverse-proxy-reachable routes only, per D-10), and cite `proj/ADR-114-oauth-capture-
disposition.md` by path.

`proj/ADR-113-spiffe-disposition.md`'s section structure (full file, already loaded via
`<context>`) — the shape to mirror: `## Context` (with the ROADMAP-staleness-correction pattern),
`## Decision` (with a "D-01's positive proof" subsection), a `## D-0N/SC3: Re-confirming the
ADR-86 Boundary` section, an `## OD-1: ...` named-permanent-decision section, a `## Consequences`
section, a `## References` section.

The `.gitignore:16` bare `proj/` pattern trap (confirmed still present this session) — NEW files
under `proj/` are silently untracked by plain `git add`; use `git add -f
proj/ADR-114-oauth-capture-disposition.md`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write proj/ADR-114-oauth-capture-disposition.md</name>
  <files>proj/ADR-114-oauth-capture-disposition.md</files>
  <read_first>
    proj/ADR-113-spiffe-disposition.md (full file — the structural precedent)
    .planning/phases/112-security-residual-sync/112-OAUTH-CAPTURE-DISPOSITION.md (§1c — the
    forward.rs-only framing ADR-114 must explicitly correct, not silently supersede)
    .planning/phases/114-oauth-capture-absorb-sec-02/114-CONTEXT.md (D-01r through D-14, full)
    .planning/phases/114-oauth-capture-absorb-sec-02/114-05-SUMMARY.md,
    114-06-SUMMARY.md, 114-07-SUMMARY.md (what was actually shipped, to ground the ADR in reality
    rather than the plan's predictions)
  </read_first>
  <action>
    Write `proj/ADR-114-oauth-capture-disposition.md` with these sections, mirroring ADR-113's
    structure: (1) `## Context` — state the SEC-02 carve-out origin (Phase 112), then the
    retracted-decline history verbatim as CONTEXT.md's `<specifics>` requires: a formal DECLINE
    was the first locked decision, reasoning that `forward.rs`'s `ResponseRewrite` hook was the
    ONLY possible enforcement point (citing `112-OAUTH-CAPTURE-DISPOSITION.md` §1c by name); the
    operator retracted it mid-discussion; the retraction was correct because that reasoning
    conflated response VISIBILITY (which `reverse.rs`'s own `tls_stream` to upstream already
    gives the fork) with response BUFFERING (which the fork genuinely lacked, by deliberate
    streaming-by-design). State explicitly that `112-OAUTH-CAPTURE-DISPOSITION.md`'s security
    conclusion is not overturned, only resolved differently — a future reader must not re-derive
    the DECLINE from that document alone. (2) `## Decision` — D-01r (ADAPT, fork-native
    enforcement point) confirmed as locked, with a "D-06's positive proof" subsection mirroring
    ADR-113's shape: a table of the FOUR request-arrival paths (reverse-proxy / CONNECT /
    forward-HTTP / external-proxy-chain) and each one's confirmed capture disposition, citing the
    actual shipped grep/test evidence from Plans 114-05/114-06/114-07's SUMMARYs (verify the
    external-proxy-chain claim directly — per RESEARCH.md's architecture diagram and ADR-113's own
    D-01 table, it is covered by the SAME unconditional `is_route_upstream` check the CONNECT arm
    uses, before `use_external` is computed; confirm this is still true post-Phase-114 by reading
    the current `server.rs`, do not merely cite ADR-113). (3) `## D-09/SC3: Re-confirming the
    ADR-86 Boundary` — grep-backed proof (not prose) that `CaptureAuditContext` carries no
    enforcement logic (`grep -rn "if \|match " crates/nono/src/undo/types.rs`, spot-checking the
    struct definition specifically) and that no policy logic landed in `crates/nono/src/`
    elsewhere. (4) `## OD: The TLS-Interception Boundary Against 149abde0 — A Named, Permanent
    Decision` — mirroring ADR-113's `OD-1` shape: `149abde0` (creates `forward.rs`, brings TLS
    interception) is NOT absorbed; the fork's no-MITM stance is a standing decision reaffirmed,
    not reopened, by this phase; name the scope limit this creates (capture only works for token
    endpoints reachable as configured reverse-proxy routes; agent-initiated CONNECT flows to
    arbitrary hosts remain out of reach without TLS interception). (5) `## Consequences` —
    cross-reference the ledger update (Task 2) and the REQUIREMENTS checkbox flip (Task 2). This
    section MUST accurately describe what SHIPPED, not merely what was descoped — a prior draft of
    this ADR described the deferred item as "egress phantom resolution beyond same-route scope,"
    which wrongly implies a same-route resolution baseline existed before it was written; it did
    not. State plainly: (a) SHIPPED — the mint-to-resolve loop is closed end-to-end.
    `CapturePhantomStore::resolve()` (Plan 114-04) has a real, grep-provable production call site:
    `resolve_capture_request_body()` in `handle_reverse_proxy` (Plan 114-06 Task 3) resolves a
    previously-minted, admitted phantom presented in a `request_nonce_fields`-configured JSON
    request-body path back to the real captured token before the request is forwarded upstream —
    fail-closed (an unknown or non-admitted phantom is left unchanged, never substituted) and
    proven by two independent tests
    (`capture_egress_resolves_admitted_phantom_in_request_body`,
    `capture_egress_never_substitutes_real_token_for_unadmitted_consumer`). (b) NOT SHIPPED, named
    explicitly, not silently dropped: header-based egress resolution (an `Authorization: Bearer
    <phantom>` presented directly, rather than embedded in a JSON request-body field, is NOT
    resolved by this phase — only the `request_nonce_fields` JSON-body path is wired); resolution
    is wired ONLY into `handle_reverse_proxy`'s own dispatch path (the static-credential /
    no-credential route flow) — it is NOT threaded into `handle_spiffe_route`' or
    `handle_spiffe_assertion_credential`'s own outbound request-building code, so a route reached
    exclusively via SPIFFE-based request auth has no egress-resolution path yet, only the (already
    complete, per Tasks 1-2) response-side capture; disk persistence of real tokens (D-08, upstream
    `oauth_capture/persist.rs`); login-flow helper commands and upstream's
    `credential_store`/`helpers`/provider-registry indirection (D-12 discretion). Verify this
    subsection against Plan 114-06's actual SUMMARY before writing it — do not describe a broader
    capability than what Task 3's SUMMARY reports as shipped. (6) `##
    References` — cite `112-OAUTH-CAPTURE-DISPOSITION.md`, `112-REVIEW.md` (WR-13),
    `proj/ADR-113-spiffe-disposition.md`, `proj/ADR-86-library-boundary-convergence.md`,
    `114-RESEARCH.md`, `114-PATTERNS.md`, and every `114-0N-SUMMARY.md` this ADR draws evidence
    from. Add the file with `git add -f proj/ADR-114-oauth-capture-disposition.md` (the
    `.gitignore:16` bare `proj/` trap — plain `git add` would silently drop it).
  </action>
  <acceptance_criteria>
    `grep -c "D-01r" proj/ADR-114-oauth-capture-disposition.md` returns at least 1.
    `grep -c "retracted" proj/ADR-114-oauth-capture-disposition.md` returns at least 1 (the
    retraction history is present, not erased).
    `grep -c "149abde0" proj/ADR-114-oauth-capture-disposition.md` returns at least 1.
    `grep -c "external-proxy-chain" proj/ADR-114-oauth-capture-disposition.md` returns at least 1
    (the 4th arrival path explicitly confirmed, per PATTERNS.md's note that this needed explicit
    confirmation).
    `grep -c "resolve_capture_request_body" proj/ADR-114-oauth-capture-disposition.md` returns at
    least 1 (the Consequences section names the real shipped resolution mechanism, not a vague
    "egress resolution" placeholder).
    `grep -c "beyond same-route scope" proj/ADR-114-oauth-capture-disposition.md` returns 0 (the
    old, misleading phrasing implying a same-route baseline existed is not present).
    `git status --porcelain proj/ADR-114-oauth-capture-disposition.md` shows the file as tracked
    (staged), not untracked — proving `git add -f` succeeded against the `.gitignore:16` trap.
  </acceptance_criteria>
  <verify>
    <automated>git status --porcelain proj/ADR-114-oauth-capture-disposition.md</automated>
  </verify>
  <done>ADR-114 exists, records the retracted decline and why it was wrong, proves D-06's 4-path
  coverage with grep-backed evidence (not prose), confirms the ADR-86 boundary, and names
  149abde0 as the permanent scope boundary; the file is tracked despite the gitignore trap.</done>
</task>

<task type="auto">
  <name>Task 2: Update 108-DIVERGENCE-LEDGER.md + REQUIREMENTS.md</name>
  <files>.planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md (the SEC-02 rows at
    lines ~1800-1802, the SPIFFE Carry-Forward Note at line ~659 for structure, the summary bullet
    list near line ~1885)
    .planning/REQUIREMENTS.md (the SEC-02 line at ~131, and the already-resolved SEC-01 line at
    ~130 for the resolution-note style to mirror)
  </read_first>
  <action>
    In `108-DIVERGENCE-LEDGER.md`: update the SEC-02a/b/c rows' rightmost column from "deferred ->
    Phase 114 — CONFIRMED" to "ADAPTED-with-scope-limit — see SEC-02 Carry-Forward Note (Phase
    114, D-10)"; move SEC-02a/b/c from the "deferred" summary bucket to an "adapt" bucket; add a
    new `### SEC-02 Carry-Forward Note (Phase 114, D-10)` section (mirroring the SPIFFE note's
    structure — Filed date, the scope limit stated plainly, `149abde0` named explicitly, an
    "Obligation for whichever future plan absorbs `149abde0`" closing paragraph). In
    `REQUIREMENTS.md`: flip SEC-02's `- [ ]` to `- [x]` and replace its trailing note with a
    resolution note citing `proj/ADR-114-oauth-capture-disposition.md`, the ADAPT disposition, and
    the scope limit — mirroring SEC-01's resolution-note style exactly (RESOLVED date + one-line
    summary + doc citation).
  </action>
  <acceptance_criteria>
    `grep -c "ADAPTED-with-scope-limit" .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md`
    returns at least 3 (one per SEC-02a/b/c row).
    `grep -c "SEC-02 Carry-Forward Note (Phase 114, D-10)" .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md`
    returns at least 1.
    `grep -n "SEC-02" .planning/REQUIREMENTS.md | grep -c "\[x\]"` returns at least 1.
    `grep -c "ADR-114" .planning/REQUIREMENTS.md` returns at least 1.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "SEC-02" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>Ledger SEC-02 rows updated to ADAPTED-with-scope-limit with a new Carry-Forward Note;
  REQUIREMENTS.md's SEC-02 checkbox flipped and cites ADR-114.</done>
</task>

<task type="auto">
  <name>Task 3: Combined verification — both cross-target clippy gates, fmt, test baseline diff</name>
  <files>none (verification only)</files>
  <read_first>
    .planning/templates/cross-target-verify-checklist.md (the single source of truth for both
    gate invocations — full file, already loaded via `<context>`)
    .planning/STATE.md (Phase 113's entry — the 11-name baseline failure set this task diffs
    against; this SUPERSEDES the phase_specific_direction's older "4 nono-cli + 1 nono lib" figure,
    which is a stale memory-note figure the project's own history has already corrected once)
  </read_first>
  <action>
    Run `cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D
    clippy::unwrap_used`; confirm exit 0 (fix any error structurally per the checklist's
    anti-patterns — never `#[allow]`-silence). Run `cargo-zigbuild clippy --workspace --target
    x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used` (direct-binary form, `SDKROOT`
    unset); confirm exit 0. Run `cargo fmt --all --check`; confirm clean. Run `cargo test
    --workspace --no-fail-fast 2>&1 | tee /tmp/nono-114-test-run.log` — DO NOT run the full
    `-p nono-sandbox-cli --tests` 36-binary sweep (documented ~25min stall on this host per
    CONTEXT.md); scope to `-p nono-proxy` / `-p nono-cli` / `-p nono` plus targeted filters if the
    full workspace run is impractical. Diff the resulting failing-test-name set against the most
    recently confirmed baseline (Phase 113's 11-name set, per `STATE.md`'s Phase 113 entry) — the
    new set must be a STRICT SUBSET with zero new names, or every new name must be individually
    investigated and either fixed or explicitly justified as a genuine pre-existing condition this
    phase did not introduce.
  </action>
  <acceptance_criteria>
    `cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D
    clippy::unwrap_used` exits 0.
    `cargo-zigbuild clippy --workspace --target x86_64-apple-darwin -- -D warnings -D
    clippy::unwrap_used` exits 0.
    `cargo fmt --all --check` exits 0.
    The failing-test-name set from this session's `cargo test --workspace` run is a strict subset
    of Phase 113's documented 11-name baseline (zero new failing test names attributable to this
    phase's changes).
  </acceptance_criteria>
  <verify>
    <automated>cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used 2>&1 | tail -20</automated>
  </verify>
  <done>Both cross-target clippy gates GREEN, cargo fmt clean, workspace test suite diffed against
  the current documented baseline with zero new failures — SC4 fully discharged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Phase disposition record -> future UPST planners | ADR-114 and the ledger note are the canonical record a future planner reads before deciding whether/how to lift this phase's scope limit — an inaccurate or silently-omitted record misleads future work, as Phase 113 found the fork's own prior comment had done |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-22 | Repudiation | `108-DIVERGENCE-LEDGER.md` / `REQUIREMENTS.md` | mitigate | Both updated in this plan with grep-verifiable acceptance criteria, not left stale — a future reader gets the accurate, current disposition |
| T-114-23 | Tampering | Cross-target clippy gates | mitigate | Both gates run locally per the checklist's mandatory (not PARTIAL→CI) posture; no `#[allow]`-silencing permitted |
</threat_model>

<verification>
ADR-114 exists and is tracked in git; ledger and REQUIREMENTS updated with grep-verifiable
citations; both cross-target clippy gates GREEN; cargo fmt clean; test baseline diff shows zero
new failures.
</verification>

<success_criteria>
SEC-02 is genuinely resolved: ADR-114 records the full disposition including the retracted
decline, D-06's 4-path coverage is grep-proven, the ADR-86 boundary is confirmed, the ledger and
REQUIREMENTS reflect the final ADAPTED-with-scope-limit disposition, and both mandatory
cross-target gates plus the workspace test suite are green.
</success_criteria>

<output>
Create `.planning/phases/114-oauth-capture-absorb-sec-02/114-11-SUMMARY.md` when done
</output>
