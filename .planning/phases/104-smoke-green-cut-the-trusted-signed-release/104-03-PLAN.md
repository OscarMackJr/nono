---
phase: 104
plan: 03
type: execute
wave: 2
depends_on: ["104-01", "104-02"]
files_modified: []
autonomous: false
requirements: [REL-01]

must_haves:
  truths:
    - "The operator confirms the hardened 'Trusted Signing Smoke Test' workflow is GREEN on windows-latest immediately before any tag push (SC1) — not relying on a stale prior-day green (104-RESEARCH.md Pitfall 104-B: a green smoke run today is evidence for today, not a permanent guarantee)"
    - "The operator pushes tag v0.66.1 and the Release workflow completes green, with all top-level .exe (nono.exe, nono-shell-broker.exe, nono-wfp-service.exe) and both MSIs passing the hardened Assert-TrustedSignature dual-engine verify (SC2)"
    - "The published OscarMackJr/nono GitHub Release artifacts show Verified publisher, gated ONLY on Get-AuthenticodeSignature.Status -eq 'Valid' plus a non-test-cert signer CN — NEVER on an issuer-substring match (SC3; Finding B disproved the issuer heuristic)"
    - "No step in this plan loosens, bypasses, or reinterprets the fail-closed Status -ne 'Valid' gate, regardless of how long the external root-propagation blocker persists"
  artifacts: []
  key_links:
    - from: "git tag push (v0.66.1)"
      to: ".github/workflows/release.yml (on: push: tags: 'v*.*.*')"
      via: "tag-triggered workflow dispatch"
      pattern: "tags:\\s*\\n\\s*-\\s*'v\\*\\.\\*\\.\\*'"
    - from: "release.yml Verify Authenticode signatures (Windows) step"
      to: "scripts/verify-authenticode.ps1 Assert-TrustedSignature -Mode Strict"
      via: "dot-source + call, hardened by Plan 104-01"
      pattern: "Assert-TrustedSignature -Path \\$artifact -Mode Strict"
---

<objective>
Execute the operator-in-loop portion of Phase 104's goal: confirm the hardened Trusted Signing
smoke workflow is GREEN (SC1), push the `v0.66.1` tag and confirm the `Release` workflow completes
green with every signed artifact passing the hardened fail-closed verify (SC2), and confirm the
published GitHub Release shows Verified publisher using the Finding-B-corrected gate condition, not
the disproven issuer-substring heuristic (SC3).

This is explicitly and irreducibly operator-gated: `104-RESEARCH.md` empirically proved the
blocking condition is external (Microsoft has not yet propagated `Microsoft Enterprise ID Root CA
2021` to the public Trusted Root Program CTL) and time-bound, not fixable by any code change in this
repository. No task in this plan can resolve that blocker directly — the poll-until-green operator
loop (backed by Plan 104-02's local pre-check script) is the only sanctioned response.

Purpose: this is the actual go-live moment for v3.5's headline blocker (Azure Trusted Signing
verify-gate `UnknownError`) — the first publicly-trusted-signed `0.66.1` release.

Output: no new files (verification/operator-action plan). Produces the live evidence (smoke run
URL, tag, Release run URL, a downloaded artifact's confirmed Authenticode status) that REL-01's 3
success criteria hold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-smoke-green-cut-the-trusted-signed-release/104-RESEARCH.md
@.planning/phases/104-smoke-green-cut-the-trusted-signed-release/104-VALIDATION.md
@.planning/phases/104-smoke-green-cut-the-trusted-signed-release/104-01-SUMMARY.md
@.planning/phases/104-smoke-green-cut-the-trusted-signed-release/104-02-SUMMARY.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight automated status check</name>
  <files></files>
  <read_first>
    scripts/azure/check-trusted-signing-root.ps1 and scripts/verify-release-yml-publish-selectors.ps1
    (Plan 104-02's deliverables — this task simply invokes them, no new code). scripts/gates/release-readiness.ps1
    (existing Phase 97/100 gate, dot-sourced via scripts/verify-dark.ps1 -Gate release-readiness).
  </read_first>
  <action>
    Run three read-only checks and report their current, dated status to the operator before either
    checkpoint below is presented: (1) `pwsh -File scripts/azure/check-trusted-signing-root.ps1` —
    reports whether the Microsoft root-certificate propagation blocker has resolved; (2)
    `pwsh -File scripts/verify-dark.ps1 -Gate release-readiness` — reconfirms version-family
    (0.66.1), no-stale-version, leapfrog, no-private-paths (`build_notes/`/`.gsd/`), and Cargo.lock
    consistency immediately before this session's release-cut attempt, not relying on a stale prior
    run; (3) `pwsh -File scripts/verify-release-yml-publish-selectors.ps1` — reconfirms the
    publish-crates neutralization is still intact (no intervening edit reintroduced the stale
    selectors or removed the `if: false` guard). None of these three commands modify any file or
    trigger any outward-facing dispatch — they are the automated groundwork the operator needs
    before deciding whether to proceed to Task 2's poll-until-green checkpoint.
  </action>
  <verify>
    <automated>pwsh -File scripts/verify-dark.ps1 -Gate release-readiness</automated>
  </verify>
  <acceptance_criteria>
    - All three commands run to completion without an unhandled exception (they may report FAIL/ABSENT — that is informative, not a task failure).
    - `pwsh -File scripts/verify-dark.ps1 -Gate release-readiness` exits `0` (PASS) — if it exits
      `2` (FAIL), this task surfaces the exact failing assertion (`version_family` /
      `stale_062_2` / `leapfrog` / `private_paths` / `cargo_lock`) to the operator before Task 2
      proceeds, rather than silently continuing.
    - `pwsh -File scripts/verify-release-yml-publish-selectors.ps1` exits `0`.
    - The root pre-check's PRESENT/ABSENT result and cert-count are recorded verbatim in this plan's
      SUMMARY, dated to this session.
  </acceptance_criteria>
  <done>
    The operator has current, dated evidence of (a) whether the Microsoft root has propagated, (b)
    release-readiness gate status, and (c) the publish-crates neutralization's continued integrity,
    before Task 2's checkpoint is presented.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Poll-until-green — confirm the hardened smoke workflow is GREEN before any tag push (SC1)</name>
  <files></files>
  <read_first>
    104-RESEARCH.md "Practical implication for planning" + Open Question 1 (why this must be a
    recurring poll, not a one-shot retry) and Pitfall 104-B (a green smoke run today is evidence for
    today only). .planning/research/PITFALLS.md Pitfall 9 (`AADSTS700213`/FIC-subject canary —
    re-check on every dispatch, not just the first).
  </read_first>
  <action>
    Operator loops: run the local root pre-check periodically; once it reports PRESENT, re-verify
    the OIDC FIC-subject canary; then dispatch/re-run the "Trusted Signing Smoke Test" workflow on
    `windows-latest` and confirm it completes GREEN with a genuine dual-engine pass (not a
    retry-then-throw). Full exact command sequence and decision tree is in `<how-to-verify>` below.
  </action>
  <what-built>
    Plan 104-01 already landed the D-04 diagnostic-flush fix and the NoCheck-mode classification
    override, so any future smoke failure (this root-propagation cause or a new one) will be fully
    legible in the CI log on the first attempt, with no wasted retry backoff on a non-transient
    cause. Plan 104-02 already landed a free, local, no-CI-minutes pre-check script
    (`scripts/azure/check-trusted-signing-root.ps1`) plus the `publish-crates` neutralization, so a
    real dispatch attempt is never wasted on the already-known, already-diagnosed stale-selector
    defect. Task 1 of this plan already ran that pre-check plus the release-readiness and
    publish-selector guards once this session and reported their current status above.
  </what-built>
  <how-to-verify>
    1. Run `pwsh -File scripts/azure/check-trusted-signing-root.ps1` periodically (daily is
       reasonable per 104-RESEARCH.md's own recommendation) — it costs seconds and needs no CI
       minutes. Only proceed to step 2 once it reports PRESENT (exit 0).
    2. Re-verify the OIDC FIC-subject canary: confirm the sole federated credential subject is still
       exactly `repo:OscarMackJr/nono:environment:Development`, and that `release.yml`'s `build` job
       still has an unmodified `environment: Development` key — any drift here reproduces
       `AADSTS700213` independently of the signing/root issue and would be misdiagnosed as "still
       blocked on the root" if skipped.
    3. Dispatch/re-run the "Trusted Signing Smoke Test" workflow (`gh workflow run
       trusted-signing-smoke.yml` or the GitHub Actions UI) and wait for it to complete.
    4. If it fails, treat this as new information — do not assume it is the same already-diagnosed
       root-propagation cause; a NEW cause may exist and would need its own diagnosis before
       re-looping to step 1.
    5. Confirm success via `gh run view <run-id> --json conclusion -q .conclusion` returning
       `success`, and that the run's "Verify Authenticode signatures" step log contains the literal
       line `Authenticode OK (dual-engine)` (not a retry-then-throw sequence).
  </how-to-verify>
  <verify>
    <human-check>Operator reports a GREEN smoke-run URL/ID with a genuine dual-engine pass (not a retry-then-throw), and confirms the FIC-subject canary was re-checked immediately before this dispatch.</human-check>
  </verify>
  <resume-signal>Provide the GREEN smoke-run URL/ID to proceed to Task 3 (tag push), or reply "still blocked" — Claude will not silently re-poll on your behalf across sessions (cf. STATE.md Assumption A1: if no progress after ~1-2 weeks, consider an Azure support escalation referencing thumbprint 991D364E97882715B80ED978F53E1F35DC2F07C2 and the "Microsoft Enterprise ID Root CA 2021" issuing hierarchy).</resume-signal>
  <done>Operator has reported a genuinely GREEN smoke-run URL/ID, with the FIC-subject canary re-confirmed, sufficient for Task 3 to proceed to the tag push.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Tag push v0.66.1 — confirm Release workflow green and Verified publisher (SC2 + SC3)</name>
  <files></files>
  <read_first>
    101-SIGN03-SMOKE-VERDICT.md Finding B (the disproven issuer-substring heuristic — do not
    resurrect it here). scripts/gates/trusted-signed-assertion.ps1 (Phase 103's already-correct
    `Status -eq 'Valid'` + non-test-signer gate pattern to mirror when confirming SC3 manually).
    .github/workflows/release.yml lines ~259-343 (the 3 Windows-leg verify steps this push exercises,
    all hardened by Plan 104-01).
  </read_first>
  <action>
    Operator pushes tag `v0.66.1` (only after Task 2's GREEN smoke confirmation), watches the
    `Release` workflow run to completion, and confirms every signed artifact passes the hardened
    fail-closed verify and that the published GitHub Release shows Verified publisher using the
    Finding-B-corrected gate condition. Full exact command sequence is in `<how-to-verify>` below.
  </action>
  <what-built>
    Plan 104-01 hardened `Assert-TrustedSignature` (dual-engine, D-04 flush fix, NoCheck
    classification) at every Windows-leg verify site this push exercises: "Verify Authenticode
    signatures (Windows)" (nono.exe, broker, both MSI wrappers), "Verify MSI payload signatures
    (Windows)" (embedded nono.exe/broker/nono-wfp-service.exe inside each MSI's administrative
    extract), and "Verify signed binaries inside zip (Windows)". Plan 104-02 neutralized the stale
    `publish-crates` job so it reports skipped rather than failing the overall run. None of this
    loosens the fail-closed gate — it only makes a genuine pass/fail more legible and removes an
    unrelated, already-diagnosed failure mode from this push.
  </what-built>
  <how-to-verify>
    1. Re-confirm nothing has changed since Task 1's checks: `git status` and
       `git ls-files -- 'build_notes/' '.gsd/'` should show nothing staged or tracked under either
       path (the release-readiness gate already checks this statically — re-confirm at push time
       since staging state can change between Task 1's check and now).
    2. Push: `git push origin v0.66.1`. This triggers `.github/workflows/release.yml`'s `on: push:
       tags: 'v*.*.*'` trigger — the `build` matrix (5 targets, including `windows-latest`), then
       `release` (GitHub Release creation), then (neutralized, per Plan 104-02) `publish-crates`
       reporting skipped/not-run rather than failing, then `update-homebrew-core`.
    3. Watch the run to completion: `gh run watch` (or the Actions UI). Confirm every Windows-leg
       verify step passed — "Verify Authenticode signatures (Windows)", "Verify MSI payload
       signatures (Windows)", and "Verify signed binaries inside zip (Windows)" — all call
       `Assert-TrustedSignature -Mode Strict`.
    4. Once the Release is published, download one artifact (e.g. via
       `gh release download v0.66.1 -p 'nono.exe'`) and run
       `Get-AuthenticodeSignature -LiteralPath .\nono.exe` locally. Confirm `Status` is exactly
       `Valid` and `SignerCertificate.Subject` is NOT `CN=nono Test Signing` (or any other known
       test-cert CN).
    5. Do NOT gate step 4's confirmation on the issuer string containing `Microsoft ID Verified CS`
       or rejecting `Enterprise ID Verified Policy` — `101-SIGN03-SMOKE-VERDICT.md` Finding B
       empirically disproved that heuristic; a genuine PublicTrust signature can and does chain
       through `Microsoft Enterprise ID Verified Policy AOC CA NN`. The only two conditions that
       matter are `Status -eq 'Valid'` and "signer is not a known test cert."
    6. Confirm via `gh run list --workflow=release.yml -L 1 --json conclusion -q '.[0].conclusion'`
       returning `success`.
  </how-to-verify>
  <verify>
    <human-check>Operator reports the tag name, the Release-run URL with conclusion=success, and the downloaded artifact's confirmed Get-AuthenticodeSignature.Status=Valid + non-test-cert signer.</human-check>
  </verify>
  <resume-signal>Reply "released" with the tag name and the Release-run URL once all of the above is confirmed, or describe the specific failing step/artifact for a new diagnostic pass (do not proceed to declare REL-01 satisfied on a partial result).</resume-signal>
  <done>Release workflow run for tag v0.66.1 shows conclusion=success, all Windows-leg verify steps passed, and a downloaded published artifact independently confirms Status=Valid with a non-test-cert signer — REL-01's SC2 and SC3 are both satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator (human) <-> outward-facing GitHub Actions dispatch + irreversible git tag push | Both the smoke re-dispatch and the tag push are live, outward-facing, effectively-irreversible actions under this fork's explicit prepare-only/push-operator-gated posture |
| Published GitHub Release artifact <-> end-user trust decision | "Verified publisher" is the artifact-consumer-facing claim this phase exists to make true; misjudging it here propagates to every downstream installer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-03-01 | Tampering | Accepting `Status: UnknownError` (or any non-`Valid` status) under release-pressure "since we know the cause" | mitigate | Task 2's instructions explicitly forbid proceeding to Task 3 without a genuine GREEN smoke run; `Assert-TrustedSignature`'s gate itself is unmodified by Plans 104-01/02 (grep-confirmed there) |
| T-104-03-02 | Spoofing | Gating "Verified publisher" (SC3) on an issuer-substring match instead of `Status`+non-test-signer | mitigate | Task 3's action/instructions explicitly specify the Finding-B-corrected gate condition and explicitly forbid the disproven heuristic, matching `scripts/gates/trusted-signed-assertion.ps1`'s already-correct pattern |
| T-104-03-03 | Repudiation | OIDC FIC-subject drift (Pitfall 9) silently reproducing `AADSTS700213`, misread as "still blocked on the root" | mitigate | Task 2's instructions require an explicit FIC-subject + `environment:` key re-check before every smoke re-dispatch, not just the first one |
| T-104-03-04 | Elevation of scope / process drift | Operator jumps ahead to Phase 107 secret-retirement / `trusted-signing-smoke.yml` removal immediately after this plan's green release | accept (explicit out-of-scope reminder) | Pitfall 10 / ROADMAP Phase 107 dependency: Gate 3 (Phase 106 clean-host UAT) must pass first; this plan's scope ends at REL-01 (Gate 2) and does not touch POC secrets or the smoke workflow file |

</threat_model>

<verification>
Smoke workflow run shows `conclusion: success` with a genuine `Authenticode OK (dual-engine)` verify
line; the Release workflow run for tag `v0.66.1` shows `conclusion: success`; a downloaded published
artifact shows `Get-AuthenticodeSignature.Status -eq 'Valid'` with a non-test-cert signer.
</verification>

<success_criteria>
REL-01's three success criteria (ROADMAP.md Phase 104) are all confirmed true against live,
operator-witnessed evidence: SC1 (smoke green immediately pre-push), SC2 (tag push -> Release
workflow green, all `.exe`+MSIs hardened-verified), SC3 (published artifacts show Verified publisher
via the Finding-B-corrected gate, never the disproven issuer heuristic).
</success_criteria>

<output>
Create .planning/phases/104-smoke-green-cut-the-trusted-signed-release/104-03-SUMMARY.md when done
</output>
