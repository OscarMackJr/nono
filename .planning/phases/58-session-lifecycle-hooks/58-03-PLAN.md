---
phase: 58-session-lifecycle-hooks
plan: 03
type: execute
wave: 2
depends_on:
  - 58-01
files_modified:
  - crates/nono-cli/src/hook_runtime_windows.rs
  - crates/nono-cli/src/exec_strategy/env_sanitization.rs
  - .planning/architecture/adr-58-windows-hook-executor.md
autonomous: true
requirements:
  - REQ-HOOK-01

must_haves:
  truths:
    - "Windows hook executor validates script path (canonical \\?\\, regular file, owner check, effective-rights ACL mask on file AND parent dir, mandatory-label check) before spawning (D-10)"
    - "validate_hook_script_windows rejects scripts in world-writable or lower-IL-writable parent directories (no conditional skip)"
    - "Windows hook filesystem scope is session-dir write + cwd write + script-path read only; everything broader is denied at the OS boundary (D-06)"
    - "Before-hook env-export is ported to Windows: the hook writes KEY=VALUE to the session-dir env file and the Medium-IL parent reads, filters, and injects them into the child (D-07)"
    - "Windows env file uses CREATE_NEW (O_EXCL equivalent) + Low-IL mandatory label; zeroes-then-unlinks on Drop (D-08)"
    - "Windows hooks spawn via LowIlPrimary (nono::create_low_integrity_primary_token) NOT WriteRestricted (D-05)"
    - ".ps1 hooks invoke powershell.exe -NoProfile -NonInteractive -File; .cmd/.bat via cmd.exe /D /C; .exe direct (D-05)"
    - "timeout_secs enforced via TerminateJobObject on a per-hook ProcessContainment (NOT the main session containment)"
    - "is_dangerous_env_var() blocks 10 Windows-specific vars case-insensitively: PATH, PATHEXT, COMSPEC, PSModulePath, PSModuleAnalysisCachePath, __PSLockdownPolicy, SystemRoot, windir, TEMP, TMP (D-09)"
    - "Windows hook env-var values are zeroized after injection into env_vars"
    - "ADR at .planning/architecture/adr-58-windows-hook-executor.md documents D-05..D-10, trust boundary, and fail-closed fork invariant"
    - "Windows hook runtime is gated #[cfg(windows)] in main.rs (done in Plan 02)"
  artifacts:
    - path: "crates/nono-cli/src/hook_runtime_windows.rs"
      provides: "Full Windows hook runtime (replaces Plan 02 stub): execute_before_hook, execute_after_hook, validate_hook_script_windows (with unconditional ACL check), WindowsEnvFileGuard, build_windows_hook_command"
      contains: "fn execute_before_hook"
    - path: "crates/nono-cli/src/exec_strategy/env_sanitization.rs"
      provides: "10 Windows danger vars added to is_dangerous_env_var() with eq_ignore_ascii_case + test"
      contains: "eq_ignore_ascii_case(\"PATH\")"
    - path: ".planning/architecture/adr-58-windows-hook-executor.md"
      provides: "ADR documenting D-05..D-10, trust gap, fail-closed divergence"
      contains: "LowIlPrimary"
  key_links:
    - from: "crates/nono-cli/src/execution_runtime.rs"
      to: "crates/nono-cli/src/hook_runtime_windows.rs"
      via: "#[cfg(windows)] crate::hook_runtime_windows::execute_before_hook(before, session_id, &current_dir)? -- wired in Plan 02"
      pattern: "hook_runtime_windows::execute_before_hook"
    - from: "crates/nono-cli/src/hook_runtime_windows.rs"
      to: "crates/nono/src/sandbox/windows.rs"
      via: "nono::create_low_integrity_primary_token() for Low-IL token creation (D-05)"
      pattern: "create_low_integrity_primary_token"
    - from: "crates/nono-cli/src/hook_runtime_windows.rs"
      to: "crates/nono-cli/src/exec_strategy_windows/labels_guard.rs"
      via: "nono::try_set_mandatory_label for env-file Low-IL label (D-08)"
      pattern: "try_set_mandatory_label"
---

<objective>
Implement the Windows hook executor (`hook_runtime_windows.rs`), replacing the stub created by Plan 02 Task 1 with the full implementation. Extend `is_dangerous_env_var()` with 10 Windows-specific danger vars (D-09), and author the ADR at `.planning/architecture/adr-58-windows-hook-executor.md` documenting D-05..D-10.

Purpose: This is the net-new Windows-specific work of Phase 58, running in parallel with Plan 02 (no file overlap). It delivers the Windows half of REQ-HOOK-01 (SC1 Windows path, SC3 ADR) and closes the security requirements for the env-file trust boundary (D-08, D-09, D-10).

Key design invariants (all locked decisions, not discretionary):
- D-05: LowIlPrimary arm via nono::create_low_integrity_primary_token() — NOT WriteRestricted (CLR fails under WriteRestricted; proven in Phase 60)
- D-06: Hook filesystem scope = session-dir write + cwd write + read on script path only
- D-07: Env-export mechanism ported to Windows; before-hook can write KEY=VALUE to env file
- D-08: CREATE_NEW + Low-IL mandatory label on env file
- D-09: is_dangerous_env_var() extended for 10 Windows vars (case-insensitive)
- D-10: validate_hook_script_windows: canonical \\?\ path + regular file + path_is_owned_by_current_user + effective-rights ACL mask on file AND parent dir (unconditional — no fallback path) + mandatory-label check

Output:
- hook_runtime_windows.rs: execute_before_hook, execute_after_hook, validate_hook_script_windows (full D-10 ACL check, no conditional skip), WindowsEnvFileGuard, build_windows_hook_command, tests including test_validate_rejects_world_writable_parent
- env_sanitization.rs: 10 Windows danger vars + test_windows_dangerous_vars_blocked test
- adr-58-windows-hook-executor.md: ADR with all required sections (Context, Goals, Non-goals, Decision Table, Trust Boundary, Invariants, Fork Divergence Record, Alternatives Considered)
</objective>

<execution_context>
@C:\Users\OMack\Nono\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\Nono\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\OMack\Nono\.planning\PROJECT.md
@C:\Users\OMack\Nono\.planning\ROADMAP.md
@C:\Users\OMack\Nono\.planning\phases\58-session-lifecycle-hooks\58-CONTEXT.md
@C:\Users\OMack\Nono\.planning\phases\58-session-lifecycle-hooks\58-RESEARCH.md
@C:\Users\OMack\Nono\.planning\phases\58-session-lifecycle-hooks\58-PATTERNS.md
@C:\Users\OMack\Nono\.planning\phases\58-session-lifecycle-hooks\58-01-SUMMARY.md
@C:\Users\OMack\Nono\.planning\architecture\v2.6-upstream-merge-deferral-ADR.md

<interfaces>
<!-- Key types and contracts extracted from codebase. -->

From crates/nono/src/sandbox/windows.rs:
- create_low_integrity_primary_token() at line 534: `pub fn create_low_integrity_primary_token() -> Result<OwnedHandle>`
- try_set_mandatory_label() at line 1040: `pub fn try_set_mandatory_label(path: &Path, mask: u32) -> Result<()>`
  - mask 0x5 = NO_WRITE_UP | NO_EXECUTE_UP (used in labels_guard.rs line 365 for Low-IL)

From crates/nono/src/lib.rs (re-exports at line 88):
- `pub use ... try_set_mandatory_label, ... OwnedHandle, ...`
- nono::create_low_integrity_primary_token and nono::try_set_mandatory_label are in scope

From crates/nono-cli/src/exec_strategy_windows/launch.rs:
- terminate_job_object() at line 290: `pub(super) fn terminate_job_object(job: HANDLE, exit_code: u32) -> Result<()>`
  - Visibility: pub(super) — hook_runtime_windows.rs is NOT inside exec_strategy_windows/
  - Solution: expose as pub(crate) in launch.rs (preferred — single source of truth)
- create_low_integrity_primary_token is imported as `nono::create_low_integrity_primary_token` at launch.rs line 1281

From crates/nono-cli/src/exec_strategy_windows/dacl_guard.rs:
- AppliedDaclGrantsGuard: RAII guard for DACL grants; grant_sid_write_on_path / revoke_sid_on_path
- drop() pattern: revoke on Drop; mirrors RAII contract for env file cleanup
- GetEffectiveRightsFromAclW usage pattern for checking SID ACE write rights

From crates/nono-cli/src/exec_strategy_windows/labels_guard.rs:
- AppliedLabelsGuard (RAII mandatory-label guard)
- path_is_owned_by_current_user usage at lines 66-80: ALWAYS pair with effective-rights ACL mask check (feedback_windows_mandatory_label_write_owner discipline)
- labels_guard.rs line 365: `try_set_mandatory_label(&file, 0x5)` — use 0x5 mask for Low-IL NO_WRITE_UP|NO_EXECUTE_UP

From crates/nono-cli/src/exec_strategy/env_sanitization.rs:
- is_dangerous_env_var() at line 14 — extend this function
- env_key_matches at line 55-61: already uses eq_ignore_ascii_case on Windows
- Existing Windows test block at line ~222-231 (cfg(target_os = "windows")) — add new test here
- Current function does NOT include PATH, PATHEXT, COMSPEC (test line 201-202 confirms "PATH" and "HOME" are currently NOT dangerous) — the new Windows entries ADD them for the hook context

From crates/nono-cli/src/session.rs:
- ensure_sessions_dir() and generate_session_id() — use for session-dir path construction (same as Unix)

From .planning/architecture/v2.6-upstream-merge-deferral-ADR.md:
- ADR format reference (Status, Date, Context, Decision, Consequences structure)

From .planning/phases/58-session-lifecycle-hooks/58-RESEARCH.md (Pattern 5, ~lines 319-339):
- GetEffectiveRightsFromAclW pattern for checking if Everyone (S-1-1-0) has a Write ACE
- dacl_guard.rs has the SID construction pattern (Everyone SID) and the ACL retrieval helpers
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend is_dangerous_env_var() with 10 Windows danger vars + test</name>
  <files>crates/nono-cli/src/exec_strategy/env_sanitization.rs</files>
  <read_first>
    - crates/nono-cli/src/exec_strategy/env_sanitization.rs — read the full file (it is ~270 lines per grep; read lines 1-270)
    - Note line 201-202: current tests assert `!is_dangerous_env_var("HOME")` and `!is_dangerous_env_var("PATH")` — these are UNIX tests on a non-Windows host; the Windows extension uses eq_ignore_ascii_case and is gated differently (read the existing cfg(target_os = "windows") test block at line ~222-231 to understand the gating pattern)
    - .planning/phases/58-session-lifecycle-hooks/58-PATTERNS.md — Section "env_sanitization.rs" for exact extension pattern and test
    - .planning/phases/58-session-lifecycle-hooks/58-RESEARCH.md — is_dangerous_env_var Extension Pattern (D-09) for the 10 vars with inline rationale comments
  </read_first>
  <action>
    Read the full is_dangerous_env_var() function body. Understand the existing structure (all existing entries use exact `== "KEY"` comparison).

    Extend is_dangerous_env_var() to add 10 Windows-specific vars using eq_ignore_ascii_case (matching the existing env_key_matches pattern at lines 55-61). Add a comment block before the Windows vars: `// Windows hook env-file injection vectors (Low-IL-writer → Medium-IL-reader trust gap, D-09).` Each var gets an inline rationale comment explaining the attack vector:
    - PATH (eq_ignore_ascii_case): executable resolution hijacking
    - PATHEXT: extension association hijacking
    - COMSPEC: cmd interpreter redirect
    - PSModulePath: PowerShell module injection
    - PSModuleAnalysisCachePath: PS analysis cache poisoning
    - __PSLockdownPolicy: PS constrained-language bypass
    - SystemRoot: system DLL resolution redirect
    - windir: system directory redirect
    - TEMP: temp file redirect from parent perspective
    - TMP: same as TEMP

    Place these entries at the end of the existing || chain in is_dangerous_env_var(), using `|| key.eq_ignore_ascii_case("PATH")` form. The function is a cross-platform boolean or-chain; no cfg gating needed on the function body itself (eq_ignore_ascii_case is stdlib; the vars exist on Windows and are harmless to check on Unix for this filtering purpose).

    Add test `test_windows_dangerous_vars_blocked` in the existing `#[cfg(target_os = "windows")]` test block (analog at line ~222-231). The test must assert:
    - is_dangerous_env_var("PATH") == true
    - is_dangerous_env_var("Path") == true (case-insensitive)
    - is_dangerous_env_var("PATHEXT") == true
    - is_dangerous_env_var("COMSPEC") == true
    - is_dangerous_env_var("PSModulePath") == true
    - is_dangerous_env_var("__PSLockdownPolicy") == true
    - is_dangerous_env_var("SystemRoot") == true
    - is_dangerous_env_var("windir") == true
    - is_dangerous_env_var("TEMP") == true
    - is_dangerous_env_var("TMP") == true

    NOTE: The existing non-Windows test at line ~201-202 asserts `!is_dangerous_env_var("PATH")`. After this change, that assertion will FAIL on Windows because PATH is now dangerous on Windows. Verify if the existing `assert!(!is_dangerous_env_var("PATH"))` line is in a `#[cfg(not(windows))]` block or an unconditional block. If unconditional, wrap it in `#[cfg(not(target_os = "windows"))]` to prevent the contradiction.
  </action>
  <verify>
    <automated>cargo test -p nono-cli --lib -- env_sanitization 2>&1 | tail -20</automated>
  </verify>
  <done>
    - is_dangerous_env_var() returns true for all 10 Windows vars (case-insensitive)
    - test_windows_dangerous_vars_blocked passes on Windows host
    - Existing env_sanitization tests pass (no regressions)
    - No `unwrap()` in production paths
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace hook_runtime_windows.rs stub with full implementation (D-05..D-10, ACL check, zeroize)</name>
  <files>crates/nono-cli/src/hook_runtime_windows.rs</files>
  <read_first>
    - crates/nono-cli/src/hook_runtime_windows.rs — read the stub created by Plan 02 Task 1 to understand the existing public API surface being replaced
    - crates/nono-cli/src/exec_strategy_windows/launch.rs — read lines 280-300 (terminate_job_object body to understand if replication is needed), lines 1110-1160 (LowIlPrimary arm documentation), lines 1270-1310 (create_low_integrity_primary_token usage pattern)
    - crates/nono-cli/src/exec_strategy_windows/dacl_guard.rs — read lines 40-100 (imports, AppliedDaclGrantsGuard::new, Drop pattern) and the GetEffectiveRightsFromAclW usage pattern for Everyone SID ACE checking
    - crates/nono-cli/src/exec_strategy_windows/labels_guard.rs — read lines 60-90 (path_is_owned_by_current_user pattern, try_set_mandatory_label call at line 140, mask 0x5 at line 365)
    - crates/nono-cli/src/session.rs — search for ensure_sessions_dir, generate_session_id signatures
    - crates/nono-cli/src/test_env.rs — ENV_LOCK, EnvVarGuard::set_all
    - .planning/phases/58-session-lifecycle-hooks/58-PATTERNS.md — Section "hook_runtime_windows.rs" for all code patterns: public API surface, build_windows_hook_command, LowIlPrimary spawn note, WindowsEnvFileGuard, validate_hook_script_windows
    - .planning/phases/58-session-lifecycle-hooks/58-RESEARCH.md — Pattern 3 (Windows exec path), Pattern 4 (env-file ACL), Pattern 5 (GetEffectiveRightsFromAclW vet bar, D-10), Pitfall 4 (LowIlPrimary vs BrokerLaunchNoPty), Open Questions (RESOLVED)
    - .planning/phases/58-session-lifecycle-hooks/58-CONTEXT.md — D-05 through D-10 locked decisions
  </read_first>
  <behavior>
    - test_env_file_create_new_prevents_clobber: call WindowsEnvFileGuard::create twice with the same session_id; second call must return Err (CREATE_NEW prevents overwrite)
    - test_validate_hook_script_windows_rejects_relative: call validate_hook_script_windows with a relative path; must return Err containing "absolute"
    - test_validate_hook_script_windows_rejects_non_file: call with a path to a directory; must return Err containing "regular file"
    - test_validate_rejects_world_writable_parent: create a temp directory, grant Everyone (S-1-1-0) a Write ACE on it, create a dummy file inside it owned by the current user, call validate_hook_script_windows on that file; must return Err. This test verifies the effective-rights ACL mask check on the parent dir (D-10 + feedback_windows_mandatory_label_write_owner discipline).
    - test_windows_dangerous_vars_filtered_from_env_file: create an env file containing "PATH=evil\nMY_VAR=safe"; simulate read_env_file + filter; assert PATH is absent and MY_VAR is present in the result
  </behavior>
  <action>
    This task REPLACES the stub created in Plan 02 Task 1 with the full Windows hook executor implementation. The public API signatures (execute_before_hook, execute_after_hook) MUST remain identical to the stub so execution_runtime.rs dispatch (wired in Plan 02 Task 2) continues to compile.

    Add a module-level doc comment recording the D-01/D-02 fork divergence (same as hook_runtime.rs) and citing the ADR. Add: "Windows execution design documented in `.planning/architecture/adr-58-windows-hook-executor.md`."

    **validate_hook_script_windows (D-10) — UNCONDITIONAL, no fallback path:** D-10 is a LOCKED decision. The effective-rights ACL mask check on both the file and its parent directory MUST be implemented. There is no conditional skip or "note it in the SUMMARY and accept the limitation" path. If a direct helper does not exist in dacl_guard.rs, write it inline in this file using windows_sys::Win32::Security. RESEARCH.md Pattern 5 (~lines 319-339) shows the GetEffectiveRightsFromAclW approach; dacl_guard.rs has the SID construction pattern (Everyone SID = S-1-1-0). Implement the check as a private function `fn check_no_world_writable_acl(path: &Path) -> Result<()>` and call it for both the canonical script path AND its parent directory. Steps in order:

    1. `if !path.is_absolute()` → Err (use Path::is_absolute(), NOT string starts_with — CLAUDE.md footgun)
    2. `path.canonicalize()` → std::fs::canonicalize adds `\\?\` prefix on Windows automatically
    3. `canonical.metadata()` + `if !meta.is_file()` → Err
    4. `nono::path_is_owned_by_current_user(&canonical)?` → if false → Err (owner check)
    5. `check_no_world_writable_acl(&canonical)?` — effective-rights ACL check on the FILE: use GetEffectiveRightsFromAclW with the Everyone SID (S-1-1-0) as trustee; if the returned mask includes FILE_WRITE_DATA or GENERIC_WRITE → Err. Pair with the owner check per feedback_windows_mandatory_label_write_owner discipline.
    6. Get `canonical.parent()` → `check_no_world_writable_acl(parent)?` — same ACL check on the PARENT DIRECTORY: if Everyone has FILE_ADD_FILE or GENERIC_WRITE → Err. This is the defense against scripts placed in world-writable directories even if the file itself has tighter permissions.
    7. Mandatory-label check: `nono::try_set_mandatory_label` is not a read-only check; use the existing label-read helper from labels_guard.rs or windows_sys SACL query. Log a warning (not Err) if the file's mandatory label is above Medium-IL.

    **build_windows_hook_command:** Implement explicit interpreter dispatch per PATTERNS.md — `.ps1` → `powershell.exe -NoProfile -NonInteractive -File <path>`, `.cmd`/`.bat` → `cmd.exe /D /C <path>`, other/`.exe` → `Command::new(script)`. Call `cmd.env_clear()` on all variants. NOTE: Research assumption A1 (`cmd.exe /D` disables AutoRun) — add a SAFETY-style comment noting "Assumption A1: /D disables AutoRun registry keys — see RESEARCH.md §Assumptions." The no-JSON-injection rule: script path is always an argument to the interpreter flag (`-File <path>`, `/C <path>`), never inline code.

    **LowIlPrimary spawn (D-05):** The hook executor does NOT call spawn_windows_child or execute_supervised_runtime. It creates its own low-IL primary token via `nono::create_low_integrity_primary_token()` and uses it to spawn the hook `Command` using the `CommandExt` trait or manual `CreateProcessAsUserW`. The hook process is NOT the main sandboxed child — it is a separate short-lived process. Scope the job object to the hook only.

    **timeout_secs enforcement:** terminate_job_object in launch.rs is currently `pub(super)`. Change it to `pub(crate)` in launch.rs (preferred — single source of truth), then call it from hook_runtime_windows.rs for timeout enforcement. On timeout: terminate the hook job, then return Err per D-01/D-03.

    **WindowsEnvFileGuard (D-08):** Use `OpenOptions::new().create_new(true).write(true).open(&path)` for CREATE_NEW disposition. After file creation, call `nono::try_set_mandatory_label(&path, 0x5)` (mask 0x5 = NO_WRITE_UP | NO_EXECUTE_UP — same mask as labels_guard.rs line 365 for Low-IL). The Drop impl: zero-fill then std::fs::remove_file (same contract as Unix EnvFileGuard).

    **execute_before_hook:** Same structure as Unix — validate script (via validate_hook_script_windows, unconditional), create env file guard, build command, spawn with Low-IL token, wait with recv_timeout, read env file, filter dangerous vars. Fail-closed per D-01/D-03.

    **Env-value zeroize (CLAUDE.md §Memory, WARNING 5):** After read_env_file returns the Vec<(String, String)> and before returning from execute_before_hook, zeroize the values: iterate over the filtered env pairs and call `zeroize::Zeroize::zeroize(v)` on each value string after the caller will have consumed them. The implementation pattern mirrors what Plan 02 Task 2 does in execution_runtime.rs. Add `use zeroize::Zeroize;` import. Add comment: `// Zeroize hook env-var values after caller injection; values may be credentials (CLAUDE.md §Memory).`

    **execute_after_hook:** Same structure — validate script, build command, spawn, wait. Fail-closed per D-04.

    **Test module:** Acquire ENV_LOCK + set HOME via EnvVarGuard (same pattern as hook_runtime.rs). All tests in `#[cfg(test)] #[allow(clippy::unwrap_used)] mod tests`. Write RED tests first, then implement. No test should depend on a real broker binary.

    For test_validate_rejects_world_writable_parent: use windows_sys ACL manipulation to grant Everyone a Write ACE on a temp directory, create a dummy owned file inside it, call validate_hook_script_windows, assert Err. Use a teardown guard to revoke the ACE after the test. This test may require the test process to run with sufficient rights — document in the test with `#[ignore]` if it requires admin elevation, but DO NOT skip implementing the test.
  </action>
  <verify>
    <automated>cargo test -p nono-cli --lib -- hook_runtime_windows 2>&1 | tail -25</automated>
  </verify>
  <done>
    - hook_runtime_windows.rs replaces the Plan 02 stub with the full implementation
    - execute_before_hook and execute_after_hook retain the same signatures as the stub (execution_runtime.rs dispatch continues to compile)
    - All 5 behavioral tests pass on Windows host: env_file_create_new_prevents_clobber, validate_rejects_relative, validate_rejects_non_file, test_validate_rejects_world_writable_parent (or marked #[ignore] with admin-rights note), dangerous_vars_filtered
    - validate_hook_script_windows calls check_no_world_writable_acl on BOTH the file AND its parent directory — no conditional skip path exists
    - WindowsEnvFileGuard::create uses create_new(true) and calls try_set_mandatory_label with mask 0x5
    - validate_hook_script_windows uses Path::is_absolute() (not string starts_with) and std::fs::canonicalize
    - build_windows_hook_command uses explicit interpreter dispatch with -NoProfile -NonInteractive for .ps1 and /D for .cmd/.bat
    - terminate_job_object is pub(crate) in launch.rs (no visibility error)
    - Hook env-var values are zeroized after read_env_file returns (zeroize::Zeroize::zeroize called on each value)
    - `cargo build -p nono-cli` clean
    - `cargo clippy -p nono-cli -- -D warnings -D clippy::unwrap_used` clean in production paths
    - No `unwrap()` / `expect()` in non-test code
  </done>
</task>

<task type="auto">
  <name>Task 3: Author ADR at .planning/architecture/adr-58-windows-hook-executor.md (SC3 deliverable)</name>
  <files>.planning/architecture/adr-58-windows-hook-executor.md</files>
  <read_first>
    - .planning/architecture/v2.6-upstream-merge-deferral-ADR.md — read for ADR format used in this repo (Status, Date, Context, Decision, Consequences structure)
    - docs/architecture/broker-trust-anchor.md — read for Phase 32 ADR structure (the primary analog per PATTERNS.md)
    - .planning/phases/58-session-lifecycle-hooks/58-PATTERNS.md — Section ".planning/architecture/adr-58-windows-hook-executor.md" for required sections and header format
    - .planning/phases/58-session-lifecycle-hooks/58-CONTEXT.md — D-05 through D-10 verbatim (these are the canonical Decision IDs to document)
    - .planning/phases/58-session-lifecycle-hooks/58-RESEARCH.md — Security Domain section + Known Threat Patterns table for trust-boundary content
  </read_first>
  <action>
    Create `.planning/architecture/adr-58-windows-hook-executor.md`. This file is the SC3 hard deliverable — it MUST exist with substantive content, not a placeholder.

    Required header:
    ```
    # Windows Session Lifecycle Hook Executor
    **Status:** Accepted
    **Date:** [today]
    **Phase:** 58 (session-lifecycle-hooks)
    **Decision IDs:** D-05, D-06, D-07, D-08, D-09, D-10
    **Related ADR:** docs/architecture/broker-trust-anchor.md (Phase 32 — establishes LowIlPrimary arm)
    ```

    Required sections (each must have substantive prose, not bullet-only):

    **1. Context:** Why Windows hooks diverge from upstream's "host-privileged, outside sandbox" semantics. Upstream runs hooks with the user's full Medium-IL token. The fork runs them Low-IL via broker to enforce least-privilege even for setup/cleanup scripts. Cite D-05 and the Phase 60 lesson (CLR fails under WriteRestricted, proven in project_sandbox_the_tools).

    **2. Goals:** Enumerate D-05, D-06, D-07, D-08, D-09, D-10 verbatim with one-sentence explanation each.

    **3. Non-goals:** Explicitly state that upstream's fail-open behavior is NOT adopted (D-01 is the fork invariant). Also: no host-trusted hook execution, no inline script execution (no-JSON-injection rule).

    **4. Decision Table:** Three rows minimum:
    - LowIlPrimary vs WriteRestricted (D-05): chose LowIlPrimary because CLR/PowerShell fails under WriteRestricted (0xC0000142); Phase 60/62 evidence cited.
    - Env-file Low-IL mandatory label vs DACL-only (D-08): chose label as primary gate; DACL narrowing to hook token SID is defense-in-depth.
    - Fail-closed vs fail-open (D-01): fork chose unconditional fail-closed; no profile-level toggle.

    **5. Trust Boundary:** The Low-IL-writer → Medium-IL-reader env-file gap. The hook process runs Low-IL and writes KEY=VALUE entries to the env file. The Medium-IL parent reads and injects them into the child session. Mitigation: CREATE_NEW (D-08) prevents pre-created env file injection; Low-IL mandatory label restricts env file access; is_dangerous_env_var() filter (D-09) blocks all env vars that could hijack the parent's execution context. The 10 Windows danger vars must be listed by name with the attack vector for each.

    **6. Invariants:** List each invariant that the hook executor implementation MUST preserve:
    - Mandatory-label enforcement: env file carries Low-IL label (mask 0x5); reduces attack surface for other Low-IL processes
    - No host-trusted hook execution: hooks run Low-IL, not at user's full IL
    - Session-dir + cwd-only scope (D-06): filesystem capability grant is minimal
    - Script-file references only: no inline scripts — preserves upstream no-JSON-injection rule
    - Fail-closed: hook failure → session abort (before-hook) or non-zero exit (after-hook)
    - LowIlPrimary arm only: WriteRestricted is FORBIDDEN for hook execution
    - Unconditional ACL check: validate_hook_script_windows MUST check effective-rights (GetEffectiveRightsFromAclW with Everyone SID) on both the file and its parent directory. There is no fallback path that skips this check. D-10 is a locked, unconditional requirement.

    **7. Fork Divergence Record (D-02 requirement):** State explicitly: "This ADR is the canonical record of the fail-closed divergence from upstream commit daa55c8, as required by decision D-02 in .planning/phases/58-session-lifecycle-hooks/58-CONTEXT.md. Upstream fail-open behavior (warn + continue on hook failure) is intentionally overridden. The mechanism is preserved exactly; the fail-policy is hardened."

    **8. Alternatives Considered:**
    - WriteRestricted arm: REJECTED — CLR/PowerShell fails 0xC0000142 under WriteRestricted (proven Phase 60)
    - BrokerLaunchNoPty: REJECTED for hooks — hooks are short-lived, no PTY needed; LowIlPrimary direct spawn is appropriate
    - Profile-level fail-open toggle: REJECTED — D-01 chose unconditional fail-closed; deferred as v2 idea only
    - Skipping effective-rights ACL check when no helper available: REJECTED — D-10 is unconditional; check must be written inline if no existing helper

    Write the ADR as a self-contained document that a new contributor can read without needing to consult CONTEXT.md or RESEARCH.md.
  </action>
  <verify>
    <automated>grep -c "LowIlPrimary\|D-05\|D-08\|D-09\|D-10\|fail-closed" .planning/architecture/adr-58-windows-hook-executor.md</automated>
  </verify>
  <done>
    - .planning/architecture/adr-58-windows-hook-executor.md exists
    - File contains all 8 required sections (Context, Goals, Non-goals, Decision Table, Trust Boundary, Invariants, Fork Divergence Record, Alternatives Considered)
    - grep -c "LowIlPrimary" returns >= 2 (Decision Table + Alternatives)
    - grep -c "D-05\|D-08\|D-09\|D-10" returns >= 4 (Goals section at minimum)
    - Fork Divergence Record section explicitly cites D-02 and declares this as the canonical record
    - The 10 Windows danger vars are named in the Trust Boundary section
    - Invariants section explicitly states the ACL check (D-10) is unconditional — no fallback path
    - `cargo test --workspace` still passes (ADR is a markdown file, no compilation impact)
    - All commits for this plan include the DCO sign-off trailer `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All three plans are now code-complete:
    - Plan 01: SessionHooks type + 4-lockstep profile pipeline + schema
    - Plan 02: Unix hook_runtime.rs (fail-closed) + Windows stub + execution_runtime.rs dispatch + zeroize
    - Plan 03: Windows hook_runtime_windows.rs (full implementation, replacing stub) + is_dangerous_env_var Windows extension + ADR
  </what-built>
  <how-to-verify>
    Manual Windows UAT for the hook executor (automated tests cover unit behavior; live execution requires real console + dev-layout binary):

    1. Build the dev-layout binary: `cargo build -p nono-cli --release`
    2. Create a test profile at `%TEMP%\hook-test-profile.json` with content:
       `{"session_hooks": {"before": {"script": "%TEMP%\\hook-before.ps1", "timeout_secs": 10}}}`
    3. Create hook script `%TEMP%\hook-before.ps1` owned by your user (Create-Item, then check ownership):
       `Add-Content -Path $env:NONO_ENV_FILE -Value "HOOK_VAR=hello_from_hook"`
    4. Run from a profile-covered cwd (e.g., `%USERPROFILE%\.claude`):
       `target\release\nono.exe run --profile %TEMP%\hook-test-profile.json --allow-read %TEMP%\hook-before.ps1 -- powershell.exe -Command "echo $env:HOOK_VAR"`
    5. Expected: output contains `hello_from_hook` (env var exported by hook, injected into child)
    6. Verify hook ran Low-IL: check Process Explorer or event log for the hook process IL
    7. Test fail-closed: modify hook script to `exit 1`; re-run; confirm nono exits non-zero and session did NOT start

    If `LowIlPrimary` causes 0xC0000142 for powershell.exe hook: record in SUMMARY.md as an open issue referencing Research Open Question 1 (RESOLVED: "Implement with LowIlPrimary direct spawn first; Plan 03 Task 4 human-verify checkpoint provides the 0xC0000142 escalation path (per Research Assumption A3)."), and file a debug session. This is the expected failure mode if CSRSS console-attach is required — do not block plan completion; record and defer.
  </how-to-verify>
  <resume-signal>
    Type "approved" if hooks work (HOOK_VAR exported, fail-closed confirmed), OR describe any failure (exit code, error message, 0xC0000142 etc.) for triage.

    Approved automatically for CI-only environments where real console is unavailable — record as manual_needed in SUMMARY.md with the VALIDATION.md UAT instructions.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Low-IL hook process → env file | Hook writes KEY=VALUE; Medium-IL parent reads; D-08/D-09 protect |
| env file → parent env_vars | Parent reads env file and injects into child; dangerous-var filter applied |
| script path → validate_hook_script_windows | User-controlled path from profile; must be canonical, owned, not world-writable (unconditional ACL check) |
| interpreter dispatch → hook process spawn | Explicit interpreter selection; no shell-association lookup (attacker-influenceable) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-03-01 | Elevation of Privilege | env-file Low-IL-writer → Medium-IL-reader gap | mitigate | CREATE_NEW prevents pre-created file injection (D-08); Low-IL mandatory label (mask 0x5) restricts access to Low-IL+ principals; is_dangerous_env_var() blocks 10 Windows vars that could hijack parent execution context (D-09) |
| T-58-03-02 | Spoofing | hook script not owned by current user | mitigate | validate_hook_script_windows calls nono::path_is_owned_by_current_user(); returns Err if false; paired with effective-rights ACL mask check per feedback_windows_mandatory_label_write_owner discipline |
| T-58-03-03 | Tampering | hook script in world-writable / lower-IL-writable directory | mitigate | validate_hook_script_windows unconditionally calls check_no_world_writable_acl on the file AND parent dir via GetEffectiveRightsFromAclW with Everyone SID (S-1-1-0); returns Err if Write ACE found; no conditional skip path |
| T-58-03-04 | Tampering | HKEY_CLASSES_ROOT\.ps1 shell-association hijacking | mitigate | Explicit powershell.exe -NoProfile -NonInteractive -File dispatch — NOT shell-association lookup; registry manipulation cannot redirect the hook interpreter |
| T-58-03-05 | Tampering | cmd.exe AutoRun registry injection | mitigate | cmd.exe /D flag disables AutoRun registry keys; research assumption A1 (well-known Windows behavior; verify via Windows docs before implementing) |
| T-58-03-06 | Tampering | PowerShell $PROFILE injection | mitigate | -NoProfile flag on powershell.exe prevents $PROFILE execution; hook cannot be bootstrapped via profile script |
| T-58-03-07 | Denial of Service | hanging hook blocks session indefinitely | mitigate | recv_timeout on timeout_secs; on timeout → terminate_job_object (kills entire hook job tree) → return Err (fail-closed per D-03) |
| T-58-03-08 | Tampering | symlink swap after validate_hook_script_windows (TOCTOU) | accept | canonicalize() resolves symlinks at validation time; residual TOCTOU is same accepted trade-off as upstream and REQ-UNDO-TOCTOU-01; documented in ADR |
| T-58-03-09 | Information Disclosure | env file readable by other Low-IL processes on same session | mitigate | Low-IL mandatory label is the primary gate; DACL narrowing to hook token SID is defense-in-depth (Phase 58 V1 uses label alone; DACL narrowing deferred and documented in ADR per research Pattern 4 assumption A2) |
| T-58-03-10 | Elevation of Privilege | WriteRestricted arm used for hook spawn | mitigate | D-05 locks to LowIlPrimary arm; WriteRestricted is explicitly forbidden in hook_runtime_windows.rs and ADR; CLR startup failure (0xC0000142) is the proven consequence |
| T-58-03-11 | Information Disclosure | hook env-var values persist in-memory after child spawn | mitigate | zeroize::Zeroize::zeroize called on each env-var value after read_env_file returns; limits in-memory lifetime of credential values |
| T-58-03-SC | Tampering | npm/pip/cargo installs | accept | No new external packages; all deps are existing workspace crates |
</threat_model>

<verification>
1. `cargo test -p nono-cli --lib -- hook_runtime_windows` — all 5 behavioral tests pass (env_file_create_new, validate_rejects_relative, validate_rejects_non_file, test_validate_rejects_world_writable_parent, dangerous_vars_filtered)
2. `cargo test -p nono-cli --lib -- env_sanitization` — test_windows_dangerous_vars_blocked passes; existing tests unchanged
3. `grep -c "LowIlPrimary\|D-05\|D-08\|D-09\|D-10\|fail-closed" .planning/architecture/adr-58-windows-hook-executor.md` — at least 6 matches
4. `grep "unwrap\|expect" crates/nono-cli/src/hook_runtime_windows.rs` filtered to non-test lines — returns 0
5. `grep -c "check_no_world_writable_acl\|GetEffectiveRightsFromAclW\|world_writable" crates/nono-cli/src/hook_runtime_windows.rs` — at least 2 (function definition + call for both file and parent)
6. `grep -c "Zeroize\|zeroize" crates/nono-cli/src/hook_runtime_windows.rs` — at least 1 (env-value zeroize)
7. `cargo test --workspace` — no new failures relative to Phase 57 baseline
8. `cargo clippy -p nono-cli -- -D warnings -D clippy::unwrap_used` — clean in production paths
9. Cross-target clippy: PARTIAL/deferred-to-CI — hook_runtime_windows.rs is #[cfg(windows)] only; new cfg-gated unix code from Plan 02 (hook_runtime.rs) also requires Linux/macOS clippy. Record in SUMMARY.md with cross-target-verify-checklist.md PARTIAL format.
</verification>

<success_criteria>
- hook_runtime_windows.rs: full implementation (stub replaced) with execute_before_hook + execute_after_hook + validate_hook_script_windows (unconditional ACL check on file AND parent dir, no conditional skip) + WindowsEnvFileGuard + build_windows_hook_command + zeroize on env-var values
- test_validate_rejects_world_writable_parent exists and tests that validate_hook_script_windows returns Err for a script in an Everyone-writable directory
- env_sanitization.rs: 10 Windows danger vars present in is_dangerous_env_var() with eq_ignore_ascii_case; test_windows_dangerous_vars_blocked passes
- adr-58-windows-hook-executor.md: all 8 required sections present; Fork Divergence Record cites D-02; D-05..D-10 all documented; 10 Windows danger vars named in Trust Boundary section; Invariants section states ACL check is unconditional
- cargo test --workspace passes with no new failures
- No unwrap/expect in production paths
- Cross-target clippy PARTIAL noted in SUMMARY with specific affected files
</success_criteria>

<output>
Create `.planning/phases/58-session-lifecycle-hooks/58-03-SUMMARY.md` when done. Record:
- Cross-target clippy verification status (FULL PASS or PARTIAL with specific cfg-gated files named)
- Result of the Windows UAT checkpoint (approved / manual_needed / failure description)
- Any open issues (e.g., Research Open Question 1 on LowIlPrimary + powershell.exe)
- ADR file path confirmed
</output>
