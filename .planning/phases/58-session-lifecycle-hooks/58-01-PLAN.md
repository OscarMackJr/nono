---
phase: 58-session-lifecycle-hooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono-cli/src/profile/mod.rs
  - crates/nono-cli/src/policy.rs
  - crates/nono-cli/src/sandbox_prepare.rs
  - crates/nono-cli/src/launch_runtime.rs
  - crates/nono-cli/data/nono-profile.schema.json
autonomous: true
requirements:
  - REQ-HOOK-01

must_haves:
  truths:
    - "A profile JSON with session_hooks.before and session_hooks.after deserializes into Profile.session_hooks without data loss"
    - "merge_profiles merges session_hooks with Option-semantics: child slot wins per before/after slot"
    - "to_raw_profile() includes session_hooks in the produced Profile struct literal"
    - "PreparedSandbox carries session_hooks from the loaded profile"
    - "ExecutionFlags.session_hooks receives session_hooks from PreparedSandbox via LaunchPlan wiring"
    - "nono-profile.schema.json contains SessionHooks and SessionHook $defs"
  artifacts:
    - path: "crates/nono-cli/src/profile/mod.rs"
      provides: "SessionHook + SessionHooks structs + Profile.session_hooks + ProfileDeserialize.session_hooks + From<ProfileDeserialize> + merge_profiles update"
      contains: "pub struct SessionHooks"
    - path: "crates/nono-cli/data/nono-profile.schema.json"
      provides: "SessionHooks and SessionHook $defs + session_hooks property"
      contains: "SessionHooks"
    - path: "crates/nono-cli/src/policy.rs"
      provides: "ProfileDef.session_hooks + to_raw_profile() forwarding"
      contains: "session_hooks: self.session_hooks.clone()"
    - path: "crates/nono-cli/src/sandbox_prepare.rs"
      provides: "PreparedSandbox.session_hooks field"
      contains: "session_hooks: profile::SessionHooks"
    - path: "crates/nono-cli/src/launch_runtime.rs"
      provides: "ExecutionFlags.session_hooks + defaults() + LaunchPlan wiring"
      contains: "session_hooks: profile::SessionHooks"
  key_links:
    - from: "crates/nono-cli/src/policy.rs"
      to: "crates/nono-cli/src/profile/mod.rs"
      via: "to_raw_profile() produces Profile; ProfileDef.session_hooks cloned"
      pattern: "session_hooks: self\\.session_hooks\\.clone"
    - from: "crates/nono-cli/src/sandbox_prepare.rs"
      to: "crates/nono-cli/src/launch_runtime.rs"
      via: "PreparedSandbox.session_hooks carried into ExecutionFlags via prepare_run_launch_plan"
      pattern: "flags\\.session_hooks"
---

<objective>
Add the `SessionHooks` / `SessionHook` types to the fork and thread them through the full profile pipeline so that executors in Plans 02 and 03 can wire before/after hook dispatch. This plan delivers the type system and data plumbing only — no runtime execution logic.

Purpose: The `SessionHooks` type does NOT exist in the fork yet. Phase 55 deferred the production hunk of upstream `1a764d05`. This plan adds the types and ensures they survive the complete 4-location lockstep (Profile, ProfileDeserialize, From<ProfileDeserialize>, merge_profiles), flow into to_raw_profile and PreparedSandbox, and are reflected in the JSON schema. Plans 02 and 03 depend on these types existing.

Output:
- SessionHook + SessionHooks structs in profile/mod.rs (with deny_unknown_fields, serde defaults)
- session_hooks field added at all 4 lockstep locations in profile/mod.rs
- ProfileDef.session_hooks + to_raw_profile() in policy.rs
- PreparedSandbox.session_hooks in sandbox_prepare.rs
- ExecutionFlags.session_hooks + defaults() + LaunchPlan wiring in launch_runtime.rs
- SessionHooks + SessionHook $defs + session_hooks property in nono-profile.schema.json
- Unit tests: deserialization, unknown-field rejection, merge semantics (2 cases), to_raw_profile passthrough
</objective>

<execution_context>
@C:\Users\OMack\Nono\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\Nono\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\OMack\Nono\.planning\PROJECT.md
@C:\Users\OMack\Nono\.planning\ROADMAP.md
@C:\Users\OMack\Nono\.planning\phases\58-session-lifecycle-hooks\58-CONTEXT.md
@C:\Users\OMack\Nono\.planning\phases\58-session-lifecycle-hooks\58-RESEARCH.md
@C:\Users\OMack\Nono\.planning\phases\58-session-lifecycle-hooks\58-PATTERNS.md

<interfaces>
<!-- Key types and integration points extracted from codebase. -->

From crates/nono-cli/src/profile/mod.rs:
- Profile struct field (analog at line 2184): `pub windows_low_il_broker: bool,`
- ProfileDeserialize struct field (analog at line 2246): `windows_low_il_broker: bool,`
- From<ProfileDeserialize> body (line 2281): `windows_low_il_broker: raw.windows_low_il_broker,`
- merge_profiles function signature (line 2957): `fn merge_profiles(base: Profile, child: Profile) -> Profile`
- merge_profiles windows_low_il_broker line (line 3171): `windows_low_il_broker: base.windows_low_il_broker || child.windows_low_il_broker,`
- HooksConfig at line ~2230: DO NOT CONFUSE with SessionHooks — that is the Claude Code PreToolUse/PostToolUse install config

From crates/nono-cli/src/policy.rs:
- ProfileDef struct (line ~146-150): contains `pub windows_low_il_broker: bool,`
- to_raw_profile() struct literal (line ~155-212): `windows_low_il_broker: self.windows_low_il_broker,` at line 210

From crates/nono-cli/src/sandbox_prepare.rs:
- PreparedSandbox struct definition at line 66
- allowed_env_vars field pattern (lines 89-91): `pub(crate) allowed_env_vars: Option<Vec<String>>,`
- PreparedSandbox construction at line 506-529: `allowed_env_vars: profile_allowed_env_vars,` at line 529

From crates/nono-cli/src/launch_runtime.rs:
- ExecutionFlags struct (line 163)
- ExecutionFlags::defaults() (line 206)
- LaunchPlan struct at line 33, flags field at line 47: `pub(crate) flags: ExecutionFlags,`
- prepare_run_launch_plan function at line 243

From crates/nono-cli/data/nono-profile.schema.json:
- HookConfig $def exists at schema line ~763 — insert SessionHooks + SessionHook AFTER it
- "hooks" property at schema line ~67 — insert "session_hooks" property after it
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SessionHook + SessionHooks types and thread through profile/mod.rs (4 lockstep locations)</name>
  <files>crates/nono-cli/src/profile/mod.rs</files>
  <read_first>
    - crates/nono-cli/src/profile/mod.rs — read lines 2180-2300 (Profile struct, ProfileDeserialize, From impl) and lines 2957-3180 (merge_profiles) and lines 7600-7650 (existing test module pattern near windows_low_il_broker_tests)
    - Run: git show daa55c8 -- crates/nono-cli/src/profile/mod.rs | head -300 (get the upstream SessionHook/SessionHooks struct definitions and merge_profiles hunk verbatim)
    - .planning/phases/58-session-lifecycle-hooks/58-PATTERNS.md — Section "crates/nono-cli/src/profile/mod.rs" for exact code patterns at all 4 locations
    - .planning/phases/58-session-lifecycle-hooks/58-RESEARCH.md — Pattern 1 (fail-closed) and Pitfall 2 (4-location lockstep) and Pitfall 1 (do NOT confuse SessionHooks with HooksConfig at line 2230)
  </read_first>
  <behavior>
    - test_session_hooks_basic_deserialize: profile JSON with session_hooks.before.script + timeout_secs and session_hooks.after.script parses into SessionHooks with correct fields
    - test_session_hooks_rejects_unknown_field: JSON with session_hooks.before.unknown_field returns serde error (deny_unknown_fields enforced on SessionHook)
    - test_merge_profiles_session_hooks_child_overrides_per_field: child.session_hooks.before wins; base.session_hooks.after is inherited when child.after is None
    - test_merge_profiles_session_hooks_child_inherits_when_absent: child with no session_hooks at all inherits base.session_hooks.before and base.session_hooks.after unchanged
  </behavior>
  <action>
    Run `git show daa55c8 -- crates/nono-cli/src/profile/mod.rs` to extract the upstream SessionHook + SessionHooks struct definitions and merge_profiles hunk verbatim before writing any code.

    Add `SessionHook` and `SessionHooks` structs to profile/mod.rs, placed near the other hook-related types (after HooksConfig, before the ProfileDeserialize section). Use `#[serde(deny_unknown_fields)]` on SessionHook (per upstream), `#[derive(Debug, Clone, Default, Serialize, Deserialize)]` on SessionHooks, `#[derive(Debug, Clone, Serialize, Deserialize)]` on SessionHook (no Default — it has a required `script` field). Fields: `pub script: PathBuf` and `#[serde(default, skip_serializing_if = "Option::is_none")] pub timeout_secs: Option<u64>`. Add doc comments referencing the ADR at `.planning/architecture/adr-58-windows-hook-executor.md` for Windows execution details.

    Thread session_hooks at all FOUR lockstep locations — each must be present or the code will not compile due to the struct-literal completeness check:
    1. Profile struct: add `#[serde(default)] pub session_hooks: SessionHooks,` alongside windows_low_il_broker (line ~2184); add doc comment "Phase 58: session lifecycle hooks. Field is cross-platform; runtime execution is platform-gated in hook_runtime.rs / hook_runtime_windows.rs."
    2. ProfileDeserialize struct: add `#[serde(default)] session_hooks: SessionHooks,` alongside windows_low_il_broker (line ~2246); note that deny_unknown_fields on ProfileDeserialize REQUIRES this entry for round-tripping.
    3. From<ProfileDeserialize> body: add `session_hooks: raw.session_hooks,` at the appropriate position in the struct literal (line ~2281); the completeness check will fail to compile if omitted.
    4. merge_profiles function: add the per-slot Option-semantics block — `session_hooks: SessionHooks { before: child.session_hooks.before.or(base.session_hooks.before), after: child.session_hooks.after.or(base.session_hooks.after), },` (do NOT use || semantics as with bool fields; hooks use Option::or semantics per upstream daa55c8).

    Add tests in a `#[cfg(test)] mod session_hooks_tests` block (following the windows_low_il_broker_tests pattern at line ~7637). Write RED tests first, then implement.

    Per D-02: the field deserializes on all platforms — do NOT gate it with `#[cfg(unix)]` or `#[cfg(windows)]`.
  </action>
  <verify>
    <automated>cargo test -p nono-cli --lib -- session_hooks 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `pub struct SessionHooks` and `pub struct SessionHook` exist in profile/mod.rs
    - `Profile.session_hooks`, `ProfileDeserialize.session_hooks`, `From<ProfileDeserialize>` body, and `merge_profiles` all carry the field (4-location lockstep)
    - `cargo test -p nono-cli --lib -- session_hooks` passes all 4 tests: basic deserialize, unknown-field rejection, child-overrides-per-slot, child-inherits-absent
    - `cargo clippy -p nono-cli -- -D warnings -D clippy::unwrap_used` passes (production code paths only — tests use `#[allow(clippy::unwrap_used)]`)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread session_hooks through policy.rs, sandbox_prepare.rs, launch_runtime.rs, and schema</name>
  <files>
    crates/nono-cli/src/policy.rs,
    crates/nono-cli/src/sandbox_prepare.rs,
    crates/nono-cli/src/launch_runtime.rs,
    crates/nono-cli/data/nono-profile.schema.json
  </files>
  <read_first>
    - crates/nono-cli/src/policy.rs — read lines 110-215 (ProfileDef struct + to_raw_profile full body); note windows_low_il_broker at line 150 and line 210 as exact analogs
    - crates/nono-cli/src/sandbox_prepare.rs — read lines 60-115 (PreparedSandbox struct) and lines 500-545 (PreparedSandbox construction at line 506)
    - crates/nono-cli/src/launch_runtime.rs — read lines 160-250 (ExecutionFlags struct + defaults() + prepare_run_launch_plan start)
    - crates/nono-cli/data/nono-profile.schema.json — read lines 60-80 (properties block, find "hooks") and lines 755-780 (HookConfig $def, insertion point for new $defs)
    - .planning/phases/58-session-lifecycle-hooks/58-PATTERNS.md — sections for policy.rs, sandbox_prepare.rs, launch_runtime.rs, and nono-profile.schema.json
    - Run: git show daa55c8 -- crates/nono-cli/data/nono-profile.schema.json (extract SessionHooks + SessionHook $def JSON verbatim)
  </read_first>
  <behavior>
    - test_to_raw_profile_includes_session_hooks: a ProfileDef with session_hooks.before set produces a Profile with session_hooks.before set (non-None) from to_raw_profile()
    - test_schema_has_session_hooks_property: nono-profile.schema.json contains "session_hooks" as a property key
    - test_schema_has_session_hooks_defs: nono-profile.schema.json contains "$defs/SessionHooks" and "$defs/SessionHook"
  </behavior>
  <action>
    Run `git show daa55c8 -- crates/nono-cli/data/nono-profile.schema.json` to extract the upstream SessionHooks and SessionHook $defs JSON verbatim before editing the schema.

    **policy.rs:** Read the ProfileDef struct (line ~110-155). Add `#[serde(default)] pub session_hooks: profile::SessionHooks,` to ProfileDef alongside windows_low_il_broker (line ~150). In to_raw_profile() (line ~155), add `session_hooks: self.session_hooks.clone(),` to the Profile struct literal alongside the windows_low_il_broker forwarding at line ~210. The inline comment pattern: "// Phase 58: forward session_hooks from policy.json built-in profile verbatim."

    **sandbox_prepare.rs:** Add `pub(crate) session_hooks: profile::SessionHooks,` to PreparedSandbox struct (lines ~66-105), with doc comment "Phase 58: session lifecycle hooks carried forward from loaded profile for dispatch in execution_runtime::execute_sandboxed." In the PreparedSandbox construction block at line ~506-529, add: `session_hooks: loaded_profile.as_ref().map(|p| p.session_hooks.clone()).unwrap_or_default(),` following the allowed_env_vars pattern at lines 527-532. Also add `session_hooks: profile::SessionHooks::default(),` to the earlier PreparedSandbox construction at line ~289-312 (the prepare_sandbox path that does not load a profile).

    **launch_runtime.rs:** Add `pub(crate) session_hooks: profile::SessionHooks,` to ExecutionFlags struct (line ~163-205), with doc comment explaining cross-platform field. Add `session_hooks: profile::SessionHooks::default(),` to ExecutionFlags::defaults() (line ~206-235). In prepare_run_launch_plan (line ~243), after PreparedSandbox is available via `prepared`, add `flags.session_hooks = prepared.session_hooks.clone();` — this is the 3-line wiring block from upstream daa55c8. If ExecutionFlags fields are only consumed inside cfg-gated blocks in execution_runtime.rs, add `#[cfg_attr(not(any(unix, target_os = "windows")), allow(dead_code))]` following the pattern at launch_runtime.rs:177-191.

    **nono-profile.schema.json:** Port upstream daa55c8 schema additions verbatim. Add `"session_hooks": { "$ref": "#/$defs/SessionHooks", "description": "..." }` to the properties block after the existing "hooks" entry. Add "SessionHooks" and "SessionHook" object definitions to the "$defs" section after the existing HookConfig $def. Use `"additionalProperties": false` on both objects per upstream. SessionHook requires ["script"].

    Add tests for to_raw_profile passthrough and schema content checks — place in policy.rs test module (existing tests at line ~2428) and a schema_shape assertion in tests/schema_shape.rs or as a doc-test.
  </action>
  <verify>
    <automated>cargo test -p nono-cli --lib -- "session_hooks\|to_raw_profile\|schema" 2>&1 | tail -25</automated>
  </verify>
  <done>
    - ProfileDef has session_hooks field; to_raw_profile() includes it in the produced Profile
    - PreparedSandbox has session_hooks field at both construction sites
    - ExecutionFlags has session_hooks field; defaults() sets it to Default; prepare_run_launch_plan wires it from prepared
    - nono-profile.schema.json contains "session_hooks" property + "SessionHooks" + "SessionHook" $defs
    - `cargo test -p nono-cli --lib -- session_hooks` still green after this task
    - `cargo build -p nono-cli` compiles without errors (struct-literal completeness checks pass)
    - `cargo clippy -p nono-cli -- -D warnings -D clippy::unwrap_used` clean
    - All commits for this plan include the DCO sign-off trailer `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON profile deserialization | Profile JSON from user-controlled file crosses into validated Rust types |
| schema validation surface | nono-profile.schema.json determines what profile fields are accepted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01-01 | Spoofing | ProfileDeserialize.session_hooks | mitigate | `#[serde(deny_unknown_fields)]` on SessionHook prevents unknown field injection at deserialization; unknown fields cause serde error, not silent drop |
| T-58-01-02 | Tampering | schema — session_hooks.script type | mitigate | schema type="string" only; script path validation (absolute, canonical, owned) occurs at execution time in Plans 02/03, not schema-level |
| T-58-01-03 | Information Disclosure | merge_profiles Option::or semantics | accept | Child-wins-per-slot is the correct merge behavior per upstream daa55c8; no information is exposed by this merge; base and child are both operator-controlled |
| T-58-01-SC | Tampering | npm/pip/cargo installs | accept | No new external packages; all deps are existing workspace crates — no supply-chain risk this plan |
</threat_model>

<verification>
1. `cargo test -p nono-cli --lib -- session_hooks` — all 4 profile/mod.rs tests and to_raw_profile test green
2. `cargo build -p nono-cli` — compiles cleanly; struct-literal completeness check passes (no "missing field" error)
3. `cargo clippy -p nono-cli -- -D warnings -D clippy::unwrap_used` — clean in production paths
4. `grep -c "SessionHooks" crates/nono-cli/src/profile/mod.rs` — at least 4 occurrences (struct def, Profile field, ProfileDeserialize field, merge_profiles)
5. `grep -c "session_hooks" crates/nono-cli/data/nono-profile.schema.json` — at least 1 occurrence
6. Cross-target clippy: `cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used` — PARTIAL/deferred-to-CI per CLAUDE.md MUST rule (dev host is Windows; profile/mod.rs contains no cfg-gated Unix code itself but Plans 02/03 will add cfg-gated files). Record PARTIAL in SUMMARY.
</verification>

<success_criteria>
- SessionHook + SessionHooks types exist in profile/mod.rs with correct serde attributes
- 4-location lockstep in profile/mod.rs is complete (compiler enforces via struct-literal completeness check)
- session_hooks threads from ProfileDef → to_raw_profile → Profile → PreparedSandbox → ExecutionFlags
- nono-profile.schema.json has SessionHooks/$defs and session_hooks property
- All new tests pass; `cargo build -p nono-cli` is green
- No `unwrap()`/`expect()` in production paths
</success_criteria>

<output>
Create `.planning/phases/58-session-lifecycle-hooks/58-01-SUMMARY.md` when done.
</output>
