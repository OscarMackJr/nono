---
phase: 101
plan: 04
type: execute
wave: 4
depends_on: ["101-02", "101-03"]
files_modified:
  - .planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-SIGN03-SMOKE-VERDICT.md
autonomous: true
requirements: [SIGN-03]

must_haves:
  truths:
    - "The Trusted Signing Smoke Test workflow runs GREEN on windows-latest"
    - "The embedded signature's issuer chains to a public Microsoft ID Verified CS EOC/AOC CA root — not PublicTrustTest, not the POC root, not the Enterprise ID Verified Policy AOC CA naming"
  artifacts:
    - path: ".planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-SIGN03-SMOKE-VERDICT.md"
      provides: "Recorded smoke-run verdict: run URL, final Status, verbatim Issuer string, pass/fail"
  key_links:
    - from: "101-SIGN03-SMOKE-VERDICT.md"
      to: ".github/workflows/trusted-signing-smoke.yml run"
      via: "gh run URL reference"
      pattern: "github.com/OscarMackJr/nono/actions/runs"
---

<objective>
Dispatch the hardened "Trusted Signing Smoke Test" workflow on GitHub's clean `windows-latest` runner
and assert it is GREEN with an issuer chaining to a public `Microsoft ID Verified CS EOC/AOC CA NN`
root — proving the signing chain is live end-to-end before Phase 104 cuts the real release.

Purpose: SIGN-03 is the phase's terminal acceptance gate — it can only go green once both the SIGN-02
hardening (Plans 01-02) and the SIGN-01 operator profile-fix (Plan 03) have landed.

Output: A recorded GREEN smoke-run verdict in
`.planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-SIGN03-SMOKE-VERDICT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-CONTEXT.md
@.planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-03-SUMMARY.md
@.github/workflows/trusted-signing-smoke.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch the smoke workflow and assert a GREEN, correctly-chained result</name>
  <files></files>
  <read_first>
    .github/workflows/trusted-signing-smoke.yml (post-Plan-02 wiring) — `workflow_dispatch` trigger,
    the `Development` environment requirement, and the verify step now calling
    `Assert-TrustedSignature -Path "smoke\nono-smoke.exe" -Mode Strict`.
    101-RESEARCH.md "Specific Ideas" / Code Examples "Extracting a raw distinguishing name check" —
    the exact issuer-naming differentiator: `Microsoft ID Verified CS.*(EOC|AOC) CA` = correct public
    root; `Enterprise ID Verified Policy` = the `PublicTrustTest` tell to explicitly reject.
    101-03-SUMMARY.md (Plan 03's checkpoint outcome) — confirms the profile-type fix (if any) has
    landed before this dispatch.
  </read_first>
  <action>
    Run `gh workflow run trusted-signing-smoke.yml` against the `OscarMackJr/nono` repo, capture the
    dispatched run ID (`gh run list --workflow=trusted-signing-smoke.yml --limit 1 --json
    databaseId,url,status`), then `gh run watch &lt;run-id&gt; --exit-status` to block until completion.
    On completion, fetch the job log for the "Verify the embedded signature is OUR Trusted Signing
    cert" step via `gh run view &lt;run-id&gt; --log` and extract the `Issuer:` line. Assert: (1) the run's
    overall conclusion is `success`; (2) the extracted issuer string matches the regex
    `Microsoft ID Verified CS.*(EOC|AOC) CA` (case-sensitive substring match is acceptable); (3) the
    issuer string does NOT match `PublicTrustTest` or `Enterprise ID Verified Policy` — if either
    negative match fires even though the run reports `success`, treat this as a FAILED verdict (the
    workflow could go green on a status check while still chaining to the wrong root — the issuer
    assertion is the real gate, not merely `conclusion == success`).
  </action>
  <verify>
    <automated>gh run list --workflow=trusted-signing-smoke.yml --limit 1 --json conclusion,url</automated>
  </verify>
  <acceptance_criteria>
    - behavior: `gh run view &lt;run-id&gt; --json conclusion --jq .conclusion` returns `success`.
    - behavior: the extracted `Issuer:` line from the run log matches
      `Microsoft ID Verified CS.*(EOC|AOC) CA` and does NOT contain `PublicTrustTest` or
      `Enterprise ID Verified Policy`.
    - test-command: `gh run view &lt;run-id&gt; --log | grep -i "Issuer:"` returns exactly the expected
      issuer line (used as the evidence quoted into Task 2's verdict file).
  </acceptance_criteria>
  <done>
    The dispatched `trusted-signing-smoke.yml` run completed with `conclusion == success` AND its
    logged `Issuer:` line chains to the public `Microsoft ID Verified CS EOC/AOC CA` root, not
    `PublicTrustTest` or the POC root.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record the SIGN-03 smoke verdict</name>
  <files>.planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-SIGN03-SMOKE-VERDICT.md</files>
  <read_first>
    Task 1's captured run URL, conclusion, and verbatim `Issuer:` line.
    .planning/REQUIREMENTS.md SIGN-03 (the locked acceptance wording this verdict must satisfy).
  </read_first>
  <action>
    Create `101-SIGN03-SMOKE-VERDICT.md` recording: the run URL (from `gh run view &lt;run-id&gt; --json
    url`), the timestamp, the overall conclusion, the verbatim `Issuer:` line captured in Task 1, and
    an explicit statement that the issuer matches the required public-root pattern and does not match
    either rejected pattern — cross-referencing SIGN-03. If Task 1's assertions failed (non-success
    conclusion, or a wrong-root issuer despite a green status), record the FAILING verdict here instead
    of a false PASS — this file must reflect the actual outcome, not an assumed one.
  </action>
  <verify>
    <automated>test -f .planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-SIGN03-SMOKE-VERDICT.md &amp;&amp; grep -q "SIGN-03" .planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-SIGN03-SMOKE-VERDICT.md</automated>
  </verify>
  <acceptance_criteria>
    - source: the file contains the run URL, the overall conclusion, and the verbatim `Issuer:` string
      captured in Task 1 — not a placeholder or an assumed-passing value.
    - diff assertion: `grep -n "SIGN-03" .planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-SIGN03-SMOKE-VERDICT.md` matches.
    - behavior: if Task 1's run was NOT a correctly-chained success, this file records that failure
      verbatim rather than a false PASS.
  </acceptance_criteria>
  <done>
    `101-SIGN03-SMOKE-VERDICT.md` exists, records the actual run outcome (run URL, conclusion, verbatim
    issuer string) and cross-references SIGN-03 — closing the phase's terminal acceptance gate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions `windows-latest` runner → live Azure Trusted Signing service | The actual live-signing round trip this gate exists to prove |
| `gh` CLI (Claude's execution context) → GitHub Actions API | Read-only log/status retrieval plus a `workflow_dispatch` trigger; no secrets are read or handled directly by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-10 | Spoofing | Smoke verify issuer check | mitigate | Assert the issuer regex explicitly EXCLUDES `PublicTrustTest`/`Enterprise ID Verified Policy` naming AND requires `Microsoft ID Verified CS` + `EOC`/`AOC CA` — a green `conclusion == success` alone is never treated as sufficient proof of the correct root |
| T-101-11 | Tampering | Smoke verdict recording | mitigate | The verdict file records the actual run URL and raw issuer string (auditable, re-checkable), not a manually-typed claim; a failing Task 1 result is recorded as a failure, never silently upgraded to a PASS |

</threat_model>

<verification>
`gh run view &lt;run-id&gt; --json conclusion --jq .conclusion` returns `success`, and the logged
`Issuer:` line matches the public-root pattern while excluding both rejected patterns.
`101-SIGN03-SMOKE-VERDICT.md` exists and reflects the actual (not assumed) outcome.
</verification>

<success_criteria>
The "Trusted Signing Smoke Test" workflow runs GREEN on `windows-latest` with an issuer chaining to
the public `Microsoft ID Verified CS EOC/AOC CA` root, and the verdict is recorded in
`101-SIGN03-SMOKE-VERDICT.md` — satisfying SIGN-03 and clearing the way for Phase 104's release cut.
</success_criteria>

<output>
Create `.planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-04-SUMMARY.md` when done
</output>
