---
phase: 101
plan: 03
type: execute
wave: 3
depends_on: ["101-02"]
files_modified:
  - .planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-SIGN01-FINDING.md
autonomous: false
requirements: [SIGN-01]

must_haves:
  truths:
    - "The Azure Trusted Signing certificate profile's type is confirmed via az CLI (or portal) and documented — not guessed from issuer-string pattern-matching alone"
    - "If the profile was PublicTrustTest, a PublicTrust profile now exists and TRUSTED_SIGNING_PROFILE + the FIC subject repo:OscarMackJr/nono:environment:Development are corrected in the GitHub Development environment"
  artifacts:
    - path: ".planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-SIGN01-FINDING.md"
      provides: "Documented profileType + issuer chain finding, cross-referencing SIGN-01"
  key_links:
    - from: "101-SIGN01-FINDING.md"
      to: ".planning/REQUIREMENTS.md SIGN-01"
      via: "cross-reference"
      pattern: "SIGN-01"
---

<objective>
Operator confirms (and fixes if needed) the Azure Trusted Signing certificate profile type — the
suspected root cause of the observed `Status: UnknownError` / `CN=Microsoft Enterprise ID Verified
Policy AOC CA 01` issuer on the first live smoke run — per D-05's locked sequencing (hardening ships
first and autonomously; this operator step is a documented checkpoint that must pass before SIGN-03
smoke can go green).

Purpose: SIGN-01 requires the root cause to be documented, not guessed. This is an Azure
subscription-level control-plane action outside any CI-automatable surface — the operator's own `az`
session, not Claude's.

Output: `.planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-SIGN01-FINDING.md`
recording the confirmed profile type, the observed issuer chain, and (if a fix was applied) the new
profile name + corrected `TRUSTED_SIGNING_PROFILE` value.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-CONTEXT.md
@.planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-RESEARCH.md
@.planning/quick/260630-trusted-signing-golive/AZURE-TRUSTED-SIGNING-GOLIVE-COOKBOOK.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Confirm/fix the Azure Trusted Signing certificate profile type</name>
  <read_first>
    .planning/quick/260630-trusted-signing-golive/AZURE-TRUSTED-SIGNING-GOLIVE-COOKBOOK.md section 2
    (lines 53-83, profile creation) and section 3b (lines 100-116, FIC subject correction) — the
    exact `az trustedsigning certificate-profile create --profile-type PublicTrust` and
    `az ad app federated-credential create` command shapes, RG/account/profile naming
    (`rg-nono-signing` / `nono-trusted-signing` / `nono-public` in the cookbook's example — confirm
    actual live values before running).
    101-RESEARCH.md "Standard Stack" (`az trustedsigning certificate-profile show`) and Pitfall 6
    (the `trustedsigning` extension is Preview — tolerate an unexpected/missing `profileType` key).
    Auto-memory `azure_trusted_signing_golive.md` — the first smoke run's observed evidence:
    `Status: UnknownError`, `Issuer: CN=Microsoft Enterprise ID Verified Policy AOC CA 01`.
  </read_first>
  <files></files>
  <action>
    Operator runs `az trustedsigning certificate-profile show ... --query profileType -o tsv` from
    their own authenticated Azure session (outside Claude's execution context) against the live
    `TRUSTED_SIGNING_PROFILE` account/profile. If the result is `PublicTrustTest`, the operator creates
    a new `PublicTrust` profile per the go-live cookbook §2, updates the `TRUSTED_SIGNING_PROFILE`
    GitHub Environment variable, and confirms the federated-credential subject is exactly
    `repo:OscarMackJr/nono:environment:Development` per cookbook §3b. Full exact command sequence and
    decision tree is in `<how-to-verify>` below.
  </action>
  <what-built>
    SIGN-02 hardening (Plans 01-02) has already landed autonomously — the shared verify helper with
    dual-engine AND-gate, bounded retry, and full chain diagnostics is live at all 3 call sites. That
    hardening is independent of this step; it exists so that once the profile is fixed (or confirmed
    already correct), the smoke workflow's failure-path diagnostics will make any remaining issue
    immediately legible from the CI log alone.
  </what-built>
  <how-to-verify>
    1. From your own `az`-logged-in session (NOT Claude's sandbox — this needs your Azure subscription
       access), run:
       `az trustedsigning certificate-profile show -g <your-resource-group> --account-name <your-account> -n <your-profile> --query profileType -o tsv`
       (fill in the actual RG/account/profile — the cookbook's example values are `rg-nono-signing`,
       `nono-trusted-signing`, `nono-public`; confirm against your live `TRUSTED_SIGNING_PROFILE`
       GitHub variable in the `Development` environment).
    2. If the output is `PublicTrust` — the profile is already correct. The observed `UnknownError` is
       NOT a profile-type issue; note this so a subsequent smoke re-run's diagnostics (Plan 01's D-04
       chain dump) can be used to investigate a chain-build/revocation-transient cause instead.
    3. If the output is `PublicTrustTest` (the suspected root cause) — create a new `PublicTrust`
       profile per the cookbook's section 2 command (requires an Approved identity validation), record
       the new profile name, update the `TRUSTED_SIGNING_PROFILE` GitHub Environment variable
       (`Development` environment) to the new profile name, and confirm the federated-credential
       subject is exactly `repo:OscarMackJr/nono:environment:Development` per cookbook section 3b
       (delete any stale `ref:refs/...` or `oscarmackjr-twg` subject credential).
    4. Report back: the `profileType` value observed, whether a fix was applied (and if so, the new
       profile name), and the issuer string from any prior smoke-run log you have on hand.
  </how-to-verify>
  <verify>
    <human-check>Operator reports the confirmed profileType and, if applicable, the fix actions taken (new profile name, TRUSTED_SIGNING_PROFILE update, FIC subject correction).</human-check>
  </verify>
  <resume-signal>Reply with the confirmed profileType (and, if you fixed it, the new profile name + confirmation that TRUSTED_SIGNING_PROFILE and the FIC subject are updated) — e.g. "confirmed: PublicTrust" or "fixed: created nono-public-v2 (PublicTrust), updated TRUSTED_SIGNING_PROFILE and FIC subject"</resume-signal>
  <done>Operator has reported a profileType value (and any fix applied) sufficient for Task 2 to document the finding without guessing.</done>
</task>

<task type="auto">
  <name>Task 2: Document the SIGN-01 finding</name>
  <files>.planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-SIGN01-FINDING.md</files>
  <read_first>
    Task 1's reported operator response (profileType + fix status).
    .planning/REQUIREMENTS.md SIGN-01 (the locked acceptance wording this finding must satisfy).
  </read_first>
  <action>
    Create `101-SIGN01-FINDING.md` recording: the date this was confirmed; the exact `profileType`
    value the operator reported; whether it was already `PublicTrust` or required a fix; if fixed, the
    new profile name and confirmation that `TRUSTED_SIGNING_PROFILE` + the FIC subject
    (`repo:OscarMackJr/nono:environment:Development`) were updated; and a cross-reference to
    `.planning/REQUIREMENTS.md` SIGN-01 and the go-live cookbook. This is the "documented, not guessed"
    artifact SIGN-01 requires — do not infer or guess any field; only record what the operator actually
    reported in Task 1.
  </action>
  <verify>
    <automated>test -f .planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-SIGN01-FINDING.md && grep -q "SIGN-01" .planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-SIGN01-FINDING.md</automated>
  </verify>
  <acceptance_criteria>
    - source: the file contains a `profileType:` (or equivalent labeled) field with the value reported
      in Task 1, not a placeholder or an inferred guess.
    - diff assertion: `grep -n "SIGN-01" .planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-SIGN01-FINDING.md` matches.
  </acceptance_criteria>
  <done>
    `101-SIGN01-FINDING.md` exists and records the operator-reported profile type, fix status, and
    cross-references SIGN-01 — the root cause is documented, not guessed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator's `az` CLI session → Azure Trusted Signing account (control plane) | Outside CI; requires the operator's own Azure subscription access, not Claude's |
| GitHub Environment `Development` variables/FIC → Azure AD app registration | Config surface the operator corrects if the profile type was wrong |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-08 | Spoofing | Federated-credential subject | mitigate | Operator confirms/corrects the FIC subject to the exact `repo:OscarMackJr/nono:environment:Development` string; any mismatch fails OIDC login (`AADSTS700213`), which is itself fail-closed |
| T-101-09 | Tampering | Certificate profile type | mitigate | Operator confirms `profileType` via live `az` query rather than guessing from issuer-string pattern-matching alone; the finding is recorded verbatim in `101-SIGN01-FINDING.md`, not inferred |

</threat_model>

<verification>
`101-SIGN01-FINDING.md` exists, references SIGN-01, and records an operator-reported (not guessed)
`profileType` value plus fix status.
</verification>

<success_criteria>
The Azure Trusted Signing certificate profile type is confirmed (and corrected if it was
`PublicTrustTest`) by the operator, and the finding is documented in a phase artifact — satisfying
SIGN-01's "documented, not guessed" requirement and unblocking Plan 04's smoke-gate dispatch.
</success_criteria>

<output>
Create `.planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-03-SUMMARY.md` when done
</output>
