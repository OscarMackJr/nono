---
phase: 101
plan: 02
type: execute
wave: 2
depends_on: ["101-01"]
files_modified:
  - .github/workflows/release.yml
  - .github/workflows/trusted-signing-smoke.yml
autonomous: true
requirements: [SIGN-02]

must_haves:
  truths:
    - "release.yml Site 1 (loose .exe/broker/MSI-wrapper loop) calls Assert-TrustedSignature -Mode Strict for every artifact in the loop"
    - "release.yml Site 2's MSI-payload .exe files call -Mode Strict; its .sys WHQL carve-out calls -Mode Informational — both modes passed explicitly by the call site, never inferred"
    - "trusted-signing-smoke.yml's verify step calls Assert-TrustedSignature -Mode Strict"
    - "No inline Get-AuthenticodeSignature / Status-ne-Valid gating logic remains duplicated at any of the 3 sites — the check lives only inside scripts/verify-authenticode.ps1"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Both verify sites (Site 1 ~L259, Site 2 ~L281) dot-source and call the shared helper"
    - path: ".github/workflows/trusted-signing-smoke.yml"
      provides: "Verify step (~L62) dot-sources and calls the shared helper"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "scripts/verify-authenticode.ps1"
      via: "dot-source in a `run:` block"
      pattern: "\\. \\(Join-Path \\$PWD .scripts.verify-authenticode\\.ps1.\\)"
    - from: ".github/workflows/trusted-signing-smoke.yml"
      to: "scripts/verify-authenticode.ps1"
      via: "dot-source in a `run:` block"
      pattern: "\\. \\(Join-Path \\$PWD .scripts.verify-authenticode\\.ps1.\\)"
---

<objective>
Wire the Plan 01 `Assert-TrustedSignature` helper into all three fail-closed verify sites — replacing
duplicated inline `Get-AuthenticodeSignature`/`if`/`Write-Error`/`exit 1` logic with a dot-sourced call
— while proving (via grep/diff acceptance criteria) that the original fail-closed condition and the
`.sys` WHQL informational carve-out are both preserved exactly, not weakened.

Purpose: SIGN-02 requires this collapsing of duplicated logic into one shared helper; success criterion
3 requires diff review to confirm the fail-closed contract is unweakened.

Output: `release.yml` (2 sites) and `trusted-signing-smoke.yml` (1 site), all now calling
`Assert-TrustedSignature` with an explicit `-Mode` argument.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-CONTEXT.md
@.planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-PATTERNS.md
@.planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-01-SUMMARY.md
@scripts/verify-authenticode.ps1
@scripts/verify-dark.ps1
</context>

<interfaces>
<!-- From Plan 01's scripts/verify-authenticode.ps1 (already landed) -->

`Assert-TrustedSignature -Path <string> -Mode Strict|Informational` — the only function these call
sites need. `-Mode` is mandatory with no default (ValidateSet, no fallback) — every call site below
must pass it explicitly. Dot-source pattern for a `shell: pwsh` step running with `$PWD` = repo root
(GitHub Actions `runs-on: windows-latest` convention already used throughout these files):
`. (Join-Path $PWD "scripts\verify-authenticode.ps1")`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire release.yml Site 1 and Site 2</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    .github/workflows/release.yml lines 259-274 (Site 1, "Verify Authenticode signatures (Windows)")
    — the current inline `foreach` loop over `@($binary, $broker, $machineMsi, $userMsi)` with
    `Get-AuthenticodeSignature`/`if ($sig.Status -ne "Valid")`/`exit 1`.
    .github/workflows/release.yml lines 281-321 (Site 2, "Verify MSI payload signatures (Windows)")
    — the admin-extract `msiexec /a` block (lines 285-297, UNCHANGED — do not touch), the `.exe`
    payload `foreach` (lines 304-316) with its inline GAS/if/Write-Error/exit1, the `.sys` WHQL
    carve-out comment (lines 298-303, PRESERVE VERBATIM) and its informational `foreach`
    (lines 317-320).
    scripts/verify-dark.ps1 lines 180-181, 278-280 (`. $gateFile`) — the in-repo dot-sourcing
    mechanics precedent this refactor follows.
    scripts/verify-authenticode.ps1 (Plan 01, already landed) — `Assert-TrustedSignature` signature.
  </read_first>
  <action>
    In Site 1 (~line 259-274): as the first line of the `run:` block add
    `. (Join-Path $PWD "scripts\verify-authenticode.ps1")`, keep the existing `$binary`/`$broker`/
    `$machineMsi`/`$userMsi` variable assignments and the `foreach ($artifact in @($binary, $broker,
    $machineMsi, $userMsi))` loop header unchanged, and replace the loop body's
    `$sig = Get-AuthenticodeSignature -FilePath $artifact` / `if ($sig.Status -ne "Valid") {
    Write-Error ...; exit 1 }` / `Write-Host "Authenticode OK: $artifact"` block with a single call:
    `Assert-TrustedSignature -Path $artifact -Mode Strict` (let a non-pass throw naturally — the step
    already runs under pwsh's default terminating-error behavior, so an uncaught `throw` fails the
    step). In Site 2 (~line 281-321): dot-source the helper as the first line of this `run:` block
    too; leave the `msiexec /a` admin-extract block (lines 285-297) and the "No payload .exe binaries
    found" guard clause completely unchanged; replace the `.exe` `foreach ($f in $exePayload)` body's
    inline GAS/if/Write-Error/exit1 with `Assert-TrustedSignature -Path $f.FullName -Mode Strict`;
    replace the `.sys` `foreach ($d in ...)` body's inline GAS/Write-Host with
    `Assert-TrustedSignature -Path $d.FullName -Mode Informational` — keep the WHQL carve-out comment
    block (lines 298-303) verbatim immediately above the `.exe` foreach, unmoved. Do not touch anything
    outside these two `run:` blocks (the zip-creation step at line 325+ is out of scope).
  </action>
  <verify>
    <automated>python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/release.yml', encoding='utf-8')); print('yaml ok')"</automated>
  </verify>
  <acceptance_criteria>
    - diff assertion: `grep -n "Assert-TrustedSignature -Path \$artifact -Mode Strict" .github/workflows/release.yml` matches (Site 1).
    - diff assertion: `grep -n "Assert-TrustedSignature -Path \$f.FullName -Mode Strict" .github/workflows/release.yml` matches (Site 2, `.exe` payloads).
    - diff assertion: `grep -n "Assert-TrustedSignature -Path \$d.FullName -Mode Informational" .github/workflows/release.yml` matches (Site 2, `.sys` carve-out).
    - diff assertion (carve-out preserved): `grep -n "SEPARATE signing regime" .github/workflows/release.yml` still matches.
    - diff assertion (no duplicated inline gate left behind): `grep -c "sig.Status -ne" .github/workflows/release.yml` returns `0` — the fail-closed check now lives only inside `scripts/verify-authenticode.ps1`, not duplicated in the YAML.
    - diff assertion (admin-extract untouched): `grep -n "msiexec" .github/workflows/release.yml` still matches with the same `-ArgumentList` shape as before (line 293 region unmoved).
    - test-command: the YAML-load verify command above exits 0 (no broken syntax from the edit).
  </acceptance_criteria>
  <done>
    Both Site 1 and Site 2 in `release.yml` dot-source `scripts/verify-authenticode.ps1` and call
    `Assert-TrustedSignature` with an explicit `-Mode` argument at every call; the `.sys` carve-out
    comment and its `-Mode Informational` call are both present; no inline `Status -ne` check remains
    in the YAML.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire trusted-signing-smoke.yml verify site</name>
  <files>.github/workflows/trusted-signing-smoke.yml</files>
  <read_first>
    .github/workflows/trusted-signing-smoke.yml lines 62-73 ("Verify the embedded signature is OUR
    Trusted Signing cert") — the current inline `$sig = Get-AuthenticodeSignature smoke\nono-smoke.exe`
    / `Write-Host "Status:.../Signer:.../Issuer:..."` / `if ($sig.Status -ne 'Valid') { ...; exit 1 }`
    block; this is the near-duplicate of `release.yml` Site 1 referenced in 101-CONTEXT.md's Reusable
    Assets note.
    scripts/verify-authenticode.ps1 (Plan 01) — `Assert-TrustedSignature` signature.
  </read_first>
  <action>
    As the first line of the `run:` block, add `. (Join-Path $PWD "scripts\verify-authenticode.ps1")`.
    Keep the existing `Write-Host "Status: ..."` / `"Signer: ..."` / `"Issuer: ..."` echo lines
    immediately after the sign step (they are cheap ambient context, not the D-04 full chain dump —
    that stays reserved for the failure path inside the helper itself). Replace the trailing
    `if ($sig.Status -ne 'Valid') { Write-Error ...; exit 1 }` block with a single call:
    `Assert-TrustedSignature -Path "smoke\nono-smoke.exe" -Mode Strict`. Keep the final
    `Write-Host "OK: Trusted Signing smoke test PASSED — the D-02 signing path is live."` line after
    the call (only reached if `Assert-TrustedSignature` did not throw).
  </action>
  <verify>
    <automated>python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/trusted-signing-smoke.yml', encoding='utf-8')); print('yaml ok')"</automated>
  </verify>
  <acceptance_criteria>
    - diff assertion: `grep -n "Assert-TrustedSignature -Path \"smoke\\\\nono-smoke.exe\" -Mode Strict" .github/workflows/trusted-signing-smoke.yml` matches (adjust quoting to whatever the editor produces, but the literal call with an explicit `-Mode Strict` must be present).
    - diff assertion: `grep -c "sig.Status -ne 'Valid'" .github/workflows/trusted-signing-smoke.yml` returns `0` — inline check removed, now inside the helper only.
    - diff assertion: `grep -n "Signer:" .github/workflows/trusted-signing-smoke.yml` and `grep -n "Issuer:" .github/workflows/trusted-signing-smoke.yml` still match — the pre-existing ambient echo lines are preserved.
    - test-command: the YAML-load verify command above exits 0.
  </acceptance_criteria>
  <done>
    `trusted-signing-smoke.yml`'s verify step dot-sources the shared helper and calls
    `Assert-TrustedSignature -Path "smoke\nono-smoke.exe" -Mode Strict`; no inline
    `Status -ne 'Valid'` check remains in this file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow YAML `run:` blocks → shared PowerShell helper | The call-site edit is the boundary where D-03's strict-vs-informational policy decision is made explicitly per file |
| GitHub Actions secrets/vars → `azure/trusted-signing-action@v0` | Unchanged, out of scope for this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-05 | Tampering | `.sys` carve-out call site (release.yml Site 2) | mitigate | `-Mode Informational` is passed explicitly ONLY at the `.sys` foreach call; the helper itself never branches on extension (verified in Plan 01's grep gate: `grep -c '\.sys\b'` inside `verify-authenticode.ps1` returns 0) |
| T-101-06 | Tampering | Site 1 / Site 2 `.exe` / smoke Strict call sites | mitigate | grep-verified `-Mode Strict` argument present at every non-`.sys` call; `-Mode` has no default (ValidateSet, no fallback) so an omitted argument throws at runtime rather than silently picking a mode |
| T-101-07 | Repudiation | YAML diff review | mitigate | Diff assertions (grep for removed inline checks + preserved carve-out comment) are codified as acceptance criteria, proving the inline `Status -ne 'Valid'` checks were removed BECAUSE they now live inside the helper, not because the gate was dropped |

</threat_model>

<verification>
`python -c "import yaml; yaml.safe_load(open(...))"` exits 0 for both edited workflow files. All grep
acceptance criteria above pass — every call site has an explicit `-Mode` argument, the `.sys` carve-out
comment survives, and no inline duplicate of the fail-closed check remains in either file.
</verification>

<success_criteria>
`release.yml` (2 sites) and `trusted-signing-smoke.yml` (1 site) all dot-source and call
`Assert-TrustedSignature` with an explicit `-Mode` argument; the diff-review acceptance criteria prove
the original fail-closed contract and the `.sys` informational carve-out are both unweakened.
</success_criteria>

<output>
Create `.planning/phases/101-verify-gate-hardening-azure-profile-confirmation/101-02-SUMMARY.md` when done
</output>
