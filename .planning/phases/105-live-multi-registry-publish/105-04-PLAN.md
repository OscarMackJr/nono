---
phase: 105
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [scripts/verify-registry-availability.ps1, scripts/verify-post-publish-resolve.ps1]
autonomous: true
requirements: [PUB-02]

must_haves:
  truths:
    - "A single script re-checks live name-availability/scope-ownership for all 5 registry identities plus the npm @oscarmackjr scope immediately before every live-publish checkpoint, closing the name-squatting window between research and execution"
    - "A single script verifies cargo install / pip install / npm i resolve in isolated, self-cleaning temp environments post-publish (SC4), distinguishing a genuine failure from the expected not-yet-live pre-publish state"
    - "Neither script duplicates the crates.io sparse-index logic already authored in Plan 105-01 -- both reuse it"
  artifacts:
    - path: "scripts/verify-registry-availability.ps1"
      provides: "consolidated live re-check of crates.io x3, PyPI, npm main + win32-x64-msvc, and the npm @oscarmackjr scope-ownership state"
    - path: "scripts/verify-post-publish-resolve.ps1"
      provides: "isolated temp-dir cargo install / pip install / npm i checks with guaranteed cleanup (SC4)"
  key_links:
    - from: "scripts/verify-registry-availability.ps1"
      to: "scripts/crates-index-poll.ps1"
      via: "dot-source reuse of Get-CratesSparseIndexPath / Test-CratesVersionPublished"
      pattern: "\\.\\s+.*crates-index-poll\\.ps1"
---

<objective>
Author the two verification-tooling scripts PUB-02 needs but that don't belong to any single
registry's publish leg: a consolidated pre-live-checkpoint availability/scope-ownership re-check
(closing the "a name can be squatted between research and execution" window, per 105-RESEARCH.md's
Security Domain and the Phase 102 precedent it cites), and the SC4 post-publish resolve wrapper
(`cargo install` / `pip install` / `npm i` all succeed in an isolated, self-cleaning temp
environment).

Purpose: 105-VALIDATION.md's Wave 0 Requirements name both scripts explicitly as having "no existing
equivalent in this repo." The availability script must be re-run immediately before EVERY live
checkpoint (per the Security Domain's Spoofing mitigation), not just once during planning research --
this plan authors that reusable mechanism once so Wave 2 can invoke it repeatedly.

Output: scripts/verify-registry-availability.ps1 (new), scripts/verify-post-publish-resolve.ps1 (new).

This plan is fully autonomous and dev-host-verifiable today: both scripts are dry-run/read-only by
construction and can be exercised right now against the genuinely-not-yet-published live state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-live-multi-registry-publish/105-CONTEXT.md
@.planning/phases/105-live-multi-registry-publish/105-RESEARCH.md
@.planning/phases/105-live-multi-registry-publish/105-VALIDATION.md
@scripts/crates-index-poll.ps1
</context>

<interfaces>
From scripts/crates-index-poll.ps1 (Plan 105-01's deliverable, dot-source and reuse rather than
re-implement): functions Get-CratesSparseIndexPath(name) and Test-CratesVersionPublished(name,
version) already implement the correct sparse-index path-shape rules and 404-vs-error handling with
a descriptive User-Agent. Note: this plan is in the same Wave (1) as 105-01 with no file overlap --
if executed before 105-01 lands, dot-source the file by its planned path regardless (both plans are
independent within the wave; the executor should confirm scripts/crates-index-poll.ps1 exists before
dot-sourcing it, and if this plan runs first, implement the two functions locally in this script and
note the intended future consolidation rather than blocking).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/verify-registry-availability.ps1 (pre-every-live-checkpoint squat + scope re-check)</name>
  <files>scripts/verify-registry-availability.ps1</files>
  <read_first>
    105-RESEARCH.md Strategic Question 2 (the exact re-verification commands run this session:
    www.npmjs.com/~oscarmackjr, www.npmjs.com/org/oscarmackjr, registry.npmjs.org/-/v1/search, npm
    whoami) and the Security Domain "Name-squatting between research and live publish" threat pattern
    (re-run immediately before EVERY live-publish checkpoint, not just once). scripts/crates-index-poll.ps1
    (dot-source and reuse Get-CratesSparseIndexPath / Test-CratesVersionPublished).
  </read_first>
  <action>
    Create scripts/verify-registry-availability.ps1. Dot-source scripts/crates-index-poll.ps1 at the
    top (per the interfaces note above) to reuse its sparse-index helper rather than re-implementing
    the path-shape logic. For each of the 3 crates.io names (nono-sandbox, nono-sandbox-proxy,
    nono-sandbox-cli), report both "any version ever published" (a GET against the crate's sparse
    index path returning any content at all, 200 vs 404) and "0.66.1 specifically published" (via
    Test-CratesVersionPublished) as two separate facts -- crates.io names are permanent once claimed
    by anyone, so distinguishing "never claimed" from "claimed by us, resuming" from "claimed by a
    third party" matters; a pre-existing 0.66.1 published under our own prior partial-publish attempt
    is benign/resumable, never a hard failure by itself. For PyPI, query (Invoke-RestMethod)
    https://pypi.org/pypi/nono-sandbox/json and report either "404 -- unclaimed" or the full releases
    key list found. For npm, query https://registry.npmjs.org/@oscarmackjr%2Fnono-ts and
    https://registry.npmjs.org/@oscarmackjr%2Fnono-ts-win32-x64-msvc the same way. For the npm
    @oscarmackjr scope ownership itself (D-04), query www.npmjs.com/~oscarmackjr,
    www.npmjs.com/org/oscarmackjr, and registry.npmjs.org/-/v1/search?text=scope:oscarmackjr,
    reporting UNCLAIMED (all three come back empty/404) or CLAIMED. Every web request in this script
    must set an explicit, descriptive User-Agent header. Print a single consolidated verdict table
    (identity, registry, status) at the end. The script's own exit code must be 0 whenever every
    identity is either genuinely unclaimed (the expected pre-live state) or already-ours-and-resumable
    -- it should only exit non-zero if a name is found published with content that looks like it was
    NOT produced by this project (best-effort: for PyPI/npm, any existing release/version content
    where the reported values don't match this project's expected identity is flagged for the operator
    to manually judge, since automated ownership attribution isn't reliably possible from public,
    unauthenticated endpoints alone).
  </action>
  <verify>
    <automated>pwsh -File scripts/verify-registry-availability.ps1</automated>
  </verify>
  <acceptance_criteria>
    - Running the script today reports all 3 crates.io names as not-yet-published-at-0.66.1, PyPI nono-sandbox as 404/unclaimed, both npm identities (main + win32-x64-msvc) as 404/unclaimed, and the npm @oscarmackjr scope as UNCLAIMED -- matching 105-RESEARCH.md's live-verified state from this session.
    - `grep -c "crates-index-poll.ps1"` in the script is at least 1 (dot-source reuse, not a duplicated Get-CratesSparseIndexPath implementation) -- OR, if executed before Plan 105-01 lands, the script documents the intended future consolidation per the interfaces note.
    - Every Invoke-WebRequest/Invoke-RestMethod call sets an explicit User-Agent header.
    - The script exits 0 on today's genuinely-unclaimed state.
  </acceptance_criteria>
  <done>
    scripts/verify-registry-availability.ps1 exists, runs clean today reporting the expected
    pre-live "everything unclaimed" state across all 5 registry identities plus the npm scope, and
    reuses (not duplicates) the crates-index-poll helper.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author scripts/verify-post-publish-resolve.ps1 (SC4 isolated resolve wrapper)</name>
  <files>scripts/verify-post-publish-resolve.ps1</files>
  <read_first>
    105-RESEARCH.md Architecture Patterns "Post-publish resolve verification (SC4, side-effect-free)"
    block and the Validation Architecture SC4 row (isolated temp env, only meaningful post-live but
    must run clean pre-live too).
  </read_first>
  <action>
    Create scripts/verify-post-publish-resolve.ps1. Implement three independent checks, each wrapped
    in its OWN try/catch/finally (not a single shared $ErrorActionPreference = "Stop" spanning all
    three -- one leg's failure or not-yet-live state must never prevent the other two from running):
    (1) crates.io: create a fresh temp directory under $env:TEMP, run
    `cargo install nono-sandbox-cli --root <tmp>` inside it, classify the result as PASS (binary
    installed, --version runs), the expected NOT_YET_LIVE state (parse cargo's specific
    "no matching package"/"failed to select a version" error text), or a genuine unexpected FAIL (any
    other error) -- remove the temp directory in a finally block regardless of outcome; (2) PyPI:
    create a fresh temp directory, run `python -m venv <tmp>`, then invoke that venv's own pip to
    install nono-sandbox, with the same PASS / expected-NOT_YET_LIVE (a PyPI 404-equivalent
    "No matching distribution" pip error) / genuine-FAIL classification, and the same guaranteed
    cleanup; (3) npm: create a fresh temp directory, run `npm i @oscarmackjr/nono-ts --prefix <tmp>`,
    with the same three-way classification (an npm E404 is the expected NOT_YET_LIVE state) and
    guaranteed cleanup. Print a final 3-row summary table (registry, status, detail). The script's
    overall exit code is 0 if every leg is either PASS or the expected NOT_YET_LIVE state, and
    non-zero only if any leg reports a genuine unexpected FAIL.
  </action>
  <verify>
    <automated>pwsh -File scripts/verify-post-publish-resolve.ps1</automated>
  </verify>
  <acceptance_criteria>
    - Running the script today (nothing is live yet) exits 0 and reports all three legs as the expected NOT_YET_LIVE state -- proves the full isolated-temp-dir-plus-cleanup code path runs cleanly against the real not-yet-published condition, not merely that the code parses.
    - A directory listing of $env:TEMP taken immediately before and immediately after the run shows no new leftover directories attributable to this script (every temp dir it created has been removed by its own finally block).
    - `grep -c "try {"` in the script shows at least 3 occurrences, confirming each of the three registry legs has its own independent try block rather than one shared block spanning all three.
  </acceptance_criteria>
  <done>
    scripts/verify-post-publish-resolve.ps1 exists, runs clean today against the genuinely
    not-yet-published state with full temp-directory cleanup proven, and is ready to re-run for-real
    once Plan 105-05's live publish completes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Research-time name-availability snapshot <-> live-checkpoint-time reality | A name confirmed unclaimed during planning can be squatted before the operator actually publishes |
| Isolated temp-install probes <-> this dev host's filesystem | Each probe must not leak a persistent directory or mutate any tool's global state (e.g. global npm/cargo config) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-04-01 | Spoofing | A third party claims any of the 5 registry identities or the npm scope between research and the operator's actual live-publish checkpoint | mitigate | scripts/verify-registry-availability.ps1 is designed to be re-run immediately before EVERY live checkpoint in Plan 105-05, not just once during this plan's authoring |
| T-105-04-02 | Denial of Service (of this dev host) | Repeated resolve-check runs leaking temp directories across many re-executions | mitigate | every registry leg wraps its temp-directory creation in its own finally-block cleanup, verified by a before/after directory listing diff |
| T-105-04-03 | Repudiation | Misreading a package-not-found error as a genuine tooling failure (or vice versa), producing a false PASS/FAIL verdict | mitigate | each leg explicitly classifies three distinct outcomes (PASS / expected NOT_YET_LIVE / genuine FAIL) rather than a binary success/failure |

</threat_model>

<verification>
Both scripts run clean today against the real, currently-unpublished registry state:
verify-registry-availability.ps1 reports all 5 identities + the npm scope as unclaimed;
verify-post-publish-resolve.ps1 reports all 3 legs as the expected NOT_YET_LIVE state with proven
temp-directory cleanup.
</verification>

<success_criteria>
The verification-tooling layer PUB-02 needs across all registries is fully authored and
dev-host-verifiable today: a reusable pre-checkpoint availability/scope re-check, and a
self-cleaning, three-way-classifying post-publish resolve wrapper (SC4). No live registry action has
occurred.
</success_criteria>

<output>
Create .planning/phases/105-live-multi-registry-publish/105-04-SUMMARY.md when done
</output>
