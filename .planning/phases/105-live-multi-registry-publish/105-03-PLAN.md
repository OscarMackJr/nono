---
phase: 105
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: ["../nono-ts/npm/win32-x64-msvc/package.json", "../nono-ts/npm/win32-x64-msvc/README.md", "../nono-ts/npm/win32-x64-msvc/nono.win32-x64-msvc.node", "scripts/publish-npm-live.ps1"]
autonomous: true
requirements: [PUB-02]

must_haves:
  truths:
    - "The win32-x64-msvc npm platform package exists on disk with a genuinely loadable native addon, closing the reproduced missing-platform-package gap (105-RESEARCH.md Pitfall 1)"
    - "A publish script exists that hard-gates on the win32-x64-msvc platform package's presence before attempting any npm publish, and publishes platform packages before the main package, never the reverse"
    - "The reduced platform scope (Windows only, this phase) is explicitly documented in the script's own output, not merely in planning artifacts, per D-02"
    - "npm publish is never invoked live by this plan -- the default mode is --dry-run only"
  artifacts:
    - path: "../nono-ts/npm/win32-x64-msvc/package.json"
      provides: "the previously-missing 5th platform package manifest, generated by napi artifacts (not hand-authored)"
    - path: "scripts/publish-npm-live.ps1"
      provides: "confirm-gated, platform-first-then-main-last npm publish with a hard pre-publish completeness gate"
  key_links:
    - from: "scripts/publish-npm-live.ps1"
      to: "../nono-ts/npm/win32-x64-msvc/package.json"
      via: "Test-Path hard gate before any npm publish call"
      pattern: "npm/win32-x64-msvc/package\\.json"
    - from: "../nono-ts/npm/win32-x64-msvc/package.json"
      to: "../nono-ts/package.json optionalDependencies"
      via: "matching @oscarmackjr/nono-ts-win32-x64-msvc version pin"
      pattern: "@oscarmackjr/nono-ts-win32-x64-msvc"
---

<objective>
Author the npm leg of the live multi-registry publish machinery (PUB-02 SC3): close the concrete,
already-reproduced missing-platform-package gap (105-RESEARCH.md Pitfall 1 -- `npm/win32-x64-msvc/`
does not exist on disk even though package.json's napi.targets and optionalDependencies already
declare it) by building the host-native Windows addon and generating the platform package via
`napi artifacts`, then author a publish script with a hard pre-publish completeness gate and strict
platform-packages-first, main-package-last ordering (the Anti-Patterns entry: a broken
optionalDependency published live is a DoS for end users).

Purpose: per D-02, this phase's npm publish scope is what this Windows dev host can build --
win32-x64-msvc. The pre-existing darwin/linux platform folders (4 of them, package.json+README
stubs with no built .node file, confirmed this session) are NOT built or published in this phase;
that reduced coverage is an explicit, documented known limitation (deferred to a future phase that
forks `nono-ts` to a real `OscarMackJr/nono-ts` repo with its own CI), not a silent scope cut.

Output: ../nono-ts/npm/win32-x64-msvc/ (new, in the sibling repo, committed there with DCO sign-off),
scripts/publish-npm-live.ps1 (new, in this repo).

This plan is fully autonomous and dev-host-verifiable. Its default (unconfirmed) mode performs a
real hard-gate check + dry-run publish for both the platform and main packages, with zero live
upload -- matching D-01's operator-checkpoint sequencing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-live-multi-registry-publish/105-CONTEXT.md
@.planning/phases/105-live-multi-registry-publish/105-RESEARCH.md
@.planning/phases/105-live-multi-registry-publish/105-VALIDATION.md
@../nono-ts/package.json
@scripts/release-dry-run.ps1
</context>

<interfaces>
From ../nono-ts/package.json (already correct, unchanged by this plan -- both napi.targets and
optionalDependencies already list all 5 platforms including x86_64-pc-windows-msvc /
@oscarmackjr/nono-ts-win32-x64-msvc at version 0.66.1; the gap is purely that the
npm/win32-x64-msvc/ directory does not yet exist on disk):
napi.targets: x86_64-apple-darwin, aarch64-apple-darwin, x86_64-unknown-linux-gnu,
aarch64-unknown-linux-gnu, x86_64-pc-windows-msvc
optionalDependencies: @oscarmackjr/nono-ts-darwin-x64, @oscarmackjr/nono-ts-darwin-arm64,
@oscarmackjr/nono-ts-linux-x64-gnu, @oscarmackjr/nono-ts-linux-arm64-gnu,
@oscarmackjr/nono-ts-win32-x64-msvc (all pinned "0.66.1")

From ../nono-ts/npm/darwin-x64/package.json (the shape reference the new win32-x64-msvc
package.json must match, auto-generated by napi artifacts -- do not hand-author):
name, version, cpu, main (points at the platform-suffixed .node file), files, description,
keywords, author, license, engines, repository (with a directory field pointing at
"npm/<platform>"), os
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build the win32-x64-msvc platform package, smoke-test it, commit in the nono-ts repo</name>
  <files>../nono-ts/npm/win32-x64-msvc/package.json, ../nono-ts/npm/win32-x64-msvc/README.md, ../nono-ts/npm/win32-x64-msvc/nono.win32-x64-msvc.node</files>
  <read_first>
    105-RESEARCH.md Pitfall 1 (the exact reproduced gap this session), Don't Hand-Roll table (napi
    artifacts auto-generates the folder from napi.targets -- do not hand-author the package.json),
    Pitfall 5 (never trust a clean build exit code alone for a newly-produced native artifact --
    smoke-test by actually loading it). ../nono-ts/package.json (napi.targets + optionalDependencies,
    already correct). ../nono-ts/npm/darwin-x64/package.json (the shape reference).
  </read_first>
  <action>
    In ../nono-ts, run `npm run build` (== `napi build --platform --release`) to produce the
    host-native x86_64-pc-windows-msvc addon on this Windows dev host -- guaranteed-buildable per
    105-RESEARCH.md (no cross-compilation involved, this is the host's own native target). Then run
    `napi artifacts --npm-dir npm` to auto-generate npm/win32-x64-msvc/ (package.json, README.md, and
    the .node binary) from the package.json napi.targets declaration, matching the existing 4
    platform folders' shape exactly (repository.directory = "npm/win32-x64-msvc", os: ["win32"],
    cpu: ["x64"]) -- do not hand-write this JSON. Smoke-test that the produced .node file is a
    genuinely loadable native addon, not just a clean build exit code: write and run a short one-off
    `node -e` invocation that requires the produced .node file directly and calls a trivial exported
    zero-argument function from it (e.g. a support/availability check the crate already exposes),
    confirming it returns a real value rather than throwing. Then, from within the ../nono-ts repo
    (a separate git repository from this one), stage and commit exactly the new
    npm/win32-x64-msvc/ directory with a DCO sign-off trailer, mirroring the Phase 102-03/102-04
    precedent of landing sibling-repo changes as their own commit in that repo's own history (not in
    this repo).
  </action>
  <verify>
    <automated>node -e "const a=require('../nono-ts/npm/win32-x64-msvc/package.json'); console.log(a.os, a.cpu, a.main)"</automated>
  </verify>
  <acceptance_criteria>
    - `ls ../nono-ts/npm | wc -l` (or an equivalent directory count) equals 5 -- directory-structure parity restored across all 5 declared optionalDependencies/napi.targets entries, closing the exact Pitfall 1 warning sign ("entry count (5) not matching folder count (4)").
    - ../nono-ts/npm/win32-x64-msvc/package.json exists with "os": ["win32"] and "cpu": ["x64"], and its "main" field names a .node file present in the same directory.
    - The node smoke-test require of the produced .node file succeeds and prints a real value (not a thrown error) -- proves the artifact is genuinely loadable, not merely present on disk.
    - `git -C ../nono-ts log -1 --format=%B` contains a "Signed-off-by:" trailer (DCO) and `git -C ../nono-ts status --short` is clean after the commit.
  </acceptance_criteria>
  <done>
    ../nono-ts/npm/win32-x64-msvc/ exists with a genuinely loadable .node binary, generated via napi
    artifacts (not hand-authored), committed with DCO sign-off in the nono-ts repo; the platform-folder
    count now matches all 5 declared platforms.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author scripts/publish-npm-live.ps1 (hard completeness gate, platform-first ordering, documented scope)</name>
  <files>scripts/publish-npm-live.ps1</files>
  <read_first>
    105-CONTEXT.md D-02 (locally-buildable-only scope, documented known limitation). 105-RESEARCH.md
    Anti-Patterns "Publishing the main npm package before its platform subpackages" and the npm
    platform-completeness Code Example (adapted here to the scoped win32-x64-msvc-only publish set,
    not the full 5-platform set -- the other 4 remain unbuilt stubs by design this phase).
  </read_first>
  <action>
    Create scripts/publish-npm-live.ps1 with a param block: -Confirm string default empty (must
    equal exactly "PUBLISH LIVE" to perform any live publish), -NonoTsDir string defaulting to the
    ../nono-ts relative resolution. Hard pre-publish gate, runs unconditionally regardless of
    -Confirm: assert that ../nono-ts/npm/win32-x64-msvc/package.json exists AND at least one
    npm/win32-x64-msvc/*.node file exists alongside it; if either check fails, throw immediately and
    refuse to proceed to any npm publish or dry-run call whatsoever -- this is the scoped,
    precise completeness gate for the ONE platform this phase actually ships, deliberately narrower
    than checking all 5 declared optionalDependencies (those 4 others remain unbuilt by design this
    phase, not a gate failure). Immediately after the hard gate passes, print a prominent, explicit
    "KNOWN LIMITATION" notice: the darwin-x64, darwin-arm64, linux-x64-gnu, and linux-arm64-gnu
    platform packages are NOT built or published by this phase (D-02 -- no fork-owned nono-ts GitHub
    repository/CI exists yet); `npm install` on those platforms will silently skip the unmet optional
    dependency and the native binding will not load there until a future phase forks nono-ts and
    restores multi-platform CI. Perform a pre-publish squat/idempotency informational re-check: query
    (Invoke-RestMethod) https://registry.npmjs.org/@oscarmackjr%2Fnono-ts-win32-x64-msvc and
    https://registry.npmjs.org/@oscarmackjr%2Fnono-ts, printing each one's current existence status
    (a 404 means not yet published; npm publish is itself safely idempotent per-version, so a
    pre-existing match is informational, not a hard stop). If -Confirm does not exactly equal
    "PUBLISH LIVE": run `npm publish --dry-run` first inside npm/win32-x64-msvc, THEN inside the
    repo root (../nono-ts) -- in that literal order -- and exit 0 with zero live network mutation.
    If -Confirm exactly equals "PUBLISH LIVE": publish the platform package FIRST (change into
    npm/win32-x64-msvc, run `npm publish --access public`, return to the prior directory), confirm it
    resolves live via a short bounded poll of the registry URL above, and ONLY THEN publish the main
    package (change into the repo root, run `npm publish --access public`) -- never the reverse
    order. This script must never read, construct, or reference an npm auth token or `.npmrc` entry
    directly; npm's own CLI authentication (already configured by the operator per D-04) is assumed
    present and is never touched, duplicated, or echoed by this script.
  </action>
  <verify>
    <automated>pwsh -File scripts/publish-npm-live.ps1</automated>
  </verify>
  <acceptance_criteria>
    - Running with no -Confirm exits 0, performs the hard completeness gate, prints the KNOWN LIMITATION notice, and performs exactly two `npm publish --dry-run` invocations with zero live network mutation -- `grep -c "npm publish --dry-run"` returns 2, and the win32-x64-msvc-directory invocation appears strictly before the repo-root invocation (byte-offset ordering in the file).
    - Temporarily renaming ../nono-ts/npm/win32-x64-msvc/package.json and re-running the script (any mode) causes it to throw before reaching any npm publish/dry-run call -- proves the hard gate is unconditional and unskippable; restore the file afterward.
    - `grep -ci "KNOWN LIMITATION" scripts/publish-npm-live.ps1` is at least 1.
    - `grep -c "npm publish --access public"` (the live-mode calls) shows exactly 2 occurrences, with the win32-x64-msvc block appearing strictly before the main-package block in the file.
    - `grep -c "npm login\|_authToken\|NPM_TOKEN"` in the script returns 0 -- the script never handles an npm credential directly.
  </acceptance_criteria>
  <done>
    scripts/publish-npm-live.ps1 exists; the hard completeness gate correctly blocks when
    win32-x64-msvc is absent; default (dry-run) mode succeeds against the current tree with correct
    platform-first-then-main ordering; the D-02 known-limitation is printed by the script itself, not
    only documented in planning artifacts.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator's local machine <-> npm registry (no fork-owned CI exists for the sibling repo) | Both the build and the publish run from this Windows dev host under the operator's own npm auth |
| Published main package's optionalDependencies <-> end-user install | A broken/missing platform optionalDependency is a soft npm-install failure but a hard runtime DoS for that platform's users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-03-01 | Denial of Service | Publishing the main package before its win32-x64-msvc platform package succeeds, leaving a live but broken optionalDependency reference | mitigate | strict platform-package-first, main-package-last ordering enforced in scripts/publish-npm-live.ps1; grep-verified ordering |
| T-105-03-02 | Tampering | Publishing a platform package whose .node file was never proven to actually load (a clean build exit code alone is not proof) | mitigate | Task 1's node smoke-test requires the built .node file and calls a real export before it is ever committed or eligible for publish |
| T-105-03-03 | Spoofing | A third party claims `@oscarmackjr/nono-ts` or the win32-x64-msvc platform name between research and the live checkpoint | mitigate | script prints the current registry.npmjs.org existence status for both names immediately before any publish action |
| T-105-03-04 | Elevation of scope | Attempting to publish an unbuilt platform (darwin/linux) that was never validated this phase | mitigate | hard gate checks specifically for win32-x64-msvc; the script contains no code path that publishes any of the other 4 platform directories |

</threat_model>

<verification>
`npm/win32-x64-msvc/` exists with a smoke-tested, genuinely loadable .node addon, committed with DCO
sign-off in the nono-ts repo; `pwsh -File scripts/publish-npm-live.ps1` (no -Confirm) passes the hard
completeness gate and performs a correctly-ordered dry-run publish for both the platform and main
packages with zero live network mutation.
</verification>

<success_criteria>
The npm leg of PUB-02 (SC3) has fully-authored, dev-host-verifiable machinery: the previously-missing
win32-x64-msvc platform package now exists and loads correctly; the publish script hard-gates on its
presence, publishes platform-before-main, and explicitly documents the D-02 reduced-coverage known
limitation. No live npm action has occurred.
</success_criteria>

<output>
Create .planning/phases/105-live-multi-registry-publish/105-03-SUMMARY.md when done
</output>
