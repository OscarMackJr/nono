---
phase: 105
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [scripts/publish-pypi-live.ps1]
autonomous: true
requirements: [PUB-02]

must_haves:
  truths:
    - "A PyPI publish script exists that builds nono-sandbox wheels + sdist, checks them with twine, and by default performs zero live upload -- it only uploads when passed an exact confirmation phrase"
    - "Post-publish coverage is verified against the real PyPI JSON API (which filenames actually uploaded for 0.66.1), not merely a zero exit code from twine"
    - "No PyPI credential ever appears as a command-line argument or in a printed line -- twine consumes it exclusively from its own environment-variable convention"
    - "twine is installed on this host so the live upload leg is not blocked by a missing toolchain"
  artifacts:
    - path: "scripts/publish-pypi-live.ps1"
      provides: "confirm-gated maturin build + sdist + twine check + (if confirmed) twine upload --skip-existing + post-publish coverage verification"
  key_links:
    - from: "scripts/publish-pypi-live.ps1"
      to: "https://pypi.org/pypi/nono-sandbox/json"
      via: "Invoke-RestMethod, parsing the releases.<version> array for filenames"
      pattern: "pypi\\.org/pypi/nono-sandbox/json"
---

<objective>
Author the PyPI leg of the live multi-registry publish machinery (PUB-02 SC2): a script that builds
nono-sandbox (../nono-py) wheels + sdist via maturin, twine-checks them, and -- only when explicitly
confirmed -- uploads via `twine upload --skip-existing`, then verifies actual post-publish wheel
coverage against the PyPI JSON API rather than trusting exit code alone (105-RESEARCH.md's explicit
SC2 wording).

Purpose: per the Architectural Responsibility Map, PyPI build+publish has no fork-owned CI surface
(../nono-py has no `OscarMackJr/nono-py` GitHub repo -- D-02/Pitfall 2) so this leg is necessarily a
local-machine script, not a workflow. `twine` is the phase-mandated upload mechanism and is currently
absent on this host (105-RESEARCH.md Environment Availability) -- this plan installs it as a safe,
autonomous, non-live action.

Output: scripts/publish-pypi-live.ps1 (new); twine installed on this host.

This plan is fully autonomous and dev-host-verifiable. Its default (unconfirmed) mode performs a
real maturin build + twine check with zero live upload, matching D-01's operator-checkpoint
sequencing -- the actual upload only ever runs from Wave 2 with an explicit confirmation phrase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-live-multi-registry-publish/105-CONTEXT.md
@.planning/phases/105-live-multi-registry-publish/105-RESEARCH.md
@.planning/phases/105-live-multi-registry-publish/105-VALIDATION.md
@scripts/release-dry-run.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and verify twine (Wave 0 gap)</name>
  <files></files>
  <read_first>
    105-RESEARCH.md Environment Availability table (twine: NOT installed; `pip install twine` --
    safe, autonomous, no live-registry action) and scripts/release-dry-run.ps1 lines ~124-141 (the
    existing exit-code-based twine-detection fix -- do NOT reintroduce the string-match-on-output bug
    that false-positived on Python's own "No module named twine" error text containing the substring
    "twine").
  </read_first>
  <action>
    Run `pip install twine` on this host. Confirm the tool is now resolvable via exit code, not text
    matching: invoke `twine --version` and check its exit code; if the bare `twine` command is not on
    PATH, fall back to checking `python -m twine --version`'s exit code (mirroring
    release-dry-run.ps1's own fallback order and its exit-code-only detection discipline).
  </action>
  <verify>
    <automated>twine --version</automated>
  </verify>
  <acceptance_criteria>
    - `twine --version` (or `python -m twine --version`) exits 0 on this host.
    - `pwsh -File scripts/release-dry-run.ps1` re-run after the install shows `pypi.twine_check` as PASS (not SKIP) -- proves the pre-existing dry-run harness now exercises the real twine check path instead of its toolchain-absent SKIP branch.
  </acceptance_criteria>
  <done>twine is installed and resolvable by exit code; the existing dry-run harness's PyPI leg now runs a real twine check instead of SKIPping.</done>
</task>

<task type="auto">
  <name>Task 2: Author scripts/publish-pypi-live.ps1 (confirm-gated build + upload + coverage verification)</name>
  <files>scripts/publish-pypi-live.ps1</files>
  <read_first>
    105-RESEARCH.md Architecture Patterns "PyPI leg" sequence, the "PyPI post-publish coverage
    verification (not just exit code -- SC2)" Code Example, Pitfall 5 (cross-compile legs unproven --
    this script only builds the host-native Windows wheel + universal sdist, no --zig/--target leg),
    and the Security Domain table (token never logged). scripts/release-dry-run.ps1 lines ~104-174
    (existing maturin build + twine check pattern and its $NoonoPyDir path-resolution convention --
    reuse the same relative-path approach, not a hardcoded absolute path).
  </read_first>
  <action>
    Create scripts/publish-pypi-live.ps1 with a param block: -Confirm string default empty (must
    equal exactly "PUBLISH LIVE" to perform any live upload), -NonoPyDir string defaulting to the same
    ../nono-py relative resolution pattern release-dry-run.ps1 already uses. Always perform, regardless
    of -Confirm: `maturin build --release` followed by `maturin sdist`, both from within -NonoPyDir,
    then locate every produced .whl and .tar.gz under target/wheels and run `twine check` against all
    of them (fail the script if twine check reports any file as invalid). If -Confirm does not exactly
    equal "PUBLISH LIVE", print a clear refusal message ("dry-run only -- pass -Confirm 'PUBLISH LIVE'
    to upload") and exit 0 without ever invoking `twine upload` -- this is the safe default. If
    -Confirm is exactly "PUBLISH LIVE": first print the CURRENT https://pypi.org/pypi/nono-sandbox/json
    response's releases keys (an informational squat/idempotency re-check, printed before any upload
    action, per the Security Domain "re-run the live availability checks immediately before every live
    publish checkpoint" mitigation); then invoke `twine upload --skip-existing` against the built wheel
    and sdist paths, relying exclusively on twine's own TWINE_USERNAME / TWINE_PASSWORD (or
    TWINE_USERNAME=__token__ / TWINE_PASSWORD=<api-token>) environment-variable convention for
    credentials -- the script must never read, print, echo, or embed either variable's value anywhere,
    and must never accept a credential as a script parameter; after the upload command completes,
    poll https://pypi.org/pypi/nono-sandbox/json a small bounded number of times (PyPI indexing has
    brief propagation delay) via Invoke-RestMethod, parse the JSON releases object's "0.66.1" key, and
    assert it exists with at least one file entry containing a filename field -- throw a clear error if
    the key is absent or empty after the bounded retries (this is the literal SC2 "verified post-publish,
    not just exit code" requirement). Print the confirmed filenames on success.
  </action>
  <verify>
    <automated>pwsh -File scripts/publish-pypi-live.ps1</automated>
  </verify>
  <acceptance_criteria>
    - Running `pwsh -File scripts/publish-pypi-live.ps1` with no -Confirm argument builds a wheel + sdist under ../nono-py/target/wheels, runs twine check successfully, and exits 0 without ever calling twine upload -- `grep -B8 "twine upload" scripts/publish-pypi-live.ps1` shows the call is textually inside the -Confirm-equals-"PUBLISH LIVE" branch, never reachable otherwise.
    - `grep -c "TWINE_PASSWORD\|TWINE_USERNAME"` in the script shows these referenced only as environment-variable names (documentation/comments or bare `$env:` reads passed opaquely to the twine process), never assigned a literal value.
    - `grep -c "Write-Host.*env:TWINE_PASSWORD\|Write-Output.*env:TWINE_PASSWORD"` returns 0 (the credential is never printed).
    - The script contains the literal URL `pypi.org/pypi/nono-sandbox/json` and parses the JSON `releases` field programmatically (via ConvertFrom-Json / Invoke-RestMethod), not a bare string `-match` against raw response text.
    - A fresh `pwsh -File scripts/release-dry-run.ps1` run after this task still exits 0 with `pypi.twine_check` as PASS.
  </acceptance_criteria>
  <done>
    scripts/publish-pypi-live.ps1 exists; its default (unconfirmed) mode produces a real, twine-checked
    wheel + sdist with zero live upload; its confirmed-mode upload path and post-publish JSON-API
    coverage verification are both present and correctly gated behind the exact confirmation phrase.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator's local machine <-> PyPI (no fork-owned CI exists for this leg) | The upload runs from this Windows dev host under the operator's own twine credentials |
| PyPI credential <-> process environment | TWINE_USERNAME/TWINE_PASSWORD must never leave the environment-variable boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-02-01 | Information Disclosure | TWINE_PASSWORD/API token appearing in a Write-Host line or a command-line argument | mitigate | script only ever reads these as environment variables consumed directly by the twine process; grep-verified no literal-value assignment or print |
| T-105-02-02 | Spoofing | A third party claims `nono-sandbox` on PyPI between research and the live checkpoint | mitigate | script prints the current pypi.org JSON releases state immediately before any upload, surfacing an unexpected pre-existing release before the upload proceeds |
| T-105-02-03 | Tampering | Accidental live upload from a routine dry-run invocation | mitigate | upload path requires -Confirm to exactly equal "PUBLISH LIVE"; default mode performs build+check only and prints a refusal message |
| T-105-02-04 | Repudiation | Trusting a clean twine-upload exit code as proof of complete platform coverage | mitigate | post-publish verification queries the real PyPI JSON API and asserts non-empty filenames for the target version, not merely $LASTEXITCODE |

</threat_model>

<verification>
`pwsh -File scripts/publish-pypi-live.ps1` (no -Confirm) builds and twine-checks a real wheel + sdist
with zero live upload; the confirmed-mode branch's upload + coverage-verification logic is present and
correctly gated; twine is installed and resolvable.
</verification>

<success_criteria>
The PyPI leg of PUB-02 (SC2) has fully-authored, dev-host-verifiable machinery: a safe-by-default
build+check script whose live-upload branch never leaks a credential and verifies real post-publish
coverage via the PyPI JSON API. No live PyPI action has occurred.
</success_criteria>

<output>
Create .planning/phases/105-live-multi-registry-publish/105-02-SUMMARY.md when done
</output>
