---
phase: 105
plan: 05
type: execute
wave: 2
depends_on: ["105-01", "105-02", "105-03", "105-04"]
files_modified: []
autonomous: false
requirements: [PUB-02]

user_setup:
  - service: npm
    why: "First-ever publish under the @oscarmackjr scope requires the scope to exist and this machine to be authenticated (D-04) -- confirmed still unresolved as of 105-RESEARCH.md's live re-check this session"
    env_vars: []
    dashboard_config:
      - task: "Create an npm user OR an npm Organization named exactly 'oscarmackjr' (npm org create oscarmackjr, requiring an existing personal npm login first, or the npmjs.com signup / Add Organization UI)"
        location: "npmjs.com"
      - task: "Authenticate this machine (npm login interactively, or configure //registry.npmjs.org/:_authToken=<token> in .npmrc) -- npm whoami currently returns ENEEDAUTH on this host"
        location: "local machine"
  - service: crates.io
    why: "Live cargo publish (via publish-crates.yml, a GitHub Actions workflow) requires a registry token available to that workflow"
    env_vars:
      - name: CARGO_REGISTRY_TOKEN
        source: "crates.io -> Account Settings -> API Tokens; stored as a GitHub Actions repository secret named CARGO_REGISTRY_TOKEN so publish-crates.yml can consume it"
    dashboard_config: []
  - service: PyPI
    why: "twine upload (via scripts/publish-pypi-live.ps1, run locally) requires PyPI credentials in this shell's environment"
    env_vars:
      - name: TWINE_USERNAME
        source: "set to the literal value __token__ when using an API token"
      - name: TWINE_PASSWORD
        source: "PyPI -> Account Settings -> API tokens"
    dashboard_config: []

must_haves:
  truths:
    - "0.66.1 is live on crates.io (nono-sandbox, nono-sandbox-proxy, nono-sandbox-cli), PyPI (nono-sandbox), and npm (@oscarmackjr/nono-ts + @oscarmackjr/nono-ts-win32-x64-msvc) under the fork-owned identities"
    - "cargo install nono-sandbox-cli, pip install nono-sandbox, and npm i @oscarmackjr/nono-ts all resolve successfully in an isolated post-publish environment (SC4)"
    - "Every live-publish action was preceded by a fresh availability/scope re-check run in this same session, not a stale one from research or Wave 1 authoring"
    - "Phase 104's v0.66.1 tag was confirmed released (Release workflow conclusion=success) before any live publish executed, per D-01's version-consistency precondition"
    - "The crates.io leg published in strict dependency order (nono-sandbox before nono-sandbox-proxy before nono-sandbox-cli), never out of order"
  artifacts: []
  key_links:
    - from: "operator workflow_dispatch trigger"
      to: ".github/workflows/publish-crates.yml"
      via: "gh workflow run publish-crates.yml -f tag=v0.66.1 -f confirm='PUBLISH LIVE'"
      pattern: "publish-crates\\.yml"
    - from: "operator local invocation"
      to: "scripts/publish-pypi-live.ps1 -Confirm 'PUBLISH LIVE'"
      via: "PowerShell invocation from this repo's scripts/ directory"
      pattern: "publish-pypi-live\\.ps1"
    - from: "operator local invocation"
      to: "scripts/publish-npm-live.ps1 -Confirm 'PUBLISH LIVE'"
      via: "PowerShell invocation from this repo's scripts/ directory"
      pattern: "publish-npm-live\\.ps1"
---

<objective>
Execute the operator-in-loop portion of Phase 105's goal (PUB-02): confirm Phase 104's `v0.66.1` tag
is genuinely released (D-01's version-consistency precondition) and every registry identity is still
unclaimed (a fresh re-check, not a stale one), then trigger the three live publishes -- crates.io via
the `publish-crates.yml` workflow_dispatch, PyPI via `scripts/publish-pypi-live.ps1 -Confirm`, npm via
`scripts/publish-npm-live.ps1 -Confirm` -- and finally confirm all three published packages resolve
in an isolated post-publish environment (SC4).

This is explicitly and irreducibly operator-gated: every action in this plan is outward-facing,
credentialed, and effectively irreversible (a published registry version can never be un-published,
only yanked, which still burns the version number). Wave 1 (Plans 105-01 through 105-04) already
authored and dev-host-verified every piece of machinery this plan invokes -- no new code is written
here.

Purpose: this is the actual go-live moment for PUB-02 -- `0.66.1` becomes installable from all three
registries under the fork-owned `nono-sandbox` / `@oscarmackjr/nono-ts` identities for the first
time.

Output: no new files (operator-action plan). Produces the live evidence (workflow run URL, twine
upload output + PyPI JSON coverage, npm publish output, and the SC4 resolve-script output) that
PUB-02's four success criteria hold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-live-multi-registry-publish/105-CONTEXT.md
@.planning/phases/105-live-multi-registry-publish/105-RESEARCH.md
@.planning/phases/105-live-multi-registry-publish/105-VALIDATION.md
@.planning/phases/105-live-multi-registry-publish/105-01-SUMMARY.md
@.planning/phases/105-live-multi-registry-publish/105-02-SUMMARY.md
@.planning/phases/105-live-multi-registry-publish/105-03-SUMMARY.md
@.planning/phases/105-live-multi-registry-publish/105-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Pre-publish gate -- confirm Phase 104's tag, fresh availability re-check, credentials/scope checklist</name>
  <files></files>
  <read_first>
    105-CONTEXT.md D-01 (version-consistency precondition, not a build dependency) and D-04 (npm
    scope creation is a hard operator precondition). scripts/verify-registry-availability.ps1
    (Plan 105-04's deliverable -- this task invokes it fresh, no new code). ROADMAP.md Phase 104
    entry (the v0.66.1 tag/Release-workflow precondition this task must confirm before proceeding).
  </read_first>
  <action>
    Operator confirms four preconditions before Task 2 may proceed. Full exact steps are in
    &lt;how-to-verify&gt; below. None of these four steps writes any file or triggers any live-publish
    action by themselves -- they are the automated/manual groundwork Task 2 depends on.
  </action>
  <what-built>
    Wave 1 (Plans 105-01 through 105-04) already authored and dev-host-verified: the
    workflow_dispatch-only, dependency-ordered, index-polled crates.io publish workflow; the
    confirm-gated PyPI build+upload+coverage script; the confirm-gated, platform-first npm
    publish script (with the win32-x64-msvc gap closed); and the reusable availability/scope
    re-check + SC4 resolve wrapper. Nothing remains to author -- this task is pure
    precondition-confirmation before the irreversible action in Task 2.
  </what-built>
  <how-to-verify>
    1. Confirm Phase 104's tag is genuinely released: `gh release view v0.66.1` and
       `gh run list --workflow=release.yml -L 1 --json conclusion -q '.[0].conclusion'` must show
       the tag exists and the most recent Release workflow run for it concluded `success`. If Phase
       104 has not yet cut the tag, STOP here -- do not proceed to Task 2 under any circumstance
       (D-01: publishing an orphaned version with no corresponding signed release burns a
       crates.io/PyPI/npm version number permanently).
    2. Run `pwsh -File scripts/verify-registry-availability.ps1` right now (not relying on
       105-RESEARCH.md's session-old snapshot) and confirm none of the 5 registry identities or the
       npm scope show a surprise "claimed by someone else" state.
    3. Confirm the npm scope precondition (D-04) is fully resolved: `npm whoami` succeeds and
       returns a user, AND `https://www.npmjs.com/~oscarmackjr` or
       `https://www.npmjs.com/org/oscarmackjr` now resolves 200 (not 404).
    4. Confirm crates.io and PyPI credentials are configured: the `CARGO_REGISTRY_TOKEN` GitHub
       Actions secret exists on the `OscarMackJr/nono` repo (repo Settings -> Secrets and
       variables -> Actions), and this shell has `TWINE_USERNAME` / `TWINE_PASSWORD` set for the
       PyPI upload.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms all four preconditions (tag+release green, fresh availability re-check clean, npm scope+auth resolved, cargo/PyPI credentials present) with the specific evidence for each (tag/run URL, script output, npm whoami output, secret/env confirmation).</human-check>
  </verify>
  <resume-signal>Reply "ready" with the confirmed tag/run URL and confirmation that all 4 preconditions passed, or describe which precondition is unmet -- Claude will not proceed to Task 2 on a partial result.</resume-signal>
  <done>v0.66.1 confirmed released with a green Release workflow run; fresh availability re-check clean; npm scope+auth and cargo/PyPI credentials all confirmed present.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Execute the live publish -- crates.io (SC1), PyPI (SC2), npm (SC3)</name>
  <files></files>
  <read_first>
    .github/workflows/publish-crates.yml, scripts/publish-pypi-live.ps1, scripts/publish-npm-live.ps1
    (all three Wave 1 deliverables this task invokes as-is). 105-RESEARCH.md Anti-Patterns
    (platform-before-main npm ordering) and Architecture Patterns (crates.io dependency order).
  </read_first>
  <action>
    Operator triggers the three live publishes in this order: crates.io first (it has the strictest
    internal dependency order and the workflow_dispatch mechanism), then PyPI, then npm. Full exact
    commands are in &lt;how-to-verify&gt; below. Each registry's own idempotency (crates.io's
    index-poll skip-if-published, twine's `--skip-existing`, npm's per-version publish rejection)
    makes a partial failure-then-retry safe to resume without re-doing already-completed steps.
  </action>
  <how-to-verify>
    1. crates.io: `gh workflow run publish-crates.yml -f tag=v0.66.1 -f confirm='PUBLISH LIVE'`;
       watch with `gh run watch`; confirm all 3 crates (nono-sandbox, then nono-sandbox-proxy, then
       nono-sandbox-cli, in that literal order) each report either a fresh publish or an
       already-published skip, with the corroborating index-poll confirming each crate before the
       next one's step begins.
    2. PyPI: from this repo, run `pwsh -File scripts/publish-pypi-live.ps1 -Confirm "PUBLISH LIVE"`
       (with `TWINE_USERNAME`/`TWINE_PASSWORD` already set in this shell per Task 1's precondition
       4). Confirm it prints the post-publish PyPI JSON coverage verification with at least one
       filename listed for release `0.66.1`.
    3. npm: run `pwsh -File scripts/publish-npm-live.ps1 -Confirm "PUBLISH LIVE"`. Confirm the
       win32-x64-msvc platform package publishes and resolves live BEFORE the main
       `@oscarmackjr/nono-ts` package is published, and that the script's own KNOWN LIMITATION
       notice (darwin/linux platforms not published this phase) was printed and is understood as an
       accepted, documented gap -- not an unexpected failure.
  </how-to-verify>
  <verify>
    <human-check>Operator reports the crates.io workflow run URL with a successful conclusion (all 3 crates published/skipped in order), the PyPI script's coverage-verification output (filenames for 0.66.1), and the npm script's output (platform package published + resolved before the main package).</human-check>
  </verify>
  <resume-signal>Reply "published" with the crates.io run URL, the PyPI coverage output, and the npm publish output, or describe the specific failing leg -- each registry's own idempotency makes a partial-then-retry safe to resume from where it stopped.</resume-signal>
  <done>All three registries show 0.66.1 (or the resolved matching version) live for the fork-owned identities: nono-sandbox/-proxy/-cli on crates.io, nono-sandbox on PyPI, @oscarmackjr/nono-ts + its win32-x64-msvc platform package on npm.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Post-publish resolve verification (SC4)</name>
  <files></files>
  <read_first>
    scripts/verify-post-publish-resolve.ps1 (Plan 105-04's deliverable -- this task simply invokes
    it for real now that the packages genuinely exist live).
  </read_first>
  <action>
    Operator runs the SC4 resolve wrapper against the now-live packages and confirms all three legs
    report PASS (not the pre-live NOT_YET_LIVE state this script reported during Wave 1 authoring).
  </action>
  <how-to-verify>
    Run `pwsh -File scripts/verify-post-publish-resolve.ps1` and confirm the final summary table
    shows PASS for all three rows: `cargo install nono-sandbox-cli` (isolated temp root), `pip
    install nono-sandbox` (isolated temp venv), and `npm i @oscarmackjr/nono-ts` (isolated temp
    prefix) -- each installs cleanly and each temp directory is removed afterward.
  </how-to-verify>
  <verify>
    <human-check>Operator reports the script's final summary table showing PASS for all three registries.</human-check>
  </verify>
  <resume-signal>Reply "resolved" with the script's PASS/PASS/PASS output, or describe which leg failed to resolve for a targeted follow-up.</resume-signal>
  <done>All three resolve checks report PASS; PUB-02 is fully satisfied end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator (human) <-> three separate outward-facing, irreversible registry publish actions | Each of crates.io, PyPI, and npm treats a published version as permanent (crates.io: publish-only, no delete; PyPI: no re-upload of the same version; npm: publish rejects an existing version) |
| Published npm main package <-> end-user install experience | The platform-before-main ordering is the final live enforcement of the DoS mitigation authored in Plan 105-03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-05-01 | Spoofing | Publishing against a stale (session-old) availability snapshot instead of a fresh one | mitigate | Task 1 explicitly requires re-running scripts/verify-registry-availability.ps1 in this same session, not trusting 105-RESEARCH.md's prior-session snapshot |
| T-105-05-02 | Tampering | Publishing 0.66.1 to any registry before Phase 104's tag is confirmed released, burning a permanent version number with no corresponding signed release | mitigate | Task 1 hard-stops the entire plan if the v0.66.1 tag/Release workflow is not confirmed green, per D-01 |
| T-105-05-03 | Denial of Service | Publishing the npm main package before its win32-x64-msvc platform package resolves live | mitigate | Task 2's how-to-verify explicitly requires confirming platform-package-before-main ordering, matching scripts/publish-npm-live.ps1's built-in enforcement |
| T-105-05-04 | Information Disclosure | A registry token/credential appearing in any command the operator runs or in this plan's SUMMARY | mitigate | every invocation in this plan relies on credentials already present in the operator's shell/GitHub secrets environment; no task instructs printing or pasting a token value |

</threat_model>

<verification>
crates.io shows nono-sandbox, nono-sandbox-proxy, and nono-sandbox-cli at 0.66.1; PyPI's JSON API
shows filenames for nono-sandbox 0.66.1; npm registry shows @oscarmackjr/nono-ts and its
win32-x64-msvc platform package; scripts/verify-post-publish-resolve.ps1 reports PASS for all three
resolve checks.
</verification>

<success_criteria>
PUB-02's four success criteria (ROADMAP.md Phase 105) are all confirmed true against live,
operator-witnessed evidence: SC1 (crates.io dependency-ordered + index-polled publish), SC2 (PyPI
publish + real coverage verification), SC3 (npm publish with the platform-completeness gate honored
and platform-before-main ordering), SC4 (all three resolve in an isolated post-publish environment).
</success_criteria>

<output>
Create .planning/phases/105-live-multi-registry-publish/105-05-SUMMARY.md when done
</output>
