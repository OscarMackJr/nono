---
phase: 78-cross-process-classification
plan: 02
type: execute
wave: 2
depends_on:
  - 78-01
files_modified:
  - crates/nono-cli/src/app_runtime.rs
  - crates/nono-cli/src/agent_cli.rs
  - crates/nono-cli/src/cli.rs
  - crates/nono-cli/tests/daemon_handle_baseline.rs
autonomous: false
requirements:
  - CLAS-01
  - CLAS-02

must_haves:
  truths:
    - "nono classify <pid> attempts the daemon pipe first; structural fallback fires only when daemon is absent"
    - "When daemon is running, the response is labelled authoritative=true in JSON output"
    - "When daemon is absent (GLE=2 / ERROR_FILE_NOT_FOUND), run_classify structural fallback runs unchanged"
    - "Cross-process integration test (NONO_DAEMON_INTEGRATION_TESTS=1) verifies AiAgent for a real confined agent (SC1) AND NotAnAgent for a non-agent PID (SC2) against a live daemon"
    - "CLI help text updated to reflect authoritative path via daemon"
  artifacts:
    - path: "crates/nono-cli/src/app_runtime.rs"
      provides: "daemon-first classify dispatch replacing the standalone-only path"
      contains: "windows_control_pipe_request"
    - path: "crates/nono-cli/src/agent_cli.rs"
      provides: "classify_daemon_request fn (Windows-only) + windows_control_pipe_request promoted to pub(crate)"
      contains: "classify_daemon_request"
    - path: "crates/nono-cli/tests/daemon_handle_baseline.rs"
      provides: "cross-process integration test for SC1/SC2/SC4 — non-optional SC1 with real confined agent"
      contains: "classify"
  key_links:
    - from: "app_runtime.rs Commands::Classify dispatch"
      to: "agent_cli::classify_daemon_request"
      via: "cfg(target_os = 'windows') guard + is_pipe_not_found fallback"
      pattern: "classify_daemon_request"
    - from: "classify_daemon_request"
      to: "windows_control_pipe_request"
      via: "JSON payload {\"action\":\"classify\",\"pid\":N}"
      pattern: "action.*classify"
    - from: "daemon_handle_baseline.rs integration test"
      to: "running nono-agentd"
      via: "NONO_DAEMON_INTEGRATION_TESTS=1 gate"
      pattern: "NONO_DAEMON_INTEGRATION_TESTS"
---

<objective>
Wire `nono classify <pid>` to attempt the daemon control pipe first, with structural fallback when the daemon is absent. Promote `windows_control_pipe_request` to `pub(crate)` in `agent_cli.rs` (single concrete decision — no executor choice). Add `classify_daemon_request` to `agent_cli.rs` (Windows-only, calling the promoted `windows_control_pipe_request` transport directly). Update `app_runtime.rs` to route `Commands::Classify` through this daemon-first path. Update CLI help text. Add a cross-process integration test in `daemon_handle_baseline.rs` that verifies SC1 (AiAgent from a real daemon-launched confined agent), SC2 (NotAnAgent), and SC4 (no SID in response) via a real running daemon (gated by `NONO_DAEMON_INTEGRATION_TESTS=1`). SC1 is NON-optional — the test MUST start a real confined agent, obtain its PID, and assert "AiAgent" from the classify response.

Purpose: CLAS-01 requires the client to use the authoritative daemon verdict. D4 (CONTEXT.md) locks the daemon-first-then-fallback routing. The integration test is the host gate required for SC1; "AiAgent" positive proof cannot be deferred.

Output: Four modified files. The integration test at `daemon_handle_baseline.rs` provides the host-proof gate (not unattended — requires `NONO_DAEMON_INTEGRATION_TESTS=1` and a live daemon with a real confined agent). Plan 01's unit tests remain the unattended gate.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\OMack\Nono\.planning\PROJECT.md
@C:\Users\OMack\Nono\.planning\ROADMAP.md
@C:\Users\OMack\Nono\.planning\STATE.md
@C:\Users\OMack\Nono\.planning\phases\78-cross-process-classification\78-CONTEXT.md
@C:\Users\OMack\Nono\.planning\phases\78-cross-process-classification\78-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From crates/nono-cli/src/app_runtime.rs (lines 61-69):
  Commands::Classify(args) => run_command_with_update(update_handle, silent, || {
      let registry = std::sync::Arc::new(std::sync::Mutex::new(nono::AgentRegistry::new()));
      classify_runtime::run_classify(args, registry)
  }),
  // Replace this block: attempt daemon first; fallback to run_classify when absent.

From crates/nono-cli/src/agent_cli.rs (lines 695-727):
  fn agent_demote(tenant_id: String) -> Result<()> {
      #[cfg(target_os = "windows")]
      {
          let payload = serde_json::json!({ "action": "demote", "tenant_id": tenant_id });
          let payload_str = serde_json::to_string(&payload)...?;
          match windows_control_pipe_request(&payload_str) {
              Ok(response) => { println!("{}", response.trim()); Ok(()) }
              Err(e) if is_pipe_not_found(&e) => Err(NonoError::SandboxInit("daemon not running".into())),
              Err(e) => Err(e),
          }
      }
      #[cfg(not(target_os = "windows"))]
      { Err(NonoError::SandboxInit("...Windows-only...".into())) }
  }
  // classify_daemon_request follows the same cfg pattern but routes fallback to the caller

From crates/nono-cli/src/agent_cli.rs (lines 818-977):
  // CURRENTLY: fn windows_control_pipe_request(json_payload: &str) -> Result<String>
  // CHANGE TO:  pub(crate) fn windows_control_pipe_request(json_payload: &str) -> Result<String>
  // This is the sole transport change — one visibility keyword. The function body is unchanged.
  // Making it pub(crate) allows daemon_handle_baseline.rs integration tests to call it directly
  // for the classify SC1/SC2 assertions without duplicating the framing logic.

From crates/nono-cli/src/agent_cli.rs (lines 979-988):
  #[cfg_attr(not(target_os = "windows"), allow(dead_code))]
  fn is_pipe_not_found(err: &nono::NonoError) -> bool {
      let msg = err.to_string();
      msg.contains("GLE=2") || msg.contains("pipe not available") || msg.contains("not available")
  }
  // Also promote to pub(crate) so app_runtime.rs can call it for fallback detection.

From crates/nono-cli/src/cli.rs (lines 755-775):
  /// Classify a PID's AI_AGENT marker (structural, NON-authoritative)
  // Update help text to reflect daemon-first authoritative path.
  #[command(after_help = "... This check is STRUCTURAL ONLY and NON-AUTHORITATIVE ... ")]
  Classify(ClassifyArgs),

From crates/nono-cli/tests/daemon_handle_baseline.rs (lines 95-116):
  fn daemon_integration_tests_enabled() -> bool {
      std::env::var("NONO_DAEMON_INTEGRATION_TESTS").as_deref() == Ok("1")
  }
  macro_rules! require_integration! { ... }
  // SC1 analog: mirror cross-tenant denial test (lines 980-1158) for agent launch + classify

Daemon classify response format (from Plan 01):
  The daemon returns a plain string: "AiAgent" or "NotAnAgent".
  JSON output for classify maps these to: { "pid": N, "verdict": "ai_agent"/"not_an_agent",
    "authoritative": true }
  MUST NOT include "package_sid" field or SID string (SC4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote windows_control_pipe_request/is_pipe_not_found to pub(crate), add classify_daemon_request, update app_runtime.rs dispatch and cli.rs help text</name>
  <read_first>
    crates/nono-cli/src/agent_cli.rs (lines 695-727 agent_demote pattern showing cfg structure, 815-977 windows_control_pipe_request full body, 979-988 is_pipe_not_found)
    crates/nono-cli/src/app_runtime.rs (lines 61-69 current Classify dispatch)
    crates/nono-cli/src/classify_runtime.rs (lines 55-95 run_classify signature and behavior — the fallback)
    crates/nono-cli/src/agent_daemon/control_loop.rs (Plan 01 output — Classify response is a plain "AiAgent" or "NotAnAgent" string)
  </read_first>
  <action>
    STEP 1 — Promote visibility of two existing functions in crates/nono-cli/src/agent_cli.rs.

    Change `fn windows_control_pipe_request` (line ~819) to `pub(crate) fn windows_control_pipe_request`. This is a single-word visibility change; the function body, cfg gate (`#[cfg(target_os = "windows")]`), and all documentation are unchanged. This promotion is required so the integration test in daemon_handle_baseline.rs can call the transport directly without duplicating the frame-write logic.

    Change `fn is_pipe_not_found` (line ~984) to `pub(crate) fn is_pipe_not_found`. Remove the `#[cfg_attr(not(target_os = "windows"), allow(dead_code))]` attribute (the function becomes used on all platforms via app_runtime.rs fallback detection). The function body is unchanged.

    STEP 2 — Add `pub(crate) fn classify_daemon_request(pid: u32, json_output: bool) -> Result<()>` to crates/nono-cli/src/agent_cli.rs, placed after the existing agent_demote function. The function uses the same cfg(windows)/cfg(not(windows)) dual-arm pattern as agent_demote.

    Windows implementation arm:
    a. Serialize `serde_json::json!({ "action": "classify", "pid": pid })` to a string; propagate serialization error as `NonoError::SandboxInit`.
    b. Call `windows_control_pipe_request(&payload_str)`.
    c. On `Ok(response)`: parse the trimmed response string ("AiAgent" or "NotAnAgent"). If `json_output` is true, emit a JSON object with fields `pid` (u64), `verdict` ("ai_agent" or "not_an_agent" — map from the daemon string), `authoritative: true`. If `json_output` is false, print a human-readable line: "PID {pid}: AiAgent (authoritative)" or "PID {pid}: NotAnAgent (authoritative)". Neither output path may include "package_sid" or any SID string. Return `Ok(())`.
    d. On `Err(e)` where `is_pipe_not_found(&e)` is true: return `Err(NonoError::SandboxInit("daemon-absent: use structural fallback".into()))`. The caller (app_runtime.rs) detects this specific message to route to the fallback.
    e. On any other `Err(e)`: propagate `Err(e)`.

    Non-Windows stub arm: `return Err(NonoError::SandboxInit("nono classify daemon path is Windows-only".into()))`.

    STEP 3 — Update the `Commands::Classify(args)` dispatch in crates/nono-cli/src/app_runtime.rs (lines 61-69). The new body:

    On cfg(target_os = "windows"): attempt `agent_cli::classify_daemon_request(args.pid, args.json)`. If the error message from a returned `Err` contains "daemon-absent" (the sentinel from classify_daemon_request step d above), fall back to `classify_runtime::run_classify(args, registry)`. For any other `Err`, propagate it. On `Ok(())`, return `Ok(())`. Non-Windows: keep the existing `classify_runtime::run_classify` path unchanged. The `registry` binding for the fallback path is still constructed with `nono::AgentRegistry::new()` as before.

    The cfg(target_os = "windows") compile gate for the frame-send path is verified by `cargo build --bin nono` succeeding — the classify_daemon_request Windows arm calls `windows_control_pipe_request` which calls `CreateFileW` etc.; if any of those types are missing or the fn is not pub(crate), the build fails.

    STEP 4 — Update the `Commands::Classify` help text in crates/nono-cli/src/cli.rs (lines ~755-775). Change the `after_help` string to: "When `nono-agentd` is running, classification is authoritative (registry-backed, daemon verdict). Falls back to structural (non-authoritative) when the daemon is absent. Use `--json` for machine-readable output." Keep the existing ClassifyArgs fields and examples unchanged.
  </action>
  <verify>
    <automated>cargo build --bin nono 2>&amp;1 &amp;&amp; cargo clippy --bin nono -- -D warnings -D clippy::unwrap_used 2>&amp;1 &amp;&amp; echo BUILD_AND_CLIPPY_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --bin nono` succeeds with no new warnings.
    - `cargo clippy --bin nono -- -D warnings -D clippy::unwrap_used` passes.
    - `windows_control_pipe_request` is `pub(crate)` in agent_cli.rs (grep: `pub(crate) fn windows_control_pipe_request`).
    - `is_pipe_not_found` is `pub(crate)` in agent_cli.rs.
    - `classify_daemon_request` exists in agent_cli.rs as `pub(crate)`, has cfg(windows)/cfg(not(windows)) arms.
    - The `Commands::Classify` arm in app_runtime.rs calls `classify_daemon_request` on Windows (grep: `classify_daemon_request` appears in app_runtime.rs).
    - The daemon-absent fallback path in app_runtime.rs calls `classify_runtime::run_classify` (grep: `run_classify` still appears in the Classify arm).
    - Neither `classify_daemon_request` nor the updated Classify arm contains "package_sid" or "S-1-15-2-" in any String literal or format string.
    - The cfg(target_os = "windows") build compiles the frame-send path (CreateFileW call chain via windows_control_pipe_request) — confirmed by `cargo build --bin nono` succeeding on the Windows dev host.
  </acceptance_criteria>
  <done>nono classify pid on Windows attempts the daemon pipe first; on daemon-absent falls back to structural. windows_control_pipe_request is pub(crate) and callable from integration tests. The response carries no SID data.</done>
</task>

<task type="auto">
  <name>Task 2: Add cross-process integration test in daemon_handle_baseline.rs (SC1 + SC2 + SC4, non-optional)</name>
  <read_first>
    crates/nono-cli/tests/daemon_handle_baseline.rs (lines 95-116 require_integration macro and daemon_integration_tests_enabled fn, 980-1158 cross-tenant denial test — the structural model for agent launch + classify; study the AppContainer tenant setup, PID capture, and pipe-send pattern)
    crates/nono-cli/src/agent_daemon/control_loop.rs (Plan 01 output: classify response is "AiAgent" or "NotAnAgent" plain string)
    crates/nono-cli/src/agent_cli.rs (Task 1 output: windows_control_pipe_request is now pub(crate); use it directly for the pipe send)
  </read_first>
  <action>
    Add a new integration test function `classify_pid_returns_verdict_from_daemon` to crates/nono-cli/tests/daemon_handle_baseline.rs. This test is the host gate for SC1 (a daemon-launched confined agent returns "AiAgent" from a separate process). SC1 is NON-optional — the test MUST complete all assertions, not skip or short-circuit them.

    The test structure:

    a. Call `require_integration!()` as the first statement (standard gate — without `NONO_DAEMON_INTEGRATION_TESTS=1` the test skips cleanly with a clear message, does not fail).

    b. Gate the entire test body with `#[cfg(target_os = "windows")]` (the classify verb and control pipe are Windows-only).

    c. SC1 — AiAgent positive proof: start a real nono-agentd-confined agent, mirroring the setup in the cross-tenant denial test (lines 980-1158). That test stands up real AppContainer tenants via the daemon's Launch verb; use the same pattern: send `{"action":"launch","profile":"<test-profile>","cmd":["cmd.exe","/c","timeout","30"]}` to the control pipe and capture the response. Parse the launched agent's PID from the response. Then send `{"action":"classify","pid":<agent-pid>}` to the control pipe and capture the response. Assert `response.trim() == "AiAgent"` (SC1 positive proof). If the response is "NotAnAgent" or any other string, the test FAILS — this is not a skip condition.

    d. SC2 — NotAnAgent negative proof: send `{"action":"classify","pid":<test-process-pid>}` where `<test-process-pid>` is `std::process::id()` (the integration test's own process, which is NOT a daemon-launched AppContainer agent). Assert `response.trim() == "NotAnAgent"` (SC2: non-agent PID returns NotAnAgent).

    e. SC4 — no SID in either response: assert that neither the AiAgent response nor the NotAnAgent response contains "package_sid" or the substring "S-1-15-2-". Both assertions must pass.

    f. Clean up: send `{"action":"demote","tenant_id":"<launched-tenant-id>"}` (or equivalent cleanup) to leave the daemon in a clean state after the test. Mirror the teardown pattern from the cross-tenant denial test.

    For the pipe transport: use `agent_cli::windows_control_pipe_request` (now `pub(crate)` from Task 1) to send each JSON frame. Do NOT inline a duplicate framing implementation — the promoted function IS the correct reuse point. Import it from the crate: `use crate::agent_cli::windows_control_pipe_request` or the appropriate module path for an integration test file in the tests/ directory.

    Function signature: `fn classify_pid_returns_verdict_from_daemon()` with `#[test]`.

    Do NOT use `unwrap()` or `expect()` outside the test module's `#[allow(clippy::unwrap_used)]` scope. The existing test file already has this annotation — confirm it covers this function. Use proper `Result` propagation or `assert!` / `assert_eq!` for all assertions.
  </action>
  <verify>
    <automated>cargo test -p nono-cli --test daemon_handle_baseline classify_pid -- --nocapture 2>&amp;1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p nono-cli --test daemon_handle_baseline classify_pid` (without `NONO_DAEMON_INTEGRATION_TESTS=1`) exits 0 with a SKIP message (not a failure).
    - `fn classify_pid_returns_verdict_from_daemon` exists in daemon_handle_baseline.rs (grep confirms).
    - With `NONO_DAEMON_INTEGRATION_TESTS=1` and a live daemon: the test launches a real confined agent via the daemon Launch verb, classifies its PID via the classify verb, and asserts the response == "AiAgent" (SC1 positive proof — NOT optional).
    - The test also classifies the test process's own PID and asserts the response == "NotAnAgent" (SC2).
    - Both assertions include a `!response.contains("package_sid")` and `!response.contains("S-1-15-2-")` check (SC4).
    - `cargo clippy -p nono-cli --tests -- -D warnings -D clippy::unwrap_used` passes (unwrap_used is allowed via existing test module attribute; the test body does not introduce new unwrap calls outside that scope).
    - The test calls `agent_cli::windows_control_pipe_request` (the promoted pub(crate) function) — does NOT duplicate the framing logic inline.
  </acceptance_criteria>
  <done>The integration test is in place, skips cleanly without the env var, and — with the env var and a live daemon — proves SC1 (AiAgent from a real confined agent), SC2 (NotAnAgent for non-agent), and SC4 (no SID in either response).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01 added the daemon-side Classify verb, the pure classify_response_string function, and unit tests (unattended gate: `cargo test --bin nono-agentd -- classify`). Plan 02 wired the client routing (daemon-first + fallback), added classify_daemon_request, promoted windows_control_pipe_request to pub(crate), and added the integration test. The host gate for SC1 requires a real running nono-agentd with a real confined agent — this step is NOT optional and must pass before the phase is closed.
  </what-built>
  <how-to-verify>
    Prerequisites: Win11 26200 host, `nono-agentd` running (`nono daemon start`), a release build of nono in PATH.

    Step 1 — Unattended gate (no daemon required, no env var):
    `cargo test --bin nono-agentd -- classify`
    Expected: all 4 classify_* tests PASS (classify_response_aiagent_omits_package_sid, classify_response_notanagent, classify_request_deserializes, classify_non_appcontainer_pid_returns_not_an_agent).

    Step 2 — Structural fallback (daemon absent):
    Stop the daemon: `nono daemon stop`
    Run: `nono classify <any-pid>`
    Expected: prints structural/non-authoritative output with the existing NOTE_* disclaimer (same behavior as Phase 73). No crash.

    Step 3 — Daemon-present SC2 (non-agent PID):
    Start the daemon: `nono daemon start`
    Run: `nono classify <pid-of-notepad-or-cmd-or-any-non-agent>`
    Expected: prints "NotAnAgent (authoritative)" or equivalent JSON with `"authoritative": true`. No SID in output.

    Step 4 — Daemon-present SC1 (agent PID — REQUIRED):
    With daemon running, launch a confined agent: `nono agent launch --profile <any> -- cmd.exe /c timeout 30`
    Note the PID from the output.
    Run: `nono classify <agent-pid>`
    Expected: prints "AiAgent (authoritative)" or equivalent JSON with `"authoritative": true`. No SID in output. This step MUST pass — it is the SC1 host gate.

    Step 5 — SC4 JSON output (no SID):
    Run: `nono classify <any-pid> --json`
    Assert the JSON output does NOT contain a "package_sid" key or "S-1-15-2-" substring.

    Step 6 — Integration test with live daemon:
    `NONO_DAEMON_INTEGRATION_TESTS=1 cargo test -p nono-cli --test daemon_handle_baseline classify_pid -- --nocapture`
    Expected: test PASSES, output shows "AiAgent" for the launched agent and "NotAnAgent" for the test process's own PID.

    Step 7 — SC3 structural gating:
    The SDDL enforcement is structural (kernel gate). If a Low-IL test environment is available, confirm `CreateFileW` on `\\.\pipe\nono-agentd-control` fails with ERROR_ACCESS_DENIED. If not available, document as VERIFIED_BY_SDDL and cite the unchanged CONTROL_PIPE_SDDL constant.
  </how-to-verify>
  <resume-signal>Type "approved" after steps 1-6 pass. Step 7 may be noted as VERIFIED_BY_SDDL if the Low-IL test environment is unavailable. If any step fails, describe which step and the exact error output. SC1 (step 4) is NOT optional — the phase cannot be closed without it.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nono CLI process → control pipe | nono classify sends a classify frame; the pipe SDDL enforces Medium-IL minimum (SC3, inherited from Plan 01 daemon side). |
| Classify response → caller output | The JSON/human output function must not include SID data regardless of the daemon's internal registry state (SC4). |
| Structural fallback path | When the daemon is absent, run_classify uses the Phase 73 non-authoritative path — deliberate and labelled; must not be confused with the authoritative path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-06 | Information Disclosure | classify_daemon_request response printing | mitigate | Output function omits SID fields; only verdict enum string is printed. Acceptance criterion asserts no "package_sid" in any String literal or format string in classify_daemon_request. |
| T-78-07 | Spoofing | Fallback mis-labelled as authoritative | mitigate | Fallback (run_classify) retains its existing `authoritative: false` JSON flag and NOTE_* disclaimers unchanged (no modification to classify_runtime.rs). Only the daemon-sourced response carries `authoritative: true`. |
| T-78-08 | Elevation of Privilege | Low-IL caller on client side | accept | Low-IL caller cannot open the control pipe (SDDL, T-78-04). Even if a Low-IL process runs nono classify, the CreateFileW call fails at the kernel before the classify payload is sent. |
| T-78-09 | Denial of Service | Pipe timeout on classify request | accept | 5-second timeout already set in windows_control_pipe_request (TIMEOUT_MS=5_000). On timeout the error propagates as a non-GLE=2 error; app_runtime.rs falls through to structural fallback — non-fatal. |
| T-78-SC | Tampering | npm/pip/cargo installs | accept | No new package installs. |
</threat_model>

<verification>
1. `cargo test --bin nono-agentd -- classify` — unattended gate from Plan 01; must pass in CI without a live daemon. This IS the dark-factory unattended gate for Phase 78 per the ROADMAP "Unattended gate" line and the locked decision in STATE.md ("CLAS-01/02 (Phase 78) is NOT harness-gated — the daemon test binary is the unattended gate"). No `scripts/gates/classify-e2e.ps1` is created — the exemption is ROADMAP-backed and cited here explicitly so it is not re-flagged.
2. `cargo test -p nono-cli --test daemon_handle_baseline classify_pid` — compiles and SKIPs cleanly without `NONO_DAEMON_INTEGRATION_TESTS=1`. With the env var and a live daemon, PASSES (SC1 + SC2 + SC4 all asserted, SC1 is non-optional).
3. `cargo build --bin nono` — CLI binary builds cleanly on the Windows dev host. The cfg(target_os = "windows") frame-send path (classify_daemon_request calling windows_control_pipe_request) MUST compile — a build failure here indicates windows_control_pipe_request is not pub(crate) or the Windows types are missing.
4. `cargo clippy --bin nono -- -D warnings -D clippy::unwrap_used` passes.
5. `cargo clippy --bin nono-agentd -- -D warnings -D clippy::unwrap_used` passes.
6. Cross-target clippy disposition: app_runtime.rs and agent_cli.rs both contain cfg(target_os = "windows") guards. The non-Windows stubs must compile cleanly on Linux/macOS. Mark PARTIAL if cross-toolchain is absent on the dev host; defer to CI per CLAUDE.md cross-target rule.
7. `nono classify <pid> --json` output does not contain "package_sid" key (grep or jq assertion on the output).
8. Human checkpoint: all steps 1-6 in the checkpoint task must pass, especially SC1 (step 4, daemon-present AiAgent verdict for a real confined agent) and the integration test (step 6).
</verification>

<success_criteria>
Maps to ROADMAP Phase 78 success criteria:
- SC1: `nono classify <daemon-agent-pid>` returns "AiAgent" from a separate process without elevated privileges — verified by the human checkpoint (Task 3, step 4) AND by the NONO_DAEMON_INTEGRATION_TESTS=1 integration test in daemon_handle_baseline.rs. Both are REQUIRED (SC1 is non-optional per ROADMAP).
- SC2: `nono classify <notepad-pid>` returns "NotAnAgent" — verified by human checkpoint step 3 and integration test SC2 assertion.
- SC3: Low-IL caller is denied — verified by SDDL constant (unchanged, tested in Plan 01 unit test `control_pipe_sddl_is_medium_il_only`).
- SC4: No SID in any classify response — verified by Plan 01 pure-function unit test `classify_response_aiagent_omits_package_sid`, integration test SC4 assertions, and `nono classify --json` output check.
</success_criteria>

<output>
Create `.planning/phases/78-cross-process-classification/78-02-SUMMARY.md` when done.
</output>
