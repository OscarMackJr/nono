---
phase: 64-minifilter-spike-implementation-macos-p1-cherry-pick-wave
plan: 04
type: execute
wave: 3
depends_on:
  - 64-01
  - 64-02
  - 64-03
files_modified:
  - crates/nono/src/sandbox/macos.rs
  - drivers/nono-fltmgr/nono-fltmgr.inf
autonomous: false
requirements:
  - DRV-01
  - DRV-03
  - MACOS-02

must_haves:
  truths:
    - "8f84d454 (platform rules after write allows) is applied to macos.rs with verbatim Upstream-commit: trailer"
    - "test_generate_profile_platform_rules_after_writes passes (deny after write — post-fix ordering)"
    - "test_platform_rules_after_write_allows passes (deny after write for ReadWrite capability)"
    - "test_platform_deny_symlink_and_canonical_path passes (both /etc/... and /private/etc/... in profile)"
    - "cargo clippy -p nono -p nono-cli --target x86_64-apple-darwin passes (no cfg-gated drift)"
    - "aarch64-apple-darwin cross-target is marked PARTIAL in the verification artifact (defer to CI)"
    - "Driver loads on VM at a non-colliding FSFilter Activity Monitor altitude (fltmc instances confirms)"
    - "Deny harness asserts ERROR_ACCESS_DENIED (5) when attempting to open the deny-target path"
    - "INF altitude placeholder 370020 replaced with the actual non-colliding altitude number"
  artifacts:
    - path: "crates/nono/src/sandbox/macos.rs"
      provides: "macos.rs with 8f84d454 applied: platform_rules() loop moved AFTER write-allows loop"
      contains: "Upstream-commit: 8f84d454"
    - path: "drivers/nono-fltmgr/nono-fltmgr.inf"
      provides: "INF with actual altitude number replacing 370020 placeholder"
      contains: "Instance1.Altitude"
    - path: ".planning/phases/64-.../64-SC1-driver-evidence.md"
      provides: "SC1 evidence: fltmc instances output + deny harness output + DRV-03 complete"
      contains: "SC1 PASS"
  key_links:
    - from: "crates/nono/src/sandbox/macos.rs generate_profile"
      to: "test_generate_profile_platform_rules_after_writes (same file)"
      via: "platform_rules() loop AFTER write-allows loop; assert write_pos < deny_pos"
      pattern: "write_pos < deny_pos"
    - from: "drivers/nono-fltmgr/nono-fltmgr.inf"
      to: "VM fltmc instances output (SC1 evidence)"
      via: "Instance1.Altitude = <actual>; fltmc instances confirms registration at that altitude"
      pattern: "Instance1.Altitude"
---

<objective>
Two tracks merged in Wave 3:

Track B finale: Apply the third P1 macOS commit (`8f84d454` — platform rules after write allows)
to `macos.rs`. This moves the `platform_rules()` loop to after the write-allows loop, fixing the
Seatbelt last-match-wins security defect. Updates the existing ordering test to assert the post-fix
invariant. The Wave 0 RED tests (Plans 01 + 02) must turn GREEN. Runs x86_64-apple-darwin cross-
target clippy (aarch64 PARTIAL → CI per D-12). This sub-task is fully autonomous (no VM needed).

Track A finale: On the Azure test VM, reprovision a fresh VM from Phase 63 scripts, enumerate
`fltmc filters` to pick a non-colliding altitude in 360000–389999, update `nono-fltmgr.inf`, build
the extended driver (Plans 02 output), run the full test-signing pipeline (`makecert → inf2cat →
signtool → certmgr → bcdedit → pnputil`), and execute the deny harness to capture SC1 evidence.
This sub-task requires the human to operate the Azure VM (autonomous: false).

Purpose: After this plan, Track B is complete (all three P1 commits absorbed, all ordering tests
GREEN, cross-target clippy clean), Track A has VM evidence satisfying DRV-01 + DRV-03, and Plan 05
can write `drivers/README.md`.

Output:
- `crates/nono/src/sandbox/macos.rs` — 8f84d454 cherry-picked, ordering tests GREEN
- `drivers/nono-fltmgr/nono-fltmgr.inf` — altitude placeholder replaced
- `.planning/phases/64-.../64-SC1-driver-evidence.md` — VM evidence artifact (new)
</objective>

<execution_context>
@C:\Users\OMack\AppData\Local\nvs\default\node_modules\@twg-gsd\gsd-sdk\workflows\execute-plan.md
@C:\Users\OMack\AppData\Local\nvs\default\node_modules\@twg-gsd\gsd-sdk\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-minifilter-spike-implementation-macos-p1-cherry-pick-wave/64-CONTEXT.md
@.planning/phases/64-minifilter-spike-implementation-macos-p1-cherry-pick-wave/64-RESEARCH.md
@.planning/phases/64-minifilter-spike-implementation-macos-p1-cherry-pick-wave/64-PATTERNS.md
@.planning/phases/63-minifilter-spike-groundwork-macos-divergence-ledger-audit/63-DIVERGENCE-LEDGER.md
@.planning/phases/63-minifilter-spike-groundwork-macos-divergence-ledger-audit/63-SC1-vm-state.md
@.planning/templates/cross-target-verify-checklist.md

<interfaces>
<!-- macos.rs generate_profile function — current location of platform_rules() loop (PRE-FIX) -->
From crates/nono/src/sandbox/macos.rs lines 667-676 (pre-fix, will be changed):
  // Platform deny rules are placed BETWEEN read and write rules. (OLD comment — update to reflect fix)
  for rule in caps.platform_rules() {
      profile.push_str(rule);
      profile.push('\n');
  }
  [This loop is between the read-allows block (~lines 640-654) and write-allows block (~lines 702-717)]
  
  Fix: move the platform_rules() loop to AFTER the write-allows block (after line ~717).
  Update the comment to: "Platform deny rules are placed AFTER write rules (last-match-wins: deny wins over preceding allows)"

<!-- Existing test to update (lines 997-1030) — already scaffolded as renamed test in Plan 01 -->
test_generate_profile_platform_rules_after_writes:
  Old assertion: read_pos < deny_pos && deny_pos < write_pos  (WRONG — pre-fix)
  New assertion: read_pos < write_pos && write_pos < deny_pos  (post-fix — what Plan 01 wrote)

<!-- D-12 PARTIAL cross-target: x86_64-apple-darwin installed, aarch64-apple-darwin NOT installed -->
x86_64-apple-darwin: cargo clippy -p nono -p nono-cli --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used
aarch64-apple-darwin: NOT installed on dev host — PARTIAL → defer to live CI

<!-- Azure VM reuse: Phase 63 VM may still exist (IP: 20.51.161.15, rg-nono-fltmgr-spike) -->
<!-- If VM was deallocated, reprovision from 63-preflight-azure.ps1 (Standard security type) -->
<!-- Snapshot: 63-SC1-vm-state.md confirms nono-fltmgr-snap-testsigning-ready was taken -->
<!-- The snap is the Phase 64 starting point — if driver install BSODs, restore from it -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cherry-pick 8f84d454 into macos.rs + turn ordering tests GREEN + x86_64-apple-darwin clippy</name>
  <files>
    crates/nono/src/sandbox/macos.rs
  </files>
  <read_first>
    - crates/nono/src/sandbox/macos.rs lines 640-720: the full generate_profile rule-emission
      section — read to identify exact current positions of read-allows, platform_rules(), and
      write-allows loops before making changes.
    - crates/nono/src/sandbox/macos.rs lines 997-1030: the existing
      test_generate_profile_platform_rules_after_writes test (renamed+scaffolded in Plan 01) —
      confirm Wave 0 assertions are already in place.
    - .planning/phases/64-.../64-RESEARCH.md §8f84d454 commit detail: exact block to move
      (lines 667-676) and the Seatbelt last-match-wins rationale.
    - .planning/phases/64-.../64-PATTERNS.md §macos.rs: post-fix ordering + comment text.
    - .planning/phases/63-.../63-DIVERGENCE-LEDGER.md §C14: diff-inspect note for 8f84d454.
    - .planning/templates/cross-target-verify-checklist.md: PARTIAL mark format for aarch64.
  </read_first>
  <action>
    Step 1 — Diff-inspect call-site. Read macos.rs lines 640-730 to confirm the `platform_rules()`
    loop is currently BETWEEN the read-allows block and the write-allows block (pre-fix ordering).
    Confirm the loop body: `for rule in caps.platform_rules() { profile.push_str(rule); ... }`.
    If the structure has diverged from what RESEARCH.md documents, note the divergence in the
    commit body and port the fix at the actual location (D-10 manual-port rule).

    Step 2 — Move the platform_rules() loop. Cut the entire `for rule in caps.platform_rules() {}`
    block (and its preceding comment) from its current between-reads-and-writes location and paste
    it AFTER the write-allows loop (after the last `}` of the write-allows `for` loop). Update the
    comment to: "Platform deny rules appear AFTER write rules (last-match-wins: deny overrides
    preceding write-allows)". This is the entirety of the 8f84d454 fix.

    Step 3 — Confirm ordering tests now pass (turn RED to GREEN):
    The three tests added/updated in Plan 01 Task 2 should now pass:
    - test_generate_profile_platform_rules_after_writes: write_pos < deny_pos must hold
    - test_platform_rules_after_write_allows: write_pos < deny_pos must hold
    - test_platform_deny_symlink_and_canonical_path: both path strings in profile (already passed)

    Step 4 — Run cross-target clippy (D-12):
    Run: cargo clippy -p nono -p nono-cli --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used
    This MUST pass. The macos.rs change is a pure block reorder within an existing function —
    no new imports, no new cfg-gated code. If clippy fails, fix before committing.

    Step 5 — Mark aarch64 PARTIAL per .planning/templates/cross-target-verify-checklist.md.
    Create/update the cross-target checklist entry in the SUMMARY.md for this plan noting:
    "x86_64-apple-darwin: PASS (local). aarch64-apple-darwin: PARTIAL — target not installed on
    dev host; deferred to live CI per CLAUDE.md cross-target MUST rule."

    Step 6 — Commit with verbatim D-19 Upstream-commit: trailer:
    "fix(macos): emit platform rules after user write allows
    
    Port of upstream 8f84d454: moves the platform_rules() loop in generate_profile to after
    the write-allows block, ensuring Seatbelt last-match-wins semantics allow targeted deny
    rules to override preceding write-allow rules.
    
    Existing test updated to assert post-fix ordering (read_pos < write_pos < deny_pos).
    New D-11 ordering tests (test_platform_rules_after_write_allows) confirm the invariant.
    
    x86_64-apple-darwin: PASS. aarch64-apple-darwin: PARTIAL (deferred to CI).
    
    Upstream-commit: 8f84d454
    Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>"
  </action>
  <verify>
    <automated>
      cargo test -p nono -- sandbox::macos::tests::test_generate_profile_platform_rules_after_writes
      cargo test -p nono -- sandbox::macos::tests::test_platform_rules_after_write_allows
      cargo test -p nono -- sandbox::macos::tests::test_platform_deny_symlink_and_canonical_path
      cargo test -p nono -- sandbox::macos
      cargo clippy -p nono -p nono-cli --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used
    </automated>
    All five commands must exit 0. The first three tests must now PASS (transition from RED).
  </verify>
  <done>
    All three macos.rs ordering/symlink tests PASS.
    `cargo clippy --target x86_64-apple-darwin` exits 0.
    Git log shows commit with `Upstream-commit: 8f84d454` and DCO sign-off.
    aarch64-apple-darwin marked PARTIAL in the SUMMARY cross-target section.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The extended `nono-fltmgr.c` (Plan 02) + `nono-fltmgr.h` IPC header + `nono-fltmgr-client`
    Rust user-mode client (Plans 01+03) are ready for the Phase 64 VM test-signing pipeline.
    The INF altitude placeholder `370020` must be replaced with a non-colliding number in
    FSFilter Activity Monitor band (360000–389999) before driver installation.

    This checkpoint drives the Track A VM operations: altitude enumeration, INF update, full
    test-signing pipeline, driver load, and deny-harness execution. All these steps require the
    Azure VM (or a fresh reprovision) and cannot be automated from the dev host.
  </what-built>
  <how-to-verify>
    Prerequisites:
    - Confirm the Phase 63 Azure VM is running (or reprovision from 63-preflight-azure.ps1).
    - Confirm the VM has the testsigning-ready snapshot (63-SC1-vm-state.md §Step 13).
    - Copy the updated drivers/nono-fltmgr/ directory (from Plan 02) to the VM.
    - Build the extended driver on the VM: msbuild nono-fltmgr.vcxproj /p:Configuration=Release /p:Platform=x64
      (should succeed 0 errors, 0 warnings; if the .vcxproj or .h has issues, fix on dev host + rebuild).

    Step 1 — Enumerate existing filters and pick the altitude:
    On the VM (elevated PowerShell): fltmc filters
    Pick a number in 360000–389999 NOT occupied by any existing driver (avoid 385201 = Sysmon).
    Example: if fltmc shows nothing in the range, use 365500. Record the chosen number.

    Step 2 — Update INF altitude:
    On the VM, edit drivers/nono-fltmgr/nono-fltmgr.inf line: Instance1.Altitude = "370020"
    Replace 370020 with the chosen number. Commit this change back to the dev host (update the
    repo copy of nono-fltmgr.inf with the actual non-colliding altitude number).

    Step 3 — Full test-signing pipeline on the VM (per D-07):
    Execute these commands in sequence in an elevated EWDK command prompt on the VM:
    a. makecert -r -pe -ss PrivateCertStore -n "CN=NonoTestSign" NonoTestSign.cer
    b. certmgr /add NonoTestSign.cer /s /r localMachine root
    c. inf2cat /driver:. /os:10_x64 /uselocaltime (produces nono-fltmgr.cat)
    d. signtool sign /fd sha256 /s PrivateCertStore /n "CN=NonoTestSign" /t http://timestamp.digicert.com nono-fltmgr.cat
    e. Confirm bcdedit /enum shows testsigning Yes (already set in Phase 63)
    f. pnputil /add-driver nono-fltmgr.inf /install
       Expected: "Driver package added successfully."

    Step 4 — Verify driver is loaded:
    fltmc instances
    fltmc filters
    Expected: nono-fltmgr appears in the list at the chosen altitude.

    Step 5 — Provision deny-target and run scripted harness (D-01):
    Copy the PowerShell deny harness from RESEARCH.md §Scripted Deny Harness (or adapt).
    Create the deny-target path: New-Item -ItemType Directory -Force "C:\nono-deny-test"
    Set-Content -Path "C:\nono-deny-test\secret.txt" -Value "deny-target"
    Start the nono-fltmgr-client user-mode policy client (cargo run or pre-built binary):
      - Build on dev host: cargo build -p nono-fltmgr-client (Windows target)
      - Copy the .exe to the VM
      - In one terminal: run nono_fltmgr_client.exe C:\nono-deny-test\secret.txt
        (or adapt if run_policy_client is a library — a minimal main.rs wrapper may be needed)
    In a second terminal: run the deny harness script.

    Expected harness output: "SC1 PASS: CreateFile denied with ERROR_ACCESS_DENIED (5) as expected"
    Expected fltmc instances: nono-fltmgr at chosen altitude.

    Step 6 — Capture evidence:
    Paste into .planning/phases/64-.../64-SC1-driver-evidence.md:
    - fltmc filters output (altitude confirmed, no collision)
    - pnputil /add-driver output
    - fltmc instances output (driver loaded)
    - Deny harness output (SC1 PASS: ERROR_ACCESS_DENIED 5)
    - Chosen altitude number

    Step 7 — Take a post-load snapshot:
    az snapshot create --resource-group rg-nono-fltmgr-spike --name nono-fltmgr-snap-loaded --source $diskName

    BSOD mitigation: if the driver causes a BSOD on load, restore from nono-fltmgr-snap-testsigning-ready,
    fix the issue in nono-fltmgr.c (check DESIGN.md BSOD triad), rebuild, and retry.
    Common causes: IRQL violation (check NT_ASSERT placement), NULL timeout (check FltSendMessage call),
    port connect failure (check security descriptor width per RESEARCH.md Pitfall F).
  </how-to-verify>
  <resume-signal>
    Type "approved" when:
    - fltmc instances shows nono-fltmgr at the chosen altitude (non-colliding, 360000-389999)
    - Deny harness output is "SC1 PASS: CreateFile denied with ERROR_ACCESS_DENIED (5)"
    - 64-SC1-driver-evidence.md is committed with the captured outputs
    - nono-fltmgr.inf altitude has been updated from 370020 to the chosen number and committed
    Or describe any failure mode and the BSOD recovery step taken.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kernel driver → test VM OS | Test-signed driver with Secure-Boot-OFF; isolated to test VM only; never installed on production host |
| User-mode deny harness → kernel IRP decision | Harness gets ERROR_ACCESS_DENIED from CreateFile because kernel enforced the IRP deny; no user-mode bypass possible once kernel returns STATUS_ACCESS_DENIED |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | Denial of Service | NonoPreCreate own-I/O recursion (BSOD) | mitigate | Zero ZwCreateFile/NtCreateFile calls verified by grep gate in Plan 02; BSOD triad documents recovery via snapshot restore |
| T-63-03 | Denial of Service | IRQL violation in callback (BSOD) | mitigate | NT_ASSERT(KeGetCurrentIrql() <= APC_LEVEL) in NonoPreCreate; pre-load snapshot = rollback safeguard (D-06) |
| T-63-05 | Tampering | Spike .sys committed to repo | mitigate | .sys binary stays VM-local; DESIGN.md T-63-05 documented; only nono-fltmgr.inf and nono-fltmgr.c are committed |
| T-64-ALT | Tampering | Altitude collision with EDR driver | mitigate | fltmc filters enumeration before load (D-08); Activity Monitor band 360000-389999 only; avoid AV range 320000-329998 |
| T-64-10 | Elevation of Privilege | Seatbelt deny-after-allow ordering bypass | mitigate | 8f84d454 cherry-pick moves platform_rules() loop AFTER write-allows; three ordering tests assert write_pos < deny_pos; x86_64-apple-darwin clippy clean |
| T-64-11 | Information Disclosure | macOS /private/etc symlink bypass | mitigate | test_platform_deny_symlink_and_canonical_path asserts both /etc/... and /private/etc/... in profile |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
Wave 3 completion gates:

Track B (autonomous):
1. `cargo test -p nono -- sandbox::macos` passes — all three ordering tests GREEN
2. `cargo clippy -p nono -p nono-cli --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used` exits 0
3. Git log shows commit with `Upstream-commit: 8f84d454` and DCO sign-off
4. aarch64 cross-target marked PARTIAL in SUMMARY

Track A (human checkpoint):
5. `64-SC1-driver-evidence.md` committed with fltmc instances + deny harness output
6. Deny harness shows "ERROR_ACCESS_DENIED (5)" on the deny-target path
7. `drivers/nono-fltmgr/nono-fltmgr.inf` altitude updated from 370020 to actual non-colliding value
8. `fltmc instances` confirms driver loaded at chosen altitude in 360000-389999 band
</verification>

<success_criteria>
Track B:
- `test_generate_profile_platform_rules_after_writes` PASSES
- `test_platform_rules_after_write_allows` PASSES
- `cargo clippy --target x86_64-apple-darwin` exits 0
- Commit `Upstream-commit: 8f84d454` + DCO sign-off in git log

Track A:
- nono-fltmgr.sys test-signed and loaded on the Azure VM
- `fltmc instances` confirms registration at non-colliding Activity Monitor altitude
- Scripted deny harness output: "SC1 PASS: CreateFile denied with ERROR_ACCESS_DENIED (5)"
- 64-SC1-driver-evidence.md committed with all captured outputs
- nono-fltmgr.inf altitude updated + committed
- nono-wfp-driver.sys placeholder and MSI untouched (git status shows no changes to crates/nono-cli/data/windows/)
</success_criteria>

<output>
Create `.planning/phases/64-minifilter-spike-implementation-macos-p1-cherry-pick-wave/64-04-SUMMARY.md` when done.
</output>
