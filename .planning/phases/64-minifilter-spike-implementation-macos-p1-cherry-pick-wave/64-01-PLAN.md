---
phase: 64-minifilter-spike-implementation-macos-p1-cherry-pick-wave
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/nono-fltmgr-client/Cargo.toml
  - crates/nono-fltmgr-client/src/lib.rs
  - crates/nono-fltmgr-client/src/main.rs
  - crates/nono/src/sandbox/macos.rs
autonomous: true
requirements:
  - DRV-02
  - MACOS-02

must_haves:
  truths:
    - "cargo build -p nono-fltmgr-client succeeds on Windows (compile-time NonoIpcRequest layout assertion passes)"
    - "cargo build -p nono-fltmgr-client succeeds on Linux/macOS CI (empty crate, no errors)"
    - "nono_fltmgr_client.exe binary is buildable on Windows (cargo build -p nono-fltmgr-client --bin nono_fltmgr_client)"
    - "Three test stubs exist in macos.rs: two new (post-fix ordering, symlink+canonical) and one updated (existing test asserts read_pos < write_pos < deny_pos)"
    - "All new/updated tests fail with the current macos.rs code (Wave 0 RED state)"
  artifacts:
    - path: "crates/nono-fltmgr-client/Cargo.toml"
      provides: "Cargo workspace member config for spike crate"
      contains: "publish = false"
    - path: "crates/nono-fltmgr-client/src/lib.rs"
      provides: "NonoIpcRequest #[repr(C)] struct + compile-time layout assertion"
      contains: "NonoIpcRequest"
    - path: "crates/nono-fltmgr-client/src/main.rs"
      provides: "CLI binary entry point: passes first arg as deny_path to run_policy_client"
      contains: "run_policy_client"
    - path: "Cargo.toml"
      provides: "Updated workspace members list"
      contains: "crates/nono-fltmgr-client"
    - path: "crates/nono/src/sandbox/macos.rs"
      provides: "Wave 0 test stubs: test_platform_rules_after_write_allows, test_platform_deny_symlink_and_canonical_path, renamed test_generate_profile_platform_rules_after_writes"
      contains: "test_platform_rules_after_write_allows"
  key_links:
    - from: "crates/nono-fltmgr-client/src/lib.rs"
      to: "drivers/nono-fltmgr/nono-fltmgr.h (Phase 64 Plan 02)"
      via: "NonoIpcRequest payload == 532 bytes matching C-side NONO_IPC_REQUEST _Static_assert"
      pattern: "size_of::<NonoIpcRequest>.*FILTER_MESSAGE_HEADER.*532"
    - from: "crates/nono-fltmgr-client/src/main.rs"
      to: "crates/nono-fltmgr-client/src/lib.rs run_policy_client"
      via: "main() passes argv[1] as deny_path; drives the DRV-02 VM round-trip in Plan 04 Step 5"
      pattern: "run_policy_client"
---

<objective>
Create the Wave 0 scaffolding that gates the rest of Phase 64: the new `nono-fltmgr-client` Cargo
workspace member (with the `#[repr(C)] NonoIpcRequest` compile-time layout assertion and a minimal
`src/main.rs` CLI binary), and the macOS ordering/symlink test stubs in `macos.rs` that must fail
until the cherry-picks land.

Purpose: Every subsequent Track A plan (IPC client logic) and Track B plan (cherry-picks) depends
on these two Wave 0 artifacts. The spike crate's layout assertion links the Rust user-mode client
to the C-side NONO_IPC_REQUEST struct; the failing tests give Track B a RED state to turn GREEN.
The `nono_fltmgr_client.exe` binary produced by `src/main.rs` is the runnable artifact that Plan 04
Step 5 uses on the VM to drive the DRV-02 round-trip proof — a library-only crate cannot be used
as the `nono_fltmgr_client.exe C:\...\secret.txt` invocation in the VM checkpoint.

Output:
- `crates/nono-fltmgr-client/` — new Cargo workspace member (Cargo.toml + src/lib.rs + src/main.rs)
- `Cargo.toml` root — `nono-fltmgr-client` added to `[workspace] members`
- `crates/nono/src/sandbox/macos.rs` — three test functions added/updated (all RED)
</objective>

<execution_context>
@C:\Users\OMack\AppData\Local\nvs\default\node_modules\@twg-gsd\gsd-sdk\workflows\execute-plan.md
@C:\Users\OMack\AppData\Local\nvs\default\node_modules\@twg-gsd\gsd-sdk\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-minifilter-spike-implementation-macos-p1-cherry-pick-wave/64-CONTEXT.md
@.planning/phases/64-minifilter-spike-implementation-macos-p1-cherry-pick-wave/64-RESEARCH.md
@.planning/phases/64-minifilter-spike-implementation-macos-p1-cherry-pick-wave/64-PATTERNS.md

<interfaces>
<!-- Extracted from Cargo.toml (root) — current workspace members block -->
From Cargo.toml lines 1-10:
[workspace]
resolver = "2"
members = [
    "crates/nono",
    "crates/nono-cli",
    "crates/nono-proxy",
    "crates/nono-shell-broker",
    "bindings/c",
    "tools/sign-fixture",
]
<!-- Add "crates/nono-fltmgr-client" in alphabetical position among crates/ entries -->

<!-- Extracted from crates/nono-shell-broker/Cargo.toml — workspace-member inheritance pattern -->
Key fields to replicate (PATTERNS.md §nono-fltmgr-client/Cargo.toml):
- edition.workspace = true
- rust-version.workspace = true
- authors.workspace = true
- license.workspace = true
- repository.workspace = true
- homepage.workspace = true
- [lints] workspace = true          (inherits clippy::unwrap_used deny)
- publish = false                   (spike crate, not for crates.io)
- [target.'cfg(windows)'.dependencies] windows-sys = { version = "0.59", features = ["Win32_Foundation", "Win32_Storage_InstallableFileSystems"] }

<!-- Extracted from crates/nono/src/sandbox/macos.rs — test module pattern (lines 852-857) -->
#[cfg(test)]
#[allow(clippy::unwrap_used)]
mod tests {
    use super::*;
    use crate::capability::{CapabilitySource, FsCapability};
    use std::path::PathBuf;
    ...
}
<!-- Test at line ~998: test_generate_profile_platform_rules_between_reads_and_writes -->
<!-- Currently asserts: read_pos < deny_pos (WRONG — pre-fix); must become read_pos < write_pos < deny_pos -->

<!-- FILTER_MESSAGE_HEADER size: windows-sys 0.59 type in Win32::Storage::InstallableFileSystems -->
<!-- NonoIpcRequest layout (per RESEARCH.md Pattern 3 / PATTERNS.md §lib.rs):
  FILTER_MESSAGE_HEADER (header, first field — added by FilterGetMessage, not in C struct)
  [u16; 260]  path_buffer   520 bytes
  u32         process_id      4 bytes
  u32         desired_access  4 bytes
  u32         reserved        4 bytes
  Total payload (excl. header): 532 bytes
  Static assert: size_of::<NonoIpcRequest>() - size_of::<FILTER_MESSAGE_HEADER>() == 532
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create nono-fltmgr-client Cargo workspace member + NonoIpcRequest layout assertion + CLI binary</name>
  <files>
    Cargo.toml,
    crates/nono-fltmgr-client/Cargo.toml,
    crates/nono-fltmgr-client/src/lib.rs,
    crates/nono-fltmgr-client/src/main.rs
  </files>
  <read_first>
    - Cargo.toml (root) lines 1-10: current workspace members block (MUST update)
    - crates/nono-shell-broker/Cargo.toml: workspace-member inheritance pattern to replicate
    - crates/nono/src/sandbox/windows.rs: #[cfg(windows)] module header + SAFETY: comment discipline
    - .planning/phases/64-.../64-PATTERNS.md §nono-fltmgr-client/Cargo.toml and §lib.rs: exact patterns to follow
    - .planning/phases/64-.../64-RESEARCH.md Pattern 3: NonoIpcRequest field layout + static-assert formula
  </read_first>
  <action>
    1. In root Cargo.toml, insert `"crates/nono-fltmgr-client"` into `[workspace] members` in
       alphabetical position between `"crates/nono-cli"` and `"crates/nono-proxy"`.
       No other changes to root Cargo.toml.

    2. Create `crates/nono-fltmgr-client/Cargo.toml` mirroring the nono-shell-broker pattern
       (PATTERNS.md §Cargo.toml):
       - name = "nono-fltmgr-client"
       - version = "0.62.2" (current workspace version — match other crates)
       - description = "Spike: user-mode policy client for nono minifilter IPC (Windows only)"
       - publish = false  (spike crate — NOT for crates.io)
       - [lib] name = "nono_fltmgr_client" path = "src/lib.rs"
       - [[bin]] name = "nono_fltmgr_client" path = "src/main.rs"
         (the binary shares the crate name; it is the runnable entry point for the VM DRV-02 proof)
       - [target.'cfg(windows)'.dependencies] windows-sys = { version = "0.59", features =
           ["Win32_Foundation", "Win32_Storage_InstallableFileSystems"] }
       - [lints] workspace = true  (inherits clippy::unwrap_used = deny)
       - All workspace package fields (edition, rust-version, authors, license, repository,
         homepage) use .workspace = true

    3. Create `crates/nono-fltmgr-client/src/lib.rs`:
       - Module-level doc comment explaining: Windows-only spike crate, compiles to empty on
         Linux/macOS, Phase 64 DRV-02, connects to \NonoPolicyPort.
       - Top-level `#[cfg(not(windows))]` empty stub (compile-to-nothing guarantee on non-Windows
         per PATTERNS.md §cfg guard discipline; a brief doc comment explaining why).
       - `#[cfg(windows)]` module `client` containing:
         a. Imports grouped by Win32 sub-module (PATTERNS.md §windows_sys import organization):
            - windows_sys::Win32::Foundation::{CloseHandle, GetLastError, HANDLE, INVALID_HANDLE_VALUE}
            - windows_sys::Win32::Storage::InstallableFileSystems::{
                FilterConnectCommunicationPort, FilterGetMessage, FilterReplyMessage,
                FILTER_MESSAGE_HEADER, FILTER_REPLY_HEADER }
         b. `#[repr(C, packed(1))]` struct `NonoIpcRequest` with fields in this order:
            - header: FILTER_MESSAGE_HEADER  (MUST be first — FilterGetMessage writes it as prefix)
            - path_buffer: [u16; 260]        (MAX_PATH WCHARs — 520 bytes payload)
            - process_id: u32               (4 bytes)
            - desired_access: u32           (4 bytes)
            - reserved: u32                 (4 bytes spike padding)
            The payload (excluding header) is 532 bytes.
         c. Compile-time layout assertion (prefer const _: () over unit test — PATTERNS.md
            §compile-time size assertions):
            `const _: () = assert!(std::mem::size_of::<NonoIpcRequest>()
                - std::mem::size_of::<FILTER_MESSAGE_HEADER>() == 532,
                "NonoIpcRequest payload size mismatch with C-side NONO_IPC_REQUEST");`
         d. `#[repr(C, packed(1))]` struct `NonoIpcReply` with field: decision: u32
            (0 = allow, 1 = deny).
         e. Pub re-export at the crate root: `pub use client::{NonoIpcRequest, NonoIpcReply};`
            (only under #[cfg(windows)]).
       - No .unwrap() or .expect() in production code — CLAUDE.md Unwrap Policy. The test module
         may use .unwrap() inside `#[allow(clippy::unwrap_used)]`.
       - No fenced code blocks in this action — see patterns for exact struct/import shapes.
       - SAFETY: comments are mandatory on every unsafe block (PATTERNS.md §SAFETY comment
         convention). No unsafe blocks needed in this scaffold task; they come in Plan 03.

    4. Create `crates/nono-fltmgr-client/src/main.rs`:
       This is the minimal CLI wrapper for the VM DRV-02 round-trip proof (Plan 04 Step 5).
       It calls `run_policy_client` (implemented in Plan 03 Task 1) with the deny-target path
       passed as the first CLI argument, then runs until Ctrl-C or the port disconnects.

       The file must be guarded with `#[cfg(windows)]` at the module level so it compiles to
       nothing on Linux/macOS CI (matching the lib.rs cfg-guard discipline).

       Implementation (ALL code inside `#[cfg(windows)] fn main()`):
       - Parse argv[1] as the deny_path string. If no argument is provided, print a usage line
         to stderr and exit with a non-zero code. Do NOT use .unwrap() or .expect() — use
         match or if-let with an explicit error message printed to stderr and std::process::exit(1).
       - Call `nono_fltmgr_client::run_policy_client(&deny_path)` (the function is a stub at
         this point — Plan 03 fills it in).  Match on the Result: on Err print the error to
         stderr and std::process::exit(1); on Ok return normally.
       - On non-Windows platforms, provide a `#[cfg(not(windows))] fn main()` that prints
         "nono_fltmgr_client is Windows-only" to stderr and exits with 1.

       No .unwrap() or .expect() anywhere. No unsafe blocks needed.
  </action>
  <verify>
    <automated>
      cargo build -p nono-fltmgr-client
      cargo clippy -p nono-fltmgr-client -- -D warnings -D clippy::unwrap_used
    </automated>
    On Windows host: both commands exit 0; nono_fltmgr_client.exe appears in target/debug/ (or
    target/release/ with --release). On Linux/macOS CI: cargo build exits 0 (cfg-guarded stubs).
  </verify>
  <done>
    `cargo build -p nono-fltmgr-client` succeeds on the Windows dev host with 0 warnings and 0 errors.
    `Cargo.toml` workspace members list includes `"crates/nono-fltmgr-client"`.
    `NonoIpcRequest` is defined with `#[repr(C, packed(1))]` and the 532-byte payload static assert is
    present and compiles. `publish = false` is set. `[lints] workspace = true` is present.
    `src/main.rs` exists; `cargo build` produces `nono_fltmgr_client.exe` on Windows.
    No `.unwrap()` or `.expect()` in `src/main.rs` or production (non-test) code of `src/lib.rs`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Wave 0 macOS ordering test stubs (RED state) to macos.rs</name>
  <files>
    crates/nono/src/sandbox/macos.rs
  </files>
  <read_first>
    - crates/nono/src/sandbox/macos.rs lines 852-870: test module header + first tests (mod tests structure)
    - crates/nono/src/sandbox/macos.rs lines 997-1030: test_generate_profile_platform_rules_between_reads_and_writes (existing test with WRONG assertion to update)
    - crates/nono/src/sandbox/macos.rs lines 1842-1876: test_generate_profile_gpu_rules_between_reads_and_writes (closest analog for new D-11 test shape)
    - crates/nono/src/sandbox/macos.rs lines 1147-1160: test_generate_profile_denies_keychain_mach_by_default (contains() assertion style for symlink test)
    - .planning/phases/64-.../64-PATTERNS.md §macos.rs: exact test patterns with field names
    - .planning/phases/64-.../64-RESEARCH.md §D-11 Ordering Test Requirements: test body specs
  </read_first>
  <action>
    This task adds/updates EXACTLY THREE tests in the existing `mod tests` block inside
    `crates/nono/src/sandbox/macos.rs`. Do NOT rewrite the file; edit the test module only.

    1. UPDATE existing test `test_generate_profile_platform_rules_between_reads_and_writes`
       (currently at line ~998):
       - The test setup (CapabilitySet construction, generate_profile call) is CORRECT — keep it.
       - Change ONLY the assertions: replace the pre-fix assertion pair
         (`assert!(read_pos < deny_pos)` AND `assert!(deny_pos < write_pos)`) with
         the post-fix assertion pair:
         `assert!(read_pos < write_pos, "read rules must come before write rules");`
         `assert!(write_pos < deny_pos, "platform deny rules must come AFTER write rules (last-match-wins)");`
       - Rename the test to `test_generate_profile_platform_rules_after_writes` to reflect the
         corrected invariant (the old name "between_reads_and_writes" would be misleading).
       - This test MUST FAIL with the current (pre-cherry-pick) macos.rs code — that is the
         intended Wave 0 RED state for Track B.

    2. ADD new test `test_platform_rules_after_write_allows` (D-11 ordering — per PATTERNS.md
       §ordering test pattern, modeled on test_generate_profile_gpu_rules_between_reads_and_writes):
       - Build a CapabilitySet with one ReadWrite FsCapability for path "/test" and one platform
         rule "(deny file-write-unlink)".
       - Call generate_profile(&caps).unwrap() (allowed inside test module).
       - Find read_pos via profile.find("(allow file-read* (subpath \"/test\"))").expect(...)
       - Find write_pos via profile.find("(allow file-write* (subpath \"/test\"))").expect(...)
       - Find deny_pos via profile.find("(deny file-write-unlink)").expect(...)
       - Assert: write_pos < deny_pos (deny comes AFTER write — post-fix invariant).
       - Assert: read_pos < write_pos (reads before writes — invariant should always hold).
       - This test MUST FAIL with the current macos.rs code (deny is currently between reads and
         writes, so deny_pos < write_pos, violating write_pos < deny_pos).

    3. ADD new test `test_platform_deny_symlink_and_canonical_path` (D-11 symlink+canonical
       coverage — per PATTERNS.md §symlink+canonical test pattern):
       - Build a CapabilitySet (no FsCapabilities needed).
       - Add TWO platform rules: "(deny file-read* (literal \"/etc/passwd\"))" AND
         "(deny file-read* (literal \"/private/etc/passwd\"))".
       - Call generate_profile(&caps).unwrap().
       - Assert profile.contains("(deny file-read* (literal \"/etc/passwd\"))").
       - Assert profile.contains("(deny file-read* (literal \"/private/etc/passwd\"))").
       - This test SHOULD PASS immediately (it only checks that platform rules added by the
         caller are emitted — it validates the dual-path discipline required by D-11/Pitfall 11).

    All three tests go inside the existing `#[cfg(test)] #[allow(clippy::unwrap_used)] mod tests`
    block. Use the `.find().expect()` style (not `.find().unwrap()`) per PATTERNS.md §ordering
    test assertion style.

    Do NOT modify any non-test code in this task. The cherry-pick task (Plan 03) moves the
    platform_rules() loop; this task only prepares the RED test state.
  </action>
  <verify>
    <automated>
      cargo test -p nono -- sandbox::macos::tests::test_generate_profile_platform_rules_after_writes 2>&1 | tail -20
      cargo test -p nono -- sandbox::macos::tests::test_platform_rules_after_write_allows 2>&1 | tail -20
      cargo test -p nono -- sandbox::macos::tests::test_platform_deny_symlink_and_canonical_path 2>&1 | tail -20
    </automated>
    Expected: tests 1 and 2 FAIL (RED — pre-cherry-pick macos.rs has deny between reads and writes,
    not after writes). Test 3 SHOULD pass (it asserts emitted platform rules, not ordering).
    Confirm all three tests exist (grep for their names in macos.rs).
  </verify>
  <done>
    Three test functions exist in macos.rs tests module:
    - `test_generate_profile_platform_rules_after_writes` (renamed + updated assertions)
    - `test_platform_rules_after_write_allows` (new)
    - `test_platform_deny_symlink_and_canonical_path` (new)
    The first two tests fail when run against the current (pre-cherry-pick) macos.rs code.
    The third test passes. cargo build -p nono succeeds (test compilation is valid).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust compile-time → runtime | Layout assertion `const _: () = assert!()` prevents size mismatch between Rust and C structs from reaching the VM |
| Cargo workspace → dev host CI | `#[cfg(not(windows))]` empty stub prevents Linux/macOS CI from failing on Windows-only imports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-SC-01 | Tampering | NonoIpcRequest layout (Rust/C ABI boundary) | mitigate | `const _: () = assert!(size_of::<NonoIpcRequest>() - size_of::<FILTER_MESSAGE_HEADER>() == 532)` — fails at compile time if struct layout diverges from the 532-byte C-side NONO_IPC_REQUEST |
| T-64-SC-02 | Denial of Service | cargo build on Linux/macOS CI | mitigate | `#[cfg(not(windows))]` empty stub ensures the crate compiles to nothing on Unix CI targets — no missing imports, no build failures |
| T-64-SC-03 | Tampering | Seatbelt deny-after-allow ordering (RESEARCH.md Pitfall 10) | mitigate | Wave 0 RED tests assert `write_pos < deny_pos`; test 1 and 2 must fail until cherry-pick lands, preventing a false-green close |
| T-64-SC | Tampering | npm/pip/cargo installs | mitigate | No new registry packages introduced (windows-sys 0.59 is existing; feature flag addition only per RESEARCH.md Package Legitimacy Audit) |
</threat_model>

<verification>
Wave 1 completion gates:
1. `cargo build -p nono-fltmgr-client` exits 0 on Windows dev host
2. Root `Cargo.toml` workspace members contains `"crates/nono-fltmgr-client"`
3. `crates/nono-fltmgr-client/Cargo.toml` has `publish = false` and `[lints] workspace = true`
4. `NonoIpcRequest` static assert present and compiles
5. `src/main.rs` exists; `nono_fltmgr_client.exe` produced by `cargo build` on Windows
6. `test_generate_profile_platform_rules_after_writes` exists in macos.rs and FAILS (pre-cherry-pick)
7. `test_platform_rules_after_write_allows` exists in macos.rs and FAILS (pre-cherry-pick)
8. `test_platform_deny_symlink_and_canonical_path` exists in macos.rs and PASSES
9. `cargo clippy -p nono-fltmgr-client -- -D warnings -D clippy::unwrap_used` passes
</verification>

<success_criteria>
- `cargo build -p nono-fltmgr-client` succeeds; workspace `Cargo.lock` updated
- `cargo test -p nono -- sandbox::macos::tests::test_platform_deny_symlink_and_canonical_path` PASSES
- `cargo test -p nono -- sandbox::macos::tests::test_platform_rules_after_write_allows` FAILS (RED — intended)
- `cargo test -p nono -- sandbox::macos::tests::test_generate_profile_platform_rules_after_writes` FAILS (RED — intended)
- `src/main.rs` exists with `#[cfg(windows)]`-guarded main that accepts a deny-path argument
- No `.unwrap()` or `.expect()` in production (non-test) code of `nono-fltmgr-client/src/lib.rs` or `src/main.rs`
- DCO sign-off on commit: `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`
</success_criteria>

<output>
Create `.planning/phases/64-minifilter-spike-implementation-macos-p1-cherry-pick-wave/64-01-SUMMARY.md` when done.
</output>
