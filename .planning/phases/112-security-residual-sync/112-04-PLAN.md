---
phase: 112-security-residual-sync
plan: 04
type: execute
wave: 2
depends_on: ["112-01"]
files_modified:
  - crates/nono-cli/src/pty_proxy.rs
  - crates/nono-cli/src/timeouts.rs
  - crates/nono/src/supervisor/socket.rs
  - crates/nono-cli/tests/socket_access_run.rs
autonomous: true
requirements: [RES-02]

must_haves:
  truths:
    - "RES-02 (503045801a): a late cursor-position-report reply (ESC[<row>;<col>R) left by an exiting TUI child is drained on final PTY teardown via discard_late_terminal_input, so it is never pasted into the next shell prompt; restore_terminal() itself stays non-draining so suspend/resume type-ahead is preserved"
    - "RES-02 (4cc0af2c): the /tmp-vs-target/ socket-test-tempdir change is SKIPPED with recorded reasoning — crates/nono/src/supervisor/socket.rs's existing doc comment explicitly documents choosing target/ to avoid macOS Seatbelt denials on /var/folders, and this project validates macOS only via cross-target clippy + CI, never a live host, so switching is not positively confirmed safe (RESEARCH.md Assumption A2, unresolved)"
    - "RES-02 (9840a16f): crates/nono-cli/tests/socket_access_run.rs's post-run save-prompt is suppressed in test runs (NONO_NO_SAVE_PROMPT=1) and its denial assertion is tightened from a broad string-contains check to a specific denial-marker match"
  artifacts:
    - path: "crates/nono-cli/src/pty_proxy.rs"
      provides: "CprReplyParse state machine + discard_late_terminal_input teardown drain"
    - path: "crates/nono-cli/src/timeouts.rs"
      provides: "TERMINAL_QUERY_REPLY_TIMEOUT = 30ms constant"
  key_links:
    - from: "crates/nono-cli/src/pty_proxy.rs (final teardown path)"
      to: "crates/nono-cli/src/pty_proxy.rs::discard_late_terminal_input"
      via: "direct call immediately before restore_terminal(), while still in raw mode"
      pattern: "discard_late_terminal_input"
---

<objective>
Review and disposition RES-02's three commits individually per D-03 (skip-biased default,
absorb only where diff-proven to apply): adopt `503045801a`'s genuinely new, additive PTY teardown
drain (prevents a stray terminal reply from being pasted into the next shell prompt); explicitly
SKIP `4cc0af2c`'s `/tmp` test-tempdir switch (a real, documented macOS Seatbelt risk this fork's own
code comment already warns against, per RESEARCH.md Assumption A2); adopt `9840a16f`'s trivial,
zero-production-risk test-quality tightening.

Purpose: RES-02 is "individually reviewed and absorbed or explicitly skipped with recorded
reasoning — never silently dropped" per ROADMAP SC2. This plan delivers exactly that for all 3 sub-items.
Output: `pty_proxy.rs`/`timeouts.rs` gain the teardown drain; `socket.rs` is explicitly UNCHANGED
with reasoning recorded; `tests/socket_access_run.rs` gets the assertion tightening.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-security-residual-sync/112-CONTEXT.md
@.planning/phases/112-security-residual-sync/112-RESEARCH.md
@.planning/phases/112-security-residual-sync/112-PATTERNS.md
@.planning/phases/112-security-residual-sync/112-DISPOSITION-TABLE.md
@./CLAUDE.md
</context>

<interfaces>
Confirmed live 2026-08-05 via `git show 503045801a -- crates/nono-cli/src/pty_proxy.rs crates/nono-cli/src/timeouts.rs`:

```rust
// timeouts.rs — new constant
pub const TERMINAL_QUERY_REPLY_TIMEOUT: Duration = Duration::from_millis(30);

// pty_proxy.rs — new state machine + drain function
#[derive(Clone, Copy)]
enum CprReplyParse { NeedEsc, NeedBracket, InParams }
impl CprReplyParse {
    fn step(&mut self, byte: u8) -> bool { /* ESC [ <digits/;>* terminator=R */ }
}
fn discard_late_terminal_input(fd: RawFd, max_wait: Duration) {
    // isatty guard -> drain_terminal_output(fd) -> poll-with-deadline byte-at-a-time read loop
    // using CprReplyParse::step; stops at terminator or first non-reply byte (leaves type-ahead
    // for the shell). See full body via `git show 503045801a -- crates/nono-cli/src/pty_proxy.rs`.
}
```

Call site: inserted immediately before `self.restore_terminal();` on the FINAL teardown path only
(confirmed via the diff hunk context around `crates/nono-cli/src/pty_proxy.rs:445` in upstream's
numbering — locate the fork's own final-teardown call site by finding `restore_terminal()`'s
caller that is NOT the suspend/resume path).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RES-02 adopt — late CPR-reply teardown drain</name>
  <files>crates/nono-cli/src/pty_proxy.rs, crates/nono-cli/src/timeouts.rs</files>
  <behavior>
    - `CprReplyParse::step` returns `true` while consuming a well-formed `ESC [ <digits/;>* ` prefix,
      and `false` exactly at the `R` terminator or the first byte that cannot extend the grammar.
    - `discard_late_terminal_input` is a no-op when `fd` is not a terminal (`isatty` != 1).
    - Test: feed the byte sequence `[0x1b, b'[', b'3', b';', b'1', b'R']` through `CprReplyParse`
      step-by-step; assert `true` for the first 5 bytes and `false` for the final `R`.
    - Test: feed a non-CPR byte first (e.g. a real keystroke `b'a'`) after `NeedEsc`; assert `step`
      returns `false` immediately (type-ahead is left alone, not scanned for a stray `R`).
  </behavior>
  <read_first>
    `git show 503045801a -- crates/nono-cli/src/pty_proxy.rs crates/nono-cli/src/timeouts.rs`
    (full diff, primary source — includes the exact `discard_late_terminal_input` body, poll-based
    byte-at-a-time read loop, and EINTR-retry convention); `crates/nono-cli/src/pty_proxy.rs:1495-1513`
    (`drain_terminal_output`, the isatty-guard/EINTR-retry pattern to imitate); the current
    `restore_terminal()` call site(s) — grep `fn restore_terminal` and every caller, to identify
    which one is the FINAL teardown path (not suspend/resume); current `crates/nono-cli/src/timeouts.rs`
    (existing constants, to place the new one in the same style/doc-comment convention).
  </read_first>
  <action>
    Add `TERMINAL_QUERY_REPLY_TIMEOUT: Duration = Duration::from_millis(30)` to `timeouts.rs`
    following the existing doc-comment style of neighboring constants. Add the `CprReplyParse` enum
    and `discard_late_terminal_input(fd: RawFd, max_wait: Duration)` function to `pty_proxy.rs`,
    porting the exact byte-grammar and poll/EINTR-retry loop from the upstream diff. Insert the call
    `discard_late_terminal_input(libc::STDIN_FILENO, timeouts::TERMINAL_QUERY_REPLY_TIMEOUT);`
    immediately before the final-teardown call to `self.restore_terminal();` — confirmed by reading
    the current file to locate the correct call site (the one NOT reached by the suspend/resume
    path). Add `CprReplyParse`/`discard_late_terminal_input` to the test module's `use super::{...}`
    import list per the diff.
  </action>
  <acceptance_criteria>
    `grep -n "TERMINAL_QUERY_REPLY_TIMEOUT" crates/nono-cli/src/timeouts.rs` returns 1 hit;
    `grep -n "fn discard_late_terminal_input" crates/nono-cli/src/pty_proxy.rs` returns 1 hit;
    the new unit tests for `CprReplyParse::step` pass; `restore_terminal()`'s own body remains
    byte-for-byte unchanged (confirm via `git diff` showing no hunk inside that function).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-cli --lib -- pty_proxy:: 2>&1 | tail -25</automated>
  </verify>
  <done>New drain mechanism + tests land; restore_terminal() itself is unmodified; workspace tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: RES-02 skip decision (socket.rs) + RES-02 adopt (test tightening)</name>
  <files>crates/nono/src/supervisor/socket.rs, crates/nono-cli/tests/socket_access_run.rs</files>
  <read_first>
    `git show 4cc0af2c -- crates/nono/src/supervisor/socket.rs` (the proposed `/tmp` switch);
    current `crates/nono/src/supervisor/socket.rs`'s `socket_test_dir()`-equivalent function and its
    existing doc comment explaining the `target/`-not-`/tmp` choice (read the FULL comment before
    deciding — this is the load-bearing evidence for the skip); `git show 9840a16f -- crates/nono-cli/tests/socket_access_run.rs`
    (the trivial, safe test-only change); 112-RESEARCH.md's Assumption A2 (`/tmp` Seatbelt safety
    is UNVERIFIED — this project validates macOS only via cross-target CI, never a live host).
  </read_first>
  <action>
    For `4cc0af2c`: read `crates/nono/src/supervisor/socket.rs`'s existing tempdir-choice doc
    comment in full. Confirm it explicitly cites avoiding macOS Seatbelt `deny network*` denials on
    `/var/folders` (the macOS `TMPDIR` default) as the reason for the current `target/`-relative
    choice. Since RESEARCH.md's Assumption A2 (whether `/tmp` is Seatbelt-safe) is explicitly
    unverified and this project cannot verify it live (no macOS host), make NO code change to this
    file — disposition = SKIP. Record the skip reasoning as a doc comment addition immediately above
    the existing tempdir-choice comment (a single new comment line citing this plan's decision and
    RESEARCH.md's Assumption A2, not a functional change) so a future auditor with macOS access can
    revisit it. For `9840a16f`: port the diff into `tests/socket_access_run.rs` verbatim — set
    `NONO_NO_SAVE_PROMPT=1` in the env of the relevant test's `run_nono()`/subprocess invocation, and
    tighten its assertion from a broad string-contains check to the specific denial-marker match the
    diff shows.
  </action>
  <acceptance_criteria>
    `git diff --stat -- crates/nono/src/supervisor/socket.rs` shows ONLY a comment-line addition (no
    functional/logic change — the tempdir path construction itself is byte-for-byte unchanged);
    `grep -n "NONO_NO_SAVE_PROMPT" crates/nono-cli/tests/socket_access_run.rs` returns &gt;= 1 hit;
    the tightened assertion test still passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-cli --test socket_access_run -- af_unix_mediation_pathname_allows_connect_to_listed_socket 2>&1 | tail -15</automated>
  </verify>
  <done>socket.rs has zero functional diff (comment-only); socket_access_run.rs's 9840a16f change lands and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Exiting TUI child's terminal query reply -> next shell prompt | An un-drained cursor-position reply is pasted as literal input into the next interactive command |
| Test-only tempdir path choice -> macOS Seatbelt test isolation | A tempdir switch that is fine on Linux could silently reintroduce macOS Seatbelt test denials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-112-10 | Tampering (of the next shell command) | `crates/nono-cli/src/pty_proxy.rs` final teardown | mitigate | `discard_late_terminal_input` drains the stray CPR reply before it can be pasted into the shell prompt; grammar-bounded so real type-ahead is left alone |
| T-112-11 | Denial of Service (test flake, not production) | `crates/nono/src/supervisor/socket.rs` test tempdir | accept-via-skip | SKIP `4cc0af2c`'s `/tmp` switch; the fork's existing `target/`-relative choice's Seatbelt-avoidance rationale is preserved unchanged since it cannot be positively re-verified without a live macOS host |

Package Legitimacy Gate: N/A — no new `Cargo.toml` dependencies.
</threat_model>

<verification>
`cargo test -p nono-sandbox-cli --lib -- pty_proxy::` passes (new `CprReplyParse` tests).
`cargo test -p nono-sandbox-cli --test socket_access_run` passes. `git diff -- crates/nono/src/supervisor/socket.rs`
shows only a comment addition, zero functional change.
</verification>

<success_criteria>
- `discard_late_terminal_input`/`CprReplyParse` land in `pty_proxy.rs`, called only on the final teardown path.
- `socket.rs`'s `/tmp` switch is explicitly SKIPPED with recorded reasoning; zero functional diff.
- `9840a16f`'s test-quality tightening lands and passes.
</success_criteria>

<output>
Create `.planning/phases/112-security-residual-sync/112-04-SUMMARY.md` when done
</output>
