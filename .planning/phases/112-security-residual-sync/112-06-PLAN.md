---
phase: 112-security-residual-sync
plan: 06
type: execute
wave: 3
depends_on: ["112-02", "112-04"]
files_modified:
  - crates/nono-cli/src/exec_strategy.rs
  - crates/nono-cli/tests/socket_access_run.rs
autonomous: true
requirements: [SEC-06]

must_haves:
  truths:
    - "D-06: SEC-06 verified via cross test --target x86_64-unknown-linux-gnu plus both mandatory cross-target clippy gates — no live-kernel UAT checkpoint"
    - "SEC-06: the supervisor sets PR_SET_CHILD_SUBREAPER before forking whenever it may need to read a descendant's /proc/<pid>/mem — gated on the fork's existing linux_child_requires_dumpable(capability_elevation, seccomp_proxy_fallback || af_unix_mediation.is_pathname()) predicate, with upstream's tool_sandbox_runtime disjunct dropped entirely (confirmed absent from this fork, D-01)"
    - "SEC-06: reap_reparented_orphans(child) is called inside the Linux-only run_supervisor_loop (crates/nono-cli/src/exec_strategy.rs, the #[cfg(target_os = \"linux\")] variant at the fn's own definition, NOT the #[cfg(not(target_os = \"linux\"))] variant), immediately after handle_pty_suspension and before the primary waitpid(child, WNOHANG) call — resolving RESEARCH.md's Assumption A3 with certainty via direct read of both cfg-gated definitions"
  artifacts:
    - path: "crates/nono-cli/src/exec_strategy.rs"
      provides: "PR_SET_CHILD_SUBREAPER pre-fork setup + reap_reparented_orphans() + its call site in the Linux run_supervisor_loop"
  key_links:
    - from: "crates/nono-cli/src/exec_strategy.rs::execute_supervised (pre-fork, parent)"
      to: "libc::prctl(PR_SET_CHILD_SUBREAPER, ...)"
      via: "gated on linux_child_requires_dumpable(...)"
      pattern: "PR_SET_CHILD_SUBREAPER"
    - from: "crates/nono-cli/src/exec_strategy.rs::run_supervisor_loop (Linux cfg arm)"
      to: "crates/nono-cli/src/exec_strategy.rs::reap_reparented_orphans"
      via: "direct call after handle_pty_suspension, before the primary waitpid"
      pattern: "reap_reparented_orphans"
---

<objective>
Absorb upstream `ac5ccd70` (#1401, "fix(sandbox): keep orphaned descendants in supervisor ancestry
for seccomp-notify mediation") — adapted, not a clean port, because upstream's gating predicate
(`config.tool_sandbox_runtime.is_some() || config.seccomp_policy.child_requires_dumpable()`)
references two fields/methods this fork does not have. Runs after 112-02 (shares `exec_strategy.rs`)
and after 112-04 (shares `tests/socket_access_run.rs`).

Purpose: under Yama `ptrace_scope=1`, a descendant that backgrounds/daemonizes and reparents to pid
1 leaves the supervisor's ancestry, and the supervisor's `ptrace_may_access`-gated
`/proc/<pid>/mem` reads for seccomp-notify classification (AF_UNIX mediation, network fallback) then
fail with EPERM even for otherwise-allowed traffic — a real availability/mediation-bypass-adjacent
bug closed by `PR_SET_CHILD_SUBREAPER` + reaping.
Output: `exec_strategy.rs` gains the subreaper setup + `reap_reparented_orphans()`, wired into the
Linux supervisor loop only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-security-residual-sync/112-CONTEXT.md
@.planning/phases/112-security-residual-sync/112-RESEARCH.md
@.planning/phases/112-security-residual-sync/112-PATTERNS.md
@.planning/phases/112-security-residual-sync/112-DISPOSITION-TABLE.md
@.planning/templates/cross-target-verify-checklist.md
@./CLAUDE.md
</context>

<interfaces>
Confirmed live 2026-08-05 (this planning session, exact fork line numbers, not upstream's):

- `crates/nono-cli/src/exec_strategy.rs:445-447` — the existing predicate to reuse:
  `const fn linux_child_requires_dumpable(capability_elevation: bool, network_notify: bool) -> bool { capability_elevation || network_notify }`
- `crates/nono-cli/src/exec_strategy.rs:1020-1026` — pre-fork parent setup, immediately before
  `let fork_result = unsafe { fork() };` (line 1026). This is the insertion point for the new
  `PR_SET_CHILD_SUBREAPER` block, matching upstream's own placement (pre-fork, common setup).
- `crates/nono-cli/src/exec_strategy.rs:2897` — `fn run_supervisor_loop(...)` under
  `#[cfg(not(target_os = "linux"))]` — do NOT add the reap call here.
- `crates/nono-cli/src/exec_strategy.rs:3111` — `fn run_supervisor_loop(...)` under
  `#[cfg(target_os = "linux")]` — the ONLY correct location for the reap call.
- `crates/nono-cli/src/exec_strategy.rs:3295` — inside the Linux `run_supervisor_loop`, the exact
  line `handle_pty_suspension(pty.as_deref_mut(), child);` immediately followed by
  `match waitpid(child, Some(WaitPidFlag::WNOHANG)) {` at `:3297`. The reap call goes between these
  two lines — confirmed to be the fork's structural analog of upstream's own hunk context
  (`handle_pty_suspension` -> insertion -> `match waitpid(...)`).

Upstream `ac5ccd70`'s exact `reap_reparented_orphans` body (port verbatim, from `git show ac5ccd70`):
```rust
#[cfg(target_os = "linux")]
fn reap_reparented_orphans(child: Pid) -> Option<WaitStatus> {
    loop {
        match waitpid(Some(Pid::from_raw(-1)), Some(WaitPidFlag::WNOHANG)) {
            Ok(status @ (WaitStatus::Exited(pid, _) | WaitStatus::Signaled(pid, _, _))) => {
                if pid == child { return Some(status); }
                debug!("Reaped reparented orphan {}", pid);
            }
            Ok(_) => return None,
            Err(nix::errno::Errno::EINTR) => continue,
            Err(nix::errno::Errno::ECHILD) => return None,
            Err(e) => { debug!("waitpid(-1) during orphan reap failed: {}", e); return None; }
        }
    }
}
```
Upstream's pre-fork gate (adapt, do not port verbatim — the predicate differs):
```rust
// upstream: if config.tool_sandbox_runtime.is_some() || config.seccomp_policy.child_requires_dumpable() {
// fork adaptation (drop the tool_sandbox_runtime disjunct — confirmed absent, D-01):
if linux_child_requires_dumpable(config.capability_elevation, config.seccomp_proxy_fallback || config.af_unix_mediation.is_pathname()) {
    let ret = unsafe { libc::prctl(libc::PR_SET_CHILD_SUBREAPER, 1, 0, 0, 0) };
    if ret != 0 {
        return Err(NonoError::SandboxInit(format!(
            "Failed to set PR_SET_CHILD_SUBREAPER for supervisor: {}",
            std::io::Error::last_os_error()
        )));
    }
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PR_SET_CHILD_SUBREAPER pre-fork setup + reap_reparented_orphans()</name>
  <files>crates/nono-cli/src/exec_strategy.rs</files>
  <behavior>
    - New Wave-0 unit test proving the orphan-reap mechanism's core loop logic: construct a scenario
      (or, if a live-fork-based test is impractical in this file's existing test-authoring style, a
      focused test on the gating predicate reuse — confirm `linux_child_requires_dumpable` continues
      to correctly gate both the existing `PR_SET_DUMPABLE` site at `:1524` and the new
      `PR_SET_CHILD_SUBREAPER` site with the SAME arguments, i.e. both call sites must agree on
      when subreaping/dumpable-preservation is required).
    - `reap_reparented_orphans` returns `Some(status)` only when the reaped pid equals the tracked
      `child`; any other reaped pid is logged and looped past without returning.
  </behavior>
  <read_first>
    `git show ac5ccd70` (full diff, primary source); `crates/nono-cli/src/exec_strategy.rs:990-1026`
    (the pre-fork setup region, full read before editing); `:1520-1544` (the existing
    `linux_child_requires_dumpable` call site and its `nix::sys::prctl`/fail-fast-and-kill-child
    convention to match for error handling style, though `PR_SET_CHILD_SUBREAPER` itself is a
    PARENT-side, pre-fork call using raw `libc::prctl`, matching upstream's own convention — do not
    conflate the two call sites); `:3280-3297` (the exact Linux `run_supervisor_loop` insertion
    point, `handle_pty_suspension` -> insertion -> `waitpid`); `:4450-4457`
    (`test_linux_child_requires_dumpable_only_for_seccomp_driven_features`, the existing predicate
    test — confirm it still passes unmodified, it is not touched by this absorb).
  </read_first>
  <action>
    Insert the adapted `PR_SET_CHILD_SUBREAPER` block from `&lt;interfaces&gt;` above into
    `execute_supervised`, gated `#[cfg(target_os = "linux")]`, placed immediately before
    `let fork_result = unsafe { fork() };` at `:1026` (after "Clear any stale forwarding target
    before forking."). Use `linux_child_requires_dumpable(config.capability_elevation,
    config.seccomp_proxy_fallback || config.af_unix_mediation.is_pathname())` as the gate — the
    SAME predicate arguments already used at `:1524-1527` — dropping upstream's `tool_sandbox_runtime`
    disjunct entirely (confirmed absent from this fork per D-01/SEC-09). Port
    `reap_reparented_orphans(child: Pid) -> Option&lt;WaitStatus&gt;` verbatim into the file, placed
    near the Linux `run_supervisor_loop` (`:3111`+) per upstream's own placement. Add the call
    `if let Some(status) = reap_reparented_orphans(child) { return Ok((status, denials,
    ipc_denials)); }` between `:3295`'s `handle_pty_suspension(...)` and `:3297`'s
    `match waitpid(child, ...)`. Do NOT add anything to the `#[cfg(not(target_os = "linux"))]`
    `run_supervisor_loop` at `:2897`.
  </action>
  <acceptance_criteria>
    `grep -n "PR_SET_CHILD_SUBREAPER" crates/nono-cli/src/exec_strategy.rs` returns &gt;= 1 hit inside
    `execute_supervised`, before the `fork()` call; `grep -n "fn reap_reparented_orphans"
    crates/nono-cli/src/exec_strategy.rs` returns exactly 1 hit; `grep -n "reap_reparented_orphans(child)"
    crates/nono-cli/src/exec_strategy.rs` shows the call site is inside the `#[cfg(target_os =
    "linux")]` `run_supervisor_loop` (the one starting at the file's `:3111`-region definition), NOT
    the `#[cfg(not(target_os = "linux"))]` one; `test_linux_child_requires_dumpable_only_for_seccomp_driven_features`
    still passes unmodified.
  </acceptance_criteria>
  <verify>
    <automated>cross test --target x86_64-unknown-linux-gnu -p nono-sandbox-cli -- test_linux_child_requires_dumpable_only_for_seccomp_driven_features 2>&1 | tail -15</automated>
  </verify>
  <done>Subreaper setup + reap function land; call site confirmed Linux-only; existing predicate test unmodified and passing.</done>
</task>

<task type="auto">
  <name>Task 2: Wave-0 orphan-reap integration test + cross-target verification</name>
  <files>crates/nono-cli/tests/socket_access_run.rs</files>
  <read_first>
    `crates/nono-cli/tests/socket_access_run.rs:1-58` (the `#![cfg(any(target_os = "linux",
    target_os = "macos"))]` gate, `nono_bin()`/`run_nono()` subprocess harness, `short_tempdir()`
    convention — this file is ALSO edited by `112-04`'s `9840a16f` change in an earlier wave; read
    its CURRENT state, not an assumption of its pre-112-04 shape); `git show ac5ccd70 -- tests/socket_access_run.rs`
    (upstream's own regression-test addition for this fix, if the diffstat includes one — confirm
    via `git show ac5ccd70 --stat`); `.planning/templates/cross-target-verify-checklist.md`.
  </read_first>
  <action>
    Add a new integration test to `socket_access_run.rs` (following the file's existing
    `nono_bin()`/`run_nono()` subprocess-harness pattern) that exercises a scenario requiring
    seccomp-notify mediation of a backgrounding/daemonizing descendant under
    `--af-unix-mediation`/network-notify config, asserting the run completes without an EPERM
    seccomp-notify denial that would indicate the descendant escaped supervisor ancestry. If a true
    daemonizing-descendant integration test is impractical to author deterministically in CI, instead
    port upstream's own `ac5ccd70`-added test (confirm via `git show ac5ccd70 -- tests/socket_access_run.rs`)
    verbatim, adapted only for the fork's config field names. Run both mandatory cross-target clippy
    gates per the checklist.
  </action>
  <acceptance_criteria>
    New test function exists in `socket_access_run.rs`, distinct from `112-04`'s `9840a16f`-tightened
    test; both cross-target clippy gates exit 0 with zero findings.
  </acceptance_criteria>
  <verify>
    <automated>cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used 2>&1 | tail -10</automated>
  </verify>
  <done>New orphan-reap regression test lands; both cross-target clippy gates GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sandboxed child's backgrounding/daemonizing descendant | A descendant that reparents to pid 1 under Yama ptrace_scope=1 escapes the supervisor's ptrace-gated /proc/<pid>/mem read access, which the seccomp-notify AF_UNIX/network mediation depends on to classify the caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-112-13 | Elevation of Privilege | `crates/nono-cli/src/exec_strategy.rs` seccomp-notify mediation of reparented descendants | mitigate | `PR_SET_CHILD_SUBREAPER` (adapted gate: `linux_child_requires_dumpable`) keeps such descendants in supervisor ancestry; `reap_reparented_orphans()` prevents them lingering as zombies |
| T-112-14 | Denial of Service | Unreaped zombie processes from reparented orphans | mitigate | `reap_reparented_orphans()` drains `waitpid(-1, WNOHANG)` each supervisor-loop iteration |

Package Legitimacy Gate: N/A — no new `Cargo.toml` dependencies.
</threat_model>

<verification>
`cross test --target x86_64-unknown-linux-gnu -p nono-sandbox-cli -- test_linux_child_requires_dumpable_only_for_seccomp_driven_features`
passes. Both mandatory cross-target clippy gates GREEN. New orphan-reap test in
`socket_access_run.rs` passes.
</verification>

<success_criteria>
- `PR_SET_CHILD_SUBREAPER` set pre-fork, gated on the fork's existing `linux_child_requires_dumpable` predicate.
- `reap_reparented_orphans()` wired ONLY into the Linux `run_supervisor_loop` (A3 resolved with certainty).
- Both cross-target clippy gates GREEN; new Wave-0 test passes.
</success_criteria>

<output>
Create `.planning/phases/112-security-residual-sync/112-06-SUMMARY.md` when done
</output>
