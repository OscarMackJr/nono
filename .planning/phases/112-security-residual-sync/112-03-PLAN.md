---
phase: 112-security-residual-sync
plan: 03
type: execute
wave: 2
depends_on: ["112-01"]
files_modified:
  - crates/nono/src/trust/types.rs
  - crates/nono/src/trust/policy.rs
  - crates/nono/src/trust/mod.rs
  - crates/nono-cli/src/trust_cmd.rs
  - crates/nono-cli/src/trust_scan.rs
  - docs/cli/features/trust.mdx
  - tests/integration/test_trust_cli.sh
  - proj/ADR-112-allow-vars-fail-closed-preserved.md
autonomous: true
requirements: [SEC-04, SEC-08]

must_haves:
  truths:
    - "SEC-04: TrustPolicy gains a predicate: Option<String> field and TRUST_POLICY_PREDICATE = \"https://nono.sh/attestation/trust-policy/v1\" const; TRUST_POLICY_VERSION is marked #[deprecated(since = \"0.66.0\")]; a foreign trust-policy.json (no predicate field) is skipped with a warning instead of crashing on a full-parse schema error"
    - "D-05: SEC-08's disposition is the contingent ADR-112 escalation trigger D-05 names (an adopt that would loosen an existing fork guard) — the fork PRESERVES its existing fail-closed allow_vars semantics unchanged, recorded in proj/ADR-112-allow-vars-fail-closed-preserved.md, git added with -f (proj/ is gitignored but tracked)"
    - "SEC-08: crates/nono-cli/src/profile_runtime.rs's allowed_env_vars closure (:840-872) is untouched by this plan — zero diff — and empty_allow_vars_fails_closed (:989) still passes, proving no accidental regression toward upstream a5a441c2's opposite Option<Vec<String>>-None-means-allow-all default"
  artifacts:
    - path: "crates/nono/src/trust/types.rs"
      provides: "TRUST_POLICY_PREDICATE const + TrustPolicy.predicate field + deprecated TRUST_POLICY_VERSION"
    - path: "proj/ADR-112-allow-vars-fail-closed-preserved.md"
      provides: "The D-05 contingent-escalation ADR recording the decision to reject upstream a5a441c2's allow_vars reinterpretation"
  key_links:
    - from: "crates/nono-cli/src/trust_cmd.rs"
      to: "crates/nono/src/trust/types.rs::TRUST_POLICY_PREDICATE"
      via: "peek-before-full-parse check on load"
      pattern: "TRUST_POLICY_PREDICATE"
---

<objective>
Two independent, small/medium items batched per D-04's Claude's-Discretion allowance (no shared
files): (1) adopt SEC-04's trust-policy predicate discriminator so `trust-policy.json` files from
foreign tools (e.g. AWS IAM, which uses the identical filename) are skipped with a warning instead
of crashing nono on a schema-parse error; (2) settle SEC-08 by writing the one ADR this phase
requires — CONTEXT.md D-05's contingent trigger ("any adopt that loosens an existing fork guard")
has fired, per RESEARCH.md's finding that upstream `a5a441c2` reverses the fork's own deliberate
fail-closed `allow_vars` default. The correct action is to NOT adopt the code change and instead
record why in an ADR.

Purpose: close SEC-04 (genuine hardening, zero fork conflict) and close SEC-08 (genuine security
judgment call, zero code change, ADR-documented).
Output: `crates/nono/src/trust/types.rs` gains the predicate discriminator; `proj/ADR-112-allow-vars-fail-closed-preserved.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-security-residual-sync/112-CONTEXT.md
@.planning/phases/112-security-residual-sync/112-RESEARCH.md
@.planning/phases/112-security-residual-sync/112-PATTERNS.md
@.planning/phases/112-security-residual-sync/112-DISPOSITION-TABLE.md
@proj/ADR-111-resource-limits-boundary.md
@proj/ADR-108-deny-domain-posture.md
@./CLAUDE.md
</context>

<interfaces>
Confirmed live 2026-08-05 via `git show f943fb5a -- crates/nono/src/trust/types.rs`:

```rust
// New/changed items:
#[deprecated(since = "0.66.0", note = "versioning is encoded in TRUST_POLICY_PREDICATE; \
    this constant and the version field will be removed in v1.0.0")]
pub const TRUST_POLICY_VERSION: u32 = 1;

pub const TRUST_POLICY_PREDICATE: &str = "https://nono.sh/attestation/trust-policy/v1";

pub struct TrustPolicy {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub predicate: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub version: Option<u32>,
    // ...existing fields unchanged...
}
```

`crates/nono/src/trust/policy.rs`'s `merge_policies` changes to set
`predicate: Some(TRUST_POLICY_PREDICATE.to_string()), version: None` on the merged result (instead
of `version: policies.iter().map(|p| p.version).max().unwrap_or(1)`), and its test
`merge_rejects_unsupported_version` is renamed `merge_ignores_legacy_version_field` (merge no
longer errors on a version mismatch since version is deprecated/informational only — confirmed via
`git show f943fb5a -- crates/nono/src/trust/policy.rs`).

Confirmed live via `git show a5a441c2 -- crates/nono-cli/src/profile/mod.rs`: upstream changes
`EnvironmentConfig.allow_vars` from `Vec<String>` to `Option<Vec<String>>` with `None` = "allow
everything", `Some([])` = "block everything" — this is the semantic the fork must NOT adopt (its
current `Vec<String>` + always-`Some(...)`-when-`environment`-is-set closure at
`crates/nono-cli/src/profile_runtime.rs:840-872` already fail-closes correctly).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SEC-04 trust-policy predicate discriminator</name>
  <files>crates/nono/src/trust/types.rs, crates/nono/src/trust/policy.rs, crates/nono/src/trust/mod.rs, crates/nono-cli/src/trust_cmd.rs, crates/nono-cli/src/trust_scan.rs, docs/cli/features/trust.mdx, tests/integration/test_trust_cli.sh</files>
  <behavior>
    - A `trust-policy.json` with no `predicate` field is SKIPPED with a warning message suggesting
      `nono trust init --force` (not a full-parse crash).
    - A `trust-policy.json` with an unrecognized `predicate` value reports the unexpected value.
    - A `trust-policy.json` carrying only the legacy `version` field (no `predicate`) still parses
      cleanly for backward compatibility.
    - `nono trust init` no longer emits a `"version"` key in newly generated policy files.
  </behavior>
  <read_first>
    `git show f943fb5a` (full diff, all 7 files — primary source); the exact `types.rs` excerpt in
    `&lt;interfaces&gt;` above; current `crates/nono/src/trust/types.rs:9-80` (existing
    `TRUST_POLICY_VERSION`/`TrustPolicy`/`Default` impl, full read before editing);
    `crates/nono/src/trust/policy.rs`'s current `merge_policies` and its
    `merge_rejects_unsupported_version` test (both change per the diff).
  </read_first>
  <action>
    Port `f943fb5a`'s diff into `crates/nono/src/trust/types.rs` exactly as shown in
    `&lt;interfaces&gt;`: add `TRUST_POLICY_PREDICATE` const, add `predicate: Option&lt;String&gt;` field,
    change `version: u32` to `version: Option&lt;u32&gt;`, mark `TRUST_POLICY_VERSION`
    `#[deprecated(since = "0.66.0", ...)]`, update `Default for TrustPolicy` to set
    `predicate: Some(TRUST_POLICY_PREDICATE.to_string()), version: None`. Port `trust/policy.rs`'s
    `merge_policies` change and rename `merge_rejects_unsupported_version` to
    `merge_ignores_legacy_version_field` per the diff (merge no longer rejects a version mismatch).
    Port `trust_cmd.rs`/`trust_scan.rs`'s peek-before-full-parse loading logic (read the raw JSON,
    check for the `predicate` field before attempting full `TrustPolicy` deserialization) and the
    `nono trust init` generator change to stop emitting `"version"`. Port the `docs/cli/features/trust.mdx`
    and `tests/integration/test_trust_cli.sh` updates verbatim.
  </action>
  <acceptance_criteria>
    `grep -n "TRUST_POLICY_PREDICATE" crates/nono/src/trust/types.rs` returns &gt;= 1 hit;
    `grep -n "#\[deprecated" crates/nono/src/trust/types.rs` returns 1 hit on `TRUST_POLICY_VERSION`;
    `cargo test -p nono-sandbox --lib -- trust::` passes with zero failures, including the renamed
    `merge_ignores_legacy_version_field`.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox --lib -- trust:: 2>&1 | tail -20</automated>
  </verify>
  <done>Predicate discriminator lands; trust:: module tests pass; nono trust init no longer emits "version".</done>
</task>

<task type="auto">
  <name>Task 2: SEC-08 — ADR-112, preserve the fork's fail-closed allow_vars default</name>
  <files>proj/ADR-112-allow-vars-fail-closed-preserved.md</files>
  <read_first>
    `crates/nono-cli/src/profile_runtime.rs:840-872` (the exact fail-closed closure to preserve —
    do NOT edit this code in this task); `crates/nono-cli/src/profile_runtime.rs:975-1010`
    (`empty_allow_vars_fails_closed`, the regression guard); `proj/ADR-111-resource-limits-boundary.md`
    (the exact header/Context/Decision shape to mirror); 112-RESEARCH.md's SEC-08 table row and its
    "State of the Art" table entry contrasting the fork's vs upstream's default; CONTEXT.md D-05
    (the contingent ADR-112 escalation trigger this fires).
  </read_first>
  <action>
    Run `git show a5a441c2 -- crates/nono-cli/src/profile_runtime.rs crates/nono-cli/src/profile/mod.rs`
    to confirm the exact opposite-default semantics one more time immediately before writing the
    ADR. Write `proj/ADR-112-allow-vars-fail-closed-preserved.md` mirroring
    `ADR-111-resource-limits-boundary.md`'s Status/Phase/Date/Authors header and Context/Decision
    section shape: Context states upstream `a5a441c2`'s reinterpretation (`Vec&lt;String&gt;` ->
    `Option&lt;Vec&lt;String&gt;&gt;`, `None` = allow-all) and cites the fork's existing fail-closed
    resolution at `profile_runtime.rs:840-872` (provenance: "Plan 34-08a Task 3/5, D-20 manual
    replay of upstream `1b412a7`/`780965d7`"). Decision: **PRESERVE, do not adopt** — the fork's
    `allow_vars: Vec&lt;String&gt;` field and its always-`Some(...)`-when-`environment`-is-set
    resolution stay exactly as they are; zero source-code change accompanies this ADR. State
    explicitly that adopting upstream's reinterpretation verbatim would reopen the exact
    env-var-leak class the fork's Phase 34 fix closed. Reference
    `empty_allow_vars_fails_closed` as the durable regression guard against future accidental
    drift. Add the file with `git add -f proj/ADR-112-allow-vars-fail-closed-preserved.md`
    (`proj/` is gitignored but tracked — a plain `git add` exits 1).
  </action>
  <acceptance_criteria>
    File exists at `proj/ADR-112-allow-vars-fail-closed-preserved.md`; contains "PRESERVE, do not
    adopt" (or equivalent unambiguous decision language); `git status` shows it staged after
    `git add -f` (confirm via `git diff --cached --stat -- proj/ADR-112-allow-vars-fail-closed-preserved.md`
    showing a non-empty diff); `git diff -- crates/nono-cli/src/profile_runtime.rs` is EMPTY
    (zero source-code change).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-cli --lib -- empty_allow_vars_fails_closed 2>&1 | tail -10</automated>
  </verify>
  <done>ADR-112 written and force-added; empty_allow_vars_fails_closed still passes unchanged; profile_runtime.rs has zero diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Foreign trust-policy.json (e.g. AWS IAM) -> nono's trust loader | Untrusted, identically-named file must be distinguished from a real nono policy without a crash |
| Profile-declared environment.allow_vars -> sandboxed child's inherited env | The single highest-leverage env-var-leak control point in the profile system |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-112-07 | Denial of Service | `crates/nono/src/trust/types.rs` foreign-JSON full-parse crash | mitigate | SEC-04's predicate peek-before-parse avoids attempting a full schema deserialization of a non-nono JSON file |
| T-112-08 | Information Disclosure | `crates/nono-cli/src/profile_runtime.rs` allow_vars resolution | mitigate | SEC-08: explicitly REJECT upstream `a5a441c2`'s opposite default; preserve the fork's existing fail-closed `Some(env_config.allow_vars.clone())` semantics unchanged; regression-guarded by `empty_allow_vars_fails_closed` |
| T-112-09 | Repudiation | Undocumented rejection of a security-adjacent upstream change | mitigate | `proj/ADR-112-allow-vars-fail-closed-preserved.md` records the decision durably, per D-05's contingent-escalation trigger, so a future sync auditor finds a documented decision instead of re-litigating |

Package Legitimacy Gate: N/A — no new `Cargo.toml` dependencies.
</threat_model>

<verification>
`cargo test -p nono-sandbox --lib -- trust::` passes (SEC-04). `cargo test -p nono-sandbox-cli --lib
-- empty_allow_vars_fails_closed` passes unchanged (SEC-08 regression guard).
`git diff -- crates/nono-cli/src/profile_runtime.rs` is empty. `proj/ADR-112-allow-vars-fail-closed-preserved.md`
exists and is staged via `git add -f`.
</verification>

<success_criteria>
- SEC-04's predicate discriminator lands; foreign JSON is skipped with a warning, not a crash.
- SEC-08's decision is PRESERVE (not adopt), recorded in ADR-112, with zero source-code change.
- `empty_allow_vars_fails_closed` passes before and after this plan (proves no accidental drift).
</success_criteria>

<output>
Create `.planning/phases/112-security-residual-sync/112-03-SUMMARY.md` when done
</output>
