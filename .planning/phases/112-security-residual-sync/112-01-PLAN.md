---
phase: 112-security-residual-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/112-security-residual-sync/112-DISPOSITION-TABLE.md
  - .planning/phases/112-security-residual-sync/112-AWS-SIGV4-PROXY-AUTH-FINDING.md
  - .planning/phases/112-security-residual-sync/112-OAUTH-CAPTURE-DISPOSITION.md
autonomous: true
requirements: [SEC-01, SEC-02, RES-01]

must_haves:
  truths:
    - "D-02: Wave 1 diff-verifies and finalizes all 18 SHA dispositions with fresh, live-re-run evidence (not copied blindly from 112-RESEARCH.md) before any absorb plan (Wave 2+) executes"
    - "D-01: SEC-09's target guard is re-confirmed absent from crates/nono-cli/src/execution_runtime.rs — zero live grep hits for command_policies, tool_sandbox_runtime, tool_sandbox_initial_shim"
    - "D-03: all 4 RES-01 commits (f0506434, 7fe0c828, 0158d52f, 762eb05b) carry written skip-with-reasoning; none is silently dropped"
    - "D-07: crossbeam-epoch is confirmed 0.9.20 in Cargo.lock and a live cargo audit run reports 0 vulnerabilities, so RUSTSEC-2026-0204 is closed and 373a67ae is NOT re-absorbed"
    - "SEC-01 (0ecc476b) is recorded won't-sync via a finding document citing live evidence that crates/nono-proxy/src/aws/ and crates/nono-proxy/src/tls_intercept/ are both absent from the fork"
    - "SEC-02a/b/c's (9b692e07, 3c59c62e, d033c631) adapt-vs-defer judgment (Open Question 1) is made explicitly with cited evidence, not silently assumed either way"
  artifacts:
    - path: ".planning/phases/112-security-residual-sync/112-DISPOSITION-TABLE.md"
      provides: "Execution-time-finalized disposition for all 18 SHAs, feeding every downstream plan's read_first and the Wave 4 ledger addendum"
    - path: ".planning/phases/112-security-residual-sync/112-AWS-SIGV4-PROXY-AUTH-FINDING.md"
      provides: "SEC-01 won't-sync finding, mirroring 109-AWS-SIGV4-TLS-INTERCEPT-FINDING.md"
    - path: ".planning/phases/112-security-residual-sync/112-OAUTH-CAPTURE-DISPOSITION.md"
      provides: "SEC-02a/b/c go/no-go decision + RES-01 4-item skip reasoning"
  key_links: []
---

<objective>
Execute Phase 112's D-02 front-gate: diff-verify every one of the 18 `security-residual-and-misc`
SHAs against the LIVE fork tree (fresh commands, not a copy of 112-RESEARCH.md's table), finalize
the disposition each downstream absorb plan will implement, and produce the two smallest, entirely
non-code deliverables outright: SEC-01's won't-sync finding document and SEC-02a/b/c's explicit
adapt-vs-defer decision. Also record RES-01's skip reasoning (D-03) and confirm D-07 (crossbeam-epoch
already closed).

Purpose: prevent the failure mode D-02 exists to close — an absorb plan burning an executor's
context discovering mid-task that its target subsystem doesn't exist (this happened with SEC-09
during discussion). This plan is the gate; Wave 2+ only executes confirmed-real work.
Output: `112-DISPOSITION-TABLE.md` (all 18 SHAs, finalized), `112-AWS-SIGV4-PROXY-AUTH-FINDING.md`,
`112-OAUTH-CAPTURE-DISPOSITION.md`. No source code is touched by this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-security-residual-sync/112-CONTEXT.md
@.planning/phases/112-security-residual-sync/112-RESEARCH.md
@.planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md
@.planning/phases/109-proxy-network-absorb/109-AWS-SIGV4-TLS-INTERCEPT-FINDING.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reality-check finalization of all 18 dispositions + D-07 confirmation</name>
  <files>.planning/phases/112-security-residual-sync/112-DISPOSITION-TABLE.md</files>
  <read_first>
    112-RESEARCH.md § "Per-SHA Disposition Table" (the 18-row table with cited evidence, HIGH
    confidence — this is the starting point to re-verify, not re-derive from scratch);
    108-DIVERGENCE-LEDGER.md § "security-residual-and-misc cluster commits (18)" (~line 626) for
    the canonical SHA/subject/anchor list; CLAUDE.md § "Security Considerations" (fail-secure
    framing for any disposition involving a security-adjacent default).
  </read_first>
  <action>
    Re-run the highest-risk confirmatory commands live (do not trust the cached research text
    alone) and record fresh output in the new table:
    - SEC-01 (0ecc476b): `ls crates/nono-proxy/src/` — confirm no `aws/` or `tls_intercept/` dir.
    - SEC-09 (f6f02751): `grep -rn "command_policies\|tool_sandbox_runtime\|tool_sandbox_initial_shim" crates/nono-cli/src/execution_runtime.rs` — confirm 0 hits.
    - SEC-05 (d84b4818): `grep -n "AccessFs::Refer" crates/nono/src/sandbox/linux.rs` — confirm no
      hit inside `restrict_execute()` (lines ~1105-1157), only the pre-existing general grant near
      `:365`.
    - SEC-08 (a5a441c2): `grep -n "empty_allow_vars_fails_closed" crates/nono-cli/src/profile_runtime.rs`
      — confirm the regression-guard test exists at `:989`.
    - D-07: run `cargo audit`; `grep -n -A1 'name = "crossbeam-epoch"' Cargo.lock` — confirm
      `version = "0.9.20"`.
    Write a Markdown table with columns SHA / Req / disposition (adopt / adapt / won't-sync /
    defer / skip) / cited evidence (command + output excerpt) / confidence, one row per each of
    the 18 SHAs, reusing 112-RESEARCH.md's table content where the fresh re-run confirms it
    unchanged, and flagging explicitly any row where live re-verification diverges from the
    research document. Mark the document header "Wave 1 reality-check — finalized <today's date>,
    per CONTEXT.md D-02."
  </action>
  <acceptance_criteria>
    112-DISPOSITION-TABLE.md exists with exactly 18 SHA rows; every row has a non-empty cited
    evidence cell; the D-07 row cites a live `cargo audit` run with 0 vulnerabilities and
    `crossbeam-epoch 0.9.20` confirmed in Cargo.lock.
  </acceptance_criteria>
  <verify>
    <automated>cargo audit 2>&1 | tail -5</automated>
  </verify>
  <done>Fresh cargo audit output shows 0 vulnerabilities; 112-DISPOSITION-TABLE.md committed with 18 rows.</done>
</task>

<task type="auto">
  <name>Task 2: SEC-01 won't-sync finding document</name>
  <files>.planning/phases/112-security-residual-sync/112-AWS-SIGV4-PROXY-AUTH-FINDING.md</files>
  <read_first>
    109-AWS-SIGV4-TLS-INTERCEPT-FINDING.md (full file — this is the exact shape to mirror:
    Written/Status header -&gt; Summary -&gt; Re-verification evidence with command transcripts -&gt;
    Conclusion); `crates/nono-proxy/src/credential.rs:125-140` (the `aws_routes: HashMap&lt;String, ()&gt;`
    placeholder comment); `crates/nono-proxy/src/reverse.rs:255-270` (the hand-authored 501 guard
    citing prior fork decision D-15); 112-RESEARCH.md's SEC-01 table row.
  </read_first>
  <action>
    Run `git show 0ecc476b --stat` to confirm the 11-file/+1,873/-412 diffstat. Run
    `ls crates/nono-proxy/src/aws crates/nono-proxy/src/tls_intercept` and capture the
    "No such file or directory" output verbatim. Read `credential.rs`'s `aws_routes` placeholder
    field and `reverse.rs`'s 501 guard in full. Write
    `112-AWS-SIGV4-PROXY-AUTH-FINDING.md` following 109's section skeleton exactly: header
    (Written/Status/Phase), Summary, Re-verification Evidence (with the `ls`/`git show --stat`
    transcripts inline), per-item subsections quoting the placeholder/guard code, and a Conclusion
    stating explicitly that `0ecc476b` is the ORIGIN commit of the same absent `aws/`/`tls_intercept/`
    subsystem that 109's finding already covered for its two later sibling bug-fix commits
    (`6fb7ecbf`, `23d93fc9`) — cross-reference `109-AWS-SIGV4-TLS-INTERCEPT-FINDING.md` by path.
    State that implementing the missing subsystem is out of this phase's scope and, if ever
    prioritized, needs its own future phase with its own design review and threat model.
  </action>
  <acceptance_criteria>
    File exists, contains the literal string "won't-sync", cites the `ls` command's "No such file
    or directory" output for both `aws` and `tls_intercept`, and cross-references
    `109-AWS-SIGV4-TLS-INTERCEPT-FINDING.md` by path.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "won't-sync" .planning/phases/112-security-residual-sync/112-AWS-SIGV4-PROXY-AUTH-FINDING.md</automated>
  </verify>
  <done>Finding document committed; grep count &gt;= 1.</done>
</task>

<task type="auto">
  <name>Task 3: SEC-02a/b/c go/no-go decision + RES-01 skip documentation</name>
  <files>.planning/phases/112-security-residual-sync/112-OAUTH-CAPTURE-DISPOSITION.md</files>
  <read_first>
    112-RESEARCH.md § "Open Questions" #1 and § "Security Domain" (the "Real OAuth tokens reaching
    the sandboxed client" threat pattern row); 112-RESEARCH.md's SEC-02a/b/c table rows;
    112-RESEARCH.md's RES-01 table rows (`f0506434`, `7fe0c828`, `0158d52f`, `762eb05b`); CONTEXT.md
    D-03.
  </read_first>
  <action>
    Run `git show 9b692e07 -- crates/nono-proxy/src/oauth_capture` and
    `git show 9b692e07 -- crates/nono-proxy/src/forward.rs` to read the declarative OAuth-capture
    module and confirm whether the captured token is stripped/rewritten specifically inside
    `forward.rs`'s response-rewrite hook (`ResponseRewrite`) before reaching the sandboxed client,
    or whether an equivalent enforcement point exists elsewhere. Run
    `git log --oneline --all --diff-filter=A -- crates/nono-proxy/src/forward.rs` to confirm the
    ancestor commit (`149abde0`) that created it is outside this 18-commit window and was never
    absorbed. If the rewrite hook is confirmed load-bearing for keeping real OAuth tokens out of
    the sandboxed client (the expected outcome per RESEARCH.md's Security Domain finding), record
    disposition = DEFER to a future dedicated phase, mirroring the NET-02/SPIFFE precedent
    (Phase 109 -&gt; Phase 113 split) — do NOT attempt any partial code absorb for `9b692e07`,
    `3c59c62e`, or `d033c631` in Phase 112. If the investigation instead finds the hook is NOT
    load-bearing (a genuine surprise, not the expected result), record the alternative adapt-down
    scope with the same evidentiary rigor. Separately, in the same document, record RES-01's 4
    SHAs each with skip-with-reasoning per D-03 (registry/update-check collides with the fork's own
    HKLM machine-policy spine and divergent update behavior — cite
    `grep -rln HKLM crates/nono-cli/src/*.rs`), including a flagged-but-not-acted-on note for
    `f0506434`'s `always-further` pack-registry-namespace staleness (15+ `grep -rn "always-further"`
    hits in `cli.rs`/`policy.json`/`policy.rs`/`profile/builtin.rs` — a UX-consistency issue, not
    security-relevant, out of scope this phase per D-03's skip bias).
  </action>
  <acceptance_criteria>
    File exists and contains an explicit disposition line for SEC-02a/b/c (either "DEFER" with
    cited evidence, or an alternative adapt-down scope with equally cited evidence — not silent);
    contains 4 distinct skip-with-reasoning entries, one per RES-01 SHA, each citing at least one
    concrete piece of evidence (grep output or file:line).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "DEFER\|ADOPT\|ADAPT" .planning/phases/112-security-residual-sync/112-OAUTH-CAPTURE-DISPOSITION.md</automated>
  </verify>
  <done>Document committed with an explicit SEC-02a/b/c disposition and 4 RES-01 skip entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning-time disposition -> execution-time absorb | An incorrect "won't-sync"/"defer" call on a security-relevant commit silently leaves a real gap the fork never revisits |
| Upstream commit message framing -> fork's actual current behavior | Trusting a commit's own bug-description over the fork's actual code (RESEARCH.md Pitfall 2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-112-01 | Repudiation | 112-DISPOSITION-TABLE.md accuracy | mitigate | Every disposition cites a freshly re-run command (not copied from 112-RESEARCH.md verbatim) with visible output, per Task 1 |
| T-112-02 | Information Disclosure | SEC-02a/b/c OAuth-capture disposition | mitigate | Explicit go/no-go investigation of whether `forward.rs`'s response-rewrite hook is load-bearing for keeping real tokens out of the sandbox; default to DEFER (not a partial unsafe absorb) unless positively disproven |
| T-112-03 | Information Disclosure | SEC-01 AWS SigV4 absent-subsystem finding | accept | Subsystem is confirmed absent; no code exists to misconfigure. Won't-sync is the safe default, matching the 109 precedent |

No package installs occur in this plan (documentation-only); the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
Run `cargo audit` and confirm 0 vulnerabilities. Confirm all three new files exist under
`.planning/phases/112-security-residual-sync/` and each satisfies its task's acceptance_criteria.
Confirm 112-DISPOSITION-TABLE.md has exactly 18 rows (one per SHA in
`108-DIVERGENCE-LEDGER.md`'s "security-residual-and-misc cluster commits (18)" table).
</verification>

<success_criteria>
- 112-DISPOSITION-TABLE.md exists, 18 rows, every disposition cited with fresh evidence.
- 112-AWS-SIGV4-PROXY-AUTH-FINDING.md exists, disposition = won't-sync, cross-references 109's finding.
- 112-OAUTH-CAPTURE-DISPOSITION.md exists with an explicit SEC-02a/b/c decision and 4 RES-01 skip entries.
- `cargo audit` confirms 0 vulnerabilities; `crossbeam-epoch 0.9.20` confirmed in Cargo.lock (D-07).
- Zero source (`crates/`) files touched by this plan.
</success_criteria>

<output>
Create `.planning/phases/112-security-residual-sync/112-01-SUMMARY.md` when done
</output>
