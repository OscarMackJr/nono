---
phase: 112-security-residual-sync
plan: 08
type: execute
wave: 4
depends_on: ["112-01", "112-02", "112-03", "112-04", "112-05", "112-06", "112-07"]
files_modified:
  - .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [SEC-09]

must_haves:
  truths:
    - "D-05: all 18 SHA dispositions are recorded as a standing-divergence addendum appended to 108-DIVERGENCE-LEDGER.md, following the exact shape of the Phase 111 Standing Divergence Addendum (~line 1648) — a new `## Phase 112 Security + Residual Sync Addendum` section AFTER the existing addendum, and the 'Ledger closed' declaration (~line 1642) is left textually unchanged"
    - "D-01: SEC-09's carry-forward note lands directly inside 108-DIVERGENCE-LEDGER.md's tool-sandbox-surface cluster section (~line 596-624, the canonical v3.7 work-list a future auditor reads), stating explicitly that the fork must consciously decide whether to bring upstream f6f02751's relaxed non-shim-entry guard when tool-sandbox is eventually absorbed"
    - "D-08: the 18-commit cluster's disposition accounting is arithmetically closed in the addendum (10 D-18-named anchors + f6f02751 + 4 RES-01 + 3 RES-02 = 18), with no invented remainder bucket"
    - "SEC-02 (9b692e07/3c59c62e/d033c631) is explicitly recorded in the ledger addendum with disposition 'deferred -> Phase 114', citing 112-01's reality-check evidence (112-OAUTH-CAPTURE-DISPOSITION.md) and the ROADMAP Amendment (2026-08-05); REQUIREMENTS.md is updated to reflect the carved-out status rather than silently left Pending with no explanation"
    - "Both mandatory cross-target clippy gates and cargo test --workspace --no-fail-fast (diffed against the inherited 27-name baseline) are re-confirmed GREEN over the FULL combined Wave 2+3 surface in one final pass, consistent with VERIFY-01's Phase 111 framing"
  artifacts:
    - path: ".planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md"
      provides: "Phase 112 Security + Residual Sync Addendum (all 18 dispositions) + SEC-09 carry-forward note in the tool-sandbox-surface section"
  key_links: []
---

<objective>
Close Phase 112: append the D-05 standing-divergence addendum recording all 18 SHA dispositions to
`108-DIVERGENCE-LEDGER.md`, write SEC-09's carry-forward note directly into the ledger's
tool-sandbox-surface work-list section, run the final combined-surface fork-invariant verification
pass (both cross-target clippy gates + full workspace test baseline diff), and reconcile
`REQUIREMENTS.md`/`ROADMAP.md` — being explicit that SEC-02 is carved out to Phase 114 per the
ROADMAP Amendment (2026-08-05), not completed and not left as an open question this phase decides.

Purpose: this is the phase's closing gate. Every disposition this phase made (Wave 1's finding
docs, Wave 2/3's absorbs/adapts/skips, and the SEC-02 evidence-capture-then-carve-out) gets a
single, durable, cited record; nothing is left implicit in scattered plan SUMMARYs.
Output: the addendum, the SEC-09 note, updated REQUIREMENTS.md/ROADMAP.md, and a final verification
pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/112-security-residual-sync/112-CONTEXT.md
@.planning/phases/112-security-residual-sync/112-RESEARCH.md
@.planning/phases/112-security-residual-sync/112-DISPOSITION-TABLE.md
@.planning/phases/112-security-residual-sync/112-AWS-SIGV4-PROXY-AUTH-FINDING.md
@.planning/phases/112-security-residual-sync/112-OAUTH-CAPTURE-DISPOSITION.md
@.planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md
@.planning/phases/111-core-carry-resource-cli-verify-release-leapfrog/111-04-VERIFICATION-NOTES.md
@.planning/templates/cross-target-verify-checklist.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-05 ledger addendum + D-01 SEC-09 carry-forward note</name>
  <files>.planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</files>
  <read_first>
    108-DIVERGENCE-LEDGER.md `~1642-1682` (the "Ledger closed" declaration + the full "Phase 111
    Standing Divergence Addendum" section — this is the EXACT shape to replicate, read it in full);
    108-DIVERGENCE-LEDGER.md `~596-624` ("tool-sandbox-surface cluster commits (20)" table — the
    insertion point for the SEC-09 note); 112-DISPOSITION-TABLE.md (finalized, execution-time
    dispositions for all 18 SHAs, including the SEC-02a/b/c rows reading "deferred -> Phase 114" —
    the primary source for the addendum's per-SHA table, gathering each Wave 2/3 plan's actual
    landed outcome, not just the pre-execution plan intent); 112-AWS-SIGV4-PROXY-AUTH-FINDING.md and
    112-OAUTH-CAPTURE-DISPOSITION.md (SEC-01 final text / SEC-02 reality-check evidence + carve-out
    record); ROADMAP.md's Phase 112 Amendment (2026-08-05) and its new `### Phase 114: OAuth Capture
    Absorb (SEC-02)` section — the authority the addendum's SEC-02 row cites.
  </read_first>
  <action>
    Append a new `## Phase 112 Security + Residual Sync Addendum` section immediately after the
    existing "## Phase 111 Standing Divergence Addendum" section, following its exact shape
    (commits/disposition/rationale, then a closing paragraph distinguishing this addendum's own
    disposition classes). Build the per-SHA table from `112-DISPOSITION-TABLE.md` updated with each
    plan's ACTUAL landed outcome (read each `112-0N-SUMMARY.md` if present to confirm what actually
    happened, not just what was planned): SEC-01 won't-sync, SEC-02a/b/c **deferred -> Phase 114**
    (cite 112-OAUTH-CAPTURE-DISPOSITION.md's reality-check evidence and the ROADMAP Amendment
    2026-08-05 — do not write an adopt/adapt/decline verdict here; that decision is Phase 114's to
    make, this row records the carve-out and its evidence trail), SEC-03 adopt, SEC-04 adopt, SEC-05
    adopt, SEC-06 adapt, SEC-07 adopt (adapted API), SEC-08 won't-sync/preserve (ADR-112), SEC-09
    won't-sync + carry-forward, RES-01's 4 SHAs skip, RES-02's 3 items (adopt/skip/adopt). Confirm
    the arithmetic: 10 named anchors + `f6f02751` (SEC-09) + 4 RES-01 + 3 RES-02 = 18, with zero
    remainder, per D-08 — the carve-out changes SEC-02a/b/c's disposition value, not the 18-commit
    accounting; all three SHAs remain present in the table. Do **NOT** edit the "Ledger closed"
    sentence at `~1642` — the new section goes strictly after the Phase 111 addendum, following the
    same "deliberate, locked exception" framing.
    Separately, insert a new subsection titled "### SEC-09 Carry-Forward Note (Phase 112, D-01)"
    directly after the "tool-sandbox-surface cluster commits (20)" table (`~624`), stating: upstream
    `f6f02751` (#1301) relaxes a non-shim-entry guard in `execution_runtime.rs` that gates
    `env_credentials`/`command_policies` on `tool_sandbox_runtime.is_some()` — a guard this fork does
    not currently have (confirmed absent, D-01) because it never absorbed the base `tool_sandbox_runtime`
    subsystem. When v3.7 (Windows Tool-Sandbox Parity) eventually absorbs that base subsystem, it
    MUST consciously re-decide whether to bring `f6f02751`'s relaxed posture along with it or
    preserve a stricter guard — this is not an automatic inheritance. Cite this note by SHA and
    requirement ID (SEC-09) so a future v3.7 planner finds it via a search of this section.
  </action>
  <acceptance_criteria>
    `grep -n "Phase 112 Security + Residual Sync Addendum" .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md`
    returns 1 hit; `grep -n "SEC-09 Carry-Forward Note" .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md`
    returns 1 hit, located after the tool-sandbox-surface table; the addendum's SEC-02a/b/c row(s)
    read "deferred -> Phase 114" and cite the ROADMAP Amendment (2026-08-05), not an adopt/adapt/
    decline verdict; `git diff -- .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md`
    shows ZERO deletions on the "Ledger closed" line (addendum-only, append-only diff aside from
    the SEC-09 note insertion point).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Phase 112 Security + Residual Sync Addendum\|SEC-09 Carry-Forward Note" .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</automated>
  </verify>
  <done>Addendum and carry-forward note both land; "Ledger closed" line unmodified; 18-commit arithmetic closes; SEC-02a/b/c recorded as deferred -> Phase 114.</done>
</task>

<task type="auto">
  <name>Task 2: Combined-surface fork-invariant verification pass</name>
  <files></files>
  <read_first>
    111-04-VERIFICATION-NOTES.md (the inherited 24-name + 3-name-addendum = 27-name failing-test
    baseline this phase inherits — read in full before running the sweep); 112-VALIDATION.md
    § "Inherited Baseline"; .planning/templates/cross-target-verify-checklist.md.
  </read_first>
  <action>
    Run both mandatory cross-target clippy gates one final time over the FULL combined Wave 2+3
    surface (linux-gnu via `cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D
    warnings -D clippy::unwrap_used`; apple-darwin via `cargo-zigbuild clippy --workspace --target
    x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used`, `SDKROOT` unset). Run `cargo fmt
    --all -- --check`. Run `cargo test --workspace --no-fail-fast` (expect a non-zero exit — do NOT
    treat this as failure per se) and diff the failing-test name set against the inherited 27-name
    baseline (111-04-VERIFICATION-NOTES.md's 24 names + the profile_cmd.rs 3-name addendum): any
    NEW name outside that 27-name set is a real regression requiring investigation before this plan
    can close; any name already in the set is expected host noise. Record the diff result (strict
    subset confirmed, or new names found + how they were resolved) in this plan's SUMMARY.
  </action>
  <acceptance_criteria>
    Both cross-target clippy gates exit 0 with zero findings; `cargo fmt --all -- --check` exits 0;
    the full workspace-test failing-name set is a confirmed subset of the inherited 27-name
    baseline (or any new name is investigated and either fixed or explicitly logged as unrelated,
    matching the Phase 111 precedent for out-of-scope pre-existing failures).
  </acceptance_criteria>
  <verify>
    <automated>cargo fmt --all -- --check 2>&1 | tail -10</automated>
  </verify>
  <done>Both cross-target clippy gates GREEN; fmt clean; test failing-name set is a confirmed subset of the 27-name baseline.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile REQUIREMENTS.md and ROADMAP.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    .planning/REQUIREMENTS.md § "Security + Residual Sync (SEC / RES) — Phase 112" (current
    checkbox state, all 11 unchecked); .planning/ROADMAP.md § "Phase 112" (amended SC1-SC4 and the
    Amendment paragraph dated 2026-08-05) and § "Phase 114: OAuth Capture Absorb (SEC-02)" (already
    written by the orchestrator ahead of this revision — read, do not re-derive); this plan's Task 1
    addendum (the authoritative final disposition for every requirement, including SEC-02's
    "deferred -> Phase 114" row).
  </read_first>
  <action>
    Mark complete in `.planning/REQUIREMENTS.md`: SEC-01 (won't-sync finding delivered), SEC-03,
    SEC-04, SEC-05, SEC-06, SEC-07 (absorbed), SEC-08 (ADR-112 delivered, guard preserved), SEC-09
    (carry-forward note delivered), RES-01 (4-item skip documentation delivered), RES-02
    (individually reviewed, adopt/skip dispositions delivered) — exactly 10 requirement IDs; this is
    Phase 112's full, settled scope per the amended ROADMAP Requirements line. Do **NOT** mark SEC-02
    complete and do **NOT** treat its disposition as this plan's decision to make: per the ROADMAP
    Amendment (2026-08-05, operator decision during `/gsd:plan-phase 112`), SEC-02 is carved out of
    Phase 112 into its own Phase 114 — this is settled, not conditional on any finding in this phase.
    Update SEC-02's `.planning/REQUIREMENTS.md` line to state explicitly "carved out to Phase 114 per
    ROADMAP Amendment (2026-08-05)", reference `112-OAUTH-CAPTURE-DISPOSITION.md` for the
    reality-check evidence this phase captured, and reference ROADMAP's `### Phase 114: OAuth
    Capture Absorb (SEC-02)` section for where the actual disposition decision will be made —
    mirroring how NET-02 was handled when it moved from Phase 109 to its own Phase 113 (do not
    silently leave the checkbox unchecked with no explanation). Confirm — do not re-derive or
    rewrite — that `ROADMAP.md`'s Phase 112 entry already carries the Amendment paragraph
    (2026-08-05) and that the `### Phase 114: OAuth Capture Absorb (SEC-02)` section already exists;
    both were written by the orchestrator ahead of this revision. If either is missing or
    inconsistent with this plan's own Task 1 addendum (e.g. the Amendment's evidence summary
    contradicts `112-OAUTH-CAPTURE-DISPOSITION.md`), flag the discrepancy in the plan's SUMMARY
    rather than silently rewriting ROADMAP.md's Amendment prose. Flip Phase 112's top-level `- [ ]`
    checklist box in ROADMAP.md's `## Phases` index — the carve-out is Phase 112's own resolution
    per its amended SC1 (Phase 112 closes with SEC-02 explicitly carved out; SEC-02 itself is not
    decided until Phase 114), matching the Phase 108->112 D-19 precedent of a corrected,
    explicitly-surfaced SC rather than silent non-satisfaction. Update the Traceability table's
    Phase 112 rows' Status column for the 10 Phase-112-owned requirement IDs, and update or add
    SEC-02's Traceability row to point at Phase 114 rather than Phase 112.
  </action>
  <acceptance_criteria>
    `grep -c "^\- \[x\]" .planning/REQUIREMENTS.md` increases by exactly 10 (the Phase-112-owned
    requirements: SEC-01, SEC-03..SEC-09, RES-01, RES-02); SEC-02's line in REQUIREMENTS.md contains
    an explicit "carved out to Phase 114" (or equivalent unambiguous "Phase 114" status note) citing
    the ROADMAP Amendment — not a bare unchecked box with no explanation, and not conditional/TBD
    phrasing; ROADMAP.md's Traceability table Phase 112 rows all show a non-"Pending" status and
    SEC-02's row points at Phase 114; ROADMAP.md's pre-existing Amendment paragraph and
    `### Phase 114: OAuth Capture Absorb (SEC-02)` section are confirmed present and are NOT
    rewritten by this task (verification-only for those two elements).
  </acceptance_criteria>
  <verify>
    <automated>grep -n "SEC-01\|SEC-02\|SEC-03\|SEC-04\|SEC-05\|SEC-06\|SEC-07\|SEC-08\|SEC-09\|RES-01\|RES-02" .planning/REQUIREMENTS.md | tail -15</automated>
  </verify>
  <done>REQUIREMENTS.md and ROADMAP.md both reflect the final, explicit disposition: 10 requirements resolved in Phase 112 (SEC-01, SEC-03..SEC-09, RES-01, RES-02), and SEC-02 explicitly carved out to Phase 114 citing the ROADMAP Amendment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase-close documentation -> future upstream-sync auditor | An incomplete or silently-dropped disposition record causes a future sync to re-litigate or, worse, miss a real security gap |
| Phase-boundary carve-out -> Phase 114's disposition authority | If the ledger addendum or REQUIREMENTS.md loses SEC-02's evidence trail, Phase 114 starts from zero instead of from Phase 112's reality-check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-112-18 | Repudiation | 108-DIVERGENCE-LEDGER.md Phase 112 addendum | mitigate | Every one of the 18 SHAs gets an explicit disposition row with cited evidence, including SEC-02a/b/c's "deferred -> Phase 114"; arithmetic (10+1+4+3=18) is verified, per D-08 |
| T-112-19 | Repudiation | SEC-09's future re-decision obligation | mitigate | The carry-forward note is placed directly in the canonical v3.7 tool-sandbox work-list section (not buried in a phase-specific addendum only), so a future v3.7 planner encounters it while reading the work-list itself |
| T-112-20 | Repudiation | SEC-02's carve-out evidence chain | mitigate | The ledger addendum and REQUIREMENTS.md both cite `112-OAUTH-CAPTURE-DISPOSITION.md` and the ROADMAP Amendment by name/date, so Phase 114 inherits the reality-check evidence rather than re-deriving it |

Package Legitimacy Gate: N/A — no new `Cargo.toml` dependencies; this plan is documentation +
verification only.
</threat_model>

<verification>
Both cross-target clippy gates GREEN (ROADMAP SC4). `cargo fmt --all -- --check` clean. Full
workspace test failing-name set confirmed a subset of the inherited 27-name baseline. Ledger
addendum + SEC-09 note both present with the "Ledger closed" declaration unmodified, and the
SEC-02a/b/c row reads "deferred -> Phase 114". REQUIREMENTS.md/ROADMAP.md reconciled: 10
requirements resolved, SEC-02's carve-out to Phase 114 explicit and evidence-cited.
</verification>

<success_criteria>
- All 18 SHAs recorded with final disposition + evidence in the ledger addendum, including
  SEC-02a/b/c as "deferred -> Phase 114".
- SEC-09 carry-forward note lands in the canonical tool-sandbox-surface work-list section.
- Both cross-target clippy gates GREEN; workspace test baseline shows no new regressions.
- REQUIREMENTS.md/ROADMAP.md reflect the true final state: 10 requirements resolved in Phase 112,
  SEC-02 explicitly carved out to Phase 114 citing the ROADMAP Amendment (2026-08-05).
</success_criteria>

<output>
Create `.planning/phases/112-security-residual-sync/112-08-SUMMARY.md` when done
</output>
