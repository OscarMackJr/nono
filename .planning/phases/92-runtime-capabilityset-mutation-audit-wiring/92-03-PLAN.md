---
phase: 92-runtime-capabilityset-mutation-audit-wiring
plan: 03
type: execute
wave: 2
depends_on:
  - 92-01
  - 92-02
files_modified:
  - crates/nono-cli/src/cli.rs
  - crates/nono-cli/src/launch_runtime.rs
  - crates/nono-cli/src/cli_bootstrap.rs
  - crates/nono-cli/src/telemetry/mod.rs
  - crates/nono-cli/src/execution_runtime.rs
autonomous: true
requirements:
  - AUD-01
  - AUD-02
  - AUD-03
  - AUD-04
must_haves:
  truths:
    - "nono-cli accepts --override-audit <base64-json> as a hidden SandboxArgs field"
    - "OverrideAuditMeta struct with serde(deny_unknown_fields) is defined and used to decode the flag value"
    - "SecurityEventLayer exposes a #[must_use] emit_override_event() method that advances the HMAC chain and returns Result<String, &'static str>"
    - "emit_override_event returns Err when the mutex is poisoned (AUD-04 fail-closed)"
    - "A module-level OnceLock<Arc<SecurityEventLayer>> is populated by init_tracing so execute_sandboxed can reach the layer"
    - "execute_sandboxed decodes --override-audit, calls emit_override_event, and returns Err before any spawn if emit fails"
    - "execute_sandboxed with no --override-audit flag is byte-for-byte unchanged (MUT-05)"
    - "chain_sequence() advances by exactly 1 after emit_override_event on a fresh layer"
  artifacts:
    - path: "crates/nono-cli/src/cli.rs"
      provides: "--override-audit flag in SandboxArgs; OverrideAuditMeta struct"
      contains: "override_audit"
    - path: "crates/nono-cli/src/telemetry/mod.rs"
      provides: "emit_override_event method on SecurityEventLayer; SECURITY_LAYER OnceLock accessor"
      contains: "emit_override_event"
    - path: "crates/nono-cli/src/execution_runtime.rs"
      provides: "AUD-04 pre-spawn gate that aborts before spawn if audit emission fails"
      contains: "override audit emission failed"
  key_links:
    - from: "crates/nono-cli/src/cli_bootstrap.rs::init_tracing"
      to: "crates/nono-cli/src/telemetry/SECURITY_LAYER"
      via: "OnceLock::set after SecurityEventLayer::new"
      pattern: "SECURITY_LAYER\\.set"
    - from: "crates/nono-cli/src/execution_runtime.rs::execute_sandboxed"
      to: "crates/nono-cli/src/telemetry::SECURITY_LAYER"
      via: "SECURITY_LAYER.get() to call emit_override_event"
      pattern: "emit_override_event"
    - from: "crates/nono-cli/src/launch_runtime.rs::ExecutionFlags"
      to: "crates/nono-cli/src/execution_runtime.rs::execute_sandboxed"
      via: "flags.override_audit carries the decoded OverrideAuditMeta"
      pattern: "override_audit"
---

<objective>
Wire the nono-cli (Nono repo, `C:\Users\OMack\Nono`) side of the D-01/D-02 bilateral handshake:

1. `--override-audit <base64-json>` flag — add to `SandboxArgs` in `cli.rs`; add `OverrideAuditMeta` struct with `serde(deny_unknown_fields)`.

2. `override_audit: Option<OverrideAuditMeta>` field — thread through `ExecutionFlags` / `LaunchPlan` from `launch_runtime.rs` to `execution_runtime.rs`.

3. `OnceLock<Arc<SecurityEventLayer>>` — add a module-level `SECURITY_LAYER` static in `telemetry/mod.rs`; populate it in `cli_bootstrap.rs::init_tracing` so `execute_sandboxed` can reach the layer instance. This solves OQ-1 from RESEARCH.md.

4. `SecurityEventLayer::emit_override_event()` — add a `#[must_use]` direct-emit method (Option B from RESEARCH.md) that advances `advance_chain`, emits to the Windows event log, and returns `Result<String, &'static str>`. Returns `Err` when the mutex is poisoned (AUD-04 fail-closed).

5. AUD-04 pre-spawn gate in `execution_runtime.rs::execute_sandboxed` — after `start_proxy_runtime` and BEFORE `apply_pre_fork_sandbox` (line 261): if `flags.override_audit` is `Some(meta)`, call `SECURITY_LAYER.get().emit_override_event(PolicyOverrideVerified, ...)`. On `Err`: `return Err(NonoError::SandboxInit("override audit emission failed — aborting before spawn (AUD-04): ...")`.

Purpose: This completes the nono-cli half of the bilateral handshake. Depends on Plan 01 (SecurityEventType variants) and Plan 02 (nono-py passing --override-audit). Wave 2.

Output: Modified files in `C:\Users\OMack\Nono`. Commits go in the Nono repo.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\OMack\Nono\.planning\PROJECT.md
@C:\Users\OMack\Nono\.planning\ROADMAP.md
@C:\Users\OMack\Nono\.planning\REQUIREMENTS.md
@C:\Users\OMack\Nono\.planning\phases\92-runtime-capabilityset-mutation-audit-wiring\92-CONTEXT.md
@C:\Users\OMack\Nono\.planning\phases\92-runtime-capabilityset-mutation-audit-wiring\92-RESEARCH.md
@C:\Users\OMack\Nono\.planning\phases\92-runtime-capabilityset-mutation-audit-wiring\92-PATTERNS.md
@C:\Users\OMack\Nono\.planning\phases\92-runtime-capabilityset-mutation-audit-wiring\92-01-SUMMARY.md
@C:\Users\OMack\Nono\.planning\phases\92-runtime-capabilityset-mutation-audit-wiring\92-02-SUMMARY.md

<interfaces>
<!-- Verified line numbers from 92-RESEARCH.md and direct code reads. -->

From crates/nono-cli/src/cli.rs:
- SandboxArgs struct: existing optional hidden flags around lines 1960-1997 (e.g., dangerous_force_wfp_ready, dry_run) — use hide = true pattern
- Adding override_audit: Option<String> with #[arg(long, value_name = "META", hide = true, help_heading = "OPTIONS")]
- OverrideAuditMeta struct: add near SandboxArgs with #[derive(Debug, Clone, serde::Deserialize, serde::Serialize)] + #[serde(deny_unknown_fields)]

From crates/nono-cli/src/launch_runtime.rs:
- ExecutionFlags struct: lines 167-218; add override_audit: Option<OverrideAuditMeta> field
- ExecutionFlags::defaults: line 220+; add override_audit: None to the initialization
- The LaunchPlan itself doesn't need a field — it carries ExecutionFlags; the override_audit threads via flags.override_audit

From crates/nono-cli/src/cli_bootstrap.rs (CRITICAL for OQ-1):
- init_tracing: line 107 — currently calls SecurityEventLayer::new then passes it to init_tracing_with_security (line 126); does NOT return or store the layer
- Fix: before passing security_layer to init_tracing_with_security, call SECURITY_LAYER.set(Arc::new(security_layer.clone())) — or refactor to store an Arc-wrapped clone
- SECURITY_LAYER static: define in telemetry/mod.rs as:
  pub(crate) static SECURITY_LAYER: std::sync::OnceLock<std::sync::Arc<SecurityEventLayer>> = std::sync::OnceLock::new();

From crates/nono-cli/src/telemetry/mod.rs:
- SecurityEventLayer struct: line 189; inner field is a Mutex<SecurityEventLayerInner>
- chain_sequence() test accessor: line 211
- advance_chain() function: line 113
- on_event(): line 260
- emit_override_event() does NOT yet exist — add to impl SecurityEventLayer block
- SecurityEventLayer must implement Clone if we store Arc<SecurityEventLayer> in the OnceLock (check if Clone is derived; if not, add #[derive(Clone)] or use Arc<SecurityEventLayer> as the OnceLock type where clone() clones the Arc, not the layer)

From crates/nono-cli/src/execution_runtime.rs:
- execute_sandboxed signature: line 112
- start_proxy_runtime call: line 242 (AUD-04 gate goes AFTER this)
- apply_pre_fork_sandbox call: line 261 (AUD-04 gate goes BEFORE this)
- Existing fail-secure Err pattern (line 236-239): return Err(NonoError::SandboxInit("...".to_string()))
- flags access: destructured from LaunchPlan at lines 113-125; override_audit accessed as flags.override_audit

From plan 92-01 (completed):
- SecurityEventType::PolicyOverrideVerified (EventID 10007) now exists in event.rs — use this variant for the AUD-04 gate emit (a successful apply emits PolicyOverrideVerified)
- SecurityEventType::PolicyOverrideRejected (EventID 10008) — used if the gate detects the override cannot proceed (future; for now the gate either succeeds or aborts)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: --override-audit flag, OverrideAuditMeta struct, and ExecutionFlags threading (cli.rs + launch_runtime.rs)</name>
  <files>crates/nono-cli/src/cli.rs, crates/nono-cli/src/launch_runtime.rs</files>
  <read_first>
    - crates/nono-cli/src/cli.rs — read the SandboxArgs struct (lines ~1960-1997) to understand existing optional hidden flag patterns; search for "dry_run" and "dangerous_force_wfp_ready" as exact models
    - crates/nono-cli/src/launch_runtime.rs — read ExecutionFlags struct (lines 167-218) and ExecutionFlags::defaults (line 220+); note every existing field to place override_audit in the right position
    - .planning/phases/92-runtime-capabilityset-mutation-audit-wiring/92-PATTERNS.md — § "crates/nono-cli/src/cli.rs — add --override-audit" for exact field doc, arg attributes, and OverrideAuditMeta struct definition
  </read_first>
  <behavior>
    - Test: `OverrideAuditMeta` deserializes from `{"zt_audit_hash":"abc","kms_key_id":"arn:test","jti":"uuid","granted_paths":["/tmp/x"],"expires_at":"2026-01-01T00:00:00Z"}` to a valid struct
    - Test: `OverrideAuditMeta` deserialization fails with `serde(deny_unknown_fields)` when an extra field is present (prevents forged metadata injection)
    - Test: `OverrideAuditMeta` allows `zt_audit_hash: null` (since the field is `Option<String>` with `#[serde(default)]`)
  </behavior>
  <action>
    REPO: `C:\Users\OMack\Nono`. All edits and the commit go in this repo.

    In `crates/nono-cli/src/cli.rs`:

    1. Define `OverrideAuditMeta` struct BEFORE or NEAR `SandboxArgs` (co-location for context). Use this exact shape:
       ```
       #[derive(Debug, Clone, serde::Deserialize, serde::Serialize)]
       #[serde(deny_unknown_fields)]
       pub(crate) struct OverrideAuditMeta {
           #[serde(default)]
           pub(crate) zt_audit_hash: Option<String>,
           pub(crate) kms_key_id: String,
           pub(crate) jti: String,
           pub(crate) granted_paths: Vec<String>,
           pub(crate) expires_at: String,
       }
       ```
       Doc comment on the struct: "Trusted override audit metadata passed via --override-audit. Deserialized from base64url-no-pad-encoded JSON. All fields are read from the already-verified OverrideGrant in nono-py (D-06: closes the TOCTOU verify→apply gap)."
       Doc comment on zt_audit_hash: "ZT-Infra audit chain hash (bi-directional link, AUD-02). None if the token predates ZT-Infra audit chain integration."
       Doc comment on kms_key_id: "KMS key ARN used to sign the override token."
       Doc comment on jti: "JWT ID — single-use nonce."
       Doc comment on granted_paths: "Paths granted by this override, already sanitized in nono-py (D-05)."
       Doc comment on expires_at: "RFC3339 expiry timestamp."

    2. In `SandboxArgs`, add `override_audit: Option<String>` with:
       ```
       #[arg(long, value_name = "META", hide = true, help_heading = "OPTIONS")]
       pub override_audit: Option<String>,
       ```
       Doc comment: "Signed override audit metadata (base64url-no-pad-encoded JSON). Present only when nono-py has verified a signed policy override. nono-cli treats --allow paths as override-granted only if this flag is also present (AUD-04 bilateral gate, D-02). INTERNAL — not intended for direct user invocation."
       Place after `dry_run` (or at the end of SandboxArgs fields) to minimize diff.

    3. In `launch_runtime.rs`, add `override_audit: Option<OverrideAuditMeta>` to `ExecutionFlags`:
       - Import `OverrideAuditMeta` from `crate::cli::OverrideAuditMeta` (or via `crate::cli` depending on module layout)
       - Add the field after `session_hooks` (end of the struct)
       - In `ExecutionFlags::defaults`, add `override_audit: None` to the initialization

    4. Find where `SandboxArgs.override_audit` is translated into `ExecutionFlags` (search `launch_runtime.rs` or `command_runtime.rs` for the location where SandboxArgs fields are mapped to ExecutionFlags fields; use the `dry_run` field as a reference for the mapping pattern). Add the base64-decode + JSON-parse step there:
       ```
       override_audit: run_args.override_audit.as_deref().map(|b64| {
           use base64::Engine as _;
           let json = base64::engine::general_purpose::URL_SAFE_NO_PAD
               .decode(b64.as_bytes())
               .map_err(|e| NonoError::SandboxInit(format!("override-audit: base64 decode failed: {e}")))?;
           serde_json::from_slice::<OverrideAuditMeta>(&json)
               .map_err(|e| NonoError::SandboxInit(format!("override-audit: JSON parse failed: {e}")))
       }).transpose()?,
       ```
       (Note: the exact location depends on the mapping site found; follow the codebase pattern precisely.)

    Add inline tests for `OverrideAuditMeta` in `cli.rs` test module or as a separate `#[cfg(test)]` block:
    - `override_audit_meta_deserializes_valid_json`
    - `override_audit_meta_rejects_unknown_fields`
    - `override_audit_meta_allows_null_zt_audit_hash`
  </action>
  <verify>
    <automated>cd /c/Users/OMack/Nono && cargo test -p nono-cli --lib cli:: 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "override_audit" crates/nono-cli/src/cli.rs` shows OverrideAuditMeta struct and SandboxArgs field (>= 5 occurrences)
    - `grep -n "override_audit" crates/nono-cli/src/launch_runtime.rs` shows ExecutionFlags field and defaults initialization (>= 2 occurrences)
    - `grep -n "deny_unknown_fields" crates/nono-cli/src/cli.rs` shows the attribute on OverrideAuditMeta (>= 1 occurrence)
    - `cargo test -p nono-cli --lib cli::` exits 0 (OverrideAuditMeta deserialization tests pass)
    - `cargo build -p nono-cli` exits 0 (no compilation errors from struct addition or field threading)
    - No `.unwrap()` or `.expect()` in production code paths (the base64/json parsing uses `map_err` + `?`)
  </acceptance_criteria>
  <done>OverrideAuditMeta struct defined with deny_unknown_fields; --override-audit flag added to SandboxArgs; override_audit field threaded through ExecutionFlags; 3 deserialization tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: emit_override_event method, SECURITY_LAYER OnceLock, and AUD-04 pre-spawn gate (telemetry/mod.rs + cli_bootstrap.rs + execution_runtime.rs)</name>
  <files>crates/nono-cli/src/telemetry/mod.rs, crates/nono-cli/src/cli_bootstrap.rs, crates/nono-cli/src/execution_runtime.rs</files>
  <read_first>
    - crates/nono-cli/src/telemetry/mod.rs — read fully; note SecurityEventLayer struct (line 189), SecurityEventLayerInner (fields: chain, config, session_id), chain_sequence() (line 211), advance_chain() (line 113), on_event() (line 260), and the existing test module (lines ~480-510) as the pattern for emit_override_event tests
    - crates/nono-cli/src/cli_bootstrap.rs — read fully; note init_tracing (line 107) constructs SecurityEventLayer::new at line 121, then moves it into init_tracing_with_security (line 126); the layer is NOT stored anywhere accessible — this must change
    - crates/nono-cli/src/execution_runtime.rs — read lines 100-270; focus on execute_sandboxed signature (line 112), start_proxy_runtime call (line 242), apply_pre_fork_sandbox call (line 261), and existing fail-secure abort pattern (lines 234-240) as the template for the AUD-04 gate
    - .planning/phases/92-runtime-capabilityset-mutation-audit-wiring/92-PATTERNS.md — § "emit_override_event method pattern", § "AUD-04 pre-spawn gate pattern", § Pitfall 1 (SecurityEventLayer Mutex poisoned-silently), Pitfall 3 (AUD-04 window)
    - crates/nono-cli/src/agent_daemon/telemetry_init.rs — read lines 130-165 for the SpyLayer/Arc<SecurityEventLayer> pattern and the existing test structure that uses SecurityEventLayer directly
  </read_first>
  <behavior>
    - Test (AUD-01): `layer.emit_override_event(&SecurityEventType::PolicyOverrideVerified, "test-jti", "arn:aws:kms:test", Some("abc123"))` on a fresh SecurityEventLayer returns Ok and chain_sequence() advances from 0 to 1
    - Test (AUD-04 fail-closed): emit_override_event on a SecurityEventLayer whose mutex has been poisoned (via panic in lock guard in test) returns Err (never silently Ok)
    - Test (AUD-01 idempotent ordering): calling emit_override_event twice advances chain_sequence from 0 to 2
    - Test (MUT-05): execute_sandboxed with override_audit: None — does NOT call SECURITY_LAYER; behavior is unchanged (the gate code path is only entered when override_audit is Some)
  </behavior>
  <action>
    REPO: `C:\Users\OMack\Nono`. All edits and commits go in this repo.

    In `crates/nono-cli/src/telemetry/mod.rs`:

    1. Add a module-level static for layer access (resolves OQ-1 from RESEARCH.md):
       ```
       /// Global SecurityEventLayer instance, set once by init_tracing / init_daemon_telemetry.
       /// Used by execute_sandboxed to call emit_override_event (AUD-04 gate).
       /// Arc wrapper avoids Clone requirement on SecurityEventLayer itself.
       pub(crate) static SECURITY_LAYER: std::sync::OnceLock<std::sync::Arc<SecurityEventLayer>> =
           std::sync::OnceLock::new();
       ```

    2. Add `emit_override_event` to `impl SecurityEventLayer` (add after `chain_sequence()`, before `on_event()`):
       - Signature: `#[must_use] pub fn emit_override_event(&self, event_type: &SecurityEventType, jti: &str, kms_key_id: &str, zt_audit_hash: Option<&str>) -> Result<String, &'static str>`
       - Body (following 92-PATTERNS.md Pattern 2 exactly):
         a. `let mut inner = self.inner.lock().map_err(|_| "mutex poisoned")?;`
         b. Build `timestamp_unix_ms` from `SystemTime::now().duration_since(UNIX_EPOCH).map(|d| d.as_millis() as u64).unwrap_or(0)` (this is in a test-only context; `.unwrap_or` is not `.unwrap()`)
         c. Build `pre_chain_bytes` as a formatted string: `"{event_type:?}|{jti}|{kms_key_id}|{zt}|{session_id}|{ts}"` where `zt = zt_audit_hash.unwrap_or("")` and `session_id = inner.session_id`
         d. Call `advance_chain(&mut inner.chain, pre_chain_bytes.as_bytes())`
         e. `let chain_head = chain_head_hex(&inner.chain.head);`
         f. If `inner.config.enabled`: construct a `SecurityEvent` and call `windows::emit_security_event` (or the platform equivalent). For non-Windows, emit nothing (telemetry-disabled path); the chain still advances.
         g. Return `Ok(chain_head)`
       - Doc comment: "Emit a PolicyOverride lifecycle event directly into the HMAC chain. Bypasses the tracing-intercept path (on_event) because override events carry zt_audit_hash/kms_key_id fields, not path/host. Returns Ok(chain_head_hex) when the event was emitted and the chain advanced. Returns Err if the mutex is poisoned or the event cannot be committed. AUD-04: callers MUST treat Err as FATAL and abort before spawning the sandboxed child."
       - CRITICAL: For the Windows-specific ETW/AppLog emission in step (f), check if `windows::emit_security_event` requires the full `SecurityEvent` struct. Read the existing `SecurityEvent` construction in `on_event` to understand required fields. If `path_hash` / `path_category` / `host` are required in the struct, set them to `None` for override events (they carry kms_key_id/jti instead, which are in the HMAC chain bytes; the event log entry just needs event_type + session_id + chain_head).
       - For non-Windows: guard the ETW emit with `#[cfg(target_os = "windows")]` or check `inner.config.enabled` using the existing platform-awareness pattern in `on_event`.

    In `crates/nono-cli/src/cli_bootstrap.rs`:

    3. In `init_tracing` (line 107), after `SecurityEventLayer::new(config, session_id)` (line 121) and before `init_tracing_with_security(cli, security_layer)` (line 126), add:
       ```
       // Expose the layer for direct access from execute_sandboxed (AUD-04 gate, OQ-1 resolution).
       // Use Arc to avoid requiring Clone on SecurityEventLayer.
       // OnceLock::set silently fails if already set (daemon double-init guard pattern).
       let _ = crate::telemetry::SECURITY_LAYER.set(std::sync::Arc::new(security_layer.clone()));
       ```
       This requires `SecurityEventLayer` to implement `Clone`. Check if it does; if not, add `#[derive(Clone)]` to `SecurityEventLayer` or adjust the approach: alternatively, store `Arc<Mutex<SecurityEventLayerInner>>` and share the same inner rather than cloning. The simplest approach if Clone is not derived: store `Arc<SecurityEventLayer>` before moving into the registry by using `Arc::clone`.
       
       Alternative if Clone cannot be added safely: create the layer as `let security_layer = Arc::new(SecurityEventLayer::new(config, session_id)); let _ = crate::telemetry::SECURITY_LAYER.set(Arc::clone(&security_layer)); init_tracing_with_security(cli, security_layer);` — but this requires updating `init_tracing_with_security` to accept `Arc<SecurityEventLayer>`. Evaluate which approach involves fewer changes and follow that path; document the choice in a code comment.
       
       Also update `init_daemon_telemetry` in `agent_daemon/telemetry_init.rs` with the same `SECURITY_LAYER.set(...)` call after `SecurityEventLayer::new`.

    In `crates/nono-cli/src/execution_runtime.rs`:

    4. Add the AUD-04 pre-spawn gate AFTER line 242 (`start_proxy_runtime`) and BEFORE line 261 (`apply_pre_fork_sandbox`). Follow the exact error-return pattern from lines 234-240:
       ```
       // AUD-04 bilateral gate: if override audit metadata was passed (--override-audit flag),
       // the PolicyOverrideApplied event MUST be committed to the SecurityEventLayer HMAC
       // chain BEFORE the sandboxed child spawns. An override that cannot emit its audit
       // record is blocked — never silently applied (D-02 bilateral gate; AUD-04).
       if let Some(ref meta) = flags.override_audit {
           let emit_result = crate::telemetry::SECURITY_LAYER
               .get()
               .ok_or_else(|| NonoError::SandboxInit(
                   "override audit emission failed — SecurityEventLayer not initialized (AUD-04)".to_string()
               ))?
               .emit_override_event(
                   &crate::telemetry::SecurityEventType::PolicyOverrideVerified,
                   &meta.jti,
                   &meta.kms_key_id,
                   meta.zt_audit_hash.as_deref(),
               );
           match emit_result {
               Ok(_chain_head) => { /* chain advanced — spawn may proceed */ }
               Err(e) => {
                   return Err(NonoError::SandboxInit(format!(
                       "override audit emission failed — aborting before spawn (AUD-04): {e}"
                   )));
               }
           }
       }
       ```

    Add inline tests in `telemetry/mod.rs` test block:
    - `emit_override_event_advances_chain_by_one`: construct SecurityEventLayer, call emit_override_event with PolicyOverrideVerified, assert chain_sequence() == 1
    - `emit_override_event_err_on_poisoned_mutex`: poison the mutex via panic in lock guard (use std::panic::catch_unwind), then assert emit_override_event returns Err (AUD-04 fail-closed)
    - `emit_override_event_two_calls_advance_by_two`: call twice, assert chain_sequence() == 2
    - `emit_override_event_none_zt_audit_hash_ok`: call with zt_audit_hash=None, assert Ok
  </action>
  <verify>
    <automated>cd /c/Users/OMack/Nono && cargo test -p nono-cli --lib telemetry:: 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "emit_override_event" crates/nono-cli/src/telemetry/mod.rs` shows method definition and test invocations (>= 5 occurrences)
    - `grep -n "must_use" crates/nono-cli/src/telemetry/mod.rs` shows #[must_use] on emit_override_event (>= 1 occurrence)
    - `grep -n "SECURITY_LAYER" crates/nono-cli/src/telemetry/mod.rs` shows the OnceLock static (>= 1 occurrence)
    - `grep -n "SECURITY_LAYER" crates/nono-cli/src/cli_bootstrap.rs` shows the .set() call (>= 1 occurrence)
    - `grep -n "override audit emission failed" crates/nono-cli/src/execution_runtime.rs` shows the AUD-04 gate error message (>= 1 occurrence)
    - `grep -n "apply_pre_fork_sandbox" crates/nono-cli/src/execution_runtime.rs` — the AUD-04 gate code block appears BEFORE this call (verified by line numbers)
    - `grep -n "start_proxy_runtime" crates/nono-cli/src/execution_runtime.rs` — the AUD-04 gate code block appears AFTER this call (verified by line numbers)
    - `cargo test -p nono-cli --lib telemetry::` exits 0 (all 4 emit_override_event tests pass)
    - `cargo test -p nono-cli` exits 0 (no regressions in existing CLI tests)
    - `make test` exits 0 (full workspace; includes the 4 pre-existing nono-cli Windows baseline failures — do NOT chase those as regressions per MEMORY note on nono_cli_windows_baseline_test_failures)
    - Cross-target clippy: attempt `cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used`; mark PARTIAL→CI if cross-toolchain absent
  </acceptance_criteria>
  <done>emit_override_event method added with #[must_use] and AUD-04 fail-closed behavior; SECURITY_LAYER OnceLock populated by init_tracing; AUD-04 pre-spawn gate in execute_sandboxed; 4 telemetry tests green; cargo test passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nono-py → nono.exe process boundary | --override-audit base64-JSON metadata crosses as a CLI arg (untrusted process boundary) |
| execute_sandboxed ↔ SecurityEventLayer | AUD-04 gate calls emit_override_event before spawn; Err must abort (never silently succeed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-FAILCLOSED | Elevation of Privilege | execute_sandboxed AUD-04 gate (execution_runtime.rs) | mitigate | emit_override_event returns Result; execute_sandboxed returns Err before spawn on emit failure; no child ever runs override paths without a committed audit record (AUD-04) |
| T-92-MUTEX | Tampering | SecurityEventLayer::emit_override_event mutex poison | mitigate | map_err on lock() returns Err("mutex poisoned"); AUD-04 caller treats Err as fatal abort (D-02 bilateral gate) |
| T-92-META-FORGE | Tampering | --override-audit base64-JSON flag (forged metadata) | accept | nono-cli treats metadata as log-only (no re-verification); security invariant is the OS confinement layer applying to the already-resolved CapabilitySet + the AUD-04 pre-spawn audit record; forged metadata is harmless to security, only to auditability |
| T-92-WINDOW | Elevation of Privilege | AUD-04 gate placement relative to early-return paths | mitigate | Gate placed AFTER start_proxy_runtime (line 242) and BEFORE apply_pre_fork_sandbox (line 261) per RESEARCH.md Pitfall 3; no early-return path between those lines can skip the gate |
| T-92-DENY_UNKNOWN | Tampering | OverrideAuditMeta deserialization (--override-audit value) | mitigate | serde(deny_unknown_fields) on OverrideAuditMeta; unexpected JSON fields cause deserialization Err → SandboxInit error before spawn |
| T-92-SC | Tampering | npm/pip/cargo installs | accept | No new packages; base64 and serde_json are existing workspace deps |
</threat_model>

<verification>
Full Nono repo check after this plan:
- `cargo test -p nono-cli --lib telemetry::` exits 0 (emit_override_event tests)
- `cargo test -p nono-cli` exits 0 (excluding known pre-existing 4 Windows baseline failures)
- `make test` exits 0 (workspace)
- `cargo build --workspace` exits 0
</verification>

<success_criteria>
- `OverrideAuditMeta` struct exists in `cli.rs` with `serde(deny_unknown_fields)` and fields: `zt_audit_hash: Option<String>`, `kms_key_id`, `jti`, `granted_paths`, `expires_at`
- `SandboxArgs.override_audit: Option<String>` with `hide = true` arg attribute
- `ExecutionFlags.override_audit: Option<OverrideAuditMeta>` initialized to `None` in defaults
- `SECURITY_LAYER: OnceLock<Arc<SecurityEventLayer>>` static in `telemetry/mod.rs`; populated by `init_tracing` and `init_daemon_telemetry`
- `emit_override_event(event_type, jti, kms_key_id, zt_audit_hash)` method on `SecurityEventLayer`; `#[must_use]`; returns `Err` on poisoned mutex
- AUD-04 gate in `execute_sandboxed`: present after `start_proxy_runtime`, before `apply_pre_fork_sandbox`; returns `Err(NonoError::SandboxInit(...))` if emit fails
- 4 `emit_override_event` unit tests green (chain advance, mutex poison, two-call advance, None zt_audit_hash)
- 3 `OverrideAuditMeta` deserialization tests green
- `make test` exits 0 (workspace)
</success_criteria>

<output>
Create `.planning/phases/92-runtime-capabilityset-mutation-audit-wiring/92-03-SUMMARY.md` when done.
</output>
