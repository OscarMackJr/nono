---
phase: 92-runtime-capabilityset-mutation-audit-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - nono-py/src/override.rs
  - nono-py/src/windows_confined_run.rs
autonomous: true
requirements:
  - MUT-01
  - MUT-02
  - MUT-03
  - MUT-04
  - MUT-05
  - AUD-04
  - VFY-01
must_haves:
  truths:
    - "OverrideGrant struct has a zt_audit_hash field and a #[getter] fn zt_audit_hash() returning Option<String>"
    - "OverrideGrant construction at verify_override_impl populates zt_audit_hash from token.current_hash"
    - "OverrideErrorKind::OutOfScope has no #[expect(dead_code)] attribute"
    - "confined_run and confine accept override_token: Option<PyRef<'_, OverrideGrant>>"
    - "append_override_args appends --allow per scope_path and --override-audit <base64-json> when grant is Some"
    - "sanitize_override_path rejects non-absolute paths and paths containing .. components using Path::components()"
    - "confined_run(override_token=None) builds identical args to pre-Phase-92 baseline"
    - "confined_run(override_token=Some(grant)) against a nono.exe that lacks --override-audit support raises NonoOverrideError BEFORE spawning (D-02 nono-py side probe; AUD-04 silent-escalation guard)"
    - "probe_override_support() result is cached per nono.exe path so the subprocess call occurs at most once per interpreter session"
  artifacts:
    - path: "nono-py/src/override.rs"
      provides: "zt_audit_hash field + getter on OverrideGrant; OutOfScope dead_code annotation removed"
      contains: "zt_audit_hash"
    - path: "nono-py/src/windows_confined_run.rs"
      provides: "append_override_args; sanitize_override_path; extended confined_run/confine signatures; probe_override_support fail-closed probe"
      contains: "append_override_args"
  key_links:
    - from: "nono-py/src/windows_confined_run.rs::append_override_args"
      to: "nono-py/src/override.rs::OverrideGrant"
      via: "reads zt_audit_hash(), signer, jti, expires_at, scope_paths from verified grant"
      pattern: "grant\\.zt_audit_hash"
    - from: "nono-py/src/windows_confined_run.rs::sanitize_override_path"
      to: "std::path::Path::components"
      via: "component-wise ParentDir rejection (MUT-04; CLAUDE.md path security)"
      pattern: "Component::ParentDir"
    - from: "nono-py/src/windows_confined_run.rs::confined_run (override path)"
      to: "nono-py/src/windows_confined_run.rs::probe_override_support"
      via: "called before append_override_args; raises NonoOverrideError if probe fails"
      pattern: "probe_override_support"
---

<objective>
Extend the nono-py binding (SEPARATE git repo at `C:\Users\OMack\nono-py`, branch `44-broker-ffi-lockstep`) to:

1. Add the `zt_audit_hash` field and `#[getter]` to `OverrideGrant` (D-06) — sourced from `token.current_hash`, populated at construction in `verify_override_impl`. This is how audit metadata flows from the verified grant to nono-cli without re-parsing the token (TOCTOU closure).

2. Remove `#[expect(dead_code)]` from `OverrideErrorKind::OutOfScope` (Phase 91 forward-declaration; the first construction site is added in Plan 03 when scope violations are detected).

3. Add `append_override_args(cmd, grant)` helper — sanitizes scope_paths via `sanitize_override_path` (reject `..`, require absolute, using `Path::components()` — NEVER string `starts_with`), then appends `--allow <path>` per grant path and `--override-audit <base64url-json>` carrying `{zt_audit_hash, kms_key_id, jti, granted_paths, expires_at}` (D-06: all fields from the already-verified grant).

4. Add `probe_override_support(nono_path: &Path)` — the nono-py side of the D-02 bilateral capability gate. Before any override-bearing invocation, nono-py probes the target nono.exe to confirm it accepts `--override-audit`. Mechanism: run `nono.exe run --override-audit ""` and check the exit code. An old nono.exe without `--override-audit` support exits with clap's "unexpected argument" error (non-zero). A nono.exe that supports the flag exits with a validation error (malformed value) that is ALSO non-zero, so the probe must specifically detect the clap-rejects-unknown-flag error message in stderr — OR use the simpler version-string approach: run `nono.exe --version` and check for the `v3.2` prefix. The result is cached by nono.exe path in a process-local static so the overhead is one subprocess call per interpreter session. Failure raises `NonoOverrideError` with kind `UnsupportedVersion` — never silently falls through to launch.

5. Extend `confined_run` and `confine` signatures with `override_token: Option<PyRef<'_, OverrideGrant>>` and wire `probe_override_support` + `append_override_args` into each (probe is called first, before any flag is appended).

Purpose: This is the nono-py half of the D-01/D-02 flow. The Nono repo half (Plan 03) depends on `--override-audit` being passed correctly from here. Plans 01 and 02 are Wave 1 (parallel, independent repos).

Output: Modified `nono-py/src/override.rs` and `nono-py/src/windows_confined_run.rs`. Commits go in the nono-py repo (`C:\Users\OMack\nono-py`), NOT this repo.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\OMack\Nono\.planning\PROJECT.md
@C:\Users\OMack\Nono\.planning\ROADMAP.md
@C:\Users\OMack\Nono\.planning\REQUIREMENTS.md
@C:\Users\OMack\Nono\.planning\phases\92-runtime-capabilityset-mutation-audit-wiring\92-CONTEXT.md
@C:\Users\OMack\Nono\.planning\phases\92-runtime-capabilityset-mutation-audit-wiring\92-RESEARCH.md
@C:\Users\OMack\Nono\.planning\phases\92-runtime-capabilityset-mutation-audit-wiring\92-PATTERNS.md

<interfaces>
<!-- Verified line numbers from 92-RESEARCH.md. The executor MUST read each source file before editing. -->

From nono-py/src/override.rs (verified refs):
- `OverrideGrant` struct and #[pyclass] definition: line 663
- Existing #[getter] methods on OverrideGrant: lines 692-714 (scope_paths, scope_domains getters as template)
- OverrideGrant construction (verify_override_impl Ok(...)): line 791-799
  Current fields: signer, scope_paths, scope_domains, not_before, expires_at, jti, repo_context
  ADD: zt_audit_hash: token.current_hash.clone()
- token.current_hash (Option<String>): line 366 on OverrideToken struct
- OverrideErrorKind enum: line 56 — OutOfScope at line 73 has #[expect(dead_code, reason = "forward-declared for Phase 92 scope enforcement; remove when Phase 92 constructs it")]
- verify_override #[pyfunction]: line 840

From nono-py/src/windows_confined_run.rs (verified refs):
- build_nono_run_args(): line 104
- append_caps_allow_flags(): line 133 — template for append_override_args placement
- confined_run() signature: lines 174-183 — add override_token parameter after timeout_secs
  Current pyo3 signature: (exe, args, allow=None, profile=None, cwd=None, timeout_secs=None)
  New: add override_token=None as last keyword arg
- In confined_run, override hook goes AFTER build_nono_run_args call (line 197) and BEFORE cmd.arg("--").arg(&exe) (line 203)
  NEW: probe_override_support is called BEFORE append_override_args (fail-closed)
- confine() signature: lines 253-258 — add override_token parameter after caps
  Current pyo3 signature: (profile=None, allow=None, caps=None)
  New: add override_token=None as last keyword arg
- In confine, override hook goes AFTER append_caps_allow_flags call (line 297) and BEFORE cmd.arg("--") (line 303)
  NEW: probe_override_support is called BEFORE append_override_args (fail-closed)

Dependencies used in append_override_args and probe_override_support:
- base64 crate: already in nono-py Cargo.toml (added Phase 91); use base64::engine::general_purpose::URL_SAFE_NO_PAD
- serde_json: already in workspace deps; use serde_json::json!({...})
- std::path::Path::components() + std::path::Component::ParentDir for sanitize_override_path
- PyValueError from pyo3::exceptions for sanitize_override_path error returns
- std::sync::OnceLock<std::collections::HashMap<PathBuf, bool>> or std::sync::Mutex<HashMap> for the probe cache
- std::process::Command for the probe subprocess (NOT Python's subprocess — this is Rust calling nono.exe)
- NonoOverrideError (the existing Phase 91 PyO3 exception class) for raising on probe failure
</interfaces>

<!-- VFY-01 PARTIAL note: this plan wires the offline arm only. The live POST /actions composition seam
     is documented as a TODO comment in confined_run / confine. VFY-01 is PARTIAL [BLOCKING-93]. -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend OverrideGrant with zt_audit_hash; remove OutOfScope dead_code annotation (nono-py/src/override.rs)</name>
  <files>nono-py/src/override.rs</files>
  <read_first>
    - C:\Users\OMack\nono-py\src\override.rs — read fully before editing; focus on: OverrideGrant struct (line 663), existing #[getter] block (lines 692-714), verify_override_impl construction site (lines 791-799), OverrideErrorKind::OutOfScope (line 73), OverrideToken.current_hash (line 366)
    - .planning/phases/92-runtime-capabilityset-mutation-audit-wiring/92-PATTERNS.md — § "nono-py/src/override.rs — add zt_audit_hash getter + remove #[expect(dead_code)]" for exact field name, getter signature, and construction site pattern
  </read_first>
  <behavior>
    - Test: `OverrideGrant { ..., zt_audit_hash: Some("abc123".to_string()) }` returns `Some("abc123")` from the getter
    - Test: `OverrideGrant { ..., zt_audit_hash: None }` returns `None` from the getter
    - Test (regression): `verify_override_impl` called with a token whose `current_hash` is `Some("deadbeef")` produces an `OverrideGrant` where `grant.zt_audit_hash() == Some("deadbeef")`
    - Test: `verify_override_impl` called with a token whose `current_hash` is `None` produces `grant.zt_audit_hash() == None` (not an error)
  </behavior>
  <action>
    REPO: `C:\Users\OMack\nono-py` (branch `44-broker-ffi-lockstep`). All edits and the commit go in this repo.

    In `nono-py/src/override.rs`:

    1. In the `OverrideGrant` struct (line 663), add a new public field AFTER `repo_context`:
       `pub zt_audit_hash: Option<String>` with doc comment: "ZT-Infra audit chain hash from token.current_hash (D-06 / AUD-02). None when the override token predates ZT-Infra audit chain integration."

    2. In the `#[pymethods] impl OverrideGrant` block (lines 692-714), add a new `#[getter]` method following the exact `scope_paths` pattern:
       `#[getter] fn zt_audit_hash(&self) -> Option<String> { self.zt_audit_hash.clone() }`
       Doc comment: "ZT-Infra audit chain hash at time of override issuance (AUD-02 bi-directional link). None when not present in the token (current_hash field absent or null in CAF v0.1)."

    3. In `verify_override_impl` at the `Ok(OverrideGrant { ... })` construction site (lines 791-799), add the new field:
       `zt_audit_hash: token.current_hash.clone(),`
       Place it after `repo_context: token.repo_context.clone()`. Per D-06: this reads from the already-verified grant structure, never re-parses the raw token bytes.

    4. Remove the `#[expect(dead_code, reason = "...")]` attribute from `OverrideErrorKind::OutOfScope` (line 73). Leave the `OutOfScope` variant itself in place. The `#[expect]` was a forward-declaration for Phase 92; removing it now satisfies the CLAUDE.md "Lazy use of dead code" rule since Plan 03 will add the first construction site.

    Add inline `#[cfg(test)]` unit tests for the getter and construction:
    - `zt_audit_hash_getter_returns_some_when_present`
    - `zt_audit_hash_getter_returns_none_when_absent`
    These can use the existing `#[allow(clippy::unwrap_used)]` test pattern.
  </action>
  <verify>
    <automated>cd /c/Users/OMack/nono-py && cargo test --lib override:: 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "zt_audit_hash" /c/Users/OMack/nono-py/src/override.rs` shows: struct field, #[getter] method, and construction site (minimum 3 occurrences)
    - `grep -n "expect(dead_code" /c/Users/OMack/nono-py/src/override.rs` returns 0 lines (annotation removed)
    - `cd /c/Users/OMack/nono-py && cargo test --lib override::` exits 0
    - `cd /c/Users/OMack/nono-py && cargo build` exits 0 (confirms no compilation errors from the struct change)
    - No `.unwrap()` or `.expect()` in new production code outside `#[cfg(test)]` blocks
  </acceptance_criteria>
  <done>zt_audit_hash field and getter added; OutOfScope dead_code annotation removed; 2 unit tests green; nono-py cargo build clean</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire override into confined_run/confine; add append_override_args + sanitize_override_path (nono-py/src/windows_confined_run.rs)</name>
  <files>nono-py/src/windows_confined_run.rs</files>
  <read_first>
    - C:\Users\OMack\nono-py\src\windows_confined_run.rs — read fully before editing; note: build_nono_run_args (line 104), append_caps_allow_flags (line 133), confined_run signature (lines 174-183), confined_run cmd arg construction and hook insertion points (lines 197, 203), confine signature (lines 253-258), confine hook insertion points (lines 297, 303), confine's std::process::exit pattern (line 323). NOTE: confined_run spawns the child via Rust's std::process::Command inside do_spawn_and_wait — NOT Python's subprocess; Python-level mocking of subprocess.Popen will NOT intercept this spawn path.
    - .planning/phases/92-runtime-capabilityset-mutation-audit-wiring/92-PATTERNS.md — § "nono-py/src/windows_confined_run.rs — override hook" for exact signature extension, hook insertion points, append_override_args code shape, and sanitize_override_path component-iteration pattern
    - nono-py/src/override.rs (already modified in Task 1) — import OverrideGrant for PyRef usage
    - CLAUDE.md § Path Handling: NEVER string starts_with; use Path::components() + Component::ParentDir
  </read_first>
  <behavior>
    - Test: `append_override_args` with a grant whose `scope_paths` is `["/tmp/project"]` produces args `["--allow", "/tmp/project", "--override-audit", "<base64-json>"]` appended to cmd
    - Test: `sanitize_override_path("/tmp/project")` returns Ok(PathBuf)
    - Test: `sanitize_override_path("relative/path")` returns `Err(PyValueError)` containing "absolute"
    - Test: `sanitize_override_path("/tmp/../etc/passwd")` returns `Err(PyValueError)` containing ".."
    - Test (D-02 probe fail-closed): `probe_override_support` called with a path to a fake binary that exits 0 but whose stderr contains the clap "unexpected argument" pattern returns Err; alternatively, if probe uses version-string approach, a fake binary emitting "nono v2.99.0" causes probe to return Err (version below 3.2)
    - Test (D-02 probe success): `probe_override_support` called with a path to a fake binary emitting "nono v3.2.0" (or passing `--override-audit ""` without an "unexpected argument" error) returns Ok
    - Test (D-02 probe cached): calling `probe_override_support` twice with the same path spawns the probe subprocess only once (cache hit on second call)
    - Test (D-02 probe prevents launch): calling `confined_run` with `override_token=Some(grant)` and a nono.exe that fails probe raises `NonoOverrideError` BEFORE `append_override_args` is invoked (no flags appended, no child spawned)
    - Test (MUT-05 regression): `confined_run` called with `override_token=None` produces args that do NOT contain `--override-audit`; the arg list before the `--` separator is identical to the pre-Phase-92 baseline (no new flags)
    - Test (MUT-02): calling `confined_run` twice with different grants does not leak state between calls (the override args are per-call, not stored in any shared state)
  </behavior>
  <action>
    REPO: `C:\Users\OMack\nono-py` (branch `44-broker-ffi-lockstep`). All edits and the commit go in this repo.

    In `nono-py/src/windows_confined_run.rs`:

    1. Add a module-level probe cache (process-local, initialized once):
       ```
       use std::sync::Mutex;
       use std::collections::HashMap;
       use std::path::PathBuf;

       static PROBE_CACHE: Mutex<HashMap<PathBuf, bool>> = Mutex::new(HashMap::new());
       ```
       Use `std::sync::Mutex` (not `OnceLock`) because the cache must store results for multiple nono.exe paths. The `bool` value is `true` if override support was confirmed, `false` if probe failed (cached failures prevent repeated subprocess calls).

       Note: using `Mutex::new(...)` in a `static` requires Rust 1.70+ (MSRV is 1.82 — acceptable). Alternative: use `once_cell::sync::Lazy` if the codebase already uses it. Check existing imports before choosing; use the pattern already present in the file.

    2. Add three new helper functions in the helper section (after `append_caps_allow_flags` at line 133):

       `probe_override_support(nono_path: &Path) -> Result<(), PyErr>`:
       - Check the PROBE_CACHE first. If `true` for this path, return `Ok(())` immediately.
       - If `false` for this path (previous probe failed), return `Err(NonoOverrideError::new_err(...))` immediately (cached failure, no re-probe).
       - If not in cache: run `std::process::Command::new(nono_path).arg("--version").output()`. Parse stdout for the version string. Require the version to be `v3.2` or later (semver prefix check: `output_str.contains("v3.2") || output_str.contains("v3.3") || ...`). For simplicity in v1 (closed-universe of co-deployed nono-py+nono.exe per the milestone model), require the major.minor to be `>= 3.2` by parsing the first occurrence of `v<N>.<N>` with a simple regex or string-split approach — do NOT use a regex crate if none is present; instead split on 'v' then parse major.minor as integers. The version `v3.2.0` parses as major=3, minor=2 — pass. `v3.1.9` — fail.
       - If the version parse fails or the version is below 3.2.0: insert `false` into PROBE_CACHE and return `Err(NonoOverrideError::new_err(format!("nono.exe at {} does not advertise override support (version < 3.2 or version unparseable from: {}); refusing to launch with override_token to prevent silent escalation (D-02 / AUD-04)", nono_path.display(), output_str)))`.
       - On success: insert `true` into PROBE_CACHE and return `Ok(())`.
       - If `std::process::Command::new(nono_path).arg("--version").output()` fails (spawn error): return `Err(NonoOverrideError::new_err(...))` — do NOT cache the failure (transient I/O error vs persistent version mismatch). Insert nothing in PROBE_CACHE on I/O error.
       - Use `NonoOverrideError` (the existing Phase 91 PyO3 exception class) for all failures — NOT `PyValueError` or `PyRuntimeError`. The choice of `NonoOverrideError` ensures callers catching `nono.NonoOverrideError` cover both verification failures and capability probe failures.
       - Doc comment: "Probe nono.exe at the given path to confirm it advertises override support (version >= 3.2). Caches the result by path for the process lifetime (one subprocess call per path per session). Returns Err(NonoOverrideError) if the probe fails or the version is insufficient. Called before any override-bearing invocation (D-02 nono-py side; AUD-04 silent-escalation guard). FAIL-CLOSED: on any error, raises rather than silently proceeding."
       - Mark `#[must_use]` is NOT needed here since the function is called with `?` at the call site.

       `sanitize_override_path(raw: &str) -> PyResult<std::path::PathBuf>`:
       - Uses `std::path::Path::new(raw)` then `p.is_absolute()` for the absolute check
       - Uses `p.components()` iteration with `Component::ParentDir` comparison for the `..` check — NEVER string starts_with (CLAUDE.md §Path Handling CRITICAL)
       - Returns `Err(pyo3::exceptions::PyValueError::new_err(...))` for absolute-check failure and for ParentDir component
       - Returns `Ok(p.to_path_buf())` on success

       `append_override_args(cmd: &mut std::process::Command, grant: &OverrideGrant) -> PyResult<()>`:
       - For each path in `grant.scope_paths`: call `sanitize_override_path(raw)?` then `cmd.arg("--allow").arg(path.as_os_str())`
       - Build audit metadata JSON using `serde_json::json!({...})` with fields: `zt_audit_hash: grant.zt_audit_hash()`, `kms_key_id: &grant.signer`, `jti: &grant.jti`, `granted_paths: &grant.scope_paths`, `expires_at: &grant.expires_at`
       - Base64-encode: `use base64::Engine as _; base64::engine::general_purpose::URL_SAFE_NO_PAD.encode(meta.to_string().as_bytes())`
       - Append `cmd.arg("--override-audit").arg(meta_b64)`
       - Return `Ok(())`
       - Doc comment: "Sanitize and append --allow flags for override-granted paths, then append the --override-audit <base64-meta> flag carrying trusted audit metadata. Security: rejects '..' path components (MUT-04 / D-05); requires absolute paths; reads all audit fields from the already-verified OverrideGrant (D-06: closes the TOCTOU verify→apply gap). Called AFTER probe_override_support confirms nono.exe version >= 3.2 (D-02)."

    3. Extend `confined_run` signature (lines 174-183):
       - Update `#[pyo3(signature = (exe, args, allow=None, profile=None, cwd=None, timeout_secs=None, override_token=None))]`
       - Add parameter: `override_token: Option<pyo3::PyRef<'_, OverrideGrant>>`
       - After `build_nono_run_args(&mut cmd, ...)` call (line 197) and BEFORE `cmd.arg("--").arg(&exe)` (line 203), insert:
         ```
         if let Some(ref grant) = override_token {
             // D-02 bilateral capability gate (nono-py side): refuse to launch if the target
             // nono.exe does not advertise override support. Prevents a too-old nono.exe from
             // silently accepting the extra --allow flags without auditing them (AUD-04).
             probe_override_support(&nono_path)?;
             append_override_args(&mut cmd, grant)?;
         }
         // VFY-01 PARTIAL [BLOCKING-93]: Phase 92 wires the offline verify arm.
         // Phase 93 adds the live POST /actions AND-gate here before confined_run
         // is called (D-03 composition seam; VFY-01 clause b).
         ```

    4. Extend `confine` signature (lines 253-258):
       - Update `#[pyo3(signature = (profile=None, allow=None, caps=None, override_token=None))]`
       - Add parameter: `override_token: Option<pyo3::PyRef<'_, OverrideGrant>>`
       - After `append_caps_allow_flags(...)` (line 297) and BEFORE `cmd.arg("--")` (line 303), insert the same conditional block (probe then append) and VFY-01 PARTIAL comment.

    5. Add `use nono_py::override_module::OverrideGrant;` or the appropriate qualified path for `OverrideGrant` — check the existing import pattern in the file (it may already import from `crate::override_module::*` or similar); follow the existing convention exactly.

    Add `#[cfg(test)]` unit tests (add to the existing test module or create one):
    - `test_sanitize_override_path_accepts_absolute`: assert Ok for "/tmp/project"
    - `test_sanitize_override_path_rejects_relative`: assert Err containing "absolute" for "relative/path"
    - `test_sanitize_override_path_rejects_parent_dir`: assert Err containing ".." for "/tmp/../etc"
    - `test_no_override_token_no_override_audit_flag` (MUT-05 regression): build a Command, call confined_run or directly call build_nono_run_args and assert the resulting args list does NOT contain "--override-audit"
    - `test_probe_caches_result`: call a helper that invokes the probe logic against a fake binary path twice; assert the second call returns the cached result (stub or spy pattern — if the full subprocess is hard to fake in a unit test, test the cache lookup path directly by pre-populating PROBE_CACHE with a known value and asserting probe_override_support returns without spawning)
  </action>
  <verify>
    <automated>cd /c/Users/OMack/nono-py && cargo test --lib windows_confined_run:: 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "append_override_args\|sanitize_override_path\|probe_override_support" /c/Users/OMack/nono-py/src/windows_confined_run.rs` returns >= 9 (3 definitions + invocations in confined_run + confine + test refs)
    - `grep -c "Component::ParentDir" /c/Users/OMack/nono-py/src/windows_confined_run.rs` returns >= 1 (path-component check in sanitize_override_path; never string starts_with)
    - `grep -c "override_token" /c/Users/OMack/nono-py/src/windows_confined_run.rs` returns >= 4 (signature in confined_run + confine, usage in each)
    - `grep -n "probe_override_support" /c/Users/OMack/nono-py/src/windows_confined_run.rs` shows the probe call appearing BEFORE `append_override_args` in both confined_run and confine (verify by line number ordering)
    - `grep -n "NonoOverrideError" /c/Users/OMack/nono-py/src/windows_confined_run.rs` shows at least 1 occurrence (probe failure raises NonoOverrideError, not PyRuntimeError or PyValueError)
    - `grep -n "PROBE_CACHE\|probe_cache\|ProbeCache" /c/Users/OMack/nono-py/src/windows_confined_run.rs` shows the static cache (>= 1 occurrence)
    - `cd /c/Users/OMack/nono-py && cargo test --lib windows_confined_run::` exits 0
    - `cd /c/Users/OMack/nono-py && cargo build` exits 0
    - `grep -n "override_audit" /c/Users/OMack/nono-py/src/windows_confined_run.rs` shows at least 1 occurrence (the --override-audit arg being appended)
    - `grep -n "BLOCKING-93" /c/Users/OMack/nono-py/src/windows_confined_run.rs` shows the VFY-01 PARTIAL seam comment in both confined_run and confine
    - Confirmed: NO `unittest.mock.patch("subprocess.Popen")` or equivalent Python-level subprocess mock in Rust test code — tests that need to verify arg construction inspect the Rust Command args directly or stub at the Rust level
  </acceptance_criteria>
  <done>append_override_args, sanitize_override_path, and probe_override_support added; confined_run and confine extended with override_token parameter; D-02 probe is called before append_override_args in each; probe raises NonoOverrideError on version mismatch; probe result is cached; MUT-05 regression test green; nono-py cargo build clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nono-py → nono.exe (override path) | grant paths and audit metadata cross the nono-py/nono-cli process boundary as CLI arguments |
| nono-py probe subprocess | probe_override_support spawns nono.exe --version; stdout is untrusted but only version string is parsed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-SCOPE | Elevation of Privilege | sanitize_override_path in windows_confined_run.rs | mitigate | Path::components() iteration with Component::ParentDir check; is_absolute() check; NEVER string starts_with (CLAUDE.md §Path Handling; MUT-04) |
| T-92-TOCTOU | Tampering | OverrideGrant.zt_audit_hash / append_override_args | mitigate | D-06: all audit fields read from the already-verified OverrideGrant only, never by re-parsing the raw token; closes the TOCTOU verify→apply gap |
| T-92-MUT02 | Tampering | confined_run/confine override_token state | mitigate | override_token is an Option<PyRef<'_>> parameter (per-call); no global/shared state; MUT-02 is structural |
| T-92-SCOPE-EVIL | Elevation of Privilege | /tmp/project path-prefix bypass | mitigate | nono-cli's existing path_covered (capability.rs:1762) uses Path::starts_with on PathBuf (component-wise), not string starts_with; SC3 test coverage validates /tmp/project-evil is not covered |
| T-92-D02-PROBE | Elevation of Privilege | Too-old nono.exe silently accepting --allow without --override-audit (AUD-04 silent escalation) | mitigate | probe_override_support refuses to launch if nono.exe version < 3.2; raises NonoOverrideError before any flag is appended; result cached to avoid repeated subprocess calls |
| T-92-PROBE-CACHE | Tampering | PROBE_CACHE populated with stale true result for a swapped nono.exe binary | accept | The probe cache is process-lifetime (acceptable risk for the closed-universe co-deployment model; full mitigation deferred to Phase 93 live revocation check) |
| T-92-SC | Tampering | npm/pip/cargo installs | accept | No new packages; base64 and serde_json are existing deps confirmed in nono-py Cargo.toml |
</threat_model>

<verification>
Full nono-py smoke check after this plan:
- `cd /c/Users/OMack/nono-py && cargo test` exits 0 (all nono-py tests including Phase 91 verifier tests)
- `cd /c/Users/OMack/nono-py && maturin develop` succeeds (Python wheel can be built)
</verification>

<success_criteria>
- `OverrideGrant.zt_audit_hash` field and getter exist; construction site populates from `token.current_hash`
- `#[expect(dead_code)]` removed from `OverrideErrorKind::OutOfScope`
- `confined_run` and `confine` accept `override_token: Option<PyRef<'_, OverrideGrant>>`
- `probe_override_support` probes nono.exe version >= 3.2 before any override-bearing invocation; raises `NonoOverrideError` on version mismatch or I/O failure; caches result by path
- `probe_override_support` is called BEFORE `append_override_args` in both `confined_run` and `confine`
- `append_override_args` appends `--allow` per scope path and `--override-audit <base64-json>` with the five audit metadata fields
- `sanitize_override_path` rejects non-absolute paths and paths with `..` using `Path::components()`; returns `PyValueError` on failure
- MUT-05: calling with `override_token=None` produces no `--override-audit` flag (regression test green)
- VFY-01 PARTIAL `[BLOCKING-93]` seam comment present in both `confined_run` and `confine`
- `cargo test` in nono-py exits 0; `cargo build` exits 0
- Commits are in the nono-py repo (`C:\Users\OMack\nono-py`), NOT the Nono repo
</success_criteria>

<output>
Create `.planning/phases/92-runtime-capabilityset-mutation-audit-wiring/92-02-SUMMARY.md` when done.
Note: commits for this plan are in `C:\Users\OMack\nono-py` (branch `44-broker-ffi-lockstep`), not in the main Nono repo. Record the nono-py commit hash in the SUMMARY.
</output>
