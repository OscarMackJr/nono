---
phase: 92-runtime-capabilityset-mutation-audit-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono/src/audit.rs
  - crates/nono-cli/src/telemetry/event.rs
autonomous: true
requirements:
  - AUD-01
  - AUD-03
must_haves:
  truths:
    - "AuditEventPayload::PolicyOverrideApplied variant exists in crates/nono/src/audit.rs with fields jti, kms_key_id, zt_audit_hash, granted_path_hashes, expires_at"
    - "SecurityEventType has 10 variants (5 existing + 5 new PolicyOverride*); event_id_for and severity_for are exhaustive"
    - "EventID constants EVENT_ID_POLICY_OVERRIDE_PRESENTED through EVENT_ID_POLICY_OVERRIDE_REVOKED (10006–10010) are defined in event.rs"
    - "cargo test -p nono --lib audit passes; cargo test -p nono-cli --lib telemetry::event passes"
  artifacts:
    - path: "crates/nono/src/audit.rs"
      provides: "PolicyOverrideApplied variant in AuditEventPayload enum"
      contains: "PolicyOverrideApplied"
    - path: "crates/nono-cli/src/telemetry/event.rs"
      provides: "EventID constants 10006–10010; SecurityEventType PolicyOverride* variants"
      contains: "EVENT_ID_POLICY_OVERRIDE_PRESENTED"
  key_links:
    - from: "crates/nono-cli/src/telemetry/event.rs"
      to: "crates/nono-cli/src/telemetry/mod.rs"
      via: "SecurityEventType enum used in event_id_for / severity_for"
      pattern: "SecurityEventType::PolicyOverride"
    - from: "crates/nono/src/audit.rs"
      to: "crates/nono-cli"
      via: "AuditEventPayload::PolicyOverrideApplied constructed in emit_override_event (Plan 03)"
      pattern: "PolicyOverrideApplied"
---

<objective>
Add the two data-model additions that all downstream Phase 92 work depends on:

1. `AuditEventPayload::PolicyOverrideApplied` — a new variant in the core `nono` crate's audit enum. This is a **data carrier only** (policy-free core invariant). It carries the tamper-evident link fields (`zt_audit_hash`, `kms_key_id`, `jti`) and path-hashed grant paths (`granted_path_hashes`) for AUD-02/AUD-03. No policy logic in the core crate.

2. EventID constants 10006–10010 and five new `SecurityEventType` variants (`PolicyOverridePresented`, `PolicyOverrideVerified`, `PolicyOverrideRejected`, `PolicyOverrideExpired`, `PolicyOverrideRevoked`) in `telemetry/event.rs`. These are the EventID range mandated by AUD-03 and ROADMAP SC4. All exhaustive `match` arms in `event.rs` (`event_id_for`) must be updated.

Purpose: Plans 02, 03, and 04 all depend on these types existing. Wave 1 — no dependencies.

Output: Modified `crates/nono/src/audit.rs` and `crates/nono-cli/src/telemetry/event.rs`; all Rust tests green.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\OMack\Nono\.planning\PROJECT.md
@C:\Users\OMack\Nono\.planning\ROADMAP.md
@C:\Users\OMack\Nono\.planning\REQUIREMENTS.md
@C:\Users\OMack\Nono\.planning\phases\92-runtime-capabilityset-mutation-audit-wiring\92-CONTEXT.md
@C:\Users\OMack\Nono\.planning\phases\92-runtime-capabilityset-mutation-audit-wiring\92-RESEARCH.md
@C:\Users\OMack\Nono\.planning\phases\92-runtime-capabilityset-mutation-audit-wiring\92-PATTERNS.md

<interfaces>
<!-- Verified line numbers from 92-RESEARCH.md. -->

From crates/nono/src/audit.rs (line 67):
```rust
#[derive(Clone, Serialize, Deserialize)]
#[serde(tag = "type", rename_all = "snake_case")]
pub enum AuditEventPayload {
    SessionStarted { ... },
    SessionEnded { ended: DateTime<Utc>, exit_code: Option<i32> },
    CapabilityDecision { ... },
    UrlOpen { ... },
    Network { event: nono_proxy::ProxyAuditEvent },
}
```
`AuditEventPayload` is used only for construction (no exhaustive match sites in the codebase).
Redaction pattern for optional fields (lines 94-95): `#[serde(default, skip_serializing_if = "Option::is_none")]`

From crates/nono-cli/src/telemetry/event.rs (lines 31-41, 52-64, 71-79):
```rust
// EventID constants (lines 31-41)
pub const EVENT_ID_PATH_DENY: u32 = 10001;
pub const EVENT_ID_NETWORK_DENY: u32 = 10002;
pub const EVENT_ID_LABEL_VIOLATION: u32 = 10003;
pub const EVENT_ID_HOOK_FAIL_CLOSED: u32 = 10004;
pub const EVENT_ID_TELEMETRY_DEGRADED: u32 = 10005;

// SecurityEventType enum (lines 52-64)
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum SecurityEventType {
    PathDeny, NetworkDeny, LabelViolation, HookFailClosed, TelemetryDegraded,
}

// event_id_for exhaustive match (lines 71-79)
#[must_use]
pub fn event_id_for(t: &SecurityEventType) -> u32 { match t { ... } }
```
`event.rs` is cfg-unconditional — new variants must also be exhaustive in `severity_for` in `mod.rs` (lines 154-162). Cross-target clippy is MANDATORY after this change.

`path_hash_for(session_salt: &[u8], path: &Path) -> String` exists in event.rs — use for `granted_path_hashes` in audit-record tests.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PolicyOverrideApplied to AuditEventPayload (crates/nono/src/audit.rs)</name>
  <files>crates/nono/src/audit.rs</files>
  <read_first>
    - crates/nono/src/audit.rs — read fully before editing; note the AuditEventPayload enum at line 67, the serde tag/rename_all attributes, the #[serde(default, skip_serializing_if = "Option::is_none")] pattern on optional fields (lines 94-95 and 75-76), and existing variant field styles
    - .planning/phases/92-runtime-capabilityset-mutation-audit-wiring/92-PATTERNS.md — § "crates/nono/src/audit.rs — add PolicyOverrideApplied variant" for the exact field names, serde attributes, and doc-comment template
    - CLAUDE.md — policy-free core invariant: this variant is a data carrier ONLY; no logic in the core crate
  </read_first>
  <behavior>
    - Test: `AuditEventPayload::PolicyOverrideApplied { jti: "test-jti".into(), kms_key_id: "arn:aws:kms:test".into(), zt_audit_hash: Some("abc123".into()), granted_path_hashes: vec!["hash1".into()], expires_at: "2026-01-01T00:00:00Z".into() }` serializes with `serde_json::to_string` to JSON containing `"type":"policy_override_applied"` (snake_case rename_all)
    - Test: the `zt_audit_hash` field is absent from JSON when `None` (skip_serializing_if = "Option::is_none")
    - Test: round-trip: `serde_json::from_str` of the serialized value deserializes back to an equal value
  </behavior>
  <action>
    Append a new variant `PolicyOverrideApplied` to the `AuditEventPayload` enum in `crates/nono/src/audit.rs` after the existing `Network` variant.

    Exact variant name: `PolicyOverrideApplied` (PascalCase; serde renames to `policy_override_applied` via the existing `rename_all = "snake_case"` on the enum).

    Fields:
    - `jti: String` — JWT ID (single-use nonce, for correlation). Doc: "JWT ID of the applied override token (single-use nonce)."
    - `kms_key_id: String` — KMS key ARN. Doc: "Signing key identity (KMS key ARN) — redaction-safe (not raw DER)."
    - `zt_audit_hash: Option<String>` — with `#[serde(default, skip_serializing_if = "Option::is_none")]`. Doc: "ZT-Infra audit chain hash at time of override issuance (bi-directional link, AUD-02). None when token predates ZT-Infra audit chain integration."
    - `granted_path_hashes: Vec<String>` — Doc: "Paths granted by this override, path-hashed per existing redaction policy (AUD-03). Uses path_hash_for(session_salt, path). Never raw paths."
    - `expires_at: String` — ISO-8601 expiry timestamp. Doc: "ISO-8601 expiry timestamp."

    Copy the doc-comment style from `CapabilityDecision` variant. The variant-level doc comment is: "A signed policy override was verified and applied to this invocation. Emitted by nono-cli SecurityEventLayer before the sandboxed child spawns (AUD-01 / AUD-04). Fields use the same redaction contract as other variants: paths appear as salted hashes (AUD-03), not raw strings."

    This variant is a pure data carrier — add NO policy logic, NO validation, NO conditional behavior in the core crate (CLAUDE.md policy-free core invariant; D-01 in 92-CONTEXT.md).

    Add inline unit tests in the existing test module of `audit.rs` (or add a new `#[cfg(test)]` block):
    - `policy_override_applied_serializes_with_type_tag`: assert serde_json output contains `"type":"policy_override_applied"` and all non-optional fields
    - `policy_override_applied_zt_audit_hash_absent_when_none`: assert field is absent from JSON when `zt_audit_hash: None`
    - `policy_override_applied_round_trips`: serialize then deserialize, assert equality
  </action>
  <verify>
    <automated>cd /c/Users/OMack/Nono && cargo test -p nono --lib audit:: 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PolicyOverrideApplied" crates/nono/src/audit.rs` returns >= 2 (variant definition + at least one test reference)
    - `cargo test -p nono --lib audit::` exits 0
    - Serialized output for the variant contains the string `"type":"policy_override_applied"` (confirmed by the round-trip test)
    - `zt_audit_hash` key is absent from the JSON when set to `None` (confirmed by the skip_serializing test)
    - No `.unwrap()` or `.expect()` in new production code (clippy::unwrap_used; `#[allow(clippy::unwrap_used)]` is permitted in the `#[cfg(test)]` block only)
  </acceptance_criteria>
  <done>PolicyOverrideApplied variant added to AuditEventPayload; 3 unit tests green; no clippy warnings on Windows host</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add EventIDs 10006–10010 and SecurityEventType variants (crates/nono-cli/src/telemetry/event.rs)</name>
  <files>crates/nono-cli/src/telemetry/event.rs, crates/nono-cli/src/telemetry/mod.rs</files>
  <read_first>
    - crates/nono-cli/src/telemetry/event.rs — read fully before editing; note the EventID constant block (lines 31-41), SecurityEventType enum (lines 52-64), event_id_for exhaustive match (lines 71-79), and path_hash_for function signature
    - crates/nono-cli/src/telemetry/mod.rs — read fully; note severity_for exhaustive match on SecurityEventType (lines 154-162) and on_event target-suffix dispatch (lines 278-285) — severity_for MUST be extended when new variants are added
    - .planning/phases/92-runtime-capabilityset-mutation-audit-wiring/92-PATTERNS.md — § "crates/nono-cli/src/telemetry/event.rs — add EventIDs 10006–10010" for exact constant names, variant names, doc comments, and severity_for pipe-grouped pattern
    - CLAUDE.md § "Cross-target clippy MUST/NEVER rule" — this file is cfg-unconditional; new match arms must be exhaustive on ALL targets
  </read_first>
  <behavior>
    - Test: `event_id_for(&SecurityEventType::PolicyOverridePresented) == 10006`
    - Test: `event_id_for(&SecurityEventType::PolicyOverrideVerified) == 10007`
    - Test: `event_id_for(&SecurityEventType::PolicyOverrideRejected) == 10008`
    - Test: `event_id_for(&SecurityEventType::PolicyOverrideExpired) == 10009`
    - Test: `event_id_for(&SecurityEventType::PolicyOverrideRevoked) == 10010`
  </behavior>
  <action>
    In `crates/nono-cli/src/telemetry/event.rs`:

    1. After the existing `EVENT_ID_TELEMETRY_DEGRADED: u32 = 10005;` constant (line 41), add 5 new constants following the exact doc-comment + spacing format:
       - `EVENT_ID_POLICY_OVERRIDE_PRESENTED: u32 = 10006` — doc: "EventID for a policy-override token presented (lifecycle start)."
       - `EVENT_ID_POLICY_OVERRIDE_VERIFIED: u32 = 10007` — doc: "EventID for a policy-override token verified and applied."
       - `EVENT_ID_POLICY_OVERRIDE_REJECTED: u32 = 10008` — doc: "EventID for a policy-override token rejected (any OverrideErrorKind)."
       - `EVENT_ID_POLICY_OVERRIDE_EXPIRED: u32 = 10009` — doc: "EventID for a policy-override token rejected because it was expired."
       - `EVENT_ID_POLICY_OVERRIDE_REVOKED: u32 = 10010` — doc: "EventID for a policy-override token rejected due to replay (jti already consumed)."

    2. In the `SecurityEventType` enum (lines 52-64), add 5 new variants after `TelemetryDegraded`, with doc comments mapping each to its EventID:
       - `PolicyOverridePresented` — "Signed override token presented for verification (EventID 10006)."
       - `PolicyOverrideVerified` — "Signed override token verified and applied (EventID 10007)."
       - `PolicyOverrideRejected` — "Signed override token rejected (EventID 10008 — maps from OverrideErrorKind)."
       - `PolicyOverrideExpired` — "Signed override token rejected as expired (EventID 10009)."
       - `PolicyOverrideRevoked` — "Signed override token rejected as already-consumed/revoked (EventID 10010)."

    3. In `event_id_for` (lines 71-79), add the 5 new match arms mapping each new variant to its constant. The match must remain exhaustive.

    In `crates/nono-cli/src/telemetry/mod.rs`:

    4. In `severity_for` (lines 154-162), extend the existing pipe-grouped match to include all 5 new variants under `TelemetrySeverity::Warning`. Copy the pipe-grouping style exactly: add ` | SecurityEventType::PolicyOverridePresented | SecurityEventType::PolicyOverrideVerified | SecurityEventType::PolicyOverrideRejected | SecurityEventType::PolicyOverrideExpired | SecurityEventType::PolicyOverrideRevoked` to the existing Warning arm OR add a new grouped arm. The match must remain exhaustive.

    CRITICAL: Do NOT touch `on_event` target-suffix dispatch (per RESEARCH.md recommendation Option B — direct-emit method; the tracing-intercept path is NOT extended for override events).

    Add inline unit tests in the existing test module of `event.rs`:
    - `override_event_ids_are_10006_through_10010`: assert each `event_id_for` mapping
    - `override_event_type_serde_roundtrip`: serialize `PolicyOverrideVerified` to JSON and assert it contains `"policy_override_verified"` (from `rename_all = "snake_case"`)

    After editing, the executor MUST attempt cross-target clippy (see acceptance_criteria).
  </action>
  <verify>
    <automated>cd /c/Users/OMack/Nono && cargo test -p nono-cli --lib telemetry::event 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PolicyOverridePresented\|PolicyOverrideVerified\|PolicyOverrideRejected\|PolicyOverrideExpired\|PolicyOverrideRevoked" crates/nono-cli/src/telemetry/event.rs` returns >= 10 (5 constants + 5 variants + test refs)
    - `grep -c "EVENT_ID_POLICY_OVERRIDE" crates/nono-cli/src/telemetry/event.rs` returns 10 (5 constant declarations + 5 match arms in event_id_for)
    - `cargo test -p nono-cli --lib telemetry::event` exits 0
    - `cargo build -p nono-cli` exits 0 (confirms severity_for match is also exhaustive)
    - Cross-target clippy: execute `cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used`; if the cross-toolchain is absent, mark this acceptance item PARTIAL and add `[CROSS-TARGET-PARTIAL→CI]` note to the SUMMARY. Windows host `cargo check` is NOT a substitute.
  </acceptance_criteria>
  <done>5 new EventID constants and 5 new SecurityEventType variants added; event_id_for and severity_for are exhaustive; 2 unit tests green; cross-target check completed or marked PARTIAL→CI</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core nono crate ↔ nono-cli | AuditEventPayload is a data carrier; policy lives in CLI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01 | Tampering | AuditEventPayload::PolicyOverrideApplied (audit.rs) | mitigate | Variant is a data carrier only; serde deny_unknown_fields not applicable to enums but the enum tag enforces type identity at deserialization |
| T-92-02 | Information Disclosure | granted_path_hashes in PolicyOverrideApplied | mitigate | Field stores path HASHES, never raw paths (AUD-03); path_hash_for(session_salt, path) is the required pattern |
| T-92-03 | Elevation of Privilege | SecurityEventType match exhaustiveness on Linux/macOS | mitigate | event.rs is cfg-unconditional; cross-target clippy MANDATORY per CLAUDE.md; new variants must appear in all exhaustive match arms (event_id_for + severity_for) |
| T-92-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; Standard Stack confirmed no new Cargo.toml entries required |
</threat_model>

<verification>
Full phase smoke check after this plan:
- `cargo test -p nono --lib audit::` exits 0
- `cargo test -p nono-cli --lib telemetry::event` exits 0
- `cargo build --workspace` exits 0 (confirms no exhaustive-match breaks)
</verification>

<success_criteria>
- `AuditEventPayload::PolicyOverrideApplied` variant exists in `crates/nono/src/audit.rs` with fields: `jti`, `kms_key_id`, `zt_audit_hash: Option<String>` (skip_serializing_if = none), `granted_path_hashes: Vec<String>`, `expires_at`
- 3 audit.rs unit tests green (serialization, None-omission, round-trip)
- `SecurityEventType` has 10 variants; `event_id_for` maps 10006–10010 to the 5 new variants; `severity_for` covers all 10 variants
- 2 event.rs unit tests green (EventID mapping, serde round-trip)
- No clippy warnings introduced; `cargo build --workspace` exits 0
</success_criteria>

<output>
Create `.planning/phases/92-runtime-capabilityset-mutation-audit-wiring/92-01-SUMMARY.md` when done.
</output>
