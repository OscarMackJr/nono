---
phase: 92-runtime-capabilityset-mutation-audit-wiring
plan: 04
type: execute
wave: 3
depends_on:
  - 92-03
files_modified:
  - scripts/gates/override-01.ps1
  - nono-py/tests/test_override_wiring.py
autonomous: true
requirements:
  - MUT-01
  - MUT-02
  - MUT-03
  - MUT-04
  - MUT-05
  - VFY-01
  - AUD-04
  - DF-01
must_haves:
  truths:
    - "verify-dark.ps1 --gate OVERRIDE-01 emits a machine-readable PASS verdict (exit 0)"
    - "OVERRIDE-01 gate proves SC1: valid token → --override-audit + --allow flags appear in nono.exe args"
    - "OVERRIDE-01 gate proves SC2: bad-sig / expired / out-of-scope / replay / algorithm:none all raise NonoOverrideError"
    - "OVERRIDE-01 gate proves SC3: no-token path produces no --override-audit flag (MUT-05 regression)"
    - "pytest test_override_wiring.py passes: MUT-01..05 path assertions + AUD-04 regression"
    - "scripts/gates/override-01.ps1 follows the Test-Precondition/Invoke-Gate contract; NEVER calls exit; NEVER calls Persist-Verdict"
  artifacts:
    - path: "scripts/gates/override-01.ps1"
      provides: "OVERRIDE-01 Dark Factory gate (DF-01)"
      contains: "Test-Precondition"
    - path: "nono-py/tests/test_override_wiring.py"
      provides: "pytest coverage for MUT-01..05 + AUD-04 nono-py path"
      contains: "test_override_regression_no_token"
  key_links:
    - from: "scripts/verify-dark.ps1"
      to: "scripts/gates/override-01.ps1"
      via: "auto-discovery by filename pattern; dot-sources Test-Precondition + Invoke-Gate"
      pattern: "override-01"
    - from: "scripts/gates/override-01.ps1"
      to: "nono-py/tests/fixtures/override_test_key.der"
      via: "Phase 91 test keypair reused for token minting (never a production trust root)"
      pattern: "override_test_key"
---

<objective>
Close the verification loop for Phase 92 — the two remaining deliverables are the Dark Factory gate (DF-01) and pytest coverage for the nono-py wiring changes from Plan 02.

1. `scripts/gates/override-01.ps1` — a new OVERRIDE-01 gate following the exact `Test-Precondition`/`Invoke-Gate` contract of the existing gate structural twin (`scripts/gates/telemetry-event-emit.ps1`). The gate exercises SC1 (valid token → args correct), SC2 (fail-closed cases all raise NonoOverrideError), and SC3 (no-token regression path). Uses Phase 91 committed test keypair for token minting (never a production trust root). Must return a verdict object, NEVER call `exit`, NEVER call `Persist-Verdict`.

2. `nono-py/tests/test_override_wiring.py` — pytest coverage for MUT-01 (override appends `--allow` flags), MUT-02 (invocation-scoped, no shared state), MUT-04 (path sanitization rejects `..` and non-absolute), MUT-05 (byte-identical no-token args), and AUD-04 (verify `--override-audit` flag is present when override_token is provided).

   CRITICAL: `confined_run` is a PyO3-wrapped Rust function. It spawns the child process via Rust's `std::process::Command` (see `do_spawn_and_wait` called at line 214 in `windows_confined_run.rs`). A Python `unittest.mock.patch("subprocess.Popen")` will NOT intercept this spawn path — the Rust-level spawn is invisible to Python's `subprocess` module. Tests MUST use one of the verified working approaches:
   - (a) Point `find_nono_exe()` at a test stub — a small `echo-args` binary (or a PowerShell script that echoes its argv to a tempfile) — by temporarily setting the `NONO_EXE_PATH` or equivalent env var that `find_nono_exe()` reads; OR
   - (b) Use a Rust-level `build_nono_run_args` or arg-builder function that is exposed as a `#[pyfunction]` solely for tests (if `build_nono_run_args` can be surfaced for test without spawning), returning the arg list as `Vec<String>`; OR
   - (c) Run `confined_run` end-to-end against a real `nono.exe` (integration test) — only viable if a nono.exe is on PATH and the test can inspect its stdout for the reflected args.
   Read `nono-py/src/windows_confined_run.rs` to confirm how `find_nono_exe()` resolves the exe path (env var? PATH search? fixed path?) and choose the approach that lets tests substitute a lightweight stub.

These tests use `maturin develop` to build the wheel and import `nono`.

Purpose: Wave 3. Depends on the complete wiring from Plans 01, 02, 03. After this plan, all 11 requirements for Phase 92 are covered and `verify-dark.ps1 --gate OVERRIDE-01` is the final integration gate before `/gsd:verify-work`.

Output: `scripts/gates/override-01.ps1` (Nono repo) and `nono-py/tests/test_override_wiring.py` (nono-py repo, branch `44-broker-ffi-lockstep`). Each file commits in its respective repo.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\OMack\Nono\.planning\PROJECT.md
@C:\Users\OMack\Nono\.planning\ROADMAP.md
@C:\Users\OMack\Nono\.planning\REQUIREMENTS.md
@C:\Users\OMack\Nono\.planning\phases\92-runtime-capabilityset-mutation-audit-wiring\92-CONTEXT.md
@C:\Users\OMack\Nono\.planning\phases\92-runtime-capabilityset-mutation-audit-wiring\92-RESEARCH.md
@C:\Users\OMack\Nono\.planning\phases\92-runtime-capabilityset-mutation-audit-wiring\92-PATTERNS.md
@C:\Users\OMack\Nono\.planning\phases\92-runtime-capabilityset-mutation-audit-wiring\92-03-SUMMARY.md

<interfaces>
<!-- Structural twin for override-01.ps1 -->
From scripts/gates/telemetry-event-emit.ps1 (structural twin):
- Gate header + contract comment (lines 1-54): copy verbatim, adapt gate name + SC assertions
- Assert-True helper (lines 91-101): copy verbatim
- Test-Precondition function contract: returns $null on success, "reason string" on SKIP
- Invoke-Gate function contract: returns [ordered]@{gate; verdict; reason; detail; timestamp}; NEVER calls exit or Persist-Verdict
- Verdict shape: [ordered]@{gate='override-01'; verdict='PASS'|'FAIL'|'SKIP_HOST_UNAVAILABLE'; reason='...'; detail=[ordered]@{...}; timestamp='...'}

From scripts/gates/egress-policy-deny.ps1 (gate structural model):
- gate = 'override-01' (not egress-policy-deny)
- Same Test-Precondition/Invoke-Gate structure

Phase 91 test fixtures (verified available):
- C:\Users\OMack\nono-py\tests\fixtures\override_test_key.der — DER public key for verify_override
- C:\Users\OMack\nono-py\tests\fixtures\override_test_private.der — private key for signing test tokens
- C:\Users\OMack\nono-py\tests\fixtures\override_test_key.pem — PEM form of public key
- C:\Users\OMack\nono-py\tests\fixtures\vectors.json — canonical form test vectors

From nono-py (Plan 02 deliverables now available):
- verify_override(token_json: str, pubkey_der: bytes, allowed_key_arns: list[str]) -> OverrideGrant
- confined_run(exe, args, allow=None, profile=None, cwd=None, timeout_secs=None, override_token=None)
- OverrideGrant: signer, scope_paths, scope_domains, not_before, expires_at, jti, repo_context, zt_audit_hash
- NonoOverrideError: raised for all verification failures AND for probe failures (D-02 bilateral gate)

SPAWN MECHANISM (CRITICAL for test design):
- confined_run is a #[pyfunction] (PyO3 / Rust). It spawns nono.exe via Rust's std::process::Command
  inside do_spawn_and_wait (windows_confined_run.rs line 214). Python's subprocess.Popen is NOT
  involved — patching it at the Python level does nothing.
- The correct test approach: read find_nono_exe() in windows_confined_run.rs to learn how the
  exe path is resolved. If it checks an env var (e.g., NONO_EXE or NONO_PATH), tests can
  point that env var to a stub binary. If it uses PATH search, tests can prepend a temp dir to
  PATH containing an echo-args stub. Choose the approach that the codebase already supports.
  If no env-var hook exists, the simplest stub is a .bat / .ps1 that writes its args to a
  tempfile — tests read the tempfile to assert arg presence.

From 92-RESEARCH.md § Phase Requirements → Test Map:
- MUT-01: pytest tests/test_confined_run.py::test_override_appends_allow_flags
- MUT-04: pytest tests/test_confined_run.py::test_override_path_scope
- MUT-05: pytest tests/test_confined_run.py::test_override_regression_no_token
- DF-01: pwsh -File scripts/verify-dark.ps1 --gate OVERRIDE-01

INVOCATION RULE (durable MEMORY):
- pwsh -File scripts\verify-dark.ps1 --gate override-01
- NEVER: pwsh -Command "<bare path>" (swallows exit N → 1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pytest coverage for nono-py wiring (nono-py/tests/test_override_wiring.py)</name>
  <files>nono-py/tests/test_override_wiring.py</files>
  <read_first>
    - C:\Users\OMack\nono-py\src\windows_confined_run.rs — read find_nono_exe() to understand how nono.exe is located (env var? PATH? fixed path?). This determines the stub injection strategy for tests.
    - C:\Users\OMack\nono-py\tests\ — list files to understand existing test structure (test_override.py, conftest.py, etc.); read conftest.py to understand shared fixtures (keypair loading, token minting helpers from Phase 91)
    - C:\Users\OMack\nono-py\tests\fixtures\vectors.json — understand the valid token format used in Phase 91 tests (reuse the same minting approach)
    - .planning/phases/92-runtime-capabilityset-mutation-audit-wiring/92-RESEARCH.md — § "Phase Requirements → Test Map" for exact test names and assertions
  </read_first>
  <action>
    REPO: `C:\Users\OMack\nono-py` (branch `44-broker-ffi-lockstep`). Commit goes in this repo.

    First, run `maturin develop` to ensure the wheel is current before writing tests.

    MANDATORY PRE-STEP — determine the correct stub injection strategy:
    Read `find_nono_exe()` in `windows_confined_run.rs`. Identify how the exe path is resolved.
    Based on that, choose ONE of:

    Strategy A (env var): If `find_nono_exe()` checks an env var (e.g., `NONO_EXE`, `NONO_PATH`, or similar), write tests that `os.environ["NONO_EXE"] = str(stub_path)` before calling `confined_run`. The stub is a tiny script that writes its argv to a tempfile and exits 0 (for arg-capture tests) or exits with a specific code (for failure tests). Use `os.environ["NONO_EXE"] = original` in teardown to restore.

    Strategy B (PATH prepend): If `find_nono_exe()` uses PATH search for "nono.exe", write tests that create a temp directory containing a `nono.exe` stub script, then `os.environ["PATH"] = str(tmpdir) + os.pathsep + os.environ["PATH"]`. Restore PATH in teardown.

    Strategy C (arg-builder exposure): If Plan 02 exposed a `build_nono_run_args_for_test` or similar Rust function as a `#[pyfunction]`, call it directly to get the arg list as `list[str]` without spawning. This is the cleanest approach if available.

    Regardless of strategy chosen, document the choice in a comment at the top of the test file:
    `# Test strategy: <Strategy A/B/C> — <reason based on find_nono_exe() implementation>`

    Create `nono-py/tests/test_override_wiring.py` with the following 6 tests:

    1. `test_override_appends_allow_flags` (MUT-01):
       - Mint a valid token using Phase 91 test private key (check conftest.py or test_override.py for the minting helper; if one exists, reuse it)
       - Call `nono.verify_override(token_json, pubkey_der, allowed_arns=["arn:aws:kms:test"])` to get `grant`
       - Using the chosen stub strategy: capture the arg list that `confined_run` would pass to nono.exe
       - Assert `"--allow"` appears in the captured args
       - Assert the scope path from the grant appears immediately after `"--allow"`
       - Assert `"--override-audit"` appears in the captured args

    2. `test_override_invocation_scoped` (MUT-02):
       - Call `confined_run` twice with different grants (two different tokens with different scope_paths)
       - Assert the second call's args contain the SECOND grant's scope paths, not the first's (no shared state leakage)

    3. `test_override_path_scope` (MUT-04):
       - Grant has scope_path `/tmp/project`
       - Confirm that `--allow /tmp/project-evil` does NOT appear in the args (only `--allow /tmp/project` appears)
       - Directly test `sanitize_override_path` if exposed as a Python-callable test function, or test at the confined_run level

    4. `test_sanitize_rejects_dotdot` (MUT-04):
       - Attempt to create a grant with a scope_path containing `..` (e.g., `/tmp/../etc`) — or directly test `sanitize_override_path` if exposed
       - Assert that the call raises `NonoOverrideError` or `ValueError` (sanitize_override_path raises PyValueError which surfaces as ValueError in Python)

    5. `test_override_regression_no_token` (MUT-05):
       - Capture args from `confined_run(exe="nono.exe", args=[], override_token=None)` using the chosen stub strategy
       - Assert `"--override-audit"` is NOT in the captured args
       - Assert the args list is byte-identical to a reference call made before Phase 92 (capture a reference call without override_token and compare arg-by-arg)

    6. `test_override_audit_flag_present_when_token_given` (AUD-04 nono-py side):
       - Mint a valid grant, call `confined_run(..., override_token=grant)` via the chosen stub strategy
       - Assert `"--override-audit"` IS in the captured args

    Import `nono` at the top. Use `pytest.importorskip("nono")` if the module may not be built.
    Skip tests with `pytest.mark.skipif` if the stub injection strategy cannot be set up (e.g., no nono.exe available and no env-var hook), documenting the skip reason.
    Follow the pytest convention: use `assert` and `pytest.raises`. NEVER use `unittest.mock.patch("subprocess.Popen")` — it will not intercept the Rust-level spawn and tests using it will always pass vacuously while proving nothing.
  </action>
  <verify>
    <automated>cd /c/Users/OMack/nono-py && maturin develop --quiet && python -m pytest tests/test_override_wiring.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_override_wiring.py -x -q` exits 0 (all 6 tests pass or skip with documented reason)
    - `grep -c "def test_" /c/Users/OMack/nono-py/tests/test_override_wiring.py` returns 6 (6 test functions)
    - `grep -n "override_audit\|--override-audit" /c/Users/OMack/nono-py/tests/test_override_wiring.py` shows flag assertions in MUT-05 and AUD-04 tests
    - `grep -n "NonoOverrideError\|ValueError" /c/Users/OMack/nono-py/tests/test_override_wiring.py` shows sanitization failure assertions
    - `grep -c "subprocess.Popen\|mock.patch.*Popen" /c/Users/OMack/nono-py/tests/test_override_wiring.py` returns 0 (Python subprocess mock MUST NOT be used — it cannot intercept the Rust-level spawn and would produce vacuous passing tests that prove nothing for MUT-01/MUT-05/AUD-04)
    - `grep -n "Test strategy:" /c/Users/OMack/nono-py/tests/test_override_wiring.py` shows a comment documenting the stub injection approach chosen (confirms executor understood find_nono_exe() mechanism)
    - No test accesses `--override-audit` by parsing the raw base64 value (tests check presence/absence only)
  </acceptance_criteria>
  <done>6 pytest tests green (or skipped with documented reason) covering MUT-01..05 and AUD-04 (nono-py side); committed in nono-py repo; no subprocess.Popen mock used</done>
</task>

<task type="auto">
  <name>Task 2: OVERRIDE-01 Dark Factory gate (scripts/gates/override-01.ps1)</name>
  <files>scripts/gates/override-01.ps1</files>
  <read_first>
    - scripts/verify-dark.ps1 — read the Test-Precondition/Invoke-Gate invocation contract, gate auto-discovery pattern (how it finds and dot-sources gate files), and the Persist-Verdict / exit-code mapping (0=PASS/2=FAIL/3=SKIP_HOST_UNAVAILABLE/4=HARNESS_ERROR)
    - scripts/gates/telemetry-event-emit.ps1 — read fully; this is the structural twin; copy the header comment block, Assert-True helper, Test-Precondition structure, Invoke-Gate structure, and verdict object shape verbatim (adapting for override-01)
    - scripts/gates/egress-policy-deny.ps1 — secondary reference for the gate contract; confirms the same Test-Precondition → $null / "reason" + Invoke-Gate → [ordered]@{} contract
    - C:\Users\OMack\nono-py\tests\fixtures\ — list to confirm override_test_key.der, override_test_private.der, override_test_key.pem all exist (needed for token minting in the gate)
    - .planning/phases/92-runtime-capabilityset-mutation-audit-wiring/92-PATTERNS.md — § "scripts/gates/override-01.ps1 structure" for the gate header comment template, Assert-True, Test-Precondition, and Invoke-Gate body structure
    - C:\Users\OMack\nono-py\src\windows_confined_run.rs — confirm the stub injection strategy (same env var / PATH approach used in Task 1) so the gate inline Python script uses the same method to capture args without spawning a real child
  </read_first>
  <action>
    REPO: `C:\Users\OMack\Nono`. Commit goes in this repo (Nono repo contains all scripts/).

    Create `scripts/gates/override-01.ps1` following the EXACT contract of `scripts/gates/telemetry-event-emit.ps1`.

    Gate header: copy the structured header comment block from telemetry-event-emit.ps1 (lines 1-54) verbatim, adapting:
    - Gate name: `override-01`
    - SC description: SC1/SC2/SC3 as defined in the objective above
    - "WHY SKIP" section: requires Python + nono module importable + nono on PATH; admin NOT required
    - "INVOCATION RULE": `pwsh -File scripts\verify-dark.ps1 --gate override-01`; NEVER `pwsh -Command "<bare path>"`

    `Assert-True` helper: copy from telemetry-event-emit.ps1 lines 91-101 verbatim (not adapting — it is identical across all gates).

    `Test-Precondition` function:
    1. Check `Get-Command nono -ErrorAction SilentlyContinue` — return "nono is not on PATH — build and install nono before running this gate" if absent
    2. Check `python -c "import nono; print('ok')"` — return "nono Python module not importable ($check) — run 'maturin develop' in nono-py then re-run" if not 'ok'
    3. Return `$null` on success (preconditions met)

    `Invoke-Gate` function:
    - `$ErrorActionPreference = 'Continue'`
    - Set `$fixturesPath = 'C:\Users\OMack\nono-py\tests\fixtures'`
    - Build a Python inline script that exercises SC1/SC2/SC3.

      CRITICAL — arg capture in inline Python: use the SAME stub injection strategy confirmed in Task 1 (env var or PATH stub). The inline Python script must NOT use `unittest.mock.patch("subprocess.Popen")` — confined_run is a PyO3 Rust function that spawns via Rust's std::process::Command, not Python's subprocess. The mock would intercept nothing and SC1/SC3 assertions would vacuously pass regardless of whether the override wiring works.

      Use a stub binary that writes its argv to a tempfile:
      - Create a temp directory with a script named `nono.exe` (on Windows: a .bat or .ps1 wrapper, or a small Python script renamed/wrapped); the stub writes `sys.argv` as JSON to a fixed tempfile path and exits 0.
      - Set the appropriate env var or PATH so `find_nono_exe()` in the Rust code resolves to the stub.
      - Call `confined_run(exe="test_target.exe", args=[], allow=["/tmp/test"], override_token=grant)`.
      - Read the tempfile and parse the JSON argv. Assert `--override-audit` is present (SC1).
      - After the assertion, clean up the tempfile and restore the env var / PATH.

      SC1 (valid token → args correct):
      - Load `pubkey_der` from `override_test_key.der`, `privkey_der` from `override_test_private.der`
      - Mint a valid token (check how Phase 91 tests mint tokens; use the same helper). If Phase 91 tests have a `mint_test_token()` helper in conftest.py or test_override.py, import and call it; otherwise use the Phase 91 private key to sign a minimal CAF v0.1 token JSON directly
      - Call `nono.verify_override(token_json, pubkey_der, allowed_key_arns=["arn:aws:kms:test"])` → `grant`
      - Use the stub injection approach to capture args from `confined_run("test.exe", [], allow=["/tmp/scope"], override_token=grant)`
      - Assert `"--override-audit"` is in the captured args
      - Assert `"--allow"` is in the captured args (if grant has scope_paths)
      - Report SC1: True/False

      SC2 (fail-closed cases all raise NonoOverrideError):
      - Test 5 cases: bad-sig, expired, algorithm:none, out-of-scope (path not in grant), replay (call verify_override twice with same jti)
      - Each must raise `nono.NonoOverrideError`
      - Report SC2: True/False

      SC3 (no-token regression):
      - Capture args from `confined_run("nono.exe", [], allow=["/tmp/scope"], override_token=None)` via stub
      - Assert `"--override-audit"` NOT in args
      - Report SC3: True/False

    - Run the script: `$raw = & python -c $script 2>&1` or via stdin
    - Parse SC1/SC2/SC3 booleans from the JSON output
    - If all True: return `[ordered]@{gate='override-01'; verdict='PASS'; reason='SC1/SC2/SC3 verified: override wiring, fail-closed cases, regression path.'; detail=[ordered]@{sc1=$true;sc2=$true;sc3=$true}; timestamp=(Get-Date -Format 'yyyy-MM-ddTHH:mm:ss.fffZ')}`
    - If any False: return FAIL verdict with the failing SC identified in `reason`
    - If exception thrown: let it propagate (harness catches it as HARNESS_ERROR exit 4)

    CRITICAL CONTRACT rules (never violate):
    - `Invoke-Gate` MUST NOT call `exit` at any point
    - `Invoke-Gate` MUST NOT call `Persist-Verdict`
    - `Invoke-Gate` MUST return exactly one `[ordered]@{gate; verdict; reason; detail; timestamp}` object
    - `Test-Precondition` MUST return `$null` or a plain string (not throw)
    - Both functions are dot-sourced by verify-dark.ps1; any syntax error kills the harness

    Add a comment at the top of Invoke-Gate's inline Python script:
    `# OVERRIDE-01 SC1+SC2+SC3 inline gate script`
    Include the Phase 91 test keypair paths as variables (not hardcoded strings) to make the script maintainable.
  </action>
  <verify>
    <automated>cd /c/Users/OMack/Nono && pwsh -File scripts/verify-dark.ps1 --gate override-01 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pwsh -File scripts/verify-dark.ps1 --gate override-01` exits 0 (PASS verdict)
    - The output contains `"verdict": "PASS"` (or the structured machine-readable PASS line per verify-dark.ps1 output format)
    - `grep -c "Test-Precondition\|Invoke-Gate" scripts/gates/override-01.ps1` returns >= 2 (both functions present)
    - `grep -c "exit\b" scripts/gates/override-01.ps1` returns 0 (NEVER calls exit — the most critical contract rule)
    - `grep -c "Persist-Verdict" scripts/gates/override-01.ps1` returns 0 (NEVER calls Persist-Verdict)
    - `grep -c "sc1\|sc2\|sc3" scripts/gates/override-01.ps1` returns >= 3 (SC1/SC2/SC3 assertions present)
    - `grep -c "NonoOverrideError" scripts/gates/override-01.ps1` returns >= 1 (SC2 fail-closed cases check the right exception type)
    - `grep -c "subprocess.Popen\|mock.patch.*Popen" scripts/gates/override-01.ps1` returns 0 (Python subprocess mock MUST NOT be used in the inline gate script — same reason as Task 1)
    - `grep -n "BLOCKING-93\|VFY-01" scripts/gates/override-01.ps1` returns 0 (the live arm is NOT in this gate — Phase 93's OVERRIDE-02 gate covers it)
  </acceptance_criteria>
  <done>override-01.ps1 gate created with Test-Precondition/Invoke-Gate contract; verify-dark.ps1 --gate override-01 exits 0 with PASS verdict; SC1/SC2/SC3 all proven; no subprocess.Popen mock used; committed in Nono repo</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gate script ↔ Python subprocess | override-01.ps1 invokes Python inline; untrusted stdin/stdout boundary |
| Test fixtures | Phase 91 test keypair is NEVER a production trust root; its use is gated to test-only paths |
| Stub binary | The echo-args stub used in tests/gate is a test-only artifact; its path must not persist in PROBE_CACHE after test teardown (cache is process-lifetime; test process is separate from production) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-GATE-EXIT | Integrity | override-01.ps1 gate contract | mitigate | Gate MUST NOT call exit (verify-dark.ps1 contract); grep gate for bare exit calls before commit |
| T-92-GATE-PERSIST | Integrity | override-01.ps1 Persist-Verdict | mitigate | Gate MUST NOT call Persist-Verdict; grep before commit |
| T-92-TESTKEY | Elevation of Privilege | override_test_key used as production trust root | accept | Test keypair is only referenced in tests/ and gates/; never in production code paths; NEVER embedded as machine policy pubkey |
| T-92-SC2-INCOMPLETE | Denial of Service | SC2 fail-closed case coverage | mitigate | Gate tests all 5 fail-closed cases: bad-sig, expired, algorithm:none, out-of-scope, replay (jti replay) — same set as Phase 91 vectors |
| T-92-VACUOUS-MOCK | Integrity | subprocess.Popen mock intercepting nothing | mitigate | Tests and gate MUST NOT use Python subprocess.Popen mock; confined_run spawns via Rust Command; wrong mock = vacuous pass; acceptance criterion explicitly bans it |
| T-92-SC | Tampering | npm/pip/cargo installs | accept | No new packages; Python inline scripts use only already-installed nono module |
</threat_model>

<verification>
Full Phase 92 gate:
- `pwsh -File scripts/verify-dark.ps1 --gate override-01` exits 0 (PASS)
- `cd /c/Users/OMack/nono-py && python -m pytest tests/test_override_wiring.py -x -q` exits 0
- `cd /c/Users/OMack/Nono && make test` exits 0 (Rust workspace)
- `cd /c/Users/OMack/nono-py && cargo test` exits 0 (nono-py Rust unit tests)

VFY-01 PARTIAL status confirmation:
- OVERRIDE-01 gate covers the OFFLINE verify arm (Phase 92 scope)
- Live POST /actions AND-gate is [BLOCKING-93]; OVERRIDE-02 gate (Phase 93) covers it
- VFY-01 is correctly recorded as PARTIAL in REQUIREMENTS.md with [BLOCKING-93] note
</verification>

<success_criteria>
- `scripts/gates/override-01.ps1` exists with Test-Precondition/Invoke-Gate contract; no `exit` calls; no `Persist-Verdict` calls
- `verify-dark.ps1 --gate override-01` exits 0 with PASS verdict
- Gate proves SC1 (valid token → correct args via stub, NOT subprocess.Popen mock), SC2 (5 fail-closed cases → NonoOverrideError), SC3 (no-token path → no --override-audit flag)
- `nono-py/tests/test_override_wiring.py` has 6 tests covering MUT-01..05 and AUD-04 (nono-py side); all pass or skip with documented reason; zero uses of unittest.mock.patch("subprocess.Popen")
- All Phase 92 requirements (MUT-01..05, AUD-01..04, VFY-01 PARTIAL, DF-01) addressed by plans 01-04
- VFY-01 PARTIAL [BLOCKING-93] clearly documented; no gap
</success_criteria>

<output>
Create `.planning/phases/92-runtime-capabilityset-mutation-audit-wiring/92-04-SUMMARY.md` when done.
Note: scripts/gates/override-01.ps1 commits in Nono repo; nono-py/tests/test_override_wiring.py commits in nono-py repo (C:\Users\OMack\nono-py, branch 44-broker-ffi-lockstep). Record both commit hashes in the SUMMARY.
</output>
