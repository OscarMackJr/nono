---
phase: 113-spiffe-spire-workload-identity
plan: 07
type: execute
wave: 4
depends_on: [113-05, 113-06]
files_modified:
  - crates/nono-proxy/tests/spiffe_integration.rs
  - crates/nono-cli/tests/spiffe_run.rs
  - .github/workflows/spire.yml
  - scripts/spire-test.sh
  - testdata/spire/server.conf
  - testdata/spire/agent.conf
  - Makefile
autonomous: true
requirements: [NET-02]

must_haves:
  truths:
    - "D-06: fail-closed tests (spiffe_integration.rs's test_spiffe_jwt_fails_closed_on_missing_socket) run everywhere with no SPIRE needed; live tests are gated on SPIRE_AGENT_SOCKET; a real SPIRE CI lane (spire.yml) exists so the live path is genuinely executed somewhere, not merely compile-checked."
    - "D-07: every SPIRE-gated test that skips locally emits a structurally greppable SKIP[...] stderr marker; none silently reports ok while exercising nothing (the socket_access_run.rs anti-pattern this requirement exists to prevent)."
    - "The new fork-original D-03 test (asserting a SPIFFE-declared route reached via CONNECT/forward-HTTP/external-proxy fails closed) has no upstream equivalent — upstream HAS tls_intercept on those paths; this fork does not."
  artifacts:
    - path: "crates/nono-proxy/tests/spiffe_integration.rs"
      provides: "First integration test file in a brand-new nono-proxy/tests/ directory"
    - path: "crates/nono-cli/tests/spiffe_run.rs"
      provides: "End-to-end binary-level SPIFFE tests against the real nono binary"
    - path: ".github/workflows/spire.yml"
      provides: "Linux CI lane running a real SPIRE server+agent"
  key_links:
    - from: "crates/nono-proxy/tests/spiffe_integration.rs"
      to: "crates/nono-proxy/src/server.rs"
      via: "nono_proxy::server::start"
      pattern: "nono_proxy::server::start"
    - from: ".github/workflows/spire.yml"
      to: "crates/nono-proxy/tests/spiffe_integration.rs"
      via: "cargo test -p nono-sandbox-proxy --test spiffe_integration"
      pattern: "nono-sandbox-proxy --test spiffe_integration"
---

<objective>
Port upstream's SPIFFE test suite (`spiffe_integration.rs`, `spiffe_run.rs`) and CI infrastructure
(`spire.yml`, `spire-test.sh`, `testdata/spire/*.conf`), adapted for this fork's renamed crates
(`nono-sandbox-proxy`/`nono-sandbox-cli`, not upstream's `nono-proxy`/`nono-cli`), introduce the
D-07 `SKIP[...]` loud-skip-reporting convention (no existing fork precedent — the closest is
`socket_access_run.rs`'s ad-hoc, uncounted `eprintln!("skipping: ...")`, exactly the failure mode
D-07 exists to prevent), and author the fork-original D-03 fail-closed test upstream has no
equivalent for.

Purpose: D-03's request-time guard and D-04's eager async connect otherwise ship with compile-time
proof only — this plan is where the live SPIFFE path is genuinely executed, or, on this Windows dev
host, where the phase honestly reports that it is not, by name and count (D-07).
Output: the full upstream test suite ported, one new fork-original test, a real Linux CI lane, and
a verification-ready skip report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-spiffe-spire-workload-identity/113-CONTEXT.md
@.planning/phases/113-spiffe-spire-workload-identity/113-RESEARCH.md
@.planning/phases/113-spiffe-spire-workload-identity/113-VALIDATION.md
@.planning/phases/112-security-residual-sync/112-REVIEW.md
@.planning/phases/113-spiffe-spire-workload-identity/113-05-SUMMARY.md
@.planning/phases/113-spiffe-spire-workload-identity/113-06-SUMMARY.md

<interfaces>
<!-- .github/workflows/spire.yml — FULL upstream content (112 lines), ADOPT with ONE required
     adaptation: the `-p nono-proxy`/`-p nono-cli` package selectors must become
     `-p nono-sandbox-proxy`/`-p nono-sandbox-cli` (Phase 102's fork-owned rename). Verified via
     `git show c831dade -- .github/workflows/spire.yml` this session. -->

```yaml
name: SPIRE

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -Dwarnings

jobs:
  spiffe:
    name: Integration (Linux)
    if: ${{ !startsWith(github.head_ref, 'dependabot/github_actions/') }}
    runs-on: ubuntu-latest
    steps:
      # NOTE: use THIS fork's currently-pinned actions/checkout SHA from .github/workflows/ci.yml
      # (confirmed this session: de0fac2e4500dabe0009e67214ff5f5447ce83dd / v6), not upstream's
      # 9c091bb2.../v7.0.0 — for pin consistency across this fork's own workflow files, per
      # 113-PATTERNS.md Pattern Assignment 7's explicit note.
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@631a55b12751854ce901bb631d5902ceb48146f7 # stable
        with:
          toolchain: stable

      - name: Cache cargo registry
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-spire-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-spire-
            ${{ runner.os }}-cargo-

      - name: Download SPIRE
        run: |
          set -euo pipefail
          VERSION="1.9.6"
          TARBALL="spire-${VERSION}-linux-amd64-musl.tar.gz"
          curl -fsSL "https://github.com/spiffe/spire/releases/download/v${VERSION}/${TARBALL}" -o /tmp/spire.tar.gz
          mkdir -p /tmp/spire-bin
          tar -xzf /tmp/spire.tar.gz -C /tmp/spire-bin --strip-components=1
          echo "/tmp/spire-bin/bin" >> "$GITHUB_PATH"

      - name: Start SPIRE server
        run: |
          set -euo pipefail
          mkdir -p /tmp/nono-test-spire/data/server
          spire-server run -config testdata/spire/server.conf &
          for _ in $(seq 1 30); do [ -S /tmp/nono-test-spire/server.sock ] && break; sleep 0.5; done
          [ -S /tmp/nono-test-spire/server.sock ] || { echo "SPIRE server did not start"; exit 1; }

      - name: Start SPIRE agent
        run: |
          set -euo pipefail
          mkdir -p /tmp/nono-test-spire/data/agent
          RAW=$(spire-server token generate -socketPath /tmp/nono-test-spire/server.sock -spiffeID spiffe://test.nono/agent)
          TOKEN=$(echo "$RAW" | grep -oP '(?<=Token: )\S+')
          echo "::add-mask::$TOKEN"
          spire-agent run -config testdata/spire/agent.conf -joinToken "$TOKEN" &
          for _ in $(seq 1 30); do [ -S /tmp/nono-test-spire/agent.sock ] && break; sleep 0.5; done
          [ -S /tmp/nono-test-spire/agent.sock ] || { echo "SPIRE agent did not start"; exit 1; }

      - name: Register workload entry
        run: |
          set -euo pipefail
          spire-server entry create -socketPath /tmp/nono-test-spire/server.sock \
            -spiffeID "spiffe://test.nono/nono-proxy" -parentID "spiffe://test.nono/agent" \
            -selector "unix:uid:$(id -u)"
          for _ in $(seq 1 20); do
            spire-agent api fetch jwt -socketPath /tmp/nono-test-spire/agent.sock -audience warmup 2>/dev/null | grep -q "spiffe://" && break
            sleep 0.5
          done

      - name: Run SPIFFE proxy-level integration tests
        env:
          SPIRE_AGENT_SOCKET: /tmp/nono-test-spire/agent.sock
          SPIRE_TRUST_DOMAIN: test.nono
          SPIRE_WORKLOAD_SPIFFE_ID: spiffe://test.nono/nono-proxy
        # PACKAGE NAME ADAPTED: nono-proxy -> nono-sandbox-proxy (Phase 102 rename).
        run: cargo test -p nono-sandbox-proxy --test spiffe_integration -- --nocapture

      - name: Run SPIFFE binary-level integration tests
        env:
          SPIRE_AGENT_SOCKET: /tmp/nono-test-spire/agent.sock
          SPIRE_TRUST_DOMAIN: test.nono
          SPIRE_WORKLOAD_SPIFFE_ID: spiffe://test.nono/nono-proxy
        # PACKAGE NAME ADAPTED: nono-cli -> nono-sandbox-cli (Phase 102 rename).
        run: cargo test -p nono-sandbox-cli --test spiffe_run -- --nocapture
```

<!-- testdata/spire/server.conf and agent.conf — small (29 / 26 lines), ADOPT verbatim, no fork
     adaptation needed (paths/trust-domain are test-only constants, not fork-specific). Retrieve via
     `git show c831dade:testdata/spire/server.conf` and `:agent.conf` — both already confirmed to
     exist and parse cleanly this session. -->

<!-- Makefile — ADOPT the 3-line addition verbatim: -->
```makefile
test-spiffe:
	bash scripts/spire-test.sh
```
Add `test-spiffe` to the `.PHONY` list and the `make help` output, matching this fork's existing
style for other `test-*` targets (see `test-doc`'s neighboring entries).

<!-- scripts/spire-test.sh — 187 lines, ADOPT verbatim except its own internal `cargo test -p
     nono-proxy`/`-p nono-cli` invocations, which need the SAME `-p nono-sandbox-proxy`/
     `-p nono-sandbox-cli` adaptation as spire.yml. Fetch via
     `git show c831dade:scripts/spire-test.sh`. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: spiffe_integration.rs (new nono-proxy/tests/ dir) + D-07 SKIP mechanism + D-03 fork-original test</name>
  <files>crates/nono-proxy/tests/spiffe_integration.rs</files>
  <behavior>
    - `test_spiffe_jwt_fails_closed_on_missing_socket` runs unconditionally (no SPIRE needed) and
      asserts a `SpiffeError::Config`-shaped failure string.
    - The other 3 ported tests early-emit `eprintln!("SKIP[{}]: SPIRE_AGENT_SOCKET not set",
      module_path!())` and return when `SPIRE_AGENT_SOCKET` is unset, rather than a bare
      `eprintln!("skipping: ...")`.
    - The new fork-original test asserts D-03's fail-closed guard (Plan 113-05, Task 2) via a real
      `nono_proxy::server::start` call with a SPIFFE-declared route and a request on a non-reverse
      path — this test needs NO live SPIRE agent if it uses a mock Workload API `TcpListener`-style
      double, OR is itself `SPIRE_AGENT_SOCKET`-gated with its own `SKIP[...]` marker if a real
      connect is unavoidable (executor's call, document which in the SUMMARY).
  </behavior>
  <read_first>
    `git show c831dade -- crates/nono-proxy/tests/spiffe_integration.rs` for the full 188-line
    upstream test file (design reference / literal port target — confirmed by 113-RESEARCH.md's
    disposition table as HIGH confidence, ADOPT verbatim except the skip-marker convention).
    `crates/nono-cli/tests/socket_access_run.rs`'s `python3_available()` skip idiom (lines ~63-77,
    excerpted verbatim in 113-PATTERNS.md's "Landmines" L-section item on D-07) — this is the
    ANTI-PATTERN to explicitly not repeat.
    `crates/nono-proxy/src/server.rs`'s `pub async fn start` (Plan 113-05's landed signature) and
    `crates/nono-proxy/src/spiffe.rs`'s `SpiffeJwtSource` (Plan 113-01) for the exact types this test
    constructs against.
  </read_first>
  <action>
    Create `crates/nono-proxy/tests/spiffe_integration.rs` (the FIRST file in a brand-new
    `crates/nono-proxy/tests/` directory — confirmed via `ls` this session that no such directory
    exists yet; Rust auto-discovers `tests/*.rs` as integration-test binaries with no `Cargo.toml`
    wiring needed, but confirm `nono-proxy`'s `[dev-dependencies]` already carries `tokio` with
    `rt-multi-thread`/`macros`/`test-util` features — confirmed present this session). Port
    upstream's 4 tests, replacing every bare `eprintln!("skipping: ...")`-style early-return with
    the `SKIP[{module_path}]: <reason>` marker convention from this plan's `<objective>`. Add ONE new
    fork-original test (no upstream equivalent) asserting: a `RouteStore` (or a full
    `nono_proxy::server::start`-launched proxy) with a SPIFFE-declared route, when hit via a
    CONNECT/forward-HTTP/external-proxy-shaped request targeting that route's upstream host:port,
    is denied — exercising Plan 113-05's Task 2 guard end-to-end rather than merely at the unit level
    Plan 113-05 already covers. Prefer a construction that needs no live SPIRE agent (reusing Plan
    113-04's `declares_spiffe: true` test-only field, which does not require `managed_auth` to be
    genuinely connected for THIS specific guard, since the guard checks `has_spiffe_source()`, not
    `managed_auth`'s liveness) — if that is not achievable within this file's existing test
    scaffolding, gate the test on `SPIRE_AGENT_SOCKET` with its own `SKIP[...]` marker rather than
    silently skip.
  </action>
  <acceptance_criteria>
    - `cargo test -p nono-sandbox-proxy --test spiffe_integration -- --nocapture` runs; the
      fail-closed test passes unconditionally; the SPIRE-gated tests either pass (if
      `SPIRE_AGENT_SOCKET` happens to be set) or print a `SKIP[...]`-prefixed line and pass (skip is
      not a failure).
    - `grep -c "eprintln!(\"skipping:" crates/nono-proxy/tests/spiffe_integration.rs` returns `0`
      (confirms the anti-pattern is not repeated).
    - `grep -c "SKIP\[" crates/nono-proxy/tests/spiffe_integration.rs` returns at least `3` (one per
      SPIRE-gated ported test, plus the new fork-original test if it is also gated).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-proxy --test spiffe_integration -- --nocapture</automated>
  </verify>
  <done>The first nono-proxy integration test file exists, ported with loud-skip reporting, plus one fork-original D-03 test.</done>
</task>

<task type="auto">
  <name>Task 2: spiffe_run.rs (nono-cli/tests/) — 100% live-agent-gated e2e binary tests</name>
  <files>crates/nono-cli/tests/spiffe_run.rs</files>
  <behavior>
    - Both ported tests early-`return` with a `SKIP[...]` marker when `SPIRE_AGENT_SOCKET` is unset
      (confirmed by 113-RESEARCH.md: unlike `spiffe_integration.rs`, this file has ZERO
      fail-closed-without-SPIRE assertions — 100% live-agent-gated).
    - The mock HTTP server used by these tests is a plain `std::net::TcpListener` (`MockHttpServer::start()`),
      not Python — confirmed present in upstream's diff, zero `python3` dependency.
  </behavior>
  <read_first>
    `git show c831dade -- crates/nono-cli/tests/spiffe_run.rs` for the full 285-line upstream file.
    `crates/nono-cli/tests/socket_access_run.rs` for this fork's established
    `Command::new(env!("CARGO_BIN_EXE_nono"))` / `setup_isolated_home` / `run_nono` scaffolding
    conventions (113-PATTERNS.md's Pattern Assignment 6 already excerpts the exact functions to
    mirror).
  </read_first>
  <action>
    Port `spiffe_run.rs` verbatim, adapting only: (a) both tests' skip marker to the `SKIP[{}]:
    SPIRE_AGENT_SOCKET not set` convention (both are currently bare `return`s per
    113-RESEARCH.md's confirmed finding — add the marker before each), and (b) any
    `Command::new`/env-setup scaffolding to match this fork's `socket_access_run.rs` conventions if
    upstream's own scaffolding differs materially (e.g. different tempdir helper names) — prefer
    reusing this fork's existing helpers over introducing parallel ones.
  </action>
  <acceptance_criteria>
    - `cargo test -p nono-sandbox-cli --test spiffe_run -- --nocapture` runs; both tests print a
      `SKIP[...]` line and pass (no local SPIRE agent on this dev host).
    - `grep -c "SKIP\[" crates/nono-cli/tests/spiffe_run.rs` returns `2`.
    - `grep -c "python3\|spiffe-mock-server.py" crates/nono-cli/tests/spiffe_run.rs` returns `0`
      (confirms the pure-Rust mock server claim, resolving 113-CONTEXT.md's flagged
      re-verification item).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-cli --test spiffe_run -- --nocapture</automated>
  </verify>
  <done>spiffe_run.rs is ported with loud-skip reporting on both tests; zero python3 dependency confirmed.</done>
</task>

<task type="auto">
  <name>Task 3: SPIRE CI lane + local test-runner script + testdata + Makefile target</name>
  <files>.github/workflows/spire.yml, scripts/spire-test.sh, testdata/spire/server.conf, testdata/spire/agent.conf, Makefile</files>
  <read_first>
    This plan's `<interfaces>` block above for the exact `spire.yml` content (already adapted for
    this fork's `nono-sandbox-proxy`/`nono-sandbox-cli` package names).
    `.github/workflows/ci.yml`'s header block (permissions/env/checkout-SHA) for pin consistency,
    per 113-PATTERNS.md Pattern Assignment 7.
    `git show c831dade:scripts/spire-test.sh` for the full 187-line script.
  </read_first>
  <action>
    Create `.github/workflows/spire.yml` exactly as given in `<interfaces>` (package names already
    adapted; `actions/checkout` SHA already corrected to this fork's own pin). Create
    `scripts/spire-test.sh` from upstream's content, adapting its internal `cargo test -p
    nono-proxy`/`-p nono-cli` invocations to `-p nono-sandbox-proxy`/`-p nono-sandbox-cli`. Create
    `testdata/spire/server.conf` and `testdata/spire/agent.conf` verbatim (small, fork-agnostic test
    fixtures). Add the `test-spiffe` target to `Makefile` verbatim, plus the `.PHONY` list entry and
    `make help` line.
  </action>
  <acceptance_criteria>
    - `bash -n scripts/spire-test.sh` (syntax check only — do NOT run it live; no SPIRE binaries on
      this Windows dev host) exits 0.
    - `grep -c "nono-sandbox-proxy\|nono-sandbox-cli" scripts/spire-test.sh` returns at least `2`.
    - `grep -c "nono-sandbox-proxy\|nono-sandbox-cli" .github/workflows/spire.yml` returns exactly `2`.
    - `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/spire.yml'))" 2>/dev/null || true`
      — or this repo's own existing YAML-lint gate if one is already wired into CI — confirms
      `spire.yml` is syntactically valid YAML.
    - `make -n test-spiffe` (dry-run, since `make` itself may not be invokable on this host per the
      standing project fact — if `make` is genuinely absent, `grep -A1 "^test-spiffe:" Makefile`
      confirming the target body is the fallback check) shows `bash scripts/spire-test.sh`.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/spire-test.sh</automated>
  </verify>
  <done>A real Linux CI lane exists for the live SPIFFE path; the local runner script and test fixtures are adapted for this fork's crate names.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| CI runner (ubuntu-latest, GitHub-hosted) <-> downloaded SPIRE 1.9.6 release tarball | Fetched over HTTPS from a pinned version+checksum-free GitHub release URL; consistent with upstream's own trust model for this dependency (no fork-side hardening added or removed). |
| SPIRE join token <-> CI logs | Masked via `::add-mask::$TOKEN` immediately after generation, before any command that might echo it — preserved verbatim from upstream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-113-18 | Information Disclosure | SPIRE agent join token appearing in CI logs | mitigate | `::add-mask::` applied before the token is used in any subsequent command — verbatim from upstream, unmodified. |
| T-113-19 | Repudiation (evidence quality) | A silently-skipped test being mistaken for a passing live-path proof | mitigate | D-07's `SKIP[...]` marker convention, this plan's Task 1/2 — the exact mechanism this requirement exists to establish. |
| T-113-SC | Tampering | New CI-lane-only dependency: SPIRE 1.9.6 binary download | accept | CI-only (runs on GitHub's ephemeral runner, never on a dev host or in the shipped product); not part of the supply chain for any released artifact. |

</threat_model>

<verification>
- `cargo test -p nono-sandbox-proxy --test spiffe_integration -- --nocapture` runs, fail-closed test
  passes unconditionally, gated tests SKIP loudly.
- `cargo test -p nono-sandbox-cli --test spiffe_run -- --nocapture` runs, both tests SKIP loudly.
- `cargo build --workspace --all-targets` exits 0 (new test files compile).
- `cargo fmt --all --check` passes.
</verification>

<success_criteria>
- The full upstream SPIFFE test suite is ported with loud-skip reporting (D-07) and adapted for this
  fork's renamed crates.
- One fork-original test asserts D-03's fail-closed guard end-to-end.
- A real Linux CI lane exists that will genuinely exercise the live SPIFFE path (D-06).
</success_criteria>

<output>
Create `.planning/phases/113-spiffe-spire-workload-identity/113-07-SUMMARY.md` when done
</output>
