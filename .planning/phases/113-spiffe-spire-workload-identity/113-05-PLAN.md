---
phase: 113-spiffe-spire-workload-identity
plan: 05
type: execute
wave: 3
depends_on: [113-03, 113-04]
files_modified:
  - crates/nono-proxy/src/server.rs
autonomous: true
requirements: [NET-02]

must_haves:
  truths:
    - "D-04: server.rs's two blast-radius call sites (RouteStore::load, CredentialStore::load) are awaited; the stale server.rs:231-236 divergence comment (which currently asserts 'no TLS intercept, no SPIFFE, no async RouteStore::load') is rewritten since 2 of its 3 clauses are now false."
    - "D-03: a request on the CONNECT-tunnel, forward-HTTP, or external-proxy-chain path whose target host:port matches a SPIFFE-declared route's upstream is fail-closed denied at request time, composing with (not replacing) Phase 112's require_auth/strict_connect_auth gating."
    - "L3: log_reverse_proxy is not deleted (it has a live call site in reverse.rs this fork still uses)."
  artifacts:
    - path: "crates/nono-proxy/src/server.rs"
      provides: "async RouteStore::load/CredentialStore::load call sites, bound_port field, D-03 dispatcher guard"
  key_links:
    - from: "crates/nono-proxy/src/server.rs"
      to: "crates/nono-proxy/src/route.rs"
      via: "state.route_store.spiffe_declared_for_upstream(&host_port)"
      pattern: "spiffe_declared_for_upstream"
---

<objective>
Wire the two async-load call sites (D-04's actual blast radius: exactly 1 production site each in
this file, per `113-RESEARCH.md`'s "D-04 Blast-Radius Analysis"), add the `bound_port` self-request
plumbing, rewrite the stale divergence comment at `server.rs:231-236`, and implement D-03's
fail-closed guard: the three non-reverse-proxy dispatch paths (bypass-route CONNECT, external-proxy
chain, forward-HTTP) must refuse to silently tunnel/forward a request whose target matches a
SPIFFE-declared route's upstream, since none of those three paths has any SPIFFE implementation of
their own (D-01's residual risk).

Purpose: this is the phase's fork-original security control — upstream never needed it (upstream
has `tls_intercept` on these paths); this fork does not, so D-01's absorb creates exactly the gap
D-03 closes.
Output: `nono_proxy::server::start` awaits both stores; the three non-reverse-proxy paths fail
closed on a SPIFFE-declared target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-spiffe-spire-workload-identity/113-CONTEXT.md
@.planning/phases/113-spiffe-spire-workload-identity/113-RESEARCH.md
@.planning/phases/113-spiffe-spire-workload-identity/113-PATTERNS.md
@.planning/phases/112-security-residual-sync/112-REVIEW.md
@.planning/phases/113-spiffe-spire-workload-identity/113-03-SUMMARY.md
@.planning/phases/113-spiffe-spire-workload-identity/113-04-SUMMARY.md

<interfaces>
<!-- server.rs's real, current call sites (verified by direct read this session,
     crates/nono-proxy/src/server.rs lines 519-566) -->

```rust
pub async fn start(config: ProxyConfig) -> Result<ProxyHandle> {
    // ... listener bind, local_addr/port ...
    let route_store = if config.routes.is_empty() {
        RouteStore::empty()
    } else {
        RouteStore::load(&config.routes)?   // <- add .await here (D-04)
    };
    let route_hosts = route_store.route_upstream_hosts();
    validate_no_proxy_route_conflicts(&config.no_proxy, &route_hosts)?;
    let credential_store = if config.routes.is_empty() {
        CredentialStore::empty()
    } else {
        CredentialStore::load(&config.routes)?   // <- add .await here (D-04, from Plan 113-03)
    };
    // ...
}
```

<!-- server.rs's real dispatcher structure for D-03 (verified this session):
     handle_connection at line 1155; handle_forward_http at line 941 (a SEPARATE async fn, not
     inline in the dispatcher); the three-arm CONNECT dispatch (external chain / bypass-route direct
     / non-bypass) is INLINE inside handle_connection, per 113-PATTERNS.md Pattern Assignment 4's
     verbatim excerpt of lines ~1271-1317: -->

```rust
if let Some(ext_config) = use_external {
    let ext_ctx = external::ExternalProxyCtx { /* ..., require_auth: state.config.require_auth */ };
    external::handle_external_proxy(first_line, &mut stream, &header_bytes, &ext_ctx, ext_config).await
} else if state.config.external_proxy.is_some() {
    if state.config.require_auth {
        token::validate_proxy_auth(&header_bytes, &state.session_token)?;
    }
    connect::handle_connect(/* ... */).await
} else {
    connect::handle_connect(/* same shape, non-bypass */).await
}
```

D-03's guard must be inserted BEFORE each of these three arms dispatches, using the SAME target
host:port each arm already derives for its own use (CONNECT targets via `parse_connect_target`,
forward-HTTP targets via `parse_non_connect_target` — both already imported/used in this file per
the existing WR-13 fix, `112-REVIEW.md`). Do not add a `route_store` parameter to `connect.rs`/
`external.rs` — per `113-PATTERNS.md`'s own recommendation, the guard belongs in this dispatcher
since it already has `state.route_store` in scope and all three arms are inline or one call away.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Await both store loads + bound_port self-request plumbing</name>
  <files>crates/nono-proxy/src/server.rs</files>
  <behavior>
    - `nono_proxy::server::start` with zero `spiffe`-declared routes behaves byte-for-byte unchanged
      (no new await introduces any observable delay/behavior change for non-SPIFFE profiles — the
      awaited calls simply resolve immediately when the per-route branch that awaits is never taken).
    - `start` with a SPIFFE-declared route pointing at an unreachable socket returns `Err` (propagated
      from `RouteStore::load`, per Plan 113-04's Task 1 contract) rather than panicking or hanging.
  </behavior>
  <read_first>
    crates/nono-proxy/src/server.rs lines ~519-570 (the real `start` function, already read this
    session) and the `ProxyHandle` struct (confirm current field list around `managed_loopback_upstream`
    at line ~68, to see the established doc-comment style for a similar anticipatory field).
    113-RESEARCH.md's Symbol-Level Disposition Table row for `server.rs`: `bound_port` is
    "absent, needs adding (small, self-contained: one struct field + one handle_forward_http
    self-request check)".
    `crates/nono-proxy/src/server.rs`'s `handle_forward_http` (line ~941) to find its existing target
    host:port extraction, as the insertion point for the new self-request check.
  </read_first>
  <action>
    Add `.await` to both `RouteStore::load(&config.routes)?` and `CredentialStore::load(&config.routes)?`
    at their call sites inside `start()`. Add a `bound_port: u16` field to `ProxyHandle` (set from
    `local_addr.port()`, already computed in `start()` just above the route-loading block), with a
    doc comment following the existing `managed_loopback_upstream` field's established style. In
    `handle_forward_http`, add a check: if the parsed target's port equals `state.bound_port` and the
    host is a loopback address, this is the proxy's own reverse-proxy path arriving via absolute-form
    HTTP (matching the architecture diagram in 113-RESEARCH.md's "System Architecture Diagram") —
    treat it accordingly per this file's existing self-request handling convention if one already
    exists (grep for any existing loopback/self-detection logic in this file before adding a new one
    from scratch; if none exists, add a minimal one that logs and treats it as a normal forward
    rather than inventing new denial semantics not requested by any locked decision).
  </action>
  <acceptance_criteria>
    - `cargo build -p nono-sandbox-proxy --all-targets` exits 0.
    - `cargo test -p nono-sandbox-proxy --lib` — full pre-existing suite passes with zero new
      failures (confirms the two new `.await`s are behavior-neutral for non-SPIFFE profiles).
    - A new test: `nono_proxy::server::start` with a SPIFFE route at an unreachable socket returns
      `Err` within a bounded time (no hang).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-proxy --lib</automated>
  </verify>
  <done>Both store loads are awaited; bound_port exists on ProxyHandle; the full pre-existing test suite is unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-03 fail-closed guard on CONNECT / forward-HTTP / external-proxy paths</name>
  <files>crates/nono-proxy/src/server.rs</files>
  <behavior>
    - A CONNECT request (either the bypass-route or non-bypass arm) targeting a host:port that
      matches a SPIFFE-declared route's upstream is denied (403, or 503 if that better matches this
      file's existing denial-status convention for a config/credential-unavailable class of failure —
      match whatever `handle_forward_http`'s WR-13 guard already returns for its own denial) with an
      audit record carrying `denial_category: Some(NetworkAuditDenialCategory::SpiffeUnsupportedPath)`.
    - The same guard applies to `handle_forward_http` and the external-proxy-chain arm.
    - A CONNECT/forward-HTTP/external request targeting a host:port that does NOT match any
      SPIFFE-declared route passes through exactly as before (zero behavior change for non-SPIFFE
      traffic).
    - The new guard composes with, not replaces, the existing `require_auth`/`strict_connect_auth`
      checks — both can independently deny the same request; the guard should run regardless of
      `require_auth`'s value (a SPIFFE-declared route bypassing its only auth mechanism is a
      structural gap even when session-token auth is disabled via `--no-auth`).
  </behavior>
  <read_first>
    crates/nono-proxy/src/server.rs's `handle_connection` (line ~1155) in full, and
    `handle_forward_http` (line ~941) in full — the REAL current bodies, not the excerpt in this
    plan's `<interfaces>` block (that excerpt is a verified-accurate but partial quote; read the
    full functions to find the exact host:port variable names already in scope at each of the 3
    arms before inserting the guard).
    `112-REVIEW.md`'s WR-13 finding (`handle_forward_http` was the one path of six that ignored
    `config.require_auth` — the exact shape of bug D-03 exists to prevent for a DIFFERENT check).
    `crates/nono/src/undo/types.rs`'s `NetworkAuditDenialCategory::SpiffeUnsupportedPath` (landed in
    Plan 113-01 specifically for this task to consume).
  </read_first>
  <action>
    At each of the 3 non-reverse-proxy dispatch points (the bypass-route CONNECT arm, the non-bypass
    CONNECT arm, and `handle_forward_http`), after the target host:port is parsed (each arm already
    does this for its own auth/filter checks) and before the request is forwarded/tunneled, add:
    `if state.route_store.spiffe_declared_for_upstream(&host_port) { /* deny */ }`. On match: call
    `audit::log_denied` with `EventContext { denial_category:
    Some(nono::undo::NetworkAuditDenialCategory::SpiffeUnsupportedPath), ..Default::default() }`,
    then send an error response and return — mirror WR-13's exact control-flow shape (early return
    after the audit call, no partial forward). Apply this guard unconditionally (not gated on
    `state.config.require_auth`) — see `<behavior>` above for why. For the external-proxy-chain arm
    specifically, thread the check through BEFORE constructing `ExternalProxyCtx`/calling
    `external::handle_external_proxy` (do not add a parameter to `external.rs` itself — perform the
    lookup and denial entirely in this file's dispatcher, consistent with 113-PATTERNS.md's guidance).
  </action>
  <acceptance_criteria>
    - `cargo build -p nono-sandbox-proxy --all-targets` exits 0.
    - A new unit or integration test in this file's own `#[cfg(test)] mod tests` (or a lightweight
      `crates/nono-proxy/tests/` integration test if the guard cannot be exercised without a live
      TCP connection): constructs a `RouteStore` with one SPIFFE-declared route (via Plan 113-04's
      test-only `declares_spiffe: true` construction, no live agent needed) at a known upstream
      host:port, and asserts a request targeting that host:port via each of the 3 guarded paths is
      denied rather than forwarded.
    - `cargo test -p nono-sandbox-proxy --lib` — full pre-existing suite (218+ tests) still passes
      unchanged for non-SPIFFE traffic.
    - `grep -c "SpiffeUnsupportedPath" crates/nono-proxy/src/server.rs` returns at least `3` (one
      per guarded dispatch arm).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-sandbox-proxy --lib</automated>
  </verify>
  <done>All three non-SPIFFE-implementing proxy paths fail closed on a SPIFFE-declared target, composing with existing auth gating; non-SPIFFE traffic is unaffected.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the stale server.rs:231-236 divergence comment (Landmine L4)</name>
  <files>crates/nono-proxy/src/server.rs</files>
  <read_first>
    crates/nono-proxy/src/server.rs's comment block immediately preceding the `no_proxy`
    (#1415, D-06/D-07) section (re-locate by content — search for the string "no TLS intercept, no
    SPIFFE, no async RouteStore::load"; confirmed present at approximately line 231-236 this session
    but re-verify by content per this phase's own stale-line-number warning).
  </read_first>
  <action>
    Rewrite the parenthetical from "adapted to this fork's simpler `ProxyHandle`/`RouteStore` shape
    (no TLS intercept, no SPIFFE, no async `RouteStore::load`)" to something that stops asserting the
    two now-false clauses — e.g. "adapted to this fork's `ProxyHandle`/`RouteStore` shape (no TLS
    intercept — see `proj/ADR-113-spiffe-disposition.md`/D-01 — but SPIFFE-route support and async
    `RouteStore::load` landed in Phase 113)". Exact wording is at the executor's discretion; the only
    hard constraint is that neither "no SPIFFE" nor "no async RouteStore::load" may remain asserted.
  </action>
  <acceptance_criteria>
    - `grep -c "no TLS intercept, no SPIFFE, no async RouteStore::load" crates/nono-proxy/src/server.rs`
      returns `0`.
    - `cargo build -p nono-sandbox-proxy --all-targets` exits 0 (comment-only change).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "no TLS intercept, no SPIFFE, no async RouteStore::load" crates/nono-proxy/src/server.rs</automated>
  </verify>
  <done>The stale divergence comment no longer asserts two now-false clauses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Untrusted client (sandboxed agent or standalone-proxy caller) <-> proxy dispatcher | The point where D-03's guard and the existing require_auth/strict_connect_auth checks both apply, independently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-113-13 | Tampering / Elevation of Privilege | CONNECT/forward-HTTP/external-proxy silently tunneling to a SPIFFE-declared upstream, bypassing credential injection entirely | mitigate | D-03's guard, Task 2 — this is the phase's own residual risk from D-01's tls_intercept drop, closed here at request time on all 3 non-reverse paths. |
| T-113-14 | Denial of Service (safe failure) | The guard itself becoming a false-positive DoS against legitimate non-SPIFFE traffic sharing a hostname with a SPIFFE route | accept (structurally low-risk) | `spiffe_declared_for_upstream` matches on the SAME normalised host:port matching logic `is_route_upstream` already uses for the (already-shipped, already-tested) `no_proxy` conflict-detection feature — no new matching surface, only a new boolean AND-ed onto an existing, proven comparison. |

</threat_model>

<verification>
- `cargo build -p nono-sandbox-proxy --all-targets` exits 0.
- `cargo test -p nono-sandbox-proxy --lib` — full suite passes, zero new failures.
- `cargo fmt --all --check` passes.
- `cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used`
  and `cargo-zigbuild clippy --workspace --target x86_64-apple-darwin -- -D warnings -D
  clippy::unwrap_used` both GREEN (D-12 — this file has no cfg-gated Unix code itself, but is part
  of the workspace-wide gate; run at minimum at this wave's boundary per `113-VALIDATION.md`'s
  sampling rate).
</verification>

<success_criteria>
- Both async store loads are awaited; `ProxyHandle.bound_port` exists.
- D-03's fail-closed guard is live on all 3 non-reverse-proxy paths, composing with existing auth
  gating, with zero regression to non-SPIFFE traffic.
- The stale divergence comment is corrected.
</success_criteria>

<output>
Create `.planning/phases/113-spiffe-spire-workload-identity/113-05-SUMMARY.md` when done
</output>
