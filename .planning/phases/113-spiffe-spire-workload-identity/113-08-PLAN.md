---
phase: 113-spiffe-spire-workload-identity
plan: 08
type: execute
wave: 5
depends_on: [113-07]
files_modified:
  - proj/ADR-113-spiffe-disposition.md
  - .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md
  - ../nono-py/src/proxy.rs
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [NET-02]

must_haves:
  truths:
    - "SC1: proj/ADR-113-spiffe-disposition.md exists, settles adopt-vs-adapt-vs-defer, and discharges D-01's positive grep-backed proof, D-05's full (a)+(b)+(c) dependency review, D-08's re-confirmed ADR-86 reading, and OD-1's named scope-boundary against b1ecbc02."
    - "D-02: the dropped tls_intercept hunks (~478 lines) get a carry-forward note in 108-DIVERGENCE-LEDGER.md, mirroring the SEC-09 Carry-Forward Note precedent, additionally recording b1ecbc02 as a newly-identified, previously-unledgered divergence per OD-1."
    - "D-05: cargo audit is clean with the spiffe dependency live; the JNI/Android path is proven cfg-gated out (zero cargo tree output) on all three shipped targets (windows-msvc, linux-gnu, apple-darwin)."
    - "D-09: ../nono-py's RustRouteConfig exhaustive literal gains spiffe: None, and maturin build succeeds; ../nono-ts's napi build succeeds (expected no-op, zero nono_proxy references)."
    - "SC4: both cross-target clippy gates (linux-gnu via cross clippy, apple-darwin via cargo-zigbuild clippy) run GREEN on the full Phase 113 diff, no PARTIAL->CI, and both sibling bindings rebuild green."
    - "D-12: both cross-target clippy gates are mandatory and locally runnable for this phase — no PARTIAL->CI fallback, per the same D-12 obligation Plans 113-01/113-05 already carry."
    - "D-07: the phase verification artifact names, counts, and reports every SPIRE-gated test that skipped locally, compensated by (not confused with) the new spire.yml CI lane."
    - "D-11: this phase's own re-verification discipline (113-RESEARCH.md's Symbol-Level Disposition Table, corrected in 3 places by this plan set's own direct code reads — e.g. route.rs/reverse.rs's absent forward module, OAuth2Config's missing #[serde(default)], the fork's real 4-function audit.rs vs the table's assumed 5) is itself D-11 in practice: every feature-presence claim in this phase was confirmed by behavior/symbol, never by identifier name alone."
    - "NET-02: SPIFFE/SPIRE workload-identity auth for upstream proxy routes is absorbed under this ADR-gated review, satisfying the sole requirement this phase owns."
    - "The ADR-113 file, new under proj/, is committed via git add -f (gitignore's bare proj/ pattern silently drops new files under proj/ otherwise — Landmine L2)."
  artifacts:
    - path: "proj/ADR-113-spiffe-disposition.md"
      provides: "The phase's binding disposition record, mirroring proj/ADR-111-resource-limits-boundary.md's shape"
  key_links:
    - from: "proj/ADR-113-spiffe-disposition.md"
      to: ".planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md"
      via: "D-02 carry-forward note cross-reference"
      pattern: "108-DIVERGENCE-LEDGER"
---

<objective>
Close the phase: write `proj/ADR-113-spiffe-disposition.md` (SC1), discharging every proof
obligation D-01/D-05/D-08/OD-1 placed on it with commands-and-output, not prose assurance; file the
D-02 carry-forward note in `108-DIVERGENCE-LEDGER.md`; complete D-05's dependency review now that
`spiffe` is genuinely live in `Cargo.lock`; fix and rebuild the `nono-py` binding (D-09); run both
mandatory cross-target clippy gates over the full phase diff; and produce the D-07 skip-count
verification statement.

Purpose: this is the phase's evidence-and-record plan — every prior plan built the feature; this
plan proves it, documents the scope decisions a future absorb needs to find, and closes the
binding-rebuild/dependency-audit obligations that only make sense once everything else has landed.
Output: SC1-SC4 all satisfied with cited evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-spiffe-spire-workload-identity/113-CONTEXT.md
@.planning/phases/113-spiffe-spire-workload-identity/113-RESEARCH.md
@.planning/phases/113-spiffe-spire-workload-identity/113-VALIDATION.md
@proj/ADR-111-resource-limits-boundary.md
@proj/ADR-86-library-boundary-convergence.md
@.planning/phases/112-security-residual-sync/112-OAUTH-CAPTURE-DISPOSITION.md
@.planning/templates/cross-target-verify-checklist.md
@.planning/phases/113-spiffe-spire-workload-identity/113-01-SUMMARY.md
@.planning/phases/113-spiffe-spire-workload-identity/113-02-SUMMARY.md
@.planning/phases/113-spiffe-spire-workload-identity/113-03-SUMMARY.md
@.planning/phases/113-spiffe-spire-workload-identity/113-04-SUMMARY.md
@.planning/phases/113-spiffe-spire-workload-identity/113-05-SUMMARY.md
@.planning/phases/113-spiffe-spire-workload-identity/113-06-SUMMARY.md
@.planning/phases/113-spiffe-spire-workload-identity/113-07-SUMMARY.md

<interfaces>
<!-- ../nono-py/src/proxy.rs's exact break site, verified via 113-RESEARCH.md's D-09 section and
     113-PATTERNS.md's Pattern Assignment 8 (both independently confirm the same lines). -->

```rust
// ../nono-py/src/proxy.rs, inside the exhaustive RustRouteConfig { .. } literal, immediately
// after the existing `aws_auth: None,` line:
spiffe: None,
```

<!-- D-05(c) JNI cfg-gate proof — exact commands, verbatim from 113-RESEARCH.md's Package
     Legitimacy Audit section: -->

```bash
cargo tree --target x86_64-pc-windows-msvc -e features -p nono-sandbox-proxy | grep -i jni
cargo tree --target x86_64-unknown-linux-gnu -e features -p nono-sandbox-proxy | grep -i jni
cargo tree --target x86_64-apple-darwin -e features -p nono-sandbox-proxy | grep -i jni
# Expected: zero output on all three.
cargo tree -i jni   # confirms the pull path is scoped to an Android-only feature/target.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ADR-113 + D-02 ledger carry-forward note</name>
  <files>proj/ADR-113-spiffe-disposition.md, .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</files>
  <read_first>
    proj/ADR-111-resource-limits-boundary.md in full (the shape precedent: Status/Phase/Date/Authors
    frontmatter, `## Context`, `## Decision`, a "why the ADR-86 carve-out does/doesn't extend here"
    section, `## Consequences`, `## References`).
    108-DIVERGENCE-LEDGER.md's "SEC-09 Carry-Forward Note" section in full (the shape precedent for
    this task's ledger addition — read its exact heading style, "Filed:" line, and "Obligation for
    whichever future plan..." closing paragraph).
    113-RESEARCH.md's "⚠ Decision Conflicts" section and "Open Questions" section 2 (the exact
    reasoning ADR-113 must record for OD-1's scope-boundary decision).
  </read_first>
  <action>
    Write `proj/ADR-113-spiffe-disposition.md` with: (1) Context — the ~90%-lands-cleanly-but-larger-
    than-CONTEXT.md-characterized finding from 113-RESEARCH.md's Decision Conflicts section; (2)
    Decision — ADAPT-DOWN confirmed (D-01), with a POSITIVE proof discharging D-01's obligation: cite
    `grep -rn 'tls_intercept' crates/nono-proxy/src/ | wc -l` returning `0`, AND enumerate every
    proxy request-entry path (reverse-proxy via `handle_spiffe_route`/`handle_spiffe_assertion_credential`;
    CONNECT/forward-HTTP/external-proxy via Plan 113-05's D-03 guard) with its confirmed SPIFFE
    disposition — this is the "no fork route type is left silently unauthenticated" proof D-01
    demands, not an assertion; (3) a section discharging D-05's full review: (a) the 19-crate
    enumeration (from 113-RESEARCH.md's Standard Stack table, re-confirmed live via `cargo tree -p
    nono-sandbox-proxy` now that the dependency is actually in `Cargo.lock`), (b) `cargo audit` clean
    (Task 2's output, cite it), (c) the JNI cfg-gate proof (Task 2's output, cite it); (4) a section
    re-confirming D-08/SC3's ADR-86 reading against the FULL diff (not inherited from CONTEXT.md) —
    cite the exact field/variant list added to `undo/types.rs`/`audit.rs` and assert zero
    enforcement/policy logic, per Plan 113-01's landed code; (5) OD-1's named, permanent scope
    boundary — record explicitly that `b1ecbc02`'s general (non-SPIFFE) OAuth2 `client_credentials`
    route-wiring layer (`CredentialStore.oauth2_routes`/`get_oauth2()`/`OAuth2Route`,
    `LoadedRoute.requires_managed_credential`/`managed_auth_mechanism`/`managed_injection_mode`/
    `missing_managed_credential()`, `crate::forward`) was deliberately NOT built, mirroring
    ADR-111's "reject the core-module absorb" shape, so a future planner absorbing `b1ecbc02` proper
    finds this ADR and understands the SPIFFE-only slice it is reconciling against; (6) Consequences
    — the D-02 ledger note (below) is cross-referenced here, not duplicated.
    Then append a "SPIFFE Carry-Forward Note (Phase 113, D-02)" section to
    `108-DIVERGENCE-LEDGER.md`'s existing tool-sandbox-surface / carry-forward area, mirroring the
    SEC-09 Carry-Forward Note's exact shape: record the ~478 dropped `tls_intercept/{h2_forward,
    handle}.rs` lines as a standing gap for whichever future phase introduces a `tls_intercept/`
    module, AND additionally record `b1ecbc02` (974 lines / 8 files, `v0.38.0-3-gb1ecbc02`) as a
    newly-identified, previously-unledgered divergence per OD-1 — this is new information this
    phase's research surfaced that the ledger has never recorded before.
  </action>
  <acceptance_criteria>
    - `test -f proj/ADR-113-spiffe-disposition.md` (once force-added, per Task 3 below).
    - `grep -c "b1ecbc02" proj/ADR-113-spiffe-disposition.md` returns at least `1`.
    - `grep -c "SPIFFE Carry-Forward Note" .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md`
      returns `1`.
    - `grep -c "b1ecbc02" .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md`
      returns at least `1`.
  </acceptance_criteria>
  <verify>
    <automated>test -f proj/ADR-113-spiffe-disposition.md</automated>
  </verify>
  <done>ADR-113 exists with every proof obligation discharged by citation, not assertion; the ledger carries the D-02 carry-forward note including the newly-found b1ecbc02 divergence.</done>
</task>

<task type="auto">
  <name>Task 2: D-05 dependency review completion + D-09 nono-py fix + both binding rebuilds</name>
  <files>../nono-py/src/proxy.rs</files>
  <read_first>
    This plan's `<interfaces>` block above for the exact `nono-py` fix and JNI-proof commands.
    113-RESEARCH.md's "Package Legitimacy Audit" section (the 13-crate slopcheck table, already run
    this session — this task does NOT need to re-run slopcheck, only the two proofs that require the
    dependency to be LIVE: `cargo audit` and the per-target `cargo tree` JNI check).
  </read_first>
  <action>
    Run `cargo audit` against the workspace with `spiffe` now live in `Cargo.lock`; confirm clean (or
    record any new advisory verbatim if one appears — do not silently suppress). Run the 4 `cargo
    tree` commands from this plan's `<interfaces>` block; confirm zero output for all three shipped
    targets' `grep -i jni`, and inspect `cargo tree -i jni`'s output to confirm the pull path is
    scoped to `rustls-platform-verifier`'s Android-only feature (not unconditional). In
    `../nono-py/src/proxy.rs`, add `spiffe: None,` immediately after the existing `aws_auth: None,`
    line inside the exhaustive `RustRouteConfig { .. }` literal. Run `maturin build` in `../nono-py`;
    confirm it succeeds. Run `napi build --platform --release` in `../nono-ts`; confirm it succeeds
    (expected no-op — zero `nono_proxy` references in that repo, per 113-RESEARCH.md's D-09 finding).
  </action>
  <acceptance_criteria>
    - `cargo audit` exits 0 (or the new advisory, if any, is recorded verbatim in the SUMMARY with a
      disposition, not silently ignored).
    - `cargo tree --target x86_64-pc-windows-msvc -e features -p nono-sandbox-proxy | grep -i jni`,
      the linux-gnu variant, and the apple-darwin variant all produce zero output.
    - `maturin build` (in `../nono-py`) exits 0.
    - `napi build --platform --release` (in `../nono-ts`) exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo audit</automated>
  </verify>
  <done>cargo audit clean, JNI cfg-gate proven closed on all 3 shipped targets, both sibling bindings rebuild green.</done>
</task>

<task type="auto">
  <name>Task 3: Force-add the ADR + both mandatory cross-target clippy gates + D-07 skip report + requirement/roadmap reconciliation</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    `.planning/templates/cross-target-verify-checklist.md` in full (single source of truth for the
    two gate invocations — already read this session).
    `.gitignore`'s line 16 bare `proj/` pattern (Landmine L2 — confirmed empirically this phase's
    research: a plain `git add`/`git status` silently omits a brand-new file under `proj/`).
    `.planning/REQUIREMENTS.md`'s NET-02 line (~114) and traceability row (~164) — currently reads
    "MOVED to Phase 113" pending this phase's completion.
    `.planning/ROADMAP.md`'s Phase 113 checklist box and Progress table row.
  </read_first>
  <action>
    `git add -f proj/ADR-113-spiffe-disposition.md` (the `.gitignore:16` bare `proj/` pattern would
    otherwise silently drop this new file — do NOT rely on a plain `git add`). Run both mandatory
    cross-target clippy gates over the tree with all of Waves 1-4 merged:
    `cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D
    clippy::unwrap_used` and `cargo-zigbuild clippy --workspace --target x86_64-apple-darwin -- -D
    warnings -D clippy::unwrap_used` (`SDKROOT` unset) — both must exit 0; close any finding
    structurally (never `#[allow]`), per the checklist's Anti-Pattern 2. Run `cargo fmt --all
    --check` (confirms every let-chain rewrite across Plans 113-01/113-02 landed clean). Run `cargo
    test --workspace --no-fail-fast`; diff the failing-test-name set against the documented
    pre-existing baseline (per prior-phase precedent — e.g. `111-04-VERIFICATION-NOTES.md`'s 17-name
    baseline) and confirm it is a strict subset (zero new failure names). Produce the D-07 skip
    report: run `cargo test -p nono-sandbox-proxy --test spiffe_integration -- --nocapture 2>&1 |
    grep -c '^SKIP\['` and the same for `-p nono-sandbox-cli --test spiffe_run`; record BOTH counts
    and the full list of named skipped tests in this plan's own SUMMARY, worded per
    `113-VALIDATION.md`'s "Expected phase-verification wording" template — e.g. "N SPIFFE tests
    SKIPPED (no local SPIRE agent): `<names>` — compensated by the new `spire.yml` CI lane, not
    locally exercised." Flip `NET-02`'s checkbox and traceability row in `.planning/REQUIREMENTS.md`
    to Complete, citing this phase. Flip Phase 113's checklist box and add its Progress-table row in
    `.planning/ROADMAP.md` (the SDK's `roadmap.update-plan-progress` does not reliably add the
    Progress row per recurring project precedent — add it by hand if the automated step misses it).
  </action>
  <acceptance_criteria>
    - `git show --stat HEAD | grep -c "proj/ADR-113-spiffe-disposition.md"` returns `1` after commit
      (confirms Landmine L2 did not silently drop the file).
    - `cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D
      clippy::unwrap_used` exits 0.
    - `cargo-zigbuild clippy --workspace --target x86_64-apple-darwin -- -D warnings -D
      clippy::unwrap_used` exits 0.
    - `cargo fmt --all --check` exits 0.
    - The full `cargo test --workspace --no-fail-fast` failing-test-name set is a subset of the
      documented pre-existing baseline (zero new names) — recorded explicitly in the SUMMARY, not
      merely asserted.
    - `.planning/REQUIREMENTS.md`'s NET-02 checkbox is checked with a "Complete" disposition citing
      Phase 113.
  </acceptance_criteria>
  <verify>
    <automated>cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used</automated>
  </verify>
  <done>ADR-113 is provably committed; both cross-target gates GREEN; D-07's skip count is named and reported; NET-02/Phase 113 are reconciled in REQUIREMENTS.md/ROADMAP.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Documented disposition (ADR-113) <-> future absorb decisions | The ADR is the binding record a future planner reads before re-deciding any of this phase's scope calls (D-01/OD-1 especially) — its accuracy is itself a security control against re-introducing the SPIFFE-drop residual risk unknowingly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-113-20 | Tampering | `spiffe` dependency's transitive supply chain (19 crates, incl. a gRPC/protobuf stack) | mitigate | `cargo audit` clean (this plan, Task 2) + `slopcheck` clean (113-RESEARCH.md, prior) + JNI cfg-gate proof closing the one flagged residual (Android path never linking on a shipped target). |
| T-113-21 | Repudiation | An incomplete or silently-favorable ADR understating the absorb's real risk | mitigate | Every proof obligation in Task 1 is discharged by a cited command/output or a cited landed-code artifact, not prose — matching this phase's own "positive, grep-backed proof" standard applied to itself. |
| T-113-22 | Elevation of Privilege (regression) | Cross-target clippy gate finding closed with `#[allow]` instead of structurally | mitigate | Cross-target-verify-checklist's Anti-Pattern 2 explicitly forbidden; any finding in Task 3 must be closed via cfg-gate/visibility/structural fix. |

</threat_model>

<verification>
- `cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used` exits 0.
- `cargo-zigbuild clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used` exits 0.
- `cargo fmt --all --check` exits 0.
- `cargo audit` exits 0.
- `maturin build` (../nono-py) and `napi build --platform --release` (../nono-ts) both exit 0.
- `test -f proj/ADR-113-spiffe-disposition.md` and it is confirmed present in `git show --stat HEAD`.
</verification>

<success_criteria>
- SC1: `proj/ADR-113-spiffe-disposition.md` exists and settles the disposition with cited proof.
- SC2: SPIFFE/SPIRE auth is absorbed per the ADR's disposition and is profile-configurable (landed
  across Plans 113-01 through 113-07; confirmed here).
- SC3: the ADR-86 boundary is confirmed non-regressed.
- SC4: both cross-target clippy gates are GREEN and both sibling bindings rebuild green.
- NET-02 is marked Complete in `.planning/REQUIREMENTS.md`; Phase 113 is marked complete in
  `.planning/ROADMAP.md`.
</success_criteria>

<output>
Create `.planning/phases/113-spiffe-spire-workload-identity/113-08-SUMMARY.md` when done
</output>
