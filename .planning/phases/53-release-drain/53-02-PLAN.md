---
phase: 53-release-drain
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/todos/pending/44-class-d-validator-preflight-investigation.md
  - .planning/todos/pending/44-validate-restore-target-fd-relative-hardening.md
  - .planning/todos/done/44-class-d-validator-preflight-investigation.md
  - .planning/todos/done/44-validate-restore-target-fd-relative-hardening.md
autonomous: true
requirements:
  - REQ-DRN-02
must_haves:
  truths:
    - "Todos 2 and 3 have explicit written rationale committed explaining why they are NOT done in-phase"
    - "Both todo files are moved from todos/pending/ to todos/done/ with pointers to their REQUIREMENTS.md backlog entries"
    - "REQUIREMENTS.md v2 Deferred section contains backlog entries for both todos with their dedicated tracking information"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated v2 Deferred section with backlog entries for Todo 2 and Todo 3"
      contains: "44-class-d-validator-preflight-investigation"
    - path: ".planning/todos/done/44-class-d-validator-preflight-investigation.md"
      provides: "Closed todo file with pointer to backlog entry"
    - path: ".planning/todos/done/44-validate-restore-target-fd-relative-hardening.md"
      provides: "Closed todo file with pointer to backlog entry"
  key_links:
    - from: ".planning/todos/done/44-class-d-validator-preflight-investigation.md"
      to: ".planning/REQUIREMENTS.md"
      via: "pointer to backlog entry REQ-DENY-PREFLIGHT-01 in v2 Deferred section"
    - from: ".planning/todos/done/44-validate-restore-target-fd-relative-hardening.md"
      to: ".planning/REQUIREMENTS.md"
      via: "pointer to backlog entry REQ-UNDO-TOCTOU-01 in v2 Deferred section"
---

<objective>
Satisfy REQ-DRN-02 by re-dispositioning Todos 2 and 3 with committed rationale (D-53-08). Neither todo is done in-phase; both are promoted to the REQUIREMENTS.md v2 "Deferred" backlog with explicit written rationale, then their pending files are moved to todos/done/ with pointers back to the backlog entries so they are roadmap-visible rather than loose todos.

Purpose: Close the "3 pending todos" carry-forward by recording the decision, not by doing the work. The rationale is what satisfies REQ-DRN-02's "re-dispositioned with committed rationale" clause.

Output: REQUIREMENTS.md extended with two new v2 Deferred backlog entries; both todo files moved to done/ with disposition notes; pending/ folder cleared of these two todos.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\OMack\Nono\.planning\PROJECT.md
@C:\Users\OMack\Nono\.planning\ROADMAP.md
@C:\Users\OMack\Nono\.planning\STATE.md
@C:\Users\OMack\Nono\.planning\phases\53-release-drain\53-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backlog entries to REQUIREMENTS.md v2 Deferred section</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    Read REQUIREMENTS.md in full to understand the exact current structure of the v2 Deferred
    section and confirm where to insert the two new entries.
    Read both pending todo files to extract the key context fields for the backlog entries:
    - .planning/todos/pending/44-class-d-validator-preflight-investigation.md
    - .planning/todos/pending/44-validate-restore-target-fd-relative-hardening.md
  </read_first>
  <action>
    Append two new requirement entries to the `## v2 Requirements (Deferred)` section in
    REQUIREMENTS.md, immediately after the existing "Release-pipeline attestation" entry.
    Use the same format as the existing deferred entries (bold REQ-ID, italic deferred tag,
    rationale sentence).

    Entry for Todo 2 (class-d validator preflight):
    - REQ-ID: REQ-DENY-PREFLIGHT-01
    - Deferred tag: from v2.7 carry-forward (Phase 44 Plan 44-02 D-44-C3 follow-up)
    - Body: Linux-host-gated investigation of why `validate_deny_overlaps` pre-flight does not
      fire on CI Linux (the either-or assertion already proves security equivalence — both
      code paths deny the read, neither leaks the secret). This is a latent-diagnostic
      investigation, not a security gap. Deferred until a dedicated Linux CI runner is
      available for the instrumented-trace investigation (RUST_LOG=trace + strace).
      Source: `.planning/todos/pending/44-class-d-validator-preflight-investigation.md`.
      Rationale per D-53-08: security equivalence is proven; investigation is low-priority
      and Linux-host-gated.
    - Priority: low
    - Affects: crates/nono-cli/src/policy.rs:1032-1088, crates/nono-cli/tests/deny_overlap_run.rs

    Entry for Todo 3 (validate_restore_target TOCTOU hardening):
    - REQ-ID: REQ-UNDO-TOCTOU-01
    - Deferred tag: from v2.7 carry-forward (Phase 44 Plan 44-01 WR-01 P43 follow-up)
    - Body: Full fd-relative TOCTOU hardening of `validate_restore_target` + downstream
      write sequence in crates/nono/src/undo/snapshot.rs. Requires O_NOFOLLOW + openat/
      mkdirat/renameat/fchmodat on Linux/macOS and NtCreateFile-with-OBJ_DONT_REPARSE or
      documented defense-in-depth on Windows. Estimated ~2-3 weeks of focused cross-platform
      work. Warrants a dedicated security-scoped phase.
      Source: `.planning/todos/pending/44-validate-restore-target-fd-relative-hardening.md`.
      Rationale per D-53-08: residual TOCTOU race is a known local-attacker-with-write-access
      scenario; doc note already shipped (Phase 44); full closure is a standalone security
      phase, not a drain item.
    - Priority: medium
    - Affects: crates/nono/src/undo/snapshot.rs

    Do NOT modify any existing v1 requirement entries. Do NOT remove any existing v2 Deferred
    entries. Append only.

    DCO sign-off on the commit: `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`
  </action>
  <verify>
    <automated>grep -c "REQ-DENY-PREFLIGHT-01\|REQ-UNDO-TOCTOU-01" .planning/REQUIREMENTS.md</automated>
    Expected: 2 (one match per new entry).
    <automated>grep -c "44-class-d-validator-preflight-investigation\|44-validate-restore-target-fd-relative-hardening" .planning/REQUIREMENTS.md</automated>
    Expected: 2 (source references present in backlog entries).
  </verify>
  <acceptance_criteria>
    1. REQUIREMENTS.md v2 Deferred section contains an entry with REQ-DENY-PREFLIGHT-01
       including the words "security equivalence" and the D-53-08 rationale.
    2. REQUIREMENTS.md v2 Deferred section contains an entry with REQ-UNDO-TOCTOU-01
       including the words "dedicated security-scoped phase" and the D-53-08 rationale.
    3. Both entries include the source todo file path as a reference.
    4. No existing REQUIREMENTS.md entries are modified or removed.
    5. Commit includes DCO sign-off.
  </acceptance_criteria>
  <done>REQUIREMENTS.md v2 Deferred section has REQ-DENY-PREFLIGHT-01 and REQ-UNDO-TOCTOU-01 with D-53-08 rationale committed.</done>
</task>

<task type="auto">
  <name>Task 2: Move todo files from pending/ to done/ with disposition notes</name>
  <files>
    .planning/todos/pending/44-class-d-validator-preflight-investigation.md,
    .planning/todos/pending/44-validate-restore-target-fd-relative-hardening.md,
    .planning/todos/done/44-class-d-validator-preflight-investigation.md,
    .planning/todos/done/44-validate-restore-target-fd-relative-hardening.md
  </files>
  <read_first>
    Read both pending todo files again to confirm their full contents before creating the
    done/ versions (the done/ files are the same content with a disposition header appended):
    - .planning/todos/pending/44-class-d-validator-preflight-investigation.md
    - .planning/todos/pending/44-validate-restore-target-fd-relative-hardening.md
    Verify that .planning/todos/done/ directory exists (it does — confirmed by directory
    listing; existing done/ files include 41-10-linux-deny-overlap-regression.md etc.).
  </read_first>
  <action>
    For each of the two pending todos, perform a git mv equivalent: create the done/ copy
    with the full original content plus a disposition footer, then delete (or git-remove)
    the pending/ copy. Since the Write tool creates new files, use it to write the done/
    copies, then use Bash to `git rm` the pending/ originals.

    The disposition footer to append to each file (add as a new top-level section after
    all existing content):

    For 44-class-d-validator-preflight-investigation.md:
    ---
    ## Disposition (Phase 53)
    **Status:** Promoted to backlog — NOT done in-phase per D-53-08.
    **Rationale:** Security equivalence is already proven (the either-or assertion in the
    Class D test proves both branches deny the read). This is a latent-diagnostic
    investigation, not a security gap. Low-priority; requires a Linux host with
    instrumented trace capability (RUST_LOG=trace + strace) which the agent shell lacks.
    **Backlog entry:** REQUIREMENTS.md REQ-DENY-PREFLIGHT-01 (v2 Deferred section).
    **Date:** 2026-05-28

    For 44-validate-restore-target-fd-relative-hardening.md:
    ---
    ## Disposition (Phase 53)
    **Status:** Promoted to backlog — NOT done in-phase per D-53-08.
    **Rationale:** Full closure requires ~2-3 weeks of focused cross-platform work
    (O_NOFOLLOW + fd-relative openat/mkdirat/renameat/fchmodat on Linux/macOS;
    NtCreateFile-based or documented defense-in-depth on Windows). The residual TOCTOU
    race is a local-attacker-with-write-access scenario; a doc note was shipped in
    Phase 44. Full closure warrants a dedicated security-scoped phase.
    **Backlog entry:** REQUIREMENTS.md REQ-UNDO-TOCTOU-01 (v2 Deferred section).
    **Date:** 2026-05-28

    After writing both done/ files, `git rm` the two pending/ files:
      git rm .planning/todos/pending/44-class-d-validator-preflight-investigation.md
      git rm .planning/todos/pending/44-validate-restore-target-fd-relative-hardening.md

    DCO sign-off on the commit: `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`
  </action>
  <verify>
    <automated>test ! -f .planning/todos/pending/44-class-d-validator-preflight-investigation.md && echo "ABSENT" || echo "STILL_PRESENT"</automated>
    Expected: ABSENT
    <automated>test ! -f .planning/todos/pending/44-validate-restore-target-fd-relative-hardening.md && echo "ABSENT" || echo "STILL_PRESENT"</automated>
    Expected: ABSENT
    <automated>test -f .planning/todos/done/44-class-d-validator-preflight-investigation.md && echo "EXISTS" || echo "MISSING"</automated>
    Expected: EXISTS
    <automated>test -f .planning/todos/done/44-validate-restore-target-fd-relative-hardening.md && echo "EXISTS" || echo "MISSING"</automated>
    Expected: EXISTS
    <automated>grep -l "Disposition (Phase 53)" .planning/todos/done/44-class-d-validator-preflight-investigation.md .planning/todos/done/44-validate-restore-target-fd-relative-hardening.md | wc -l</automated>
    Expected: 2
  </verify>
  <acceptance_criteria>
    1. Both pending todo files are absent from .planning/todos/pending/ (git rm'd).
    2. Both done/ copies exist and contain the original content plus a "## Disposition (Phase 53)" section.
    3. Each done/ file's disposition section contains the REQ-ID of its REQUIREMENTS.md backlog entry.
    4. `ls .planning/todos/pending/` shows neither file remains.
    5. Commit includes DCO sign-off.
  </acceptance_criteria>
  <done>Both todo files moved from pending/ to done/ with committed disposition rationale; REQUIREMENTS.md backlog entries created; pending/ is clear of these two files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning artifacts → future phases | The REQUIREMENTS.md backlog entries become the authoritative tracking record; inaccurate rationale could cause future phases to misinterpret scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-02-01 | Tampering | REQUIREMENTS.md v1 section | mitigate | Accept-criteria explicitly require no modification to v1 entries; verify with grep that v1 REQ-IDs remain unchanged |
| T-53-02-02 | Information Disclosure | todo file contents copied to done/ | accept | Planning artifacts are non-sensitive; disposition notes contain no credentials or keys |
| T-53-02-03 | Denial | Git rm of pending files without confirmed done/ copies | mitigate | Write done/ copies first; verify done/ existence before git rm |
| T-53-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs; documentation-only changes |
</threat_model>

<verification>
After both tasks complete:
- `ls .planning/todos/pending/` shows neither 44-class-d nor 44-validate-restore files
- `ls .planning/todos/done/` shows both done/ files exist
- `grep "REQ-DENY-PREFLIGHT-01\|REQ-UNDO-TOCTOU-01" .planning/REQUIREMENTS.md` returns 2 matches
- REQ-DRN-02 acceptance condition is met: both todos have committed written rationale
</verification>

<success_criteria>
REQ-DRN-02 is satisfied: Todos 2 and 3 have explicit written disposition rationale committed to planning artifacts. Both pending todo files are moved to done/ with disposition notes. REQUIREMENTS.md v2 Deferred section has REQ-DENY-PREFLIGHT-01 and REQ-UNDO-TOCTOU-01 entries. The 3-pending-todos carry-forward is cleared of these two items.
</success_criteria>

<output>
Create `.planning/phases/53-release-drain/53-02-SUMMARY.md` when done.
</output>
