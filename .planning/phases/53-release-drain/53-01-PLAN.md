---
phase: 53-release-drain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/nono/Cargo.toml
  - crates/nono-cli/Cargo.toml
  - crates/nono-proxy/Cargo.toml
  - crates/nono-shell-broker/Cargo.toml
  - bindings/c/Cargo.toml
autonomous: true
requirements:
  - REQ-RLS-01
must_haves:
  truths:
    - "cargo metadata reports version 0.57.4 for all 5 crates"
    - "nono-cli, nono-proxy, nono-shell-broker, and nono-ffi all declare their internal path-dep version pins as 0.57.4"
    - "cargo build --workspace compiles cleanly with the bumped versions"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace package version anchor (not versioned directly here — but workspace.package.version is absent; all 5 crates carry their own version fields)"
    - path: "crates/nono/Cargo.toml"
      provides: "Core library at 0.57.4"
      contains: "version = \"0.57.4\""
    - path: "crates/nono-cli/Cargo.toml"
      provides: "CLI at 0.57.4 with path-dep pins also at 0.57.4"
      contains: "version = \"0.57.4\""
    - path: "crates/nono-proxy/Cargo.toml"
      provides: "Proxy at 0.57.4 with nono path-dep at 0.57.4"
      contains: "version = \"0.57.4\""
    - path: "crates/nono-shell-broker/Cargo.toml"
      provides: "Shell broker at 0.57.4 with nono path-dep at 0.57.4"
      contains: "version = \"0.57.4\""
    - path: "bindings/c/Cargo.toml"
      provides: "C FFI at 0.57.4 with nono path-dep at 0.57.4"
      contains: "version = \"0.57.4\""
  key_links:
    - from: "crates/nono-cli/Cargo.toml"
      to: "crates/nono/Cargo.toml"
      via: "nono = { version = \"0.57.4\", path = \"../nono\" }"
      pattern: "nono.*version.*0\\.57\\.4"
    - from: "crates/nono-cli/Cargo.toml"
      to: "crates/nono-proxy/Cargo.toml"
      via: "nono-proxy = { version = \"0.57.4\", path = \"../nono-proxy\" }"
      pattern: "nono-proxy.*version.*0\\.57\\.4"
    - from: "crates/nono-cli/Cargo.toml"
      to: "crates/nono-shell-broker/Cargo.toml"
      via: "nono-shell-broker = { path = \"../nono-shell-broker\", version = \"0.57.4\" }"
      pattern: "nono-shell-broker.*version.*0\\.57\\.4"
---

<objective>
Bump the workspace version from 0.57.3 to 0.57.4 across all 5 crate Cargo.toml files and their internal path-dep version pins. This is the D-53-05 version bump that makes the v2.8 release binaries unambiguously distinct from the stale local v0.57.3 MSIs (built at 3fbafac2, pre-005b4c9e).

Purpose: Every CI artifact, MSI filename, and `nono --version` output is keyed to RELEASE_TAG, which derives from the git tag. The version must read `0.57.4` before the tag is cut.

Output: 6 modified Cargo.toml files; `cargo build --workspace` passes; `cargo metadata` reports `0.57.4` for all 5 crates.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\OMack\Nono\.planning\PROJECT.md
@C:\Users\OMack\Nono\.planning\ROADMAP.md
@C:\Users\OMack\Nono\.planning\STATE.md
@C:\Users\OMack\Nono\.planning\phases\53-release-drain\53-CONTEXT.md

<interfaces>
<!-- Current version state extracted from crate Cargo.toml files -->

All 5 crates currently at version = "0.57.3":
- crates/nono/Cargo.toml:3 — version = "0.57.3"
- crates/nono-cli/Cargo.toml:3 — version = "0.57.3"
- crates/nono-proxy/Cargo.toml:3 — version = "0.57.3"
- crates/nono-shell-broker/Cargo.toml:3 — version = "0.57.3"
- bindings/c/Cargo.toml:3 — version = "0.57.3"

Internal path-dep pins in crates that import siblings (all at 0.57.3, all need bumping):
- crates/nono-cli/Cargo.toml:43 — nono = { version = "0.57.3", path = "../nono", ... }
- crates/nono-cli/Cargo.toml:44 — nono-proxy = { version = "0.57.3", path = "../nono-proxy", ... }
- crates/nono-cli/Cargo.toml:158 — nono-shell-broker = { path = "../nono-shell-broker", version = "0.57.3" }
- crates/nono-proxy/Cargo.toml:21 — nono = { version = "0.57.3", path = "../nono", ... }
- crates/nono-shell-broker/Cargo.toml:19 — nono = { version = "0.57.3", path = "../nono" }
- bindings/c/Cargo.toml:18 — nono = { version = "0.57.3", path = "../../crates/nono", ... }

The root Cargo.toml (workspace manifest) does NOT contain a `version` field — it is a pure workspace coordinator. No change needed there.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump all 5 crate version fields from 0.57.3 to 0.57.4</name>
  <files>
    crates/nono/Cargo.toml,
    crates/nono-cli/Cargo.toml,
    crates/nono-proxy/Cargo.toml,
    crates/nono-shell-broker/Cargo.toml,
    bindings/c/Cargo.toml
  </files>
  <read_first>
    Read all 5 files in full before touching any of them. The version field and path-dep pins
    must be seen in situ to avoid mis-editing.
    - crates/nono/Cargo.toml
    - crates/nono-cli/Cargo.toml
    - crates/nono-proxy/Cargo.toml
    - crates/nono-shell-broker/Cargo.toml
    - bindings/c/Cargo.toml
  </read_first>
  <action>
    Edit each file per D-53-05. The target version string is "0.57.4" throughout.

    Changes required in each file (use Edit tool, one file at a time):

    1. crates/nono/Cargo.toml: change `version = "0.57.3"` → `version = "0.57.4"`. No path-dep
       entries in this file (it imports workspace deps only, no sibling crates).

    2. crates/nono-proxy/Cargo.toml: change `version = "0.57.3"` → `version = "0.57.4"` (the
       crate's own version line). Change the path-dep pin `nono = { version = "0.57.3",
       path = "../nono", ... }` → bump to `"0.57.4"`.

    3. crates/nono-shell-broker/Cargo.toml: change `version = "0.57.3"` → `version = "0.57.4"`.
       Change `nono = { version = "0.57.3", path = "../nono" }` → `"0.57.4"`.

    4. bindings/c/Cargo.toml: change `version = "0.57.3"` → `version = "0.57.4"`. Change
       `nono = { version = "0.57.3", path = "../../crates/nono", ... }` → `"0.57.4"`.

    5. crates/nono-cli/Cargo.toml: change the crate's own `version = "0.57.3"` → `"0.57.4"`.
       Also bump all THREE internal path-dep pins:
         nono = { version = "0.57.3", path = "../nono", ... }         → "0.57.4"
         nono-proxy = { version = "0.57.3", path = "../nono-proxy", ... } → "0.57.4"
         nono-shell-broker = { path = "../nono-shell-broker", version = "0.57.3" } → "0.57.4"

    Do NOT change any external dependency versions (tokio, clap, etc.). Do NOT change the
    root Cargo.toml (workspace manifest has no version field). Do NOT change Cargo.lock
    manually — it will regenerate on next build.

    DCO sign-off on the commit: `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`
  </action>
  <verify>
    <automated>
      grep -r "version = " crates/nono/Cargo.toml crates/nono-cli/Cargo.toml crates/nono-proxy/Cargo.toml crates/nono-shell-broker/Cargo.toml bindings/c/Cargo.toml | grep "0\.57\.3"
    </automated>
    Expected: zero lines output. Any line containing "0.57.3" is a missed bump.

    Also run:
    <automated>cargo metadata --no-deps --format-version 1 | python -c "import sys,json; d=json.load(sys.stdin); bad=[p['name']+':'+p['version'] for p in d['packages'] if '0.57.3' in p['version']]; print(bad)"</automated>
    Expected: empty list [].

    Then verify the workspace compiles:
    <automated>cargo check --workspace</automated>
    Expected: no errors (warnings acceptable — this is a version-string-only change).
  </verify>
  <acceptance_criteria>
    1. grep for "0.57.3" across the 5 files returns zero matches.
    2. grep for "version = \"0.57.4\"" returns exactly 5 matches (one per file).
    3. All 6 path-dep version pins (nono in nono-proxy, nono in nono-shell-broker, nono in
       bindings/c, nono in nono-cli, nono-proxy in nono-cli, nono-shell-broker in nono-cli)
       read "0.57.4".
    4. `cargo check --workspace` exits 0.
    5. Commit includes DCO sign-off line `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`.
  </acceptance_criteria>
  <done>All 5 crate Cargo.toml files read version "0.57.4" and all internal path-dep version pins also read "0.57.4"; cargo check --workspace passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cargo.toml edit → cargo resolver | Version pin changes must satisfy semver compatibility; incorrect pins cause resolver failures at CI build time |
| cargo lock regeneration | Cargo.lock update on next build; no manual edit permitted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01-01 | Tampering | Cargo.toml version pins | mitigate | Edit each file explicitly; grep-verify no "0.57.3" remains; `cargo check` confirms resolver acceptance |
| T-53-01-02 | Denial | Missed path-dep pin leaves 0.57.3 pin on a bumped crate | mitigate | Explicit grep gate in verify step catches any remaining 0.57.3 token before commit |
| T-53-01-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; version-string-only edits |
</threat_model>

<verification>
After both tasks complete, confirm:
- `grep -r "0\.57\.3" crates/*/Cargo.toml bindings/c/Cargo.toml` returns zero lines
- `cargo check --workspace` exits 0
</verification>

<success_criteria>
All 5 crate Cargo.toml files declare `version = "0.57.4"` and all 6 internal path-dep version pins also reference "0.57.4". `cargo check --workspace` compiles cleanly. The version bump commit is signed with DCO.
</success_criteria>

<output>
Create `.planning/phases/53-release-drain/53-01-SUMMARY.md` when done.
</output>
