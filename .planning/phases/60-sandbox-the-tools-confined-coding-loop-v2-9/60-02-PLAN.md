---
phase: "60"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/claude-code/CLAUDE.md
autonomous: true
requirements:
  - REQ-STW-02

must_haves:
  truths:
    - "packages/claude-code/CLAUDE.md contains a ## Windows Shell Syntax section instructing Claude to use PowerShell syntax for Bash commands"
    - "The CLAUDE.md section instructs Claude to retry Write/Edit tool denials using the Bash tool with the PowerShell command from the denial context"
    - "The CLAUDE.md section includes a tool-support matrix (what works / what is denied) so POC users have correct expectations"
    - "packages/claude-code/claude-code-tools-windows-runner.profile.json has workdir.access==readwrite and network.block==true — no changes needed for confined file ops"
    - "The cross-target clippy requirement for claude_code_hook.rs is documented as PARTIAL (deferred to CI) in the phase summary"
    - "D-02: the Windows shell story is a PowerShell tool runner — Claude is steered to emit PowerShell syntax via the CLAUDE.md note; no confined real-bash path is attempted for the POC"
    - "D-03: Medium-IL agent reads (Read/Glob/Grep) remain unconfined and this accepted posture is documented in the tool-support matrix"
    - "D-04: unwrappable tools (WebFetch/WebSearch/Task/mcp__*) keep a fail-closed deny with a clear reason, and the tool-support matrix sets POC-user expectations"
  artifacts:
    - path: "packages/claude-code/CLAUDE.md"
      provides: "PowerShell steering note + tool-support matrix for POC users"
      contains: "re-issue"
    - path: "packages/claude-code/claude-code-tools-windows-runner.profile.json"
      provides: "Runner profile verification — no changes needed"
      contains: "readwrite"
  key_links:
    - from: "CLAUDE.md ## Windows Shell Syntax section"
      to: "Claude agent session context"
      via: "loaded as project instructions at session start (confirmed model-visible per RESEARCH.md Pattern 3)"
      pattern: "Windows Shell Syntax"
    - from: "CLAUDE.md retry instruction for Write/Edit denials"
      to: "deny+additionalContext mechanism from Plan 60-01"
      via: "Claude reads the denial context and re-issues as Bash tool call"
      pattern: "Bash tool"
---

<objective>
Wire the PowerShell-syntax steering note into the model-visible instruction surface (CLAUDE.md)
so Claude automatically uses PowerShell syntax for Bash commands on Windows — removing the manual
"use PowerShell syntax" prompting that the PR #4 UAT required. Also ship the "what works / what's
denied" tool-support matrix (D-04) and verify the runner profile needs no changes for the file-op
confinement path.

Purpose: Closes REQ-STW-02 (usable shell story). A POC user can now start a session with the
experimental profile and receive correct PS-syntax shell commands and file-edit routing without
manually instructing Claude about the platform.

Output: Updated packages/claude-code/CLAUDE.md with a ## Windows Shell Syntax section, a tool-support
matrix, and a retry instruction. Verified (no change needed) claude-code-tools-windows-runner.profile.json.
Cross-target clippy PARTIAL note documented for plan summary.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\OMack\nono\.planning\ROADMAP.md
@C:\Users\OMack\nono\.planning\phases\60-sandbox-the-tools-confined-coding-loop-v2-9\60-CONTEXT.md
@C:\Users\OMack\nono\.planning\phases\60-sandbox-the-tools-confined-coding-loop-v2-9\60-RESEARCH.md
@C:\Users\OMack\nono\.planning\phases\60-sandbox-the-tools-confined-coding-loop-v2-9\60-PATTERNS.md

<interfaces>
<!-- Extracted from packages/claude-code/CLAUDE.md (current state, commit 7488dbba) -->
<!-- 28 lines total; append AFTER line 28 -->

Current sections and their heading levels:
  ## Constraints            (line 3)
  ## When an operation is denied   (line 12)
  ## Working directory      (line 23)  ← append new section after this

Current file ends at line 28 (## Working directory body).
New section appends at EOF using ## heading (match existing heading level).

<!-- Extracted from packages/claude-code/claude-code-tools-windows-runner.profile.json -->
<!-- 30 lines; read before deciding no change is needed -->

Relevant fields:
  "workdir": { "access": "readwrite" }   ← CWD r+w grant (D-01 scope — already correct)
  "network": { "block": true }           ← network blocked (already correct)
  "policy": { "add_deny_access": ["$HOME/.claude", "$WORKDIR/.claude"] }  ← macOS-effective; Windows no-op per D-05
  "extends": "default"                   ← picks up default security groups
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Windows Shell Syntax section and tool-support matrix to CLAUDE.md</name>
  <files>packages/claude-code/CLAUDE.md</files>

  <read_first>
    1. packages/claude-code/CLAUDE.md — read the full file (28 lines) before editing
    2. .planning/phases/60-sandbox-the-tools-confined-coding-loop-v2-9/60-PATTERNS.md — the CLAUDE.md pattern section (analog: existing ## Constraints + ## When an operation is denied heading/body style)
    3. .planning/phases/60-sandbox-the-tools-confined-coding-loop-v2-9/60-RESEARCH.md — Pattern 3 (PowerShell-Steering Wiring) and Pattern 4 (Tool-Support Matrix)
  </read_first>

  <action>
  Read the full CLAUDE.md file first.

  Append a new ## Windows Shell Syntax section after the ## Working directory section (at EOF).
  Match the heading level (##) and body style (plain prose + bullet list) of the existing sections.

  The section must contain three parts:

  PART A — Shell syntax steering (per D-02):
  On Windows, the Bash tool is backed by native PowerShell (not Git Bash or MSYS2).
  Always use PowerShell syntax for Bash tool commands:
    - List files: Get-ChildItem not ls
    - Read file: Get-Content not cat
    - Check a path exists: Test-Path not [ -f ... ]
    - Copy: Copy-Item, move: Move-Item, delete: Remove-Item
    - Write to a file: use the Bash tool with Set-Content (see "File edits" below)

  PART B — File edit routing (per RESEARCH.md Pattern 1 and the deny+additionalContext mechanism
  from Plan 60-01):
  If the Write or Edit tool call is blocked (you receive a denial with a PowerShell command
  in the denial context), re-issue the operation as a Bash tool call using the PowerShell
  command provided. This routes the file write through the Low-IL confined shell instead of
  executing it in-process at Medium IL.

  Do NOT retry Write/Edit directly after a denial. Do NOT use alternative paths or workarounds.
  Use the Bash tool with the exact PowerShell command from the denial context.

  PART C — Tool-support matrix (per D-04):
  What works in this sandbox:
    | Tool | Status | Notes |
    |------|--------|-------|
    | Read, Glob, Grep | Allowed | Unconfined reads; D-03 accepted posture |
    | Bash | Confined (Low-IL) | PowerShell-backed; use PS syntax |
    | Write, Edit, MultiEdit | Confined (Low-IL via Bash retry) | Hook converts to Bash+PS; files inside CWD only |
    | NotebookEdit | Denied | Not yet supported in the Windows POC |
    | WebFetch, WebSearch | Denied | Network is blocked |
    | Task | Denied | Subagent spawn is unconfined |
    | mcp__* | Denied | MCP runs at Medium-IL outside the confinement boundary |

  FORMATTING: Match the ## heading level and blank-line style of the existing sections in the file.
  Do not add a trailing newline beyond what the existing file uses.

  DO NOT change any existing content in the file — append only.
  </action>

  <verify>
    <automated>grep -c "Windows Shell Syntax" packages/claude-code/CLAUDE.md && grep -c "re-issue" packages/claude-code/CLAUDE.md</automated>
  </verify>

  <acceptance_criteria>
    - packages/claude-code/CLAUDE.md contains exactly one ## Windows Shell Syntax section
    - The section contains PowerShell syntax examples for common shell commands (Get-ChildItem, Get-Content)
    - The section contains an instruction to retry Write/Edit denials as Bash tool calls using the PS command from the denial context
    - The section contains the tool-support matrix (all 7 rows: Read/Glob/Grep, Bash, Write/Edit/MultiEdit, NotebookEdit, WebFetch/WebSearch, Task, mcp__*)
    - No existing content in CLAUDE.md was changed
    - grep "Windows Shell Syntax" packages/claude-code/CLAUDE.md returns at least 1 match
    - grep "PowerShell" packages/claude-code/CLAUDE.md returns at least 3 matches (steering note + matrix entries)
    - Commit includes DCO sign-off: Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>
  </acceptance_criteria>

  <done>
    CLAUDE.md has a ## Windows Shell Syntax section with PS steering, file-edit retry instruction,
    and tool-support matrix. grep confirms presence. Existing content unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify runner profile + document cross-target clippy PARTIAL</name>
  <files>packages/claude-code/claude-code-tools-windows-runner.profile.json</files>

  <read_first>
    1. packages/claude-code/claude-code-tools-windows-runner.profile.json — read the full file (30 lines)
    2. .planning/phases/60-sandbox-the-tools-confined-coding-loop-v2-9/60-PATTERNS.md — the runner profile section (verification-only: no changes expected)
    3. .planning/templates/cross-target-verify-checklist.md — cross-target PARTIAL procedure
    4. .planning/phases/60-sandbox-the-tools-confined-coding-loop-v2-9/60-RESEARCH.md — Standard Stack section (confirms no new packages; Crates Extended table shows runner profile as "Verify no changes needed")
  </read_first>

  <action>
  STEP 1 — Read the runner profile JSON and verify it already has:
    - "workdir": { "access": "readwrite" } — CWD r+w grant (required for Write/Edit confinement per D-01)
    - "network": { "block": true } — network blocked (already correct)
    - "extends": "default" — picks up security groups
  If any of these fields is absent or incorrect, fix it. If all are correct, make no change.

  STEP 2 — Document the cross-target clippy status in a comment block in the task output
  (not a file — include this in the plan summary):
  "claude_code_hook.rs contains #[cfg(target_os = \"windows\")] gated code added in Plan 60-01.
  Per CLAUDE.md §Coding Standards, cross-target clippy is required:
    cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used
    cargo clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used
  Status: PARTIAL — Windows host cannot run these commands. Deferred to CI (linux runner).
  The non-Windows path in claude_code_hook.rs returns deny_response without any PS command
  construction, so Unix branches are simple and unlikely to have lint issues, but CI is the
  authoritative verification."

  STEP 3 — Run the Windows-host clippy to catch any local lint issues:
    cargo clippy --package nono-cli -- -D warnings -D clippy::unwrap_used
  This exercises the Windows-cfg branches. The non-Windows branches are untested on this host.

  STEP 4 — Run the full test suite one final time to confirm the phase gate:
    make test
  or if make is unavailable on this host:
    cargo test --workspace

  Report the result in the plan summary. If make test exits 0, record as green.
  </action>

  <verify>
    <automated>cargo clippy --package nono-cli -- -D warnings -D clippy::unwrap_used && cargo test --package nono-cli --bin nono</automated>
  </verify>

  <acceptance_criteria>
    - packages/claude-code/claude-code-tools-windows-runner.profile.json has workdir.access == "readwrite" (confirmed, no change needed)
    - packages/claude-code/claude-code-tools-windows-runner.profile.json has network.block == true (confirmed)
    - cargo clippy --package nono-cli -- -D warnings -D clippy::unwrap_used exits 0
    - cargo test --package nono-cli --bin nono exits 0
    - Plan summary documents the cross-target clippy PARTIAL status with explicit statement that CI (linux runner) is required for x86_64-unknown-linux-gnu and x86_64-apple-darwin targets
    - If the runner profile required a change, the change is committed with DCO sign-off; if no change was needed, that is documented in the summary
    - Commit (or no-change note) includes DCO sign-off: Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>
  </acceptance_criteria>

  <done>
    Runner profile verified (no change needed or change committed). Clippy clean on Windows host.
    Full test suite green. Cross-target PARTIAL documented in summary.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE.md → Claude agent context | The steering note is model-visible; a prompt injection that overrides CLAUDE.md could suppress the PowerShell steering. This is an inherent limitation of instruction-based steering, not a structural confinement flaw. The hook handler is the structural enforcement point. |
| Runner profile → nono capability grant | The profile defines what the Low-IL PowerShell child can access; any misconfiguration (too-broad workdir grant, missing network block) is a confinement leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-08 | Tampering | CLAUDE.md steering note — prompt injection suppressing PS steering | accept | The CLAUDE.md note is a UX aid, not a security control. The hook handler enforces confinement structurally (deny+additionalContext); a suppressed steering note does not bypass confinement. Accepted: defense-in-depth framing documented in ROADMAP.md Phase 60 SC-3. |
| T-60-09 | Elevation of Privilege | Runner profile misconfiguration (missing network block or over-broad workdir) | mitigate | Verify network.block==true and workdir.access==readwrite in Task 2 before committing. Any deviation from these values is a confinement breach; fail verification if absent. |
| T-60-10 | Information Disclosure | Tool-support matrix in CLAUDE.md reveals denied tool list | accept | Disclosure is intentional — D-04 says ship a clear "what works / what's denied" matrix. Knowing which tools are denied does not help bypass confinement because the hook handler is structural. |
| T-60-SC | Tampering | npm/pip/cargo installs | accept | No new external packages in this plan. No package-manager install tasks. |
</threat_model>

<verification>
## Phase Gate (run before /gsd:verify-work)

1. grep "Windows Shell Syntax" packages/claude-code/CLAUDE.md — must return 1 match
2. grep -c "PowerShell" packages/claude-code/CLAUDE.md — must return >= 3
2b. grep "re-issue" packages/claude-code/CLAUDE.md — must return at least 1 match (retry instruction present)
3. grep "readwrite" packages/claude-code/claude-code-tools-windows-runner.profile.json — must return 1 match (workdir field)
4. grep "\"block\": true" packages/claude-code/claude-code-tools-windows-runner.profile.json — must return 1 match (network field)
5. cargo clippy --package nono-cli -- -D warnings -D clippy::unwrap_used — must exit 0
6. cargo test --package nono-cli --bin nono — must exit 0

HUMAN-UAT (from VALIDATION.md — Manual-Only; requires real Win11 host + Claude Code TUI):
a. Start Claude Code with the experimental profile from a project directory (not ~/.claude)
b. Ask Claude to "list the files in the current directory" WITHOUT mentioning PowerShell
   Expected: Claude uses Get-ChildItem or dir, not ls
c. Ask Claude to write "hello world" to test.txt
   Expected: Claude's Write tool call is denied; Claude automatically retries as a Bash tool call
   with a PowerShell Set-Content command; test.txt is created with content "hello world"
d. Verify test.txt is inside the project CWD (not e.g. ~/.claude/)
e. Ask Claude to write to a path outside the CWD (e.g. ../outside.txt)
   Expected: denied at the OS boundary (Low-IL mandatory label); clear denial message
f. Self-disable guard: start Claude from ~/.claude (or a dir with .claude/ child)
   Expected: all file tools and Bash are denied with the self-disable reason; no PS command is offered

Note per RESEARCH.md Open Question A1: if deny+additionalContext does NOT cause Claude to retry
as Bash in step (c), document the fallback approach (direct nono subprocess from hook handler)
and file a follow-on todo before closing the phase.
</verification>

<success_criteria>
1. packages/claude-code/CLAUDE.md has a ## Windows Shell Syntax section with PS steering note,
   file-edit retry instruction, and tool-support matrix (D-02, D-04 — per CONTEXT.md)
2. POC user running the experimental profile receives PS-syntax shell commands without manually
   prompting "use PowerShell syntax" (HUMAN-UAT step b above)
3. POC user's Write tool call is automatically retried as Bash+PS (HUMAN-UAT step c above)
   — or the fallback mechanism is documented if deny+retry is unreliable (A1 assumption)
4. Runner profile verified: workdir.access==readwrite, network.block==true — no confinement
   gap introduced by this plan
5. cargo clippy --package nono-cli -- -D warnings -D clippy::unwrap_used exits 0
6. Cross-target clippy: PARTIAL (deferred to CI; documented in plan summary)
7. All commits include DCO Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>
</success_criteria>

<output>
Create .planning/phases/60-sandbox-the-tools-confined-coding-loop-v2-9/60-02-SUMMARY.md when done.
</output>
