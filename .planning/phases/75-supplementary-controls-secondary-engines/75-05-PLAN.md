---
phase: 75-supplementary-controls-secondary-engines
plan: 05
type: execute
wave: 3
depends_on:
  - 75-01
  - 75-02
  - 75-03
  - 75-04
files_modified:
  - .planning/phases/75-supplementary-controls-secondary-engines/75-HUMAN-UAT.md
autonomous: false
requirements:
  - SUPP-01
  - SUPP-02
  - SUPP-03

must_haves:
  truths:
    - "GitHub Copilot CLI (copilot.exe) is confined end-to-end on a real Win11 host via the daemon launch path (SC3)"
    - "nono-ts confinedRun confines a Node/JS process on real Win11 with write denial outside workspace (SC5)"
    - "Per-agent WFP isolation is verified: two concurrent agents with different allowed domains, no cross-bleed (A1 gate)"
    - "nono agent demote <tenant_id> drops IL on a live agent without reaping it (SC1 supplement)"
    - "The abstraction is proven across 2 engines (Aider + Copilot CLI) and 2 bindings (nono-py + nono-ts)"
    - "D-08: the SC3 (Copilot) and SC5 (nono-ts) acceptance bars are live Win11 UAT gates (confined end-to-end on a real host), not build-green-only"
  artifacts:
    - path: ".planning/phases/75-supplementary-controls-secondary-engines/75-HUMAN-UAT.md"
      provides: "UAT report with SC1-SC5 go/no-go verdicts"
      contains: "SC3"
  key_links:
    - from: "SC3 gate"
      to: "copilot-cli profile + Phase 71 engine-neutral launch"
      via: "nono agent launch --profile copilot-cli -- copilot ask"
      pattern: "copilot-cli"
    - from: "SC5 gate"
      to: "nono-ts confinedRun"
      via: "node script calling require('nono').confinedRun(...)"
      pattern: "confinedRun"
---

<objective>
Live Win11 UAT: verify SUPP-01 demote, SUPP-02 per-agent WFP isolation, SC3 Copilot CLI end-to-end, and SC5 nono-ts confined-run proof.

Purpose: Build-green-only is insufficient (D-08). This plan produces the 75-HUMAN-UAT.md report that gates the phase close. It runs after all four Wave 1 plans are complete and verifies the observable behaviors the phase was built to deliver.

Output: 75-HUMAN-UAT.md with SC1-SC5 go/no-go verdicts, any A2/A4 assumption confirmations (copilot.exe install path, node.exe grandchild presence), and the A1 per-agent isolation result.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\OMack\Nono\.planning\PROJECT.md
@C:\Users\OMack\Nono\.planning\ROADMAP.md
@C:\Users\OMack\Nono\.planning\STATE.md
@C:\Users\OMack\Nono\.planning\phases\75-supplementary-controls-secondary-engines\75-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full CI sweep + 75-HUMAN-UAT.md scaffold</name>
  <files>.planning/phases/75-supplementary-controls-secondary-engines/75-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/74-persistent-multi-tenant-daemon/74-HUMAN-UAT.md (if it exists — use as the UAT report template; understand the SC1-SC5 go/no-go format, the evidence format, and the PASS/FAIL verdict rows)
    - .planning/phases/75-supplementary-controls-secondary-engines/75-RESEARCH.md §"Validation Architecture" (per-requirement test map; manual-only UAT gates; A1/A2/A4 assumptions to verify)
    - .planning/phases/75-supplementary-controls-secondary-engines/75-01-SUMMARY.md (confirm SUPP-02 unit tests green)
    - .planning/phases/75-supplementary-controls-secondary-engines/75-02-SUMMARY.md (confirm SUPP-01 unit tests green)
    - .planning/phases/75-supplementary-controls-secondary-engines/75-03-SUMMARY.md (confirm copilot-cli profile present)
    - .planning/phases/75-supplementary-controls-secondary-engines/75-04-SUMMARY.md (confirm nono-ts build + tests green)
  </read_first>
  <action>
    Run `make ci` (nono workspace) — must be green (clippy + fmt + tests). Run `cargo test` in ../nono-ts/ — must be green.

    Create 75-HUMAN-UAT.md in the phase directory. Structure it as: header + pre-flight (CI sweep results) + SC1-SC5 table + per-SC verification instructions + A1/A2/A4 assumption confirmations + verdict. The pre-flight section records the make ci result and nono-ts test result. The SC table has columns: SC | Requirement | Behavior | Evidence | Verdict.

    Pre-populate all rows as PENDING. The checkpoint task (Task 2) fills in verdicts after the operator runs the UAT steps.

    Include SC5 operator instructions for the nono-ts test script: create a small JS file that requires the nono-ts build output and calls confinedRun with a node executable; run it on Win11. The script should attempt to write outside the allowed workspace and expect denial.
  </action>
  <verify>
    <automated>make ci 2>&1 | tail -30 && cd /c/Users/OMack/nono-ts && cargo test 2>&1 | tail -15</automated>
  </verify>
  <done>
    make ci passes. nono-ts tests pass. 75-HUMAN-UAT.md exists with all SC rows pre-populated as PENDING and clear operator instructions for each gate.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All Phase 75 Wave 1 implementations are complete and CI-green:
    - SUPP-02 (plan 75-01): per-agent WFP filter at launch/reap, D-05 fail-secure gate
    - SUPP-01 (plan 75-02): nono agent demote with IL-drop + WFP-cut, leak limits documented
    - SUPP-03a (plan 75-03): copilot-cli engine profile in policy.json (native PE shape)
    - SUPP-03b (plan 75-04): nono-ts confinedRun/confine (Shape A+B), nono pin 0.62, cross-target clippy
  </what-built>
  <how-to-verify>
    Run the following verification gates on a real Win11 host (build 26200+). Record each result in 75-HUMAN-UAT.md.

    **Pre-flight (must all pass before SC gates):**
    - `make ci` in the nono workspace → all green
    - `cargo test` in `../nono-ts/` → all green
    - `nono-wfp-service` is installed and running (`sc query nono-wfp-service` → STATE: 4 RUNNING)
    - `nono-agentd` daemon is running (`nono daemon status` → reports running)

    **SC1 — Demote does not reap:**
    1. `nono agent launch --profile aider --workspace C:\poc\workspace -- aider --no-auto-commits` (start a long-running agent)
    2. `nono agent list` → copy the tenant_id
    3. `nono agent demote <tenant_id>` → expect success message (not "reaping" or "terminated")
    4. `nono agent list` again → the agent is still listed (not reaped)
    5. Verify the IL-drop took effect: the confined agent's token integrity level should now be Low (use Process Hacker or `Get-Process | Where-Object {$_.Name -eq "aider"}` → check token IL)
    Expected: demote succeeds, agent still in list (not reaped), agent IL is now Low.

    **SC2 — Per-agent WFP isolation (A1 gate):**
    1. Launch two agents with different profiles:
       - Agent A: profile with allowed domain api.openai.com (or a test endpoint)
       - Agent B: profile with allowed domain api.anthropic.com (different endpoint)
    2. From inside Agent A's confined process, attempt to reach Agent B's allowed domain → expect BLOCKED
    3. From inside Agent B's confined process, attempt to reach Agent A's allowed domain → expect BLOCKED
    4. Each agent can reach only its own allowed domain.
    Note: if the existing WFP service test infrastructure from Phase 62 can be reused, use it. Otherwise a curl/Invoke-WebRequest from inside the confined process is sufficient.
    Expected: per-agent isolation confirmed; one agent's allowed domain does not leak to another. Record A1 assumption result (FWPM_CONDITION_ALE_USER_ID + AppContainer SID: does it correctly filter?).

    **SC3 — Copilot CLI confined end-to-end:**
    1. Install Copilot CLI: `winget install GitHub.Copilot` (or npm install @github/copilot) on the test host
    2. `where copilot` → record the actual install path (A2 assumption check)
    3. `nono agent launch --profile copilot-cli --workspace C:\poc\copilot-workspace -- copilot ask "What is 2+2?"` (or equivalent Copilot CLI invocation)
    4. Confirm the command runs confined (AppContainer + Job via the daemon's broker arm)
    5. Attempt a write outside the workspace from within copilot → expect denial
    6. Monitor child processes: does copilot.exe spawn node.exe as a grandchild? (A4 assumption check — use Process Monitor or `Get-CimInstance Win32_Process | Where-Object {$_.ParentProcessId -eq <copilot-pid>}`)
    Expected: copilot.exe runs confined end-to-end. Write outside workspace is denied. Record A2 (actual install path) and A4 (node.exe grandchild: yes/no → if yes, add windows_interpreters to the copilot-cli profile).

    **SC4 — Daemon privilege split (carry-forward from Phase 74 SC4):**
    - Already verified in Phase 74 UAT. Confirm daemon is still running as the current user (not SYSTEM). `sc qc nono-agentd` → verify account.

    **SC5 — nono-ts confinedRun on Win11:**
    1. Build nono-ts: `npm run build` in `../nono-ts/` (uses napi-rs; produces .node file)
    2. Create a test script (e.g., `test-confined.js`):
       ```
       const { confinedRun } = require('./index.js');
       const result = confinedRun(
         'node.exe',
         ['-e', 'require("fs").writeFileSync("C:\\\\outside\\\\test.txt", "pwned")'],
         ['C:\\poc\\ts-workspace'],
         undefined, 'C:\\poc\\ts-workspace', 30
       );
       console.log('exit_code:', result.exitCode);
       ```
    3. `node test-confined.js` → expect exit_code non-zero (write outside workspace denied by AppContainer)
    4. Repeat with an allowed write (within C:\poc\ts-workspace) → expect exit_code 0
    Expected: confinedRun enforces workspace boundary on Win11 via the daemon broker arm. Record the proof.

    **Abstraction proof (SC5 final):** After SC1-SC5 all pass, the abstraction is proven across:
    - ≥2 engines: Aider (Phase 71 SC1) + Copilot CLI (SC3 above)
    - ≥2 bindings: nono-py (Phase 72) + nono-ts (SC5 above)
  </how-to-verify>
  <resume-signal>
    Fill in 75-HUMAN-UAT.md with verdicts (PASS/FAIL/PARTIAL) for SC1-SC5 and the A1/A2/A4 assumption results.
    Then type "approved" to close Phase 75, or describe any failures for gap closure.

    If A4 finds node.exe grandchild: add "windows_interpreters": ["node.exe"] to the copilot-cli profile in policy.json (update plan 75-03 result) before typing "approved".
    If A1 finds FWPM_CONDITION_ALE_USER_ID does NOT match AppContainer SIDs: update wfp_filter_add to use FWPM_CONDITION_ALE_PACKAGE_ID path in the nono-wfp-service (gap-closure plan 75-06) before typing "approved".
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT operator → live Win11 host | The UAT runs under the interactive Medium-IL session. All daemon/WFP operations are scoped to the current user. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-05-01 | Information Disclosure | SC3 UAT: copilot.exe phone-home during confinement | accept | copilot.exe is allowed network in the profile (network.block:false). The UAT tests confinement boundaries, not network filtering. Per-agent WFP (SUPP-02) provides runtime network scoping once configured. |
| T-75-05-02 | Denial of Service | nono-wfp-service not running blocks SC2 UAT | accept | D-05 fail-secure: if WFP service absent and profile network-scoped, launch is refused with actionable message. The operator must ensure the service is running before the UAT. Pre-flight check included in instructions. |
| T-75-05-SC | Tampering | npm/pip/cargo installs | mitigate | SC5 uses the nono-ts build already done in plan 75-04. No new packages installed during UAT. |
</threat_model>

<verification>
This plan IS the verification. Success is:
- 75-HUMAN-UAT.md exists with SC1-SC5 verdicts recorded
- SC3 (Copilot CLI) PASS
- SC5 (nono-ts confinedRun) PASS
- A1 (per-agent WFP isolation) confirmed or gap-closure plan created
- A2 (copilot.exe install path) recorded (for profile documentation)
- A4 (node.exe grandchild) recorded + copilot-cli profile updated if YES
- Operator types "approved"
</verification>

<success_criteria>
SC1: nono agent demote drops IL, does not reap — PASS
SC2: per-agent WFP isolation confirmed across two concurrent agents — PASS (or gap-closure plan for A1)
SC3: copilot.exe confined end-to-end on real Win11 — PASS
SC4: daemon runs as current user (carry-forward from Phase 74) — PASS
SC5: nono-ts confinedRun enforces workspace boundary on Win11 — PASS
A2 install path recorded in UAT report
A4 node.exe grandchild answered (profile updated if YES)
Phase 75 milestone SUPP-01/02/03 all complete
</success_criteria>

<output>
Create `.planning/phases/75-supplementary-controls-secondary-engines/75-05-SUMMARY.md` when done.
Commit 75-HUMAN-UAT.md with UAT results.
</output>
