---
phase: 75-supplementary-controls-secondary-engines
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono-cli/data/policy.json
autonomous: true
requirements:
  - SUPP-03

must_haves:
  truths:
    - "policy.json contains a copilot-cli engine profile with windows_low_il_broker: true and no windows_interpreters field (native PE profile)"
    - "The copilot-cli profile has a meta section, security groups, signal_mode: isolated, and workdir readwrite access"
    - "A policy parse unit test confirms copilot-cli profile is present and has the correct native-PE shape"
  artifacts:
    - path: "crates/nono-cli/data/policy.json"
      provides: "copilot-cli engine profile (native PE, windows_low_il_broker: true)"
      contains: "copilot-cli"
  key_links:
    - from: "crates/nono-cli/data/policy.json::copilot-cli"
      to: "Phase 71 engine-neutral launch path (BrokerLaunchNoPty arm)"
      via: "nono agent launch --profile copilot-cli -- copilot [args]"
      pattern: "copilot-cli"
---

<objective>
Add the GitHub Copilot CLI engine profile to `policy.json` (SUPP-03a) and add a unit test confirming its presence and shape.

Purpose: GitHub Copilot CLI is a native PE executable (`copilot.exe`) — NOT a node.exe-wrapped npm script (D-06 research finding). Its profile follows the `node-dev` bare-exe pattern (no `windows_interpreters` field) combined with `windows_low_il_broker: true` from the aider profile. This enables Phase 71's engine-neutral launch path to confine `copilot.exe` end-to-end.

Output: `"copilot-cli"` profile entry in `policy.json`; `copilot_cli_profile_present` unit test in `nono-cli` policy tests; operator-visible SC3 UAT instructions embedded in the plan.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\OMack\Nono\.planning\PROJECT.md
@C:\Users\OMack\Nono\.planning\ROADMAP.md
@C:\Users\OMack\Nono\.planning\STATE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from 75-PATTERNS.md and 75-RESEARCH.md. -->

From crates/nono-cli/data/policy.json — aider profile (exact template per 75-PATTERNS.md lines 36-52):
```json
"aider": {
  "extends": "default",
  "meta": { "name": "aider", "version": "1.0.0",
    "description": "Aider AI pair-programming engine (Python entry point)",
    "author": "nono-project" },
  "security": { "groups": ["python_runtime","git_config","unlink_protection"], "signal_mode": "isolated" },
  "filesystem": {},
  "network": { "block": false },
  "workdir": { "access": "readwrite" },
  "windows_low_il_broker": true,
  "windows_interpreters": ["python.exe"]
}
```

Target copilot-cli profile (native PE, from 75-PATTERNS.md lines 79-95):
```json
"copilot-cli": {
  "extends": "default",
  "meta": { "name": "copilot-cli", "version": "1.0.0",
    "description": "GitHub Copilot CLI (copilot.exe native PE engine — v1.0.62+)",
    "author": "nono-project" },
  "security": { "groups": [], "signal_mode": "isolated" },
  "filesystem": {},
  "network": { "block": false },
  "workdir": { "access": "readwrite" },
  "windows_low_il_broker": true
}
```
No "windows_interpreters" field — copilot.exe is a native PE, not a node.exe wrapper (D-06 finding).
If SC3 UAT reveals copilot.exe spawns node.exe as a grandchild (pitfall 5), add "windows_interpreters": ["node.exe"] at that point.

D-06 installation note (A2 — verify on test host):
  MSI install path: %LOCALAPPDATA%\Programs\GitHub Copilot\copilot.exe
  npm global path: %AppData%\npm\copilot.exe (the npm shim calls the native binary)
  WinGet: winget install GitHub.Copilot
  Confirm via: where copilot

Policy parse test pattern: look at existing profile parse tests in nono-cli (grep for "profile" or "policy" in tests/) to understand how to load the embedded policy.json and assert profile presence.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add copilot-cli profile to policy.json + unit test</name>
  <files>crates/nono-cli/data/policy.json</files>
  <read_first>
    - crates/nono-cli/data/policy.json lines ~880-960 (aider profile ~line 884; langchain-python profile; node-dev profile ~line 938 — these are the exact shape templates; find the JSON array/object structure to insert copilot-cli in the correct position alphabetically or adjacent to the other engine profiles)
    - crates/nono-cli/src/policy.rs (or wherever policy.json is loaded and profiles are parsed — understand the Profile or EngineProfile struct to write a correct parse test; find how to load embedded policy in tests: look for include_str! or the embedded module)
    - crates/nono-cli/src/config/embedded.rs (likely where policy.json is embedded at build time — find the constant or function that returns the raw JSON bytes)
    - .planning/phases/75-supplementary-controls-secondary-engines/75-PATTERNS.md §"policy.json — copilot-cli engine profile" (full profile JSON shape + note about groups: [] placeholder + pitfall 5 re: node.exe grandchild)
    - .planning/phases/75-supplementary-controls-secondary-engines/75-RESEARCH.md §"D-06 Resolution" (native PE finding; install paths; Pitfall 5 monitoring instruction for SC3 UAT)
  </read_first>
  <behavior>
    - Test copilot_cli_profile_present: load the embedded policy.json (via the same mechanism existing policy tests use — likely PolicyResolver::load_embedded() or similar); assert the "copilot-cli" profile key exists; assert windows_low_il_broker = true; assert no windows_interpreters field (or the field is absent / empty); assert signal_mode = "isolated"
    - Test copilot_cli_profile_is_native_pe: specifically assert windows_interpreters is absent or empty in the copilot-cli profile (distinguishes from aider/langchain-python which have python.exe)
  </behavior>
  <action>
    Add "copilot-cli" to the engine profiles section of policy.json. Insert it adjacent to the other engine profiles (aider, langchain-python, node-dev) in alphabetical or logical order. The exact JSON shape is documented in the interfaces section above. Key requirements:
    - "windows_low_il_broker": true (required — this is the D-06 native-PE broker arm)
    - NO "windows_interpreters" field (copilot.exe is a native PE per D-06 research)
    - "security": { "groups": [], "signal_mode": "isolated" } — groups is intentionally empty; add "node_runtime" only if SC3 UAT (plan 75-05) finds that copilot.exe requires node.exe coverage
    - "network": { "block": false } — allows network access by default (copilot.exe needs GitHub API); operator can override with --allow when invoking
    - "workdir": { "access": "readwrite" } — grants workspace per the --workspace flag from Phase 71

    The description field MUST note it is a native PE (not a node wrapper) and the approximate version tested (v1.0.62+). This helps operators understand why windows_interpreters is absent.

    Add a note comment (JSON does not support comments — put it in the description field): "Native PE (copilot.exe) — no windows_interpreters needed. If copilot.exe spawns node.exe as a grandchild at runtime, add windows_interpreters: [node.exe] after SC3 UAT confirmation."

    For the unit test: add it to the existing policy test file in crates/nono-cli/tests/ or crates/nono-cli/src/ wherever policy parse tests live. The test should load the embedded policy JSON (using the same test helper or PolicyResolver::load_embedded() / include_str! path that existing profile tests use), deserialize, and assert:
    1. "copilot-cli" key exists in the profiles map
    2. windows_low_il_broker is true
    3. windows_interpreters is None or empty (native PE assertion)
    4. security.signal_mode is "isolated"
  </action>
  <verify>
    <automated>cargo test -p nono-cli copilot_cli_profile_present copilot_cli_profile_is_native_pe --target x86_64-pc-windows-msvc 2>&1 | tail -20</automated>
  </verify>
  <done>
    policy.json parses as valid JSON (cargo build succeeds — embedded JSON is validated at build time). The copilot-cli profile key is present. windows_low_il_broker is true. windows_interpreters is absent. Both unit tests pass. The profile description explains native PE shape.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| policy.json (static config) → engine-neutral launch path | The copilot-cli profile is a static JSON declaration. The exe-coverage gate (Phase 71) enforces that the copilot.exe directory is in the allowed paths before launching — no runtime trust boundary here beyond what Phase 71 already provides. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-03-01 | Elevation of Privilege | copilot.exe spawns node.exe grandchild without exe-coverage | mitigate | Pitfall 5: During SC3 UAT (plan 75-05), monitor child processes spawned by the confined copilot.exe. If node.exe appears as a grandchild, add "windows_interpreters": ["node.exe"] to the copilot-cli profile. The Phase 71 exe-coverage gate fails secure if node.exe is not covered. |
| T-75-03-02 | Information Disclosure | copilot.exe profile grants unrestricted network | accept | network.block is false to allow GitHub API calls. The operator must supply an explicit --allow workspace path. This is consistent with the aider/langchain-python profiles. Per-agent WFP filtering (SUPP-02, plan 75-01) provides the agent-level egress control layer at runtime. |
| T-75-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages. policy.json is embedded at build time via build.rs; changes are part of the nono-cli build artifact, not a runtime-loaded untrusted file. |
</threat_model>

<verification>
After task completes:

1. `cargo build -p nono-cli` succeeds (policy.json is embedded at build time; JSON parse errors surface here).
2. `cargo test -p nono-cli copilot_cli_profile_present copilot_cli_profile_is_native_pe` passes.
3. `make ci` passes (clippy + fmt + tests) — policy.json changes require no Rust code changes so cross-target clippy is not affected.
4. Manual: inspect policy.json to confirm no windows_interpreters field in the copilot-cli profile.
</verification>

<success_criteria>
- policy.json is valid JSON with a "copilot-cli" profile entry
- windows_low_il_broker: true is present; windows_interpreters is absent
- Both unit tests (profile_present + is_native_pe) pass
- Description field explains native PE shape and the SC3 UAT monitoring instruction
- SC3 UAT (plan 75-05) instructions: `nono agent launch --profile copilot-cli -- copilot ask "What is 2+2?"` on real Win11; confirm confined child, note any node.exe grandchild spawns
</success_criteria>

<output>
Create `.planning/phases/75-supplementary-controls-secondary-engines/75-03-SUMMARY.md` when done.
</output>
