---
context: phase
phase: 75-supplementary-controls-secondary-engines
task: 2
total_tasks: 5
status: in_progress
last_updated: 2026-06-15T22:20:44.472Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered/locked this session. Acknowledge before proceeding.

- [ ] CONSTRAINT: **Sibling-repo, not a submodule** — Plan 75-04 (already done) and any future binding work touch `../nono-ts` (`C:\Users\OMack\nono-ts`), a SEPARATE git repo. Worktree isolation breaks its relative path + commit protocol. Mitigation: run executors WITHOUT `isolation="worktree"` (sequential on main tree); commit nono-ts changes inside that repo on branch `44-broker-ffi-lockstep`.
- [ ] CONSTRAINT: **Wave 3 (75-05) is host-gated** — SC3/SC5/A1 require a REAL Win11 host (`autonomous: false`). Do not fabricate UAT results; stop and hand to the operator.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Worktree isolation on a phase that edits a sibling repo | `isolation="worktree"` only isolates the nono repo; `../nono-ts` paths + cross-repo commits break silently | blocking | Dispatch executors with NO worktree isolation; serialize plans on the main tree; commit nono-ts in its own repo/branch |
| Claiming cross-target clippy PASS on a Win11 host | The dev host cannot cross-compile (ring/aws-lc C toolchain absent) — Linux/macOS clippy cannot actually run here | advisory | Record cross-target as PARTIAL + defer to CI per `.planning/templates/cross-target-verify-checklist.md`; never claim a PASS you did not run |
| Treating baseline test noise as regressions | `cargo test -p nono-cli` has ~4 pre-existing env-specific failures (profile_cmd init + protected_paths) + 1 `nono` lib fail | advisory | Judge acceptance on NEW tests + a clean build, not the full suite green |

<current_state>
Executing Phase 75 via `/gsd:execute-phase 75`. **Wave 1 is COMPLETE and committed** (75-01, 75-03, 75-04). I was mid-dispatch of the **75-02** executor (Wave 2) when the user paused. Strategy in force: **sequential on main tree, worktrees OFF**; stop at the Wave 3 (75-05) human UAT gate.

nono repo branch: `fix/win-confinement-rb4-ra1`. nono-ts repo branch: `44-broker-ffi-lockstep`.
</current_state>

<completed_work>

Completed Plans (committed, SUMMARYs present, integration build clean):
- 75-01 SUPP-02 — per-agent WFP add-at-launch / remove-at-reap + D-05 fail-secure gate. `wfp_filter_remove` is `pub(crate)` at `crates/nono-cli/src/agent_daemon/launch.rs:265`. 6 unit tests. Commit `8da346d6` (+ `195a7c11`, `1bdfc56e`). (Auto-fixed a session_sid-path bug in `nono-wfp-service.rs` + a Send/blocking-pipe issue.)
- 75-03 SUPP-03a — `copilot-cli` native-PE engine profile in `policy.json` (`windows_low_il_broker: true`, NO `windows_interpreters`; `groups: []` placeholder pending SC3 UAT). 2 unit tests. Commit `69057404` (+ `f3f8f9bf`, `f1b8a6e6`).
- 75-04 SUPP-03b — nono-ts `confinedRun` + `confine` (Windows-cfg-gated) + non-Windows stubs; nono pin 0.33.0 → path-dep `{ path = "../Nono/crates/nono", version = "0.62" }`. 5 unit tests. nono-ts commit `e218827` (branch 44-broker-ffi-lockstep); nono tracking commit `c336bde0`.
</completed_work>

<remaining_work>

- **75-02 (Wave 2 — NEXT, not started):** SUPP-01 operator demote. `ControlRequest::Demote { tenant_id }` + `handle_demote` (Win32 `OpenProcessToken(TOKEN_ADJUST_DEFAULT|TOKEN_QUERY)` + `SetTokenInformation(TokenIntegrityLevel, Low)`) + `nono agent demote <tenant>` CLI verb (cli.rs `AgentCommands::Demote` + agent_cli.rs dispatch). MUST: call `wfp_filter_remove` for the D-03 WFP-cut (non-fatal on failure); document the spike-002 leak limits in BOTH the `handle_demote` doc comment AND the CLI help text; target a daemon `tenant_id` only (unknown → clear error); do NOT reap/kill; do NOT weaken the Medium-IL control-pipe SDDL. Tests: `demote_returns_ok_for_known_tenant`, `demote_returns_err_for_unknown_tenant`, leak-limit help assertion.
- **75-05 (Wave 3 — host-gated, `autonomous: false`):** live Win11 UAT — SC3 (Copilot confined end-to-end), SC5 (nono-ts confinedRun confines a node/JS process), A1 (two-agent per-agent WFP isolation, no cross-bleed), plus A2 (`where copilot` path) / A4 (any node.exe grandchild → add `windows_interpreters`). Produces `75-HUMAN-UAT.md`. If A1 fails, gap-closure plan 75-06 switches WFP keying to `FWPM_CONDITION_ALE_PACKAGE_ID`.
</remaining_work>

<decisions_made>

- Sequential-on-main, worktrees OFF — sibling nono-ts repo + Windows worktree fragility + security-critical code.
- nono-ts commits on `44-broker-ffi-lockstep`, not main.
- nono-ts nono pin = local path dep (crates.io 0.62.0 fails on Windows; matches nono-py).
- Cross-target clippy = PARTIAL everywhere (CI is the load-bearing gate).
- D-03 = YES: demote cuts the per-agent WFP filter (75-02), non-fatal on failure.
</decisions_made>

<blockers>
- 75-05 needs a real Win11 host (SC3/SC5/A1). No workaround — host-gated by design.
</blockers>

## Required Reading (in order)
1. `.planning/phases/75-supplementary-controls-secondary-engines/75-02-PLAN.md` — the next plan to execute
2. `.planning/phases/75-supplementary-controls-secondary-engines/75-PATTERNS.md` — analog Launch/List ControlRequest variants + verb dispatch
3. `.claude/skills/spike-findings-nono/references/windows-confinement-model.md` — spike-002 demote-only leak limits (authoritative source for the leak-limit doc)
4. `crates/nono-cli/src/agent_daemon/launch.rs` (line ~265) — exact `wfp_filter_remove` signature to call

## Infrastructure State
- nono repo: branch `fix/win-confinement-rb4-ra1`; Wave 1 fully committed; integration build clean (`cargo build -p nono-cli` OK).
- nono-ts repo (`C:\Users\OMack\nono-ts`): branch `44-broker-ffi-lockstep`; HEAD `e218827`; nono dep is a local path to `../Nono/crates/nono`.
- Untracked debris (pre-existing, NOT this phase — leave alone): `check_changes.py`, `fix_74_01.py`, `fix_74_03.py`, `fix_t1_ac.py`, `.planning/spikes/003-daemon-as-launcher/daemon_grant/`.
- Repo MUST stay PUBLIC — verify no `build_notes/`/`.gsd/` staged before any push.

<context>
The plan set passed the plan-checker (1 revision) and all coverage gates before execution. Wave order is 75-01 → (75-03, 75-04) in Wave 1, then 75-02 in Wave 2 (depends on 75-01's `wfp_filter_remove`), then 75-05 UAT in Wave 3. I deliberately disabled worktrees for the whole phase because of the sibling nono-ts repo. The 75-02 executor prompt was fully prepared (in the prior turn) — re-dispatching it is the immediate next step.
</context>

<next_action>
Start with: re-run `/gsd:execute-phase 75`. It will discover 75-02 + 75-05 as the only incomplete plans and resume at Wave 2. Dispatch a `gsd-executor` for **75-02** WITHOUT `isolation="worktree"` (sequential on main tree), using the guardrails above (D-01/D-02/D-03, spike-002 leak-limit docs, call `wfp_filter_remove`). After 75-02 lands + spot-checks pass, present the Wave 3 (75-05) human UAT gate to the operator and STOP — do not auto-run host-gated UAT.
</next_action>
