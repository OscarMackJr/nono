---
phase: 63-minifilter-spike-groundwork-macos-divergence-ledger-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/63-minifilter-spike-groundwork-macos-divergence-ledger-audit/63-DIVERGENCE-LEDGER.md
autonomous: true
requirements: [MACOS-01]
user_setup: []

must_haves:
  truths:
    - "git fetch upstream --tags has been run and v0.61.2 (3e605f27) resolves locally (the range no longer truncates to v0.61.1)"
    - "63-DIVERGENCE-LEDGER.md audits upstream v0.57.0..v0.61.2 scoped to the macOS Seatbelt surface, with reproducible drift-tool frontmatter"
    - "Every macOS-relevant commit is dispositioned will-sync / fork-preserve / won't-sync / split with a macos-only column and a diff-inspect note"
    - "The three P1 commits 8f84d454, 362ada22, 8f1b0b74 are present, each dispositioned will-sync"
    - "The Headline explicitly records that this ledger supersedes Phase 54 cluster C14 (won't-sync -> will-sync) on the v2.10 macOS-parity scope change"
    - "Disposition is by diff-inspecting re-export AND function-call surfaces, NOT git log --name-only (the feedback_cluster_isolation_invalid lesson)"
  artifacts:
    - path: ".planning/phases/63-minifilter-spike-groundwork-macos-divergence-ledger-audit/63-DIVERGENCE-LEDGER.md"
      provides: "Complete macOS-scoped upstream-divergence audit for v0.57.0..v0.61.2"
      contains: "macos-only"
  key_links:
    - from: "63-DIVERGENCE-LEDGER.md"
      to: "Phase 54 cluster C14"
      via: "explicit supersession note in Headline"
      pattern: "C14"
    - from: "63-DIVERGENCE-LEDGER.md"
      to: "the three P1 commits"
      via: "will-sync disposition rows"
      pattern: "8f84d454|362ada22|8f1b0b74"
---

<objective>
Produce `63-DIVERGENCE-LEDGER.md`: a complete macOS-scoped audit of upstream `always-further/nono` `v0.57.0..v0.61.2`, with every macOS-relevant commit dispositioned (will-sync / fork-preserve / won't-sync / split), a `macos-only` column mirroring the Phase 42/47/54 `windows-touch` shape, and a diff-inspect note per the `feedback_cluster_isolation_invalid` lesson. The three P1 commits are dispositioned `will-sync`, and the ledger explicitly supersedes Phase 54's C14 `won't-sync` verdict. This is the entirely autonomous, read-only-git Track B deliverable — Phase 64's cherry-pick wave then just executes this ledger.

Purpose: MACOS-01 requires a full macOS upstream inventory so cherry-picks can begin in Phase 64. The audit is the gate that prevents re-deferring (Phase 54 C14) the exact security/correctness commits v2.10 exists to absorb.
Output: `63-DIVERGENCE-LEDGER.md` with reproducible frontmatter, a cluster summary, per-commit rows, an ADR-review verdict, an empirical cross-check, and a cross-cluster re-export-deps section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63-minifilter-spike-groundwork-macos-divergence-ledger-audit/63-CONTEXT.md
@.planning/phases/63-minifilter-spike-groundwork-macos-divergence-ledger-audit/63-RESEARCH.md
@.planning/phases/63-minifilter-spike-groundwork-macos-divergence-ledger-audit/63-PATTERNS.md
@.planning/phases/54-upst7-audit/54-DIVERGENCE-LEDGER.md
@.planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md

<notes>
- MANDATORY FIRST STEP: `git fetch upstream --tags` then `git cat-file -t 3e605f27` (must print `commit`). VERIFIED at plan-time: `v0.61.2` (`3e605f27`) is NOT in the local object store — without the fetch the range silently truncates to `v0.61.1` and any v0.61.2 macOS commit is dropped (RESEARCH Pitfall E). The upstream remote is configured (`github.com/always-further/nono`).
- The pinned drift tool is `scripts/check-upstream-drift.{sh,ps1}` (pinned sha `0834aa664fbaf4c5e41af5debece292992211559`). The tool EXCLUDES `*_windows.rs` + `exec_strategy_windows/` — fine for a macOS audit, but it means the `macos-only` column is derived from the tool's category data + per-commit diff-inspect, NOT from the filter.
- LEDGER SHAPE: copy from `54-DIVERGENCE-LEDGER.md` (frontmatter lines 1-14, Headline, Reproduction, Cluster Summary, ADR review, Empirical cross-check, Cross-cluster re-export deps). Swap the `windows-touch` column for `macos-only` (Phase 42 precedent). The ADR under review is `docs/architecture/upstream-parity-strategy.md`.
- DIFF-INSPECT (D-11, NOT `--name-only`): for every will-sync candidate, `git show <sha>` and inspect (a) added `pub use`/`pub mod`/`pub(crate)` re-exports AND (b) function-call cross-cluster deps. Audit target sites: `crates/nono/src/sandbox/macos.rs::generate_profile` (the 8f84d454 ordering site + the c6730e43 java_runtime 2-line touch), `crates/nono-cli/src/sandbox_prepare.rs` resolved_workdir/cwd_canonical (the 362ada22+8f1b0b74 symlink-CWD site), and `crates/nono-cli/src/policy.rs` add_platform_rule call sites. The note must answer: does the fork's call site that produces the affected rule match the upstream site the commit patches?
- SUPERSESSION (D-13, Pitfall F): Phase 54 C14 dispositioned 8f84d45/362ada2/8f1b0b7 as `won't-sync` (Windows-only milestone). This ledger OVERRIDES to `will-sync` (v2.10 macOS parity) and MUST state the supersession explicitly in the Headline — do NOT silently copy C14.
- The three P1 SHAs are full 8-hex (8f84d454 / 362ada22 / 8f1b0b74); Phase 54 abbreviates to 7-hex — match the drift-tool output form in the rows but keep the full SHAs discoverable.
- `729697c2` (`--trust-proxy-ca`) is a known P2 IN-RANGE candidate: inventory it in the ledger, but it is below the three P1s in cherry-pick priority (the non-macOS UPST8 slice stays deferred).
- `c6730e43` (java_runtime) is macOS-relevant even though its subject says nothing about macOS — `git show c6730e43 --stat` shows it touches `crates/nono/src/sandbox/macos.rs` (2 lines). Mark `macos-only = yes`; the will-sync vs won't-sync disposition (java group is cross-platform policy) is the auditor's call.
- `fe233db4` is a MERGE commit (PR #680) — the actual non-merge fixes are `362ada22` + `8f1b0b74`. The drift tool excludes merges; disposition the two non-merge SHAs, not the merge.
- This plan is read-only git + write one ledger file. It touches NO Rust source — `git diff --name-only -- crates/ bindings/ scripts/ Makefile` must stay empty (zero-source-edits invariant, Phase 54 precedent).
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch upstream tags, run the pinned drift tool, and produce the macOS-relevant commit inventory</name>
  <files>ci-logs-local/drift/63-macos-v057-v0612.json (gitignored working artifact — NOT committed)</files>
  <read_first>
    - .planning/phases/63-minifilter-spike-groundwork-macos-divergence-ledger-audit/63-RESEARCH.md (§ Code Examples "Track B — exact git inventory commands" lines 373-396; § Common Pitfalls E lines 249-251; Open Question 3 lines 435-438)
    - .planning/phases/63-minifilter-spike-groundwork-macos-divergence-ledger-audit/63-PATTERNS.md (§ Shared Patterns "Reproducible-audit frontmatter" lines 161-164 — the fetch precondition + sha pin)
    - .planning/phases/54-upst7-audit/54-DIVERGENCE-LEDGER.md (lines 1-50 — frontmatter + Reproduction shape to mirror)
  </read_first>
  <action>
    Run `git fetch upstream --tags`, then assert `git cat-file -t 3e605f27` prints `commit` (the v0.61.2 precondition — abort and re-fetch if it does not). Record `upstream_head_at_audit` (the post-fetch upstream HEAD sha) and `refetch_date`.
    Assert the drift-tool sha pin: confirm `scripts/check-upstream-drift.sh` (and `.ps1`) hashes to `0834aa664fbaf4c5e41af5debece292992211559` before relying on it.
    Run the authoritative inventory `make check-upstream-drift ARGS="--from v0.57.0 --to v0.61.2 --format json"` (Windows-host fallback: `bash scripts/check-upstream-drift.sh --from v0.57.0 --to v0.61.2 --format json`). Write the JSON to a gitignored path under `ci-logs-local/drift/` (NOT committed) and record `total_unique_commits`.
    Narrow to the macOS surface with the three RESEARCH commands: `git log --no-merges v0.57.0..v0.61.2 -- crates/nono/src/sandbox/macos.rs`, the same for `crates/nono-cli/src/sandbox_prepare.rs`, and a `--grep='macos\|seatbelt\|symlink\|Keychain\|sandbox' -i` pass. Also run `git log --no-merges v0.61.1..v0.61.2` to capture any v0.61.2-only macOS delta (Open Question 3).
    Confirm the three P1 SHAs (8f84d454, 362ada22, 8f1b0b74), the P2 729697c2, and c6730e43 (`git show c6730e43 --stat` → touches macos.rs) all resolve in range.
    Produce a working inventory list of macOS-relevant commits with their `git show <sha> --stat` file footprints — this is the raw material for the ledger rows in Task 2.
  </action>
  <acceptance_criteria>
    - `git cat-file -t 3e605f27` prints `commit` (v0.61.2 fetched and resolvable).
    - The drift-tool sha matches `0834aa664fbaf4c5e41af5debece292992211559`.
    - The drift-tool JSON is produced under a gitignored `ci-logs-local/` path and is NOT staged for commit.
    - The macOS-narrowing `git log` over `crates/nono/src/sandbox/macos.rs` and `crates/nono-cli/src/sandbox_prepare.rs` for `v0.57.0..v0.61.2` runs without error and the three P1 SHAs + 729697c2 + c6730e43 are confirmed in range.
    - `git diff --name-only -- crates/ bindings/ scripts/ Makefile` is empty (no source edits).
  </acceptance_criteria>
  <verify>
    <automated>git cat-file -t 3e605f27 | grep -qx commit && git rev-list v0.57.0..v0.61.2 | grep -q . && for s in 8f84d454 362ada22 8f1b0b74 729697c2 c6730e43; do git cat-file -t $s | grep -qx commit || exit 1; done && test -z "$(git diff --name-only -- crates/ bindings/ scripts/ Makefile)"</automated>
  </verify>
  <done>Upstream tags fetched (v0.61.2 resolvable), the drift tool's pin asserted and JSON produced (gitignored), and a macOS-relevant commit inventory with per-commit file footprints assembled for the range — with the three P1s + P2 + c6730e43 confirmed in range and zero source edits.</done>
</task>

<task type="auto">
  <name>Task 2: Write 63-DIVERGENCE-LEDGER.md with dispositions, diff-inspect notes, and the C14 supersession</name>
  <files>.planning/phases/63-minifilter-spike-groundwork-macos-divergence-ledger-audit/63-DIVERGENCE-LEDGER.md</files>
  <read_first>
    - .planning/phases/63-minifilter-spike-groundwork-macos-divergence-ledger-audit/63-PATTERNS.md (§ "63-DIVERGENCE-LEDGER.md" lines 33-96 — exact frontmatter, required sections in order, the two table headers to copy, disposition vocabulary, the C14 sample rows, diff-inspect-note requirement)
    - .planning/phases/63-minifilter-spike-groundwork-macos-divergence-ledger-audit/63-RESEARCH.md (§ Architecture Patterns "Pattern 2" lines 187-191; § State of the Art lines 400-408; § Common Pitfalls F lines 253-255; the c6730e43 / fe233db4-merge notes)
    - .planning/phases/54-upst7-audit/54-DIVERGENCE-LEDGER.md (the full canonical structure: Headline lines 18-37, Reproduction 39-50, Cluster Summary 52+, C3 worked will-sync cluster 111-128, C5 function-call-cross-cluster-dep 146-167, ADR review, Empirical cross-check, Cross-cluster re-export deps; and the C14 verdict lines ~300-315 this phase supersedes)
    - .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md (the windows-touch-column template MACOS-01 mirrors with a macos-only column)
    - docs/architecture/upstream-parity-strategy.md (the ADR under review for the ADR-review section)
  </read_first>
  <action>
    Write `63-DIVERGENCE-LEDGER.md` mirroring the Phase 54 structure. Frontmatter: `ledger_type: macos-audit`, `range: v0.57.0..v0.61.2`, `upstream_head_at_audit` + `refetch_date` from Task 1, the drift-tool sha pins + verbatim invocation, `fork_baseline` (the UPST7 sync point v0.57.0), `total_unique_commits` from the drift tool, `date: 2026-06-06`.
    Required sections in order: `## Headline` (disposition breakdown counts + the marquee finding — MUST state the Phase-54-C14 supersession: won't-sync -> will-sync on the v2.10 macOS-parity scope change, citing D-13); `## Reproduction` (verbatim invocation, gitignored JSON path, the sha-pin assertion, the auditor-rerun recipe + the `git fetch upstream --tags` + `git cat-file -t 3e605f27` precondition); `## Cluster Summary` (the master table with header `| cluster_id | theme | commits | disposition | macos-only | rationale |` copied exactly and `windows-touch` swapped to `macos-only`, plus per-cluster subsections); `## ADR review` (the L/M/H verdict table across security/windows/maintenance/divergence/contributor; the ADR is `docs/architecture/upstream-parity-strategy.md`); `## Empirical cross-check` (>=4 fork-shared macOS files walked against the upstream log via the re-export/call-site diff-inspect, NOT `--name-only`); `## Cross-cluster re-export deps detected` (the feedback_cluster_isolation_invalid closure).
    Per-commit rows use the header `| sha | subject | upstream-tag | categories | files-changed | macos-only |` copied exactly. Disposition vocabulary is the four locked values: `will-sync` / `fork-preserve` / `won't-sync` / `split`.
    Disposition every macOS-relevant commit. The three P1 SHAs are `will-sync` (D-13). For each will-sync candidate, write a diff-inspect note answering "does the fork's call site that produces the affected rule match the upstream site the commit patches?" — covering the macos.rs `generate_profile` ordering site (8f84d454), the sandbox_prepare.rs resolved_workdir/cwd_canonical site (362ada22 + 8f1b0b74), and the policy.rs add_platform_rule call sites. Mark `c6730e43` `macos-only = yes` (it touches macos.rs) and disposition it (auditor's call given java is cross-platform policy). Inventory `729697c2` as the in-range P2. Disposition the non-merge `362ada22`/`8f1b0b74`, NOT the `fe233db4` merge.
    Do NOT edit any Rust source — this task writes only the ledger markdown.
  </action>
  <acceptance_criteria>
    - `63-DIVERGENCE-LEDGER.md` frontmatter contains `range: v0.57.0..v0.61.2`, the drift-tool sha `0834aa664fbaf4c5e41af5debece292992211559`, and a non-empty `upstream_head_at_audit`.
    - The Cluster Summary table header contains the `macos-only` column (and NOT `windows-touch`).
    - All three P1 SHAs `8f84d454`, `362ada22`, `8f1b0b74` appear in the ledger, each on a row/cluster whose disposition is `will-sync`.
    - The Headline contains an explicit Phase-54 `C14` supersession statement (won't-sync -> will-sync).
    - The ledger contains all four disposition vocabulary tokens used appropriately and a diff-inspect note for each will-sync cluster (it references `generate_profile`, `sandbox_prepare`, and `add_platform_rule` call sites).
    - The ledger inventories `729697c2` and marks `c6730e43` `macos-only` = yes.
    - `git diff --name-only -- crates/ bindings/ scripts/ Makefile` is empty.
  </acceptance_criteria>
  <verify>
    <automated>L=.planning/phases/63-minifilter-spike-groundwork-macos-divergence-ledger-audit/63-DIVERGENCE-LEDGER.md; grep -q 'range: v0.57.0..v0.61.2' $L && grep -q '0834aa664fbaf4c5e41af5debece292992211559' $L && grep -q 'macos-only' $L && ! grep -q 'windows-touch' $L && for s in 8f84d454 362ada22 8f1b0b74; do grep -q "$s" $L || exit 1; done && grep -q 'C14' $L && grep -Eq 'will-sync' $L && grep -Eq 'fork-preserve|won.t-sync|split' $L && grep -q '729697c2' $L && grep -q 'c6730e43' $L && test -z "$(git diff --name-only -- crates/ bindings/ scripts/ Makefile)"</automated>
  </verify>
  <done>`63-DIVERGENCE-LEDGER.md` exists with reproducible frontmatter, a `macos-only` column, every macOS-relevant commit dispositioned with diff-inspect notes, the three P1s as `will-sync`, the explicit Phase-54-C14 supersession in the Headline, and zero source edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream git history → fork audit | Read-only; the audit ingests upstream commits but changes no fork code (the cherry-picks are Phase 64) |
| audit disposition → Phase 64 cherry-pick wave | A wrong disposition (e.g. silently re-copying C14 won't-sync) would drop the security/correctness fixes v2.10 exists to absorb |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-10 | Information Disclosure | Seatbelt deny silently overridden by later allow (last-match-wins) | mitigate | The `8f84d454` P1 fix (platform denies emitted AFTER write allows) is dispositioned `will-sync` so Phase 64 absorbs it; Phase 64 asserts ordering by unit test (Pitfall 10) |
| T-63-11 | Information Disclosure | macOS `/etc` vs `/private/etc` symlink deny bypass | mitigate | `362ada22`/`8f1b0b74` (symlink/`$PWD` CWD capture) dispositioned `will-sync`; the ledger's diff-inspect note confirms the fork's dual-path emission site so Phase 64 applies the fix at the right call site (Pitfall 11) |
| T-63-12 | Tampering | audit completeness (range truncation) | mitigate | `git fetch upstream --tags` + `git cat-file -t 3e605f27` precondition prevents the range silently truncating to v0.61.1 and dropping v0.61.2 macOS commits (Pitfall E) |
| T-63-13 | Tampering | cross-cluster re-export prereq missed | mitigate | Diff-inspect re-export AND function-call surfaces per commit (not `--name-only`) — the feedback_cluster_isolation_invalid lesson; the ledger records a per-will-sync-cluster note |
| T-63-14 | Repudiation | silent re-deferral of P1 commits | mitigate | The Headline explicitly supersedes Phase 54 C14 (won't-sync -> will-sync) with the scope-change rationale — no silent copy-paste (Pitfall F) |
| T-63-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — Track B is read-only git analysis; no package installs, no Cargo change (per 63-RESEARCH § Package Legitimacy Audit). No `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- `git fetch upstream --tags` run; `git cat-file -t 3e605f27` = `commit`.
- `63-DIVERGENCE-LEDGER.md` covers `v0.57.0..v0.61.2` with reproducible frontmatter (drift-tool sha pin, upstream_head, refetch_date).
- Every macOS-relevant commit dispositioned; `macos-only` column present; `windows-touch` absent.
- The three P1 SHAs present and `will-sync`; the Headline supersedes Phase 54 C14.
- A diff-inspect note per will-sync cluster referencing the real call sites; `729697c2` inventoried; `c6730e43` `macos-only = yes`.
- Zero source edits: `git diff --name-only -- crates/ bindings/ scripts/ Makefile` empty.
</verification>

<success_criteria>
SC4 / MACOS-01 satisfied:
- A complete macOS-scoped `DIVERGENCE-LEDGER.md` for `v0.57.0..v0.61.2` is committed with every commit classified, a `macos-only` column, and diff-inspect notes.
- The three P1 commits are identified and dispositioned `will-sync`; the ledger explicitly supersedes Phase 54 C14.
</success_criteria>

<output>
Create `.planning/phases/63-minifilter-spike-groundwork-macos-divergence-ledger-audit/63-03-SUMMARY.md` when done.
</output>
