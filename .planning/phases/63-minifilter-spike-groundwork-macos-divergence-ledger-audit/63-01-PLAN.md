---
phase: 63-minifilter-spike-groundwork-macos-divergence-ledger-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drivers/nono-fltmgr/nono-fltmgr.c
  - drivers/nono-fltmgr/nono-fltmgr.vcxproj
  - drivers/nono-fltmgr/nono-fltmgr.vcxproj.filters
  - drivers/nono-fltmgr/nono-fltmgr.inf
  - drivers/nono-fltmgr/DESIGN.md
  - docs/architecture/minifilter-spike-design-pointer.md
autonomous: true
requirements: [DRV-03]
user_setup: []

must_haves:
  truths:
    - "The drivers/nono-fltmgr/ WDK scaffold source exists and is structured to compile to a .sys (compile proof itself runs in Plan 63-02 on the VM)"
    - "The skeleton .c registers an EMPTY operation-callbacks array and does NO file I/O (no pre-create callback, no comm port, no ZwCreateFile)"
    - "The INF declares SERVICE_DEMAND_START (StartType = 3) and an FSFilter Activity Monitor altitude placeholder"
    - "DESIGN.md exists as the hard pre-code gate specifying ring-buffer+worker-thread IPC, no driver-originated file I/O, finite FltSendMessage timeout, NonPagedPoolNx, IRQL assertions, and the chosen altitude band"
    - "A pointer stub in docs/architecture/ cross-links to the canonical DESIGN.md"
    - "drivers/ is NOT a Cargo workspace member — make ci stays green with no Rust changes"
  artifacts:
    - path: "drivers/nono-fltmgr/nono-fltmgr.c"
      provides: "Skeleton DriverEntry + FltRegisterFilter + FltStartFiltering + NonoFltUnload"
      contains: "FltRegisterFilter"
    - path: "drivers/nono-fltmgr/nono-fltmgr.inf"
      provides: "FltMgr service install config with SERVICE_DEMAND_START and altitude placeholder"
      contains: "StartType"
    - path: "drivers/nono-fltmgr/nono-fltmgr.vcxproj"
      provides: "WDK MSBuild project (ConfigurationType=Driver, DriverType=WDM)"
      contains: "Driver"
    - path: "drivers/nono-fltmgr/DESIGN.md"
      provides: "Pre-code BSOD-avoidance gate (D-10)"
      contains: "NonPagedPoolNx"
    - path: "docs/architecture/minifilter-spike-design-pointer.md"
      provides: "ADR-home pointer stub to the canonical DESIGN.md (D-09, path-corrected)"
      contains: "drivers/nono-fltmgr/DESIGN.md"
  key_links:
    - from: "drivers/nono-fltmgr/nono-fltmgr.inf"
      to: "drivers/nono-fltmgr/nono-fltmgr.sys"
      via: "ServiceBinary %12%\\nono-fltmgr.sys"
      pattern: "nono-fltmgr\\.sys"
    - from: "docs/architecture/minifilter-spike-design-pointer.md"
      to: "drivers/nono-fltmgr/DESIGN.md"
      via: "Related-doc cross-link"
      pattern: "drivers/nono-fltmgr/DESIGN\\.md"
---

<objective>
Author the out-of-workspace `drivers/nono-fltmgr/` WDK MSBuild scaffold (skeleton `.c`, `.vcxproj`, `.inf`) plus the hard pre-code design-doc gate (`DESIGN.md`) and an ADR-home pointer stub. This is the dev-host-authorable half of Track A: it produces the source the VM compile proof (Plan 63-02) consumes, and the BSOD-avoidance contract that gates all future driver code.

Purpose: DRV-03 (partial) requires a reproducible, documented driver build scaffold to exist before any real driver logic. The design doc is the security artifact for the future kernel driver — its required elements ARE the mitigations against the host-availability/integrity threats (BSOD, host hang).
Output: `drivers/nono-fltmgr/` source scaffold + `DESIGN.md` + `docs/architecture/` pointer stub. NO binary (the `.sys` is produced on the VM in 63-02 and is never committed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63-minifilter-spike-groundwork-macos-divergence-ledger-audit/63-CONTEXT.md
@.planning/phases/63-minifilter-spike-groundwork-macos-divergence-ledger-audit/63-RESEARCH.md
@.planning/phases/63-minifilter-spike-groundwork-macos-divergence-ledger-audit/63-PATTERNS.md
@.planning/phases/63-minifilter-spike-groundwork-macos-divergence-ledger-audit/63-VALIDATION.md
@docs/architecture/broker-trust-anchor.md

<notes>
- `drivers/`, `proj/`, and `.planning/adr/` do NOT exist in the tree (verified). The repo's real ADR/design-doc home is `docs/architecture/` (6 existing ADRs). D-09's "pointer stub in .planning/adr/" is path-corrected: the canonical DESIGN.md stays at `drivers/nono-fltmgr/DESIGN.md` (co-located with code, D-09) and the pointer stub lives at `docs/architecture/minifilter-spike-design-pointer.md` consistent with the 6 siblings.
- The prescriptive scaffold source (skeleton `.c`, INF, vcxproj guidance) lives in 63-RESEARCH.md § Code Examples (lines 257-371). Do NOT invent driver code — mirror that scaffold + Microsoft's `nullFilter` sample.
- The DESIGN.md content backbone is 63-RESEARCH.md § Security Domain "Known Threat Patterns" (lines 504-513) — that STRIDE table IS the doc's required content. The house-style header is `docs/architecture/broker-trust-anchor.md` lines 1-7.
- Phase 63 adds NO Rust code and `drivers/` must NOT become a Cargo workspace member, so `make ci` is a no-regression check only.
- Do NOT touch `crates/nono-cli/data/windows/nono-wfp-driver.sys` (out-of-scope placeholder; the spike's `nono-fltmgr.sys` is a separate, non-MSI-bundled driver).
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the drivers/nono-fltmgr/ WDK scaffold (.c, .vcxproj, .vcxproj.filters, .inf)</name>
  <files>drivers/nono-fltmgr/nono-fltmgr.c, drivers/nono-fltmgr/nono-fltmgr.vcxproj, drivers/nono-fltmgr/nono-fltmgr.vcxproj.filters, drivers/nono-fltmgr/nono-fltmgr.inf</files>
  <read_first>
    - .planning/phases/63-minifilter-spike-groundwork-macos-divergence-ledger-audit/63-RESEARCH.md (§ Code Examples lines 257-371 — the prescriptive skeleton DriverEntry/INF/msbuild; § Anti-Patterns lines 193-198; Pitfall C lines 240-242)
    - .planning/phases/63-minifilter-spike-groundwork-macos-divergence-ledger-audit/63-PATTERNS.md (§ "drivers/nono-fltmgr/ WDK scaffold" lines 139-149 — scope guard + nullFilter reference)
  </read_first>
  <action>
    Create the new top-level `drivers/nono-fltmgr/` directory (NOT a Cargo workspace member — do not edit any Cargo.toml).
    Author four scaffold files mirroring 63-RESEARCH.md § Code Examples and Microsoft's `nullFilter` sample:
    (1) `nono-fltmgr.c` — skeleton `DriverEntry` calling `FltRegisterFilter` + `FltStartFiltering`; a registered `NonoFltUnload` callback that calls `FltUnregisterFilter`; an `FLT_REGISTRATION` table with the Unload callback set; and an EMPTY operation-callbacks array (`{ IRP_MJ_OPERATION_END }`). The skeleton must compile and do nothing else.
    (2) `nono-fltmgr.vcxproj` — set `ConfigurationType = Driver` and `DriverType = WDM` (a minifilter is a WDM-class FS filter, NOT KMDF), import the WDK `.props`/`.targets`, and carry the Spectre-mitigation flags. Copy this structure from the `nullFilter.vcxproj` sample rather than hand-authoring.
    (3) `nono-fltmgr.vcxproj.filters` — optional solution-explorer grouping for the source/INF files.
    (4) `nono-fltmgr.inf` — `Class = "ActivityMonitor"` with the standard ActivityMonitor ClassGuid; `ServiceBinary = %12%\nono-fltmgr.sys`; `Dependencies = "FltMgr"`; `ServiceType = 2`; `StartType = 3` (SERVICE_DEMAND_START, the D-06 boot-loop safeguard); `LoadOrderGroup = "FSFilter Activity Monitor"`; and an `Instance1.Altitude` PLACEHOLDER (e.g. `"370020"`) in the FSFilter Activity Monitor band 360000-389999 with an inline comment that the final number is Phase 64's call after `fltmc filters` enumeration and MUST NOT be in the AV range 320000-329998.
    HARD scope guard (Pitfall C): NO `FLT_OPERATION_REGISTRATION` pre-op body, NO `FltCreateCommunicationPort`, NO `ZwCreateFile`/`NtCreateFile` — those are Phase 64. The skeleton must do NO file I/O at all.
    Do NOT commit any `.sys` binary; commit only the source scaffold.
  </action>
  <acceptance_criteria>
    - `drivers/nono-fltmgr/nono-fltmgr.c` exists and contains `FltRegisterFilter`, `FltStartFiltering`, and `FltUnregisterFilter`.
    - `drivers/nono-fltmgr/nono-fltmgr.c` contains the empty-callbacks marker `IRP_MJ_OPERATION_END` and does NOT contain `ZwCreateFile`, `NtCreateFile`, `FltCreateCommunicationPort`, or a pre-create `IRP_MJ_CREATE` callback body.
    - `drivers/nono-fltmgr/nono-fltmgr.vcxproj` contains `Driver` (ConfigurationType) and `WDM` (DriverType).
    - `drivers/nono-fltmgr/nono-fltmgr.inf` contains `StartType` set to `3`, `LoadOrderGroup` = `"FSFilter Activity Monitor"`, and an altitude value within 360000-389999 (NOT within 320000-329998).
    - No `*.sys` file is staged in `drivers/`.
    - No Cargo.toml/Cargo.lock was modified (`git diff --name-only` shows no `Cargo.toml`/`Cargo.lock` change).
  </acceptance_criteria>
  <verify>
    <automated>test -f drivers/nono-fltmgr/nono-fltmgr.c && grep -q 'FltRegisterFilter' drivers/nono-fltmgr/nono-fltmgr.c && ! grep -Eq 'ZwCreateFile|NtCreateFile|FltCreateCommunicationPort' drivers/nono-fltmgr/nono-fltmgr.c && grep -q 'IRP_MJ_OPERATION_END' drivers/nono-fltmgr/nono-fltmgr.c && grep -q 'StartType' drivers/nono-fltmgr/nono-fltmgr.inf && grep -q 'FSFilter Activity Monitor' drivers/nono-fltmgr/nono-fltmgr.inf && ! git diff --name-only | grep -Eq 'Cargo\.(toml|lock)'</automated>
  </verify>
  <done>The four scaffold files exist under `drivers/nono-fltmgr/`, the `.c` is a no-op-callbacks/no-file-I/O skeleton, the INF declares SERVICE_DEMAND_START + an FSFilter band altitude placeholder, and no Cargo workspace file was touched.</done>
</task>

<task type="auto">
  <name>Task 2: Write DESIGN.md (hard pre-code gate) + docs/architecture/ pointer stub</name>
  <files>drivers/nono-fltmgr/DESIGN.md, docs/architecture/minifilter-spike-design-pointer.md</files>
  <read_first>
    - .planning/phases/63-minifilter-spike-groundwork-macos-divergence-ledger-audit/63-RESEARCH.md (§ Security Domain "Known Threat Patterns" lines 504-513 — the STRIDE backbone; § Common Pitfalls A-F lines 226-255; D-10 / D-08 content list)
    - .planning/phases/63-minifilter-spike-groundwork-macos-divergence-ledger-audit/63-PATTERNS.md (§ "drivers/nono-fltmgr/DESIGN.md" lines 99-124 and § ".planning/adr/ pointer stub" lines 128-135 — header convention + path correction)
    - docs/architecture/broker-trust-anchor.md (lines 1-40 — the house-style header block + Context framing to mirror)
  </read_first>
  <action>
    Create `drivers/nono-fltmgr/DESIGN.md` as the hard pre-code gate (D-10). Open with the `docs/architecture/*.md` house-style header block: `# <Title>`, then `**Status:** Accepted (pre-code gate)`, `**Date:** 2026-06-06`, `**Phase:** 63 (v2.10 Kernel-Driver Spike)`, `**Requirement:** DRV-03 (partial)`, `**Related ADR:**` → the `docs/architecture/` pointer stub; then a `## Context` section framing the BSOD-avoidance purpose.
    The doc MUST specify (each grep-asserted at SC3, per D-10 / D-08):
    - the ring-buffer + worker-thread IPC pattern (callback enqueues to a ring buffer; a worker thread drains it and does the `FltSendMessage`);
    - NO driver-originated file I/O — explicitly forbid `ZwCreateFile`/`NtCreateFile`; permit only `FltCreateFile` on the minifilter's own instance if internal I/O is ever unavoidable (Pitfall 2 recursion-BSOD);
    - a finite `FltSendMessage` timeout (~500 ms → `STATUS_TIMEOUT`, never an infinite wait; fail-open for the spike, with an explicit "production ADR revisits fail-direction" note — Pitfall 3);
    - `NonPagedPoolNx` for callback-reachable allocations;
    - IRQL assertions (e.g. `NT_ASSERT(KeGetCurrentIrql() <= APC_LEVEL)`) — Pitfall 1;
    - the chosen altitude band: FSFilter Activity Monitor 360000-389999, with the AV-range 320000-329998 avoidance constraint stated; and a placeholder line for the Microsoft altitude-request status (the date + `pending` is filled by Plan 63-02 when the email is sent — leave a clearly-marked slot).
    Map each future-driver threat (driver-originated recursive I/O, infinite FltSendMessage, IRQL violation, altitude/EDR collision) to the design requirement that mitigates it (a STRIDE-style table mirroring 63-RESEARCH § Security Domain is the right shape).
    Then create `docs/architecture/minifilter-spike-design-pointer.md` (path correction: `.planning/adr/` does not exist; this is the real ADR home). One-line-plus-header stub: the house-style header block + a single sentence pointing at `drivers/nono-fltmgr/DESIGN.md` as the canonical pre-code gate, with `**Related ADR:**` → the DESIGN.md. State in the stub that the canonical doc is co-located with the driver code per D-09.
  </action>
  <acceptance_criteria>
    - `drivers/nono-fltmgr/DESIGN.md` contains ALL of: `NonPagedPoolNx`, `FltSendMessage`, `STATUS_TIMEOUT`, `ring buffer` (or `ring-buffer`), `worker thread` (or `worker-thread`), an IRQL-assertion token (`KeGetCurrentIrql` or `IRQL`), a no-`ZwCreateFile` prohibition, the band `360000` and `389999`, and the AV-range `320000` and `329998`.
    - `drivers/nono-fltmgr/DESIGN.md` has a clearly-marked altitude-request-status slot (a `pending`/date placeholder Plan 63-02 fills).
    - `drivers/nono-fltmgr/DESIGN.md` opens with the `**Status:**` / `**Date:**` / `**Phase:**` header block and a `## Context` section.
    - `docs/architecture/minifilter-spike-design-pointer.md` exists and contains the literal path `drivers/nono-fltmgr/DESIGN.md`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'NonPagedPoolNx' drivers/nono-fltmgr/DESIGN.md && grep -q 'FltSendMessage' drivers/nono-fltmgr/DESIGN.md && grep -q 'STATUS_TIMEOUT' drivers/nono-fltmgr/DESIGN.md && grep -Eqi 'ring[- ]buffer' drivers/nono-fltmgr/DESIGN.md && grep -Eqi 'worker[- ]thread' drivers/nono-fltmgr/DESIGN.md && grep -Eq 'KeGetCurrentIrql|IRQL' drivers/nono-fltmgr/DESIGN.md && grep -q 'ZwCreateFile' drivers/nono-fltmgr/DESIGN.md && grep -q '360000' drivers/nono-fltmgr/DESIGN.md && grep -q '329998' drivers/nono-fltmgr/DESIGN.md && grep -q 'drivers/nono-fltmgr/DESIGN.md' docs/architecture/minifilter-spike-design-pointer.md</automated>
  </verify>
  <done>`DESIGN.md` exists as a grep-passing hard pre-code gate specifying every D-10 element plus the altitude band/AV-range constraint and a request-status slot; the `docs/architecture/` pointer stub cross-links to it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future kernel callback → user-mode supervisor | The future `FltSendMessage` round-trip (Phase 64) crosses kernel↔user; DESIGN.md pre-specifies its safety envelope |
| driver source → host kernel | A malformed/unsafe driver scaffolded wrong can BSOD or hang the host once loaded (Phase 64); the design gate constrains what may be written |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | Denial of Service | future pre-create callback (drivers/nono-fltmgr) | mitigate | DESIGN.md forbids driver-originated file I/O (`ZwCreateFile`/`NtCreateFile`); `FltCreateFile`-on-own-instance escape only — prevents Pitfall-2 recursion BSOD |
| T-63-02 | Denial of Service | future FltSendMessage round-trip | mitigate | DESIGN.md mandates a finite (~500ms) timeout → `STATUS_TIMEOUT`, never infinite wait — prevents Pitfall-3 host hang |
| T-63-03 | Denial of Service | future callback allocations / IRQL | mitigate | DESIGN.md mandates `NonPagedPoolNx` + IRQL assertions (`NT_ASSERT(KeGetCurrentIrql() <= APC_LEVEL)`) — prevents Pitfall-1 BSOD |
| T-63-04 | Tampering / Elevation | INF altitude declaration | mitigate | INF + DESIGN.md record the FSFilter Activity Monitor band (360000-389999) and the AV-range (320000-329998) avoidance — prevents Pitfall-5 EDR collision |
| T-63-05 | Tampering | spike .sys leaking into main / MSI | mitigate | Scaffold commits source only (no `.sys`); `drivers/` is out of the Cargo workspace; the `nono-wfp-driver.sys` placeholder + MSI are untouched (V10 hygiene) |
| T-63-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — Phase 63 installs no registry packages; no Cargo change (per 63-RESEARCH § Package Legitimacy Audit). No `[ASSUMED]`/`[SUS]` packages exist. |
</threat_model>

<verification>
- `drivers/nono-fltmgr/` scaffold + `DESIGN.md` + `docs/architecture/minifilter-spike-design-pointer.md` all exist.
- DESIGN.md passes the grep content-assertion for every D-10 element (Task 2 automated check).
- The skeleton `.c` registers an empty callbacks array and does no file I/O (Task 1 automated check).
- No Cargo workspace file modified; `drivers/` is not a workspace member. `make ci` (run at wave merge) stays green as a no-regression check.
- No `.sys` binary committed; `crates/nono-cli/data/windows/nono-wfp-driver.sys` untouched.
</verification>

<success_criteria>
SC2 prerequisite (scaffold exists, ready to compile in 63-02) and SC3 (design-doc pre-code gate) satisfied on the dev host:
- The `drivers/nono-fltmgr/` source compiles-ready scaffold exists (the compile PROOF is Plan 63-02 on the VM).
- DESIGN.md is a grep-passing hard pre-code gate with the altitude band + AV-range constraint + a request-status slot.
- The ADR-home pointer stub cross-links to the canonical DESIGN.md.
</success_criteria>

<output>
Create `.planning/phases/63-minifilter-spike-groundwork-macos-divergence-ledger-audit/63-01-SUMMARY.md` when done.
</output>
