---
phase: 110-profile-policy-absorb-platform-overrides
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono/src/capability.rs
  - crates/nono/src/sandbox/macos.rs
  - crates/nono/src/sandbox/linux.rs
  - crates/nono-cli/src/exec_strategy.rs
  - crates/nono-cli/src/exec_strategy/supervisor_linux.rs
  - crates/nono-cli/src/supervised_runtime.rs
autonomous: true
requirements: [PROF-03]

must_haves:
  truths:
    - "CapabilitySet can hold inclusive localhost port ranges (start,end) distinct from and additive to the existing discrete localhost_ports list"
    - "Overlapping or adjacent port ranges are merged into a minimal sorted set before any platform expands them into rules"
    - "macOS expands each range into individual Seatbelt rules and refuses (hard error) to apply a capability set whose combined range-port count exceeds 16,384 (MACOS_PORT_RANGE_LIMIT), because sandbox_init() SIGILLs above ~17,770 rules"
    - "Linux expands each range into individual Landlock NetPort rules with no artificial cap — landlock v0.4.4 has no native range rule type, confirmed by inspecting the vendored crate source"
    - "The Linux seccomp proxy-only fallback path (supervisor_linux.rs) accepts bind() on a port that falls inside any configured proxy_bind_port_ranges entry, not just the discrete proxy_bind_ports list"
    - "Existing discrete-port tests (localhost_ports, tcp_connect_ports, tcp_bind_ports) remain green and unmodified"
  artifacts:
    - path: "crates/nono/src/capability.rs"
      provides: "MACOS_PORT_RANGE_LIMIT const, merge_port_ranges(&[(u16,u16)]) -> Vec<(u16,u16)>, localhost_port_ranges field + allow_localhost_port_range/add_localhost_port_range builders + localhost_port_ranges() accessor"
    - path: "crates/nono/src/sandbox/macos.rs"
      provides: "Range-aware Seatbelt emission with the 16,384-port cumulative cap and its SIGILL rationale in a code comment"
    - path: "crates/nono/src/sandbox/linux.rs"
      provides: "Range-aware Landlock NetPort emission, unrolled per-port with no cap, plus new #[test] coverage (Wave-0 gap PROF-03c)"
  key_links:
    - from: "crates/nono/src/sandbox/macos.rs::generate_profile"
      to: "crates/nono/src/capability.rs::merge_port_ranges"
      via: "merge_port_ranges(caps.localhost_port_ranges()) computed once at the top of generate_profile, before the cumulative-limit check"
      pattern: "merge_port_ranges"
    - from: "crates/nono/src/sandbox/linux.rs::apply_with_abi_inner"
      to: "landlock::NetPort::new"
      via: "for port in start..=end { ruleset.add_rule(NetPort::new(port, AccessNet::ConnectTcp)) ... AccessNet::BindTcp }"
      pattern: "NetPort::new(port"
    - from: "crates/nono-cli/src/supervised_runtime.rs::execute_supervised_runtime"
      to: "crates/nono-cli/src/exec_strategy.rs::SupervisorConfig.proxy_bind_port_ranges"
      via: "proxy_bind_port_ranges: caps.localhost_port_ranges().to_vec()"
      pattern: "proxy_bind_port_ranges"
---

<objective>
Absorb the library-mechanism and Unix-emitter portions of upstream `d5803b99` (#1398, port-range
profile schema). Implements the mechanism half of PROF-03 — `CapabilitySet`'s new
`localhost_port_ranges` primitive and both Unix sandbox backends (Seatbelt, Landlock) that consume it,
plus the Linux seccomp proxy-only fallback's range-aware bind check. Profile-schema plumbing
(NetworkConfig fields, capability_ext.rs wiring, manifest layer, Windows WFP emitter) is deliberately
OUT of this plan's scope — see plans 110-04/110-05/110-06.

Purpose: give `CapabilitySet` a caller-supplied port-range mechanism that is ADR-86-compliant (pure
mechanism, no policy — confirmed by CONTEXT.md D-04: this is the same shape as the existing
`localhost_ports` list, just range-typed) and prove it end-to-end on both Unix sandbox backends before
any profile-facing plumbing exists to drive it.

Output: `crates/nono/src/capability.rs` gains `MACOS_PORT_RANGE_LIMIT`, `merge_port_ranges`, and the
`localhost_port_ranges` field with its builder/accessor pair — `CapabilitySet` derives `Default`, so
this addition is purely additive (no exhaustive-literal site forced anywhere, materially lower
regression risk than the `Profile`/`NetworkConfig` changes in plan 110-04). Both Unix sandbox backends
gain range-aware emission, and the Linux seccomp fallback path gains a `proxy_bind_port_ranges`
sibling field. This plan resolves the previously-UNVERIFIED question in `110-PATTERNS.md`/
`110-RESEARCH.md` about whether `landlock` v0.4 exposes a native range API — it does not (confirmed by
reading the vendored `landlock-0.4.4/src/net.rs` source: `NetPort::new(port: u16, access)` takes only a
single `u16`, no range type exists) — so the Linux emitter unrolls each range into per-port `NetPort`
calls, matching upstream's own `d5803b99` implementation exactly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-profile-policy-absorb-platform-overrides/110-CONTEXT.md
@CLAUDE.md

<interfaces>
Ground truth: `git show d5803b99 -- crates/nono/src/capability.rs crates/nono/src/sandbox/macos.rs
crates/nono/src/sandbox/linux.rs crates/nono-cli/src/exec_strategy.rs
crates/nono-cli/src/exec_strategy/supervisor_linux.rs crates/nono-cli/src/supervised_runtime.rs` —
this SHA is reachable locally on the `upstream` remote; every excerpt below was read directly from
that diff and cross-checked against live fork HEAD 2026-07-30.

capability.rs (add near the top of the file, before CapabilitySet's struct definition):

  pub const MACOS_PORT_RANGE_LIMIT: u32 = 1 << 14;  // 16,384

  pub fn merge_port_ranges(ranges: &[(u16, u16)]) -> Vec<(u16, u16)> {
      if ranges.is_empty() { return Vec::new(); }
      let mut sorted = ranges.to_vec();
      sorted.sort_unstable_by_key(|&(s, _)| s);
      let mut merged: Vec<(u16, u16)> = Vec::with_capacity(sorted.len());
      let (mut cur_start, mut cur_end) = sorted[0];
      for &(start, end) in &sorted[1..] {
          if start <= cur_end.saturating_add(1) {
              if end > cur_end { cur_end = end; }
          } else {
              merged.push((cur_start, cur_end));
              cur_start = start;
              cur_end = end;
          }
      }
      merged.push((cur_start, cur_end));
      merged
  }

CapabilitySet gains a new field `localhost_port_ranges: Vec<(u16, u16)>` (sibling to the existing
`localhost_ports: Vec<u16>` field, verified at line 894 today) and:

  pub fn allow_localhost_port_range(mut self, start: u16, end: u16) -> Result<Self> {
      if start == 0 {
          return Err(NonoError::ConfigParse(
              "port range starting at 0 is invalid; port 0 has no defined meaning in a range".to_string(),
          ));
      }
      self.localhost_port_ranges.push((start, end));
      Ok(self)
  }

  pub fn add_localhost_port_range(&mut self, start: u16, end: u16) -> Result<()> {
      // identical zero-start check, mutable sibling of the above
      self.localhost_port_ranges.push((start, end));
      Ok(())
  }

  #[must_use]
  pub fn localhost_port_ranges(&self) -> &[(u16, u16)] {
      &self.localhost_port_ranges
  }

Exact 10 new tests to port verbatim (all present in `d5803b99`'s capability.rs diff):
`test_localhost_port_range_builder`, `test_localhost_port_range_mutable`,
`test_localhost_port_range_rejects_zero_start`, `test_merge_port_ranges_empty`,
`test_merge_port_ranges_no_overlap`, `test_merge_port_ranges_overlapping`,
`test_merge_port_ranges_adjacent`, `test_merge_port_ranges_contained`,
`test_merge_port_ranges_unsorted_input`, `test_merge_port_ranges_three_overlap`.

sandbox/macos.rs — `push_localhost_tcp_outbound_seatbelt_rules` gains a third parameter
`localhost_port_ranges: &[(u16, u16)]` and, after the existing discrete-port loop, a new loop:
`for &(start, end) in localhost_port_ranges { for port in start..=end { profile.push_str(&format!(
"(allow network-outbound (remote tcp \"localhost:{}\"))\n", port)); } }`. `generate_profile` computes
`let merged_ranges = merge_port_ranges(caps.localhost_port_ranges());` at its very top, sums
`(e as u32).saturating_sub(s as u32).saturating_add(1)` across all merged ranges via
`.fold(0u32, |acc, n| acc.saturating_add(n))`, and returns
`Err(NonoError::SandboxInit(format!("port ranges expand to {} unique ports, which exceeds the macOS
limit of {} (sandbox_init crashes above ~17,770 rules); use smaller or fewer ranges", total,
MACOS_PORT_RANGE_LIMIT)))` if that sum exceeds `MACOS_PORT_RANGE_LIMIT` — BEFORE any profile string is
built. `has_localhost_tcp` becomes `!localhost_ports.is_empty() || !merged_ranges.is_empty()`, replacing
bare `!localhost_ports.is_empty()` checks at the 2 sites that gate `system-socket`/bind/inbound allows
(Blocked mode and ProxyOnly mode). Preserve the debug-logging change: replace the old
`debug!("Generated Seatbelt profile:\n{}", profile)` with `debug!("Generated Seatbelt profile ({}
bytes)", profile.len())` (upstream stopped logging the full profile string verbatim — this is an
intentional, unrelated hardening bundled in the same commit; port it too). 7 new tests to port
verbatim: `test_port_range_small_expands_to_individual_rules`, `test_port_range_under_limit_expands`,
`test_port_range_over_limit_returns_error`, `test_cumulative_port_ranges_over_limit_returns_error`,
`test_multiple_port_ranges_all_expand`, `test_port_range_in_proxy_only_mode`.

sandbox/linux.rs — import `merge_port_ranges` alongside the existing capability imports. In
`apply_with_abi_inner`'s existing discrete-port `for port in caps.localhost_ports()` loop block
(verified at lines 862-886 today), add a SIBLING loop immediately after it (NOT replacing it):

  for (start, end) in merge_port_ranges(caps.localhost_port_ranges()) {
      debug!("Adding localhost TCP rules for port range {}..={}", start, end);
      for port in start..=end {
          ruleset = ruleset.add_rule(NetPort::new(port, AccessNet::ConnectTcp))
              .map_err(|e| NonoError::SandboxInit(format!(
                  "Cannot add TCP connect rule for localhost port range {}..={}: {}", start, end, e)))?;
          ruleset = ruleset.add_rule(NetPort::new(port, AccessNet::BindTcp))
              .map_err(|e| NonoError::SandboxInit(format!(
                  "Cannot add TCP bind rule for localhost port range {}..={}: {}", start, end, e)))?;
      }
  }

This is a genuine per-port unroll with NO artificial cap (confirmed: `landlock` v0.4.4's `NetPort::new`
takes only a single `u16`, no range type exists in the vendored crate source at
`net.rs:109-136` — do not attempt to find or invent a native range API, there is none). Upstream added
NO dedicated `#[test]` for this in `linux.rs` itself (confirmed via diff inspection) — this plan's
Task 2 must add original test coverage (Wave-0 gap PROF-03c), modeled on the existing discrete-port
tests in the same file's `mod tests` block.

exec_strategy.rs — `SupervisorConfig` gains, immediately after the existing `#[cfg(target_os =
"linux")] pub proxy_bind_ports: Vec<u16>,` field (verified at line 413):
`#[cfg(target_os = "linux")] pub proxy_bind_port_ranges: Vec<(u16, u16)>,`. Every existing exhaustive
`SupervisorConfig { ... }` test-fixture literal in this file (11 occurrences, all under `#[cfg(target_os
= "linux")] proxy_bind_ports: Vec::new(),`) needs a paired `#[cfg(target_os = "linux")]
proxy_bind_port_ranges: Vec::new(),` line immediately after — the compiler will flag every missed site
as a build error since this struct is NOT `#[derive(Default)]`-constructed at these sites.

exec_strategy/supervisor_linux.rs — `decide_network_notification`'s `SYS_BIND` arm (verified at line
730 today) changes from a single `.contains(&sockaddr.port)` check to:

  let port = sockaddr.port;
  let allowed = config.proxy_bind_ports.contains(&port)
      || config.proxy_bind_port_ranges.iter().any(|&(s, e)| port >= s && port <= e);

3 new tests to port verbatim: `bind_within_port_range_is_allowed`, `bind_outside_port_range_is_denied`,
`bind_allowed_by_individual_port_or_range` (these use a new test helper `make_config_with_ranges`
alongside the existing `make_config` — port both).

supervised_runtime.rs — in `execute_supervised_runtime`'s `SupervisorConfig { ... }` construction
(verified `#[cfg(target_os = "linux")] proxy_bind_ports: ...` around line 298), add immediately after:
`#[cfg(target_os = "linux")] proxy_bind_port_ranges: caps.localhost_port_ranges().to_vec(),`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CapabilitySet mechanism — MACOS_PORT_RANGE_LIMIT, merge_port_ranges, localhost_port_ranges (interface-first)</name>
  <files>crates/nono/src/capability.rs</files>
  <behavior>
    - Builder `allow_localhost_port_range(start, end)` and mutable `add_localhost_port_range(start, end)` both push `(start, end)` and both reject `start == 0` with a ConfigParse error.
    - `localhost_port_ranges()` accessor returns exactly what was pushed, in push order.
    - `merge_port_ranges` correctly handles: empty input, no-overlap, overlapping, adjacent (off-by-one via saturating_add(1)), one-range-contains-another, unsorted input, and a 3-range transitive-overlap chain.
  </behavior>
  <action>
    Add MACOS_PORT_RANGE_LIMIT, merge_port_ranges, the localhost_port_ranges field, and the 3
    builder/accessor methods to capability.rs per the exact shapes in the interfaces section above
    (source of truth: `git show d5803b99 -- crates/nono/src/capability.rs`). Port all 10 named tests
    verbatim into the existing `mod tests` block.
  </action>
  <verify>
    <automated>cargo test -p nono capability -- port_range merge_port_ranges</automated>
  </verify>
  <acceptance_criteria>
    cargo test -p nono capability -- port_range passes (3 CapabilitySet-level tests).
    cargo test -p nono capability -- merge_port_ranges passes (7 merge tests).
    grep -n "pub const MACOS_PORT_RANGE_LIMIT" crates/nono/src/capability.rs shows the value `1 << 14`.
    grep -n "fn allow_localhost_port_range\|fn add_localhost_port_range\|fn localhost_port_ranges" crates/nono/src/capability.rs returns 3 hits.
    Existing discrete-port tests (test_tcp_connect_ports, test_tcp_bind_ports, and any localhost_ports
    test) still pass unmodified: cargo test -p nono capability -- localhost_ports tcp_connect_ports
    tcp_bind_ports exits 0.
  </acceptance_criteria>
  <done>CapabilitySet has a working, tested, additive localhost_port_ranges mechanism with zero regression to the existing discrete-port API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: macOS Seatbelt range emitter + cumulative cap</name>
  <files>crates/nono/src/sandbox/macos.rs</files>
  <behavior>
    - A small range (e.g. 3000-3002) expands to individual `localhost:N` outbound rules for every port in the range, and does NOT emit a `localhost:*` wildcard rule.
    - A range under the 16,384 cap expands successfully.
    - A single range whose port count exceeds 16,384 makes generate_profile return an Err whose message contains "exceeds the macOS limit".
    - Two ranges whose COMBINED (not individual) port count exceeds 16,384 also error — the cap is cumulative across all ranges, not per-range.
    - Multiple small ranges all expand correctly together.
    - A range combined with ProxyOnly mode still emits the expected bind/inbound allows.
  </behavior>
  <action>
    Extend push_localhost_tcp_outbound_seatbelt_rules to accept and emit localhost_port_ranges per the
    interfaces section above. Add the cumulative-limit check as the FIRST statement in generate_profile
    (before any profile string is built), with the SIGILL rationale preserved verbatim in the error
    message and a code comment (CONTEXT.md &lt;specifics&gt;: "a bare magic number invites removal" —
    do not strip the ~17,770-rule rationale when porting). Update has_localhost_tcp at both gating call
    sites. Port the debug! logging change (byte-count only, not the full profile string). Port all 6
    named tests verbatim.
  </action>
  <verify>
    <automated>cargo test -p nono sandbox::macos -- port_range</automated>
  </verify>
  <acceptance_criteria>
    cargo test -p nono sandbox::macos -- port_range passes all 6 ported tests.
    grep -n "MACOS_PORT_RANGE_LIMIT\|SIGILL\|17,770\|17770" crates/nono/src/sandbox/macos.rs returns at
    least 2 hits (the const usage AND a human-readable rationale comment/error string — not just the
    bare number).
    grep -n "Generated Seatbelt profile.*bytes\|profile.len()" crates/nono/src/sandbox/macos.rs returns
    1 hit (full-profile-string debug logging was replaced, not left duplicated).
    Existing macOS discrete-port tests (test_generate_profile_blocked_with_localhost_ports and siblings)
    still pass unmodified.
  </acceptance_criteria>
  <done>macOS emits range-aware Seatbelt rules, enforces the cumulative 16,384-port cap with the SIGILL rationale preserved in code, and all discrete-port behavior is unregressed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Linux Landlock range emitter (no cap) + seccomp proxy-bind-range fallback (closes Wave-0 gap PROF-03c)</name>
  <files>crates/nono/src/sandbox/linux.rs, crates/nono-cli/src/exec_strategy.rs, crates/nono-cli/src/exec_strategy/supervisor_linux.rs, crates/nono-cli/src/supervised_runtime.rs</files>
  <behavior>
    - A localhost_port_ranges entry adds a Landlock NetPort ConnectTcp+BindTcp rule for EVERY port in
      the range (verify at least the boundary ports — start, end, and one interior port — are present;
      landlock's own add_rule has no batch/range API to assert against directly, so assert via the
      ruleset's resulting sandbox behavior or via a lower-level rule-count/port-membership check,
      whichever the file's existing discrete-port test pattern uses).
    - No artificial cap: a large range (e.g. spanning several thousand ports) does not error the way
      macOS's cumulative check would.
    - supervisor_linux.rs: bind() on a port strictly inside a configured proxy_bind_port_ranges entry
      is Allowed; bind() one port below the range start or one port above the range end is Denied; a
      port allowed via EITHER the discrete list OR a range is Allowed (union semantics).
  </behavior>
  <action>
    In sandbox/linux.rs, import merge_port_ranges alongside the existing capability imports and add the
    per-port unroll loop (both ConnectTcp and BindTcp rules per port) immediately after the existing
    discrete-port loop in apply_with_abi_inner, per the interfaces section above — this is confirmed
    the only viable shape since landlock v0.4.4's NetPort::new takes a single u16 with no range
    variant (verified by reading the vendored crate source at
    $USERPROFILE/.cargo/registry/src/index.crates.io-*/landlock-0.4.4/src/net.rs:109-136 — re-confirm
    this yourself before implementing, per CONTEXT.md's "verify by behavior, confirm the file exists"
    rule, do not assume the version pinned in Cargo.lock is still 0.4.4 without checking). Add original
    test coverage in linux.rs's mod tests block proving range expansion (Wave-0 gap PROF-03c — there is
    no upstream test to port here, upstream's own diff added none).
    In exec_strategy.rs, add the proxy_bind_port_ranges field to SupervisorConfig and update all 11
    exhaustive test-fixture literals. In supervisor_linux.rs, extend the SYS_BIND arm's allow check per
    the interfaces section, port the 3 named tests plus the make_config_with_ranges test helper. In
    supervised_runtime.rs, populate proxy_bind_port_ranges from caps.localhost_port_ranges().to_vec()
    at the SupervisorConfig construction site.
  </action>
  <verify>
    <automated>cargo test -p nono sandbox::linux -- port_range && cargo test -p nono-cli exec_strategy::supervisor_linux -- port_range</automated>
  </verify>
  <acceptance_criteria>
    cargo test -p nono sandbox::linux -- port_range passes (new tests, closing Wave-0 gap PROF-03c).
    cargo test -p nono-cli -- bind_within_port_range_is_allowed exits 0.
    cargo test -p nono-cli -- bind_outside_port_range_is_denied exits 0.
    cargo test -p nono-cli -- bind_allowed_by_individual_port_or_range exits 0.
    cargo build --workspace --all-targets exits 0 (every exhaustive SupervisorConfig literal updated).
    grep -c "proxy_bind_port_ranges: Vec::new()" crates/nono-cli/src/exec_strategy.rs returns 11 (all
    existing test fixtures paired).
    grep -n "landlock.*range\|NetPort.*range" crates/nono/src/sandbox/linux.rs shows NO native-range API
    usage — confirm the implementation is a per-port loop, not a range-typed rule call (grep for `for
    port in start..=end` inside the new block).
  </acceptance_criteria>
  <done>Linux emits per-port Landlock rules for every port in a range with no artificial cap, and the seccomp proxy-only fallback path accepts bind() on any port within a configured range, in addition to the existing discrete-port list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| CapabilitySet caller (CLI, not yet wired in this plan) -> library mechanism | The library trusts whatever (start,end) tuples the CLI supplies; validation of profile-sourced values happens CLI-side in plan 110-04 |
| Sandboxed child process -> kernel enforcement (Seatbelt/Landlock) | The two Unix backends are the actual enforcement boundary; a bug here is a real confinement gap, not cosmetic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-07 | Denial of Service (local, self-inflicted) | `sandbox/macos.rs::generate_profile` | mitigate | cumulative range-port count checked against `MACOS_PORT_RANGE_LIMIT` (2^14) BEFORE any Seatbelt profile is built, preserving the empirically-determined SIGILL rationale (`sandbox_init()` crashes above ~17,770 rules) in both the error message and a code comment |
| T-110-08 | Elevation of Privilege | `capability.rs::allow_localhost_port_range` / `add_localhost_port_range` | mitigate | `start == 0` is rejected (port 0 has no defined range meaning; the discrete-port wildcard semantics stay on `allow_localhost_port(0)` only) — ported verbatim, tested |
| T-110-09 | Elevation of Privilege | `sandbox/linux.rs` per-port unroll | accept | no cumulative cap on Linux is a deliberate upstream design choice (Landlock accepts the full 1-65535 space) — accepted because it matches upstream's own posture and Landlock has no equivalent crash mode to macOS's Seatbelt compiler |
| T-110-SC | Tampering | npm/pip/cargo installs | mitigate | zero new external dependencies; `windows-sys`/`landlock` versions unchanged by this plan |
</threat_model>

<verification>
`cargo build --workspace --all-targets` exits 0 after each task.
`cargo test -p nono capability sandbox::macos sandbox::linux` passes.
`cargo test -p nono-cli exec_strategy exec_strategy::supervisor_linux` passes.
`cargo fmt --all -- --check` passes.
This plan's files trigger D-12 (cross-target clippy mandatory) — the phase-gate plan (110-08) is
responsible for running `cross clippy`/`cargo-zigbuild clippy`; this plan's own verification is
build+test only.
</verification>

<success_criteria>
`CapabilitySet` has a tested `localhost_port_ranges` mechanism; both macOS and Linux sandbox backends
expand ranges into kernel-enforced rules (Seatbelt capped at 16,384 with its crash rationale
preserved, Landlock uncapped); the Linux seccomp proxy-only fallback recognizes range-based bind
allowances; existing discrete-port behavior is completely unregressed.
</success_criteria>

<output>
Create `.planning/phases/110-profile-policy-absorb-platform-overrides/110-03-SUMMARY.md` when done
</output>
