---
phase: 110-profile-policy-absorb-platform-overrides
plan: 08
type: execute
wave: 4
depends_on: ["110-01", "110-02", "110-03", "110-04", "110-05", "110-06", "110-07"]
files_modified:
  - ../nono-py/Cargo.toml
  - ../nono-ts/Cargo.toml
autonomous: true
requirements: [PROF-01, PROF-02, PROF-03, PROF-04]

must_haves:
  truths:
    - "Both cross-target clippy gates (cross linux-gnu + cargo-zigbuild apple-darwin) exit 0 for -D warnings -D clippy::unwrap_used, confirmed live on this dev host, not assumed or defaulted to PARTIAL"
    - "make ci (clippy + fmt + tests) is fully green"
    - "Both sibling binding repos (../nono-py, ../nono-ts) build green against this phase's capability.rs field addition, confirmed by actually running maturin build / napi build, not static inspection"
    - "Discrete-Vec<u16> back-compat holds: every pre-existing localhost_ports/tcp_connect_ports/tcp_bind_ports test across the phase's touched files passes unmodified (SC3's explicit requirement)"
    - "The ADR-86 policy-free-library boundary is confirmed unregressed: capability.rs's new mechanism carries no policy/enforcement decision, only caller-supplied-list plumbing"
  artifacts:
    - path: ".planning/phases/110-profile-policy-absorb-platform-overrides/110-08-VERIFICATION-NOTES.md"
      provides: "Recorded live evidence for both cross-target clippy gates, make ci, and both binding rebuilds — not a claim, a paste of actual command output"
  key_links:
    - from: "this plan's Task 2"
      to: "../nono-py (maturin build) and ../nono-ts (napi build --platform --release)"
      via: "real build commands run against this phase's actual capability.rs/nono-proxy changes, not static inspection (D-13)"
      pattern: "maturin build|napi build"
---

<objective>
Phase-gate verification for Phase 110. Runs both MANDATORY cross-target clippy gates (D-12), the full
`make ci` suite, rebuilds both sibling language bindings (D-13), and performs a fork-invariant pass
confirming discrete-port back-compat (SC3) and the ADR-86 policy-free-library boundary. This plan adds
no new features — it is the closing proof that plans 110-01 through 110-07 together satisfy the
phase's four success criteria without regressing the Windows/Linux/macOS security model.

Purpose: this phase touches `crates/nono/src/sandbox/macos.rs`, `crates/nono/src/sandbox/linux.rs`,
and `crates/nono-cli/src/exec_strategy/supervisor_linux.rs` — all cfg-gated-Unix scope per
`.planning/templates/cross-target-verify-checklist.md` — making cross-target clippy MANDATORY, not the
PARTIAL→CI fallback. `capability.rs` gains a field consumed by both sibling binding repos; Phase 109
proved this drift is real (`E0063` on `../nono-py`), so this plan verifies by building, not assuming.

Output: a `110-08-VERIFICATION-NOTES.md` artifact pasting the actual command output for both
cross-target gates, `make ci`, and both binding builds — satisfying the "confirmed live, not assumed"
Wave-0/verification-gate requirement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-profile-policy-absorb-platform-overrides/110-CONTEXT.md
@.planning/phases/110-profile-policy-absorb-platform-overrides/110-VALIDATION.md
@.planning/templates/cross-target-verify-checklist.md
@CLAUDE.md

<interfaces>
Cross-target commands (verbatim from `.planning/templates/cross-target-verify-checklist.md` and
CLAUDE.md's cross-target-clippy bullet — do not substitute a bare `cargo clippy --target ...`, that is
structurally blind to cfg-gated Unix code on this Windows host):
```
cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used
cargo-zigbuild clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used
```
Both are documented as local-runnable on this dev host (established Phase 96, reconfirmed by every
subsequent phase through 109). PARTIAL→CI is the fallback ONLY on a documented runner failure (a
stopped Docker daemon or an absent-but-installable tool does NOT qualify) — read the checklist's
decision tree before choosing that fallback, and if chosen, name the SPECIFIC documented failure.

Binding rebuild commands (D-13):
```
cd ../nono-py && maturin build
cd ../nono-ts && napi build --platform --release
```
Both sibling repos are OUTSIDE this Cargo workspace (path-deps by relative path) — `cargo build
--workspace` never touches them. This phase's `crates/nono/src/capability.rs` gains a new
`localhost_port_ranges` field; `CapabilitySet` derives `Default` so no exhaustive-literal site is
force-updated anywhere in THIS workspace, but the sibling bindings may still construct
`CapabilitySet`/manifest-adjacent types by hand and could silently miss the new field's accessor
surface — the only way to know is to actually run these builds, per Phase 109's `E0063` precedent on
`../nono-py`.

Fork-invariant checks to perform and record (not new code — a documented review pass):
1. SC3 (discrete-Vec&lt;u16&gt; back-compat): grep for every pre-existing `localhost_ports`/
   `tcp_connect_ports`/`tcp_bind_ports` test name across `crates/nono/src/capability.rs`,
   `crates/nono/src/sandbox/{macos,linux,windows,mod}.rs` and confirm NONE were edited by this phase's
   plans (git diff each file against the phase's start point, or `git log --oneline` per file since
   phase start, looking for any hunk touching a pre-existing discrete-port test body).
2. ADR-86 boundary: confirm `crates/nono/src/capability.rs`'s new mechanism
   (`localhost_port_ranges`/`merge_port_ranges`/`allow_localhost_port_range`) contains no policy
   decision — it is a pure, caller-supplied-list builder, identical in shape to the pre-existing
   `localhost_ports` mechanism it sits beside. Confirm no group/deny-list/dangerous-command logic
   leaked into the library from this phase's CLI-side plans (110-01, 110-04, 110-05, 110-07 all touch
   `crates/nono-cli/`, never `crates/nono/`, except for the mechanism itself in 110-03 and the Windows
   compile_network_policy touch in 110-06 — both are policy-free per D-04/D-07).
3. Confirm the 4 Wave-0 gaps named in `110-VALIDATION.md` are all closed: `sandbox/linux.rs` port-range
   test coverage (110-03), `nono-wfp-service.rs` range test coverage (110-06), schema-validation-plus-
   resolution tests for `platform_overrides`/`open_port_range`/`listen_port_range` (110-01/110-04),
   and "resolve `bun-dev`/`mise-dev` by name" tests (110-07).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-target clippy (both gates, mandatory) + make ci</name>
  <files>.planning/phases/110-profile-policy-absorb-platform-overrides/110-08-VERIFICATION-NOTES.md</files>
  <read_first>
    .planning/templates/cross-target-verify-checklist.md (the decision tree — read it before running anything, especially the PARTIAL→CI fallback conditions)
    CLAUDE.md (cross-target clippy MUST/NEVER bullet)
  </read_first>
  <action>
    Run `cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D
    clippy::unwrap_used` and `cargo-zigbuild clippy --workspace --target x86_64-apple-darwin -- -D
    warnings -D clippy::unwrap_used`. Fix any lint findings in this phase's own changed files (do not
    silence with `#[allow(...)]` unless an existing project convention already does so for the exact
    same lint class elsewhere in the touched file). Run `make ci` (or its constituent
    `cargo clippy --workspace --all-targets`, `cargo fmt --all -- --check`,
    `cargo test --workspace` commands if `make` is unavailable on this host — confirmed absent from
    PATH in prior phases per project memory, substitute accordingly). Create
    `110-08-VERIFICATION-NOTES.md` pasting the actual terminal output (or a faithful summary with exit
    codes) for all three commands.
  </action>
  <verify>
    <automated>cross clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used</automated>
  </verify>
  <acceptance_criteria>
    Both cross-target clippy commands exit 0, with their exit codes and a truncated output tail
    recorded in 110-08-VERIFICATION-NOTES.md.
    `cargo test --workspace` (or `make test` if available) exits 0.
    `cargo fmt --all -- --check` exits 0.
    110-08-VERIFICATION-NOTES.md exists and contains evidence for all 3 gates, not a bare "PASSED"
    claim with no output.
    If PARTIAL→CI was invoked for either gate, the file names the SPECIFIC documented runner failure
    (per the checklist's decision tree) — a missing/uninstalled tool does not qualify.
  </acceptance_criteria>
  <done>Both cross-target clippy gates and make ci are confirmed GREEN with recorded live evidence, not assumed.</done>
</task>

<task type="auto">
  <name>Task 2: Binding rebuild (D-13) + fork-invariant review (SC3, ADR-86, Wave-0 gap closure)</name>
  <files>../nono-py/Cargo.toml, ../nono-ts/Cargo.toml, .planning/phases/110-profile-policy-absorb-platform-overrides/110-08-VERIFICATION-NOTES.md</files>
  <read_first>
    ../nono-py/src/policy.rs, ../nono-py/src/sandboxed_exec.rs, ../nono-py/src/windows_confined_run.rs (any hand-written CapabilitySet/manifest-adjacent construction sites — grep for localhost_port/PortConfig usage)
    ../nono-ts/src/lib.rs (same check for the TypeScript binding)
  </read_first>
  <action>
    Run `maturin build` inside `../nono-py` and `napi build --platform --release` inside `../nono-ts`.
    If either fails on a struct-drift error (e.g. an `E0063` missing-field error mirroring Phase 109's
    finding), fix the sibling repo's construction site with the minimal additive fix (add the missing
    field with a sensible default, matching the Phase 109/100 precedent of adding a no-op-compatible
    default rather than threading full functionality into the binding in this same commit) and re-run
    until green. Perform the 3 fork-invariant checks named in the interfaces section (SC3 back-compat,
    ADR-86 boundary, Wave-0 gap closure) and record the findings in
    110-08-VERIFICATION-NOTES.md.
  </action>
  <verify>
    <automated>cd ../nono-py && maturin build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    `maturin build` in ../nono-py exits 0.
    `napi build --platform --release` in ../nono-ts exits 0.
    110-08-VERIFICATION-NOTES.md records, for each of the 3 fork-invariant checks, a concrete
    grep/diff-based finding (not a bare assertion) — e.g. the exact grep command and output confirming
    zero edits to pre-existing discrete-port test bodies.
    110-08-VERIFICATION-NOTES.md explicitly lists all 4 Wave-0 gaps from 110-VALIDATION.md and, for
    each, names the plan/task that closed it and the automated test command proving closure.
    If either sibling repo required a fix, that fix is committed in ITS OWN repo with its own DCO-signed
    commit (per the Phase 102/109 precedent — sibling repos are outside this workspace's git history).
  </acceptance_criteria>
  <done>Both bindings build green against this phase's actual changes, and the phase's fork-invariant posture (SC3 back-compat, ADR-86 boundary, Wave-0 gap closure) is documented with live evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| This workspace -> sibling binding repos | Struct-shape drift between `crates/nono`/`crates/nono-proxy` and the Python/TypeScript bindings is a real, previously-observed failure mode (Phase 109's `E0063`), not a hypothetical |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-20 | Tampering | cross-target clippy PARTIAL→CI misuse | mitigate | this plan's acceptance criteria require a NAMED documented runner failure if PARTIAL→CI is invoked — a missing/absent tool is explicitly disqualified per the checklist's decision tree |
| T-110-21 | Elevation of Privilege | sibling-repo struct drift silently shipping a stale binding | mitigate | both bindings are rebuilt via a REAL build command, not static inspection — a compile error surfaces drift immediately rather than shipping silently |
| T-110-SC | Tampering | npm/pip/cargo installs | mitigate | no new package installs in this plan; `maturin`/`napi` are pre-existing toolchain components already used by prior phases |
</threat_model>

<verification>
Both cross-target clippy gates exit 0 (recorded, not assumed).
`make ci` (or its constituent commands) exits 0.
`maturin build` and `napi build --platform --release` both exit 0.
All 4 Wave-0 validation gaps confirmed closed with a named test command per gap.
</verification>

<success_criteria>
Phase 110 is provably non-regressing: both cross-target clippy gates are GREEN, `make ci` is GREEN,
both sibling bindings build against this phase's real changes, discrete-port back-compat is unbroken,
the ADR-86 boundary holds, and every Wave-0 validation gap named in `110-VALIDATION.md` is closed with
a named, passing automated test.
</success_criteria>

<output>
Create `.planning/phases/110-profile-policy-absorb-platform-overrides/110-08-SUMMARY.md` when done
</output>
