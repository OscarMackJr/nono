---
phase: 56-fine-grained-network-filtering
plan: 04
type: execute
wave: 3
depends_on:
  - 56-02
  - 56-03
files_modified:
  - crates/nono-cli/src/query_ext.rs
  - crates/nono-cli/src/why_runtime.rs
autonomous: true
requirements:
  - REQ-NET-01

must_haves:
  truths:
    - "nono why --host api.github.com lists allowed path-prefixes and methods when the domain has scoped entries (SC3, D-11: nono why --host must list each allowed path-prefix and its allowed methods; D-01: domain_endpoints derive from AllowDomainEntry::WithEndpoints)"
    - "nono why --host https://api.github.com/repos/my-org/** parses the URL and shows an endpoint-restricted verdict (SC3, D-09: percent-decode + canonicalize path before matching; ambiguity -> DENY)"
    - "query_network accepts domain_endpoints and populates endpoint_rules in QueryResult::Allowed"
    - "credential.rs SHA prefix matches c9f25164 (byte-identical, unchanged throughout Phase 56, D-04: credential.rs is a Phase 09/11 invariant -- nothing in this phase may touch it)"
    - "All three ported commits carry Upstream-commit trailers: 0ced085, 75b2265, 22e6c40"
    - "Full test suite make test passes"
  artifacts:
    - path: "crates/nono-cli/src/query_ext.rs"
      provides: "parse_host_input fn, path_matches_endpoint_rules fn, query_network extended with domain_endpoints param, QueryResult::Allowed with endpoint_rules field"
      contains: "parse_host_input"
    - path: "crates/nono-cli/src/why_runtime.rs"
      provides: "Updated resolve_allowed_domains to also return domain_endpoints; WhyContext gains domain_endpoints field; query_network call passes domain_endpoints"
      contains: "domain_endpoints"
  key_links:
    - from: "crates/nono-cli/src/why_runtime.rs"
      to: "crates/nono-cli/src/query_ext.rs"
      via: "query_network call passes domain_endpoints; parse_host_input called to split URL input"
      pattern: "parse_host_input"
    - from: "crates/nono-cli/src/query_ext.rs"
      to: "crates/nono-cli/src/sandbox_state.rs"
      via: "query_network accepts &[DomainEndpointState]; endpoint_rules populated from matching entry"
      pattern: "DomainEndpointState"
---

<objective>
Implement SC3: extend `nono why --host` to surface path/method scoping rules when the host has endpoint-restricted entries. This requires extending query_ext.rs with parse_host_input + path_matches_endpoint_rules + query_network signature update + QueryResult::Allowed endpoint_rules field, and updating why_runtime.rs to derive and pass domain_endpoints.

After this plan, the full workspace compiles, the test suite passes, and the Phase 56 success criteria are verifiable. The plan closes with a verification sweep: credential.rs byte-identical check, Upstream-commit trailer audit, cross-target clippy gate documentation, and full make test.

Purpose: SC3 is the user-visible diagnostic face of the feature. Without it the filter works but operators cannot inspect what rules are active for a host.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/56-fine-grained-network-filtering/56-02-SUMMARY.md
@.planning/phases/56-fine-grained-network-filtering/56-03-SUMMARY.md
@.planning/phases/56-fine-grained-network-filtering/56-RESEARCH.md
@.planning/phases/56-fine-grained-network-filtering/56-PATTERNS.md
@.planning/phases/56-fine-grained-network-filtering/56-VALIDATION.md

<interfaces>
<!-- Types needed in this plan -->

From crates/nono-cli/src/sandbox_state.rs (Plan 01 output):
  pub struct DomainEndpointState { pub domain: String, pub endpoints: Vec<EndpointRuleState> }
  pub struct EndpointRuleState { pub method: String, pub path: String }

From crates/nono-cli/src/query_ext.rs (current state — to be extended):
  pub enum QueryResult { Allowed { reason, granted_path, access, source }, Denied { ... }, ... }
  pub fn query_network(host: &str, port: u16, caps: &CapabilitySet, allowed_domains: &[String]) -> QueryResult

Current query_network allowed arm (line ~260):
  nono::net_filter::FilterResult::Allow => QueryResult::Allowed { reason: "host is in allow list", granted_path: None, access: None, source: None }

From crates/nono-cli/src/why_runtime.rs (current state — to be extended):
  struct WhyContext { caps, overridden_paths, allowed_domains: Vec<String> }
  fn resolve_allowed_domains(profile: &Profile) -> Vec<String>  (uses expand_proxy_allow — needs domain_endpoints companion)
  query_network(host, args.port, &ctx.caps, &ctx.allowed_domains)  (line ~159)

Upstream patterns to port (sources: 75b2265 and 0ced085):
  parse_host_input(host: &str) -> (String, Option<String>) — see PATTERNS.md §query_ext.rs
  path_matches_endpoint_rules(path: Option<&str>, endpoints: &[EndpointRuleState]) -> bool
    (uses globset::Glob or equivalent; for nono why display this is the diagnostic helper — it does NOT run the enforce path, just checks if the path would match)
  QueryResult::Allowed gains: #[serde(skip_serializing_if="Option::is_none")] endpoint_rules: Option<Vec<EndpointRuleDisplay>>
    where EndpointRuleDisplay { method: String, path: String } — a display-only struct
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: query_ext.rs — parse_host_input + path_matches_endpoint_rules + query_network extension + QueryResult::Allowed endpoint_rules</name>
  <files>crates/nono-cli/src/query_ext.rs</files>
  <read_first>
    - crates/nono-cli/src/query_ext.rs — read lines 44-90 (QueryResult enum with all variants and their fields), lines 235-295 (query_network function body), and lines 383-455 (print_result function, specifically the Allowed arm). Extract: the exact fields on QueryResult::Allowed, the AllowAll arm logic, and the print_result Allowed arm format.
    - git show 75b2265 -- crates/nono-cli/src/query_ext.rs — diff-inspect for parse_host_input, path_matches_endpoint_rules, query_network signature + body changes, and the endpoint_rules display block in print_result.
    - git show 0ced085 -- crates/nono-cli/src/query_ext.rs — diff-inspect for any additional query_ext changes in the foundation commit (DomainEndpointState usage, query_network domain_endpoints param).
    - .planning/phases/56-fine-grained-network-filtering/56-PATTERNS.md — §query_ext.rs (parse_host_input, path_matches_endpoint_rules analog, QueryResult::Allowed extension, print_result extension, query_network signature).
    - crates/nono-proxy/src/config.rs — grep `fn is_loopback_domain\|fn normalize_path\|fn is_allowed` to confirm the exact normalize_path and is_allowed patterns used by the proxy enforcement layer (the diagnostic helper mirrors this behavior but is non-enforcing).
  </read_first>
  <behavior>
    - parse_host_input("api.github.com") → ("api.github.com", None)
    - parse_host_input("https://api.github.com/repos/my-org/**") → ("api.github.com", Some("/repos/my-org/**"))
    - parse_host_input("https://api.github.com/") → ("api.github.com", None) — root path → None
    - query_network("api.github.com", 443, caps, allowed_domains, domain_endpoints_with_rules) returns QueryResult::Allowed with endpoint_rules: Some([{method, path}...]) when host is allowed and has scoped entries
    - query_network("api.github.com", 443, caps, allowed_domains, []) returns QueryResult::Allowed with endpoint_rules: None (no scoping, backward compatible)
    - print_result for Allowed with endpoint_rules: Some outputs "Endpoint rules:" followed by one "  METHOD path" line per rule
    - Wave 0 gap tests for parse_host_input and path_matches_endpoint_rules pass (port from 75b2265)
  </behavior>
  <action>
    In crates/nono-cli/src/query_ext.rs:

    (a) Add imports: `use crate::sandbox_state::DomainEndpointState;` and `use globset::Glob;` (globset is now in nono-cli/Cargo.toml from Plan 01).

    (b) Add display-only struct `EndpointRuleDisplay { pub method: String, pub path: String }` — derive Debug, Clone, Serialize, Deserialize. This is the display-only type, separate from proxy's EndpointRule.

    (c) Add `endpoint_rules: Option<Vec<EndpointRuleDisplay>>` field to `QueryResult::Allowed` variant with `#[serde(skip_serializing_if = "Option::is_none")]`. All existing Allowed construction sites gain `endpoint_rules: None` — this is backward-compatible.

    (d) Add `pub fn parse_host_input(host: &str) -> (String, Option<String>)` (port from 75b2265 — PATTERNS.md §query_ext.rs). Uses `url::Url::parse(host).host_str()` to extract domain; path is None if empty or "/", Some(path) otherwise.

    (e) Add `fn path_matches_endpoint_rules(path: Option<&str>, endpoints: &[crate::sandbox_state::EndpointRuleState]) -> bool` — diagnostic helper (NOT enforcement). For display purposes: if no path provided, return true (URL has no path → show all rules). If path provided, normalize the path before glob matching to mirror the proxy's `normalize_path` behavior: (1) percent-decode via `urlencoding::decode(path).unwrap_or_default()` (or `urlencoding::decode_binary` + `from_utf8_lossy`), (2) collapse duplicate slashes and empty segments by splitting on `/` and filtering empty parts (same logic as `config.rs::normalize_path`). Then check if any endpoint's path glob matches the normalized path via `globset::Glob::new(&e.path).ok()?.compile_matcher().is_match(&normalized)`. Add a code comment: `// Display approximation of proxy enforcement — diverges from CompiledEndpointRules::is_allowed in edge cases (ad-hoc Glob vs GlobMatcher compilation); diagnostic only, not access control.` This helper only affects what rules are shown in the output, not what the proxy enforces.

    (f) Update `query_network` signature to add `domain_endpoints: &[DomainEndpointState]` as a 5th parameter. In the AllowAll and ProxyFilter::Allow arms that produce `QueryResult::Allowed`, look up domain in domain_endpoints; if found and not empty, populate `endpoint_rules: Some(entries.iter().map(|r| EndpointRuleDisplay{method:r.method.clone(), path:r.path.clone()}).collect())`. If not found → `endpoint_rules: None`.

    (g) In `print_result`, in the `QueryResult::Allowed` arm: if `endpoint_rules` is Some and non-empty, after printing the existing allowed reason, print:
      println!("  Endpoint rules:");
      for r in rules { println!("    {} {}", r.method, r.path); }

    (h) Port Wave 0 gap tests for parse_host_input + path_matches_endpoint_rules from upstream 75b2265 into the #[cfg(test)] mod.

    (i) Update the existing `query_network` call sites in query_ext.rs (if any test call sites pass only 4 args) to pass &[] as the 5th arg.

    D-11: The endpoint_rules display in print_result satisfies `nono why --host <host>` showing path/method scoping rules (SC3). The WATCH-ITEM (D-12 most-permissive-wins visibility) is handled by showing all effective endpoint_rules — if a host has a bare entry, endpoint_rules will be None (no scoping visible), which is correct.
  </action>
  <verify>
    <automated>cargo test -p nono-cli -- parse_host_input query_network endpoint 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `grep "fn parse_host_input" crates/nono-cli/src/query_ext.rs` shows the function
    - `grep "fn path_matches_endpoint_rules" crates/nono-cli/src/query_ext.rs` shows the function
    - `grep "endpoint_rules" crates/nono-cli/src/query_ext.rs` shows the field in QueryResult::Allowed and the print_result block
    - `grep "domain_endpoints" crates/nono-cli/src/query_ext.rs` shows the 5th parameter in query_network
    - Tests for parse_host_input (bare host, URL with path, root URL) pass
    - Tests for path_matches_endpoint_rules (matching, non-matching, no path) pass
    - Tests for query_network with domain_endpoints (host with rules shows them, host without shows None) pass
    - Commit carries Upstream-commit: 75b2265 trailer (primary source for this function set) and DCO Signed-off-by
  </done>
</task>

<task type="auto">
  <name>Task 2: why_runtime.rs extension + full verification sweep</name>
  <files>crates/nono-cli/src/why_runtime.rs</files>
  <read_first>
    - crates/nono-cli/src/why_runtime.rs — read the full file (lines 1-180). Extract: WhyContext struct fields, resolve_allowed_domains function body, all query_network call sites, and where profile.network.allow_domain is accessed.
    - git show 0ced085 -- crates/nono-cli/src/why_runtime.rs — diff-inspect for the exact shape upstream uses to extend resolve_allowed_domains / WhyContext / query_network call.
    - git show 75b2265 -- crates/nono-cli/src/why_runtime.rs — diff-inspect for parse_host_input usage in the args.host processing path (why_runtime may call parse_host_input to split a URL arg).
    - .planning/phases/56-fine-grained-network-filtering/56-PATTERNS.md — §why_runtime.rs (WhyContext extension, resolve_proxy_context pattern, query_network call update).
  </read_first>
  <action>
    In crates/nono-cli/src/why_runtime.rs:

    (a) Add `domain_endpoints: Vec<crate::sandbox_state::DomainEndpointState>` to `WhyContext` struct.

    (b) Extend `resolve_allowed_domains` — rename or add a companion function to also return domain_endpoints. The simplest approach (follow upstream 0ced085 shape): modify the function to return a tuple `(Vec<String>, Vec<DomainEndpointState>)`, or add a separate `resolve_domain_endpoints(profile) -> Vec<DomainEndpointState>` helper. Implement by iterating profile.network.allow_domain, extracting WithEndpoints entries into DomainEndpointState values.

    (c) Update the current `resolve_allowed_domains(profile)` call site in run_why (where WhyContext is constructed from a profile) to also populate domain_endpoints.

    (d) Where WhyContext is constructed from a sandbox state file (the --self path), populate domain_endpoints from the loaded SandboxState.domain_endpoints field.

    (e) Update all query_network call sites in run_why to pass `&ctx.domain_endpoints` as the 5th argument (e.g., line ~159: `query_network(host, args.port, &ctx.caps, &ctx.allowed_domains, &ctx.domain_endpoints)`).

    (f) If why_runtime processes a URL-style `--host` arg (e.g., `nono why --host https://api.github.com/repos/**`), call `parse_host_input(&args.host)` to split domain from path — pass the domain to query_network as `host`. Check the 75b2265 diff for why_runtime.rs to see if it adds this or if the host extraction already happens elsewhere.

    After all edits: run `cargo check --bin nono 2>&1 | grep "^error"` — expect zero errors across the full workspace (Plans 01-04 are now all applied).
    Then run `make test 2>&1 | tail -30` — expect full suite green.

    VERIFICATION SWEEP (part of this task — document results in commit message or SUMMARY):

    SC2 structural check: `grep -n "endpoint_rules\|is_allowed\|credential" crates/nono-proxy/src/reverse.rs | head -15` — confirm endpoint check line number precedes credential lookup line number (no reordering occurred; this was already satisfied structurally before Phase 56, per RESEARCH.md).

    SC4 credential.rs invariant: `git show HEAD:crates/nono-proxy/src/credential.rs | sha256sum` — the output SHA must have prefix c9f25164 (first 8 hex chars). If it does not match, STOP — credential.rs was modified unexpectedly. Report immediately.

    SC4 Upstream-commit trailers: `git log --oneline --grep="Upstream-commit" -10` — must show entries for 0ced085, 75b2265, 22e6c40.

    Cross-target clippy gate (CLAUDE.md MUST): The AllowDomainEntry type and all 12 changed files are in platform-generic code (no cfg(unix)/cfg(macos) gating). However, execution_runtime.rs and why_runtime.rs may have cfg-gated Unix branches. After this plan, run:
      `cargo clippy --bin nono -- -D warnings -D clippy::unwrap_used 2>&1 | tail -20` (Windows host)
    For cross-target Linux/macOS: attempt `cargo clippy --target x86_64-unknown-linux-gnu --bin nono -- -D warnings -D clippy::unwrap_used 2>&1 | head -5`. If cross-toolchain not installed (expected on Windows dev host), mark REQ-NET-01 verification as PARTIAL for cross-target, record in SUMMARY with `deferred-to-CI` status per CLAUDE.md cross-target verify checklist at `.planning/templates/cross-target-verify-checklist.md`.

    D-03 disposition documentation: In the 22e6c40 rider commit message (already applied in Plan 02), confirm the commit body notes: "Fork-preserve: upstream tls_intercept/handle.rs changes skipped (fork has no tls_intercept/ module). Only proxy_runtime.rs filter-allowlist rider ported. credential.rs untouched."

    Commit this task's why_runtime.rs changes with: `Upstream-commit: 0ced085` trailer (primary source for domain_endpoints in WhyContext). DCO Signed-off-by mandatory.
  </action>
  <verify>
    <automated>make test 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `grep "domain_endpoints" crates/nono-cli/src/why_runtime.rs` shows WhyContext field + query_network call
    - `cargo check --bin nono` exits 0 (zero errors)
    - `make test` passes (full workspace test suite green)
    - `git show HEAD:crates/nono-proxy/src/credential.rs | sha256sum` — SHA prefix matches c9f25164 (byte-identical invariant holds)
    - `git log --oneline --grep="Upstream-commit" -10` shows commits with 0ced085, 75b2265, 22e6c40 trailers
    - `cargo clippy --bin nono -- -D warnings -D clippy::unwrap_used` exits 0 on Windows host
    - Cross-target clippy either PASS (if cross-toolchain installed) or PARTIAL with deferred-to-CI note in SUMMARY
    - nono why --host output with a scoped domain shows "Endpoint rules:" section (manual smoke test)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| --host URL arg → parse_host_input | User-controlled URL in why_runtime; url::Url::parse normalizes before domain extraction |
| DomainEndpointState from sandbox state file → query_ext display | State file may be tampered; endpoint_rules displayed are for diagnostic only — no enforcement consequence |
| credential.rs byte-identical gate | Any modification to credential.rs is an invariant violation; the verification sweep catches it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-11 | Tampering | parse_host_input used for display only (SC3) — not enforcement | accept | The diagnostic path is intentionally non-enforcing; enforcement is only in CompiledEndpointRules::is_allowed in the proxy layer. This is the correct architectural separation. |
| T-56-12 | Tampering | path_matches_endpoint_rules uses globset directly (not normalize_path) | mitigate | path_matches_endpoint_rules is a display helper for nono why; it does not control access. Mismatches between display output and actual proxy behavior are acceptable (display approximates enforcement). Document in SUMMARY. |
| T-56-13 | Spoofing | credential.rs modified by a concurrent plan or merge conflict | mitigate | SC4 invariant check in Task 2 verification sweep: if SHA does not match c9f25164, task FAILS. This provides a hard gate against undetected credential.rs modification. |
| T-56-14 | Repudiation | Missing Upstream-commit trailers on ported commits | mitigate | Verification sweep `git log --grep="Upstream-commit"` checks all three required SHAs present; absence is a done-criteria failure. |
</threat_model>

<verification>
Full Phase 56 gate (after Plan 04):
- SC1: `cargo test -p nono-proxy -- endpoint` — existing endpoint rule tests pass, confirming hard-deny path is intact.
- SC1: `cargo test -p nono-proxy -- reverse` — existing reverse handler tests pass.
- SC2: `grep -n "endpoint_rules\|is_allowed" crates/nono-proxy/src/reverse.rs | head -5` confirms endpoint check precedes credential lookup (structural check; no code changed).
- SC2: `cargo test -p nono-proxy -- audit` — existing audit tests pass.
- SC3: `cargo test -p nono-cli -- parse_host_input query_network` — new tests pass.
- SC4: `git show HEAD:crates/nono-proxy/src/credential.rs | sha256sum` — prefix c9f25164.
- SC4: `git log --oneline --grep="Upstream-commit" -10` — shows 0ced085, 75b2265, 22e6c40.
- Full suite: `make test` — zero new failures.
- Windows clippy: `cargo clippy --bin nono -- -D warnings -D clippy::unwrap_used` exits 0.
- Cross-target: PASS or PARTIAL with deferred-to-CI note.
</verification>

<success_criteria>
1. parse_host_input and path_matches_endpoint_rules present in query_ext.rs with ported tests passing.
2. query_network accepts domain_endpoints as 5th param; QueryResult::Allowed has endpoint_rules field populated from domain_endpoints when host has scoped entries.
3. print_result shows "Endpoint rules:" section for allowed hosts with scoped entries.
4. WhyContext has domain_endpoints; why_runtime derives it from profile.network.allow_domain WithEndpoints entries; query_network call passes it.
5. credential.rs SHA prefix c9f25164 verified unchanged.
6. Upstream-commit trailers present for 0ced085, 75b2265, 22e6c40.
7. make test passes (full workspace suite green).
8. cargo clippy --bin nono exits 0 on Windows host; cross-target either PASS or PARTIAL/deferred-to-CI.
</success_criteria>

<output>
Create `.planning/phases/56-fine-grained-network-filtering/56-04-SUMMARY.md` when done
</output>
