---
phase: 56-fine-grained-network-filtering
plan: 03
type: execute
wave: 2
depends_on:
  - 56-01
files_modified:
  - crates/nono-cli/src/profile_cmd.rs
  - crates/nono-cli/data/nono-profile.schema.json
autonomous: true
requirements:
  - REQ-NET-01

must_haves:
  truths:
    - "nono profile show renders each AllowDomainEntry: Plain entries display as hostname strings; WithEndpoints entries display as 'domain (N endpoint rules)' (D-01: AllowDomainEntry is the C3 foundation type; D-07: profile JSON field carries scoped rules alongside the CLI flag)"
    - "nono profile diff handles Vec<AllowDomainEntry> by extracting domain strings for comparison"
    - "nono-profile.schema.json allows both plain strings and AllowDomainWithEndpoints objects in the allow_domain array (D-07: profile schema extended to match CLI semantics from upstream #960)"
    - "resolve_to_manifest produces manifest.Network.endpoints from WithEndpoints profile entries"
  artifacts:
    - path: "crates/nono-cli/src/profile_cmd.rs"
      provides: "Updated allow_domain display rendering, diff helpers using .domain(), manifest endpoint mapping"
      contains: "AllowDomainEntry::WithEndpoints"
    - path: "crates/nono-cli/data/nono-profile.schema.json"
      provides: "Extended allow_domain items schema with oneOf + AllowDomainWithEndpoints $ref"
      contains: "AllowDomainWithEndpoints"
  key_links:
    - from: "crates/nono-cli/src/profile_cmd.rs"
      to: "crates/nono-cli/data/nono-profile.schema.json"
      via: "Profile show + schema describe same allow_domain shape; must be consistent"
      pattern: "allow_domain"
    - from: "crates/nono-cli/src/profile_cmd.rs"
      to: "crates/nono/src/manifest.rs"
      via: "resolve_to_manifest maps AllowDomainEntry::WithEndpoints to manifest::NetworkEndpoint via generated types"
      pattern: "manifest::NetworkEndpoint"
---

<objective>
Update profile display rendering and the JSON schema for the allow_domain field. profile_cmd.rs has three change sites: cmd_show (display rendering), cmd_diff (domain-string extraction for comparison), and resolve_to_manifest (manifest endpoint mapping). The schema JSON gains an AllowDomainWithEndpoints $defs entry and changes allow_domain items from a plain string type to oneOf(string, $ref).

Purpose: These two files (profile_cmd.rs and nono-profile.schema.json) have zero file overlap with Plan 02 — they can run in Wave 2 parallel. Profile display completeness is required for SC3 visibility (D-11 WATCH-ITEM: nono why output + profile show must make effective openness visible).

Output: profile_cmd.rs compiles with Vec<AllowDomainEntry>; nono-profile.schema.json accepts structured endpoint entries; `nono profile show` renders endpoint rule counts.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\OMack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/56-fine-grained-network-filtering/56-01-SUMMARY.md
@.planning/phases/56-fine-grained-network-filtering/56-RESEARCH.md
@.planning/phases/56-fine-grained-network-filtering/56-PATTERNS.md

<interfaces>
<!-- Types introduced in Plan 01 and used here -->

From crates/nono-cli/src/profile/mod.rs (Plan 01 output):
  pub enum AllowDomainEntry { Plain(String), WithEndpoints { domain: String, endpoints: Vec<EndpointRule> } }
  impl AllowDomainEntry { pub fn domain(&self) -> &str }
  NetworkConfig.allow_domain: Vec<AllowDomainEntry>

From crates/nono/src/manifest.rs (generated types, already in fork — verified):
  pub struct Network { pub allow_domains: Vec<String>, pub endpoints: Vec<NetworkEndpoint>, ... }
  pub struct NetworkEndpoint { pub host: NetworkEndpointHost, pub endpoint_rules: Vec<EndpointRule> }
  pub struct NetworkEndpointHost(String); impl TryFrom<&str> for NetworkEndpointHost
  pub struct EndpointRuleMethod(String); impl TryFrom<&str> for EndpointRuleMethod
  pub struct EndpointRulePath(String); impl TryFrom<&str> for EndpointRulePath
  pub struct EndpointRule { pub method: EndpointRuleMethod, pub path: EndpointRulePath }

Current profile_cmd.rs allow_domain display (to be updated, lines ~1575-1579):
  if !net.allow_domain.is_empty() {
    println!("    {}: {}", theme::fg("allow_domain", t.subtext), net.allow_domain.join(", "));
  }

Current profile_cmd.rs diff comparison (to be updated, lines ~2068-2070):
  ("allow_domain", &p1.network.allow_domain, &p2.network.allow_domain)

Current profile_cmd.rs manifest construction (to be updated, lines ~3234-3245):
  allow_domains: prof.network.allow_domain.clone()  ← was Vec<String>; now Vec<AllowDomainEntry>
  endpoints: Vec::new()  ← must be populated from WithEndpoints entries
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: profile_cmd.rs — allow_domain display, diff, and manifest endpoint mapping</name>
  <files>crates/nono-cli/src/profile_cmd.rs</files>
  <read_first>
    - crates/nono-cli/src/profile_cmd.rs — read lines 1035-1065 (cmd_show allow_domain display block), lines 1489-1520 (cmd_diff allow_domain comparison block), lines 1949-1980 (diff_to_json allow_domain block), lines 3225-3260 (resolve_to_manifest Network construction). Extract: all four change sites exactly.
    - git show 0ced085 -- crates/nono-cli/src/profile_cmd.rs — read the full diff to see all change hunks verbatim. The diff has ~78 lines of changes across 4 sites.
    - crates/nono/src/manifest.rs — confirm `pub struct Network` fields: allow_domains (Vec<String>), endpoints (Vec<NetworkEndpoint>), dns, ports. Confirm NetworkEndpoint { host: NetworkEndpointHost, endpoint_rules: Vec<EndpointRule> } and the TryFrom impls for the newtype wrappers.
    - .planning/phases/56-fine-grained-network-filtering/56-PATTERNS.md — §profile_cmd.rs (display rendering, diff comparison helpers, manifest endpoint mapping).
  </read_first>
  <action>
    Run `cargo check --bin nono 2>&1 | grep "profile_cmd"` FIRST to see all the current type errors in this file. Use the error list to confirm each change site.

    Site 1 — cmd_show display (lines ~1575-1580):
    Replace `net.allow_domain.join(", ")` with a mapped display:
      let display: Vec<String> = net.allow_domain.iter().map(|e| match e {
        profile::AllowDomainEntry::Plain(s) => s.clone(),
        profile::AllowDomainEntry::WithEndpoints { domain, endpoints } =>
          format!("{} ({} endpoint rules)", domain, endpoints.len()),
      }).collect();
      display.join(", ")
    (Verbatim from upstream 0ced085 diff — PATTERNS.md §profile_cmd.rs.)

    Site 2 — cmd_diff string comparison (lines ~1489-1510):
    Before the `let net_vec_diffs = diff_string_vecs(...)` call, derive domain-string vectors:
      let p1_allow_domain_strs: Vec<String> = p1.network.allow_domain.iter().map(|e| e.domain().to_string()).collect();
      let p2_allow_domain_strs: Vec<String> = p2.network.allow_domain.iter().map(|e| e.domain().to_string()).collect();
    Replace the tuple `("allow_domain", &p1.network.allow_domain, &p2.network.allow_domain)` with
    `("allow_domain", &p1_allow_domain_strs, &p2_allow_domain_strs)`.

    Site 3 — diff_to_json (lines ~1967-1990):
    Same domain-string extraction pattern as Site 2. Replace
    `"allow_domain": diff_vec(&p1.network.allow_domain, &p2.network.allow_domain)`
    with derived string vecs per the 0ced085 diff verbatim.

    Site 4 — resolve_to_manifest Network construction (lines ~3234-3260):
    (a) Build manifest_endpoints: Vec<manifest::NetworkEndpoint> from WithEndpoints entries:
      let manifest_endpoints: Vec<manifest::NetworkEndpoint> = prof.network.allow_domain.iter()
        .filter_map(|e| match e {
          profile::AllowDomainEntry::WithEndpoints { domain, endpoints } if !endpoints.is_empty() => {
            let host: manifest::NetworkEndpointHost = domain.as_str().try_into().ok()?;
            let rules = endpoints.iter().filter_map(|r| {
              let method: manifest::EndpointRuleMethod = r.method.as_str().try_into().ok()?;
              let path: manifest::EndpointRulePath = r.path.as_str().try_into().ok()?;
              Some(manifest::EndpointRule { method, path })
            }).collect();
            Some(manifest::NetworkEndpoint { host, endpoint_rules: rules })
          }
          _ => None,
        }).collect();
    (b) Change allow_domains construction from `.clone()` (was Vec<String>) to:
      allow_domains: prof.network.allow_domain.iter().map(|e| e.domain().to_string()).collect()
    (c) Change endpoints: Vec::new() to endpoints: manifest_endpoints.
    NOTE: The manifest::EndpointRule from generated types uses EndpointRuleMethod/EndpointRulePath newtypes; the field name on NetworkEndpoint is `endpoint_rules` (verified from generated types). Verify field names by running `cargo check --bin nono` after the change.

    Add `use nono::manifest;` import if not already present in the function's scope (it's currently in a nested test module; the resolve_to_manifest function uses it directly).

    After all edits: `cargo check --bin nono 2>&1 | grep "profile_cmd" | grep "^error"` — expect zero.
  </action>
  <verify>
    <automated>cargo check --bin nono 2>&1 | grep "profile_cmd" | grep "^error" | head -10</automated>
  </verify>
  <done>
    - `cargo check --bin nono 2>&1 | grep "profile_cmd" | grep "^error"` shows zero errors
    - `grep "WithEndpoints.*endpoint rules\|endpoint rules.*WithEndpoints" crates/nono-cli/src/profile_cmd.rs` shows the display format string
    - `grep "domain().to_string\|allow_domain_strs" crates/nono-cli/src/profile_cmd.rs` shows domain-string extraction in both diff sites
    - `grep "manifest_endpoints\|manifest::NetworkEndpoint" crates/nono-cli/src/profile_cmd.rs` shows manifest endpoint mapping
    - Commit carries Upstream-commit: 0ced085 trailer and DCO Signed-off-by
  </done>
</task>

<task type="auto">
  <name>Task 2: nono-profile.schema.json — extend allow_domain items to oneOf(string, AllowDomainWithEndpoints)</name>
  <files>crates/nono-cli/data/nono-profile.schema.json</files>
  <read_first>
    - crates/nono-cli/data/nono-profile.schema.json — search for "allow_domain" to find the current items schema. Also read the "$defs" section to understand where to insert the new AllowDomainWithEndpoints definition.
    - git show 0ced085 -- crates/nono-cli/data/nono-profile.schema.json — diff-inspect for the exact JSON schema additions: the AllowDomainWithEndpoints $defs entry and the allow_domain items change.
    - .planning/phases/56-fine-grained-network-filtering/56-PATTERNS.md — §nono-profile.schema.json (verbatim upstream schema for AllowDomainWithEndpoints and the oneOf change).
  </read_first>
  <action>
    In crates/nono-cli/data/nono-profile.schema.json:

    (a) In the "$defs" section: add an "AllowDomainWithEndpoints" definition:
      "AllowDomainWithEndpoints": {
        "type": "object",
        "required": ["domain"],
        "properties": {
          "domain": { "type": "string" },
          "endpoints": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["method", "path"],
              "properties": {
                "method": { "type": "string" },
                "path": { "type": "string" }
              }
            }
          }
        }
      }
    This is verbatim from upstream 0ced085 diff (PATTERNS.md §nono-profile.schema.json).

    (b) Find the allow_domain field's "items" schema. It currently is `{ "type": "string" }`. Change it to:
      { "oneOf": [ { "type": "string" }, { "$ref": "#/$defs/AllowDomainWithEndpoints" } ] }

    Validate the JSON is well-formed after editing: `python -m json.tool crates/nono-cli/data/nono-profile.schema.json > /dev/null` or use cargo check which will pick it up via build.rs if the schema is included.

    Commit: `docs(proxy): extend allow_domain schema with AllowDomainWithEndpoints (per 0ced085)`. Include `Upstream-commit: 0ced085` trailer and DCO Signed-off-by.
  </action>
  <verify>
    <automated>cargo build --bin nono 2>&1 | grep "^error" | head -10</automated>
  </verify>
  <done>
    - `grep "AllowDomainWithEndpoints" crates/nono-cli/data/nono-profile.schema.json` shows the new $defs entry
    - `grep "oneOf" crates/nono-cli/data/nono-profile.schema.json` shows the updated allow_domain items
    - `cargo build --bin nono` succeeds (schema is embedded via build.rs; a parse error in the JSON would fail the build)
    - Commit carries Upstream-commit: 0ced085 trailer and DCO Signed-off-by
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON schema → profile deserialization | Schema describes what the deserializer accepts; must align with AllowDomainEntry untagged enum |
| manifest::NetworkEndpoint construction | try_into().ok()? on host/method/path newtypes: fail-open (filter_map skips invalid) — acceptable for display; enforcement happens in proxy, not manifest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-08 | Tampering | Schema allows arbitrary method/path strings | accept | Schema is advisory (JSON Schema validation is non-enforcing in CLI); actual enforcement is in CompiledEndpointRules::is_allowed in the proxy — method and path are normalized there |
| T-56-09 | Information Disclosure | profile show reveals endpoint rules | accept | Profile display is intentional user-facing output; no secrets in endpoint rules |
| T-56-10 | Tampering | resolve_to_manifest try_into().ok()? silently skips invalid entries | accept | Manifest is a diagnostic artifact; enforcement is always in the proxy layer; if a host fails TryFrom validation the entry is omitted from manifest display (visible gap) |
</threat_model>

<verification>
After Plan 03 completes (parallel with Plan 02):
- `cargo check --bin nono 2>&1 | grep "profile_cmd" | grep "^error"` shows zero errors.
- `grep "AllowDomainWithEndpoints" crates/nono-cli/data/nono-profile.schema.json` shows the definition.
- `grep "oneOf" crates/nono-cli/data/nono-profile.schema.json` shows the updated items schema.
- `cargo build --bin nono` succeeds (embeds the schema via build.rs; JSON parse errors fail the build).
</verification>

<success_criteria>
1. profile_cmd.rs renders Plain entries as hostname strings and WithEndpoints entries as "domain (N endpoint rules)" in cmd_show output.
2. cmd_diff and diff_to_json use domain-string extraction (.domain().to_string()) for comparisons — no type errors.
3. resolve_to_manifest populates manifest::Network.endpoints from WithEndpoints entries.
4. nono-profile.schema.json has AllowDomainWithEndpoints in $defs and allow_domain items as oneOf.
5. cargo build --bin nono succeeds (JSON schema valid).
6. Commits carry Upstream-commit: 0ced085 trailer and DCO Signed-off-by.
</success_criteria>

<output>
Create `.planning/phases/56-fine-grained-network-filtering/56-03-SUMMARY.md` when done
</output>
