---
quick_id: "260529-mb6"
slug: "rebuild-all-5-apps-rebuild-msi-installer"
date: "2026-05-29"
type: quick
mode: quick
execution: inline-orchestrator
---

<objective>
Produce the build artifacts and operator reference materials needed to run the Phase 60
Win11 UAT (the 5 human-verification items from 60-VERIFICATION.md). Four deliverables:

1. Rebuild all 5 workspace apps (release, x86_64-pc-windows-msvc).
2. Rebuild the MSI installers (per-user + machine scope; no WFP — nono-wfp-service is
   absent in this fork, so the WFP backend is omitted, which the build-windows-msi.ps1
   scope-coherence guard permits when neither -ServiceBinaryPath nor -DriverBinaryPath
   is passed).
3. Create a test Claude Code profile reference bundle in C:\temp\nono-uat.
4. Write a UAT cookbook covering all 5 Phase 60 UAT items to C:\temp.

These binaries contain the unreleased Phase 60 confined-file-edit arms + the CR-01
path_covers fail-open fix (commits ddb711dc..c4714a0c). The Cargo version is still 0.57.5,
so `nono --version` reports 0.57.5 even though the code is ahead of the tagged v0.57.5
release — the MSIs are tagged -poc.1 to mark them as local dev builds.

Execution note: run inline by the orchestrator (not a worktree subagent) — the deliverables
are build artifacts (gitignored) + external reference files in C:\temp (outside the repo);
the only repo change is this planning pair. A worktree would force a from-scratch recompile.
</objective>

<tasks>

<task>
  <name>Task 1: Rebuild all 5 apps (release, MSVC target)</name>
  <action>cargo build --release --target x86_64-pc-windows-msvc — builds nono, nono-cli,
  nono-proxy, nono-shell-broker, nono-ffi (bindings/c). Artifacts land in
  target/x86_64-pc-windows-msvc/release/ where the MSI script expects them.</action>
  <verify>nono.exe and nono-shell-broker.exe exist and report version 0.57.5.</verify>
</task>

<task>
  <name>Task 2: Rebuild MSI installers (user + machine, no WFP)</name>
  <action>scripts/build-windows-msi.ps1 -VersionTag v0.57.5-poc.1 -BinaryPath ...nono.exe
  -BrokerPath ...nono-shell-broker.exe -Scope {user|machine} -OutputDir dist\windows.
  No -ServiceBinaryPath / -DriverBinaryPath (WFP backend absent in this fork).</action>
  <verify>Both dist\windows\nono-v0.57.5-poc.1-*-{user,machine}.msi files exist.</verify>
</task>

<task>
  <name>Task 3: Test Claude Code profile reference bundle -> C:\temp\nono-uat</name>
  <action>Copy the experimental nono profile (claude-code-tools-windows.profile.json),
  nono-tool-hook.ps1, the sandbox CLAUDE.md, and a Claude Code settings.json showing the
  PreToolUse hook wiring; add a README explaining how the pieces fit.</action>
  <verify>C:\temp\nono-uat contains the profile, hook script, settings.json, CLAUDE.md, README.</verify>
</task>

<task>
  <name>Task 4: UAT cookbook -> C:\temp</name>
  <action>Write C:\temp\nono-phase60-uat-cookbook.md covering all 5 Phase 60 UAT items
  (from 60-VERIFICATION.md) with concrete PowerShell steps, expected output, pass/fail
  criteria, supported-working-directory + profile-covered-child gotchas, and a triage payload.</action>
  <verify>Cookbook file exists in C:\temp with all 5 UAT items.</verify>
</task>

</tasks>

<success_criteria>
1. All 5 apps rebuilt for x86_64-pc-windows-msvc (release).
2. Per-user and machine MSIs rebuilt under dist\windows (v0.57.5-poc.1, unsigned local builds).
3. C:\temp\nono-uat reference bundle created (profile + hook + settings + CLAUDE.md + README).
4. C:\temp\nono-phase60-uat-cookbook.md written, covering all 5 UAT items.
</success_criteria>

<output>
Create 260529-mb6-SUMMARY.md when done.
</output>
