---
phase: 117-fail-direction-contract-startup-self-attestation
plan: 260815-b0s
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono-cli/tests/layer_force_unavailable.rs
  - crates/nono-cli/tests/layer_registry_meta_test.rs
  - crates/nono-cli/tests/layer_registry_selfcheck.rs
  - proj/SPEC-windows-fail-direction-contract.md
  - crates/nono/src/machine_policy.rs
  - crates/nono-cli/src/exec_strategy_windows/launch.rs
  - crates/nono-cli/src/agent_daemon/launch.rs
  - .planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md
autonomous: true
requirements: [CINT-01, CINT-03]

must_haves:
  truths:
    - "`layer_force_unavailable.rs`'s module doc names `ALSO_AUTOMATED` (not `MANUALLY_VERIFIED`) for `RestrictedToken` and `JobObjectContainment`, and states the actual coverage shape (an in-process unit test calling the guarded function directly), while keeping the host/harness investigation writeup and the manual PowerShell reproduction."
    - "A machine gate FAILS the build if that module doc's prose assigns any `LayerId` to a coverage list the row is not actually on — SENTENCE-scoped with nearest-preceding-list-name association, verifying 5 claims at HEAD, 3 of them inside the `Of the 13 LayerId rows` paragraph. Proved by TWO recorded perturbations (a wrong claim; and a correct claim reworded out of the gate's reach), not by inspection."
    - "The row mentions that rule does NOT verify (those in sentences naming no coverage list) are pinned at their exact HEAD count, so the unverified remainder cannot grow silently and a verified claim cannot be quietly demoted into it."
    - "Every tracked `crates/*/src/**.rs` file carrying an `OPEN` marker line is in `marker_sources()` — asserted by a repo-wide discovery gate over `tracked_files`, not by an author remembering to extend a hand-written allow-list."
    - "`layer_registry_selfcheck.rs` carries no stale `TODO(117-12)`; `registry_call_sites_exist`'s doc describes what it actually does at HEAD (symbol-form citations, content-verified)."
    - "The SPEC states, in the `## Contract vs. code discrepancies` section, that the D-15 ledger scopes to contract-vs-code divergences and that verification-machinery defects live in the committed `117-REVIEW*`/`117-REVIEW-FIX*` artifacts — and indexes rounds 4, 6 and 8 with their finding COUNTS and their real per-finding dispositions read from those artifacts."
    - "An operator grepping the source for `OPEN` finds all FOUR open items (WR-10, WR-14, CR-02, RF-13), each resolving to an `OPEN`-marked row of the SPEC's review-fix ledger — with CR-02's `OPEN` on the Iteration-6 row specifically, and no second `| CR-02 ` row carrying the token."
    - "`117-VERIFICATION.md` records the operator's SC3 override (accepted_by / accepted_at) without softening the 10/13 finding, the named 3-row remainder, or the host-blocked WR-20 disclosure — and reports SC4's status honestly against what items 1-5 actually closed."
  artifacts:
    - path: "crates/nono-cli/tests/layer_force_unavailable.rs"
      provides: "Corrected coverage-list prose (item 1). Its evidence is the new gate passing over it, not a substring."
    - path: "crates/nono-cli/tests/layer_registry_meta_test.rs"
      provides: "New discovery-based gate machine-checking the module doc's LIST ASSIGNMENT (item 2)"
      contains: "module_doc_assigns_each_claimed_row_to_the_list_it_is_on"
    - path: "crates/nono-cli/tests/layer_registry_selfcheck.rs"
      provides: "Refreshed `registry_call_sites_exist` doc (item 3), hoisted `marker_sources()` + repo-wide marker discovery gate (item 3b), extended `marker_sources()` (item 5)"
      contains: "every_marker_carrying_source_file_is_in_marker_sources"
    - path: "proj/SPEC-windows-fail-direction-contract.md"
      provides: "D-15 scoping statement + rounds 4/6/8 index with finding counts (item 4); `OPEN`-marked CR-02 (Iteration 6) and RF-13 ledger rows (item 5)"
    - path: ".planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md"
      provides: "Applied SC3 override (item 6)"
      contains: "overrides_applied: 1"
  key_links:
    - from: "layer_force_unavailable.rs module doc prose"
      to: "MANUALLY_VERIFIED / ALSO_AUTOMATED lists in layer_registry_meta_test.rs"
      via: "the new sentence-scoped list-assignment gate, discovery-based over LayerId::ALL"
      pattern: "extract_all_layer_id_names"
    - from: "code `⚠ CR-02 OPEN` / `⚠ RF-13 OPEN` markers"
      to: "SPEC review-fix ledger rows"
      via: "every_open_marker_in_code_has_a_ledger_row"
      pattern: "starts_with\\(&prefix\\) && r\\.contains\\(\"OPEN\"\\)"
    - from: "any tracked production src file carrying a marker line"
      to: "marker_sources()"
      via: "every_marker_carrying_source_file_is_in_marker_sources (git-tracked discovery)"
      pattern: "line_carries_marker_tokens"
---

<objective>
Close the four `missing` bullets under SC4 in `117-VERIFICATION.md`, and apply the
operator-accepted SC3 override.

Purpose: SC4 forbids a standing contract/code disagreement. One live one exists (a
coverage-declaring module doc that names the wrong list), one stale record exists (a
`TODO` describing a gate that no longer behaves that way), the D-15 ledger has no stated
disposition for the last three review rounds, and two of the four open items have no
greppable code marker. SC3's 10/13 shortfall is an operator decision that the verifier
correctly refused to write for itself.

Output: 5 prose/record corrections + TWO new test gates + the applied override.

**This plan changes NO production confinement behavior.** Every code edit is a comment or
a test. If you find yourself changing a guard, a gate's decision, or an attestation path,
STOP — that is out of scope. Task 2 enforces this mechanically with a comment-only diff
assertion, not on the honour system.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md

Read first, per task:
- Task 1: `crates/nono-cli/tests/layer_force_unavailable.rs` (whole file, esp. the `//!`
  region lines 3-112); `crates/nono-cli/tests/layer_registry_meta_test.rs` (whole file);
  `crates/nono-cli/tests/common/mod.rs`; `crates/nono-cli/tests/layer_registry_selfcheck.rs`
  lines 260-460, 1029-1130 and 1880-2030.
- Task 2: the SPEC's `## Contract vs. code discrepancies` section and the
  `## Review-fix pass (code review 2026-08-10)` ledger; `layer_registry_selfcheck.rs`
  lines 1029-1225 (`every_open_marker_in_code_has_a_ledger_row`) and 1890-2030
  (`tracked_files`, `is_finding_id`, `parse_marker_ids`, `line_carries_marker_tokens`);
  `crates/nono-cli/src/exec_strategy_windows/layer_registry.rs` lines 915-950 and
  `crates/nono/src/error.rs` lines 498-535 (the marker convention to copy).
- Task 3: `117-VERIFICATION.md` in full.

Project skill `.claude/skills/spike-findings-nono` is NOT applicable — it covers daemon /
multi-engine / token-labeling implementation, and this plan writes no confinement code.

<interfaces>
Facts established by reading HEAD. Use them directly — do not re-derive.

**`layer_registry_meta_test.rs` (the file the new gate lands in) already provides:**
```rust
fn manifest_dir() -> PathBuf;
fn workspace_root() -> PathBuf;
fn read_layer_registry() -> String;          // layer_registry.rs source text
fn read_force_unavailable_tests() -> String; // layer_force_unavailable.rs source text
fn extract_all_layer_id_names(src: &str) -> Vec<String>; // reads `const ALL: &[LayerId] = &[`
fn contains_fn_exact(src: &str, fn_name: &str) -> bool;
const MANUALLY_VERIFIED: &[(&str, &str)];            // (LayerId, reason) — 3 rows
const ALSO_AUTOMATED: &[(&str, &str, &str)];         // (LayerId, file, fn) — 8 rows
mod common;                                          // common::is_ident_boundary
```
```rust
// crates/nono-cli/tests/common/mod.rs
pub fn is_ident_boundary(next: Option<char>) -> bool; // true when `next` is None or not [A-Za-z0-9_!]
```

**`layer_registry_selfcheck.rs` already provides (module scope — usable from a new
sibling test in that same file):**
```rust
fn manifest_dir() -> PathBuf;
fn workspace_root() -> PathBuf;
fn read_spec() -> String;
fn tracked_files(pathspec: &str) -> Vec<String>; // `git ls-files -z -- <pathspec>`, FAILS CLOSED,
                                                 // returns workspace-root-relative `/`-separated paths
fn line_carries_marker_tokens(line: &str) -> bool; // the marker DETECTOR (wider than the parser)
fn parse_marker_ids(line: &str) -> Vec<String>;    // the marker PARSER (adjacency + whole word)
```
`marker_sources()` is currently a **nested fn INSIDE**
`every_open_marker_in_code_has_a_ledger_row` (~:1090) and is therefore not callable from a
sibling test. Item 3b hoists it — see that item.

**The `//!` region of `layer_force_unavailable.rs`, as the new gate will see it (derived
by hand at HEAD — reconcile your implementation against this, do not just trust its
output):**

| Region | Content | `LayerId`s spelled | Coverage-list identifiers |
|---|---|---|---|
| P5 (WR-07 counts, ~:37-43) | the 2+8+3=13 arithmetic | 0 | BOTH |
| P6 sentence A (~:45-52) | "Of the 13 `LayerId` rows, this file automates the 2 …" | 0 | none |
| **P6 sentence B (~:52-58)** | **"The remaining 11 rows are split across … 8 on `ALSO_AUTOMATED` (…) and 3 on `MANUALLY_VERIFIED` (`DaclSessionSidGrant`, `MinifilterAbsence`, `BrokerAuthenticodeTrustGate`) — each with its own named, loud reason (D-31)."** | **3** | **BOTH, but `MANUALLY_VERIFIED` is the nearest-preceding one for all 3** |
| **P6 sentence C (~:58-81)** | **"The reasons below cover rows on BOTH lists: … (`WfpEgressFilters`), … (`MinifilterAbsence`), … (`FirewallRulesEgress`), … (`BrokerAuthenticodeTrustGate`), … (`InterpreterCoverageGate`), … (`AppContainerProfile`), … (`DaclAncestorTraverse`, `DaclAncestorReadAttrs`) … `RestrictedToken` and `JobObjectContainment`."** | **10** | **NONE — it spells neither identifier** |
| P7 (`## RestrictedToken / JobObjectContainment` heading, ~:83-84) | heading | 2 | none |
| P8 (the defective paragraph, ~:86-112) | ends "Both rows are therefore on the `MANUALLY_VERIFIED` list …" | 1 (in the quoted diagnostic string at ~:109) | `MANUALLY_VERIFIED`, in a sentence that names NO row (it says "Both rows" anaphorically) |

Three consequences that shape items 1 and 2 — read them before writing either:

1. **A paragraph-scoped rule is the wrong rule.** P6 names both lists, so a
   paragraph-scoped rule skips it and is blind to all 11 of the doc's primary per-row
   claims. A SENTENCE-scoped, nearest-preceding rule verifies sentence B's 3 claims
   correctly and skips sentence C — which is honest, not blind: sentence C literally says
   "rows on BOTH lists" and makes no per-row assignment claim to verify.
2. **Sentence C is the hazard.** Do NOT "fix" it by splitting P6 into single-list
   paragraphs. Its rows genuinely span both lists in one breath; forcing it under one list
   name makes the gate assert the wrong list for several rows and fail spuriously.
   Leave sentence C exactly as it is.
3. **At HEAD the defective claim is INVISIBLE to any such rule**, because P8's final
   sentence says "Both rows" instead of naming them. Item 1's rewrite must therefore put
   the list identifier and both row names in ONE sentence, list identifier FIRST — see
   item 1. Without that the perturbation proof cannot fail and the gate is decorative.

**Expected counts after item 1 (state these in the test; reconcile before pinning):**
`verified claims = 5` — sentence B's `DaclSessionSidGrant`, `MinifilterAbsence`,
`BrokerAuthenticodeTrustGate` → `MANUALLY_VERIFIED`; plus P8's rewritten
`RestrictedToken`, `JobObjectContainment` → `ALSO_AUTOMATED`.
`unverified row mentions = 13` — sentence C's 10, P7's 2, P8's quoted-diagnostic 1.

**`every_open_marker_in_code_has_a_ledger_row` (layer_registry_selfcheck.rs:1058) — the
rules item 5 must satisfy:**
1. `fn is_finding_id(token)` accepts exactly `[A-Z][A-Z]-[0-9]+`. `CR-02` ✓ `RF-13` ✓.
2. `parse_marker_ids` requires the id token IMMEDIATELY adjacent to a whole-word ` OPEN`
   (`WR-14 (OPEN)` and `WR-14 — OPEN` are deliberately NOT parsed, and trip the blind-file
   assertion). Copy `⚠ <ID> OPEN (NOT FIXED — grep \`<ID> OPEN\`; ...)` exactly.
3. The ledger match is `row.starts_with("| {id} ") && row.contains("OPEN")` — **case
   sensitive**. Verified at HEAD: only the WR-10 (`:300`) and WR-14 (`:301`) rows contain
   `OPEN`. The CR-02 row and the RF-13 row (`:262`) say `**Open operator decision:**` —
   lowercase `pen`, which does NOT match. Both MUST gain the literal `OPEN` token.
4. `marker_sources()` (:1090) is a fixed 5-file list: `crates/nono/src/error.rs`,
   `exec_strategy_windows/layer_registry.rs`, `exec_strategy_windows/launch.rs`,
   `exec_strategy_windows/attestation.rs`, `crates/nono-cli/src/output.rs`. It has NO
   hardcoded marker count (`!markers.is_empty()` plus a per-file blind-file check plus
   `rows.len() >= 40`). Files not on the list are simply unscanned — so any file that gains
   a marker MUST be added, or the marker is unprotected. Item 3b makes that mechanical.

**Ledger-id collision hazard — TWO of them, do not get either wrong:**
- The fleet-control item is **`RF-13` at SPEC `:262`** (single row, no collision). There IS
  a separate, unrelated `WR-12` row at `:289` (ancestor-DACL walk classification). A
  `WR-12 OPEN` marker would resolve against the WRONG row. Use `RF-13`.
- **`| CR-02 ` matches TWO rows**: `:276` (Iteration 4, gap-closure Plan 117-21) and `:299`
  (Iteration 6, code-review fix pass). The gate's `rows.iter().any(...)` cannot tell them
  apart. The `OPEN` token belongs on the **Iteration-6 row at `:299`** — that is the one the
  code marker is about. Neither row contains `OPEN` today, so the gate is currently
  unambiguous by accident only. (`| WR-10 ` at `:286`/`:300` and `| WR-14 ` at `:291`/`:301`
  have the same pre-existing shape; both already resolve via their Iteration-6 rows.)

**Registry-surface raw-citation ban:** `no_line_number_citations_remain_in_the_registry_surface`
(selfcheck:1414) rejects any `file.rs:<digits>` in exactly three files:
`layer_registry.rs`, `tests/layer_registry_meta_test.rs`, `tests/layer_force_unavailable.rs`.
Items 1 and 2 both edit files on that list — write `` `layer_registry_meta_test.rs`'s
`ALSO_AUTOMATED` list ``, never `layer_registry_meta_test.rs:198-239`.

**SPEC citation gates:** `every_spec_symbol_citation_resolves_to_a_real_definition`
(selfcheck:786) is a WHOLE-DOCUMENT scan — any `file.rs::Symbol` you add to the SPEC must
resolve to a real definition. Prefer bare function names in the item-4 index
(`every_markdown_file_gated_by_a_test_runs_the_code_jobs`) over `file.rs::Symbol` form.
`spec_call_site_cells_match_registry_call_sites` (:723) only reads the registry table's
cells — untouched by this plan.

**Real production sites for item 5's markers (verified at HEAD):**
- RF-13 reader: `crates/nono/src/machine_policy.rs::read_required_layers` (~:716-739),
  emits the `RequiredLayersNotEnforced` warning.
- RF-13 gate call site: `crates/nono-cli/src/exec_strategy_windows/launch.rs` ~:1585-1586,
  `required_layers_override: &[]` / `machine_required_layers: &[]` under the existing
  "D-26 tighten-only union: not yet wired at this gate" comment.
- CR-02 daemon site: `crates/nono-cli/src/agent_daemon/launch.rs` ~:925, the
  `match daemon_attest_and_decide(` call — the hand-written mirror bound to the registry
  only by the source-text drift gate `daemon_expected_rows_are_all_named_by_the_daemon_gate`.

None of these four files contains a `#[cfg(target_os = "linux")]` or `"macos"` block
(verified: grep count 0 in each), none is under `exec_strategy/` or `bindings/c/src/`, so
CLAUDE.md's cross-target clippy rule does NOT apply to this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: correct the coverage-list prose, gate it, refresh the stale TODO, and close the marker-source class (items 1+2+3+3b)</name>
  <files>crates/nono-cli/tests/layer_force_unavailable.rs, crates/nono-cli/tests/layer_registry_meta_test.rs, crates/nono-cli/tests/layer_registry_selfcheck.rs</files>
  <action>
Do these in order. Item 1 MUST land before item 2's perturbation proof. Item 3b MUST land
before Task 2, so that Task 2's new markers are policed rather than trusted.

**ITEM 1 — `layer_force_unavailable.rs`, the sentence at ~:109-112.**
It currently reads "Both rows are therefore on the `MANUALLY_VERIFIED` list with their
manual reproduction steps (the exact command above), not silently dropped — D-31's 'loud
gap, never a silent skip.'" That is false: both rows are on `ALSO_AUTOMATED`, each with a
real in-process test —
`restricted_token.rs::create_restricted_token_with_sid_fails_when_forced_unavailable` and
`launch.rs::apply_process_handle_to_containment_fails_when_forced_unavailable`.

Rewrite ONLY that list-assignment claim. State: both rows are on `ALSO_AUTOMATED`, and
their coverage shape is an in-process unit test that calls the guarded function directly,
rather than this file's external-subprocess `fn force_unavailable_<snake>` convention.
KEEP the surrounding investigation writeup and the manual PowerShell reproduction verbatim
— they remain accurate and are the evidence for WHY the external-subprocess shape was not
used (the post-event-loop teardown stall on this host). Reframe them as supplementary
manual evidence, not as the row's coverage of record.

**Three hard constraints on the wording** (all load-bearing for item 2):
- This file is on the raw-citation ban list. Cite by name/symbol, never `file.rs:123`.
- **The claim sentence must name the list identifier FIRST and BOTH row names AFTER it, in
  ONE sentence** — e.g. "Both rows are therefore on the `ALSO_AUTOMATED` list:
  `RestrictedToken` via … and `JobObjectContainment` via …". Item 2's gate binds a row name
  to the nearest coverage-list identifier PRECEDING it in the same sentence. The current
  anaphoric "Both rows … on the `MANUALLY_VERIFIED` list" names no row, so no rule can
  check it; that is why the defect survived, and it is the shape you are eliminating.
- Do NOT touch P6 (the `Of the 13 LayerId rows` paragraph), and in particular do NOT split
  its "The reasons below cover rows on BOTH lists: …" sentence into single-list paragraphs.
  That sentence enumerates rows from both lists deliberately; forcing it under one list
  name makes item 2's gate assert the wrong list for several rows and fail spuriously. The
  cheapest wrong "fix" for that spurious failure is to weaken the gate — do not go there.

**ITEM 2 — new gate in `layer_registry_meta_test.rs`.**
`coverage_split_accounts_for_every_layer_id` (:437) enforces arithmetic and disjointness
and is structurally blind to WHICH list the prose names — which is exactly why item 1's
defect survived. Add a sibling `#[test]` named
`module_doc_assigns_each_claimed_row_to_the_list_it_is_on` that closes that.

**THE ASSOCIATION RULE — write it ONCE, in that test's doc comment, and implement it
ONCE.** Do not write a second variant of it anywhere; this phase has already shipped two
mirrors with contradictory rules that both passed every gate.

> Within the module-doc (`//!`) region, for each occurrence of a `LayerId` name at
> identifier boundaries, look BACKWARDS within the SAME SENTENCE for the nearest coverage-
> list identifier (`MANUALLY_VERIFIED` or `ALSO_AUTOMATED`). If one is found, that is a
> CLAIM and the row must actually be on that list. If the sentence names no coverage list
> before the row, the mention makes no assignment claim: it is UNVERIFIED, and counted.

Why sentence-scoped and nearest-preceding, rather than paragraph-scoped (state this in the
doc comment too, so the next author cannot re-derive a weaker rule and think it equivalent):
a paragraph-scoped rule would skip any paragraph naming both lists, and P6 — which carries
11 of the doc's 13 row mentions and all of its primary per-row claims — names both. That
rule would check 2 claims out of 13 mentions and be blind in exactly the region WR-07
already had to correct once. Worse, it would give an author a silent escape hatch: adding
the second list name to a paragraph makes a failing assertion disappear. Under the
sentence-scoped nearest-preceding rule there is NO ambiguity case and therefore no escape
hatch — naming both lists binds each row to whichever one precedes it, and moving a row
name away from its list name shows up in the unverified-mention pin below.

Mechanism:
1. Read `layer_force_unavailable.rs` via the existing `read_force_unavailable_tests()`.
2. Scope to the module doc: keep only lines whose trimmed start is `//!`, and strip that
   prefix. Nothing else in the file. Say in the doc comment that the scope is the `//!`
   region and WHY (the claim being checked is a module-doc claim; widening to the whole
   file would sweep in per-test doc comments, which describe individual tests rather than
   the coverage split).
3. Split that region into paragraphs on blank `//!` lines, join each paragraph's lines with
   a single space, then split each paragraph into SENTENCES. A paragraph end is also a
   sentence end (P7 is a heading with no terminal period). A `.` terminates a sentence only
   when followed by whitespace or end-of-paragraph — which already excludes `mod.rs`,
   `nono.exe`, `layer_registry_meta_test.rs`'s — and only when the word ending at it is not
   an abbreviation; carry a small explicit list (`e.g`, `i.e`) and say in the doc comment
   that an unlisted abbreviation over-splits, which can only move a claim into the
   unverified count and trip the pin loudly, never silently pass a wrong claim.
4. Derive the `LayerId` name set from source via the existing
   `extract_all_layer_id_names(&read_layer_registry())`.
   **Name NO layer literally anywhere in the test.** A test that hardcodes the two rows
   would be blind to a third.
5. Derive the two coverage-list identifier strings from the consts themselves rather than
   retyping them — pair `stringify!(MANUALLY_VERIFIED)` with `MANUALLY_VERIFIED`'s own
   names (and likewise for `ALSO_AUTOMATED`) in one expression, so renaming a const breaks
   compilation here instead of silently un-scoping the gate.
6. Identifier-boundary matching on BOTH sides of a candidate occurrence (row names and list
   identifiers alike) uses `common::is_ident_boundary` — the file's established idiom. Do
   not hand-roll a second boundary predicate.
7. For each CLAIM, assert the named list is the list the row is ACTUALLY on. On mismatch the
   message must name the row, the list the prose claims, the list it is really on, and quote
   the offending sentence.
8. **TWO PINS, both required.** Narrowing the scan is the fail-OPEN direction here, so both
   directions are pinned, following the file's existing floor idiom (`>= 13`, `>= 8`):
   - **Claims floor:** `verified_claims >= 5` (the count item 1's corrected doc really
     produces — see the interfaces table). Name the expected 5 in the assertion message.
   - **Unverified-mention pin:** `assert_eq!(unverified_mentions, 13, ...)`. Equality, not a
     ceiling, and that is deliberate — it is the ONE assertion that makes the residual
     visible. An increase means a new row mention was written where the gate cannot verify
     it; a decrease means either a mention was deleted or, more importantly, a claim was
     REWORDED out of gate reach (list name moved to another sentence, or replaced by "both
     rows"), which is exactly how this defect hid at HEAD. The message must list the
     unverified mentions it found with their sentences, state that the remainder is
     UNVERIFIED and not merely unclaimed, and tell the author to either put the row in a
     sentence that names its list (preferred) or update the pin deliberately.
   Reconcile your actual numbers against the interfaces table's 5 / 13 BEFORE pinning. If
   they differ, your sentence splitter or item 1's wording is wrong — find out which and say
   so in the SUMMARY. Do not pin whatever the implementation happens to emit.
9. **State the residual limit in the test's own doc comment**: the gate verifies per-row
   list assignments made in sentences that name a list; it does NOT verify the 10 row
   mentions in P6's both-lists reasons sentence, the 2 in the P7 heading, or the 1 inside
   P8's quoted diagnostic — those make no assignment claim and are held constant by the
   pin, not checked.

**PERTURBATION PROOFS — TWO, MANDATORY, BEFORE COMMIT.** A discovery test without a
perturbation proof is not evidence; this phase produced three consecutive rounds of exactly
that mistake. With item 1's correction in place:
- **(a) wrong claim:** flip the corrected sentence's `ALSO_AUTOMATED` to
  `MANUALLY_VERIFIED`. The test MUST fail naming both rows, the claimed list and the real
  list.
- **(b) claim demoted out of reach:** reword the corrected sentence back to the anaphoric
  shape (list identifier in a sentence that names no row, row names in a sentence that names
  no list). The test MUST fail — claims drop to 3 (below the floor) and unverified mentions
  rise to 15 (pin). This is the proof that the fail-open escape hatch is alarmed.
After each, `git checkout --` that file and confirm `git status --porcelain` is clean of the
perturbation. Record both perturbations, their exact failure messages, and the reverts in
the SUMMARY.

**ITEM 3 — `layer_registry_selfcheck.rs:354-358`, the stale `TODO(117-12)`.**
It claims `registry_call_sites_exist` "asserts file EXISTENCE ... does not verify the cited
line NUMBER". Both halves are false at HEAD: zero raw `file:line` citations remain (enforced
by `no_line_number_citations_remain_in_the_registry_surface`), and symbol citations ARE
content-verified via `content_defines_symbol` + `nearest_preceding_impl_names` (definition-
line prefix + identifier boundary + enclosing-`impl` scoping). Replace it with an accurate
description of what the test does now: raw-form citations resolve to an existing file;
symbol-form citations resolve to a file whose CONTENT defines the symbol at a real
definition site; and both non-vacuity floors (`regions >= 13`, `citations >= 20`) exist
because narrowing the region scan is the fail-OPEN direction. Do NOT leave a `TODO(` marker
behind. If you judge a residual risk genuinely remains, state that risk precisely as prose —
do not restate the old, wrong one.

**ITEM 3b — close the marker-source class in `layer_registry_selfcheck.rs`.**
`marker_sources()` is a hand-written allow-list; a marker landing in an unlisted file is
simply unscanned, which is the fail-open direction and is precisely the trap Task 2 walks
into. Convert it to a closed class:
1. **Hoist `marker_sources()`** out of `every_open_marker_in_code_has_a_ledger_row` to
   module scope, unchanged in content. Do NOT copy it — one list, two callers. (This phase's
   recurring defect is two mirrors of one rule drifting apart.)
2. Add a sibling `#[test] fn every_marker_carrying_source_file_is_in_marker_sources`:
   enumerate `tracked_files("crates/")`, keep paths that contain `/src/` and end in `.rs`,
   read each, and assert that any file with a line satisfying `line_carries_marker_tokens`
   is present in `marker_sources()`'s label set. Failure message: name the file and the
   offending line, and say that the file must be added to `marker_sources()` so its marker
   is protected by the ledger gate.
3. Non-vacuity: assert the enumeration is non-empty and that the scan actually found the
   known marker-carrying files — a floor on the number of DETECTED files, **`>= 2` at HEAD**,
   which Task 2 raises to 5. Otherwise a broken pathspec silently asserts nothing.

   **The detected-file count is NOT `marker_sources()`'s entry count — do not conflate them.**
   Measured against HEAD with the real per-line `line_carries_marker_tokens` predicate
   (whole-word `OPEN` AND an `[A-Z][A-Z]-[0-9]+` token on the SAME line), exactly **2** files
   carry a marker:
     - `crates/nono/src/error.rs:506` (`WR-14 OPEN`)
     - `crates/nono-cli/src/exec_strategy_windows/layer_registry.rs:925` (`WR-10 OPEN`)
   `marker_sources()` has **5** entries because `exec_strategy_windows/launch.rs`,
   `exec_strategy_windows/attestation.rs` and `output.rs` are listed-but-marker-free
   (`grep -c '\bOPEN\b'` returns 0 for all three). A listed file with no marker is fine — the
   allow-list is a superset. Pinning the detected floor at 5 here would make this test FAIL on
   its first run, because Task 2 has not added any marker yet.
4. **Scope limit, stated in the test's doc comment:** production `src/` only. `tests/` is
   excluded because `layer_registry_selfcheck.rs` itself carries marker-SHAPED string
   literals in `the_open_marker_detector_separates_markers_from_win32_constants`'s table —
   scanning tests would assert over test DATA, not over markers. Markers are a production-
   code convention; note that a marker placed in a test file is out of this gate's reach.
5. If the scan surfaces a file that is neither already in `marker_sources()` nor a genuine
   marker site, do NOT add a silent exclusion. Either add the file (if it genuinely carries a
   marker) or STOP and report it — an unexpected hit is a finding, not noise.

Test code may carry `#[allow(clippy::unwrap_used)]` (both files already do at module level).
  </action>
  <verify>
    <automated>cd /c/Users/OMack/Nono &amp;&amp; cargo test -p nono-sandbox-cli --test layer_registry_selfcheck --test layer_registry_meta_test --test layer_force_unavailable</automated>
    <automated>cd /c/Users/OMack/Nono &amp;&amp; cargo test -p nono-sandbox-cli --features layer-fault-injection --test layer_force_unavailable</automated>
    <automated>cd /c/Users/OMack/Nono &amp;&amp; cargo clippy -p nono-sandbox-cli --test layer_registry_meta_test --test layer_registry_selfcheck -- -D warnings -D clippy::unwrap_used</automated>
    <automated>cd /c/Users/OMack/Nono &amp;&amp; cargo fmt --all -- --check</automated>
    <automated>cd /c/Users/OMack/Nono &amp;&amp; git status --porcelain</automated>
  </verify>
  <done>
`layer_registry_selfcheck` is 21 passed / 0 failed (the HEAD baseline of 20, plus item 3b's
gate — no regression) and `layer_registry_meta_test` is its prior count + 1 (item 2's gate),
all passing, RUN IN THE REAL TREE at `C:\Users\OMack\Nono` — not a worktree. The feature leg
of `layer_force_unavailable` is 2 passed / 0 failed. `cargo fmt --all -- --check` exits 0.
BOTH perturbations were performed and FAILED as required — (a) naming the row, the claimed
list and the real list; (b) tripping the claims floor and the unverified-mention pin — and
both were reverted (`git status --porcelain` shows no trace). The gate's actual claim count
and unverified-mention count were reconciled against the plan's 5 / 13 and any difference is
explained. `marker_sources()` is module-scope with exactly one definition, and
`every_marker_carrying_source_file_is_in_marker_sources` passes with its detected-file floor
pinned at **2** (the HEAD truth: `error.rs` + `layer_registry.rs`), NOT at `marker_sources()`'s
5-entry count.
No `TODO(117-12)` remains in `layer_registry_selfcheck.rs`. No new `file.rs:<digits>` citation
exists in `layer_force_unavailable.rs` or `layer_registry_meta_test.rs`.
  </done>
</task>

<task type="auto">
  <name>Task 2: scope the D-15 ledger, index rounds 4/6/8, and mark the two deferred items OPEN (items 4+5)</name>
  <files>proj/SPEC-windows-fail-direction-contract.md, crates/nono/src/machine_policy.rs, crates/nono-cli/src/exec_strategy_windows/launch.rs, crates/nono-cli/src/agent_daemon/launch.rs, crates/nono-cli/tests/layer_registry_selfcheck.rs</files>
  <action>
Item 4's ledger rows must land before (or with) item 5's markers — a marker whose ledger row
does not contain `OPEN` fails `every_open_marker_in_code_has_a_ledger_row`.

**ITEM 4 — resolve the D-15 ledger question in the SPEC.**

Read FIRST, do not invent dispositions:
`.planning/phases/117-fail-direction-contract-startup-self-attestation/117-REVIEW.round4.md`,
`117-REVIEW-FIX.round5.md`, `117-REVIEW.round6.md`, `117-REVIEW-FIX.round7.md`,
`117-REVIEW.round8.md`, and `117-REVIEW-FIX.md` (that last file IS the round-9 fix report —
there is no `117-REVIEW-FIX.round9.md`).

**Verify the classification per-finding, and leave a trace.** The verifier judged rounds
4/6/8 to be defects in the phase's own VERIFICATION MACHINERY (its gates, meta-tests,
discovery scans and CI wiring) rather than contract-vs-code divergences. Confirm that
against the artifacts, finding by finding — enumerate EVERY finding ID in rounds 4, 6 and 8,
and for each record its disposition (`verification-machinery` or `contract-vs-code`) and the
artifact line you read it from. A blanket "all machinery" with no per-finding trace is not
verification; a later reader must be able to tell which findings were examined and which
were assumed.

Pay particular attention to round-6 WR-04 ("the enumeration that feeds the D-28 scan still
skips unreadable directories silently"), which is the one finding with a plausible claim to
being production-side — establish from `117-REVIEW-FIX.round7.md` whether the fixed
`collect_files` is the D-28 leak-scan GATE or production code, and disposition it on what
you find. **If any round 4/6/8 finding IS a genuine contract-vs-code divergence, it gets a
real ledger row, not an index line.** Do not use the scoping statement to sweep a real
divergence out of the ledger — that is precisely the "quietly reconciled" outcome D-15
exists to prevent.

Then do BOTH halves (the scoping statement alone loses the pointer):

(a) Add an explicit SCOPING STATEMENT to the `## Contract vs. code discrepancies` section
(the prose block at ~:120-125, before the `| # | Finding |` table): the D-15 ledger records
contract-vs-code divergences — disagreements between what this contract says and what the
confinement code does. Defects in the phase's own verification machinery are recorded in the
committed `117-REVIEW*` / `117-REVIEW-FIX*` artifacts instead, not here. State it as a rule,
so future rounds have a stated disposition rather than an implicit one.

(b) Add a compact INDEX in that same section naming review rounds 4, 6 and 8, each with **its
finding COUNT**, its disposition and a pointer to the committed artifact. The count is
load-bearing: without it the index can silently drop a finding and still read as complete.
Round 8's Critical is CR-01: `every_markdown_file_gated_by_a_test_runs_the_code_jobs`
resolved its file discovery against the DISK instead of the REPOSITORY, so it failed in the
real repo (15 gitignored worktrees under `.claude/worktrees/` plus `.gsd/`) while passing in
a clean worktree; fixed by making `tracked_files()` resolve against `git ls-files -z` and
fail CLOSED (commit `70691d04`). Rounds 4 and 6 come from the artifacts you just read.

SPEC constraints:
- Any `file.rs::Symbol` you add is scanned by `every_spec_symbol_citation_resolves_to_a_real_definition`
  and MUST resolve. Prefer bare function names.
- If you render the index as a Markdown table, its header must NOT be `| # | What was wrong |`
  — that string is how `ledger_rows()` locates the review-fix ledger, and a second match
  would repoint that parser. A bulleted list is the safer shape.
- Introduce no blank line inside the existing review-fix ledger table: a blank line
  TERMINATES a GFM table and drops every row after it (the exact defect WR-04 was written
  to catch). Its floor is `rows.len() >= 40`.

**ITEM 5 — greppable `OPEN` markers for the two operator-deferred items.**

First, make the ledger rows resolvable. Both target rows currently say `**Open operator
decision:**`, which the gate's case-sensitive `contains("OPEN")` does not match:
- `RF-13` (`:262`, fleet-control `RequiredLayers`) — single row, no id collision. Add the
  literal `OPEN` in the WR-10/WR-14 convention, e.g. `**OPEN — recorded, not fixed.**`, and
  keep "Enforcement is NOT implemented" and the stated operator decision intact.
- `CR-02` — **the token goes on the Iteration-6 row at `:299`** (`| CR-02 (Iteration 6,
  code-review fix pass) |`), NOT the Iteration-4 row at `:276`. `| CR-02 ` matches both and
  the gate cannot distinguish them, so a stray `OPEN` on `:276` would satisfy the gate for a
  marker that is about `:299`. This one is genuinely PARTIAL (the drift gate is closed; the
  wiring decision is not taken), so do not overstate it as wholly unfixed: word it so the row
  carries the literal token `OPEN` for the still-open half while preserving the existing
  "Partial: the drift gate is closed; the wiring decision is NOT taken" framing and Option A /
  Option B, which were deliberately not guessed. After editing, CONFIRM that no other
  `| CR-02 ` row contains `OPEN` (`grep -n '^| CR-02 ' proj/SPEC-windows-fail-direction-contract.md`)
  and record that confirmation in the SUMMARY.

Then add the code markers, matching `layer_registry.rs:925` and `error.rs:506` EXACTLY in
shape — `// ⚠ <ID> OPEN (NOT FIXED — grep \`<ID> OPEN\`; recorded in the SPEC's ...)`
followed by a precise statement of what is open and what the fail direction is meanwhile.
The id token must be IMMEDIATELY adjacent to a whole-word ` OPEN`; `CR-02 (OPEN)` or
`RF-13 — OPEN` will parse to nothing and trip the blind-file assertion.

- **`RF-13`** at the site(s) an operator would grep. Place one at the reader —
  `crates/nono/src/machine_policy.rs::read_required_layers` (~:716-739), where the
  `RequiredLayersNotEnforced` warning is emitted — and one at the production gate call site,
  `crates/nono-cli/src/exec_strategy_windows/launch.rs` ~:1585-1586, folded into the
  existing "D-26 tighten-only union: not yet wired at this gate" comment. State the fail
  direction: the union is tighten-only, so passing empty slices cannot fail open — only the
  "force MORE rows to abort" capability is deferred.
- **`CR-02`** at the daemon-facing site an operator would actually grep:
  `crates/nono-cli/src/agent_daemon/launch.rs` ~:925, at the `daemon_attest_and_decide` call.
  State that `nono-agentd` `#[path]`-includes only `agent_daemon/mod.rs`, `telemetry/mod.rs`
  and `agent_daemon/telemetry_init.rs`, never declares `exec_strategy_windows`, and therefore
  cannot link the registry; the binding is the source-text drift gate
  `daemon_expected_rows_are_all_named_by_the_daemon_gate`, not a wiring.

Finally, extend the now module-scope `marker_sources()` in `layer_registry_selfcheck.rs`
with every file that now carries a marker and is not already listed —
`crates/nono/src/machine_policy.rs` and `crates/nono-cli/src/agent_daemon/launch.rs`
(`exec_strategy_windows/launch.rs` is already there). Task 1's item 3b makes this
mechanical: if you forget one, `every_marker_carrying_source_file_is_in_marker_sources`
fails and names the file. The ledger gate has NO hardcoded marker count, so nothing else
needs bumping there.

**You MUST also EDIT item 3b's detected-file floor — confirming it is not enough.** These are
two different quantities and both change here:
  - `marker_sources()` **entries**: 5 → **7** (append `machine_policy.rs` and
    `agent_daemon/launch.rs`; `exec_strategy_windows/launch.rs` is already listed).
  - item 3b's **detected-file floor**: 2 → **5**. After this task the five marker-carrying
    files are `crates/nono/src/error.rs`, `exec_strategy_windows/layer_registry.rs`,
    `crates/nono/src/machine_policy.rs`, `exec_strategy_windows/launch.rs`, and
    `crates/nono-cli/src/agent_daemon/launch.rs`.
Left at 2, the floor silently goes slack the moment this task lands. Prefer an **equality**
pin — `assert_eq!(detected.len(), 5, ..)` — over a floor, matching item 2's equality-pin
idiom: a floor of 5 over a 5-element truth is a pin only by coincidence and stops discriminating
as soon as a sixth marker appears. Record both numbers in the SUMMARY.

**Comments only in production files.** Change no code, no signature, no behavior — and prove
it mechanically, not by assertion: before committing, run the comment-only diff check in
`<verify>` and confirm it produces NO output.
  </action>
  <verify>
    <automated>cd /c/Users/OMack/Nono &amp;&amp; ! ( git diff HEAD -U0 -- crates/nono/src/machine_policy.rs crates/nono-cli/src/exec_strategy_windows/launch.rs crates/nono-cli/src/agent_daemon/launch.rs | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -vE '^[+-][[:space:]]*//' | grep -q . )</automated>
    <automated>cd /c/Users/OMack/Nono &amp;&amp; git diff HEAD -U0 -- crates/nono/src/machine_policy.rs crates/nono-cli/src/exec_strategy_windows/launch.rs crates/nono-cli/src/agent_daemon/launch.rs | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -vE '^[+-][[:space:]]*//' ; echo "(the lines above, if any, are NON-comment changes and must be reverted)"</automated>
    <automated>cd /c/Users/OMack/Nono &amp;&amp; cargo test -p nono-sandbox-cli --test layer_registry_selfcheck --test layer_registry_meta_test</automated>
    <automated>cd /c/Users/OMack/Nono &amp;&amp; grep -rn "OPEN (NOT FIXED" crates/ | wc -l</automated>
    <automated>cd /c/Users/OMack/Nono &amp;&amp; grep -n '^| CR-02 ' proj/SPEC-windows-fail-direction-contract.md</automated>
    <automated>cd /c/Users/OMack/Nono &amp;&amp; cargo check -p nono-sandbox -p nono-sandbox-cli --all-targets</automated>
    <automated>cd /c/Users/OMack/Nono &amp;&amp; cargo fmt --all -- --check</automated>
  </verify>
  <done>
The comment-only diff check **exited 0** (the `! ( ... | grep -q . )` form, whose exit code
carries the verdict: 0 = comment-only, 1 = a non-comment line changed) and its companion
diagnostic printed **NO** offending lines — every line added or removed in the three production
files is a `//` comment line. Note the plain-pipeline form exits 1 on success, which is why the
verdict is taken from the negated form, not from the raw grep. (Run it BEFORE committing
Task 2; if already committed, run it against the pre-task commit.)
`layer_registry_selfcheck` is 21 passed / 0 failed in the REAL TREE, including
`every_open_marker_in_code_has_a_ledger_row` (which now resolves FOUR marker ids — WR-10,
WR-14, CR-02, RF-13 — each against an `OPEN`-marked ledger row),
`every_marker_carrying_source_file_is_in_marker_sources` (detected-file pin now **5**, and
`marker_sources()` now has **7** entries — two different quantities, both recorded) and
`the_wr14_open_record_matches_the_actual_swallow_sites`. The `OPEN (NOT FIXED` grep count
is 4 or more (was 2). Exactly ONE `| CR-02 ` row contains `OPEN`, and it is the Iteration-6
row — confirmed by the grep and recorded in the SUMMARY.
`every_spec_symbol_citation_resolves_to_a_real_definition` and `host_gated_rows_are_loud`
still pass, so nothing added to the SPEC broke a citation or the manual-verification section
scoping. The `## Contract vs. code discrepancies` section states the D-15 scoping rule and
indexes rounds 4, 6 and 8 with their finding COUNTS and dispositions READ FROM the committed
artifacts, and the SUMMARY enumerates every finding ID in those rounds with its disposition
and the artifact line it came from — including whether any was reclassified as a genuine
contract-vs-code divergence (and if so, that it got a real ledger row). `cargo check`
and `cargo fmt --all -- --check` both clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: apply the operator's SC3 override and reconcile SC4's status (item 6)</name>
  <files>.planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md</files>
  <action>
LAST — this task reports what Tasks 1 and 2 actually closed, so it cannot be written before
they land and pass.

**Capture the STRICT-constraint baselines FIRST**, before editing the file, by running each
`grep -c` in `<verify>` against HEAD and recording the numbers. The constraints below are
"nothing was lost" constraints; they can only be checked against a before-count.

Apply the block from the file's own `### Suggested Override (SC3)` section (~:301-307)
verbatim, with `accepted_by: "Oscar Mack Jr"` and `accepted_at: "2026-08-15T00:00:00Z"`.

Frontmatter:
- `overrides_applied: 0` → `1`.
- Add the `overrides:` block with `must_have`, `reason`, `accepted_by`, `accepted_at`
  exactly as that section specifies.
- Update `score:` (currently "2/4 truths fully verified (SC1 VERIFIED, SC2
  VERIFIED-with-disclosed-limits, SC3 PARTIAL 10/13, SC4 PARTIAL)") to reflect SC3
  accepted-via-override and SC4's honest post-Task-1/2 state.
- Update the SC3 entry under `gaps:` — its status becomes accepted-via-override rather than
  `partial`. Keep the full `reason` text: it is the evidence for the acceptance.
- Update the SC4 gap entry's `missing` list against what Tasks 1 and 2 really closed.

Body, for consistency:
- the SC3 row in `### Observable Truths (ROADMAP Success Criteria)`;
- the CINT-03 row in `### Requirements Coverage` (currently "⚠ BLOCKED by its own literal
  wording (10/13)");
- the `### Suggested Override (SC3)` prose — its closing "I have deliberately **not** applied
  this myself ..." is now stale; rewrite it to record that the operator accepted it on
  2026-08-15;
- the `### Gaps Summary` narrative's SC3 paragraph (item 1 of that numbered list), and its
  SC4 paragraph (item 2) where Tasks 1 and 2 changed the facts;
- the `### Anti-Patterns Found` rows for `layer_force_unavailable.rs:110-112` and
  `layer_registry_selfcheck.rs:354-358`, and the `### Deferred Items` table's "Greppable
  `OPEN` marker?" column — all three are now stale.

STRICT CONSTRAINTS:
- Do NOT alter the substance of SC3's evidence. Do NOT soften or restate the 10/13 factual
  finding. The override ACCEPTS a disclosed shortfall; it does not erase it.
- The 3-row remainder (`DaclSessionSidGrant`, `MinifilterAbsence`,
  `BrokerAuthenticodeTrustGate`) stays named and disclosed — each by name, individually.
- The host-blocked WR-20 pin
  (`non_owned_path_with_a_foreign_label_is_exempt_not_a_coverage_gap`) stays disclosed in
  BOTH `gaps` and `human_verification`, and the plain statement that
  `cargo test --bin nono` is RED at HEAD (1688/12) stays. The override is about SC3's ROW
  COVERAGE, not about that test.
- Do NOT touch the SC4 gap entry's accounting beyond what Tasks 1 and 2 legitimately closed.
  If all four SC4 `missing` bullets are closed, say so and update SC4's status honestly;
  if any bullet is left open, say which and why. SC4's disposition must also state, in one
  line, what item 2's gate does NOT cover (the 13 pinned-but-unverified row mentions) — the
  record must not overclaim the gate's reach.
- `.planning/REQUIREMENTS.md` is OUT OF SCOPE — do not edit it. Its CINT-01/CINT-02/CINT-03
  markings (`:119`, `:186`, `:187`, `:188` Pending) now under-state reality. Mention it in
  the SUMMARY as a phase-close follow-up.
- A PARALLEL MILESTONE (v3.5) is open: do not overwrite `.planning/REQUIREMENTS.md` or
  `.planning/ROADMAP.md`, and run no `phases.clear`.
- STATE.md is hand-maintained here: invoke NO SDK state writer (`state.begin-phase`,
  `phase.complete`, unscoped `**Status:**` replacements). The orchestrator handles STATE.md.
  </action>
  <verify>
    <automated>cd /c/Users/OMack/Nono &amp;&amp; grep -n "overrides_applied: 1" .planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md</automated>
    <automated>cd /c/Users/OMack/Nono &amp;&amp; for t in "Oscar Mack Jr" "2026-08-15T00:00:00Z" "DaclSessionSidGrant" "MinifilterAbsence" "BrokerAuthenticodeTrustGate" "non_owned_path_with_a_foreign_label_is_exempt_not_a_coverage_gap" "1688"; do printf '%s\t' "$t"; grep -c "$t" .planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md; done</automated>
    <automated>cd /c/Users/OMack/Nono &amp;&amp; git diff --stat -- .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md</automated>
  </verify>
  <done>
`overrides_applied: 1` with an `overrides:` block carrying the Suggested Override's exact
`must_have` and `reason`, plus `accepted_by: "Oscar Mack Jr"` and
`accepted_at: "2026-08-15T00:00:00Z"`.
**Per-token counts, captured at HEAD before the edit and again after, are recorded in the
SUMMARY as a before/after table, and NO count decreased.** (One `grep -c` per token — a
single alternation would exit 0 on any count ≥ 1 and would pass a rewrite that dropped two
of the three named remainder rows.) `Oscar Mack Jr` and `2026-08-15T00:00:00Z` each go from
0 to ≥ 1.
`score:`, the SC3 `gaps` entry, the SC3 truths-table row, the CINT-03 requirements row, the
Suggested Override prose, the Gaps Summary items 1 and 2, the two Anti-Patterns rows and the
Deferred Items marker column are all consistent with each other and with what Tasks 1-2
closed. SC4's disposition names what item 2's gate does not cover. The 10/13 finding, the
three named remainder rows, the WR-20 host-blocked disclosure and the 1688/12 RED statement
all survive unchanged in substance. `git diff --stat` shows ZERO changes to
`.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` and `.planning/STATE.md`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPEC/contract ↔ confinement code | An inaccurate record lets an operator believe a layer is enforced when it is not (SC2/SC4's whole subject). |
| Coverage declaration ↔ actual tests | A doc naming the wrong coverage list sends an auditor hunting for a test that does not exist (Iteration 4's WR-07). |
| Deferred item ↔ operator discovery | An open deferral with no greppable marker is invisible to `grep`, so its risk is carried silently. |
| Gate ↔ the author who can reword around it | A prose gate whose scope an author can escape by rewording is a gate that reports green while covering nothing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117b0s-01 | Repudiation | `layer_force_unavailable.rs` module doc | mitigate | Item 1 corrects the claim; item 2 adds a sentence-scoped, nearest-preceding gate that verifies 5 claims (3 of them in P6, the paragraph carrying the doc's primary per-row claims), with a claims floor and a mandatory perturbation proof. |
| T-117b0s-01b | Tampering (silent gate evasion) | item 2's gate scope | mitigate | The rule has no ambiguity/skip case, so there is no "name both lists to silence it" escape hatch. Rewording a claim out of gate reach moves it into the unverified-mention count, which is pinned by equality and fails LOUD; perturbation (b) proves this. The unverified remainder is stated in the test's doc comment and in Task 3's SC4 disposition, so the record does not overclaim. |
| T-117b0s-02 | Information disclosure (of a false assurance) | `registry_call_sites_exist` `TODO(117-12)` | mitigate | Item 3 replaces the stale record with what the gate actually does; no `TODO(` left behind. |
| T-117b0s-03 | Repudiation | SPEC D-15 ledger scope | mitigate | Item 4 states the scoping rule AND indexes rounds 4/6/8 with per-round finding COUNTS, requires a per-finding disposition trace in the SUMMARY, and explicitly instructs promotion of any genuine contract-vs-code divergence to a real row rather than sweeping it into the index. |
| T-117b0s-04 | Information disclosure (deferral invisible to grep) | CR-02 / RF-13 | mitigate | Item 5 adds convention-matching markers at the sites an operator greps and makes both ledger rows carry the literal `OPEN` (CR-02's on the Iteration-6 row specifically, with a confirmed-unique check against the `:276` id collision). Item 3b converts `marker_sources()` from an allow-list into a git-tracked discovered class, so a future marker in an unlisted file fails the build. |
| T-117b0s-05 | Tampering | production confinement code | mitigate | No production behavior changes — comments only, enforced by a comment-only `git diff -U0` assertion over the three production files (`cargo check` alone cannot detect a behavior change), plus the full selfcheck/meta-test suites. |
| T-117b0s-06 | Elevation of privilege (scope creep in a record) | `117-VERIFICATION.md` override | mitigate | Item 6 forbids softening the 10/13 finding, keeps the 3-row remainder and the WR-20/1688-12 disclosures — verified per-token with before/after counts rather than a single alternation grep — and bans edits to REQUIREMENTS/ROADMAP/STATE. |
| T-117b0s-SC | Tampering | dependency installs | accept | No package-manager install occurs in this plan; no new dependency of any kind. |
</threat_model>

<verification>
All runs happen IN THE REAL TREE at `C:\Users\OMack\Nono` — not a worktree. Round-8's CR-01
was a defect that ONLY reproduced in the real repo, whose defect condition is the 15
gitignored worktrees under `.claude/worktrees/` plus `.gsd/`. Worktree isolation is disabled
for this task (`workflow.use_worktrees=false`); keep it that way. Item 3b's new gate calls
`tracked_files`, which is `git ls-files` in the workspace root — same reason.

1. `cargo test -p nono-sandbox-cli --test layer_registry_selfcheck --test layer_registry_meta_test --test layer_force_unavailable` — MUST pass. Baseline to beat: `layer_registry_selfcheck` 20 passed / 0 failed at HEAD, 21 after item 3b.
2. `cargo test -p nono-sandbox-cli --features layer-fault-injection --test layer_force_unavailable` — 2 passed / 0 failed.
3. `cargo fmt --all -- --check` — MUST exit 0.
4. `cargo check -p nono-sandbox -p nono-sandbox-cli --all-targets` — clean.
5. The comment-only `git diff -U0` assertion over the three production files — NO output.
6. BOTH item-2 perturbation proofs performed, their failure messages recorded in the SUMMARY, and reverted with `git checkout --`; `git status --porcelain` clean of them afterwards.

DOCUMENTED WINDOWS-HOST BASELINE — do NOT chase as regressions:
`cargo test -p nono-sandbox-cli --bin nono` is **1688 passed / 12 failed / 2 ignored** at
HEAD (6 `config::tests` env races, 3 `protected_paths::tests`,
`profile_cmd::tests::test_init_allowed_when_pack_has_same_short_name`,
`audit_session::tests::discover_sessions_does_not_warn_when_legacy_audit_root_is_empty`, and
the host-blocked WR-20 pin). `-p nono-sandbox` is 844 passed / 0 failed. This plan does not
require running that suite.

Cross-target clippy is NOT required: no file touched contains a
`#[cfg(target_os = "linux")]` or `#[cfg(target_os = "macos")]` block (verified: grep count 0
in each of the four production files), none is under `exec_strategy/` or `bindings/c/src/`.
If execution ends up touching such a file after all, CLAUDE.md's cross-target rule applies
and both gates must be run locally.

Commits: DCO sign-off `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`; Conventional
Commit titles with a lowercase-initial subject.
</verification>

<success_criteria>
- All four SC4 `missing` bullets are closed, or any left open is named with a reason.
- The coverage-list prose is correct AND machine-checked by a SENTENCE-scoped, nearest-
  preceding-list-name gate that verifies 5 claims (3 inside P6), states its residual in its
  own doc comment, pins that residual at 13 by equality, and has two recorded perturbation
  proofs — one for a wrong claim, one for a claim reworded out of reach.
- `marker_sources()` has exactly one definition, at module scope, and every tracked
  `crates/*/src/**.rs` file carrying a marker line is in it, asserted by a discovery gate.
- No `TODO(117-12)` remains; the refreshed prose describes HEAD behavior.
- The SPEC states the D-15 scoping rule and indexes rounds 4/6/8 with finding counts and
  dispositions read from the committed artifacts — with any genuine contract-vs-code
  divergence promoted to a real ledger row instead of an index line.
- `grep -rn "OPEN (NOT FIXED" crates/` returns 4+ markers; all resolve to `OPEN` ledger rows;
  exactly one `| CR-02 ` row carries `OPEN` and it is the Iteration-6 row.
- `117-VERIFICATION.md` carries the applied SC3 override with the operator's name and date,
  and its body is internally consistent, with every disclosed shortfall intact and no
  per-token count decreased.
- Zero production confinement behavior changed, proved by the comment-only diff assertion.
  Zero edits to `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`.
</success_criteria>

<output>
Create `.planning/quick/260815-b0s-close-the-phase-117-sc4-recording-gap-an/260815-b0s-SUMMARY.md` when done.

The SUMMARY MUST record:
- BOTH item-2 perturbations: what was changed, the exact failure message, and the revert.
- Item 2's actual verified-claim list (row → list, per claim) and its unverified-mention
  list, reconciled against the plan's expected 5 / 13, with any difference explained.
- EVERY finding ID in review rounds 4, 6 and 8, each with its disposition
  (`verification-machinery` | `contract-vs-code`) and the artifact line it was read from;
  and whether any was reclassified as a genuine contract-vs-code divergence (and if so, the
  ledger row added).
- Confirmation that exactly one `| CR-02 ` ledger row contains `OPEN`, and that it is the
  Iteration-6 row.
- The four `OPEN` marker sites, the `marker_sources()` hoist, and BOTH counts kept distinct:
  `marker_sources()` entries **5 → 7**, and item 3b's detected marker-carrying files
  **2 → 5** (naming the five). Plus confirmation that
  `every_open_marker_in_code_has_a_ledger_row` has no hardcoded marker count needing a bump.
- The comment-only diff assertion's exit code (must be 0 via the negated form) and its
  diagnostic output (must be empty) for the three production files.
- Task 3's per-token before/after grep table, showing no count decreased.
- Test counts before/after for all three test binaries, and that they ran in the REAL TREE.
- Follow-up for phase close: `.planning/REQUIREMENTS.md` CINT-01/CINT-02 markings (`:119`,
  `:186`, `:187`) now under-state reality and CINT-03 (`:188`) is Pending — deliberately
  left untouched by this task.
</output>
