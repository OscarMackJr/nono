---
phase: quick-260624-p1c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.lock
  - .cargo/audit.toml   # only if fallback allow-list path is taken
autonomous: true
requirements:
  - RUSTSEC-2026-0185-remediation
must_haves:
  truths:
    - "`cargo audit` exits 0 with no `error:` lines for RUSTSEC-2026-0185"
    - "Workspace builds without errors (`cargo build --workspace` succeeds)"
    - "`Cargo.lock` reflects the updated or allow-listed state and is committed"
  artifacts:
    - path: "Cargo.lock"
      provides: "Updated lockfile with patched quinn-proto (preferred) or unchanged if allow-listed"
    - path: ".cargo/audit.toml"
      provides: "Advisory ignore list (only modified on fallback path)"
  key_links:
    - from: "reqwest"
      to: "quinn → quinn-proto"
      via: "transitive Cargo dependency chain"
      pattern: "quinn-proto.*0\\.11\\."
---

<objective>
Resolve the `cargo audit` hard error for RUSTSEC-2026-0185 (remote memory exhaustion in
`quinn-proto` via unbounded out-of-order stream reassembly). The advisory fires on a
transitive dependency: `reqwest → quinn (0.11.9) → quinn-proto (0.11.14)`.

Purpose: Green CI `Cargo Audit` job, which is currently the only hard `error:` (4 other
advisories are `unmaintained` warnings already tolerated by CI and are OUT OF SCOPE here).

Output: Either (a) an updated `Cargo.lock` pointing at a patched `quinn-proto` 0.11.x
release, OR (b) `.cargo/audit.toml` extended with a documented ignore entry if no patched
0.11.x is yet published. Lockfile change is the deliverable; commit it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/todos/pending/20260624-cargo-audit-quinn-proto.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm dep chain and attempt semver-compatible lockfile update</name>
  <files>Cargo.lock</files>
  <action>
    Run the following commands in sequence and record their output before acting:

    1. Confirm the full dependency chain:
       `cargo tree -i quinn-proto --workspace`
       Expected: `reqwest → quinn 0.11.x → quinn-proto 0.11.14`. If the chain differs,
       note it — the rest of the task still applies.

    2. Check what patched versions are available:
       `cargo update -p quinn-proto --dry-run 2>&1`
       This shows whether a semver-compatible 0.11.x release exists in the registry that
       is newer than 0.11.14 and contains the fix.

    3a. If a patched 0.11.x release is available (dry-run shows an update):
        Run: `cargo update -p quinn-proto`
        This writes the new version into `Cargo.lock` without touching any `Cargo.toml`.
        Verify the lock was actually updated:
        `grep -A2 'name = "quinn-proto"' Cargo.lock`
        The version line must show a version higher than 0.11.14.

    3b. If `cargo update -p quinn-proto` reports nothing to update (no newer 0.11.x patch
        published yet), proceed to Task 2 (allow-list fallback). Do NOT attempt to bump
        `quinn` or `reqwest` — that is a semver-breaking change that requires research and
        is out of scope.

    Do NOT touch Cargo.toml under any crate. Do NOT attempt to fix the four
    `unmaintained` warnings (RUSTSEC-2025-0052, RUSTSEC-2025-0057, RUSTSEC-2024-0436,
    RUSTSEC-2025-0134) — those are out of scope for this task.
  </action>
  <verify>
    <automated>cargo update -p quinn-proto --dry-run 2>&1 &amp;&amp; grep -A2 'name = "quinn-proto"' Cargo.lock</automated>
  </verify>
  <done>
    Either (a) `Cargo.lock` shows `quinn-proto` at a version above 0.11.14 (update path),
    OR (b) dry-run confirms no 0.11.x update is available and Task 2 is needed (allow-list
    path). Both outcomes are valid — the task is done when the situation is determined and
    acted on.
  </done>
</task>

<task type="auto">
  <name>Task 2 (fallback only): Allow-list RUSTSEC-2026-0185 in .cargo/audit.toml</name>
  <files>.cargo/audit.toml</files>
  <action>
    Execute this task ONLY if Task 1 step 3b applies (no patched 0.11.x release exists).

    The workspace already has `.cargo/audit.toml` with an `[advisories] ignore` array
    (currently holds two AWS-LC advisories with documented rationale comments). Extend it
    with RUSTSEC-2026-0185 using the same comment pattern:

    Add inside the `ignore = [...]` array:
      "RUSTSEC-2026-0185", # quinn-proto remote memory exhaustion — no patched 0.11.x
                           # published as of YYYY-MM-DD; reqwest → quinn → quinn-proto
                           # chain; revisit when 0.11.x patch is released upstream.

    Fill in the actual date. The rationale must state:
    - No patched 0.11.x was available in the registry at the time of this fix.
    - The dependency chain (reqwest → quinn → quinn-proto).
    - A "revisit when" condition tied to an upstream patch release.

    This is a FALLBACK path only. Prefer the lockfile update in Task 1. Allow-listing a
    medium-severity memory-exhaustion advisory is acceptable only when the upstream patch
    does not yet exist — and only with the documented revisit condition above.

    After editing, run `cargo audit` and confirm the output changes from `error:` to
    `warning:` for RUSTSEC-2026-0185 (or it disappears entirely from output).
  </action>
  <verify>
    <automated>cargo audit 2>&amp;1 | grep -v "RUSTSEC-2026-0185" | grep "^error:" ; echo "exit:$?"</automated>
  </verify>
  <done>
    `.cargo/audit.toml` contains RUSTSEC-2026-0185 in the ignore array with a rationale
    comment. `cargo audit` produces no `error:` line for that advisory.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify workspace build and commit the fix</name>
  <files>Cargo.lock, .cargo/audit.toml</files>
  <action>
    After either Task 1 (lockfile update) or Task 2 (allow-list) is complete:

    1. Run a full workspace build to confirm nothing regressed:
       `cargo build --workspace 2>&amp;1`
       The build must succeed (exit 0). If it fails, do not commit — diagnose and fix.
       Note: this is Windows dev host; Linux/macOS cfg-gated code is not compiled here.
       Cross-target clippy is a CI concern and is out of scope for this quick task.

    2. Run `cargo audit` one final time and confirm:
       - Exit code is 0 (no errors — only warnings are acceptable).
       - The output does NOT contain `RUSTSEC-2026-0185` under an `error:` heading.
       - The four `unmaintained` warnings may still appear — that is expected and correct.

    3. Stage and commit the changed files:
       - If lockfile update path: stage `Cargo.lock` only.
       - If allow-list path: stage both `Cargo.lock` (unchanged) and `.cargo/audit.toml`.
       Use this commit message format:
         ```
         fix(deps): bump quinn-proto past RUSTSEC-2026-0185

         `cargo update -p quinn-proto` advances the lockfile to quinn-proto X.Y.Z,
         which contains the fix for remote memory exhaustion via unbounded out-of-order
         stream reassembly.

         Signed-off-by: Oscar Mack Jr &lt;oscar.mack.jr@gmail.com&gt;
         ```
       OR for the allow-list fallback:
         ```
         fix(audit): allow-list RUSTSEC-2026-0185 (no patched 0.11.x available)

         No patched quinn-proto 0.11.x is yet published. Added a documented ignore
         entry in .cargo/audit.toml with a revisit condition. The four unmaintained
         warnings remain and are out of scope.

         Signed-off-by: Oscar Mack Jr &lt;oscar.mack.jr@gmail.com&gt;
         ```

    Close the todo file at `.planning/todos/pending/20260624-cargo-audit-quinn-proto.md`
    by moving it to `.planning/todos/done/` after the commit succeeds.
  </action>
  <verify>
    <automated>cargo audit 2>&amp;1 | grep "^error:" ; echo "audit_errors_above (empty=good)"</automated>
  </verify>
  <done>
    `cargo audit` produces no `error:` lines. `cargo build --workspace` succeeds.
    Changes are committed with DCO sign-off. Todo is moved to done/.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crates.io registry → Cargo.lock | Dependency resolution fetches from the public registry; lockfile pins the exact version+checksum |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-p1c-01 | Tampering | Cargo.lock update via `cargo update` | accept | `cargo update` pins the new checksum in Cargo.lock; registry checksums are verified by Cargo against crates.io index |
| T-quick-p1c-02 | Denial of Service | quinn-proto RUSTSEC-2026-0185 | mitigate | This entire task is the mitigation — update to patched version or document allow-list with revisit |
| T-quick-p1c-SC | Tampering | npm/pip/cargo installs | mitigate | `cargo update -p quinn-proto` is a scoped lockfile-only update; no new package-manager install beyond pulling the patched version from crates.io (already in the trust boundary) |
</threat_model>

<verification>
Final state verification:
1. `cargo audit` exits 0, no `error:` lines for any advisory.
2. `cargo build --workspace` exits 0.
3. `git log -1` shows the fix commit with DCO `Signed-off-by:` line.
4. `.planning/todos/done/20260624-cargo-audit-quinn-proto.md` exists.
</verification>

<success_criteria>
- `Cargo Audit` CI check will pass for RUSTSEC-2026-0185 (the sole hard error).
- The four `unmaintained` warnings (async-std, fxhash, paste, rustls-pemfile) are
  deliberately left untouched — they are out of scope and were pre-existing.
- No Cargo.toml files were edited (lockfile-only fix, or audit.toml allow-list).
- Workspace continues to build cleanly on Windows dev host.
</success_criteria>

<output>
Create `.planning/quick/260624-p1c-cargo-audit-bump-quinn-proto-past-rustse/260624-p1c-SUMMARY.md` when done.
</output>
