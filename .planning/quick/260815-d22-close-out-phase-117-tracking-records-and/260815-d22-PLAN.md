---
quick_id: 260815-d22
type: execute
plan: 01
wave: 1
depends_on: []
autonomous: true
requirements: [CINT-01, CINT-02, CINT-03]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md
  - .github/workflows/ci.yml

must_haves:
  truths:
    - "A reader scanning REQUIREMENTS.md's status table sees CINT-01/02/03 as done, and sees that CINT-03 was ACCEPTED via operator override rather than met on its own terms."
    - "ROADMAP's Phase 117 bullet and Progress row both say the phase is complete, with the true end state (round 9, re-verified 2026-08-15, one human item open) instead of the stale 'awaiting iteration 6'."
    - "Every place 117-VERIFICATION.md describes the WR-20 blocker says the coverage already exists and only a RUN is missing, blocked on pushing a branch 684 commits ahead of origin — not that CI wiring must be built."
    - "The ci.yml windows-layer-fault-injection job carries a comment recording that it is the only Windows Rust-unit-test gate in CI and that it is what covers the WR-20 pin."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "CINT-01/02/03 flipped in checklist and traceability table"
      contains: "operator override"
    - path: ".github/workflows/ci.yml"
      provides: "WR-20 coverage comment on windows-layer-fault-injection"
      contains: "WR-20"
  key_links:
    - from: ".github/workflows/ci.yml windows-layer-fault-injection"
      to: "labels_guard.rs WR-20 pin"
      via: "unfiltered `cargo test -p nono-sandbox-cli --features layer-fault-injection`"
      pattern: "WR-20"
---

<objective>
Close out Phase 117's tracking records and sharpen the WR-20 finding from "a runner must be
found" to "the coverage exists; only a run is missing, blocked on an operator push."

Purpose: The records currently understate what shipped (CINT-01/02/03 read `Pending` while the
phase is verified) and mis-state what is blocking the one open human item. Both are misleading in
opposite directions.

Output: Four documentation edits. No code changes. No `git push`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md

Read the frontmatter `overrides:` block, the SC3 `gaps:` entry, `human_verification:`, the
Observable Truths table and the Requirements Coverage table. That file is the source of truth for
what CINT-01/02/03 actually achieved. Do not re-derive the verdicts — transcribe them.
</context>

<facts_established>
These were confirmed live before planning. Cite them; do not re-derive them.

- The WR-20 test `non_owned_path_with_a_foreign_label_is_exempt_not_a_coverage_gap` is at
  `crates/nono-cli/src/exec_strategy_windows/labels_guard.rs:1188`, inside `#[cfg(test)] mod tests`
  (declared :567-570), carrying ONLY `#[test]`. The file's `#[cfg(feature = "layer-fault-injection")]`
  occurrences (including :595) each sit on a single item, and there are NO nested `mod` declarations
  inside `mod tests` — so nothing feature-gates this test.
- `.github/workflows/ci.yml:387` is
  `cargo test -p nono-sandbox-cli --features layer-fault-injection -- --test-threads=1` — no
  `--bin`/`--test`/`--lib` filter, so it runs every target in the package, including the `nono`
  bin's unit tests.
- The `test` job matrix at `ci.yml:129-131` is `[ubuntu-latest, macos-latest]` — no Windows.
- Branch `milestone/v2.13-carryforward-closeout` is 684 commits ahead of `origin`, 0 behind. Last
  CI run on it: 2026-07-29, which predates Phases 115/116/117 entirely.
- The 12 `cargo test --bin nono` failures at HEAD = 11 documented Windows-host baseline + the
  host-blocked WR-20 pin.
</facts_established>

<constraints>
- **NO `git push`.** Local commits only.
- **A PARALLEL MILESTONE IS OPEN (v3.5, Phases 101-107).** `.planning/REQUIREMENTS.md` and
  `.planning/ROADMAP.md` are SHARED. Use targeted Edit calls on the specific lines. NEVER overwrite
  either file, never regenerate a section, never reorder rows.
- **Do NOT invoke any SDK state/roadmap writer** — no `state.begin-phase`, no `phase.complete`, no
  `roadmap.annotate-dependencies`, no unscoped `**Status:**` replacement. Those have previously
  DELETED tracked milestone blocks on this project. Hand-Edit only; this deviation is deliberate
  and is the ninth consecutive phase to use it.
- Do NOT edit `.planning/STATE.md`.
- No production code changes. `.github/workflows/ci.yml` is comment-only.
- DCO sign-off on every commit: `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`.
  Conventional Commit titles, lowercase-initial subject.
- Before starting, confirm `git status --porcelain` is clean and record the output.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Flip the Phase 117 tracking records in REQUIREMENTS.md and ROADMAP.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Six line edits in REQUIREMENTS.md, two in ROADMAP.md. Targeted Edit calls only — do not rewrite
either file.

**`.planning/REQUIREMENTS.md` checklist (:119, :120, :121):** change `- [ ] **CINT-01**` →
`- [x] **CINT-01**`, and the same for CINT-02 and CINT-03. Change nothing else on those lines —
the requirement prose stays byte-identical.

**`.planning/REQUIREMENTS.md` traceability table (:186, :187, :188):** replace the `Pending` cell
in each of the three CINT rows. Keep the `| CINT-0N | Phase 117 |` prefix and the trailing `|`
exactly as-is; only the status cell changes.

- `:186` CINT-01 → `Complete`
- `:187` CINT-02 → `Complete (two operator deferrals recorded: CR-02 daemon wiring, RF-13 fleet-control RequiredLayers)`
- `:188` CINT-03 → `Complete via operator override 2026-08-15 — 10/13 rows automated; 3 host/structurally-gated rows (DaclSessionSidGrant, MinifilterAbsence, BrokerAuthenticodeTrustGate) accepted, not met`

CINT-03's cell is the one piece of judgment here. Its literal bar ("a contract entry with no such
test is not satisfied") is STILL UNMET at 10/13. The wording above mirrors `117-VERIFICATION.md`'s
`overrides:` block. A reader scanning only this table must not come away thinking CINT-03 passed on
its own terms — if you reword, preserve that property.

**Touch no other requirement.** TSBX-01/TSBX-02 at `:184-185` are also stale (Phase 116 is Complete
but they read `Pending`). Leave them exactly as they are and flag them in the SUMMARY as a
follow-up. Do not touch any DRAIN, RCPT or BOUND row.

**`.planning/ROADMAP.md:41` — the Phase 117 bullet.** Change `- [ ]` → `- [x]`. Keep every existing
factual detail: 34/34 plans, gap-closure round 3 EXECUTED 2026-08-11 (waves 12-14), CR-03 (BLOCKER)
and WR-12..WR-21, D-37 as the three-arm ancestor classification, the 1624 passed / 12 failed suite
figure and the host-blocked WR-20 note. Replace ONLY the stale trailing clause
`Awaiting code review iteration 6 + re-verification` with the true end state — something like:

`Code review ran to round 9; re-verified 2026-08-15 (117-VERIFICATION.md, status: human_needed): SC1/SC2 VERIFIED, SC3 ACCEPTED via operator override (still factually 10/13), SC4 VERIFIED with disclosed limits — its recording gap closed the same day by quick task 260815-b0s. One human item open: the WR-20 CI run.`

**`.planning/ROADMAP.md:435` — the Progress table row.** `26/34` → `34/34`; status
`Gap-closure round 3 planned` → `Complete (SC3 via operator override; 1 human item open)`;
completed date `-` → `2026-08-15`. Match the column formatting of the 115/116 rows at `:433-434`.

Commit both files together: `docs(117): close out the phase tracking records`.
  </action>
  <verify>
    <automated>
# Exact change counts: 6 added lines in REQUIREMENTS, 2 in ROADMAP.
test "$(git diff -U0 -- .planning/REQUIREMENTS.md | grep -cE '^\+[^+]')" = 6 &&
test "$(git diff -U0 -- .planning/REQUIREMENTS.md | grep -cE '^-[^-]')" = 6 &&
test "$(git diff -U0 -- .planning/ROADMAP.md | grep -cE '^\+[^+]')" = 2 &&
test "$(git diff -U0 -- .planning/ROADMAP.md | grep -cE '^-[^-]')" = 2 &&
# No v3.5 (Phases 101-107) content and no other requirement family appears in either diff.
! ( git diff -U0 -- .planning/REQUIREMENTS.md .planning/ROADMAP.md \
    | grep -E '^[+-][^+-]' \
    | grep -qE 'TSBX-0|DRAIN-0|RCPT-0|BOUND-0|Phase 10[1-7]|^\| 10[1-7]\.' ) &&
# CINT-03's cell records acceptance, not a clean pass.
grep -q 'CINT-03 | Phase 117 | Complete via operator override' .planning/REQUIREMENTS.md &&
# The stale clause is gone from the ROADMAP bullet.
! grep -q 'Awaiting code review iteration 6' .planning/ROADMAP.md &&
echo TASK1_VERIFIED
    </automated>
  </verify>
  <done>
CINT-01/02/03 read `- [x]` at :119-121 and carry completion states at :186-188, with CINT-03's cell
explicitly recording operator acceptance of the 10/13 shortfall. ROADMAP:41 is checked with the
round-9 / re-verified-2026-08-15 end state, and ROADMAP:435 reads `34/34` / `Complete (SC3 via
operator override; 1 human item open)` / `2026-08-15`. TSBX-01/02 and every v3.5 row are
byte-unchanged. One commit, DCO-signed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record the WR-20 finding — coverage exists, only a run is missing</name>
  <files>.planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md, .github/workflows/ci.yml</files>
  <action>
Today the records say the WR-20 pin "needs an elevated/CI Windows runner", which reads as *work to
be done* — find or build a runner. The truth is narrower and more actionable: **the coverage
already exists; only a run is missing, and the run is blocked on pushing the branch.** This edit
sharpens *what is blocking*. It must NOT downgrade the finding.

Update the THREE places `117-VERIFICATION.md` states this, and only those three:

1. **Frontmatter `human_verification:` item 1** (`:176-178`) — the `test:` / `expected:` /
   `why_human:` triple.
2. **The SC3 `gaps:` entry's `missing:` bullet that begins `"STILL OPEN: ..."`** (`:75`).
3. **`### Human Verification Required` item 1** (`:359-361`).

Each of the three must carry all four of these points:

- The test is **not feature-gated** — `labels_guard.rs:1188`, a plain `#[test]` inside
  `#[cfg(test)] mod tests` — and it lives in the `nono` bin's unit tests.
- CI job `windows-layer-fault-injection` (`.github/workflows/ci.yml:343-388`) **already runs it by
  construction**, because its cargo invocation carries no `--bin`/`--test`/`--lib` filter and
  therefore runs every target in the package.
- **No new CI wiring is required.** What is missing is a RUN: the branch is 684 commits ahead of
  origin and the last CI run on it (2026-07-29) predates the entire phase, so the job has never
  executed this test. Pushing is an operator decision, which is why this stays a human item.
- **Bounding facts, so the job's evidentiary value is not overstated:** the `test` job matrix
  (`ci.yml:129-131`) is `[ubuntu-latest, macos-latest]`, making `windows-layer-fault-injection`
  the ONLY job running Rust unit tests on Windows anywhere in CI; and because it runs the whole
  package suite, the other 11 documented Windows-host baseline failures will surface there too.
  State that the 6 `config::tests` env races may well pass under its `--test-threads=1` as an
  explicit **PREDICTION, NOT A MEASUREMENT** — use that framing verbatim in at least the
  `why_human` field and the `missing` bullet.

**Preserve every existing disclosure verbatim in substance**, in each of the three places that
already carries it:
- the test still fails LOUDLY on this host with a full explanatory message per D-31 rather than
  skipping (this host runs a non-elevated domain account; `icacls /setowner` fails for both SYSTEM
  and `BUILTIN\Administrators`);
- `cargo test --bin nono` is still RED at HEAD (1688 passed / 12 failed);
- WR-20 stays **authored-but-unverified** until a real run exists;
- in the SC3 `missing` bullet specifically, keep the existing clause that the operator override
  does not touch this item — the override is about SC3's ROW COVERAGE, not about this test.

Do not touch the SC3 `artifacts:` entries, the `overrides:` block, the Observable Truths table, or
any other section. `human_verification` items 2 and 3 are unchanged.

**Then add a comment to `.github/workflows/ci.yml`.** Insert it immediately above the
`windows-layer-fault-injection:` job key at `:343`, at the job key's indentation. Record two facts:
(a) this is the only job in the workflow that runs Rust unit tests on Windows — the `test` job
matrix is `[ubuntu-latest, macos-latest]`; and (b) because the cargo invocations below carry no
target filter, they run the `nono` bin's unit tests, which is what covers the Phase 117 WR-20 pin
(`labels_guard.rs::non_owned_path_with_a_foreign_label_is_exempt_not_a_coverage_gap`) — a test that
needs `SeTakeOwnershipPrivilege` and is host-blocked on the dev box, so it needs a RUN, not extra
wiring. Add the corollary that this job therefore also inherits the documented Windows-host
baseline failures. The point is that the fact is discoverable at the job, not only in a planning
document.

The job already carries precedent-setting explanatory comments — the NR-08 broker-build note
(`:367-373`) and the CR-10 note above the test step (`:377-384`). Match that voice and placement
style.

**Comment only. Change no job logic, no matrix, no command, no `runs-on`, no `if:`.**

Commit both files together: `docs(117): record that the WR-20 pin needs a CI run, not CI wiring`.
  </action>
  <verify>
    <automated>
# (a) ci.yml is comment-only at the text level. Negated so exit code carries the verdict.
! ( git diff -U0 -- .github/workflows/ci.yml | grep -E '^[+-]' \
    | grep -vE '^(\+\+\+|---)' | grep -vE '^[+-][[:space:]]*#' | grep -q . ) &&
# (b) ci.yml still parses AND is semantically identical to HEAD — parsed, not eyeballed.
python -c "
import subprocess, yaml
new = yaml.safe_load(open('.github/workflows/ci.yml', encoding='utf-8'))
old = yaml.safe_load(subprocess.run(['git','show','HEAD:.github/workflows/ci.yml'],
                                    capture_output=True, text=True, check=True).stdout)
assert new == old, 'ci.yml semantic content changed - must be comment-only'
print('CI_YAML_PARSES_AND_IS_SEMANTICALLY_IDENTICAL')
" &&
# (c) The comment names the pin at the job.
grep -q 'WR-20' .github/workflows/ci.yml &&
# (d) All three VERIFICATION locations carry the sharpened framing.
test "$(grep -c 'windows-layer-fault-injection' .planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md)" -ge 4 &&
test "$(grep -ci 'no new ci wiring\|not ci wiring\|wiring is required' .planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md)" -ge 3 &&
test "$(grep -c '684 commits' .planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md)" -ge 3 &&
# (e) Prediction is labelled as such, not stated as measured.
grep -qi 'PREDICTION, NOT A MEASUREMENT' .planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md &&
# (f) Existing disclosures survive.
grep -q '1688 passed / 12 failed' .planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md &&
grep -qi 'authored-but-unverified' .planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md &&
grep -q 'D-31' .planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md &&
# (g) No production code touched.
! ( git diff --name-only HEAD~2 | grep -qE '^(crates|bindings)/' ) &&
echo TASK2_VERIFIED
    </automated>
  </verify>
  <done>
All three WR-20 locations in `117-VERIFICATION.md` state that the coverage already exists, that
`windows-layer-fault-injection` runs the test by construction, that no new CI wiring is required
and only a run is missing (branch 684 commits ahead, last run 2026-07-29), plus the two bounding
facts with the `config::tests` claim labelled a prediction. Every prior disclosure (loud D-31
failure, RED `--bin nono` at 1688/12, authored-but-unverified) survives. `ci.yml` gained a
comment above the job and parses to a structure byte-identical to HEAD's. One commit, DCO-signed.
  </done>
</task>

</tasks>

<verification>
Before starting: `git status --porcelain` is clean; record the output in the SUMMARY.

After both tasks:
- `git status --porcelain` shows nothing unexpected (no stray files, no unstaged leftovers).
- `git diff HEAD~2 --stat` lists exactly four files: `.planning/REQUIREMENTS.md`,
  `.planning/ROADMAP.md`, the Phase 117 `117-VERIFICATION.md`, and `.github/workflows/ci.yml`.
- No `git push`. Confirm the branch is still N commits ahead of origin with nothing pushed.

No test run is required — there are no code changes.
</verification>

<success_criteria>
- CINT-01/02/03 are marked done in both REQUIREMENTS.md locations, with CINT-03 recording operator
  acceptance rather than a clean pass.
- ROADMAP's Phase 117 bullet and Progress row are complete and accurate as of 2026-08-15.
- The WR-20 finding reads as "a run is missing" everywhere it appears in `117-VERIFICATION.md`,
  with the blocker correctly identified as the unpushed branch, and with no disclosure weakened.
- `ci.yml` records the WR-20 coverage fact at the job, comment-only, semantically unchanged.
- Two DCO-signed commits. Nothing pushed.
</success_criteria>

<output>
Create `.planning/quick/260815-d22-close-out-phase-117-tracking-records-and/260815-d22-SUMMARY.md`
when done. Flag in it as follow-up: **TSBX-01/TSBX-02 at `.planning/REQUIREMENTS.md:184-185` still
read `Pending` although Phase 116 is Complete** — deliberately left untouched by this task.
</output>
