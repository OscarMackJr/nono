---
phase: 260802-e9u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/109-proxy-network-absorb/109-AWS-SIGV4-TLS-INTERCEPT-FINDING.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "The 3 stale .continue-here.md files no longer exist on disk"
    - "108-DIVERGENCE-LEDGER.md no longer dispositions 6fb7ecbf or 23d93fc9 as adopt anywhere a disposition column exists"
    - "108-DIVERGENCE-LEDGER.md's global totals row arithmetic sums to exactly 100"
    - "REQUIREMENTS.md NET-03 reads the corrected non-applicable wording verbatim from the finding doc, with its checkbox state unchanged"
    - "ROADMAP.md Phase 109 SC3 reflects the confirmed-non-applicable posture instead of claiming #1430/#1437 are absorbed"
    - "109-AWS-SIGV4-TLS-INTERCEPT-FINDING.md records that both proposed corrections were operator-approved and applied, without deleting the original proposal text"
  artifacts:
    - path: ".planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md"
      provides: "Corrected won't-sync dispositions for 6fb7ecbf/23d93fc9, corrected NET disposition summary, corrected global totals row, corrected DOCS row for 63c9589f"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected NET-03 requirement wording"
    - path: ".planning/ROADMAP.md"
      provides: "Corrected Phase 109 SC3 wording"
  key_links:
    - from: ".planning/REQUIREMENTS.md NET-03"
      to: "109-AWS-SIGV4-TLS-INTERCEPT-FINDING.md"
      via: "wording cites the finding doc as evidence for non-applicable disposition"
      pattern: "109-AWS-SIGV4-TLS-INTERCEPT-FINDING\\.md"
---

<objective>
Clear 3 stale, untracked `.continue-here.md` checkpoint files, and apply the 2 pending Phase 109
amendments that the finding document `109-AWS-SIGV4-TLS-INTERCEPT-FINDING.md` proposed and the
operator has now approved: correcting the Phase 108 ledger's `adopt` dispositions for
`6fb7ecbf`/`23d93fc9` to `won't-sync (target subsystem absent)` (plus every dependent count/summary
that references those dispositions), and correcting `REQUIREMENTS.md` NET-03's wording (with a
matching `ROADMAP.md` Phase 109 SC3 correction, since it makes the identical false claim).

Purpose: These two commits (#1430, #1437) target `crates/nono-proxy/src/aws/sign.rs` and
`crates/nono-proxy/src/tls_intercept/handle.rs` — neither file exists in this fork. The ledger,
REQUIREMENTS.md, and ROADMAP.md currently claim they were/are "absorbed" or dispositioned `adopt`,
which is false and independently re-verified false in the finding document. This plan makes the
project's planning artifacts consistent with that verified reality. It is a docs-only correction:
no source code, no requirement/phase completion status changes.

Output: 3 stale files deleted; `108-DIVERGENCE-LEDGER.md` disposition/summary/totals corrected;
`REQUIREMENTS.md` NET-03 and `ROADMAP.md` Phase 109 SC3 reworded; finding doc annotated APPLIED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-proxy-network-absorb/109-AWS-SIGV4-TLS-INTERCEPT-FINDING.md
@.planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@./CLAUDE.md

**Important — `.planning/STATE.md` is hand-maintained** (it tracks two parallel milestones, v3.5
and v3.6, in one file). Do NOT run any SDK state-mutating query against it as part of this plan
(no `phase.complete`, `requirements.mark-complete`, `state.update`, etc.), and never touch its
`parallel_milestone*` frontmatter keys. This plan does not touch STATE.md at all.

**This plan does not flip any checkbox.** `- [ ]`/`- [x]` states in REQUIREMENTS.md, ROADMAP.md,
and the ledger's bucket dispositions for anything other than the two named SHAs must be preserved
byte-for-byte. This is a wording/disposition correction, not a completion event.

**DCO sign-off is mandatory on every commit** per CLAUDE.md: `Signed-off-by: Oscar Mack Jr
<oscar.mack.jr@gmail.com>`. Use lowercase-initial Conventional Commit subjects.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete 3 stale, untracked .continue-here.md checkpoint files</name>
  <files>
    .continue-here.md
    .planning/.continue-here.md
    .planning/phases/108-upst12-divergence-audit/.continue-here.md
  </files>
  <action>
    Delete these 3 files. All three are UNTRACKED (confirmed via `git ls-files --error-unmatch` on
    each — all three return "not tracked"), so deleting them produces no git diff and needs no
    commit. Before deleting, re-confirm each is still untracked with `git status --short` (if any
    file shows as tracked, STOP and report — do not delete a tracked file silently). Each file is
    stale relative to `.planning/STATE.md` (the authoritative, far newer state record):
    1. `.continue-here.md` (repo root, dated 2026-07-03) — describes v3.5 Phase 104; superseded by
       later v3.5 progress recorded in STATE.md.
    2. `.planning/.continue-here.md` (dated 2026-06-11) — describes the v2.10 milestone close;
       that milestone shipped and archived long ago.
    3. `.planning/phases/108-upst12-divergence-audit/.continue-here.md` (dated 2026-07-28) — says
       "Phase 108 not yet planned"; Phase 108 is COMPLETE (5/5 plans, verification passed).
  </action>
  <verify>
    <automated>test ! -f .continue-here.md && test ! -f .planning/.continue-here.md && test ! -f .planning/phases/108-upst12-divergence-audit/.continue-here.md && echo ALL_DELETED</automated>
  </verify>
  <done>All 3 files absent from disk; `git status --short` shows no new entries for them (they were never tracked).</done>
</task>

<task type="auto">
  <name>Task 2: Correct Phase 108 ledger dispositions for 6fb7ecbf/23d93fc9</name>
  <files>.planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md</files>
  <action>
    Re-confirm current line numbers with Grep for `6fb7ecbf` and `23d93fc9` before editing (they
    may have shifted from the approximate numbers below). Apply these edits:

    1. **NET Cluster — Per-Commit Table** (~line 686-687): in the row for
       `6fb7ecbf36e5d18760d8084b7f4900e93582004c` (#1430) and the row for
       `23d93fc96abf795d672c712e9c1834a8f97aa0aa` (#1437), change the `disposition` column value
       from `adopt` to `won't-sync (target subsystem absent)` for both rows. Rewrite each row's
       final "re-export scan" cell — it currently asserts the diff is "Clean" as if being adopted;
       replace with a note that the target file (`aws/sign.rs` for 6fb7ecbf, `tls_intercept/
       handle.rs` for 23d93fc9) is absent from the fork's tree, per
       `109-AWS-SIGV4-TLS-INTERCEPT-FINDING.md` (Phase 109 re-verification, 2026-07-29).

    2. **NET disposition summary** (~line 697-698): currently "**NET disposition summary:** 1
       ADAPT (`3b207eeb` #1374, per ADR-108) + 11 adopt. All 12 rows map to exactly one of ...".
       Change the count to: 1 ADAPT + 9 adopt + 2 won't-sync (target subsystem absent) = 12.
       Preserve every other assertion in that paragraph (the mapping-to-NET-01/02/03/none claim,
       "Zero blank cells", the `windows-touch: no` grep claim) — only the count needs correcting.

    3. **DOCS Cluster row for `63c9589fa30f0244c542c026faf27d517aaa9b39`** (~line 1334, "docs
       (credential-injection): document AWS SigV4 proxy signing (#1329)"): its reconcile-note
       currently claims "the fork already adopted the code this documents (`6fb7ecbf`, SigV4
       URI-encoding fix, NET cluster, Plan 108-03 — dispositioned `adopt`); this doc should land
       alongside NET absorb in Phase 109, not independently". That claim is now false. Replace it
       to mirror the established precedent in this same table for `73952cd4` (the tool-sandbox
       README doc, dispositioned `needs-doc-follow-up, **deferred**` because it "would describe a
       feature the fork does not have"): reclassify this row's reconcile-note prefix to
       `needs-doc-follow-up, **deferred**`, stating that `6fb7ecbf` is now dispositioned
       `won't-sync (target subsystem absent)` — the fork has no `aws/sign.rs` SigV4-signing
       implementation for this doc to describe (per `109-AWS-SIGV4-TLS-INTERCEPT-FINDING.md`) —
       and that this doc should be held unless/until full AWS SigV4 signing is ever built in a
       future phase, then reconciled alongside it. Also update the **DOCS disposition summary**
       paragraph immediately below the table (currently: "4 needs-doc-follow-up ... 1
       needs-doc-follow-up-but-deferred (README tool-sandboxing section, held for v3.7)") to count
       this row correctly: 3 needs-doc-follow-up + 2 needs-doc-follow-up-but-deferred (README
       tool-sandboxing section held for v3.7, AND this SigV4 doc held pending a future AWS SigV4
       phase) + 3 safe-to-ignore = 8. Do not change the per-bucket table header count ("DOCS | 8 |
       ...") in the Headline section — 8 total rows is still correct, only the sub-tally within the
       summary paragraph shifts.

    4. **Global totals row** (~line 31, the Headline `| **Total** | **100** | ...` row): currently
       "overall: adopt 21 (11 NET + 8 PROF + 2 CORE) / adapt 3 (1 NET + 2 CORE) / DEFERRED->v3.7 20
       ... / DEFERRED->proposed-Phase-112 18 / individually-reviewed-non-CODE 38 ... Arithmetic:
       21+3+20+18+38 = 100." Change to: adopt 19 (9 NET + 8 PROF + 2 CORE) / adapt 3 (1 NET + 2
       CORE) / won't-sync (target subsystem absent) 2 (2 NET) / DEFERRED->v3.7 20 / DEFERRED->
       proposed-Phase-112 18 / individually-reviewed-non-CODE 38. New arithmetic:
       19+3+2+20+18+38 = 100. Also update the per-bucket NET row directly above (currently
       "| NET | 12 | 11 adopt + 1 adapt ... |") to "| NET | 12 | 9 adopt + 1 adapt + 2 won't-sync
       (target subsystem absent) ... |", preserving the existing `3b207eeb` ADR-108 citation.

    Do NOT touch the tables at ~line 349-350, ~line 560-561, or ~line 1524-1525 — these are
    `sha | subject | ...` inventory/requirement-mapping tables with no `disposition` column, so
    they need no correction (verify this by re-reading each table's header row before skipping it).
  </action>
  <verify>
    <automated>grep -n "won't-sync (target subsystem absent)" .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md | wc -l</automated>
  </verify>
  <done>
    Both NET-cluster rows for 6fb7ecbf/23d93fc9 read `won't-sync (target subsystem absent)` in
    their disposition column; the NET disposition summary reads 1 ADAPT + 9 adopt + 2 won't-sync =
    12; the DOCS row for 63c9589f no longer claims 6fb7ecbf was adopted; the global totals row's
    arithmetic explicitly verified to sum to exactly 100 (19+3+2+20+18+38=100), confirmed by manual
    addition before considering this task complete.
  </done>
</task>

<task type="auto">
  <name>Task 3: Correct REQUIREMENTS.md NET-03, ROADMAP.md Phase 109 SC3, and annotate the finding doc APPLIED</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/phases/109-proxy-network-absorb/109-AWS-SIGV4-TLS-INTERCEPT-FINDING.md
  </files>
  <action>
    Re-confirm current line numbers with Grep for "SigV4 encoded-URI" in both REQUIREMENTS.md and
    ROADMAP.md before editing (approximate line numbers below may have shifted).

    1. **REQUIREMENTS.md NET-03** (~line 115): replace ONLY the sentence text after the
       `**NET-03**:` label, preserving the `- [ ] **NET-03**:` checkbox prefix exactly as-is (do
       not flip it, do not touch the `| NET-03 | Phase 109 | Pending |` status-table row at ~line
       165). Replace the current wording:

       "The SigV4 encoded-URI generation fix (#1430) and the sibling-route cross-deny fix (#1437)
       are absorbed; HTTP/2 injection, `HTTP_PROXY` forward-proxy, and `no_proxy` bypass are
       verified non-regressed; `maturin` + `napi` binding builds are green."

       with this wording, copied VERBATIM from the finding document's "REQUIREMENTS.md NET-03
       correction proposal" section:

       "The SigV4 encoded-URI generation fix (#1430) and the sibling-route cross-deny fix (#1437)
       are confirmed non-applicable — both target upstream subsystems (`aws/sign.rs` full SigV4
       signing, `tls_intercept/handle.rs` multi-route TLS-intercept dispatch) absent from the
       fork's architecture, per `109-AWS-SIGV4-TLS-INTERCEPT-FINDING.md`; `no_proxy` bypass and
       `HTTP_PROXY` forward-proxy are verified non-regressed; `maturin` + `napi` binding builds are
       green."

       This deliberately drops the original "HTTP/2 injection" clause — this is intentional and
       operator-approved. The finding doc explains why (the clause traces to an upstream commit
       message context, not a fork-verified behavior distinct from the forward-proxy path). Flag
       this drop explicitly in the plan SUMMARY.md so the operator can restore the clause later if
       they disagree with dropping it.

    2. **ROADMAP.md Phase 109 SC3** (~line 236): currently "3. The SigV4 encoded-URI fix (#1430)
       and sibling-route cross-deny fix (#1437) are absorbed; HTTP/2, `HTTP_PROXY` forward-proxy,
       and `no_proxy` bypass are verified non-regressed." Reword to match the corrected NET-03
       posture — SC3 is satisfied via "confirmed non-applicable, with evidence" rather than a code
       change (this is the finding doc's own framing in its "ROADMAP Phase 109 SC3 satisfaction
       statement" section). Suggested replacement: "3. The SigV4 encoded-URI fix (#1430) and
       sibling-route cross-deny fix (#1437) are confirmed non-applicable — both target upstream
       subsystems (`aws/sign.rs`, `tls_intercept/handle.rs`) absent from the fork's architecture,
       per `109-AWS-SIGV4-TLS-INTERCEPT-FINDING.md`; `no_proxy` bypass and `HTTP_PROXY`
       forward-proxy are verified non-regressed." Do not flip the Phase 109 checklist box (already
       `[x]`, per the 5/5 plans line at ~line 34 — leave as-is) and do not alter any other SC line.

    3. **109-AWS-SIGV4-TLS-INTERCEPT-FINDING.md**: this document currently frames both corrections
       as "proposed ... for operator approval" and states "This plan does not edit
       `108-DIVERGENCE-LEDGER.md`" (in the "Second correction proposal" section) and "This plan
       does NOT edit `.planning/REQUIREMENTS.md`" (in the "REQUIREMENTS.md NET-03 correction
       proposal" section). Add a short dated note near the top of the document (directly under the
       existing "Status:" line, before "## Summary") recording: both corrections were approved by
       the operator and applied on 2026-08-02 via quick task `260802-e9u`, with a one-line pointer
       to each of the two sections it changed (ledger disposition, REQUIREMENTS.md NET-03 wording)
       plus a note that ROADMAP.md Phase 109 SC3 received the matching correction, and that the
       "HTTP/2 injection" clause was deliberately dropped from NET-03's wording per operator
       approval (restorable if the operator later wants a distinct HTTP/2-injection check). Do NOT
       delete or rewrite the original "proposed" prose in either section below — it is the
       reasoning record for why the correction was made; the new note is purely additive.
  </action>
  <verify>
    <automated>grep -c "confirmed non-applicable" .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
  </verify>
  <done>
    REQUIREMENTS.md NET-03 reads the corrected wording verbatim (checkbox untouched, status table
    untouched); ROADMAP.md Phase 109 SC3 reads the corrected non-applicable wording (checklist box
    untouched); the finding doc has an APPLIED note dated 2026-08-02 referencing quick task
    260802-e9u, with all original proposal prose intact below it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Docs-only correction to planning artifacts; no code, no build, no external input, no trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260802e9u-01 | Tampering (data integrity) | 108-DIVERGENCE-LEDGER.md arithmetic | mitigate | Task 2's `<done>` requires explicit manual verification that the corrected global-totals arithmetic sums to exactly 100 before the task is considered complete. |
| T-260802e9u-02 | Repudiation (silent scope drift) | REQUIREMENTS.md/ROADMAP.md checkbox state | mitigate | Task 3 explicitly instructs no checkbox flips; context section states this constraint globally; done-criteria confirm checkbox/status-table state is unchanged. |
</threat_model>

<verification>
1. `git status --short` confirms no entries for the 3 deleted `.continue-here.md` files (untracked, so deletion is invisible to git).
2. `grep -n "adopt" .planning/phases/108-upst12-divergence-audit/108-DIVERGENCE-LEDGER.md` around the NET Cluster table no longer shows `adopt` for the `6fb7ecbf`/`23d93fc9` rows — both now read `won't-sync (target subsystem absent)`.
3. Manually re-add the global totals row's numbers (19+3+2+20+18+38) and confirm the sum is 100.
4. `grep -n "NET-03" .planning/REQUIREMENTS.md` shows the corrected non-applicable wording with checkbox `- [ ]` unchanged.
5. `grep -n "SigV4 encoded-URI" .planning/ROADMAP.md` shows the corrected SC3 wording under Phase 109.
6. `grep -n "APPLIED" .planning/phases/109-proxy-network-absorb/109-AWS-SIGV4-TLS-INTERCEPT-FINDING.md` shows the new dated note, and the original "proposed" sections remain present below it.
</verification>

<success_criteria>
- 3 stale `.continue-here.md` files no longer exist on disk (no git diff, since untracked).
- 108-DIVERGENCE-LEDGER.md: both `6fb7ecbf`/`23d93fc9` rows read `won't-sync (target subsystem absent)`; NET disposition summary reads 1 ADAPT + 9 adopt + 2 won't-sync = 12; DOCS row for `63c9589f` no longer falsely claims `6fb7ecbf` was adopted; global totals row arithmetic sums to exactly 100.
- REQUIREMENTS.md NET-03 reads the corrected non-applicable wording verbatim from the finding doc; checkbox and status-table row unchanged.
- ROADMAP.md Phase 109 SC3 reads the corrected non-applicable wording; Phase 109 checklist box unchanged (stays `[x]`).
- 109-AWS-SIGV4-TLS-INTERCEPT-FINDING.md carries a dated APPLIED note (2026-08-02, quick task 260802-e9u) without deleting original proposal prose.
- No source files touched; no build/test/clippy run needed (docs-only task).
</success_criteria>

<output>
Create `.planning/quick/260802-e9u-clear-3-stale-continue-here-md-files-and/260802-e9u-SUMMARY.md` when done. Explicitly flag in the summary that NET-03's "HTTP/2 injection" clause was dropped per operator approval, so the operator sees this called out even though it was pre-approved.
</output>
