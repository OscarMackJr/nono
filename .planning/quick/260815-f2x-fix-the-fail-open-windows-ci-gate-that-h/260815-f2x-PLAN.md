---
quick_id: 260815-f2x
type: execute
plan: 01
wave: 1
depends_on: []
autonomous: true
requirements: [CINT-03]
files_modified:
  - .github/workflows/ci.yml
  - .planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md
  - proj/SPEC-windows-fail-direction-contract.md

must_haves:
  truths:
    - "In `windows-layer-fault-injection`, a failure of the nono-sandbox-cli leg fails the job even when the nono-shell-broker leg passes — because the two cargo invocations are now separate steps, each with its own exit-code check."
    - "In `windows-packaging`, a failed `cargo build --release -p nono-sandbox-cli` fails the job even when the broker build succeeds."
    - "A re-run of the multi-command-step class scan over ci.yml flags exactly one Windows step (the MSI-contract step, allowlisted with a stated reason), and the SAME scan run against `git show HEAD:.github/workflows/ci.yml` flags three — proving the scan can find the defect class it reports absent."
    - "Every comment line that existed in the two edited jobs before the change still exists after it, exactly once, attached to the step it now describes."
    - "No record in the tree still asserts that the pre-fix `windows-layer-fault-injection` job could fail the build: 117-VERIFICATION.md's SC3 evidence and the SPEC both state that as run on 2026-08-15 the gate was fail-open, that it is fixed by this task, and that the job is separately RED with 22 failures pending triage."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "One cargo command per step in both Windows jobs, plus a do-not-recombine note"
      contains: "one command per step"
    - path: ".planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md"
      provides: "Corrected SC3 evidence about the CI gate (verdict, score and override untouched)"
      contains: "fail-open"
    - path: "proj/SPEC-windows-fail-direction-contract.md"
      provides: "Dated D-15 ledger row for the false 'fails the build' claim"
      contains: "2026-08-15"
  key_links:
    - from: ".github/workflows/ci.yml windows-layer-fault-injection"
      to: "SPEC RF-11/RF-12 'fail the build' claim"
      via: "one cargo invocation per step, so each exit code is checked"
      pattern: "layer-fault-injection"
---

<objective>
Close a fail-open verification gate found inside Phase 117's own CI wiring, then correct the three
records that assert the gate works.

CI run 31888431118 on `milestone/v2.13-carryforward-closeout` reported `conclusion: success` for
job `windows-layer-fault-injection` while that job's own log reported
`test result: FAILED. 1684 passed; 22 failed`. The step "Run layer-fault-injection suites" has no
`shell:` override, so on `windows-latest` it runs under pwsh, and GitHub Actions ends a pwsh step
with a trailing `exit $LASTEXITCODE` — the step's status is the exit code of the LAST native
command only. (`$ErrorActionPreference='stop'` governs cmdlets, not native-executable exit codes,
so the first failure neither aborted nor propagated.) The passing `nono-shell-broker` leg reset
`$LASTEXITCODE` to 0 and turned the job green over 22 real failures.

Purpose: this is the gate Phase 117 leans on for CINT-03, and it is a fail-open verification gate
recorded as a working one — the exact defect class Phase 117 exists to close, now found in Phase
117's own machinery. Three records assert it works and are false as written.

Output: one cargo command per step in the two affected Windows jobs; corrected SC3 evidence in
117-VERIFICATION.md; a dated D-15 ledger row in the SPEC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

Files to edit — these three and NOTHING else:
- `.github/workflows/ci.yml`
- `.planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md`
- `proj/SPEC-windows-fail-direction-contract.md`

<already_established>
Do NOT re-derive these. They were established before planning and are inputs, not questions.

1. **The mechanism is proven by observation**, not by reasoning about shell semantics. The live run
   is the proof. Do not go re-reading GitHub Actions docs to confirm it.

2. **The class scan is already done.** Every Windows job's steps were parsed with pyyaml. Exactly
   two instances of the swallowing shape exist, plus one false positive:
   - (a) `windows-layer-fault-injection` / "Run layer-fault-injection suites" — 2 `cargo test`,
     no `shell:` → pwsh default. CONFIRMED fail-open by the live run.
   - (b) `windows-packaging` / "Build Windows release binaries" — 2 `cargo build`
     (`-p nono-sandbox-cli` then `-p nono-shell-broker`), `shell: pwsh`. Same latent shape: a
     failed nono-sandbox-cli build passes the step if the broker build succeeds, letting a broken
     `nono.exe` reach packaging. Unproven only because that job is currently red for other
     reasons. Same class, same fix.
   - (c) NOT a defect — `windows-packaging` / "Validate Windows MSI contract". Its guard uses an
     explicit `exit 1` and the `.ps1` invocation is the final command. LEAVE IT ALONE.

3. **Tooling is present and verified on this host:** `python` 3.12.10 with `pyyaml` 6.0.2. Use
   `python`, never `python3` (a Microsoft Store alias stub that exits 49).
</already_established>

<current_shape>
`.github/workflows/ci.yml`, job `windows-layer-fault-injection` (job key at :355), final step:

```
      - name: Run layer-fault-injection suites
        run: |
          cargo test -p nono-sandbox-cli --features layer-fault-injection -- --test-threads=1
          cargo test -p nono-shell-broker --features layer-fault-injection -- --test-threads=1
```

Job `windows-packaging` (job key at :402):

```
      - name: Build Windows release binaries
        shell: pwsh
        env:
          RUSTFLAGS: -Dwarnings -C target-feature=+crt-static
        run: |
          cargo build --release -p nono-sandbox-cli
          cargo build --release -p nono-shell-broker
```

The three load-bearing comment blocks in scope (line numbers as of HEAD):
- **WR-20 coverage comment**, :343-354 — job-level header immediately above the
  `windows-layer-fault-injection:` key. Added earlier today by quick task 260815-d22. States this
  is the ONLY job running Rust unit tests on Windows anywhere in CI, that the cargo invocations
  carry no `--bin`/`--test`/`--lib` filter and therefore cover the WR-20 pin
  (`labels_guard.rs::non_owned_path_with_a_foreign_label_is_exempt_not_a_coverage_gap`), and that
  the job consequently inherits the documented Windows-host baseline failures.
- **NR-08 broker-build note**, :379-385 — above `Build nono-shell-broker (release)` in the same
  job. That step is NOT being split; this comment does not move.
- **CR-10 note**, :389-396 — above `Run layer-fault-injection suites`. Two distinct claims: the
  coverage claim ending *"The SPEC claims these 'fail the build'; this is the build."*, and the
  `--test-threads=1` rationale (each force-unavailable seam is a process-global `AtomicBool`).

`windows-packaging`'s "Build Windows release binaries" step carries NO comment. The MSI-contract
step's inline comments (:437-442) are inside a step that is not being touched.
</current_shape>

<gates_that_read_these_files>
Two live unit tests in `crates/nono-cli/src/main.rs` scan
`proj/SPEC-windows-fail-direction-contract.md` by `include_str!`. Task 2's new ledger row must not
trip either. These are hard constraints on the row's WORDING:

- `the_spec_ledger_does_not_point_abort_path_layers_at_the_event_log` (:602) — fails if any line
  starting with `|` mentions the Windows Application event log without an attached qualifier.
  **Rule for the new row: do not mention the event log at all.**
- `the_spec_never_prescribes_the_medium_label_command` (:431) — scans for
  `/setintegritylevel Medium`. **Rule: do not mention it.**
- `every_spec_symbol_citation_resolves_to_a_real_definition`
  (`crates/nono-cli/tests/layer_registry_selfcheck.rs`) content-verifies every
  `file.rs::Symbol`-shaped citation anywhere in the SPEC. **Rule: the new row cites `ci.yml` job
  and step NAMES and the run URL — no `something.rs::Symbol` citations.**

`layer_registry_selfcheck.rs`'s ci.yml gates parse only the `changes` job's `run_code_jobs`
classifier region, which this task does not touch. Task 1's "no change outside the two job
regions" check is what proves that.
</gates_that_read_these_files>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split each multi-command Windows step into one step per command, preserving every comment</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Split the two swallowing steps so the runner checks each exit code independently. Use the
one-step-per-command shape, NOT `shell: bash` and NOT a `$LASTEXITCODE` guard: it removes the
shell-semantics reasoning entirely and cannot silently regress when someone later appends a third
command.

**(a) `windows-layer-fault-injection` / "Run layer-fault-injection suites" → two steps.**
Suggested names (keep them parallel and explicit about the package):
`Run layer-fault-injection suites (nono-sandbox-cli)` and
`Run layer-fault-injection suites (nono-shell-broker)`.
Each gets a single-line `run:` carrying its cargo command **byte-identical** to today's —
same package, same `--features layer-fault-injection`, same `-- --test-threads=1`, same order
(nono-sandbox-cli first). Add no `shell:` key (today's step has none; keep the default). Do not
touch `runs-on`, `needs`, `if:`, the checkout/toolchain/cache steps, or the broker-build step.

**(b) `windows-packaging` / "Build Windows release binaries" → two steps.**
Suggested names: `Build release binary: nono-sandbox-cli` and
`Build release binary: nono-shell-broker` (do not reuse the fault-injection job's
`Build nono-shell-broker (release)` name — same-name steps in different jobs read confusingly in
logs). Commands unchanged and in the same order. **Both new steps MUST carry `shell: pwsh` and a
full copy of the `env: RUSTFLAGS: -Dwarnings -C target-feature=+crt-static` block** — dropping
RUSTFLAGS from either step silently changes what is built. Duplicating that `env` block is
required and is not a comment duplication.

**Comment re-homing — a hard requirement. Do not delete, do not duplicate.**
- The WR-20 job-header comment (:343-354) stays exactly where it is, above the
  `windows-layer-fault-injection:` job key. Its claim ("the cargo invocations below carry no
  `--bin`/`--test`/`--lib` target filter") remains true of both new steps. If any wording reads as
  singular in a way the split falsifies, adjust that wording minimally in place — do not move the
  block.
- The NR-08 broker-build note (:379-385) does not move.
- The CR-10 note (:389-396) moves as ONE block above the FIRST of the two new test steps: it
  introduces the pair, and its `--test-threads=1` rationale covers both. Do not copy it onto the
  second step. Instead, give the second step a short NEW one-line comment tying it back
  (e.g. `# Second leg of the CR-10 pair above; same --test-threads=1 rationale.`).

**Record WHY the split exists** (task requirement, and the CR-10 claim only becomes true once the
split lands). Add one or two NEW lines inside the CR-10 block — after the "this is the build"
sentence — saying in substance: one cargo command per step, deliberately; a pwsh step's status is
the exit code of its LAST native command, so combining these into one `run:` block makes a failing
first leg invisible when the second passes — observed live on run 31888431118, where 22 failures
reported `conclusion: success`. Do not recombine. Add the equivalent short note above the
`windows-packaging` split too, so the same reasoning is discoverable there.
  </action>
  <verify>
    <automated>
Run all five from the repo root. Every one must pass before the commit.

1. YAML parses (parse it, do not eyeball):
   `python -c "import yaml;yaml.safe_load(open('.github/workflows/ci.yml',encoding='utf-8'));print('YAML OK')"`

2. Class scan, POST-fix — expected: exactly one flagged step, the MSI-contract step:
```
python - <<'EOF'
import yaml
d=yaml.safe_load(open('.github/workflows/ci.yml',encoding='utf-8'))
hits=[]
for jid,j in d['jobs'].items():
    if 'windows' not in str(j.get('runs-on','')): continue
    for s in j.get('steps',[]):
        r=s.get('run')
        if not r: continue
        sh=str(s.get('shell','pwsh'))
        n=len([l for l in r.splitlines() if l.strip() and not l.strip().startswith('#')])
        if sh not in ('bash','sh') and n>=2: hits.append((jid,s.get('name'),sh,n))
for h in hits: print('FLAG',h)
print('TOTAL',len(hits))
EOF
```
   PASS = TOTAL 1 and the single flag is `windows-packaging` / `Validate Windows MSI contract`.
   That one is allowlisted: its guard uses an explicit `exit 1` and the `.ps1` invocation is the
   final command. Record the allowlist and its reason in the SUMMARY.

3. **Non-vacuity of the scan — mandatory.** Run the IDENTICAL scan body against the pre-fix file:
   `git show HEAD:.github/workflows/ci.yml > "$SCRATCH/ci-head.yml"` then re-run the block above
   reading `$SCRATCH/ci-head.yml`. PASS = TOTAL 3, flagging
   `Run layer-fault-injection suites` and `Build Windows release binaries` alongside the MSI step.
   A scan that finds nothing must be shown capable of finding something. If the HEAD run does not
   flag all three, the scan is broken — fix the scan, not the report.

4. Comment-line accounting, before vs after. Extract every `#`-comment line inside the two edited
   job regions from `git show HEAD:.github/workflows/ci.yml` and from the working tree, and diff
   the two lists. PASS = zero removals, zero duplications; every addition is one of the new
   do-not-recombine / second-leg lines. Paste the accounting into the SUMMARY as an explicit
   before→after table, one row per pre-existing comment block (WR-20 header, NR-08, CR-10).

5. Change scope: `git diff --stat` shows `.github/workflows/ci.yml` only, and `git diff` shows
   hunks only inside the two job regions — no hunk in the `changes` job's `run_code_jobs`
   classifier, no `runs-on`/`needs`/`if:`/matrix change, and every cargo command string unchanged.
   Confirm `git status --porcelain` carries nothing unexpected before and after.
    </automated>
  </verify>
  <done>
Both jobs run one cargo command per step. The post-fix class scan flags exactly the MSI step; the
same scan flags three instances at HEAD. YAML parses. Every pre-existing comment line survives
exactly once at the step it now describes, accounted for in a before→after table. Only ci.yml is
modified. No test run is needed — no code changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the three records that assert the gate worked</name>
  <files>.planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md, proj/SPEC-windows-fail-direction-contract.md</files>
  <action>
Task 1 must land first so this task describes what actually shipped (cite the real post-split step
names and line numbers).

**2a — `.github/workflows/ci.yml`** is already handled by Task 1 (the CR-10 claim becomes true once
split, plus the do-not-recombine note). Nothing further here.

**2b — `117-VERIFICATION.md`. Correct the EVIDENCE, not the verdict.**
Do NOT touch: `status`, `score`, `overrides`, `overrides_applied`, `re_verification`, the SC3 row
coverage statement (10/13 stands), or the operator acceptance. Do not restate SC3's row coverage.
The four places whose evidence leans on this job:

1. Frontmatter `gaps[0].reason` (~:49-51): *"CI job `windows-layer-fault-injection`
   (`.github/workflows/ci.yml:343-388`) runs the feature legs, so the SPEC's 'or that test fails
   the build' claim is backed by an actual build."* — false as run. It RAN the legs but could not
   FAIL on them.
2. Frontmatter `gaps[0].missing`, second bullet (~:75) — the "ALREADY runs it by construction"
   claim. The by-construction COVERAGE claim stays true (the unfiltered invocation does run the
   WR-20 pin); what is false is the implied gating value. Correct the gating half, keep the
   coverage half.
3. Required Artifacts table row for `.github/workflows/ci.yml` (~:270), currently `✓ VERIFIED`.
4. Human Verification item 1 (~:359-377), including the `ci.yml:343-388` / `ci.yml:387` citations.

Each correction must state three facts: (i) as run on 2026-08-15 (run 31888431118) the job was
FAIL-OPEN — a pwsh step's status is its last native command's exit code, so the passing broker leg
masked the nono-sandbox-cli leg's failure and the job reported success over
`1684 passed; 22 failed`; (ii) it is FIXED by quick task 260815-f2x, which splits the commands
into one step each — cite the new step names, not just line numbers; (iii) separately and still
open, the job is RED with 22 failures whose triage is an explicit follow-up, out of scope here.

Prefer job+step-name citations over line numbers (line numbers shifted in Task 1). Where a line
range is kept, update it to post-split reality and date it. Also update the SPEC line-count
parenthetical in the Required Artifacts table (~:260, "was 344 lines; 372 after 2026-08-15") to
the post-2c count, since 2c adds a row.

**2c — `proj/SPEC-windows-fail-direction-contract.md`. Add a dated correction in the D-15
ledger's established style.**

*The routing call, made here and not defaulted.* The ledger's scoping statement (:127-138) reserves
D-15 for contract-vs-code divergences and routes verification-machinery defects to the committed
`117-REVIEW*` artifacts — but it also states that **a finding that is both gets a row here as
well**. This finding is both, and the deciding fact is that THIS DOCUMENT'S OWN TEXT is false:
RF-11's resolution cell (:315) claims *"a `Windows Layer Fault Injection` CI job now run that
build"* and RF-12's (:316) claims *"the `make` target and CI job above run the rest"* as the
remedy for *"none of this phase's own evidence executed."* As run, the job could not fail on that
evidence, so the remedy those cells assert is not the remedy that shipped. The in-document
precedent is decisive and runs both ways: pure-machinery findings (rounds 4, 6 and 8 — 20
findings, including round 6's WR-04 fail-open `read_dir` inside a gate) earned NO row and were
indexed in prose instead; findings where the SPEC'S OWN CELLS were false (RF-10's stale
"Deferred to Plan 117-10/117-04" cells, WR-19's stale citation) DID earn rows. This one falsifies
the SPEC's own cells. It gets a row. **The SUMMARY must restate this reasoning and name the
precedent** — getting it wrong in either direction is itself the failure mode: sweeping a real
divergence out of the ledger, or diluting the ledger with machinery noise.

Row requirements:
- Append to the review-fix ledger table (the RF-/WR-/CR- table ending at :355), matching the
  existing two-column `| # | What was wrong | What changed |` style, dense evidence, re-runnable
  commands with the date they were run.
- Identifier: continue the established convention (an `(Iteration …)` / quick-task attribution in
  the `#` cell, e.g. `CI-01 (2026-08-15, quick task 260815-f2x)` — match whatever the neighbouring
  rows' shape actually is at HEAD; do not invent a scheme that clashes).
- State plainly that RF-11's and RF-12's resolution cells overclaimed, cite run 31888431118 and
  `test result: FAILED. 1684 passed; 22 failed` against `conclusion: success`, name the mechanism
  in one sentence, and state the fix (one cargo command per step) with the new step names.
- State the still-open half: the job is RED with 22 failures; triage is a separate follow-up. Do
  not claim the suite passes.
- Also correct RF-11's and RF-12's own resolution cells in place with a short dated pointer to the
  new row (the RF-10 precedent for correcting a stale cell rather than leaving it to be
  contradicted elsewhere in the document). Do not rewrite their "What was wrong" columns.
- **Wording constraints (live gates read this file — see `<gates_that_read_these_files>`):** the
  new row and the edited cells must contain no mention of the Windows Application event log, no
  `/setintegritylevel Medium`, and no `file.rs::Symbol`-shaped citation.
  </action>
  <verify>
    <automated>
1. 117-VERIFICATION.md frontmatter still parses as YAML:
```
python - <<'EOF'
import yaml
p='.planning/phases/117-fail-direction-contract-startup-self-attestation/117-VERIFICATION.md'
t=open(p,encoding='utf-8').read().split('---\n',2)
d=yaml.safe_load(t[1])
print('FM OK', d['status'], d['overrides_applied'], len(d['gaps']))
EOF
```
   PASS = parses, `status: human_needed`, `overrides_applied: 1`, gap count unchanged from HEAD.

2. Verdict untouched — diff the frontmatter's protected keys against HEAD:
   `git show HEAD:<path>` into the scratch dir, parse both, and assert `status`, `score`,
   `overrides`, `overrides_applied`, `re_verification` compare EQUAL. Any inequality is a
   scope violation — revert it.

3. No stale claim survives:
   `grep -n "backed by an actual build" <verification path>` → 0 hits, or the hit is inside an
   explicitly-dated correction that negates it.
   `grep -rn "343-388\|ci.yml:387" <verification path>` → every remaining hit is a dated
   historical citation, not a present-tense claim.

4. SPEC gate-wording compliance (cheap mechanical stand-in for the two `main.rs` scanners, which
   would otherwise require building a crate that is currently RED). On the diff of
   `proj/SPEC-windows-fail-direction-contract.md` only:
   `git diff -U0 proj/SPEC-windows-fail-direction-contract.md | grep '^+' | grep -in "event log\|setintegritylevel"` → 0 hits.
   `git diff -U0 proj/SPEC-windows-fail-direction-contract.md | grep '^+' | grep -n "\.rs::"` → 0 hits.
   State in the SUMMARY that this is a wording pre-check, not a test run, and why.

5. Scope: `git status --porcelain` shows only the three named files across both tasks. No
   production code touched. No `.planning/REQUIREMENTS.md`, no `.planning/ROADMAP.md`, no
   `.planning/STATE.md` — a parallel milestone (v3.5) is open and those files must not move. Do
   not run any SDK state/roadmap writer.

6. Commit locally only. **Do NOT `git push`** — the operator decides when to push and re-run CI.
   `git commit -s` stamps the wrong name on this host (`user.name` is `oscarmackjr-twg`); write
   the trailer explicitly: `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`.
   Conventional Commit title, lowercase-initial subject, e.g.
   `fix(ci): split multi-command windows steps so each exit code is checked`.
    </automated>
  </verify>
  <done>
No record in the tree still asserts the pre-fix job could fail the build. 117-VERIFICATION.md's
four SC3 evidence sites state the fail-open-as-run fact, the fix, and the separate 22-failure RED
status — with `status`/`score`/`overrides`/`re_verification` and SC3's 10/13 row coverage
byte-identical to HEAD. The SPEC carries a dated D-15 ledger row plus in-place pointers on RF-11
and RF-12, and the SUMMARY justifies the D-15-vs-review-artifact routing by naming the
in-document precedent. Work committed locally with the explicit DCO trailer; nothing pushed.
  </done>
</task>

</tasks>

<out_of_scope>
Do NOT: investigate or fix the 22 failing tests (agreed separate follow-up); touch the
clippy/cross-compile failures (separate follow-up); change any `runs-on`, matrix, `needs`, or
`if:`; alter the cargo commands themselves (same flags, same order, same `--test-threads=1`);
touch the MSI-contract step; change production code; edit `.planning/REQUIREMENTS.md`,
`.planning/ROADMAP.md` or `.planning/STATE.md`; run any GSD SDK state or roadmap writer; push.
</out_of_scope>

<verification>
- `.github/workflows/ci.yml` parses as valid YAML via pyyaml (parsed, not eyeballed).
- The class scan flags exactly one Windows step post-fix (the allowlisted MSI-contract step) and
  exactly three at HEAD — the scan is proven capable of finding the class it reports absent.
- Comment-line before/after diff on the two edited jobs accounts for every comment: zero removed,
  zero duplicated, additions limited to the new do-not-recombine / second-leg lines.
- 117-VERIFICATION.md frontmatter parses; `status`, `score`, `overrides`, `overrides_applied` and
  `re_verification` are equal to HEAD.
- The SPEC diff introduces no event-log mention, no `/setintegritylevel Medium`, and no
  `file.rs::Symbol` citation.
- `git status --porcelain` clean of anything beyond the three named files, before and after.
- No test run required — no code changed.
</verification>

<success_criteria>
A failure in either leg of either split job now fails the job. The three false records tell the
truth about what the gate did on 2026-08-15, what fixed it, and what is still red. The routing of
this finding into the D-15 ledger (rather than the review artifacts) is justified in the SUMMARY
against the ledger's own scoping rule and in-document precedent. Everything is committed locally,
nothing is pushed.
</success_criteria>

<output>
Create `.planning/quick/260815-f2x-fix-the-fail-open-windows-ci-gate-that-h/260815-f2x-SUMMARY.md` when done.

The SUMMARY must contain, explicitly:
1. The comment before→after accounting table (one row per pre-existing comment block).
2. The class-scan results both post-fix and at HEAD, with the MSI allowlist entry and its reason.
3. The D-15-vs-review-artifact routing justification, naming the in-document precedent.
</output>
