---
phase: 260624-q98
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono-cli/src/audit_ledger.rs   # DELETE (whole file)
  - crates/nono-cli/src/state_paths.rs
  - crates/nono-cli/src/audit_session.rs
  - crates/nono-cli/src/exec_identity.rs
autonomous: true
requirements: [Q98-REMOVE-ORPHAN-AUDIT-LEDGER]
must_haves:
  truths:
    - "audit_ledger.rs is gone from the codebase"
    - "AUDIT_LEDGER_FILENAME constant is removed from state_paths.rs"
    - "maybe_migrate_legacy_audit_ledger function and its dedicated test are removed from state_paths.rs"
    - "No #[allow(dead_code)] remains for the deleted items"
    - "audit_root() and legacy_audit_root() are untouched (have live callers)"
    - "workspace builds clean with no warnings"
    - "clippy passes (native; cross-target PARTIAL→CI for the cfg-gated file)"
  artifacts:
    - path: "crates/nono-cli/src/state_paths.rs"
      provides: "State paths module without dead ledger helpers"
      contains: "pub fn audit_root"
    - path: "crates/nono-cli/src/audit_session.rs"
      provides: "Audit session module with trimmed comment"
    - path: "crates/nono-cli/src/exec_identity.rs"
      provides: "Exec identity module with trimmed comment"
  key_links:
    - from: "state_paths.rs"
      to: "audit_session.rs"
      via: "audit_root() / legacy_audit_root() calls"
      pattern: "state_paths::audit_root"
---

<objective>
Remove the orphan `crates/nono-cli/src/audit_ledger.rs` module (never compiled because no
`mod audit_ledger;` declaration exists anywhere) and the dead helpers in `state_paths.rs`
that exist solely to serve it. Clean up stale `audit_ledger.rs` references in comment text
in two other files.

Purpose: The fork deliberately does NOT want the upstream append-only audit ledger or its
migration helper — the fork uses a flat `nono audit verify` shape instead. These items
have been inert dead code since commit c64c5977, gated only by `#[allow(dead_code)]`.
Removing them eliminates confusion, suppresses no-longer-needed allow attributes, and
prevents future plan authors from accidentally wiring them in.

Output: Deleted `audit_ledger.rs`; pruned `state_paths.rs`; two comment-trimmed files;
one atomic commit with DCO sign-off.
</objective>

<execution_context>
@C:\Users\OMack\.claude\get-shit-done\workflows\execute-plan.md
</execution_context>

<context>
@C:\Users\OMack\Nono\.planning\STATE.md
@C:\Users\OMack\Nono\CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compiler-confirm cascade, then surgically delete dead code</name>
  <files>
    crates/nono-cli/src/audit_ledger.rs (DELETE)
    crates/nono-cli/src/state_paths.rs
    crates/nono-cli/src/audit_session.rs
    crates/nono-cli/src/exec_identity.rs
  </files>
  <action>
**Step A — Cascade check (compile first).**

Before deleting anything, run:

    cargo build -p nono-cli 2>&1 | head -60

This establishes the baseline. The build should succeed (audit_ledger.rs is never compiled).
Verify no existing errors reference the items you are about to remove.

Then grep for any `mod audit_ledger` declaration across the entire workspace to confirm the
file is truly unregistered:

    grep -rn "mod audit_ledger" crates/

Expect: zero results. If any declaration is found, stop and report — do not proceed until
understood.

**Step B — Delete `crates/nono-cli/src/audit_ledger.rs`.**

Delete the whole file. It is an unregistered orphan; deletion has no compilation effect.

**Step C — Prune `crates/nono-cli/src/state_paths.rs`.**

Remove these three items (all are dead without audit_ledger.rs, per the comment at line 13):

1. The comment block at lines 13-17 and the constant itself:
   ```
   // Used by `maybe_migrate_legacy_audit_ledger` (below) and tests. The sole
   // non-test caller (`audit_ledger.rs`) is not currently wired into the module
   // tree, so this is dead on all platforms until that module is declared.
   #[allow(dead_code)]
   const AUDIT_LEDGER_FILENAME: &str = "ledger.ndjson";
   ```
   Remove all five lines.

2. The function `maybe_migrate_legacy_audit_ledger` and its doc comment (lines 262-321):
   The entire block from the `/// Copy a legacy audit ledger...` doc comment through the
   closing `}` of the function body. Do NOT remove `audit_root()` or `legacy_audit_root()`
   — both have live callers in audit_session.rs and rollback_runtime.rs.

3. The dedicated test `maybe_migrate_legacy_audit_ledger_copies_via_temp_rename` (the test
   starting at line 429 through its closing `}`). This is the only test that calls
   `maybe_migrate_legacy_audit_ledger` or references `AUDIT_LEDGER_FILENAME` directly.

After removing these three items, verify the remaining tests in the `mod tests` block still
compile (the other tests use `audit_root`, `legacy_audit_root`, `sessions_dir`, etc., which
are untouched).

**Step D — Trim stale `audit_ledger.rs` references in comment text.**

Two files contain references to the deleted module in comments only (no code impact):

- `crates/nono-cli/src/audit_session.rs` line 367: The comment says
  `// alongside the audit_ledger.rs port (which is the upstream caller of`
  Trim that parenthetical phrase from the comment so it reads cleanly. The test it annotates
  (`discover_sessions_excludes_rollback_backed_entries`) remains; only the comment changes.

- `crates/nono-cli/src/exec_identity.rs` line 9: The module doc comment mentions
  `` `audit_ledger.rs` flock changes, and ``
  Remove that fragment from the sentence so the comment describes only what this module
  actually does. The surrounding context (`MerkleScheme::DomainSeparatedV3` and
  `cross-file refactors`) can stay or be removed for clarity — use judgment to leave a
  coherent sentence.

**Step E — Verify the cascade did not leave any orphan items.**

After the edits, run:

    grep -rn "AUDIT_LEDGER_FILENAME\|maybe_migrate_legacy_audit_ledger\|audit_ledger" \
        crates/nono-cli/src/

Expect: zero matches (all references are now gone or in the deleted file).

**Step F — Cross-target clippy note.**

`state_paths.rs` contains a `#[cfg_attr(target_os = "windows", allow(dead_code))]`
attribute on `resolve_xdg_state_base`. This change removes only non-cfg-gated items
(the constant and function had no cfg gates themselves), which lowers cross-target drift
risk significantly. However, per CLAUDE.md MUST/NEVER rule, a commit touching a file
with cfg-gated code MUST be verified on both Linux and macOS targets. On this Windows
host, run:

    cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used

If the cross toolchain is unavailable (linker error), record the result as PARTIAL→CI
per `.planning/templates/cross-target-verify-checklist.md` and do not claim a full pass.
Native `make clippy` is still required and must be green.
  </action>
  <verify>
    <automated>
      cargo build --workspace 2>&amp;&amp; echo "BUILD OK"
      cargo clippy --workspace -- -D warnings -D clippy::unwrap_used 2>&amp;&amp; echo "CLIPPY OK"
      cargo test -p nono-cli 2>&amp;&amp; echo "TESTS OK"
    </automated>
  </verify>
  <done>
    - `crates/nono-cli/src/audit_ledger.rs` does not exist.
    - `state_paths.rs` contains neither `AUDIT_LEDGER_FILENAME` nor `maybe_migrate_legacy_audit_ledger`.
    - The test `maybe_migrate_legacy_audit_ledger_copies_via_temp_rename` is gone from `state_paths.rs`.
    - No `#[allow(dead_code)]` attribute remains for the deleted items.
    - `grep -rn "audit_ledger" crates/nono-cli/src/` returns zero results.
    - `cargo build --workspace` is green.
    - `make clippy` (native) is green; cross-target is PARTIAL→CI if toolchain unavailable.
    - `cargo test -p nono-cli` passes (removed test is gone; surviving tests pass).
    - Commit is atomic with DCO sign-off: `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit</name>
  <files>(no new file changes — commit what Task 1 produced)</files>
  <action>
Stage and commit the four changed/deleted paths:

    git add -u crates/nono-cli/src/audit_ledger.rs \
               crates/nono-cli/src/state_paths.rs \
               crates/nono-cli/src/audit_session.rs \
               crates/nono-cli/src/exec_identity.rs

Use `git add -u` so the deleted file is staged as a deletion. Verify staging:

    git status

Then commit with DCO sign-off:

    git commit -m "chore(nono-cli): remove orphan audit_ledger.rs and dead state_paths helpers

The file crates/nono-cli/src/audit_ledger.rs was never compiled (no mod declaration
exists). The fork deliberately does not want the upstream append-only audit ledger;
the flat nono audit verify shape is kept instead. Remove the file and its dead
helpers in state_paths.rs (AUDIT_LEDGER_FILENAME constant,
maybe_migrate_legacy_audit_ledger function and its dedicated test), which existed
only to serve the orphan. Also trim stale audit_ledger.rs references from two
comment blocks in audit_session.rs and exec_identity.rs.

Dead code was gated by #[allow(dead_code)] added in c64c5977; those suppressors
are no longer needed.

Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>"
  </action>
  <verify>
    <automated>git log -1 --oneline</automated>
  </verify>
  <done>
    `git log -1` shows the chore commit. `git show --stat HEAD` lists audit_ledger.rs as deleted
    and shows net line reduction in state_paths.rs. DCO sign-off line present in commit message.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure deletion of dead code; no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q98-01 | Tampering | Cascade deletion removing live callers | accept | audit_root/legacy_audit_root confirmed live via grep before deletion; compiler validates |
| T-q98-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies introduced in this task |
</threat_model>

<verification>
After both tasks complete, confirm:

1. `ls crates/nono-cli/src/audit_ledger.rs` → "No such file"
2. `grep -rn "audit_ledger\|AUDIT_LEDGER_FILENAME\|maybe_migrate_legacy_audit_ledger" crates/nono-cli/src/` → zero matches
3. `cargo build --workspace` → green
4. `make clippy` → green (native); cross-target PARTIAL→CI if toolchain absent
5. `cargo test -p nono-cli` → all tests pass, count is lower by exactly 1 (the migrate test)
6. `git log -1 --format="%B" | grep "Signed-off-by"` → present
</verification>

<success_criteria>
The orphan `audit_ledger.rs` file is deleted. The `state_paths.rs` module is free of the
constant, function, and test that existed only for it. No `#[allow(dead_code)]` suppresses
these items. The workspace builds and passes clippy and tests. The commit is atomic with DCO
sign-off. Cross-target verification is either fully green or explicitly marked PARTIAL→CI.
</success_criteria>

<output>
No SUMMARY.md is required for quick tasks. Return "DONE" to the orchestrator when both tasks
complete and the git log confirms the commit.
</output>
