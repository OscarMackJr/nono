---
phase: 260624-q98
plan: 01
subsystem: nono-cli
tags: [dead-code, cleanup, audit-ledger, state-paths]
dependency_graph:
  requires: []
  provides: [Q98-REMOVE-ORPHAN-AUDIT-LEDGER]
  affects: [crates/nono-cli/src/state_paths.rs, crates/nono-cli/src/audit_session.rs, crates/nono-cli/src/exec_identity.rs]
tech_stack:
  added: []
  patterns: []
key_files:
  created: []
  modified:
    - crates/nono-cli/src/state_paths.rs
    - crates/nono-cli/src/audit_session.rs
    - crates/nono-cli/src/exec_identity.rs
  deleted:
    - crates/nono-cli/src/audit_ledger.rs
decisions:
  - "Deleted audit_ledger.rs outright (orphan, never compiled); no migration path needed"
  - "Removed AUDIT_LEDGER_FILENAME constant, maybe_migrate_legacy_audit_ledger function, and its dedicated test from state_paths.rs"
  - "Kept audit_root() and legacy_audit_root() — confirmed live callers in audit_session.rs and rollback_runtime.rs"
  - "Cross-target clippy PARTIAL->CI: x86_64-linux-gnu-gcc not installed on Windows host"
metrics:
  duration: "~8 minutes"
  completed: "2026-06-24"
  tasks_completed: 2
  files_changed: 4
---

# Phase 260624-q98 Plan 01: Remove Orphan audit_ledger.rs and Dead state_paths Helpers Summary

**One-liner:** Deleted the never-compiled `audit_ledger.rs` orphan and its dead `state_paths.rs` helpers (constant + migration function + test), trimming 405 lines of inert dead code.

## What Was Done

The fork deliberately does not use the upstream append-only audit ledger — it uses the flat `nono audit verify` shape instead. The file `crates/nono-cli/src/audit_ledger.rs` had been inert since commit c64c5977 because no `mod audit_ledger;` declaration exists anywhere in the workspace (confirmed via grep). The `#[allow(dead_code)]` suppressors in `state_paths.rs` existed solely to silence warnings from helpers that only existed to serve the orphan.

### Changes Made

**Deleted:**
- `crates/nono-cli/src/audit_ledger.rs` — 313 lines of never-compiled code (append_session, verify_session_in_ledger, LedgerLock, and their tests)

**Pruned from `crates/nono-cli/src/state_paths.rs`:**
- `AUDIT_LEDGER_FILENAME` constant and its `#[allow(dead_code)]` + explanatory comment (5 lines)
- `maybe_migrate_legacy_audit_ledger()` function and its doc comment (61 lines)
- `maybe_migrate_legacy_audit_ledger_copies_via_temp_rename` test (21 lines)
- Net: 87 lines removed

**Comment trimmed in `crates/nono-cli/src/audit_session.rs`:**
- Removed the parenthetical `(which is the upstream caller of this discovery path)` referencing the deleted module from the deferral comment above `discover_sessions_excludes_rollback_backed_entries`

**Comment trimmed in `crates/nono-cli/src/exec_identity.rs`:**
- Removed `` `audit_ledger.rs` flock changes, and `` from the module-level doc comment's description of what the upstream cherry-pick included

## Cascade Verification

- `grep -rn "mod audit_ledger" crates/` → zero results (confirmed orphan before deletion)
- `grep -rn "AUDIT_LEDGER_FILENAME|maybe_migrate_legacy_audit_ledger|audit_ledger" crates/nono-cli/src/` → zero results after edits
- `audit_root()` and `legacy_audit_root()` are untouched; both have live callers

## Verification Gates

| Gate | Result |
|------|--------|
| `cargo build --workspace` | GREEN |
| `cargo clippy --workspace -- -D warnings -D clippy::unwrap_used` (native) | GREEN |
| `cargo test -p nono-cli` | 1376 passed, 4 failed (all 4 are pre-existing baseline failures: profile_cmd init + 3 protected_paths — documented in memory `nono_cli_windows_baseline_test_failures.md`; not regressions) |
| Cross-target clippy (x86_64-unknown-linux-gnu) | PARTIAL→CI — cross C compiler not installed on Windows host |
| Cross-target clippy (x86_64-apple-darwin) | PARTIAL→CI — cross C compiler not installed on Windows host |

## Deviations from Plan

None — plan executed exactly as written.

## Commit

| Hash | Message |
|------|---------|
| e350df23 | chore(nono-cli): remove orphan audit_ledger.rs and dead state_paths helpers |

DCO sign-off: `Signed-off-by: Oscar Mack Jr <oscar.mack.jr@gmail.com>`
Net diff: 4 files changed, 3 insertions(+), 405 deletions(-)

## Known Stubs

None.

## Threat Flags

None — pure deletion of dead code; no new trust boundary introduced.

## Self-Check: PASSED

- `crates/nono-cli/src/audit_ledger.rs` does not exist (deleted)
- `state_paths.rs` contains neither `AUDIT_LEDGER_FILENAME` nor `maybe_migrate_legacy_audit_ledger`
- Commit `e350df23` exists: confirmed via `git log -1 --oneline`
- DCO sign-off present: confirmed via `git log -1 --format="%B" | grep "Signed-off-by"`
